---
phase: 37-enrich-before-ingest
plan: 09
type: execute
wave: 7
depends_on: ["37-08"]
files_modified:
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/README.md
autonomous: false
requirements: [INGEST-02, STRUCT-02, PREVIEW-01, DISPATCH-03]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All suites are green at or above their corrected baselines before anything is released: plugin >= 1052 passed / 5 skipped, repo >= 1962 passed / 6 skipped, node >= 617 pass on the FILE glob, and the arming grep reports 0 for every file under n8n/."
    - "`.claude-plugin/plugin.json`'s `version` is bumped in the SAME commit as the CHANGELOG entry. Claude Desktop decides whether to offer an update purely by comparing that string, so an unbumped version means the Update button stays grey no matter what shipped."
    - "The release is pushed AND merged to master. `0.9.0` shipped with a correct bump sitting on a feature branch and the Update button stayed grey until master had it — master is the branch the marketplace reads."
    - "The marketplace clone is refreshed after the merge. A reinstall never refreshes the clone on its own, and verification is by CONTENT, never by the version string, which is hand-written."
    - "The 9 Gold Coast Turf Club directors walk end to end — extract, match, confirm, enrich, enriched preview, ingest — and every row that reaches HubSpot carries an email."
    - "A row still emailless after the waterfall is named and held, never sent, and the dispatch CSV is not created for a batch containing one."
    - "The deliberate test flip is recorded in the SUMMARY as an intended behaviour change, with the old assertion and the new one both stated."
    - "The match-ceiling number is either replaced by its live measurement during this walk, or explicitly carried forward as still provisional with the reason — never left ambiguous."
    - statement: "The live walk enriches the nine directors without re-spending on any row a prior broken attempt already completed, observed against provider credit balances before and after."
      verification: backstop
  artifacts:
    - operator-claude-plugin/CHANGELOG.md
    - operator-claude-plugin/.claude-plugin/plugin.json
  key_links:
    - "`.claude-plugin/plugin.json` version -> the marketplace clone on master -> the operator's Update button — three places that must agree, and the historical failure was any two of them agreeing while the third did not."
    - "the live walk -> `max_rows_per_match_request`'s provisional note -> its replacement measurement — the probe that retires the provisional label."
  prohibitions:
    - "MUST NOT release with a suite skipped or deleted to make it pass. A test skipped rather than fixed is the premise-falsifying move the phase's non-negotiables forbid, and it ships as green."
    - "MUST NOT verify the marketplace refresh by reading a version string. That string is hand-written and has been wrong before; verify by content."
    - "MUST NOT touch `~/.claude/plugins/` from any script or test in this repo."
---

<objective>
Prove the phase against the case that motivated it, then ship it the way this plugin has to be
shipped.

Purpose: every DoD item except two is provable offline and has been proven by plans 37-01 through
37-08. The remaining two — the nine directors walking end to end, and the release actually
reaching the operator's Update button — are only provable by doing them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/plugin-entrypoint/ROADMAP.md
@.planning/workstreams/plugin-entrypoint/STATE.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-08-SUMMARY.md
</context>

## Artifacts this phase produces

See 37-04-PLAN.md's table for the full phase list. This plan produces the CHANGELOG entry and
the version bump; everything else was built by plans 37-01 through 37-08.

<tasks>

<task type="auto">
  <name>Task 1: all gates green, and the CHANGELOG cut in the same commit as the bump</name>

  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` — the header blockquote about bumping
      `.claude-plugin/plugin.json`'s version in the SAME commit, and the most recent released
      entry's structure and voice.
    - `operator-claude-plugin/.claude-plugin/plugin.json` — the current `version` string.
    - `operator-claude-plugin/README.md` — whether it describes the available skills, and if so
      where the new lane belongs in that list.
    - Every SUMMARY from 37-01 through 37-08 — the CHANGELOG entry is written from what was
      actually built and what actually deviated, not from the plans' intentions.
    - `37-CONTEXT.md` § 8, § 9.7, § 10.
  </read_first>

  <files>operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/README.md</files>

  <action>
Run all four gates first and record their exact numbers, before writing anything:
the plugin suite, the repo suite, the node file-glob suite, and the arming grep. If any is below
its baseline, STOP and fix the regression — do not release, and do not adjust a baseline to match
what the suite currently does.

Write the CHANGELOG entry from the SUMMARY files. It must name, at minimum: the email gate at
`write_dispatch_csv` and that it RAISES, the two arming phrases and why there are two, the match
lane and its four outcome groups including `unchecked`, the join-by-`row_id` rule, the run
manifest and idempotent resume, the post-ingest queue handoff as 37-07's checkpoint resolved it,
and the deliberate behaviour change: rows without an email are no longer written to the dispatch
CSV. That last one is a breaking change for anyone relying on the old behaviour and must read as
one, not as a bug fix.

Record the deliberate test flip explicitly in the CHANGELOG's own words as well as in the
SUMMARY: the extraction round-trip case previously asserted an emailless row was written and now
asserts the refusal.

Bump `.claude-plugin/plugin.json`'s `version` in the SAME commit as the CHANGELOG entry. Commit
explicit paths only — never `git commit -a`.

Update README.md only if it enumerates the plugin's skills; add the new lane there in the same
voice. If it does not enumerate them, leave it alone and say so in the SUMMARY.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -m pytest operator-claude-plugin/tests/ -q && node --test tests/n8n/*.test.mjs && grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` reports at least 1962 passed and exactly 6 skipped.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` reports at least 1052 passed
      and exactly 5 skipped.
    - `node --test tests/n8n/*.test.mjs` reports at least 617 pass.
    - `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` reports 0 for every file.
    - `git show --stat HEAD` for the release commit names BOTH `CHANGELOG.md` and
      `.claude-plugin/plugin.json`.
    - The `version` string in `.claude-plugin/plugin.json` differs from the previous release's.
    - The CHANGELOG entry describes the emailless-row refusal as a behaviour change.
    - `git status --short operator-claude-plugin/scratch` is empty.
  </acceptance_criteria>

  <done>The release commit exists, carries both files, and every offline gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: walk the nine directors end to end</name>
  <what-built>
The whole phase: the match lane, the enriched preview, the email gate, the run manifest, the
queue handoff and the skill. Everything provable offline has been proven. This is the walk that
proves the case the phase was written for.
  </what-built>

  <how-to-verify>
Walk the nine Gold Coast Turf Club directors end to end, with the released plugin.

1. Extract the nine directors from their board page. Confirm you get nine rows with names, roles
   and company, and no emails.
2. Ask for the enrich-first flow. Confirm step 1 tells you, before anything else, that you will
   be asked to arm TWICE and why.
3. Confirm the match step reports four groups by name, and that anything it could not look up is
   reported as unchecked rather than as unmatched.
4. Confirm each proposal is one per turn and shows you the candidate's own fields beside the
   question. Try answering yes to two at once and confirm it is not accepted.
5. Read the cost preview. Confirm it prices the unmatched set, not all nine.
6. Say "arm the enrichment". Let it run.
7. **Read the enriched preview carefully — this is the phase's whole point.** For each row: what
   came in, what enrichment added, and SEND or HELD. Confirm every held row is named with a
   reason, and confirm it states that nothing has reached HubSpot.
8. Say "arm the upload". Confirm you were asked for a SECOND phrase even though you armed
   earlier in the same conversation.
9. Read the backend report, then confirm the held rows are restated AFTER it.
10. **In HubSpot, check every record this run created: every one must carry an email.** If any
    does not, the phase has not met its definition of done.
11. Record the wall-clock time the match step took per row. That number replaces
    `max_rows_per_match_request`'s provisional value; if the walk did not produce a usable
    measurement, say so and the provisional label stays.

Then run the disarmed close: `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` must report 0.
  </how-to-verify>

  <resume-signal>Type "approved" plus the measured per-row match latency, or describe which step
  behaved differently from the above.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: push, merge to master, refresh the marketplace clone</name>

  <action>
Operator-run release. Not automatable here, and it has failed before in a way no test catches:
`0.9.0` shipped with a correct bump sitting on a feature branch and the Update button stayed
grey until master had it. Master is the branch the marketplace reads.
  </action>

  <instructions>
1. Push the branch.
2. **Merge to master.** The bump is invisible to the marketplace until master carries it.
3. Refresh the marketplace clone. A reinstall does not refresh it on its own, and a same-version
   reinstall deletes `operator.local.json`.
4. **Verify by CONTENT, not by the version string.** Open a file the clone should now carry —
   `skills/enrich-before-ingest/SKILL.md` — and confirm it is there and current. The version
   string is hand-written and has been correct while the content was stale.
5. Confirm the Update button offers the new version in Claude Desktop.

Do not run any of this against `~/.claude/plugins/` from a script in this repo.
  </instructions>

  <verification>
Master's `operator-claude-plugin/.claude-plugin/plugin.json` carries the new version string, the
refreshed marketplace clone contains `skills/enrich-before-ingest/SKILL.md` with this phase's
content, and Claude Desktop offers the update.
  </verification>

  <resume-signal>Type "released" once master carries the bump and the refreshed clone contains
  the new skill file, or describe where it stopped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> marketplace clone -> operator install | The released artifact crosses into the operator's environment. |
| client -> live HubSpot tenant | The walk performs real writes to real records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-36 | Tampering | live walk writes | high | mitigate | The walk runs the shipped two-arm gate; the disarmed close (`grep -c` reporting 0) is re-run afterwards, so a run that left the backend armed is caught rather than assumed. |
| T-37-37 | Spoofing | marketplace clone freshness | medium | mitigate | Verification is by file content, not by the hand-written version string, which has been correct while the content was stale. |
| T-37-38 | Repudiation | release provenance | medium | mitigate | The version bump and the CHANGELOG entry are asserted to be in one commit by a `git show --stat` criterion, so a release cannot be described in one commit and shipped in another. |
| T-37-39 | Information Disclosure | live walk record data | low | accept | The walk uses the operator's own tenant and their own contact data; no new data crosses any boundary this phase did not already plan for. |
| T-37-SC | Tampering | package installs | low | accept | This plan installs no npm/pip/cargo package. |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q                                    # >= 1962 passed / exactly 6 skipped
.venv/bin/python -m pytest operator-claude-plugin/tests/ -q     # >= 1052 passed / exactly 5 skipped
node --test tests/n8n/*.test.mjs                                 # >= 617 pass (FILE glob only)
grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json              # 0 for every file, before AND after the walk
git show --stat HEAD                                             # names CHANGELOG.md and plugin.json together
git status --short operator-claude-plugin/scratch                # empty
```
</verification>

<success_criteria>
- Every offline gate green at or above its corrected baseline, with no test skipped or deleted
  to get there.
- The nine directors walk end to end and every record that reached HubSpot carries an email.
- Every emailless row is named and held, and no dispatch CSV was created for a batch with one.
- The version bump and the CHANGELOG cut are one commit; master carries it; the refreshed clone
  contains the new skill file, verified by content.
- The match-ceiling number is either measured and replaced or explicitly carried forward as
  provisional with its reason.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-09-SUMMARY.md` when done.
</output>
