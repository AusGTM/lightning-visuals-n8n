---
phase: 33-durable-operator-state
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - operator-claude-plugin/README.md
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/scripts/review_queue.py
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/tests/test_plugin_manifest.py
  - .planning/workstreams/plugin-entrypoint/OPERATOR-RUNBOOK.md
  - .planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-FINDINGS.md
autonomous: false
requirements: [PLUGIN-02, PLUGIN-03, STATUS-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No file this plugin ships still tells an operator to hand-copy their config between install directories — the standing violation of the Out-of-Scope line 'Terminal instructions to the operator are a requirement failure' is gone from the docs as well as from the code."
    - "The plugin's version string is 0.7.0 and it was bumped in the same commit as the CHANGELOG cut, so Claude Desktop actually offers the update."
    - "One real migration has been performed and observed on this host, and whether a 'sensitive location' permission prompt fired is written down verbatim rather than assumed."
  artifacts:
    - .planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-FINDINGS.md
  key_links:
    - "The CHANGELOG's closing 'Two traps around installing the result' paragraph is the exact terminal instruction this phase deletes. Shipping 0.7.0 with it intact would leave the docs contradicting the code on the one point the phase exists to fix."
    - "The version bump and the CHANGELOG cut must be ONE commit. The release checklist records that this has already cost the project once — the Update button sat greyed out through ten phases of shipped work."
    - "The marketplace clone never fetches on its own; a bumped version in the repo is invisible to the operator until the clone is reset to the pushed commit."
---

<objective>
Ship it. Bring every operator-facing document into agreement with the code, cut plugin `0.7.0`,
and then perform one real migration on this host and write down what happened.

The last step is not ceremony. Research Open Question 1 cites a report that writes into
`~/.claude/plugins/data/<id>/` trigger a "sensitive location" permission prompt at the Bash-tool
layer — closed upstream as not-planned, MEDIUM confidence, not reproduced this session. A
migration that stops to ask for confirmation is a terminal step by another name, which would
defeat the phase. It cannot be settled by more research; it is settled by looking.

Purpose: PLUGIN-02 (the docs stop describing a one-time step as a per-update step), PLUGIN-03
(no shipped text instructs the operator to fix something the plugin now fixes itself), STATUS-05
(the release that makes the dashboard pointer durable actually reaches the operator).
Output: corrected README and CHANGELOG, `plugin.json` at `0.7.0`, RB-10 in the operator runbook,
and `33-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-VALIDATION.md
@.planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-03-SUMMARY.md
@operator-claude-plugin/CHANGELOG.md
@operator-claude-plugin/README.md
</context>

## Artifacts this plan produces

| Artifact | Kind | Purpose |
|---|---|---|
| `33-FINDINGS.md` | new planning doc | Verbatim record of the live migration observation, in the shape of `28-FINDINGS.md` |
| RB-10 | new runbook section + table row | The observation's steps, so it is repeatable and its result is attributable |
| `test_plugin_manifest.py::test_no_shipped_document_instructs_a_manual_config_copy` | new test | Two-sided pin: the code stopped needing the instruction, so the instruction must stop existing |
| `plugin.json` `version` | `0.6.2` → `0.7.0` | |

No new script, no new symbol, no new config key. The separately-scheduled `0.7.1` empty-input
zero-row fix (`.planning/todos/pending/2026-08-04-empty-input-previews-zero-rows-silently.md`)
is NOT folded in here.

<tasks>

<task type="auto">
  <name>Task 1: Every shipped document agrees with the code</name>

  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` — the closing release-checklist section, and the paragraph that follows its four numbered steps
    - `operator-claude-plugin/README.md` — the setup steps around line 158, the `scripts/` tree around line 520, and the three settings references at lines 344, 358 and 422
    - `operator-claude-plugin/scripts/review_queue.py` line 253 and `operator-claude-plugin/skills/enrich-records/SKILL.md` line 66 — two more strings naming a directory-prefixed config path
    - `operator-claude-plugin/tests/test_plugin_manifest.py` — the existing assertion style
  </read_first>

  <files>operator-claude-plugin/README.md, operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/scripts/review_queue.py, operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/tests/test_plugin_manifest.py</files>

  <behavior>
    - Neither `README.md` nor `CHANGELOG.md` contains the substring `operator.local.json across`.
    - `README.md`'s `scripts/` tree lists `durable_paths.py`.
    - The README setup section tells the operator to run the initialize skill for the path rather than describing a directory they cannot locate.
  </behavior>

  <action>
Correct the closing paragraph of `CHANGELOG.md` — the one immediately after the four numbered
release-checklist steps, headed with the two install traps. Both halves are now false and both
are the standing requirement violation this phase closes. Rewrite it to state the new facts: a
same-version reinstall no longer destroys the operator's settings, because they live outside the
install directory; and updating to a new version no longer strands them, because the first
resolution after an update finds the newest previous install's copy, moves it into the durable
home at mode 0600, and removes the copy it moved from. Name the durable home explicitly as
`~/.claude/plugins/data/operator-claude-plugin-lightning-visuals-operator/`. Say that this
behaviour arrived in `0.7.0` and that an operator updating FROM a version before it gets the
migration for free on their first action. Do not replace one instruction with another — the
whole point is that the paragraph now describes something the operator does not do.

In `README.md`: add `durable_paths.py` to the `scripts/` tree with a one-line comment naming it
as the resolver for where operator state lives, placed above `config_gate.py` since that is the
dependency direction. In the setup steps around line 158, keep the template-copy explanation but
replace any description of the settings file's location as a directory beside the example with
an instruction to run `/operator-claude-plugin:initialize`, which prints the exact absolute path
— that is the skill's entire reason for existing, and it is now the only honest answer since the
path depends on which of three locations resolved. Add two sentences under the setup section
recording that the settings file survives plugin updates and where it lives.

At README lines 344, 358 and 422, and at `review_queue.py` line 253 and
`skills/enrich-records/SKILL.md` line 66, drop the `config/` directory prefix from the config
filename. Each of these names a settings KEY and points at the file that holds it; the filename
is still correct and the directory no longer is. `review_queue.py`'s string is operator-facing
output, so its correction is a PLUGIN-03 matter, not cosmetics.

Add `test_no_shipped_document_instructs_a_manual_config_copy` to
`tests/test_plugin_manifest.py`. It reads `README.md` and `CHANGELOG.md` and asserts the
substring `operator.local.json across` appears in neither. Docstring it with the Out-of-Scope
line it enforces — terminal instructions to the operator are a requirement failure — and note
that the code stopped needing the instruction in 33-02, so this test is the second side of that
pin: a doc that keeps telling the operator to do it by hand is as much a defect as code that
requires it.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'operator.local.json across' operator-claude-plugin/README.md operator-claude-plugin/CHANGELOG.md` reports 0 for both.
    - `grep -n 'durable_paths.py' operator-claude-plugin/README.md` returns at least one line.
    - `grep -rn 'config/operator.local.json' operator-claude-plugin/README.md operator-claude-plugin/scripts/ operator-claude-plugin/skills/` returns nothing.
    - `test_no_shipped_document_instructs_a_manual_config_copy` exists and passes.
    - Full plugin suite green.
  </acceptance_criteria>

  <done>No document this plugin ships describes the hand-copy the code no longer requires, and a test keeps it that way.</done>
</task>

<task type="auto">
  <name>Task 2: Cut 0.7.0 — version bump and CHANGELOG entry in one commit</name>

  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` — the `## [Unreleased]` section and the four-step release checklist at the bottom
    - `operator-claude-plugin/.claude-plugin/plugin.json` — currently `0.6.2`
    - The three summaries `33-01-SUMMARY.md`, `33-02-SUMMARY.md`, `33-03-SUMMARY.md`
  </read_first>

  <files>operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json</files>

  <behavior>
    - `plugin.json`'s `version` reads `0.7.0`.
    - `CHANGELOG.md` has a `## [0.7.0] - 2026-08-04` section and an empty `## [Unreleased]` above it.
    - `git show --stat HEAD` for the release commit lists both files.
  </behavior>

  <action>
Write the `0.7.0` CHANGELOG section from the three plan summaries, under `Changed` and `Fixed`.
The entry must say what an operator experiences, not what the modules are called: settings and
the dashboard pointer now live outside the versioned install folder and survive updates; the
first run after an update moves them across by itself, at `0600`, and clears the copy it moved
from; `/operator-claude-plugin:initialize` now says where the settings actually are. Under
`Fixed`, record the dashboard pointer specifically — the same-URL guarantee (STATUS-05, proven
by RB-4) had been silently false since the first plugin update because the pointer sat in the
install directory, and this restores it. Keep `## [Unreleased]` on top and empty.

Set `version` to `0.7.0` in `.claude-plugin/plugin.json`. Minor, not patch: the storage location
of operator state is a surface change, and an operator on `0.6.x` behaves differently after this.

Commit BOTH files together. Claude Desktop decides whether to offer an update purely by
comparing that version string, so a CHANGELOG cut whose bump lands in a later commit ships
nothing — this is written into the release checklist because it has already cost the project
ten phases of invisible work. Do not perform the marketplace-clone refresh here; that is step 4
of the checklist and belongs with the human gate in Task 3, where the result is observed.

Do not fold in the empty-input zero-row fix tracked at
`.planning/todos/pending/2026-08-04-empty-input-previews-zero-rows-silently.md`. It is scheduled
as `0.7.1` immediately after this release and stays a separate cut.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -x -q && grep -q '"version": "0.7.0"' operator-claude-plugin/.claude-plugin/plugin.json && grep -q '## \[0.7.0\]' operator-claude-plugin/CHANGELOG.md && echo RELEASE_METADATA_OK</automated>
  </verify>

  <acceptance_criteria>
    - `plugin.json` reads `"version": "0.7.0"`.
    - `CHANGELOG.md` contains a `## [0.7.0]` heading and `## [Unreleased]` is present and empty above it.
    - The commit containing the CHANGELOG cut also contains `plugin.json` — `git show --stat HEAD` lists both.
    - The `0.7.0` entry mentions the dashboard pointer and STATUS-05 under `Fixed`.
    - No change relating to the empty-input zero-row preview appears in this entry.
  </acceptance_criteria>

  <done>Plugin `0.7.0` is cut: the CHANGELOG describes what an operator experiences, and the version string that Claude Desktop actually compares was bumped in the same commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: RB-10 — refresh the clone, update, and observe one real migration</name>
  <what-built>
Config and the dashboard pointer now resolve from
`~/.claude/plugins/data/operator-claude-plugin-lightning-visuals-operator/`, and the first
resolution after an update migrates them up from the newest previous install — 0600, verified,
source removed. Plugin `0.7.0` is committed but the marketplace clone has not been refreshed, so
nothing has reached the installed copy yet.

Two things need a human. The clone refresh is release-checklist step 4 and the clone never
fetches on its own. And Research Open Question 1 is still open: a closed-as-not-planned Claude
Code issue (anthropics/claude-code#41156) reports that writes into `~/.claude/plugins/data/<id>/`
raise a "sensitive location" permission confirmation at the Bash-tool layer, even under
`bypassPermissions`. It was NOT reproduced during research — MEDIUM confidence. A migration that
stops to ask is a terminal step by another name, so this is observed, not assumed.
  </what-built>

  <how-to-verify>
Add RB-10 to `.planning/workstreams/plugin-entrypoint/OPERATOR-RUNBOOK.md` before running it —
one row in the §0b readiness table and one short section, matching the shape of RB-4 and RB-8.
The section is these steps, and its outcome goes in
`.planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-FINDINGS.md`.

**Step 0 — record the before state.** List
`~/.claude/plugins/cache/lightning-visuals-operator/operator-claude-plugin/` and
`~/.claude/plugins/data/`. Note which install directories exist and confirm no
`operator-claude-plugin-lightning-visuals-operator` directory is there yet. Copy
`operator.local.json` from the newest install directory to somewhere outside `~/.claude/` as a
safety net — this is the one run where an unproven delete path touches a live credential file,
and the backup costs nothing.

**Step 1 — push and refresh the clone.** Push the release commit to `master`, then:
`git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator fetch --depth=1 origin master`
and `git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator reset --hard FETCH_HEAD`.

**Step 2 — update the plugin** through the Claude Desktop plugin manager. Confirm the Update
button is actually offered; if it is greyed out, the version bump or the clone refresh did not
land, and that is the finding.

**Step 3 — trigger one resolution in a NEW conversation.** Run
`/operator-claude-plugin:initialize`. Watch for a permission-confirmation dialog. Record
verbatim: whether one appeared, its exact wording, whether it blocked, and whether declining or
ignoring it left the plugin working or hanging.

**Step 4 — read the outcome.** `initialize` should report the settings file at the durable
path and say the settings live outside the install folder. Then confirm on disk: the durable
directory now holds `operator.local.json`, its mode is `600` (`ls -l`), its contents match the
backup from Step 0, and the previous install directory's copy is gone. Confirm the CURRENT
install directory holds no `config/operator.local.json` that was deleted — it should never have
had one.

**Step 5 — the pointer.** Ask for backend status and request the dashboard. Note the Artifact
URL. In a BRAND NEW conversation, ask for it again. Same URL means STATUS-05 holds across the
update — the thing that has been silently false since the first one.

**If a permission prompt fires:** that is a finding about the chosen location, and the finding
is the deliverable. Record it and stop. Under no circumstances change a permission setting,
edit `settings.json`, or add a hook to suppress it — the mitigation this phase already built is
`LV_OPERATOR_CONFIG`, and the correct next step would be a decision about the location, not a
workaround around the guard.

**If the migration did not happen:** record which of the five resolution steps it stopped at.
`initialize` naming a path inside the new install directory means the sibling scan found
nothing; naming the previous install's path means step 4 or 5 mis-ordered.
  </how-to-verify>

  <resume-signal>Type "approved" once `33-FINDINGS.md` records the observation verbatim, or describe what was seen instead</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → marketplace clone → installed plugin | Code reaches the operator through a manual clone reset; a stale clone means the operator runs different code than the repo describes |
| first live migration → real credential file | The one run where the unlink path touches a real `webhook_secret` and `n8n_api_key` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-14 | Denial of Service | first live migration deletes a real config | critical | mitigate | Step 0 of RB-10 takes a backup outside `~/.claude/` before anything runs. The delete path is already pinned by the entrypoint tests in 33-02 Task 3, including the never-delete-the-current-install invariant. |
| T-33-15 | Denial of Service | permission prompt blocking migration | medium | mitigate | Observed rather than assumed (RB-10 Step 3). If it fires, the recorded mitigation is `LV_OPERATOR_CONFIG` plus the legacy fallback — both already built — and NOT a permission-setting change, which is prohibited by this task explicitly. |
| T-33-16 | Repudiation | an unrecorded observation | medium | mitigate | `33-FINDINGS.md` carries the verbatim result, in the shape `28-FINDINGS.md` set. The phase does not close without it. |
| T-33-17 | Tampering | stale marketplace clone | high | mitigate | RB-10 Step 2 checks the Update button is actually offered; a greyed-out button is itself a recorded finding. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package added, upgraded or removed anywhere in this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green.
- `.venv/bin/python -m pytest -q` — repo suite, no cross-suite regression against the 1792/6 baseline.
- `node --test tests/n8n/*.test.mjs` — node suite, 550 baseline, unchanged (this phase touches no `n8n/` file).
- `git show --stat HEAD` for the release commit lists both `CHANGELOG.md` and `plugin.json`.
- `33-FINDINGS.md` exists and records the RB-10 observation.
</verification>

<success_criteria>
- No shipped document tells the operator to move their config by hand, and a test keeps it that way.
- Plugin `0.7.0` is cut with the version bump and CHANGELOG in one commit.
- RB-10 exists in the operator runbook and has been walked.
- One real migration has been performed on this host and `33-FINDINGS.md` records what was
  observed — including, explicitly, whether a permission prompt fired.
- A dashboard requested in a brand-new conversation after the update lands on the same URL.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/33-durable-operator-state/33-04-SUMMARY.md` when done.
</output>