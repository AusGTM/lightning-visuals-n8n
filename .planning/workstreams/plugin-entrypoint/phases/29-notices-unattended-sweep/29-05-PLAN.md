---
phase: 29-notices-unattended-sweep
plan: 05
type: execute
wave: 3
depends_on: ["29-03"]
files_modified:
  - operator-claude-plugin/scripts/sweep_conditions.py
  - operator-claude-plugin/scripts/sweep_notify.py
  - operator-claude-plugin/scripts/sweep_entry.py
  - operator-claude-plugin/tests/test_sweep_conditions.py
  - operator-claude-plugin/tests/test_sweep_attribution.py
autonomous: true
requirements: [NOTICE-03, NOTICE-04]

must_haves:
  truths:
    - "All five conditions D-08 names fire correctly, plus the stuck-armed backend D-10 adds."
    - "A provider whose balance cannot be read is never reported as out of credits."
    - "A scheduled maintenance run reporting success is not treated as evidence the backend is healthy."
    - "A healthy backend produces no notice of any kind — not a heartbeat, not an all-clear."
    - "Every notice states whether the operator or an admin can act, and an unrecognized cause attributes to an admin."
  artifacts:
    - operator-claude-plugin/scripts/sweep_conditions.py (all conditions)
    - operator-claude-plugin/tests/test_sweep_conditions.py
    - operator-claude-plugin/tests/test_sweep_attribution.py
  key_links:
    - "Credential-failure and exhausted-quota are new classification logic over Phase 27's EXISTING credit-probe data (D-08a) — new judgment, not a new read. Adding a read here would breach the credential boundary the milestone is built on."
    - "The maintenance workflow's own HubSpot Search nodes are onError: continueRegularOutput (D-08b), so run status alone cannot answer 'did the scheduled job actually work'."
---

<objective>
Expand outward from 29-03's proven slice to the full condition set. Three groups, in ascending order
of how much new thinking each needs (D-08a graded them uneven, and that grading is the plan's shape):

1. **New classification logic** — credential failure and exhausted quota. These are not execution-
   status questions at all. Phase 27 established that provider auth failures and rate limits mostly
   do not fail an n8n execution, so they must be read from the credit-probe data instead. The data
   already exists; the judgment does not.
2. **Reuse of Phase 27's reads unmodified** — failed scheduled run and review backlog over threshold,
   plus the stuck-armed backend D-10 adds as the backstop for Phase 28 D-03's arm/disarm crash window.
3. **Behaviour across conditions** — silence when healthy, grouping when several fire at once, and
   attribution on every notice.

The condition that carries the most subtlety is the first one, and for a reason worth restating:
"unknown" and "exhausted" both feel like "can't rely on this provider" but mean opposite things and
require opposite attribution. Apollo's key in this deployment is not a master key and returns 403 by
design — telling the operator Apollo is out of credits when it has never once returned a readable
balance is a confidently wrong answer on the surface whose entire job is telling someone whether
they can act.

Purpose: the sweep notices everything D-08 and D-10 name, and nothing else.
Output: the full condition set, the notice-grouping behaviour, and the attribution guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/29-notices-unattended-sweep/29-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/29-notices-unattended-sweep/29-RESEARCH.md
@.planning/workstreams/plugin-entrypoint/phases/29-notices-unattended-sweep/29-VALIDATION.md
@.planning/workstreams/plugin-entrypoint/phases/27-backend-status-surface/27-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Credential failure and exhausted quota — new judgment over existing data (D-08a)</name>
  <files>operator-claude-plugin/scripts/sweep_conditions.py, operator-claude-plugin/tests/test_sweep_conditions.py</files>
  <read_first>
    - `29-RESEARCH.md` §"Pitfall 5: Treating 'unknown balance' as 'exhausted quota'" and its
      `is_quota_exhausted` sketch — the three-way return and why the unknown case is a hard
      requirement rather than a discretionary refinement.
    - `29-RESEARCH.md` §Open Question 5 — the floor value per provider is Claude's Discretion; the
      unknown-versus-exhausted distinction is not.
    - `27-CONTEXT.md` D-08 — unknown is never zero and never healthy, inherited from Phase 25 D-10.
    - `27-CONTEXT.md` D-04a — provider-facing nodes are `onError: continueRegularOutput`, which is
      why an expired credential does not show up as a failed run and has to be read here instead.
    - `scripts/check_provider_credits.py` — the measured, live-validated per-provider probe behaviour
      the status endpoint mirrors: Lusha reports a remaining balance, ZoomInfo GTM needs its
      vendor-specific Accept header or refuses, and the Apollo key returns 403 by design. Read for
      the behaviours, not as an import — the plugin holds no provider credential.
    - `operator-claude-plugin/tests/conftest.py` — 29-02's `backend_status_*` fixtures covering all
      four provider states.
  </read_first>
  <behavior>
    - Quota-exhausted fires only on an explicit numeric balance at or below a configured floor.
    - A provider configured with an unreadable balance returns unknown from the quota check, and
      unknown never becomes a quota-exhausted notice.
    - A provider that is not configured at all is a third state, distinct from both unknown and
      exhausted, and does not fire quota-exhausted either.
    - Credential-failure fires on the signature of an auth refusal, and is distinguishable from a
      legitimately-zero balance — a provider at zero credits with a working credential is exhausted,
      not broken, and the two carry different remedies.
    - Both conditions attribute to an admin. Neither is something a non-technical operator can act
      on, since provider credentials live in n8n by design (REQUIREMENTS.md §Credential boundary).
    - Every condition returns its reason alongside its outcome, so the notice text is derived rather
      than re-guessed downstream.
  </behavior>
  <action>
    Extend `sweep_conditions.py` with the two classification functions, keeping them pure over
    already-fetched status data exactly as the stuck-lock condition is — no I/O reaches this module,
    which is what keeps 29-03's import-graph guard meaningful.

    Make the three-way outcome explicit in the return type rather than encoding it as a boolean plus
    a convention. A function that can only answer yes or no forces the caller to invent a
    representation for unknown, and that invention is where unknown quietly becomes false.

    Take the floor value as a parameter with a documented default rather than hardcoding it, so an
    admin who wants headroom above zero has somewhere to put it. Record the chosen default and its
    reasoning in the module docstring.

    For credential failure, classify from the shape of the probe result rather than from a provider's
    prose message, which varies and changes. Where the status endpoint cannot distinguish an auth
    refusal from an unreadable balance, return unknown and let the notice say so — Phase 27 D-05's
    guardrail applies: an honest "I cannot tell, an admin can check" beats a confident wrong cause.

    Extend `test_sweep_conditions.py` across all four provider fixture states for both conditions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_conditions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_conditions.py -q` exits 0.
    - A test asserts `backend_status_unknown_balance` yields unknown from the quota check and produces no exhausted-quota notice.
    - A test asserts `backend_status_unconfigured_provider` is a distinct outcome from both unknown and exhausted.
    - A test asserts `backend_status_exhausted` fires, and that a balance just above the floor does not.
    - A test asserts a zero balance with a working credential classifies as exhausted rather than as a credential failure.
    - A test asserts both conditions attribute to an admin.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0.
  </acceptance_criteria>
  <done>The two conditions Phase 27's data can support but not answer are answered, and an unreadable balance can never be reported as an empty one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failed scheduled run, review backlog, and the stuck-armed backend (D-08b, D-10)</name>
  <files>operator-claude-plugin/scripts/sweep_conditions.py, operator-claude-plugin/tests/test_sweep_conditions.py</files>
  <read_first>
    - `29-RESEARCH.md` §"Pitfall 1: 'Failed scheduled run' is not fully visible from execution status"
      — the verified node-by-node finding that `wf_scheduled_maintenance_cloud.json`'s own HubSpot
      Search nodes swallow their errors, so a broken credential during the maintenance job still
      reports a successful run.
    - `29-RESEARCH.md` §"Anti-Patterns to Avoid" — the entry stating this swallowing pattern is not
      only an enrichment-workflow problem.
    - `27-CONTEXT.md` D-07c — the real `lv_`-prefixed property names for the review backlog
      (`lv_enrichment_needs_review`, `lv_icp_needs_review`); the generic names in the root `CLAUDE.md`
      do not exist in this deployment. Phase 27's count query is reused; this task adds only the
      threshold comparison.
    - `28-CONTEXT.md` D-01, D-02, D-03 — the arm-dispatch-disarm cycle and the named crash window it
      leaves. D-03 designates this sweep as that window's backstop, which is what D-10 implements.
    - `28-CONTEXT.md` D-04 and `27-CONTEXT.md` D-03 — the armed state is read from the backend, never
      asserted from local config. The sweep reads it the same way.
    - `operator-claude-plugin/tests/conftest.py` — the `executions_with_failure` and
      `execution_maintenance_falsely_successful` fixtures.
  </read_first>
  <behavior>
    - A scheduled run in an error state fires the failed-run condition, naming the workflow.
    - The `execution_maintenance_falsely_successful` fixture — a run reporting success whose search
      node returned an error or nothing — is **not** treated as evidence of health. Whatever this
      condition can and cannot see is stated in the notice text and in the module docstring, so the
      blind spot is documented rather than implied covered.
    - Review backlog fires when the count exceeds a configured threshold, and does not fire at or
      below it.
    - The stuck-armed condition fires when the backend's write-safety flag reads enabled with no
      dispatch in flight, and does not fire during a legitimate in-flight dispatch window.
    - The stuck-armed notice attributes to whoever can actually disarm, and points at Phase 28's
      control rather than instructing anyone to run anything.
  </behavior>
  <action>
    Extend `sweep_conditions.py` with the three conditions, all pure over already-fetched data.

    For the failed-run condition, do **not** stop at run status. Phase 27 D-04b already established
    that this deployment's real failures live in per-node output data rather than in the run's status
    field, and 29-RESEARCH.md confirmed the same pattern on the maintenance workflow's own read
    nodes. Inspect the per-node data for the read nodes the way the repo already walks `runData`, and
    where a swallowed failure is detectable, detect it. Where it is not detectable, say so in the
    notice rather than letting a run's success field stand in for health — a maintenance job whose
    executions read success for weeks while the queue counts stop moving is the exact shape this
    condition must not miss quietly.

    For the stuck-armed condition, distinguish "armed right now because a dispatch is in flight" from
    "armed and nothing is happening". The cheap discriminator is whether an execution is currently
    running on the lane the flag governs — data the sweep already has. Getting this wrong in the
    noisy direction is the worse failure: a sweep that cries stuck-armed during every normal dispatch
    is a sweep the operator learns to ignore, and an ignored sweep is the same as no sweep.

    Take both thresholds — review backlog count, and how long armed-with-nothing-running must persist
    — as parameters with documented defaults, consistent with Task 1's floor.

    Extend `test_sweep_conditions.py` for all three, including explicitly asserting that the
    falsely-successful maintenance execution does not read as healthy.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_conditions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_conditions.py -q` exits 0.
    - A test asserts `executions_with_failure` fires the failed-run condition and names the workflow.
    - A test asserts `execution_maintenance_falsely_successful` does not produce a healthy verdict.
    - A test asserts the review-backlog condition fires above the threshold and not at or below it.
    - A test asserts the stuck-armed condition fires when armed with nothing running, and does not fire when a dispatch is in flight.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0, including `test_sweep_read_only.py`.
  </acceptance_criteria>
  <done>All five of D-08's conditions plus D-10's stuck-armed backstop are implemented, and the one that has a known blind spot says what it cannot see instead of implying coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Silence when healthy, grouping when several fire, attribution on every notice</name>
  <files>operator-claude-plugin/scripts/sweep_notify.py, operator-claude-plugin/scripts/sweep_entry.py, operator-claude-plugin/tests/test_sweep_attribution.py</files>
  <read_first>
    - `29-CONTEXT.md` D-08, D-09 and §Specific Ideas — silence when healthy, attribution on every
      notice, and the observation that the sweep's value is proportional to how rarely it speaks.
    - `27-CONTEXT.md` D-04, D-05 — the attribution guardrail this reuses: an unmatched cause is
      labelled an interpretation, shows the raw text, and defaults to admin. This is a verbatim
      reuse, not a re-derivation.
    - `29-HOST-PROBE.md` (from 29-01) Section 2 — the observed delivery surface and its text ceiling.
      Grouping decisions are constrained by what actually renders there.
    - `29-CONTEXT.md` §"Claude's Discretion" — wording and grouping are discretionary; record the
      chosen convention in the module docstring so a later reader can tell it was chosen rather than
      drifted into.
  </read_first>
  <behavior>
    - All conditions healthy: the entrypoint produces nothing. Not an empty report, not an all-clear.
    - One condition fired: one notice, stating the cause in plain language and who can act.
    - Several conditions fired at once: grouped into one delivery that fits the observed ceiling,
      with the most actionable item first and nothing dropped silently — a truncated tail states how
      many further items exist.
    - A cause the attribution table does not recognize is labelled as an interpretation, shows the
      raw text, and attributes to an admin.
    - No notice contains an instruction to run a command, edit a file, or open a terminal. Notices
      point at Phase 28's controls or name the person who can act.
  </behavior>
  <action>
    Extend `sweep_notify.py` and `sweep_entry.py` so the multi-condition case is handled once, in one
    place, rather than per condition.

    Keep the healthy path structurally silent: the entrypoint's healthy branch should produce nothing
    to say, so that a future condition added carelessly cannot turn the sweep into a heartbeat. This
    matters more than it looks — 29-CONTEXT is explicit that a noisy sweep gets ignored, and an
    ignored sweep is the same as no sweep.

    Choose the grouping and ordering convention against the ceiling `29-HOST-PROBE.md` recorded. If
    that file recorded no usable ceiling, pick a conservative one and say in the docstring that it is
    conservative pending observation, rather than assuming a generous limit and discovering
    truncation in production.

    Reuse Phase 27's attribution helper if it exposes one. If it does not, mirror its three rules
    exactly and note in the docstring that the two must be kept in step — a second attribution
    convention that drifts from Phase 27's is how the operator ends up being told two different
    things about the same error on two different surfaces.

    Write `test_sweep_attribution.py` covering the healthy silence, single and multiple firing,
    the unrecognized-cause default, and the absence of operator-run instructions in notice text.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_attribution.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_sweep_attribution.py -q` exits 0.
    - A test asserts an all-healthy input produces no notice output at all.
    - A test asserts several simultaneously-fired conditions produce one grouped delivery within the recorded ceiling, with a stated count of any items not shown.
    - A test asserts an unrecognized cause is attributed to an admin and carries both the interpretation label and the raw text.
    - A test asserts every generated notice carries an operator-or-admin attribution field.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The sweep says nothing when there is nothing to say, says one clear thing when several things are wrong at once, and never tells the operator they can fix something it does not understand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider probe data → operator-facing cause | The place a confident wrong classification does the most damage |
| n8n run status → "the backend is healthy" | A boundary that looks trustworthy and is not (D-08b) |
| notice text → operator action | Attribution decides whether the operator acts or escalates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-15 | Spoofing / operator harm | quota + credential classification | high | mitigate | Three-way outcome with unknown as a first-class value; tests assert an unreadable balance never yields an exhausted-quota notice, across all four provider fixture states |
| T-29-16 | Repudiation | failed-scheduled-run detection | high | mitigate | Per-node data inspection rather than run status alone (Phase 27 D-04b's pattern); the residual blind spot is stated in the notice and the docstring rather than implied covered |
| T-29-17 | Spoofing | unrecognized error cause | high | mitigate | Phase 27 D-05's guardrail reused verbatim — interpretation label, raw text shown, admin attribution by default |
| T-29-18 | Denial of Service | notice volume | medium | mitigate | Structural silence on the healthy path plus single grouped delivery for simultaneous conditions; a sweep that cries wolf is functionally a disabled sweep |
| T-29-19 | Repudiation | the sweep's own liveness | medium | accept | A sweep that stops running is indistinguishable from a healthy backend (29-RESEARCH.md §Known Threat Patterns). D-08 locks the notice list to five conditions, so self-liveness is recorded as a known gap for a future requirement rather than added here |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `.venv/bin/python -m pytest -q` green.
- `test_sweep_read_only.py` still green — expanding the condition set introduced no reachable mutation.
</verification>

<success_criteria>
- Every condition D-08 and D-10 name is implemented and tested against a fixture.
- Unknown is never reported as exhausted, and a successful maintenance run is never reported as health.
- Healthy means silent, structurally.
</success_criteria>

## Artifacts this phase produces

This plan's share:
- `operator-claude-plugin/scripts/sweep_conditions.py` — credential failure, exhausted quota, failed
  scheduled run, review backlog over threshold, stuck-armed backend (added to 29-03's stuck lock)
- `operator-claude-plugin/scripts/sweep_notify.py` — grouping and attribution across conditions
- `operator-claude-plugin/scripts/sweep_entry.py` — structurally silent healthy path
- `operator-claude-plugin/tests/test_sweep_conditions.py`
- `operator-claude-plugin/tests/test_sweep_attribution.py`

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/29-notices-unattended-sweep/29-05-SUMMARY.md` when done
</output>
