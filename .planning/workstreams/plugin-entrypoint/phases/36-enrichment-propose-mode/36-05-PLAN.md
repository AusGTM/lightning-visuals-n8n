---
phase: 36-enrichment-propose-mode
plan: 05
type: execute
wave: 5
depends_on: ["36-04"]
files_modified:
  - .planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-05-SUMMARY.md
autonomous: false
requirements: [DISPATCH-02, STRUCT-02, STRUCT-04, PREVIEW-03]
estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All three suites are green against the §1 baselines with the expected growth: repo pytest ≥ 1933 passed / 6 skipped, plugin pytest exactly 1052 passed / 5 skipped (unchanged — this phase touches no plugin file), node ≥ 553 pass, with roughly +30 node and +15 pytest from this phase."
    - "`grep -c 'ALLOW_HUBSPOT_[A-Z_]* = \"true\"' n8n/*.json` reports 0 for every file — no arming anywhere in this phase, and no `ENABLE_BAKED_FLAGS` anywhere in the diff."
    - "`.venv/bin/python scripts/build_cloud_workflows.py` is idempotent: a re-run against the committed tree leaves `git diff --stat n8n/` empty, proving every committed `n8n/*.json` came from the builder rather than a hand edit."
    - "No file under `operator-claude-plugin/` and no file under `~/.claude/plugins/` appears in this phase's diff — Phase 36 is backend-only, with no plugin release."
    - "The deployed tenant is REBUILT, deployed DISARMED, every active workflow BOUNCED deactivate→activate, and read back with `--expectation disarmed`; `LV Review Decision` is inactive at rest and stays inactive."
    - statement: "The disarmed deploy itself is operator-executed: `scripts/deploy_n8n_workflows.py` is denied to agents by the permission classifier in every form. The executor hands over the one-liner and does not attempt it. The bounce and the read-back DO pass and are agent-run."
      verification: backstop
    - statement: "The HubSpot `CONTAINS_TOKEN` operator and Lusha v3's acceptance of name+company on the cloud endpoint are both `[ASSUMED]` offline. The first live `mode:\"propose\"` run against the deployed tenant is their shared proof; per-provider and per-node errors already surface in the response body and in n8n execution history."
      verification: backstop
  artifacts:
    - .planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-05-SUMMARY.md
  key_links:
    - "A bare PUT never reloads a RUNNING workflow — only a deactivate→activate cycle does, and a read-back proves STORED content only. Deploy without the bounce leaves the tenant serving the pre-PUT body while every read-back reports success."
    - "Four workflows are active at rest (`LV Backend Status`, `LV Contact Ingest`, `LV Enrichment`, `LV Scheduled Maintenance`); `LV Review Decision` is inactive and must not be activated by the bounce."
  prohibitions:
    - statement: "MUST NOT arm any write flag as part of shipping this phase — no `ENABLE_BAKED_FLAGS`, no `ALLOW_HUBSPOT_*` set true. A feature that needs arming to be demonstrated is not this feature."
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT report a deploy as landed on the strength of a read-back alone — a read-back proves stored content, and only a deactivate→activate bounce proves the running body changed."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT silence a failing test by amending its premise — the one pinned assertion this phase changes (`IF Bare Event`'s false lane) carries its reason inline and is the only one."
      category: values
      status: resolved
      verification: judgment
---

<objective>
Close the phase: prove the offline gates, hand the operator the disarmed deploy they alone can
run, then bounce and read back.

Purpose: 36-CONTEXT.md §8 items 7-9 and §9's non-negotiables are the seal conditions. Item 9
is the only one with no agent path — the permission classifier denies
`scripts/deploy_n8n_workflows.py` to agents in every form, so it is a blocking human checkpoint
by design, not a fallback. The bounce and the read-back are agent-runnable and must not be
folded into the checkpoint, because "did the fix deploy" and "is the tenant still disarmed" are
separate questions this repo has learned to keep separable.

Output: three green suites, an arming grep of zero, an idempotent rebuild, a deployed and
bounced tenant, and a disarmed read-back verdict.
</objective>

## Artifacts this plan produces

No new source symbols. This plan verifies and ships what plans 36-01 through 36-04 built; the
full artifact inventory for the phase is in `36-01-PLAN.md`'s "Artifacts this phase produces".

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/STATE.md
@.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-VALIDATION.md
@.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The offline gates — suites, arming grep, rebuild idempotence, scope</name>
  <files>.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-05-SUMMARY.md</files>
  <precondition>`.venv/bin/python` exists and imports the repo's test dependencies — the system python lacks them and every command below assumes the venv interpreter.</precondition>
  <read_first>
    - `36-CONTEXT.md` §8 (the nine Definition-of-Done items) and §11 (the exact test command forms — alternatives are broken in this repo).
    - `36-VALIDATION.md`'s DoD → test-command table, which is the verification contract every earlier plan's `<verify>` was written against.
    - `.planning/workstreams/plugin-entrypoint/STATE.md` — the baselines to beat and the current tenant state.
  </read_first>
  <action>
    Run the four gates in the exact command forms 36-CONTEXT.md §11 gives — the directory form of `node --test` is broken on node 24 and the system python lacks the dependencies, so no substitute is acceptable. Record each observed count against its baseline in the summary, not just a pass/fail: repo pytest against 1933 passed / 6 skipped, plugin pytest against 1052 passed / 5 skipped, node against 553 pass, arming grep against 0.

    Re-run the builder against the committed tree and confirm `git diff --stat n8n/` is empty. A non-empty diff means a committed workflow file was hand-edited or a builder edit was committed without its rebuild — either is a stop-and-report condition, not something to fix by committing the diff.

    Confirm the phase's scope boundary with `git diff --stat` over the whole phase range: no path under `operator-claude-plugin/`, and nothing at all under `~/.claude/plugins/`.

    Walk 36-CONTEXT.md §8's items 1 through 8 and record, per item, which committed test or command proves it and what it observed. Where an item's proof is structural rather than behavioural (items 1, 3, 5), say so plainly — this repo's own convention is to name what a proof does and does not establish rather than let a green tick imply more than it earned.

    If any gate is red, STOP and report. Do not amend a test to make it pass; the one pinned assertion this phase was authorised to change was amended in 36-02 with its reason inline, and there is no second one.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q &amp;&amp; .venv/bin/python -m pytest operator-claude-plugin/tests/ -q &amp;&amp; node --test tests/n8n/*.test.mjs &amp;&amp; .venv/bin/python scripts/build_cloud_workflows.py &amp;&amp; test -z "$(git diff --stat n8n/)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0 reporting at least 1933 passed and 6 skipped.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0 reporting exactly 1052 passed and 5 skipped.
    - `node --test tests/n8n/*.test.mjs` exits 0 reporting at least 553 passing.
    - `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` reports `0` for every file.
    - `grep -rn 'ENABLE_BAKED_FLAGS' scripts/ n8n/ tests/ | grep -v '^tests/' | wc -l` is unchanged from before the phase (this phase adds no arming affordance).
    - `.venv/bin/python scripts/build_cloud_workflows.py` followed by `git diff --stat n8n/` produces empty output.
    - `git diff --stat` over the phase range lists zero paths under `operator-claude-plugin/`.
    - The summary records an observed count for each of the four gates and a per-item proof for 36-CONTEXT.md §8 items 1-8.
  </acceptance_criteria>
  <done>Every offline seal condition is observed and recorded with its number, and the committed workflow JSON is proven to be builder output rather than hand-edited.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Plans 36-01 through 36-04 changed `scripts/build_cloud_workflows.py` and regenerated every
    `n8n/*.json`. The generated artifacts are committed and every offline gate in Task 1 is green,
    but the running tenant is still serving the pre-Phase-36 body.
  </what-built>
  <how-to-verify>
    **This step is yours to run — the permission classifier denies `scripts/deploy_n8n_workflows.py`
    to agents in every form (shell and python-driver alike). Run it via `!` from the repo root.**

    1. Confirm the rebuild is current (safe for the agent, may already be done):

       `.venv/bin/python scripts/build_cloud_workflows.py`

    2. Disarmed deploy — this is the one line only you can run:

       `DRY_RUN=false ALLOW_N8N_DEPLOY=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv('.env'); import runpy; runpy.run_path('scripts/deploy_n8n_workflows.py', run_name='__main__')"`

    3. Report back what it printed — specifically the per-workflow update lines and the count of
       nodes it rewrote. **Do not memorise or compare against a remembered declaration count**;
       both runbooks derive the expected count at deploy time, and a stale figure makes a correct
       deploy look like a misfire.

    **No arming.** If you find yourself reaching for `ENABLE_BAKED_FLAGS` or setting any
    `ALLOW_HUBSPOT_*` flag, stop — that is out of scope for this phase, and nothing this phase
    built needs it. The propose path reads no write flag at all.

    The agent will handle the bounce and the read-back after you confirm.
  </how-to-verify>
  <resume-signal>Paste the deploy output, or type "deployed" — then the agent bounces and reads back. If it failed, paste the error instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Bounce every active workflow, then read back disarmed</name>
  <precondition>The operator has run the disarmed deploy in Task 2 and reported it succeeded — a bounce before the PUT lands proves nothing.</precondition>
  <files>.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-05-SUMMARY.md</files>
  <read_first>
    - `.planning/workstreams/plugin-entrypoint/STATE.md` — the four workflows active at rest and the standing rule that `LV Review Decision` is inactive and must stay inactive.
    - `36-CONTEXT.md` §9 non-negotiable 7 — a bare PUT never reloads a running workflow; only a deactivate→activate cycle does, and a read-back proves stored content only.
    - The `n8n_control.set_active` entry point and `scripts/verify_live_write_safety.py` — both pass the permission classifier and are agent-runnable, unlike the deploy script.
  </read_first>
  <action>
    Bounce each of the four active workflows with `n8n_control.set_active`: deactivate, then activate. Take each verdict from an INDEPENDENT second read rather than trusting the mutation's own return value. Do not touch `LV Review Decision` — it is inactive at rest and activating it is a separate, unrelated decision.

    Then run the disarmed read-back:

    `.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv('.env'); import runpy; runpy.run_path('scripts/verify_live_write_safety.py', run_name='__main__')" --expectation disarmed`

    Record the verdict verbatim, along with the number of workflows and declaring nodes it
    discovered. That count is DERIVED, not remembered — this phase adds no new declaring node, so
    the count should match the pre-phase discovery; if it does not, report the difference rather
    than rationalising it.

    Additionally GET the live `LV Enrichment` workflow and confirm the RUNNING body contains the
    four new node names (`IF Has Email`, `IF Name Searchable`, `HubSpot Name Search`,
    `Adapt Name Search`) and that `HubSpot Name Search` has a credential attached. A read of the
    stored body is not the same as a read of the running one, and an unbound HubSpot node is
    exactly the failure this repo has shipped four times.

    Record in the summary that the two `[ASSUMED]` items — the `CONTAINS_TOKEN` operator name and
    Lusha v3 name+company on the cloud endpoint — remain unproven until the first live
    `mode:"propose"` run, and that that run is the operator's, in Phase 37's walk, not this
    plan's.
  </action>
  <verify>
    <human-check>The disarmed read-back printed a disarmed PASS verdict, all four previously-active workflows are active again, `LV Review Decision` is still inactive, and the live `LV Enrichment` body carries the four new nodes with `HubSpot Name Search` credential-bound.</human-check>
    <automated>grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json</automated>
  </verify>
  <acceptance_criteria>
    - The read-back command prints a disarmed PASS verdict, quoted verbatim in the summary along with its discovered workflow and declaring-node counts.
    - An independent read confirms all four previously-active workflows are active, and `LV Review Decision` is inactive.
    - A GET of the live `LV Enrichment` workflow returns a body containing all four new node names, and `HubSpot Name Search` carries a `hubspotAppToken` credential reference.
    - `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` reports `0` for every file after the deploy.
    - The summary states plainly that `CONTAINS_TOKEN` and Lusha name+company on cloud are still `[ASSUMED]` and names the first live propose run as their proof.
  </acceptance_criteria>
  <done>The running tenant carries Phase 36's workflow bodies, is proven disarmed by an independent read-back, and the two remaining live-proof items are named rather than quietly assumed closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → n8n Cloud tenant | A deploy replaces executable workflow bodies on a live tenant holding real HubSpot and provider credentials |
| agent → deploy tooling | The permission classifier is the boundary; the deploy script is denied to agents in every form and the bounce/read-back are not |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-21 | Elevation of Privilege | an agent performing an armed or unreviewed deploy | high | mitigate | The deploy is a blocking `checkpoint:human-action`; the executor is instructed not to attempt it, and the classifier denies it independently of this plan's text. |
| T-36-22 | Repudiation | reporting a deploy as landed when the running body is unchanged | high | mitigate | Deactivate→activate bounce plus a GET of the RUNNING body for the four new node names — not a read-back of stored content alone. |
| T-36-23 | Information Disclosure | `HubSpot Name Search` deployed unbound → 401 with request context in execution history | high | mitigate | The credential-map entry landed in 36-02; Task 3 confirms the attachment on the live body, which is the only place an unbound node is observable. |
| T-36-24 | Tampering | an unintended workflow (`LV Review Decision`) activated by the bounce | medium | mitigate | The bounce enumerates exactly the four workflows active at rest, and the acceptance criterion asserts `LV Review Decision` is still inactive afterwards. |
| T-36-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependency in this phase; no install task exists. |
</threat_model>

<verification>
1. Three suites green in the exact command forms of 36-CONTEXT.md §11, with observed counts recorded.
2. `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` → `0` for every file.
3. Builder rebuild idempotent — `git diff --stat n8n/` empty on re-run.
4. Disarmed deploy executed by the operator; output recorded.
5. Four active workflows bounced; `LV Review Decision` untouched.
6. `verify_live_write_safety.py --expectation disarmed` → disarmed PASS, verdict quoted.
7. Live `LV Enrichment` body carries the four new nodes with `HubSpot Name Search` credential-bound.
</verification>

<success_criteria>
- 36-CONTEXT.md §8 items 1 through 9 each have a named proof and an observed result.
- No arming anywhere; no plugin file touched; no plugin release.
- The tenant runs Phase 36's bodies and is proven disarmed.
- The two remaining `[ASSUMED]` items are named as open, not silently sealed.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-05-SUMMARY.md` when done
</output>
