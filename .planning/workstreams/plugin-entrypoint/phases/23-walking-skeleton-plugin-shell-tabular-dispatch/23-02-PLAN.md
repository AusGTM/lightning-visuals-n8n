---
phase: 23-walking-skeleton-plugin-shell-tabular-dispatch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INGEST-02]

must_haves:
  truths:
    - "It is known, from a real Claude Desktop Code-tab session, whether a file the operator attaches resolves to a path a plugin script can open."
    - "It is known whether the operator's Code-tab session can run python3 and install the plugin's three dependencies."
    - "The answer is recorded in this plan's SUMMARY, where plan 23-04 reads it before writing any file-handoff code."
  artifacts:
    - .planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-02-SUMMARY.md
  key_links:
    - "23-04's SKILL.md file-handoff branch is written from this outcome — if the attachment leg does not resolve to a path, no attachment plumbing is written beyond a single try/except (D-14a)."
---

<objective>
Close the one question 23-RESEARCH.md could not answer from documentation before any code is built
on top of a guess: does an operator-attached CSV in the Claude Desktop **Code tab** become a real
filesystem path a plugin's Python script can open, and can that session run the plugin's scripts at
all.

Purpose: D-14a is explicit — build the `@mention` autocomplete path as the reliable mechanism,
attempt attachment opportunistically, and gate any attachment-specific plumbing behind ONE early
live smoke test. Research Pitfall 3 is exactly the failure this plan prevents: sinking effort into
temp-directory scanning and upload-shim guessing for a leg that cannot work.

Output: two recorded facts, no code. This plan is deliberately cheap and deliberately first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke-test the attachment leg in a real Code-tab session</name>
  <read_first>
    - `23-RESEARCH.md` §"File Handoff (D-14) — the one open risk" — the two behaviours that are easy
      to conflate (the Chat-tab `/mnt/user-data/uploads/` convention vs. the Code tab), and
      assumption A1.
    - `23-CONTEXT.md` D-14 / D-14a.
  </read_first>
  <precondition>The operator (or you) has Claude Desktop open on the **Code** tab — not Chat, not Cowork — with this repository as the working folder. Nothing in this plan works in the CLI-only environment the executor normally runs in.</precondition>
  <what-built>Nothing yet. This is a pre-build environment probe whose answer decides how plan 23-04 writes its file-handoff branch.</what-built>
  <how-to-verify>
    1. Create a throwaway CSV anywhere outside this repo — three columns with deliberately messy
       headers (for example `Email Address`, `First Name`, `Notes`) and two rows.
    2. In a Claude Desktop **Code tab** session, attach that CSV to a prompt (drag-and-drop or the
       attachment button) and ask: "print the absolute filesystem path of the file I just attached,
       then read its first line with a shell command." Record verbatim whether a real path came
       back and whether the shell command succeeded, or whether the file was only visible as
       conversation content.
    3. In the same session, ask it to run `python3 --version` and
       `python3 -c "import openpyxl, requests, yaml; print('deps ok')"`. Record both outputs. If the
       import fails, record whether `pip install -r operator-claude-plugin/requirements.txt` (the
       file does not exist yet — try the three package names directly) succeeds or is blocked.
    4. Separately, confirm the fallback leg works: type `@` in the Code tab and fuzzy-pick a file by
       name, and confirm the session reads it. This is the mechanism 23-04 will rely on regardless
       of step 2's answer.
  </how-to-verify>
  <acceptance_criteria>
    - The SUMMARY records, as a yes/no with the observed evidence: (a) attachment resolves to a
      readable path, (b) python3 is available in that session, (c) the three dependencies import or
      can be installed, (d) `@mention` file selection resolves to a real path.
    - If (a) is no, the SUMMARY states plainly that 23-04 writes no attachment plumbing beyond one
      try/except, per D-14a.
  </acceptance_criteria>
  <resume-signal>Paste the four observed answers, or type "attachment works" / "attachment does not work" plus the python3 result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's machine → plugin scripts | The file the operator hands over is the only untrusted input this phase reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Information Disclosure | smoke-test CSV | low | mitigate | The probe uses a throwaway file with invented values — no real contact data is attached to answer an environment question |
| T-23-08 | Tampering | attachment-path guessing | medium | mitigate | The probe's whole purpose is to stop 23-04 from guessing at temp directories; a "no" answer is recorded as a build instruction, not worked around |
</threat_model>

<verification>
The SUMMARY carries four recorded answers with the evidence observed, and states which file-handoff
mechanism 23-04 must treat as primary.
</verification>

<success_criteria>
- No code was written in this plan.
- Plan 23-04 can start without assuming anything about the attachment leg.
</success_criteria>

## Artifacts this phase produces

This plan produces no source artifacts by design. Its output is the recorded environment fact that
constrains `operator-claude-plugin/skills/contact-upload/SKILL.md` (written in 23-04): which of the
two file-handoff legs is the reliable one.

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-02-SUMMARY.md` when done
</output>
