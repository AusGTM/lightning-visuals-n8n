---
phase: 23-walking-skeleton-plugin-shell-tabular-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/wf_contact_ingest_cloud.json
  - tests/n8n/contactCreateGateFlow.test.mjs
  - tests/test_contact_create_overlay.py
  - CHANGELOG.md
autonomous: true
requirements: [DISPATCH-01]

must_haves:
  truths:
    - "A plain (disarmed) deploy of wf_contact_ingest_cloud.json routes a net_new contact row to action=review, exactly as today."
    - "A deploy carrying the ENABLE_BAKED_FLAGS write-safety overlay routes the same net_new row to action=create, so an armed dispatch can actually create a contact."
    - "Arming contact creation still cannot happen without an allowlist in the same deploy invocation — the existing TEST_RECORD_* coupling is unchanged."
  artifacts:
    - scripts/build_cloud_workflows.py (Decide Action carries the baked write-safety constant)
    - n8n/wf_contact_ingest_cloud.json (rebuilt, constant committed disabled)
    - tests/n8n/contactCreateGateFlow.test.mjs
    - tests/test_contact_create_overlay.py
  key_links:
    - "The declaration string in Decide Action must be byte-identical to what _write_safety_const('ALLOW_HUBSPOT_CREATE') emits, or deploy_n8n_workflows.enable_baked_flags cannot reach it and the deploy fails closed."
    - "Decide Action must not depend on a row field surviving Extract From File — the constant is read in the node that decides, not seeded upstream of a node that replaces json."
---

<objective>
Close the backend blocker D-15 found: `wf_contact_ingest_cloud.json` forces every net-new contact
row to `needs_review` regardless of arming, so the lane this phase demonstrates cannot create a
contact at all.

Purpose: this phase's stated flow ends in a contact landing in HubSpot. Without this fix the
walking skeleton demonstrates a flow that stops short of its own goal (D-18: land it early, not
last). Per D-17 this is a deliberate, separately-justified BACKEND gate fix — success criterion 4
("no backend file is modified to make the client work") still binds every file under
`operator-claude-plugin/`; the client works either way, the backend gate is what is broken.

Output: the contact lane's create decision reads the same deploy-time-overlayable write-safety
constant its downstream write gate already reads, plus two regression tests that prove both the
disarmed and the armed behaviour offline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/ROADMAP.md
@.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bake the overlayable create constant into the contact lane's Decide Action</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_contact_ingest_cloud.json</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` — `DECIDE_CLOUD` (the contact-lane CLOUD variant, around
      line 346, note its `const allow_create = row.allow_create === true;` line), the contact chain
      build site that appends it (`("Decide Action", DECIDE_CLOUD)`, around line 640),
      `WRITE_SAFETY_DEFAULTS` / `_write_safety_const` (around lines 843-856), and `Set Config`
      (around line 578).
    - `scripts/deploy_n8n_workflows.py` — `_OVERLAY_FLAG_SPEC` (line 128), `enable_baked_flags`
      (line 297) and `_requested_overlay_flags` (line 378). These define the EXACT literal the
      overlay rewrites and the allowlist coupling that must survive.
    - `n8n/wf_contact_ingest_cloud.json` — the `Set Config`, `Extract From File`, `Map Columns`,
      `Decide Action`, `HubSpot Create Write Gate` nodes.
    - `tests/test_write_gate_coverage.py` and `tests/test_enabled_build_invariants.py` — the
      invariants any new write-safety declaration must satisfy.
  </read_first>
  <behavior>
    - Committed artifact: `Decide Action` declares the write-safety create constant at its
      disabled literal, so `test_committed_write_safety_constants_are_all_disabled` still passes.
    - Committed artifact behaviour: a row with `identity.outcome === "net_new"` yields
      `action === "review"`.
    - Overlay-applied artifact behaviour: the same row yields `action === "create"` and the create
      branch seeds email/firstname/lastname into `properties` (BUG 19 behaviour preserved).
    - `deploy_n8n_workflows.enable_baked_flags` reports a rewrite count for the create constant in
      this workflow strictly greater than the two write-gate nodes alone.
    - `lookup_failed === true` still downgrades a create to review even when the constant is armed.
  </behavior>
  <action>
    Make the contact lane's create decision read the deploy-time-overlayable write-safety constant
    instead of a row field (D-16). Reuse the EXISTING `ALLOW_HUBSPOT_CREATE` constant that
    `HubSpot Create Write Gate` in this same workflow already reads — do not introduce a fifth
    overlay flag: `tests/test_enabled_build_invariants.py::test_overlayable_flags_is_a_strict_subset_of_config_flag_defaults`
    pins `_OVERLAYABLE_FLAGS` to exactly four names, `CONFIG_FLAG_DEFAULTS` is parity-guarded
    across both builders, and reusing the existing flag keeps the `_requested_overlay_flags`
    fail-safe (writes cannot be armed without a `TEST_RECORD_IDS`/`TEST_RECORD_DOMAINS` allowlist
    in the same invocation) applying to contact creation for free.

    Bake it at the node that decides, not at `Set Config`. `Extract From File` emits fresh items
    parsed from the binary CSV, so a value seeded onto the webhook item's json upstream of it is
    not a channel this fix may rely on — that is the same class of silent row loss BUG 12/BUG 21
    already cost this repo two armed windows. `Decide Action` (`DECIDE_CLOUD`) is contact-lane and
    Cloud-only, sits downstream of every transform, and already computes `allow_create`.

    Compose the declaration at the build site (`build_cloud`'s contact chain list) rather than at
    `DECIDE_CLOUD`'s module-level definition — `_write_safety_const` is defined later in the module
    and calling it at definition time would raise NameError. Prepend
    `_write_safety_const("ALLOW_HUBSPOT_CREATE")` plus a newline to `DECIDE_CLOUD` where the
    contact chain appends the node, so the emitted jsCode opens with that declaration verbatim.

    Inside `DECIDE_CLOUD`, derive the decision from the constant using the same coercion
    `_writeSafetyAllows` uses (`String(...).toLowerCase() === "true"`), keeping the local
    identifier name `allow_create` so `tests/test_create_payload_identity.py`'s window search still
    matches. Do not OR it with the row's own value — the deploy-time constant is the sole authority
    on the Cloud lane. Leave `MAP_COLUMNS` untouched: it is shared with the local build, whose
    dry-run echo lane legitimately keeps its row-seeded gate.

    Update the `Set Config` comment so it points at where the create gate now actually lives, and
    leave its row seed in place for the local/dry-run lane.

    Rebuild the committed artifacts with `python scripts/build_cloud_workflows.py`. That command
    rewrites all six workflow JSONs; the diff must be confined to
    `n8n/wf_contact_ingest_cloud.json`. If any other workflow JSON changes, STOP and report — that
    means the committed artifacts had already drifted from the builder and rebasing that drift is
    not this task's business.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_write_gate_coverage.py tests/test_enabled_build_invariants.py tests/test_row_carry.py tests/test_create_payload_identity.py tests/test_cloud_write_path.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json;wf=json.load(open('n8n/wf_contact_ingest_cloud.json'));n=[x for x in wf['nodes'] if x['name']=='Decide Action'][0];js=n['parameters']['jsCode'];assert 'const ALLOW_HUBSPOT_CREATE = \"false\";' in js"` exits 0.
    - `git diff --name-only -- n8n/` lists exactly `n8n/wf_contact_ingest_cloud.json`.
    - The pytest command above exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Reverting is a one-line builder edit plus a rebuild; no data or deployed state depends on it until an admin deploys.</reversibility>
  <done>The committed contact workflow declares the create constant disabled, and the create decision reads that constant rather than a row field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests — disarmed routes to review, overlay routes to create</name>
  <files>tests/n8n/contactCreateGateFlow.test.mjs, tests/test_contact_create_overlay.py</files>
  <read_first>
    - `tests/n8n/researchChainRowFlow.test.mjs` — the established offline item-flow harness that
      executes committed node jsCode via `new Function`; mirror its shape and its safety note.
    - `tests/test_deploy_write_safety_overlay.py` — the established pytest idiom for asserting on
      `enable_baked_flags` output.
    - `n8n/wf_contact_ingest_cloud.json` — `Decide Action` and `HubSpot Create Write Gate` bodies
      as rebuilt by Task 1.
  </read_first>
  <behavior>
    - JS flow test, committed artifact: a net_new row through `Decide Action` yields
      `action === "review"`.
    - JS flow test, literal swapped to the enabled form (mirroring what the deploy overlay does):
      the same row yields `action === "create"`.
    - JS flow test: a create-action row through `HubSpot Create Write Gate` is dropped while the
      allowlist constants are empty, and passes once a matching `TEST_RECORD_DOMAINS` value and the
      two enabling constants are set — the allowlist remains a real second key.
    - Python test: `enable_baked_flags` on the committed workflow rewrites the create constant in
      three or more nodes and raises nothing; the committed file carries only the disabled literal.
  </behavior>
  <action>
    Write `tests/n8n/contactCreateGateFlow.test.mjs` following the `researchChainRowFlow` harness:
    read the committed workflow JSON, pull a node's `jsCode` by name, execute it with a stub
    `$input.all()` returning the seed items, and assert on the returned items. Cover the four JS
    behaviours above. Perform the armed variant by string-replacing the disabled declaration with
    the enabled one in the loaded jsCode before executing — the same exact-literal swap
    `enable_baked_flags` performs — so the test fails if the declaration's spelling drifts out of
    the overlay's reach.

    Write `tests/test_contact_create_overlay.py` importing `scripts.deploy_n8n_workflows`, calling
    `enable_baked_flags` on the committed contact workflow with the create + record-writes flags
    mapped to their enabled literals, and asserting the rewrite count and the absence of the
    fail-closed ValueError. Add a companion assertion that the committed file carries the disabled
    literal only, so the pair cannot both pass on an accidentally armed artifact.

    Non-vacuity matters more than coverage here: assert the seed row actually reaches
    `Decide Action` with `outcome === "net_new"` before asserting on the action, so a harness that
    silently produced zero items cannot pass.
  </action>
  <verify>
    <automated>node --test tests/n8n/contactCreateGateFlow.test.mjs && .venv/bin/python -m pytest tests/test_contact_create_overlay.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/n8n/contactCreateGateFlow.test.mjs` exits 0 and reports at least 4 passing tests.
    - `.venv/bin/python -m pytest tests/test_contact_create_overlay.py -q` exits 0.
    - Reverting Task 1's builder change and rebuilding makes `contactCreateGateFlow.test.mjs` fail (guard is not vacuous) — confirm by reasoning against the assertions, not by committing a revert.
  </acceptance_criteria>
  <done>Both new test files pass, and each asserts a behaviour that changes when the constant flips.</done>
</task>

<task type="auto">
  <name>Task 3: Record the gate fix in the backend changelog</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` — the repo-root backend changelog; match its existing entry format and heading
      conventions.
    - `.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-CONTEXT.md` — D-15 through D-18, the justification this entry records.
  </read_first>
  <action>
    Add one Unreleased entry describing: the contact lane forced every net-new row to review
    because the create decision read a hardcoded row field; the decision now reads the existing
    deploy-time-overlayable write-safety constant that the lane's own create write gate already
    reads; arming therefore requires the same two-key combination as every other write path in this
    repo, allowlist included. Name it as a backend gate fix made for phase 23 (per D-17), not as a
    client change — the client-side changelog for this milestone lives in
    `operator-claude-plugin/CHANGELOG.md` and is written by a later plan.
  </action>
  <verify>
    <automated>grep -c "ALLOW_HUBSPOT_CREATE" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ALLOW_HUBSPOT_CREATE" CHANGELOG.md` returns at least 1.
    - The new entry sits under the Unreleased heading and names the contact-upload lane explicitly.
  </acceptance_criteria>
  <done>The backend changelog records the gate fix and why it was made inside a client-scoped milestone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy script → n8n Cloud | A deploy decides what the live lane is permitted to write |
| n8n workflow → HubSpot | Contact create/update writes leave the system here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Elevation of Privilege | `Decide Action` create decision | high | mitigate | The decision reads the overlayable constant that already gates the write node, so arming creation still requires `ALLOW_HUBSPOT_RECORD_WRITES` + `ALLOW_HUBSPOT_CREATE` + a non-empty `TEST_RECORD_*` allowlist in the same deploy invocation (`_requested_overlay_flags` fail-safe, unchanged) |
| T-23-05 | Tampering | committed workflow artifact | high | mitigate | `test_committed_write_safety_constants_are_all_disabled` already scans every cloud workflow; the new declaration is covered by it automatically, and Task 2 adds a positive test that the committed literal is the disabled one |
| T-23-06 | Spoofing | CSV-supplied `allow_create` column | medium | mitigate | The Cloud decision no longer reads any row-derived value, so an uploaded column of that name cannot influence the create decision |
| T-23-SC | Tampering | package installs | low | accept | This plan installs nothing; RESEARCH.md's Package Legitimacy Audit clears the three plugin dependencies for later plans |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (full python suite) is green.
- `node --test tests/n8n/contactCreateGateFlow.test.mjs` is green.
- `git diff --name-only` shows no file under `operator-claude-plugin/` touched by this plan.
</verification>

<success_criteria>
- A disarmed deploy still cannot create a contact; an overlay-armed deploy can.
- The armed path still requires an allowlist naming the target record or domain.
- No client file was modified to achieve it (criterion 4 remains intact per D-17).
</success_criteria>

## Artifacts this phase produces

Complete phase-23 index; this plan contributes the backend rows.

**Backend (this plan — D-16/D-17):**
- `scripts/build_cloud_workflows.py` — `Decide Action` (contact CLOUD) emits
  `const ALLOW_HUBSPOT_CREATE = "false";` and derives its create decision from it
- `n8n/wf_contact_ingest_cloud.json` — rebuilt artifact
- `tests/n8n/contactCreateGateFlow.test.mjs`, `tests/test_contact_create_overlay.py`

**Plugin package (plans 23-03 → 23-05):**
- `operator-claude-plugin/.claude-plugin/plugin.json` — manifest (`name`, `description`, `version`, `author`)
- `operator-claude-plugin/skills/contact-upload/SKILL.md` — the skill; slash handle
  `/operator-claude-plugin:contact-upload`
- `operator-claude-plugin/scripts/config_gate.py` — `load_config()`, `describe_target()`, `ConfigError`
- `operator-claude-plugin/scripts/tabular.py` — `read_table()`, `to_csv_bytes()`
- `operator-claude-plugin/scripts/preview.py` — `build_preview()`, `label_headers()`
- `operator-claude-plugin/scripts/dispatch.py` — `dispatch(path, armed, config, transport)`, `NotArmedError`
- `operator-claude-plugin/config/operator.local.example.json` — tracked template; keys `n8n_url`,
  `webhook_secret`, optional `column_mapping_path`
- `operator-claude-plugin/config/operator.local.json` — gitignored, operator-created (D-03/D-05)
- `operator-claude-plugin/requirements.txt` — `openpyxl`, `requests`, `PyYAML`
- `operator-claude-plugin/tests/` — `conftest.py`, `test_transport_guard.py`, `test_config_gate.py`,
  `test_dispatch_multipart.py`, `test_preview_rendering.py`, `test_plugin_manifest.py`,
  `test_no_backend_imports.py`
- `.gitignore` — entry for `operator-claude-plugin/config/operator.local.json`

**Wire contract fixed by this phase (no new flag):** multipart field name `data`, header
`X-Enrichment-Secret`, URL `${n8n_url}/webhook/hubspot/contact-upload`, CSV bytes only.

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/23-walking-skeleton-plugin-shell-tabular-dispatch/23-01-SUMMARY.md` when done
</output>
