---
phase: 28-control-actions
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - operator-claude-plugin/scripts/probe_n8n_semantics.py
  - operator-claude-plugin/tests/test_control_probe.py
  - .planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-FINDINGS.md
autonomous: false
requirements: [CONTROL-03, CONTROL-06]

must_haves:
  truths:
    - "It is an observed fact, not an inference, whether this n8n Cloud instance round-trips settings and connections cleanly through GET→PUT (D-20)."
    - "It is an observed fact, not an inference, whether a deactivate→PUT→activate bracket makes a changed Schedule Trigger interval take effect on the running instance (D-17, D-18, research A1)."
    - "It is an observed fact whether POST /api/v1/workflows/{id}/execute exists on this instance, confirming or overturning D-05a's amendment (research A2)."
    - "The probe module has no code path that can write a write-safety constant — it cannot arm anything even if run wrongly."
    - "Findings are written down so later plans cite an observation rather than repeating the research's MEDIUM-LOW confidence."
  artifacts:
    - operator-claude-plugin/scripts/probe_n8n_semantics.py
    - operator-claude-plugin/tests/test_control_probe.py
    - .planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-FINDINGS.md
  key_links:
    - "The probe reuses 28-01's apply_mutation and MutationResult unchanged — if the probe needs a different pipeline to work, the pipeline is wrong and that is what this plan exists to discover."
    - "The cadence reload probe is the phase's FIRST genuine CONTROL-03 mutation performed early and observed. It is explicitly NOT the cadence-as-one-shot-fire workaround D-05c rejected: nothing is being made to fire in place of a manual trigger, and this is a human-supervised diagnostic, never an operator-facing verb."
---

<objective>
Convert this phase's two MEDIUM-LOW-confidence, load-bearing assumptions into observed facts before
any plan builds on them, and confirm the endpoint gap that D-05a already amended CONTROL-01 around.

Purpose: 28-RESEARCH.md is explicit that a bare GET-after-PUT proves the value was *persisted*, not
that the running instance is *honoring* it, and that its evidence for the deactivate→activate fix is
community-sourced rather than probed against this account. Baking that inference into the arming
lifecycle (28-03) and the cadence surface (28-04) would mean discovering it wrong during an armed
live window — the most expensive place to find out.

Output: a disarmed probe module, three recorded observations, and `28-FINDINGS.md` as the citable
source later plans point at instead of repeating "MEDIUM-LOW".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-RESEARCH.md
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-01-SUMMARY.md
@operator-claude-plugin/scripts/n8n_control.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The disarmed probe module — no-op round-trip, execute-endpoint check, cadence reload observation</name>
  <files>operator-claude-plugin/scripts/probe_n8n_semantics.py, operator-claude-plugin/tests/test_control_probe.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/n8n_control.py` — `fetch_workflow`, `put_body`,
      `apply_mutation`, `MutationResult`. The probe drives these, it does not reimplement them.
    - `n8n/wf_scheduled_maintenance_cloud.json` — the five `scheduleTrigger` nodes and their exact
      `parameters.rule.interval` shapes: `SJ-3 Trigger (15 min)`, `SJ-1 Trigger (hourly)`,
      `SJ-2 Trigger (monthly)`, `Dedupe Trigger (weekly)`, `Review Trigger (15 min)`.
    - `scripts/deploy_n8n_workflows.py` lines 146–166 — `_has_n8n()`, `_instance_ok()`,
      `_writes_allowed()`. The repo's wrong-instance guard and two-key convention, which this probe
      mirrors rather than inventing its own.
    - `28-RESEARCH.md` §"Open Questions" 1, 2 and 3 — the three questions this module answers.
  </read_first>
  <behavior>
    - `roundtrip` on a stubbed transport sends a PUT whose body is byte-identical, under a key-sorted
      dump, to `put_body` applied to what the GET returned — a genuine no-op.
    - `roundtrip` reports the diff when the post-PUT fetch does not match what was sent.
    - `execute_probe` treats 404 and 405 as the expected outcome and any 2xx as a finding that
      overturns D-05a rather than an error.
    - `cadence_reload` refuses to run when the target workflow's fetched `active` is false, naming
      why: the question is specifically about an already-running instance.
    - `cadence_reload` restores the captured prior interval and reports `failed` if the restoring
      read-back does not match.
    - Every subcommand refuses before any network call unless the probe's own enabling environment
      variable is set, and refuses when the instance guard rejects the configured URL.
    - The module exposes no function that writes a write-safety constant.
  </behavior>
  <action>
    Write `operator-claude-plugin/scripts/probe_n8n_semantics.py` as a small CLI with three
    subcommands, each driving 28-01's pipeline rather than its own HTTP.

    `roundtrip` implements D-20: fetch the target workflow, run it through `put_body` unchanged, PUT
    it straight back through `apply_mutation` with an empty allowed-node set (a genuine no-op passes
    the structural diff trivially — which is itself worth proving), then fetch again and report
    whether `settings` and `connections` survived identically. This is the first live PUT the phase
    performs and it changes nothing by construction.

    `execute_probe` implements research A2: POST to the workflow-execute path against a real workflow
    id and report the status code. Expect 404 or 405. A 2xx would mean this Cloud account has the
    unmerged upstream endpoint and D-05a's amendment could be reconsidered — report that as a
    finding, not a failure, and do not act on it in this phase.

    `cadence_reload` implements D-18 and research A1. Take a target workflow id and Schedule Trigger
    node name, capture the current interval, mutate that ONE node's `parameters.rule.interval` to a
    short interval through `apply_mutation` with that node as the only allowed name, wait a bounded
    number of minutes polling `GET /api/v1/executions` filtered to that workflow, and report the
    observed spacing between execution start times against the interval that was set. Then restore
    the captured interval through the same pipeline and report the restoring read-back verdict.
    Report the restore verdict separately and prominently — a probe that leaves a schedule changed
    is worse than a probe that answers nothing.

    Gate the whole module behind its own enabling environment variable, defaulting off, and behind
    the same wrong-instance check the deploy script uses (`N8N_EXPECTED_URL`, else a genuine
    `.n8n.cloud` host). Do not import the deploy script for this — PLUGIN-04 forbids it; reimplement
    the two checks in ten lines and cite the source.

    The module must contain no path that writes a write-safety constant. Arming is 28-03's job,
    behind a human gate; a diagnostic that could arm by accident defeats the point of running the
    diagnostic first.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_probe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_probe.py -q` exits 0 with at least 7 tests collected.
    - `! grep -q "ALLOW_HUBSPOT" operator-claude-plugin/scripts/probe_n8n_semantics.py` exits 0 — the module names no write-safety constant at all.
    - A test asserts that with the enabling environment variable unset, every subcommand returns a refusal and the recording transport's call log has length 0.
    - A test asserts `roundtrip`'s PUT body equals `put_body(fetched)` under `json.dumps(..., sort_keys=True)`.
    - A test asserts `cadence_reload` refuses when the fetched workflow's `active` is false.
    - A test asserts `cadence_reload` issues a second `apply_mutation` restoring the captured interval, and reports `failed` when the restoring read-back mismatches.
  </acceptance_criteria>
  <done>A probe exists that can answer all three open questions, cannot arm anything, and cannot run by accident.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live gate — no-op round-trip (D-20) and the execute-endpoint check (A2)</name>
  <precondition>`N8N_URL` and `N8N_API_KEY` are set in the executing shell and `N8N_URL` matches `N8N_EXPECTED_URL` (or is a genuine `.n8n.cloud` host) — the probe refuses otherwise and no call is made.</precondition>
  <what-built>
    `probe_n8n_semantics.py` with `roundtrip` (a genuine no-op GET→PUT→GET, changing nothing) and
    `execute_probe` (one POST expecting 404/405). Both are read-only in effect. Neither can touch a
    write-safety constant — the module has no code path that writes one, asserted by this plan's
    grep criterion.
  </what-built>
  <how-to-verify>
    Agent tooling in this repo is blocked from performing arming writes, and `roundtrip` performs a
    real PUT — so a human runs both, from a shell with the n8n credentials loaded.

    1. Pick the lowest-risk target. `LV Scheduled Maintenance (Cloud)` is the recommended
       `roundtrip` target: its `settings` object is `{}` on inspection of the committed JSON, so if
       any workflow round-trips cleanly it is this one. Get its live id from
       `GET /api/v1/workflows`.
    2. Run the probe's `roundtrip` subcommand with the probe's enabling environment variable set.
       Expected: a `verified` verdict, and `settings` and `connections` identical before and after.
       A schema rejection naming additional properties means the four-key filter is wrong and 28-01
       needs a fix before anything else in this phase proceeds. A `settings` mismatch means research
       Open Question 3's community report applies to this instance — record exactly which nested key
       changed.
    3. Run the probe's `execute_probe` subcommand against the same id. Expected: 404 or 405,
       confirming D-05a. Record the actual status code and response body verbatim.
    4. Confirm the workflow's `active` state is unchanged from before step 2 — `roundtrip` restores
       the prior active state and this is the cheapest place to see that working for real.
  </how-to-verify>
  <resume-signal>Type "approved" with the roundtrip verdict, the settings/connections comparison result, and the execute-endpoint status code — or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live gate — is the deactivate→PUT→activate bracket actually effective? (D-18 / A1)</name>
  <precondition>The `roundtrip` probe returned `verified` in the previous task — a PUT that cannot round-trip cleanly makes the reload observation meaningless.</precondition>
  <what-built>
    `probe_n8n_semantics.py cadence_reload` — the phase's first genuine CONTROL-03 mutation,
    performed early and observed rather than assumed. It changes ONE Schedule Trigger node's
    interval through the allowlist-diffed pipeline, watches the executions API to see whether the
    running instance actually retimed, then restores the captured interval and read-back verifies
    the restore.

    This is deliberately not the cadence-as-one-shot-fire workaround D-05c rejected. Nothing is
    being made to fire in place of a manual trigger, and it is a one-time human-supervised
    diagnostic, never an operator-facing verb. The phase has to perform a live cadence change to
    satisfy CONTROL-03 regardless; this does it early, where the answer is still cheap.
  </what-built>
  <how-to-verify>
    1. Confirm `LV Scheduled Maintenance (Cloud)` is currently active. If it is inactive, the
       reload question does not apply to it — activation is itself the load event by n8n's own
       documented model — and that is the finding to record; skip to step 5 and note the question
       as unanswerable on this instance rather than answered.
    2. Choose the target trigger. `Review Trigger (15 min)` is recommended: its downstream write
       gate is disarmed, so extra fires perform reads only, and its committed interval is
       `minutes`/15 so a short-interval change is a small delta rather than a large one. Confirm
       the choice against current provider-credit headroom before running.
    3. Run `cadence_reload` with that workflow id and node name. Watch the reported execution start
       times for the target workflow over the bounded polling window.
    4. Read the result. Executions appearing at roughly the new spacing proves the bracket makes a
       Schedule Trigger change effective on a running instance — research A1 answered, and the
       arming lifecycle in 28-03 can rely on it. Executions continuing at the old spacing for the
       whole window means the bracket is insufficient and 28-03 needs a different mechanism, which
       is exactly what this probe exists to find out before an armed window depends on it.
    5. Confirm the restore. The probe reports a separate verdict for putting the captured interval
       back; that verdict must read `verified`. If it does not, restore by hand from the committed
       JSON's interval for that node and say so plainly — a probe that leaves a schedule changed is
       the failure mode D-05c warned about, and it is not to be quietly tidied.
    6. Write `28-FINDINGS.md` recording, for each of the three questions: what was run, what was
       observed verbatim, and the resulting confidence. State plainly where an answer is partial.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed execution spacing, the restore verdict, and confirmation that 28-FINDINGS.md is written — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe module → live n8n Cloud | The probe performs real PUTs against production workflows |
| probe module → write-safety constants | A boundary the probe must be unable to cross at all |
| cadence change window → provider credit burn | A schedule left short-intervalled burns credits silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-07 | Elevation of Privilege | `probe_n8n_semantics.py` | critical | mitigate | The module has no write-safety-writing code path; asserted by a grep in this plan's acceptance criteria, and both live steps are `blocking-human` |
| T-28-08 | Denial of Service (self-inflicted, cost) | cadence reload window | high | mitigate | Bounded polling window, a read-only downstream trigger chosen deliberately, a separately-reported restore verdict, and a hand-restore instruction if the restore fails |
| T-28-09 | Tampering | wrong n8n instance | high | mitigate | The probe reimplements the deploy script's `N8N_EXPECTED_URL` / `.n8n.cloud` instance guard and refuses before any call |
| T-28-10 | Repudiation | undocumented probe outcome | medium | mitigate | `28-FINDINGS.md` records what was run and what was observed verbatim, so later plans cite an observation rather than an inference |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `28-FINDINGS.md` exists and answers all three of 28-RESEARCH.md's Open Questions, each marked
  answered, partially answered, or unanswerable-on-this-instance.
- `git diff --stat n8n/` is empty — the probe touches live workflows, never committed artifacts.
</verification>

<success_criteria>
- The reload question is settled by observation before the arming lifecycle depends on it.
- The no-op round-trip is confirmed clean, or its failure mode is documented before any real
  mutation is designed around it.
- D-05a's amendment is confirmed against this instance rather than against an upstream PR's state.
</success_criteria>

## Artifacts this phase produces

This plan's share:
- `operator-claude-plugin/scripts/probe_n8n_semantics.py` — subcommands `roundtrip`,
  `execute_probe`, `cadence_reload`
- `operator-claude-plugin/tests/test_control_probe.py`
- `.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-FINDINGS.md` — the three
  observed facts, cited by 28-03 and 28-04

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-02-SUMMARY.md` when done
</output>
