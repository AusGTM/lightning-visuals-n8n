---
phase: 28-control-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/n8n_control.py
  - operator-claude-plugin/scripts/config_gate.py
  - operator-claude-plugin/tests/test_control_pipeline.py
  - operator-claude-plugin/tests/test_control_allowlist_diff.py
  - operator-claude-plugin/tests/test_control_verify_reporting.py
  - operator-claude-plugin/tests/test_config_gate.py
  - operator-claude-plugin/tests/test_transport_guard.py
autonomous: true
requirements: [CONTROL-02, CONTROL-05, CONTROL-06, CONTROL-07]

must_haves:
  truths:
    - "Turning a workflow on or off goes through one pipeline: capture prior state, mutate, re-read the backend independently, report verified or failed with the reversal stated (CONTROL-02, CONTROL-06, CONTROL-07, D-11)."
    - "A 200 from n8n is never reported as success on its own — the verdict comes from a separate GET whose value is compared against what was asked for (D-14)."
    - "A read-back that still shows the OLD value reports FAILED, not verified — the stale-instance case cannot pass as success (D-17)."
    - "A mutation body that differs from the freshly-fetched original anywhere outside the named allowlisted node(s) is refused before any network call is made (D-15, D-19)."
    - "The deactivate→PUT→activate bracket restores the workflow's PRIOR active state — a workflow that was off before the mutation is off after it."
    - "No plugin test can reach the network by any requests verb, PUT included."
    - "Every reader this phase needs already exists in n8n_read.py — n8n_control.py adds no fetch of its own and defines no second declaration regex."
    - "The `transport` parameter defaults to the bare `requests` module, never `requests.post`/`requests.put`, so test_retry_reuses_dispatch.py's send-shaped-function guard stays true with `_EXPECTED_SEND_SHAPED` unchanged."
  artifacts:
    - operator-claude-plugin/scripts/n8n_control.py
    - operator-claude-plugin/tests/test_control_pipeline.py
    - operator-claude-plugin/tests/test_control_allowlist_diff.py
    - operator-claude-plugin/tests/test_control_verify_reporting.py
  key_links:
    - "test_retry_reuses_dispatch.py rglobs EVERY operator-claude-plugin/scripts/*.py, including modules this phase has not written yet, and allowlists exactly two send-shaped functions. Phase 28 introduces PUT across four new modules; all four must take `transport=requests` (the module) and call `transport.put(...)`. Widening `_EXPECTED_SEND_SHAPED` to accommodate them is a WEAKENING and is forbidden by this phase."
    - "The PUT body filter must be exactly the four keys scripts/deploy_n8n_workflows.py::_update_workflow_live() already filters to (name, nodes, connections, settings) — n8n rejects any other key outright (D-16). PLUGIN-04 forbids importing it, so the tuple is copied and pinned by a parity test that reads the deploy script as TEXT."
    - "assert_only_allowlisted_change must run BEFORE the deactivate call, not just before the PUT — a refusal that has already deactivated a live workflow is a failed mutation dressed as a refusal."
---

<objective>
The tracer for this phase: one mutation pipeline, wired end to end, proven on the thinnest real
capability the phase ships — turning a workflow on or off (CONTROL-02). Every later control action
in Phase 28 is a different payload through this same pipeline.

Purpose: this phase is the only one in the milestone that mutates the backend. Getting the
pipeline's shape wrong — trusting a 200, deactivating before validating, blindly re-activating a
workflow that was deliberately off — is the class of bug that is discovered live, in production,
after ten layers are already committed. Proving the whole path on a no-JSON-write mutation first
converts that risk into one commit.

Output: `operator-claude-plugin/scripts/n8n_control.py` — fetch, four-key PUT body filter, the
structural allowlist diff, the deactivate→PUT→restore-prior-active bracket, the independent
read-back, and one `MutationResult` shape that carries verified/failed plus the reversal statement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-RESEARCH.md
@.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-VALIDATION.md
@operator-claude-plugin/tests/conftest.py
@operator-claude-plugin/scripts/n8n_read.py
@operator-claude-plugin/scripts/config_gate.py
@operator-claude-plugin/tests/test_retry_reuses_dispatch.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "turn this workflow on/off" — one path, wired through every layer</name>
  <files>operator-claude-plugin/scripts/n8n_control.py, operator-claude-plugin/tests/test_control_pipeline.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/n8n_read.py` lines 50–91 — `_base_url()`, `_headers()`,
      `_get_json()`, `get_workflow()`. **`get_workflow(config, workflow_id, transport)` already
      does the fetch this task needs**: same endpoint, same `X-N8N-API-KEY` header, config-based,
      injectable transport, `None` on every failure mode. Import it; do not add a `fetch_workflow`.
      Note the argument order is `(config, workflow_id, ...)`.
    - `operator-claude-plugin/tests/test_retry_reuses_dispatch.py` lines 100–110, 129–146, 192–210 —
      **the structural guard this plan must not trip.** `_SEND_CALL_ATTRS = {"post", "put"}`;
      `_plugin_source_files()` `rglob`s every `scripts/*.py`, including modules this phase has not
      written yet; `_has_send_shaped_transport_default` flags any function whose `transport`
      parameter defaults to `requests.post`/`requests.put`; `_calls_requests_send_verb_directly`
      flags a literal `requests.put(...)` call. `_EXPECTED_SEND_SHAPED` allowlists exactly
      `backend_status.py::fetch_backend_status` and `dispatch.py::dispatch`.
    - `scripts/deploy_n8n_workflows.py` lines 182–201 — `_base_url()`, `_n8n_headers()`,
      `_get_live_workflows()`. The backend-side convention for the same endpoint, for reference
      only; PLUGIN-04 forbids importing it.
    - `operator-claude-plugin/scripts/config_gate.py` lines 28–31 — `CAPABILITY_KEYS`. Phase 27
      established per-capability gating (`require_capability(cfg, "status")`); this phase adds the
      control capability's row rather than reintroducing a global gate.
    - `operator-claude-plugin/tests/conftest.py` — the `stub_transport` recorder and the autouse
      `no_network` guard. Every network-touching function in this phase takes an injectable
      transport so tests can drive it without the guard biting.
    - `28-CONTEXT.md` D-11, D-12, D-13, D-14 — prior-state capture, the reversal statement, and
      "a 200 is never success on its own".
    - `28-RESEARCH.md` §"Pattern 4" and §"Pattern 5" — activate/deactivate take no body and echo the
      workflow; that echo is the mutation's own voice and must not be trusted as the read-back.
  </read_first>
  <action>
    Create `operator-claude-plugin/scripts/n8n_control.py`.

    **The transport rule, stated exactly because getting it wrong trips a guard silently.** Every
    function here that touches the network takes an explicit `transport` parameter that defaults to
    the BARE `requests` MODULE — `transport=requests` — never to `requests.post` or `requests.put`,
    and every call goes through `transport.post(...)` / `transport.put(...)`. This matches
    `n8n_read.py`'s injectable-read seam, NOT `dispatch.py`'s send seam: `dispatch.py:26` is
    `transport=requests.post`, and `test_retry_reuses_dispatch.py` treats that exact shape as the
    signature of a record-sending function. A `transport=requests.put` default, or a literal
    `requests.put(...)` call, makes `n8n_control.py` a fourth entry in a list that allowlists two —
    and the obvious remedy, appending to `_EXPECTED_SEND_SHAPED`, would weaken the one guard
    standing between a retry and the arming gate. Do not weaken it; take the module-level default.

    Do not write a `fetch_workflow`. `n8n_read.get_workflow(config, workflow_id, transport)` shipped
    in 27-01 and is the same GET against the same endpoint with the same header, already injectable
    and already returning `None` for every failure mode there is. Import `n8n_read` and call it. Its
    `None`-on-failure contract feeds the verdict rule below directly: an unreadable read-back is not
    a verified one.

    Provide `set_active(workflow_id, desired_active, config, transport)` calling
    `POST /api/v1/workflows/{id}/activate` or `/deactivate` — no request body — then performing a
    SEPARATE `n8n_read.get_workflow` and deciding the verdict from that fetched object's `active`
    field, never from the activate/deactivate response.

    Add the control capability to the config gate rather than inventing a second credential path:
    add `"control": ("n8n_url", "n8n_api_key")` to `config_gate.CAPABILITY_KEYS` and a matching
    `_CAPABILITY_DESCRIPTIONS` entry. Control needs the same two keys the status capability needs
    but is a distinct capability, so a plugin that can read but must not mutate is expressible, and
    27-03's "add a row, do not re-add a global gate" instruction is followed literally. 28-05 calls
    `require_capability(cfg, "control")` at the surface entry point; this task only adds the row.

    Define one result type, `MutationResult`, carrying: the action performed, the prior value read
    before the mutation, the requested value, the observed value from the independent read-back, a
    verdict of `verified` or `failed`, a plain-language `reversal` sentence built from the prior
    value per D-11 ("it was off; to undo, I'll turn it back off"), and on failure a `detail` naming
    what was observed versus what was asked for. Nothing in this module composes operator prose
    beyond the reversal sentence — the consequence statement and confirmation are 28-05's job.

    The verdict rule, which is the whole point of the type: `verified` requires the independently
    fetched value to equal the requested value. A non-2xx at any step, a fetch that raises, a fetch
    returning `None`, or a fetched value that still equals the prior value all produce `failed`.
    There is no code path in which a status code alone yields `verified` (D-14).

    Capture the prior value with a fetch BEFORE the mutation. That fetch is required anyway for the
    read-back comparison, so the reversibility statement costs nothing extra (D-12).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_pipeline.py -q` exits 0.
    - A test drives `set_active` to turn an inactive workflow on against a recording transport and asserts the recorded call sequence is: GET, POST to the activate path, GET — three calls, the last one a GET, and no request body on the POST.
    - A test where the post-mutation GET returns `active` still false asserts the returned verdict is `failed`, not `verified`, even though every stubbed status code was 200.
    - A test asserts `MutationResult.reversal` for a false→true transition names turning it back off, and quotes the prior value rather than a generic phrase.
    - A test asserts a non-2xx on the activate call yields verdict `failed` and that `detail` is non-empty.
    - A test asserts a read-back returning `None` (`n8n_read.get_workflow`'s unreadable contract) yields `failed`, not `verified`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_retry_reuses_dispatch.py -q` exits 0, and `_EXPECTED_SEND_SHAPED` in that file is unchanged by this phase — verified with `git diff operator-claude-plugin/tests/test_retry_reuses_dispatch.py`, which must be empty.
    - `grep -c "def read_write_safety\|def fetch_workflow" operator-claude-plugin/scripts/n8n_control.py` returns 0 — both readers come from `n8n_read`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_config_gate.py -q` exits 0 with the new `control` capability row present.
  </acceptance_criteria>
  <done>Turning a workflow on or off works end to end through one pipeline, the verdict comes from an independent read, and the reversal is stated from the captured prior value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The PUT half — four-key body filter, structural allowlist diff, prior-active-restoring bracket</name>
  <files>operator-claude-plugin/scripts/n8n_control.py, operator-claude-plugin/tests/test_control_allowlist_diff.py</files>
  <read_first>
    - `scripts/deploy_n8n_workflows.py` lines 474–488 — `_create_workflow_live()` /
      `_update_workflow_live()`. The four-key filter tuple is live-tested against this exact n8n
      Cloud instance; copy it verbatim (D-16). Note `_update_workflow_live` itself calls
      `requests.put` directly — that is the backend script's own convention and must NOT be copied
      into the plugin, where it would trip the send-shaped-function guard.
    - `28-RESEARCH.md` §"Pattern 1", §"Pattern 2", §"Pitfall 3" — n8n rejects a body carrying any
      other top-level key with a schema error, and the structural diff is what makes an
      out-of-allowlist change impossible rather than merely unattempted (D-15, D-19).
    - `28-CONTEXT.md` D-18 — every mutating PUT is bracketed deactivate → PUT → activate.
  </read_first>
  <behavior>
    - `put_body` on a full GET response returns exactly four keys and drops `id`, `active`, `tags`,
      `createdAt`, `updatedAt`, `staticData`, `versionId`, `pinData`, `meta`.
    - `assert_only_allowlisted_change` passes when only nodes named in the allowed set differ.
    - It refuses when a node outside the allowed set differs in any field.
    - It refuses when a node is added or removed (the node-name set itself changed).
    - It refuses when `connections` differ at all.
    - It refuses when `settings` differ at all.
    - It refuses when the allowed set names a node that does not exist in the fetched original.
    - `apply_mutation` on a refusal makes ZERO network calls — the recording transport's call log is
      empty, so nothing was deactivated.
    - `apply_mutation` on a workflow whose prior `active` was false does NOT leave it active: the
      bracket restores the prior state, it does not blindly activate.
  </behavior>
  <action>
    Add `put_body(workflow)` to `n8n_control.py`, filtering to the four keys the deploy script
    already filters to. Copy the tuple verbatim from `_update_workflow_live()` with a source comment
    naming that function and the line range — PLUGIN-04 forbids importing across the client/backend
    boundary, so parity is held by the pin test in Task 3, not by an import.

    Add `assert_only_allowlisted_change(original, modified, allowed_node_names)`. Compare the two
    node lists by name. Refuse if the name sets differ at all, refuse if any node whose name is not
    in the allowed set serializes differently under a key-sorted dump, refuse if `connections`
    differ, refuse if `settings` differ, and refuse if a name in the allowed set is absent from the
    original. Each refusal raises with a message naming the specific node or top-level key that
    differed, so a failure reads as an accusation rather than a puzzle. Refusing on an allowed name
    that is not present matters because a typo in a node name would otherwise silently produce a
    PUT that changes nothing while reporting a successful mutation.

    Add `apply_mutation(workflow_id, mutate_fn, allowed_node_names, config, transport)`. Sequence:
    fetch fresh via `n8n_read.get_workflow`, refusing on a `None` return rather than proceeding with
    an empty body (never reuse a workflow object fetched earlier in the session — a stale copy is the
    self-inflicted tampering path in the threat register); deep-copy; call `mutate_fn` on the copy;
    run `assert_only_allowlisted_change` — a refusal here happens BEFORE any deactivate, so a
    rejected mutation leaves the backend untouched; then deactivate, PUT the filtered body,
    and restore the PRIOR active state rather than unconditionally activating; then fetch again
    independently and hand that fetched object to the caller's verdict check.

    The prior-active restoration is not a detail. A workflow an operator deliberately left off must
    not come back on because the plugin re-timed a schedule inside it. Where the prior state was
    active, the reactivation is also what forces the running instance to reload the changed content
    (D-18); where it was inactive there is no running instance to reload and nothing to force.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_allowlist_diff.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_allowlist_diff.py -q` exits 0 with at least 9 tests collected.
    - `python3 -c "import sys;sys.path.insert(0,'operator-claude-plugin/scripts');import n8n_control,json;wf=json.load(open('n8n/wf_enrichment_cloud.json'));wf['id']='x';wf['active']=True;assert set(n8n_control.put_body(wf))=={'name','nodes','connections','settings'}"` exits 0.
    - A test mutates a node NOT in the allowed set and asserts the raised message names that node.
    - A test asserts that after a refusal the recording transport's call log has length 0.
    - A test runs `apply_mutation` against a fetched workflow whose `active` is false and asserts the recorded call sequence contains no activate call.
  </acceptance_criteria>
  <done>A change outside the allowlist cannot reach the network, and a bracketed PUT never turns on a workflow that was off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verified-vs-persisted reporting, four-key parity pin, and a network guard that also blocks PUT</name>
  <files>operator-claude-plugin/tests/test_control_verify_reporting.py, operator-claude-plugin/tests/test_transport_guard.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_transport_guard.py` — the existing guard test from 23-03.
      It proves `requests.post` raises inside a test; this phase introduces PUT and GET, so the
      guard's coverage of those verbs needs to be asserted rather than assumed.
    - `28-RESEARCH.md` §"Pitfall 1" — a GET-after-PUT that only checks for a 200 reports "verified"
      in both the good case and the stuck-stale case, which is the failure this file exists to
      prevent.
  </read_first>
  <behavior>
    - A stubbed sequence where every status code is 200 but the final GET still reads the prior
      value yields verdict `failed`, and `detail` names the observed value and the requested value.
    - A stubbed non-2xx on the PUT yields `failed` and no read-back verdict is fabricated.
    - A stubbed transport that raises a connection error mid-sequence yields `failed`, not an
      unhandled traceback reaching the operator.
    - No input to the reporting layer produces `verified` without an independently fetched value
      matching the requested one.
    - The four-key filter tuple in `n8n_control.py` matches the tuple in
      `scripts/deploy_n8n_workflows.py::_update_workflow_live`, compared by reading the deploy
      script as text — not by importing it.
    - `requests.put` and `requests.get` called inside a test both raise, the same as `requests.post`.
  </behavior>
  <action>
    Write `operator-claude-plugin/tests/test_control_verify_reporting.py` covering the stale
    read-back, the non-2xx, and the raising-transport cases against `apply_mutation` and
    `set_active`. Include one test that enumerates every verdict-producing path in the module and
    asserts none returns `verified` where the fetched value differs from the requested one — this
    is the test that would catch a future refactor quietly reintroducing status-code optimism.

    Add the parity pin to the same file: read `scripts/deploy_n8n_workflows.py` with `Path.read_text`,
    extract the key tuple from `_update_workflow_live`'s payload comprehension with a regex, and
    assert it equals the set `n8n_control.put_body` filters to. Reading the file as text rather than
    importing it keeps `test_no_backend_imports.py`'s boundary intact while still failing loudly if
    an admin widens or narrows the deploy script's filter without the plugin following.

    Append two assertions to `operator-claude-plugin/tests/test_transport_guard.py` proving
    `requests.put` and `requests.get` raise inside a test. Both route through the already-patched
    `Session.request`, so this is a coverage assertion rather than a new guard — but this phase is
    the first that can PUT to a live workflow, so the coverage needs to be stated rather than
    inferred from an implementation detail of the requests package.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_verify_reporting.py operator-claude-plugin/tests/test_transport_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_verify_reporting.py operator-claude-plugin/tests/test_transport_guard.py -q` exits 0.
    - A test named for the stale read-back case exists and asserts verdict `failed` against an all-200 stub sequence.
    - The parity test fails if the deploy script's filter tuple is edited — assert this by monkeypatching the read text to a four-key-minus-one variant and confirming the assertion raises.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` (whole plugin suite) exits 0.
  </acceptance_criteria>
  <done>The reporting layer fails closed on the stale-instance case, the PUT body filter cannot silently desync from the deploy script, and no plugin test can PUT to anything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → n8n Public API (mutating verbs) | The first boundary in this milestone that carries a write; anything crossing it can change production behavior |
| plugin test suite → n8n Cloud | A test that reaches the network can perform a real, unapproved workflow mutation |
| fetched workflow object → outgoing PUT body | n8n replaces `nodes`/`connections`/`settings` wholesale, so anything that drifts between fetch and PUT lands in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | `apply_mutation` outgoing body | critical | mitigate | `assert_only_allowlisted_change` runs before any network call; a diff outside the named nodes raises and the transport call log stays empty (Task 2) |
| T-28-02 | Repudiation | `MutationResult` verdict | high | mitigate | `verified` requires an independently fetched value equal to the requested one; the all-200-but-stale case is a named test (Task 1, Task 3) |
| T-28-03 | Elevation of Privilege | deactivate→PUT→activate bracket | high | mitigate | The bracket restores the PRIOR active state; a workflow that was off is not turned on by a mutation to its content (Task 2) |
| T-28-04 | Tampering | plugin test suite | high | mitigate | The autouse `no_network` guard from 23-03 covers every requests verb; Task 3 asserts PUT and GET explicitly |
| T-28-05 | Tampering (concurrent, non-adversarial) | a concurrent admin `deploy_n8n_workflows.py` run racing a plugin PUT | medium | accept | n8n exposes no optimistic-concurrency field on this endpoint (28-RESEARCH.md §Summary). Mitigation is procedural: always fetch fresh immediately before mutating, keep the window minimal, and report a mismatched read-back loudly |
| T-28-06 | Tampering | stale in-session workflow copy reused for a later mutation | medium | mitigate | `apply_mutation` always fetches fresh; no cached workflow object is accepted as an argument |
| T-28-30 | Elevation of Privilege | `_EXPECTED_SEND_SHAPED` widened to accommodate this phase's PUT modules | high | mitigate | All four new modules take `transport=requests` (the bare module) and call `transport.put(...)`, so the guard is satisfied rather than edited; `git diff --stat` on `test_retry_reuses_dispatch.py` must be empty. Widening that allowlist would let a future retry path bypass `dispatch()`'s no-default `armed` gate |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `.venv/bin/python -m pytest -q` (full repo suite) green — no basename collision, no backend import.
- Every module this PHASE adds goes through its injected transport, not `requests` directly. The
  grep must cover all four, because 28-02/03/04's modules are otherwise uncovered and they are the
  ones `test_retry_reuses_dispatch.py` will `rglob` next:
  `for f in n8n_control n8n_arming n8n_cadence probe_n8n_semantics; do p=operator-claude-plugin/scripts/$f.py; [ -f "$p" ] && grep -nE 'requests\.(put|post|get)\(' "$p"; done` returns nothing.
  (Later plans re-run this once their module exists; 28-01 runs it for the file it creates.)
- The default is the bare module, checked on code lines only so an explanatory comment naming the
  forbidden shape cannot fail its own gate:
  `grep -vE '^\s*#' operator-claude-plugin/scripts/n8n_control.py | grep -cE 'transport\s*=\s*requests\.(post|put)'`
  returns 0.
- `git diff --stat operator-claude-plugin/tests/test_retry_reuses_dispatch.py` is empty: the guard
  was satisfied, not edited.
</verification>

<success_criteria>
- Turning a workflow on or off is a real, working capability end to end (CONTROL-02).
- The verdict machinery every later plan depends on exists and fails closed.
- An out-of-allowlist change is structurally unreachable, not merely unattempted.
</success_criteria>

## Artifacts this phase produces

This plan's share:
- `operator-claude-plugin/scripts/n8n_control.py` — `put_body`, `set_active`,
  `assert_only_allowlisted_change`, `apply_mutation`, `MutationResult`. Fetching is
  `n8n_read.get_workflow` (27-01), imported.
- `operator-claude-plugin/scripts/config_gate.py` — one new `CAPABILITY_KEYS` row, `"control"`
- `operator-claude-plugin/tests/test_control_pipeline.py`
- `operator-claude-plugin/tests/test_control_allowlist_diff.py`
- `operator-claude-plugin/tests/test_control_verify_reporting.py`
- `operator-claude-plugin/tests/test_transport_guard.py` — extended with PUT/GET coverage

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/28-control-actions/28-01-SUMMARY.md` when done
</output>
