---
phase: 35-url-structured-fallback
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - .planning/workstreams/plugin-entrypoint/STATE.md
  - .planning/workstreams/plugin-entrypoint/ROADMAP.md
autonomous: false
requirements: [INGEST-05, INGEST-06]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: >-
        https://gctc.com.au/board-of-directors/ yields all 9 of its directors — Brett Cook,
        Trent Watson, Jarrad Young, Peter Ward, Luke Henderson, Greg Leeson, Royce Ahrens,
        Tara Hastings and Sandy Cowell — end to end through the operator-facing path, with the
        candidate URLs shown before any was fetched and provenance naming the wp-json URL.
      verification: backstop
    - "The four suite baselines are met or beaten: plugin 1022/5 plus this phase's delta, python 1903/6 plus the same delta, node 553, arming grep 0."
    - "plugin.json's version reads 0.10.0 and was bumped in the SAME commit as the CHANGELOG section cut."
    - "master carries the release commit, and the marketplace clone has been fetched and reset to it, so an installed operator is offered the update."
  artifacts:
    - operator-claude-plugin/CHANGELOG.md
    - operator-claude-plugin/.claude-plugin/plugin.json
  key_links:
    - "Master is the branch the marketplace clone reads. 0.9.0 shipped with a correct bump sitting on a feature branch and the Update button stayed grey until master had it."
    - "The walk exercises extraction.md's instructions with web_fetch — the model-invoked server tool — which is the only place in this phase a fetch happens at all."
  prohibitions:
    - "No scraping library, headless browser, user-agent or viewport control is introduced during the walk — the walk uses web_fetch and nothing else."
    - "No candidate URL is fetched off gctc.com.au during the walk."
    - "The ladder is not run on a tool error during the walk."
    - "No value is inferred from a slug or a URL — a director with no email in the wp-json response is recorded with no email."
    - "No `git commit -a`. Every commit names explicit paths."
    - "Nothing under ~/.claude/plugins/ is touched except the marketplace clone fetch/reset in Task 3, which is the documented release step."
---

<objective>
Walk the acceptance case live, then release it.

Purpose: Success Criterion 1 says the gctc board page must yield its 9 directors "walked live, not
merely unit-tested". Every test in 35-01 and 35-02 pins a bound; none of them proves the ladder
actually recovers the roster, because the fetch is a server tool no test may invoke. The walk is the
only evidence for the criterion, and it comes before the release deliberately — shipping first and
walking after means a failed walk is a second release.

Output: a recorded live walk, plugin `0.10.0` on master, and a refreshed marketplace clone.
</objective>

<artifacts_this_phase_produces>
| Artifact | Produced by | Purpose |
|---|---|---|
| `operator-claude-plugin/scripts/url_fallback.py` | 35-01 | Deterministic candidate-URL ladder, same-host check, fetch cap, give-up message |
| `operator-claude-plugin/tests/test_url_fallback.py` | 35-01, 35-02 | Pins the ladder order, the cap, the same-host rule, and the module's I/O-free import set |
| `operator-claude-plugin/skills/contact-upload/extraction.md` (URL adapter) | 35-01, 35-02 | The instruction half: when to escalate, what the operator sees first, what provenance records |
| `operator-claude-plugin/tests/test_extraction_contract.py` (additions) | 35-02 | Pins the instruction half against the module and against the removed verdict phrasing |
| `operator-claude-plugin/CHANGELOG.md` + `.claude-plugin/plugin.json` @ `0.10.0` | 35-03 | The release that reaches an installed operator |
| The recorded gctc walk (in `35-03-SUMMARY.md`) | 35-03 | The only evidence for Success Criterion 1 |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/35-url-structured-fallback/35-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/35-url-structured-fallback/35-01-SUMMARY.md
@.planning/workstreams/plugin-entrypoint/phases/35-url-structured-fallback/35-02-SUMMARY.md
@operator-claude-plugin/skills/contact-upload/extraction.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk the acceptance case live against gctc.com.au</name>
  <read_first>
    - `operator-claude-plugin/skills/contact-upload/extraction.md` — the rewritten
      `## Adapter: a public URL (INGEST-05)` section, as an operator's Claude would read it. Follow
      it as written; do not improvise past it. Where it is unclear, that is a finding.
    - `35-CONTEXT.md` §"The acceptance case, measured live 2026-08-05" — the 9 names, the role
      strings, and the fact that the response carries no emails and no phones.
  </read_first>
  <precondition>
    `web_fetch` is available in the session performing this walk, and `gctc.com.au` is reachable. If
    the tool returns a tool-level error on the pasted URL, this walk cannot proceed — and per
    criterion 2 the ladder must NOT be run in that case; halt and report the error instead.
  </precondition>
  <what-built>
    The escalation ladder: `scripts/url_fallback.py` builds the candidate URLs, and
    `extraction.md`'s URL adapter tells Claude to show them to the operator before fetching, fetch
    only what is approved, stop at the first that yields people, and record provenance against the
    URL that actually served the row.
  </what-built>
  <how-to-verify>
    Run the walk exactly as an operator would, in a session that has the plugin's skill files:

    1. Say: **"Load contacts from https://gctc.com.au/board-of-directors/"**
    2. Expect: the page is fetched with `web_fetch` and yields nothing usable — measured 2026-08-05,
       the HTML truncates immediately after `<title>` and contains 0 people.
    3. Expect: **no same-URL retry is offered.** The tool caches per URL for about 15 minutes, so a
       retry with a tighter prompt reads byte-identical content. If a retry is suggested, that is a
       FAIL against 35-CONTEXT.md §2 detail 2.
    4. Expect: **no verdict about why the page was empty** — no claim about JavaScript rendering. The
       walk that produced this phase repeated exactly such a claim to the operator and it was wrong.
    5. Expect: the candidate URLs are printed **before any of them is fetched**, in the locked order,
       with the fetch cap named. The first must be
       `https://gctc.com.au/wp-json/wp/v2/pages?slug=board-of-directors`.
    6. Approve the candidates. Expect the first one to return the roster and the ladder to stop there
       — not to keep walking rungs it no longer needs.
    7. Expect **9 rows**, naming: Brett Cook (Chairman), Trent Watson (Deputy Chairman, Finance),
       Jarrad Young, Peter Ward, Luke Henderson, Greg Leeson, Royce Ahrens, Tara Hastings,
       Sandy Cowell.
    8. Expect **no emails and no phones** on any row — the wp-json response carries none, and a row
       carrying one would be invention (STRUCT-04). Rows failing the identity rule are expected to be
       reported as rejected with their reason; that is the correct outcome, not a defect.
    9. Expect provenance on every row to name
       `https://gctc.com.au/wp-json/wp/v2/pages?slug=board-of-directors`, not
       `https://gctc.com.au/board-of-directors/`.

    Then walk the negative case in the same session:

    10. Paste a URL that returns a tool-level error (an `url_not_allowed` case, or any host the tool
        declines). Expect the existing named refusal and **no candidate list, no ladder** — the fence
        stays a fence.

    Record every observation in `35-03-SUMMARY.md`, including anything the adapter's wording made
    ambiguous. A wording finding here is worth more than a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - [ ] All 9 directors appear, by name, in the extracted rows.
    - [ ] The candidate list was shown before any candidate was fetched, first candidate exactly
          `https://gctc.com.au/wp-json/wp/v2/pages?slug=board-of-directors`, cap named.
    - [ ] No same-URL retry was offered at any point.
    - [ ] No claim about how the page renders was made to the operator.
    - [ ] Provenance on every row names the wp-json URL, not the pasted page URL.
    - [ ] No row carries an email or a phone.
    - [ ] The tool-error case produced the named refusal and no ladder.
    - [ ] The observations, including the exact wording the walk produced, are written into
          `35-03-SUMMARY.md` before Task 2 begins.
  </acceptance_criteria>
  <resume-signal>Type "walked" plus the observed row count, or describe what diverged.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Suites, CHANGELOG cut and version bump — one commit</name>
  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` — the `## [Unreleased]` section and the four-step
      `## Release checklist — how a change reaches an installed operator` at the bottom.
    - `operator-claude-plugin/.claude-plugin/plugin.json` — currently `0.9.0`.
    - `.planning/workstreams/plugin-entrypoint/STATE.md` — the current-position block and the
      accepted-amendments table, to see the register the update is written in.
  </read_first>
  <files>
    operator-claude-plugin/CHANGELOG.md,
    operator-claude-plugin/.claude-plugin/plugin.json,
    .planning/workstreams/plugin-entrypoint/STATE.md,
    .planning/workstreams/plugin-entrypoint/ROADMAP.md
  </files>
  <reversibility rating="costly">
    A released version reaches installed operators; correcting it costs another full release cycle
    (bump, cut, push, master, clone refresh). Not one-way — nothing is deleted and the previous
    version remains installable — so no decision checkpoint is inserted, but the four suites and the
    Task 1 walk both gate it.
  </reversibility>
  <action>
    Run all four gates first and record the exact numbers:

    ```
    .venv/bin/python -m pytest operator-claude-plugin/tests/ -q
    .venv/bin/python -m pytest -q
    node --test tests/n8n/*.test.mjs
    grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json
    ```

    Against baselines: plugin 1022 passed / 5 skipped, python 1903 passed / 6 skipped, node 553 pass,
    arming grep 0. Plugin and python counts rise by this phase's new tests; skips, node and the
    arming grep must be unchanged. A changed node count or a non-zero arming grep means a backend
    file moved — this phase touches none, so investigate before releasing rather than explaining it
    away.

    Move the phase's work out of `## [Unreleased]` into `## [0.10.0] - 2026-08-05` (use the actual
    date). Write it for an operator, not for a reviewer: what changed is that a page whose HTML
    carries nothing usable is now retried against the structured representation the site itself
    publishes, same-host and capped, with the candidate URLs shown first — and that the plugin no
    longer tells them why a page was empty when it does not know. Record the gctc measurement as the
    case it was built from. Record under a Notes or Known-limitation heading what the ladder does not
    do: it is not a crawler, it does not leave the pasted host, and a site that publishes no
    structured representation still ends at the named give-up.

    Set `.claude-plugin/plugin.json`'s `version` to `0.10.0`. Minor, not patch: the URL adapter gains
    behaviour an operator can observe.

    Update `.planning/workstreams/plugin-entrypoint/STATE.md` — current position, the phase-35 note
    with the walked evidence from Task 1, and the suite numbers. Update
    `.planning/workstreams/plugin-entrypoint/ROADMAP.md`'s Phase 35 entry and the progress table.

    Commit with explicit paths. Never `git commit -a`:

    ```
    git commit operator-claude-plugin/CHANGELOG.md operator-claude-plugin/.claude-plugin/plugin.json \
      .planning/workstreams/plugin-entrypoint/STATE.md .planning/workstreams/plugin-entrypoint/ROADMAP.md \
      -m "release(plugin): 0.10.0 — URL structured-representation fallback"
    ```

    The version bump and the CHANGELOG cut must be in that one commit. `0.9.0`'s release notes say
    why: Claude Desktop compares only the version string, so a cut without a bump ships silently.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q && .venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs && grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json</automated>
    <automated>git show --stat HEAD | grep -E 'plugin.json|CHANGELOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Plugin suite passes with 5 skipped; python suite passes with 6 skipped; node reports 553
          pass; the arming grep prints 0.
    - [ ] `grep '"version"' operator-claude-plugin/.claude-plugin/plugin.json` shows `0.10.0`.
    - [ ] `git show --stat HEAD` lists BOTH `operator-claude-plugin/.claude-plugin/plugin.json` and
          `operator-claude-plugin/CHANGELOG.md` in the same commit.
    - [ ] `## [Unreleased]` remains at the top of the CHANGELOG and is empty.
    - [ ] The CHANGELOG entry states what the ladder does not do, not only what it does.
    - [ ] `git status --short operator-claude-plugin/scratch` is empty.
    - [ ] No commit in this task used `-a`.
  </acceptance_criteria>
  <done>
    All four gates recorded and green, `0.10.0` bumped and cut in one commit with explicit paths,
    STATE and ROADMAP updated with the walked evidence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push to master and refresh the marketplace clone</name>
  <read_first>
    - `operator-claude-plugin/CHANGELOG.md`'s release checklist, steps 3 and 4, verbatim — including
      the two `git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator` commands.
    - `35-CONTEXT.md` §5 non-negotiable 7 — why this step is a blocking human gate rather than an
      automated one.
  </read_first>
  <precondition>
    Task 2's release commit exists on the working branch, and the four gates were recorded green.
  </precondition>
  <what-built>
    Plugin `0.10.0` is committed on `feat/v0.6-plugin-entrypoint`. The marketplace clone reads
    `master` and never fetches on its own, so nothing has reached an installed operator yet.
  </what-built>
  <how-to-verify>
    1. Push the working branch.
    2. Get the release commit onto `master` — this is the step that failed for `0.9.0`, where a
       correct version bump sat on a feature branch and the Update button stayed grey. Master is the
       branch the marketplace reads.
    3. Refresh the marketplace clone (it never fetches on its own; a reinstall re-copies whatever the
       clone already holds):
       ```
       git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator fetch --depth=1 origin master
       git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator reset --hard FETCH_HEAD
       ```
    4. Confirm by CONTENT, not by version string: read
       `~/.claude/plugins/marketplaces/lightning-visuals-operator/operator-claude-plugin/skills/contact-upload/extraction.md`
       and check it names `url_fallback.py`. A version string is hand-written and can be right while
       the content is stale.
    5. Confirm the Update button is offered in Claude Desktop.
    6. Optional but recommended: re-walk Task 1's gctc case against the freshly installed `0.10.0` to
       confirm the shipped copy behaves as the branch copy did.
  </how-to-verify>
  <acceptance_criteria>
    - [ ] `git log origin/master --oneline -1` shows the `0.10.0` release commit.
    - [ ] `git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator log --oneline -1` matches
          that commit.
    - [ ] `grep -c 'url_fallback.py' ~/.claude/plugins/marketplaces/lightning-visuals-operator/operator-claude-plugin/skills/contact-upload/extraction.md`
          is greater than 0 — verified by content, not by the version string.
    - [ ] The operator confirms the Update button is offered.
    - [ ] Nothing else under `~/.claude/plugins/` was written to.
  </acceptance_criteria>
  <resume-signal>Type "released" once the Update button is offered, or describe what is stale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → installed operator | The marketplace clone is the only path from a commit to a running plugin, and it is manual on both ends (push to master, fetch the clone). A half-done release leaves the operator running old code while every doc says otherwise. |
| live web → the walk | Task 1 performs the phase's only real fetches, against a third-party host, via `web_fetch`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-11 | Repudiation | a release whose version bump and CHANGELOG cut are in different commits | high | mitigate | Single-commit requirement with an explicit-path `git commit`, verified by `git show --stat HEAD` naming both files (Task 2) |
| T-35-12 | Tampering | a stale marketplace clone serving old skill files under a new version string | high | mitigate | Task 3 verifies by CONTENT (`url_fallback.py` present in the clone's `extraction.md`), not by version string |
| T-35-13 | Information Disclosure | fetched gctc content carrying embedded instructions during the walk | medium | mitigate | `extraction.md`'s Trust note governs the walk; the walk reads content as data, and the same-host filter bounds any URL the content nominates |
| T-35-14 | Denial of Service | the walk running the ladder against a tool error and hammering a declining host | medium | mitigate | The `<precondition>` and step 10 both state the ladder does not run on a tool error; the cap bounds every other case |
| T-35-15 | Tampering | a release that touches files outside the four named paths | medium | mitigate | Explicit-path commit only; `git commit -a` forbidden in acceptance criteria (Task 2) |
| T-35-SC | Tampering | package-manager installs | n/a | accept | This plan installs no packages. The only third-party artifact touched is the marketplace clone, refreshed from this repo's own `master` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — 5 skipped, passes ≥ 1022.
- `.venv/bin/python -m pytest -q` — 6 skipped, passes ≥ 1903.
- `node --test tests/n8n/*.test.mjs` — 553 pass, unchanged.
- `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` — 0.
- The gctc walk, recorded in `35-03-SUMMARY.md` with the 9 names and the provenance URL observed.
- `git log origin/master --oneline -1` — the `0.10.0` release commit.
</verification>

<success_criteria>
- The acceptance case is walked live and produces 9 rows with wp-json provenance and no invented
  contact fields; the tool-error case still refuses without a ladder.
- All four suite gates recorded green against their baselines.
- `0.10.0` is bumped and cut in one explicit-path commit, is on master, and the marketplace clone
  has been verified by content.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/35-url-structured-fallback/35-03-SUMMARY.md`
when done. It carries the walk transcript observations — they are the only evidence for Success
Criterion 1.
</output>
