---
status: complete
kind: knowledge_base
---

# GSD Debug Knowledge Base

Resolved debug sessions. Used by `gsd-debugger` to surface known-pattern hypotheses at the start of new investigations.

---

## empty-evidence-by-field — closed-won smoke reported evidence_by_field empty for every company
- **Date:** 2026-07-21
- **Error patterns:** evidence_by_field, empty evidence, lv_produces_content, stop_reason, max_tokens, USE_MOCK_WEB_RESEARCH, setdefault, mock fixture, live smoke, identical verdicts, claude_web_research
- **Root cause:** PRIMARY: scripts/smoke_closed_won_research.py used `os.environ.setdefault("USE_MOCK_WEB_RESEARCH", "false")` to force live research mode, but the documented run command sources `.env` first (which sets `USE_MOCK_WEB_RESEARCH=true`), so the key was already present and `setdefault` silently no-op'd — every "live" run was actually replaying the static mock fixture (identical data for every company, no `evidence_by_field` key since the fixture predates that field). SECONDARY (real, independently confirmed): `max_tokens=2000` for the live Claude web-research call is insufficient once live calls do happen — claude-sonnet-5's extended thinking consumes ~1000-1300 tokens on this prompt, causing `stop_reason=max_tokens` truncation that drops `evidence_by_field` (emitted after the `data` block) before the JSON closes.
- **Fix:** (1) Changed `os.environ.setdefault(...)` to direct assignment `os.environ["USE_MOCK_WEB_RESEARCH"] = "false"` in scripts/smoke_closed_won_research.py. (2) Raised `max_tokens` 2000->4096 in both src/web_research.py (Python dev oracle) and scripts/build_cloud_workflows.py (n8n production prompt, parity requirement), rebuilt n8n workflows.
- **Files changed:** scripts/smoke_closed_won_research.py, src/web_research.py, scripts/build_cloud_workflows.py, n8n/wf_enrichment_local_live.json
---

## lusha-v2-person-contract — live n8n Cloud canary 400'd on Lusha Enrich, real response never unwrapped
- **Date:** 2026-07-28
- **Error patterns:** Lusha, v2/person, contacts, contactId, property email should not exist, BadRequest, identity_keys, linkedinUrl, lushaCandidates, contact.data, rawResponse.contact, 400
- **Root cause:** Two independent contract mismatches against the real live Lusha `/v2/person` API (confirmed live against portal 22617666), both required to fix end to end (AND-gate): (1) REQUEST — `scripts/build_cloud_workflows.py`'s `build_enrichment_cloud()` "Lusha Enrich" node POSTed the bare `identity_keys` object directly instead of the real `{"contacts":[{contactId, email?, linkedinUrl?}]}` array shape (only `email`/`linkedinUrl` accepted per element; `firstName`/`lastName`/`companyName`/`companyDomain`/`domain`/`phoneNumber`/`jobTitle` all rejected). (2) RESPONSE — `n8n/code/normalizeProviders.js` `lushaCandidates()` (line 138) unwrapped a singular `rawResponse.contact.data`, but the real response is a plural, contactId-keyed map `{"contacts":{"<id>":{"error","data":{...}}}}`.
- **Fix:** (1) Lusha Enrich's jsonBody expression now maps `identity_keys` down to `{contactId:"1", email?, linkedinUrl?}` (camelCase), omitting absent fields, and emits an empty `contacts` array (never a malformed element) when neither `email` nor `linkedin_url` is present — skip-not-retry, row still flows to Apollo/ZoomInfo. (2) `lushaCandidates()` now checks `rawResponse.contacts` (plural map) first, takes the single entry's `.data`, and treats a truthy per-contact `error` or missing `data` as zero candidates without throwing. Pre-existing singular `contact.data` and flat-fixture fallback branches left untouched for offline back-compat (exactly one caller in the repo, confirmed by grep).
- **Scope notes:** An existing fixture (`tests/fixtures/enrichment/lusha_live_person.json`) was mislabeled "real v2 person" using the singular shape that was never actually observed live — kept (still exercises a legitimate offline-fallback path) but its comment corrected, not silently rewritten. The companies Lusha path (`/v2/company`) uses a different, already-correct flat `{"data":{...}}` unwrap and is unaffected — untouched. `build_enrichment_local_live()`'s separate GET-querystring Lusha request (`ENRICH_BUILD_REQUESTS`) has its own, different, unrelated contract break — flagged, not fixed, deferred to a future session.
- **Files changed:** scripts/build_cloud_workflows.py, n8n/code/normalizeProviders.js, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, tests/n8n/lushaRequestContract.test.mjs (new), tests/n8n/enrichment.test.mjs, tests/fixtures/enrichment/lusha_live_person_v2.json (new)
---

## scheduled-lane-canary-timing — a scheduled-lane canary that "finds nothing" is usually a clock problem, not a filter problem
- **Date:** 2026-07-29
- **Error patterns:** total=0, Extract Rows items=0, scheduled tick, mode=trigger, Review Search, SJ-1, search index lag, eventual consistency, seeded canary not found, 15-minute trigger
- **Root cause:** Testing a schedule-triggered lane against a freshly-seeded HubSpot record has TWO independent race conditions, and both present identically to a broken filter (`total: 0`, downstream nodes emitting 0 items, execution status `success`): (1) HubSpot CRM search is EVENTUALLY CONSISTENT — a record created via the API is not immediately returned by `/search`; observed lag was ~6s in one case and ~3 minutes in another, so it is not a fixed bound. (2) The tick you capture may PREDATE the seed — polling "for the next scheduled execution" catches whichever tick fires first, which is frequently the one already in flight before the canary existed. This cost three separate cycles in one session: execution 45 (04:15:11Z) predated a 04:17:46Z seed; execution 49 (04:45:11Z) predated a 04:50:37Z indexing.
- **Fix:** Two guards on any scheduled-lane canary: (a) poll the REAL search filter directly until the seeded id actually comes back before trusting any tick, recording the indexed-at time; (b) filter captured executions by `startedAt >= indexed_at`, not merely "the next one seen". Independently: the same eventual consistency means a search-gated create path can double-create inside the index window (recorded in BUG 19's resolution) — a genuine production property, not only a test artifact.
- **Scope notes:** Distinguishing a timing miss from a real defect is not optional cleverness — BUG 22a was a genuinely empty filter that looked exactly like this, so "total=0 on a scheduled search" must be diagnosed, never assumed either way. The tell is the execution's `startedAt` versus the record's `createdate` / observed index time.
- **Files changed:** (none — methodology; applied in .planning/phases/16-scheduled-workflows-review-surface/16-UAT.md)
---

## canary-wait-date-parse-footgun — a `date -u -d <ISO8601>` wait-target computation silently returns immediately on Darwin
- **Date:** 2026-07-29
- **Error patterns:** date -u -d, until loop, wait-until-epoch, background bash, run_in_background, sandbox blocks sleep, GNU date vs BSD date, target epoch, canary ≥3 minute gap, false total:0
- **Root cause:** The execution sandbox blocks a plain leading `sleep N` (required instead: `run_in_background` with an `until [ "$(date +%s)" -ge "$TARGET" ]; do sleep 5; done` poll loop). Computing `$TARGET` by parsing an ISO-8601 timestamp string with `date -u -d "<ts>" +%s` works on GNU/Linux but is not a valid flag combination on macOS/BSD `date`; on this Darwin host it did not error (which would have tripped an `||` fallback to `date -j -f`), it silently produced a **wrong epoch far in the past**. The until-loop's condition (`now >= target`) was then already true at the very first check, so the "wait ≥3 minutes" loop returned in under a second, and a HubSpot search fired immediately after looked like a valid second data point for a scheduled-lane / canary "prove nothing was created" check.
- **Fix:** Never parse an ISO-8601 string with shell `date -d` inside this repo's canary tooling. Compute epoch targets with a `.venv/bin/python -c "from datetime import datetime,timezone; ..."` one-liner (portable, unambiguous) or capture `$(date -u +%s)` directly at the moment of the reference event and add the required delta in pure integer arithmetic — never round-trip through a re-parsed timestamp string. Always sanity-check a completed wait by diffing the two real timestamps before trusting the gap (this is what caught it here: the second search's timestamp was ~40s after the first, not ≥180s, and was discarded before being recorded as evidence).
- **Scope notes:** Distinct from `scheduled-lane-canary-timing` (that entry is about HubSpot search-index eventual consistency and tick-predates-seed races; this one is about the wait-target *computation* itself being wrong on this host, independent of any HubSpot behavior). Applies to any future live canary in this repo that needs a "wait N minutes, then re-check" step on a Darwin execution host.
- **Files changed:** (none — methodology; applied in .planning/phases/17-enrichment-contacts-reachability-bug-23/17-02 live canary, Case B)
---

## walk-write-path-defects — three-defect chain silently swallowed a real HubSpot write during an operator walk
- **Date:** 2026-08-25
- **Error patterns:** "No failures, nothing to re-send" over write_blocked, action:write_blocked, hs_object_id:null, match.reason "searched, no hit", total:0 name search, company: null on a company-associated contact, ALLOW_HUBSPOT_RECORD_WRITES=false, TEST_RECORD_IDS empty, per-send yes did not arm the backend, VOCAB-05 consent passed but nothing wrote
- **Root cause(s):** F3 (client) — `enrich-records/SKILL.md` step 8's blanket "do not claim per-record outcomes" rule (written to stop invention) also suppressed a REAL `write_blocked`/no-hit outcome the synchronous webhook body actually carried; `report_enrichment.py`'s existing `write_blocked`→"blocked" mapping was simply never reached on that path. F1 (n8n backend) — the deployed `HubSpot Name Search` node filters `lastname EQ ... AND company CONTAINS_TOKEN ...` against the contact's `company` TEXT property, which is null for contacts the ingest lane associates to a company OBJECT instead of a text field, so the search silently returns `total:0` for every record this system itself creates. F2 (client+backend contract) — the deployed `Decide Action` node ships with compiled-in `ALLOW_HUBSPOT_RECORD_WRITES="false"`; only the standing-grant path ever called `n8n_arming.armed_window` to flip it, so a per-send "yes" with no open grant authorized the client's own POST but never armed the backend — every interactive send without a pre-existing grant was structurally incapable of writing, regardless of consent.
- **Fix:** F3 — added `report_enrichment.build_sync_report(body)` (reusing the existing `_ACTION_TO_OUTCOME`/`_OUTCOME_REASON`/`_build_row_report` machinery, extended with `match_level`/`match_reason`), and narrowed (never deleted) the SKILL's suppression rule to "never invent what the body does not carry; always relay what it does". F1 — added a second, weaker "HubSpot Name Search Fallback" node (lastname-only) wired SEQUENTIALLY after the primary search, feeding `matchProposal.js`'s existing `mediumCandidates()` (new `requireCompanyToken:false` option, default `true` = byte-identical for existing callers) so multi-hit or fallback-only results surface as an unverified candidate/proposal, never an auto-match. F2 — added `write_grant.authorize_ungranted_send()`, composing the EXISTING `plan_grant()`+`open_grant(proposal, "yes", config)` into a single-lane, single-send grant scoped to that send's own record ids/domains, returning the identical `{armed, workflow_id, grant, refusal, detail}` shape `authorize_send()` returns — inherits Guardrail A (dirty-backend refusal) for free, touches zero headless/cron arming code (`arm_for_dispatch`, `scheduled_arm.py` byte-identical).
- **Files changed:** operator-claude-plugin/scripts/report_enrichment.py, operator-claude-plugin/tests/test_report_enrichment.py, operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/tests/test_enrich_skill_contract.py, n8n/code/matchProposal.js, tests/n8n/matchProposal.test.mjs, scripts/build_cloud_workflows.py, scripts/deploy_n8n_workflows.py, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, tests/test_enrichment_contacts_search_transport.py, operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py, operator-claude-plugin/skills/contact-upload/SKILL.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md
- **Why not caught:** No gate existed for any of the three classes. F3 was a wording-level over-suppression in a SKILL.md prose contract — no test asserted the synchronous body's real per-row fields were relayed, only that outcomes weren't invented. F1 was a query built against the wrong property for a record shape (company-as-association) the search node's author didn't anticipate; nothing exercised the name lane against a company-associated (rather than company-text) contact until a real operator walk did. F2 was an authorization-path asymmetry (grant path armed the backend, ungranted path only armed the client's own POST) that unit tests never caught because every prior interactive test used a standing grant; no test exercised "yes, no grant" end to end against a live write gate.
- **Recurrence guard:** F3 — `operator-claude-plugin/tests/test_enrich_skill_contract.py::test_the_skill_relays_what_the_sync_body_says_never_inventing_beyond_it` plus `test_report_enrichment.py`'s `build_sync_report` suite (write_blocked relay, bare-object acceptance, ICP/tier trace guard on its own output). F1 — `tests/n8n/matchProposal.test.mjs`'s `requireCompanyToken:false` tests plus `tests/test_enrichment_contacts_search_transport.py::test_hubspot_name_search_fallback_carries_no_company_filter_at_all` and its sequential-wiring test (`$()` node-lookup ordering). F2 — `operator-claude-plugin/tests/test_write_grant.py`'s `authorize_ungranted_send` suite (tracer, admin-not-enabled refusal, Guardrail A refusal, empty-record-set refusal, never-widens-beyond-send, same-shape-as-authorize_send, never-calls-arm_for_dispatch-directly). Open item for the operator: a genuine live walk (real consent, real write) to close F2's one untested dimension — see the archived session's Handover section for the required sequence (plugin update ordering, then walk).
---
