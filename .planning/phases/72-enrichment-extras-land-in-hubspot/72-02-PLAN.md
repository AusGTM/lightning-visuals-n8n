---
phase: 72-enrichment-extras-land-in-hubspot
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - config/column_mapping.yaml
  - operator-claude-plugin/config/column_mapping.yaml
  - n8n/code/columnMap.js
  - config/field_policy.yaml
  - operator-claude-plugin/config/field_policy.yaml
  - n8n/code/mergeContacts.js
  - scripts/build_cloud_workflows.py
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_review_decision_cloud.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - tests/n8n/ingestWidenedFieldsFlow.test.mjs
  - tests/n8n/widenedKeyParity.test.mjs
  - operator-claude-plugin/tests/test_extraction_handoff.py
  - operator-claude-plugin/tests/test_preingest_merge.py
autonomous: true
requirements: [D-72-01, D-72-02, D-72-04]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-72-01: every enrichment extra the waterfall promotes for a contact — `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`, `lv_persona_group` alongside plan 01's `mobilephone` — has a CSV header alias and reaches the ingest lane's `mergeContacts()` candidate; the ingest candidate loop now mirrors `ENRICH_MERGE`'s already-wide loop."
    - "D-72-04: a `linkedin_url` on an ingest row lands as BOTH `lv_linkedin_url` (canonical, PN-1 unchanged) and native `hs_linkedin_url` on the created/updated contact; `hs_linkedin_url` carries its own explicit `class` and `min_confidence` in `config/field_policy.yaml` and a matching entry in `DEFAULT_CONTACT_POLICY`."
    - "D-72-02: `config/field_policy.yaml` is the ONE source of which keys may reach HubSpot; `config/column_mapping.yaml`, `n8n/code/columnMap.js` and the ingest lane's candidate loop are all pinned against it by a derived parity test — no consumer restates a key list, and no test hardcodes the number twelve."
    - "The enrichment gate's `REQUIRED` array stays the same twelve keys after new `field_policy.yaml` entries land — a write-only target such as `hs_linkedin_url` must never make every contact read as incomplete and trigger provider spend (Phase 66 D-66-01 economics)."
    - "`config/field_policy.yaml` and `operator-claude-plugin/config/field_policy.yaml` stay byte-identical, and `config/column_mapping.yaml` and its plugin copy stay byte-identical — both pairs are pinned by existing drift tests."
  artifacts:
    - config/column_mapping.yaml
    - config/field_policy.yaml
    - n8n/code/columnMap.js
    - n8n/code/mergeContacts.js
    - tests/n8n/widenedKeyParity.test.mjs
  key_links:
    - "`ENRICH_MERGE` (scripts/build_cloud_workflows.py ~L1958-1980) is the reference candidate loop; `MERGE_CONTACTS` (~L395-421) is widened to match it, reading from `row.*` instead of `winners.*`."
    - "`tests/test_field_policy_conformance.py::test_key_sets_are_identical` forces every new `config/field_policy.yaml` `contacts:` key into `n8n/code/mergeContacts.js`'s `DEFAULT_CONTACT_POLICY` in the same commit — the two are one contract."
    - "`ENRICH_GATE`'s `REQUIRED` (scripts/build_cloud_workflows.py ~L1880-1883) is a hardcoded twelve-key literal, NOT derived from the policy — a new policy key does not widen it, and the acceptance criterion pins that it stays twelve."
  prohibitions:
    - "SAFE-01..05: every new policy key gets an EXPLICIT `class` and `min_confidence`. No default fallthrough, no threshold lowered, no `fill_blank_only` weakened."
    - "Never hand-edit any `n8n/wf_*.json`; the JSON entries in `files_modified` are regeneration outputs only."
    - "No `column_mapping.yaml` change for LinkedIn: `required_identity.any_of` keeps its three groups `[email]`, `[firstname, lastname, company]`, `[linkedin_url]` verbatim (D-72-19 closes the naming inside `merge_enriched`, plan 03), and `tests/n8n/columnMapIdentityParity.test.mjs` stays green UNMODIFIED."
    - "No new hardcoded key list in Python or JS; no test may assert the promotable count as the literal 12 (rewrite `test_promotable_contact_props_names_the_twelve_promotable_contact_keys` to derive from the YAML)."
    - "Nothing armed, deployed or bounced. No live HubSpot or n8n call."
    - "D-72-16: no widened contact geo key is wired to `lv_country_region_normalized` anywhere."
---

<objective>
Widen the ingest lane from plan 01's one-field tracer to the full contact set the waterfall
already pays for, and close the LinkedIn write-target half of the naming defect.

Purpose: `preingest.promotable_contact_props()` returns twelve keys; the CSV boundary carried
eight; `strip_enrichment_extras` threw away the nine-key difference at dispatch. Plan 01
recovered `mobilephone`. This plan recovers `city`, `state`, `country`, `hs_state_code`,
`hs_country_region_code`, `seniority` and `lv_persona_group`, and adds native
`hs_linkedin_url` as a second write target beside the existing `lv_linkedin_url` (D-72-04) —
the property that actually exists on this portal, which no contact has ever received.

`lv_linkedin_url` itself needs no alias: the lane already maps the `linkedin_url` header to
the `lv_linkedin_url` candidate key (PN-1), and D-72-19 reconciles the provider response's own
`lv_linkedin_url` name inside `merge_enriched` in plan 03. The CSV-side vocabulary stays
`linkedin_url`.

Output: eight new canonical alias targets across the two byte-identical `column_mapping.yaml`
copies and `columnMap.js`; a widened `MERGE_CONTACTS` candidate loop; a widened ingest search
fetch list; one new `field_policy.yaml` key with its `DEFAULT_CONTACT_POLICY` twin; a derived
three-way parity test; and no change to the enrichment gate's twelve-key `REQUIRED`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-PATTERNS.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The remaining seven widened keys reach the ingest candidate</name>
  <files>config/column_mapping.yaml, operator-claude-plugin/config/column_mapping.yaml, n8n/code/columnMap.js, scripts/build_cloud_workflows.py, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json, tests/n8n/ingestWidenedFieldsFlow.test.mjs, operator-claude-plugin/tests/test_extraction_handoff.py</files>
  <read_first>
    - config/column_mapping.yaml (as plan 01 left it)
    - n8n/code/columnMap.js (`ALIASES`, `mapRow`)
    - scripts/build_cloud_workflows.py `MERGE_CONTACTS` (~L395-430, as plan 01 left it) and `ENRICH_MERGE` (~L1958-1980, the loop to mirror)
    - scripts/build_cloud_workflows.py ~L1190-1205 (`HubSpot Search by Email` `properties` list, as plan 01 left it)
    - config/field_policy.yaml `contacts:` block (the twelve promotable keys and their classes)
    - tests/n8n/ingestWidenedFieldsFlow.test.mjs (plan 01's harness)
    - operator-claude-plugin/tests/test_extraction_handoff.py `CANONICAL_PROPS`
  </read_first>
  <behavior>
    - A CSV row carrying `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority` and `lv_persona_group` values produces a `HubSpot Create` body containing all seven.
    - The same seven, on a matched row whose HubSpot contact already holds different non-blank values, are ABSENT from the update body (all seven are `fill_blank_only` or `system_owned`; the two `system_owned` keys — `seniority`, `lv_persona_group` — DO promote, so assert those two present and the five `fill_blank_only` location keys absent).
    - `mapRow` maps each new header alias to its canonical key and drops anything outside the table.
  </behavior>
  <action>
Add alias groups to `config/column_mapping.yaml`'s `aliases:` map for the seven remaining
promotable contact keys, then copy the file byte-for-byte to
`operator-claude-plugin/config/column_mapping.yaml` and mirror the same additions into
`n8n/code/columnMap.js`'s `ALIASES`. Canonical targets and the headers to accept:
`city` (`city`, `town`, `locality`); `state` (`state`, `region`, `province`, `state/region`);
`country` (`country`, `country/region`); `hs_state_code` (`hs_state_code`, `state code`,
`state/region code`); `hs_country_region_code` (`hs_country_region_code`, `country code`,
`country/region code`); `seniority` (`seniority`, `seniority level`); `lv_persona_group`
(`lv_persona_group`, `persona`, `persona group`). Every key lowercase and
whitespace-collapsed. Do NOT add any alias whose canonical value is `lv_linkedin_url` — the
CSV vocabulary for that fact stays `linkedin_url` (D-72-19).

Widen the `HubSpot Search by Email` node's `properties` array in
`scripts/build_cloud_workflows.py` to the full promotable set so the lane can gate on each:
add `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`,
`lv_persona_group`, `lv_linkedin_url` to the list plan 01 left (`email`, `firstname`,
`lastname`, `jobtitle`, `phone`, `mobilephone`, `hs_object_id`).

Widen `MERGE_CONTACTS`'s candidate `for (const f of [...])` loop to
`email, firstname, lastname, jobtitle, company, mobilephone, seniority, city, state, country,
hs_state_code, hs_country_region_code, lv_persona_group`, mirroring `ENRICH_MERGE`'s loop but
reading `row[f]` rather than `winners[f]`. Keep the existing `row.linkedin_url` →
`candidate.lv_linkedin_url` branch and the `row.phone_normalized` → `candidate.phone` branch
exactly as they are.

Update `operator-claude-plugin/tests/test_extraction_handoff.py`'s `CANONICAL_PROPS` literal to
the new sorted sixteen: `city`, `company`, `company_id`, `country`, `email`, `firstname`,
`hs_country_region_code`, `hs_state_code`, `jobtitle`, `lastname`, `linkedin_url`,
`lv_persona_group`, `mobilephone`, `phone`, `seniority`, `state`.

Regenerate with `.venv/bin/python scripts/build_cloud_workflows.py`, then extend
`tests/n8n/ingestWidenedFieldsFlow.test.mjs` with the two behaviors above using its existing
fixture/arming helper.
  </action>
  <reversibility rating="reversible">Config and generated JSON; revert plus regenerate restores.</reversibility>
  <verify>
    <automated>node --test tests/n8n/ingestWidenedFieldsFlow.test.mjs tests/n8n/columnMapAliasParity.test.mjs tests/n8n/columnMapIdentityParity.test.mjs</automated>
    <fails_when>non-zero exit, or any "not ok" line in the TAP output</fails_when>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import preingest, extraction; p=set(preingest.promotable_contact_props()); c=set(extraction.canonical_props()); print('UNCOVERED:', sorted(p-c)); sys.exit(1 if (p-c) - {'lv_linkedin_url'} else 0)"</automated>
    <fails_when>non-zero exit, or the printed `UNCOVERED:` list contains any key other than `lv_linkedin_url` (which is deliberately lane-side only, D-72-19)</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_extraction_handoff.py operator-claude-plugin/tests/test_column_mapping_shipped.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
    <automated>/usr/bin/grep -c "lv_persona_group" scripts/build_cloud_workflows.py</automated>
    <fails_when>the count is fewer than 3 — the key must appear in the ingest candidate loop, the ingest search fetch list, and the pre-existing enrichment lane usages</fails_when>
  </verify>
  <acceptance_criteria>
    - `set(promotable_contact_props()) - set(canonical_props())` equals exactly `{"lv_linkedin_url"}`.
    - `diff -q config/column_mapping.yaml operator-claude-plugin/config/column_mapping.yaml` reports nothing.
    - `n8n/wf_contact_ingest_cloud.json`'s `Merge Contacts` node body contains the thirteen-name candidate loop literal including `lv_persona_group` and `hs_country_region_code`.
    - `n8n/wf_contact_ingest_cloud.json`'s `HubSpot Search by Email` `jsonBody` contains `"lv_persona_group"` and `"hs_country_region_code"`.
    - The walker test asserts all seven land on create; the five location keys are withheld and the two `system_owned` keys promote on a conflicting update.
  </acceptance_criteria>
  <done>Every promotable contact key except the deliberately lane-side `lv_linkedin_url` has a CSV header and reaches the ingest merge candidate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LinkedIn lands natively too — `hs_linkedin_url` as a second write target</name>
  <files>config/field_policy.yaml, operator-claude-plugin/config/field_policy.yaml, n8n/code/mergeContacts.js, scripts/build_cloud_workflows.py, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, n8n/wf_review_decision_cloud.json, n8n/wf_scheduled_maintenance_cloud.json, tests/n8n/ingestWidenedFieldsFlow.test.mjs, operator-claude-plugin/tests/test_preingest_merge.py</files>
  <read_first>
    - config/field_policy.yaml `contacts:` block, specifically the `lv_linkedin_url` entry (`fill_blank_only`, `min_confidence: 85`, `protect_if_current_present: true`) — the shape to mirror
    - n8n/code/mergeContacts.js lines 40-60 (`DEFAULT_CONTACT_POLICY`)
    - tests/test_field_policy_conformance.py (all four tests — key-set identity, class match, min_confidence match, system_correctable_sources match)
    - scripts/build_cloud_workflows.py `MERGE_CONTACTS` (the `row.linkedin_url` → `candidate.lv_linkedin_url` branch) and `ENRICH_MERGE` (`canonicalizeLinkedin(winners.linkedin_url)`)
    - scripts/build_cloud_workflows.py ~L1875-1885 (`ENRICH_GATE`'s twelve-key `REQUIRED` literal and the T-66-04 cost comment above it)
    - operator-claude-plugin/tests/test_preingest_merge.py lines 360-400 (`test_promotable_contact_props_names_the_twelve_promotable_contact_keys`)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md, "Portal facts": `hs_linkedin_url` present on contacts; no `linkedin_url` property exists
  </read_first>
  <behavior>
    - A row with a `linkedin_url` header value produces a create body carrying BOTH `lv_linkedin_url` and `hs_linkedin_url`, each holding the canonicalized URL.
    - On a matched row whose HubSpot contact already holds a different non-blank `hs_linkedin_url`, that key is ABSENT from the update body (`fill_blank_only`).
    - `promotable_contact_props()` returns thirteen keys, derived — no test asserts the literal twelve.
    - The enrichment gate's `REQUIRED` array still lists exactly twelve keys and does not include `hs_linkedin_url`.
  </behavior>
  <action>
Add an `hs_linkedin_url` entry to `config/field_policy.yaml`'s `contacts:` block mirroring the
existing `lv_linkedin_url` entry exactly: `class: fill_blank_only`, `promote_to_canonical:
true`, `min_confidence: 85`, `protect_if_current_present: true`. Add a comment naming D-72-04
and stating that this is a write-only mirror of `lv_linkedin_url` for the native portal
property, deliberately NOT added to `ENRICH_GATE`'s `REQUIRED` because a blank native mirror
must not mark a contact incomplete and trigger provider spend (the D-66-01/T-66-04 economics).
Copy the file byte-for-byte to `operator-claude-plugin/config/field_policy.yaml`.

Add the matching `hs_linkedin_url: { class: "fill_blank_only", min_confidence: 85 }` entry to
`n8n/code/mergeContacts.js`'s `DEFAULT_CONTACT_POLICY` in the SAME commit —
`tests/test_field_policy_conformance.py::test_key_sets_are_identical` requires the two key
sets to be equal and will fail otherwise.

In `scripts/build_cloud_workflows.py`'s `MERGE_CONTACTS`, extend the existing
`row.linkedin_url` branch so it sets `candidate.hs_linkedin_url` to the same value it already
sets on `candidate.lv_linkedin_url`. Do the same in `ENRICH_MERGE` for
`canonicalWinnerLinkedin`, so both lanes write both targets from one canonicalization. Leave
`ENRICH_GATE`'s `REQUIRED` literal untouched at twelve keys.

Rewrite `operator-claude-plugin/tests/test_preingest_merge.py`'s
`test_promotable_contact_props_names_the_twelve_promotable_contact_keys`: rename it to drop
the count from the name, and derive the expected set by parsing
`preingest.resolve_policy_path()`'s YAML `contacts:` keys with PyYAML rather than restating a
literal list. The test's job is "the function returns exactly the policy's contact keys", not
"the policy has N keys".

Regenerate with `.venv/bin/python scripts/build_cloud_workflows.py` and extend the walker
test with the two behaviors above.
  </action>
  <reversibility rating="reversible">Policy keys and generated JSON; a revert restores the previous key set.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_field_policy_conformance.py operator-claude-plugin/tests/test_preingest_merge.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
    <automated>node --test tests/n8n/ingestWidenedFieldsFlow.test.mjs tests/n8n/mergeContacts.test.mjs</automated>
    <fails_when>non-zero exit, or any "not ok" line in the TAP output</fails_when>
    <automated>/usr/bin/grep -c "hs_linkedin_url" config/field_policy.yaml operator-claude-plugin/config/field_policy.yaml n8n/code/mergeContacts.js</automated>
    <fails_when>any of the three files reports 0</fails_when>
    <automated>.venv/bin/python -c "import re,sys; s=open('scripts/build_cloud_workflows.py').read(); m=re.search(r'const REQUIRED = \[\s*\n\s*\"city\".*?\];', s, re.S); n=len(re.findall(r'\"[a-z_]+\"', m.group(0))); print('REQUIRED_LEN', n); sys.exit(0 if n==12 else 1)"</automated>
    <fails_when>non-zero exit, or the printed `REQUIRED_LEN` is not 12 — a new policy key leaked into the enrichment gate's chase list</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider -k "promotable" operator-claude-plugin/tests/test_preingest_merge.py</automated>
    <fails_when>non-zero exit, or the summary reports "no tests ran" (the derived replacement test was never written)</fails_when>
  </verify>
  <acceptance_criteria>
    - `config/field_policy.yaml` and its plugin copy each contain an `hs_linkedin_url:` entry with an explicit `class` and `min_confidence`; `diff -q` between the two reports nothing.
    - `n8n/code/mergeContacts.js`'s `DEFAULT_CONTACT_POLICY` contains `hs_linkedin_url`.
    - `tests/test_field_policy_conformance.py` passes with no edit to the test file itself.
    - `ENRICH_GATE`'s `REQUIRED` literal in `scripts/build_cloud_workflows.py` still names exactly twelve keys and does not contain `hs_linkedin_url`.
    - No test in `operator-claude-plugin/tests/` asserts the promotable count as the literal integer 12.
    - The walker test asserts both `lv_linkedin_url` and `hs_linkedin_url` in a create body from one `linkedin_url` header.
  </acceptance_criteria>
  <done>LinkedIn reaches the portal property that actually exists, gated by its own explicit policy entry, without widening the enrichment gate's chase list.</done>
</task>

<task type="auto">
  <name>Task 3: One derived parity test binds policy, column map and lane candidate</name>
  <files>tests/n8n/widenedKeyParity.test.mjs</files>
  <read_first>
    - tests/n8n/columnMapIdentityParity.test.mjs (the Python-oracle-subprocess idiom to reuse: drive the JS side from the YAML through `.venv/bin/python`, never restate values in JS)
    - tests/n8n/columnMapAliasParity.test.mjs (`yamlAliases()` helper shape)
    - config/field_policy.yaml `contacts:` block
    - n8n/wf_contact_ingest_cloud.json (the `Merge Contacts` node's `parameters.jsCode`, to read the candidate loop literal out of the shipped artifact)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md D-72-02
  </read_first>
  <action>
Write `tests/n8n/widenedKeyParity.test.mjs`, the third dimension of the existing parity family.
Reuse `columnMapIdentityParity.test.mjs`'s idiom verbatim: a `.venv/bin/python -c` oracle
parses `config/field_policy.yaml` and `config/column_mapping.yaml` with PyYAML and prints
JSON; the JS side compares, never restating a key list of its own.

Three assertions, all derived:
(1) every `config/field_policy.yaml` `contacts:` key that is reachable from a CSV column has a
canonical alias target in `config/column_mapping.yaml` — computed as
`policy_contacts_keys - column_mapping_canonical_targets`, which must equal exactly the
lane-side-only set `{lv_linkedin_url, hs_linkedin_url}`, with the exemption named in an inline
comment citing D-72-19 and D-72-04 rather than being an unexplained allowance.
(2) the candidate-loop field list read out of the COMMITTED `n8n/wf_contact_ingest_cloud.json`
`Merge Contacts` node's `jsCode` (extract the `for (const f of [...])` array literal) is a
subset of `policy_contacts_keys ∪ column_mapping_canonical_targets` — no key reaches HubSpot
that no policy governs.
(3) `n8n/code/columnMap.js`'s `ALIASES` values, deduplicated, equal
`config/column_mapping.yaml`'s alias values, deduplicated (the existing alias-parity test
covers the whole map; this asserts the canonical-target SET specifically, which is what
`extraction.canonical_props()` derives).

Each failure message must name which file to edit and say that the fix is to widen the YAML,
never to add a second list.
  </action>
  <reversibility rating="reversible">A new test file.</reversibility>
  <verify>
    <automated>node --test tests/n8n/widenedKeyParity.test.mjs</automated>
    <fails_when>non-zero exit, or fewer than 3 "ok" lines in the TAP output, or any "not ok" line</fails_when>
    <automated>/usr/bin/grep -c "field_policy.yaml" tests/n8n/widenedKeyParity.test.mjs</automated>
    <fails_when>the count is 0 — the test does not read the single source of truth it exists to pin</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or any "not ok" line in the TAP output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/ tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/n8n/widenedKeyParity.test.mjs` exists, contains no hardcoded contact-key array of its own, and drives both YAML files through a `.venv/bin/python` oracle.
    - The test reads the candidate loop out of the committed `n8n/wf_contact_ingest_cloud.json`, not out of `scripts/build_cloud_workflows.py`.
    - The lane-side-only exemption set is exactly `{lv_linkedin_url, hs_linkedin_url}` and carries an inline comment citing D-72-19 and D-72-04.
    - Both full suites are green.
  </acceptance_criteria>
  <done>Adding a key to `config/field_policy.yaml` without giving it a CSV header, or adding a lane candidate no policy governs, now fails a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator spreadsheet → plugin CSV parse | untrusted header names and cell values |
| plugin dispatch CSV → n8n webhook | `write_dispatch_csv`'s STRUCT-01 allowlist |
| n8n lane → HubSpot CRM write | seven more properties now land per contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Tampering | widened `MERGE_CONTACTS` candidate + `config/field_policy.yaml` | high | mitigate | Every one of the eight newly-reachable keys already carries an explicit `class` and `min_confidence` in `config/field_policy.yaml`; the one NEW key (`hs_linkedin_url`) is given `fill_blank_only`/85 explicitly in Task 2. Task 3's parity test refuses a lane candidate that no policy governs, closing the default-fallthrough path structurally. |
| T-72-09 | Tampering | `ENRICH_GATE`'s `REQUIRED` chase list | high | mitigate | A write-only policy key that leaked into `REQUIRED` would mark every contact incomplete and drive unbounded provider spend on every scheduled tick (the D-66-01/T-66-04 economics). Task 2's verify parses the literal and refuses any length other than twelve. |
| T-72-10 | Spoofing | `required_identity.any_of` | medium | accept | This plan adds no identity group and does not touch `required_identity`; `tests/n8n/columnMapIdentityParity.test.mjs` stays green unmodified, which is the acceptance evidence. Accepted because the surface is unchanged. |
| T-72-07 | Elevation of Privilege | regenerated `n8n/wf_*.json` | critical | mitigate | Nothing armed, deployed or bounced in this plan; the walker test arms only an in-memory copy. Plan 01 Task 3's grep for an armed literal is re-run by this plan's full-suite verify. |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/ tests/` green.
- `diff -q config/field_policy.yaml operator-claude-plugin/config/field_policy.yaml` and `diff -q config/column_mapping.yaml operator-claude-plugin/config/column_mapping.yaml` both report nothing.
- `set(promotable_contact_props()) - set(canonical_props())` is exactly `{"lv_linkedin_url"}`.
</verification>

<success_criteria>
Every contact field the waterfall promotes has a route from a CSV column to the ingest lane's
merge candidate, LinkedIn lands in the native portal property as well as the canonical one,
and a derived test refuses any future drift between the policy, the column map and the lane.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 72 (exclude from drift verification — none of these exist yet):

- **HubSpot custom properties:** `lv_phone_2` (contacts), `lv_mobilephone_2` (contacts), `lv_phone_2` (companies)
- **`config/column_mapping.yaml` new canonical alias targets:** `mobilephone`, `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`, `lv_persona_group`
- **`config/field_policy.yaml` new keys:** contacts `hs_linkedin_url`, `lv_phone_2`, `lv_mobilephone_2`, `hs_additional_emails`; companies `state`, `hs_state_code`, `hs_country_region_code`, `lv_phone_2`, `hs_additional_domains`
- **`config/hubspot_properties.yaml` new declarations:** the three `_2` slot properties above
- **JS engine symbols:** `opts.now`, `opts.historyByField`, `_isStale()`, `_overflowSlot()` in `n8n/code/mergeContacts.js` and `n8n/code/mergeCompanies.js`; new `DEFAULT_CONTACT_POLICY` / `DEFAULT_COMPANY_POLICY` keys matching the YAML additions
- **Python symbols:** `now=` kwarg and `_is_stale()` in `src/merge_policy.py`; `create_row_ids=` kwarg and `PROVIDER_KEY_ALIASES` in `operator-claude-plugin/scripts/preingest.py`
- **New n8n nodes:** `HubSpot Contact History`, `Contact History Carry Merge`, `Contact History Absent Sentinel Gate` (ingest lane)
- **New test files:** `tests/n8n/ingestWidenedFieldsFlow.test.mjs`, `tests/n8n/widenedKeyParity.test.mjs`, `tests/n8n/mergeRecencyGate.test.mjs`, `tests/n8n/contactHistoryFlow.test.mjs`, `tests/n8n/overflowSlots.test.mjs`
- **New docs section:** the D-72-17 gate spec in `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`

<output>
Create `.planning/phases/72-enrichment-extras-land-in-hubspot/72-02-SUMMARY.md` when done.
</output>
