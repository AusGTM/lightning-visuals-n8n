---
phase: 72-enrichment-extras-land-in-hubspot
plan: 07
type: execute
wave: 7
depends_on: ["72-03", "72-06"]
files_modified:
  - .planning/todos/completed/2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md
  - .planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - CLAUDE.md
  - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
autonomous: true
requirements: [D-72-01, D-72-11, D-72-17, D-72-21]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-72-01: the folded charter todo `2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md` is in `.planning/todos/completed/` carrying its resolution and the three places the phase's answer differs from its original mapping table (one `_2` slot per kind, provenance-only stamps, companies in scope)."
    - "CLAUDE.md §31: every pending todo this phase leaves behind is triaged — `scripts/todo_triage.py --check` exits clean and every new pending file declares a `kind` with its required companion key."
    - "The plugin ships as a NEW version: `operator-claude-plugin/.claude-plugin/plugin.json` is bumped past 0.48.0 with a matching CHANGELOG section, because a `preingest.py` change without a bump lands as an invisible update in the marketplace UI."
    - "The `mobile` header no longer maps to `phone` (D-72-03) — a behaviour change for every existing operator spreadsheet template — and the CHANGELOG says so in the operator's own words."
    - "D-72-17 and D-72-21: the live gate is written down as an executable spec in `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` before plan 08 runs it, including the property creation that must precede the armed send and the read-back list."
    - "CLAUDE.md carries an as-built delta for this phase in the same style as its existing delta subsections, so §29's never-write list and §17.2's promotion rules read true against the shipped code."
  artifacts:
    - .planning/todos/completed/2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md
    - operator-claude-plugin/.claude-plugin/plugin.json
    - operator-claude-plugin/CHANGELOG.md
    - CLAUDE.md
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
  key_links:
    - "`scripts/todo_triage.py`'s `KINDS` map is the contract: `defect` needs `evidence:`, `question` needs `trigger:` and `owner:`, `design` needs `decision_needed:`, `accepted` belongs in `completed/`."
    - "The plugin marketplace shows an Update button only when `plugin.json`'s version changes AND the marketplace clone is refreshed — the bump and the CHANGELOG cut go together (project memory `plugin-release-requires-version-bump`)."
    - "`docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` is what the operator reads while running plan 08's gate; the spec must be complete before the gate, not written up after it."
  prohibitions:
    - "No code change in this plan — no file under `src/`, `n8n/code/`, `scripts/`, `config/` or `operator-claude-plugin/scripts/` is edited. This is documentation, versioning and todo hygiene only."
    - "Do not mark any todo complete that this phase did not actually resolve; read each todo body rather than matching on a `resolves_phase` key (project memory `close-phase-todos-closes-unfixed-todos`)."
    - "Nothing armed, deployed or bounced. No live HubSpot or n8n call."
    - "Do not rewrite CLAUDE.md's §4/§5 target tables or any existing as-built delta — append a new delta subsection in the established style."
---

<objective>
Close the phase's paperwork before the one live gate runs: retire the charter todo, file the
one honest gap this phase leaves, ship the plugin as a visible new version, and write the gate
down so the operator can execute it without reconstructing it from eight plan files.

Purpose: three of these are load-bearing rather than cosmetic. A `preingest.py` change without
a `plugin.json` bump reaches no operator — the marketplace shows no Update button, and the
skills bind to whatever version the session started with. A CLAUDE.md whose §29 never-write
list and §17.2 promotion rules no longer describe the shipped code is how the next phase's
research reaches a wrong premise. And a live gate specified only inside a plan file is a gate
the operator runs from memory.

The `mobile` alias flip deserves its own CHANGELOG line: every existing operator spreadsheet
with a "Mobile" column has been landing that number in `phone`, and from this release it lands
in `mobilephone` instead. That is the ruled-for behaviour (D-72-03), and it is exactly the kind
of change an operator should read about before their next upload rather than discover in a
HubSpot view.

Output: one todo moved with its resolution, one new triaged todo for the unwired history hops,
a version bump with a CHANGELOG section, a CLAUDE.md as-built delta, and the D-72-17 gate spec.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-03-SUMMARY.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-04-SUMMARY.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-05-SUMMARY.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the charter todo, file the history gap, leave the queue triaged</name>
  <files>.planning/todos/completed/2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md, .planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md</files>
  <read_first>
    - .planning/todos/pending/2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md in full (all 67 lines — its frontmatter, its original mapping table, and its `decision_needed`)
    - scripts/todo_triage.py in full (the `KINDS` map, the `--check` and `--since` modes, what `--check` refuses)
    - tests/test_todo_triage.py
    - CLAUDE.md §31 in full (the three rules: triage at creation, zero-inbox at batch close, widen scope for an adjacent residual)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-04-SUMMARY.md (the recorded enrichment-lane / companies-branch history gap and the decision it needs)
    - An existing file under `.planning/todos/completed/` for the resolution-note shape to copy
  </read_first>
  <action>
Move `2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md` from
`.planning/todos/pending/` to `.planning/todos/completed/` with `git mv`, and append a
`## Resolution` section to it naming Phase 72 and stating the three places the shipped answer
differs from the todo's own original mapping table, as CONTEXT.md records: overflow is capped
at ONE `_2` slot per kind rather than an open set; verification stamps are provenance JSON only
rather than per-field `_source`/`_verified_at` properties; and companies are in scope, not
contacts alone. Name the plan that delivered each part.

Create `.planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md`
recording plan 04's stated scope line. Frontmatter must satisfy `scripts/todo_triage.py`:
`kind: design` with a non-empty `decision_needed:` naming the choice — add a
`propertiesWithHistory` hop to the enrichment lane's contacts branch and the companies branch,
or accept the pipeline's own `lv_<field>_verified_at` cache keys as a narrower substitute
there. Include `owner: operator`, a `severity`, an `area`, a `files:` list naming
`scripts/build_cloud_workflows.py` and `n8n/code/mergeCompanies.js`, and a body stating plainly
that the absence is behaviour-preserving — an absent history means the gate takes the pre-72
`needs_review` branch — so company `industry` recency is unobservable rather than wrong.

Read every other file in `.planning/todos/pending/` and confirm none of them was actually
resolved by this phase. CONTEXT.md's "Reviewed Todos (not folded)" list names five matched by
keyword only; leave all five where they are. Do not complete a todo on the strength of a
`resolves_phase` key or a filename match — read the body.
  </action>
  <reversibility rating="reversible">File moves and one new markdown file.</reversibility>
  <verify>
    <automated>.venv/bin/python scripts/todo_triage.py --check</automated>
    <fails_when>non-zero exit, or the output names any todo as UNTRIAGED or missing a required companion key</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_todo_triage.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
    <automated>ls .planning/todos/pending/ | /usr/bin/grep -c "ingest-lane-drops-paid-for-enrichment-extras"</automated>
    <fails_when>the count is 1 or more — the charter todo is still pending</fails_when>
    <automated>/usr/bin/grep -c "^## Resolution" .planning/todos/completed/2026-09-12-ingest-lane-drops-paid-for-enrichment-extras-map-them-instead.md</automated>
    <fails_when>the count is 0 — the todo was moved without recording what resolved it</fails_when>
    <automated>/usr/bin/grep -c "decision_needed:" .planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md</automated>
    <fails_when>the count is 0 — a `kind: design` todo without a `decision_needed:` is untriaged and fails the root suite</fails_when>
  </verify>
  <acceptance_criteria>
    - The charter todo is under `.planning/todos/completed/` with a `## Resolution` section naming Phase 72 and the three differences from its original mapping table.
    - The new history-gap todo exists under `.planning/todos/pending/` with `kind: design`, a non-empty `decision_needed:`, an `owner:` and a `files:` list.
    - `scripts/todo_triage.py --check` exits 0.
    - The five "Reviewed Todos (not folded)" named in CONTEXT.md are still in `.planning/todos/pending/`.
  </acceptance_criteria>
  <done>The charter this phase was built from is retired with its resolution, and the one gap it leaves is a triaged, decidable question rather than an untracked residual.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the plugin as a visible new version, with the alias flip called out</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    - operator-claude-plugin/.claude-plugin/plugin.json (the current `"version": "0.48.0"`)
    - operator-claude-plugin/CHANGELOG.md (the most recent two sections — the heading format, the grouping, the level of operator-facing detail)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-03-SUMMARY.md (what actually changed in `preingest.py` and `held_queue.py`)
    - Project memory notes `plugin-release-requires-version-bump` and `plugin-install-traps`
  </read_first>
  <action>
Bump `operator-claude-plugin/.claude-plugin/plugin.json`'s `version` from `0.48.0` to `0.49.0`
— a minor bump: the plugin gains behaviour (widened merge, create-time provider precedence,
richer held rows) without breaking an existing call signature.

Add a matching `## [0.49.0]` section to `operator-claude-plugin/CHANGELOG.md` in the file's own
established format, written for the operator rather than for a reviewer. It must cover, in
plain language:

- Enrichment extras a batch paid for now reach the created contact — mobile, LinkedIn,
  seniority, persona and city/state/country — instead of being dropped before upload.
- **A spreadsheet column headed "Mobile" now lands in HubSpot's Mobile Phone field, not the
  Phone field.** State the previous behaviour explicitly so an operator reading the release
  note understands what changes for their existing template.
- LinkedIn now lands in HubSpot's own LinkedIn field as well as the enrichment one.
- On a brand-new person, a researched value now takes precedence over a differing spreadsheet
  cell for everything except the person's own name, email and company; the spreadsheet value is
  shown in the run report rather than discarded silently.
- A second phone or mobile the waterfall finds is kept in a second slot rather than thrown away.
- A person held for a later sitting now keeps their location, seniority and persona.
- A job title older than its refresh window can now be updated by newer research; a recent one
  is still left alone.

Add a one-line note that this release pairs with a backend workflow deploy, so a operator on
the new plugin against an un-deployed backend sees the plugin-side changes only.
  </action>
  <reversibility rating="reversible">A version string and a changelog section.</reversibility>
  <verify>
    <automated>/usr/bin/grep -c '"version": "0.49.0"' operator-claude-plugin/.claude-plugin/plugin.json</automated>
    <fails_when>the count is 0 — the version was not bumped, so the marketplace shows no Update button</fails_when>
    <automated>/usr/bin/grep -c "^## \[0.49.0\]" operator-claude-plugin/CHANGELOG.md</automated>
    <fails_when>the count is 0 — the bump shipped without a changelog section</fails_when>
    <automated>/usr/bin/grep -A 40 "^## \[0.49.0\]" operator-claude-plugin/CHANGELOG.md | /usr/bin/grep -c -i "mobile"</automated>
    <fails_when>the count is 0 — the `mobile` header behaviour change is not mentioned, so an operator's existing template silently changes meaning</fails_when>
    <automated>.venv/bin/python -c "import json,sys; v=json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version']; print('VERSION',v); sys.exit(0 if v=='0.49.0' else 1)"</automated>
    <fails_when>non-zero exit, or the printed `VERSION` is not 0.49.0</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/.claude-plugin/plugin.json` reads `"version": "0.49.0"`.
    - `operator-claude-plugin/CHANGELOG.md` has a `## [0.49.0]` section covering all seven bullets above.
    - The section states both the old and the new destination for a "Mobile" column.
    - The section notes that a paired backend deploy is required for the full effect.
  </acceptance_criteria>
  <done>The release is visible in the marketplace and an operator reading it knows their "Mobile" column changed destination.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md as-built delta and the D-72-17 gate spec</name>
  <files>CLAUDE.md, docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</files>
  <read_first>
    - CLAUDE.md §4.0, §9, §13.0.1, §13.0.2, §15.0, §17.2.1, §29.1 — every existing "As-built delta" subsection, for the established style: a dated heading, what the surrounding section still says, what the shipped code now does, and the evidence
    - CLAUDE.md §29 (the MVP scope cut, its never-write list, and §29.1's scoped `numberofemployees` exception)
    - CLAUDE.md §17.2 (the promotion rules) and §17.2.1 (the four-conjunct system-correctable clause)
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md (its section numbering and the shape of an existing gate/test entry)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md D-72-17, D-72-21 and the Busteed reference record
    - The SUMMARYs of plans 01 and 05 (the changed-workflow table and the portal probe verdict)
  </read_first>
  <action>
Append an as-built delta subsection to CLAUDE.md in the established style, covering three
things §29 and §17.2 no longer describe correctly:

- §17.2's PROMOTE list gains a real recency arm: a `stale_refreshable` field past its own
  `stale_after_days`, with a newer observation, now promotes rather than returning
  `needs_review`, and §17.2.1's four-conjunct clause is extended to provider sources on exactly
  two fields (`contacts.jobtitle`, `companies.industry`). Name D-72-06/07/08/09, state that
  unknown freshness is still conservative, and state which lane carries the history fetch and
  which does not.
- Overflow slots: `lv_phone_2` and `lv_mobilephone_2` on contacts and `lv_phone_2` on companies
  exist, capped at one per kind by D-72-11, with the tail in provenance and no `_3` ever.
- The widened ingest set: the CSV boundary now carries the full promotable contact set, the
  `mobile` header maps to `mobilephone`, LinkedIn lands in `hs_linkedin_url` as well as
  `lv_linkedin_url`, and the ingest lane now gates against the matched contact's real current
  properties rather than an empty object. Include the operator-visible consequence plan 01
  recorded for `firstname`/`lastname`/`company` on the update path.

Follow §4.0's own discipline: state what the section's tables still say, and that they are the
roadmap rather than an inventory.

Then add a D-72-17 gate section to `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`, written as steps
the operator executes in order. It must specify, in this sequence: (1) create the three slot
properties with `DRY_RUN=false ALLOW_HUBSPOT_PROPERTY_WRITES=true .venv/bin/python
scripts/sync_hubspot_properties.py` and read the three back; (2) regenerate, then deploy and
bounce DISARMED every cloud workflow whose body changed per plan 01's table (D-72-21 supersedes
D-72-18's ingest-only wording — the shared engines make a diff-clean non-ingest body
impossible), reading node counts and `settings.executionOrder: "v1"` back; (3) run one absent
person at a company HubSpot already holds through `enrich-before-ingest`, answer `create all 1`
under a single armed window, then disarm and confirm all `ALLOW_*` read false; (4) re-read the
created contact and assert `mobilephone`, `hs_linkedin_url` AND `lv_linkedin_url`, and the geo
fields; (5) run one UPDATE row against a contact holding a non-blank `phone` and confirm it was
NOT overwritten, and check the `firstname` consequence from plan 01; (6) record how
`hs_additional_emails` serialised if it was written at all; (7) hand-delete the created contact.
State explicitly that arming needs `ALLOW_N8N_ARM=true`, that the bounce script exits 1 while a
workflow is armed, and that nothing may be armed before step 3 or left armed after it.
  </action>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <verify>
    <automated>/usr/bin/grep -c "D-72-06\|D-72-11\|D-72-03" CLAUDE.md</automated>
    <fails_when>the count is 0 — the as-built delta does not cite the decisions it records</fails_when>
    <automated>/usr/bin/grep -c "As-built delta" CLAUDE.md</automated>
    <fails_when>the count is fewer than 8 — an existing as-built delta subsection was removed rather than a new one appended</fails_when>
    <automated>/usr/bin/grep -c "D-72-17\|ALLOW_HUBSPOT_PROPERTY_WRITES\|lv_mobilephone_2" docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</automated>
    <fails_when>the count is 0 — the gate spec is absent or does not name the property-creation step</fails_when>
    <automated>/usr/bin/grep -c "hand-delete\|hand delete" docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</automated>
    <fails_when>the count is 0 — the clean-up step the gate depends on is unspecified</fails_when>
    <automated>git diff --stat "$(git log --format=%H -1 -- .planning/phases/72-enrichment-extras-land-in-hubspot/72-06-SUMMARY.md)"..HEAD -- src/ n8n/ scripts/ config/ operator-claude-plugin/scripts/</automated>
    <fails_when>the output is non-empty — every commit since plan 06 sealed must be documentation only; this plan touches no code</fails_when>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md gains one new "As-built delta" subsection citing D-72-03, D-72-06..09 and D-72-11, and every pre-existing as-built subsection is intact.
    - The delta states which lane carries the property-history fetch and which does not.
    - The delta records the `firstname`/`lastname`/`company` update-path consequence.
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` gains a D-72-17 gate section with the seven ordered steps, the two env flags named, and the arm/disarm rule stated.
    - The commit range since plan 06's SUMMARY landed introduces no change under `src/`, `n8n/`, `scripts/`, `config/` or `operator-claude-plugin/scripts/`.
  </acceptance_criteria>
  <done>The repo's own contract document describes the shipped code, and the operator has an ordered gate script to run rather than eight plan files to reconstruct one from.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → operator action | `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` is executed by a human against a live portal |
| plugin release → operator machine | a version bump changes what runs in the operator's next session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-23 | Tampering | the gate spec's arm/disarm instructions | high | mitigate | An incomplete gate spec is how an armed window gets left open. The spec must name `ALLOW_N8N_ARM`, state that the bounce script exits 1 while armed, and require a post-send disarm confirmation reading all `ALLOW_*` false — Task 3's acceptance criteria pin all three. |
| T-72-24 | Repudiation | a stale CLAUDE.md | medium | mitigate | §29's never-write list and §17.2's promotion rules become false with this phase's code; a later phase's research reading them would form a wrong premise (the exact failure §4.0's own delta discipline exists to prevent). The delta subsection is the control. |
| T-72-25 | Elevation of Privilege | the property-creation step in the gate spec | critical | mitigate | The spec names the two-key gate (`DRY_RUN=false` AND `ALLOW_HUBSPOT_PROPERTY_WRITES=true`) explicitly and never `ALLOW_N8N_ARM`, keeping the HubSpot schema privilege boundary distinct from the n8n arming one. |
| T-72-26 | Information Disclosure | CHANGELOG and gate spec content | low | accept | Both describe property names and workflow behaviour already documented in this repo; neither carries a token, a portal id or record data. Accepted. |
</threat_model>

<verification>
- `.venv/bin/python scripts/todo_triage.py --check` exits 0.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` green.
- `node --test tests/n8n/*.test.mjs` green (unchanged by this plan).
- The commit range since plan 06's SUMMARY landed introduces no change under `src/`, `n8n/`, `scripts/`, `config/` or `operator-claude-plugin/scripts/`.
</verification>

<success_criteria>
The charter todo is retired with its resolution, the one gap is a decidable question, the
plugin is a visible new release whose notes name the `mobile` behaviour change, CLAUDE.md
describes the code that actually ships, and the operator has an ordered gate script.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 72 (exclude from drift verification — none of these exist yet):

- **HubSpot custom properties:** `lv_phone_2` (contacts), `lv_mobilephone_2` (contacts), `lv_phone_2` (companies)
- **`config/column_mapping.yaml` new canonical alias targets:** `mobilephone`, `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`, `lv_persona_group`
- **`config/field_policy.yaml` new keys:** contacts `hs_linkedin_url`, `lv_phone_2`, `lv_mobilephone_2`, conditionally `hs_additional_emails`; companies `state`, `hs_state_code`, conditionally `hs_country_region_code`, `lv_phone_2`, conditionally `hs_additional_domains`; plus `system_correctable_sources` on `contacts.jobtitle` and `companies.industry`
- **`config/hubspot_properties.yaml` new declarations:** the three `_2` slot properties above
- **JS engine symbols:** `opts.now`, `opts.historyByField`, `_isStale()`, `_isSystemCorrectable()` (new in `mergeContacts.js`), `_overflowSlot()`; new company geo/phone producers in `n8n/code/normalizeProviders.js`
- **Python symbols:** `now=` kwarg, `_is_stale()`, `_is_system_correctable()`, `_overflow_slot()` in `src/merge_policy.py`; `create_row_ids=` kwarg, `IDENTITY_FIELDS`, `PROVIDER_KEY_ALIASES` in `operator-claude-plugin/scripts/preingest.py`
- **New n8n nodes:** `HubSpot Contact History`, its carry Merge and its sentinel gate (ingest lane)
- **New planning artifacts:** `.planning/phases/72-enrichment-extras-land-in-hubspot/72-PORTAL-PROBE.json`; `.planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md`
- **New test files:** `tests/n8n/ingestWidenedFieldsFlow.test.mjs`, `tests/n8n/widenedKeyParity.test.mjs`, `tests/n8n/mergeRecencyGate.test.mjs`, `tests/n8n/contactHistoryFlow.test.mjs`, `tests/n8n/overflowSlots.test.mjs`
- **New docs:** the Phase 72 as-built delta in `CLAUDE.md`; the D-72-17 gate section in `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`; plugin `0.49.0` in `operator-claude-plugin/CHANGELOG.md`

<output>
Create `.planning/phases/72-enrichment-extras-land-in-hubspot/72-07-SUMMARY.md` when done.
</output>
