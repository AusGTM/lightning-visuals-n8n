---
context: phase
phase: 72-enrichment-extras-land-in-hubspot
task: null
total_tasks: null
status: paused
last_updated: 2026-09-15T10:22:08.296Z
---
# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

- **[blocking] Never arm n8n writes from Claude.** The auto-mode classifier hard-denies the plugin's inline arming step ("Blind Apply"); the operator runs the runner via `!`. Deploy/bounce are the operator's commands too (`DRY_RUN=false ALLOW_N8N_DEPLOY=true`, else the deploy script only prints a table).
- **[blocking] Clean tree before spawning any agent that commits.** The background debugger swept uncommitted edits into its commit and then reverted them (`2b6840f0`/`f24848ce`/`2906445d`); three noise commits and an `index.lock` collision. Memory `subagent-shares-working-tree`.
- **[blocking] Trust runData over the plugin report and over static reasoning.** The plugin's run report cannot see enrich/update outcomes (F-B5); the debugger's static claim about ack ordering was contradicted by node start times in `12429`; the plugin session's "fix not live" note was contradicted by `12454`. Read `Decide*` / `HubSpot Create` / `HubSpot Update` node outputs, never a Webhook Trigger block (embeds the secret).
- **[advisory] `.env` is permission-blocked.** Every token-bearing command is handed to the operator as `! set -a; . ./.env; set +a; …`. Plugin-side reads use `operator.local.json` via `config_gate.load_config()`.
- **[advisory] `git push` from this session returns 403 intermittently.** Commit, then hand `! git push origin master` when it fails.

# Where we are

Phase 72 is complete, verified, security-audited; plugin 0.49.1 released. The work in flight is **GA readiness of the operator plugin + backend**, driven by the stress test in `tests/stress-tests/` (RUNBOOK.md, SESSION-2026-09-15.md, uat_reset.py) and the standalone demo kit in `tests/demo-run/`.

Two full stress attempts ran 2026-09-15. Attempt 2 (all six stages, by the plugin session): B, C, D, F pass; A fails on F-A6; E partial on F-E1. Portal reset to pre-UAT set (35 deletes, 0 pre-existing touched). Backend disarmed PASS at 10:19Z.

# Findings that gate GA (all on the session sheet, with execution ids)

| Id | Sev | What |
| --- | --- | --- |
| F-A6 | blocker | ingest `HubSpot Create` has no continue-on-error; one 409 duplicate aborts the batch, discards output, skips associations; creates land orphaned (exec `12454`) |
| F-A5 | high | duplicate rows within a CSV not collapsed before create (the F-A6 trigger) |
| F-E1 | high | review approve sends array candidates raw (`lv_content_type: ['unknown']`) → HubSpot 400 (exec `12502`) |
| F-B7 | high | `domain EQ bare` misses `www.`-stored portal records → duplicate companies (RV, Wyong, Canberra) |
| F-B2 | high | name + TLD variants duplicate (Perth Racing) — partial mitigation only |
| F-B3 | medium | freemail accepted as company domain (gmail company created) |
| F-B5 | medium-high | plugin run report cannot see enrich/update outcomes |
| F-A3r | medium | 250 ms throttle leaves no headroom (1 residual 429 in 144 searches) |
| F-A1/A2/B1/B6 | low-medium | cost envelope wrong lane rates/execution model; provider balances unreadable |

Fixed and live: F-A3 (throttle), F-A4 (lookup-failed reason) — commits `248754c6`, `2b6840f0`, deployed 05:58Z.

# Next action

`/gsd-quick` (or a small phase) for the fix list above, in this order: F-A6 + F-A5 (ingest, builder), F-E1 (review lane), F-B7 + F-B3 (companies branch / plugin domain clean), F-B5 (plugin report), F-A3r, envelope. Regenerate JSON, suites green (4952 py / 1170 node at HEAD), operator deploys + bounces disarmed, then reset + attempt 3 per RUNBOOK.md "Restart procedure". Then clean-machine install test, then the real-data demo recording.

Also: `/gsd-debug continue ingest-search-429-rate-limit` to archive (note F-A3r), and F-B6 decision (provider keys on backend status, or accept unknown).

# Files to read first

- `tests/stress-tests/SESSION-2026-09-15.md` — every stage, id, and finding
- `tests/stress-tests/RUNBOOK.md` — procedure incl. restart and stop conditions
- `.planning/debug/ingest-search-429-rate-limit.md` — the fix's reasoning and the ack-timing evidence
- `scripts/build_cloud_workflows.py` — `_http_node(batch_interval_ms=…)`, `MERGE_CONTACTS`, `DECIDE_CLOUD`, companies `HubSpot Company Search` body
