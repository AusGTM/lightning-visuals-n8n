---
phase: 72-enrichment-extras-land-in-hubspot
plan: 12
type: execute
wave: 4
depends_on: ["72-09", "72-10", "72-11"]
gap_closure: true
autonomous: false
files_modified:
  - .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md
  - CLAUDE.md
  - operator-claude-plugin/.claude-plugin/plugin.json
requirements: [D-72-04, D-72-17, D-72-21, D-72-01, D-72-06, D-72-09]

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Regeneration is idempotent: running scripts/build_cloud_workflows.py from the committed tree produces zero git diff, so committed == generated for all eight workflows."
    - "D-72-04 / D-72-17 observed live: a newly CREATED contact carries BOTH lv_linkedin_url and hs_linkedin_url, read back from HubSpot after the write."
    - "Every cloud workflow whose JSON changed is deployed and bounced, and reads back disarmed: node counts match committed, settings.executionOrder is v1, every ALLOW_* write flag is false."
  artifacts:
    - ".planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md — Test 3 recording the gap-closure read-back"
    - "CLAUDE.md — the two F72-1 passages that recorded the defect as unresolved, updated to observed-closed"
  key_links:
    - "committed n8n/wf_*_cloud.json -> scripts/deploy_n8n_workflows.py PUT -> scripts/bounce_n8n_workflows.py reload -> the running n8n Cloud instance that produced the read-back"
---

<objective>
Seal the three gap closures: prove the generator is idempotent and both suites are green, then
take the one live gate that can upgrade F72-1 from an open defect to observed-closed — a
D-72-17-shaped CREATE on one record, read back showing BOTH `lv_linkedin_url` and
`hs_linkedin_url` populated — and only then edit the two CLAUDE.md passages that record the
defect as open.

The live redeploy and the one armed send are the only one-way doors in this gap closure; they are
the blocking-human checkpoint in Task 2. Everything before it is reversible and automated.

Purpose: a code fix that has not been observed on the deployed instance is not a closed gap in
this repo's own discipline — F72-1 was found by a live gate precisely because the suite was green.
Output: a green, idempotent tree; `72-UAT.md` Test 3; CLAUDE.md telling the truth about F72-1.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: idempotent regeneration, both suites, config copies, todo triage, plugin-version check</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json</files>
  <read_first>
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-09-SUMMARY.md, 72-10-SUMMARY.md,
      72-11-SUMMARY.md (what changed, and each plan's recorded node counts)
    - CLAUDE.md §31 (todo triage rules — a batch never ends with an untriaged residual)
    - operator-claude-plugin/.claude-plugin/plugin.json (current version, for the bump check)
  </read_first>
  <action>
    Pre-flight the live gate. No production code changes in this task — if any check below fails,
    STOP and report rather than patching here; the failure belongs to whichever gap plan owns it.

    1. Regenerate from the committed tree and prove idempotency: `scripts/build_cloud_workflows.py`
       must leave `n8n/` with no git diff.
    2. Record the eight node counts and compare against the values plans 09-11 asserted
       (78 / 13 / 287 / 10 / 82 / 55 / 43 / 30 for contact_ingest_cloud, contact_ingest_local,
       enrichment_cloud, enrichment_local, enrichment_local_live, review_decision_cloud,
       scheduled_maintenance_cloud, backend_status_cloud). All eight must be unchanged from the
       live values in 72-UAT.md Task 1 — this whole gap closure moves jsCode strings and one
       properties list, never a node.
    3. Confirm the two plugin config copies are still byte-identical to their repo originals:
       `config/field_policy.yaml` vs `operator-claude-plugin/config/field_policy.yaml`, and the
       same pair for `column_mapping.yaml`.
    4. Plugin version: list the files changed by plans 09-11 under `operator-claude-plugin/`.
       Expected: none — G1's fix is in `scripts/build_cloud_workflows.py`, G2's in the same file
       plus a test, G3's in `n8n/code/` + `src/` + tests. Bump
       `operator-claude-plugin/.claude-plugin/plugin.json` and add a CHANGELOG entry ONLY if that
       list is non-empty; if it is empty, record "no plugin change, no bump" in the SUMMARY and
       leave both files alone.
    5. Both full suites green, and `scripts/todo_triage.py --check` exits 0. If a gap plan left a
       new pending todo, triage it now per §31 (fix / keep as question-or-design with its trigger
       named / accept into `completed/`) rather than letting the batch close untriaged.

    Determine and record, for Task 2's operator brief, the exact list of CLOUD workflows whose
    JSON changed across plans 09-11 (expected: `wf_contact_ingest_cloud.json`,
    `wf_enrichment_cloud.json`, `wf_review_decision_cloud.json`,
    `wf_scheduled_maintenance_cloud.json`; `wf_backend_status_cloud.json` unchanged;
    `wf_enrichment_local_live.json` changed but is NOT deployed by the deploy script).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python scripts/build_cloud_workflows.py && git status --porcelain n8n/` produces EMPTY output
    - All eight node counts equal 78 / 13 / 287 / 10 / 82 / 55 / 43 / 30 respectively
    - `diff config/field_policy.yaml operator-claude-plugin/config/field_policy.yaml` and the `column_mapping.yaml` equivalent both produce empty output
    - `node --test tests/n8n/*.test.mjs` exits 0
    - `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` exits 0
    - `scripts/todo_triage.py --check` exits 0
    - The SUMMARY states, explicitly, whether any file under `operator-claude-plugin/` changed and therefore whether the plugin version was bumped
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && test -z "$(git status --porcelain n8n/)"</automated>
    <fails_when>exit 1 with a non-empty `git status --porcelain n8n/` — regeneration is not idempotent, so a committed workflow was hand-edited or a generator change was never regenerated</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -c 'import json;c={f:len(json.load(open("n8n/wf_%s.json"%f))["nodes"]) for f in ["contact_ingest_cloud","contact_ingest_local","enrichment_cloud","enrichment_local","enrichment_local_live","review_decision_cloud","scheduled_maintenance_cloud","backend_status_cloud"]};e={"contact_ingest_cloud":78,"contact_ingest_local":13,"enrichment_cloud":287,"enrichment_local":10,"enrichment_local_live":82,"review_decision_cloud":55,"scheduled_maintenance_cloud":43,"backend_status_cloud":30};assert c==e,(c,e);print("counts ok")'</automated>
    <fails_when>AssertionError printing the two dicts — a node was added or removed somewhere in plans 09-11, which invalidates the unchanged-node-count read-back Task 2 depends on</fails_when>
  </verify>
  <verify>
    <automated>diff config/field_policy.yaml operator-claude-plugin/config/field_policy.yaml && diff config/column_mapping.yaml operator-claude-plugin/config/column_mapping.yaml</automated>
    <fails_when>non-zero exit printing differing lines — the plugin's shipped config copy drifted from the repo original and the plugin would ship a different policy than the lane enforces</fails_when>
  </verify>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit; the node suite is below its green baseline</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit; the python suite is below its 4947-passed baseline</fails_when>
  </verify>
  <verify>
    <automated>scripts/todo_triage.py --check</automated>
    <fails_when>non-zero exit naming an untriaged pending todo — CLAUDE.md §31's zero-inbox rule is unmet</fails_when>
  </verify>
  <done>The tree is green, idempotent, and level; the deploy scope for the live gate is known and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: OPERATOR — deploy + bounce disarmed, then one armed CREATE, read back both LinkedIn properties, hand-delete</name>
  <files>n8n/wf_contact_ingest_cloud.json, n8n/wf_enrichment_cloud.json, n8n/wf_review_decision_cloud.json, n8n/wf_scheduled_maintenance_cloud.json</files>
  <what-built>
    Three gap closures are committed and green offline: G1 (72-09) makes a `linkedin_url` on an
    ingest CREATE row land as BOTH `lv_linkedin_url` and `hs_linkedin_url`; G2 (72-10) widens the
    local-live lane's two existing-record fetch lists so its non-clobber guard stops reading a real
    value as blank; G3 (72-11) makes the overflow-slot dedup case-insensitive in all three merge
    engines. Four CLOUD workflow JSONs changed and are committed but NOT yet deployed:
    `wf_contact_ingest_cloud`, `wf_enrichment_cloud`, `wf_review_decision_cloud`,
    `wf_scheduled_maintenance_cloud`. `wf_backend_status_cloud` is unchanged.
    `wf_enrichment_local_live.json` also changed but is NOT deployed by `deploy_n8n_workflows.py`
    (it globs `wf_*_cloud.json` only, L280) — it is the docker-replica lane, committed not pushed.
    Regeneration is idempotent and both suites are green (Task 1).
  </what-built>
  <how-to-verify>
    This is the only step in the gap closure that touches the live instance — a redeploy and one
    armed CREATE are both one-way; nothing before this task is.

    **Step 1 — deploy + bounce, DISARMED.**
    Run `scripts/deploy_n8n_workflows.py` with `DRY_RUN=false ALLOW_N8N_DEPLOY=true`, then
    `scripts/bounce_n8n_workflows.py`. The deploy plan will list all five cloud workflows; the
    backend-status PUT is a no-op re-push, same as 72-UAT.md Task 1. Do NOT set `ALLOW_N8N_ARM`
    in this step.

    Read back and paste, for all five cloud workflows: `id`, `active`, live node count,
    `settings.executionOrder`, and every `ALLOW_*` write-safety constant. Expected node counts,
    UNCHANGED from 72-UAT.md Task 1 because this batch moved jsCode strings only:
    contact ingest 78, enrichment 287, review decision 55, scheduled maintenance 43,
    backend status 30.

    **Step 2 — one armed window, one CREATE.** Same shape as 72-UAT.md Task 2's CREATE row: run
    `enrich-before-ingest` for ONE absent person at a test-record domain whose waterfall is
    expected to return a LinkedIn URL. Arm contacts writes (`ALLOW_N8N_ARM=true`, domain-scoped),
    bounce so the running workflow picks up the armed body (a bare PUT never reloads a running
    workflow), send, then disarm and bounce again. Watch executions for the window and confirm
    zero new executions after the final disarm.

    **Step 3 — read back the created contact** with a direct HubSpot GET (portal 22617666 — see
    F72-4's wrong-portal caveat; use the plugin's portal, not an MCP connector that may point
    elsewhere) and paste `lv_linkedin_url`, `hs_linkedin_url`, and
    `lv_contact_enrichment_provenance`.

    **Step 4 — hand-delete the created contact** (restorable archive), and confirm every `ALLOW_*`
    flag reads `false` after the send.

    **STOP conditions — report instead of proceeding:** any live node count differing from the
    expected value above; any `settings.executionOrder` not `v1`; any `ALLOW_*` flag reading `true`
    after the final disarm; `lv_linkedin_url` still null on the created contact (that would mean
    G1's fix does not hold live and the gap re-opens rather than closes).

    Paste back only the enumerated values — never a raw Webhook Trigger runData block, which
    embeds `x-enrichment-secret` and the caller IP verbatim.
  </how-to-verify>
  <resume-signal>
    Type "approved" plus the paste-back Task 3 needs to transcribe, or describe what failed:
    (a) the five cloud workflows' `id` / `active` / node count / `executionOrder` / `ALLOW_*` flags
        after deploy+bounce;
    (b) the created contact id, and its `lv_linkedin_url` AND `hs_linkedin_url` values (both must be
        non-null — that is the gate);
    (c) the `lv_contact_enrichment_provenance` entry for the LinkedIn field, whose source should be
        the provider name rather than `csv` (the observable consequence of 72-09's `sourceByField`
        aliasing);
    (d) the n8n execution id(s) and the burst-watch list, with zero new executions after the final
        disarm and every `ALLOW_*` flag `false`;
    (e) confirmation the created contact was hand-deleted.
  </resume-signal>
  <done>F72-1's truth is observed live on the deployed instance: a newly created contact carries both LinkedIn properties.</done>
</task>

<task type="auto">
  <name>Task 3: record UAT Test 3 and update both CLAUDE.md F72-1 passages to observed-closed</name>
  <files>.planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md (Task 2's table format — Test 3 mirrors it)
    - CLAUDE.md lines ~2698-2710 (the §13.0.2 deployment-state passage whose final clause marks F72-1 as still open and awaiting this batch)
    - CLAUDE.md lines ~3358-3378 (the §17.2.2 passage that root-causes F72-1 and closes by deferring the claim to a later closure plan)
    - Task 2's operator paste-back (execution ids, contact id, both property values, node counts)
  </read_first>
  <action>
    Transcribe only what the operator actually pasted — no inference, no rounding, and record any
    item the operator did not report as an honest gap the way Task 1 of the original gate did.

    1. Append `## Test 3 — gap closure live read-back` to
       `72-UAT.md`, in the same shape as Task 2: the deploy/bounce read-back table, the created
       contact's field table (at minimum `lv_linkedin_url`, `hs_linkedin_url`,
       `lv_contact_enrichment_provenance`), the burst-watch execution list, the confirmations
       (a)-(e), and the clean-up checklist. Update the file's frontmatter `updated:` and, if the
       read-back passed, note in the findings section that F72-1 is CLOSED with its evidence
       (contact id + execution id), leaving F72-2/F72-3/F72-4 exactly as they stand.
       Also compare `hs_linkedin_url`'s VALUE SHAPE against the original gate's full URL
       (`http://www.linkedin.com/in/jimmybusteed`, 72-UAT.md Task 2): 72-09 makes the CREATE path
       newly promote that property from `row.linkedin_url` at 85, so if the read-back value is a
       bare handle rather than a URL, record it as a new finding F72-5 naming this batch's new
       promotion path — do NOT seal a value-shape regression inside a closed gap.
    2. CLAUDE.md, §13.0.2 passage (~L2704): replace that sentence's closing clause — the one
       marking F72-1 as unresolved and deferred to this batch — with the observed-closed statement:
       D-72-04's dual write is now `[observed live]` on BOTH paths, citing the new contact id and
       execution id and the gap-closure plans (72-09..72-12). Leave no wording anywhere in the file
       that still describes this defect as awaiting closure.
    3. CLAUDE.md, §17.2.2 passage (~L3358-3378): the paragraph currently root-causes the defect and
       ends by deferring the claim to a later closure plan. Keep the
       root-cause narrative (it is the historical record of how the key-vocabulary mismatch
       happened) and replace that deferral sentence with the closure: what the fix was (one `CANDIDATE_ALIASES` map in
       `MERGE_CONTACTS` read by both the confidence/source derivation and the candidate builder),
       when it was observed live, and on which contact/execution. Add, in the same amendment, the
       two sibling closures this batch landed — the local-live fetch widening (CR-01) and the
       overflow-dedup case parity (WR-02) — so §17.2.2 describes the as-built state rather than the
       state at the end of plan 08.
    4. Re-run both suites and `scripts/todo_triage.py --check` after the doc edits (a CLAUDE.md
       edit can break a doc-contract test in this repo).

    If Task 2's read-back did NOT show both properties, do NOT edit either CLAUDE.md passage:
    record the failure in `72-UAT.md` Test 3 and stop — an unclosed gap must keep saying so.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c "Test 3" .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md` returns at least `1`
    - `/usr/bin/grep -c "gap-closure pending" CLAUDE.md` returns `0`
    - `/usr/bin/grep -c "CANDIDATE_ALIASES" CLAUDE.md` returns at least `1` (the fix is named where the defect was described)
    - `/usr/bin/grep -c "observed live" CLAUDE.md` increases relative to its pre-task value (the §17.2.2 amendment carries its own evidence tag)
    - The created contact id and n8n execution id from Task 2 appear in both `72-UAT.md` and CLAUDE.md
    - `node --test tests/n8n/*.test.mjs`, `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` and `scripts/todo_triage.py --check` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c 'gap-closure pending' CLAUDE.md)" = "0"</automated>
    <fails_when>exit 1 — CLAUDE.md still records F72-1 as open after a passing live read-back, so the project document contradicts the observed state</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit — a doc-contract test broke on the CLAUDE.md edit</fails_when>
  </verify>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs && scripts/todo_triage.py --check</automated>
    <fails_when>non-zero exit — the node suite regressed or an untriaged pending todo remains at batch close</fails_when>
  </verify>
  <done>The live evidence is recorded in 72-UAT.md Test 3 and CLAUDE.md describes the as-built state of all three closed gaps.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where |
|---|---|---|
| `Test 3 — gap closure live read-back` | new UAT section | `.planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md` |
| F72-1 observed-closed amendment (x2 passages) | changed project doc | `CLAUDE.md` §13.0.2, §17.2.2 |
| deploy/bounce read-back + burst watch | operator evidence, transcribed | `72-UAT.md` Test 3 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local committed JSON -> n8n Cloud instance | a PUT replaces the body of a running, credential-bound workflow |
| armed n8n workflow -> HubSpot CRM | a real record is created in the production portal |
| operator paste-back -> project documentation | live evidence becomes the repo's record of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-12-01 | Tampering | live HubSpot data during the armed window | critical | mitigate | Exactly one record, domain-scoped arming, arm -> bounce -> send -> disarm -> bounce, burst watch for zero post-disarm executions, every `ALLOW_*` flag read back `false`, and the created contact hand-deleted — the identical discipline 72-UAT.md Task 2 already executed. |
| T-72-12-02 | Denial of Service | redeploy of a running workflow | high | mitigate | Deploy and bounce DISARMED first, with node counts / `executionOrder` / write flags read back before anything is armed; explicit STOP conditions on any mismatch. The Phase 70 runaway precedent (§13.0.3) is why the disarmed proof precedes the armed send. |
| T-72-12-03 | Repudiation | UAT/CLAUDE.md transcription | medium | mitigate | Task 3 transcribes only pasted values, records unreported items as honest gaps, and is forbidden from editing either CLAUDE.md passage unless the read-back actually passed. |
| T-72-12-04 | Information Disclosure | pasted execution runData | medium | mitigate | Paste only the enumerated fields (ids, property values, flags, counts); never a raw Webhook Trigger runData block, which embeds `x-enrichment-secret` and the caller IP verbatim. |
| T-72-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package; the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
- Regeneration idempotent, all eight node counts unchanged, both config copies identical
- Both full suites and `scripts/todo_triage.py --check` green before AND after the doc edits
- Operator read-back shows both LinkedIn properties on a newly created contact, and all five cloud workflows disarmed at v1
</verification>

<success_criteria>
- All three gap truths are demonstrable: two offline (G2, G3), one live (G1)
- CLAUDE.md no longer claims F72-1 is open
- No file under `operator-claude-plugin/` changed without a matching version bump
</success_criteria>

<output>
Create `.planning/phases/72-enrichment-extras-land-in-hubspot/72-12-SUMMARY.md` when done.
</output>

<!-- planner-discipline-allow: gap-closure pending -->
