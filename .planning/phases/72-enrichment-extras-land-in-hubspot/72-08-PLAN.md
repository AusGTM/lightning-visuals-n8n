---
phase: 72-enrichment-extras-land-in-hubspot
plan: 08
type: execute
wave: 7
depends_on: ["72-07"]
files_modified:
  - .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md
  - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
  - CLAUDE.md
autonomous: false
requirements: [D-72-11, D-72-17, D-72-18, D-72-21, D-72-10]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

user_setup:
  - service: hubspot
    why: "Create the three overflow-slot properties and perform the one armed create + read-back"
    env_vars:
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: "HubSpot -> Settings -> Integrations -> Private Apps -> the LV enrichment app -> Auth"
      - name: ALLOW_HUBSPOT_PROPERTY_WRITES
        source: "Set to true for the property-creation step only; never reuse ALLOW_N8N_ARM for it"
    dashboard_config:
      - task: "Confirm the three new properties appear, then hand-delete the test contact after the gate"
        location: "HubSpot -> Settings -> Properties -> Contacts / Companies; and the contact record itself"
  - service: n8n
    why: "Deploy + bounce every regenerated cloud workflow disarmed, then arm one window for one record"
    env_vars:
      - name: N8N_API_KEY
        source: "n8n Cloud -> Settings -> n8n API"
      - name: ALLOW_N8N_ARM
        source: "Set to true only for the single armed send; the bounce script exits 1 while a workflow is armed"

must_haves:
  truths:
    - "D-72-11: the three overflow-slot properties (`lv_phone_2` and `lv_mobilephone_2` on contacts, `lv_phone_2` on companies) exist on the live portal, created ONCE by `scripts/sync_hubspot_properties.py` under its two-key gate, and read back by a GET before any send."
    - "D-72-21 (superseding D-72-18): every cloud workflow whose regenerated body changed is deployed and bounced DISARMED, node counts and `settings.executionOrder: \"v1\"` read back, so committed and live are level. A byte-identical body is not redeployed."
    - "D-72-17: one absent person at a company HubSpot already holds is run through `enrich-before-ingest` and created with `create all 1`, and the re-read contact shows `mobilephone`, BOTH `hs_linkedin_url` and `lv_linkedin_url`, and the geo fields the waterfall returned — the exact fields F71-5 recorded as dropped for Busteed `352422766048`."
    - "D-72-17 / SAFE-01: one UPDATE row against a contact holding a non-blank `fill_blank_only` field proves that field was NOT overwritten — the assertion the pre-72 lane could not have passed at all, because it merged against an empty existing-props object."
    - "D-72-10: how `hs_additional_emails` actually serialises on write (or that it was not written at all) is observed and recorded in the SUMMARY, closing the one Claude's-discretion item CONTEXT.md left open for live verification."
    - "Nothing was armed before this plan, exactly one window was armed during it, and every `ALLOW_*` flag reads false afterwards; the created contact is hand-deleted."
  artifacts:
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md
  key_links:
    - "Property creation must precede the first armed send: a PATCH naming a property that does not exist is dropped by HubSpot without an error, which would read as a code defect at the read-back."
    - "`scripts/deploy_n8n_workflows.py` has an `--only` flag; `scripts/bounce_n8n_workflows.py` bounces all five unconditionally and is left that way (D-72-21 deploys every changed body, so a scoped bounce buys nothing)."
    - "Plan 01 Task 3's SUMMARY table of changed workflow bodies is the deploy set; plan 07's `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` section is the ordered script the operator follows."
  prohibitions:
    - "Nothing may be armed before Task 2's checkpoint, and nothing may be left armed after it. `scripts/bounce_n8n_workflows.py` exits 1 while a workflow is armed — that non-zero exit is the disarm check, not a failure to work around."
    - "Never reuse `ALLOW_N8N_ARM` to authorise the HubSpot property creation, and never reuse `ALLOW_HUBSPOT_PROPERTY_WRITES` to authorise a record write — two distinct privilege boundaries."
    - "Exactly ONE record is created by the armed send (D-72-17). No batch, no second row, no unattended run — the first live unattended credit-spending batch remains a separate, ungranted question."
    - "No code, config or workflow-JSON edit in this plan. If the gate finds a defect, it is recorded as a finding and closed by a gap-closure plan, never patched inside the gate."
    - "The created contact is hand-deleted after the read-back; the gate leaves no test record behind."
    - "Never hand-edit any `n8n/wf_*.json`; if a regenerate is needed the generator produces it."
---

<objective>
Run the phase's one live gate, in the one order that makes it meaningful: create the properties
the code writes to, level the live workflows with the committed ones disarmed, then arm exactly
one window for exactly one record and read the contact back.

Purpose: every claim this phase makes is offline until now. The tests walk a committed JSON with
stubbed HTTP; they cannot show that HubSpot accepts `lv_mobilephone_2`, that
`hs_additional_emails` takes a write at all, or that a real created contact carries a LinkedIn
URL in both fields. F71-5 is the reference: Busteed `352422766048` landed with email, phone and
title while his held row carried a mobile and a LinkedIn URL. An equivalent create must now show
both.

The sequence is not interchangeable. HubSpot silently ignores a PATCH property that does not
exist — no error, no warning, just an absent value on re-read. Creating the three slot
properties before the send is what makes a missing value at step four mean "the code is wrong"
rather than "the property was never there." Deploying disarmed before arming is what keeps the
armed window as short as one send.

Output: `72-UAT.md` with each read-back recorded, the `hs_additional_emails` serialisation
answer, any findings, and CLAUDE.md's as-built delta upgraded from documented to observed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-CONTEXT.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-01-SUMMARY.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-05-SUMMARY.md
@.planning/phases/72-enrichment-extras-land-in-hubspot/72-07-SUMMARY.md
@docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Create the three slot properties, then level live with committed — all disarmed</name>
  <precondition>Every prior plan in this phase is committed and both full test suites are green. `git status` is clean. No n8n workflow is armed: every `ALLOW_*` write-safety constant in every live body reads `"false"`.</precondition>
  <read_first>
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md — the D-72-17 gate section plan 07 wrote (this checkpoint executes its steps 1 and 2)
    - scripts/sync_hubspot_properties.py (the two-key gate and the CLI flags)
    - scripts/deploy_n8n_workflows.py (the `--only` flag) and scripts/bounce_n8n_workflows.py (bounces all five; exits 1 while armed)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-01-SUMMARY.md (the changed-workflow table = the deploy set)
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-PORTAL-PROBE.json (plan 05's verdicts)
    - CLAUDE.md §13.0.2 (the committed-vs-live history and the last recorded live node counts) and §13.0.3
    - Project memory notes `n8n-stored-vs-running-content` (a bare PUT never reloads a running workflow — bounce after every deploy) and `n8n-deploy-diff-noise`
  </read_first>
  <what-built>
Plans 01-07 shipped, committed and green: the widened ingest lane, the recency gate, the
overflow-slot routing, the company producers, and the three slot-property DECLARATIONS in
`config/hubspot_properties.yaml`. Nothing has been created on the portal and nothing has been
deployed to n8n Cloud — the committed workflow bodies are ahead of the live instance by the
whole of Phase 72. This checkpoint is the operator levelling the live surface, disarmed.
  </what-built>
  <how-to-verify>
Two live operations, both disarmed, in this order.

**Step 1 — create the properties.** Run `.venv/bin/python scripts/sync_hubspot_properties.py`
in its default dry run first and confirm it reports exactly the three new properties as
pending. Then run it once with `DRY_RUN=false ALLOW_HUBSPOT_PROPERTY_WRITES=true` and confirm a
per-property success status. Then read all three back with `GET
/crm/v3/properties/contacts/lv_phone_2`, `GET /crm/v3/properties/contacts/lv_mobilephone_2` and
`GET /crm/v3/properties/companies/lv_phone_2`, recording each one's `type`, `fieldType` and
`modificationMetadata.readOnlyValue`. Do not set `ALLOW_N8N_ARM` for this step; it is a
different privilege boundary and has no role here.

**Step 2 — level live with committed, disarmed.** Regenerate with `.venv/bin/python
scripts/build_cloud_workflows.py` and confirm no diff (the tree should already be level after
plan 06). Deploy every cloud workflow named in plan 01 Task 3's changed-body table, using
`scripts/deploy_n8n_workflows.py`; do not deploy a workflow whose body is byte-identical to
what is live. Then run `scripts/bounce_n8n_workflows.py` — a bare PUT does not reload a running
workflow, so the bounce is mechanically required after every deploy. Leave the bounce script
bouncing all five: D-72-21 deploys every changed body precisely because the shared merge
engines make a diff-clean non-ingest body impossible, so a scoped bounce would buy nothing.

Read back, per deployed workflow: `active`, the node count, `settings.executionOrder`, and that
every write-safety constant still reads `"false"`. Compare each node count against plan 01's
table and against CLAUDE.md §13.0.2's last recorded live counts. Ignore credentials, `webhookId`
and `executeWorkflow` placeholder differences — those always differ and are not signal.

Record every observation in `.planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md`
as you go, not afterwards. If any step refuses, stop and record the refusal; do not work around
it and do not proceed to Task 2.

Confirm before resuming: (a) the three new properties are visible in HubSpot's property
settings and each read-back returned `type: string`, `fieldType: text`, `readOnlyValue: false`;
(b) every deployed workflow reports `active: true`, a node count matching plan 01's table,
`settings.executionOrder: "v1"`, and every write-safety constant `"false"`; (c) no workflow is
armed.
  </how-to-verify>
  <resume-signal>Reply "approved" once all three confirmations hold, or describe what refused — a refusal here stops the gate and is recorded as a finding, not worked around.</resume-signal>
  <reversibility rating="costly">The property creation writes schema to the operator's live portal (archivable and deletable, but the archived name persists in the property list and needs a manual clean-up); the deploy replaces live workflow bodies, whose rollback is the pre-72 bundle plus a redeploy — an operator action, not a git revert.</reversibility>
  <acceptance_criteria>
    - `72-UAT.md` records the dry-run output, the create output, and the three property read-backs with their `readOnlyValue`.
    - `72-UAT.md` records, per deployed workflow: name, node count before and after, `active`, `settings.executionOrder`, and the write-safety read-back.
    - No workflow whose committed body is byte-identical to the live one was redeployed.
    - `ALLOW_N8N_ARM` was not set at any point during this task.
    - No file under `src/`, `n8n/`, `scripts/`, `config/` or `operator-claude-plugin/` was edited.
  </acceptance_criteria>
  <done>The three slot properties exist on the portal, every changed workflow body is live and bounced, everything reads v1 and disarmed, and nothing has been written to a record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: One armed window, one record — the read-back F71-5 asked for</name>
  <precondition>Task 1 completed: the three properties exist and read back writable, and every deployed workflow is live, bounced and disarmed. A HubSpot company the portal already holds is chosen, with one person at it who is NOT in HubSpot.</precondition>
  <read_first>
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md — the D-72-17 gate section's steps 3 through 7
    - .planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-UAT.md — F71-5 in full, and Round 2's own arm/dispatch/disarm sequence as the procedure to mirror
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md — the run sequence and the `create all N` ready answer
    - operator-claude-plugin/scripts/n8n_arming.py (`set_write_safety`, `DISPATCH_FLAGS`) — what arming rewrites and what the disarm read-back must show
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-01-SUMMARY.md — the "Operator confirm:" line about `firstname`/`lastname`/`company` on the update path
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-PORTAL-PROBE.json — whether `hs_additional_emails` was expected to be writable at all
  </read_first>
  <what-built>
The three overflow-slot properties exist on the portal and every changed cloud workflow body is
live, bounced and disarmed (Task 1). Every behavioural claim of this phase is still offline: it
rests on walker tests over the committed JSON with stubbed HTTP. This checkpoint is the single
armed window that turns those claims into observations.
  </what-built>
  <how-to-verify>
Run the gate. One armed window, one created record, then disarm.

**The create row.** Run one absent person at a company HubSpot already holds through
`enrich-before-ingest`, let the waterfall run, and answer `create all 1`. Arm exactly one
window (`ALLOW_N8N_ARM=true` plus the record-scoped allowlist the skill asks for), dispatch,
then disarm and confirm every `ALLOW_*` reads false. Re-read the created contact by id and
record the actual value of: `mobilephone`, `hs_linkedin_url`, `lv_linkedin_url`, `city`,
`state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`,
`lv_persona_group`, `lv_phone_2`, `lv_mobilephone_2`, `hs_additional_emails`, and
`lv_contact_enrichment_provenance`. A field the waterfall did not return for this person is a
blank, not a failure — record which fields the waterfall actually supplied, from the run
report, alongside what landed, so the two can be compared. Confirm the contact is associated to
its company (§13.0.1, unchanged by this phase).

**The update row.** In the same sitting, run one row that resolves to an EXISTING HubSpot
contact holding a non-blank `phone`, with a different `phone` value on the row. Confirm on
re-read that the existing `phone` was NOT overwritten — the SAFE-01 assertion that was
unpassable before this phase, because the ingest lane merged against an empty existing-props
object. While you have the record open, check `firstname` too and record whether the
consequence plan 01 flagged (a CSV name correction no longer applying on update) is acceptable
to the operator or needs a follow-up ruling.

**Record and clean up.** Write every observation into `72-UAT.md` as it happens: the run id,
the execution ids, the armed window's open and close, each read-back value, and whether
`hs_additional_emails` was written and in what form (a `;`-joined string, an array, or not at
all) — that is the one Claude's-discretion item CONTEXT.md left for live verification. Then
hand-delete the created contact and confirm the deletion.

If a read-back is wrong, that is a FINDING: record it with its evidence and stop. Do not patch
code inside the gate — a defect here is closed by a gap-closure plan.

Confirm before resuming, from the RE-READ record and never from the send's 200: (a) the created
contact carries `mobilephone`, BOTH `hs_linkedin_url` and `lv_linkedin_url`, and every geo field
the waterfall actually returned; (b) the update row's pre-existing non-blank `phone` is
unchanged; (c) `lv_contact_enrichment_provenance` carries an entry per landed slot; (d) every
`ALLOW_*` flag reads false after the send; (e) the created contact has been hand-deleted.
  </how-to-verify>
  <resume-signal>Reply "approved" once all five confirmations hold, or describe the read-back that was wrong — a wrong value is a finding to record, not something to patch inside the gate.</resume-signal>
  <reversibility rating="costly">Creates one real HubSpot contact (hand-deleted at the end of the task) and spends provider credits on one person's waterfall, which is not recoverable.</reversibility>
  <acceptance_criteria>
    - `72-UAT.md` records the run id, execution ids, and the armed window's open and close times.
    - `72-UAT.md` records, per field, what the waterfall returned and what landed on the created contact.
    - `72-UAT.md` states whether `hs_additional_emails` was written and in exactly what serialisation, or that it was not written and why.
    - The update row's pre-existing `phone` value is recorded before and after and is identical.
    - A post-send read shows every `ALLOW_*` write-safety constant `"false"` on every workflow.
    - The created contact's id is recorded and its deletion confirmed.
    - No file under `src/`, `n8n/`, `scripts/`, `config/` or `operator-claude-plugin/` was edited during the gate.
  </acceptance_criteria>
  <done>A person the waterfall researched arrives in HubSpot carrying the mobile and the LinkedIn URL that were dropped for Busteed, an existing contact's phone survives an update untouched, and the portal is back to its pre-gate state.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict, and upgrade the as-built claims from documented to observed</name>
  <files>.planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md, docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md as Tasks 1 and 2 left it
    - .planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-UAT.md (the findings-table and verdict format to mirror)
    - CLAUDE.md §13.0.3's tagging rule — `[documented]` means published documentation only; `[observed live]` means this repo watched it happen
    - CLAUDE.md's Phase 72 as-built delta as plan 07 wrote it
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md's D-72-17 gate section
  </read_first>
  <action>
Finish `72-UAT.md` in the shape `71-UAT.md` uses: a findings table with an id, a severity, the
finding and its evidence; a clean-up checklist with each item ticked; and a one-line verdict.

Then update CLAUDE.md's Phase 72 as-built delta with what was actually OBSERVED, following
§13.0.3's tagging discipline: a claim this gate watched happen is `[observed live]` with its
record id or execution id; a claim still resting only on offline tests stays as it is. At
minimum, record: the live node counts and `settings.executionOrder` read back per deployed
workflow, so §13.0.2's committed-vs-live table stays true; the three slot properties' live
existence; and how `hs_additional_emails` serialised, or that it is not writable on this portal
and the second email is provenance-only.

Update `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`'s D-72-17 section with anything the run showed
the spec got wrong or left implicit — an operator running it a second time should not
rediscover the same friction.

If Task 2 produced a blocking finding, say so plainly in the verdict, name the gap-closure work
it implies, and do not mark the gate passed.
  </action>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <verify>
    <automated>/usr/bin/grep -c "observed live" CLAUDE.md</automated>
    <fails_when>the count did not increase versus the pre-task value recorded in the SUMMARY — the gate ran but upgraded no claim from documented to observed</fails_when>
    <automated>/usr/bin/grep -c "^## Verdict\|^## Clean-up" .planning/phases/72-enrichment-extras-land-in-hubspot/72-UAT.md</automated>
    <fails_when>the count is fewer than 2 — the UAT record has no verdict or no clean-up checklist</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed" or "error"</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or any "not ok" line in the TAP output</fails_when>
    <automated>git diff --stat "$(git log --format=%H -1 -- .planning/phases/72-enrichment-extras-land-in-hubspot/72-07-SUMMARY.md)"..HEAD -- src/ n8n/ scripts/ config/ operator-claude-plugin/scripts/</automated>
    <fails_when>the output is non-empty — the gate must leave no code, config or workflow-JSON change behind; a defect found at the gate is a finding for a gap-closure plan, never a patch made inside it</fails_when>
  </verify>
  <acceptance_criteria>
    - `72-UAT.md` has a findings table, a ticked clean-up checklist and a one-line verdict.
    - CLAUDE.md's Phase 72 as-built delta carries at least one `[observed live]` claim with a record or execution id, and its live node-count/executionOrder read-backs.
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`'s D-72-17 section reflects any friction the run exposed.
    - Both full suites are green and the commit range since plan 07's SUMMARY landed introduces no change under the code directories.
  </acceptance_criteria>
  <done>The gate's result is written where the next phase's research will read it, tagged honestly as observed rather than documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → HubSpot schema API | the only property-creation write this phase makes |
| operator machine → n8n Cloud API | deploy, bounce and arm on live workflows |
| n8n lane → HubSpot record write | the single armed window, one record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-03 | Elevation of Privilege | the property-creation step | critical | mitigate | `scripts/sync_hubspot_properties.py`'s two-key gate (`DRY_RUN=false` AND `ALLOW_HUBSPOT_PROPERTY_WRITES=true`) is the only route; `ALLOW_N8N_ARM` is explicitly not set during Task 1, keeping the schema privilege boundary distinct from the record-write one. The default dry run is executed first and its output recorded. |
| T-72-27 | Elevation of Privilege | the armed window | critical | mitigate | Exactly one window, record-scoped by the allowlist, closed in the same sitting, with a post-send read of every `ALLOW_*` constant on every workflow recorded in `72-UAT.md`. `scripts/bounce_n8n_workflows.py` exits 1 while armed, which is a second independent detector of a window left open. |
| T-72-28 | Denial of Service | deploying an untested Merge topology live | high | mitigate | Plan 04 added a Merge and a sentinel to the ingest lane; a starved Merge terminates a lane silently while the execution still reports success (CLAUDE.md §13.0.3, the Gate 8 failure class). The disarmed deploy in Task 1 precedes any armed send, the walker's `starvedWithData` assertion ran offline on the same committed body, and Task 2's read-back of an actual created record is the live confirmation. |
| T-72-29 | Repudiation | trusting the send's 200 | high | mitigate | HubSpot silently ignores a PATCH naming a property that does not exist, and returns 200. Every assertion in Task 2 is made from a RE-READ of the record, never from the response status — and property creation is sequenced first so an absent value means a code defect rather than a missing property. |
| T-72-30 | Information Disclosure | a live test record left on the portal | medium | mitigate | The created contact's id is recorded and its hand-deletion is a checklist item with a confirmation, mirroring the 71-UAT clean-up discipline. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` green.
- `node --test tests/n8n/*.test.mjs` green.
- The commit range since plan 07's SUMMARY landed introduces no change under `src/`, `n8n/`, `scripts/`, `config/` or `operator-claude-plugin/scripts/`.
- `72-UAT.md` exists with a verdict, and every deployed workflow's live read-back is recorded there and in CLAUDE.md §13.0.2's terms.
</verification>

<success_criteria>
A person the waterfall researched lands in HubSpot with the mobile and the LinkedIn URL that
were dropped for Busteed, an existing contact's non-blank phone survives an update untouched,
the three slot properties exist, every changed workflow is live and disarmed, the created
record is deleted, and the repo's own contract document records what was observed rather than
what was expected.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 72 (exclude from drift verification — none of these exist yet):

- **HubSpot custom properties:** `lv_phone_2` (contacts), `lv_mobilephone_2` (contacts), `lv_phone_2` (companies)
- **`config/column_mapping.yaml` new canonical alias targets:** `mobilephone`, `city`, `state`, `country`, `hs_state_code`, `hs_country_region_code`, `seniority`, `lv_persona_group`
- **`config/field_policy.yaml` new keys:** contacts `hs_linkedin_url`, `lv_phone_2`, `lv_mobilephone_2`, conditionally `hs_additional_emails`; companies `state`, `hs_state_code`, conditionally `hs_country_region_code`, `lv_phone_2`, conditionally `hs_additional_domains`; plus `system_correctable_sources` on `contacts.jobtitle` and `companies.industry`
- **`config/hubspot_properties.yaml` new declarations:** the three `_2` slot properties above
- **JS engine symbols:** `opts.now`, `opts.historyByField`, `_isStale()`, `_isSystemCorrectable()` (new in `mergeContacts.js`), `_overflowSlot()`; new company geo/phone producers in `n8n/code/normalizeProviders.js`
- **Python symbols:** `now=` kwarg, `_is_stale()`, `_is_system_correctable()`, `_overflow_slot()` in `src/merge_policy.py`; `create_row_ids=` kwarg, `IDENTITY_FIELDS`, `PROVIDER_KEY_ALIASES` in `operator-claude-plugin/scripts/preingest.py`
- **New n8n nodes:** `HubSpot Contact History`, its carry Merge and its sentinel gate (ingest lane)
- **New planning artifacts:** `72-PORTAL-PROBE.json`, `72-UAT.md`, `.planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md`
- **New test files:** `tests/n8n/ingestWidenedFieldsFlow.test.mjs`, `tests/n8n/widenedKeyParity.test.mjs`, `tests/n8n/mergeRecencyGate.test.mjs`, `tests/n8n/contactHistoryFlow.test.mjs`, `tests/n8n/overflowSlots.test.mjs`
- **New docs:** the Phase 72 as-built delta in `CLAUDE.md`; the D-72-17 gate section in `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`; plugin `0.49.0` in `operator-claude-plugin/CHANGELOG.md`

<output>
Create `.planning/phases/72-enrichment-extras-land-in-hubspot/72-08-SUMMARY.md` when done.
</output>
