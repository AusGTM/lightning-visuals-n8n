---
phase: 16-scheduled-workflows-review-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy_n8n_workflows.py
  - scripts/provision_n8n_credentials.py
  - scripts/build_cloud_workflows.py
  - config/hubspot_properties.yaml
  - n8n/wf_enrichment_cloud.json
  - tests/test_deploy_n8n_workflows.py
  - tests/test_cloud_companies_branch.py
  - tests/test_cloud_write_path.py
  - tests/test_architecture_guard.py
  - tests/test_builder_flag_parity.py
  - tests/test_hubspot_properties_config.py
  - .env.example
autonomous: false
requirements:
  - "Criterion 5 (env->credentials + build-time constants + parity)"
  - "Criterion 6 (credential-provisioning script)"
  - "Criterion 7 (deploy script)"
  - "Criterion 8 (companies-branch port)"
  - "Criterion 1 enabler (SJ-3 control properties prerequisite)"
user_setup:
  - service: n8n Cloud
    why: "Live deploy + credential provisioning are operator-runbook steps; the automated suite proves script logic against mocked API responses only."
    env_vars:
      - name: N8N_URL
        source: "n8n Cloud instance subdomain (already in .env.example)"
      - name: N8N_API_KEY
        source: "n8n Cloud -> Settings -> API (already in .env.example)"
      - name: ALLOW_N8N_DEPLOY
        source: "operator sets true only for a live run (already in .env.example, default false)"

must_haves:
  truths:
    - "scripts/deploy_n8n_workflows.py computes a create-vs-update workflow diff matched by workflow NAME against a fresh GET /api/v1/workflows, and performs no write unless DRY_RUN=false AND ALLOW_N8N_DEPLOY=true (Criterion 7)."
    - "scripts/provision_n8n_credentials.py creates the six provider/API secrets across ~5 credential objects (ZoomInfo holds client_id + client_secret in one credential) by name via POST /api/v1/credentials, create-if-missing only, introspecting GET /api/v1/credentials/schema/{type} before building each body, RECORDS the returned credential name->id map to a gitignored file, and returns exit 0 with a 'skipped' banner when N8N_URL/N8N_API_KEY are absent (Criterion 6; review consensus #1)."
    - "scripts/deploy_n8n_workflows.py, before POST/PUT, resolves each node's credential by a node-name->credential-name map (Lusha/Apollo/Anthropic are three DIFFERENT httpHeaderAuth credentials, so type-matching alone is insufficient) and injects a per-node top-level `credentials` block using the provisioned name->id map (or a fresh GET /api/v1/credentials); deploying without a resolvable id fails closed rather than importing an unbound node (review consensus #1)."
    - "The built n8n Cloud enrichment workflow resolves every one of the 6 secrets through a bound n8n credential and every one of the 6 config flags through a build-time inlined constant — no environment-variable expression survives in the built JSON (Criterion 5)."
    - "build_enrichment_local_live() and build_enrichment_cloud() source the 6 config-flag values and the 6 secret env-var names from one shared build-time constant (CONFIG_FLAG_DEFAULTS + SECRET_ENV_NAMES); tests/test_builder_flag_parity.py builds both and asserts an identical 6-flag set and identical 6-secret set, so the local-replica (docker env) and Cloud (credentials + AR-4 constants) builders cannot diverge silently (Criterion 5 parity clause)."
    - "build_enrichment_cloud() contains the full companies ICP branch (Emit Company Targets through Decide Company Action, including research + judge) reachable by BFS from the Webhook Trigger, built with credential-bound nodes (Criterion 8)."
    - "lv_enrichment_requested (bool) and lv_enrichment_status (enum queued/running/complete/failed/needs_review/skipped, per CLAUDE.md §4.1) exist in config/hubspot_properties.yaml on both companies and contacts groups, enabling the SJ-3 predicate authored in 16-02 (Criterion 1 enabler)."
    - "The Cloud webhook verifies a shared secret (X-Enrichment-Secret, CLAUDE.md §18.1) and parses the HubSpot event array (objectId/objectType, §18.2) into a per-record fetch — it never forwards an unauthenticated body of direct {email,domain} fields straight into Build Identity (review #7, verified against build_cloud_workflows.py:2055-2064 + :662-682)."
    - "The Cloud HubSpot Search nodes carry real filterGroups (company: domain EQ; contact: email EQ), the Adapt step preserves the top-level record id as hs_object_id, and Create/Update send the computed property patch against that id — a lookup FAILURE (401/timeout/malformed, onError:continueRegularOutput) fails closed to skip/needs_review and never routes to `create` (review #8, verified against :2067-2073/:2156-2168/:1303-1318/enrichmentGate.js:61)."
    - "Live HubSpot record writes are gated by a build-time write-safety constant WRITE_SAFETY_DEFAULTS (ALLOW_HUBSPOT_RECORD_WRITES default false + create switch + a test-record domain/id allowlist), baked into ENRICH_DECIDE_*_CLOUD via the same AR-4 inlining as the flags — activating the workflow alone does not enable live writes (review #9, verified against :2192-2198)."
    - "ENRICH_DECIDE_CO_CLOUD is the review-loop PRODUCER: on any needs_review decision it writes lv_enrichment_needs_review=true, lv_enrichment_status=needs_review, lv_enrichment_review_reason, and lv_enrichment_review_candidate_json=stableStringify(the held needs_review decisions), and HOLDS those fields' canonical writes so 16-02's reviewApply has the held candidates to apply post-approval (review consensus #2, seam with 16-02 Task 4)."
  artifacts:
    - "scripts/deploy_n8n_workflows.py (incl. node-name->credential injection before POST/PUT)"
    - "scripts/provision_n8n_credentials.py (incl. name->id map emission)"
    - "build_enrichment_cloud() with the companies branch + ENRICH_DECIDE_CO_CLOUD (review-loop producer) + a hardened webhook (shared-secret + HubSpot event parser) + real HubSpot Search/Create/Update nodes + WRITE_SAFETY_DEFAULTS gate"
    - "CONFIG_FLAG_DEFAULTS + SECRET_ENV_NAMES: the shared single-source constant both enrichment builders read; WRITE_SAFETY_DEFAULTS: the Cloud-only build-time write gate"
    - "config/hubspot_properties.yaml: lv_enrichment_requested, lv_enrichment_status (x2 objects)"
    - "tests/test_deploy_n8n_workflows.py, tests/test_cloud_companies_branch.py, tests/test_cloud_write_path.py, tests/test_builder_flag_parity.py, tests/test_architecture_guard.py::test_no_env_or_vars_in_cloud_workflows"
  key_links:
    - "deploy script -> n8n Public API, matched on workflow name (not JSON internal id)"
    - "provision name->id map -> deploy node-name->credential-name binding -> per-node top-level `credentials` block (Lusha/Apollo/Anthropic disambiguated by node name, not shared httpHeaderAuth type)"
    - "provider secret -> bound credential id on the cloud HTTP/HubSpot nodes (never a header literal)"
    - "Webhook shared-secret + HubSpot event parser -> Build Identity (event objectId, never a raw direct-field body)"
    - "Adapt Search preserves hs_object_id -> HubSpot Update targets the real record id (no discarded-id duplicate)"
    - "WRITE_SAFETY_DEFAULTS (build-time) -> ENRICH_DECIDE_*_CLOUD IF-gated HubSpot Create/Update"
    - "ENRICH_DECIDE_CO_CLOUD needs_review -> review flags + candidate JSON (producer) -> 16-02 reviewApply (consumer)"
    - "shared CONFIG_FLAG_DEFAULTS/SECRET_ENV_NAMES -> both build_enrichment_local_live() and build_enrichment_cloud() (parity guard: neither builder can add/drop a flag or secret independently)"
  prohibitions:
    - "No secret value (API key, token, client secret) is inlined into any committed workflow JSON, printed to script logs, or committed to git — scripts print HTTP status codes and diffs only."
    - "The built Cloud enrichment workflow JSON contains zero environment-variable expressions (neither the dollar-env nor the dollar-vars forms) — every secret is a bound credential."
    - "The deploy and credential-provisioning scripts perform no live write when either key of the two-key gate is absent."
    - "The credential-provisioning script never updates a credential in place (create-if-missing only; rotation is a manual delete+recreate operator action)."
    - "The companies-branch port's canonical write path never emits the derived ICP output fields (score, tier, anti-icp flag, recommended motion) — Approach C, inputs only."
    - "The Cloud companies branch does NOT carry the ENRICH_EMIT_COMPANIES fixture emitter (Harvey Norman / Racing NSW / … at build_cloud_workflows.py:1247-1264) — on the webhook path the company identity comes from the parsed HubSpot event, never a hard-coded row set (review #6)."
    - "No live HubSpot record write path exists that is reachable without BOTH the shared-secret webhook check AND the WRITE_SAFETY_DEFAULTS gate passing — an unauthenticated or un-gated request cannot create/update a record (reviews #7/#9)."
    - "A HubSpot lookup FAILURE (non-200 / malformed) never routes to `create` — it fails closed (review #8)."
    - "The frozen byte-identical Code files (mergeCompanies.js, judge.js, enrichmentGate.js, webResearch.js) are NOT modified — the port is a topology copy + edge-type swap, not a logic change."
---

<objective>
Make the enrichment pipeline deployable to the empty n8n Cloud instance: convert the 6
secrets to bound credentials and the 6 config flags to build-time constants, port the
companies ICP branch into the Cloud webhook template, and build the two dev-time scripts
(deploy + credential-provisioning) that push workflows and credentials over the n8n Public
API under a two-key gate. Also add the two HubSpot control properties SJ-3 depends on but
that the Phase-15 manifest never created.

Purpose: This is the first phase that will write real record data live; getting the
secret-handling, wrong-instance, and non-clobber guards right here is what makes 16-02's
schedules and review loop safe to run.
Output: Two new scripts, an expanded build_enrichment_cloud(), a shared flag/secret
single-source both enrichment builders read, two new HubSpot property manifest entries,
and five offline test files/additions (incl. a builder-parity guard) — all proven with
zero live network calls. Live deploy/provisioning is a non-gating operator-runbook step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SYSTEM-CONTRACT.md
@.planning/phases/16-scheduled-workflows-review-surface/16-RESEARCH.md
@.planning/phases/16-scheduled-workflows-review-surface/16-PATTERNS.md

# Analog + target sources (read the specific ranges named in each task's read_first)
@scripts/sync_hubspot_properties.py
@scripts/build_cloud_workflows.py
@tests/test_sync_hubspot_properties.py
@tests/test_architecture_guard.py
@config/hubspot_properties.yaml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end deploy + credential-provisioning slice (offline-proven)</name>
  <files>scripts/deploy_n8n_workflows.py, scripts/provision_n8n_credentials.py, tests/test_deploy_n8n_workflows.py, .env.example</files>
  <read_first>
    - scripts/sync_hubspot_properties.py (FULL — this is the exact idiom: `_has_credentials`, `_portal_ok`, `_writes_allowed`, `compute_property_diff`, `main()` skip-to-exit-0 banner, per-item create + post-write confirmation, partial-failure-is-not-success gate)
    - tests/test_sync_hubspot_properties.py (FULL — the monkeypatch/mock pattern for offline HTTP; mirror it, no requests-mock dependency)
    - 16-RESEARCH.md Deliverable 3 (Public API shapes, create-vs-update diff, credential create-if-missing corollary) and 16-PATTERNS.md "config/deploy script" section
    - .env.example lines with N8N_URL / N8N_API_KEY / ALLOW_N8N_DEPLOY (already present)
  </read_first>
  <action>
    Write the thin end-to-end deploy/provision slice that proves the whole architecture before
    the heavy port. Mirror sync_hubspot_properties.py's three-function gate exactly, swapping env
    var names: `_has_n8n() -> bool` (N8N_URL and N8N_API_KEY both set), `_instance_ok() -> bool`,
    and `_writes_allowed()` requiring DRY_RUN=false AND ALLOW_N8N_DEPLOY=true.

    `_instance_ok()` — the wrong-instance guard — MUST NOT fail open (review consensus #4,
    gemini/sol MEDIUM). Do NOT default N8N_EXPECTED_URL to N8N_URL (that makes the check
    tautologically true when unset, unlike sync_hubspot_properties.py's hardcoded
    EXPECTED_PORTAL_ID). Instead: require N8N_EXPECTED_URL to be set AND equal to N8N_URL, OR — if
    N8N_EXPECTED_URL is unset — validate that N8N_URL's host ends with `.n8n.cloud`; return False
    (refuse, no write, non-zero exit) otherwise. Add N8N_EXPECTED_URL to .env.example as commented
    guidance. A test asserts an unset/mismatched N8N_EXPECTED_URL against a non-`.n8n.cloud` URL
    refuses with zero HTTP calls.

    In scripts/deploy_n8n_workflows.py implement `compute_workflow_diff(local_workflows, live_workflows)`
    keyed on the top-level `name` field (n8n assigns ids server-side; never match on the JSON's internal
    id), returning {"create": [...], "update": [{"id", "body"}]} — re-derived from a FRESH GET every run,
    never from local state. Implement `main()`: skip-to-exit-0 with a "skipped (no n8n creds)" banner when
    `_has_n8n()` is false so the offline suite never calls out; refuse with a non-zero exit and no call
    when `_instance_ok()` is false; always print the diff; only POST /api/v1/workflows (create) or
    PUT /api/v1/workflows/{id} (update) when `_writes_allowed()`. Load the local set from n8n/wf_*.json
    (the deployable set); for this tracer, deploy path targets the EXISTING wf_enrichment_cloud.json only
    is not required — read all wf_*.json and diff them. Do not auto-activate here (activation is an
    operator-runbook concern; note it in a docstring).

    CREDENTIAL-ID BINDING (review consensus #1 — the built Cloud JSONs carry ZERO `credentials`
    blocks, so importing them unchanged leaves every node unbound). Before POST/PUT, deploy attaches a
    per-node top-level `"credentials"` object resolved from TWO inputs: (a) a static node-name ->
    credential-name map defined in the deploy script (e.g. "Lusha Enrich"->"LV Lusha", "Apollo
    Match"->"LV Apollo", "Claude Web Research"/"Judge Call"->"LV Anthropic", the HubSpot nodes->"LV
    HubSpot", the ZoomInfo node(s)->the Task-2 credential) — REQUIRED because Lusha/Apollo/Anthropic are
    three DIFFERENT credentials sharing one `httpHeaderAuth` type, so type-matching alone cannot
    disambiguate them; and (b) the credential name->id map the provision script emits (below). A node
    whose mapped credential name has no resolvable id FAILS CLOSED (refuse the deploy, non-zero exit)
    rather than importing an unbound node. Implement `bind_credentials(workflow, name_to_id,
    node_cred_map) -> workflow` as a pure function and test it offline against a fixture workflow + a
    mocked name->id map.

    In scripts/provision_n8n_credentials.py implement the credential corollary: a manifest of the 6
    secrets across ~5 credential objects (HubSpot app-token, Lusha header-auth, Apollo header-auth,
    Anthropic header-auth, and the single ZoomInfo credential that holds BOTH client_id + client_secret,
    whose TYPE is decided in Task 2 — leave a single clearly-marked constant for its `type`/`data` shape
    to be filled after Task 2). For each, call GET /api/v1/credentials/schema/{type} once and assert the
    field names it is about to send match before POST /api/v1/credentials; match existing credentials by
    `name` against a fresh GET /api/v1/credentials and CREATE-IF-MISSING ONLY (never PUT — n8n never
    returns secret data to diff against). After creating/finding each, RECORD the credential name->id in
    a map and WRITE it to a gitignored file (e.g. .n8n_credential_ids.json) so deploy's bind step can
    resolve ids without a second interactive step; the map contains names+ids only, never secret values.
    Wrap the schema introspection in try/except with a clean status banner (a differing type name / 400
    / 404 aborts provisioning gracefully, not with a stack trace — review gemini LOW). Same
    gate/skip/partial-failure functions as the deploy script. Read the secret values from the existing
    environment variables (HUBSPOT_PRIVATE_APP_TOKEN, LUSHA_API_KEY, APOLLO_API_KEY, ANTHROPIC_API_KEY,
    ZOOMINFO_CLIENT_ID, ZOOMINFO_CLIENT_SECRET); NEVER print a secret value — log the credential name and
    HTTP status only.

    Write tests/test_deploy_n8n_workflows.py mirroring tests/test_sync_hubspot_properties.py: (a) diff
    computes correct create-vs-update against a mocked GET returning a partial live set; (b) the two-key
    gate refuses (no HTTP call made) unless both DRY_RUN=false and ALLOW_N8N_DEPLOY=true — assert with a
    monkeypatched requests that records calls; (c) no-creds path returns 0 without any call; (d)
    `bind_credentials` attaches the correct id per node via the node-name map and RAISES/refuses when a
    mapped credential name is absent from the name->id map (fail-closed); (e) `_instance_ok()` refuses
    (zero HTTP calls) when N8N_EXPECTED_URL is unset and N8N_URL is not a `.n8n.cloud` host.
  </action>
  <verify>
    <automated>python -m pytest tests/test_deploy_n8n_workflows.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_deploy_n8n_workflows.py -q` passes.
    - Test proves: with DRY_RUN=true (or ALLOW_N8N_DEPLOY unset) NO POST/PUT is issued (mocked requests records zero write calls); with both keys set, create-vs-update is dispatched matching a mocked live set by name.
    - `python -m pytest -q` collects both new scripts' tests without importing failures and the no-creds path returns exit 0 (grep the test asserting the "skipped" banner).
    - Neither script contains a print of any secret env var value (grep both scripts: no f-string interpolates HUBSPOT_PRIVATE_APP_TOKEN / *_API_KEY / ZOOMINFO_CLIENT_SECRET into a printed line).
    - `bind_credentials` test proves each node gets its correct credential id via the node-name map, and a missing name in the name->id map fails closed (raises / non-zero exit, no unbound node emitted).
    - `_instance_ok()` test proves refuse-with-zero-HTTP-calls when N8N_EXPECTED_URL is unset and N8N_URL is not a `.n8n.cloud` host (the fail-open default is gone).
    - The provisioned name->id map path is gitignored and carries names+ids only (grep the written map fixture: no secret value present).
  </acceptance_criteria>
  <reversibility rating="costly">Live deploy pushes workflows to a shared Cloud instance; the automated slice is offline (mocked), and the live push is a gated operator-runbook step, so the code is reversible even though a live run is costly.</reversibility>
  <done>Both scripts exist, are import-clean, skip cleanly with no creds, refuse without the two-key gate, and their diff/gate logic passes offline.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: ZoomInfo credential architecture decision (spike-gated)</name>
  <decision>Which ZoomInfo credential architecture does the Cloud workflow use: native n8n OAuth2 Client-Credentials credential, or a credential-bound HTTP mint node + secret-free cache-only Code node? (Prerequisite for Task 4's one ZoomInfo path and Task 1's ZoomInfo credential type placeholder.)</decision>
  <files>scripts/build_cloud_workflows.py (ENRICH_ZOOMINFO_CACHED / company variant), scripts/provision_n8n_credentials.py (ZoomInfo credential type placeholder)</files>
  <action>Run the live ZoomInfo OAuth2 spike on a scratch Cloud workflow, then choose one option below. This decision is a prerequisite for Task 4 (which implements exactly one ZoomInfo path) and fills the ZoomInfo credential type/data placeholder left in Task 1's provisioning manifest. Do not implement both paths.</action>
  <context>
    zoominfoToken.js (used by ENRICH_ZOOMINFO_CACHED and ENRICH_ZOOMINFO_CO_CACHED via _zoom_preamble)
    is a Code node that mints its own bearer by reading the client id/secret directly — which has NO
    compliant secret source on Cloud. 16-RESEARCH.md Deliverable 2 lays out two options and flags a
    reported 422 bug in n8n's generic OAuth2 Client-Credentials grant that must be spiked live on a
    scratch Cloud workflow before committing. This decision gates Task 4's credential conversion and the
    ZoomInfo `type`/`data` shape left as a placeholder in Task 1's provisioning manifest.
  </context>
  <options>
    <option id="native-oauth2">
      <name>Native n8n OAuth2 API credential, Client Credentials grant (preferred)</name>
      <pros>Deletes zoominfoToken.js's hand-rolled cache/refresh/401-retry entirely (Don't-Hand-Roll win); n8n manages mint/cache/refresh; no Code node in the auth path.</pros>
      <cons>Unverified against ZoomInfo's token endpoint on THIS Cloud instance — a reported 422 bug on some versions. Requires a live spike on a scratch workflow (operator action).</cons>
    </option>
    <option id="split-code-node">
      <name>Credential-bound HTTP mint node + secret-free cache-only Code node (fallback)</name>
      <pros>Preserves the proven cache/401-retry behavior; secret lives only in the HTTP node's Basic-Auth credential; known-working shape.</pros>
      <cons>3-4 more nodes + an IF branch; more graph surgery in build_enrichment_cloud().</cons>
    </option>
  </options>
  <read_first>
    - 16-RESEARCH.md Deliverable 2 ("The ZoomInfo problem is not a $env->$vars swap")
    - n8n/code/zoominfoToken.js + tests/n8n/zoominfoToken.test.mjs (the cache/401-retry behavior that must survive whichever path is chosen)
    - scripts/build_cloud_workflows.py `_zoom_preamble`, ENRICH_ZOOMINFO_CACHED (~994), ENRICH_ZOOMINFO_CO_CACHED (~1190)
  </read_first>
  <resume-signal>Select: native-oauth2 (confirmed by the live spike) or split-code-node. State which, so Task 4 implements exactly one path and Task 1's ZoomInfo credential `type` placeholder is filled.</resume-signal>
  <verify>
    <human-check>Operator has run the OAuth2 spike (or accepts the fallback) and recorded the chosen path in the resume signal.</human-check>
  </verify>
  <done>One ZoomInfo credential path is selected and recorded; Task 4 can implement it unambiguously.</done>
</task>

<task type="auto">
  <name>Task 3: Add SJ-3 control properties (lv_enrichment_requested, lv_enrichment_status)</name>
  <files>config/hubspot_properties.yaml, tests/test_hubspot_properties_config.py</files>
  <read_first>
    - config/hubspot_properties.yaml lines 152-161 (lv_enrichment_needs_review bool shape) and the lv_revenue_band enumeration shape (an existing `type: enumeration`/`fieldType: select` entry) — copy these two shapes exactly
    - 16-PATTERNS.md "config/hubspot_properties.yaml" section (bool needs explicit true/false options; enum copies the revenue-band shape)
    - 16-RESEARCH.md Deliverable 5 (SJ-3 blocked-on-missing-property finding) and Pitfall 4
    - tests/test_hubspot_properties_config.py (existing manifest assertions to extend)
    - MEMORY note "bools need true/false options; property names must be lowercase" (both only fail live)
  </read_first>
  <action>
    Add two properties to BOTH object groups (lv_enrichment for companies, lv_enrichment_contacts for
    contacts) in config/hubspot_properties.yaml: `lv_enrichment_requested` as a boolean (fieldType
    booleancheckbox) copying the lv_enrichment_needs_review shape verbatim including its explicit
    Yes=true / No=false options; and `lv_enrichment_status` as an enumeration (fieldType select) copying
    the lv_revenue_band shape, with option values queued, running, complete, failed, needs_review,
    skipped — all SIX per CLAUDE.md §4.1 (review LOW: `skipped` was dropped in the prior draft; the
    failure-handling paths in CLAUDE.md §26 set it), each with label/value/displayOrder/hidden. Keep
    all names lowercase. No script logic changes — compute_property_diff consumes these unchanged.
    Extend tests/test_hubspot_properties_config.py to assert both new names exist on both groups, the
    bool carries true/false options, and the enum carries exactly the six status values.
  </action>
  <precondition>config/hubspot_properties.yaml currently defines the Phase-15 manifest (the lv_enrichment / lv_enrichment_contacts groups are both present with their existing bool + enumeration entries to copy shapes from). Assert the two group keys exist rather than an exact total-entry count.</precondition>
  <verify>
    <automated>python -m pytest tests/test_hubspot_properties_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_hubspot_properties_config.py -q` passes with the two new assertions.
    - `grep -c "name: lv_enrichment_requested" config/hubspot_properties.yaml` returns 2 (both objects); same for lv_enrichment_status.
    - The lv_enrichment_status entry lists exactly six option values: queued, running, complete, failed, needs_review, skipped.
    - The lv_enrichment_requested entry carries explicit true and false options (bool-without-options fails live).
  </acceptance_criteria>
  <reversibility rating="reversible">Manifest-only edit; live creation is create-if-missing via the existing dry-run/undo-manifest sync tooling.</reversibility>
  <done>Both control properties are in the manifest on both objects with the correct option shapes and are asserted by the config test.</done>
</task>

<task type="auto">
  <name>Task 4: Convert 6 secrets to credentials + 6 flags to build-time constants from one shared source (incl. ZoomInfo fix + parity guard)</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/test_architecture_guard.py, tests/test_builder_flag_parity.py</files>
  <read_first>
    - scripts/build_cloud_workflows.py: `_live_http` (~1790), `_http_node` (~2018, the auth="header" pattern), build_enrichment_cloud() (2050-2268) INCLUDING the sticky notes (2201-2260) that still say n8n Variables / "properties not yet created" / "POC mocks", `_zoom_preamble`/ENRICH_ZOOMINFO_CACHED
    - scripts/build_cloud_workflows.py build_enrichment_local_live() flag + secret sites (~1835-1926) — the docker-replica builder whose 6 flags + 6 secrets must stay in lockstep with the Cloud builder (this is the parity counterpart)
    - 16-PATTERNS.md "$env -> credentials / build-time constants — exact grounding" (names the 6 flags + the taxonomy/escalation single-source precedent) and "Analog B (Cloud node conversion pattern)"
    - 16-RESEARCH.md Deliverable 2 table (the 6 secrets + 6 flags, where each is read) and Pitfall 1 (the defensive dollar-vars fallback does NOT fix Cloud)
    - Task 2's decision output (native-oauth2 vs split-code-node) — implement exactly that ZoomInfo path
    - tests/test_architecture_guard.py (existing AR-2 host-guard style to mirror for the new guard test) and ROADMAP Criterion 5 (the "must not diverge without a parity story" clause the parity test discharges)
  </read_first>
  <action>
    In build_enrichment_cloud(), ensure every secret is credential-bound and every flag is a
    build-time constant. Lusha/Apollo/Anthropic HTTP nodes use `_http_node(..., auth="header")` (the
    contacts branch already proves this); the two HubSpot searches use the native n8n-nodes-base.hubspot
    node (credential-bound), never a raw Bearer header. Apply the Task-2-chosen ZoomInfo path to BOTH
    ENRICH_ZOOMINFO_CACHED (contacts) and the company variant so the fix is shared once.

    Single-source the flags and secrets (Criterion 5 parity). Define one module-level
    `CONFIG_FLAG_DEFAULTS` dict — the 6 flags mapped to their build-time values (allow-web-research,
    max-web-research, the sonnet model name, web-research-max-searches, allow-sonnet-escalation,
    max-sonnet-validations) — and one `SECRET_ENV_NAMES` collection (the 6 secret env-var names) as the
    ONE place both enrichment builders read. In build_enrichment_cloud(), inline each flag from
    `CONFIG_FLAG_DEFAULTS` at build time via plain Python string substitution into the generated jsCode —
    the same AR-4 "nothing not already in the JSON exists at runtime" precedent that governs
    taxonomy.generated.js/escalation.generated.js; do NOT leave a defensive dollar-vars-or-dollar-env
    fallback expression (Pitfall 1 — that resolves to nothing on Cloud). Refactor
    build_enrichment_local_live()'s 6 flag sites and 6 secret sites to iterate the SAME
    `CONFIG_FLAG_DEFAULTS` / `SECRET_ENV_NAMES` when it emits its docker-replica env expressions, so both
    builders reference an identical flag set and secret set BY CONSTRUCTION — a flag or secret
    added/dropped/renamed in one builder but not the other becomes structurally impossible, which is the
    parity story ROADMAP Criterion 5 requires. (Fallback ONLY if a full structural single-source is
    genuinely infeasible against the current jsCode shape: keep the shared constants for whatever is
    feasible, document the exact divergence-preventing invariant as a parity-story paragraph in the
    build_enrichment_* docstrings, and still ship the parity test below — prefer the structural route.)

    Update the sticky notes so they describe credential provisioning and no longer claim ZoomInfo uses
    n8n Variables, properties are uncreated, or responses are mocked.

    Add tests/test_architecture_guard.py::test_no_env_or_vars_in_cloud_workflows: for the Cloud
    workflow JSON files, assert the built parameters contain zero environment-variable expressions in
    either the dollar-env or dollar-vars form (regex over serialized node parameters), mirroring the
    existing AR-2 host-guard iteration style. Use a WORD-BOUNDARY regex `\$env\b|\$vars\b` (NOT
    `\$env\.|\$vars\.` — the dot form is evadable via `$env[` / `$vars[` bracket access; review kimi/sol
    LOW). Regenerate the built JSON by running the builder before the assertion. Do NOT touch the
    local/local-live workflow files (they legitimately keep those expressions as docker-replica
    variants). Keep the frozen Code files unmodified.

    Add tests/test_builder_flag_parity.py (the Wave-0 parity guard 16-VALIDATION.md requires): import
    build_enrichment_local_live() and build_enrichment_cloud(), build each in-memory, and assert (1) both
    reference exactly the 6 flag names in `CONFIG_FLAG_DEFAULTS` — local-live via its docker env
    expressions, cloud via baked literals equal to the `CONFIG_FLAG_DEFAULTS` values — with neither
    builder adding or dropping a flag; (2) both bind/reference exactly the same 6-secret set
    (`SECRET_ENV_NAMES`) — local-live via env header expressions, cloud via bound credentials — so a
    secret converted in one builder but not the other fails the test. The test reads `CONFIG_FLAG_DEFAULTS`
    /`SECRET_ENV_NAMES` as the expected sets, so editing a flag/secret in only one builder trips it.
  </action>
  <verify>
    <automated>python scripts/build_cloud_workflows.py && python -m pytest tests/test_architecture_guard.py tests/test_builder_flag_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - After rebuilding, test_no_env_or_vars_in_cloud_workflows passes: the Cloud enrichment workflow JSON has zero dollar-env and zero dollar-vars expressions.
    - The full tests/test_architecture_guard.py suite stays green (AR-1/AR-2/AR-3/PN guards unbroken).
    - `grep -rE '\$env\b|\$vars\b' n8n/wf_enrichment_cloud.json` returns nothing (word-boundary form catches bracket-access evasion).
    - The 6 config flags appear as literal values in the built jsCode (spot-check one, e.g. the max-sonnet-validations integer, is a bare number not an expression).
    - tests/test_builder_flag_parity.py passes: both enrichment builders reference an identical 6-flag set (from `CONFIG_FLAG_DEFAULTS`) and an identical 6-secret set (from `SECRET_ENV_NAMES`); the test fails if a flag or secret is added, dropped, or renamed in only one builder.
    - `CONFIG_FLAG_DEFAULTS` and `SECRET_ENV_NAMES` are the single source both build_enrichment_local_live() and build_enrichment_cloud() read for the 6 flags/6 secrets (grep: both builder functions reference the shared constants; the flag values are not re-hardcoded independently in each builder).
    - The frozen Code files (mergeCompanies.js, judge.js, enrichmentGate.js, webResearch.js) are byte-identical to their pre-task state (git diff shows no change to n8n/code/*.js except any ZoomInfo file the chosen path required).
  </acceptance_criteria>
  <reversibility rating="reversible">All output is regenerated from source by the builder; revert the builder edit and rebuild.</reversibility>
  <done>The built Cloud enrichment JSON holds no env-var expressions, every secret is credential-bound, every flag is a baked constant sourced from the shared `CONFIG_FLAG_DEFAULTS`, the two builders provably reference one shared flag/secret source (parity test green), and the sticky notes reflect the new credential model.</done>
</task>

<task type="auto">
  <name>Task 5: Port the companies ICP branch into build_enrichment_cloud()</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/test_cloud_companies_branch.py</files>
  <read_first>
    - scripts/build_cloud_workflows.py build_enrichment_local_live() lines 1872-2013 (the full company node sequence + the isolated `research_conns` dict at 1985-2005) — the topology to copy
    - build_enrichment_cloud() 2050-2268 (the contacts Cloud conversion pattern to reuse) and ENRICH_DECIDE_CLOUD (~872) vs ENRICH_DECIDE_CO_LOCAL (~1742) — the decide-node local/cloud split to mirror
    - 16-RESEARCH.md Deliverable 4 (Companies-Branch Port — "smallest correct port: two changes only") and 16-PATTERNS.md "Analog A / Analog B"
    - tests/test_judge_spec.py + tests/test_architecture_guard.py (the RO-2-style BFS graph-ancestry test precedent to mirror)
  </read_first>
  <action>
    Copy the company branch node sequence (Build Company Identity, HubSpot Company
    Search, Adapt Company Search, Company Gate, Build Company Requests, the three provider calls,
    Normalize+Score Company, Research Trigger Gate, IF Research Needed, Build Research Request, Claude Web
    Research, Validate Research Output, Judge Gate, IF Needs Judge, Build Judge Request, Judge Call, Apply
    Judge Verdict, Merge Company) and its `research_conns` dict into build_enrichment_cloud() as a sibling
    branch off the Webhook Trigger, exactly as it is a sibling off the trigger in local-live.

    DO NOT port `Emit Company Targets` / ENRICH_EMIT_COMPANIES (review #6, VERIFIED: build_cloud_workflows.py:1247-1264
    hard-codes Harvey Norman / Racing NSW / Melbourne Racing Club / Australian Turf Club / FanDuel and
    IGNORES its input). That node is a LOCAL-LIVE fixture source; porting it verbatim would make every
    Cloud webhook execution process those five fixtures instead of the company the event names, and a BFS
    reachability test would happily certify it. On the Cloud path the company branch begins at
    `Build Company Identity`, fed by the parsed HubSpot event / webhook payload (the object-type router
    added in Task 6 sends companies events down this branch), not a hard-coded row set.

    Apply the two node-conversion changes Deliverable 4 specifies: (1) every raw-HTTP `_live_http` node
    becomes the Cloud credential-bound form (native HubSpot node for HubSpot Company Search;
    `_http_node(..., auth="header")` for Lusha Company / Apollo Org / Claude Web Research / Judge Call; the
    Task-2 ZoomInfo path for the company ZoomInfo node); (2) add a new `ENRICH_DECIDE_CO_CLOUD` constant
    mirroring ENRICH_DECIDE_CLOUD (computes the property patch, routes IF nodes to real GATED HubSpot
    company Create/Update — gated by Task 6's WRITE_SAFETY_DEFAULTS) in place of ENRICH_DECIDE_CO_LOCAL's
    dry-run echo. Reuse mergeCompanies' canonicalPatch output shape unchanged so no derived ICP output
    field can enter the patch (Approach C). Add the company connections to the returned `connections` via
    the same fan(chain(...), chain(...)) + research_conns merge the local-live function uses. Do not
    duplicate any business logic — mergeCompanies/judge/webResearch are already inlined.

    REVIEW-LOOP PRODUCER (review consensus #2 — this is the seam 16-02 Task 4 depends on). mergeCompanies'
    `canonicalPatch` contains ONLY `promote` decisions (mergeCompanies.js:209-211, VERIFIED); its
    `needs_review` decisions live in the `decisions` array and are otherwise dropped. So ENRICH_DECIDE_CO_CLOUD
    must, when ANY decision is `needs_review`: (a) write `lv_enrichment_needs_review=true`,
    `lv_enrichment_status="needs_review"`, `lv_enrichment_review_reason` (a short summary of the held
    reasons), and `lv_enrichment_review_candidate_json = stableStringify(the needs_review decisions
    subset)` — the HELD candidate values a human will approve; and (b) HOLD those fields' canonical writes
    (do not promote a needs_review field to canonical), while promote-decision fields still write.
    Define the hold-vs-apply rule explicitly in the node wrapper. This makes 16-02's reviewApply a real
    operation (it applies the held candidates post-approval) rather than a no-op re-application of already-
    written promotes. Reuse `stableStringify` from mergeCompanies (do not hand-roll a serializer).

    Write tests/test_cloud_companies_branch.py: load the built Cloud workflow, BFS from the Webhook
    Trigger over `connections`, and assert every company-branch node name is reachable, mirroring the
    graph-ancestry precedent in tests/test_judge_spec.py. Assert `Emit Company Targets` is NOT present in
    the built Cloud workflow (the fixture emitter did not leak in). Assert ENRICH_DECIDE_CO_CLOUD's
    canonical patch never contains a derived ICP output property name. Assert the producer contract: the
    ENRICH_DECIDE_CO_CLOUD jsCode references `lv_enrichment_review_candidate_json` and
    `lv_enrichment_needs_review` (the flags 16-02's reviewApply consumes).
  </action>
  <verify>
    <automated>python scripts/build_cloud_workflows.py && python -m pytest tests/test_cloud_companies_branch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_cloud_companies_branch.py passes: all company-branch node names are BFS-reachable from Webhook Trigger in the built Cloud workflow.
    - `grep -c '"name": "Merge Company"' n8n/wf_enrichment_cloud.json` returns at least 1 (the branch is present in the built output).
    - `grep -c '"name": "Emit Company Targets"' n8n/wf_enrichment_cloud.json` returns 0 (the fixture emitter did not leak into the Cloud webhook workflow — review #6).
    - The decide test asserts the company canonical patch contains none of the derived ICP output field names (score/tier/anti-icp/motion).
    - The producer contract holds: ENRICH_DECIDE_CO_CLOUD's jsCode writes lv_enrichment_needs_review / lv_enrichment_review_reason / lv_enrichment_review_candidate_json on needs_review and holds those fields' canonical writes (asserted by the branch test).
    - The full offline suite (`python -m pytest -q && node --test tests/n8n/`) shows zero regressions against the 201 pytest / 123 node baseline.
    - test_architecture_guard.py (incl. test_no_env_or_vars_in_cloud_workflows from Task 4) still green after the port.
  </acceptance_criteria>
  <reversibility rating="reversible">Builder-generated topology; revert the function edit and rebuild.</reversibility>
  <done>build_enrichment_cloud() carries the full companies ICP branch with credential-bound nodes and a gated cloud decide node, proven reachable and Approach-C-clean offline.</done>
</task>

<task type="auto">
  <name>Task 6: Harden the Cloud webhook write path (auth + event parse + real HubSpot nodes + fail-closed + write-safety gate)</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/test_cloud_write_path.py, .env.example</files>
  <read_first>
    - scripts/build_cloud_workflows.py build_enrichment_cloud(): the webhook node (~2055-2061), Build Identity (~2064 -> ENRICH_BUILD_IDENTITY ~662-682, reads direct email/domain fields), HubSpot Search placeholder (~2067-2073, empty filterGroupsUi), Adapt Search (~2077 -> ENRICH_ADAPT_SEARCH ~778-795), HubSpot Create (~2156-2160, maps only email), HubSpot Update (~2164-2168, empty updateFields + $json.contact_id never set), the IF Create/IF Enrich gate (~2192-2198)
    - n8n/code/enrichmentGate.js decideAction (VERIFIED: _isEmpty({}) -> action "create", line 61) — the reason a lookup FAILURE must not reach the gate as `{}`
    - CLAUDE.md §18.1 (webhook shared-secret Code node), §18.2 (HubSpot event parser: objectId/objectType/subscriptionType), §18.3 (object-type mapping contacts/companies), §21 (safety gates), §29 (MVP write allowlist)
    - HS_CO_SEARCH_BODY_EXPR (~1292, the real filterGroups envelope + property list incl. hs_object_id sourcing) — the shape the placeholder HubSpot Search must adopt
  </read_first>
  <action>
    This task closes the sol-only HIGH blockers (reviews #7/#8/#9), all VERIFIED against source. This is
    the FIRST phase to write live HubSpot records, so the webhook write path must be authenticated,
    correctly targeted, fail-closed, and gated — offline-provable via JSON-structure asserts (no live
    calls).

    (a) WEBHOOK AUTH + EVENT PARSE (review #7). The current Cloud webhook (:2055-2061) has no auth and
    forwards its raw body to Build Identity, which reads direct {email,domain,firstname} fields — but a
    HubSpot private-app webhook delivers an EVENT ARRAY of {objectId, objectType, subscriptionType,...},
    never those fields. Insert two nodes immediately after the Webhook Trigger, before Build Identity:
    (1) a "Verify Secret" Code node that checks the X-Enrichment-Secret header against a build-time
    constant name and throws on mismatch (CLAUDE.md §18.1 — the secret VALUE is a bound credential /
    provisioned constant, never inlined); (2) a "Parse HubSpot Event" Code node (CLAUDE.md §18.2) that
    normalizes the event array into per-record {object_type, object_id} items and routes via an
    object-type map (§18.3: contact->contacts branch, company->companies branch). Build Identity /
    Build Company Identity then fetch the real record by id rather than trusting direct body fields.
    If restructuring Build Identity to consume events is larger than this task's budget, the MINIMUM
    is: the Verify-Secret node exists and gates the graph, and the event parser exists and feeds the
    object-type router — document any remaining direct-field shim as a follow-up in the node's sticky note.

    (b) REAL HUBSPOT NODES + RECORD-ID PRESERVATION + FAIL-CLOSED (review #8). The HubSpot Search nodes
    must carry real filterGroups (contact: email EQ; company: domain EQ — reuse the HS_CO_SEARCH_BODY_EXPR
    envelope) and request hs_object_id in their property list. The Adapt Search / Adapt Company Search
    steps must preserve the top-level record id as `hs_object_id` on existingRecord (today they keep only
    `.properties` and DROP the id, so Update has no target). HubSpot Create must send the computed
    property patch (not only email); HubSpot Update must target `={{ $json.hs_object_id }}` (a real id,
    not the never-set $json.contact_id) and send the patch. Critically, a lookup FAILURE must FAIL CLOSED:
    because the Search node uses onError:continueRegularOutput and decideAction({}) returns `create`, a
    401/timeout/malformed response would currently create a DUPLICATE. Distinguish confirmed-absent (HTTP
    200 + zero results) from lookup-failed (non-200 / error item / malformed) in the Adapt step, tag the
    row `lookup_failed=true` on failure, and route lookup_failed rows to skip/needs_review, NEVER to
    create. (enrichmentGate.js is frozen — do this in the Adapt wrapper + the decide/route nodes, not by
    editing decideAction.)

    (c) WRITE-SAFETY GATE (review #9). Add a module-level `WRITE_SAFETY_DEFAULTS` build-time constant —
    SEPARATE from the parity-guarded CONFIG_FLAG_DEFAULTS (these are Cloud-write-only; local-live never
    writes HubSpot, so they must NOT enter the parity set) — carrying at minimum
    `ALLOW_HUBSPOT_RECORD_WRITES` (default false), a create switch (`ALLOW_HUBSPOT_CREATE`, default
    false), and a test-record allowlist (`TEST_RECORD_DOMAINS` / ids). Bake these into
    ENRICH_DECIDE_CLOUD and ENRICH_DECIDE_CO_CLOUD via the same AR-4 inlining as the flags, and have the
    IF Create / IF Enrich gates consult them so an activated-but-not-write-enabled workflow performs zero
    record writes and only writes records whose domain/id is on the allowlist. Add these to .env.example
    (Cloud build-time knobs) as commented guidance. Update the sticky notes to describe the write gate.

    Write tests/test_cloud_write_path.py (offline, structural): assert (1) a Verify-Secret node and a
    HubSpot-event parser node exist upstream of Build Identity in the built Cloud workflow; (2) both
    HubSpot Search nodes carry a non-empty filterGroups body and request hs_object_id; (3) the Adapt
    steps' jsCode preserves hs_object_id and tags lookup_failed, and the route/decide nodes never send a
    lookup_failed row to create; (4) HubSpot Update targets hs_object_id, not contact_id; (5) the built
    decide-node jsCode references ALLOW_HUBSPOT_RECORD_WRITES and the create switch as baked constants,
    and the IF-gate is downstream of that check.
  </action>
  <verify>
    <automated>python scripts/build_cloud_workflows.py && python -m pytest tests/test_cloud_write_path.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_cloud_write_path.py passes: Verify-Secret + event-parser nodes precede Build Identity; both HubSpot Search nodes have non-empty filterGroups and request hs_object_id.
    - The Adapt step preserves hs_object_id and tags lookup_failed; the route/decide logic proves a lookup_failed row is never routed to `create` (fail-closed).
    - HubSpot Update targets the real record id via an hs_object_id expression, and the built workflow's Update node no longer references the previously-hardcoded never-set contact-id field as its target (asserted structurally in the test).
    - `grep -c ALLOW_HUBSPOT_RECORD_WRITES n8n/wf_enrichment_cloud.json` returns at least 1 (write gate is a baked constant, default false); WRITE_SAFETY_DEFAULTS is NOT in CONFIG_FLAG_DEFAULTS (parity set unchanged — tests/test_builder_flag_parity.py still asserts exactly 6 flags).
    - `grep -rE '\$env\b|\$vars\b' n8n/wf_enrichment_cloud.json` still returns nothing (write-safety constants are baked, not env expressions).
    - Full offline suite (`python -m pytest -q && node --test tests/n8n/`) shows zero regressions vs the 201/123 baseline; enrichmentGate.js is git-unchanged.
  </acceptance_criteria>
  <reversibility rating="costly">Enables the first live HubSpot record writes once the operator sets the write-safety constants true and runs a live deploy; the code path is default-gated-off and offline-proven, and the live enable is a runbook step, so reversible in code even though a live write is costly.</reversibility>
  <done>The Cloud webhook write path is authenticated, parses HubSpot events, targets real record ids, fails closed on lookup failure, and performs no live record write unless the build-time write-safety gate is enabled — all proven offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script -> n8n Public API | N8N_API_KEY authenticates deploy/provision writes to a shared Cloud instance |
| local script -> HubSpot API | provider secrets read from env, credential bodies built and POSTed |
| n8n Cloud node -> provider API | secret must live in a bound credential, never in workflow JSON |
| internet -> Cloud webhook | inbound HubSpot event must present the shared secret before any provider spend or record write |
| Cloud webhook -> HubSpot record write | create/update must be write-safety-gated + target a confirmed record id, never a lookup-failure fallthrough |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01-01 | Information Disclosure | secrets in workflow JSON / logs / git | high | mitigate | Task 4 test_no_env_or_vars_in_cloud_workflows proves zero env-var/inlined-secret survives; scripts print status codes + diffs only (Task 1 acceptance greps for no secret interpolation) |
| T-16-01-02 | Tampering | deploy script wrong-instance / clobber | high | mitigate | `_instance_ok()` N8N_EXPECTED_URL guard + match-by-name idempotency + dry-run diff; two-key gate (Task 1) |
| T-16-01-03 | Spoofing/Tampering | credential-by-id misbinding / unbound-node import | high | mitigate | provision emits a name->id map + introspects GET /credentials/schema/{type}; deploy's `bind_credentials` attaches per-node credentials via a node-name->credential-name map (Lusha/Apollo/Anthropic disambiguated, not shared-type-matched) and fails closed on an unresolvable id (Task 1, review #1) |
| T-16-01-07 | Spoofing | unauthenticated Cloud webhook triggers provider spend / record writes | high | mitigate | Task 6 Verify-Secret node gates the graph on X-Enrichment-Secret before any downstream node (CLAUDE.md §18.1); event parser rejects non-HubSpot-shaped bodies (review #7) |
| T-16-01-08 | Tampering (data) | lookup failure creates a duplicate record | high | mitigate | Task 6 Adapt step distinguishes confirmed-absent (200+0) from lookup-failed (non-200/malformed) and routes lookup_failed to skip/needs_review, never create; Update targets a real hs_object_id (review #8) |
| T-16-01-09 | Elevation of Privilege | activation alone enables live record writes | high | mitigate | Task 6 WRITE_SAFETY_DEFAULTS (ALLOW_HUBSPOT_RECORD_WRITES default false + create switch + test-record allowlist) baked into the decide/IF gate; an activated-but-not-enabled workflow writes zero records (review #9) |
| T-16-01-10 | Tampering | review-loop candidate never produced (loop silently dead) | medium | mitigate | Task 5 ENRICH_DECIDE_CO_CLOUD writes the review flags + candidate JSON and holds canonical on needs_review; branch test asserts the producer contract 16-02 reviewApply consumes (review #2) |
| T-16-01-04 | Elevation of Privilege | two-key gate fails open | high | mitigate | Task 1 test asserts no HTTP write occurs unless DRY_RUN=false AND ALLOW_N8N_DEPLOY=true |
| T-16-01-05 | Tampering | non-clobber / Approach-C breach under live writes | high | mitigate | Task 5 reuses mergeCompanies canonicalPatch (never derived ICP outputs); ENRICH_DECIDE_CO_CLOUD test asserts absence |
| T-16-01-06 | Tampering (data) | ZoomInfo credential 401-loop | medium | mitigate | Task 2 requires whichever path preserves the existing "re-mint once then continue, not retry-forever" behavior proven in zoominfoToken.test.mjs |
| T-16-01-SC | Tampering (supply chain) | npm/pip installs | low | accept | No new package introduced this phase (16-RESEARCH Package Legitimacy Audit: Not applicable) — scripts reuse already-vetted requests/PyYAML |
</threat_model>

<verification>
- Full offline suite green: `python -m pytest -q && node --test tests/n8n/` — zero regressions vs 201/123 baseline.
- Cloud enrichment JSON: zero env-var expressions (word-boundary regex), companies branch reachable, no `Emit Company Targets` fixture emitter, no derived ICP outputs in any company canonical patch.
- Builder parity: both enrichment builders source the 6 flags + 6 secrets from one shared constant (`CONFIG_FLAG_DEFAULTS`/`SECRET_ENV_NAMES`); `tests/test_builder_flag_parity.py` proves local-replica and Cloud cannot diverge silently (Criterion 5 parity clause). WRITE_SAFETY_DEFAULTS is Cloud-only and NOT in the parity set.
- Cloud write path (Task 6): Verify-Secret + event-parser precede Build Identity; HubSpot Search carries real filters + hs_object_id; lookup failure fails closed (never create); Update targets hs_object_id; record writes gated by ALLOW_HUBSPOT_RECORD_WRITES (default false).
- Credential binding: deploy attaches per-node credentials from the provisioned name->id map and fails closed on an unresolvable id.
- Both scripts skip to exit 0 with no n8n creds, refuse writes without the two-key gate, and `_instance_ok()` refuses a non-`.n8n.cloud` host when N8N_EXPECTED_URL is unset (no fail-open).

## Manual-Only (operator runbook, non-gating)
- Live: run the ZoomInfo OAuth2 spike on a scratch Cloud workflow (Task 2), then `provision_n8n_credentials.py` (creates ~5 credential objects for the 6 secrets, writes the name->id map) and `deploy_n8n_workflows.py` with both keys set; confirm the credentials + workflows appear via GET and each node is credential-bound. Live property creation of the two SJ-3 control properties via the existing sync tooling. A first live record write requires setting ALLOW_HUBSPOT_RECORD_WRITES + the test-record allowlist and is bounded to allowlisted records.
</verification>

<success_criteria>
Deployable half complete when Criteria 5-8 and the SJ-3 property prerequisite are proven offline (four passing test additions), the ZoomInfo path is decided, and the live deploy/provision path exists behind the two-key gate as a documented operator runbook.
</success_criteria>

## Artifacts this phase produces (Plan 16-01)

| Artifact | Kind | Criterion |
|----------|------|-----------|
| scripts/deploy_n8n_workflows.py (`compute_workflow_diff`, `_has_n8n`, `_instance_ok` no-fail-open, `_writes_allowed`, `bind_credentials`, `main`) | new script | 7 |
| scripts/provision_n8n_credentials.py (~5-credential manifest for 6 secrets, schema introspection, create-if-missing, name->id map emission) | new script | 6 |
| build_enrichment_cloud() companies branch + `ENRICH_DECIDE_CO_CLOUD` constant (review-loop producer, no fixture emitter) | modified builder | 8 |
| Cloud write-path hardening: Verify-Secret + HubSpot event parser + real Search/Create/Update + fail-closed Adapt + `WRITE_SAFETY_DEFAULTS` gate | modified builder | 7/8 (reviews #7/#8/#9) |
| tests/test_cloud_write_path.py | new test | 7/8 |
| Credential-bound cloud nodes + 6 build-time flag constants + updated sticky notes | modified builder | 5 |
| `CONFIG_FLAG_DEFAULTS`/`SECRET_ENV_NAMES` shared single-source + build_enrichment_local_live() refactor | modified builder | 5 (parity) |
| ZoomInfo credential path (native OAuth2 or split mint-node, per Task 2) | decision + builder | 5 |
| config/hubspot_properties.yaml: `lv_enrichment_requested`, `lv_enrichment_status` (x2 objects) | manifest entries | 1 enabler |
| tests/test_deploy_n8n_workflows.py | new test | 7 |
| tests/test_cloud_companies_branch.py | new test | 8 |
| tests/test_architecture_guard.py::test_no_env_or_vars_in_cloud_workflows | test addition | 5 |
| tests/test_builder_flag_parity.py | new test | 5 (parity) |
| tests/test_hubspot_properties_config.py (SJ-3 property assertions) | test addition | 1 enabler |
| .env.example: N8N_EXPECTED_URL guidance | config | 7 |

<output>
Create `.planning/phases/16-scheduled-workflows-review-surface/16-01-SUMMARY.md` when done.
</output>
