---
phase: 46-rubric-decision-simulation-engine-parity
plan: 05
type: execute
wave: 5
depends_on: [46-04]
files_modified:
  - docs/business/icp-scoring.md
  - CLAUDE.md
  - .planning/intel/constraints.md
  - .planning/intel/requirements.md
  - docs/WEB-RESEARCH-SPEC.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RUBRIC-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No live document prints a superseded org-type weight or a gambling graduated deduction: docs/business/icp-scoring.md, CLAUDE.md §10, .planning/intel/constraints.md, .planning/intel/requirements.md and docs/WEB-RESEARCH-SPEC.md all agree numerically with config/icp_scoring.yaml."
    - "docs/business/icp-scoring.md still carries the Club/Team closed-deal finding with its win rate and its sample size intact, alongside a recorded GTM override — the evidence is annotated, never rewritten to agree with the new weight."
    - "Every doc describes the regulator deduction in its as-built shape (a direct negative org-type weight) rather than as a new graduated-deductions entry."
    - "Nothing under .planning/milestones/ is modified, and .planning/PROJECT.md's three open-question references to the club weight are left as-is for milestone close."
    - "REQUIREMENTS.md RUBRIC-03 and the ROADMAP Phase 46 entry carry a dated amendment note recording the engine-count finding and the status of success criterion 4."
  prohibitions:
    - "No file under .planning/milestones/ may be modified."
    - "The closed-deal evidence in docs/business/icp-scoring.md must not be rewritten, softened or removed to agree with the new weights."
    - ".planning/PROJECT.md must not be edited in this phase."
    - "ROADMAP.md and REQUIREMENTS.md must be changed by scoped edits only, never rewritten wholesale."
  artifacts:
    - docs/business/icp-scoring.md
    - CLAUDE.md
    - .planning/intel/constraints.md
    - .planning/intel/requirements.md
    - docs/WEB-RESEARCH-SPEC.md
  key_links:
    - "config/icp_scoring.yaml -> every doc that prints a weight table (the numbers must agree)"
    - "46-DECISION.md -> docs/business/icp-scoring.md's recorded override (the business doc cites the decision record rather than re-deriving it)"
    - "46-ENGINE-INVENTORY.md -> the REQUIREMENTS.md RUBRIC-03 and ROADMAP Phase 46 amendment notes"
---

<objective>
Bring every live document into agreement with the rubric of record, without destroying the
evidence the rubric overrode.

Purpose: a weight table in a doc that disagrees with the engine is the same split-brain failure
class as an unported value — just with humans as the consumer (D-13). This runs after sign-off
deliberately: editing the business doc before the operator locked the values risks documenting a
number that gets overridden in review.

Output: five live documents updated, two planning documents given dated amendment notes, and one
business sign-off document whose evidentiary voice survives the edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-DECISION.md
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-CONTEXT.md
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync every live document that prints the superseded rubric</name>
  <files>docs/business/icp-scoring.md, CLAUDE.md, .planning/intel/constraints.md, .planning/intel/requirements.md, docs/WEB-RESEARCH-SPEC.md</files>
  <read_first>
    - `config/icp_scoring.yaml` (post-Plan-04 — the numbers every doc must agree with)
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-DECISION.md` (the override reasoning the business doc records rather than re-derives)
    - `docs/business/icp-scoring.md` §3 assumption scorecard, §4 best-fit/anti-ICP bullets, §5 scoring model table, graduated-deductions table, property-map table and tier illustration
    - `CLAUDE.md` §10.1 (the inline copy of `config/icp_scoring.yaml`) and §10.3 (the graduated-deductions prose)
    - `.planning/intel/constraints.md` (the machine-readable rubric mirror — one long line carrying the full org-type map and the deductions)
    - `.planning/intel/requirements.md` (the gambling-operators-are-a-deduction statement)
    - `docs/WEB-RESEARCH-SPEC.md` (the gambling-deduction semantics and the worked-example table near the end, which includes both an Australian Turf Club row and a FanDuel row describing a deduction)
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-CONTEXT.md` D-13 and D-14
  </read_first>
  <action>
    Update every live site so no document prints a value the engine no longer computes.

    **Work from a grep sweep, not only from D-13's table.** D-13's list was compiled before
    Plan 04's as-built shape was settled and is not guaranteed exhaustive — a sweep of the repo
    (excluding `.planning/milestones/` and `.planning/PROJECT.md`) for the superseded numbers and
    for prose describing gambling as a graduated deduction turns up at least one site D-13's table
    omits, in `docs/WEB-RESEARCH-SPEC.md`'s worked-example table where a gambling row is described
    as producing both a deduction and a veto. Fix every site the sweep finds; list any site beyond
    D-13's table in the SUMMARY.

    **Describe the as-built shape, not CONTEXT.md's projected one.** D-13's table says to add a
    regulator row to the graduated-deductions table. That instruction is stale: Plan 04 implemented
    the regulator deduction as a direct negative value in the org-type weight table, per
    46-RESEARCH.md Open Question 5 and as recorded in `46-DECISION.md`. Docs must describe what
    exists — a regulator org-type weight of -20 — not a graduated-deductions entry that was never
    created. Getting this wrong would reintroduce a doc/engine mismatch inside the very phase that
    exists to kill one. Note the discretion call where a reader would otherwise expect the
    CONTEXT.md shape.

    **`docs/business/icp-scoring.md` is a business sign-off document, not a config mirror (D-14).**
    Update its numbers, and preserve its evidentiary voice:
    - §5 scoring model table: the individual-club org-type weight becomes 15; the regulator weight
      becomes -20.
    - §5 graduated-deductions table: the gambling row is removed. If that empties the table, say
      plainly that there are no graduated deductions rather than deleting the section.
    - §5 property-map table: the statement that gambling and high revenue are graduated deductions
      is now true of revenue only.
    - §5 tier illustration: the individual-club, AU, content example is no longer a C-tier
      nurture-via-league case — it lands at 45 and Tier B.
    - §4 anti-ICP bullets: clubs are no longer described as suppress/disqualify, and gambling is no
      longer described as a graduated deduction.
    - §3/§4 evidence: the Club/Team closed-deal finding stays on the page with its win rate and
      sample size unchanged and its original sense intact, and the QRIC and gambling findings stay
      likewise. Add the GTM override next to each, citing `46-DECISION.md` rather than re-deriving
      its reasoning, so a reader sees both what the closed-deal data said and what the business
      decided in spite of it. Do not soften the evidence to reduce the contradiction — the
      contradiction is the record RUBRIC-01 depends on and the next recalibration reads.

    **`CLAUDE.md` §10.1** carries an inline copy of `config/icp_scoring.yaml`: update the two
    org-type values and remove the gambling graduated-deduction entry so the inline copy matches
    the file byte-for-byte in the values it prints. **§10.3**'s prose listing what the graduated
    deductions include must stop naming gambling; if revenue above 500M is now the only one, say
    so.

    **`.planning/intel/constraints.md`** is a machine-readable rubric mirror read by other agents —
    update the org-type map and the deductions inline, keeping its existing single-line format.
    **`.planning/intel/requirements.md`**'s statement that gambling operators are a graduated
    deduction and must never set the anti-ICP flag: the second half stays true and load-bearing
    (gambling still sets no veto); the first half does not. Rewrite so both halves are accurate.

    **`docs/WEB-RESEARCH-SPEC.md`**: the gambling-only-drives-a-graduated-deduction statement, the
    Australian Turf Club low-score-path worked example, and the gambling worked-example row.
    Update each to the post-change behaviour. Leave the detection semantics
    (how `lv_is_gambling_operator` is determined and the disjointness assertion) untouched —
    detection is unchanged; only the scoring consequence moved.

    **Do not touch** anything under `.planning/milestones/` — roughly a dozen archived files quote
    the old org-type map and all stay verbatim as the historical record, load-bearing for audit.
    **Do not touch `.planning/PROJECT.md`** — its three references frame the club weight as an open
    question, which stays accurate until milestone close, where it is updated.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0
    - `git status --porcelain .planning/milestones/ .planning/PROJECT.md` prints nothing
    - `grep -rn "individual_club_team:5\|individual_club_team: 5\|regulator:5\|regulator: 5" docs/ CLAUDE.md .planning/intel/` returns no matches
    - `.planning/intel/constraints.md` prints `individual_club_team:15` and `regulator:-20` and no longer prints a gambling graduated deduction of -20
    - `CLAUDE.md` §10.1's inline org-type map prints the same nine values as `config/icp_scoring.yaml`, and §10.3 no longer names gambling as a graduated deduction
    - `docs/business/icp-scoring.md` still contains the Club/Team win-rate percentage and its sample size, and contains a reference to `46-DECISION.md`
    - `docs/business/icp-scoring.md` §5's tier illustration for an AU content-producing individual club states Tier B
    - No document describes the regulator deduction as a `graduated_deductions` entry
    - `docs/WEB-RESEARCH-SPEC.md`'s gambling worked-example row no longer states that gambling produces a deduction
    - The SUMMARY lists every site fixed beyond D-13's table
  </acceptance_criteria>
  <done>Every live document agrees numerically with `config/icp_scoring.yaml`, describes the deduction in its as-built shape, and — in the business document — carries both the closed-deal evidence and the recorded override.</done>
  <reversibility rating="reversible">Prose and table edits, revertible by git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record the engine-count amendment in REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-ENGINE-INVENTORY.md` (the finding being recorded)
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-DECISION.md` (its engine-count section and its statement on success criterion 4)
    - `.planning/REQUIREMENTS.md` RUBRIC-03 (note it already carries a dated amendment note from 2026-08-11 — follow that format)
    - `.planning/ROADMAP.md` § Phase 46 (the "Scope amended 2026-08-11" paragraph and success criteria 3 and 4 — follow that format)
  </read_first>
  <action>
    Record what was actually found, so the requirement text and the roadmap stop asserting
    something the evidence contradicts.

    Use scoped `Edit` operations only. Never pass the whole file to a write — `.planning/ROADMAP.md`
    carries every phase of the milestone and a wholesale rewrite destroys entries outside the
    change window.

    In `.planning/REQUIREMENTS.md`, append a dated amendment note to RUBRIC-03 in the same style as
    its existing 2026-08-11 note: state how many engines were found to carry an org-type point
    table, name them, and — where an engine named in the requirement text carries no weight table —
    state that the requirement is satisfied for that leg by a permanent absence-guard test
    (`tests/test_n8n_org_type_absence.py`) rather than by a port, citing `46-ENGINE-INVENTORY.md`.
    Leave the original requirement text intact above the note; the amendment records the
    correction, it does not erase what was originally written.

    In `.planning/ROADMAP.md`'s Phase 46 entry, add a dated note under the success criteria
    recording the same finding and stating the status of success criterion 4 explicitly — either
    that it was satisfied with live read-back evidence, or that it is not triggered because no
    weight reached the live n8n workflow, with the condition that would re-trigger it. Do not
    silently mark criterion 4 done, and do not delete it.

    The same note must also qualify success criterion 3, which names
    `scripts/run_scoring_parity.py`. That criterion is satisfied at *engine* level — the offline
    harness, the flow conformance tests and Plan 04's running-content read-back all agree with the
    rubric of record — while the *live, record-level* sweep is red by design from the moment
    Plan 04 committed the new weights until Phase 49 re-scores the population, because it compares
    old-weight live scores against the new-weight oracle. State this, name Phase 49 as the phase
    that closes the window, and cite `46-DECISION.md`'s red-window section. Without this note a
    verifier reading criterion 3 literally will run the live sweep, see the expected red, and flag
    the phase for a failure the phase deliberately created and recorded.

    Do not restate the weight decisions here — `46-DECISION.md` owns those. These two edits record
    only the engine-count correction and the criterion-4 status.
  </action>
  <verify>
    <automated>grep -c '46-ENGINE-INVENTORY.md' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` RUBRIC-03 carries a new dated amendment note naming the engine count found and citing `46-ENGINE-INVENTORY.md`, with the original requirement text still present above it
    - `.planning/ROADMAP.md`'s Phase 46 entry carries a dated note stating success criterion 4's status explicitly, and criterion 4 itself is still present
    - The same note states that success criterion 3 is satisfied at engine level while the live record-level parity sweep is expected red until Phase 49 closes the window, and names Phase 49
    - `git diff --stat .planning/ROADMAP.md` shows only additions within the Phase 46 section — no other phase entry is touched
    - `git status --porcelain .planning/milestones/ .planning/PROJECT.md` prints nothing
  </acceptance_criteria>
  <done>The requirement and roadmap text no longer assert an engine count the evidence contradicts, and success criterion 4's status is stated rather than assumed.</done>
  <reversibility rating="reversible">Scoped documentation edits.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → human decision-making | Other agents read `.planning/intel/*` and `CLAUDE.md` §10 as machine-readable rubric mirrors; a stale value there propagates into future planning as if it were the engine's truth |
| business evidence → future recalibration | `docs/business/icp-scoring.md` is the closed-deal record the next rubric revision reads; evidence edited to agree with a later decision cannot be recovered |
| scoped edit → planning ledger | A wholesale rewrite of `ROADMAP.md` destroys phase entries outside the change window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-07 | Tampering | closed-deal evidence in `docs/business/icp-scoring.md` | high | mitigate | D-14 prohibition, plus acceptance criteria requiring the win rate and sample size to survive verbatim and the override to be recorded alongside rather than in place of the evidence |
| T-46-13 | Tampering | archived milestone artefacts | high | mitigate | Explicit prohibition plus a `git status --porcelain .planning/milestones/` acceptance criterion on both tasks |
| T-46-14 | Tampering | `ROADMAP.md` collateral loss from a wholesale write | medium | mitigate | Scoped `Edit` only, asserted by a `git diff --stat` criterion showing changes confined to the Phase 46 section |
| T-46-15 | Repudiation | doc/engine mismatch reintroduced by copying CONTEXT.md's projected shape | medium | mitigate | The action states the as-built shape explicitly and an acceptance criterion asserts no document describes the regulator deduction as a graduated-deductions entry |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — 46-RESEARCH.md § Package Legitimacy Audit records N/A for the phase |
</threat_model>

<artifacts_this_phase_produces>
This plan's additions (full phase list in `46-01-PLAN.md`):
- No new files, symbols, config keys, CLI flags or tests. Five live documents are edited in place,
  and dated amendment notes are appended to `.planning/REQUIREMENTS.md` RUBRIC-03 and the
  `.planning/ROADMAP.md` Phase 46 entry.
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest -q` green
- No document prints a superseded org-type weight or a gambling graduated deduction
- The Club/Team closed-deal finding survives with its percentage and sample size
- Nothing under `.planning/milestones/` and nothing in `.planning/PROJECT.md` changed
- The engine-count amendment notes exist in both planning documents

</verification>

<success_criteria>
- Every live document agrees with `config/icp_scoring.yaml`
- The business sign-off document records both the evidence and the override
- The engine-count finding is recorded where the original claim was made
- The historical record is untouched
</success_criteria>

<output>
Create `.planning/phases/46-rubric-decision-simulation-engine-parity/46-05-SUMMARY.md` when done.
Record: every site updated including any beyond D-13's table, the as-built shape used for the
regulator deduction, and confirmation that the milestone archive and PROJECT.md are untouched.
</output>
