---
phase: 46-rubric-decision-simulation-engine-parity
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - scripts/simulate_rubric_weights.py
  - tests/test_simulate_rubric_weights.py
  - .planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md
autonomous: true
requirements: [RUBRIC-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The simulation's row set comes from the live HAS_PROPERTY(lv_icp_fit_score) company search — the same query scripts/run_scoring_parity.py::_select_sample_ids already uses — and never from config/june_candidates.json or a committed snapshot."
    - "Every simulated row carries three distinct columns: the live lv_icp_fit_score/lv_icp_tier as HubSpot holds them today, the oracle's score/tier under the current rubric, and the oracle's score/tier under the proposed rubric."
    - "Every row whose lv_org_type is blank, and every row carrying a non-ANZ veto with a blank lv_country_region_normalized, is flagged in the report so it is not misread as a genuine outcome (D-10)."
    - "A run over an empty row set produces a loud failure verdict and a non-zero exit code, never a report reading as 'nothing changed'."
    - "The report states the club-weight sensitivity at 10 and 20 alongside the primary 15, so the distance to the Tier B floor at 40 is visible before sign-off."
    - "Any divergence between the live row-set IDs and 41-final-population.json's 66 IDs is recorded in the report as a finding, with both counts and the symmetric difference — never silently reconciled."
  prohibitions:
    - "The simulation must never write to any HubSpot record."
    - "config/june_candidates.json must never be the simulation's source of record data."
    - "The simulation must not fall back to a committed snapshot when live credentials are unavailable."
  artifacts:
    - .planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md
    - .planning/phases/46-rubric-decision-simulation-engine-parity/46-simulation-{YYYYMMDD}.json
  key_links:
    - "src/hubspot_client.search_records HAS_PROPERTY(lv_icp_fit_score) -> the simulation's authoritative row set"
    - "tests/scoring_fixtures.py::fetch_for_parity -> per-row live property read (GET only)"
    - "46-SIMULATION-REPORT.md -> the evidence Plan 03's 46-DECISION.md cites and RESCORE-03 reuses"
---

<objective>
Produce the artefact RUBRIC-02 exists for: a per-company before/after re-tier view of the live
scored population under the proposed weights, computed from current `lv_*` inputs, writing nothing
to HubSpot.

Purpose: this is what the operator signs off against in Plan 03. It has to be trustworthy enough
to carry a decision that re-tiers more than half the scored list, and honest enough that the 17
false-veto records and 18 blank-`lv_org_type` records do not masquerade as genuine outcomes.

Output: `scripts/simulate_rubric_weights.py` extended from Plan 01's one-record tracer to the full
live row set, and the committed markdown report plus its JSON twin.

`config/icp_scoring.yaml` remains untouched in this wave — the proposed rubric still exists only
in `PROPOSED_OVERRIDES` and in memory. See Plan 01's `<objective>` for why the weight commit waits
for sign-off.
</objective>

<flagged_assumptions>
- **Population freshness.** 46-RESEARCH.md's 66-company evidence
  (`41-final-population.json`, `hs_lastmodifieddate` 2026-08-08) is three days stale and four
  phases old. Task 1 re-derives the row set live and Task 2 records any divergence as a finding.
  If the live search returns a materially different set, that is a result to report, not a
  discrepancy to reconcile away.
- **Probe row `RUBRIC-02 / unclassified`** remains flagged (see Plan 01) and is covered here by
  the prohibitions above plus Plan 01 Task 3's test.
</flagged_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-CONTEXT.md
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-RESEARCH.md
@.planning/phases/46-rubric-decision-simulation-engine-parity/46-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the simulation to the full live row set, with annotations and sensitivity</name>
  <files>scripts/simulate_rubric_weights.py, tests/test_simulate_rubric_weights.py</files>
  <read_first>
    - `scripts/simulate_rubric_weights.py` (Plan 01's tracer — current shape)
    - `tests/test_simulate_rubric_weights.py` (Plan 01's tests, including the zero-write proof)
    - `scripts/run_scoring_parity.py` lines 149-165 (`_select_sample_ids`) and 366-372 (false-green guard) and 413-420 (`_write_report`)
    - `tests/scoring_fixtures.py` (`FIT_SCORE_PROPS` — the exact property slice available per row)
    - `src/hubspot_client.py` (`search_records` signature and its 100-result limit)
    - `.planning/milestones/v0.7-phases/41-validation-data-import-end-to-end-proof/41-final-population.json` (cross-check only — 66 ids, 18 blank `lv_org_type`)
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-RESEARCH.md` § Open Question 1 and § Open Question 2
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-CONTEXT.md` D-09, D-10
  </read_first>
  <action>
    Grow the tracer into the full simulation. Everything here is unit-testable offline via the
    injected `fetch_fn`; no live call is made in this task.

    Complete `PROPOSED_OVERRIDES` to all three levers: `base_score.org_type.individual_club_team`
    → 15 (D-01), `base_score.org_type.regulator` → -20 (D-02), and
    `graduated_deductions.gambling_operator` → `None` meaning delete the key (D-03). D-02 is a
    direct negative weight in the existing `base_score.org_type` map, **not** a new
    `graduated_deductions` key — 46-RESEARCH.md Open Question 5 verified by execution that the
    arithmetic is identical (`0 + -20 == -20`, no floor or clamp in the oracle) and that the
    direct-weight shape needs zero new engine logic and zero conformance-test changes. This
    resolves CONTEXT.md's "Claude's Discretion" bullet on the deduction's shape and supersedes
    D-06's "new engine logic" framing; note the supersession in the report.

    Add `SCENARIOS`: the primary scenario carries all three levers with the club weight at 15;
    two sensitivity scenarios carry the identical D-02/D-03 changes with the club weight at 10 and
    at 20. Sensitivity output is tier-only per row plus a distribution line — the operator needs
    to see how close the Tier B floor at 40 sits, not a third full table.

    Add `_select_row_ids()` mirroring `run_scoring_parity.py::_select_sample_ids` exactly: an
    explicit id-list override first (env or `--ids`), otherwise the live
    `HAS_PROPERTY(lv_icp_fit_score)` company search with `limit=100`. Do not invent a second
    definition of "the scored population". Load
    `41-final-population.json`'s id set as a *cross-check only* and compute the symmetric
    difference; carry both counts and the difference into the report payload as a finding field.
    `config/june_candidates.json` is not read by this script at all.

    Add `build_simulation(ids, fetch_fn, current_cfg, scenarios)` returning a payload carrying:
    one row per id with the three columns from Plan 01 plus the sensitivity tiers and the D-10
    flags; a tier-distribution summary per scenario (live, oracle-current, oracle-proposed);
    the row-set divergence finding; a movement summary (how many rows change tier, broken down by
    `lv_org_type`); and the run metadata (UTC timestamp, portal id, the literal
    `PROPOSED_OVERRIDES` as applied). Carry `run_scoring_parity.py`'s false-green discipline
    across: zero rows simulated must produce a failure verdict and a non-zero exit, never an empty
    report that reads as "nothing moved".

    D-10 annotation (format is Claude's discretion — choose one and apply it consistently): flag
    a row `blank_org_type` when its `lv_org_type` is absent or empty, and `false_veto` when
    `lv_anti_icp_flag` is true and `lv_anti_icp_reason` names non-ANZ geography while
    `lv_country_region_normalized` is blank. Both are derived from the live properties already in
    `FIT_SCORE_PROPS` — do not add a property to that list. Every flagged row must be visibly
    flagged in the markdown table itself, not only in a footnote, and the report must state
    plainly that flagged rows are shown as-is with no projected or speculative column (D-10) and
    that the 17 are Phase 47's scope and the 18 are Phase 48's.

    Add `render_markdown(payload)` emitting the D-09 deliverable: a per-company table (name where
    available, HubSpot id, `lv_org_type`, flags, live score/tier, oracle-current score/tier,
    proposed score/tier, sensitivity tiers), the tier-distribution summary, the movement summary,
    and the row-set finding. Write it so Phase 49's RESCORE-03 before/after can be lifted from it
    directly. Add `_write_report(payload)` emitting the JSON twin, date-stamped, following
    `run_scoring_parity.py`'s shape.

    Extend `tests/test_simulate_rubric_weights.py` with offline cases driven by a stubbed
    `fetch_fn` over hand-built property dicts: the three-column separation is preserved when the
    oracle disagrees with the live tier (the false-veto shape); a blank-`lv_org_type` row is
    flagged and contributes 0 org-type points in both scenarios; a regulator row moves to 10 and
    Unscored under the proposed rubric; a gambling row gains 20 under the proposed rubric; an
    empty row set produces the failure verdict and non-zero exit; the row-set divergence finding
    is populated when the stub's ids differ from the cross-check list. Preserve every assertion
    from Plan 01 Task 3 unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_simulate_rubric_weights.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_simulate_rubric_weights.py -q` exits 0
    - `.venv/bin/python -m pytest -q` exits 0
    - `scripts/simulate_rubric_weights.py` grep for `june_candidates` returns no matches
    - `PROPOSED_OVERRIDES` contains exactly three entries covering `individual_club_team`, `regulator`, and `graduated_deductions.gambling_operator`, and no `graduated_deductions` key is *added* by it
    - `SCENARIOS` defines exactly three scenarios whose only difference is the club weight (10 / 15 / 20)
    - A test asserts an empty row set yields a non-zero exit code and a verdict string containing a failure marker
    - A test asserts a row where live tier and oracle-current tier disagree keeps both values distinct in the payload
    - A test asserts a blank-`lv_org_type` row is flagged and scores 0 org-type points under both current and proposed rubrics
    - `git diff config/icp_scoring.yaml` is empty
  </acceptance_criteria>
  <done>The simulation covers the full live row set with three honest columns, D-10 flags, sensitivity at 10/15/20, and a false-green guard — all proven offline against stubs.</done>
  <reversibility rating="reversible">Script and test edits only; no config, no engine, no record touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the simulation live and commit the report</name>
  <files>.planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md</files>
  <precondition>Live HubSpot read credentials are reachable in this session (`HUBSPOT_PRIVATE_APP_TOKEN` present and `HUBSPOT_PORTAL_ID == 22617666` after `load_dotenv()`). Repo memory records that `.env` is Read/Bash permission-blocked but that read-only API calls succeed through the documented `python -c "from dotenv import load_dotenv; load_dotenv(); runpy.run_path(...)"` driver. If the portal id does not match or the token is absent, HALT and report — do not substitute a snapshot.</precondition>
  <read_first>
    - `scripts/simulate_rubric_weights.py` (as completed in Task 1)
    - `scripts/fetch_hubspot_flow.py` docstring (the exact `python -c` operator-invocation pattern this repo uses when `.env` cannot be sourced directly)
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-RESEARCH.md` § Open Question 1 (the caveat that the committed snapshot is stale and must be re-derived, not trusted)
  </read_first>
  <action>
    Execute the simulation against the live portal and commit its output.

    Invoke through the repo's `load_dotenv()` + `runpy.run_path` driver rather than assuming the
    shell environment carries the credentials. The run is read-only — Plan 01 Task 3's test is the
    standing proof — so no arming flag is involved and none should be set.

    **Do not fall back to `41-final-population.json` or `config/june_candidates.json` if the live
    read fails.** D-08 fixes the simulation's source as current live `lv_*` inputs; a snapshot-
    sourced report would be a different artefact wearing this one's name. On credential or portal
    failure, halt and surface the blocker so the operator can run it.

    Commit the generated markdown as
    `.planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md` and the
    JSON twin alongside it. Prepend a short header to the markdown carrying: the UTC run
    timestamp, portal id 22617666, the row count actually simulated, the row-set cross-check
    result against `41-final-population.json` (both counts and the symmetric difference, stated
    even when empty), and one paragraph in plain language telling a non-technical reader what the
    three score columns mean and why flagged rows are not genuine outcomes.

    If the live row count is not 66, say so prominently at the top of the report and carry the
    actual number everywhere the report speaks about population size. Do not describe the run as
    covering "the 66" unless it did.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md && grep -c '22617666' .planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md` exists and its header carries a UTC timestamp, portal id `22617666`, and the simulated row count
    - A date-stamped JSON twin exists in the same directory
    - The markdown contains a per-company table whose row count equals the stated simulated row count
    - The report states the row-set cross-check against `41-final-population.json` explicitly, including the case where the sets match exactly
    - The report contains a tier-distribution summary for live, oracle-current, and proposed, and sensitivity tier counts for the 10 and 20 club-weight scenarios
    - Every blank-`lv_org_type` row and every false-veto row is flagged inside the table
    - `git diff config/icp_scoring.yaml` is empty and no HubSpot record was modified
  </acceptance_criteria>
  <done>A committed, dated, portal-attributed report shows the operator exactly how the live scored population re-tiers under the proposed weights, with flagged rows visibly flagged.</done>
  <reversibility rating="reversible">Read-only run; the artefact is a committed document.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → HubSpot CRM v3 API | Live company reads across the whole scored population; the largest live surface this phase touches |
| report artefact → operator decision | The report drives a decision that re-tiers more than half the scored list; a misleading column is a correctness risk, not a cosmetic one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | `scripts/simulate_rubric_weights.py` live run | high | mitigate | Plan 01 Task 3's zero-write test remains green and unmodified; Task 2 sets no arming flag; the prohibitions above are contractual |
| T-46-02 | Tampering | portal targeting on the live run | medium | mitigate | `EXPECTED_PORTAL_ID` asserted before any call; Task 2's precondition halts on mismatch rather than proceeding |
| T-46-05 | Repudiation | report provenance | medium | mitigate | Report header carries UTC timestamp, portal id, row count, and the literal applied overrides — the artefact states what produced it |
| T-46-06 | Information Disclosure | stale-snapshot substitution presented as live | medium | mitigate | Explicit prohibition against snapshot fallback plus the halt-on-credential-failure precondition; the cross-check result is reported, never used as a source |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — 46-RESEARCH.md § Package Legitimacy Audit records N/A for the phase |
</threat_model>

<artifacts_this_phase_produces>
This plan's additions (full phase list in `46-01-PLAN.md`):
- `.planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md`
- `.planning/phases/46-rubric-decision-simulation-engine-parity/46-simulation-{YYYYMMDD}.json`
- New symbols in `scripts/simulate_rubric_weights.py`: `SCENARIOS`, `_select_row_ids`,
  `build_simulation`, `render_markdown`, `_write_report`
- New CLI flags on `scripts/simulate_rubric_weights.py`: `--ids`, `--out-dir`
- `PROPOSED_OVERRIDES` grows from one entry to three (no new key added to `config/icp_scoring.yaml`)
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest -q` green
- The committed report's row count matches its own header and its table
- No HubSpot record modified: `scripts/simulate_rubric_weights.py` still passes the zero-write test unchanged
- `config/icp_scoring.yaml` unchanged through this wave
</verification>

<success_criteria>
- The operator can read one document and see, per company, what HubSpot says today, what the
  oracle says today, and what the oracle would say under the proposed weights
- Flagged rows are unmistakably flagged
- The sensitivity at 10 and 20 makes the Tier B floor's proximity visible before sign-off
- The row set is live-derived and any divergence from the committed snapshot is on the page
</success_criteria>

<output>
Create `.planning/phases/46-rubric-decision-simulation-engine-parity/46-02-SUMMARY.md` when done.
Record: the actual live row count, the cross-check result against `41-final-population.json`, the
tier-movement headline, and whether the run required an operator-supplied credential step.
</output>
