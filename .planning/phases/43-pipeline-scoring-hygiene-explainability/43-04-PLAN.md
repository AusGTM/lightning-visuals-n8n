---
phase: 43-pipeline-scoring-hygiene-explainability
plan: 04
type: execute
wave: 2
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - .planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md
  - docs/reports/
autonomous: false
requirements: [PIPE-01, PIPE-03, PIPE-04]

user_setup:
  - service: hubspot
    why: "Every task in this plan reads or writes live HubSpot data; .env is Read/Bash permission-blocked to the agent, so the operator runs each command themselves."
    env_vars:
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: "repo .env (operator-readable only)"
      - name: HUBSPOT_PORTAL_ID
        source: "repo .env — must equal 22617666 or every fixture refuses to run"
  - service: claude-plugin-marketplace
    why: "The marketplace clone never auto-fetches; without a manual refresh the new skill's version stays invisible and the Update button stays greyed out (C5)."
    dashboard_config:
      - task: "Fetch and hard-reset the marketplace clone, then update the plugin"
        location: "~/.claude/plugins/marketplaces/lightning-visuals-operator"

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The HubSpot EQ filter demonstrably matches a record the fixed pipeline would write — proven against the real Search API, not inferred from offline greps (D-08)."
    - "What HubSpot actually does with a bare-boolean PATCH to a booleancheckbox property is recorded as measured fact, not left as a three-way assumption (research Pitfall 5)."
    - "lv_icp_score_breakdown holds valid, rubric-stamped JSON on a real record."
    - "It is known and recorded whether lv_closed_lost_reason exists in this portal, and whether the deal-to-company join is reliable."
    - "The new plugin skill is installed and invocable by the operator."
  artifacts:
    - .planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md
    - "a dated loss-reason report under docs/reports/"
  key_links:
    - "43-01 authored tests/test_review_flag_eq_filter.py; this plan is where it actually runs."
    - "43-02 authored the live breakdown test and --write-breakdown; this plan is where it writes to a real record."
    - "43-03 authored scripts/build_loss_reason_report.py and the skill; this plan is where the live answers to research Open Questions 1 and 2 get recorded."
---

<objective>
Run every credentialed proof this phase's offline work authored, and record what the live
portal actually did.

Purpose: three of this phase's four requirements make claims that only a live call can
discharge — that HubSpot's EQ filter now matches, that a real record holds a parseable
breakdown, and what the loss-reason field's live reality is. Offline greens prove the code
shape; they do not prove the portal agrees.

Output: `43-LIVE-EVIDENCE.md` recording each verdict, and the first real loss-reason report.

**This plan writes to live HubSpot.** It creates and deletes disposable companies and writes
`lv_icp_score_breakdown`. It does NOT deploy n8n workflow content — that is 43-05, gated
separately. Every task is operator-run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-pipeline-scoring-hygiene-explainability/43-CONTEXT.md
@.planning/phases/43-pipeline-scoring-hygiene-explainability/43-RESEARCH.md
</context>

<operator_command_convention>
`.env` is Read and Bash permission-blocked to the agent. Every command below is written for the
operator to run themselves, prefixed with `!`, and loads credentials by sourcing `.env` in the
operator's own shell. The agent must never attempt these itself, and must never claim a live
result it did not receive from the operator.

Credential prelude used by every command in this plan:

    set -a; . ./.env; set +a

Run from the repo root. If `HUBSPOT_PORTAL_ID` is not `22617666`, every fixture asserts and
refuses before touching anything — that guard is intentional, do not work around it.
</operator_command_convention>

<tasks>

<task type="auto">
  <name>Task 1: Live EQ-filter proof — settle Pitfall 5 empirically and discharge D-08</name>
  <files>.planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md</files>
  <precondition>The 43-01 offline suite is green and `tests/test_review_flag_eq_filter.py` exists, so the live tier has something to run.</precondition>
  <read_first>
    - `tests/test_review_flag_eq_filter.py` — the module 43-01 authored; read both test bodies before running so the verdict can be interpreted.
    - `.planning/phases/43-pipeline-scoring-hygiene-explainability/43-RESEARCH.md` Pitfall 5 — the three candidate outcomes, so the recorded verdict names which one occurred.
    - `tests/scoring_fixtures.py:85-103` — `disposable_company`'s teardown contract, so a failed run's cleanup can be confirmed.
  </read_first>
  <action>
Have the operator run the live-gated EQ-filter module and report the output verbatim.

Operator command:

    !set -a; . ./.env; set +a; RUN_LIVE_PARITY=true .venv/bin/python -m pytest tests/test_review_flag_eq_filter.py -q -s

Then record the outcome in a new `43-LIVE-EVIDENCE.md` under the phase directory. The record
must state which of research Pitfall 5's three outcomes actually occurred when a bare JSON
boolean was PATCHed to the booleancheckbox property — silent coercion to the string form, a
stored value the string filter cannot match, or an outright rejection — and it must state
whether the disposable company's id appeared in the EQ-filtered search after the corrected
string was written. Quote the observed values; do not paraphrase them into a conclusion.

If the bare-boolean probe shows HubSpot silently coerces (outcome 1), say so plainly: the fix
still stands as the permanent close of a recurring class, but the phase's severity framing for
PIPE-01 changes from "records are invisible to the queue" to "the class is closed before it
bites a property where HubSpot does not coerce". Recording that honestly matters more than
preserving the original framing.

Confirm the disposable company was deleted — the fixture asserts a 204 on teardown, so a run
that ended without one leaves a real record behind and needs manual cleanup.
  </action>
  <acceptance_criteria>
    - `43-LIVE-EVIDENCE.md` exists and contains a section naming which Pitfall 5 outcome occurred, with the observed stored value quoted.
    - The same file records whether the EQ-filtered search returned the disposable's id after the corrected string write, as an explicit yes or no.
    - The pytest run's exit code is recorded verbatim.
    - No company matching the disposable name prefix remains in the portal after the run.
  </acceptance_criteria>
  <verify>
    <human-check>Operator pastes the full `-q -s` output; the agent transcribes verdicts into 43-LIVE-EVIDENCE.md without inferring beyond what the output shows.</human-check>
    <automated>.venv/bin/python -m pytest tests/test_review_flag_eq_filter.py -q</automated>
  </verify>
  <done>D-08's live half is discharged: the EQ filter's post-fix behavior and HubSpot's bare-boolean behavior are both recorded as measured fact.</done>
</task>

<task type="auto">
  <name>Task 2: Live breakdown write — prove a real record holds parseable, rubric-stamped JSON</name>
  <files>.planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md</files>
  <precondition>43-02 has landed: `scripts/run_scoring_parity.py --help` lists `--write-breakdown` and the live-gated breakdown test exists.</precondition>
  <read_first>
    - `scripts/run_scoring_parity.py` — the amended module docstring and the flag's help text, so the operator command below matches what actually shipped.
    - `tests/test_scoring_parity.py` — the live-gated breakdown test 43-02 authored.
    - `.planning/phases/40-scoring-engine-remediation-notes/parity-report-final.json` — Phase 40's verdict shape, for comparison against this run's report.
  </read_first>
  <action>
Two operator runs, in order.

First the disposable-record test, which creates and deletes its own record:

    !set -a; . ./.env; set +a; RUN_LIVE_PARITY=true .venv/bin/python -m pytest tests/test_scoring_parity.py -k breakdown -q -s

Then the harness against a small real sample, exercising D-03's coverage rule — the records
this invocation checks, and no others:

    !set -a; . ./.env; set +a; PARITY_SAMPLE_IDS=9604614548 .venv/bin/python scripts/run_scoring_parity.py --write-breakdown

Then a read-back of the written property on that record:

    !set -a; . ./.env; set +a; .venv/bin/python -c "import sys;sys.path.insert(0,'.');import json;from src.hubspot_client import get_record;v=get_record('companies','9604614548',['lv_icp_score_breakdown','lv_icp_fit_score','lv_icp_tier'])['properties'];print(v['lv_icp_fit_score'],v['lv_icp_tier']);d=json.loads(v['lv_icp_score_breakdown']);print(json.dumps(d,indent=2))"

Record in `43-LIVE-EVIDENCE.md`: that the read-back parsed as JSON, that its total equals the
record's live `lv_icp_fit_score`, that the rubric version stamp is present, and the payload's
byte length — the last of which documents how much headroom exists under the property limit
and therefore how far the truncation path is from ever firing in practice.

Before running the flagged invocation, confirm with the operator that writing
`lv_icp_score_breakdown` on that specific company is acceptable. It is a derived explanatory
field with no downstream consumer, so this is low-risk, but it is still a write to a real
customer record and the operator decides.
  </action>
  <acceptance_criteria>
    - The read-back command's output parses as JSON and is recorded in `43-LIVE-EVIDENCE.md`.
    - The recorded breakdown's total equals the record's live `lv_icp_fit_score`.
    - The recorded breakdown contains the rubric version `lv-icp-v0.1`.
    - The payload byte length is recorded.
    - `git status` shows a new dated parity report under the report directory, and its contents include the count of breakdowns written.
  </acceptance_criteria>
  <verify>
    <human-check>Operator pastes both run outputs and the read-back JSON; the agent records the total, the version stamp and the byte length.</human-check>
    <automated>.venv/bin/python -m pytest tests/test_scoring_parity.py -q</automated>
  </verify>
  <done>A real company carries a valid, rubric-stamped breakdown whose total matches its live fit score, written only by the explicitly-flagged invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Live loss-reason truth — answer both open questions, generate the first report, install the skill</name>
  <files>.planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md, docs/reports/</files>
  <precondition>43-03 has landed: `scripts/build_loss_reason_report.py` exists and its offline suite is green, and the plugin version in `plugin.json` is above 0.11.1.</precondition>
  <read_first>
    - `scripts/build_loss_reason_report.py` — its CLI surface and its three property tri-state messages, so the live output can be read correctly.
    - `.planning/phases/43-pipeline-scoring-hygiene-explainability/43-RESEARCH.md` Open Questions 1 and 2 — the two things this task settles.
    - `operator-claude-plugin/CHANGELOG.md` "Release checklist" — the four release steps, of which the clone refresh is this task's.
  </read_first>
  <action>
First, settle research Open Question 1 with a direct schema read:

    !set -a; . ./.env; set +a; .venv/bin/python -c "import os,requests;h={'Authorization':'Bearer '+os.environ['HUBSPOT_PRIVATE_APP_TOKEN']};r=requests.get('https://api.hubapi.com/crm/v3/properties/deals',headers=h,timeout=30);r.raise_for_status();n=[p['name'] for p in r.json()['results']];print([x for x in n if 'closed_lost' in x or 'loss' in x])"

Then run the aggregator itself:

    !set -a; . ./.env; set +a; .venv/bin/python scripts/build_loss_reason_report.py

Record in `43-LIVE-EVIDENCE.md`, as answers to the two open questions: whether
`lv_closed_lost_reason` exists on the Deal object at all, whether HubSpot's native unprefixed
loss-reason property exists and is populated, how many closed-lost deals were examined, how
many carried a reason, how many joined to a company via the primary-association property
versus the Associations v4 fallback, and how many could not be joined. If the report is empty,
record it as the expected first-run outcome and move on — an empty report here is a pass, not
a finding, and the consumption path being proven to work is the deliverable.

Then complete the plugin release's operator half (C5) — without this the skill exists in the
repo and never reaches the installed plugin:

    !git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator fetch --depth=1 origin master
    !git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator reset --hard FETCH_HEAD

Note that the clone tracks `master`, so this refresh only surfaces the new version after this
phase's work has been merged there — if the work is still on the feature branch, record that
the release step is deferred to merge rather than reporting a refresh that fetched nothing.
Have the operator confirm the plugin updates and that the new skill is invocable by its slash
command, and record which of those two outcomes occurred.
  </action>
  <acceptance_criteria>
    - `43-LIVE-EVIDENCE.md` states, as an explicit yes or no, whether `lv_closed_lost_reason` exists on the Deal object in this portal, with the property-list output quoted.
    - The same file records the deal counts: examined, with-reason, joined-by-primary-association, joined-by-v4-fallback, unjoined.
    - A dated report file exists under `docs/reports/` and its content matches the tri-state the schema read established.
    - The aggregator's exit code is 0 even if zero loss reasons were found.
    - `43-LIVE-EVIDENCE.md` records either that the plugin updated and the skill is invocable, or that the release step is deferred until the branch merges to master.
  </acceptance_criteria>
  <verify>
    <human-check>Operator pastes the schema-read output, the aggregator output, and confirms the plugin update result.</human-check>
    <automated>.venv/bin/python -m pytest tests/test_loss_reason_report.py -q</automated>
  </verify>
  <done>Both live open questions are answered and recorded, the first real loss-reason report exists, and the plugin release's operator half is either completed or explicitly deferred to merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell (holding `.env`) -> live HubSpot portal | Every command here carries real write credentials |
| Disposable-company fixture -> real portal | Creates and must delete real records |
| Agent -> operator-reported results | The agent cannot observe these runs; it only transcribes what it is told |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-17 | Spoofing | Agent-transcribed live results | high | mitigate | Every task requires the operator to paste raw output; acceptance criteria demand quoted observed values, not conclusions. The agent must not record a verdict it did not receive. |
| T-43-18 | Denial of Service | Disposable-company teardown | high | mitigate | The fixture asserts a 204 delete in a `finally` block; Task 1's criteria require confirming no prefixed company survives the run. |
| T-43-19 | Tampering | Live write to a real customer record | medium | mitigate | Task 2 writes only `lv_icp_score_breakdown`, a derived explanatory field with no downstream consumer, on an operator-confirmed single id. D-03 confines coverage to the invocation's own sample. |
| T-43-20 | Information Disclosure | `.env` contents | high | mitigate | The agent never reads `.env` and never runs these commands; the prelude sources it inside the operator's own shell and no command echoes a token. |
| T-43-21 | Repudiation | An empty loss-reason report read as a failure | medium | mitigate | Task 3 states explicitly that an empty report is the expected first-run pass; the aggregator exits 0 on it and non-zero only on missing credentials. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Files created**
- `.planning/phases/43-pipeline-scoring-hygiene-explainability/43-LIVE-EVIDENCE.md` — the phase's live verdict record: Pitfall 5 outcome, EQ-filter match result, breakdown read-back with byte length, both loss-reason open-question answers with counts, plugin install result.
- A dated loss-reason report under `docs/reports/`.
- A dated parity report carrying the breakdown-written count.

**Live state changed**
- `lv_icp_score_breakdown` written on the operator-confirmed sample company.
- Disposable companies created and deleted (net zero).

**No source files are modified by this plan.**
</artifacts_this_phase_produces>

<verification>
- `43-LIVE-EVIDENCE.md` answers every question this plan was created to answer, each with quoted observed output rather than a conclusion.
- The offline suites remain green after the live runs: `.venv/bin/python -m pytest -q` at or above 2362 passed.
- No disposable company remains in the portal.
- No n8n workflow was deployed by this plan.
</verification>

<success_criteria>
- D-08's live half is discharged with a measured, not assumed, EQ-filter verdict.
- A real record carries a valid rubric-stamped breakdown whose total matches its live fit score.
- Both of research's live open questions are answered and recorded, and the first loss-reason report exists — empty or not.
</success_criteria>

<output>
Create `.planning/phases/43-pipeline-scoring-hygiene-explainability/43-04-SUMMARY.md` when done.
It must reference `43-LIVE-EVIDENCE.md` rather than restate it, and must state plainly if any
live step was skipped and why.
</output>
