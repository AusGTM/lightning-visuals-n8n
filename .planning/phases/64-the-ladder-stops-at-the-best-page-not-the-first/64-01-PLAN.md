---
phase: 64-the-ladder-stops-at-the-best-page-not-the-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/suggest_contacts.py
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - operator-claude-plugin/tests/test_suggest_contacts.py
autonomous: true
requirements:
  - LADDER-01
  - LADDER-02
  - SAFE-02
  - SAFE-03

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A company whose /contact page names one receptionist and whose /board/ page names nine officers ends the round with people from BOTH pages in one set, deduped by normalised first+last name (D-64-01, D-64-02)."
    - "The walk stops when the cumulative role-filter hit count over the deduped union reaches max(len(chosen_families), agreed_cap) — never on the mere presence of a person on the page just fetched (D-64-03, D-64-05, D-64-06)."
    - "Not clearing the bar continues the walk; clearing it stops the walk early; neither branch changes the fetch cap (D-64-07)."
    - "A page whose fetch came back with disposition 'refused' ends that company's walk even when accepted candidates still remain (D-64-10, SAFE-02)."
    - "The walk reports how it ended — one of good_enough / ladder_exhausted / cap_exhausted / refused — and acts on none of them; cap_exhausted is terminal and never a trigger to fetch again (D-64-08, D-64-11)."
    - "url_fallback.MAX_FOLLOWUP_FETCHES is still 5, cost_guard.MAX_FETCHES_PER_COMPANY still equals it, and the walk keeps no fetch counter of its own (D-64-09, D-64-13, SAFE-03)."
    - "No plugin script contains a while loop (D-64-12)."
    - "SKILL.md step 5 cites the walk predicate instead of carrying the stop rule as prose (LADDER-02)."
  artifacts:
    - "operator-claude-plugin/scripts/suggest_contacts.py — walk_bar, walk_pages, WALK_GOOD_ENOUGH, WALK_LADDER_EXHAUSTED, WALK_CAP_EXHAUSTED, WALK_REFUSED, WALK_ENDINGS"
    - "operator-claude-plugin/skills/suggest-contacts/SKILL.md — step 5 (line ~101) and the step-7 code block (lines ~333-334) cite the predicate"
    - "operator-claude-plugin/tests/test_suggest_contacts.py — walk tests: union/dedupe, bar, each ending, the constants-equality pin"
  key_links:
    - "walk_pages -> select_people (per page; len(selected) IS the D-64-03 score) -> role_classify.classify_title (D-64-04, mid-walk, pure)"
    - "walk_pages <- next_candidates(...)'s dict verbatim: the ONLY source of cap_exhausted vs ladder_exhausted (D-64-13, PATTERNS analog 3)"
    - "SKILL.md step 5 -> walk_bar/walk_pages: the prose-to-code citation, following step 3's agreed_cap precedent (D-62-11/-12)"
    - "walk_pages['ended'] -> Phase 65 only. search_fallback.eligible_after_ladder is not called, imported or modified here (D-64-08 boundary)"
---

<objective>
Make the stage-1 page walk stop at the BEST people-bearing page rather than the first one,
inside the unchanged `MAX_FOLLOWUP_FETCHES` budget, by moving the stop decision out of
`SKILL.md` prose and into a pure, testable predicate in `suggest_contacts.py`.

Purpose: a club whose `/contact` page names one receptionist while `/board/` lists the whole
committee is today walked no further than the receptionist (`SKILL.md:101`, `:334`). That is
LADDER-01 and LADDER-02. This phase spends the SAME budget better (D-64-09) — it never
enlarges it.

Output: two new pure functions (`walk_bar`, `walk_pages`) and a closed `ended` vocabulary in
`operator-claude-plugin/scripts/suggest_contacts.py`; a rewritten step 5 and step-7 code
block in `operator-claude-plugin/skills/suggest-contacts/SKILL.md` that cite them; and tests
in `operator-claude-plugin/tests/test_suggest_contacts.py` carrying every decision below.

**Zero `n8n/` diff.** Nothing in this phase touches `n8n/wf_*.json`, `scripts/build_cloud_workflows.py`,
or any HubSpot property. It is plugin-side Python plus one SKILL.md.
</objective>

## Artifacts this phase produces

**New symbols in `operator-claude-plugin/scripts/suggest_contacts.py`:**

| Symbol | Kind | Contract |
|---|---|---|
| `WALK_GOOD_ENOUGH` | module constant | `"good_enough"` |
| `WALK_LADDER_EXHAUSTED` | module constant | `"ladder_exhausted"` |
| `WALK_CAP_EXHAUSTED` | module constant | `"cap_exhausted"` |
| `WALK_REFUSED` | module constant | `"refused"` |
| `WALK_ENDINGS` | module constant | tuple of the four above, in that order |
| `walk_bar(chosen_families, per_company_cap)` | function | returns `int` — `max(len(chosen_families or []), per_company_cap)` |
| `walk_pages(pages, candidates, bar, family_list, chosen_families, known_contacts)` | function | returns the walk dict below |

**`walk_pages` return dict keys:** `people` (deduped union, walk order), `selected`,
`dropped`, `scores`, `ended`, `bar`.
**`scores` entry keys:** `url`, `score` (this page's marginal role-filter hits after dedupe),
`cumulative` (running total).
**`pages` input entry keys consumed:** `url`, `people`, `disposition` (only the value
`"refused"` is read; every other value, and absence, means "fetched, fold its people").
**`candidates` input:** `next_candidates(...)`'s return dict verbatim — keys `accepted` and
`budget_remaining` are read; nothing is re-derived.

**Prose replaced:** `SKILL.md` step 5's stop rule (line ~101) and the step-7 code block's
approve-candidates comment (lines ~333-334).

**No new files. No new dependencies. No new operator question. No new tunable constant.**

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-CONTEXT.md
@.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-PATTERNS.md
@.planning/todos/pending/2026-09-05-fallback-is-keyed-on-ladder-empty-not-round-empty.md
@operator-claude-plugin/scripts/suggest_contacts.py
@operator-claude-plugin/scripts/url_fallback.py
@operator-claude-plugin/scripts/search_fallback.py
@operator-claude-plugin/skills/suggest-contacts/SKILL.md
</context>

## Planning-input gaps, recorded rather than papered over

- **No RESEARCH.md.** Research was deliberately skipped for this phase by operator decision.
  `64-CONTEXT.md` and `64-PATTERNS.md` are the upstream artifacts; every design point below
  traces to a `D-64-NN` decision or a named in-repo analog.
- **No VALIDATION.md.** A Nyquist validation strategy could not be produced without
  RESEARCH.md and the operator chose to continue. Verification below is derived from
  CONTEXT.md's own test obligations (`§ Tests that constrain the implementation`) and from
  the behaviours each decision names.
- **Spec-less probe fallback skipped, visibly.** This phase has no requirement IDs to probe
  predicates from; `must_haves` is authored directly from the `D-64-NN` decisions.
- **API coverage:** the deterministic detector returned `detected: false`. No external API is
  integrated — the walk changes an internal stopping rule over an existing fetch seam. No
  `COVERAGE.md` is written.
- **Schema gate:** no ORM/schema files exist in this repo; no schema-push task injected.

<assumption_delta_decision>
**Detected:** `pluralization`. The detector's own signal (`term: "fallback"`) is a false
positive — it matched the brief's FILENAME. The real pluralization is the one this phase is
about: **"the page that yields people" (singular) becomes "the pages walked" (plural), whose
deduped union is the company's result.**

**Now-primary noun:** `pages` / the union across them.

**Decision: `promote`.** D-64-01 makes the union the primary product and explicitly rejects
"best page replaces" and "best page + carry held names". Every caller that read one page's
people per company now reads the union. This is the costly-reversibility change the tracer
task carries.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "two pages become one deduped union that stops at a bar" — one path only</name>

  <read_first>
    - `operator-claude-plugin/scripts/suggest_contacts.py` — read `_name_key` (~161-169),
      `_normalize_name` (~172-173), `select_people` (~176-205), `agreed_cap` (~218-263),
      `synthesise_rows` (~269-363), `company_budget`/`next_candidates` (~426-451). The
      docstring register (open by naming what the function does NOT do, and why) is the
      house style you must match.
    - `operator-claude-plugin/scripts/url_fallback.py` — `filter_candidates` (~142-192): its
      return dict is what `candidates` is; note `accepted`, `refused`, `cap`,
      `budget_remaining` and the scheme -> host -> budget check order.
    - `operator-claude-plugin/scripts/role_classify.py` — `chosen_families` (~106-119) and
      `classify_title`, so you know both are already-computed inputs, never recomputed here.
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md` lines 95-135 (step 5) — the
      caller you are changing, including the disposition transcription table.
    - `operator-claude-plugin/tests/test_suggest_contacts.py` lines 1-60 and 246-280 — the
      `_company_row` helper and the table-style, no-mock assertion shape your new tests copy.
    - `.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-CONTEXT.md`
      `<decisions>` — the authoritative decision list.
  </read_first>

  <behavior>
    - Two pages, one union: page A `[Jane Receptionist]`, page B `[nine officers]` -> the
      walk's `people` holds all ten, in walk order (D-64-01).
    - Dedupe: the same `firstname`+`lastname` (differing only in case and surrounding
      whitespace) appearing on both pages appears ONCE in `people` and is counted once
      toward the bar (D-64-02).
    - A person with a missing firstname or lastname has a `None` name key and is never
      deduped away — both copies stay in, resolved downstream, exactly as D-62-18's
      conservative half already rules.
    - Score: `scores[i]["score"]` equals the number of role-filter hits contributed by
      page i's NEW (post-dedupe) people, and `scores[i]["cumulative"]` is the running total
      (D-64-03). A 40-person page of unmatched titles scores 0; a 9-person page whose titles
      classify into a chosen family scores 9.
    - Bar: `walk_bar(["board"], 3)` is 3; `walk_bar(["board", "executive", "media", "ops"], 3)`
      is 4 (D-64-06 — count of chosen families, floored at the agreed cap).
    - Stop: with `bar == 3`, a page-A cumulative of 1 returns `ended is None` (keep walking);
      once the cumulative reaches 3 the return is `WALK_GOOD_ENOUGH` and pages after the
      clearing page are not folded (D-64-05, D-64-07).
    - End to end: the walk's `selected` feeds `synthesise_rows(company, selected, fetched_url,
      per_company_cap)` and the resulting `round_artifact` passes `extraction.validate()` —
      the same offline chain the Phase 62 tracer already proves.
  </behavior>

  <files>operator-claude-plugin/scripts/suggest_contacts.py, operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_suggest_contacts.py</files>

  <action>
Add two pure functions to `operator-claude-plugin/scripts/suggest_contacts.py`, placed
directly after `select_people` so the score primitive they reuse is adjacent.

`walk_bar(chosen_families, per_company_cap)` returns `max(len(chosen_families or []),
per_company_cap)` and nothing else — one expression, per D-64-06's reversibility note. Do NOT
add a validation guard: `per_company_cap` is documented as `agreed_cap()`'s already-validated
return, and a non-int raises loudly from `max` at the call site. Do NOT invent a new exception
class and do NOT recompute either input — `chosen_families` comes from
`role_classify.chosen_families(vocabulary, labels)` and the cap from
`suggest_contacts.agreed_cap(chosen_cap, figures)`, mirroring how `next_candidates` takes
`attempts` and calls the one existing helper rather than deriving a budget itself.

`walk_pages(pages, candidates, bar, family_list, chosen_families, known_contacts)` folds the
pages the orchestrator has fetched SO FAR, in ladder order, and returns
`{"people", "selected", "dropped", "scores", "ended", "bar"}`.

Two lists, deliberately distinct — state this in the docstring. `pages` is EVERY page fetched
for this company INCLUDING the pasted URL, entries `{"url", "people", "disposition"}`.
`attempts` (untouched, still the input to `company_budget`/`next_candidates`) is
`url_fallback.give_up_message`'s own list of what was tried AFTER the pasted URL came back
empty. Folding `attempts` instead of `pages` would silently drop the receptionist page — the
very page the live case starts from — so the walk takes its own list and does not reuse that
one.

The fold: keep a running set of `_name_key` values; for each page take only the people whose
key is not already in that set (a `None` key is never deduped — keep every such person); call
`select_people(new_people, family_list, chosen_families, known_contacts)` once per page and
extend the accumulated `selected` and `dropped` with its two lists; append
`{"url": page["url"], "score": len(this page's selected), "cumulative": len(accumulated
selected)}` to `scores`. Calling `select_people` per page rather than once over the whole
union is exactly equivalent — it is per-person independent — and it is what makes
`role_classify.classify_title` run MID-walk (D-64-04), which is the whole reason the walk can
decide whether to continue.

Stopping: after folding each page, if `cumulative >= bar` set `ended` to `WALK_GOOD_ENOUGH`
and stop folding. The bar is measured against the cumulative union, never against the page in
isolation (D-64-05): two officers on `/contact` plus two on `/about` reaches four and stops,
where a per-page bar would have walked the whole cap. The bar is a stop condition and never a
budget (D-64-07) — not clearing it simply means `ended` stays `None` and the caller fetches
the next accepted candidate.

Iterate with a `for` over the `pages` list, breaking early. No `while`, anywhere (D-64-12) —
`url_fallback.py`'s `__main__` argv scan carries the repo's own comment explaining why a `for`
over a fixed-length list stands in for one; follow that shape.

In this task, `ended` takes only `WALK_GOOD_ENOUGH` or `None`; define all four `WALK_*`
constants and `WALK_ENDINGS` now so the vocabulary and the signature are final, and leave the
three terminal endings to Task 2. Validate `candidates` now, at the signature: it must be a
dict carrying both `accepted` and `budget_remaining`, raising `ValueError` naming what was
passed, in the register `rejoin_enriched` already uses — a silently-defaulted candidate dict
would later render as a terminal ending the ladder never reached, which reads as a finding
rather than a caller bug.

Then rewrite `SKILL.md` step 5's stop rule. On line 101 the sentence about candidates being
fetched in ladder order ends with a trailing clause after "only after the operator has
approved them," — replace that trailing clause with a citation of
`suggest_contacts.walk_bar(chosen_families, per_company_cap)` and
`suggest_contacts.walk_pages(...)`, stating in one sentence each that (a) pages ACCUMULATE
into one set deduped by first+last name rather than a winner replacing the rest (D-64-01),
and (b) the walk continues until the cumulative role-filter hit count reaches
`max(len(chosen_families), per_company_cap)`. Follow this file's own precedent: step 3 was
rewritten to cite `agreed_cap()` instead of carrying the cap rule as prose (D-62-11/-12), and
`agreed_cap`'s docstring says so in as many words. Add one short paragraph naming the two-list
relationship above, so the caller cannot conflate `pages` with `attempts`. Leave the
disposition transcription table, the tool-level-refusal paragraph, and the whole search
fallback section untouched.

Write the new tests in `operator-claude-plugin/tests/test_suggest_contacts.py` following the
existing table-style shape: plain inline dicts, the existing `_company_row` helper, no mocks
and no fixtures. Cover every bullet in `<behavior>`, and finish with the end-to-end chain —
receptionist page plus board page -> `walk_pages` -> `synthesise_rows` -> `round_artifact` ->
`extraction.validate()`.
  </action>

  <reversibility rating="costly">D-64-01's union changes the round artifact's volume per company and every caller that read one page's people; D-64-08's `ended` field is planned for a Phase 65 consumer. Both are recoverable but touch callers, so the shape is fixed here rather than iterated.</reversibility>

  <acceptance_criteria>
    - `grep -n "def walk_bar" operator-claude-plugin/scripts/suggest_contacts.py` and
      `grep -n "def walk_pages" operator-claude-plugin/scripts/suggest_contacts.py` each
      return exactly one line.
    - `grep -cE "^WALK_(GOOD_ENOUGH|LADDER_EXHAUSTED|CAP_EXHAUSTED|REFUSED|ENDINGS) =" operator-claude-plugin/scripts/suggest_contacts.py`
      prints `5`.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import suggest_contacts as s; print(s.walk_bar(['board'],3), s.walk_bar(['a','b','c','d'],3))"`
      prints `3 4`.
    - `grep -cE "first (one )?that yields" operator-claude-plugin/skills/suggest-contacts/SKILL.md`
      prints `1` after this task (line 101's clause gone; the step-7 code-block comment is
      Task 3's).
    - `grep -c "walk_pages" operator-claude-plugin/skills/suggest-contacts/SKILL.md` is
      at least `1`.
    - A test named for the two-page union asserts the ten-person `people` list, and a test
      named for dedupe asserts a case-and-whitespace-differing duplicate appears once.
    - No new import appears in `suggest_contacts.py` (`git diff` on its import block is empty).
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts.py -q</automated>
    <fails_when>non-zero exit, or the summary line does not end in "passed" with a count greater than 95, or any line begins with "FAILED"</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py -q</automated>
    <fails_when>non-zero exit, or the output contains "contains a while loop"</fails_when>
  </verify>

  <done>`walk_bar` and `walk_pages` exist and are pure; a two-page fixture yields one deduped union whose people feed `synthesise_rows` into a passing `extraction.validate()`; the walk returns `None` below the bar and `good_enough` at or above it; SKILL.md step 5 cites the two functions instead of carrying the stop rule as prose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The closed `ended` vocabulary — a refusal stays terminal, and cap vs ladder is read off the library</name>

  <read_first>
    - `operator-claude-plugin/scripts/search_fallback.py` lines 65-100 (the
      `DISPOSITION_EMPTY` / `DISPOSITION_CAP_EXHAUSTED` / `DISPOSITION_REFUSED` /
      `ELIGIBLE_DISPOSITIONS` block) and lines 171-235 (`eligible_after_ladder`) — the exact
      vocabulary shape D-64-08 mirrors, and the fail-closed reader this phase must not
      disturb.
    - `operator-claude-plugin/scripts/url_fallback.py` `filter_candidates` (~142-192) — the
      `accepted` / `refused` / `budget_remaining` contract; `budget_remaining` is
      `max(MAX_FOLLOWUP_FETCHES - already_fetched, 0)`.
    - `operator-claude-plugin/scripts/suggest_contacts.py` — your Task 1 `walk_pages`, plus
      `synthesise_rows`'s docstring paragraph explaining why the
      `"suggest_contacts_web_search"` literal is written locally rather than imported from
      `search_fallback`, and held equal by a test. That is the pattern you copy for the
      constants pin.
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md` lines 118-145 — the
      disposition transcription table and the cap-exhaustion entry rule.
    - `.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-CONTEXT.md`
      D-64-08, D-64-10, D-64-11, D-64-13.
  </read_first>

  <behavior>
    - Page 1 yields one person (below the bar), page 2 comes back with disposition
      `"refused"`, and `candidates["accepted"]` still holds an unfetched URL -> `ended` is
      `WALK_REFUSED`, not `None`. The walk does not step past a refusal to the next page
      (D-64-10, SAFE-02).
    - A refused page contributes no people: it is not folded, and `scores` carries no entry
      for it.
    - Bar not cleared, no unfetched URL left in `candidates["accepted"]`, and
      `candidates["budget_remaining"] == 0` -> `ended` is `WALK_CAP_EXHAUSTED`.
    - Bar not cleared, no unfetched URL left, and `candidates["budget_remaining"] > 0` ->
      `ended` is `WALK_LADDER_EXHAUSTED`.
    - Bar not cleared and an unfetched accepted URL remains -> `ended` is `None`.
    - A URL already present in `pages` is not counted as remaining: `accepted` holding only
      URLs already walked reads as exhausted, not as "keep going".
    - `WALK_REFUSED == search_fallback.DISPOSITION_REFUSED` and
      `WALK_CAP_EXHAUSTED == search_fallback.DISPOSITION_CAP_EXHAUSTED`, asserted by a test.
    - `walk_pages` returns `cap_exhausted` and nothing else: no key, flag or list in the
      return invites another fetch (D-64-11).
  </behavior>

  <files>operator-claude-plugin/scripts/suggest_contacts.py, operator-claude-plugin/tests/test_suggest_contacts.py</files>

  <action>
Complete `walk_pages`'s ending resolution in
`operator-claude-plugin/scripts/suggest_contacts.py`, in this documented order: refused ->
good_enough -> keep walking -> cap_exhausted -> ladder_exhausted. The fence is checked first
and the docstring says why.

Inside the fold, before a page's people are touched, test
`page.get("disposition") == WALK_REFUSED`; on a match set `ended` to `WALK_REFUSED` and stop.
Key the refusal on a FETCHED page's disposition and on nothing else. This is the crux of
D-64-10 and it must be stated in the docstring: a pre-fetch refusal from
`url_fallback.filter_candidates` — an off-host or bad-scheme sitemap entry — never produces
`ended: refused`, because such a URL is never fetched in the first place. The walk fetches
only what appears in `candidates["accepted"]` (D-64-13), so the fence holds by construction,
and `same_host`/`_canonical_authority` remain the single host check with no second one added.
Getting this backwards would be a live regression, not a style point: an `ended: refused`
raised by a never-fetched off-host sitemap entry would, once Phase 65 consumes the field,
close the search path for companies `search_fallback.eligible_after_ladder` opens today —
reaching D-5sd-04 by the back door, which D-64-08's boundary forbids.

Read the other two terminal endings straight off `candidates`, never from a fetch counter the
walk keeps itself (PATTERNS analog 3; the walk has no counter — `company_budget` and
`filter_candidates` own that axis, D-64-09). Compute the unfetched remainder as the entries of
`candidates["accepted"]` whose URL does not already appear in `pages`. If that remainder is
non-empty, `ended` stays `None`. If it is empty, `ended` is `WALK_CAP_EXHAUSTED` when
`candidates["budget_remaining"] == 0` and `WALK_LADDER_EXHAUSTED` otherwise.

Read only the `refused` disposition value. Do NOT fail closed on an unknown or absent
disposition here: any other value, including none at all, means "this page was fetched, fold
its people". Fail-closed reading of that same field is
`search_fallback.eligible_after_ladder`'s job and stays there, unmodified and uncalled — the
walk emits its reason and acts on none of it (D-64-08). Add no `search_fallback` import.

Define the four `WALK_*` values as local string literals and pin two of them equal to
`search_fallback.DISPOSITION_REFUSED` and `search_fallback.DISPOSITION_CAP_EXHAUSTED` in a
test, exactly as `synthesise_rows` pins its `"suggest_contacts_web_search"` literal today —
same vocabulary, no import, drift caught by the test.

Say in the docstring that `WALK_CAP_EXHAUSTED` is a terminal, eligible ending and never a
trigger to fetch again (D-64-11, SAFE-03), and that the walk enlarges no budget: continuing
further spends the SAME `MAX_FOLLOWUP_FETCHES` better (D-64-09).

Add the tests for every bullet in `<behavior>`, in the existing table style.
  </action>

  <acceptance_criteria>
    - A test drives page 1 (one person, below the bar) + page 2 (`disposition` `"refused"`)
      with a non-empty unfetched `accepted` list and asserts the return's `ended` equals
      `suggest_contacts.WALK_REFUSED`.
    - A test asserts that same refused-page walk's `scores` carries no entry whose `url` is
      the refused page's URL.
    - Separate tests assert `WALK_CAP_EXHAUSTED` for `budget_remaining == 0` with no
      unfetched candidate, and `WALK_LADDER_EXHAUSTED` for `budget_remaining > 0` with no
      unfetched candidate.
    - A test asserts `suggest_contacts.WALK_REFUSED == search_fallback.DISPOSITION_REFUSED`
      and `suggest_contacts.WALK_CAP_EXHAUSTED == search_fallback.DISPOSITION_CAP_EXHAUSTED`.
    - `grep -cE "^(import|from) search_fallback" operator-claude-plugin/scripts/suggest_contacts.py`
      prints `0` — the vocabulary is pinned by test, never by import, so the module cannot
      call `eligible_after_ladder` at all. (Anchored on the import statement deliberately: a
      docstring naming that function is REQUIRED by the action above, so a bare token count
      would gate against its own instruction.)
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import suggest_contacts; print(hasattr(suggest_contacts,'search_fallback'))"`
      prints `False`.
    - `git diff --stat operator-claude-plugin/scripts/search_fallback.py operator-claude-plugin/scripts/url_fallback.py`
      is empty.
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts.py operator-claude-plugin/tests/test_search_fallback.py -q</automated>
    <fails_when>non-zero exit, or any line begins with "FAILED", or the summary reports any "failed"/"error" count</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_url_fallback.py operator-claude-plugin/tests/test_cost_guard_suggestion.py -q</automated>
    <fails_when>non-zero exit, or fewer tests are collected than before this task, or any line begins with "FAILED"</fails_when>
  </verify>

  <done>All four `ended` values are reachable and tested; a refusal on a fetched page ends the walk while accepted candidates remain; `cap_exhausted` and `ladder_exhausted` are read off `next_candidates`'s dict; `search_fallback.py` and `url_fallback.py` are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the caller contract in SKILL.md and pin the invariants that must not move</name>

  <read_first>
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md` lines 95-200 (step 5 as you
      left it in Task 1, plus step 6) and lines 315-403 (the step-7 executable block through
      step 9's report).
    - `operator-claude-plugin/scripts/suggest_contacts.py` — your finished `walk_bar` and
      `walk_pages`, so the block you write cites the real signatures.
    - `operator-claude-plugin/tests/test_report_sufficiency.py` lines 205-245 —
      `_has_while_loop` and the guard that runs it over every plugin script (D-64-12).
    - `operator-claude-plugin/tests/test_cost_guard_suggestion.py` — the assertions holding
      `cost_guard.MAX_FETCHES_PER_COMPANY == url_fallback.MAX_FOLLOWUP_FETCHES` and the
      `7 * MAX_FOLLOWUP_FETCHES` stage-1 ceiling (D-64-09, SAFE-03).
    - `.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-CONTEXT.md`
      `<deferred>` — the scope fence: round-empty re-entry keyed on cause is Phase 65 and is
      not written here.
  </read_first>

  <files>operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_suggest_contacts.py</files>

  <action>
Rewrite the step-7 executable block in `operator-claude-plugin/skills/suggest-contacts/SKILL.md`
so the documented sequence matches the shipped code.

Replace the two-line comment at roughly lines 333-334 that begins "The operator approves
candidates from `plan`, in the order shown" — its trailing clause is the same prose stop rule
Task 1 removed from step 5. In its place, show the per-company walk: build the bar once with
`suggest_contacts.walk_bar(chosen_families, per_company_cap)` alongside the existing
round-level `vocabulary` and `per_company_cap` resolution (it is round-level too — it is a
function of the round's chosen families and the round's agreed cap, so it is never re-asked
per company); fetch in ladder order; after each fetch append `{"url", "people", "disposition"}`
to this company's `pages` list and call `suggest_contacts.walk_pages(pages, candidates, bar,
vocabulary["families"], chosen_families, known_contacts)` with `candidates` being that
company's latest `suggest_contacts.next_candidates(...)` result; fetch the next accepted
candidate only while `ended` is `None`.

Then set `people` from the walk's own `people` (the deduped union), and pass the walk's
`selected` where the block previously passed `selection["selected"]` — `select_people` now
runs INSIDE the walk, once per page, which is what lets the bar be measured mid-walk (D-64-04).
Keep `fetched_url` as the provenance locator semantics already documented: the URL actually
fetched for the people being synthesised, never the homepage.

Leave the `if not people:` guard that gates `search_fallback.eligible_after_ladder` exactly as
it stands. Cause-keyed re-entry is Phase 65's; this phase emits `ended` and routes nothing on
it (D-64-08's boundary). Do not add a branch on `ended`, do not pass `ended` to
`eligible_after_ladder`, and do not touch the rank-3 comment at roughly line 349 unless the
surrounding edit leaves it reading wrong — it is about search results, not the ladder walk.

Add one sentence to step 9's report list: the walk's ending reason for each company, in the
operator's own words, alongside the fetches spent against the per-company bound already
reported there. Keep the per-page `scores` INTERNAL to the walk and out of the report — the
operator-facing question is what was found and what it cost, and CONTEXT.md routes any
operator-facing disclosure of per-page scores to Phase 68's disclosure audit rather than here.

Finally, add the invariant tests to `operator-claude-plugin/tests/test_suggest_contacts.py`:
one asserting `url_fallback.MAX_FOLLOWUP_FETCHES == 5` and that `walk_pages` neither reads nor
writes it (a structural source assertion that the function body contains no
`MAX_FOLLOWUP_FETCHES` reference — the budget axis belongs to `company_budget` and
`filter_candidates`, D-64-09/D-64-13); and one structural assertion that the walk's source
contains no `while`, matching the guard in `test_report_sufficiency.py` (D-64-12) so a
regression names the walk specifically rather than only the whole-plugin scan.

Then run the FULL plugin suite and confirm the pre-existing count has only grown.
  </action>

  <acceptance_criteria>
    - `grep -cE "first (one )?that yields" operator-claude-plugin/skills/suggest-contacts/SKILL.md`
      prints `0`.
    - `grep -c "walk_pages" operator-claude-plugin/skills/suggest-contacts/SKILL.md` is at
      least `2` (step 5 and the step-7 block).
    - `grep -c "walk_bar" operator-claude-plugin/skills/suggest-contacts/SKILL.md` is at
      least `1`.
    - `grep -n "eligible_after_ladder" operator-claude-plugin/skills/suggest-contacts/SKILL.md`
      still shows the `if not people:` guard unchanged in the step-7 block.
    - Step 9's report list gains exactly one new item, naming the walk's ending reason for
      each company. (Stated positively on purpose: per-page scores staying internal is a
      design instruction in the action above, and a zero-count gate on that word would fire
      on the sentence documenting the choice.)
    - `git diff --stat -- n8n/ scripts/build_cloud_workflows.py` is empty.
    - A test asserts `url_fallback.MAX_FOLLOWUP_FETCHES == 5`.
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
    <fails_when>non-zero exit, any line beginning with "FAILED", or a summary reporting fewer than 2300 passed</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>any output at all — a non-empty line means an n8n artifact was modified, which this phase forbids</fails_when>
  </verify>

  <done>SKILL.md's step-5 prose and step-7 code block both cite the walk predicate; the search-fallback gate and the disposition contract are untouched; the full plugin suite is green with more tests than before; `n8n/` has a zero diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched page content -> candidate URL list | A sitemap or page can list any URL it likes. Every candidate is attacker-influenceable data before it is fetched. |
| operator/LLM orchestrator -> `walk_pages` arguments | `pages`, `candidates` and `bar` are threaded by an LLM caller and can be malformed. |
| walk output -> Phase 65 consumer | `ended` becomes an input to a future re-entry decision that can open or close the search path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering (SSRF / host confusion) | `walk_pages` candidate handling, Task 2 | high | mitigate | The extended walk widens the fetch set by zero URLs: it reads only `candidates["accepted"]`, which is `url_fallback.filter_candidates`'s output verbatim (scheme -> host -> budget, in that order). No second host check is introduced. Phase 62's shared guards — `url_fallback._canonical_authority` and `same_host` comparing canonical authorities, refusing suffix/attacker hosts, keeping real subdomains distinct, keeping differing ports distinct, never leaving a dotless remainder (62-10, G-62-2) — remain the single check and are not modified. Pinned by `git diff --stat operator-claude-plugin/scripts/url_fallback.py` being empty and by `test_url_fallback.py` running unmodified. |
| T-64-02 | Elevation of Privilege (fence bypass) | the `ended` vocabulary, Task 2 | high | mitigate | `ended: refused` keys ONLY on a fetched page's `disposition`, never on a pre-fetch `filter_candidates` refusal. Two failure directions are both closed: accumulating people across pages never becomes a route past a refusal (the fold stops at the refused page, D-64-10), and a never-fetched off-host sitemap entry never manufactures a `refused` that would, in Phase 65, close a search path `eligible_after_ladder` opens today. `search_fallback.py` is neither imported nor modified; asserted by two greps in Task 2's acceptance criteria. |
| T-64-03 | Denial of Service (budget exhaustion) | the walk loop, Tasks 1-3 | medium | mitigate | Bounded `for` over a fixed-length `pages` list, no `while` (D-64-12, pinned by `test_report_sufficiency.py::_has_while_loop` plus a walk-specific structural test). The walk keeps no fetch counter and never resets `MAX_FOLLOWUP_FETCHES`; `cost_guard.MAX_FETCHES_PER_COMPANY == url_fallback.MAX_FOLLOWUP_FETCHES` and the `7 * MAX_FOLLOWUP_FETCHES` stage-1 ceiling still hold (`test_cost_guard_suggestion.py`, D-64-09, SAFE-03). |
| T-64-04 | Information Disclosure | per-page `scores` | low | accept | Scores are counts of people already in scope for the round. The module writes no file and makes no network call (its purity is enforced by the autouse `no_network` guard and its own structural tests), and Task 3 keeps `scores` out of the operator report. Accepted: no new data crosses any boundary. |
| T-64-05 | Repudiation | which page a proposed person came from | low | accept | `synthesise_rows` already stamps the URL actually fetched as each record's provenance locator, and Task 3 preserves that semantics under the union. A person from `/board/` is not attributed to `/contact`. No change needed. |
| T-64-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase: zero new dependencies, zero new imports in `suggest_contacts.py` (asserted in Task 1's acceptance criteria), and the suite runs on the existing `.venv`. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint applies. |

ASVS level 1; blocking severity `high`. Both `high` threats carry a concrete, testable
mitigation above and neither is accepted.
</threat_model>

<verification>
1. `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green,
   with a higher passed count than the pre-phase baseline (2300 passed / 5 skipped as of
   plugin 0.38.1).
2. `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py operator-claude-plugin/tests/test_cost_guard_suggestion.py operator-claude-plugin/tests/test_url_fallback.py -q`
   — the three constraining suites named in CONTEXT.md pass UNMODIFIED (D-64-09, D-64-12,
   SAFE-02, SAFE-03).
3. `git status --porcelain -- n8n/ scripts/build_cloud_workflows.py` — empty. The n8n suite
   (`node --test tests/n8n/*.test.mjs`, glob form only) is untouched by this phase and needs
   no run.
4. `git diff --stat operator-claude-plugin/scripts/search_fallback.py operator-claude-plugin/scripts/url_fallback.py operator-claude-plugin/scripts/role_classify.py`
   — empty. All three are called as libraries and never re-implemented (D-64-13).
5. The live-case walkthrough, offline: a fixture pairing a one-receptionist `/contact` page
   with a nine-officer `/board/` page ends with ten people in the union and an `ended` of
   `good_enough` at the board page — the case that defines success in CONTEXT.md
   `<specifics>`.
</verification>

<success_criteria>
- LADDER-01: the walk continues past the first people-bearing page when a better candidate
  remains, under the same fetch budget — demonstrated by the two-page fixture and by
  `MAX_FOLLOWUP_FETCHES` being unchanged at 5.
- LADDER-02: "better" is `len(select_people(new_people, ...)["selected"])` accumulated against
  `max(len(chosen_families), agreed_cap)` — a predicate asserted by tests, not a prose
  instruction to the model.
- All four `ended` values are reachable and individually tested; none of them is acted upon in
  this phase.
- SAFE-02 holds: a refusal is terminal and `search_fallback.eligible_after_ladder` is
  unchanged and uncalled.
- SAFE-03 holds: no cap is reset and `cap_exhausted` is never a retry trigger.
- Zero `n8n/` diff.
</success_criteria>

<output>
Create `.planning/phases/64-the-ladder-stops-at-the-best-page-not-the-first/64-01-SUMMARY.md` when done
</output>
