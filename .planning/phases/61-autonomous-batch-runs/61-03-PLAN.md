---
phase: "61-autonomous-batch-runs"
plan: "61-03"
type: execute
wave: 2
depends_on: ["61-02"]
files_modified:
  - config/column_mapping.yaml
  - operator-claude-plugin/config/column_mapping.yaml
  - n8n/code/columnMap.js
  - operator-claude-plugin/scripts/extraction.py
  - operator-claude-plugin/skills/contact-upload/extraction.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - tests/n8n/columnMapIdentityParity.test.mjs
  - operator-claude-plugin/tests/test_identity_preflight.py
  - operator-claude-plugin/tests/test_linkedin_row_composition.py
  # REVIEW-A1: this file pins the refusal sentence VERBATIM at :233-241. Task 2's derived
  # message breaks that assertion by design, so the file is a planned edit, not a surprise.
  - operator-claude-plugin/tests/test_extraction_contract.py
  # REVIEW-A3: `build_cloud_workflows.py::main()` rewrites all eight workflow JSONs; the
  # `columnMap.js` edit lands in the ingest lane files, and the other six must come back
  # byte-identical.
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_review_decision_cloud.json
autonomous: true
requirements: [INPUT-05]

estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A row carrying only a LinkedIn URL passes the identity pre-flight in all five sites the rule is written in — no site rejects what another accepts."
    - "The rejection message a refused row shows is derived from the configured identity groups, so adding a group can never again leave a stale sentence behind."
    - "A parity test fails loudly if the YAML identity groups and `columnMap.js`'s hand-written reimplementation ever drift apart."
    - "A name-only row still routes to the weak-key needs_review path — the new group is additive and gives a bare name no new strength."
    - "`extraction.md`'s no-invention sentence is byte-identical to what it was before this plan, AND a committed test asserts that exact string so the next edit fails the suite rather than a summary check that only runs once (REVIEW-A10)."
  artifacts:
    - tests/n8n/columnMapIdentityParity.test.mjs
    - operator-claude-plugin/tests/test_linkedin_row_composition.py
  key_links:
    - "The five sites of D-61-06 move in one commit; `columnMap.js` is hand-written and NOT derived from the YAML, so a YAML-only edit silently drifts the deployed gate."
---

<objective>
Stop the front end refusing a row its own backend can now act on. D-61-06: the identity rule
"email OR firstname+lastname+company" is restated in FIVE independent sites with no parity test
between the YAML and the JS. All five move together, and a parity test is left behind.

Purpose: 61-02 made a LinkedIn URL searchable. Until this plan lands, no such row can reach it
— `extraction.py`'s gate and the deployed `Map Columns` node both reject it first. This is the
second half of D-61-05 (CORRECTED)'s "both halves land together".

Output: a LinkedIn-URL-only row is accepted by every gate, and what the waterfall then finds is
proposed through D-59-08's existing `resolutions` mechanism rather than a second surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-autonomous-batch-runs/61-CONTEXT.md
@.planning/phases/61-autonomous-batch-runs/61-RESEARCH.md
@.planning/phases/61-autonomous-batch-runs/61-02-SUMMARY.md
@n8n/code/columnMap.js
@tests/n8n/columnMapAliasParity.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the third identity group to the two code gates, and pin their parity</name>
  <files>config/column_mapping.yaml, operator-claude-plugin/config/column_mapping.yaml, n8n/code/columnMap.js, tests/n8n/columnMapIdentityParity.test.mjs</files>
  <read_first>
    `config/column_mapping.yaml` lines 8-57 and its mirror
    `operator-claude-plugin/config/column_mapping.yaml` — confirm byte-identical with `diff`
    before editing, and confirm it again after.
    `n8n/code/columnMap.js` lines 74-85 — `requiredIdentity()`, a hand-written third
    reimplementation not derived from the YAML at all, inlined verbatim into the deployed
    `Map Columns` node.
    `tests/n8n/columnMapAliasParity.test.mjs` lines 1-45 — its header comment explains exactly
    why alias drift is dangerous ("the client tells the operator a header is understood and the
    backend silently drops it"); the identity rule carries the same risk with no such test.
  </read_first>
  <action>
    Per D-61-03 (strong keys only) and 61-RESEARCH.md shape A steps 1-2.

    Add `- [linkedin_url]` as a THIRD group under `required_identity.any_of` in BOTH
    `config/column_mapping.yaml` and `operator-claude-plugin/config/column_mapping.yaml`. This
    is additive: it leaves the existing two groups and the whole `aliases` table untouched, so
    a name-only row is evaluated exactly as it is today and still only ever satisfies the second
    group. Confirm with `diff` that the two files remain byte-identical.

    Add the matching branch to `n8n/code/columnMap.js`'s `requiredIdentity()`. This edit is
    mandatory and cannot be skipped by editing only the YAML: the function is hand-written, and
    the deployed `Map Columns` node runs it BEFORE `Resolve Identity` ever executes, so a
    YAML-only change leaves the deployed bulk-CSV gate rejecting rows the plugin's own preview
    just accepted.

    Write `tests/n8n/columnMapIdentityParity.test.mjs`, modelled on
    `columnMapAliasParity.test.mjs`. It reads `config/column_mapping.yaml`'s
    `required_identity.any_of` and asserts `requiredIdentity()` agrees with it on a table of
    rows — one satisfying each configured group, one satisfying none, and one satisfying a
    group only partially. Drive it FROM the YAML so a fourth group added later is covered
    automatically rather than needing this test edited too; that generality is the entire
    reason this test is worth writing rather than three hand-written cases.

    REVIEW-A9: the root YAML has a THIRD consumer this plan does not otherwise touch —
    `src/file_loader.py` reads the same repo-root file for the Python oracle lane — so
    `tests/test_e2e_ingest.py` is in this task's verify command. A green run there is what proves
    the added group did not change how the oracle loads a file; it is cheaper to run it now than
    to find it in the root suite after three more edits.

    The plugin YAML mirror needs no new guard: `test_column_mapping_shipped.py:35` already
    asserts `PLUGIN_COPY.read_bytes() == REPO_COPY.read_bytes()` permanently, which is why the
    reviewers' mirror-drift HIGH was refuted by the orchestrator's own grep pass
    (61-REVIEWS, Divergent Views). Run it in the verify anyway — it is the assertion that fails
    if only one of the two files is edited.

    Regenerate the workflow JSONs through `scripts/build_cloud_workflows.py`. Never hand-edit
    them. `main()` writes all eight; report which ones changed — only the ingest-lane files
    carry `columnMap.js`, so a diff elsewhere is a finding.
  </action>
  <verify>
    <automated>node --test tests/n8n/columnMapIdentityParity.test.mjs tests/n8n/columnMapAliasParity.test.mjs && diff config/column_mapping.yaml operator-claude-plugin/config/column_mapping.yaml && .venv/bin/python -m pytest tests/test_e2e_ingest.py operator-claude-plugin/tests/test_column_mapping_shipped.py -q</automated>
  </verify>
  <done>A linkedin-only row satisfies the identity rule in the YAML and in `columnMap.js`; the two YAML copies are byte-identical; the parity test fails if either side is changed alone.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the refusal message from the configured groups, and update the prose contract</name>
  <files>operator-claude-plugin/scripts/extraction.py, operator-claude-plugin/skills/contact-upload/extraction.md, operator-claude-plugin/tests/test_identity_preflight.py</files>
  <read_first>
    `operator-claude-plugin/scripts/extraction.py` lines 170-215 (`identity_groups`,
    `has_identity`, `_group_presence`) and lines 630-690 — the refusal reason is a hard-coded
    sentence, NOT derived from `identity_groups()`, so adding a YAML group does not update it.
    `operator-claude-plugin/skills/contact-upload/extraction.md` lines 17-30 — two adjacent and
    independently editable rules: the no-invention sentence (D-61-02, do-not-simplify) and, a
    line or two below, the identity-group sentence that names only two groups.
    `operator-claude-plugin/tests/test_extraction_contract.py` — it parses and runs
    `extraction.md`'s fenced JSON example through the real validator, so that block's shape is
    load-bearing before any example is added.
  </read_first>
  <action>
    Rewrite `extraction.py`'s hard-coded rejection reason so it is COMPOSED from
    `identity_groups()` rather than restating the rule in prose. That is the lazy fix and the
    correct one at once: it closes this drift and every future one, and it deletes a site from
    D-61-06's list of five rather than adding a fourth thing to keep in step. The message must
    still read naturally to an operator — enumerate each configured group in the operator's own
    vocabulary, not as a raw Python repr.

    In `extraction.md`, update the sentence that names the identity groups so it names all three,
    and add one worked example alongside the existing email and name+company examples: a row
    carrying a LinkedIn URL and nothing else, accepted. Check
    `test_extraction_contract.py`'s parse of this file BEFORE editing the fenced example block —
    that test runs the block through the real validator, so a malformed edit fails there rather
    than in prose review.

    REVIEW-A1, and do this DELIBERATELY rather than discovering it as a failure:
    `test_extraction_contract.py:233-241` asserts the refusal reason as a verbatim string
    ("no identity present: needs a non-blank 'email', or all three of
    'firstname'/'lastname'/'company' non-blank"). A derived message necessarily breaks it. Pick
    the rewrite, not an exact-prefix loophole: update that assertion to the new derived text in
    the same commit, because the point of the change is that the sentence now enumerates what is
    configured, and a prefix match would pass while hiding the very drift this task removes.
    The file is listed in `files_modified` for that reason.

    LEAVE THE NO-INVENTION SENTENCE EXACTLY AS IT IS. D-61-02: it stays verbatim and stays on
    the do-not-simplify list. This plan changes the GROUP LIST, which is a different sentence a
    few lines away; the two are independently editable and only one of them moves. Record in the
    summary the byte-identical check proving the no-invention sentence did not change — AND, per
    REVIEW-A10, pin it: add a test asserting that exact sentence is present in `extraction.md`.
    A summary check runs once, on the day of this plan; an assertion runs on every future edit,
    which is what a do-not-simplify item actually needs.

    Extend `test_identity_preflight.py` with the linkedin-only acceptance case and, as its
    companion, a case proving a bare name with no email and no LinkedIn is still rejected —
    D-61-03's fence, asserted rather than assumed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_identity_preflight.py operator-claude-plugin/tests/test_extraction_contract.py operator-claude-plugin/tests/test_extraction_resolvable.py -q</automated>
  </verify>
  <done>A linkedin-only row is accepted by `validate()`; a refused row's reason enumerates all three configured groups; a name-only row is still refused; the no-invention sentence is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Route the waterfall's findings through D-59-08's resolutions loop, with a composition test</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_linkedin_row_composition.py, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    `operator-claude-plugin/scripts/extraction.py` lines 62-114 and 580-680 — the D-59-08
    `resolutions` mechanism, `RESOLUTION_SOURCES`, and the ordering fact that makes it reusable
    here: `resolutions` are validated for EVERY accepted record, not only ones that failed the
    pre-flight.
    `operator-claude-plugin/scripts/resolution_sources.py` — `provider_result` is already
    defined as "a value the enrichment waterfall already returned for this row".
    `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` steps 2-5.
    `operator-claude-plugin/tests/test_skill_sequence_coverage.py` lines 1-90 — the census this
    task must satisfy; `GRANDFATHERED_UNCOVERED` is empty and `MAX_GRANDFATHERED` is 0.
  </read_first>
  <action>
    Per D-61-02 and the Claude's-Discretion ruling in 61-CONTEXT.md ("strongly prefer reuse"):
    a value the waterfall returns for a linkedin-only row is proposed through the EXISTING
    `resolutions` / `provider_result` rewrite-and-revalidate loop. Do NOT add a second proposal
    surface, a second confirmation vocabulary, or a second closed source list. The distinction
    D-61-02 draws is the one this reuse encodes: a value with provenance is resolved, a value
    from nothing is invented, and `provider_result` is provenance.

    Amend `enrich-before-ingest/SKILL.md` step 2 so a row carrying only a strong key is stated
    as proceeding — the operator is not asked for a company the backend does not need (D-61-01).
    Amend step 5 so what the waterfall returns for such a row is proposed with its source, and
    say plainly where a miss lands: per D-61-04, only Lusha can act on a bare LinkedIn URL
    (Apollo's match body and ZoomInfo's `hasZoomKey` never read the key at all), so a Lusha miss
    HOLDS the row with that reason recorded. It does not fall through to web research —
    `claude_web` is company-oriented and would be answering a different question.

    Say explicitly in the skill that the match search in step 2 spends nothing and needs no
    arming, and that widening what it can search does not change that: `fetch_matches` takes no
    `armed` parameter and sends an explicit empty provider list. The consent gate that guards
    spending is step 5's, unchanged, and every new call this enables goes through the existing
    `preingest.match_batch` and `chunking.dispatch_plan` functions — never a new transport.

    Any documented python block you add or change gets its own real composition test in
    `operator-claude-plugin/tests/test_linkedin_row_composition.py`, driving the join end to end
    (a linkedin-only row from `rows_from_table` through `build_rows_spec`, `plan_chunks`,
    `match_batch`, `classify_matches`, and a `resolutions`-carrying re-`validate()`), and
    registered in the census's `COVERED` map. A grandfather entry is not available here:
    `MAX_GRANDFATHERED` is 0 and `GRANDFATHERED_UNCOVERED` is empty.

    Bump `plugin.json` and add the `CHANGELOG.md` entry in the same commit. REVIEW's cross-plan
    versioning finding: READ the current version out of `plugin.json` and bump the MINOR
    component from whatever is actually there — do not assume a number from a plan written
    earlier. 61-02 moves it to 0.29.0; if 61-02's execution landed something else, this plan
    follows the file, not the plan text.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_linkedin_row_composition.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q</automated>
  </verify>
  <done>A linkedin-only row runs the documented sequence end to end in one test; the sequence census passes with no new grandfather entry; version and CHANGELOG moved together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input→extraction gate | An accepted row is one the system will act on and eventually write |
| plugin preview→deployed n8n gate | Two independent implementations of one rule; drift is silent by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-08 | Tampering | the five identity-rule sites (D-61-06) | high | mitigate | All sites move in one commit; `columnMapIdentityParity.test.mjs` reads the YAML and drives the JS so drift fails the suite; `extraction.py`'s message is derived, removing a site from the list entirely |
| T-61-09 | Elevation of privilege | `required_identity.any_of` | high | mitigate | Additive third group only — a name-only row still satisfies only the second group and still routes to the weak-key needs_review path (D-61-03), asserted by a companion test |
| T-61-10 | Spoofing | `resolutions` proposal loop | medium | mitigate | Reuses D-59-08's closed `RESOLUTION_SOURCES` vocabulary and its anti-laundering validation; no second proposal surface is created, so no path exists that skips that validation |
| T-61-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — nothing to audit |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` all green (glob form only).
- `.venv/bin/python -m pytest -q` green (baseline 3365 / 154).
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green (baseline 1725 / 5).
- The no-invention sentence in `extraction.md` is byte-identical to its pre-plan text — report
  the check and its result in the summary.
- `diff config/column_mapping.yaml operator-claude-plugin/config/column_mapping.yaml` is empty.
- No assertion weakened, deleted or reworded; report the count over the touched test files. The
  ONE expected exception is `test_extraction_contract.py:233-241`'s verbatim refusal sentence,
  rewritten to the derived message per REVIEW-A1 — report it by name so it reads as the planned
  edit it is rather than as a weakened assertion.
</verification>

<review_dispositions>
Cross-AI review (`61-REVIEWS.md`, 2026-08-30). Findings assigned to this plan:

- **Actionable — `test_extraction_contract.py:233-241` pins the refusal sentence verbatim.**
  INCORPORATED, Task 2: named as an explicit step, the file added to `files_modified`, and the
  rewrite (not an exact-prefix loophole) chosen deliberately with the reason stated.
- **Actionable — add `tests/test_e2e_ingest.py` to the verify.** INCORPORATED, Task 1 verify,
  with the reason (`src/file_loader.py` is a third consumer of the same root YAML).
- **Actionable — pin the no-invention sentence in a durable test.** INCORPORATED, Task 2 and a
  `must_haves` truth: an exact-string assertion, because a summary byte-check runs once and a
  do-not-simplify item needs a guard that runs forever.
- **Actionable — regenerated workflow artifacts.** INCORPORATED: all eight JSONs listed;
  `<verification>` requires reporting which changed.
- **REFUTED UPSTREAM, recorded here — "the committed parity test reads only the root YAML, so
  the plugin mirror can drift" (gpt HIGH).** `operator-claude-plugin/tests/test_column_mapping_shipped.py:35`
  already asserts the two files are byte-identical, permanently and in the suite; the
  orchestrator's grep pass excluded this from the HIGH count on that evidence
  (61-REVIEWS, Divergent Views). No new mirror guard is written; the existing one is added to
  Task 1's verify command so a one-sided edit fails immediately.
- **REJECTED — "maintain a contract census distinguishing authoritative config, executable
  implementations, generated code and prose" (gpt suggestion, the 'five sites is unstable'
  MEDIUM).** This plan DELETES a site rather than cataloguing it: the refusal message becomes
  derived, and the YAML→JS parity test makes the remaining pair fail loudly on drift. A census
  document would be a fifth artifact to keep in step with four that already disagree — the
  duplication D-61-06 exists to reduce, not another instance of it.
</review_dispositions>

<success_criteria>
Every gate that runs on a LinkedIn-URL-only row accepts it, and the walk's exact input is no
longer refused by anything in the plugin.
</success_criteria>

<output>
Create `.planning/phases/61-autonomous-batch-runs/61-03-SUMMARY.md` when done.
</output>
