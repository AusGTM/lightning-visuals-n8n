---
phase: "61-autonomous-batch-runs"
plan: "61-06"
type: execute
wave: 5
depends_on: ["61-05"]
files_modified:
  - scripts/build_cloud_workflows.py
  - operator-claude-plugin/scripts/preingest.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - tests/n8n/pairPipelineAssociationFlow.test.mjs
  - operator-claude-plugin/tests/test_unattended_pair_composition.py
  # REVIEW-11: "one grant across the lane" needs an executable change, not only SKILL.md
  # prose. `covers()` refuses any record id absent from the grant at grant time — and a
  # company created DURING the run has exactly such an id. Task 3 closes that.
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/tests/test_write_grant.py
  # REVIEW-10: n8n cannot write a file on the operator's machine. The client-side consumer
  # that routes n8n's held outcomes into 61-04's queue lives here.
  - operator-claude-plugin/scripts/held_queue.py
  # REVIEW-A3: `main()` rewrites all eight.
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_review_decision_cloud.json
autonomous: false
requirements: [RUN-01, RUN-02, AFTER-02]

estimate:
  # Raised from 105000 by the revision: Task 3 gains an executable `write_grant.py` change
  # (REVIEW-11) and Task 1 gains the client-side held-row consumer (REVIEW-10). Task 2 got
  # SMALLER (REVIEW-12 replaces the lag machinery with a by-value id), which offsets part of it.
  tokens: 120000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One grant carries a batch through ingest, enrich, create and associate — the operator consents once for the batch, not once per row."
    - "A contact that cannot resolve a company is HELD, never landed — the 2026-08-25 association contract holds on the create path too."
    - "A held outcome produced inside n8n reaches the operator's own review queue: the run's response is parsed client-side and the row is written to 61-04's held queue — n8n never writes a local file (REVIEW-10)."
    - "A company created earlier in the same run is associated using the id its create response returned, not by searching for it again (REVIEW-12)."
    - "The single batch grant actually authorizes the writes the batch performs, including writes to a record created under that same grant — proven by a test over the real grant functions, not by prose (REVIEW-11)."
    - "The search-index lag between a company create and the contact ingest that must find it is handled by the run, not by an operator watching."
    - "Failed rows are collected and re-sendable as one well-formed request; the batch does not abandon them."
    - "No live unattended run happens in this plan — the first one is gated on Phase 57's ceiling work (D-61-08)."
  artifacts:
    - tests/n8n/pairPipelineAssociationFlow.test.mjs
    - operator-claude-plugin/tests/test_unattended_pair_composition.py
  key_links:
    - "CLAUDE.md §13.0.1: the association rule is implemented in the ingest lane ONLY — `wf_enrichment_cloud`'s own contact create does not associate. That gap is load-bearing for RUN-02."
---

<objective>
One grant, one batch, all the way through: ingest, enrich, create, associate — with held rows
collected rather than guessed.

Purpose: RUN-02 and AFTER-02, absorbed from Phase 56 by D-61-08. 61-02 through 61-05 made a row
resolvable, made the system able to say when it is unsure, and took the run off the synchronous
window. This plan joins those into the pipeline the operator was promised, and stops at a
checkpoint before any live run.

Output: the full pair lane under a single grant, association enforced on creates, index lag
handled by the run, and a blocking checkpoint that names Phase 57 as the gate on the first live
unattended run.

D-61-08 is explicit: Phase 56's original gate survives the fold. The first live unattended run
is gated on Phase 57's ceiling work, and that gate now applies to this phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-autonomous-batch-runs/61-CONTEXT.md
@.planning/phases/61-autonomous-batch-runs/61-05-SUMMARY.md
@.planning/phases/61-autonomous-batch-runs/61-04-SUMMARY.md
@n8n/code/companyLink.js
@tests/n8n/companyAssociationFlow.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the association gap on the create path, or route creates through the lane that has it</name>
  <files>scripts/build_cloud_workflows.py, tests/n8n/pairPipelineAssociationFlow.test.mjs</files>
  <read_first>
    CLAUDE.md §13.0.1 in full — the operator ruling that a contact must ALWAYS be associated
    with a company and an existing company must NEVER be recreated, the eight nodes the ingest
    lane gained, the resolution order (manual `company_id`, then exact email-domain, then exact
    company-name), and the closing sentence naming the gap: `wf_enrichment_cloud`'s own contact
    create does not associate.
    `n8n/code/companyLink.js` — `resolveCompanyLink`, the resolution the ingest lane already
    performs.
    `tests/n8n/companyAssociationFlow.test.mjs` and `tests/n8n/contactCreateGateFlow.test.mjs`
    — the flow-test shapes to mirror.
    `scripts/build_cloud_workflows.py`'s `Build Association Request` node — note that it joins
    each write RESPONSE back to its row BY VALUE, not by index, because index alignment is gone
    downstream of the write IFs.
  </read_first>
  <action>
    RUN-02 requires association enforced on a run whose creates are included. There is a known
    gap: the association rule lives in the ingest lane only, so a contact created by
    `wf_enrichment_cloud` lands unassociated.

    ROUTE CREATES THROUGH THE INGEST LANE. Two reviewers independently recommended it and the
    original plan's "pick one of two routes and record why" left the load-bearing choice to
    executor preference — which the reviewers correctly read as the plan not deciding. The
    ingest lane already carries the eight nodes CLAUDE.md §13.0.1 lists, including
    `Build Association Request` (joins by VALUE, because index alignment is gone downstream of
    the write IFs) and `HubSpot Associate Company`. Reusing them keeps exactly ONE operational
    implementation of the rule; closing the gap in the enrichment lane would create a second
    copy of an association rule, which is the duplication CLAUDE.md's own as-built deltas keep
    recording the cost of.

    Deviating is allowed only on evidence found at execution — if routing through the ingest
    lane turns out to require more new wiring than closing the gap does, record the measurement
    that shows it, and say in the summary what stops the two copies from drifting. "It seemed
    simpler" is not that evidence.

    REVIEW-10, the transport for a held outcome: n8n Cloud cannot write
    `operator-claude-plugin/scripts/held_queue.py`'s file — it runs on someone else's machine.
    So the lane RETURNS the held outcome on its response (`company_hold_reason` is already the
    contract vocabulary, `companyLink.js:157-158`), and the CLIENT writes the queue entry:
    parse the response in `preingest.py` using 61-04 Task 1's outcome parser, then call
    `held_queue`. Add the client-side consumer in this task's file list rather than leaving the
    hold as an n8n-side fact nobody local ever records — a hold the operator's review pass never
    shows is indistinguishable from a row that vanished.

    Whichever route is chosen, the 2026-08-25 contract holds unchanged: a contact that cannot
    resolve a company is HELD, never landed, and a create that resolves no company is downgraded
    with its reason kept. An update is never held — it may already carry an association this
    lane cannot see. The lane RESOLVES a company and never creates one; company creation stays
    where dedupe already lives.

    Route a held-for-no-company row into 61-04's held queue with that reason, so it joins the
    single end-of-run review pass rather than becoming a second kind of held row with its own
    surface. The flow test asserts the n8n side returns the hold; the composition test in Task 2
    asserts the row actually lands in the local queue. Both, because either alone leaves the
    boundary untested in the direction it breaks.

    All n8n changes go through `scripts/build_cloud_workflows.py`. Never hand-edit
    `n8n/wf_enrichment_cloud.json`.

    Write `tests/n8n/pairPipelineAssociationFlow.test.mjs`: a batch containing a contact that
    resolves a company, one that resolves none, and one that is an update rather than a create.
    Assert the first is associated, the second is held with its reason and NOT landed, and the
    third is neither held nor blocked. The second assertion is the load-bearing one — a test
    that only proves association happens would pass on an implementation that lands an
    unassociated contact whenever resolution fails, which is precisely the outcome the contract
    forbids.
  </action>
  <verify>
    <automated>node --test tests/n8n/pairPipelineAssociationFlow.test.mjs tests/n8n/companyAssociationFlow.test.mjs tests/n8n/contactCreateGateFlow.test.mjs</automated>
  </verify>
  <done>A create that resolves a company is associated; one that resolves none is held with its reason and not landed; an update is unaffected; one implementation of the rule, not two; the held outcome is returned on the response for the client to record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the created company's id forward by value, and bound what is left of the lag</name>
  <files>scripts/build_cloud_workflows.py, operator-claude-plugin/scripts/preingest.py, operator-claude-plugin/tests/test_unattended_pair_composition.py</files>
  <read_first>
    CLAUDE.md §13.0.1's live evidence — Harness Racing NSW, company `18756544347` held under a
    different domain, which a `hrnsw.com.au` request would have duplicated (execution `11922`).
    That is the shape of a resolution miss that is NOT an index lag, and the two must not be
    conflated.
    `operator-claude-plugin/tests/test_report_sufficiency.py` lines 185-243 — the poll-loop
    guard: no plugin script outside `watch.py` may import `time`, call a sleep, or contain a
    while loop. That constraint decides where this mechanism can live.
    `.planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md` `## Substrates` — whether
    the selected substrate offers an n8n-side wait.
  </read_first>
  <behavior>
    - A contact whose company was created earlier in the same run is associated using the id
      that create RETURNED — no second search, no waiting, no retry loop.
    - Several contacts naming the same new company coalesce onto ONE company create, and all of
      them associate to the id it returned.
    - A contact whose company genuinely does not exist is held — a zero-hit search with no
      create evidence for that company in this run is "not found", not "not yet indexed".
    - A zero-hit search WITH durable evidence that this run created that company is the only
      case allowed to be called lag, and it is held with a reason naming the lag after a bounded
      number of attempts — never retried without end.
    - No plugin script outside `watch.py` gains an import of `time`, a sleep call, or a while
      loop.
  </behavior>
  <action>
    REVIEW-12 (gpt CRITICAL, gemini HIGH, kimi as a suggestion) turns this task from an addition
    into a SUBTRACTION, and the reviewers are plainly right: HubSpot's create response already
    returns the authoritative company id. Waiting for the search index to catch up is slower and
    ambiguous — a zero-hit search cannot tell indexing lag from a company that was never created,
    from a failed create, from the wrong lookup key (CLAUDE.md §13.0.1's Harness Racing NSW case,
    execution `11922`, is exactly that last one and is NOT lag).

    So: carry the created company's id forward BY VALUE. The run plan names, per contact, either
    an existing company id or the create it depends on; contacts naming the same new company
    coalesce onto one create; and the association uses the id from that create's response. This
    is the same by-value join `Build Association Request` already performs for write responses —
    reuse the shape rather than inventing a second correlation rule.

    What remains after that is small and must still be bounded: a search that returns nothing for
    a company this run has durable create evidence for. ONLY that case may be called lag, and it
    gets a bounded number of attempts and then a hold whose reason names the lag, so the operator
    reads "we could not confirm it in time" rather than "it does not exist". Everything else with
    zero hits is unresolved, and unresolved holds immediately — no waiting on a company nobody
    has evidence exists.

    Do not add a sleep or a while loop to a plugin script. The guard forbidding it is a
    deliberate design constraint; `watch.py` is the one allowed poll site, and with the id
    carried by value there is almost nothing left to poll for.

    Cover every `<behavior>` line in `test_unattended_pair_composition.py`, including the
    coalescing case and the termination case — an unbounded retry passes a test that only checks
    the happy path — and the REVIEW-10 assertion: an n8n-held row lands in the LOCAL held queue
    with its reason.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_unattended_pair_composition.py operator-claude-plugin/tests/test_report_sufficiency.py -q</automated>
  </verify>
  <done>A same-run company is associated from the id its create returned, with no second search; contacts sharing a new company coalesce onto one create; a genuinely absent company holds the row immediately; only a create-evidenced zero-hit is called lag, and it terminates with a reason; an n8n-held row is in the local queue; the poll-loop guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: One grant across the whole lane — including the records the run creates — failed rows re-sendable, and the end-of-run account</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_unattended_pair_composition.py, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    `operator-claude-plugin/scripts/chunking.py` `failed_batch` and `DispatchOutcome` — a failed
    batch is already a record specification the envelope builder accepts unmodified, which is
    the whole of the re-sendable requirement; it must not be reconstructed.
    `operator-claude-plugin/scripts/written_records.py` — the post-run account of what was
    written, and D-59-10's rule that a bookkeeping failure never stops the dispatch but is
    surfaced loudly.
    `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` step 1's "this flow arms
    twice" statement, which this task changes to one grant for the batch.
    `operator-claude-plugin/tests/test_skill_sequence_coverage.py` — `MAX_GRANDFATHERED` is 0.
  </read_first>
  <action>
    Amend `enrich-before-ingest/SKILL.md` so one grant carries the batch through ingest, enrich,
    create and associate. Consent is once for the batch, not once per row — D-61-01's whole
    point, and the difference between one run plus one review and three hundred conversations.

    REVIEW-11 — "one grant is documentation-only" — is answered in two parts, because the
    reviewers were partly working from a wrong premise and partly finding a real gap.

    The machinery EXISTS and is not prose: `plan_grant(config, lanes=[...], ...)` already opens a
    grant spanning both lanes, D-53-05 (operator, 2026-08-25) already collapses this flow's two
    asks into one when such a grant is open, `_consequence()` already has the two-lane branch,
    and `authorize_send` / `authorize_ungranted_send` already route every send through it. Cite
    those by name in the skill amendment so the next reader sees the code behind the sentence.

    THE REAL GAP, and this task's executable change: `write_grant.covers()` refuses any record id
    not present in the grant's `record_ids` at grant time — "widening it needs a new grant
    (GRANT-03)". A company or contact CREATED DURING THE RUN has exactly such an id. An
    unattended batch that creates a company and then associates a contact to it therefore hits a
    scope refusal partway through, under a grant the operator believed covered the batch. Verify
    this against the code before changing anything (it is a claim about `covers()`'s id check,
    not an assumption), and if it holds, close it in the ONE place scope is decided:
    - a record created UNDER this grant, whose id came back from a write this grant authorized,
      is inside the grant for the remainder of that grant, and nowhere else;
    - the admission is recorded on the grant object in memory only — never persisted, never
      readable off disk on a later run (GRANT-06, unchanged);
    - it admits ids this grant's own writes produced, and nothing else: it is not a widening the
      operator did not consent to, it is the same batch's own records, which is exactly what
      they did consent to;
    - the refusal wording for a genuinely outside id stays as it is.
    Pin it in `test_write_grant.py`: an id created under the grant passes `covers()`; an
    unrelated id still refuses; the admission does not survive into a second grant. If the
    verification shows creates are already covered by another path (domain scoping, for
    instance), record THAT finding and change nothing — a fix for a defect that does not exist is
    worse than no fix.

    What that grant does NOT relax, stated where the operator meets it: the non-clobber merge
    policy, the n8n write-safety gate nodes, `plan_grant`'s empty-record-set refusal, the
    material-conflict judge gate, per-send armed-window narrowing, and the post-run written-
    records account. With no HubSpot rollback and roughly 700 live company records reachable,
    those guards are the reason a batch grant can be given at all.

    Failed rows: hand back `chunking.failed_batch`'s existing specification unchanged. It is
    already well-formed for re-sending; rebuilding it would be a second derivation of one
    contract, which this codebase has repeatedly paid for.

    End the run with the account AFTER-02 needs alongside 61-04's queue: what was written, what
    was held and why, and what failed and is re-sendable. Distinguish a row that was written
    from one that would have been written under a grant that was not open — a gated record must
    never read as a completed one. Note in the summary that the fuller end-of-run report,
    per-run ceilings, and the post-run allowlist proof belong to Phase 57 (RUN-05, AFTER-01,
    AFTER-03) and are deliberately not built here.

    A resumed run gets a FRESH grant. A grant is never persisted and never rehydrated from a
    manifest — 61-05's resume brings back which rows still need work, not the authority to write
    them. Say so in the skill, and assert in the composition test that a resume with no open
    grant writes nothing.

    Extend `test_unattended_pair_composition.py` to drive the whole documented sequence under
    one grant, and register it in the census's `COVERED` map. No grandfather entry is available.
    Drive the REAL grant functions (`plan_grant` / `open_grant` / `covers`) with only the
    transport injected — mocking the grant out would leave the reviewers' "prose-only" objection
    exactly as true as they found it.

    Bump `plugin.json` and add the `CHANGELOG.md` entry in the same commit — read the current
    version out of the file and bump the MINOR component from what is actually there.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_unattended_pair_composition.py operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q</automated>
  </verify>
  <done>One grant carries the documented sequence end to end in one test driving the real grant functions; a record created under the grant is inside it and an unrelated id still refuses; a resumed run needs a fresh grant; failed rows come back as the existing re-sendable specification; written and would-have-been-written are distinguishable; the census passes with no new grandfather entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Accept the offline pipeline and hold for Phase 57</name>
  <what-built>
    The unattended pair pipeline, offline and undeployed: one grant across ingest, enrich, create
    and associate; association enforced on creates with a contact that cannot resolve a company
    held rather than landed; index lag absorbed by the run with a bound; failed rows re-sendable
    as the existing well-formed specification; held rows collected into one review queue.
    Nothing has been deployed, nothing armed, no live run performed.
  </what-built>
  <how-to-verify>
    1. Read `61-06-SUMMARY.md`'s route decision for Task 1 — whether the association rule was
       closed in the enrichment lane or creates were routed through the ingest lane — and
       confirm exactly one implementation of the rule now exists.
    2. Confirm the regenerated workflow diff carries only the nodes this plan named. Credentials,
       `webhookId` and the executeWorkflow placeholder always differ; they are not signal.
    3. THE FIRST LIVE UNATTENDED RUN IS GATED ON PHASE 57 (D-61-08). Phase 56's original gate
       survives the fold into this phase: per-run ceilings on provider credits, Anthropic dollars
       and n8n executions, enforced before the first record, plus refusal-before-start and the
       post-run allowlist proof, are Phase 57's work and are not built here. Do not deploy, arm
       or run this batch live until that work has landed.
    4. When Phase 57 has landed: deploy and bounce (admin action — a bare PUT does not reload a
       running workflow), then run one small batch under one grant, and check the observed
       execution count against 61-01's arithmetic before any larger batch.
  </how-to-verify>
  <resume-signal>Type "approved — holding for Phase 57" to accept the offline work, or describe what differed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one batch grant→many HubSpot writes | A single consent now authorises many writes to a CRM with no rollback |
| company resolution→contact create | An unresolved company could produce an orphaned or duplicate record |
| index lag handling | An unbounded wait or an unbounded retry could stall or over-spend a run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-19 | Elevation of privilege | one grant across the lane | critical | mitigate | Every write-safety gate, the non-clobber merge policy, the empty-record-set refusal and per-send armed-window narrowing remain in force; the first live run is gated on Phase 57's ceilings by a blocking checkpoint |
| T-61-20 | Tampering | contact create without association | high | mitigate | A contact that cannot resolve a company is held, never landed; one implementation of the rule; the flow test's load-bearing assertion is the held case, not the happy path |
| T-61-21 | Denial of service | index-lag handling | high | mitigate | Bounded attempts, then the row is held with a reason naming the lag; no sleep or while loop outside `watch.py`; termination asserted by test |
| T-61-24 | Elevation of privilege | `covers()`'s created-record admission (Task 3) | critical | mitigate | Only ids returned by writes THIS grant authorized are admitted; the admission lives on the in-memory grant object and is never persisted (GRANT-06); an unrelated id still refuses with the unchanged wording; a resumed run needs a fresh grant, so the admission cannot outlive the batch that earned it — all four pinned in `test_write_grant.py` |
| T-61-22 | Repudiation | end-of-run account | high | mitigate | Written and would-have-been-written are distinguished; failed rows come back as the existing re-sendable specification; the fuller proof is Phase 57's and is named as deferred rather than assumed |
| T-61-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — nothing to audit |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green (glob form only).
- `.venv/bin/python -m pytest -q` green (baseline 3365 / 154).
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green (baseline 1725 / 5).
- The poll-loop guard passes unchanged.
- The eight workflow JSONs regenerated through the builder, never hand-edited; report which
  changed.
- Zero live n8n, HubSpot, Anthropic or provider calls before the checkpoint; nothing armed.
- No assertion weakened, deleted or reworded; report the count.
</verification>

<review_dispositions>
Cross-AI review (`61-REVIEWS.md`, 2026-08-30). Findings assigned to this plan:

- **HIGH 10 — held-row transport gap (n8n cannot write the local queue).** INCORPORATED, Task 1
  and Task 2: the lane RETURNS the hold on its response using the existing `company_hold_reason`
  vocabulary, and the client parses it (61-04 Task 1's parser) and writes the queue entry.
  `held_queue.py` and `preingest.py` are in `files_modified`; the composition test asserts an
  n8n-held row reaches the LOCAL queue.
- **HIGH 11 — "one grant" is prose-only.** PART REJECTED, PART INCORPORATED. Rejected half: the
  one-grant machinery is real code, not documentation — `plan_grant(lanes=[...])`, D-53-05's
  single-ask branch, `_consequence()`'s two-lane text, `authorize_send` — and the skill
  amendment now cites it by name. Incorporated half, which is the reviewers' real find:
  `covers()` refuses any id absent from the grant at grant time, so a record CREATED during the
  run is outside its own batch's grant. Task 3 verifies that against the code and, if it holds,
  admits ids produced by that grant's own writes — in memory only, never persisted, unrelated
  ids still refused — with `write_grant.py` and `test_write_grant.py` in `files_modified`.
- **HIGH 12 — same-run company-ID propagation.** INCORPORATED, Task 2, as a SUBTRACTION: the
  create response's id is carried forward by value, contacts sharing a new company coalesce onto
  one create, and the lag machinery shrinks to the single residual case (zero hits WITH durable
  create evidence), bounded and held with a lag-naming reason. Everything else at zero hits is
  unresolved and holds immediately, which also answers "a zero-result search cannot distinguish
  lag from absence" — it no longer has to.
- **MEDIUM (kimi/gpt) — Task 1 left the association route open.** INCORPORATED: the plan now
  DECIDES — route creates through the ingest lane, keeping one implementation — and deviation
  requires a measurement, not a preference.
- **Actionable — regenerated workflow artifacts.** INCORPORATED: all eight listed.
- **Actionable — version bump.** INCORPORATED, Task 3: read the file, bump the minor.
- **REJECTED — "add a runtime readiness flag that refuses unattended mode until Phase 57
  controls are installed" (gpt suggestion).** That flag IS Phase 57's work — RUN-05/AFTER-01/
  AFTER-03, named as deferred in Task 3 and D-61-08. Building a half-version here would put a
  machine-enforced ceiling in the codebase that Phase 57 must then replace, and a refusal switch
  whose ceilings do not yet exist refuses on nothing. The gate this plan CAN enforce it does: a
  blocking checkpoint, nothing deployed, nothing armed, no live run.
- **RECORDED, not counted (kimi's sole HIGH) — one grant authorizes many writes to a no-rollback
  CRM.** This is the phase's own T-61-19 with the mitigations kimi lists, and the orchestrator's
  adjudication records it as an inherent design constraint rather than an unresolved defect. No
  change; the trade is D-61-07's, made by the operator.
</review_dispositions>

<success_criteria>
An operator hands over a batch and gets it back done: one grant, one run, one review pass over
the held rows — with the first live execution of it deliberately gated on Phase 57.
</success_criteria>

<output>
Create `.planning/phases/61-autonomous-batch-runs/61-06-SUMMARY.md` when done.
</output>
