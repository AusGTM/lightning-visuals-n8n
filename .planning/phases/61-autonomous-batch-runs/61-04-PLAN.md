---
phase: "61-autonomous-batch-runs"
plan: "61-04"
type: execute
wave: 3
depends_on: ["61-03"]
files_modified:
  - operator-claude-plugin/scripts/confidence.py
  - operator-claude-plugin/scripts/held_queue.py
  - operator-claude-plugin/scripts/run_manifest.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/tests/test_confidence.py
  - operator-claude-plugin/tests/test_held_queue.py
  - operator-claude-plugin/tests/test_run_manifest.py
  - operator-claude-plugin/tests/test_batch_finishes_composition.py
autonomous: true
requirements: [RUN-02, AFTER-02]

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every row gets a confidence verdict computed from signals that already exist — no new model call, no new scale, no per-row question."
    - "A confident row proceeds with no gate; an unconfident row is held with a named reason and never guessed at."
    - "A held row never stops the rows behind it — the batch reaches its last row whatever any single row does."
    - "A confidence-held row and a no-email-held row are DIFFERENT states: a resume does not re-spend provider credit on a row that was held for low confidence."
    - "Held rows survive the session in a durable artifact that carries the reason, and are cleared in one pass with the vocabulary the plugin already uses for match decisions."
  artifacts:
    - operator-claude-plugin/scripts/confidence.py
    - operator-claude-plugin/scripts/held_queue.py
    - operator-claude-plugin/tests/test_batch_finishes_composition.py
  key_links:
    - "`run_manifest.py`'s five verdict words are a schema, not a list — adding a sixth is a deliberate decision, and `rows_to_resume`'s branch for it decides whether credit is re-spent."
---

<objective>
Give the system the confidence self-assessment whose absence IS finding F, and turn it into the
autonomy policy D-61-07 locks: confident rows proceed unattended, unconfident rows are held
with a reason, and the batch always finishes.

Purpose: the operator's diagnosis is that there is no confidence self-assessment and therefore
no autonomy — 300 contacts become 300 conversations. D-61-07 makes deciding what confidence
MEANS core phase work, not a detail. This plan decides it, from signals the pipeline already
produces, and builds the hold-and-collect it hangs on.

Output: a deterministic confidence verdict per row, a durable held-rows queue that survives the
session, and a batch that reaches its last row regardless of what any single row does.

What is NOT relaxed here: the non-clobber merge policy, the n8n write-safety gate nodes,
`plan_grant`'s empty-record-set refusal, the material-conflict judge gate, per-send armed-window
narrowing, and the post-run written-records account. The operator kept non-clobbering by name.
What IS removed is approval friction and per-row research halts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-autonomous-batch-runs/61-CONTEXT.md
@.planning/phases/53-operator-openable-write-grant/53-WALK-RECORD-3.md
@operator-claude-plugin/scripts/run_manifest.py
@operator-claude-plugin/scripts/preingest.py
@operator-claude-plugin/scripts/durable_paths.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define confidence as a deterministic table over signals that already exist</name>
  <files>operator-claude-plugin/scripts/confidence.py, operator-claude-plugin/tests/test_confidence.py</files>
  <read_first>
    `n8n/code/matchProposal.js` lines 115-146 — `summarizeMatch`'s four tiers (`high`,
    `medium`, `none`, `unknown`) and the reason `unknown` is deliberately not `none`.
    `operator-claude-plugin/scripts/preingest.py` lines 236-335 — `classify_matches`' four
    buckets, which are the client-side shape of those tiers.
    `n8n/code/scoreEnrichment.js` lines 70-100 — `agreedBy`, the per-field set of providers
    that returned the same normalized value, and the agreement ratio computed from it.
    `n8n/code/providerConflict.js` — `detectConflicts`/`groupConflicts`, and CLAUDE.md §15.0's
    material-conflict tiers, which already say a disputed value on a veto-capable field is left
    ABSENT and the record flagged.
    `scripts/build_cloud_workflows.py` around line 2800 — `judge_confidence_by_field`, the
    judge's own per-field verdict.
  </read_first>
  <behavior>
    - A row matched by a strong key (tier `high`) with no material conflict is `confident`.
    - A row whose match tier is `unknown` is never `confident` — "we could not look" is not a
      basis for acting.
    - A row with a material conflict on a veto-capable field is never `confident`, whatever its
      match tier — the judge's own suppression rule already withholds that value.
    - A row with an adjudicated judge verdict on the disputed field is `confident` again, on the
      adjudicated value — the ban is on an unadjudicated conflict, not on the field.
    - A row with medium-tier candidates and more than one of them is never `confident` — an
      ambiguous person is exactly the wrongly-matched-person risk D-61-03 fences off.
    - The same inputs always produce the same verdict: no clock, no randomness, no network.
    - Every non-confident verdict carries a reason naming the signal that withheld it.
  </behavior>
  <action>
    Per D-61-07: "the phase needs a real confidence signal to hang this on — today there is
    none, and that absence IS the finding."

    Write `operator-claude-plugin/scripts/confidence.py`: one pure function taking a row plus
    the verdicts the pipeline already produced for it, returning a small frozen dataclass
    carrying a verdict and, when not confident, a reason.

    Compose it from signals that ALREADY EXIST. Add no new scale, no new numeric score and no
    new model call — a confidence number invented here would be a fourth thing to keep in step
    with three that already disagree, and D-61-07 asks for a signal, not a metric. The three
    inputs are: the match tier `summarizeMatch` already stamps; the provider-agreement signal
    `scoreEnrichment.js` already computes as `agreedBy`; and the material-conflict / judge
    verdict CLAUDE.md §15.0 already routes. Express the combination as an explicit decision
    table read top to bottom, first match wins, so the whole policy is legible in one screen and
    a future change is a table row rather than a nest of conditions.

    Two verdicts and no third: a row is confident or it is held. A middle band would recreate
    the per-row question this phase exists to remove.

    The module performs no I/O, no config read and no network call, mirroring
    `preingest.classify_matches`' own purity — it takes already-fetched verdicts exactly as
    `run_manifest.rows_to_resume` takes an already-loaded manifest.

    Cover every row of the decision table in `test_confidence.py`, plus the determinism property
    (same inputs, same verdict, called twice) and the property that every held verdict names a
    signal. A table with an unreachable row is a policy nobody can audit, so assert each row is
    reachable by at least one case.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_confidence.py -q</automated>
  </verify>
  <done>Every decision-table row is exercised; an `unknown` tier and an unadjudicated material conflict each independently prevent a confident verdict; every held verdict names its signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A durable held-rows queue, and a sixth verdict word so a resume does not re-spend</name>
  <files>operator-claude-plugin/scripts/held_queue.py, operator-claude-plugin/scripts/run_manifest.py, operator-claude-plugin/tests/test_held_queue.py, operator-claude-plugin/tests/test_run_manifest.py</files>
  <read_first>
    `operator-claude-plugin/scripts/run_manifest.py` IN FULL — its module docstring is the
    design brief for this task. Note three things it settles: why it is a separate file rather
    than a widening of `artifact_store.py`; why a manifest failing ANY validation degrades WHOLE
    rather than partially; and `rows_to_resume`'s branch for `held`, which re-includes a held
    row the moment it gains an email.
    `operator-claude-plugin/scripts/durable_paths.py` — `resolve_state_path` and
    `_atomic_write_0600`, the one atomic-write authority this plugin already centralised.
  </read_first>
  <behavior>
    - A row held for low confidence is recorded under its own verdict word, distinct from the
      existing no-email `held`.
    - `rows_to_resume` EXCLUDES a confidence-held row that still has an email, rather than
      re-including it — re-running it would re-spend provider credit only to hold it again.
    - A confidence-held row IS re-included once the signal that held it has changed.
    - The held queue survives the process that wrote it and survives a plugin update.
    - A queue file that is missing, truncated, malformed or schema-mismatched degrades to an
      empty queue, never to a partially-trusted one.
    - The queue refuses to persist any key or value whose name suggests an arming grant, a
      live-write permission, a secret or an API key.
  </behavior>
  <action>
    AFTER-02 needs held and failed rows to survive the session. Neither existing store can carry
    them: `run_manifest.py` holds verdict WORDS and refuses widening by design, and a
    never-landed row has no HubSpot record to flag. So this is a third durable artifact —
    written the way `run_manifest.py` was written, for the reasons its own docstring gives.

    Write `operator-claude-plugin/scripts/held_queue.py`. Give it its OWN schema and its OWN
    refusal rather than widening either existing store: a run id, a timestamp, and per row the
    `row_id`, the reason from Task 1's verdict, and enough of the row to re-send it. Write
    through `durable_paths._atomic_write_0600`, resolve the path fresh on every call against
    `durable_paths.resolve_state_path().parent`, and give the file a non-dotfile name — every
    one of those is a rule `run_manifest.py` already established and paid for. Carry over its
    forbidden-name refusal verbatim in substance: the arming grant exists as a call argument for
    one turn and must never be readable off disk on a later run. Degrade a bad read WHOLE.

    Then add a sixth word to `run_manifest.py`'s `ALLOWED_VERDICTS` for a confidence hold, and
    give `rows_to_resume` its own branch. This is the collision that would otherwise cost real
    money: today `held` means "no email, so the ingest gate will refuse it identically", and the
    branch re-includes such a row as soon as it has an email. A confidence-held row usually HAS
    an email — it was held because the system was not sure enough to act — so it would be
    re-sent on every resume, re-spending provider credit to reach the same hold. Two causes with
    opposite resume rules cannot share one word.

    Update `run_manifest.py`'s module docstring in the same edit: it currently states five words
    as the whole schema, and a docstring that outlives its own schema is how the next reader is
    misled. Extend `test_run_manifest.py` to pin the new word's resume behaviour and to prove
    the existing five words' behaviour is byte-for-byte unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_held_queue.py operator-claude-plugin/tests/test_run_manifest.py -q</automated>
  </verify>
  <done>A confidence-held row is excluded from a resume while its holding signal stands; a no-email held row's existing behaviour is unchanged; a truncated queue file reads as empty rather than partial.</done>
</task>

<task type="auto">
  <name>Task 3: Wire hold-don't-block into the run, and prove the batch always finishes</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_batch_finishes_composition.py, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    `operator-claude-plugin/scripts/chunking.py` lines 25-45 — fact 5, the D-59-10 precedent
    that a bookkeeping failure never stops the dispatch, and how the run surfaces it loudly
    instead. That is the exact shape hold-don't-block takes here.
    `operator-claude-plugin/scripts/preingest.py` lines 355-430 — `apply_match_decisions` and
    its validate-then-apply, all-or-nothing discipline.
    `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` step 3's numbered-table
    decision vocabulary (`approve` / `deny` / `pick` / `email:`), and its rule that a bare
    blanket approval with no named scope is refused outright.
  </read_first>
  <action>
    Per D-61-07, amend `enrich-before-ingest/SKILL.md` so the run's shape is: confident rows
    proceed with no per-row gate; unconfident rows are recorded to the held queue with their
    reason and the run moves to the next row; the batch reaches its last row whatever any single
    row does; and held rows are presented ONCE at the end as one review pass.

    State the three properties in the operator's own terms where they meet them, because they
    are the promise this phase is measured against: nothing is guessed, nothing is written that
    was held, and nothing waits for them mid-run.

    Present the end-of-run review with the numbered-table vocabulary step 3 already uses. Do not
    invent a second decision vocabulary — the operator has already learned this one, and a
    second one is the duplication this codebase keeps paying for. Carry over its two safety
    rules unchanged: a bare blanket approval with no named scope is refused, and a single
    malformed line refuses the whole table before anything is applied.

    Say plainly what stays: the non-clobber merge policy, the write-safety gates, the
    empty-record-set refusal, the material-conflict judge gate, the per-send armed window, and
    the post-run account of what was written. The operator kept non-clobbering by name; with no
    HubSpot rollback and roughly 700 live records reachable, those guards are what make autonomy
    grantable at all.

    Write `test_batch_finishes_composition.py`: drive a batch in which one row is confident, one
    is held for low confidence and one fails its chunk outright, and assert the run reaches its
    last row, that the held and failed rows are both in the durable queue with reasons, and that
    the confident row completed. The load-bearing assertion is the LAST row's completion — a
    test that only checks the held row was recorded would pass on an implementation that stops
    the batch at the hold. Register the sequence in the census's `COVERED` map; no grandfather
    entry is available (`MAX_GRANDFATHERED` is 0).

    Bump `plugin.json` and add the `CHANGELOG.md` entry in the same commit.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_batch_finishes_composition.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q</automated>
  </verify>
  <done>A batch containing a held row and a failed row still processes its final row; both non-completing rows are in the queue with reasons; the census passes with no new grandfather entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confidence verdict→autonomous write | A wrongly-confident verdict removes the human from a write to a CRM with no rollback |
| held queue→disk | A durable artifact on the operator's machine could become a place a grant is parked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-11 | Elevation of privilege | `confidence.py` decision table | critical | mitigate | Two verdicts only; `unknown` match tier and unadjudicated material conflict each independently block a confident verdict; the merge policy, write-safety gates and armed window all remain downstream and untouched |
| T-61-12 | Information disclosure | `held_queue.py` | high | mitigate | Carries `run_manifest.py`'s forbidden-name refusal verbatim in substance; written 0600 through the single atomic-write authority; no grant, secret or API key can be persisted under a row-shaped key |
| T-61-13 | Tampering | `held_queue.py` read path | high | mitigate | Any anomaly degrades the whole queue to empty rather than to a partial one — the same reasoning `run_manifest.load()` records, because a partial read silently drops a row that was never handled |
| T-61-14 | Repudiation | resume after a confidence hold | medium | mitigate | The sixth verdict word keeps the two hold causes distinct, so a resume neither re-spends credit nor silently drops a row |
| T-61-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — nothing to audit |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green (baseline 1725 / 5).
- `.venv/bin/python -m pytest -q` green (baseline 3365 / 154).
- `test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status`
  still passes — neither new module imports `time`, calls a sleep, or contains a while loop.
- No assertion weakened, deleted or reworded; report the count.
- Zero live n8n, HubSpot, Anthropic or provider calls.
</verification>

<success_criteria>
A batch of many contacts runs to its last row unattended, with every held row collected into one
durable queue carrying its reason, ready for a single review pass.
</success_criteria>

<output>
Create `.planning/phases/61-autonomous-batch-runs/61-04-SUMMARY.md` when done.
</output>
