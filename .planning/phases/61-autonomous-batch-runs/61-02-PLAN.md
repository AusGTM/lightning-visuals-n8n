---
phase: "61-autonomous-batch-runs"
plan: "61-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/code/matchProposal.js
  - scripts/build_cloud_workflows.py
  - src/identity.py
  - operator-claude-plugin/scripts/enrichment.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - tests/n8n/matchProposal.test.mjs
  - tests/n8n/linkedinLaneFlow.test.mjs
  - tests/test_identity.py
  - operator-claude-plugin/tests/test_rows_envelope_contract.py
autonomous: true
requirements: [INPUT-05]

estimate:
  tokens: 105000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A row carrying only a LinkedIn URL routes to a `linkedin` lane and reaches a HubSpot search node — it no longer lands in lane `none` / tier `unknown`."
    - "A stored LinkedIn value that differs only in trailing slash, scheme, or `www.` still matches the operator's input — the search does not silently return zero hits on a cosmetic variant."
    - "Every HubSpot hit is re-verified by canonicalized value in the adapter before it is reported as a match; a fuzzy server-side hit is never trusted as a match on its own."
    - "In a mixed batch (email + linkedin + name rows in one request) every row gets exactly one response item — no row is routed to one lane and filtered into another."
    - "`linkedin_url` crosses the plugin boundary on a rows envelope, and the comment stating what that boundary admits still states the current truth."
  artifacts:
    - tests/n8n/linkedinLaneFlow.test.mjs
    - n8n/code/matchProposal.js
    - scripts/build_cloud_workflows.py
  key_links:
    - "`laneOf()` is the ONE field every downstream routing IF and adapter reads — a new lane must be added to laneOf, its routing IF, and its adapter filter together, or the row vanishes."
    - "The live contacts property is `lv_linkedin_url`; a bare `linkedin_url` contact property does not exist on this portal."
---

<objective>
Make a LinkedIn URL something the backend can actually search HubSpot with. This is the
TRACER: the thinnest end-to-end path for the exact row that failed walk run 4
(`https://www.linkedin.com/in/robert-cavallucci-14698741/`, no other fields), wired through
every layer the phase touches.

Purpose: D-61-05 (CORRECTED) — HubSpot matching by `linkedin_url` is DEAD ON THE LIVE PATH.
`resolveIdentity.js:76-90`'s branch is unreachable, `matchProposal.js::laneOf()` never reads
the key, and the only HubSpot search node in the contact branch filters `email EQ` only. This
plan lands the backend half FIRST, deliberately: the front-end half alone would ship rows into
a permanently unreachable `unchecked` bucket (the trap 61-RESEARCH.md Q3/Q5 names), whereas the
backend half alone is simply unreached capability sitting behind today's honest refusal. There
is no dishonest intermediate state in this order.

Output: a `linkedin` match lane that a row can actually traverse, proven by a mixed-batch flow
test that fails if the row dead-ends.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-autonomous-batch-runs/61-CONTEXT.md
@.planning/phases/61-autonomous-batch-runs/61-RESEARCH.md
@n8n/code/matchProposal.js
@n8n/code/resolveIdentity.js
@tests/n8n/companyNameFallbackFlow.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The linkedin lane, end to end — laneOf through routing IF through search node to adapter</name>
  <files>n8n/code/matchProposal.js, scripts/build_cloud_workflows.py, tests/n8n/matchProposal.test.mjs, tests/n8n/linkedinLaneFlow.test.mjs</files>
  <read_first>
    `n8n/code/matchProposal.js` lines 1-60 (`laneOf`, and the header warning that a row routed
    to one lane and filtered into another silently disappears) and lines 115-146
    (`summarizeMatch`, and why `unknown` is not `none`).
    `scripts/build_cloud_workflows.py` lines 4814-4890 — the Phase 36 `name` lane is the
    reference implementation for adding a lane as an additive row: `IF Has Email` false-edge →
    `IF Name Searchable` → `HubSpot Name Search` → `Adapt Name Search`, with the routing IF
    reading the SAME stamped `lane` field rather than re-deriving a predicate.
    `scripts/build_cloud_workflows.py` lines 5494-5530 — the connection map those IFs are wired
    into (`conns["IF Has Email"]`, `conns["IF Name Searchable"]`).
    `scripts/build_cloud_workflows.py` lines 1402-1438 — `ENRICH_ADAPT_SEARCH`, the
    filter-then-index-align adapter idiom every lane adapter follows.
    `scripts/build_cloud_workflows.py` lines 5160-5175 — `HubSpot Company Name Search` /
    `Adapt Company Name Search`, the 2026-08-25 second-key resolution pattern on the companies
    branch (CLAUDE.md §13.0.1), which is the closest existing analogue to what this task adds.
    `tests/n8n/companyNameFallbackFlow.test.mjs` — the flow-test shape to mirror.
  </read_first>
  <behavior>
    - `laneOf({identity_keys: {linkedin_url: "https://www.linkedin.com/in/x/"}})` is `"linkedin"`.
    - `laneOf` with BOTH email and linkedin present is `"email"` — email wins, mirroring
      `resolveIdentity.js`'s own strong-key ordering (email checked before linkedin).
    - `laneOf` with linkedin AND lastName+companyName is `"linkedin"` — linkedin is a strong key
      and outranks the weak name pair (D-61-03).
    - `laneOf` with an `object_id` and no email is still `"fetch_by_id"` — that branch is
      unchanged and mirrors `IF Bare Event`'s own boolean expression.
    - A whitespace-only `linkedin_url` is not a present key and yields `"none"`.
    - `summarizeMatch({lane:"linkedin", existingRecord:{...}})` is tier `high`, auto `true`.
    - `summarizeMatch({lane:"linkedin", existingRecord:{}})` is tier `none` — "searched, no hit".
    - `summarizeMatch({lane:"linkedin", lookupFailed:true})` is tier `unknown`.
    - A linkedin-only row is NEVER tier `unknown` on a successful search. That is the whole
      point: "could not look" must stop being this row's answer.
  </behavior>
  <action>
    Per D-61-05 (CORRECTED) and 61-RESEARCH.md's recommended shape B steps 2-4.

    In `n8n/code/matchProposal.js`: add a `linkedin` branch to `laneOf()`, positioned AFTER the
    email check and BEFORE the lastName+companyName check, reading
    `identityKeys.linkedin_url` through the same `trimmedOrValue` helper every other key uses.
    Add the matching `linkedin` arm to `summarizeMatch()` — join it to the existing
    `fetch_by_id`/`email` arm rather than writing a third copy of the same two-outcome shape,
    so a future edit cannot make the strong-key lanes disagree with each other.

    In `scripts/build_cloud_workflows.py`, add a FOURTH additive row to the contact branch,
    mirroring the `name` lane's construction exactly and moving no existing node's `position`:
    - `IF Linkedin Searchable`, an `_if_bool_expr_node` reading
      `$('Build Identity').item.json.lane === "linkedin"` — the stamped field, never a
      re-derived predicate.
    - `HubSpot Linkedin Search`, an `_hs_http_search_node` over contacts. Its filter property
      is `lv_linkedin_url`. That name is not a guess: the committed live-fetched portal
      snapshot `config/hubspot_migration/baseline/portal-schema-contacts-54-03-contacts-check.json`
      (read-only GET, 2026-08-27) lists `lv_linkedin_url` on the contacts object and lists NO
      bare `linkedin_url` contact property at all. This settles 61-RESEARCH.md's flagged
      MEDIUM-risk unknown without a live call. Task 2 decides the filter's operator and shape.
      Use `ENRICH_CONTACT_FETCH_BY_ID_PROPERTIES_CSV` for `properties_csv` — it is the list that
      already includes `lv_linkedin_url`, which Task 2's re-verification needs on every hit.
    - `Adapt Linkedin Search`, a Code node following `ENRICH_ADAPT_SEARCH`'s idiom verbatim:
      filter `Build Identity` to `lane === "linkedin"` BEFORE index-aligning against its own
      search node, stamp a `match` verdict on every row including a failed lookup.
    Rewire `conns["IF Has Email"]`'s false edge to `IF Linkedin Searchable`, and that node's
    false edge to `IF Name Searchable`, leaving every true edge and the fetch-by-id row
    untouched.

    Also decide, in a comment beside the search node, what happens to HubSpot's own native
    `hs_linkedin_url` (present in the same snapshot, `hubspotDefined: true`). Either add it as a
    second filter group or state in one line why it is deliberately excluded. Silence is the
    wrong answer: a contact whose LinkedIn lives only under the native property would be missed
    by the search and then created as a duplicate.

    Never hand-edit `n8n/wf_enrichment_cloud.json` — it is generated.

    Write `tests/n8n/linkedinLaneFlow.test.mjs`. The decisive assertion is a MIXED batch: an
    email row, a linkedin-only row and a name-only row in ONE request, asserting exactly one
    response item per `row_id` and that the linkedin row's `match.tier` is not `unknown`. This
    is the test that fails if the row dead-ends in the "could not look" bucket — the failure
    this phase exists to stop, in the shape it would take.
  </action>
  <verify>
    <automated>node --test tests/n8n/linkedinLaneFlow.test.mjs tests/n8n/matchProposal.test.mjs tests/n8n/bareEventChainFlow.test.mjs tests/n8n/companyNameFallbackFlow.test.mjs</automated>
  </verify>
  <done>A linkedin-only row reaches a HubSpot search node and comes back with a tier other than `unknown`; the mixed-batch test shows one item per row_id; every pre-existing `matchProposal` assertion still passes unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the search survive stored-value variance, and fix the oracle's dead filter</name>
  <files>scripts/build_cloud_workflows.py, src/identity.py, tests/n8n/linkedinLaneFlow.test.mjs, tests/test_identity.py</files>
  <read_first>
    `n8n/code/resolveIdentity.js` lines 16-41 — `canonicalizeLinkedin`, and `src/identity.py`
    lines 19-31 — `canonicalize_linkedin`, its Python mirror. Both already exist; neither is
    reached by any live search today.
    `scripts/build_cloud_workflows.py` lines 275-292 — `MERGE_CONTACTS` writes
    `candidate.lv_linkedin_url = row.linkedin_url` UNMODIFIED, and lines 1290-1310 — the
    enrichment lane writes `winners.linkedin_url` provider-shaped. Stored values are therefore
    raw and heterogeneous.
    `n8n/code/matchProposal.js` lines 62-113 — `mediumCandidates` and its header comment on why
    a hit surviving a server-side filter is not yet a verified match (the BUG 22b lesson).
  </read_first>
  <behavior>
    - A stored `https://www.linkedin.com/in/robert-cavallucci-14698741/` matches an input of
      `https://www.linkedin.com/in/robert-cavallucci-14698741` (no trailing slash).
    - A stored value differing only in scheme case or host case matches.
    - A stored value with a `?trk=...` query string matches an input without one.
    - A DIFFERENT profile under the same host does NOT match — the widening must not turn a
      strong key into a fuzzy one. A false positive here writes to the wrong person.
    - `src/identity.py::resolve_identity` on a linkedin-only row issues its search against
      `lv_linkedin_url`, not against a property name that does not exist on this portal.
  </behavior>
  <action>
    This closes the second, deeper form of the same failure: getting the property NAME right
    still returns zero hits forever if the stored VALUE shape and the searched value shape
    disagree. `canonicalizeLinkedin` output would not equal a raw stored value.

    In `scripts/build_cloud_workflows.py`:
    - Give `HubSpot Linkedin Search` a filter shape that survives stored variance — two
      filter groups (HubSpot ORs across groups), one on the canonicalized form and one on the
      operator's raw input, or a `CONTAINS_TOKEN` on the profile slug. Choose one and record
      the reasoning in a comment beside the node. Use only operators from HubSpot CRM v3's
      closed string vocabulary — the same constraint the `name` lane's own comment records at
      line 4838; a bare substring operator is a guaranteed 400 that only surfaces live.
    - In `Adapt Linkedin Search`, re-verify EVERY hit by value before reporting it as a match:
      a hit counts only when the canonicalized form of its `lv_linkedin_url` equals the
      canonicalized form of the row's own key. This is `mediumCandidates`' discipline applied
      to a strong key — the server-side filter is a narrowing, never a verdict. A hit that
      fails re-verification is dropped, and a row left with zero verified hits reports tier
      `none`, not `unknown`: the search ran.
    - In `MERGE_CONTACTS`, canonicalize before writing `lv_linkedin_url`, so stored values
      converge from here forward and this widening can eventually be narrowed. Note in a
      comment that existing stored values are unconverted, which is exactly why the search
      still carries the wider filter.

    In `src/identity.py`, change the linkedin search filter's `propertyName` from
    `linkedin_url` to `lv_linkedin_url`. This is a confirmed latent defect, not a
    precaution: the committed live portal snapshot
    `config/hubspot_migration/baseline/portal-schema-contacts-54-03-contacts-check.json`
    contains no bare `linkedin_url` contact property, so the oracle's search has always
    returned zero hits and its linkedin branch has never once been reachable against the real
    portal. Cite that snapshot path in the code comment. This keeps the Python oracle and the
    n8n lane telling the same story, which is the Phase 46 parity discipline applied to a
    shared identity predicate.

    Extend `tests/n8n/linkedinLaneFlow.test.mjs` with the stored-variance fixtures from
    `<behavior>` — including the negative case, because a test that only proves the widening
    finds more is not evidence it did not become fuzzy. Extend `tests/test_identity.py` for the
    oracle's corrected filter.

    Expect fallout in the Python tests when the oracle's `propertyName` changes: any test that
    injects an `hs_search` double keyed on the old filter name will stop matching and its row
    will silently take a different branch. Grep the Python suite for that filter name before
    running it, and update each double to the corrected name — this is a mechanical rename of
    a mock's expectation, not a weakening of an assertion, and it should be reported as such.
  </action>
  <verify>
    <automated>node --test tests/n8n/linkedinLaneFlow.test.mjs tests/n8n/mergeContacts.test.mjs && .venv/bin/python -m pytest tests/test_identity.py tests/test_e2e_ingest.py -q</automated>
  </verify>
  <done>A trailing-slash-only difference between stored and input value still matches; a different profile under the same host does not; the oracle searches the property that exists.</done>
</task>

<task type="auto">
  <name>Task 3: Un-freeze MATCH_LOOKUP_KEYS, restate the disclosure boundary, ship the release</name>
  <files>operator-claude-plugin/scripts/enrichment.py, operator-claude-plugin/tests/test_rows_envelope_contract.py, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    `operator-claude-plugin/scripts/enrichment.py` lines 60-100 — `MATCH_LOOKUP_KEYS` and the
    comment declaring it frozen and naming what it keeps on the operator's machine — and lines
    236-300, `build_envelope`'s rows branch, which restates the same allowlist in its docstring.
    `operator-claude-plugin/tests/test_rows_envelope_contract.py` — what currently pins the
    envelope's projection.
  </read_first>
  <action>
    Add `linkedin_url` to `MATCH_LOOKUP_KEYS`. Per D-61-05 (CORRECTED), this tuple is what
    strips the key client-side before the request ever leaves the operator's machine — the
    backend lane Tasks 1-2 built is unreachable until this changes, and the two halves only
    deliver together.

    This tuple is a documented disclosure boundary, so REWRITE its rationale rather than
    deleting it. The comment must still state what crosses and what does not — `phone` and
    `jobtitle` still do not — and must now say why `linkedin_url` does: it is a strong match key
    the backend's `Build Identity` reads into `identity_keys` and the new `HubSpot Linkedin
    Search` node filters on, so withholding it means the operator's own supplied key cannot be
    used to find their own contact. Update `build_envelope`'s docstring, which restates the same
    list, in the same edit — two statements of one rule that disagree is how this codebase has
    repeatedly shipped a silent contract drift.

    Extend `test_rows_envelope_contract.py`: a row carrying `linkedin_url` projects it into the
    envelope; a row carrying `phone` and `jobtitle` still does not. Both assertions, together —
    the second is what proves this widened by exactly one key rather than opening the tuple.

    Bump `operator-claude-plugin/.claude-plugin/plugin.json` from 0.28.6 to 0.29.0 (a capability
    the front end did not have, not a patch) and add the matching `CHANGELOG.md` entry, in the
    SAME commit as the `enrichment.py` change, per this repo's release hygiene rule.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_rows_envelope_contract.py operator-claude-plugin/tests/test_enrichment_envelope.py -q</automated>
  </verify>
  <done>`linkedin_url` reaches the backend on a rows envelope; `phone` and `jobtitle` still do not; the plugin version and CHANGELOG moved in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine→n8n Cloud | `MATCH_LOOKUP_KEYS` decides which row fields leave the operator's machine |
| n8n→HubSpot search | A search filter value crosses into a live CRM query |
| HubSpot response→match verdict | An unverified server-side hit could be reported as a confident person match |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-04 | Information disclosure | `MATCH_LOOKUP_KEYS` (enrichment.py:71) | medium | mitigate | Widened by exactly one strong match key; `phone`/`jobtitle` exclusion re-pinned by test in the same task; the boundary comment restates the current rule rather than being deleted |
| T-61-05 | Spoofing | `Adapt Linkedin Search` | high | mitigate | Every hit re-verified by canonicalized value before it is reported as a match — a fuzzy server-side hit is a narrowing, never a verdict (D-61-03: a wrongly matched person is worse than an unmatched one) |
| T-61-06 | Tampering | `HubSpot Linkedin Search` filter | high | mitigate | Filters on `lv_linkedin_url`, confirmed present in the committed live portal snapshot; a name that does not exist would return zero hits silently forever |
| T-61-07 | Repudiation | mixed-lane routing | medium | mitigate | Mixed-batch flow test asserts one response item per `row_id`, so a row routed to one lane and filtered into another fails the suite instead of vanishing |
| T-61-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — nothing to audit |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` all green (glob form only — the directory form is broken
  on node 24).
- `.venv/bin/python -m pytest -q` green (baseline 3365 / 154).
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green (baseline 1725 / 5).
- No assertion anywhere was weakened, deleted or reworded to make something pass —
  `git diff HEAD | grep -cE '^-[[:space:]]*assert '` over the test files touched, reported in
  the summary with its number.
- `n8n/wf_enrichment_cloud.json` regenerated through `scripts/build_cloud_workflows.py`, never
  hand-edited.
- Zero live n8n, HubSpot, Anthropic or provider calls.
</verification>

<success_criteria>
The exact walk-failure row — a LinkedIn URL and nothing else — routes to a `linkedin` lane,
reaches a HubSpot search that filters on a property that exists, and returns a verified verdict
that is not "could not look".
</success_criteria>

<output>
Create `.planning/phases/61-autonomous-batch-runs/61-02-SUMMARY.md` when done.
</output>
