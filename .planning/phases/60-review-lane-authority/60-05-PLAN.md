---
phase: 60-review-lane-authority
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-60-1, G-60-2]
files_modified:
  - operator-claude-plugin/scripts/review_decision.py
  - operator-claude-plugin/tests/test_review_decision.py
  - scripts/verify_live_write_safety.py
  - tests/test_verify_live_write_safety.py
  - operator-claude-plugin/skills/review-triage/SKILL.md
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
autonomous: true
# Decision ids, not REQ ids — this phase's coverage contract per ROADMAP.md ("each plan's
# `requirements` frontmatter carries the D-60-NN ids it implements"). The gap ids live in
# `gap_ids` above. G-60-1 repairs the verification half of the grant-gated submit flow
# D-60-04 built; G-60-2 is about pinning the allowlist of D-60-06's one-window-per-sitting.
requirements: [D-60-04, D-60-06]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-60-1: a review approve whose business fields all landed reports `verified`, even though the backend minted `lv_enrichment_reviewed_at`, the provenance blob's embedded timestamp and the reviewed-by label AFTER the preview was captured."
    - "G-60-1: a business field that did not land, or that the backend changed between preview and submit, still reports `failed` and names the field — the fix compares MORE than before, never less, and an unknown future submit-minted key fails closed rather than passing silently."
    - "G-60-2: `verify_live_write_safety.py` can express a correctly-scoped single-workflow armed window — the named workflow armed with exactly the granted allowlist, EVERY other workflow held to the full disarmed rule — so an operator can pin allowlist MEMBERSHIP (not a count) while a per-lane window is open."
    - "G-60-2: naming the armed workflow narrows no coverage — every deployed workflow is still fetched and judged, the unnamed ones under the stricter rule, and a name matching zero scanned workflows is a hard FAIL. 27-04 D-07 stands: an operator still cannot blind the scan."
    - "Zero n8n changes, zero deploys, zero arming, zero provider credits and zero HubSpot writes are required to close either gap."
  artifacts:
    - operator-claude-plugin/scripts/review_decision.py
    - scripts/verify_live_write_safety.py
    - operator-claude-plugin/skills/review-triage/SKILL.md
    - operator-claude-plugin/.claude-plugin/plugin.json
  key_links:
    - "`review_decision.PREVIEW_UNPINNABLE_KEYS` -> `verify_decision`'s leg 1 skip list AND leg 2 expected-value source — one closed set, read in both places, never two lists."
    - "`response[\"would_write\"]` (submit-time) -> leg 2's expected value for the unpinnable keys -> compared against `response[\"verified_properties\"]` (the post-PATCH refetch) — the refetch stays the only authority on whether anything landed."
    - "`verify(..., armed_workflow=...)` -> per-report branch selecting armed-vs-disarmed rules -> the zero-match reason — the three parts that together make the scoped mode un-blindable."
    - "`.claude-plugin/plugin.json` version -> the marketplace Update button — an unbumped string means an operator keeps running the code that reports a false `failed` on every approve."
---

<objective>
Close the two defects the 2026-09-03 supervised operator walk found: `verify_decision`
returning `failed` on a fully successful approve (G-60-1), and `verify_live_write_safety.py`
having no way to express a correctly-scoped single-workflow armed window (G-60-2).

Purpose: G-60-1 makes the phase's own documented happy path produce an unsoftenable false
alarm on EVERY successful review approve — `review-triage/SKILL.md` step 8 instructs the
agent to tell the operator a `failed` means the change is not confirmed and to never soften
it. G-60-2 leaves the operator with no independent tool to pin allowlist membership while a
review window is open, which is exactly the 49-W2 lesson ("a count check is not a membership
check").

Output: a two-leg `verify_decision` that verifies strictly more than today, a scoped armed
expectation that cannot blind the scan, the skill text both changes imply, and a released
plugin version so an operator actually receives the fix.

**No n8n change is needed and none is planned. Do not deploy, do not arm, do not write to
HubSpot, do not spend a provider credit.** This was verified, not assumed: both diverging
values are minted per-request by the backend from its own clock —
`n8n/code/reviewApply.js:124` and `n8n/code/reviewDecision.js:271` stamp
`lv_enrichment_reviewed_at`, and `n8n/code/reviewDecision.js:150-156` embeds the same clock
inside the provenance blob. A preview and a submit are two separate requests, so two
different timestamps are the backend behaving CORRECTLY. The only place the comparison can
be fixed is the client. `n8n/wf_*.json` is untouched by this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-review-lane-authority/60-UAT.md

@operator-claude-plugin/scripts/review_decision.py
@operator-claude-plugin/tests/test_review_decision.py
@scripts/verify_live_write_safety.py
@tests/test_verify_live_write_safety.py
@operator-claude-plugin/skills/review-triage/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: verify_decision proves intent stability AND landing, on two legs (G-60-1)</name>
  <files>operator-claude-plugin/scripts/review_decision.py, operator-claude-plugin/tests/test_review_decision.py</files>

  <read_first>
`operator-claude-plugin/scripts/review_decision.py` lines 341-444 (`_verdict` and
`verify_decision` in full — the docstring at 346-366 is the contract being amended and the
comparison at 428-429 is the single line being replaced), lines 218-230
(`preview_decision`, whose signature has no reviewed-by parameter), lines 158-169
(`_request_body`, where a `None` reviewed-by becomes `DEFAULT_REVIEWED_BY`), and the
`D-60-04 AMENDMENT` block at lines 55-69 — copy THAT paragraph's shape for the new dated
amendment.

`operator-claude-plugin/tests/test_review_decision.py` lines 26-47 (`APPROVE_WRITE` and
`applied_response` — note both the preview map and the refetch are the SAME dict, which is
precisely why this defect was invisible to the offline suite) and lines 391-521 (the
existing read-back tests, all of which must stay green unmodified).

`.planning/phases/60-review-lane-authority/60-UAT.md` § Gaps, `gap_id: G-60-1` — the
diagnosed root cause and the two observed diverging values. Do not re-derive it.
  </read_first>

  <behavior>
Written as tests FIRST, and every one of them uses DIFFERENT preview-time and submit-time
values — the reason today's suite cannot see this defect is that it reuses one map for both.
Tests 1 and 5 below MUST be observed RED against the unmodified `review_decision.py` before
any source edit is made; if either passes before the fix, the test is not exercising the
divergence and must be corrected first.

  - Test 1 (RED first): an approve whose preview carried timestamp T1, a provenance blob
    embedding T1, and the default reviewed-by label, and whose response carried T2, a blob
    embedding T2, and the operator's real name — with `verified_properties` equal to the
    response's own `would_write` — reports `verified` and an empty `mismatched`.
  - Test 2: the same shape, but `verified_properties` holds a drifted `lv_produces_content`
    -> `failed`, and the message names `lv_produces_content`. The business-field check is
    not weakened by test 1 passing.
  - Test 3, two cases: the response's `would_write` holds a DIFFERENT
    `lv_produces_content` from the preview's -> `failed`; and the response's `would_write`
    holds a pinnable key the preview's map did not contain AT ALL -> `failed`, naming that
    key. The operator approved a patch; a backend that submits a different one — whether by
    changing a value or by adding a field nobody saw — is caught either way. The second case
    is the one leg 1's key set has to be built deliberately to reach.
  - Test 4: a key that is NOT in the unpinnable set differs between the preview map and the
    response's `would_write` -> `failed`. This is the fail-closed direction: a submit-minted
    key nobody has enumerated yet produces a loud failure, never a silent pass.
  - Test 5 (RED first): the contacts provenance property behaves exactly as the companies
    one — a contacts approve whose blob timestamp advanced between preview and submit
    reports `verified`.
  - Every existing test in the file stays green with no edit. They all build responses whose
    `would_write` equals the intended map, so leg 1 passes trivially for each; the
    provenance-missing test at line 416 still fails on leg 2.
  </behavior>

  <action>
Add ONE module-level closed set beside the existing `UNDOING_DECISIONS` constant, named
`PREVIEW_UNPINNABLE_KEYS`, holding exactly four property names: `lv_enrichment_reviewed_at`,
`lv_enrichment_provenance`, `lv_contact_enrichment_provenance` and
`lv_enrichment_reviewed_by`.

Name the concept accurately in its comment: these are not all clock-minted. Two of them
(`lv_enrichment_reviewed_at`, and the `verified_at` value embedded inside whichever
provenance blob applies) come from the backend's own clock at request time, so a preview and
a submit necessarily disagree; the companies-vs-contacts split is why BOTH provenance
property names are listed. The fourth is different in kind and must be documented as such:
`preview_decision`'s signature accepts no reviewed-by argument at all and passes `None`, so
a preview's map always carries `DEFAULT_REVIEWED_BY` while a submit carries the operator's
own label. What unites all four is that the preview-time capture cannot pin them, not that
the backend invented them. Do NOT close this by widening `preview_decision`'s signature —
that changes a public API and the skill's call for no added verification strength.

Replace the single comparison inside `verify_decision` with two legs, keeping the function
signature and the four returned keys exactly as they are:

  Leg 1 — intent stability. Read the backend's own submit-time patch from
  `response.get("would_write")`, coercing a non-dict to an empty mapping. The key set this
  leg iterates is the UNION of the approved map's keys and the backend's patch's keys, minus
  `PREVIEW_UNPINNABLE_KEYS` — not the approved map's keys alone. The union is load-bearing
  and is the whole reason this leg exists in this shape: iterating only the approved map
  cannot see a key the backend added at submit time that the operator never previewed, and
  leg 2 cannot see it either because leg 2's expected map is also built from the approved
  one. Without the union, a `would_write`-only key is invisible to BOTH legs. For every key
  in that union, the backend's value must match the approved one under the same stringwise
  rule the landing check already uses — a key missing from either side compares as absent
  and therefore differs, which is the intended outcome. Any difference is `failed`: report
  those keys in `mismatched` and say plainly that the write the backend performed is not the
  one that was previewed and approved, naming the keys, and that nothing here confirms the
  record.

  Leg 2 — landing, against the independent re-read, unchanged in authority. Build the
  expected map from `intended`, substituting the backend's submit-time value for each
  unpinnable key that the response's patch actually carries. Compare that expected map
  against `verified_properties` exactly as the current code does, and keep the existing
  failure message wording and the existing success message wording byte-for-byte.

  Leg 2 keeps `intended` as its key set and does NOT take leg 1's union — a deliberate
  decision, recorded here so it is not "fixed" on initiative. Two reasons. Leg 1 runs first
  and returns on any difference, so by the time leg 2 executes, every PINNABLE key of the
  backend's patch is already present in `intended` with an equal value: a union would add
  nothing for those keys. And for the unpinnable ones, widening leg 2 to a key the operator
  never approved and never previewed would silently grow what the refetch comparison
  asserts, which is exactly the drift this plan is guarding against in the other direction.
  Leg 2's contract stays "every key the operator approved read back" — same key count as
  today, same message wording, only two expected values re-sourced.

The consequence to state in the amendment, because it is the whole justification: leg 2 still
checks the SAME key set it checks today — nothing is verified less. All four unpinnable keys
are still required to read back from the post-PATCH refetch; only the source of their
expected value moves from the preview's guess to the backend's own report of what it wrote.
The refetch remains the sole authority on whether anything landed, so this is not a retreat
into status-code trust.

Append a dated amendment paragraph to `verify_decision`'s docstring in the file's existing
house style — a header naming the gap, the date and the source, then what changed and what
still holds. Follow the `D-60-04 AMENDMENT` block's shape. Amend; do not rewrite the
existing prose that defines `intended` as the approved map, because after this change it is
still true.

Write the five tests described in the behavior block into
`operator-claude-plugin/tests/test_review_decision.py`, under the existing read-back section.
Add module-level preview/submit fixture pairs beside `APPROVE_WRITE` rather than mutating
`APPROVE_WRITE` itself — every existing test depends on it. Fold tests 3 and 4 into one
parametrized test if that reads more cleanly; keep them separate otherwise.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_review_decision.py operator-claude-plugin/tests/test_review_outcome_parity.py -q</automated>
  </verify>

  <done>
The command above passes with every pre-existing test unmodified. Before the source edit,
tests 1 and 5 were observed FAILING against the unmodified module and that observation is
recorded in the SUMMARY (a green run of the new tests against pre-fix code means the test is
wrong, not the code). Failing direction: any non-zero exit, or a `verified` verdict on a
drifted business field.
  </done>

  <reversibility rating="reversible">A pure-function comparison change behind an unchanged signature; revert is one commit.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a scoped armed expectation that cannot blind the scan (G-60-2)</name>
  <files>scripts/verify_live_write_safety.py, tests/test_verify_live_write_safety.py</files>

  <read_first>
`scripts/verify_live_write_safety.py` in full — in particular the module docstring's Phase
23 Plan 07 paragraph (lines 13-18), which records 27-04 D-07's reasoning and is the text to
AMEND rather than replace; `verify()` (lines 191-304), whose armed branch is the one gaining
a per-report choice; `_print_report` (lines 307-343); and `main`'s argument validation
(lines 346-372), whose `--expect-armed` guard is the pattern the new argument's guard
mirrors.

`tests/test_verify_live_write_safety.py` lines 1-100 — the hermetic fixture helpers `_node`,
`_wf` and `_enrichment`, and the autouse fixture that makes any leaked HTTP call an
assertion failure. Reuse them; build no new fixture style.

`.planning/phases/60-review-lane-authority/60-UAT.md` § Gaps, `gap_id: G-60-2` — including
its explicit statement that simply adding a workflow-selection flag is NOT an acceptable fix
on its own.
  </read_first>

  <behavior>
  - A four-workflow fixture list where only the named workflow reads the review flag enabled
    with the granted id in its allowlist, and the other three read fully disarmed, PASSES
    under the scoped armed expectation. This is the shape the live 2026-09-03 walk was
    actually in, and it is RED today.
  - The same list, but with a second workflow ALSO reading a write-enabling flag enabled,
    FAILS. Scoping the assertion did not stop the scan from judging the others.
  - The same list, but with an unnamed workflow carrying leftover allowlist residue while
    every boolean reads disabled, FAILS. The unnamed workflows are held to the DISARMED
    rule, which is stricter than the global armed rule they are held to today.
  - Naming a workflow that no scanned workflow matches FAILS, with a reason naming the value
    given and listing the workflow names that were scanned. A typo cannot silently produce a
    pass.
  - The named workflow armed but with an allowlist that is not exactly the expected value
    FAILS, and an empty allowlist on the named workflow FAILS with the existing
    grants-nothing reason.
  - Omitting the new argument reproduces today's verdict on the same fixture list, verdict
    and reasons alike — every existing test in the file stays green unmodified.
  - The CLI refuses the new argument outside the armed expectation, the same way it already
    refuses `--expect-armed` there.
  - The drain-authority check keeps its own meaning and its own report line under the scoped
    mode, unchanged.
  </behavior>

  <action>
Give `verify()` a new keyword parameter, defaulting to `None`, that names the ONE workflow
expected to be armed. When it is `None`, every code path behaves exactly as it does today —
this is what keeps the completed Phase 22 runbook's command lines and every existing caller
meaning what they meant.

When it is given, judge each discovered node report by which workflow it came from: a report
whose workflow name matches exactly is judged by the armed rules already written (the named
flags enabled, every other write-enabling boolean disabled, the live allowlist exactly equal
to the expected value); every other report is judged by the DISARMED rules already written
(every boolean it declares disabled, every allowlist constant it declares empty). Route to
the existing rule bodies; do not write a third copy of either rule.

After the report loop, add the zero-match guard: when a workflow was named and no scanned
report carries that name, append a reason that quotes the value given and lists the workflow
names that WERE scanned. Without this, a mistyped name would leave every report judged
disarmed and produce a pass on a live armed instance — the exact false-success this whole
script exists to prevent.

Carry the new value into the returned dict and into `_print_report`, adding a line that says
which workflow is expected armed and that every other workflow is asserted fully disarmed.

Add the matching command-line argument, and reject it with `parser.error` when the
expectation is not armed, mirroring the existing guard for `--expect-armed`.

Amend the module docstring's D-07 paragraph with a dated addendum — do not delete or rewrite
the existing reasoning, which still stands. The addendum must make the three-part argument
for why this argument is not the workflow-selection flag D-07 refused: coverage is unchanged
(every deployed workflow is still fetched and still judged), the unnamed workflows are held
to a STRICTER rule than the global armed expectation holds them to, and a name matching
nothing is a hard failure rather than a quiet pass. Also update the Usage block with an
example of the scoped form.

Write the tests described in the behavior block into
`tests/test_verify_live_write_safety.py` using the existing `_node`/`_wf` helpers, in a new
section headed for this gap. Build the four-workflow fixture from `_wf` calls directly
rather than extending `_enrichment`, whose one-workflow shape is load-bearing for the tests
that already use it.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_verify_live_write_safety.py -q</automated>
  </verify>

  <done>
The command above passes with every pre-existing test in the file unmodified, and the new
scoped-armed pass case was observed FAILING against the unmodified script before the edit.
Failing direction: any non-zero exit; in particular a PASS verdict on the mistyped-name case
or on the second-workflow-also-armed case means the scan can be blinded and the task is not
done.
  </done>

  <reversibility rating="reversible">An additive optional parameter and CLI argument; omitting it restores today's behaviour exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 3: say what changed in the skill, and release it</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json</files>

  <read_first>
`operator-claude-plugin/skills/review-triage/SKILL.md` step 4 (lines 87-146, especially the
`armed_review_window` block at 133-141 and the sentence at 143-146 about per-record
checking) and step 8 (lines 236-268), plus the closing section at lines 270-275 — the skill's
own standing promise that it never asks the operator to run a command, which constrains how
G-60-2's tooling may be mentioned here.

`operator-claude-plugin/scripts/n8n_arming.py` lines 444-457 — the `arm_for_dispatch` return
whose `observed` key holds the post-arm re-read of the live flags and both allowlist
constants, reached from the skill as `window.arm_result["observed"]`. This already exists;
this task only tells the agent to use it.

`operator-claude-plugin/CHANGELOG.md` lines 1-20 (the empty `## [Unreleased]` heading above
`## [0.37.0]`) and its release checklist at the end of the file.
  </read_first>

  <action>
In step 8, keep the `verify_decision(preview["would_write"], response)` call line exactly as
written — the fix is inside the function, and the prescribed call is now correct. Add two or
three lines under it explaining what the verdict now means: the approved business fields are
checked twice, once against what the backend reported writing and once against the
independent re-read, while a handful of properties the preview cannot pin (the review
timestamp, the provenance blob's embedded timestamp, and the reviewed-by label) are checked
against the backend's own report of what it wrote rather than against the preview. Say that
this is why a successful approve now reports verified. Leave the `failed` bullet and its
instruction against softening a failure completely intact — that instruction is correct and
is now reachable only by a real problem.

In step 4, after the `armed_review_window` block and before the existing sentence about
per-record checking, add a short paragraph instructing the agent to read the LITERAL
allowlist back from `window.arm_result["observed"]` and state it to the operator, id by id,
before the first decision of the sitting. Say why in one clause: the number of ids is not
evidence of WHICH ids, and a window whose allowlist does not hold the record about to be
decided will refuse it. Add one sentence naming the independent second-process cross-check
available to an administrator with a repository checkout — the scoped form of
`scripts/verify_live_write_safety.py` from Task 2 — and say plainly that this skill does not
ask the operator to run it. Quote that command in full, including the new
`--armed-workflow` argument and the review workflow's own name, so a reader can use it
without reconstructing the invocation from prose.

That second sentence is the deliberate limit of how far G-60-2 is wired into the skill, and
the SUMMARY must record the reason: the plugin does not ship
`scripts/verify_live_write_safety.py` at all, and the skill's own closing section forbids
asking the operator to run a command. Relaying the arm's own post-arm read-back is the
in-conversation closure of the 49-W2 membership lesson; the repository script is the
admin-side cross-check and stays admin-side. Do not add a step that shells out.

Cut the CHANGELOG: leave `## [Unreleased]` on top and empty, and add a
`## [0.38.0] - 2026-09-03` section beneath it with a `### Fixed` subsection carrying both
gaps — the false `failed` on every successful review approve, and the scoped armed
expectation. Name the live walk as what found them. Then set the version string in
`.claude-plugin/plugin.json` to `0.38.0`, in this same commit, per the release checklist's
first two steps.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -q && .venv/bin/python -c "import json,sys; v=json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version']; sys.exit(0 if v=='0.38.0' else 1)" && grep -q '0.38.0' operator-claude-plugin/CHANGELOG.md && grep -q 'arm_result' operator-claude-plugin/skills/review-triage/SKILL.md && grep -q 'armed-workflow' operator-claude-plugin/skills/review-triage/SKILL.md && echo TASK3_OK</automated>
  </verify>

  <done>
The command above prints `TASK3_OK`. Failing direction: any non-zero exit, or the absence of
`TASK3_OK` — an unbumped version string, a missing CHANGELOG section, or a step 4 that never
mentions the arm's own read-back all stop it. The SUMMARY records why the repository
verifier was deliberately NOT wired into the skill as a step the operator runs.
  </done>

  <reversibility rating="reversible">Documentation and a version string.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n response -> operator's belief about HubSpot | the response body crosses into a claim about a live CRM record |
| live n8n instance -> the safety verdict an operator acts on | the deployed artifact crosses into a pass/fail an operator trusts before deciding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-18 | Spoofing | `verify_decision` leg 2 | high | mitigate | The post-PATCH refetch stays the sole authority on landing. Leg 1 adds a check and removes none: leg 2 still requires all four unpinnable keys to read back, sourcing only their expected VALUE from the response. A response cannot verify itself. |
| T-60-19 | Tampering | `verify_decision` leg 1 | medium | mitigate | A backend that submits a patch differing from the previewed one on any pinnable key is `failed` and names the key. A submit-minted key nobody has enumerated yet also fails, closed — the exclusion set being incomplete errs toward a loud failure, never a silent pass. |
| T-60-20 | Repudiation | `verify_live_write_safety.py` scoped mode | high | mitigate | Coverage stays discovered, never listed: every deployed workflow is still fetched and judged, the unnamed ones under the STRICTER disarmed rule, and a name matching zero scanned workflows is a hard FAIL. An operator cannot narrow the scan by naming a workflow, which is what 27-04 D-07 was protecting. |
| T-60-21 | Information disclosure | new report line and new refusal reasons | low | accept | The new output prints only workflow names, constant names and parsed literal values — the same classes the script already prints. No credential is constructed or printed. |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. No dependency is added to `requirements.txt` or any manifest, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
Both suites this plan touches, plus the two neighbouring suites most likely to catch
collateral damage from a change to `review_decision.py`:

```
.venv/bin/python -m pytest operator-claude-plugin/tests/test_review_decision.py \
  operator-claude-plugin/tests/test_review_outcome_parity.py \
  operator-claude-plugin/tests/test_plugin_manifest.py \
  operator-claude-plugin/tests/test_written_records.py \
  tests/test_verify_live_write_safety.py -q
```

All green. This run took 0.41s over the first four of these files before the plan was
written, so a long run means something unexpected is happening, not that the suite is slow.

No `node --test tests/n8n/*.test.mjs` run is required: no file under `n8n/` and no line of
`scripts/build_cloud_workflows.py` is modified by this plan. If the executor finds itself
about to edit either, the plan has been misread — stop and re-read the objective.

Nothing in this plan is verified by a live call. There is no armed window, no deploy, no
HubSpot request and no provider call anywhere in it.
</verification>

<success_criteria>
- An approve whose business fields landed reports `verified` even when the review timestamp,
  the provenance blob's embedded timestamp and the reviewed-by label all differ between the
  preview and the submit.
- A business field that did not land, or that the backend changed between preview and
  submit, still reports `failed` and names the field.
- A key outside the closed unpinnable set that differs between preview and submit reports
  `failed` — the fail-closed direction.
- `verify_live_write_safety.py` passes on a fixture where exactly one workflow is armed with
  the granted allowlist and every other reads fully disarmed, and fails when a second
  workflow is armed, when residue is left on an unnamed workflow, or when the named workflow
  matches nothing scanned.
- Omitting the new argument leaves the script's verdicts identical to today's.
- `review-triage/SKILL.md` step 4 tells the agent to state the live allowlist membership from
  the arm's own read-back, and step 8 explains the two-leg verdict without softening its
  `failed` branch.
- `plugin.json` reads `0.38.0` and the CHANGELOG carries the matching section, in one commit.
- Zero n8n files changed, zero deploys, zero arming, zero HubSpot writes, zero credits.
</success_criteria>

<output>
Create `.planning/phases/60-review-lane-authority/60-05-SUMMARY.md` when done.

Record in it: the observed RED state of the new tests against pre-fix code (Tasks 1 and 2),
the reason the repository verifier was not wired into the skill as an operator-run command
(Task 3), and an explicit statement that nothing was armed, deployed, or written.
</output>
