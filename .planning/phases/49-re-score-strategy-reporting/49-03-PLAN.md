---
phase: 49-re-score-strategy-reporting
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - scripts/gen_taxonomy_js.py
  - src/taxonomy.py
  - docs/WEB-RESEARCH-SPEC.md
  - scripts/build_cloud_workflows.py
  - n8n/code/taxonomy.generated.js
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local_live.json
  - tests/fixtures/companies_jscode_frozen.json
  - tests/n8n/orgTypeDefinitionsPrompt.test.mjs
  - tests/test_taxonomy_conformance.py
autonomous: true
requirements: [RESCORE-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "n8n/code/taxonomy.generated.js exports an ORG_TYPE_DEFINITIONS object whose keys are exactly the nine org types and whose values are the non-empty definition strings from config/taxonomy.yaml."
    - "The companies research system prompt emitted by scripts/build_cloud_workflows.py builds its allowed org-type line from those definitions, so the emitted jsCode carries each org type's definition text and not only its bare key."
    - "The regenerated n8n workflow JSON is byte-reproducible from scripts/build_cloud_workflows.py — a second build produces an identical file."
    - "tests/fixtures/companies_jscode_frozen.json is re-baselined in this commit as an explicit, reviewed act, with the reason recorded in the commit message and the plan summary."
  artifacts:
    - "scripts/gen_taxonomy_js.py — render() emits ORG_TYPE_DEFINITIONS"
    - "n8n/code/taxonomy.generated.js — regenerated"
    - "scripts/build_cloud_workflows.py — researchSystemPrompt() builds allowed_org_types from definitions"
    - "n8n/wf_enrichment_cloud.json and n8n/wf_enrichment_local_live.json — rebuilt, not hand-edited"
    - tests/n8n/orgTypeDefinitionsPrompt.test.mjs
    - "tests/fixtures/companies_jscode_frozen.json — re-baselined"
  key_links:
    - "config/taxonomy.yaml definition: keys -> src.taxonomy.ORG_TYPE_DEFINITIONS -> gen_taxonomy_js.render() -> n8n/code/taxonomy.generated.js -> researchSystemPrompt()'s emitted jsCode"
    - "the new node test mirrors tests/test_taxonomy_conformance.py's Python-side definitions assertion"
  prohibitions:
    - statement: "MUST NOT hand-edit n8n/wf_enrichment_cloud.json or n8n/wf_enrichment_local_live.json. The only editable sources for the lane are scripts/build_cloud_workflows.py, n8n/code/*.js and scripts/gen_taxonomy_js.py; the JSON is rebuilt."
    - statement: "MUST NOT re-baseline tests/fixtures/companies_jscode_frozen.json as a routine step to make a test pass. Its own header binds it to an explicit, reviewed act, and this commit's message must record what changed and why."
---

<objective>
Close the folded todo: the production n8n research prompt still enumerates the nine `lv_org_type`
values without defining any of them, so the live enrichment lane can still reproduce the
statutory-origin misclassification Phase 48-07 fixed on the Python side.

Purpose: `lv_org_type` is the single largest term in the ICP rubric — it is the input the whole
re-score reads. A lane that can still misclassify a statutory sports body as a regulator will keep
feeding wrong org types into the very population this phase re-scores. Phase 48-07 fixed the root
cause once, in `config/taxonomy.yaml`, and rendered it into both Python prompts; this closes the
third consumer.

Output: definitions flowing all the way into the emitted n8n `jsCode`, the workflow JSON rebuilt
from source, the byte-frozen fixture re-baselined as an explicit reviewed act, and a node test that
asserts definition text rather than bare keys.

This plan is offline: zero deploys, zero n8n executions, zero Anthropic calls, zero HubSpot writes.
Plan 49-04 owns the one declared deploy and bounce that makes this live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-re-score-strategy-reporting/49-CONTEXT.md
@.planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md
@config/taxonomy.yaml
@src/taxonomy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-03-01: gen_taxonomy_js.render() emits ORG_TYPE_DEFINITIONS</name>
  <files>scripts/gen_taxonomy_js.py, src/taxonomy.py, n8n/code/taxonomy.generated.js, tests/test_taxonomy_conformance.py</files>
  <read_first>
    - scripts/gen_taxonomy_js.py (whole file — the generator being extended)
    - src/taxonomy.py lines 40-70 (`ORG_TYPE_DEFINITIONS`, `org_type_definitions_block()`, and the docstring stating the generator does not currently read the definition key)
    - config/taxonomy.yaml (the `definition:` key on all nine `org_types` entries)
    - n8n/code/taxonomy.generated.js (the current generated output being changed)
    - tests/test_taxonomy_conformance.py (the currency test that catches a stale generated file, and the Python-side definitions assertion the new node test mirrors)
  </read_first>
  <behavior>
    - `render()` output contains an `ORG_TYPE_DEFINITIONS` const whose parsed object has exactly the nine org-type keys.
    - Every value in that object is a non-empty string equal to the corresponding `src.taxonomy.ORG_TYPE_DEFINITIONS` entry.
    - `ORG_TYPE_DEFINITIONS` is added to the module's exports list.
    - The existing exports (`TAXONOMY_VERSION`, `ORG_TYPES`, `ORG_TYPE_SYNONYMS`, `EVIDENCE_GATED_ORG_TYPES`, `DEFAULT_ORG_TYPE`, `CONTENT_TYPES`, `CONTENT_TYPE_SYNONYMS`, `CONTENT_TYPE_IMPLIES`, `DEFAULT_CONTENT_TYPE`) are all still present and unchanged.
  </behavior>
  <action>
Extend `scripts/gen_taxonomy_js.py`.

Import `ORG_TYPE_DEFINITIONS` from `src.taxonomy` alongside the existing imports. In `render()`,
emit a new `const ORG_TYPE_DEFINITIONS = ...;` line serialised with `json.dumps(..., indent=2)` —
the file's own standing rule is that `json.dumps` handles all JS-literal escaping and there is no
second escape path. Place it immediately after the `ORG_TYPES` const so a reader sees the key list
and its definitions together, and add `ORG_TYPE_DEFINITIONS,` to the `module.exports` block.

Update `src/taxonomy.py`'s `org_type_definitions_block()` docstring: its current text states that
`gen_taxonomy_js.render()` does not read the `definition` key and that the block never reaches
`n8n/code/taxonomy.generated.js`. That statement becomes false with this change; correct it to say
the generator now emits the definitions object so the n8n lane carries the same semantic content,
while `org_type_definitions_block()` remains the Python-prompt renderer.

Regenerate the checked-in file by running the generator, so the currency test in
`tests/test_taxonomy_conformance.py` stays green.

Extend `tests/test_taxonomy_conformance.py` with an assertion that the generated JS text contains an
`ORG_TYPE_DEFINITIONS` declaration and that every definition string from `src.taxonomy` appears in
the generated text — mirroring the existing Python-side assertion that every org type has a
definition and both prompts render them.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/gen_taxonomy_js.py && .venv/bin/python -m pytest tests/test_taxonomy_conformance.py -x -q && git diff --exit-code -- n8n/code/taxonomy.generated.js</automated>
  </verify>
  <acceptance_criteria>
    - Running `.venv/bin/python scripts/gen_taxonomy_js.py` twice leaves `n8n/code/taxonomy.generated.js` unchanged on the second run (idempotent regeneration).
    - `node -e "const t=require('./n8n/code/taxonomy.generated.js'); const d=t.ORG_TYPE_DEFINITIONS; process.exit(Object.keys(d).length===9 && Object.values(d).every(v=>typeof v==='string'&&v.trim()) ? 0 : 1)"` exits 0.
    - The nine keys of the emitted object equal `t.ORG_TYPES` as a set.
    - `tests/test_taxonomy_conformance.py` passes and now contains an assertion covering the generated definitions.
    - Every previously exported name is still exported: a node require of the generated module resolves all nine prior export names plus the new one.
  </acceptance_criteria>
  <done>The n8n vocabulary module carries the same semantic definitions the Python prompts already render, generated from the one source in `config/taxonomy.yaml`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-03-02: researchSystemPrompt() builds the allowed org-type line from definitions</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local_live.json, docs/WEB-RESEARCH-SPEC.md</files>
  <read_first>
    - scripts/build_cloud_workflows.py lines 2020-2080 (the companies `research_system_prompt_fn_js` and its allowed org-type line)
    - scripts/build_cloud_workflows.py lines 2190-2240 (the contacts-target twin, to determine whether it enumerates org types at all)
    - n8n/code/taxonomy.generated.js (the module the emitted JS reads `ORG_TYPES` from)
    - src/web_research.py lines 30-100 (`RESEARCH_SYSTEM` — the Python prompt shape whose definitions rendering this mirrors)
    - tests/n8n/researchChainRowFlow.test.mjs (the node-test harness idiom for asserting on emitted jsCode)
  </read_first>
  <behavior>
    - The emitted companies research system prompt contains, for each of the nine org types, both the key and its definition text.
    - The emitted prompt still names all nine keys as the allowed value set, so the strict enum constraint is unweakened.
    - The contacts target is changed only if it enumerates org types; if it does not, it is left byte-identical and that is recorded.
    - A second build produces byte-identical workflow JSON.
  </behavior>
  <action>
Change the companies target's `researchSystemPrompt()` in `scripts/build_cloud_workflows.py` so its
allowed org-type line is built from `ORG_TYPE_DEFINITIONS` instead of a bare stringify of
`ORG_TYPES`. Render one line per org type in `ORG_TYPES` order, each carrying the key and its
definition, under a heading equivalent to the Python side's definitions block — the same
definitions-not-values pattern `src/taxonomy.org_type_definitions_block()` produces, so the two
prompts stop being knowingly divergent.

Keep the allowed-value constraint explicit and unchanged in strength: the prompt must still state
that the returned value has to be one of those nine keys. Adding definitions is additive semantic
context, not a relaxation of the enum.

Inspect the contacts target's own `research_system_prompt_fn_js`. If it enumerates org types, apply
the identical change; if it does not, leave it untouched and record that determination in the plan
summary rather than making a speculative edit.

Rebuild the workflow JSON by running `scripts/build_cloud_workflows.py`, producing
`n8n/wf_enrichment_cloud.json` and `n8n/wf_enrichment_local_live.json` from source. Never hand-edit
either file.

Amend `docs/WEB-RESEARCH-SPEC.md` §2's dated TX-10 block with a new dated entry recording that the
knowing Python/n8n divergence it documented is closed as of this change, following the house
dated-amendment convention rather than rewriting the original text.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - A node script that loads `n8n/wf_enrichment_cloud.json`, finds the companies research node's `jsCode`, and checks that for each of the nine org types both the key and its definition text are substrings, exits 0.
    - The same check against `n8n/wf_enrichment_local_live.json` exits 0.
    - The emitted prompt still constrains the answer to the nine keys — a check that all nine key strings appear in the emitted `jsCode` exits 0.
    - Running `.venv/bin/python scripts/build_cloud_workflows.py` twice leaves both workflow JSON files unchanged on the second run.
    - `git diff --name-only` after the build lists no file under `n8n/` other than the two generated workflow JSON files and the generated taxonomy module.
    - The plan summary records whether the contacts target enumerates org types and what was done about it.
  </acceptance_criteria>
  <done>The lane the operator actually runs carries the same org-type semantics as the Python research path, built from source rather than typed into JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-03-03: node test on definition text, and the explicit fixture re-baseline</name>
  <files>tests/n8n/orgTypeDefinitionsPrompt.test.mjs, tests/fixtures/companies_jscode_frozen.json</files>
  <read_first>
    - tests/test_companies_factory_frozen.py (whole file — the byte-frozen fixture's re-baseline rule and header)
    - tests/fixtures/companies_jscode_frozen.json (the fixture being re-baselined)
    - tests/test_taxonomy_conformance.py (the Python-side assertion the new node test mirrors)
    - tests/n8n/researchErrorGateFlow.test.mjs (a recent node-test file's structure and import idiom)
  </read_first>
  <behavior>
    - A new node test asserts the emitted companies research `jsCode` contains each org type's definition text, not just its bare key.
    - The test fails if the prompt is reverted to a bare key list.
    - `tests/test_companies_factory_frozen.py` passes against the re-baselined fixture.
  </behavior>
  <action>
Create `tests/n8n/orgTypeDefinitionsPrompt.test.mjs`. Load the built companies research node's
`jsCode` the same way the sibling flow tests do, and assert two things: every org-type key from the
generated taxonomy module appears, and every corresponding definition string from that module's
`ORG_TYPE_DEFINITIONS` also appears. Add a negative-control assertion that the test would fail on a
key-only prompt — construct a synthetic key-only string in the test and assert the same predicate
returns false for it, so the test is proven to have teeth rather than trivially passing.

Re-baseline `tests/fixtures/companies_jscode_frozen.json` by regenerating it from the current build,
and record the re-baseline as an explicit, reviewed act: state in the plan summary and in the commit
message which node's `jsCode` changed, that the change is the org-type definitions rendering, and
that the fixture's own rule requires this to be deliberate rather than a routine make-the-test-pass
step. Do not weaken, delete or loosen any assertion in `tests/test_companies_factory_frozen.py`.

Run the full node suite in glob form; the directory form is broken on node 24.
  </action>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs && .venv/bin/python -m pytest tests/test_companies_factory_frozen.py tests/test_taxonomy_conformance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/n8n/*.test.mjs` passes with the new test file included in the run output.
    - `tests/n8n/orgTypeDefinitionsPrompt.test.mjs` contains a negative-control assertion proving the definition predicate returns false for a key-only prompt string.
    - `.venv/bin/python -m pytest tests/test_companies_factory_frozen.py -x -q` passes against the re-baselined fixture.
    - `git diff HEAD -- tests/test_companies_factory_frozen.py | grep -cE '^[-+][[:space:]]*assert '` returns 0 — no assertion in the frozen-fixture test was added, removed or reworded.
    - The plan summary names the changed node and states the re-baseline reason.
  </acceptance_criteria>
  <done>A regression back to a definition-free prompt fails a test, and the fixture that guards the emitted code was re-baselined deliberately and on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script -> deployed workflow JSON | Generated code becomes the prompt an LLM executes against real company data in production |
| taxonomy config -> model prompt | A definition string flows untransformed into a system prompt; a malformed or misleading definition changes every future classification |
| frozen fixture -> change detection | The fixture is the only mechanism detecting an unintended change to emitted node code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-11 | Tampering | emitted research `jsCode` | high | mitigate | The byte-frozen fixture is re-baselined deliberately with the reason recorded, and the new node test pins the definitions content so a silent revert fails |
| T-49-12 | Tampering | `n8n/wf_*.json` | high | mitigate | Both files are regenerated from source and never hand-edited; a second build is asserted byte-identical |
| T-49-13 | Spoofing | prompt-injected definition text | medium | mitigate | Definitions come only from the repo-controlled `config/taxonomy.yaml`, are serialised through `json.dumps` (the single escape path), and are reviewed in this commit's diff |
| T-49-14 | Repudiation | undisclosed prompt change | medium | mitigate | The dated amendment in `docs/WEB-RESEARCH-SPEC.md` records the divergence closing rather than silently rewriting the prior text |
| T-49-15 | Elevation of Privilege | none introduced | low | accept | This plan is offline; no credential, arming surface or deploy is touched |
</threat_model>

<artifacts_this_phase_produces>
- `ORG_TYPE_DEFINITIONS` const and export in `n8n/code/taxonomy.generated.js` — new symbol
- `tests/n8n/orgTypeDefinitionsPrompt.test.mjs` — new test file
- A dated amendment entry in `docs/WEB-RESEARCH-SPEC.md` §2 — new content
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest -q` full offline suite green.
- `node --test tests/n8n/*.test.mjs` green (glob form).
- Both workflow JSON files byte-reproducible across two consecutive builds.
- Cost consumed by this plan: zero n8n executions, zero Anthropic calls, zero provider credits, zero HubSpot writes, zero deploys.
</verification>

<success_criteria>
- The live lane's prompt source carries org-type definitions and a test guards it.
- Nothing was hand-edited in generated JSON, and the frozen fixture's re-baseline is on the record.
- Plan 49-04 has a deployable build waiting.
</success_criteria>

<output>
Create `.planning/phases/49-re-score-strategy-reporting/49-03-SUMMARY.md` when done
</output>
