---
phase: 49-re-score-strategy-reporting
plan: 04
type: execute
wave: 3
depends_on: ["49-03"]
files_modified:
  - .planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md
  - .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md
autonomous: false
requirements: [RESCORE-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Exactly ONE deploy and ONE bounce are spent this phase, matching D-05's declaration; any excess is disclosed in the run report rather than occurring silently."
    - "The RUNNING n8n instance — not the stored workflow body — is proven to carry the org-type definitions, by a live execution's own embedded node list or an independent GET after the bounce."
    - "After the deploy, no arming surface is left open: the n8n record-write allowlist is re-read independently and observed empty."
  artifacts:
    - .planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md
    - "the folded todo file moved out of pending, or annotated closed with the deploy evidence"
  key_links:
    - "scripts/deploy_n8n_workflows.py is the only deploy path; DRY_RUN=false and ALLOW_N8N_DEPLOY=true must be set in one invocation"
    - "the bounce (deactivate then reactivate) is what makes a stored PUT become running content"
    - "scripts/verify_live_write_safety.py is the disarmed-state verification pass"
  prohibitions:
    - statement: "MUST NOT accept a stored read-back of the workflow body as proof the running instance changed. A bare PUT does not reload a running workflow; only an independent GET after a bounce, or a live execution's own embedded node list, is evidence."
    - statement: "MUST NOT treat an n8n execution reporting success as proof of correctness. A node can receive an error object and pass it downstream as data with zero node-level errors; judge the run by node-level runData."
    - statement: "MUST NOT arm any HubSpot record-write allowlist for this deploy. The deploy proves a prompt change; it needs no write authority over any company record."
---

<objective>
Spend the phase's one declared deploy and bounce to make plan 49-03's prompt change live, and prove
the RUNNING instance changed.

Purpose: the folded todo is the only reason this phase has a deploy at all (D-05). A build that sits
in `n8n/wf_*.json` undeployed leaves the live enrichment lane still definition-free, which is exactly
the state the todo describes.

Output: `49-DEPLOY-PROOF.md` carrying the deploy record, the bounce, the running-content proof, and
the post-deploy disarmed verification; and the todo closed with that evidence.

Declared cost: 1 deploy, 1 bounce, and at least 1 proof execution against the 2,500/month n8n
allowance. Zero Anthropic calls, zero provider credits, zero HubSpot record writes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-re-score-strategy-reporting/49-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md
@.planning/phases/49-re-score-strategy-reporting/49-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 49-04-01: Authorise the one declared deploy and bounce</name>
  <decision>Spend Phase 49's single declared deploy and bounce to push the org-type-definitions prompt change into the running n8n instance.</decision>
  <context>
Waiver `D-49-01` (49-CONTEXT.md D-06) is a NEW, Phase-49-only delegation of the deploy, the bounce
and both arming surfaces to Claude. It does not revive `D-48-01` or `D-47.5-01`, both of which
expired with their phases and may not be cited. This checkpoint is the authorisation act; the
waiver only changes who types the command, never how many times it may be typed.

What will happen: `scripts/deploy_n8n_workflows.py` runs once with `DRY_RUN=false` and
`ALLOW_N8N_DEPLOY=true` set together in one invocation, followed by a deactivate-then-reactivate
bounce, followed by at least one execution used purely as running-content proof. No HubSpot record
-write allowlist is armed — the deploy changes a prompt, not a write path.

What it costs: 1 deploy, 1 bounce, at least 1 n8n execution out of the 2,500/month allowance
(roughly 0.04%). Zero Anthropic calls, zero provider credits, zero HubSpot writes.

Reversibility: costly. This changes the sealed enrichment lane's emitted prompt and follows a
re-baselined byte-frozen fixture; undoing it needs another rebuild, deploy and bounce, which would
exceed D-05's declaration and require disclosure.
  </context>
  <options>
    <option id="deploy-now">
      <name>Authorise the deploy and bounce now</name>
      <pros>The live lane stops being able to reproduce the statutory-origin misclassification; the folded todo closes; the declared window is spent exactly once as planned.</pros>
      <cons>Spends the phase's only deploy before the re-score window, leaving no deploy budget for a mid-phase build correction without disclosure.</cons>
    </option>
    <option id="defer-deploy">
      <name>Defer the deploy; leave the change built but not live</name>
      <pros>Keeps the deploy budget unspent until after the re-score window.</pros>
      <cons>The folded todo stays open and the live lane stays definition-free; the phase would have a built-but-unproven change, which is the state the todo already complains about.</cons>
    </option>
  </options>
  <resume-signal>Select: deploy-now or defer-deploy</resume-signal>
</task>

<task type="auto">
  <name>Task 49-04-02: Deploy, bounce, and prove the running instance carries the definitions</name>
  <files>.planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md</files>
  <precondition>n8n Cloud API credentials are present in the environment (`scripts/deploy_n8n_workflows.py::_has_n8n()` returns true) and `HUBSPOT_PORTAL_ID` reads `22617666`. If the deploy path prints a no-credentials skip, that attempt made no PUT and does not count against the declared deploy — record it and halt for the operator.</precondition>
  <read_first>
    - scripts/deploy_n8n_workflows.py (`_has_n8n`, `_base_url`, `_n8n_headers`, `_get_live_workflows`, and the arming/dry-run gates)
    - .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md (whole file — the ceremony and node-count baseline this mirrors)
    - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md (the running-content proof standard: a live execution's own node list)
    - n8n/wf_enrichment_cloud.json (the artifact being deployed, built in 49-03)
  </read_first>
  <action>
Run the deploy exactly once, with `DRY_RUN=false` and `ALLOW_N8N_DEPLOY=true` set together in a
single per-shell invocation through the absolute-path dotenv wrapper. Never set either variable in
`.env`, in a shell profile, or in a shell that outlives this task.

Then bounce: deactivate the deployed workflow and reactivate it. A bare PUT does not reload a
running workflow, so without the bounce the stored body and the running body diverge and every
subsequent read-back proves nothing.

Then prove the running instance changed, using one of the two evidence forms this repo accepts:
either an independent GET issued after the bounce (a fresh call, not the response to the PUT), or a
live execution's own embedded node data. Prefer the execution form where one is available, since it
is the standard Phase 47.5 and Phase 48 both used. Extract the companies research node's `jsCode`
from that evidence and confirm it contains each of the nine org types' definition text. Record the
node count and compare it against the prior baseline recorded in `48-DEPLOY-PROOF.md`; an unexpected
node-count change is a finding, not a rounding difference.

Judge the proof execution by its node-level run data, never by a top-level success status: a node
can receive an error object and pass it downstream as data with zero node-level errors and an
overall success verdict.

Write `.planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md` in the shape of
`48-DEPLOY-PROOF.md`: the deploy invocation and its response summary, the bounce, the independent
proof (execution id or GET) quoted rather than paraphrased, the node count before and after, and the
definitions check with the actual matched substrings. If any deploy attempt printed a
no-credentials skip and made no PUT, record it explicitly and state that it did not count against
the declaration.
  </action>
  <verify>
    <automated>test -s .planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `49-DEPLOY-PROOF.md` records exactly one deploy invocation that issued a PUT, and exactly one bounce.
    - The running-content proof is an independent GET issued after the bounce or a named live execution id, and the document says which; a stored read-back of the PUT body is not accepted as the proof.
    - The proof section quotes the org-type definition substrings actually found in the running node's emitted code, for all nine org types.
    - The node count observed is recorded alongside the prior-phase baseline, with any difference explained.
    - The document states the execution ids consumed and the count against the 2,500/month allowance.
    - The document records whether any attempt printed a no-credentials skip and, if so, that it made no PUT.
  </acceptance_criteria>
  <done>The lane the operator actually runs carries org-type definitions, proven from the running instance rather than from the file on disk.</done>
</task>

<task type="auto">
  <name>Task 49-04-03: Verify nothing is left armed, and close the folded todo</name>
  <files>.planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md, .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md</files>
  <precondition>Task 49-04-02 completed or explicitly failed. This task runs either way — closing the window always wins over the outcome of the work inside it.</precondition>
  <read_first>
    - scripts/verify_live_write_safety.py (the disarmed/drain verification pass and what it observes)
    - scripts/june_run_arm.py (the allowlist surface being confirmed empty)
    - .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md (the todo being closed)
    - .planning/phases/48-enrichment-coverage/48-ARM-RECORD.md (the disarm-and-independently-re-read ceremony)
  </read_first>
  <action>
Run `scripts/verify_live_write_safety.py`'s disarmed verification pass and record its verdict in
`49-DEPLOY-PROOF.md` under a post-deploy section. Then independently re-read the n8n record-write
allowlist surface and quote the read-back verbatim; a re-read of the stored PUT body is not
evidence, so the read must be a fresh, independent call.

Assert two things and record both: the record-write allowlist is empty, and no arming environment
variable persists into a fresh shell. Demonstrate the second by invoking the deploy script's dry
path in a new shell and showing it defaults to disarmed. This runs even if the deploy leg failed or
raised — closing the window is ungated.

Then close the folded todo. Move
`.planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md` to the
completed location this repo uses for resolved todos, or annotate it in place with a dated closure
block naming plan 49-03 (the offline fix), plan 49-04 (this deploy), the proof execution or GET, and
the new node test. Follow whichever convention the repo's existing resolved todos use — inspect the
todos directory before choosing, rather than inventing a location.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md').read_text(); sys.exit(0 if 'post-deploy' in p.lower() and 'allowlist' in p.lower() else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-DEPLOY-PROOF.md` carries a post-deploy section containing the disarmed verification verdict and a verbatim, independently-obtained allowlist read-back showing it empty.
    - The document records a fresh-shell invocation demonstrating no arming variable survived the window.
    - The document states that this verification ran regardless of the deploy leg's outcome.
    - The folded todo is no longer in `.planning/todos/pending/`, or carries a dated closure block naming plan 49-03, plan 49-04, the proof evidence, and `tests/n8n/orgTypeDefinitionsPrompt.test.mjs`.
    - `node --test tests/n8n/*.test.mjs` still passes after this task.
  </acceptance_criteria>
  <done>Nothing is armed, the closure is on the record, and the todo is discharged with evidence rather than an assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> n8n Cloud API | Two env vars grant authority to overwrite production workflow definitions |
| stored workflow body -> running workflow | A PUT changes storage; only a bounce changes what executes — the gap is where a false proof lives |
| deploy -> arming state | A deploy PUT can close or reopen an arming window as a side effect of the same call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-16 | Elevation of Privilege | deploy arming | high | mitigate | `DRY_RUN=false` and `ALLOW_N8N_DEPLOY=true` are required together in one per-shell invocation, and Task 49-04-03 proves a fresh shell defaults to disarmed |
| T-49-17 | Tampering | running vs stored workflow content | high | mitigate | Bounce plus an independent GET or a live execution's own node data; a stored read-back is explicitly refused as evidence |
| T-49-18 | Repudiation | undisclosed extra deploys | high | mitigate | D-05 declares exactly one; `49-DEPLOY-PROOF.md` records every attempt including credential-skips, and any excess is a disclosure obligation |
| T-49-19 | Elevation of Privilege | HubSpot record-write allowlist reopened by the deploy PUT | high | mitigate | Task 49-04-03 independently re-reads the allowlist after the deploy and asserts it empty, and runs even if the deploy failed |
| T-49-20 | Information Disclosure | n8n API key in logs | medium | mitigate | The deploy script's header construction is not printed; the proof document quotes response bodies and node data only |
| T-49-21 | Repudiation | a success status masking a node-level failure | medium | mitigate | The proof is judged by node-level run data, stated as a rule in the action and asserted in the acceptance criteria |
</threat_model>

<artifacts_this_phase_produces>
- `.planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md` — new file
- A dated closure block on, or the relocation of, `.planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md`
</artifacts_this_phase_produces>

<verification>
- `node --test tests/n8n/*.test.mjs` green (glob form) before and after the deploy.
- `.venv/bin/python -m pytest -q` full offline suite green.
- Window accounting: 1 deploy, 1 bounce, ≥1 proof execution, 0 HubSpot record writes, 0 Anthropic calls, 0 provider credits — recorded in `49-DEPLOY-PROOF.md` for the run report to consume.
</verification>

<success_criteria>
- The definitions change is live and proven live.
- Nothing is armed at the end of this plan.
- The folded todo is closed with evidence.
</success_criteria>

<output>
Create `.planning/phases/49-re-score-strategy-reporting/49-04-SUMMARY.md` when done
</output>
