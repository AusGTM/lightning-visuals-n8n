---
phase: 49-re-score-strategy-reporting
plan: 06
type: execute
wave: 5
depends_on: ["49-05"]
files_modified:
  - .planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json
  - .planning/phases/49-re-score-strategy-reporting/49-W2-RECORD.md
autonomous: false
requirements: [RESCORE-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BOTH of Entain's veto inputs are re-examined — region and lv_produces_content — not region alone, because a region flip provably cannot move the record while the content veto stands independently."
    - "The research returns evidence URLs, not assertion, and every URL, confidence value and summary is written into the committed evidence file whether or not the bar is cleared."
    - "The driver hard-refuses to write below config/field_policy.yaml's lv_produces_content bar — a minimum confidence of 85 AND a required evidence URL. A refusal is a refusal, never a warning and never a truncation."
    - "If W2 opens, the D→non-D tier transition is captured as a transition: an independent tier read before the write, a poll until the veto flag settles, an independent tier read after, and the elapsed interval between them."
    - "The post-write tier assertion is that the tier is not D. No specific landing tier is asserted, because the landing tier depends on a revenue band that must be read in pre-flight rather than assumed."
    - "If Entain does not clear the bar, the transition proof is re-deferred WITH the reason recorded, and the record states that it has now been deferred across three consecutive phases."
  artifacts:
    - .planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json
    - .planning/phases/49-re-score-strategy-reporting/49-W2-RECORD.md
  key_links:
    - "src/web_research.py::claude_web_research is the research path; the n8n lane is deliberately not used"
    - "config/field_policy.yaml's lv_produces_content entry is the bar that decides, not a human judgment call"
    - "the recompute POST enters at the D-18 webhook and routes the company gate straight to the decide node, spending no provider, research, judge or merge node"
    - "the decide node is the single writer of the veto flag and reason"
  prohibitions:
    - statement: "MUST NOT downgrade the evidence bar to a warning, or write a partial result, when the research falls short. D-14 overrides the human-review gate ONLY on the condition that the config bar hard-refuses; softening the refusal removes the sole remaining control."
    - statement: "MUST NOT assert a specific landing tier after the transition. A hard-coded expectation of a particular tier is the stale-literal class two prior phases each had to disclose after the fact, and the correct assertion is simply that the tier is no longer D."
    - statement: "MUST NOT clear a hard veto on a record without the evidence in the committed record. A veto marks a company a human deliberately excluded from the target list; returning it silently is the risk the locked decision named."
---

<objective>
Discharge the last carried-forward item three prior phases assigned to Phase 49 by name: re-examine
Entain `10024564084`'s two veto inputs, and — only if the evidence clears the config bar — write
them and capture the live D-to-non-D tier transition that Phase 47.5 could not close.

Purpose: Phase 47.5 discharged Ironman and Gravity Media; Entain is the residue. Its
`lv_produces_content = false` was never backed by positive evidence of absence, which the tri-state
rule requires, while Ladbrokes and Neds are Entain brands with obvious Australian operations — so
the research could plausibly clear both vetoes, or neither. Plan for both outcomes; "W2 never
opened" is a successful outcome to report, not a gap.

Output: a committed evidence file carrying the research result whichever way it falls, and a W2
record carrying either the transition proof or the re-deferral with its reason.

Declared cost: one Anthropic web-research call. If W2 opens, roughly one to two n8n executions
against the 2,500/month allowance and zero provider credits; if it does not, zero of both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-re-score-strategy-reporting/49-CONTEXT.md
@.planning/decisions/2026-08-12-org-type-venue-and-normalization.md
@.planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md
@config/field_policy.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 49-06-01: Re-examine Entain's two veto inputs against the config bar</name>
  <files>.planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json</files>
  <precondition>`ANTHROPIC_API_KEY` is set and `USE_MOCK_WEB_RESEARCH` is not left at its default true, or the research returns a fixture instead of a live result. `HUBSPOT_PORTAL_ID` reads `22617666` for the pre-flight reads. This task performs reads and one model call only; no arming variable is set.</precondition>
  <read_first>
    - src/web_research.py (whole file — `RESEARCH_SYSTEM`, `claude_web_research(record, system_prompt=None)`, `_extract_json`, and how the definitions block is appended)
    - config/field_policy.yaml lines 55-75 (the `lv_produces_content` entry: the minimum confidence and the required evidence URL that constitute the bar)
    - .planning/decisions/2026-08-12-org-type-venue-and-normalization.md § the D-V6 bright line and § "Phase 49 must RE-EXAMINE the three excluded records"
    - .planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md (the registry-grade evidence standard this must match)
    - scripts/remediate_veto_companies.py lines 330-380 and 640-690 (`build_input_patch`'s forbidden-property disjointness assertion and `estimate_cost`'s shape)
  </read_first>
  <action>
This task discharges D-13: re-examine BOTH of Entain's veto inputs, region and content, because a
region-only re-examination provably cannot move the record while the content veto stands
independently.

Pre-flight reads first, all disarmed. Read Entain `10024564084`'s current
`lv_country_region_normalized`, `lv_produces_content`, `lv_revenue_band`, `lv_org_type`,
`lv_icp_fit_score`, `lv_icp_tier`, `lv_anti_icp_flag` and `lv_anti_icp_reason`. The revenue band
matters specifically: Entain plc is a very large company, and if its band is populated at the top of
the rubric's scale the applicable revenue deduction is large enough that a cleared veto could land
the record below the lowest graded threshold — a non-D outcome that is neither B nor C. That is why
the transition assertion in Task 49-06-03 is stated as "not D" and not as a named tier. Record the
band and the arithmetic it implies in the evidence file.

Emit the ex-ante cost line before the model call, in the shape `estimate_cost()` produces: one web
research call, zero n8n executions at this stage, zero provider credits, and the Anthropic dollar
figure marked explicitly as a floor rather than a measurement — the research helper does not log
token usage, so a dollar actual cannot be captured. The house rule is that the estimate comes from
a function call, not a hand-typed paragraph. No spend checkpoint is required here: the research is
pre-authorised by the carried-forward decision that assigned this re-examination to this phase.

Then run the research once through `src/web_research.py`'s live path — not the n8n lane, whose
prompts are a separate consumer. Ask the two questions that matter, both framed on the bright line
"does it have substantive operating presence in Australia or New Zealand?", never "where is it
headquartered?":

  1. Region: does Entain have substantive ANZ operating presence? The multinational-with-local
     -operations pattern counts; a head office location does not decide it.
  2. Content: is there positive evidence that Entain produces live, near-live, streaming, broadcast
     or highlights content — or positive evidence of its absence? The tri-state rule sets the value
     false only on positive evidence of absence, and no such evidence was ever recorded for this
     record, which is why a region-only re-examination was rejected.

Apply the bar mechanically. Load `config/field_policy.yaml`'s `lv_produces_content` entry and refuse
unless the returned confidence meets or exceeds its minimum AND at least one evidence URL is present
for the content claim. Apply the same bar to the region claim. A shortfall produces a refusal with a
non-zero exit and a recorded reason — never a warning, never a truncated write, never a
"proceed anyway" path. This refusal is the only control that remains after the human-review gate was
deliberately overridden, so it carries the whole weight.

Write `.planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json` whichever way it
falls: the pre-flight property reads, the ex-ante cost line, the full research result including every
evidence URL, per-claim confidence and summary, the bar values applied, and a per-claim
cleared/refused verdict with the reason. An evidence file that exists only on success would make a
refusal invisible.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,sys,pathlib; d=json.loads(pathlib.Path('.planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json').read_text()); req=('preflight','cost_estimate','research','bar','verdict'); sys.exit(0 if all(k in d for k in req) and 'region' in d['verdict'] and 'produces_content' in d['verdict'] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-ENTAIN-EVIDENCE.json` exists and carries the pre-flight property reads, the ex-ante cost estimate, the raw research result, the bar values loaded from `config/field_policy.yaml`, and a per-claim verdict for both region and content.
    - The evidence file records Entain's current revenue band and the deduction it implies, so the transition assertion's shape is justified by data rather than assumption.
    - The cost line marks the Anthropic dollar figure as a floor and states why a measurement is unavailable.
    - Every claim that cleared the bar carries at least one evidence URL in the file; every claim that did not carries an explicit refusal reason.
    - The research ran through `src/web_research.py`; the file records the model and prompt path used, and no n8n execution was consumed by this task.
    - The bar values in the file match the values currently in `config/field_policy.yaml`, asserted by reading both rather than transcribing.
  </acceptance_criteria>
  <done>The last carried-forward re-examination is done to the registry-grade evidence standard, and the answer — clear or refuse — is on the record either way.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 49-06-02: Authorise or decline the conditional W2 window</name>
  <decision>Open the conditional W2 window to write Entain's region and content inputs, or close the phase's window budget with W2 unopened.</decision>
  <context>
This checkpoint is an ARMING authorisation, not an evidence review. The evidence bar in
`config/field_policy.yaml` has already decided whether the write is permitted — a minimum confidence
threshold and a required evidence URL, applied mechanically in Task 49-06-01 with a hard refusal on
any shortfall. The operator is not being asked to re-judge the research; that gate was overridden by
an explicit, dated decision recorded in this phase's context, precisely so this checkpoint stays a
window decision.

Read `49-ENTAIN-EVIDENCE.json` before deciding: the per-claim verdicts, the evidence URLs, the
confidence values, and Entain's revenue band.

If the bar was NOT cleared on both claims, there is no vehicle for the write and this checkpoint
resolves to declining W2. That is a legitimate outcome, and the reason is recorded rather than the
item rolling silently into another phase.

If the bar WAS cleared: W2 arms two surfaces together — the HubSpot record-write authority and the
record-scoped n8n allowlist armed by `scripts/june_run_arm.py --domains`. Unlike W1, this write does
traverse the n8n lane, because clearing a veto requires the decide node to run and that node is the
single writer of the veto flag and reason. An empty allowlist denies every write while still
reporting armed, so the driver asserts the allowlist is non-empty and exactly the intended record
before trusting the armed state.

Cost if opened: one record, roughly one to two n8n executions, zero provider credits, zero further
Anthropic calls. D-05 declared W2 as conditional; opening it is within the declaration, and exceeding
it in any way is a disclosure obligation in the run report.

Reversibility: costly. Clearing a hard veto returns to the target list a record a human deliberately
excluded; undoing it means re-establishing the exclusion and explaining the round trip.
  </context>
  <options>
    <option id="open-w2">
      <name>Open W2 — write both inputs and capture the transition (only valid if the bar cleared)</name>
      <pros>Discharges the last carried-forward item and finally proves the live D-to-non-D tier transition that has been deferred since Phase 47.5.</pros>
      <cons>Returns a previously excluded record to the target list; spends a second armed window in the phase.</cons>
    </option>
    <option id="decline-w2">
      <name>Decline W2 — record the refusal and the re-deferral reason</name>
      <pros>The bar held; nothing is written on insufficient evidence; the phase spends one armed window rather than two.</pros>
      <cons>The tier-transition proof is deferred for a third consecutive phase, which is how a permanent gap gets built one reasonable deferral at a time — so the record must say so explicitly.</cons>
    </option>
  </options>
  <resume-signal>Select: open-w2 or decline-w2</resume-signal>
</task>

<task type="auto">
  <name>Task 49-06-03: Run W2 with transition instrumentation, or record the re-deferral — and re-confirm the veto census</name>
  <files>.planning/phases/49-re-score-strategy-reporting/49-W2-RECORD.md</files>
  <precondition>Task 49-06-02 resolved. If it resolved to decline-w2, only the re-deferral and census legs of this task run and no arming occurs. If it resolved to open-w2, `ANTHROPIC_API_KEY` is not needed, but n8n API credentials and the HubSpot token are; arm and window run as separate shell invocations.</precondition>
  <read_first>
    - tests/test_scoring_parity.py lines 445-505 (`test_veto_clear_after_correction` — the settle-until and not-D assertion shape to mirror exactly)
    - scripts/remediate_veto_companies.py (`post_webhook_event(..., recompute=True)` and its 300-second default read timeout, `PINNED_COMPANY_ID_ORDER`)
    - scripts/june_run_arm.py (`--domains`, `--ids`, `--disarm` and the arming outcome shape)
    - scripts/verify_live_write_safety.py (the disarmed verification pass)
    - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md § armed window (the ceremony and the unproven-transition note this closes)
  </read_first>
  <action>
**If W2 was declined.** Write `49-W2-RECORD.md` recording: the refusal, the exact bar values the
evidence fell short of, and the explicit statement that the live tier transition proof is re-deferred
because no vehicle exists this phase — the weight re-score moves records between adjacent graded
tiers only and cannot produce a D-to-non-D crossing. State plainly that this item has now been
deferred by Phase 47.5, Phase 48 and Phase 49 in succession, and that saying so is the point: a
permanent gap gets built one reasonable deferral at a time. Then run only the census leg below. No
arming occurs and the record states that.

**If W2 was authorised.** Arm in one invocation and run the window in a separate one, so a failed
window start still has an explicit disarm path. Before trusting the armed state, assert the n8n
allowlist is non-empty and exactly the single intended record, and that the record-write authority
flag is on and the test-domain override is empty — an empty allowlist denies every write while still
reporting armed, and that silent-denial shape has already cost one execution in a prior phase.

Instrument the transition as a transition, per D-15 — which is exactly what the prior attempt could not do — its
two reads were about five seconds apart and its rehearsal read the tier as unscored before the veto,
so the flag-to-tier flow likely never wrote D at all:

  1. An independent read of the tier at t0, expected to read D. Record the timestamp.
  2. Write both veto inputs: the region and the content flag. Write inputs only — never the derived
     score, tier, veto flag or veto reason.
  3. Poll until the veto flag settles, using the settle-until helper shape the acceptance test
     already uses, with a generous timeout.
  4. An independent read of the tier at t1. Record the timestamp.
  5. Record the interval between t0 and t1.

Assert the t1 tier is not D, which is D-15's own required assertion shape. Do not assert any specific tier: the landing tier depends on the revenue
band read in pre-flight, and a large deduction can land the record below the lowest graded threshold,
which is a legitimate non-D outcome. A hard-coded expectation of a particular tier is the stale
-literal class two prior phases each had to disclose after the fact.

Judge the run by node-level run data rather than an overall success verdict, and treat a suspiciously
short execution duration as a signal the chain died early rather than as a fast success. A client-side
POST timeout is not a failure — the server completes — so never retry on a timeout without reading the
execution back, or the record gets touched twice.

Disarm both surfaces. Disarm is ungated and runs even if the write leg failed or raised. Then
independently re-read every armed surface and quote the read-back verbatim; a re-read of the stored
request body is not evidence. Run the disarmed verification pass and record its verdict.

**Census leg (D-16) — runs unconditionally, whatever happened above.** Read Jam TV `17317850381` and assert
by plain read that its veto is retained: its veto is geographic, and the org-type write it received in
Phase 48 could not and did not clear a geography veto. Re-derive the portal-wide non-ANZ veto census
and record the count. Re-run the check that no company carries a non-ANZ veto together with a blank
region, and record its count. Both figures go into `49-W2-RECORD.md` as live re-derivations with a
date stamp, not as restatements of a prior phase's numbers.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/49-re-score-strategy-reporting/49-W2-RECORD.md').read_text().lower(); sys.exit(0 if ('jam tv' in t or '17317850381' in t) and 'census' in t and ('disarm' in t or 'no arming' in t) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-W2-RECORD.md` exists and states unambiguously whether W2 opened.
    - If W2 opened: the record carries two independently-timestamped tier reads, the settle poll outcome for the veto flag, the interval between the reads, and an assertion that the later tier is not D — with no specific tier asserted anywhere.
    - If W2 opened: the record carries a verbatim, independently-obtained read-back of both armed surfaces showing them closed, plus the disarmed verification pass verdict, and states that the disarm ran regardless of the write leg's outcome.
    - If W2 opened: only input properties were written; the record enumerates the properties PATCHed and the four derived properties are absent from that list.
    - If W2 was declined: the record names the exact bar values the evidence fell short of and states the re-deferral reason, including that this is the third consecutive phase deferring it.
    - The census leg ran in both cases: Jam TV's retained veto is asserted from a plain read with its reason quoted, and both census counts are recorded as dated live re-derivations.
    - The record states the n8n executions consumed and their count against the 2,500/month allowance, and confirms zero provider credits.
  </acceptance_criteria>
  <done>Entain is resolved one way or the other with the reason on the record, the veto census is a live fact again, and if the record moved, the tier transition is proven as a transition rather than inferred from two nearby reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> production veto state | Research output can clear a hard veto and return an excluded company to the sales target list |
| operator shell -> two arming surfaces | The record-write flag and the n8n allowlist together grant write authority through the enrichment lane |
| n8n allowlist -> write scope | An empty or wrong allowlist changes what the lane will touch while still reporting armed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-30 | Spoofing | research evidence | high | mitigate | The config bar requires both a minimum confidence and a real evidence URL per claim, applied mechanically with a hard refusal; the URLs are committed so the decision is reviewable after the fact |
| T-49-31 | Elevation of Privilege | W2 arming | high | mitigate | Both surfaces armed per-shell in a separate invocation from the window; the allowlist is asserted non-empty and exactly the intended record before the armed state is trusted |
| T-49-32 | Tampering | write scope | high | mitigate | Only the two veto input properties are PATCHed; the derived score, tier, veto flag and veto reason are never written, and the decide node remains their single writer |
| T-49-33 | Repudiation | a silent un-veto | high | mitigate | The evidence file is written whichever way the bar falls, so a refusal is as visible as a clearance; a cleared veto cannot exist without its committed evidence |
| T-49-34 | Repudiation | a success verdict masking a dead chain | medium | mitigate | The run is judged by node-level run data and a short duration is treated as a signal the chain died early, not as a fast success |
| T-49-35 | Denial of Service | double-touching a record on a POST timeout | medium | mitigate | A client timeout is never retried without reading the execution back first; the helper's read timeout is long enough that server completion is the normal case |
| T-49-36 | Information Disclosure | credentials in the record | low | mitigate | The record quotes read-backs, verdicts and property values only; no header, key or webhook secret is transcribed |
</threat_model>

<artifacts_this_phase_produces>
- `.planning/phases/49-re-score-strategy-reporting/49-ENTAIN-EVIDENCE.json` — new
- `.planning/phases/49-re-score-strategy-reporting/49-W2-RECORD.md` — new
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest -q` full offline suite green.
- `node --test tests/n8n/*.test.mjs` green (glob form).
- If W2 opened: the disarmed verification pass reports both surfaces closed, confirmed by an independent re-read.
- Cost accounting: 1 Anthropic research call; 0 to 2 n8n executions; 0 provider credits.
</verification>

<success_criteria>
- Entain's two veto inputs are re-examined to the registry-grade evidence standard and the outcome is committed either way.
- The tier transition is either proven as a transition or re-deferred with its reason stated.
- Jam TV's retained veto and both census counts are live-re-derived facts with a date on them.
- Nothing is armed at the end of this plan.
</success_criteria>

<output>
Create `.planning/phases/49-re-score-strategy-reporting/49-06-SUMMARY.md` when done
</output>
