---
phase: 47-veto-remediation
plan: 03
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - scripts/veto_remediation_report.py
  - tests/test_veto_remediation_report.py
  - scripts/remediate_veto_companies.py
  - .planning/phases/47-veto-remediation/47-BEFORE.json
  - .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json
  - .planning/phases/47-veto-remediation/47-DRYRUN.md
autonomous: true
requirements: [VETO-01, COVER-01, COVER-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed before-snapshot records, for each of the 17 pinned ids, its live lv_org_type, lv_produces_content, lv_country_region_normalized, lv_icp_fit_score, lv_icp_tier, lv_anti_icp_flag and lv_anti_icp_reason, taken read-only with zero writes."
    - "Every property name appearing in any built payload is confirmed to exist live in the portal before any arming happens, and a missing name refuses the run rather than discovering a batch-wide 400 mid-window."
    - "Claude web research has run once against the live Anthropic web_search path for all 17 records and its results are committed, so the armed run consumes a cache rather than re-researching."
    - "The mandatory disarmed dry-run has printed the exact PATCH payloads and the exact webhook POST bodies for all 17 records, and made no HubSpot or n8n write."
    - "The dry-run output contains no lv_produces_content value written on absent evidence, and every omitted field carries a stated per-record reason."
    - "The research artefact is demonstrably live output, not the mock fixture — its evidence URLs differ from the committed fixture's."
    - "A predicted post-write score and tier exists per record before arming, so the outcome is predicted rather than discovered after a write."
  artifacts:
    - scripts/veto_remediation_report.py
    - tests/test_veto_remediation_report.py
    - .planning/phases/47-veto-remediation/47-BEFORE.json
    - .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json
    - .planning/phases/47-veto-remediation/47-DRYRUN.md
  key_links:
    - "check_schema_drift._get_live_properties -> the pre-arm property-existence guard in remediate_veto_companies.main -> a refused run instead of a mid-window batch 400"
    - "src/web_research.claude_web_research (USE_MOCK_WEB_RESEARCH=false) -> 47-RESEARCH-RESULTS.json -> the armed run's --from-cache input"
    - "47-BEFORE.json -> Plan 04's 47-AFTER.json diff -> the per-ID VETO-01 assertion"
  prohibitions:
    - "Never PATCH lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier."
    - "Never write a false value into lv_produces_content on absent evidence."
    - "Never touch Entain 10024564084, Gravity Media 15860277364 or Ironman 17317184159."
    - "Never create a new HubSpot property of any kind."
    - "Claude never sets ALLOW_VETO_REMEDIATION, ALLOW_N8N_ARM or DRY_RUN=false."
    - "Never gate the disarm path on the arming env var."
    - "Never attempt to fix the scripts/run_scoring_parity.py population sweep."
    - "Never edit any PORTAL-FACTS.md — read only."
---

<objective>
Everything that must happen before the write window opens, all of it read-only against HubSpot:
the per-ID before-snapshot, the live property-existence guard, the one-and-only Claude
web-research pass, and the mandatory disarmed dry-run printing the exact payloads.

Purpose: D-13 requires the dry-run to print the *exact* PATCH payloads before arming. Exact
payloads are impossible without the real researched values, so research must run before arming,
not inside the armed window — and its results must be cached so the armed run does not pay for
them twice. This plan is also where the operator gets to see, before any write, that Simtech LED
came back as a hardware vendor rather than a racing club (D-17).

Output: `scripts/veto_remediation_report.py`, its offline test, and three committed artefacts —
`47-BEFORE.json`, `47-RESEARCH-RESULTS.json`, `47-DRYRUN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-veto-remediation/47-CONTEXT.md
@.planning/phases/47-veto-remediation/47-RESEARCH.md
@.planning/phases/47-veto-remediation/47-PATTERNS.md
@.planning/phases/47-veto-remediation/47-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-ID before/after report script and the committed before-snapshot</name>
  <files>scripts/veto_remediation_report.py, tests/test_veto_remediation_report.py, .planning/phases/47-veto-remediation/47-BEFORE.json</files>
  <precondition>HUBSPOT_PRIVATE_APP_TOKEN is set and HUBSPOT_PORTAL_ID is 22617666 in the invoking shell; the script refuses with a non-zero exit and makes no API call otherwise.</precondition>
  <read_first>
    - scripts/remediate_veto_companies.py (PINNED_COMPANY_ID_ORDER, EXCLUDED_COMPANY_IDS, resolve_pinned_ids, expected_score_and_tier, _portal_ok, _has_credentials — import these, do not restate the id list a second time)
    - src/hubspot_client.py (get_record, search_records)
    - scripts/run_scoring_parity.py (the report-emitting idiom: a JSON artefact plus a non-zero exit code derived from findings; note that its population sweep is red by design and is NOT the model for this cohort report)
    - .planning/phases/46-rubric-decision-simulation-engine-parity/46-SIMULATION-REPORT.md (the before values these 17 rows are expected to match)
    - scripts/simulate_rubric_weights.py (Phase 46's read-only scoring of a live record under a given config — the prediction mechanism CONTEXT.md §Specifics asks this phase to reuse rather than discovering the outcome after a write)
    - tests/test_backfill_seed_company_scores.py (the offline-test idiom with monkeypatched requests)
  </read_first>
  <behavior>
    - `snapshot(ids, reader)` returns one row per id, in `PINNED_COMPANY_ID_ORDER` order, carrying the seven observed properties plus the id and the company name.
    - A snapshot is pure read: with `requests.post` monkeypatched to raise, `snapshot` still completes.
    - `predict(row)` returns the score and tier the record would reach given a candidate set of researched inputs, via `expected_score_and_tier`, without writing anything.
    - `diff(before_rows, after_rows)` returns a per-id record of which of the seven properties changed, and classifies each id as `cleared`, `residual_other_veto` or `still_non_anz`.
    - `residual_other_veto` requires the reason string to not contain the non-ANZ hard-veto reason; `still_non_anz` is the only failing classification.
    - An id present in one side and absent from the other is reported explicitly, never silently dropped.
  </behavior>
  <action>
Create `scripts/veto_remediation_report.py` — the per-ID before/after assertion that
47-VALIDATION.md's Wave 0 list says does not exist. `scripts/run_scoring_parity.py` samples the
wider population, not this cohort, so it cannot serve.

The script imports `PINNED_COMPANY_ID_ORDER`, `resolve_pinned_ids` and `expected_score_and_tier`
from `scripts.remediate_veto_companies` — the 17 ids are declared once, in one place. It has no
write path at all: from the HubSpot client it imports only `get_record` and `search_records`, and
no write helper of any kind is imported or called.

Observed properties per row: `name`, `lv_org_type`, `lv_produces_content`,
`lv_country_region_normalized`, `lv_icp_fit_score`, `lv_icp_tier`, `lv_anti_icp_flag`,
`lv_anti_icp_reason`.

Functions: `snapshot(ids, reader=get_record)`, `predict(row, candidate_inputs)`,
`diff(before_rows, after_rows)`, `classify(row)` returning one of `cleared`,
`residual_other_veto`, `still_non_anz`, and `main(argv)` with `--mode` accepting `before`,
`after` or `diff`, `--out <path>`, and `--before <path>` / `--after <path>` for diff mode. Exit
non-zero when any id classifies `still_non_anz` or when either side is missing an id.

The non-ANZ reason string is read from `config/icp_scoring.yaml`'s `hard_vetoes.non_anz.reason`,
never restated as a local literal — research confirmed it is byte-identical there and in the
deployed n8n Decide node, and a hand-copy would silently diverge if either changed.

Write the offline tests first against a fake reader, covering all six behaviours above. Then run
the live before-snapshot read-only and commit the artefact:

`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/veto_remediation_report.py', run_name='__main__')" --mode before --out .planning/phases/47-veto-remediation/47-BEFORE.json`

`.env` is Read and Bash permission-blocked in this repo, which is why the dotenv driver form is
used rather than sourcing the file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_veto_remediation_report.py -x && .venv/bin/python -c "import json;rows=json.load(open('.planning/phases/47-veto-remediation/47-BEFORE.json'));assert len(rows)==17, len(rows)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_veto_remediation_report.py -x` exits 0 with at least 6 tests collected.
    - `.planning/phases/47-veto-remediation/47-BEFORE.json` exists and contains exactly 17 rows, one per pinned id, in `PINNED_COMPANY_ID_ORDER` order.
    - Every row carries all 8 observed keys, and no row's id is any of `10024564084`, `15860277364`, `17317184159`.
    - The report script imports no write helper, asserted structurally rather than by substring: `.venv/bin/python -c "import ast; t=ast.parse(open('scripts/veto_remediation_report.py').read()); names={a.name for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and (n.module or '').endswith('hubspot_client') for a in n.names}; assert names <= {'get_record','search_records'}, names"` exits 0.
    - A test monkeypatches `requests.post` to raise and asserts `snapshot` still completes against a fake reader.
    - A test asserts `classify` returns `residual_other_veto` for a flag of "true" whose reason is a hardware-vendor veto, and `still_non_anz` for one whose reason contains the non-ANZ reason string.
    - A test asserts `diff` reports an id present on one side only rather than dropping it.
    - The before-snapshot's tier values agree with `46-SIMULATION-REPORT.md`'s Live column for all 17 rows, or the discrepancy is recorded in the plan summary rather than silently accepted.
  </acceptance_criteria>
  <done>A read-only per-ID report script exists with a green offline suite, and the live before-state of all 17 records is captured and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live property-existence guard before any arming</name>
  <files>scripts/veto_remediation_report.py, scripts/remediate_veto_companies.py, tests/test_veto_remediation_report.py</files>
  <precondition>HUBSPOT_PRIVATE_APP_TOKEN is set and HUBSPOT_PORTAL_ID is 22617666 in the invoking shell.</precondition>
  <read_first>
    - scripts/check_schema_drift.py lines 300-325 (`_get_live_properties(object_type)` — the existing CRM v3 properties-listing call; reuse it, do not write a second one)
    - CLAUDE.md §4.0 (the as-built delta: §4/§5/§7 are the target design, not an inventory — "re-list the portal before writing to any property named there"; several documented properties were never created)
    - CLAUDE.md §7.2 (the ICP signal metadata field names D-09 stamps — these are design-table names and are exactly what §4.0 warns about)
    - src/hubspot_client.py (batch_update_companies — note it raises on the HTTP response, so an unknown property name 400s the WHOLE batch, not one record)
    - scripts/remediate_veto_companies.py (main's pre-write ordering, where the guard call belongs)
    - .planning/phases/47-veto-remediation/47-RESEARCH.md Assumption A3
  </read_first>
  <behavior>
    - `live_property_names(object_type, lister)` returns the set of property names the portal actually has for that object type.
    - `missing_property_names(payload_keys, live_names)` returns the sorted set of keys absent from the portal.
    - The guard is called in `remediate_veto_companies.main()` after payloads are built and before any write branch, in both dry-run and armed mode.
    - The checked name set is the union of every built payload's keys AND the report script's 8 observed property names — including the four derived read-only fields, which never appear in a payload but are read by the snapshot and by the VETO-03 search.
    - A non-empty missing set refuses the run with a non-zero exit, names every missing property, and makes no PATCH.
    - An empty missing set proceeds silently.
  </behavior>
  <action>
Add `live_property_names(object_type="companies", lister=None)` and
`missing_property_names(payload_keys, live_names)` to `scripts/veto_remediation_report.py`,
delegating the actual HTTP call to `scripts.check_schema_drift._get_live_properties` rather than
issuing a second properties call of its own.

Then wire the guard into `scripts.remediate_veto_companies.main()`: after the three payloads are
built for all resolved records and before the dry-run/armed branch, refuse the run if any checked
name is absent from the portal, printing every missing name.

The checked set is deliberately WIDER than the payload keys. It is the union of (a) every
properties key across all built payloads, and (b) the 8 property names
`scripts/veto_remediation_report.py` observes — which includes the four derived read-only fields
this phase never writes. Both halves matter, for opposite reasons:

- A **written** name that does not exist makes the batch update return 400 and fail the ENTIRE
  batch, not one record — and it would surface mid-armed-window, after the ceremony is already
  open. D-09's seven metadata stamps come straight from CLAUDE.md §7.2's design tables, and §4.0
  states outright that those tables are the target design and not an inventory, listing several
  documented properties that were never created.
- A **read** name that does not exist fails *silently*: HubSpot ignores unknown names in a
  properties fetch and simply returns nothing for them. A misnamed read would make
  `47-BEFORE.json` record None for that field and `classify` misjudge the record without any
  error — which is the same silent-blank defect class that produced the false veto this phase
  exists to clear. This half is what actually discharges research Assumption A3, since the
  VETO-03 search property names appear nowhere in a written payload.

Write the offline tests first, against a fake lister: a payload key set fully covered by the live
set proceeds; a key set with two absent names returns both, sorted; and a
`remediate_veto_companies.main()` run with a fake lister missing one metadata stamp exits
non-zero and calls no write function.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_veto_remediation_report.py -k propert -x && .venv/bin/python -m pytest tests/test_remediate_veto_companies.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_veto_remediation_report.py -k propert -x` exits 0 with at least 3 tests collected.
    - `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -x` still exits 0 — the guard did not break Plan 01's suite.
    - A test asserts `missing_property_names` returns both absent names, sorted, for a two-missing case.
    - A test drives `remediate_veto_companies.main()` with a fake lister missing one of the seven metadata stamps and asserts a non-zero exit, a stdout mention of that property name, and that `batch_update_companies` was never called.
    - A test asserts the checked name set includes all four derived read-only fields and both VETO-03 search property names, proving the guard covers read-only names and not just written ones.
    - A live read-only run of the guard against the portal reports zero missing names for the full union of written keys and observed read names, and that result is recorded in the plan summary. If any name IS missing, the run is refused and the summary records which — the no-new-properties constraint means a missing stamp is a scope question for the operator, never something to create.
  </acceptance_criteria>
  <done>Every property this phase intends to write is confirmed to exist live before the window can open, and a missing one refuses the run outside the window rather than 400-ing the batch inside it.</done>
</task>

<task type="auto">
  <name>Task 3: The single live web-research pass and the mandatory disarmed dry-run</name>
  <files>.planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json, .planning/phases/47-veto-remediation/47-DRYRUN.md, scripts/remediate_veto_companies.py</files>
  <precondition>ANTHROPIC_API_KEY is set and reachable in the invoking shell, and HUBSPOT_PORTAL_ID is 22617666; the run refuses before spending anything otherwise.</precondition>
  <read_first>
    - src/web_research.py (the whole file — `USE_MOCK_WEB_RESEARCH` defaults to the string "true", so an unset env var silently returns the fixture; `claude_web_research` vs `mock_claude_web_research`; `REQUIRED_FIELDS`; `_extract_json`)
    - tests/fixtures/claude_web_research_company.json (the mock's evidence URLs — the live artefact must not match these)
    - scripts/remediate_veto_companies.py (`research_company`, `build_input_patch`, `build_metadata_patch`, `build_component_patch`, `build_webhook_event`, `unresolved_reasons`, `estimate_cost`, `refuse_if_over_budget`, `expected_score_and_tier`, and main's `--report` flag)
    - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md (the projection this run's actual call count is measured against)
    - .planning/phases/47-veto-remediation/47-CONTEXT.md D-08, D-13, D-14, D-17
    - config/field_policy.yaml (the evidence-URL obligations that decide which researched values are writable)
    - docs/WEB-RESEARCH-SPEC.md (the "prefer unknown over guessing" contract underpinning D-14)
  </read_first>
  <action>
Add a `--research-only` mode and a `--from-cache <path>` flag to
`scripts.remediate_veto_companies.main()`. `--research-only` performs the web-research pass for
every resolved pinned id and writes the raw `ProviderResult` dicts, keyed by company id, to the
given path; it performs no HubSpot write of any kind. `--from-cache` loads that file instead of
calling `research_company`, so the armed run in Plan 04 spends nothing on research a second time.

Then run the two commands, in order.

**(a) The research pass.** Set `USE_MOCK_WEB_RESEARCH=false` explicitly on the invocation — its
default is the string "true", so an unset variable silently returns
`tests/fixtures/claude_web_research_company.json` and the whole dry-run would print fixture
payloads while appearing to pass. Use the dotenv driver form, because `.env` is Read and Bash
permission-blocked:

`USE_MOCK_WEB_RESEARCH=false .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/remediate_veto_companies.py', run_name='__main__')" --research-only --out .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json`

Before the first call, print `estimate_cost(17)` and run `refuse_if_over_budget` — this is the
COVER-02 gate, and it fires before money is spent, not after.

**(b) The mandatory disarmed dry-run (D-13).** With `DRY_RUN` at its default and
`ALLOW_VETO_REMEDIATION` unset, run over all 17 with `--from-cache`, capturing stdout into
`47-DRYRUN.md`. Every exact PATCH payload and every exact webhook POST body for all 17 records
must appear. Prepend a short header to the file recording the command run, the timestamp, and the
fact that both write surfaces were disarmed.

Then write, into the same file, a per-record pre-arm table carrying: id, name, researched
`lv_org_type`, `lv_produces_content` (or the stated reason it was omitted),
`lv_country_region_normalized`, the predicted post-write score and tier from
`expected_score_and_tier`, and whether the record is predicted to clear the veto or to land on a
different, correct one. D-16 and D-17 both live here: Simtech LED is expected to come back
`hardware_vendor`, which is a genuine hard veto and a correct outcome — the operator sees that
prediction before arming, not after writing. Waikato Racing Club Inc is expected `NZ`, not `AU`,
which is inside ANZ and carries no veto.

Finally, record the actual research call count and the count of records landing on an
evidence-gated org type, so Plan 04's actuals table has a measured D-20 second-pass projection
rather than D-17's estimate of roughly four.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json;d=json.load(open('.planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json'));assert len(d)==17, len(d);f=json.load(open('tests/fixtures/claude_web_research_company.json'));fu=set(f['evidence']['evidence_urls']);assert all(not fu & set(v.get('evidence',{}).get('evidence_urls',[])) for v in d.values()), 'mock fixture leaked into live research artefact'" && grep -c 'objectId' .planning/phases/47-veto-remediation/47-DRYRUN.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json` has exactly 17 entries keyed by the 17 pinned ids.
    - No entry's `evidence.evidence_urls` intersects the URL set in `tests/fixtures/claude_web_research_company.json` — proving live output, not a silent mock run.
    - `grep -c 'objectId' .planning/phases/47-veto-remediation/47-DRYRUN.md` outputs at least `17` — one webhook POST body per record.
    - `47-DRYRUN.md` contains a batch-update payload block for each of the 17 ids, and its header records that both write surfaces were disarmed.
    - No payload block in `47-DRYRUN.md` contains any of `lv_anti_icp_flag`, `lv_anti_icp_reason`, `lv_icp_fit_score` or `lv_icp_tier` as a written property key.
    - Every record whose `lv_produces_content` is absent from its payload has a stated reason in the pre-arm table, distinguishable from never-attempted.
    - The pre-arm table carries a predicted score and tier for all 17 records.
    - `git status --porcelain .planning/phases/47-veto-remediation/` shows the three artefacts as the only additions, and `47-BEFORE.json` is unchanged — the research pass wrote nothing to HubSpot.
  </acceptance_criteria>
  <done>Research has run exactly once, live, and is cached; the disarmed dry-run has printed every exact payload and every exact webhook body; and the predicted outcome of all 17 records is on paper before any arming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic web_search → payload values | Untrusted web content becomes CRM values on production records |
| script → HubSpot CRM v3 read surfaces | Read-only in this plan, but the same client that writes elsewhere |
| shell env → script | `USE_MOCK_WEB_RESEARCH`, `ANTHROPIC_API_KEY`, HubSpot token all ambient |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-12 | Spoofing | live-vs-mock research path | high | mitigate | `USE_MOCK_WEB_RESEARCH=false` set explicitly on the invocation and an acceptance check that the artefact's evidence URLs do not intersect the committed fixture's — a silent mock run cannot pass |
| T-47-13 | Tampering | unknown property name in a batch payload | high | mitigate | Task 2's live property-existence guard refuses before any write branch; without it an unknown name 400s the whole batch mid-window |
| T-47-14 | Spoofing | web-sourced org-type and content claims | medium | mitigate | Evidence URL required for every evidence-gated org type and for produces-content; D-14 omits rather than guesses; the pre-arm table exposes every classification for operator review before arming |
| T-47-15 | Information Disclosure | dry-run artefact committed to the repo | medium | mitigate | The dry-run prints payloads only; `hs_headers()` and the webhook secret are never in a print path, and the artefact is reviewed for token-shaped strings before commit |
| T-47-16 | Denial of Service | unbounded research spend | medium | mitigate | `refuse_if_over_budget` runs before the first call; `WEB_RESEARCH_MAX_SEARCHES` bounds searches per record; the pinned set bounds records to 17 |
| T-47-SC | Tampering | package installs | low | accept | No package-manager install occurs — every dependency is already in `requirements.txt` |

**ASVS level:** L1. **Blocking threshold:** `high`. Both `high` threats are dispositioned
`mitigate` with a concrete, checked mitigation.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_veto_remediation_report.py tests/test_remediate_veto_companies.py -x` — both suites green.
- `47-BEFORE.json` has 17 rows; `47-RESEARCH-RESULTS.json` has 17 live entries; `47-DRYRUN.md`
  carries 17 webhook bodies and 17 payload blocks.
- No HubSpot write occurred in this plan: the report script has no write path, and the research
  and dry-run modes both short-circuit before `batch_update_companies`.
- `scripts/run_scoring_parity.py`'s population sweep stays red — expected, closed by Phase 49.
</verification>

<success_criteria>
- The before-state of all 17 records is captured, committed and read-only.
- Every property this phase will write is confirmed live.
- Research has run once and is cached.
- The exact payloads and webhook bodies are on paper, disarmed, with a predicted outcome per
  record — everything the operator needs to decide whether to arm.
</success_criteria>

## Artifacts this phase produces (Plan 03 contribution)

**New files**
- `scripts/veto_remediation_report.py`
- `tests/test_veto_remediation_report.py`
- `.planning/phases/47-veto-remediation/47-BEFORE.json`
- `.planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json`
- `.planning/phases/47-veto-remediation/47-DRYRUN.md`

**New symbols in `scripts/veto_remediation_report.py`**
- `snapshot`, `predict`, `diff`, `classify`, `live_property_names`, `missing_property_names`,
  `main`

**New symbols added to `scripts/remediate_veto_companies.py`**
- the pre-write property-existence guard call inside `main`

**New CLI flags**
- `scripts/veto_remediation_report.py`: `--mode {before,after,diff}`, `--out`, `--before`,
  `--after`
- `scripts/remediate_veto_companies.py`: `--research-only`, `--from-cache`, `--out`

**New env var names**
- none (`USE_MOCK_WEB_RESEARCH` already exists in `src/web_research.py`)

**New test names**
- `tests/test_veto_remediation_report.py` — snapshot, classify, diff and property-guard tests,
  at least three carrying `propert` in the identifier

<output>
Create `.planning/phases/47-veto-remediation/47-03-SUMMARY.md` when done
</output>
