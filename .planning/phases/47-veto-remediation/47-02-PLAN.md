---
phase: 47-veto-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/47-veto-remediation/47-VALIDATION.md
  - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md
  - .planning/phases/47-veto-remediation/COVERAGE.md
autonomous: true
requirements: [COVER-01, COVER-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md's Traceability table maps COVER-01 and COVER-02 to both Phase 47 and Phase 48, with wording making clear neither phase closes them alone."
    - "An estimated cost for this phase's run exists in writing before any live call is made, naming web-research calls, redundant second-pass research calls, n8n executions against the 2,500/month allowance, and zero Lusha credits."
    - "The cost document states that a run projected to exceed either budget is refused outright, and names the function that refuses it."
    - "47-VALIDATION.md's Per-Task Verification Map carries a concrete task id in every row rather than the planner-fills-this placeholder."
    - "COVERAGE.md exists as a capability matrix scoped to the external surfaces this phase actually touches, with every opt-out carrying a one-line reason."
  artifacts:
    - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md
    - .planning/phases/47-veto-remediation/COVERAGE.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/47-veto-remediation/47-VALIDATION.md
  key_links:
    - "D-02 -> REQUIREMENTS.md Traceability rows for COVER-01/COVER-02 -> Phase 48's own closure bar"
    - "estimate_cost() in scripts/remediate_veto_companies.py -> 47-COST-ESTIMATE.md's projected figures -> 47-RUN-REPORT.md's actuals in Plan 04"
  prohibitions:
    - "Never create a new HubSpot property of any kind — standing v0.9 milestone constraint."
    - "Never edit any PORTAL-FACTS.md — read only."
    - "Never claim COVER-01 or COVER-02 complete from Phase 47 alone — this phase covers only its 17 records."
    - "Never mint a new requirement ID where broadening an existing one preserves traceability — Phase 46's precedent."
---

<objective>
Land the paperwork the live run depends on and that must exist BEFORE it: the D-02 traceability
amendment, the D-03/COVER-02 ex-ante cost estimate, the filled validation map, and the API
coverage matrix.

Purpose: COVER-02's bar is "estimated before the run and reported after". An estimate written
after the money is spent is not an estimate. This plan runs in parallel with Plan 01 precisely so
nothing in the ceremony waits on it.

Output: an amended `.planning/REQUIREMENTS.md` and `47-VALIDATION.md`, plus new
`47-COST-ESTIMATE.md` and `COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-veto-remediation/47-CONTEXT.md
@.planning/phases/47-veto-remediation/47-RESEARCH.md
@.planning/phases/47-veto-remediation/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the traceability table for D-02 and fill the validation map</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/47-veto-remediation/47-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the whole file — the Traceability table at lines 107-123 currently maps COVER-01/COVER-02 to Phase 48 only; note the amendment-in-place style used on RUBRIC-01/02/03 where scope was broadened with a dated parenthetical rather than by minting a new ID)
    - .planning/phases/47-veto-remediation/47-CONTEXT.md D-01, D-02, D-05
    - .planning/phases/47-veto-remediation/47-VALIDATION.md (the Per-Task Verification Map whose own note says "Task IDs are filled in by the planner", and the Wave 0 Requirements list)
    - .planning/phases/47-veto-remediation/47-01-PLAN.md (task names and verify commands, to key the map rows against)
    - .planning/phases/47-veto-remediation/47-03-PLAN.md
    - .planning/phases/47-veto-remediation/47-04-PLAN.md
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, using scoped Edit calls against specific rows and paragraphs —
never a whole-file rewrite:

1. Traceability table — change the COVER-01 and COVER-02 rows' Phase column from `Phase 48` to
   `Phase 47 + 48`, per D-02. Add a footnote directly under the table stating that Phase 47
   satisfies both for its 17 records and Phase 48 for the remainder, and that neither phase may
   be closed claiming full coverage on its own.
2. COVER-01's requirement text — append a dated parenthetical amendment in the same style
   RUBRIC-01 and RUBRIC-03 already use, attributed to `/gsd-discuss-phase 47` on 2026-08-11,
   recording that the 18-company set is covered in two parts: 17 of them by Phase 47 (they are a
   strict subset of Phase 48's set, being simultaneously the false-veto cohort), and the 1
   remaining record — Racing NSW `15008671672`, flagged `blank_org_type` only with no
   `false_veto` flag — by Phase 48.
3. COVER-02's requirement text — append the same style of parenthetical recording that its cost
   discipline applies to Phase 47's run as well, per D-03.
4. Leave the VETO-01/VETO-02/VETO-03 checkboxes unticked. Plan 04 ticks them with test pointers
   after the live run; ticking them here would claim work that has not happened.

In `.planning/phases/47-veto-remediation/47-VALIDATION.md`:

5. Replace the `Task IDs are filled in by the planner` note and add a leading `Task` column to
   the Per-Task Verification Map, keying each fixed behavior row to the concrete task that
   satisfies it: the two offline VETO-01 rows and the never-write row to Plan 01 Tasks 1 and 3;
   the per-ID before/after row to Plan 03 Task 1; the VETO-02 armed-then-disarmed row to Plan 04
   Tasks 1 and 2; the VETO-03 manual search row to Plan 04 Task 3.
6. Update the four Wave 0 Requirements checkboxes to name where each is discharged: the per-ID
   assertion by `scripts/veto_remediation_report.py` (Plan 03 Task 1); the settle-and-assert
   wrapper and the separate veto-keyed settle path by `settle_and_assert` / `settle_tier` /
   `settle_veto` in `scripts/remediate_veto_companies.py` (Plan 01 Task 2); the never-write guard
   by the already-existing assertions in `tests/test_backfill_seed_company_scores.py` at lines
   169 and 234 — research established that the cited "T-40-22" is a Phase 40 plan-task label and
   not a test identifier — extended by the new sibling assertions in
   `tests/test_remediate_veto_companies.py` (Plan 01 Task 3).
7. Set `nyquist_compliant: true` in the frontmatter only if every map row now carries an
   automated command or an explicit manual-only justification. If any row does not, leave it
   false and state which row and why in the Validation Sign-Off block.
  </action>
  <verify>
    <automated>grep -c 'Phase 47 + 48' .planning/REQUIREMENTS.md && ! grep -q 'Task IDs are filled in by the planner' .planning/phases/47-veto-remediation/47-VALIDATION.md && grep -q 'veto_remediation_report.py' .planning/phases/47-veto-remediation/47-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 47 + 48' .planning/REQUIREMENTS.md` outputs exactly `2`, and both matches are on the COVER-01 and COVER-02 Traceability rows.
    - The footnote under the Traceability table states that neither Phase 47 nor Phase 48 closes COVER-01/COVER-02 alone.
    - COVER-01's requirement text names Racing NSW `15008671672` as the one record left to Phase 48.
    - `47-VALIDATION.md`'s Per-Task Verification Map has a `Task` column with a non-placeholder value in all 6 rows.
    - `47-VALIDATION.md`'s Wave 0 Requirements section names all four of `scripts/veto_remediation_report.py`, `settle_and_assert`, `settle_veto` and `tests/test_backfill_seed_company_scores.py`.
    - The VETO-01, VETO-02 and VETO-03 checkboxes in `.planning/REQUIREMENTS.md` are still unticked.
    - `.venv/bin/python -m pytest tests/ -x -q` exits 0 for the offline tier — no requirements-parsing or docs-conformance guard was broken by the edit.
  </acceptance_criteria>
  <done>COVER-01 and COVER-02 are traceable to both phases with the split stated, and every validation-map row names the task that satisfies it.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ex-ante cost estimate (D-03 / COVER-02)</name>
  <files>.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md</files>
  <read_first>
    - .planning/phases/47-veto-remediation/47-RESEARCH.md §"Cost estimate inputs (D-03 / COVER-02)" — the $0.0686/record Phase 20 canary provenance, why it undercounts this path, the `Research Trigger Gate` `needsResearch()` predicate with its `EVIDENCE_GATED_ORG_TYPES` list, and the note that `ALLOW_WEB_RESEARCH` is a build-time literal
    - .planning/phases/47-veto-remediation/47-CONTEXT.md D-03, D-08, D-17, D-18, D-20
    - src/web_research.py (the `WEB_RESEARCH_MAX_SEARCHES` default of 5, `max_tokens=4096`, the `ANTHROPIC_RESEARCH_MODEL` default)
    - .planning/phases/47-veto-remediation/47-01-PLAN.md (the `estimate_cost` and `refuse_if_over_budget` contract this document must agree with)
  </read_first>
  <action>
Create `.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md` recording this run's projected
cost BEFORE it happens, so Plan 04 can report actuals against the same row labels. Every figure
names its source.

- **Web-research calls: 17**, one per pinned record, on the standalone Python
  `src/web_research.py::claude_web_research` path — a single `ANTHROPIC_RESEARCH_MODEL` call per
  record with the native `web_search` server tool at up to `WEB_RESEARCH_MAX_SEARCHES` (default
  5) searches. State plainly that the native `web_search` tool bills per search on top of token
  cost and that this component is unmeasured in this repo.
- **Redundant second-pass research (D-20): projected ~4 records.** D-17 names Simtech LED, Jam
  TV, Editix and The Rumble / Pacific Action Sports as the likely landings on `hardware_vendor`
  or `content_producer`. Explain that the deployed `Research Trigger Gate`'s `needsResearch()`
  fires on org-type membership alone and does not check whether evidence is already present, and
  that `ALLOW_WEB_RESEARCH` is baked true at build time so no webhook payload flag suppresses it.
  Accepted, not suppressed — suppressing it means editing the deployed workflow, a far larger
  blast radius than four extra calls.
- **Anthropic dollar figure:** anchor on the Phase 20 canary's measured $0.0686 per record
  (execution 332, 2026-07-30: $0.052 Haiku research plus $0.017 Sonnet judge). State explicitly
  that this was measured on the n8n Haiku-plus-Sonnet chain and NOT on the path D-08 chose, and
  give the resulting figure for 17 records plus the ~4 second-pass records as a bounded
  order-of-magnitude projection rather than a precise number.
- **n8n executions: at most 17** — one per D-18 webhook POST — against the 2,500/month plan
  allowance. State that there is no n8n usage endpoint (project memory `n8n-execution-budget`),
  so this projection is a static count and month-to-date headroom is a figure the operator
  confirms at the arming checkpoint. Do not imply a live balance check that cannot be made.
- **Provider credits: zero.** D-08 routes enrichment through Claude web research rather than the
  provider waterfall, so no ZoomInfo, Apollo or Lusha call is made and the Lusha balance is
  untouched. Say so explicitly rather than leaving it inferred.
- **Refusal rule:** name `refuse_if_over_budget` in `scripts/remediate_veto_companies.py` as the
  function that refuses, state that it raises `BudgetRefused`, and state that a run projected
  over budget is refused outright rather than truncated part-way — COVER-02's literal bar.
- **Actuals placeholder:** an empty table carrying the same row labels, marked for Plan 04.
  </action>
  <verify>
    <automated>grep -q 'refuse_if_over_budget' .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md && grep -q '2,500' .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md && grep -q '0.0686' .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md` exists and contains a projected-cost table with rows for web-research calls, redundant second-pass research calls, Anthropic dollars, n8n executions and provider credits.
    - The document states `17` web-research calls and `at most 17` n8n executions, both against the `2,500`/month allowance.
    - The document states provider credits are `0` and names D-08 as the reason.
    - The document names `refuse_if_over_budget` and `BudgetRefused`, and states the run is refused rather than truncated.
    - The document states the $0.0686 figure was measured on a different code path and that the native web_search per-search billing is unmeasured here.
    - An empty Actuals table with matching row labels exists, marked as Plan 04's to fill.
  </acceptance_criteria>
  <done>A written, sourced cost projection exists before any Anthropic or n8n call is made, with a matching empty actuals table for the post-run report.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped API coverage matrix</name>
  <files>.planning/phases/47-veto-remediation/COVERAGE.md</files>
  <read_first>
    - src/hubspot_client.py (get_record, batch_update_companies, search_records — the three CRM v3 surfaces this phase uses)
    - scripts/check_schema_drift.py lines 300-325 (`_get_live_properties` — the CRM v3 properties-listing surface Plan 03 reuses)
    - operator-claude-plugin/scripts/enrichment.py (ENRICHMENT_PATH, enrichment_target, the X-Enrichment-Secret header)
    - operator-claude-plugin/scripts/n8n_arming.py (arm_for_dispatch, disarm — the n8n workflow-config API surface)
    - src/web_research.py (the Anthropic Messages API plus the native web_search server tool)
  </read_first>
  <action>
The API-coverage detector fires on this phase, but on a weak signal: a mention of "HubSpot API
constraints" in a canonical-references list. This phase *consumes* surfaces that were integrated
in earlier phases rather than integrating a new one, so the correct response is a matrix scoped
to exactly the surfaces this phase's code touches — not a re-enumeration of the HubSpot API.

Create `.planning/phases/47-veto-remediation/COVERAGE.md` with a
`| capability | decision | reason |` table. INTEGRATE is the default; every OPT-OUT carries a
one-line reason. Cover exactly these capabilities and no others:

- HubSpot CRM v3 single-record read (`get_record`) — INTEGRATE, already wrapped.
- HubSpot CRM v3 batch update (`batch_update_companies`) — INTEGRATE, the input/metadata/component
  write path.
- HubSpot CRM v3 object search (`search_records`) — INTEGRATE, used by the before/after report.
- HubSpot CRM v3 properties listing (`check_schema_drift._get_live_properties`) — INTEGRATE, the
  Plan 03 pre-arm existence check.
- HubSpot CRM v3 property creation — OPT-OUT, reason: the standing v0.9 no-new-properties
  constraint.
- HubSpot Automation v4 flows — OPT-OUT, reason: the derived chain is read-only to this phase;
  WF1 and the calculated property are consumed, never edited.
- HubSpot webhook subscription management — OPT-OUT, reason: D-18 chose a direct webhook POST
  precisely to avoid depending on a subscription this repo has never confirmed live (research
  Assumption A1).
- n8n enrichment webhook POST (`webhook/hubspot/enrichment/event`) — INTEGRATE, the D-18 trigger.
- n8n workflow arm/disarm config API (`n8n_arming.arm_for_dispatch` / `disarm`) — INTEGRATE, the
  second VETO-02 write surface per D-19.
- n8n workflow deploy (`scripts/deploy_n8n_workflows.py`) — OPT-OUT, reason: D-20 accepts the
  redundant research call rather than editing and redeploying the workflow.
- n8n executions/usage API — OPT-OUT, reason: no usage endpoint exists on this plan, so budget
  headroom is an operator confirmation, not an API read.
- Anthropic Messages API with the native `web_search` server tool — INTEGRATE, D-08's enrichment
  source.
- Provider APIs (ZoomInfo, Apollo, Lusha) — OPT-OUT, reason: D-08 routes enrichment through
  Claude web research and draws zero provider credits.

Add a short header sentence stating the scoping judgment above, so a later reader knows the
matrix is deliberately narrow rather than incomplete.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-veto-remediation/COVERAGE.md && grep -c '| OPT-OUT |' .planning/phases/47-veto-remediation/COVERAGE.md && grep -c '| INTEGRATE |' .planning/phases/47-veto-remediation/COVERAGE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-veto-remediation/COVERAGE.md` exists with a `| capability | decision | reason |` header row.
    - The table has exactly 13 capability rows: 7 INTEGRATE and 6 OPT-OUT.
    - Every OPT-OUT row has a non-empty reason cell.
    - No row is left blank or marked TBD.
    - The header paragraph states the matrix is scoped to surfaces this phase touches, not the full HubSpot API.
  </acceptance_criteria>
  <done>A scoped, fully-decided capability matrix exists; no capability is left undecided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → later execution | These documents authorise and bound a live production write; a wrong figure here is acted on later without re-derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-09 | Repudiation | 47-COST-ESTIMATE.md | medium | mitigate | Every figure names its source file or measurement, and the unmeasured components are labelled unmeasured rather than guessed |
| T-47-10 | Tampering | .planning/REQUIREMENTS.md | medium | mitigate | Scoped Edit calls only, never a whole-file Write; the VETO checkboxes are explicitly left unticked so this plan cannot claim the live run's outcome |
| T-47-11 | Information Disclosure | any of the four documents | low | mitigate | No credential, token or webhook secret value is written into any of them; only env-var names appear |
| T-47-SC | Tampering | package installs | low | accept | No package-manager install occurs in this plan — documentation edits only |

**ASVS level:** L1. **Blocking threshold:** `high`. No `high` threat exists in a
documentation-only plan.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -x -q` — the offline tier stays green; no docs-conformance
  or requirements-parsing guard is broken.
- All four target files exist and carry their acceptance strings.
- `scripts/run_scoring_parity.py`'s population sweep stays red — expected, closed by Phase 49.
</verification>

<success_criteria>
- COVER-01 and COVER-02 map to Phase 47 + 48 with the split stated and neither phase able to
  claim sole closure.
- A sourced cost projection exists in writing before any live call.
- Every validation-map row names its task; every Wave 0 item names where it is discharged.
- A scoped, fully-decided API coverage matrix exists.
</success_criteria>

## Artifacts this phase produces (Plan 02 contribution)

**New files**
- `.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md`
- `.planning/phases/47-veto-remediation/COVERAGE.md`

**Modified files**
- `.planning/REQUIREMENTS.md` — COVER-01/COVER-02 rows retargeted to `Phase 47 + 48`, a
  Traceability footnote, and dated scope parentheticals on both requirement texts
- `.planning/phases/47-veto-remediation/47-VALIDATION.md` — a new `Task` column on the Per-Task
  Verification Map, discharge pointers on all four Wave 0 items, and a `nyquist_compliant`
  frontmatter decision

**No new code symbols, CLI flags, env vars or test names.**

<output>
Create `.planning/phases/47-veto-remediation/47-02-SUMMARY.md` when done
</output>
