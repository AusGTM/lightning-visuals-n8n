---
phase: 47-veto-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/remediate_veto_companies.py
  - tests/test_remediate_veto_companies.py
autonomous: true
requirements: [VETO-01, VETO-02, COVER-01, COVER-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single pinned company can be driven end-to-end through the remediation script in dry-run — research, input PATCH, metadata PATCH, component PATCH, webhook POST body — with every payload printed and no network write performed."
    - "The script refuses any company id that is not one of the 17 pinned ids, including Entain 10024564084, Gravity Media 15860277364 and Ironman 17317184159, before any HubSpot or n8n call is made."
    - "No payload the script builds ever contains lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier as a property key."
    - "A record whose research establishes no evidence for produces-content has that property omitted from the input payload entirely and a stated per-record reason recorded, never a false value."
    - "Running with all 17 pinned ids proceeds at the default cap; a VETO_MAX_RECORDS below 17 refuses with a non-zero exit and no API call; a value above 17 is clamped down to 17 rather than honoured."
    - "The cap comparison is integer-only — VETO_MAX_RECORDS is parsed with int() and a non-integer value falls back to the default of 17 rather than being truncated or rounded, so no fractional cap can silently admit or exclude a record."
    - "An empty n8n allowlist is refused rather than armed, and the pinned allowlist of exactly 17 ids is what the arming call is given."
    - "Component values written are integers taken from the loaded rubric config, and their sum equals the oracle's own total score exactly — no rounding step exists anywhere in the path."
    - "Expected tier boundaries used by the script are the same greater-than-or-equal comparisons src/icp_scoring.py uses, so a score landing exactly on 70, 40 or 15 grades to A, B and C respectively rather than to the band below."
    - statement: "Dry-run payload output and run-report rows are emitted in the fixed order of the pinned id tuple, so two runs over the same input produce the same row order."
      verification: backstop
    - "The settle-and-assert wrapper raises on a wrong final value as well as on timeout, and the tier settle and the veto settle are two separate calls with separate timeouts."
  artifacts:
    - scripts/remediate_veto_companies.py
    - tests/test_remediate_veto_companies.py
  key_links:
    - "build_input_patch output -> HubSpot lv_country_region_normalized/lv_org_type/lv_produces_content -> the 5 component-score properties -> the lv_icp_fit_score calculated property -> WF1 (4625147345) -> lv_icp_tier"
    - "build_webhook_event + post_webhook_event -> POST {n8n_url}/webhook/hubspot/enrichment/event -> the n8n Decide Company Action node -> lv_anti_icp_flag / lv_anti_icp_reason"
    - "ProviderResult.evidence_by_field -> build_metadata_patch -> the seven lv_org_type_* / lv_produces_content_* source-metadata stamps"
    - "n8n_arming.arm_for_dispatch(TEST_RECORD_IDS = the 17 pinned ids) -> the Decide node's own _writeSafetyAllows() allowlist check"
  prohibitions:
    - "Never PATCH lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier — they are derived by the calculated property, WF1 and the n8n Decide node respectively."
    - "Never write a false value into lv_produces_content on absent evidence — false is a hard veto and manufactures the exact defect class this phase clears."
    - "Never touch Entain 10024564084, Gravity Media 15860277364 or Ironman 17317184159 — their non-ANZ regions are genuine."
    - "Never create a new HubSpot property of any kind — standing v0.9 milestone constraint."
    - "Claude never sets ALLOW_VETO_REMEDIATION, ALLOW_N8N_ARM or DRY_RUN=false — arming env vars are operator-only and per-shell."
    - "Never gate the disarm path on the arming env var — an operator must always be able to close the window."
    - "Never attempt to fix the scripts/run_scoring_parity.py population sweep — it is red by design from Phase 46 commit caae5d6 until Phase 49."
    - "Never edit any PORTAL-FACTS.md — read only."
---

<objective>
Build `scripts/remediate_veto_companies.py`: the single script that carries all four write legs
of this phase (web-research enrichment, input + metadata PATCH, component-score PATCH, and the
D-18 webhook POST that makes the n8n `Decide Company Action` node recompute the veto), plus its
offline test suite.

Purpose: every later plan in this phase is an invocation of this script. It must be
production-correct and fully verified offline before a single live byte is written, because the
live run happens once, inside an operator-armed window, against production CRM records.

Output: `scripts/remediate_veto_companies.py` and `tests/test_remediate_veto_companies.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-veto-remediation/47-CONTEXT.md
@.planning/phases/47-veto-remediation/47-RESEARCH.md
@.planning/phases/47-veto-remediation/47-PATTERNS.md
@.planning/phases/47-veto-remediation/47-VALIDATION.md
</context>

## The 17 pinned company ids (D-12 — literal, not discovered at execution time)

Enumerated from `46-SIMULATION-REPORT.md`'s rows flagged `blank_org_type, false_veto`;
independently re-counted as exactly 17 during planning.

| # | Name | HubSpot id | Live score/tier before |
|---|---|---|---|
| 1 | Tweed Valley Jockey Club | `9604732797` | 0/D |
| 2 | Sapphire Coast Turf Club (Bega Valley) | `9604794661` | 0/D |
| 3 | Port Macquarie Race Club | `9605273630` | 0/D |
| 4 | Rockhampton Jockey Club | `9604732795` | 10/D |
| 5 | Bunbury Turf Club | `9604738976` | 0/D |
| 6 | The Alice Springs Turf Club | `9604787229` | 10/D |
| 7 | Thoroughbred Park | `10152138518` | 10/D |
| 8 | Wyong | `10215097384` | 10/D |
| 9 | Coffs Harbour Racing Club | `14752488879` | 0/D |
| 10 | Editix | `17317381378` | 0/D |
| 11 | Jam TV | `17317850381` | 0/D |
| 12 | Pinjarra Park | `17696004613` | 0/D |
| 13 | Simtech LED | `18047161864` | 10/D |
| 14 | The Kalgoorlie-Boulder Racing Club | `18796602894` | 10/D |
| 15 | Newcastle Harness Racing Club | `19100977027` | 0/D |
| 16 | Waikato Racing Club Inc | `20538284384` | 0/D |
| 17 | The Rumble / Pacific Action Sports | `20943964946` | 10/D |

**Structurally excluded, never writable by this script:** Entain `10024564084`,
Gravity Media `15860277364`, Ironman `17317184159`.

<probe_coverage>
This phase has no SPEC file, so the deterministic edge probe stood in for `## Edge Coverage`. It
surfaced 8 items. All 8 are accounted for below — none dropped, none auto-dismissed.

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | VETO-01 | boundary | resolved (explicit) | Plan 01 truth: 17 proceeds at default, below-17 refuses, above-17 clamps |
| 2 | VETO-01 | adjacency | resolved (explicit) | Plan 01 truth: tier boundaries reuse the oracle's own comparisons, so 70/40/15 grade to A/B/C |
| 3 | VETO-01 | empty | resolved (explicit) | Plan 01 truth: no-evidence produces-content is omitted with a stated reason, never written false |
| 4 | VETO-01 | ordering | resolved (backstop) | Plan 01 truth carrying `verification: backstop` — pinned-tuple output order |
| 5 | VETO-01 | precision | resolved (explicit) | Plan 01 truth: integer components from the loaded config, sum equals the oracle exactly, no rounding step |
| 6 | VETO-02 | boundary | resolved (explicit) | Plan 01 truth: empty allowlist refused, allowlist is exactly the 17 |
| 7 | VETO-02 | precision | resolved (explicit) | Plan 01 truth: integer-only cap comparison, non-integer falls back rather than truncating |
| 8 | VETO-03 | unclassified | **unresolved — flagged** | Plan 04 `<flagged_assumptions>` item 1. VETO-03's bar is deliberately script-free and human-judged; there is no automated predicate to author, and a synthetic backstop would misrepresent that. Carried as the manual verification in Plan 04 Task 3. |

8 surfaced = 7 authored into `must_haves` + 1 flagged assumption.
</probe_coverage>

<assumption_delta_decision>
**Noun that is now primary:** *write surfaces* (plural). D-19 inverted D-11 — the phase arms
the batch-PATCH script's own env gate **and** the n8n workflow's
`ALLOW_HUBSPOT_RECORD_WRITES` + `TEST_RECORD_IDS` gate.

**Decision: promote.** Every arm/disarm/read-back structure in this phase is written for a set
of surfaces, not for one with a special case bolted on: the arming checkpoint hands the
operator two commands, the disarm task re-reads two states, and VETO-02's `must_haves` truth
is phrased over both. Rationale: `add-alongside` would leave the batch-PATCH gate as "the"
surface and n8n as an afterthought, which is exactly how D-11 got the premise wrong the first
time.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "remediate one pinned company" — one path only</name>
  <files>scripts/remediate_veto_companies.py, tests/test_remediate_veto_companies.py</files>
  <read_first>
    - scripts/backfill_seed_company_scores.py (the exact shape to mirror: module docstring stating what it must never write, EXPECTED_PORTAL_ID pin, two-key `_writes_allowed()`, `compute_components()` at ~93, `build_updates()` at ~120, `_chunked()`, `_settle()` at ~252, the dotenv-driver operator invocation in the docstring)
    - src/hubspot_client.py (get_record, batch_update_companies with its dry-run short-circuit and 100-entry ValueError, search_records)
    - src/web_research.py (claude_web_research, mock_claude_web_research, REQUIRED_FIELDS, the USE_MOCK_WEB_RESEARCH switch)
    - src/schemas.py (ProviderResult — note `evidence_by_field: Dict[str, str]` and `evidence: ProviderEvidence` with evidence_urls/evidence_summary)
    - src/icp_scoring.py (compute_icp_score signature including the cfg=None override; the hard_vetoes block; the tier boundary comparisons)
    - config/icp_scoring.yaml (the post-Phase-46 rubric of record; hard_vetoes reason strings)
    - config/field_policy.yaml (lv_org_type.require_evidence_url_for; lv_produces_content.require_evidence_url)
    - operator-claude-plugin/scripts/enrichment.py (ENRICHMENT_PATH, enrichment_target, dispatch_enrichment's X-Enrichment-Secret header)
    - operator-claude-plugin/scripts/dispatch.py (the `armed` parameter with NO default and NotArmedError)
    - operator-claude-plugin/scripts/config_gate.py (load_config, require_capability, the n8n_url/webhook_secret keys)
    - tests/test_backfill_seed_company_scores.py (the offline-test idiom: monkeypatched requests.post that raises, and the two existing never-write assertions at lines 169 and 234)
    - .planning/phases/47-veto-remediation/47-PATTERNS.md
  </read_first>
  <action>
Create `scripts/remediate_veto_companies.py` wiring ONE pinned company through EVERY layer this
phase touches, executed disarmed. No batching, no loop over 17 yet — one path, real error
handling on it.

Module docstring states, in the same voice as `backfill_seed_company_scores.py`'s, that this
script writes only `lv_org_type`, `lv_produces_content`, `lv_country_region_normalized`, the
seven source-metadata stamps for each of those it writes, and the five component-score
properties; and that the four derived fields owned by the calculated property, WF1 and the n8n
Decide node must never appear in a payload it builds (per D-07).

Module constants:
- `EXPECTED_PORTAL_ID = "22617666"`.
- `PINNED_COMPANY_ID_ORDER` — a tuple of the 17 id strings in the table order above; and
  `PINNED_COMPANY_IDS = frozenset(PINNED_COMPANY_ID_ORDER)`.
- `EXCLUDED_COMPANY_IDS = frozenset({"10024564084", "15860277364", "17317184159"})` with an
  inline note naming Entain, Gravity Media and Ironman as verified-correct non-ANZ records.
- `FORBIDDEN_PROPS = frozenset({"lv_icp_fit_score", "lv_icp_tier", "lv_anti_icp_flag", "lv_anti_icp_reason"})`.
- `INPUT_PROPS = ("lv_org_type", "lv_produces_content", "lv_country_region_normalized")`.
- `METADATA_SUFFIXES = ("_source", "_confidence", "_evidence_url", "_evidence_summary", "_verified_at", "_verified_by_model", "_validation_status")`.
- `EVIDENCE_REQUIRED_ORG_TYPES = ("governing_body_league", "content_producer", "hardware_vendor", "gambling_operator")` — sourced from `config/field_policy.yaml`'s `lv_org_type.require_evidence_url_for`, not hand-copied blind.
- `DEFAULT_MAX_RECORDS = 17` and `HARD_CEILING_RECORDS = 17`, read through
  `VETO_MAX_RECORDS`. Deliberately NOT `BACKFILL_MAX_RECORDS` and NOT that script's default
  of 10, which would refuse 17 (research §"Script surface corrections", Pitfall 3). The
  ceiling equals the pinned-set size so the env var can only ever lower the cap.
- `N8N_EXECUTION_BUDGET_MONTH = 2500`.

Functions (this task implements the single-record path through each):
- `resolve_pinned_ids(requested)` — raises `PinRefused` naming the offending id if any requested
  id is absent from `PINNED_COMPANY_IDS`; returns the accepted ids sorted into
  `PINNED_COMPANY_ID_ORDER` order so output ordering is deterministic. Per D-12 this is the
  membership refusal the backfill script does not have.
- `_resolved_max_records()`, `enforce_sample_cap(ids)`, `_has_credentials()`, `_portal_ok()`.
- `_writes_allowed()` — two-key: `DRY_RUN` not "true" AND `ALLOW_VETO_REMEDIATION` == "true".
- `research_company(record)` — delegates to `src.web_research.claude_web_research`; returns the
  `ProviderResult` unchanged. Do not re-implement the prompt.
- `build_input_patch(company_id, result)` — returns `{"id": ..., "properties": {...}}` carrying
  only those of `INPUT_PROPS` the research actually established. Rules, per D-14: emit
  `lv_produces_content` only when the research value is a real boolean AND an evidence URL is
  present for it in `result.evidence_by_field`; otherwise omit the key entirely. Emit
  `lv_org_type` only when non-empty and not "unknown", and when it is one of
  `EVIDENCE_REQUIRED_ORG_TYPES` only if an evidence URL is present for it. Emit
  `lv_country_region_normalized` only when the research value is one of AU/NZ/ANZ/Other.
  Booleans are emitted as the lowercase strings HubSpot booleancheckbox properties store, not
  as JSON booleans. Assert no key is in `FORBIDDEN_PROPS` before returning.
- `unresolved_reasons(company_id, result)` — returns a field-keyed dict of short prose reasons
  for every one of `INPUT_PROPS` the patch omitted, so an unresolved record is distinguishable
  from one never attempted (COVER-01, D-14).
- `build_metadata_patch(company_id, result, written_fields)` — for each field actually written,
  the seven `METADATA_SUFFIXES` stamps: `_source` = the ProviderResult provider value,
  `_confidence` = its confidence, `_evidence_url` from `result.evidence_by_field` for that
  field falling back to the first entry of `result.evidence.evidence_urls`, `_evidence_summary`
  from `result.evidence.evidence_summary`, `_verified_at` an ISO-8601 UTC timestamp,
  `_verified_by_model` the `ANTHROPIC_RESEARCH_MODEL` value actually used, `_validation_status`
  the literal `web_researched`. Assert no key is in `FORBIDDEN_PROPS`.
- `build_component_patch(company_id, props)` — imports and calls
  `scripts.backfill_seed_company_scores.compute_components`; never re-implements the point
  table. Assert no key is in `FORBIDDEN_PROPS`.
- `expected_score_and_tier(props)` — calls `src.icp_scoring.compute_icp_score` on the same
  post-write inputs and returns the oracle's score and tier, for the settle assertion to
  compare against. Use the oracle's own boundaries; do not restate 70/40/15 as local literals.
- `build_webhook_event(company_id, property_name)` — returns the raw HubSpot-shaped
  property-change event ARRAY D-18 specifies: one object carrying `objectId` (string),
  `objectType` `"company"`, `subscriptionType` `"company.propertyChange"`, `propertyName`
  (default `"lv_country_region_normalized"`), and `occurredAt` as epoch milliseconds. This
  builder has no analog in the repo (47-PATTERNS.md "No Analog Found") — write it small and
  local.
- `post_webhook_event(company_id, armed, config, transport=requests)` — `armed` has NO default,
  mirroring `dispatch.py`; raises `NotArmedError` when falsy before any network call. Target is
  `config_gate`-resolved `n8n_url` joined with `webhook/hubspot/enrichment/event`; header
  `X-Enrichment-Secret` from `config["webhook_secret"]`. Never print the secret or the token.
- `main(argv)` — argparse with `--company-id` (repeatable, matching the backfill script's flag)
  and `--ids` (comma-separated alias, matching `june_run_arm.py`'s flag), plus `--report` for
  the JSON run-report path. **With neither flag given, the script operates on the whole of
  `PINNED_COMPANY_ID_ORDER`.** There is no search-based selection path at all — unlike the
  backfill analog, which falls back to a `search_records` sweep when no ids are supplied. That
  fallback is exactly the runtime-discovery D-12 forbids, so it is deliberately absent here and
  the id set can only ever be the pinned 17 or a subset of them. Order: credentials check,
  portal pin, `resolve_pinned_ids`,
  `enforce_sample_cap`, fetch, research, build the three patches, print each payload as indented
  JSON, print the webhook event body, then branch on `_writes_allowed()`. Disarmed: print the
  arming instruction naming `DRY_RUN=false` and `ALLOW_VETO_REMEDIATION=true` and return 0
  without any write. Armed: perform the writes and settles (Task 2 fills the settle logic; wire
  the call sites now).

Then add `tests/test_remediate_veto_companies.py` with a tracer test that drives `main()` for a
single pinned id — `9604732797` — with `USE_MOCK_WEB_RESEARCH` forced true, `DRY_RUN` unset,
credentials and portal id monkeypatched, and `requests.post`/`requests.get` monkeypatched to
raise if called, asserting the run exits 0, prints all three payloads plus the webhook event
array, and performs no network call.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -k tracer -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -k tracer -x` exits 0.
    - The tracer test asserts stdout contains all of `lv_country_region_normalized`, `org_type_score`, `lv_org_type_validation_status` and `objectId`, proving all four payload shapes were printed on one path.
    - The tracer test monkeypatches both `requests.post` and `requests.get` to raise `AssertionError`, and the run still exits 0 — proving zero network calls in the disarmed path.
    - `.venv/bin/python -c "import scripts.remediate_veto_companies as m; assert len(m.PINNED_COMPANY_ID_ORDER) == 17 and len(m.PINNED_COMPANY_IDS) == 17"` exits 0.
    - `.venv/bin/python -c "import scripts.remediate_veto_companies as m; assert m.PINNED_COMPANY_IDS.isdisjoint(m.EXCLUDED_COMPANY_IDS)"` exits 0.
    - `.venv/bin/python -c "import scripts.remediate_veto_companies as m; assert m._resolved_max_records() == 17"` exits 0 with `VETO_MAX_RECORDS` unset.
    - A test invokes `main([])` with no id flags and asserts the resolved id set equals `PINNED_COMPANY_ID_ORDER`; a second asserts the module contains no call to `search_records`, checked structurally with `ast` rather than by substring.
  </acceptance_criteria>
  <done>One pinned company runs the whole path disarmed, prints every payload it would send, makes no network call, and the tracer test proves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settle-and-assert wrappers — two mechanisms, two timeouts, loud failure</name>
  <files>scripts/remediate_veto_companies.py, tests/test_remediate_veto_companies.py</files>
  <read_first>
    - scripts/backfill_seed_company_scores.py lines 244-272 (`_settle` — the stability poll with NO expected-value assertion, and its `main()` call site that ignores the printed value)
    - tests/scoring_fixtures.py (`settle()` — identical poll shape, returns `(value, elapsed)` instead of printing)
    - tests/test_scoring_parity.py lines 441-466 (`test_veto_clear_after_correction` — note it settles `lv_anti_icp_flag` with `timeout=900, interval=15`, the measured n8n-dependent window)
    - .planning/phases/47-veto-remediation/47-VALIDATION.md §"Two settle loops, never one"
    - .planning/phases/47-veto-remediation/47-RESEARCH.md §"The derived-field chain" and §"What settle observably looks like"
    - scripts/remediate_veto_companies.py (the call sites wired in Task 1)
  </read_first>
  <behavior>
    - A poll whose final observed value equals `expected` returns the value and elapsed seconds.
    - A poll that stabilises on a value other than `expected` raises `SettleFailed` naming the company id, property, observed value and expected value — a wrong-but-stable value is a failure, not a success.
    - A poll that never stabilises before `timeout` raises `SettleFailed` naming the timeout.
    - `settle_tier` polls `lv_icp_tier` with defaults timeout 120 / interval 5.
    - `settle_veto` polls `lv_anti_icp_flag` with defaults timeout 900 / interval 15, and accepts an `expected` of `"false"` or a predicate allowing any value whose accompanying `lv_anti_icp_reason` does not contain the non-ANZ reason string.
    - The two are separate functions with separate defaults and are never invoked through a shared loop.
  </behavior>
  <action>
Add to `scripts/remediate_veto_companies.py`:

- `class SettleFailed(RuntimeError)`.
- `settle_and_assert(company_id, prop, expected, timeout, interval, reader=get_record)` — the
  poll body from `_settle` (two consecutive identical reads = stable) but raising `SettleFailed`
  on timeout AND on a stable-but-unexpected final value. `expected` accepts either a literal or
  a single-argument callable predicate. Injectable `reader` so the offline tests need no
  network and no real sleeping — also take an injectable `sleeper` defaulting to `time.sleep`.
  This is the D-10 "fail loudly" wrapper; research confirmed neither `_settle` nor
  `tests/scoring_fixtures.py::settle` asserts anything, so this is new logic, not reuse.
- `settle_tier(company_id, expected_tier, timeout=120, interval=5)` — the pure-HubSpot chain
  (component PATCH → `lv_icp_fit_score` calculated property → WF1 → `lv_icp_tier`), whose
  measured latency is seconds.
- `settle_veto(company_id, timeout=900, interval=15)` — the n8n-dependent chain, which only
  moves after the D-18 webhook POST reaches the `Decide Company Action` node. Its predicate is
  the D-16 phase gate: pass when `lv_anti_icp_flag` is not the string "true", OR when it is
  "true" but the accompanying `lv_anti_icp_reason` does not contain the non-ANZ hard-veto reason
  string read from `config/icp_scoring.yaml`'s `hard_vetoes.non_anz.reason` — a legitimately
  revealed different veto (Simtech LED as a hardware vendor is the expected case) is a correct
  outcome, not a failure.

Wire both into `main()`'s armed branch at the call sites Task 1 left: `settle_tier` immediately
after the component PATCH, `settle_veto` only after the webhook POST for that record. Never one
loop, never one timeout.

Write the tests first: a fake reader returning a scripted sequence of values and a no-op
sleeper, covering the five behaviours above plus a case proving `settle_tier` and `settle_veto`
carry different default timeouts.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -k settle -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -k settle -x` exits 0 with at least 5 tests collected.
    - A test asserts `pytest.raises(SettleFailed)` when the fake reader stabilises on a value different from `expected`, and the raised message contains both the observed and the expected value.
    - A test asserts `pytest.raises(SettleFailed)` when the fake reader never stabilises before the timeout.
    - `.venv/bin/python -c "import inspect, scripts.remediate_veto_companies as m; assert inspect.signature(m.settle_tier).parameters['timeout'].default == 120 and inspect.signature(m.settle_veto).parameters['timeout'].default == 900"` exits 0.
    - A test asserts `settle_veto` passes for a record reading flag "true" with a reason that is a genuine hardware-vendor veto, and fails for one whose reason still contains the non-ANZ reason string.
    - The whole test suite runs with no real sleeping: `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -k settle --durations=0` reports the slowest settle test under 1s.
  </acceptance_criteria>
  <done>Two independent settle mechanisms exist with independent timeouts, both raise on a wrong stable value as well as on timeout, and the offline tests prove it without touching the network or the clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard suite — pin, cap, never-write, budget refusal, D-20 clobber verify, full 17-record loop</name>
  <files>scripts/remediate_veto_companies.py, tests/test_remediate_veto_companies.py</files>
  <read_first>
    - tests/test_backfill_seed_company_scores.py lines 160-260 (`test_backfill_never_writes_derived_output_properties`, `test_backfill_sample_cap_refuses_oversized_sample`, `test_backfill_refuses_to_write_without_both_arming_keys`, `test_backfill_portal_guard_refuses_wrong_portal`, `test_backfill_build_updates_payload_never_contains_derived_fields` — these are the REAL never-write guards; research confirmed "T-40-22" is a Phase 40 plan-task label, not a test identifier)
    - scripts/remediate_veto_companies.py (everything Tasks 1 and 2 added)
    - .planning/phases/47-veto-remediation/47-RESEARCH.md §"Cost estimate inputs" (the $0.0686/record Phase 20 canary figure and why it undercounts; the `Research Trigger Gate` `needsResearch()` predicate and `EVIDENCE_GATED_ORG_TYPES`)
    - operator-claude-plugin/scripts/n8n_arming.py (arm_for_dispatch's empty-allowlist REFUSED branch; disarm's independent re-read; OVERLAY_DISABLED_LITERALS)
    - .planning/phases/47-veto-remediation/47-CONTEXT.md D-20
  </read_first>
  <behavior>
    - `resolve_pinned_ids(["10024564084"])` raises `PinRefused` naming Entain's id; the same for Gravity Media and Ironman and for any arbitrary unpinned id.
    - `resolve_pinned_ids` over all 17 in shuffled order returns them in `PINNED_COMPANY_ID_ORDER` order.
    - No dict returned by `build_input_patch`, `build_metadata_patch` or `build_component_patch` has any key in `FORBIDDEN_PROPS`, for every research fixture shape tried.
    - A research result with no evidence URL for produces-content omits `lv_produces_content` and records a reason; it never emits the false string.
    - `enforce_sample_cap` accepts 17 at the default; refuses 17 when `VETO_MAX_RECORDS` is 5; clamps a `VETO_MAX_RECORDS` of 25 down to 17; falls back to 17 on a non-integer value.
    - `_writes_allowed()` is false unless BOTH `DRY_RUN=false` and `ALLOW_VETO_REMEDIATION=true`.
    - `estimate_cost(17)` reports research calls, redundant second-pass research calls and n8n executions; `refuse_if_over_budget` raises when projected n8n executions exceed `N8N_EXECUTION_BUDGET_MONTH`, and never silently truncates the id list.
    - `verify_post_run(company_id, expected_inputs, expected_metadata, reader)` returns the set of fields whose live value diverges from what this script wrote.
  </behavior>
  <action>
Complete `scripts/remediate_veto_companies.py`:

- Extend `main()` from the tracer's single record to the full pinned loop, preserving
  `PINNED_COMPANY_ID_ORDER` ordering. Per-record armed sequence, all inside the one window
  (D-01's touch-once principle): batch-PATCH inputs + metadata → batch-PATCH components →
  `settle_tier` → `post_webhook_event` → `settle_veto` → `verify_post_run` → conditional
  re-stamp → next record. The D-20 verify and re-stamp run INSIDE the armed window; discovering
  a clobber after disarm would need a second arming ceremony, which is precisely the
  twice-touched cost D-01 exists to avoid.
- `estimate_cost(n_records)` returning a dict with `web_research_calls` = n_records,
  `redundant_research_calls` = the count of records whose researched `lv_org_type` falls in
  `EVIDENCE_REQUIRED_ORG_TYPES` (D-20 — the deployed `Research Trigger Gate`'s `needsResearch()`
  fires on org-type membership alone, not on evidence presence, and `ALLOW_WEB_RESEARCH` is a
  build-time literal so a payload flag cannot suppress it), `n8n_executions` = n_records,
  `n8n_budget_month` = 2500, `lusha_credits` = 0 with a note that D-08 draws no provider credits
  at all, and `anthropic_estimate_usd` derived from the Phase 20 canary figure of $0.0686 per
  record with an explicit note that the figure was measured on the n8n Haiku-plus-Sonnet path
  and excludes the native web_search per-search billing this path incurs.
- `refuse_if_over_budget(estimate)` raising `BudgetRefused` when `n8n_executions` exceeds
  `n8n_budget_month`. There is no n8n usage endpoint (project memory `n8n-execution-budget`), so
  this asserts the static projected count only; month-to-date headroom is the operator's
  confirmation at the arming checkpoint, and the estimate prints that explicitly rather than
  implying a live balance check.
- `verify_post_run(company_id, expected_inputs, expected_metadata, reader=get_record)` — D-20's
  clobber check. Re-reads the three input properties AND the metadata stamps for every field
  written, and returns the diverging field names. Extend it beyond metadata to the input fields
  themselves: project memory `companies-research-lane-rowloss` records a suspected latent
  row-loss when the research lane runs, and the roughly four evidence-gated records (Simtech
  LED, Editix, Jam TV, The Rumble / Pacific Action Sports) are exactly the ones that lane will
  re-enter. If anything diverged, re-PATCH the affected fields once and re-verify; if it
  diverges again, raise rather than continuing silently.

Add the guard tests covering every behaviour above, following the offline idiom in
`tests/test_backfill_seed_company_scores.py` — monkeypatched `requests.post`/`requests.get` that
raise, and pure-function assertions elsewhere. Name at least one test with `never_writes` in its
identifier so the `-k never_write` selector in 47-VALIDATION.md resolves against this file too.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -x && .venv/bin/python -m pytest -k never_write -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -x` exits 0.
    - `.venv/bin/python -m pytest -k never_write -x` collects at least 3 tests across `tests/test_backfill_seed_company_scores.py` and `tests/test_remediate_veto_companies.py`, and exits 0.
    - A test asserts `pytest.raises(PinRefused)` for each of `10024564084`, `15860277364`, `17317184159`, and the raised message names the offending id.
    - A test builds all three payload types from at least 4 different `ProviderResult` fixtures and asserts, for every resulting properties dict, that its key set is disjoint from `FORBIDDEN_PROPS`.
    - A test asserts that a `ProviderResult` with `lv_produces_content` false and an empty `evidence_by_field` yields an input patch with no `lv_produces_content` key at all, and an `unresolved_reasons` entry for that field.
    - A test asserts `enforce_sample_cap` over all 17 is True at the default, False with `VETO_MAX_RECORDS=5`, and that `_resolved_max_records()` returns 17 for `VETO_MAX_RECORDS` values of `25` and `not-a-number`.
    - A test asserts `_writes_allowed()` is False for each of the three env combinations that are not both-keys-set.
    - A test asserts `refuse_if_over_budget` raises `BudgetRefused` for a projected execution count above 2500 and that the id list passed in is returned unmodified (never truncated) when it does not raise.
    - A test drives `verify_post_run` with a fake reader whose second read has lost a metadata stamp, and asserts the diverging field name is returned.
    - `.venv/bin/python -m pytest tests/ -x -q` exits 0 apart from the known live-integration selectors that require credentials.
  </acceptance_criteria>
  <done>The script handles all 17 pinned records in one armed window with pin, cap, arm, budget and clobber guards, and the offline suite proves every guard fires without a single network call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → HubSpot CRM v3 | Production CRM records; a malformed or over-broad payload mutates real sales data |
| script → n8n Cloud webhook | Shared-secret-authenticated endpoint that triggers a workflow with its own write authority |
| shell env → script | Arming flags, HubSpot token, Anthropic key and n8n webhook secret all arrive as ambient env/config |
| Anthropic web_search → script | Untrusted web content is summarised into values that get written to CRM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | `build_*_patch` payload construction | high | mitigate | `FORBIDDEN_PROPS` assertion inside every builder plus offline tests over 4+ fixture shapes; the derived-field owners stay sole producers |
| T-47-02 | Elevation of Privilege | `main()` write branch | high | mitigate | Two-key `_writes_allowed()` (`DRY_RUN=false` AND `ALLOW_VETO_REMEDIATION=true`), both operator-only and per-shell; dry-run is the default |
| T-47-03 | Tampering | id selection | high | mitigate | `resolve_pinned_ids` refuses membership outside the 17 before any call; `EXCLUDED_COMPANY_IDS` asserted disjoint at import |
| T-47-04 | Information Disclosure | webhook POST and HubSpot client | high | mitigate | Secret carried only in the `X-Enrichment-Secret` header and the token only in `hs_headers()`; no payload print path includes headers, asserted by the existing capsys assertions in the backfill tests and mirrored here |
| T-47-05 | Denial of Service | record cap | medium | mitigate | `HARD_CEILING_RECORDS` equals the pinned-set size so `VETO_MAX_RECORDS` can only lower it; `refuse_if_over_budget` refuses rather than truncates |
| T-47-06 | Spoofing | Anthropic web_search results | medium | mitigate | Evidence URL required before writing any evidence-gated org type or produces-content value; `_validation_status` records provenance; D-14 prefers unknown over a guessed hard veto |
| T-47-07 | Repudiation | post-run state | medium | mitigate | Per-record run report with before/after values, source metadata stamps carrying `_verified_at` and `_verified_by_model`, and `verify_post_run` recording divergence |
| T-47-08 | Tampering | n8n re-research lane overwriting stamps | medium | mitigate | `verify_post_run` re-reads inputs AND metadata after the webhook leg and re-stamps once inside the same armed window (D-20) |
| T-47-SC | Tampering | package installs | low | accept | No npm/pip/cargo install occurs in this phase — every dependency (`anthropic`, `requests`, `pydantic`, `PyYAML`) is already in `requirements.txt` and installed; the package-legitimacy gate has nothing to audit |

**ASVS level:** L1. **Blocking threshold:** `high`. All four `high` threats are dispositioned
`mitigate` with a concrete, tested mitigation.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_remediate_veto_companies.py -x` — full new suite green.
- `.venv/bin/python -m pytest -k never_write -x` — the derived-field guard resolves across both
  the backfill and the new test module.
- `.venv/bin/python -m pytest tests/test_backfill_seed_company_scores.py -x` — the reused
  `compute_components` import did not disturb its owner's suite.
- Zero network calls anywhere in the new suite (every test monkeypatches `requests` to raise).
- `scripts/run_scoring_parity.py`'s population sweep stays red — expected, Phase 46 commit
  `caae5d6`, closed by Phase 49. Do not touch it.
</verification>

<success_criteria>
- `scripts/remediate_veto_companies.py` exists and carries all four write legs plus pin, cap,
  arm, budget, settle-assert and clobber-verify logic.
- `tests/test_remediate_veto_companies.py` exists and is green offline.
- The 17 pinned ids and the 3 excluded ids are literals in the script, not runtime discoveries.
- No code path in the script can produce a payload containing a derived field.
</success_criteria>

## Artifacts this phase produces (Plan 01 contribution)

**New files**
- `scripts/remediate_veto_companies.py`
- `tests/test_remediate_veto_companies.py`

**New symbols in `scripts/remediate_veto_companies.py`**
- Constants: `EXPECTED_PORTAL_ID`, `PINNED_COMPANY_ID_ORDER`, `PINNED_COMPANY_IDS`,
  `EXCLUDED_COMPANY_IDS`, `FORBIDDEN_PROPS`, `INPUT_PROPS`, `METADATA_SUFFIXES`,
  `EVIDENCE_REQUIRED_ORG_TYPES`, `DEFAULT_MAX_RECORDS`, `HARD_CEILING_RECORDS`,
  `N8N_EXECUTION_BUDGET_MONTH`
- Exceptions: `PinRefused`, `SettleFailed`, `BudgetRefused`, `NotArmedError`
- Functions: `resolve_pinned_ids`, `_resolved_max_records`, `enforce_sample_cap`,
  `_has_credentials`, `_portal_ok`, `_writes_allowed`, `research_company`, `build_input_patch`,
  `unresolved_reasons`, `build_metadata_patch`, `build_component_patch`,
  `expected_score_and_tier`, `build_webhook_event`, `post_webhook_event`, `settle_and_assert`,
  `settle_tier`, `settle_veto`, `estimate_cost`, `refuse_if_over_budget`, `verify_post_run`,
  `main`

**New CLI flags**
- `--company-id` (repeatable), `--ids` (comma-separated), `--report <path>`

**New env var names**
- `ALLOW_VETO_REMEDIATION` (operator-only, per-shell), `VETO_MAX_RECORDS`

**New test names**
- `tests/test_remediate_veto_companies.py` — tracer test, five settle tests, and the guard suite
  including at least one identifier containing `never_writes`

<output>
Create `.planning/phases/47-veto-remediation/47-01-SUMMARY.md` when done
</output>
