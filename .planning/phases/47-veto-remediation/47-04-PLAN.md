---
phase: 47-veto-remediation
plan: 04
type: execute
wave: 3
depends_on: ["47-03"]
files_modified:
  - .planning/phases/47-veto-remediation/47-AFTER.json
  - .planning/phases/47-veto-remediation/47-RUN-REPORT.md
  - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md
  - .planning/REQUIREMENTS.md
  - tests/test_scoring_parity.py
autonomous: false
requirements: [VETO-01, VETO-02, VETO-03, COVER-01, COVER-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "All 17 pinned companies were written inside ONE armed window that armed two surfaces — the remediation script's own operator-only env gate and the n8n workflow's ALLOW_HUBSPOT_RECORD_WRITES plus a TEST_RECORD_IDS allowlist set to exactly the 17 pinned ids."
    - "Both write surfaces are disarmed afterward and both disarmed states are independently re-read and confirmed."
    - "Every one of the 17 reads back with lv_anti_icp_flag not equal to true, OR with a different, correct veto reason that is not the non-ANZ hard-veto reason."
    - "An after-snapshot exists and a committed diff against the before-snapshot shows, per id, what changed and how the record classifies."
    - "A HubSpot search on lv_anti_icp_reason containing the non-ANZ reason ANDed with lv_country_region_normalized is unknown returns zero results, run by the operator in the portal with no script."
    - "Actual costs are reported against the pre-run estimate: research calls made, redundant second-pass research calls observed, n8n executions consumed, and provider credits at zero."
    - "The D-20 evidence-metadata verification ran inside the armed window and any clobbered stamp was re-stamped there, not in a second window."
    - "Records legitimately remaining Tier D after remediation are recorded as correct outcomes with their revealed veto reason, not as failures."
    - "D-11: both arming gates follow the established ALLOW_N8N_ARM / ALLOW_HUBSPOT_FLOW_WRITE pattern — operator-only, per-shell, never set by Claude — and neither disarm path is gated on its arming variable, so the operator can always shut the window. D-19 widens D-11 from one armed surface to two; D-11's operator-only and ungated-disarm rules are unchanged and apply to both."
    - "D-15: for every record whose non-ANZ veto was false, lv_anti_icp_flag reads back false and lv_anti_icp_reason reads back empty — regardless of whether that record reaches a real tier. Both are verified as derived read-backs after the webhook POST settles; neither is ever written by this phase (D-07)."
  artifacts:
    - .planning/phases/47-veto-remediation/47-AFTER.json
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md
  key_links:
    - "operator's armed shell -> ALLOW_VETO_REMEDIATION + DRY_RUN=false -> the remediation script's own write gate"
    - "june_run_arm.py --ids <17> -> n8n_arming.arm_for_dispatch -> ALLOW_HUBSPOT_RECORD_WRITES + TEST_RECORD_IDS -> the Decide Company Action node's own allowlist check"
    - "the D-18 webhook POST -> the Decide Company Action node -> lv_anti_icp_flag / lv_anti_icp_reason"
    - "june_run_arm.py --disarm -> n8n_arming.disarm's independent re-read -> the confirmed-closed window"
  prohibitions:
    - "Never PATCH lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier."
    - "Never write a false value into lv_produces_content on absent evidence."
    - "Never touch Entain 10024564084, Gravity Media 15860277364 or Ironman 17317184159."
    - "Never create a new HubSpot property of any kind."
    - "Claude never sets ALLOW_VETO_REMEDIATION, ALLOW_N8N_ARM or DRY_RUN=false — arming env vars are operator-only and per-shell."
    - "Never gate the disarm path on the arming env var."
    - "Never attempt to fix the scripts/run_scoring_parity.py population sweep."
    - "Never edit any PORTAL-FACTS.md — read only."
---

<objective>
Open the write window once, write all 17 records through both legs inside it, close it, prove it
closed, and prove from HubSpot alone that the false non-ANZ veto is gone.

Purpose: this is the phase. Everything before it existed to make this one window short, bounded,
predicted, and verifiable after the fact.

Output: `47-AFTER.json`, `47-RUN-REPORT.md`, filled actuals in `47-COST-ESTIMATE.md`, and the
VETO requirement rows ticked with test pointers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-veto-remediation/47-CONTEXT.md
@.planning/phases/47-veto-remediation/47-RESEARCH.md
@.planning/phases/47-veto-remediation/47-VALIDATION.md
@.planning/phases/47-veto-remediation/47-01-SUMMARY.md
@.planning/phases/47-veto-remediation/47-03-SUMMARY.md
@.planning/phases/47-veto-remediation/47-DRYRUN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator arms both write surfaces, runs the armed remediation, and reports back</name>
  <reversibility rating="costly">Implements D-01's touch-once merge and D-11/D-19's two-surface gate. Undo means re-opening a second window against records already written and re-spending the n8n executions — flagged, not blocked, because the pre-arm dry-run and per-record prediction in 47-DRYRUN.md make the outcome known before the window opens.</reversibility>
  <read_first>
    - .planning/phases/47-veto-remediation/47-DRYRUN.md (the exact payloads, exact webhook bodies and per-record predicted outcome — reviewed BEFORE arming)
    - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md (the projection the operator confirms month headroom against)
    - scripts/june_run_arm.py (arm and disarm modes, and the note that disarm is deliberately ungated)
    - scripts/remediate_veto_companies.py (the two-key write gate, --from-cache, --report)
  </read_first>
  <what-built>
`scripts/remediate_veto_companies.py` and `scripts/veto_remediation_report.py`, both green
offline; a live before-snapshot of all 17 records; a cached live web-research result for all 17;
a live property-existence check confirming every property to be written exists in the portal; and
a disarmed dry-run printing the exact PATCH payloads and exact webhook POST bodies with a
predicted score and tier per record.

Nothing has been written to HubSpot. Both write surfaces are closed.
  </what-built>
  <action>
Halt and hand the operator the two commands below verbatim. Claude does not run either of them:
`ALLOW_N8N_ARM`, `ALLOW_VETO_REMEDIATION` and `DRY_RUN=false` are operator-only, per-shell, and
never set by Claude — a standing project rule, not a preference. Wait for the operator's pasted
stdout and exit code before proceeding to Task 2. Do not disarm here; disarming is Task 2's first
action.
  </action>
  <how-to-verify>
**Step 0 — review before arming.** Open `.planning/phases/47-veto-remediation/47-DRYRUN.md` and
read the per-record pre-arm table. Confirm the predicted classifications look right — in
particular that Simtech LED (`18047161864`) is predicted `hardware_vendor`, which is a genuine
hard veto and a correct outcome (D-16), and that Waikato Racing Club Inc (`20538284384`) is
predicted `NZ` rather than `AU`. Then confirm from `47-COST-ESTIMATE.md` that this month's n8n
execution headroom comfortably absorbs 17 more executions against the 2,500 allowance — there is
no usage endpoint on this plan, so this is your judgement call, not an API read.

If anything in that table looks wrong, stop here and say so. Nothing has been written yet.

**Step 1 — arm surface one: the n8n workflow.** `ALLOW_N8N_ARM` is operator-only and per-shell;
Claude never sets it.

! ALLOW_N8N_ARM=true .venv/bin/python scripts/june_run_arm.py --ids 9604732797,9604794661,9605273630,9604732795,9604738976,9604787229,10152138518,10215097384,14752488879,17317381378,17317850381,17696004613,18047161864,18796602894,19100977027,20538284384,20943964946

Expect an `armed` outcome with `record_ids` listing exactly those 17 ids. An empty allowlist is
refused by design: an empty allowlist denies every write and would read as a successful arm.

**Step 2 — arm surface two and run.** `ALLOW_VETO_REMEDIATION` plus `DRY_RUN=false` are the
remediation script's own two-key gate, also operator-only and per-shell. `.env` is Read and Bash
permission-blocked in this repo, which is why the dotenv driver form is used.

! ALLOW_VETO_REMEDIATION=true DRY_RUN=false .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/remediate_veto_companies.py', run_name='__main__')" --from-cache .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json --report .planning/phases/47-veto-remediation/47-RUN-LOG.json

Per record the script will: batch-PATCH the researched inputs and their source metadata,
batch-PATCH the five component scores, settle the tier against its predicted value, POST the
HubSpot-shaped property-change event to the n8n enrichment webhook, settle the veto flag against
the D-16 predicate, re-verify that the evidence metadata and input fields survived the n8n
research lane, and re-stamp once if anything was clobbered. It raises loudly and stops on any
record that settles to the wrong value or never settles.

**Step 3 — report back.** Paste the run's stdout tail, its exit code, and the path of
`47-RUN-LOG.json`. Do NOT disarm yourself — Claude runs the disarm and the independent read-back
in the next task, because the disarm path is deliberately ungated precisely so it can always be
closed by whoever is holding the session.
  </how-to-verify>
  <acceptance_criteria>
    - The n8n arm command returned an `armed` outcome with exactly 17 entries in `record_ids`.
    - The armed remediation run exited 0, or exited non-zero naming the record and property whose settle failed — either is a reportable outcome; a silent partial success is not.
    - `.planning/phases/47-veto-remediation/47-RUN-LOG.json` exists and carries one entry per attempted record.
    - The operator confirmed the pre-arm table before arming, and confirmed n8n month headroom against the 2,500 allowance.
  </acceptance_criteria>
  <done>Both write surfaces were armed with exactly the 17 pinned ids, the armed remediation run completed and its outcome was reported, and the window is still open awaiting Task 2's disarm.</done>
  <resume-signal>Paste the stdout tail and exit code of both commands, or describe what stopped you.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Disarm both surfaces, read back, snapshot after, and write the run report with actuals</name>
  <files>.planning/phases/47-veto-remediation/47-AFTER.json, .planning/phases/47-veto-remediation/47-RUN-REPORT.md, .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md</files>
  <precondition>Task 1's armed run has completed and its outcome has been reported; the operator's arming variables are not exported into this shell.</precondition>
  <read_first>
    - scripts/june_run_arm.py lines 80-98 (the disarm function takes no arming-env check at all, only arm does — this is why Claude can and must run it)
    - operator-claude-plugin/scripts/n8n_arming.py lines 366-415 (disarm proves closure by an independent re-read and returns a disarm-failed outcome carrying "LIVE WRITES MAY STILL BE ENABLED" if the re-read disagrees) and lines 46-58 (OVERLAY_DISABLED_LITERALS — the expected at-rest values)
    - scripts/veto_remediation_report.py (after mode, diff mode, classify)
    - .planning/phases/47-veto-remediation/47-BEFORE.json
    - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md (the row labels the actuals table must match)
    - .planning/phases/47-veto-remediation/47-RUN-LOG.json (Task 1's per-record output)
  </read_first>
  <action>
Close the window first, before anything else. Order matters: an open window is the risk; the
report can wait.

1. Disarm surface one by running the disarm mode of `scripts/june_run_arm.py`. That path is
   deliberately not gated on the arming env var — a kill switch that blocked disarming would
   strand an armed backend, the exact failure the whole ceremony exists to prevent. The library's
   disarm already proves closure by an independent re-read; capture its full JSON outcome
   verbatim. If it reports a disarm failure, stop immediately and escalate to the operator rather
   than continuing to the report.
2. Confirm surface two is closed: the remediation script's two keys are per-shell and were only
   ever set in the operator's shell. Run the script's own gate check in this shell and assert it
   reports dry-run, and record both variables as unset here.
3. Take the after-snapshot and the diff using `scripts/veto_remediation_report.py`, writing
   `47-AFTER.json` and diffing it against `47-BEFORE.json`.
4. Write `.planning/phases/47-veto-remediation/47-RUN-REPORT.md` carrying:
   - Both disarm outcomes verbatim, including the independent re-read values, so VETO-02's
     "disarmed and read back" bar is evidenced rather than asserted.
   - A per-id table: name, before score/tier/flag/reason, after score/tier/flag/reason, and the
     cleared / residual-other-veto / still-non-ANZ classification. Any still-non-ANZ row is a
     phase failure and must be called out as one, not softened.
   - A prose paragraph naming every record that legitimately remains Tier D and the different,
     correct veto reason it now carries — D-16's outcome recorded as correct rather than as a
     failure.
   - Every record whose produces-content value could not be established, with its stated reason,
     so an unresolved record is distinguishable from one never attempted (COVER-01).
   - The D-20 verification result: whether the n8n research lane's second pass overwrote any
     evidence metadata or input field, and whether a re-stamp was needed. State explicitly
     whether the roughly four evidence-gated records showed any sign of the suspected
     research-lane row-loss recorded in project memory.
5. Fill the Actuals table in `47-COST-ESTIMATE.md` against the same row labels the projection
   used: research calls actually made, redundant second-pass research calls actually observed in
   the n8n execution log, n8n executions consumed, provider credits (expected zero), and the
   Anthropic spend if observable. Where a figure is not measurable, say so rather than estimating
   a second time.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json;a=json.load(open('.planning/phases/47-veto-remediation/47-AFTER.json'));assert len(a)==17,len(a);bad=[r['id'] for r in a if str(r.get('lv_anti_icp_flag'))=='true' and 'Non-ANZ' in str(r.get('lv_anti_icp_reason') or '')];assert not bad,bad;print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The disarm outcome for the n8n surface is `disarmed`, and its independent re-read values match the at-rest literals in `OVERLAY_DISABLED_LITERALS`.
    - `47-RUN-REPORT.md` quotes both disarm outcomes verbatim, including the re-read values.
    - `.planning/phases/47-veto-remediation/47-AFTER.json` has exactly 17 rows.
    - No row in `47-AFTER.json` has a true flag together with a reason containing the non-ANZ reason string — the automated verify above asserts this and names any offending id.
    - `47-RUN-REPORT.md`'s per-id table has 17 rows, each classified, and every residual-other-veto row names the different veto reason.
    - `47-COST-ESTIMATE.md`'s Actuals table has a value or an explicit not-measurable note in every row, and its labels match the projection table's.
    - `.venv/bin/python -m pytest tests/test_veto_remediation_report.py tests/test_remediate_veto_companies.py -x` exits 0.
  </acceptance_criteria>
  <done>Both surfaces are closed and proven closed by independent re-read, the after-state of all 17 is captured and diffed, and actual costs are reported against the pre-run projection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator proves VETO-03 from HubSpot alone, with no script</name>
  <read_first>
    - .planning/phases/47-veto-remediation/47-RESEARCH.md §"VETO-03 acceptance search" (why the operator must use "contains exactly" rather than "is equal to": the reason field is a join of however many vetoes fired, so a record with two simultaneous vetoes carries the non-ANZ text as a substring inside a longer string)
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md
  </read_first>
  <what-built>
All 17 records have been written, both write surfaces are closed and proven closed, and
`47-AFTER.json` shows no record carrying a non-ANZ veto reason. That is the script's word for it.
VETO-03 deliberately demands proof that needs no script at all.
  </what-built>
  <action>
Halt and hand the operator the two-filter HubSpot search below. Claude does not run it and must
not substitute an API search for it — VETO-03's own text requires the proof come from HubSpot
alone with no scripts, so a scripted equivalent does not satisfy the requirement no matter what
it returns. Wait for the operator's reported count.
  </action>
  <how-to-verify>
In the HubSpot portal, open Companies and build a filtered view with these two filters ANDed
together:

- **Filter 1:** property `lv_anti_icp_reason`, operator **contains exactly**, value:
  `Non-ANZ geography`
- **Filter 2:** property `lv_country_region_normalized`, operator **is unknown**

Use *contains exactly*, not *is equal to*. The reason field is a semicolon-joined list of every
veto that fired, so a record carrying two simultaneous vetoes would hold the non-ANZ text inside
a longer string and an equality filter would miss it.

**Expected result: zero companies.**

If the count is zero, VETO-03 is satisfied and this phase's headline promise is proven from
inside HubSpot with no tooling. Report the count and, ideally, a screenshot or the filter URL.

If the count is non-zero, report which companies appear — that is a real finding, and it means
either a record was missed or the derived chain has not settled for it yet.
  </how-to-verify>
  <acceptance_criteria>
    - The operator reports a result count for the two-filter search.
    - The reported count is zero.
    - The operator confirms Filter 1 used the "contains exactly" operator and Filter 2 used "is unknown".
    - If the count is non-zero, the offending company ids are reported and recorded in `47-RUN-REPORT.md` as an open finding rather than the phase being closed.
  </acceptance_criteria>
  <done>The operator has run the two-filter search inside HubSpot and reported the result count; a zero count satisfies VETO-03, a non-zero count is recorded as an open finding.</done>
  <resume-signal>Report the result count, or list the companies that appeared.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Tick the requirements with test pointers and absorb Rule 1 fallout</name>
  <files>.planning/REQUIREMENTS.md, tests/test_scoring_parity.py, .planning/phases/47-veto-remediation/47-RUN-REPORT.md</files>
  <precondition>Task 3 reported a zero-result HubSpot search; if it did not, this task records the open finding instead of ticking anything.</precondition>
  <read_first>
    - .planning/REQUIREMENTS.md (the VETO-01/02/03 rows and the COVER rows Plan 02 amended; the pointer style used on the RUBRIC rows, which name a concrete test)
    - tests/test_scoring_parity.py lines 400-480 (`test_veto_clear_after_correction` and its four excluded siblings — known-failing before this phase for the structural reason research rediscovered: setting veto-input properties alone never dispatches the n8n pipeline)
    - .planning/phases/47-veto-remediation/47-RESEARCH.md §"Rule 1 fallout risk" (no test currently references any of the 17 ids by literal value; the one fixture to check is `tests/fixtures/companies_jscode_frozen.json` for an embedded expected blank/D-tier state)
    - tests/fixtures/companies_jscode_frozen.json
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md
  </read_first>
  <action>
1. In `.planning/REQUIREMENTS.md`, tick VETO-01, VETO-02 and VETO-03 and append a concrete
   pointer to each in the style the RUBRIC rows already use: VETO-01 to
   `scripts/veto_remediation_report.py` and `47-RUN-REPORT.md`'s per-id table; VETO-02 to the two
   verbatim disarm outcomes in `47-RUN-REPORT.md`; VETO-03 to Task 3's operator-run HubSpot
   search result. Do not tick COVER-01 or COVER-02 — per D-02 they close only when Phase 48 has
   covered the remaining record. Use scoped Edit calls, never a whole-file rewrite.
2. Rule 1 fallout — expected, budgeted, not scope creep. Phases 40, 43 and 46 each found stale
   assertions when scoring behaviour changed. Check and, where genuinely stale, correct in place
   with a comment restating each assertion's protective intent:
   - `tests/fixtures/companies_jscode_frozen.json` — check whether it embeds any of the 17's
     pre-remediation blank org-type or D-tier state as an expected value. If it does, re-baseline
     it and record the re-baseline in the summary.
   - `tests/test_scoring_parity.py::test_veto_clear_after_correction` and its four excluded
     siblings — re-run them. Research expects this phase's D-18 webhook mechanism to make them
     pass, which would close a standing gap open since Phase 40-07. If they now pass, say so. If
     they still fail, record precisely why in `47-RUN-REPORT.md` rather than editing the tests to
     agree with the current behaviour — a test weakened to match a failure is the one outcome
     worse than a red test.
   - Do NOT touch `scripts/run_scoring_parity.py`'s population sweep. It is red by design from
     Phase 46 commit `caae5d6` until Phase 49 re-scores, for a reason unrelated to this phase. A
     fix here would mask Phase 49's work.
3. Append a Rule 1 section to `47-RUN-REPORT.md` listing every test or fixture touched and why,
   so the fallout is a recorded consequence rather than an unexplained diff.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -x -q && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows VETO-01, VETO-02 and VETO-03 ticked, each with a pointer naming a concrete artefact or test.
    - COVER-01 and COVER-02 remain unticked, still mapped to `Phase 47 + 48`.
    - `.venv/bin/python -m pytest tests/ -x -q` exits 0 for the offline tier.
    - `node --test tests/n8n/*.test.mjs` exits 0 (the glob form; the directory form is broken on node 24).
    - The five previously-excluded veto tests in `tests/test_scoring_parity.py` are re-run and their outcome is stated in `47-RUN-REPORT.md` — either newly passing, or still failing with a recorded reason. No test assertion was weakened to make a failure pass.
    - `scripts/run_scoring_parity.py` is unmodified: `git diff --stat scripts/run_scoring_parity.py` shows no changes.
    - `47-RUN-REPORT.md` has a Rule 1 section listing every test or fixture touched.
  </acceptance_criteria>
  <done>The three VETO requirements are ticked with evidence pointers, the coverage requirements correctly remain open for Phase 48, and every stale-assertion consequence is fixed in place and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → production CRM | An armed window with live write authority over 17 real company records |
| operator shell → n8n Cloud | An armed workflow write-safety gate with a record allowlist |
| n8n Decide node → production CRM | The workflow writes the derived veto fields on records in its allowlist |
| HubSpot portal UI → operator judgement | VETO-03's proof is a human reading a filtered view |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-17 | Elevation of Privilege | the armed window staying open | high | mitigate | Disarm is the FIRST action of Task 2, runs ungated so it can never be blocked, and proves closure by an independent re-read that fails loudly if the state disagrees |
| T-47-18 | Tampering | n8n allowlist scope | high | mitigate | `TEST_RECORD_IDS` set to exactly the 17 pinned ids; the deployed gate denies everything on an empty allowlist and denies any id outside it; the three excluded ids are structurally absent |
| T-47-19 | Repudiation | claiming the window was closed | high | mitigate | VETO-02's bar is met by quoting the independent re-read verbatim in `47-RUN-REPORT.md`, not by asserting closure |
| T-47-20 | Information Disclosure | secrets in the run log or report | medium | mitigate | The arming outcome and run log print flag names and record ids only; the webhook secret lives solely in a request header and the HubSpot token solely in the client's header builder |
| T-47-21 | Tampering | weakening a red test to close the phase | medium | mitigate | Task 4 forbids editing an assertion to agree with a failure and requires the reason be recorded instead; the red-by-design parity sweep is explicitly out of bounds |
| T-47-22 | Denial of Service | partial write leaving records mid-state | medium | mitigate | The script raises and stops on the first record that settles wrong or never settles, and `47-RUN-LOG.json` records exactly which records were attempted, so a partial run is diagnosable rather than silent |
| T-47-SC | Tampering | package installs | low | accept | No package-manager install occurs in this phase — every dependency is already in `requirements.txt` |

**ASVS level:** L1. **Blocking threshold:** `high`. All three `high` threats are dispositioned
`mitigate` with concrete, evidenced mitigations.
</threat_model>

<flagged_assumptions>
Surfaced explicitly rather than resolved. No SPEC file exists for this phase, so these are the
probe-surfaced items that could not be turned into a defensible acceptance criterion, plus the
research assumptions still live at execution time.

1. **VETO-03 edge probe, category `unclassified` — UNRESOLVED.** The deterministic edge probe
   returned an `unclassified` row for VETO-03 with no category to reason from. VETO-03's bar is
   deliberately script-free and human-judged, so there is no automated predicate to author. It is
   carried as a manual verification (Task 3) and flagged here rather than being given a synthetic
   backstop it does not deserve.
2. **Research Assumption A1 — a live HubSpot webhook subscription on the input properties.** Not
   re-verified against the live portal. If such a subscription does exist, the input PATCH may
   dispatch n8n on its own, ahead of the D-18 POST. Harmless — the Decide node recomputes
   unconditionally and its allowlist still gates the write — but it would show as extra
   executions in the actuals. Task 2's actuals table is where this becomes visible.
3. **Research Assumption A3 — VETO-03's two search property names exist live.** Discharged by
   Plan 03 Task 2's property-existence guard, which lists the portal's real property set. If the
   guard reported either name missing, Task 3's search is not runnable as written and the
   operator must be told before being asked to run it.
4. **The n8n workflow JSON has not changed since 2026-08-11.** Research is tightly coupled to the
   deployed `Decide Company Action` and `Research Trigger Gate` node bodies. If any n8n deploy
   occurred between planning and execution, re-read those two nodes before trusting the D-18
   mechanism or the D-20 second-pass projection.
</flagged_assumptions>

<verification>
- `.venv/bin/python -m pytest tests/ -x -q` — offline tier green.
- `node --test tests/n8n/*.test.mjs` — n8n JS fixtures green (glob form; the directory form is
  broken on node 24).
- `47-AFTER.json` has 17 rows and none carries a true flag with a non-ANZ reason.
- Both disarm outcomes quoted verbatim in `47-RUN-REPORT.md`, including the independent re-reads.
- The operator's HubSpot search returned zero.
- `scripts/run_scoring_parity.py`'s population sweep stays red and unmodified — expected, closed
  by Phase 49.
</verification>

<success_criteria>
- All 17 records written inside one armed window across two surfaces, both closed and proven
  closed.
- Every one of the 17 reads back either cleared or carrying a different, correct veto.
- The operator's script-free HubSpot search returns zero.
- Actuals reported against the pre-run projection; COVER-01/COVER-02 correctly left open for
  Phase 48.
</success_criteria>

## Artifacts this phase produces (Plan 04 contribution)

**New files**
- `.planning/phases/47-veto-remediation/47-AFTER.json`
- `.planning/phases/47-veto-remediation/47-RUN-REPORT.md`
- `.planning/phases/47-veto-remediation/47-RUN-LOG.json` (emitted by the armed run's report flag)

**Modified files**
- `.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md` — Actuals table filled
- `.planning/REQUIREMENTS.md` — VETO-01/02/03 ticked with evidence pointers
- `tests/test_scoring_parity.py` and `tests/fixtures/companies_jscode_frozen.json` — only if
  genuinely stale, corrected in place with intent-restating comments

**No new code symbols, CLI flags or env vars.** The operator-only variables used here —
`ALLOW_N8N_ARM`, `ALLOW_VETO_REMEDIATION`, `DRY_RUN` — are declared by Plan 01 and by the
pre-existing arming ceremony.

<output>
Create `.planning/phases/47-veto-remediation/47-04-SUMMARY.md` when done
</output>
