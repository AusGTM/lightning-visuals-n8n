---
phase: 16.2-contacts-research-judge-mirror
plan: 02
type: execute
wave: 2
depends_on:
  - 16.2-01
autonomous: true
files_modified:
  - n8n/code/contactResearch.js
  - n8n/code/contactJudge.js
  - n8n/code/mergeContacts.js
  - scripts/build_cloud_workflows.py
  - scripts/deploy_n8n_workflows.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_enrichment_local.json
  - tests/n8n/contactResearch.test.mjs
  - tests/n8n/contactJudge.test.mjs
  - tests/n8n/mergeContacts.test.mjs
  - tests/n8n/contactResearchChainRowFlow.test.mjs
  - tests/test_cloud_contacts_branch.py
  - tests/test_builder_flag_parity.py
  - tests/test_provider_gate_topology.py
  - tests/test_deploy_n8n_workflows.py
requirements:
  - "SC-1 (the contacts branch gains Contact Research Trigger Gate -> IF Contact Research Needed -> Build Contact Research Request -> Contact Web Research -> Validate Contact Research -> Contact Judge Gate -> IF Contact Needs Judge -> Build Contact Judge Request -> Contact Judge Call -> Apply Contact Judge Verdict, wired at the 16.1 seam, feeding Merge Winners; the row survives both HTTP hops via paired-index recovery, mirroring bd682a2)"
  - "SC-3 (HONEST MIRROR — redefined per reviews gpt BLOCKER-1: contact research does gap-fill + stale-refresh + EXISTING-RECORD conflict adjudication — exactly what companies does. It does NOT do provider-vs-research adjudication, because companies does not either (escalation compares research to the HubSpot record, never to provider winners). The contact prompt/escalation/verdict are jobtitle/seniority-scoped, not company-ICP; off/none/absent providers still degrade gracefully so the research-only path works. foldContactResearch is a write-SAFETY gate, not an adjudicator.)"
  - "SC-4 (offline tests mirror the companies research/judge coverage — BFS reachability, the contact item-flow row-recovery regression, contact escalation/verdict incl. chosen_field security, contact validate never-throws + evidence-URL, the fold write-safety gate, credential parity + deploy binding; full suite green; builder deterministic; no live calls)"

must_haves:
  truths:
    - "n8n/code/contactResearch.js exports `contactResearchCandidateFromHttpItem(item)` — NEVER throws on any HTTP failure shape (execution-error item, missing/empty content, error body, unparseable text) resolving to an unmatched candidate (skip-not-retry, CLAUDE.md §26.2), reusing webResearch.js's `extractFinalJson`/`normalizeUrlForMatch`/`extractPageAgeByField` + never-throw skeleton by co-inlining; and a `validateContactResearch(raw)` that coerces `data.jobtitle`/`data.seniority` to trimmed-string-or-null (NO taxonomy normalization — free-text), carries `evidence_by_field`, `matched` (default true unless non-dict, mirroring OC-4), `confidence`, and demotes a jobtitle/seniority value to null UNLESS its `evidence_by_field.<field>` is a PARSEABLE `https:` URL (gpt #10 — presence alone is insufficient; reuse webResearch.js normalizeUrlForMatch or `new URL()` + protocol check; a `javascript:`/empty/malformed URL demotes to null, tri-state, mirroring judge.js applyEvidenceSufficiency direction)."
    - "n8n/code/contactJudge.js exports `computeContactEscalation(researchCandidate, existingRecord)` with EXACTLY two arguments (judge.js:97-102 arity discipline; field config via a module-level `_CONTACT_JUDGE_FIELDS=[\"jobtitle\",\"seniority\"]`, never a 3rd arg), returning {needsJudge,reasons}. RO-1 carries over: an unmatched candidate never escalates. Triggers: `jobtitle_conflict` (existing jobtitle known AND research jobtitle differs after NORMALIZED trim+lowercase compare — gpt #12, winners hold RAW values so a strict compare falsely conflicts on casing), `seniority_conflict` (same, normalized), `confidence_band` (confidence in ESCALATION_CONFIDENCE_BAND [75,85] AND the candidate carries a jobtitle/seniority signal). NO `jobtitle_stale_refresh` reason here (staleness is a RESEARCH gate/needsResearch trigger in CONTACTS_TARGET, where a clock is allowed — LOW-7 keeps computeContactEscalation clock-free); no vendor/veto/org reasons; no provider-vs-research comparison (SC-3 honest mirror)."
    - "n8n/code/contactJudge.js exports `applyContactUnadjudicated(researchCandidate, reasons)` that DROPS the conflicting field (deletes data.jobtitle on jobtitle_conflict, data.seniority on seniority_conflict) so the existing record value stands (contact analog of judge.js:156-161); `buildContactJudgeRequestBody(row, model, maxTokens)` producing a 'contact role conflict' prompt restricted to jobtitle/seniority with NO size/vendor field and NO tools key (JG-2), required keys = JUDGE_OUTPUT_REQUIRED + chosen_field; and a SECURITY-HARDENED `applyContactJudgeVerdict(researchCandidate, verdict, reasons)` (gpt #5) that reuses judge.js `judgeVerdictFromHttpItem` (never-throws, JG-3) but PROMOTES ONLY when the verdict's `chosen_field ∈ {jobtitle, seniority}` AND `decision ∈ {promote, confirm}` AND `confidence >= JUDGE_MIN_CONFIDENCE(80)` — a verdict naming chosen_field:'email' (or any non-target field) NEVER writes that field; every other verdict routes through applyContactUnadjudicated. On a validated promote it sets `judge_flags = {adjudicated:true, decision, promoted_field: chosen_field}`. Reuses applyCostCap/judgeVerdictFromHttpItem verbatim by co-inlining; NO A/R/G/T grounding (RESEARCH Task 3.4)."
    - "MARKER HYGIENE (gpt #5): the FIRST contact chain node (Contact Research Trigger Gate) STRIPS all caller-injectable internal markers from every row — `research_candidate`, `judge_verdict`, `judge_flags`, `judge_confidence_by_field`, `judge_promoted_fields` — because ENRICH_PARSE_EVENT spreads raw event props into the row, so a caller could otherwise inject a pre-set `judge_flags.promoted_field`/verdict and forge adjudication. All these markers are (re)set ONLY by the chain's own nodes downstream (Validate Contact Research rebuilds research_candidate from the HTTP response; Apply Contact Judge Verdict sets judge_flags/judge_promoted_fields fresh). A test asserts an injected marker on the input event does not survive the gate."
    - "ROW-RECOVERY (reviews X1/BLOCKER-1, mirror of bd682a2): the `Validate Contact Research` wrapper recovers the enrichment row by paired index from `Build Contact Research Request` (`const preHttp=(function(){try{return $(\"Build Contact Research Request\").all();}catch(e){return[];}})(); const row=(preHttp[i]&&preHttp[i].json)||it.json;`) and `Apply Contact Judge Verdict` from `Build Contact Judge Request` — because the Contact Web Research / Contact Judge Call HTTP nodes REPLACE $json with the API response. Without this the contact chain has the identical row-loss the companies chain had (Merge Winners sees no `scored`/existingRecord → merge:null). This is emitted by the Plan-01 parameterized producers via the CONTACTS_TARGET pre-HTTP node names. Proven by tests/n8n/contactResearchChainRowFlow.test.mjs (mirror of researchChainRowFlow.test.mjs): the row survives BOTH HTTP hops and Merge Winners produces a non-null merge."
    - "In the built wf_enrichment_cloud.json the ten contact-distinct nodes — 'Contact Research Trigger Gate', 'IF Contact Research Needed', 'Build Contact Research Request', 'Contact Web Research', 'Validate Contact Research', 'Contact Judge Gate', 'IF Contact Needs Judge', 'Build Contact Judge Request', 'Contact Judge Call', 'Apply Contact Judge Verdict' — are BFS-reachable from 'Webhook Trigger', wired Normalize + Score -> Contact Research Trigger Gate -> ... -> Apply Contact Judge Verdict -> Merge Winners, with BOTH IF-false lanes fanning STRAIGHT into Merge Winners (mirror of the companies false lanes into Merge Company). Emitted by the target-parameterized factories with CONTACTS_TARGET (SC-2), not duplicated code."
    - "The contact chain sits DOWNSTREAM of Normalize + Score, independent of the 16.1 provider gate: `providers: none`/off (zero provider HTTP nodes) still flows identity -> gates(bypass) -> Normalize + Score -> Contact Research Trigger Gate, and if ALLOW_WEB_RESEARCH is on and a gap is detected (provider_gap: the field is absent from BOTH existing AND provider winners), research fills jobtitle/seniority from scratch (research-only path). Gating reuses the inherited six CONFIG_FLAG_DEFAULTS (both default OFF); NO new global switch; research max_tokens 2048 and judge max_tokens >=2048 (separate budgets, gpt #11)."
    - "ENRICH_MERGE folds a SECOND mergeContacts call for the research candidate (mirror of ENRICH_MERGE_CO): when `row.research_candidate.matched`, build researchData from rc.data.{jobtitle,seniority} skipping null/blank; call mergeContacts(existingRecord, researchData, RESEARCH_POLICY, {source:'claude_web', confidence: rc.confidence, evidence: rc.evidence_by_field}) where RESEARCH_POLICY overrides jobtitle to system_owned (seniority already system_owned) AND sets require_evidence_url:true on both (LOW-9 belt-and-braces with the upstream validate demotion) so a research value can promote only WITH evidence; then fold via a pure exported `foldContactResearch(providerMerge, researchMerge, judgePromotedFields, existingRecord)`. WRITE-SAFETY GATE (NOT adjudication): for each overlapping field F (jobtitle/seniority) the research value wins ONLY when `judgePromotedFields.includes(F)` (F was JUDGE-promoted — from the fresh, entry-cleared `judge_promoted_fields`, NOT the injectable confidence map) OR when the provider produced no canonical value for F AND `isBlank(existingRecord[F])` (a GENUINE gap — kimi HIGH-2: provider-absent is not a gap when the HubSpot record already has a value); otherwise the PROVIDER/existing value stands. AUDIT CONSISTENCY (gpt #8): when the gate rejects a research promotion, its recorded decision is rewritten to stage_only + reason `withheld_by_overlap_precedence` so the audit trail agrees with the actual canonicalPatch. `judge_promoted_fields` is derived in the wrapper from `research_candidate.judge_flags.promoted_field` (only set by the security-hardened applyContactJudgeVerdict)."
    - "STALE-TIMESTAMP FIX (gpt #6): mergeContacts emits the `lv_jobtitle_verified_at` cache-key ONLY when the field is ACCEPTED (decision===promote), not for every non-blank candidate — otherwise a needs_review/stale-but-unpromoted jobtitle gets falsely marked fresh and never re-researched. The cacheKeys assignment moves inside the promote branch on the CONTACT path; a note flags that companies (mergeCompanies.js) has the same latent issue as a Track-B follow-up (not fixed here)."
    - "SENIORITY FETCH (gpt #3): the contact HubSpot search property list adds `seniority` + `lv_contact_enrichment_provenance` so existingRecord carries them and seniority_conflict/seniority-gap can compare against the real value (not a falsely-blank one). SCOPE DECISION (documented): seniority is a RIDE-ALONG — enriched when the chain runs (triggered by a jobtitle/provider gap or stale), NOT a standalone enrich trigger; the ENRICH_GATE enrich-trigger set stays email/jobtitle/mobilephone (unchanged). A seniority-only gap on an otherwise-complete, gate-skipped contact is intentionally not researched in v1."
    - "'Contact Web Research' and 'Contact Judge Call' are ANTHROPIC-credential-bound: added to SECRET_TO_CREDENTIAL_BOUND_NODES[\"ANTHROPIC_API_KEY\"] (test_builder_flag_parity.py) AND to deploy_n8n_workflows.py NODE_CREDENTIAL_MAP ({httpHeaderAuth, 'LV Anthropic'}); a NEW deploy test (gpt #9) asserts both receive the LV Anthropic credential ID after `bind_credentials()` (parity alone doesn't prove deploy binding — the C2 lesson); a reverse parity assertion (MEDIUM-5) asserts every genericCredentialType anthropic HTTP node in the workflow is in the expected set (an unlisted+unbound anthropic node fails). Zero raw ANTHROPIC_API_KEY env-var reference survives in the cloud JSON."
    - "HIGH-3 fixes: tests/test_provider_gate_topology.py's `Normalize + Score -> Merge Winners` direct-edge assertion is updated to expect `Normalize + Score -> Contact Research Trigger Gate` (the seam splice moves the edge; the forbidden-set check stays valid); and n8n/wf_enrichment_local.json is committed (the deterministic rebuild regenerates all three enrichment workflows even though the LOCAL mock never sets research_candidate — behavior unchanged there)."
    - "The full offline suite (`.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs`) is green with zero regressions vs the 332 pytest / 179 node baseline (new contact tests add to both counts), a second builder run yields byte-identical wf_enrichment_*.json, and the Plan-01 companies frozen guard stays green (only node ids shift)."
  artifacts:
    - "n8n/code/contactResearch.js — contactResearchCandidateFromHttpItem + validateContactResearch (jobtitle/seniority + parseable-https evidence, never-throws), require()ing webResearch.js generic helpers"
    - "n8n/code/contactJudge.js — _CONTACT_JUDGE_FIELDS, computeContactEscalation (2-arg, normalized compare, no stale, no clock), applyContactUnadjudicated, buildContactJudgeRequestBody, security-hardened applyContactJudgeVerdict (chosen_field allowlist + decision/confidence gate + judge_flags.promoted_field), require()ing judge.js field-agnostic exports"
    - "n8n/code/mergeContacts.js — pure exported `foldContactResearch(providerMerge, researchMerge, judgePromotedFields, existingRecord)` (write-safety gate + audit-consistency rewrite) AND the stale-timestamp fix (cacheKey only on promote); both unit-testable, co-located since ENRICH_MERGE inlines this module"
    - "scripts/build_cloud_workflows.py — contact call sites (CONTACTS_TARGET) + contact-distinct names; the entry-node marker strip; the seniority+provenance additions to the contact search property list; the ENRICH_MERGE fold via foldContactResearch (judge_promoted_fields derived from research_candidate.judge_flags.promoted_field); seam wiring in build_enrichment_cloud() + build_enrichment_local_live()"
    - "scripts/deploy_n8n_workflows.py — NODE_CREDENTIAL_MAP entries for Contact Web Research + Contact Judge Call (LV Anthropic)"
    - "tests: tests/test_cloud_contacts_branch.py (BFS + research-only bypass + no company-ICP field), tests/n8n/contactResearch.test.mjs, tests/n8n/contactJudge.test.mjs (incl. chosen_field:'email' rejection + injected-marker), tests/n8n/mergeContacts.test.mjs (fold write-safety + audit + stale-timestamp), tests/n8n/contactResearchChainRowFlow.test.mjs (row survives HTTP hops), tests/test_builder_flag_parity.py (+ reverse assertion), tests/test_provider_gate_topology.py (updated seam edge), tests/test_deploy_n8n_workflows.py (deploy binding)"
  key_links:
    - "Normalize + Score -> Contact Research Trigger Gate (marker strip) -> IF Contact Research Needed [true->Build Contact Research Request->Contact Web Research->Validate Contact Research (row-recover from Build Contact Research Request)->Contact Judge Gate->IF Contact Needs Judge; false->Merge Winners] ; IF Contact Needs Judge [true->Build Contact Judge Request->Contact Judge Call->Apply Contact Judge Verdict (row-recover from Build Contact Judge Request); false->Merge Winners] -> Merge Winners"
    - "Apply Contact Judge Verdict (validated chosen_field ∈ {jobtitle,seniority}) -> judge_flags.promoted_field -> row.judge_promoted_fields -> ENRICH_MERGE foldContactResearch write-safety gate -> research wins F iff judgePromotedFields.includes(F) OR (provider absent F AND isBlank(existingRecord[F])); else provider stands + decision rewritten withheld_by_overlap_precedence"
    - "CONTACTS_TARGET (Plan 01: provider-aware gap predicate + pre-HTTP row-recovery names) -> parameterized factories -> contact node jsCode co-inlining contactResearch.js/contactJudge.js + judge.js/webResearch.js"
    - "Contact Web Research/Contact Judge Call -> SECRET_TO_CREDENTIAL_BOUND_NODES + deploy NODE_CREDENTIAL_MAP (LV Anthropic) -> bind_credentials() assigns the LV Anthropic id (deploy test) -> no 401 silent failure"
    - "contact HubSpot search + seniority/lv_contact_enrichment_provenance -> existingRecord carries seniority -> seniority_conflict/gap compares real value"
  prohibitions:
    - "Do NOT edit n8n/code/judge.js, webResearch.js, scoreEnrichment.js, or mergeCompanies.js — reuse their field-agnostic exports by co-inlining the NEW sibling modules only (editing them breaks companies byte-identity, Plan 01's frozen guard). These four stay git-unchanged. (mergeCompanies' latent stale-timestamp issue is flagged Track-B, not fixed.)"
    - "Do NOT change any companies research/judge/validate/apply-verdict/merge Code node's jsCode — the Plan-01 frozen guard MUST stay green; the contact chain uses the parameterized factories with CONTACTS_TARGET, never a hand-rolled copy of the companies bodies."
    - "Do NOT web-research or store phone/mobilephone/email — jobtitle+seniority ONLY; no PII field enters the contact research prompt or candidate (CONTEXT 1/7, CLAUDE.md §16). Persist only the verified value + evidence URL + provenance."
    - "computeContactEscalation MUST take exactly two arguments — field config via _CONTACT_JUDGE_FIELDS, never a 3rd arg (judge.js:97-102). Do NOT feed provider winners (row.scored.winners) into computeContactEscalation — that breaks arity AND is scope creep (SC-3 honest mirror: companies does NOT adjudicate provider-vs-research either). The write-safety of the overlap is the merge-time foldContactResearch gate, not the escalation."
    - "Do NOT build provider-vs-research conflict ADJUDICATION — SC-3 is redefined to the honest mirror (gap-fill + stale-refresh + existing-record conflict). foldContactResearch PREVENTS clobbering; it does not adjudicate."
    - "applyContactJudgeVerdict MUST NOT write a verdict's chosen_field unless it is in {jobtitle, seniority} — a model/caller-supplied chosen_field:'email' (or any other field) is rejected (gpt #5). The fold gate keys on the fresh judge_promoted_fields / judge_flags.promoted_field, NEVER the caller-injectable judge_confidence_by_field."
    - "The ENRICH_MERGE fold gap-fill branch MUST require isBlank(existingRecord[F]) (kimi HIGH-2) — provider-absent alone is not a gap when the existing record already holds a value; that value stands unless the judge promoted F."
    - "The Contact Judge Gate / Build Contact Judge Request built jsCode MUST contain NO `web_search` tool key and NO size/vendor field name (JG-2); assert it the way the companies test does (LOW-8: judge.js's comment literally contains annualrevenue/numberofemployees, so a naive grep false-positives — mirror test_judge_spec.py's approach, not a bare grep)."
    - "No A/R/G/T grounding (scoreResearchCandidates) on contacts v1; no new global kill-switch / config flag (16.1 Decision 1 stands)."
---

<objective>
Fill the Phase-16.2 seam: mirror the companies web-research->judge->verdict chain onto the CONTACTS branch
for jobtitle + seniority ONLY, reusing Plan 01's target-parameterized factories (CONTACTS_TARGET) plus two
new sibling JS modules (contactResearch.js, contactJudge.js) that reuse the field-agnostic exports of
judge.js/webResearch.js by co-inlining — so both object types share one set of building blocks and the
companies branch stays byte-identical (Plan 01's frozen guard proves it). After this plan a contact can be
gap-filled, stale-refreshed, and EXISTING-RECORD-conflict-adjudicated for its current role — the HONEST
mirror of what companies does (companies does NOT adjudicate provider-vs-research; neither do we — reviews
gpt BLOCKER-1). Gated OFF by default behind the inherited ALLOW_WEB_RESEARCH/ALLOW_SONNET_ESCALATION caps.

Purpose: this is the vertical capability the phase exists for, hardened by the cross-AI review. It solves
four correctness/safety problems the review surfaced: (1) ROW-RECOVERY — the contact Validate/Apply-Verdict
wrappers recover the row by paired index across the HTTP hops (mirror of the committed bd682a2 companies fix),
or the research lane dead-ends at merge:null; (2) chosen_field SECURITY — the event parser spreads raw props,
so internal markers are cleared at chain entry and applyContactJudgeVerdict only ever writes a validated
{jobtitle,seniority} field (never a caller-supplied chosen_field:'email'); (3) WRITE-SAFETY overlap — the
merge-time foldContactResearch gate lets research win a field only when the judge promoted it OR it fills a
genuinely-blank gap (existing AND provider both empty), else the provider/existing value stands; (4) the
seniority-fetch, stale-timestamp, evidence-URL, and normalized-compare fixes. PII is minimized to public role
facts + evidence URL + provenance (no phone/email/mobile ever researched).

Output: contactResearch.js + contactJudge.js + mergeContacts.foldContactResearch, the wired contact chain
(ten contact-distinct nodes at the seam in cloud + local-live, row-recovery + marker-strip), the ENRICH_MERGE
write-safety fold, credential bookkeeping + deploy binding test, and mirrored offline tests (incl. the item-flow
row-recovery regression) — deterministic rebuild, green suite, companies byte-identity intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-RESEARCH.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-CONTEXT.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-REVIEWS.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-01-PLAN.md

# Analog + target sources (read the specific ranges named in each task's read_first)
@scripts/build_cloud_workflows.py
@n8n/code/judge.js
@n8n/code/webResearch.js
@n8n/code/mergeContacts.js
@tests/n8n/researchChainRowFlow.test.mjs
@tests/test_cloud_companies_branch.py
@tests/test_builder_flag_parity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contactResearch.js + contactJudge.js + mergeContacts.foldContactResearch (pure JS + node unit tests, security-hardened)</name>
  <files>n8n/code/contactResearch.js, n8n/code/contactJudge.js, n8n/code/mergeContacts.js, tests/n8n/contactResearch.test.mjs, tests/n8n/contactJudge.test.mjs, tests/n8n/mergeContacts.test.mjs</files>
  <read_first>
    - n8n/code/webResearch.js (FULL — reuse by require: extractFinalJson, normalizeUrlForMatch (also the https/parseable-URL check for evidence, gpt #10), extractPageAgeByField, the researchCandidateFromHttpItem never-throw skeleton :160-179, _unmatchedCandidate :152)
    - n8n/code/judge.js (FULL — reuse by require: computeEscalation :103-136 (2-arg analog + confidence_band :129-133 using ESCALATION_CONFIDENCE_BAND), applyUnadjudicated :141-175 (drop-conflicting-field), buildJudgeRequestBody :335-393 (JG-2 no-size/no-tools prompt), judgeVerdictFromHttpItem :423-447 (verbatim), applyJudgeVerdict :454-484 (promote/demote — note it writes ANY chosen_field; the contact twin MUST allowlist {jobtitle,seniority}), applyCostCap :184-195 (verbatim); the arity comment :97-102; the "never read the clock" discipline :274-275)
    - n8n/code/escalation.generated.js:9-33 (ESCALATION_CONFIDENCE_BAND=[75,85], JUDGE_MIN_CONFIDENCE=80, JUDGE_OUTPUT_REQUIRED — co-inlined alongside judge.js, so in scope)
    - n8n/code/mergeContacts.js (FULL — Plan-01 port; DEFAULT_CONTACT_POLICY jobtitle=stale_refreshable/seniority=system_owned; CONTACT_CACHE_KEY_FIELDS + where cacheKeys is assigned — the stale-timestamp fix moves it inside the promote branch; ENRICH_MERGE inlines this module so foldContactResearch lives here)
    - n8n/code/normalizeProviders.js:157-158,204-205,264-267 (providers push jobtitle/seniority as free-text — no closed vocabulary; validateContactResearch passes through un-normalized; but conflict compare must trim+lowercase, gpt #12)
    - tests/n8n/webResearchFailure.test.mjs + judge.test.mjs + judgeFailure.test.mjs + mergeCompanies.test.mjs (node:test + createRequire style to mirror)
    - RESEARCH Task 2/3, Analogs table; 16.2-REVIEWS.md gpt #5/#6/#8/#10/#12 + kimi HIGH-2 + LOW-7/8
  </read_first>
  <behavior>
    contactResearch.js:
    - never-throws on {error}, missing content, {"type":"error"} body, unparseable text, empty content -> unmatched (matched:false).
    - valid item -> matched candidate with trimmed jobtitle/seniority + evidence_by_field + confidence.
    - validateContactResearch: "  Head of Racing  " -> "Head of Racing"; missing/non-string -> null; non-dict raw -> matched:false.
    - EVIDENCE URL (gpt #10): jobtitle with evidence_by_field.jobtitle = "https://x/team" promotes; with "javascript:alert(1)" / "" / "not a url" / missing -> demoted to null.
    contactJudge.js:
    - computeContactEscalation: RO-1 (unmatched -> no escalation); jobtitle_conflict fires on existing!=research AFTER trim+lowercase, NOT on "CEO" vs "ceo" (gpt #12); seniority_conflict same; confidence_band in [75,85] with a signal; arity === 2; NO stale reason.
    - applyContactUnadjudicated drops data.jobtitle on jobtitle_conflict, data.seniority on seniority_conflict; new object.
    - buildContactJudgeRequestBody: no web_search/tools key, no size/vendor field, requires JUDGE_OUTPUT_REQUIRED+chosen_field, names jobtitle/seniority only.
    - applyContactJudgeVerdict SECURITY: verdict {decision:promote, chosen_field:"email", confidence:95} -> data.email NOT written, no judge_flags.promoted_field; {promote, jobtitle, 90} -> data.jobtitle written + judge_flags.promoted_field==="jobtitle"; {promote, jobtitle, 70} (sub-threshold) -> unadjudicated; garbage verdict -> never throws.
    mergeContacts.foldContactResearch:
    - adjudicated wins: provider jobtitle "X", research "Y", judgePromotedFields ["jobtitle"] -> "Y".
    - unadjudicated + provider present -> provider stands: provider "X", research "Y", judgePromotedFields [] -> "X"; and the rejected research decision is rewritten stage_only + withheld_by_overlap_precedence (gpt #8).
    - Path A: existing blank, provider filled "X", research "Y", [] -> "X" (provider produced X).
    - Path B: confidence_band, escalation off, [] -> "X".
    - GAP-FILL requires existing blank (kimi HIGH-2): provider absent jobtitle, existingRecord.jobtitle blank, research "Y", [] -> "Y"; but provider absent jobtitle, existingRecord.jobtitle="Old", research "Y", [] -> "Old" (existing stands, NOT a gap).
    - both-conflict, judge promotes only jobtitle: jobtitle -> research (promoted), seniority -> provider/existing (not promoted, not a blank gap).
    mergeContacts stale-timestamp (gpt #6):
    - a needs_review (stale, unpromoted) jobtitle candidate does NOT emit lv_jobtitle_verified_at; a promoted jobtitle DOES.
  </behavior>
  <action>
    Write the three JS edits + three node tests per behavior. contactResearch.js/contactJudge.js `require("./judge")`/
    `require("./webResearch")` for field-agnostic helpers (inline() drops requires + co-inlines). Key hardening vs
    the earlier plan: (1) validateContactResearch requires a PARSEABLE https: evidence URL (reuse normalizeUrlForMatch
    or new URL() + protocol check) before a value may keep — else demote to null; (2) computeContactEscalation drops
    jobtitle_stale_refresh (staleness is a CONTACTS_TARGET needsResearch gate trigger, Plan 01 — keeps this function
    clock-free, LOW-7) and compares jobtitle/seniority NORMALIZED (trim+lowercase, gpt #12); (3) applyContactJudgeVerdict
    is SECURITY-HARDENED — it allowlists chosen_field ∈ _CONTACT_JUDGE_FIELDS and requires decision∈{promote,confirm}
    + confidence>=JUDGE_MIN_CONFIDENCE before writing data[chosen_field], and records judge_flags.promoted_field on a
    validated promote (this is the ONLY trusted per-field adjudication signal); everything else -> applyContactUnadjudicated.
    Add to mergeContacts.js: (4) the pure `foldContactResearch(providerMerge, researchMerge, judgePromotedFields,
    existingRecord)` write-safety gate — research wins F iff judgePromotedFields.includes(F) OR (F not in
    providerMerge.canonicalPatch AND isBlank(existingRecord[F])); else provider stands AND the research decision for F
    is rewritten to stage_only + reason "withheld_by_overlap_precedence" (audit consistency, gpt #8); concat decisions;
    return {canonicalPatch, provenance, cacheKeys, decisions}; (5) the stale-timestamp fix — move the
    `cacheKeys[CONTACT_CACHE_KEY_FIELDS[field]] = verifiedAt` assignment INSIDE the `decision === "promote"` branch so
    an unpromoted stale title is not marked fresh (gpt #6; add a one-line comment noting mergeCompanies has the same
    latent issue, Track-B). Do NOT edit judge.js/webResearch.js/scoreEnrichment.js/mergeCompanies.js.
  </action>
  <verify>
    <automated>node --test tests/n8n/contactResearch.test.mjs tests/n8n/contactJudge.test.mjs tests/n8n/mergeContacts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - contactResearch.test.mjs passes incl. the evidence-URL cases (javascript:/empty/malformed demote; valid https promotes).
    - contactJudge.test.mjs passes: RO-1; normalized conflict (no false "CEO" vs "ceo"); arity===2; applyContactUnadjudicated drops; buildContactJudgeRequestBody no web_search/size/vendor; and the SECURITY cases — chosen_field:'email' never writes email, sub-threshold/garbage routes to unadjudicated, a validated promote sets judge_flags.promoted_field.
    - mergeContacts.test.mjs passes: foldContactResearch cases (adjudicated wins; unadjudicated+provider stands + decision rewritten; Path A; Path B; gap-fill requires existing-blank; both-conflict one-promoted); and the stale-timestamp case (unpromoted stale jobtitle emits no lv_jobtitle_verified_at).
    - `git diff --stat` shows judge.js, webResearch.js, scoreEnrichment.js, mergeCompanies.js UNCHANGED.
    - `grep -c "web_search" n8n/code/contactJudge.js` returns 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Three JS edits + three node tests; delete the new modules and revert mergeContacts.js to the Plan-01 state (no builder change yet).</reversibility>
  <done>The contact field-bound logic (evidence-gated research validate, clock-free normalized 2-arg escalation, chosen_field-allowlisted verdict, write-safety fold, stale-timestamp fix) lives in sibling modules + mergeContacts, reusing judge.js/webResearch.js generic exports, proven by node tests incl. the security + write-safety cases — shared modules untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the contact chain (row-recovery + marker-strip) at the seam + seniority fetch + ENRICH_MERGE fold + credentials/deploy + BFS/topology/row-flow tests</name>
  <files>scripts/build_cloud_workflows.py, scripts/deploy_n8n_workflows.py, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local_live.json, n8n/wf_enrichment_local.json, tests/test_cloud_contacts_branch.py, tests/test_builder_flag_parity.py, tests/test_provider_gate_topology.py, tests/test_deploy_n8n_workflows.py, tests/n8n/contactResearchChainRowFlow.test.mjs</files>
  <read_first>
    - scripts/build_cloud_workflows.py: the contacts seam (Normalize + Score -> Merge Winners -> Set Data Quality + Gap Flag -> Decide Action) in both build_enrichment_cloud() and build_enrichment_local_live(); the companies research/judge conns (the mirror shape with both IF-false lanes into the convergence); the companies contact-chain node declarations (code_node + _if_bool_node + _http_node auth=header anthropic / _live_http + _env_secret_expr(ANTHROPIC_API_KEY))
    - scripts/build_cloud_workflows.py ENRICH_VALIDATE_RESEARCH / ENRICH_APPLY_JUDGE_VERDICT (post-bd682a2 row-recovery via $("Build Research Request")/$("Build Judge Request")) — the CONTACTS_TARGET pre-HTTP names ("Build Contact Research Request"/"Build Contact Judge Request") drive the same recovery for contacts; and ENRICH_MERGE (the 2nd-merge fold target) + ENRICH_MERGE_CO (the disjoint companies analog)
    - scripts/build_cloud_workflows.py ENRICH_GATE (REQUIRED=["email","jobtitle","mobilephone"] — unchanged; seniority is ride-along) + the contact HubSpot search property list (the fetch list to add `seniority` + `lv_contact_enrichment_provenance` to) + ENRICH_BUILD_IDENTITY (name/company/domain that seed the research prompt) + CONTACTS_TARGET (Plan 01)
    - tests/n8n/researchChainRowFlow.test.mjs (FULL — the item-flow harness to mirror for contacts: CHAIN node list, mock httpResponses, runChain over the built wf_enrichment_cloud.json, assert row survives + Merge not null)
    - tests/test_cloud_companies_branch.py:26-62 (BFS to mirror); tests/test_provider_gate_topology.py (the `Normalize + Score -> Merge Winners` direct-edge assertion to UPDATE, and the forbidden-set check to keep); tests/test_builder_flag_parity.py (SECRET_TO_CREDENTIAL_BOUND_NODES + assertion); tests/test_deploy_n8n_workflows.py (bind_credentials() test style)
    - scripts/deploy_n8n_workflows.py NODE_CREDENTIAL_MAP (Claude Web Research/Judge Call are LV Anthropic; add the two contact nodes)
  </read_first>
  <action>
    (1) DECLARE the ten contact-distinct nodes in build_enrichment_cloud() + build_enrichment_local_live() via the
    Plan-01 factories with CONTACTS_TARGET: "Contact Research Trigger Gate" (research gate — ALSO the marker-strip
    entry: it clears research_candidate/judge_verdict/judge_flags/judge_confidence_by_field/judge_promoted_fields
    from each row before anything else, gpt #5), _if_bool_node("IF Contact Research Needed","research_needed"),
    "Build Contact Research Request", the "Contact Web Research" HTTP node (anthropic messages, auth=header cloud /
    _env_secret_expr(ANTHROPIC_API_KEY) local-live, json_body=$json.research_request_body), "Validate Contact
    Research" (parameterized validate producer, CONTACTS_TARGET, inlines webResearch.js+contactResearch.js,
    row-recovers from "Build Contact Research Request"), "Contact Judge Gate", _if_bool_node("IF Contact Needs
    Judge","needs_judge"), "Build Contact Judge Request" (judge maxTokens >=2048), the "Contact Judge Call" HTTP
    node, "Apply Contact Judge Verdict" (row-recovers from "Build Contact Judge Request"; sets judge_promoted_fields
    fresh from the validated verdict). Contact-distinct names throughout (n8n keys on name). Confirm the marker-strip
    is the FIRST thing the entry node does (belongs in the CONTACTS_TARGET research-gate body / a preface).

    (2) WIRE at the seam in BOTH builders: Normalize + Score -> Contact Research Trigger Gate; then the companies
    conns shape with contact names — Contact Research Trigger Gate -> IF Contact Research Needed [true->Build Contact
    Research Request; false->Merge Winners]; ...Validate Contact Research->Contact Judge Gate->IF Contact Needs Judge
    [true->Build Contact Judge Request; false->Merge Winners]; ...Apply Contact Judge Verdict->Merge Winners. Both
    IF-false lanes fan STRAIGHT into Merge Winners. Keep the tail (Merge Winners -> Set Data Quality + Gap Flag ->
    Decide Action -> IF Create ...) intact.

    (3) SENIORITY FETCH (gpt #3): add `seniority` + `lv_contact_enrichment_provenance` to the contact HubSpot search
    property list so existingRecord carries them. Do NOT add seniority to ENRICH_GATE REQUIRED (email/jobtitle/
    mobilephone unchanged) — document (comment) that seniority is a ride-along enriched when the chain runs, not a
    standalone enrich trigger.

    (4) ENRICH_MERGE FOLD: after the provider mergeContacts call, if row.research_candidate?.matched: build
    researchData={jobtitle,seniority} (skip null/blank); RESEARCH_POLICY = DEFAULT_CONTACT_POLICY with jobtitle ->
    {class:"system_owned", min_confidence:75} and require_evidence_url:true on jobtitle+seniority (LOW-9); call
    mergeContacts(existingRecord, researchData, RESEARCH_POLICY, {source:"claude_web", confidence: rc.confidence||80,
    evidence: rc.evidence_by_field||{}}); compute judgePromotedFields from
    `row.research_candidate?.judge_flags?.promoted_field` (the fresh, entry-cleared, security-validated marker — NOT
    row.judge_confidence_by_field); fold via foldContactResearch(providerMerge, researchMerge, judgePromotedFields,
    row.existingRecord||{}). Comment the write-safety gate. mergeContacts.js is inlined only into contacts (companies
    untouched — frozen guard stays green).

    (5) CREDENTIALS + DEPLOY: add "Contact Web Research" + "Contact Judge Call" to test_builder_flag_parity.py
    SECRET_TO_CREDENTIAL_BOUND_NODES["ANTHROPIC_API_KEY"] AND deploy NODE_CREDENTIAL_MAP ({httpHeaderAuth,"LV
    Anthropic"}); add the MEDIUM-5 reverse assertion (every genericCredentialType anthropic HTTP node ⊆ expected);
    add a deploy test (gpt #9) in tests/test_deploy_n8n_workflows.py asserting both contact nodes receive the LV
    Anthropic credential ID after bind_credentials().

    (6) TESTS. tests/test_cloud_contacts_branch.py (mirror companies BFS): ten contact nodes + Merge Winners reachable
    from Webhook Trigger; both IF-false lanes reach Merge Winners; the providers-bypass path reaches the contact chain;
    the contact research prompt names no company-ICP field. tests/n8n/contactResearchChainRowFlow.test.mjs (mirror
    researchChainRowFlow.test.mjs with contact node names + a contact seed row): the row survives BOTH HTTP hops and
    Merge Winners produces a non-null merge (the row-recovery regression). Update tests/test_provider_gate_topology.py
    (HIGH-3): the `Normalize + Score -> Merge Winners` direct-edge assertion now expects `Normalize + Score -> Contact
    Research Trigger Gate`; keep the forbidden-set check. For the JG-2 assertion, mirror test_judge_spec.py's approach
    (LOW-8 — not a naive grep). Rebuild all workflows (all three enrichment JSONs regenerate — commit
    wf_enrichment_local.json too, HIGH-3) and run the FULL suite.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -m pytest tests/test_cloud_contacts_branch.py tests/test_builder_flag_parity.py tests/test_provider_gate_topology.py tests/test_deploy_n8n_workflows.py tests/test_companies_factory_frozen.py -q && node --test tests/n8n/contactResearchChainRowFlow.test.mjs tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cloud_contacts_branch.py passes: ten contact nodes + Merge Winners BFS-reachable; both IF-false lanes reach Merge Winners; providers-bypass reaches the chain; contact prompt names no company-ICP field.
    - tests/n8n/contactResearchChainRowFlow.test.mjs passes: no contact chain node throws; Validate Contact Research + Apply Contact Judge Verdict recover existingRecord/scored across both HTTP hops; Merge Winners merge is not null on the research lane.
    - tests/test_provider_gate_topology.py passes with the updated seam edge (Normalize + Score -> Contact Research Trigger Gate); the forbidden-set check still holds.
    - tests/test_builder_flag_parity.py passes incl. the reverse assertion; tests/test_deploy_n8n_workflows.py asserts Contact Web Research + Contact Judge Call bind to the LV Anthropic credential id.
    - The entry node strips injected markers (asserted in the contact row-flow or a topology/unit test: an input event carrying judge_flags.promoted_field does not reach the fold as trusted).
    - `grep -c "Contact Web Research\|Contact Judge Call\|Contact Research Trigger Gate\|Apply Contact Judge Verdict" n8n/wf_enrichment_cloud.json` is non-zero; the contact judge JG-2 check (mirroring test_judge_spec.py) shows no web_search/size/vendor.
    - tests/test_companies_factory_frozen.py (Plan 01) STAYS green; `git diff --stat` shows judge.js/webResearch.js/scoreEnrichment.js/mergeCompanies.js UNCHANGED; n8n/wf_enrichment_local.json is regenerated + committed.
    - Full offline suite green vs the 332 pytest / 179 node baseline (new tests net-positive); a second builder run yields byte-identical wf_enrichment_*.json.
  </acceptance_criteria>
  <reversibility rating="reversible">Builder wiring + fold + credential/deploy edits; revert the build_cloud_workflows.py/deploy edits and rebuild to remove the contact chain.</reversibility>
  <done>The contacts branch runs the full research->judge->verdict chain (ten contact-distinct nodes) at the seam feeding Merge Winners, with row-recovery across both HTTP hops (mirror bd682a2), marker-strip at entry, seniority fetched, the ENRICH_MERGE write-safety fold (research wins only when judge-promoted or filling a genuinely-blank gap), the two anthropic nodes credential-bound + deploy-binding-tested, the topology/row-flow/parity tests updated — all offline, deterministic rebuild, companies byte-identity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> Contact Web Research / Contact Judge Call | caller-triggered anthropic HTTP calls; must be credential-bound (unbound = 401 = silent research failure, 16.1 C2) and cost-gated |
| caller event -> row markers | ENRICH_PARSE_EVENT spreads raw event props, so research_candidate/judge_verdict/judge_flags/judge_promoted_fields are caller-INJECTABLE unless stripped at chain entry |
| model verdict -> canonical write | the verdict's chosen_field/chosen_value must not write an arbitrary field (e.g. email); allowlist {jobtitle,seniority} + decision/confidence gate |
| HTTP node -> row | Contact Web Research / Contact Judge Call REPLACE $json; the row must be recovered by paired index or the research lane loses existingRecord/scored |
| provider merge vs research fold | jobtitle/seniority overlap; an unadjudicated research value must not override a provider/existing value — merge-time write-safety gate |
| public web -> contact candidate | only public role facts (jobtitle/seniority) + evidence URL; no phone/email/mobile PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.2-04 | Tampering (regression) | a shared-module edit / hand-rolled companies copy changes companies jsCode | high | mitigate | Contact logic in NEW sibling modules; chain via Plan-01 parameterized factories with CONTACTS_TARGET; Plan-01 frozen guard re-run and stays green |
| T-16.2-05 | Information disclosure (PII) | over-collection of contact PII | medium | mitigate | jobtitle/seniority only; phone/email/mobile never in prompt or candidate; persist value+evidence+provenance; test asserts prompt names only role fields |
| T-16.2-06 | Denial of Service / cost | unbounded LLM spend | medium | mitigate | Inherited ALLOW_*/MAX_* caps (default OFF), enforced upstream of the HTTP nodes; no new global switch |
| T-16.2-07 | Spoofing / availability | anthropic node deployed unbound -> 401 -> silent failure | high | mitigate | Both contact nodes in SECRET_TO_CREDENTIAL_BOUND_NODES + deploy NODE_CREDENTIAL_MAP; a deploy test asserts bind_credentials() assigns the LV Anthropic id; reverse parity assertion catches an unlisted+unbound node |
| T-16.2-08 | Tampering (correctness) | the overlap fold clobbers a good provider/existing jobtitle/seniority with an unadjudicated research value | high | mitigate | foldContactResearch write-safety gate: research wins F only if judge-promoted (fresh judge_promoted_fields) OR F blank in BOTH provider and existingRecord (kimi HIGH-2); else provider/existing stands + decision rewritten withheld_by_overlap_precedence; node test pins every branch |
| T-16.2-09 | Tampering / EoP (injection) | caller injects judge_flags.promoted_field / research_candidate / a verdict chosen_field:'email' to forge a write | high | mitigate | Entry node strips all internal markers (gpt #5); applyContactJudgeVerdict allowlists chosen_field ∈ {jobtitle,seniority} + decision/confidence gate; the fold trusts only the fresh, chain-set judge_promoted_fields; tests pin both |
| T-16.2-10 | Tampering (row loss) | HTTP nodes replace $json -> research lane dead-ends at merge:null | high | mitigate | Validate/Apply-Verdict recover the row by paired index from the pre-HTTP node (mirror bd682a2); contactResearchChainRowFlow.test.mjs proves the row survives both hops |
| T-16.2-SC | Tampering (supply chain) | npm/pip installs | low | accept | No new package — pure JS/Python; node:test + pytest (not applicable) |
</threat_model>

<verification>
- SC-1: tests/test_cloud_contacts_branch.py (BFS) + tests/n8n/contactResearchChainRowFlow.test.mjs (row survives HTTP hops, merge not null).
- SC-2: chain emitted by the Plan-01 parameterized factories with CONTACTS_TARGET; companies frozen guard stays green.
- SC-3 (HONEST MIRROR): gap-fill + stale-refresh + existing-record conflict only — NO provider-vs-research adjudication (companies lacks it too); contact prompt/escalation/verdict jobtitle/seniority-scoped; research-only path works when providers none/off. foldContactResearch is write-safety, not adjudication.
- SC-4: contactResearch/contactJudge/mergeContacts node tests + BFS + row-flow + parity + deploy binding + topology-seam-update all green; full suite green vs 332 pytest / 179 node; second build byte-identical; zero live calls.
- Security: entry marker-strip; chosen_field allowlist; the fold trusts only the fresh judge_promoted_fields.
- Shared modules (judge.js, webResearch.js, scoreEnrichment.js, mergeCompanies.js) git-unchanged.

## Flagged for the record (reviews)
- KEY QUESTION (value): jobtitle+seniority research earns its cost via gap-fill + stale-refresh + existing-record conflict (the honest triad companies uses). Value rests on jobtitle; seniority rides the same chain at ~zero marginal cost (drop to jobtitle-only is the fallback).
- Track B (NOT fixed in 16.2, stated explicitly): (a) gpt #2 — genuine HubSpot private-app events carry only objectId/objectType, not email/name/company; 16.2 is scoped to caller-envelope/direct-field payloads (same 16.1 Track-B deferral) — a genuine-event path needs a deferred fetch-by-objectId node; (b) gpt #6 — mergeCompanies.js has the same stale-timestamp latent issue as the contact fix; (c) the companies research lane row-loss is already FIXED (bd682a2) — 16.2 mirrors the fixed shape.
</verification>

<success_criteria>
The contacts research->judge mirror is complete when the contacts branch runs the ten-node chain at the seam
feeding Merge Winners with the row surviving both HTTP hops (SC-1), emitted by ONE target-parameterized factory
set shared with companies (SC-2, companies byte-identical), doing the HONEST mirror behavior — gap-fill +
stale-refresh + existing-record conflict, jobtitle/seniority-scoped, research-only path working (SC-3) — with
chain-entry marker hygiene + a chosen_field-allowlisted verdict + a merge-time write-safety fold, the two
anthropic nodes credential-bound and deploy-binding-tested, and the mirrored offline suite (incl. the item-flow
row-recovery regression) green with a deterministic rebuild (SC-4) — no live calls, no PII beyond public role
facts + evidence.
</success_criteria>

<output>
Create `.planning/phases/16.2-contacts-research-judge-mirror/16.2-02-SUMMARY.md` when done.
</output>
