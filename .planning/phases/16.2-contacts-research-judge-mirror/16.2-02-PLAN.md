---
phase: 16.2-contacts-research-judge-mirror
plan: 02
type: execute
wave: 2
depends_on:
  - 16.2-01
autonomous: true
files_modified:
  - n8n/code/contactResearch.js
  - n8n/code/contactJudge.js
  - n8n/code/mergeContacts.js
  - scripts/build_cloud_workflows.py
  - scripts/deploy_n8n_workflows.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local_live.json
  - tests/n8n/contactResearch.test.mjs
  - tests/n8n/contactJudge.test.mjs
  - tests/n8n/mergeContacts.test.mjs
  - tests/test_cloud_contacts_branch.py
  - tests/test_builder_flag_parity.py
requirements:
  - "SC-1 (the contacts branch gains Contact Research Trigger Gate -> IF Contact Research Needed -> Build Contact Research Request -> Contact Web Research -> Validate Contact Research -> Contact Judge Gate -> IF Contact Needs Judge -> Build Contact Judge Request -> Contact Judge Call -> Apply Contact Judge Verdict, wired at the 16.1 seam, feeding Merge Winners)"
  - "SC-3 (contact research prompt + escalation reasons + verdict application are contact-appropriate — jobtitle/seniority only, not company-ICP fields; off/none/absent providers still degrade gracefully so the research-only path works)"
  - "SC-4 (offline tests mirror the companies research/judge coverage — BFS reachability, contact escalation/verdict, contact validate never-throws, the merge overlap-precedence, credential parity; full suite green; builder deterministic; no live calls)"

must_haves:
  truths:
    - "n8n/code/contactResearch.js exports `contactResearchCandidateFromHttpItem(item)` — NEVER throws on any HTTP failure shape (execution-error item, missing/empty content, error body, unparseable text) resolving to an unmatched candidate (skip-not-retry, CLAUDE.md §26.2), reusing webResearch.js's `extractFinalJson`/`normalizeUrlForMatch`/`extractPageAgeByField` + never-throw skeleton by co-inlining; and a `validateContactResearch(raw)` that coerces `data.jobtitle`/`data.seniority` to trimmed-string-or-null (NO taxonomy normalization — free-text), carries `evidence_by_field`, `matched` (default true unless non-dict, mirroring OC-4), `confidence`, and demotes a jobtitle/seniority value whose `evidence_by_field` URL is missing to null (tri-state, mirroring judge.js applyEvidenceSufficiency direction)."
    - "n8n/code/contactJudge.js exports `computeContactEscalation(researchCandidate, existingRecord)` with EXACTLY two arguments (judge.js:97-102 arity discipline; field config via a module-level `_CONTACT_JUDGE_FIELDS=[\"jobtitle\",\"seniority\"]`, never a 3rd arg), returning {needsJudge,reasons}. RO-1 carries over: an unmatched candidate never escalates. Triggers: `jobtitle_conflict` (existing jobtitle known AND research jobtitle differs), `seniority_conflict` (existing seniority known AND research seniority differs), `jobtitle_stale_refresh` (existing jobtitle present-but-stale AND research proposes a different value), `confidence_band` (confidence in ESCALATION_CONFIDENCE_BAND [75,85] AND the candidate carries a jobtitle/seniority signal). No vendor/veto/org reasons."
    - "n8n/code/contactJudge.js exports `applyContactUnadjudicated(researchCandidate, reasons)` that DROPS the conflicting field from the candidate (deletes data.jobtitle when reasons include jobtitle_conflict OR jobtitle_stale_refresh; deletes data.seniority when reasons include seniority_conflict) so the existing record value stands — the contact analog of judge.js:156-161; `buildContactJudgeRequestBody(row, model, maxTokens)` producing a 'contact role conflict' prompt restricted to jobtitle/seniority with NO size/vendor field and NO tools key (JG-2), the required-key list from JUDGE_OUTPUT_REQUIRED + chosen_field; and `applyContactJudgeVerdict(researchCandidate, verdict, reasons)` reusing judge.js `judgeVerdictFromHttpItem` (never-throws, JG-3 min-confidence) + the promote/demote shape of applyJudgeVerdict, routing non-promotions through applyContactUnadjudicated. These reuse `applyCostCap`/`judgeVerdictFromHttpItem` from judge.js verbatim by co-inlining. No A/R/G/T grounding (no scoreResearchCandidates) — RESEARCH Task 3.4."
    - "In the built wf_enrichment_cloud.json the ten contact-distinct nodes — 'Contact Research Trigger Gate', 'IF Contact Research Needed', 'Build Contact Research Request', 'Contact Web Research', 'Validate Contact Research', 'Contact Judge Gate', 'IF Contact Needs Judge', 'Build Contact Judge Request', 'Contact Judge Call', 'Apply Contact Judge Verdict' — are BFS-reachable from 'Webhook Trigger', wired Normalize + Score -> Contact Research Trigger Gate -> ... -> Apply Contact Judge Verdict -> Merge Winners, with BOTH IF-false lanes ('IF Contact Research Needed' false, 'IF Contact Needs Judge' false) fanning STRAIGHT into Merge Winners (the mirror of the companies IF Research Needed/IF Needs Judge false lanes into Merge Company at :3267-3278). The chain is emitted by the target-parameterized factories from Plan 01 with CONTACTS_TARGET (SC-2), not duplicated code."
    - "The contact research/judge chain sits DOWNSTREAM of Normalize + Score, independent of the 16.1 provider gate: with `providers: none`/off (zero provider HTTP nodes execute) the row still flows identity -> gates(bypass) -> Normalize + Score -> Contact Research Trigger Gate, and if ALLOW_WEB_RESEARCH is on and a gap is detected, research fills jobtitle/seniority from scratch (research-only path) — proven by a topology assertion following only the bypass lanes into the contact chain. Gating reuses the inherited six CONFIG_FLAG_DEFAULTS (ALLOW_WEB_RESEARCH/MAX_WEB_RESEARCH_PER_RUN gate research; ALLOW_SONNET_ESCALATION/MAX_SONNET_VALIDATIONS_PER_RUN gate the judge), both default OFF; NO new global switch."
    - "ENRICH_MERGE folds a SECOND mergeContacts call for the research candidate (mirror of ENRICH_MERGE_CO:1901-1931): when `row.research_candidate.matched`, build researchData from rc.data.{jobtitle,seniority} skipping null/blank, call mergeContacts(existingRecord, researchData, RESEARCH_POLICY, {source:'claude_web', confidence: rc.confidence, evidence: rc.evidence_by_field, confidenceByField: row.judge_confidence_by_field}) where RESEARCH_POLICY overrides jobtitle to system_owned (seniority is already system_owned) so a research value CAN promote in the research merge. OVERLAP PRECEDENCE — the contact-specific case, unlike the disjoint companies fold — is resolved by an EXPLICIT ADJUDICATED-GATE, NOT by spread-last: a pure exported helper `foldContactResearch(providerMerge, researchMerge, judgeConfidenceByField)` folds the two, and for each overlapping field F (jobtitle/seniority) the RESEARCH value wins ONLY when the judge actually promoted F — signalled by `judgeConfidenceByField[F] != null` (that map is populated ONLY on the adjudicated+chosen_field path at build_cloud_workflows.py:1824-1830) — OR when the provider produced NO canonical value for F (a genuine gap → research fills it, preserving the providers-none research-only path, SC-3). In every other case (the provider produced a value for F and the judge did NOT adjudicate F) the PROVIDER value stands. This makes 'research wins only when judge-adjudicated (or fills a genuine provider gap)' LITERALLY true, closing the clobber: `computeContactEscalation` compares research against `existingRecord` NOT `row.scored.winners`, so a provider-vs-research conflict is never an escalation trigger and can never be dropped upstream — the merge-time adjudicated-gate is the ONLY thing preventing an unadjudicated research value from silently overriding a provider value (Paths A/B below)."
    - "'Contact Web Research' and 'Contact Judge Call' are ANTHROPIC-credential-bound: added to SECRET_TO_CREDENTIAL_BOUND_NODES[\"ANTHROPIC_API_KEY\"] (test_builder_flag_parity.py:120) AND to deploy_n8n_workflows.py NODE_CREDENTIAL_MAP ({cred_type:httpHeaderAuth, cred_name:'LV Anthropic'}), so neither is an unbound anthropic node -> no 401 -> no silent research failure (the 16.1 C2 lesson); zero raw ANTHROPIC_API_KEY env-var reference survives in the cloud JSON."
    - "The full offline suite (`.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs`) is green with zero regressions vs the 332 pytest / 178 node baseline (new contact tests add to both counts), and a second builder run yields byte-identical wf_enrichment_*.json; the Plan-01 companies-byte-identity guard (tests/test_companies_factory_frozen.py) stays green (only node ids shift, no companies jsCode changes)."
  artifacts:
    - "n8n/code/contactResearch.js — contactResearchCandidateFromHttpItem + validateContactResearch (jobtitle/seniority + evidence, never-throws), require()ing webResearch.js generic helpers"
    - "n8n/code/contactJudge.js — _CONTACT_JUDGE_FIELDS, computeContactEscalation (2-arg), applyContactUnadjudicated, buildContactJudgeRequestBody, applyContactJudgeVerdict, require()ing judge.js field-agnostic exports"
    - "n8n/code/mergeContacts.js — a new pure exported `foldContactResearch(providerMerge, researchMerge, judgeConfidenceByField)` implementing the adjudicated-gate overlap precedence (unit-testable), co-located with mergeContacts since ENRICH_MERGE already inlines this module"
    - "scripts/build_cloud_workflows.py — contact call sites of the parameterized factories with CONTACTS_TARGET + contact-distinct node names; the ENRICH_MERGE 2nd-mergeContacts research fold using foldContactResearch (RESEARCH_POLICY jobtitle override for the research merge + the adjudicated-gate for the overlap); wiring at the 16.2 seam in build_enrichment_cloud() (:3234) and build_enrichment_local_live() (:2325-2344 analog); SECRET_TO_CREDENTIAL_BOUND_NODES already at parity"
    - "scripts/deploy_n8n_workflows.py — NODE_CREDENTIAL_MAP entries for Contact Web Research + Contact Judge Call (LV Anthropic)"
    - "tests/test_cloud_contacts_branch.py (BFS reachability + research-only bypass path + no company-ICP field in contact prompt), tests/n8n/contactResearch.test.mjs, tests/n8n/contactJudge.test.mjs, extended tests/n8n/mergeContacts.test.mjs (fold overlap-precedence), extended tests/test_builder_flag_parity.py (the two new anthropic nodes)"
  key_links:
    - "Normalize + Score -> Contact Research Trigger Gate -> IF Contact Research Needed [true->Build Contact Research Request->Contact Web Research->Validate Contact Research->Contact Judge Gate->IF Contact Needs Judge; false->Merge Winners] ; IF Contact Needs Judge [true->Build Contact Judge Request->Contact Judge Call->Apply Contact Judge Verdict->Merge Winners; false->Merge Winners]"
    - "Apply Contact Judge Verdict judge_confidence_by_field[chosen_field] (set only when the judge promoted that field) -> ENRICH_MERGE foldContactResearch adjudicated-gate -> research wins F only if judgeConfidenceByField[F] set OR provider produced no F; else provider value stands"
    - "CONTACTS_TARGET (Plan 01) -> parameterized factories -> contact node jsCode co-inlining contactResearch.js/contactJudge.js + judge.js/webResearch.js generic exports"
    - "Contact Web Research/Contact Judge Call -> SECRET_TO_CREDENTIAL_BOUND_NODES[ANTHROPIC_API_KEY] + deploy NODE_CREDENTIAL_MAP (LV Anthropic) -> credential-bound at deploy, no 401 silent failure"
  prohibitions:
    - "Do NOT edit n8n/code/judge.js, webResearch.js, scoreEnrichment.js, or mergeCompanies.js — reuse their field-agnostic exports by co-inlining the NEW sibling modules only (editing them breaks companies byte-identity, Plan 01's frozen guard). These four stay git-unchanged."
    - "Do NOT change any companies research/judge/validate/apply-verdict/merge Code node's jsCode — the Plan-01 frozen guard (tests/test_companies_factory_frozen.py) MUST stay green; the contact chain uses the parameterized factories with CONTACTS_TARGET, never a second hand-rolled copy of the companies bodies."
    - "Do NOT web-research or store phone/mobilephone/email — jobtitle+seniority ONLY; no PII field ever enters the contact research prompt or candidate (CONTEXT 1/7, CLAUDE.md §16). Persist only the verified value + evidence URL + provenance."
    - "computeContactEscalation MUST take exactly two arguments (researchCandidate, existingRecord) — field config via _CONTACT_JUDGE_FIELDS, never a 3rd arg (judge.js:97-102). Do NOT try to fix the overlap by feeding the provider winners (row.scored.winners) into computeContactEscalation — that both breaks the 2-arg arity AND is the wrong layer; the provider-vs-research precedence is resolved at the merge (foldContactResearch adjudicated-gate), not the escalation."
    - "The ENRICH_MERGE research fold MUST NOT spread research canonicalPatch last unconditionally — an unadjudicated research value must never override a provider value for jobtitle/seniority; the foldContactResearch adjudicated-gate is mandatory and must be unit-tested with a KEPT-but-unadjudicated conflicting value (not only the already-dropped happy path)."
    - "The Contact Judge Gate / Build Contact Judge Request built jsCode MUST contain NO `web_search` tool key and NO size/vendor field name (JG-2, mirror of the companies judge's contract)."
    - "No A/R/G/T grounding object (scoreResearchCandidates) on contacts v1 — do NOT inline scoreEnrichment.js into the contact judge gate (RESEARCH Task 3.4)."
    - "No new global kill-switch / config flag — reuse the inherited six CONFIG_FLAG_DEFAULTS exactly (16.1 Decision 1 stands)."
---

<objective>
Fill the Phase-16.2 seam: mirror the companies web-research->judge->verdict chain onto the CONTACTS branch
for jobtitle + seniority ONLY, reusing Plan 01's target-parameterized factories (CONTACTS_TARGET) plus two
new sibling JS modules (contactResearch.js, contactJudge.js) that reuse the field-agnostic exports of
judge.js/webResearch.js by co-inlining — so both object types share one set of building blocks and the
companies branch stays byte-identical (Plan 01's frozen guard proves it). After this plan a contact can be
gap-filled, stale-refreshed, and conflict-adjudicated for its current role exactly the way a company is for
its ICP classification, gated OFF by default behind the inherited ALLOW_WEB_RESEARCH/ALLOW_SONNET_ESCALATION
caps.

Purpose: this is the vertical capability the phase exists for. The two hard problems it solves are (1) the
contact merge OVERLAPS the provider jobtitle/seniority (unlike the disjoint companies fold) — resolved by a
research-scoped policy override (jobtitle->system_owned) that promotes only judge-CLEARED values, relying on
applyContactUnadjudicated to have already dropped anything unsafe so the provider value stands on an
unadjudicated conflict; and (2) the credential-binding of the two new anthropic HTTP nodes (the 16.1 C2
lesson — an unbound anthropic node is a 401 and a silent research failure). PII is minimized to public role
facts + evidence URL + provenance (no phone/email/mobile ever researched).

Output: contactResearch.js + contactJudge.js, the wired contact chain (ten contact-distinct nodes at the
seam in both cloud + local-live), the ENRICH_MERGE 2nd-merge research fold with overlap precedence,
credential bookkeeping in the flag-parity test + deploy map, and mirrored offline tests — deterministic
rebuild, green suite, companies byte-identity intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-RESEARCH.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-CONTEXT.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-01-PLAN.md

# Analog + target sources (read the specific ranges named in each task's read_first)
@scripts/build_cloud_workflows.py
@n8n/code/judge.js
@n8n/code/webResearch.js
@n8n/code/mergeContacts.js
@tests/test_cloud_companies_branch.py
@tests/test_builder_flag_parity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contactResearch.js + contactJudge.js sibling modules (+ their node unit tests)</name>
  <files>n8n/code/contactResearch.js, n8n/code/contactJudge.js, tests/n8n/contactResearch.test.mjs, tests/n8n/contactJudge.test.mjs</files>
  <read_first>
    - n8n/code/webResearch.js (FULL — reuse by require: extractFinalJson :124, normalizeUrlForMatch :72, extractPageAgeByField :97, the researchCandidateFromHttpItem never-throw skeleton :160-179, _unmatchedCandidate :152; the contact twin mirrors this shape but validates jobtitle/seniority instead of org_type/content)
    - n8n/code/judge.js (FULL — reuse by require: computeEscalation :103-136 (the 2-arg analog + confidence_band :129-133 using ESCALATION_CONFIDENCE_BAND), applyUnadjudicated :141-175 (the drop-conflicting-field analog), buildJudgeRequestBody :335-393 (the JG-2 no-size/no-tools prompt analog), judgeVerdictFromHttpItem :423-447 (reuse verbatim), applyJudgeVerdict :454-484 (the promote/demote shape), applyCostCap :184-195 (reuse verbatim); the arity comment :97-102)
    - n8n/code/escalation.generated.js:9-33 (ESCALATION_CONFIDENCE_BAND=[75,85], JUDGE_MIN_CONFIDENCE=80, JUDGE_OUTPUT_REQUIRED — the contact modules co-inline this alongside judge.js at build time, so these consts are in scope)
    - n8n/code/normalizeProviders.js:157-158,204-205,264-267 (all three providers push jobtitle+seniority as free-text strings — so no closed vocabulary; validateContactResearch passes them through un-normalized, Assumption A2)
    - tests/n8n/webResearchFailure.test.mjs + tests/n8n/judge.test.mjs + tests/n8n/judgeFailure.test.mjs (the node:test + createRequire never-throw test style to mirror)
    - RESEARCH Task 2 (prompt/escalation design), Task 3.1-3.2 (validate/verdict), Analogs table
  </read_first>
  <behavior>
    contactResearch.js:
    - Test: contactResearchCandidateFromHttpItem never throws on {error:...}, missing content, {"type":"error"} body, unparseable text, empty content -> unmatched candidate (matched:false).
    - Test: a valid item with data.jobtitle/data.seniority + evidence_by_field -> matched candidate carrying trimmed jobtitle/seniority + evidence_by_field + confidence.
    - Test: validateContactResearch coerces "  Head of Racing  " -> "Head of Racing", missing/non-string -> null; non-dict raw -> matched:false.
    - Test: a jobtitle value with NO evidence_by_field.jobtitle URL is demoted to null (tri-state evidence sufficiency).
    contactJudge.js:
    - Test: computeContactEscalation returns needsJudge:false for an unmatched candidate (RO-1).
    - Test: jobtitle_conflict fires when existing jobtitle known AND research jobtitle differs; not when equal or existing blank.
    - Test: seniority_conflict fires analogously; jobtitle_stale_refresh fires when existing jobtitle present-but-stale AND research proposes a different value; confidence_band fires in [75,85] with a jobtitle/seniority signal.
    - Test: computeContactEscalation has exactly arity 2 (fn.length === 2).
    - Test: applyContactUnadjudicated drops data.jobtitle on jobtitle_conflict/jobtitle_stale_refresh, data.seniority on seniority_conflict; leaves others; returns a NEW object.
    - Test: buildContactJudgeRequestBody body contains no `web_search`/tools key and no size/vendor field, requires the JUDGE_OUTPUT_REQUIRED+chosen_field keys, and names jobtitle/seniority only.
    - Test: applyContactJudgeVerdict promotes on decision promote/confirm >= JUDGE_MIN_CONFIDENCE writing data[chosen_field]=chosen_value; routes needs_review/reject/sub-threshold through applyContactUnadjudicated; never throws on a garbage verdict (via judgeVerdictFromHttpItem).
  </behavior>
  <action>
    Write the two sibling modules per the must_have + behavior. Both `require("./judge")` / `require("./webResearch")`
    for the field-agnostic helpers (satisfies the node-test/dev-oracle path; the builder's inline() drops the
    require lines and co-inlines the named modules into the contact nodes). Keep contactResearch.js thin: a
    validateContactResearch (free-text jobtitle/seniority coercion, evidence carry, tri-state evidence demotion)
    + a contactResearchCandidateFromHttpItem that reuses webResearch.js's extractFinalJson / extractPageAgeByField /
    never-throw try-catch skeleton (mirror researchCandidateFromHttpItem :160-179, swapping validateResearchOutput
    for validateContactResearch). Keep contactJudge.js to `_CONTACT_JUDGE_FIELDS=["jobtitle","seniority"]`,
    computeContactEscalation (2-arg, module-level field const — NEVER a 3rd arg), applyContactUnadjudicated,
    buildContactJudgeRequestBody (contact 'role conflict' system prompt: adjudicate current title/seniority
    strictly from cited evidence, needs_review with null when no citation, JG-2 identity+role only, NO tools),
    and applyContactJudgeVerdict (reuse judgeVerdictFromHttpItem verbatim + the promote/demote shape). Export
    everything the CONTACTS_TARGET config names. Do NOT edit judge.js/webResearch.js. Write the two node tests
    (node:test + createRequire) covering every behavior case above.
  </action>
  <verify>
    <automated>node --test tests/n8n/contactResearch.test.mjs tests/n8n/contactJudge.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/n8n/contactResearch.test.mjs passes: never-throws on all failure shapes; validates jobtitle/seniority + evidence; tri-state evidence demotion.
    - node --test tests/n8n/contactJudge.test.mjs passes: RO-1; the four escalation triggers; arity === 2; applyContactUnadjudicated drops the conflicting field; buildContactJudgeRequestBody has no web_search/size/vendor; applyContactJudgeVerdict promote/demote + never-throws.
    - `grep -n "module.exports" n8n/code/contactResearch.js n8n/code/contactJudge.js` shows the exports the CONTACTS_TARGET names.
    - `git diff --stat` shows judge.js, webResearch.js, scoreEnrichment.js, mergeCompanies.js UNCHANGED.
    - `grep -c "web_search" n8n/code/contactJudge.js` returns 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Two new pure JS modules + two node tests; delete them to revert (no builder change yet).</reversibility>
  <done>contactResearch.js + contactJudge.js carry the contact field-bound logic (jobtitle/seniority research validate, 2-arg escalation, drop-conflicting unadjudicated, no-size/no-tools judge prompt, never-throw verdict) reusing judge.js/webResearch.js generic exports by require/co-inline, proven by mirrored node tests — the shared modules untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the contact chain at the seam (cloud + local-live) + ENRICH_MERGE research fold (overlap precedence) + credential bookkeeping + BFS/parity tests</name>
  <files>scripts/build_cloud_workflows.py, scripts/deploy_n8n_workflows.py, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local_live.json, tests/test_cloud_contacts_branch.py, tests/test_builder_flag_parity.py, tests/n8n/mergeContacts.test.mjs</files>
  <read_first>
    - scripts/build_cloud_workflows.py:2200-2205 + 2939-2954 (the contacts seam in local-live / cloud: Normalize + Score -> Merge Winners -> Set Data Quality + Gap Flag -> Decide Action); :3234 (the cloud contacts conns chain the contact chain must be spliced into); :3264-3282 (the companies research/judge conns — the EXACT mirror shape to reproduce with contact-distinct names + both IF-false lanes into the convergence)
    - scripts/build_cloud_workflows.py:3074-3110 (the companies contact-chain node declarations: code_node + _if_bool_node + _http_node with auth=header anthropic + json_body; the shape to copy with CONTACTS_TARGET and contact-distinct names) and :2251-2266 (the local-live analog with _live_http + _env_secret_expr(ANTHROPIC_API_KEY))
    - scripts/build_cloud_workflows.py:862-884 (ENRICH_MERGE — the 2nd-merge fold target; the provider mergeContacts call at :880 promoting email/mobilephone/phone/jobtitle/seniority/lv_linkedin_url) and :1901-1931 (ENRICH_MERGE_CO's 2nd mergeCompanies fold — the structural analog, DISJOINT there vs OVERLAPPING here)
    - n8n/code/mergeContacts.js (Plan 01 port: evidence/confidenceByField opts; DEFAULT_CONTACT_POLICY jobtitle=stale_refreshable :28, seniority=system_owned :30 — so RESEARCH_POLICY need only override jobtitle; this task ADDS the pure `foldContactResearch` helper here)
    - build_cloud_workflows.py:1824-1830 (judge_confidence_by_field is populated ONLY on the adjudicated+chosen_field path — the per-field 'judge promoted this' signal the fold gates on) and n8n/code/judge.js:103 (computeContactEscalation compares research to existingRecord, NOT row.scored.winners — WHY a provider-vs-research conflict is never an escalation trigger and the gate must live at the merge, not the escalation)
    - tests/test_cloud_companies_branch.py:26-62 (the _reachable_from BFS + node-list assertion to mirror for contacts) ; tests/test_builder_flag_parity.py:114-141 (SECRET_TO_CREDENTIAL_BOUND_NODES + the credential-bound assertion)
    - scripts/deploy_n8n_workflows.py:44-71 (NODE_CREDENTIAL_MAP — Claude Web Research/Judge Call are LV Anthropic; add the two contact nodes the same way)
    - RESEARCH Task 3.3 (the fold + overlap precedence), Task 4 (gating independence from providers), Task 5 (the mirrored test map), Open Questions 2 & 3
  </read_first>
  <action>
    (1) DECLARE the ten contact-distinct nodes in build_enrichment_cloud() and build_enrichment_local_live(),
    calling the Plan-01 parameterized factories with CONTACTS_TARGET: "Contact Research Trigger Gate"
    (_enrich_research_gate_js(target=CONTACTS_TARGET)), _if_bool_node("IF Contact Research Needed",
    "research_needed"), "Build Contact Research Request", the "Contact Web Research" HTTP node
    (anthropic messages, auth=header cloud / _env_secret_expr(ANTHROPIC_API_KEY) local-live,
    json_body=$json.research_request_body), "Validate Contact Research" (the parameterized validate producer
    with CONTACTS_TARGET, inlining webResearch.js+contactResearch.js), "Contact Judge Gate", _if_bool_node
    ("IF Contact Needs Judge", "needs_judge"), "Build Contact Judge Request", the "Contact Judge Call" HTTP
    node, "Apply Contact Judge Verdict". Use contact-distinct names throughout (n8n keys on node name — a
    name collision with the companies chain would break both).

    (2) WIRE at the seam. In build_enrichment_cloud(), replace the direct Normalize + Score -> Merge Winners
    edge (part of the chain at :3234) with: Normalize + Score -> Contact Research Trigger Gate; then reproduce
    the companies conns shape (:3266-3281) with contact names: Contact Research Trigger Gate -> IF Contact
    Research Needed [true -> Build Contact Research Request; false -> Merge Winners]; Build Contact Research
    Request -> Contact Web Research -> Validate Contact Research -> Contact Judge Gate -> IF Contact Needs Judge
    [true -> Build Contact Judge Request; false -> Merge Winners]; Build Contact Judge Request -> Contact Judge
    Call -> Apply Contact Judge Verdict -> Merge Winners. Both IF-false lanes fan STRAIGHT into Merge Winners.
    Do the same at the local-live seam (:2325-2344 analog). Keep the rest of the contacts tail
    (Merge Winners -> Set Data Quality + Gap Flag -> Decide Action -> IF Create ...) intact.

    (3) ENRICH_MERGE 2ND-MERGE FOLD with an EXPLICIT ADJUDICATED-GATE (the BLOCKER fix — do NOT rely on
    upstream drops). First understand the flaw the checker caught: `computeContactEscalation(researchCandidate,
    existingRecord)` (2-arg, Task 1) compares research against the EXISTING HubSpot record, but the provider
    winners live in `row.scored.winners` and are NOT folded into existingRecord until Merge Winners (downstream
    of the whole research/judge chain). So a provider-vs-research conflict is NEVER an escalation trigger and is
    NEVER dropped by applyContactUnadjudicated — a spread-last fold would let an unadjudicated research value
    silently clobber a provider value (Path A: existing blank, provider X, research Y conf 90 -> no trigger ->
    Y wins; Path B: confidence_band fires but escalation OFF -> confidence_band is not in the drop-set -> jobtitle
    survives -> wins). The fix is a merge-time gate, NOT a change to the 2-arg escalation (do NOT fold provider
    winners into computeContactEscalation — that breaks the arity discipline).

    Add a pure exported helper `foldContactResearch(providerMerge, researchMerge, judgeConfidenceByField)` to
    n8n/code/mergeContacts.js (ENRICH_MERGE already inlines this module, so no new inline; keep it a small pure
    function so it is directly unit-testable). It starts from providerMerge (canonicalPatch/provenance/cacheKeys,
    decisions) and, for each field F present in researchMerge.canonicalPatch, applies the ADJUDICATED-GATE:
    research wins F (write researchMerge's canonicalPatch[F] + provenance[F] + cacheKeys) ONLY when
    `judgeConfidenceByField && judgeConfidenceByField[F] != null` (the judge promoted F — that map is populated
    ONLY on the adjudicated+chosen_field path, build_cloud_workflows.py:1824-1830) OR when
    `!Object.prototype.hasOwnProperty.call(providerMerge.canonicalPatch, F)` (the provider produced NO canonical
    value for F, a genuine gap — research fills it, preserving the providers-none research-only path). Otherwise
    the provider value stands (skip research's F). Always concat `decisions` from both (full audit trail). Return
    {canonicalPatch, provenance, cacheKeys, decisions}.

    In ENRICH_MERGE (after the existing provider mergeContacts call at :880), if row.research_candidate?.matched:
    build researchData = {jobtitle, seniority} from rc.data skipping null/blank; define RESEARCH_POLICY =
    DEFAULT_CONTACT_POLICY with jobtitle overridden to {class:"system_owned", min_confidence:75} (seniority is
    already system_owned) SO THAT a research value can promote in the research merge; call mergeContacts(
    existingRecord, researchData, RESEARCH_POLICY, {source:"claude_web", confidence: rc.confidence || 80,
    evidence: rc.evidence_by_field || {}, confidenceByField: row.judge_confidence_by_field || {}}); then fold via
    `foldContactResearch(providerMerge, researchMerge, row.judge_confidence_by_field || {})`. Document the
    adjudicated-gate in a builder comment. mergeContacts.js is inlined only into contacts, so this changes the
    contacts Merge Winners jsCode only (companies untouched — the Plan-01 frozen guard stays green).

    (4) CREDENTIALS. Add "Contact Web Research" and "Contact Judge Call" to
    test_builder_flag_parity.py SECRET_TO_CREDENTIAL_BOUND_NODES["ANTHROPIC_API_KEY"] and to
    deploy_n8n_workflows.py NODE_CREDENTIAL_MAP ({cred_type:"httpHeaderAuth", cred_name:"LV Anthropic"}).
    ZoomInfo/Lusha/Apollo/HubSpot maps are unaffected.

    (5) TESTS. Write tests/test_cloud_contacts_branch.py (mirror test_cloud_companies_branch.py:26-62): BFS
    from "Webhook Trigger" reaches all ten contact-distinct nodes + Merge Winners; both IF-false lanes reach
    Merge Winners; the research-only bypass path — following only the provider gates' bypass lanes from the
    contacts dispatch into Normalize + Score -> Contact Research Trigger Gate — reaches the contact chain
    (providers-independent gating, RESEARCH Task 4); and the contact research prompt jsCode names no company-ICP
    field (no lv_org_type/lv_produces_content/hardware/gambling).

    Extend tests/n8n/mergeContacts.test.mjs to test `foldContactResearch` DIRECTLY (the pure helper) with the
    cases that catch the clobber the checker found — these MUST fail against a naive spread-last fold:
      (a) ADJUDICATED WINS: providerMerge.canonicalPatch.jobtitle="X", researchMerge.canonicalPatch.jobtitle="Y",
          judgeConfidenceByField={jobtitle:88} -> folded canonicalPatch.jobtitle === "Y" (research wins).
      (b) UNADJUDICATED, PROVIDER PRESENT -> PROVIDER STANDS: provider jobtitle="X", research jobtitle="Y",
          judgeConfidenceByField={} -> folded canonicalPatch.jobtitle === "X" (provider stands).
      (c) PATH A (blank existing, provider filled X, research Y conf 90, no trigger): same shape as (b) —
          provider produced X, judgeConfidenceByField empty -> canonicalPatch.jobtitle === "X".
      (d) PATH B (confidence_band fired, escalation OFF -> not adjudicated): provider jobtitle="X", research
          jobtitle="Y", judgeConfidenceByField={} -> canonicalPatch.jobtitle === "X" (provider stands).
      (e) RESEARCH-ONLY GAP-FILL: provider canonicalPatch has NO jobtitle (providers:none), research jobtitle="Y"
          unadjudicated -> canonicalPatch.jobtitle === "Y" (research fills the genuine gap — SC-3 preserved).
    Also keep a seniority twin of (a)/(b). Extend the flag-parity test if needed for the two new nodes. Rebuild
    and run the FULL suite (`.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs`).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -m pytest tests/test_cloud_contacts_branch.py tests/test_builder_flag_parity.py tests/test_companies_factory_frozen.py -q && node --test tests/n8n/mergeContacts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cloud_contacts_branch.py passes: all ten contact-distinct nodes + Merge Winners are BFS-reachable from Webhook Trigger; both IF-false lanes reach Merge Winners; the providers-bypass path reaches the contact chain; the contact research prompt names no company-ICP field.
    - tests/test_builder_flag_parity.py passes: ANTHROPIC_API_KEY's credential-bound set includes Contact Web Research + Contact Judge Call, and zero raw ANTHROPIC_API_KEY env-var reference survives in the cloud JSON.
    - tests/test_companies_factory_frozen.py (Plan 01) STAYS green — companies Code-node jsCode is unchanged (only node ids shift).
    - node --test tests/n8n/mergeContacts.test.mjs passes the foldContactResearch adjudicated-gate cases (a)-(e): adjudicated research wins; unadjudicated-with-provider-present leaves the provider value (Paths A & B); research-only gap-fill still fills — cases that FAIL against a spread-last fold, so the guard actually catches the clobber.
    - `grep -c "Contact Web Research\|Contact Judge Call\|Contact Research Trigger Gate\|Apply Contact Judge Verdict" n8n/wf_enrichment_cloud.json` returns a non-zero count.
    - `grep -c "web_search" <the built Contact Judge Gate / Build Contact Judge Request jsCode>` is 0 (JG-2); the contact judge nodes name no size/vendor field.
    - The full offline suite is green vs the 332 pytest / 178 node baseline (new tests net-positive, zero regressions); a second builder run yields byte-identical wf_enrichment_*.json.
    - `git diff --stat` shows judge.js, webResearch.js, scoreEnrichment.js, mergeCompanies.js UNCHANGED.
  </acceptance_criteria>
  <reversibility rating="reversible">Builder wiring + fold + credential-map edits; revert the build_cloud_workflows.py/deploy edits and rebuild to remove the contact chain.</reversibility>
  <done>The contacts branch runs the full research->judge->verdict chain (ten contact-distinct nodes) at the 16.2 seam feeding Merge Winners, emitted by the Plan-01 parameterized factories with CONTACTS_TARGET; the ENRICH_MERGE research fold uses foldContactResearch's adjudicated-gate so a research jobtitle/seniority wins ONLY when the judge promoted it or the provider produced nothing for it (an unadjudicated conflict leaves the provider value standing — cases (a)-(e) pinned by test); the two anthropic nodes are credential-bound; providers none/off still reaches the chain; and the mirrored offline tests are green with companies byte-identity intact and a deterministic rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> Contact Web Research / Contact Judge Call | caller-triggered anthropic HTTP calls; must be credential-bound (an unbound node = 401 = silent research failure, the 16.1 C2 lesson) and cost-gated |
| public web -> contact candidate | only public role facts (jobtitle/seniority) + evidence URL may be captured; no phone/email/mobile PII |
| provider merge vs research fold | jobtitle/seniority overlap; an UNADJUDICATED research value must NOT override a provider value — the merge-time adjudicated-gate (foldContactResearch) is the only thing preventing it, since the 2-arg escalation compares research to existingRecord not the provider winners |
| shared JS module -> companies Code node | contact logic must live in sibling modules co-inlined into contacts; editing judge.js/webResearch.js breaks companies byte-identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.2-04 | Tampering (regression) | a shared-module edit or a second hand-rolled companies copy changes companies jsCode | high | mitigate | Task 1 puts contact logic in NEW sibling modules reusing judge.js/webResearch.js by co-inline; Task 2 wires via the Plan-01 parameterized factories with CONTACTS_TARGET, never a companies-body copy; the Plan-01 frozen guard is re-run and must stay green |
| T-16.2-05 | Information disclosure (PII) | over-collection of contact PII into HubSpot/provenance | medium | mitigate | jobtitle/seniority ONLY; phone/email/mobile never enter the research prompt or candidate; persist only the verified value + evidence URL + provenance (CONTEXT 1/7, CLAUDE.md §16); tests assert the prompt names only role fields |
| T-16.2-06 | Denial of Service / cost | unbounded LLM spend on contact research/judge | medium | mitigate | Reuse the inherited ALLOW_WEB_RESEARCH/MAX_WEB_RESEARCH_PER_RUN + ALLOW_SONNET_ESCALATION/MAX_SONNET_VALIDATIONS_PER_RUN caps (default OFF), enforced upstream of the HTTP nodes by the shared factory scaffolding; no new global switch |
| T-16.2-07 | Spoofing / availability | an anthropic HTTP node deployed without a bound credential -> 401 -> silent research failure | high | mitigate | Task 2 adds Contact Web Research + Contact Judge Call to SECRET_TO_CREDENTIAL_BOUND_NODES (parity test) AND deploy NODE_CREDENTIAL_MAP (LV Anthropic); test asserts both are credential-bound and zero raw ANTHROPIC_API_KEY env-ref survives |
| T-16.2-08 | Tampering (correctness) | the overlapping research fold clobbers a good provider jobtitle/seniority with an UNADJUDICATED research value (computeContactEscalation compares research against existingRecord, NOT the provider winners in row.scored.winners, so a provider-vs-research conflict is never an escalation trigger — the checker-found blocker) | high | mitigate | `foldContactResearch` applies an explicit ADJUDICATED-GATE at merge time: a research value wins jobtitle/seniority ONLY when the judge promoted that field (judgeConfidenceByField[F] set, populated only on the adjudicated+chosen_field path) OR the provider produced no value for F (genuine gap); otherwise the provider value stands. NOT relying on upstream drops (which cannot see provider winners). A node test pins cases (a)-(e) including the two clobber paths — cases that fail against spread-last |
| T-16.2-SC | Tampering (supply chain) | npm/pip installs | low | accept | No new package — pure JS/Python; tests reuse node:test + pytest (RESEARCH Package Legitimacy: not applicable) |
</threat_model>

<verification>
- SC-1: tests/test_cloud_contacts_branch.py — the ten contact-distinct nodes + Merge Winners BFS-reachable from Webhook Trigger; both IF-false lanes into Merge Winners.
- SC-2: the chain is emitted by the Plan-01 parameterized factories with CONTACTS_TARGET (no duplicated companies bodies); the companies frozen guard stays green.
- SC-3: contact prompt/escalation/verdict are jobtitle/seniority-scoped (no company-ICP field, no size/vendor, no tools in the judge); the providers-bypass path reaches the contact chain (research-only works).
- SC-4: contactResearch.test.mjs / contactJudge.test.mjs / the mergeContacts fold test / the BFS + credential-parity tests all green; full suite green vs 332 pytest / 178 node baseline; second builder run byte-identical; zero live calls.
- Credentials: Contact Web Research + Contact Judge Call credential-bound (flag-parity test + deploy NODE_CREDENTIAL_MAP); no raw ANTHROPIC_API_KEY env-ref in cloud JSON.
- Shared modules (judge.js, webResearch.js, scoreEnrichment.js, mergeCompanies.js) git-unchanged.

## Flagged for cross-AI review (KEY OPEN DECISION, RESEARCH §KEY QUESTION + Open Questions)
- Value: does jobtitle+seniority contact research earn its LLM+PII cost given providers already return both? Recommendation: YES-narrow-OFF-by-default (gap-fill + stale-refresh + conflict-adjudication, the same triad that justifies it for companies). Fallback if reviewers disagree: jobtitle-only (seniority reuses the same chain at ~zero marginal cost, so dropping it saves little).
- The overlap-precedence design is the contact-specific deviation from the disjoint companies fold: a merge-time ADJUDICATED-GATE (foldContactResearch) — research wins jobtitle/seniority only when the judge promoted that field OR the provider produced nothing for it, else the provider value stands. This replaces the earlier (rejected) "safe by upstream drop" claim, which was false because computeContactEscalation compares research to existingRecord not the provider winners. Reviewers should confirm the gate handles gap-fill vs conflict correctly.
- Omitting the A/R/G/T grounding (scoreResearchCandidates) for contacts v1 (RESEARCH Task 3.4 / Assumption A3) — additive to add later; reviewers may want grounding parity.
- max_tokens=1024 for the 2-field contact research response (Assumption A5) — tune upward only if a future live smoke shows truncation.
</verification>

<success_criteria>
The contacts research->judge mirror is complete when the contacts branch runs the ten-node
research->judge->verdict chain at the 16.2 seam feeding Merge Winners (SC-1), emitted by ONE
target-parameterized factory set shared with companies (SC-2, companies byte-identical), with a
contact-appropriate jobtitle/seniority prompt + escalation + verdict and a working research-only path when
providers are none/off (SC-3), the two anthropic nodes credential-bound, the ENRICH_MERGE research fold
resolving the jobtitle/seniority overlap by promoting only judge-cleared values, and the mirrored offline
suite green with a deterministic rebuild (SC-4) — no live calls, no PII beyond public role facts + evidence.
</success_criteria>

<output>
Create `.planning/phases/16.2-contacts-research-judge-mirror/16.2-02-SUMMARY.md` when done.
</output>
