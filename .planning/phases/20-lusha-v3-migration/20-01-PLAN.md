---
phase: 20-lusha-v3-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe_lusha_v3.py
  - docs/LUSHA-V3-CONTRACT.md
autonomous: false
requirements:
  - REQ-lusha-v3-contract-probe
user_setup: []

must_haves:
  truths:
    - "A live `POST https://api.lusha.com/v3/contacts/search-and-enrich` returned 200 for a real identity, and the exact request body shape that produced it is written down verbatim."
    - "A live `POST https://api.lusha.com/v3/companies/search-and-enrich` returned 200 for a real domain, and its request/response envelope is written down verbatim."
    - "The two-step `POST /v3/contacts/search` -> `POST /v3/contacts/enrich` pair was exercised, and the doc states whether it saves credits over the combined endpoint for this waterfall's one-identity-per-call usage."
    - "The reveal vocabulary is confirmed from a live response (not guessed), and the measured credit delta between a reveal-nothing call and a reveal-phones call is recorded as numbers."
    - "Passing a previously returned Lusha record id back on a second enrich call is recorded with its observed credit cost (confirms or refutes the free-re-enrichment premise)."
    - "A deliberate no-match identity's response envelope and its charged/not-charged signal are recorded."
    - "`check_provider_credits.py` printed a Lusha remaining-credits number from `GET https://api.lusha.com/v3/account/usage`, and the `Lusha Usage` HTTP node is confirmed to source that URL from `provider_registry.py` rather than a hard-coded literal."
    - "Total credits spent by the whole probe session is recorded and is under 40."
  artifacts:
    - scripts/probe_lusha_v3.py
    - docs/LUSHA-V3-CONTRACT.md
  key_links:
    - "docs/LUSHA-V3-CONTRACT.md -> every later plan in this phase reads its confirmed field names instead of RESEARCH.md's hypothesis."
    - "scripts/probe_lusha_v3.py -> os.getenv('LUSHA_API_KEY') only; the key is never printed, never written into the contract doc."
    - "provider_registry.PROVIDER_REGISTRY['lusha']['credit']['url'] -> build_cloud_workflows.py:3943 `Lusha Usage` node (already v3; verify, do not migrate)."
---

<objective>
Establish the Lusha v3 wire contract from live 200s before a single request builder changes.

RESEARCH.md's entire v3 contract section is WebSearch-snippet-derived (`docs.lusha.com` is a
JS-rendered SPA that could not be fetched byte-exact) and is explicitly flagged as "the probe's
starting hypothesis, not ground truth". Assumptions A2 (body field names), A3 (reveal vocabulary
and credit model) and A7 (record id -> free re-enrichment) are load-bearing for
REQ-lusha-selective-reveal and REQ-lusha-id-staging respectively — if A3 or A7 is wrong, those
two requirements cannot be built as specified and the phase must be re-planned rather than
built on a false premise. BUG 17 is the in-repo precedent for what happens when a provider
request shape is assumed instead of probed: every call 400'd for weeks, invisible because
`onError: "continueRegularOutput"` turns a malformed request into a normal-looking item.

Purpose: make the next three plans deterministic edits against a known contract.
Output: `scripts/probe_lusha_v3.py` (re-runnable, disarmed by default) and
`docs/LUSHA-V3-CONTRACT.md` (the contract of record, with an assumption-verdict table).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-lusha-v3-migration/20-RESEARCH.md
@.planning/phases/20-lusha-v3-migration/20-PATTERNS.md
@scripts/check_provider_credits.py
@scripts/provider_registry.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end contacts-lane v3 probe — one identity, one endpoint, real 200</name>
  <files>scripts/probe_lusha_v3.py</files>
  <read_first>
    - `scripts/check_provider_credits.py` (lines 1-45 and `_check_lusha`/`_extract_lusha`) — the
      idioms this script must copy: a `_has_<provider>()` env gate, "skipped (no provider creds)"
      + exit 0 when the key is absent, NEVER print a secret value, never raise on 4xx/5xx/timeout.
    - `scripts/provider_registry.py` lines 16-24 — the already-v3 Lusha `credit` entry
      (`GET https://api.lusha.com/v3/account/usage`, header `api_key`, path `credits.remaining`).
    - `scripts/dryrun_batch.mjs` lines 28-76 — the `CANDIDATES` list (real identities with known
      2026-07-30 match outcomes), `identityKeys()`, and the resilient `call()` wrapper that
      returns `{status, body, err}` and never throws. Port that resilience shape to Python.
    - `.planning/phases/20-lusha-v3-migration/20-RESEARCH.md` "Assumptions Log" (A1-A7) and
      "Common Pitfalls" 1 and 2.
    - `.planning/milestones/v0.4-phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md`
      "Command form — `.env` loading" — the in-process `load_dotenv()` wrapper this repo uses
      because `.env` cannot be read by any shell command here.
  </read_first>
  <precondition>`LUSHA_API_KEY` is present in `.env` at the repo root; it is loaded in-process by the `python-dotenv` wrapper shown in the verify block, never by reading `.env` from a shell.</precondition>
  <reversibility rating="costly">Live probe calls debit real Lusha credits (balance ~3,921 as of 2026-07-30); the debit is not refundable, so the script caps cumulative spend and aborts rather than sweeping.</reversibility>
  <action>
Create `scripts/probe_lusha_v3.py`, a read-only contract prover in the same shape as
`scripts/check_provider_credits.py` (stdlib + `requests`; both already installed — add no
dependency, run no package-manager install).

Behavior:

1. `LUSHA_API_KEY` via `os.getenv`. Absent -> print `skipped (no LUSHA_API_KEY)`, exit 0, zero
   HTTP calls. The key value is never printed, never logged, never included in any captured
   request echo: when echoing a request for the report, emit the header as
   `api_key: <redacted>`.
2. Disarmed by default. Live HTTP only when `ALLOW_LUSHA_PROBE=true`. Disarmed mode prints the
   full ladder of request shapes it WOULD send (method, URL, headers with the key redacted,
   JSON body) and makes zero HTTP calls — so the URL and body literals are inspectable and
   greppable offline.
3. Cost guard (this is the cost-abuse mitigation, threat T-20-02): read
   `GET https://api.lusha.com/v3/account/usage` -> `credits.remaining` once at start, again
   after every billable step. Maintain a cumulative delta. Abort the remaining ladder and print
   a `PROBE ABORTED: credit cap reached` banner if the cumulative delta exceeds
   `PROBE_MAX_CREDITS` (default 40). Also cap the number of billable calls at
   `PROBE_MAX_BILLABLE` (default 8).
4. Probe step P1, the tracer path — contacts lane, ONE identity, first 200 wins. Use
   `{firstName: "Kyle", lastName: "Bettler", companyName: "Racing NSW", companyDomain: "racingnsw.com.au"}`
   from the existing `CANDIDATES` list (a known 2026-07-30 matcher). `POST` to
   `https://api.lusha.com/v3/contacts/search-and-enrich` with headers
   `api_key: <key>` and `Content-Type: application/json`. Try body shapes in order and STOP at
   the first HTTP 200, recording status + full response body + response headers for EVERY
   attempt including the failures (the 400 bodies are contract evidence — they name the
   rejected/expected properties, which is exactly how the v2 identity restriction was
   discovered):
     - shape A: `{"contacts": [{"contactId": "1", "firstName": ..., "lastName": ..., "companyName": ..., "companyDomain": ...}]}`
     - shape B: `{"contacts": [{"contactId": "1", "fullName": ..., "companyName": ...}]}`
     - shape C: flat single identity, no array wrapper: `{"firstName": ..., "lastName": ..., "companyName": ..., "companyDomain": ...}`
   Capture, per attempt: HTTP status, the response body verbatim, and every response header whose
   lowercased name contains `rate` or `left` or `limit` (the rate-limit signal set from
   assumption A6).
5. Emit one machine-readable ledger to stdout: a JSON object
   `{"steps": [{"name", "method", "url", "request_body", "status", "response_body", "headers", "credits_before", "credits_after", "credit_delta"}], "totals": {...}}`.
   Accept `--out <path>` to also write that JSON next to stdout, so Task 2 can transcribe from a
   file rather than from scrollback.
6. Never raise. Any transport error becomes a step with `status: 0` and an `err` string, matching
   the never-fail contract `check_provider_credits.py` documents.

Do NOT probe any `/prospecting/*` endpoint (RESEARCH.md Pitfall 1: a different product line,
declared out of scope in CLAUDE.md). If a response contains a top-level `requestId` paired with a
plural `contactIds` array, record it in the ledger as a wrong-endpoint warning rather than
treating it as the enrichment contract.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/probe_lusha_v3.py 2>&1 | tee /tmp/probe-disarmed.txt; grep -q 'v3/contacts/search-and-enrich' /tmp/probe-disarmed.txt &amp;&amp; ! grep -qi 'DISARMED.*http 200' /tmp/probe-disarmed.txt</automated>
    <human-check>Live run (spends credits): `DRY_RUN=false ALLOW_LUSHA_PROBE=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/probe_lusha_v3.py', run_name='__main__')" --out /tmp/lusha-v3-probe.json` — confirm at least one contacts-lane attempt returned HTTP 200 and the ledger names which body shape produced it.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/probe_lusha_v3.py` exists and `grep -c 'https://api.lusha.com/v3/contacts/search-and-enrich' scripts/probe_lusha_v3.py` returns at least 1.
    - `grep -c 'ALLOW_LUSHA_PROBE' scripts/probe_lusha_v3.py` returns at least 2 (the gate is read and its default is false).
    - `grep -c 'PROBE_MAX_CREDITS' scripts/probe_lusha_v3.py` returns at least 1 and `grep -c 'PROBE_MAX_BILLABLE' scripts/probe_lusha_v3.py` returns at least 1.
    - `grep -cE '<redacted>' scripts/probe_lusha_v3.py` returns at least 1, and `grep -cE 'print.*LUSHA_API_KEY|print.*\bkey\b' scripts/probe_lusha_v3.py` returns 0 (the key is never printed).
    - `grep -c 'prospecting' scripts/probe_lusha_v3.py` may be non-zero only inside the wrong-endpoint warning; no request URL in the file contains `/prospecting/`.
    - Running the script with no `LUSHA_API_KEY` in the environment exits 0 and prints `skipped (no LUSHA_API_KEY)`.
    - The live run's ledger records a 200 for the contacts lane, with the winning request body captured verbatim.
  </acceptance_criteria>
  <done>The contacts-lane v3 endpoint has been called for real, returned 200, and the exact body that produced it plus every rejected attempt's error body is captured in a JSON ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the probe ladder (companies, two-step, reveal A/B, id reuse, no-match) and write the contract of record</name>
  <files>scripts/probe_lusha_v3.py, docs/LUSHA-V3-CONTRACT.md</files>
  <read_first>
    - `scripts/probe_lusha_v3.py` as written in Task 1 (extend it; do not fork a second script).
    - The Task 1 live ledger (`/tmp/lusha-v3-probe.json`) — the confirmed contacts body shape is
      the starting point for the reveal A/B and id-reuse steps.
    - `.planning/phases/20-lusha-v3-migration/20-RESEARCH.md` "Open Questions" 1-4 and
      "Measured Lusha economics (baseline to beat)" (~2.5 credits/call, ~4.65 credits/matched
      reveal, 172 credits for 68 calls, 4093 -> 3921 balance).
    - `scripts/build_cloud_workflows.py` lines 3930-3945 — the `Lusha Usage` node is built from
      `provider_registry.PROVIDER_REGISTRY["lusha"]["credit"]["url"]`, so it is already v3 by
      construction; this task's job is to state that as verified fact, not to change it.
    - `scripts/check_provider_credits.py` — run it, do not modify it.
  </read_first>
  <precondition>Task 1's live run produced at least one HTTP 200 from `https://api.lusha.com/v3/contacts/search-and-enrich`; without a known-good contacts body shape the reveal A/B and id-reuse steps have nothing to vary.</precondition>
  <reversibility rating="costly">Adds up to 6 further billable calls; the same `PROBE_MAX_CREDITS` cap governs, and every step records its own credit delta so the spend is auditable after the fact.</reversibility>
  <action>
Extend `scripts/probe_lusha_v3.py` with the remaining ladder steps, then transcribe the ledger
into `docs/LUSHA-V3-CONTRACT.md`.

Probe steps to add, each recording credits before/after:

- **P2 companies lane:** `POST https://api.lusha.com/v3/companies/search-and-enrich` for
  `racingnsw.com.au` (the domain BUG 17's fix was live-probed against). Try
  `{"companies": [{"companyId": "1", "domain": "racingnsw.com.au"}]}` then a flat
  `{"domain": "racingnsw.com.au"}`. Record whether the response carries any reveal-style or
  billing-style structure at all — this answers Open Question 1 (does the companies lane have a
  selective-reveal credit model, or is it a flat per-match charge).
- **P3 two-step:** `POST https://api.lusha.com/v3/contacts/search` with the same identity, then
  `POST https://api.lusha.com/v3/contacts/enrich` using whatever handle the search returned.
  Record both request/response pairs and both credit deltas. The question to answer in the doc is
  narrow and specific: for this waterfall's usage pattern (one identity per call, not bulk), does
  two-step cost fewer credits than the combined endpoint? If not, the doc must say so plainly so
  Plan 02 can ship on the combined endpoint and keep one HTTP node per lane.
- **P4 reveal A/B:** call the combined contacts endpoint twice with the SAME identity — once
  requesting no reveal at all, once requesting the phone reveal — using whatever parameter name
  the P1 response's own vocabulary implies. Record both credit deltas and their difference. This
  is the measurement REQ-lusha-selective-reveal's cost premise rests on: if the two deltas are
  identical, selective reveal buys nothing and assumption A3 is REFUTED.
- **P5 id reuse:** take the record identifier the P1 200 returned and send it back on a second
  enrich call for the same person. Record the credit delta and any per-field cost structure in the
  response. Delta 0 (or an explicit zero-cost marker per field) confirms assumption A7; a normal
  charge REFUTES it.
- **P6 no-match:** a fabricated identity certain not to exist
  (`{firstName: "Zzz", lastName: "Qqqnotreal", companyName: "Nonexistent Holdings Pty Ltd", companyDomain: "nonexistent-holdings-zz.example"}`).
  Record the full envelope and whether the response marks itself as charged. This is the shape
  Plan 03's parser must survive without throwing.
- **P7 error shapes:** one deliberately malformed request (an unknown property in the identity
  object) and one call with a deliberately wrong key value, to capture the 400 and 401 body
  shapes and confirm whether a rejected call is billed. Do not retry either.
- **P8 usage endpoint:** run `.venv/bin/python scripts/check_provider_credits.py` through the
  dotenv wrapper and record the printed Lusha remaining-credits number alongside the probe's own
  direct `GET https://api.lusha.com/v3/account/usage` read. The two must agree.

Then write `docs/LUSHA-V3-CONTRACT.md` with these sections, in this order:

1. **Status header** — probe date, portal/account context, total credits spent (before/after
   balance), and a one-line "this document, not RESEARCH.md, is the contract of record" statement.
2. **Endpoints and auth** — the exact URLs used, `api_key` header (assumption A5 confirmed by a
   real 200, not by inference), required `Content-Type`.
3. **Contacts: request** — the winning body verbatim, plus a table of every attempted property
   and whether it was accepted or rejected (with the rejection message).
4. **Contacts: response** — the envelope verbatim, with the path to each field
   `lushaCandidates()` needs: email list, phone list (and the mobile-vs-other discriminator),
   job title, seniority, department, update dates, and the record identifier.
5. **Companies: request and response** — same treatment.
6. **Reveal model** — the exact parameter name and its accepted value strings as observed, the
   measured A/B credit deltas as numbers, and the verdict on whether a reveal-nothing call is
   cheaper than a reveal-phones call.
7. **Two-step vs combined** — measured deltas and the recommendation for this waterfall.
8. **Record id re-enrichment** — measured delta and the verdict.
9. **No-match envelope** and **error shapes** (400/401/429 if observed) and rate-limit headers.
10. **Assumption verdicts** — a table with one row per RESEARCH.md assumption A1-A7, each marked
    CONFIRMED / REFUTED / UNKNOWN with the probe step that decided it.
11. **Usage endpoint** — confirmed working, with the note that `Lusha Usage` sources its URL from
    `provider_registry.py` and therefore needs no migration.

Redact PII in the doc: the matched person's revealed email and phone digits must be replaced with
structurally identical synthetic values (same format, same length, same type) with a one-line note
saying so. Field names, list shapes and types are the contract; the real values are not, and a
committed contract doc is not a place to accumulate revealed provider PII (threat T-20-03).
  </action>
  <verify>
    <automated>test -f docs/LUSHA-V3-CONTRACT.md &amp;&amp; grep -q 'v3/companies/search-and-enrich' docs/LUSHA-V3-CONTRACT.md &amp;&amp; grep -qE 'A7' docs/LUSHA-V3-CONTRACT.md &amp;&amp; .venv/bin/python -m pytest tests/test_check_provider_credits.py tests/test_provider_registry_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/LUSHA-V3-CONTRACT.md` exists and contains all eleven sections named above.
    - `grep -c 'CONFIRMED\|REFUTED\|UNKNOWN' docs/LUSHA-V3-CONTRACT.md` returns at least 7 (one verdict per assumption A1-A7).
    - The reveal-model section contains two numeric credit deltas and an explicit verdict sentence; no phrase in that section is left as an unmeasured estimate.
    - The record-id section contains a numeric credit delta for the second enrich call.
    - `.venv/bin/python -m pytest tests/test_check_provider_credits.py -q` passes unchanged (this task must not edit that script or its test).
    - `grep -c 'credits.remaining' docs/LUSHA-V3-CONTRACT.md` returns at least 1 and the recorded number matches what `check_provider_credits.py` printed.
    - Total probe credit spend recorded in the status header is under 40.
    - No unredacted revealed email address or phone number appears in `docs/LUSHA-V3-CONTRACT.md`.
  </acceptance_criteria>
  <done>Both lanes, the two-step pair, the reveal A/B, the id-reuse call, the no-match case and the error shapes are all captured in `docs/LUSHA-V3-CONTRACT.md`, every assumption A1-A7 has a verdict, and total spend is recorded and under 40 credits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Contract review gate — confirm the assumptions the rest of the phase is built on</name>
  <files>docs/LUSHA-V3-CONTRACT.md</files>
  <action>Halt for operator review of the contract doc before any request builder changes. The two assumption verdicts named below decide whether Plans 02 and 04 are buildable as written; a refutation means re-planning, not proceeding.</action>
  <verify>
    <human-check>Operator reads docs/LUSHA-V3-CONTRACT.md and confirms the A3 and A7 verdicts, the credit spend, and the PII redaction per the steps below.</human-check>
  </verify>
  <done>Operator has approved the contract, or has named the refuted assumption so Plans 02/04 can be re-planned before any code changes.</done>
  <what-built>
    `scripts/probe_lusha_v3.py` (disarmed by default, credit-capped, secret-safe) and
    `docs/LUSHA-V3-CONTRACT.md` — the live-confirmed Lusha v3 contract with a verdict against each
    of RESEARCH.md's seven assumptions, measured reveal credit deltas, and a measured record-id
    re-enrichment cost.
  </what-built>
  <how-to-verify>
    1. Read `docs/LUSHA-V3-CONTRACT.md` end to end. It should read as observed fact with verbatim
       payloads, not as restated hypothesis.
    2. Check the assumption-verdict table. Two rows decide whether the rest of this phase is
       buildable as planned:
       - **A3 (reveal vocabulary + per-field credit model).** If REFUTED — a reveal-nothing call
         costs the same as a reveal-phones call, or there is no reveal parameter — then
         REQ-lusha-selective-reveal's cost-control premise is dead. STOP. Do not run Plan 02.
         Re-plan: the requirement needs renegotiation with the roadmap's success criterion 3.
       - **A7 (record id -> zero-cost re-enrichment).** If REFUTED — sending a stored id back is
         billed normally — then REQ-lusha-id-staging's "re-enriches free" criterion cannot be met
         as written. STOP and re-plan Plan 04; staging the id may still be worth it for
         match stability, but the success criterion has to change to say what was actually
         observed.
    3. Confirm the total credit spend in the status header is under 40 and the closing balance is
       consistent with `.venv/bin/python scripts/check_provider_credits.py`.
    4. Confirm no real revealed email or phone number was committed into the doc.
    5. Confirm the two-step-vs-combined verdict is stated plainly, since Plan 02 ships on whichever
       one this doc recommends.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 02, or name which assumption was refuted so Plans 02/04 can be re-planned before any builder changes.</resume-signal>
</task>

</tasks>

<api_coverage_matrix>
## Lusha v3 API surface — coverage decisions

Every v3 capability reachable from this phase's scope, with an explicit decision. The Prospecting
API product line (`/prospecting/*` — Prospecting Search & Enrich, Lookalikes, Tables, Decision
Makers) is OUT OF SCOPE per CLAUDE.md and REQUIREMENTS.md "Out of Scope"; RESEARCH.md Pitfall 1
documents the conflation risk in detail, and Task 1 records any Prospecting-shaped response as a
wrong-endpoint warning rather than treating it as the contract.

| v3 capability | Endpoint | Decision | Reason |
|---|---|---|---|
| Contacts combined search-and-enrich | `POST /v3/contacts/search-and-enrich` | **build** | Direct replacement for `GET /v2/person`; keeps one HTTP node per lane (existing topology 1:1). REQ-lusha-v3-request-builders. |
| Companies combined search-and-enrich | `POST /v3/companies/search-and-enrich` | **build** | Direct replacement for `GET /v2/company`. REQ-lusha-v3-request-builders. |
| Contacts two-step search then enrich | `POST /v3/contacts/search` + `POST /v3/contacts/enrich` | **probe, then defer unless it measurably wins** | REQ-lusha-v3-contract-probe requires exercising and documenting it (Task 2 / P3). Adopting it costs an extra HTTP node + IF branch per lane; that only pays if the measured credit delta beats the combined endpoint for one-identity-per-call usage. Probe decides; the doc records the decision either way. |
| Selective reveal parameter | on the contacts endpoints | **build** | REQ-lusha-selective-reveal. Contacts lane only — the companies gate tracks `lv_org_type`/`lv_produces_content`, fields Lusha has never supplied, so there is no `missingFields` -> reveal mapping on that lane (RESEARCH.md Pitfall 4). |
| Companies selective reveal | on `POST /v3/companies/search-and-enrich` | **probe, then decide in Plan 02** | Open Question 1 — unconfirmed whether the companies endpoint exposes a reveal model at all. Task 2 / P2 answers it; Plan 02 adds reveal to that lane only if the doc says the mechanism exists. |
| Account usage / credit balance | `GET /v3/account/usage` | **verify only, already built** | `provider_registry.py:22` and `check_provider_credits.py` already target it (live-confirmed 2026-07-24), and `build_cloud_workflows.py:3943` builds the `Lusha Usage` node from the registry entry, so it is v3 by construction. Task 2 / P8 verifies; no migration. |
| Record id re-enrichment (zero-cost path) | on the contacts/companies enrich endpoints | **build** | REQ-lusha-id-staging. Premise probed at Task 2 / P5 before Plan 04 builds on it. |
| `waterfallReveal` third-party fall-through (beta) | contacts enrich | **out of scope** | REQUIREMENTS.md "Future Requirements": needs support-enabled account access this account does not have. |
| Signals / webhooks (job change, headcount) | `/v3/signals*` | **out of scope** | REQUIREMENTS.md "Future Requirements" — deferred to v0.6+ as HubSpot staleness triggers. |
| Prospecting API (Search & Enrich, Lookalikes, Tables, Decision Makers) | `/prospecting/*` | **out of scope** | Different product line: net-new acquisition, not enrichment of existing CRM records. CLAUDE.md + REQUIREMENTS.md "Out of Scope". Actively guarded against in Task 1. |
| Bulk / multi-identity batching | contacts + companies endpoints | **defer** | The waterfall is one identity per row per call today (`contactId: "1"`, a single-element array). Batching is a throughput change with its own row-alignment risk against `nodeAll()` paired-index recovery; nothing in this phase's requirements asks for it. |
</api_coverage_matrix>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe script -> api.lusha.com | The `api_key` secret crosses here. It is read from the process env (in-process `load_dotenv`) and must never be echoed into stdout, the ledger JSON, or the committed contract doc. |
| api.lusha.com -> repo working tree | Provider responses containing real personal email addresses and mobile numbers cross into a file that gets committed. |
| probe script -> Lusha credit balance | Every billable call spends real money from a ~3,921-credit balance that a full sweep already exceeds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `scripts/probe_lusha_v3.py`, `docs/LUSHA-V3-CONTRACT.md` | high | mitigate | Key read via `os.getenv` only; request echoes emit `api_key: <redacted>`; no `print` of the key or any variable holding it (asserted by grep in Task 1's acceptance criteria); the contract doc records header NAMES, never values. Mirrors `check_provider_credits.py`'s documented "NEVER prints a secret value" contract. |
| T-20-02 | Elevation of resource consumption (cost abuse) | probe ladder | high | mitigate | Disarmed by default (`ALLOW_LUSHA_PROBE` must be explicitly true); `PROBE_MAX_CREDITS` (40) and `PROBE_MAX_BILLABLE` (8) caps enforced by reading `credits.remaining` before and after every billable step and aborting the ladder on breach; every step's delta recorded so spend is auditable; single-identity probes, never a sweep. |
| T-20-03 | Information Disclosure | `docs/LUSHA-V3-CONTRACT.md` committed to git | medium | mitigate | Revealed email and phone values replaced with structurally identical synthetic values before commit, with the redaction noted in-doc. Field names/shapes/types (the actual contract) are preserved. |
| T-20-04 | Repudiation (a malformed request that looks like a clean no-match) | v3 request shape | high | mitigate | This whole plan is the mitigation: raw HTTP status and body are inspected per attempt, including the 400s, rather than trusting "the workflow executed" as evidence. BUG 17's `onError: "continueRegularOutput"` masking is why the probe precedes the builders instead of following them. |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan or this phase: the probe uses `requests` and `python-dotenv`, both already in the venv and already used by `check_provider_credits.py` and the Phase 19 deploy runbook. No new dependency is introduced, so there is no new supply-chain surface and no legitimacy audit to run. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01 contribution)

New files:
- `scripts/probe_lusha_v3.py` — re-runnable, disarmed-by-default, credit-capped v3 contract prover.
- `docs/LUSHA-V3-CONTRACT.md` — the contract of record for every later plan in this phase.

New symbols / knobs introduced here:
- env gate `ALLOW_LUSHA_PROBE` (default false — no live call without it)
- env caps `PROBE_MAX_CREDITS` (default 40), `PROBE_MAX_BILLABLE` (default 8)
- CLI flag `--out <path>` on `probe_lusha_v3.py` (writes the step ledger as JSON)

Verified-not-changed (recorded as fact, no edit):
- `provider_registry.PROVIDER_REGISTRY["lusha"]["credit"]["url"]` is already
  `https://api.lusha.com/v3/account/usage`
- `build_cloud_workflows.py:3943` builds the `Lusha Usage` node from that registry entry, so it
  carries the v3 URL by construction — Open Question 4 resolved, zero work.
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python scripts/probe_lusha_v3.py` with no key present: exits 0, prints the skip banner, makes zero HTTP calls.
- `.venv/bin/python scripts/probe_lusha_v3.py` disarmed with a key present: prints the full request ladder with v3 URLs and a redacted key, makes zero HTTP calls.
- Live armed run: at least one 200 per lane, every step's credit delta recorded, total spend under 40.
- `.venv/bin/python -m pytest tests/test_check_provider_credits.py tests/test_provider_registry_parity.py -q` passes (this plan changes neither).
- `docs/LUSHA-V3-CONTRACT.md` carries a verdict for each of A1-A7.
</verification>

<success_criteria>
Roadmap success criterion 1 is met: the v3 contract is documented from live probes with minimal
credit spend (both combined endpoints plus the two-step pair, with envelope, reveal/billing
structure and error shapes captured), and `check_provider_credits.py` is confirmed reading usage
correctly from `GET /v3/account/usage`. Assumptions A3 and A7 have explicit verdicts, so Plans 02
and 04 either proceed on confirmed fact or get re-planned before any code changes.
</success_criteria>

<output>
Create `.planning/phases/20-lusha-v3-migration/20-01-SUMMARY.md` when done. Record the assumption
verdict table verbatim in the summary — Plans 02-05 read it.
</output>
</content>
</invoke>
