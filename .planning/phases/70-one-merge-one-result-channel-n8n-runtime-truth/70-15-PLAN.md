---
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
plan: 15
type: execute
wave: 3
depends_on: ["70-13", "70-14"]
gap_closure: true
gap_ids: [G-70-5]
files_modified:
  - CLAUDE.md
  - CHANGELOG.md
  - n8n/README.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
autonomous: true
requirements: [D-70-24, D-70-27]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's request-level flags section records the fan-out flag as RETIRED, with the execution ids that retired it, and the remaining count is corrected."
    - "Every platform fact this runaway bought is in CLAUDE.md §13.0.3 tagged observed live with the execution ids behind it, and the one whose cause is unknown says so."
    - "Merge-behaviour claims stay documented-only until the live re-proof gate runs — this round upgrades nothing about Merge."
    - "The deployment-parity note states the truth as of this round: the live enrichment lane is the pre-Phase-70 body, the other four are the gap-closure JSON, and the committed JSON is ahead of live."
    - "Every deploy in this repo now carries a two-minute burst watch before any send, written as a runbook step with the stop procedure that actually worked."
    - "D-70-27: Gates 7, 8 and 9 are recorded as deferred operator gates in the same shape as Gates 1-6, in order, with Gate 6 marked superseded."
    - "Gate 8's text states that Gate 5 recovered zero real rows on the enrichment lane, so a marker-free but empty result is not a pass."
  artifacts:
    - CLAUDE.md
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md
  key_links:
    - "A platform claim tagged observed live without an execution id is indistinguishable from a documented one; every new row carries its citation."
    - "The burst watch is the cheapest possible detector for the class of failure that consumed 138 executions: it costs two minutes and precedes every send."
    - "Gate ordering is the safety property — disarmed re-proof before any armed run, and the armed run is the operator's alone."
  prohibitions:
    - "The executor never deploys, never bounces, never arms, never sends anything live. Every gate here is text for an operator to follow."
    - "Do not upgrade any Merge-behaviour claim to observed live in this round — that is Gate 8's job."
    - "Do not state a cause for the unconnected-node runs. The mechanism was not isolated; record the observation only."
    - "Do not duplicate Gate 6's text as Gate 9 — supersede it and point at it."
---

<objective>
Write down what the runaway taught, make the detector for it a standing step, and hand the operator
the three ordered gates that close this phase.

Purpose: 138 executions bought a set of facts about this engine — that deactivating a workflow does
stop its in-flight self-dispatch after a lag, that a node can run with an item no declared
connection delivered, that a six-minute unattended burst is survivable but expensive. Unrecorded,
the next agent buys them again. And the removal in plan 70-13 changes what the request contract is,
what the live instance holds, and what has to be proven before anything is ever armed again.

Output: the corrected request-flag and deployment-parity notes, the new observed-live platform
facts with their citations, a mandatory post-deploy burst watch in the runbook, and Gates 7, 8 and
9.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-13-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

| Symbol | Location | Kind |
|---|---|---|
| retired-flag row and corrected flag count | `CLAUDE.md` §13.0.2 | the request contract after D-70-24 |
| three observed-live platform facts | `CLAUDE.md` §13.0.3 | what 12211–12348 and 12316 bought, with citations |
| corrected deployment-parity note | `CLAUDE.md` §13.0.2 | what is live versus committed after the rollback |
| post-deploy burst watch | `70-ROLLBACK-RUNBOOK.md` | the standing detector, with the stop procedure |
| Gates 7, 8, 9 | `70-DEFERRED-GATES.md` | D-70-27's deferred operator gates |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the retirement and the facts the runaway bought</name>
  <files>CLAUDE.md, n8n/README.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md D-70-24 — the sentence requiring this amendment, and D-70-26 for what must NOT be claimed
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md § Test 4 `observed:` — every execution id, the burst shape, the stop procedure and the mechanism paragraph
    - CLAUDE.md §13.0.2 — the request-level flags table, its heading count, the list of nodes the fan-out added, the node-count table and the deployment-parity block
    - CLAUDE.md §13.0.3 — the tagging rule and the platform-facts table this task extends
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-13-SUMMARY.md — the measured before-and-after node counts to copy in
    - n8n/README.md — the as-built enrichment graph description, which names the deleted lane
  </read_first>
  <action>
Amend CLAUDE.md §13.0.2 to record the fan-out flag as retired, following the precedent that section
already set for the early-ack flag it retired: the row stays in the table so a reader meeting the
name in old code finds the answer, marked retired with its date and the execution ids that retired
it, and the paragraph explaining what it did is replaced by one recording that it was removed
because no in-graph guard is trusted after the runaway. Correct the section heading's flag count and
the surrounding prose — two request-level signals remain. Correct the list of nodes that section
says the fan-out added: those nodes no longer exist. Update the node-count table from plan 70-13's
recorded numbers.

Correct the deployment-parity block to the state as of this round: the enrichment lane live is the
pre-Phase-70 body restored on 2026-09-10, the other four are the gap-closure JSON, and the
committed JSON is ahead of live by the whole of this gap-closure round. Say plainly that nothing is
armed and that the deploy is the operator's step, deferred to this round's gates.

Extend §13.0.3's platform-facts table with three rows, each tagged observed live and each carrying
the execution ids behind it: that deactivating a workflow stops further sub-workflow dispatch after
a short lag, with the tail children erroring rather than running; that a node on this engine can
execute with an item that no declared connection delivered, observed three times in one execution
with the runData source naming a node that has no edge to it, cause NOT isolated; and the scale of
the burst — child executions per minute, how many were in flight, and the total consumed against
the monthly budget. Say for the second one, in the row itself, that the cause is unknown and that
no in-graph guard was trusted afterwards.

Upgrade nothing about Merge in this round. The Merge-behaviour claims stay documented-only until
the live re-proof gate runs; note that explicitly next to them so a reader does not assume this
round covered them.

Update n8n/README.md's as-built enrichment description to match the regenerated graph. Add a
CHANGELOG entry for the removal, the refusal, the generation-time assertion, the marker filter and
the recorded facts.
  </action>
  <acceptance_criteria>
    - CLAUDE.md §13.0.2 marks the fan-out flag retired with its date and the execution id range, and the remaining request-level signal count is corrected everywhere it appears in that section
    - The node list that section attributes to the fan-out no longer claims those nodes exist
    - The node-count table matches plan 70-13's recorded counts
    - The deployment-parity block states which body each of the five live workflows holds and that nothing is armed
    - §13.0.3 gains exactly three new rows, each tagged observed live, each citing execution ids
    - The row for the unconnected-node runs states the cause is not isolated
    - The Merge rows still read as documented-only and say which gate would upgrade them
    - `/usr/bin/grep -c '12316' CLAUDE.md` is at least 1 and `/usr/bin/grep -c '12348' CLAUDE.md` is at least 1
    - `70-ROLLBACK-RUNBOOK.md` names the literal filter `mode: integrated` in the watch step
    - n8n/README.md's enrichment graph description matches the regenerated JSON
    - CHANGELOG.md carries this round's entry
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -q '12316' CLAUDE.md && /usr/bin/grep -q '12348' CLAUDE.md && /usr/bin/grep -q 'observed live' CLAUDE.md && .venv/bin/python -m pytest -q --tb=short</automated>
    <fails_when>a new platform fact carries no execution id, a Merge claim was upgraded without a live observation, or the node counts contradict the regenerated JSON.</fails_when>
  </verify>
  <done>The next agent reading CLAUDE.md learns what this engine did, what it cost, what is still unexplained, and what is actually live.</done>
</task>

<task type="auto">
  <name>Task 2: Make the burst watch a standing deploy step</name>
  <files>.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md D-70-27 — the sentence making the watch a standing step for every deploy
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md — the existing step structure, in particular the mandatory bounce-and-read-back step this one follows
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md § Test 4 `observed:` — the stop procedure that worked: deactivate first, then a PUT of the previous body, with the observed lag and the tail behaviour
    - operator-claude-plugin/scripts/executions_client.py — how an executions list is read, and the key the list is filtered on
    - scripts/bounce_n8n_workflows.py, scripts/deploy_n8n_workflows.py — what the operator has already run when this step begins
  </read_first>
  <action>
Add one step to the runbook, immediately after the mandatory bounce and read-back, and before any
step that sends anything.

The step is a two-minute watch of the executions list for executions with `mode: integrated`
(the executions-API field value that marks an Execute Workflow child), started with nothing sent — a freshly deployed workflow that produces
executions on its own is producing them from nothing, which is the entire signature of what
happened on 2026-09-10. State the pass condition as zero such executions appearing in the window,
and state that the watch precedes every send, not only a suspicious one.

Give the stop procedure exactly as it worked, in order: deactivate the workflow first, note that
children already queued still run and that the tail errors rather than executing, then PUT the
previous known-good body as a belt-and-braces second stop, then confirm no execution id appears
after the last one seen. Record the observed lag so an operator does not conclude the deactivate
failed while it is still draining.

Say that this step now applies to every deploy in this repo, not only to a rollback, and cite the
executions that made it a rule.
  </action>
  <acceptance_criteria>
    - The runbook has a new step between the bounce step and any send step
    - The step states a two-minute window, a zero-unrequested-execution pass condition, and that it precedes every send
    - The stop procedure lists deactivate first, then the previous-body PUT, with the observed drain lag and tail behaviour
    - The step cites the execution ids that made it a rule
    - The step says it applies to every deploy, not only a rollback
    - The existing steps are renumbered consistently and no existing step lost its content
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -q 'deactivate' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md && /usr/bin/grep -q '12348' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md && /usr/bin/grep -q 'mode: integrated' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md && /usr/bin/grep -q '^## Step 8' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md && ! /usr/bin/grep -q '^## Step 9' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md</automated>
    <fails_when>the watch is described without a stop procedure an operator could follow under pressure, or a step was renumbered in a way that dropped its content.</fails_when>
  </verify>
  <done>Nobody deploys in this repo again without a two-minute look at whether the thing started running by itself.</done>
</task>

<task type="auto">
  <name>Task 3: Record Gates 7, 8 and 9 for the operator, and supersede Gate 6</name>
  <files>.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md</files>
  <read_first>
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md D-70-27 — the three gates, their order, and the standing ruling that defers them
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md §§ Gate 3, Gate 5, Gate 6 — the section shape to mirror, the four sends, the executionOrder read, the ordering rule stated once for both armed and disarmed
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md § Test 5 `observed:` — what Gate 5 actually recovered, including that no recovered enrichment row carried a row id
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json — the verdict fields a re-run must fill, and the recorded shapes
    - scripts/prove_phase70_runtime.py — the driver, its four sends and its environment guard
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-13-SUMMARY.md and 70-14-SUMMARY.md — what changed, so each gate names the body it is proving
  </read_first>
  <action>
Append three gate sections in the file's existing shape — what the gate proves, the risk accepted
by deferring it, the operator steps, and what must be true before it is signed off.

Gate 7 is the disarmed deploy and bounce of the loop-free body, followed by the two-minute burst
watch from the runbook, with nothing sent. It passes only if no unrequested execution appears in
the window and every live node count and write flag reads back as expected. Nothing else happens in
this gate.

Gate 8 is the disarmed live re-proof: the four sends, all four required to compare shape-equal, the
live executionOrder setting read from the running bodies, zero writes. Write into this gate the
uncomfortable fact from Gate 5 — that the enrichment lane recovered no row carrying a row id at
all, one of its executions errored and was never examined, and therefore removing the phantom rows
does not by itself mean the four real rows will appear. A marker-free but empty result is a
failure, not progress. Add one step: for each of the four executions, compare every node's runData
source against the workflow's declared connections and report any node that ran with a source no
edge declares. That is the exact detector for what happened on 2026-09-10 and the executions are
already in hand.

Gate 9 is the armed mixed-verdict re-run, which is the previously recorded armed gate re-pointed at
the fixed body. Do not restate its procedure — mark the earlier gate superseded, say which gate
replaced it and why, and have Gate 9 reference it for the steps while stating the two things that
changed: the body it runs against, and that Gate 8 must have passed first.

State the ordering rule once for all three: 7 before 8, 8 before 9, and nothing is armed until 8
has passed. Note that the operator alone opens the armed window.
  </action>
  <acceptance_criteria>
    - Three new gate sections exist, in the file's existing shape, in order
    - Gate 7 covers deploy, bounce, burst watch and read-back, and sends nothing
    - Gate 8 requires all four sends shape-equal, the live executionOrder read, zero writes, and the runData-source versus declared-connections comparison
    - Gate 8 states that Gate 5 recovered no row carrying a row id on the enrichment lane and that an empty result is a failure
    - Gate 9 references the superseded gate for its steps rather than duplicating them, and names Gate 8 as its precondition
    - The earlier armed gate is marked superseded and points at Gate 9
    - The ordering rule appears once and covers all three
  </acceptance_criteria>
  <verify>
    <human-check>Operator reads Gates 7, 8 and 9 and confirms each is followable as written: the steps name the exact commands and files, the pass conditions are unambiguous, and nothing in them asks the operator to arm before the disarmed proof has passed.</human-check>
    <automated>/usr/bin/grep -q '^## Gate 7' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -q '^## Gate 8' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -q '^## Gate 9' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && ! /usr/bin/grep -q '^## Gate 10' .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md</automated>
    <fails_when>a gate's steps are not runnable without guessing, Gate 9 duplicates the superseded gate's text instead of referencing it, or the ordering rule permits an armed run before the disarmed re-proof.</fails_when>
  </verify>
  <done>The operator has three ordered gates that end the phase, and the one that arms anything cannot be reached before the one that proves the graph disarmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded fact → future decision | a claim without its citation becomes an assumption the next phase builds on; a claim with a guessed cause is worse than none |
| gate text → live arming | the armed gate's text is what an operator will follow with credentials in the shell; ambiguity there is a production risk |
| deploy → unattended execution | the window between a deploy and the first send is where the 2026-09-10 burst happened; it is the only place a cheap detector fits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-69 | Spoofing | a platform fact tagged observed live on reasoning rather than an observation | high | mitigate | every new row carries execution ids; the verify command checks the ids are present in the document |
| T-70-70 | Repudiation | the unconnected-node observation written up with an invented cause, closing an open question falsely | high | mitigate | the row states the cause is not isolated; D-70-26's prohibition is restated in the plan's own prohibitions |
| T-70-71 | Elevation of Privilege | the armed gate reachable before the disarmed re-proof passes | critical | mitigate | one ordering rule covering all three gates; Gate 9 names Gate 8 as its precondition; arming is the operator's action alone |
| T-70-72 | Denial of Service | another unattended burst consuming the monthly execution budget after a future deploy | critical | mitigate | the two-minute burst watch becomes a standing runbook step before every send, with the stop procedure that worked |
| T-70-73 | Information Disclosure | a gate or runbook step naming a credential, token or webhook secret value | high | mitigate | the steps name environment variable NAMES and script paths only, never values |
| T-70-74 | Tampering | Gate 9 duplicating the superseded gate's text, so the two drift and an operator follows the stale one | medium | mitigate | Gate 9 references rather than duplicates; the superseded gate is marked and points forward |
| T-70-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package installs in this plan; if one becomes necessary the package-legitimacy gate and a blocking human checkpoint apply |
</threat_model>

<verification>
- CLAUDE.md carries the retirement, the corrected parity note and three new observed-live rows with execution ids
- No Merge claim was upgraded in this round
- The runbook has a burst-watch step with a followable stop procedure, before any send step
- `70-DEFERRED-GATES.md` carries Gates 7, 8 and 9 in order, with the earlier armed gate superseded
- `.venv/bin/python -m pytest -q --tb=short` still green (documentation-only changes must not break a doc-asserting test)
- Nothing deployed, bounced, armed or sent by the executor

## Gate 3 — end-of-phase UAT ordering

Gates 7, 8 and 9 are recorded, not run. They belong to `/gsd-verify-work 70` and are the operator's.
</verification>

<success_criteria>
The runaway is written down where it will be read, the detector for it runs before every future
deploy, and the operator holds three ordered gates that close the phase without arming anything
prematurely.
</success_criteria>

<output>
Create `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-15-SUMMARY.md` when done
</output>
