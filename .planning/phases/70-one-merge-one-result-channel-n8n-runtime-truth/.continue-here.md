---
context: phase
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
task: 3
total_tasks: 3
status: almost_done
last_updated: 2026-09-09T22:44:14.493Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting the walker as runtime truth | `tests/n8n/lib/walkWorkflow.mjs` models a Merge as fire-once-when-every-input-has-data; its own header says that is a spec, not n8n's multi-wave behaviour. No native Merge has ever run live in this repo. | advisory | Gates 1 / 70-05-A / 3 in `70-DEFERRED-GATES.md` are the observation; do not upgrade any `[documented]` Merge claim in CLAUDE.md before Gate 3's verdict JSON reads `shapes_equal: true`. |
| Executor stops after analysis | Three `sonnet` dispatches on 70-05 each traced a hazard and returned `CHECKPOINT REACHED` without implementing. | advisory | When the hazard is already written in the SUMMARY, dispatch implementation-only with the recorded plan quoted as binding; `opus` finished it in one dispatch. |
| Shared Merge input for refusal + write | 2b/2c first wired a gate's refusal onto the same Merge input the write path feeds; the zero-hop refusal beat the multi-hop write and the Merge locked. Every disarmed suite was green because disarmed rows are all refused. | advisory | `wire_gate_refusal_lane`: one Merge input per refusal lane plus sentinels; pinned in `tests/n8n/writeGateShape.test.mjs`; Gate 70-05-A observes it live. |

<current_state>
Phase 70 fully executed (7/7 plans, 21 tasks) and pushed; `origin/master` level at `c587a0d`.
`70-VERIFICATION.md` = `human_needed` (37/39 offline truths verified, no gaps). `70-REVIEW.md` =
`issues_found` (0 critical, 6 warnings, 3 info). `70-UAT.md` holds the three deferred live gates.
Phase NOT marked complete — `update_roadmap` must not run before `/gsd-verify-work 70` passes.
</current_state>

<completed_work>
- 70-01 walker + detector — `ab99f1b`
- 70-02 tracer (contract answered `proceed`; Gate 1 deferred) — `5fa9d4b`
- 70-03 enrichment/review Merges, ack-only webhook — `9334277`
- 70-04 carry Merges everywhere, `nodeRunRecovery.js` deleted, assertion in `main()` — `0350531`
- 70-05 canonical `write_request`, IF-shaped gates, enrichment gate, one verdict — `0ad5a8c` (4 dispatches)
- 70-06 runData-only client, key refusal, write-only ledger — `b779864`
- 70-07 acceptance tests, docs/pins, plugin 0.43.0, proof driver offline half — `d375c4c`
- verification `e4e95f0`, UAT `71e98d3`, review `6be2894`, chain flag cleared `c587a0d`
</completed_work>

<remaining_work>
- `/gsd-verify-work 70` — Gates 1, 70-05-A, 3 (operator deploys + bounces first; steps in `70-DEFERRED-GATES.md`).
- After Gate 3: the CLAUDE.md `[observed live]` edits written at the end of Gate 3's section.
- `/gsd-code-review 70 --fix` for WR-01..06; delete or guard `scripts/prove_async_recovery.py`.
- `/gsd-secure-phase 70`, `/gsd-validate-phase 70` (advisory hooks, skipped this session for context).
- `/gsd-complete-milestone` (v1.2) once phase 70 closes.
</remaining_work>

<decisions_made>
- `proceed` on D-70-05/07 (one-way contract) — recorded in 70-02-SUMMARY.md; never re-ask.
- Live `blocking-human` gates back-loaded to end-of-phase UAT (operator, 2026-09-09).
- No executionOrder v1 flip; Merge combine-by-position carry; starved-lane sentinels not NoOp+alwaysOutputData.
- D-70-13 lane-wide (companies lose domain-allowlist on review writebacks) — flagged for operator.
- `association: "none"` kept (live reader `preingest.py:1186`).
</decisions_made>

<blockers>
- Three live gates need the operator: deploy + bounce, one disarmed single-lane send, one armed 2-row mixed-verdict send, the 4-send proof driver. Committed JSON is far ahead of live.
- Review warnings WR-01 (`watch.py` N>1 runData merge replaces instead of unions), WR-02 (dead `lane="ingest"` branch), WR-04 (`Build Response Merge` 15 inputs vs documented 2–10) are open and consequential for Gate 3.
</blockers>

## Required Reading (in order)
1. `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md` — the three live gates, verbatim steps.
2. `70-UAT.md` — the test list `/gsd-verify-work 70` walks.
3. `70-VERIFICATION.md` § Human Verification Required and § Anti-Patterns Found.
4. `70-REVIEW.md` — WR-01..06 before any live send that relies on `watch.recover_async_dispatch` merging N executions.
5. `70-05-SUMMARY.md` § Decisions Made — D-70-13 lane-wide flag; Associate Lane Sentinel as a third arming surface.
6. `CLAUDE.md` §13.0–§13.0.3 — the deployment-parity note is now stale by seven plans; Gate 3's follow-on edits fix it.

## Infrastructure State
- n8n Cloud: live workflows still the 2026-09-09 (F10/F11/F12) versions; nothing from Phase 70 deployed; nothing armed; both write flags `"false"` in every committed JSON.
- git: `master` = `origin/master` = `c587a0d`; push works only with gh active account `dr-robert-li`.
- Uncommitted: `.DS_Store` (junk), `.planning/milestone.lock` (harness artifact).
- No background processes; no async-job manifests.

<context>
The whole phase ran under `--chain` with human gates deferred by ruling. The load-bearing
unknown is whether n8n's real Merge engine matches the walker's model; every offline suite is
green, so the next real information comes only from the live sends. Treat a hang at Gate 1 or
a `shapes_equal: false` at Gate 3 as a phase-level finding, never a workaround.
</context>

<next_action>
Start with: operator deploys + bounces disarmed (`scripts/deploy_n8n_workflows.py`, then
`scripts/bounce_n8n_workflows.py`), then `/gsd-verify-work 70` beginning with Gate 1 (single-lane
ingest send; settle vs stuck; read live `settings.executionOrder`).
</next_action>
