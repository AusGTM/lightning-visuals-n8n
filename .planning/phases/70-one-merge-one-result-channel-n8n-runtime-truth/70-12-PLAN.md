---
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
plan: 12
type: execute
wave: 4
depends_on: ["70-11"]
gap_closure: true
gap_ids: [G-70-4, G-70-2, G-70-3]
files_modified:
  - operator-claude-plugin/scripts/dispatch.py
  - operator-claude-plugin/tests/test_dispatch_multipart.py
  - scripts/prove_phase70_runtime.py
  - tests/test_prove_phase70_runtime.py
  - CLAUDE.md
  - CHANGELOG.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
autonomous: false
requirements: [D-70-22, D-70-19, D-70-02, D-70-21]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-70-22: on the ingest lane the proof driver compares the RAW recovered rows against the walker's raw rows, so a correct runtime reads shape-equal instead of failing on a key the client itself added."
    - "The comparator change is proven by a test that fails on execution 12207's recorded shape before the fix and passes after."
    - "Every platform fact this UAT observed is recorded in CLAUDE.md tagged as observed live, with the execution ids that observed it — including the one that stays confounded."
    - "D-70-02's documented-only claim is upgraded: the live executionOrder setting was read from the running workflow bodies and is absent on all five."
    - "The deployment-parity note is corrected — the Phase 70 JSON went live disarmed on 2026-09-10, and the gap-closure JSON is ahead of live again until the redeploy gate runs."
    - "The two remaining live proofs — a disarmed redeploy and re-proof, and an armed mixed-verdict re-run — are recorded as end-of-phase gates with exact steps, not left implicit."
  artifacts:
    - scripts/prove_phase70_runtime.py
    - operator-claude-plugin/scripts/dispatch.py
    - CLAUDE.md
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
  key_links:
    - "The reconciled row set is the one the operator should see; the raw row set is the one comparable to the walker. Both must remain available from one recovery, because a second poll would break the single-poll-site invariant the plugin suite enforces."
    - "A platform claim tagged observed live without an execution id is indistinguishable from a documented one; the tag is only worth what the citation behind it is."
    - "Nothing in this phase's close is armed except the one gate that explicitly re-runs the mixed-verdict batch, and that gate is the operator's to open."
  prohibitions:
    - "The executor never deploys, never bounces, never arms, and never runs the proof driver against the live instance."
    - "No second poll loop is added; the plugin suite permits exactly one poll site."
    - "The walker is not touched — a comparator artifact is fixed in the comparator, never by moving the instrument."
    - "No platform fact is upgraded to observed live without the execution id that observed it."
---

<objective>
Close the comparator artifact, write the observed platform facts down where the next agent will
read them, and hand the operator the two live gates that end the phase.

Purpose: G-70-4 is the last thing that would keep the phase's own verdict false after the real
defect is fixed — the ingest lane compares client-reconciled rows against raw walker rows and can
never read equal. And the 2026-09-10 UAT bought a set of engine facts this repo has wanted since
the first Merge was committed; unrecorded, the next agent re-learns them the expensive way.

Output: a like-with-like comparator, the observed-live platform facts in CLAUDE.md with their
execution ids, and Gates 5 and 6 recorded for the end-of-phase UAT.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-11-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

| Symbol | Location | Kind |
|---|---|---|
| raw recovered rows on the dispatch result | `operator-claude-plugin/scripts/dispatch.py` | the like-with-like comparison source (D-70-22) |
| ingest-lane raw comparison | `scripts/prove_phase70_runtime.py` | G-70-4 closed |
| observed-live platform facts | `CLAUDE.md` §13.0.2 / §13.0.3 | the engine rules this UAT bought, with execution ids |
| `70-DEFERRED-GATES.md` §§ Gate 5, Gate 6 | phase dir (appended) | the two remaining live proofs |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G-70-4 — compare like with like on the ingest lane</name>
  <files>operator-claude-plugin/scripts/dispatch.py, operator-claude-plugin/tests/test_dispatch_multipart.py, scripts/prove_phase70_runtime.py, tests/test_prove_phase70_runtime.py</files>
  <read_first>
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md § Gaps, G-70-4 — the gap statement and its recorded root cause
    - operator-claude-plugin/scripts/dispatch.py — the recovery block that already computes both the raw recovered rows and the reconciled rows, and the return dict that only exposes the reconciled ones
    - operator-claude-plugin/scripts/report.py — `reconcile`, the function that adds the key the comparison trips on
    - scripts/prove_phase70_runtime.py — `row_shape`, `shapes_equal`, and the ingest branch of `run_live`
    - operator-claude-plugin/tests/test_report_sufficiency.py — the invariant that permits exactly one poll site, which forbids solving this with a second recovery call
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json — the recorded 12207 and 12208 shapes, which are the test's fixture
  </read_first>
  <behavior>
    - The dispatch result exposes the raw recovered rows alongside the reconciled rows, from the same single recovery
    - The proof driver's ingest branch compares the raw rows against the walker's rows
    - A row set carrying the client-added outcome key compares equal to the same rows without it, on the ingest lane
    - The reconciled rows remain the row set every existing caller sees; no caller changes behaviour
    - The enrichment lane's comparison is unchanged — it never went through reconciliation
  </behavior>
  <action>
Expose the raw recovered rows on the dispatch result under a distinct key and leave the reconciled
rows exactly where they are, then have the proof driver's ingest branch read the raw key. That is
D-70-22's first option and it needs no key-exclusion list to maintain, no second poll, and no
change to what the operator sees. Note in the code comment that the reconciled rows stay the
operator-facing set and the raw rows exist for instrument comparison only.

If exposing the raw rows turns out to break a caller invariant, fall back to D-70-22's second
option — exclude the client-added key from the compared shape — and record in the summary which
invariant forced it.

Write the test first, in the repo-root proof-driver test module, using execution 12207's recorded
row shape from the verdict artifact: assert that a raw row set and its reconciled counterpart
compare equal on the ingest lane. Confirm it fails before the change.

Also close the loop on G-70-1's pinned test: `test_dispatch_multipart.py` pinned the content type
that caused the multipart part to be filed as binary. Check it now pins the two-tuple form, and if
it does not, fix it here so the resolved gap has a regression behind it.
  </action>
  <acceptance_criteria>
    - The dispatch result carries both the reconciled rows under their existing key and the raw recovered rows under a new one, from one recovery call
    - No new poll loop, sleep or wait appears in the plugin package
    - `scripts/prove_phase70_runtime.py`'s ingest branch compares the raw rows; the enrichment branch is unchanged
    - `tests/test_prove_phase70_runtime.py` contains a case built from execution 12207's recorded shape that fails before the change
    - `operator-claude-plugin/tests/test_dispatch_multipart.py` pins the two-tuple multipart form for both parts
    - `.venv/bin/python -m pytest tests/test_prove_phase70_runtime.py -q` passes and the plugin suite passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_prove_phase70_runtime.py -q && cd operator-claude-plugin && ../.venv/bin/python -m pytest -q</automated>
    <fails_when>the ingest comparison still trips on the client-added key (a correct runtime would still read unequal), or the plugin suite fails because a second poll site was introduced or a caller lost the row set it reads.</fails_when>
  </verify>
  <done>The phase's own verdict can now read true when the runtime is right, instead of failing on an artifact of the client.</done>
</task>

<task type="auto">
  <name>Task 2: Record the platform facts this UAT bought, tagged and cited</name>
  <files>CLAUDE.md, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md §13.0.3 — the observed-live versus documented tagging rule and the existing platform-facts table this task extends
    - CLAUDE.md §13.0.2 — the deployment-parity note whose Phase 70 paragraph is now stale, and the node-count table
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md — the three observed blocks, which carry every execution id
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-11-SUMMARY.md — the measured before-and-after node-count table this task copies
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md — D-70-02, whose documented-only status this task upgrades
  </read_first>
  <behavior>
    - The platform-facts table carries the engine's Merge rules as observed live with execution ids
    - The one fact that remains confounded is recorded as confounded, not as settled
    - The executionOrder setting is recorded as read from the live workflow bodies
    - The deployment-parity note states what is live today and what is ahead of live
    - The node-count table matches the regenerated workflows
  </behavior>
  <action>
Extend CLAUDE.md's platform-facts table with the rules this UAT observed, each carrying the
execution ids behind it: that a code node's empty output is delivered to a Merge input; that the
first delivery to an input is the one kept and a later arrival is discarded; that a Merge fires at
most once in an execution; that a Merge does not fire until every declared input has received a
delivery, and one that never fires terminates its downstream silently while the execution still
reports success; that a node fed zero items does not run and so contributes no delivery; and that
the multipart parser files any part carrying a content type as binary rather than as JSON body.

Record the fifteen-input observation as confounded: that Merge also had inputs which received no
delivery, so its non-firing is explained by starvation, and the documented input range stays an
independent reason to split rather than a proven cause.

Upgrade D-70-02's entry from documented to observed live, citing that the setting was read absent
from all five running workflow bodies on 2026-09-10.

Correct the deployment-parity paragraph: the Phase 70 JSON was deployed disarmed on 2026-09-10 and
is what ran during the UAT; the pre-Phase-70 rollback is available at Gate 4; and the gap-closure
JSON is ahead of live again until the redeploy gate. Refresh the node-count table from the
measured values and state plainly that nothing is armed.

Add the CHANGELOG entry for the gap closure.
  </action>
  <acceptance_criteria>
    - Every new platform-fact row carries at least one execution id from the 2026-09-10 UAT
    - The fifteen-input row is present and explicitly recorded as confounded rather than settled
    - The executionOrder entry states it was read from the live workflow bodies and names the date
    - The deployment-parity paragraph names what is live, what is ahead of live, and that nothing is armed
    - The node-count table matches the node counts of the regenerated workflows
    - CHANGELOG.md has an entry for this gap closure
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python - <<'PY'
import json,pathlib,re,sys
doc=pathlib.Path("CLAUDE.md").read_text()
missing=[]
for f in sorted(pathlib.Path("n8n").glob("wf_*_cloud.json")):
    n=len(json.loads(f.read_text())["nodes"])
    if not re.search(rf"\b{n}\b",doc): missing.append(f"{f.name}={n}")
if missing: sys.exit("node counts absent from CLAUDE.md: "+", ".join(missing))
for exec_id in ("12203","12206","12200"):
    if exec_id not in doc: sys.exit("execution id not cited in CLAUDE.md: "+exec_id)
PY</automated>
    <fails_when>a regenerated workflow's node count does not appear anywhere in CLAUDE.md, or an execution id that a newly recorded fact depends on is not cited — either meaning the document records a claim without the observation behind it.</fails_when>
  </verify>
  <done>The engine rules that cost a live UAT to learn are written where the next agent reads them, with the citations that make them evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Gates 5 and 6 — the two live proofs that end the phase</name>
  <files>.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md</files>
  <read_first>
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md — Gates 1, 70-05-A, 3 and 4, whose shape these follow
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md — the expectations Tests 2 and 3 set, which Gates 6 and 5 re-run unchanged
    - scripts/prove_phase70_runtime.py — the driver Gate 5 runs, and the environment variable that permits it
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md — the deploy and bounce sequence Gate 5 reuses, pointed at the fixed JSON instead of the historical bundle
  </read_first>
  <action>
Append two gates to `70-DEFERRED-GATES.md` in the established shape.

Gate 5, disarmed, is the re-run of the phase-closing proof: deploy the gap-closure JSON, bounce
it, read back the node counts and both write flags, and run the proof driver with its permission
variable set. It passes when the verdict records shape equality on all four sends, every execution
settled, zero writes, and the live executionOrder recorded again from the running bodies. It also
answers the one question left open — whether the split response merges behave live the way the
walker now says they do.

Gate 6, armed, is the re-run of the mixed-verdict batch: one window armed for exactly one of two
contacts resolving the same company, both sent in one batch. It passes when the response carries
exactly two rows, the permitted row reports its association and the refused row reports a blocked
write with a reason, HubSpot shows one contact updated with one association and the other
untouched, arming rewrote all three declaring nodes on the ingest lane, and the window is disarmed
afterwards with every flag read back at its disarmed literal.

For each gate record why it exists, what is deferred by not running it now, the operator steps,
and exactly what to report back. State that Gate 5 precedes Gate 6 — nothing is armed against a
graph that has not first been proven disarmed.

Then raise the checkpoint. Per the standing back-load ruling, record and continue: these are live
gates for the end-of-phase UAT, not mid-flight blockers. Say in the checkpoint return that Gate 4
from plan 70-08 may already have been run and, if it was, that Gate 5 supersedes the rolled-back
bodies with the fixed ones.
  </action>
  <what-built>The gap-closure JSON is committed and the whole offline harness is green under a walker that reproduces the 2026-09-10 engine behaviour. Gates 5 and 6 are written up in `70-DEFERRED-GATES.md`. Nothing has been deployed, bounced or armed by the executor.</what-built>
  <how-to-verify>
    1. Gate 5 first, DISARMED. Deploy the gap-closure JSON and bounce it, then read back the node counts, both write flags and the live executionOrder from the running bodies.
    2. Run the proof driver with its permission variable set. It passes when the verdict records shape equality on all four sends, all executions settled, and zero writes.
    3. Gate 6 second, and only if Gate 5 passed. Arm one window for exactly one of two contacts that resolve the same company, send both in one ingest batch.
    4. It passes when exactly two rows come back, the permitted row reports its association, the refused row reports a blocked write with a reason, HubSpot shows one contact updated with one association and the other untouched, and arming rewrote all three declaring nodes on the ingest lane.
    5. Disarm afterwards and read every flag back at its disarmed literal.
  </how-to-verify>
  <resume-signal>Type "gate 5 passed" with the verdict's shape-equality result, then "gate 6 passed" with the two row outcomes — or describe what either run showed.</resume-signal>
  <acceptance_criteria>
    - `70-DEFERRED-GATES.md` contains a Gate 5 section and a Gate 6 section
    - Gate 5 names the proof driver, its permission variable, the bounce requirement, and shape equality as the pass condition
    - Gate 6 names the two-contact mixed-verdict shape, the three declaring nodes, the exact expected row count, and the disarm-and-read-back step
    - Both sections state the ordering rule that the disarmed proof precedes the armed run
    - The checkpoint return names both gates and the standing ruling under which they are deferred
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -q "Gate 5" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -q "Gate 6" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -q "prove_phase70_runtime.py" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md</automated>
    <fails_when>either gate section is missing or Gate 5 does not name the proof driver — leaving the phase with no recorded route from a green harness to a live proof.</fails_when>
  </verify>
  <done>The phase's remaining proof is two written gates the operator can run, in an order that never arms against an unproven graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client reconciliation → instrument comparison | mixing the two produces a verdict that is false in both directions; the comparator must name which set it reads |
| documentation → future agent decisions | a fact recorded without its citation becomes an assumption the next phase builds on |
| recorded gate → live arming | Gate 6 is the only armed action in this phase's close, and its text is what an operator will follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-52 | Repudiation | a verdict reading shape-equal because the comparison was loosened rather than corrected | high | mitigate | the fix exposes the raw rows and compares those; the fallback that excludes a key must record which invariant forced it, and the test is built from execution 12207's recorded shape |
| T-70-53 | Information Disclosure | the verdict or a gate section carrying a credential, token or webhook secret | high | mitigate | the driver records execution ids and row shapes only; the gate text names environment variable NAMES and never values |
| T-70-54 | Elevation of Privilege | Gate 6's armed window widened beyond one record, or left armed | critical | mitigate | the gate text fixes the allowlist at exactly one contact and makes the disarm-and-read-back a pass condition; arming is the operator's action, never the executor's |
| T-70-55 | Tampering | an armed run against a graph never proven disarmed | critical | mitigate | both gate sections state the ordering rule, and Gate 5 is the disarmed proof that must pass first |
| T-70-56 | Spoofing | a platform claim tagged observed live on documentation alone | medium | mitigate | the verify command requires the execution ids behind the new facts to be cited in the document |
| T-70-57 | Denial of Service | a second poll loop added to satisfy the comparator, breaking the single-poll invariant | medium | mitigate | the fix reads both row sets from one recovery and the plugin suite's poll-site invariant is part of acceptance |
| T-70-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package installs in this plan; if one becomes necessary the package-legitimacy gate and a blocking human checkpoint apply |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_prove_phase70_runtime.py -q` passes with the 12207-shaped case
- `cd operator-claude-plugin && ../.venv/bin/python -m pytest -q` passes, one poll site unchanged
- CLAUDE.md carries the new facts with their execution ids and the refreshed node counts
- `70-DEFERRED-GATES.md` carries Gate 5 and Gate 6 with the disarmed-before-armed ordering
- Nothing deployed, nothing bounced, nothing armed by the executor
</verification>

<success_criteria>
The phase's own verdict can read true when the runtime is right, the engine facts are recorded
where they will be read, and the operator has two ordered live gates that close the phase.
</success_criteria>

<output>
Create `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-12-SUMMARY.md` when done
</output>
