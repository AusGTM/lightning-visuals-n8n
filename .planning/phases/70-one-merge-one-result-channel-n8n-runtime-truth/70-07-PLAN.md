---
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
plan: 07
type: execute
wave: 5
depends_on: ["70-05", "70-06"]
files_modified:
  - tests/n8n/enrichmentMixedBatch.test.mjs
  - tests/n8n/ingestMixedBatch.test.mjs
  - scripts/prove_phase70_runtime.py
  - CLAUDE.md
  - README.md
  - CHANGELOG.md
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/README.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json
autonomous: false
requirements: [D-70-17, D-70-19, D-70-02, D-70-07, D-70-11, D-70-08a]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-70-17: one mixed-batch test per lane — 2 identity lanes by 2 actions — asserts every row returns exactly once from the write node's output, driven by the walker over the committed JSON."
    - "D-70-17 addendum: each mixed-batch test also carries a single-lane-only case, because that is the common real shape and the Merge-hang case the 2x2 shape does not cover."
    - "D-70-19: the phase closes on a DISARMED live mixed batch whose recovered runData rows are shape-equal to the walker's predicted rows for the same input, with zero writes and zero arming."
    - "D-70-19: the disarmed run logs `settings.executionOrder` read from the LIVE workflow body, which is the observed-live upgrade for the platform fact this phase has only documented evidence for (D-70-02)."
    - "D-70-07 / D-70-11 / D-70-08a: the operator-facing documents state the new contract — three request-level flags not four, an ack-only body on the two row-outcome lanes, and the design fact that under autonomy a new person is never created without the operator's end-of-run approval."
    - "The pins this phase moved — node counts, flag declaration counts, builder idempotency — are updated as expected consequences, not treated as regressions."
  artifacts:
    - tests/n8n/enrichmentMixedBatch.test.mjs
    - tests/n8n/ingestMixedBatch.test.mjs
    - scripts/prove_phase70_runtime.py
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json
  key_links:
    - "The verdict's shape-equality is the whole point: the walker predicts, the live run observes, and a mismatch means the offline harness is not a faithful model of the runtime — which would invalidate every GREEN this phase produced."
    - "Deployment and bouncing are the operator's step; the executor never deploys, and a bare stored update never reloads a running workflow."
    - "The plugin's marketplace clone does not self-refresh, so a released change is invisible without a version bump in the plugin manifest."
  prohibitions:
    - "The executor never deploys and never bounces — the operator does both, disarmed."
    - "Nothing is armed at any point: both write flags stay false, no allowlist is populated, and not one row is written to HubSpot."
    - "No armed row is part of this phase's close."
    - "The unattended gate stays shut; this phase does not open it as a side effect (SAFE-05)."
    - "`n8n/wf_*.json` is never hand-edited."
---

<objective>
Prove the phase: two mixed-batch acceptance tests offline, the operator-facing documents brought
to the new contract, and one disarmed live run whose rows match what the walker predicted.

Purpose: everything before this plan is a refactor asserted by tests written alongside it. This
plan asks the two questions that matter — does a realistic batch return every row exactly once,
and does the offline model actually match the runtime? The second question is the one this repo
has never been able to answer about a Merge node, because until this phase there was not one in
any committed workflow.

Output: the acceptance tests, the updated documents and version bump, and a verdict JSON recording
the disarmed live comparison.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-05-SUMMARY.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

| Symbol | Location | Kind |
|---|---|---|
| `tests/n8n/enrichmentMixedBatch.test.mjs` | new file | D-70-17 acceptance, enrichment lane |
| `tests/n8n/ingestMixedBatch.test.mjs` | new file | D-70-17 acceptance, ingest lane |
| `scripts/prove_phase70_runtime.py` | new file | disarmed live driver; walker prediction vs recovered runData |
| `70-RUNTIME-VERDICT.json` | phase dir | the D-70-19 verdict, with `shapes_equal` and the live `executionOrder` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The two mixed-batch acceptance tests</name>
  <files>tests/n8n/enrichmentMixedBatch.test.mjs, tests/n8n/ingestMixedBatch.test.mjs</files>
  <read_first>
    - tests/n8n/lib/walkWorkflow.mjs — the walker's API, its HTTP stub format, and `trace.stalled`
    - tests/n8n/walkWorkflow.test.mjs — the walker's own RED cases, which these acceptance tests rely on being green to mean anything
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md — D-70-17 and D-70-18, including the explicit instruction not to expand this scope to one test per historical instance
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RESEARCH.md § "Pitfall 1" — why the 2x2 shape alone does not cover the hang case
    - .planning/uat/UAT-autonomous-batch-2026-09-09.md — the batch shape the live UAT used, which these tests mirror
  </read_first>
  <behavior>
    - Enrichment: a batch of 4 rows spanning 2 identity lanes and 2 actions returns exactly 4 rows at the response builder, one per input row, each carrying the outcome the write node produced or the gate's refusal
    - Ingest: the same, on the ingest lane's own response builder
    - No row appears twice and no row is missing, on either lane
    - A single-lane-only batch on each lane returns its rows and stalls no Merge
    - A fully refused batch on each lane returns one row per input row, each carrying its refusal reason
    - The responder fires exactly once per request on both lanes, with the ack shape
  </behavior>
  <action>
Create the two acceptance tests, one per lane, driving the committed
`n8n/wf_enrichment_cloud.json` and `n8n/wf_contact_ingest_cloud.json` through the walker.

Each test's primary case is the 2-identity-lane by 2-action batch D-70-17 specifies: four rows,
each with a distinct identity, two resolving to an existing record and two not. Assert each input
row appears exactly once at the response builder and that its reported outcome came from the write
node's own output — matching a row to the write response by the row's own identity, not by
position.

Add the single-lane-only case to each file. The 2x2 shape exercises every lane by construction and
therefore cannot catch a Merge waiting on an input that never fires, which is the common real
shape. Add the fully-refused case too, since that is the shape that dead-ended before this phase.

Do not add a test per historical instance. The operator declined that scope explicitly, and the
walker's own unit tests carry the detection evidence.
  </action>
  <verify>
    <automated>node --test tests/n8n/enrichmentMixedBatch.test.mjs tests/n8n/ingestMixedBatch.test.mjs</automated>
    <fails_when>non-zero exit, or the summary reports `fail` greater than 0, or `pass 0`</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the summary reports `fail` greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Each test file contains a 2x2 case, a single-lane-only case and a fully-refused case, each named so the shape is readable from the test output
    - Both files assert `trace.stalled` is empty in every case
    - Both files assert the responder fired exactly once with the ack shape
    - The 2x2 case asserts an exact row count equal to the input row count, and asserts no duplicate identities among the returned rows
    - `node --test tests/n8n/*.test.mjs` exits 0
    - `.venv/bin/python -m pytest -q --tb=short` exits 0
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0
  </acceptance_criteria>
  <done>Both lanes return every row of a mixed batch exactly once from the write node's output, and neither a single-lane batch nor a fully refused batch breaks that.</done>
</task>

<task type="auto">
  <name>Task 2: Bring the operator-facing documents to the new contract, and move the pins</name>
  <files>CLAUDE.md, README.md, CHANGELOG.md, operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/README.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/skills/enrich-records/SKILL.md, docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</files>
  <read_first>
    - CLAUDE.md §13.0.2 — the four-flag table that becomes three, the node counts, and the deployment-parity note recording what is committed versus live
    - CLAUDE.md §13.0 and §13.0.1 — the recompute lane and the association rule; check whether either statement is now stale after the gate and Merge changes
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md — the F5b paragraph added on 2026-09-09, which D-70-05 makes wrong, and step 5's opt-in flag which is retired
    - operator-claude-plugin/skills/enrich-records/SKILL.md — step 9's per-record report rule, which now applies to runData rows
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md — the batch shape, which becomes mixed lanes and mixed actions by default
    - operator-claude-plugin/.claude-plugin/plugin.json — current version 0.42.0; a released change is invisible without a bump
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-05-SUMMARY.md and 70-06-SUMMARY.md — the node counts and pin updates recorded there
  </read_first>
  <action>
Update the operator-facing documents to the contract this phase landed. These are not
housekeeping — an operator following a stale step 5 would pass a flag that no longer exists, and a
stale flag table is exactly the kind of drift that produced the defects this phase closed.

CLAUDE.md: §13.0.2's flag table becomes three flags, the retired one recorded as retired by this
phase with its date; the node counts for all six workflows updated from the values 70-05 recorded;
the deployment-parity note updated to say what is committed and what is live after Task 3's
deploy; §13.0's recompute-lane text and §13.0.1's association text checked against the new gate
and corrected where the mechanism moved. Preserve the documented-versus-observed-live tagging
discipline: the Merge behaviour becomes observed-live only after Task 3's run, and not before.

The two SKILL.md files: rewrite `enrich-before-ingest`'s F5b paragraph, which described the sync
body as a data channel and is now wrong; remove step 5's retired opt-in flag; update
`enrich-records` step 9 so its per-record rule reads the runData rows.

README and the plugin README: state the design fact D-70-11 exposed — under autonomy a new person
is never created without the operator's end-of-run approval, because a row with no match is by
definition unconfident. This is unchanged behaviour being made legible, not a policy change; say
so plainly rather than as a caveat.

`docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`: the batch shape becomes mixed lanes and mixed actions by
default, matching the acceptance tests, and the procedure gains the single-lane send.

Both CHANGELOGs, and bump `operator-claude-plugin/.claude-plugin/plugin.json` from 0.42.0. Cut the
CHANGELOG entry and the version bump together — an unbumped manifest leaves the change invisible
to an installed plugin.

Finally, move the pins this phase moved and record them as expected: node counts, the flag
declaration counts, the builder idempotency assertion, and the workflow parity test. A moved pin
here is a consequence, not a regression.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short</automated>
    <fails_when>non-zero exit, or the summary reports any `failed` or `error`</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the summary reports `fail` greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c '"version": "0.42.0"' operator-claude-plugin/.claude-plugin/plugin.json` prints `0` — the version was bumped
    - The bumped version appears as the newest entry in `operator-claude-plugin/CHANGELOG.md`
    - CLAUDE.md §13.0.2's flag table lists exactly three request-level flags, and the retired one appears only in a sentence recording its retirement
    - The node counts stated in CLAUDE.md match the committed JSON, verified by a check whose output is quoted in the plan summary
    - `/usr/bin/grep -c 'async_ack' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` prints `0`
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` describes a mixed-lane, mixed-action batch and a single-lane send
    - `.venv/bin/python -m pytest -q --tb=short` exits 0 with every moved pin updated
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0
  </acceptance_criteria>
  <done>Every operator-facing document states the contract the code now implements, the plugin version is bumped alongside its CHANGELOG entry, and every pin this phase moved is updated as an expected consequence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: The disarmed live run — the operator deploys and bounces, and the rows must match the walker</name>
  <files>scripts/prove_phase70_runtime.py, .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json</files>
  <read_first>
    - scripts/prove_async_recovery.py — the driver precedent: its cross-package import setup, its refusal preconditions, and how it wrote its verdict
    - .planning/milestones/v1.1-phases/61-autonomous-batch-runs/61-ASYNC-RECOVERY-VERDICT.json — the shape-equality verdict format this run reuses
    - scripts/deploy_n8n_workflows.py and scripts/bounce_n8n_workflows.py — the operator's deploy and bounce steps; a stored update never reloads a running workflow, so the bounce is not optional
    - CLAUDE.md §13.0.3 — the documented-versus-observed-live tagging rule this run's findings are recorded under
    - tests/n8n/lib/walkWorkflow.mjs — the CLI entry the driver subprocesses for the prediction side
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md — D-70-19: zero writes, zero arming, one disarmed 2-lane by 2-action send per lane
  </read_first>
  <precondition>The operator has deployed all workflows with `scripts/deploy_n8n_workflows.py` and bounced them with `scripts/bounce_n8n_workflows.py`, disarmed, with both write flags reading false in the live bodies. The executor never performs either step.</precondition>
  <what-built>
`scripts/prove_phase70_runtime.py` — a disarmed live driver, committed before the checkpoint is
presented, following `scripts/prove_async_recovery.py`'s precedent exactly: the same explicit
environment preconditions and refusal-before-anything discipline, the same cross-package import
setup, the same verdict-writing shape. It refuses loudly if a write flag reads anything other
than false in the live body, and it never arms anything.

Behind it: the whole phase's committed JSON — Merges at every convergence point, carry Merges at
every HTTP hop, IF-shaped write gates emitting refusals, and an ack-only response on both
row-outcome lanes — all green offline against the walker, and never yet run on n8n Cloud. No
committed workflow in this repo has ever contained a native Merge node before this phase.
  </what-built>
  <how-to-verify>
1. **Operator deploys and bounces.** Run `scripts/deploy_n8n_workflows.py`, then
   `scripts/bounce_n8n_workflows.py`. Both disarmed. Confirm each workflow is active and that its
   node count matches the committed JSON. Confirm both write flags read false in each live body. A
   stored update alone never reloads a running workflow, so a deploy without a bounce proves
   nothing about what ran.
2. **Read `settings.executionOrder` from the LIVE workflow body** and record the value. This is
   the observed-live upgrade for the platform fact this phase has only documented evidence for.
   Record what it actually is, not what it was expected to be.
3. **Send, disarmed:** one 2-identity-lane by 2-action batch on the enrichment lane, one on the
   ingest lane, and one single-lane-only batch on each. Four sends. Zero writes: the allowlist is
   empty, so every row is expected to come back blocked, and that is the intended result rather
   than a failure.
4. **Recover** each run's rows from runData by its own run id, through the same client path the
   plugin uses.
5. **Predict** the same rows offline by subprocessing the walker CLI against the same committed
   JSON with the same input rows.
6. **Compare** the recovered rows against the predicted rows and write
   `70-RUNTIME-VERDICT.json` in the phase directory with, at minimum: `shapes_equal`, the per-send
   row counts recovered and predicted, the live `settings.executionOrder` value, each execution
   id, whether any execution failed to settle, and an explicit confirmation that zero writes
   occurred.

**The pass criteria the operator is judging:**
- No execution stayed stuck running — no Merge hung on an input that never fired. This is the
  single most important observation in the run, because the Merge design rests on documented
  evidence and multiple dated community reports of exactly this failing, and this repo has never
  run a native Merge node before.
- The recovered rows are shape-equal to the walker's prediction on all four sends. A mismatch
  means the offline harness is not a faithful model of the runtime, which would invalidate every
  offline GREEN this phase produced — report it as a finding, do not adjust the walker to match
  and call it passed.
- Every row came back blocked, and HubSpot shows no write.

If the run reveals that the Merge hangs on a single-lane batch on this n8n build, STOP and report.
That is the one outcome the whole phase's design rests on, and the correct response is a finding,
not a workaround improvised at the checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" once `70-RUNTIME-VERDICT.json` shows `shapes_equal: true`, all four executions settled, and HubSpot shows zero writes — or describe what the run observed instead. A hung execution or a shape mismatch is a finding to report, not something to work around here.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-RUNTIME-VERDICT.json` exists and parses as JSON
    - Its `shapes_equal` field is `true`
    - It records the live `settings.executionOrder` value read from the deployed workflow body
    - It records four execution ids, one per send, each settled rather than running
    - It records zero writes and confirms both write flags read false in the live bodies
    - CLAUDE.md's Merge behaviour statement is upgraded from documented to observed-live, citing this verdict file and its execution ids
    - `git ls-files -- scripts/prove_phase70_runtime.py` prints the path — the driver is committed alongside its verdict
  </acceptance_criteria>
  <done>A disarmed live mixed batch and a disarmed single-lane batch on each lane settled without a hung Merge, their recovered rows are shape-equal to the walker's prediction, and the verdict is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live n8n Cloud | Deploy, bounce and four disarmed sends against the production instance; the only live boundary this phase crosses. |
| live execution -> verdict file | The verdict is the record future phases will cite; it must not carry a credential and must not overstate what was observed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-19 | Tampering | an accidental live write during the proof run | high | mitigate | Both write flags false in the committed JSON and re-confirmed in the live bodies before sending; the allowlist stays empty so every row is expected blocked; the driver refuses to run if a flag reads anything but false; the checkpoint is blocking-human and the executor never deploys |
| T-70-03 | Information Disclosure | the executions-API key or the webhook secret landing in the verdict JSON or a log | medium | mitigate | The driver follows the existing never-print-the-secret discipline; the verdict records execution ids and row shapes only, and the acceptance criteria enumerate its fields |
| T-70-04 | Denial of Service | a Merge hanging on the live build, leaving an execution stuck and consuming the execution budget | high | mitigate | Four sends only, each small; a stuck execution is the run's primary pass criterion and is reported as a finding rather than worked around; the phase stops if it happens |
| T-70-20 | Repudiation | a verdict claiming shape-equality that was achieved by adjusting the walker after seeing the live rows | high | mitigate | The action states plainly that a mismatch is a finding, not a prompt to adjust the walker; the walker's own unit tests are frozen by this point and the prediction is generated by subprocessing the committed CLI |

No package-manager install tasks in this plan.
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` — full node suite green including both acceptance tests
- `.venv/bin/python -m pytest -q --tb=short` — full root suite green with every moved pin updated
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green
- `70-RUNTIME-VERDICT.json` present with `shapes_equal: true` and zero writes
</verification>

<success_criteria>
- A 2-lane by 2-action batch returns every row exactly once on both lanes, offline
- A single-lane batch and a fully refused batch both behave correctly on both lanes, offline
- Every operator-facing document states the new contract; the plugin version is bumped with its CHANGELOG entry
- A disarmed live run on both lanes settles with no hung Merge and rows shape-equal to the walker's prediction
- The live `settings.executionOrder` value is recorded and the Merge behaviour claim is upgraded to observed-live
- Zero writes, zero arming, unattended gate still shut
</success_criteria>

<output>
Create `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-07-SUMMARY.md` when done.
Record in it: the verdict's `shapes_equal` result and the four execution ids; the live
`settings.executionOrder` value; every pin moved with its old and new value; the plugin version
released.
</output>
