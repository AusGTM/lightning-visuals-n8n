---
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
plan: 18
type: execute
wave: 3
depends_on: [70-16, 70-17]
gap_closure: true
gap_ids: [G-70-6]
files_modified:
  - CLAUDE.md
  - CHANGELOG.md
  - n8n/README.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md
  - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/deferred-items.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-70-31]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md section 13.0.3 gains two documented rows — n8n's legacy empty-item push and the v1 end-of-run drain at requiredInputs — each citing the source file and symbol rather than a docs page, and each tagged documented, never observed."
    - "CLAUDE.md section 13.0.3 gains an observed-live row for Gate 8's symptoms carrying execution ids 12349-12353, and the two pre-existing Merge rows (zero-item output is a delivery; first delivery wins) are annotated as observed under the LEGACY order only and now explained by the empty-item push — their assertions are not deleted."
    - "CLAUDE.md section 13.0.2 states the corrected live state: all five cloud workflows are on the pre-Phase-70 59812be bundle (backend_status 17, contact_ingest 29, enrichment 123, review 26, maintenance 39), disarmed; the committed JSON is ahead by the whole of Phase 70 plus this round's v1 flip; node counts did not move for the flip; nothing is armed."
    - "D-70-31: Gates 10, 11 and 12 exist in 70-DEFERRED-GATES.md in the same shape as Gates 7, 8 and 9, with explicit preconditions, numbered operator steps, an explicit pass criteria list, and a resume signal."
    - "D-70-31: Gate 11's pass criteria require live_settings_execution_order to read v1 on every workflow — a null reading is a FAILURE of this gate, the exact inverse of Gate 8's expectation — and retain Gate 8's runData-source-versus-declared-connections check unchanged."
    - "D-70-31: Gate 11 carries the explicit instruction that if the legacy symptoms persist under v1 the operator STOPS and reports, and does not adjust the walker or the driver to match."
    - "Gate 9 is marked SUPERSEDED by Gate 12 and left in the record; Gates 7 and 8 are left in the record as run and failed respectively."
    - "The rollback runbook's read-back step reports each workflow's execution order alongside its node count and write flags, and the two-minute burst watch remains a standing deploy step."
    - "The ROADMAP's Phase 70 plan list names 70-16, 70-17 and 70-18 under a round-3 gap-closure heading, and 70-UAT.md records what closed G-70-6 offline and what remains live."
  artifacts:
    - CLAUDE.md
    - CHANGELOG.md
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
    - .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md
    - .planning/ROADMAP.md
  key_links:
    - "Gate 11's pass criteria name the proof driver's verdict field created by plan 70-17. The spelling is `execution_order_all_v1`. This plan runs in wave 3, AFTER 70-17, and Task 2 takes the spelling from `70-17-SUMMARY.md` — which records the field name as shipped — rather than from 70-17's plan."
    - "Gate 10 deploys the bodies plan 70-16 regenerates. Its expected node counts are enrichment 287, contact_ingest 69, review 55, maintenance 43, backend_status 30 — UNCHANGED by the flip, which is why an unchanged count is the expected reading rather than a regression signal."
    - "CLAUDE.md's existing live-state table describes the MIXED state left by the 2026-09-10 runaway stop. It is stale: the Gate 8 session rolled all five workflows back to 59812be. Correct the table rather than appending a contradicting note beside it."
  prohibitions:
    - "The executor never deploys, never bounces, never arms, never sends anything live. Gates 10, 11 and 12 are written down, not performed."
    - "Do NOT edit 70-CONTEXT.md — it is the operator's record of their own decisions."
    - "Do NOT upgrade any v1 claim to observed-live. Nothing about v1 has been seen on this instance; every new v1 row stays tagged documented until Gate 11 observes it."
    - "Do NOT delete the two pre-existing Merge rows in section 13.0.3. Annotate them; the observations stand, only their scope narrows to the legacy order."
    - "Do NOT restate Gate 12's procedure inline. Point at Gate 6's steps with named substitutions, exactly as Gate 9 already does."
    - "Never hand-edit any n8n/wf_*.json."
---

<objective>
Record what this round established, correct what the incident left stale, and write the three
live gates that close G-70-6 for real.

Purpose: D-70-31. The offline half of G-70-6 lands in plans 70-16 and 70-17 — the committed
bodies carry the v1 execution order, generation refuses anything else, and no PUT path can
revert it. None of that has been SEEN on the engine. The live instance is still running the
pre-Phase-70 bundle the Gate 8 session rolled it back to, and every claim this round makes about
v1 is documentation-derived, not observation-derived. CLAUDE.md's tagging rule
(section 13.0.3) exists precisely so those two are never confused, and this repo has already been
burned once by treating a green offline suite as evidence about the engine.

Output: the platform-facts table carrying the new documented rules with their source citations
and Gate 8's observations with their execution ids; a corrected deployment-state note; and Gates
10, 11 and 12 written up in the shape Gates 7, 8 and 9 established, with Gate 9 marked superseded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md
@.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-15-SUMMARY.md
</context>

<artifacts_this_phase_produces>

| Symbol / artifact | Kind | Where | Contract |
|---|---|---|---|
| Gate 10 | new deferred gate section | `70-DEFERRED-GATES.md` | Disarmed deploy plus bounce of the v1 bodies, execution-order read-back on all five, then the two-minute integrated-burst watch with nothing sent. Precondition: none beyond the committed JSON being the v1 generation. |
| Gate 11 | new deferred gate section | `70-DEFERRED-GATES.md` | The D-70-19 proof re-run under v1. Precondition: Gate 10 passed. Pass criteria include `execution_order_all_v1` reading true on every workflow. |
| Gate 12 | new deferred gate section | `70-DEFERRED-GATES.md` | The armed mixed-verdict re-run. Precondition: Gate 11 passed. Points at Gate 6's steps with named substitutions; restates nothing. |
| section 13.0.3 rows | new table rows | `CLAUDE.md` | Two documented engine rules with source-file and symbol citations; one observed-live row for Gate 8 carrying executions 12349-12353; two existing Merge rows annotated as legacy-scoped. |

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: the platform-facts table records the engine rule, and the deployment note stops being stale</name>
  <files>CLAUDE.md, CHANGELOG.md, n8n/README.md</files>
  <read_first>
    - `CLAUDE.md` lines 2560-2600 — the section 13.0.2 deployment-state notes, including the
      2026-09-10 live-state table left by the runaway stop.
    - `CLAUDE.md` lines 2670-2700 — the section 13.0.3 platform-facts table, especially the
      execution-order row (2682) and the two Merge rows (2683, 2684) this task annotates, and
      the paragraph beneath the table stating which rows the previous round did not touch.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md` lines
      95-200 — the Test 7 and Test 8 records, including line 189's statement of what the live
      instance was rolled back to.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md`
      "Gap-closure round 3 decisions", the source-cited engine-rule paragraph in full.
    - `CHANGELOG.md` — its most recent entry, for the heading style and level of detail.
  </read_first>
  <action>
    Edit `CLAUDE.md` section 13.0.3's platform-facts table. Use scoped edits; never rewrite the
    file.

    Add two DOCUMENTED rows, each citing a source file and the symbol inside it, never a docs
    page — the docs page on execution order describes branch ordering only and says nothing about
    empty-input execution, and that absence is itself worth one clause:
      - the legacy path pushing one empty item onto every node of an empty branch so a waiting
        multi-input node can execute, citing
        `packages/core/src/execution-engine/workflow-execute.ts`, `addNodeToBeExecuted`, the
        `addEmptyItem` branch. Note that this single item is the shape of every Gate 8 symptom
        and of G-70-2, G-70-3 and G-70-5.
      - the v1 path having no such push, with nodes still waiting at end of run executing only
        once their arrived-with-data input count reaches `requiredInputs`, citing
        `packages/nodes-base/nodes/Merge/v3/actions/versionDescription.ts` for the value: the
        chooseBranch pair versus 1 for every other mode. Record that every Merge this repo
        generates is typeVersion 3.2 in `append` or `combine` mode, so the value that applies
        here is 1 — with the per-workflow counts (enrichment 10 append and 23 combine, ingest 2
        and 8, review 3 and 7, backend_status 7 combine, maintenance none).

    Add one OBSERVED-LIVE row for Gate 8 carrying executions 12349-12353: a write node executing
    with no real input item and PATCHing an empty id, an IF emitting a refusal on an empty list
    lane, gated sentinels delivering markers on inputs whose sentinel emitted zero items, a Merge
    firing twice and dropping every real row, and a judge-verdict node crashing on a marker — all
    with the execution-order setting ABSENT on every live body throughout.

    Annotate the two existing Merge rows (zero-item output is a delivery; first delivery to an
    input wins) so each states it was observed under the LEGACY order, on bodies whose settings
    carried no execution order, and that the empty-item push above is the likelier mechanism than
    a genuine delivery. Do not delete either row and do not weaken its execution ids — the
    observations stand; only their scope narrows.

    Update the paragraph beneath the table so it names which rows this round touched, replacing
    the previous round's claim that no Merge row was touched.

    Edit section 13.0.2's deployment-state notes. The live-state table there describes the MIXED
    state left by the runaway stop and is now stale: the Gate 8 session rolled ALL FIVE workflows
    back to the pre-Phase-70 `59812be` bundle — backend_status 17, contact_ingest 29, enrichment
    123, review 26, maintenance 39 — all active, all disarmed. Correct the table in place, then
    add the current round's line: the committed JSON now carries the v1 execution order on all
    eight bodies from one generator constant (D-70-28), node counts did NOT move for the flip
    (enrichment 287, contact_ingest 69, review 55, maintenance 43, backend_status 30, and the
    local 82 / 10 / 13), and the deploy is Gate 10 — not done, nothing armed.

    Check `n8n/README.md` for any statement about execution order or about what the committed
    bodies' settings contain; if it says nothing on the subject, leave it untouched and say so in
    the SUMMARY rather than adding a paragraph for symmetry.

    Add a CHANGELOG entry in the existing style: the flip, the generation-time refusal, the PUT
    preservation pins, the walker's refusal of a legacy body, and the fact that nothing was
    deployed or armed.
  </action>
  <acceptance_criteria>
    - Section 13.0.3 contains two new rows whose basis column is tagged documented and whose text
      names both source file paths and the symbols `addNodeToBeExecuted`, `addEmptyItem` and
      `requiredInputs`.
    - Each of those two rows is a SINGLE table row, with its documented basis tag in the basis
      cell on the same line as its citation — the automated check anchors the tag to the symbol,
      so a citation split across lines from its tag reads as an untagged claim.
    - Read the finished section 13.0.3 and confirm by eye that no row claims v1 behaviour has
      been observed on this instance. This one is deliberately a manual assertion: a correct row
      may carry a version string and an observed tag on the same line (Gate 8's own row says the
      setting was absent, and the annotated Merge rows stay observed-live), so no negative regex
      can tell a correct row from an upgraded one.
    - Section 13.0.3 contains one new row tagged observed-live citing executions 12349-12353.
    - The two pre-existing Merge rows both still carry their original execution ids AND now say
      they were observed under the legacy order.
    - Section 13.0.2's live-state table names all five workflows on the pre-Phase-70 bundle with
      counts 17 / 29 / 123 / 26 / 39, and the committed-versus-live gap is stated for the v1 flip
      with the unchanged node counts.
    - `CHANGELOG.md` has one new entry covering this round.
    - `git diff --stat -- CLAUDE.md` shows a scoped edit, not a whole-file rewrite.
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c 'addEmptyItem.*\[documented\]' CLAUDE.md && /usr/bin/grep -c 'requiredInputs.*\[documented\]' CLAUDE.md && /usr/bin/grep -c "12349" CLAUDE.md && /usr/bin/grep -c "12353" CLAUDE.md && /usr/bin/awk 'END{print "claude_lines="NR}' CLAUDE.md && /usr/bin/grep -c "^### 13.0" CLAUDE.md</automated>
    <fails_when>Either of the first two grep counts is 0 — the row citing that symbol is missing, or it is present but its basis cell does not carry the documented tag on the same table row (the anchor is deliberately positive and same-line: it pins the tag of the two source-cited rows this task adds, and cannot be tripped by an unrelated row that merely mentions a version string); OR either execution-id grep count is 0 (Gate 8's observed row is missing its ids); OR `claude_lines=` reads below 4285 (the pre-task line count — a drop means a scoped edit turned into a lossy rewrite); OR the final grep count of the section-13.0 headings is anything other than 3. The no-upgraded-claim assertion is a manual acceptance criterion below, not an automated grep — a correctly worded row can legitimately carry a version string and an observed tag on one line, so no negative regex over this table is safe.</fails_when>
  </verify>
  <done>The engine rule is on the record with its sources and its tags, Gate 8's observations carry their execution ids, the two legacy-scoped Merge rows say so, and the deployment-state note describes the instance as it actually is.</done>
</task>

<task type="auto">
  <name>Task 2: Gates 10, 11 and 12 written up; Gate 9 superseded; the runbook read-back gains the order</name>
  <files>.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md, .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md</files>
  <read_first>
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md`
      lines 431-660 — Gate 7 (the deploy plus burst watch), Gate 8 (the proof re-run, including
      its step 4 connections check and its pass-criteria list), and Gate 9 (the armed re-run that
      points at Gate 6 with substitutions). These three are the template; match their headings,
      their numbered-step style, their explicit pass-criteria list and their resume signal.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md`
      — its step 6 burst watch and its read-back step.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md`
      decision D-70-31 in full.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-17-SUMMARY.md`
      — for the exact spelling of the proof driver's new verdict field AS SHIPPED. This plan
      depends on 70-17 and runs after it, so the SUMMARY exists; read the shipped name from it,
      not from 70-17's plan.
  </read_first>
  <action>
    Append three gate sections to `70-DEFERRED-GATES.md`, after Gate 9.

    **Gate 10 — disarmed deploy and bounce of the v1 bodies, then the burst watch, nothing sent.**
    Precondition: the committed JSON is the v1 generation. Steps: deploy with
    `scripts/deploy_n8n_workflows.py`, then bounce with `scripts/bounce_n8n_workflows.py`. Pass
    criteria: all five active; node counts enrichment 287, contact_ingest 69, review 55,
    maintenance 43, backend_status 30 — and state explicitly that these are UNCHANGED by the flip,
    so an unchanged count is the expected reading and not a sign the deploy did nothing (the
    settings differ, the nodes do not); both write flags reading the false literal everywhere; and
    the bounce table's execution-order column reading v1 on all five. Note that the bounce script
    now exits non-zero by itself on a non-v1 reading, so a zero exit is part of the evidence.
    Then the two-minute integrated-burst watch from the rollback runbook, with NOTHING sent —
    this is the step the 2026-09-10 runaway made standing, and the enrichment lane is going from
    the 123-node pre-Phase-70 body to the 287-node v1 body in one deploy. Resume signal in the
    same form Gate 7 uses.

    **Gate 11 — the D-70-19 proof re-run under v1.** Precondition: Gate 10 passed. Reuse Gate 8's
    steps with two changes and nothing else:
      - step 2 is INVERTED: read each live workflow's execution order and expect v1 on every one.
        State plainly that a null or absent reading is a FAILURE of this gate — the exact opposite
        of Gate 8, where null was the expected observation — and that this inversion is the whole
        point of the round.
      - step 4, the runData-source-versus-declared-connections check, is RETAINED unchanged,
        including its instruction to reuse the `declaredProducersOf` pattern from
        `tests/n8n/walkerEngineFidelity.test.mjs` in a short one-off script rather than
        reinventing the walk. The mechanism execution 12316 exhibited was never isolated; the flip
        does not explain it and does not excuse skipping the check.
    The verdict field named below is the one plan 70-17 shipped. Take its spelling from
    `70-17-SUMMARY.md`; if the SUMMARY records anything other than `execution_order_all_v1`,
    STOP and surface the divergence in this plan's SUMMARY rather than silently adopting either
    spelling — a gate whose pass criterion names a field the driver does not emit is unrunnable.

    Pass criteria: all four sends shape-equal including both enrichment sends; every recovered
    enrichment row carrying a non-null row id matching its input row; all four primary executions
    settled; the connections check clean; writes performed zero with every live write flag reading
    the false literal and HubSpot showing no write; and the verdict's `execution_order_all_v1`
    field reading true, with `live_settings_execution_order` recording v1 per workflow. Carry Gate
    8's warning verbatim in substance: a marker-free but EMPTY result is a failure, not progress.
    Add D-70-31's explicit instruction: **if the legacy symptoms persist under v1, STOP and
    report — do not adjust the walker or the driver to match.** Add the follow-on note that a pass
    is what upgrades this round's documented v1 rows in CLAUDE.md section 13.0.3 to observed-live,
    citing this verdict file and its execution ids — and that the walker's rule (b), the end-of-run
    Merge drain deliberately left unmodelled by plan 70-16, is the first thing a pass makes it
    safe to consider modelling.

    **Gate 12 — the armed mixed-verdict re-run.** Precondition: Gate 11 passed. Follow Gate 9's
    own pattern: point at Gate 6's steps 1 to 6 with named substitutions rather than restating
    them, substituting the precondition (Gate 11, not Gate 8) and the body (the v1 generation).

    Then mark Gate 9 SUPERSEDED by Gate 12 in its own heading and opening line, the same way Gate
    6 was marked superseded, leaving its text in the record. Leave Gates 7 and 8 exactly as they
    are — Gate 7 ran and passed, Gate 8 ran and failed, and both are evidence.

    Update the document's front matter or status line so the gate inventory names 10, 11 and 12 as
    outstanding and 9 as superseded.

    Finally, edit `70-ROLLBACK-RUNBOOK.md` so its read-back step reports each workflow's execution
    order alongside its node count and write flags, and so it says which value is expected after a
    v1 deploy and which after a rollback to the pre-Phase-70 bundle (the bundle predates the flip,
    so a rollback legitimately reads no execution order at all — an operator must not read that as
    a failed rollback). Leave the two-minute burst watch as it stands.
  </action>
  <acceptance_criteria>
    - `70-DEFERRED-GATES.md` contains sections headed Gate 10, Gate 11 and Gate 12, each with a
      precondition, numbered operator steps, an explicit pass-criteria list and a resume signal.
    - Gate 11's pass criteria name `execution_order_all_v1` and state that a null execution-order
      reading is a failure.
    - Gate 11 retains the runData-source-versus-declared-connections check and names
      `declaredProducersOf`.
    - Gate 11 carries the stop-and-report instruction in D-70-31's words.
    - Gate 12 restates none of Gate 6's procedure; it names the substitutions only.
    - Gate 9's heading marks it superseded and its text is otherwise intact; Gates 7 and 8 are
      unmodified.
    - `70-ROLLBACK-RUNBOOK.md`'s read-back step reports the execution order and distinguishes the
      expected reading after a v1 deploy from the expected reading after a rollback.
    - No gate in this document instructs an executor to do anything: every step is addressed to
      the operator.
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c "^## Gate 10\|^## Gate 11\|^## Gate 12" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -c "execution_order_all_v1" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -c "declaredProducersOf" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -ci "superseded" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md && /usr/bin/grep -ci "executionOrder\|execution order" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-ROLLBACK-RUNBOOK.md && /usr/bin/grep -c "^## Gate 7 \|^## Gate 8 " .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-DEFERRED-GATES.md</automated>
    <fails_when>The first grep count is anything other than 3 (a gate heading is missing or misspelled); OR any of the next four grep counts is 0 (`execution_order_all_v1`, `declaredProducersOf`, a superseded marker, or an execution-order mention in the runbook is absent); OR the final grep count of the Gate 7 and Gate 8 headings is anything other than 2 (a heading was renamed or a section removed).</fails_when>
  </verify>
  <done>The three live gates that close G-70-6 exist in writing, in the shape the operator already runs, with Gate 11 inverting Gate 8's one expectation and keeping every other check.</done>
</task>

<task type="auto">
  <name>Task 3: the roadmap and the UAT record say what this round closed and what it did not</name>
  <files>.planning/ROADMAP.md, .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md, .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/deferred-items.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 318-345 — the Phase 70 plan list and the two existing
      gap-closure round headings, for the exact heading and bullet style.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md` lines 1-20
      (the front matter and the paused-testing line) and lines 313-345 (the G-70-6 gap block).
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/deferred-items.md`.
    - `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-15-SUMMARY.md` —
      how the previous round recorded its own bookkeeping, so this one matches.
  </read_first>
  <action>
    The Phase 70 section of `.planning/ROADMAP.md` ALREADY carries a "Gap closure, round 3"
    heading naming G-70-6 and D-70-28 through D-70-31, with three bullets for 70-16, 70-17 and
    70-18 — the planner added it. Do not add a second heading. Read those three bullets and
    correct a bullet ONLY where what was actually built diverged from its one-line summary; if
    nothing diverged, leave the roadmap untouched and say so in the SUMMARY. Any correction is a
    scoped edit; never rewrite the roadmap.

    Update `70-UAT.md`'s G-70-6 gap block so it records what closed offline and what did not,
    without changing the `status` field — the gap is not closed until Gate 11 observes it, and
    marking it closed on an offline green is exactly the mistake this phase was created to stop.
    Add, in the block's own vocabulary: that the operator ruled the flip (D-70-28), that the
    committed bodies now carry it from one generator constant with a generation-time refusal,
    that both PUT paths are pinned against reverting it (D-70-29), that the walker now refuses a
    legacy body and records which v1 rules it does and does not model (D-70-30), and that the
    remaining work is Gates 10, 11 and 12 (D-70-31). Correct the document's paused-testing line
    to name the outstanding gates.

    Also record the one deliberate non-closure so it cannot be mistaken for an oversight: the
    walker does NOT model the v1 end-of-run Merge drain (D-70-30 rule (b)), and the treatment of a
    zero-item output as a delivery (rule (c)) is unobserved under v1. Put it in
    `deferred-items.md` with the reasoning in one sentence — both are unobservable offline, and
    modelling them before Gate 11 would replace one unobserved model with another.

    Do not touch `70-CONTEXT.md`. Do not mark any requirement or gap complete.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` has EXACTLY ONE "Gap closure, round 3" heading for Phase 70 naming
      G-70-6 and D-70-28 through D-70-31, with exactly three plan bullets — no duplicate heading
      was introduced.
    - `70-UAT.md`'s G-70-6 block records the offline closure and names Gates 10, 11 and 12 as the
      remaining work, and its `status` field is unchanged.
    - `deferred-items.md` records the two unmodelled walker rules with their reason.
    - `70-CONTEXT.md` is unmodified.
    - No requirement or gap is marked complete anywhere.
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c "70-16-PLAN.md\|70-17-PLAN.md\|70-18-PLAN.md" .planning/ROADMAP.md && /usr/bin/grep -c "Gap closure, round 3" .planning/ROADMAP.md && /usr/bin/grep -c "D-70-28" .planning/ROADMAP.md && /usr/bin/grep -c "Gate 1[012]" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md && CT="$(git status --porcelain -- .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-CONTEXT.md)" && echo "context_touched=[$CT]" && /usr/bin/grep -c "status: failed" .planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-UAT.md</automated>
    <fails_when>The first grep count is anything other than 3 (a plan bullet is missing or duplicated); OR the "Gap closure, round 3" count is anything other than 1 (a second heading was added); OR the D-70-28 or Gate-1x grep count is 0; OR `context_touched=` is anything other than the empty pair `[]` (70-CONTEXT.md was edited); OR the final grep count drops below its pre-task value, meaning a gap's failed status was silently flipped.</fails_when>
  </verify>
  <done>The roadmap lists this round's three plans, the UAT record separates what closed offline from what only a live gate can close, and the two deliberately unmodelled walker rules are on the deferred list rather than lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation to operator action | a gate's written steps are what the operator executes against a live, credential-bearing instance |
| documented claim to observed fact | CLAUDE.md section 13.0.3's tagging rule — the boundary this repo has already been burned crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-18-01 | Repudiation | a documented v1 claim read later as an observed fact | high | mitigate | every new v1 row is tagged documented with a source-file and symbol citation; the verify step anchors each of the two source-cited rows' tags to its own symbol on one table row, and the no-upgraded-claim check is a manual acceptance criterion because no negative regex over this table can distinguish a correct row from an upgraded one |
| T-70-18-02 | Denial of Service | Gate 10's deploy re-triggering a self-dispatch burst | critical | mitigate | Gate 10 carries the two-minute burst watch BEFORE anything is sent, and names the 123-to-287-node jump on the enrichment lane as the reason it is not optional |
| T-70-18-03 | Elevation of Privilege | Gate 12's armed run writing to HubSpot on an unproved graph | critical | transfer | Gate 12's precondition is Gate 11, which is itself preconditioned on Gate 10; all three are operator steps with explicit stop-and-report instructions |
| T-70-18-04 | Tampering | a gap silently marked closed on an offline green | high | mitigate | Task 3 forbids changing the gap's status field and the verify step counts the failed-status markers |
| T-70-18-SC | Tampering | npm / pip / cargo installs | high | mitigate | none — this plan installs no package and adds no dependency |
</threat_model>

<verification>
- Two documented engine-rule rows with source-file and symbol citations; one observed-live row
  with executions 12349-12353; two existing Merge rows annotated as legacy-scoped, neither
  deleted.
- No observed-live claim about v1 anywhere.
- Section 13.0.2's live-state table corrected to all five workflows on the pre-Phase-70 bundle.
- Gates 10, 11 and 12 present in the established shape; Gate 11 inverts Gate 8's step 2 and keeps
  its step 4; Gate 9 superseded; Gates 7 and 8 untouched.
- The rollback runbook reads back the execution order and distinguishes the two expected values.
- Roadmap and UAT bookkeeping updated; 70-CONTEXT.md untouched; nothing marked complete.
- Nothing deployed, bounced, armed or sent.
</verification>

<success_criteria>
Everything this round established is on the record with the right tag, everything the incident
left stale is corrected, and the three live gates that alone can close G-70-6 are written down
precisely enough for the operator to run them without interpretation.
</success_criteria>

<output>
Create `.planning/phases/70-one-merge-one-result-channel-n8n-runtime-truth/70-18-SUMMARY.md`
when done. Record: the rows added and annotated in section 13.0.3, whether `n8n/README.md`
needed a change, the three gate headings as written, and confirmation that Gates 7 and 8 and
70-CONTEXT.md were not modified.
</output>
