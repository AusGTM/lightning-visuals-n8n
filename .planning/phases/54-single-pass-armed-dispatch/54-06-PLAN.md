---
phase: 54-single-pass-armed-dispatch
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/code/reviewApply.js
  - tests/test_review_contact_property_sets.py
  - tests/test_hubspot_enums_generated_currency.py
  - n8n/wf_review_decision_cloud.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - .planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md
autonomous: true
gap_closure: true
requirements: [G-3]

estimate:
  tokens: 96000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The contacts review-decision lane fetches every config/field_policy.yaml `contacts` key, so reviewApply's compare-and-set has a real live baseline for all twelve fields instead of ten silent `undefined`s (WR-02, test-proven with synthetic candidates — no live contacts candidate producer exists to prove it live)."
    - "The up-to-100-record contacts queue read is NOT widened by that fix, matching the companies lane's own deliberate narrow/wide split."
    - "No comment in scripts/build_cloud_workflows.py still asserts that a contacts approve resolves without a write (WR-01), including the one inside the deployed `Build Review Decision` jsCode literal."
    - "reviewApply.js's header states that the enum guard is company-only today and says why, instead of claiming symmetric guard coverage across both policies (WR-03)."
    - "A drift-guard test fails the moment any DEFAULT_CONTACT_POLICY field becomes enumeration-typed in the pinned contacts snapshot, forcing a real CONTACT_ENUM_PROPERTIES table then rather than leaving a silent no-op."
    - "The running LV Review Decision (Cloud) workflow carries the corrected jsCode and the widened contacts fetch, proven by an independent re-GET, and is still disarmed afterwards."
  artifacts:
    - scripts/build_cloud_workflows.py
    - n8n/code/reviewApply.js
    - tests/test_review_contact_property_sets.py
    - tests/test_hubspot_enums_generated_currency.py
    - n8n/wf_review_decision_cloud.json
    - .planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md
  key_links:
    - "config/field_policy.yaml `contacts` -> _CONTACT_POLICY_FIELDS -> REVIEW_CONTACT_DECISION_PROPERTIES_CSV -> `Review Contact Fetch By Id` + `Review Contact Verify Fetch` in the built JSON"
    - "REVIEW_CONTACT_QUEUE_PROPERTIES_CSV -> `Review Queue Contact Search` ONLY (the up-to-100-record read stays narrow)"
    - "n8n/code/mergeContacts.js DEFAULT_CONTACT_POLICY -> the pinned contacts schema snapshot `type` field -> the new enum drift-guard assertion"
    - "scripts/build_cloud_workflows.py -> python3 scripts/build_cloud_workflows.py -> n8n/wf_review_decision_cloud.json -> apply_mutation deploy + bounce -> the running instance"
---

<objective>
Close WR-01, WR-02 and WR-03 — the three findings that all live on the contacts review-decision
lane and all reach the running workflow through one rebuild.

Purpose: the operator declined residual status for these on 2026-08-27. All three are DORMANT
today — no live contacts candidate producer exists (`engine-only` scope decision, 54-03), so
nothing is currently broken and no urgency is claimed. They are being closed by operator choice.
WR-02 is nonetheless a real non-clobber bypass the moment a producer exists, and WR-01's stale
text ships inside the deployed node's own jsCode, which is why this plan ends in a deploy rather
than a repo-only fix.

Output: a widened contacts decision-fetch set with the queue read left narrow, three truthful
comment regions, a reviewApply.js header that describes the guard it actually has, two new
drift-guard tests, both built workflows regenerated, and the review-decision workflow deployed
disarmed with an independent re-GET.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-single-pass-armed-dispatch/54-REVIEW.md
@.planning/phases/54-single-pass-armed-dispatch/54-04-SUMMARY.md
@.planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Split the contacts property set so the decision lane fetches its whole policy baseline and the queue read stays narrow</name>
  <files>scripts/build_cloud_workflows.py, tests/test_review_contact_property_sets.py, n8n/wf_review_decision_cloud.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py lines 7015-7080 — `_COMPANY_POLICY_FIELDS`, `_REVIEW_FAMILY`, `REVIEW_DECISION_PROPERTIES_CSV`, `REVIEW_CONTACT_PROPERTIES_CSV`, `REVIEW_QUEUE_PROPERTIES_CSV`. The companies lane already models the exact wide/narrow split this task mirrors, and states its own rationale in the 7064-7070 comment.
    - scripts/build_cloud_workflows.py lines 7386-7440 and 7515-7536 — the three call sites: `Review Contact Fetch By Id` (limit=1), `Review Contact Verify Fetch` (limit=1), `Review Queue Contact Search` (limit=queue_limit_expr, up to 100 records).
    - scripts/build_cloud_workflows.py `_hs_http_search_node` — to see where `properties_csv` lands inside the emitted node's parameters, so the built-JSON assertion below reads the right place.
    - tests/test_field_policy_conformance.py — the established idiom for a drift guard between config/field_policy.yaml and a second surface. Mirror its failure-message discipline (name both sides, say "edit both in the same commit").
    - n8n/code/mergeContacts.js DEFAULT_CONTACT_POLICY (around line 47-60) — the twelve keys, confirmed identical to config/field_policy.yaml's `contacts` block.
  </read_first>
  <action>
    Replace the single `REVIEW_CONTACT_PROPERTIES_CSV` constant with two, mirroring the companies
    lane's existing wide/narrow pair.

    Derive the policy keys from YAML exactly the way `_COMPANY_POLICY_FIELDS` already does at
    line 7024 — `tuple(sorted(yaml.safe_load(...)["contacts"]))` — never re-typed by hand. The
    review's own fix sketch names this discipline and it is the whole point: a policy key added
    later and not refetched comes back `undefined` and reads to reviewApply as a manual edit.

    Emit `REVIEW_CONTACT_DECISION_PROPERTIES_CSV` = the existing identity keys + `_REVIEW_FAMILY`
    + `lv_contact_enrichment_provenance` + the twelve derived contacts policy fields, deduped with
    `dict.fromkeys` as the sibling constants do. Emit `REVIEW_CONTACT_QUEUE_PROPERTIES_CSV` = the
    current narrow set, byte-identical in membership to what the constant holds today.

    Point `Review Contact Fetch By Id` and `Review Contact Verify Fetch` at the DECISION set, and
    `Review Queue Contact Search` at the QUEUE set. Both limit=1 nodes take the wide set for the
    same reason the companies lane gives at line 7037-7040: the verify fetch must be able to cover
    every key `would_write` may carry, or a landed write reports `verified_properties` that cannot
    see it. The queue node keeps the narrow set for the reason the companies lane gives at line
    7066-7070 — the queue compares nothing, so a baseline fetched for up to 100 records is payload
    nobody reads. Do NOT widen the queue read.

    Write the comment above each new constant so it states the current, true reason for the split
    rather than the pre-54-03 premise the old comment carried. Say plainly that a contacts approve
    now runs the SAME reviewApply engine as companies keyed on DEFAULT_CONTACT_POLICY, and that
    the wide set exists to give that engine its compare-and-set baseline.

    Create `tests/test_review_contact_property_sets.py` with two assertions:
    (a) every key of config/field_policy.yaml's `contacts` block appears in
    `REVIEW_CONTACT_DECISION_PROPERTIES_CSV` — the non-clobber invariant, and the thing that fails
    loudly when the policy grows;
    (b) against the CHECKED-IN `n8n/wf_review_decision_cloud.json` (not an in-memory `build_cloud()`
    result): the `Review Contact Fetch By Id` and `Review Contact Verify Fetch` nodes request a
    representative unfetched-today policy field such as `mobilephone`, and the
    `Review Queue Contact Search` node does not. Reading the checked-in file is deliberate — it is
    what makes this test the end-to-end proof that the builder change reached the built artifact,
    and it doubles as a currency guard for these three nodes.

    Then run `python3 scripts/build_cloud_workflows.py` to regenerate the built JSON so assertion
    (b) passes. Expect the diff to touch only the three contacts search nodes in
    `n8n/wf_review_decision_cloud.json`; node counts stay 26 (review-decision) and 39
    (maintenance). Never hand-edit either built JSON file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review_contact_property_sets.py -q && .venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    The two new constants exist and are derived from config/field_policy.yaml; the two limit=1
    contacts nodes in the checked-in built JSON request all twelve policy fields and the
    up-to-100-record queue node does not; both new assertions pass; both full suites stay green;
    `n8n/wf_review_decision_cloud.json` still has 26 nodes and
    `n8n/wf_scheduled_maintenance_cloud.json` still has 39.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the three stale comment regions and reviewApply.js's header describe what the code actually does</name>
  <files>scripts/build_cloud_workflows.py, n8n/code/reviewApply.js, tests/test_hubspot_enums_generated_currency.py, n8n/wf_review_decision_cloud.json, n8n/wf_scheduled_maintenance_cloud.json</files>
  <read_first>
    - n8n/code/reviewDecision.js's own header and its contacts approve branch — the current, correct reasoning is already written there; reuse it rather than inventing a second account of the same fact.
    - n8n/code/reviewApply.js lines 1-48 — the header block, in particular the "One engine, two policies" sentence and the ENUM GUARD paragraph.
    - n8n/code/hubspotEnums.js `isEnumBound` and n8n/code/hubspotEnums.generated.js `COMPANY_ENUM_PROPERTIES` — six company-only keys.
    - tests/test_hubspot_enums_generated_currency.py::test_enum_properties_matches_field_policy_enumeration_keys — the exact drift-guard idiom to mirror: read the policy module as TEXT (never import it, this file must not require a JS runtime), check each key's `type` against the pinned snapshot.
    - config/hubspot_migration/baseline/portal-schema-contacts-54-03-contacts-check.json — the freshest contacts schema snapshot, produced by 54-03.
  </read_first>
  <action>
    WR-01 — rewrite three comment regions in scripts/build_cloud_workflows.py that all carry the
    same pre-54-03 premise. Two are the ones the review cites (around 7049-7055, governing the
    contacts property set, and around 7215-7220, inside the `r"""..."""` jsCode literal for the
    `Build Review Decision` node). The third is around 7527-7531 above `Review Queue Contact
    Search`, which asserts the queue and the decision endpoint share one constant — Task 1 makes
    that false as well, so it is the same finding's third instance and must land here.

    In each, state the current reasoning already written in reviewDecision.js's header: a contacts
    approve now calls the SAME reviewApply engine as companies, keyed on DEFAULT_CONTACT_POLICY.
    Frame the no-write shape as a LIVE-SHAPE FACT scoped to today — it holds because the one
    candidate producer in this repo (`Decide Company Action`) never stages a contacts candidate,
    not because the code forces it. Do not write an unconditional or permanent claim; that is the
    exact defect being closed.

    WR-03 — rewrite reviewApply.js's ENUM GUARD paragraph and the "One engine, two policies"
    sentence so they stop claiming symmetric guard coverage. State that the guard consults
    COMPANY_ENUM_PROPERTIES only, and that this is correctly inert for contacts TODAY because
    every DEFAULT_CONTACT_POLICY field is `type: string` in the pinned contacts snapshot — so
    there is nothing for a contacts table to hold and generating one would emit an empty object.
    Name the drift-guard test below as what makes that a pinned fact rather than an assumption,
    and name the follow-on work it forces if the pin ever breaks: generate a
    CONTACT_ENUM_PROPERTIES table and extend isEnumBound/normalizeEnumValue to consult the right
    table by object type. Do NOT generate an empty table now.

    Add that drift guard as a fourth test in tests/test_hubspot_enums_generated_currency.py:
    extract DEFAULT_CONTACT_POLICY's keys from n8n/code/mergeContacts.js as TEXT, look each up in
    the pinned contacts snapshot, and assert none is `type: "enumeration"`. Make the failure
    message say what to build when it fires, not just that it fired.

    Then run `python3 scripts/build_cloud_workflows.py`. reviewApply.js's header ships verbatim
    into inlined jsCode (confirmed: its ENUM GUARD text is present in both built files), so this
    rebuild legitimately touches `n8n/wf_review_decision_cloud.json` AND
    `n8n/wf_scheduled_maintenance_cloud.json`. Both are committed; only the review-decision one is
    deployed in Task 3 — the maintenance workflow's stored delta stays undeployed per 54-04's
    standing decision. Never hand-edit either built JSON file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_hubspot_enums_generated_currency.py -q && ! grep -qi 'contacts approve resolves to' scripts/build_cloud_workflows.py && .venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    No comment in scripts/build_cloud_workflows.py asserts a contacts approve resolves without a
    write; all three regions read as scoped-to-today live-shape facts. reviewApply.js's header
    names the guard's real, company-only scope and its reason. The new contacts-enum drift guard
    passes and would fail if any contacts policy field went enumeration-typed. Both built files
    regenerated by the builder; both suites green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy the review-decision workflow disarmed, bounce it, and prove the running content changed</name>
  <files>.planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md</files>
  <reversibility rating="costly">Touches the running production n8n instance. Reversible by redeploying the prior workflow body, but a bad deploy is visible to the operator before it is reverted.</reversibility>
  <read_first>
    - .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md — 54-04's record, five days old, is the template and the proof this ceremony costs 0 n8n executions.
    - operator-claude-plugin/scripts/n8n_control.py `apply_mutation` — signature is
      `(workflow_id, mutate_fn, allowed_node_names, config, *, verify_fn, ...)`. `allowed_node_names`
      is a collection, so one call can scope several nodes.
  </read_first>
  <action>
    Deploy the committed `n8n/wf_review_decision_cloud.json` delta to the live
    `LV Review Decision (Cloud)` workflow (`WBJwoZOo63wzeP69`) via
    `n8n_control.apply_mutation`, node-scoped to exactly the nodes Tasks 1 and 2 changed:
    `Build Review Decision`, `Review Contact Fetch By Id`, `Review Contact Verify Fetch` and
    `Review Queue Contact Search`. Node-scoped rather than a whole-file deploy script run, for the
    same reason 54-04 gave: a whole-file run would also push the maintenance workflow's
    committed-but-undeployed delta live.

    The deploy stays DISARMED throughout. `ALLOW_HUBSPOT_RECORD_WRITES` and
    `ALLOW_HUBSPOT_REVIEW_WRITES` stay `"false"`; no allowlist is widened; no write-granting helper
    is called. If any step would require an armed window, stop and report rather than arming.

    `apply_mutation` already performs deactivate -> PUT -> restore prior active state, which is the
    bounce that forces the running instance to reload (D-18). A bare PUT does not reload a running
    workflow, so do not substitute one. After it returns, take a SECOND, INDEPENDENT fresh GET —
    distinct from the PUT's own response and from `apply_mutation`'s internal post-mutation GET —
    and read the running content off that. That independent GET is the only accepted proof.

    Assert against it: node count still 26; the deployed `Build Review Decision` jsCode is
    byte-identical to the committed local node body; the deployed `Review Contact Fetch By Id`
    node requests the widened contacts property set; `active` matches its prior state.

    Then run `python3 scripts/verify_live_write_safety.py` and confirm its disarmed expectation
    passes across all live workflows and declaring nodes, including the nodes this deploy touched.

    Write `.planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md` in the shape of
    54-DEPLOY-RECORD.md: the mutation call and its node scope, the bounce, the independent GET's
    evidence quoted distinctly from the PUT response, the disarm verdict, and the n8n execution
    count consumed (expected 0 — this ceremony runs no workflow). Record explicitly that
    `n8n/wf_scheduled_maintenance_cloud.json` now carries TWO committed-but-undeployed deltas —
    54-04's and Task 2's reviewApply.js header — and that leaving it undeployed is 54-04's standing
    decision, not an oversight.
  </action>
  <verify>
    <automated>python3 scripts/verify_live_write_safety.py && test -f .planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md && grep -q 'WBJwoZOo63wzeP69' .planning/phases/54-single-pass-armed-dispatch/54-06-DEPLOY-RECORD.md</automated>
  </verify>
  <done>
    The running LV Review Decision (Cloud) workflow serves the corrected jsCode and the widened
    contacts fetch, proven by an independent re-GET distinct from the PUT response; the disarm
    verifier passes across every live workflow after the deploy; the deploy record exists and
    names the maintenance workflow's two stranded deltas as a standing decision.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> the live n8n Cloud instance | Task 3 PUTs workflow content into a running production orchestrator |
| the n8n review-decision endpoint -> HubSpot CRM | the deployed nodes can PATCH real records once armed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.06-01 | Tampering | Task 3 deploy to `WBJwoZOo63wzeP69` | high | mitigate | `apply_mutation` refuses before any network call if anything outside `allowed_node_names` differs, and always fetches fresh rather than PUTting a session-stale body (T-28-06) |
| T-54.06-02 | Elevation of Privilege | write-safety literals on the deployed nodes | critical | mitigate | Deploy stays disarmed; `verify_live_write_safety.py` re-asserts `ALLOW_HUBSPOT_*_WRITES` false and both allowlists empty across all live workflows after the PUT; arming is out of scope and would require a human checkpoint |
| T-54.06-03 | Tampering | contacts non-clobber baseline | high | mitigate | Task 1 widens the decision fetch so reviewApply's compare-and-set can see all twelve live values instead of ten `undefined`s; the new pin test fails when the policy grows past the fetch |
| T-54.06-04 | Repudiation | what actually reached the running instance | medium | mitigate | An independent fresh GET, distinct from the PUT response and from `apply_mutation`'s internal GET, is the only accepted evidence; recorded in 54-06-DEPLOY-RECORD.md |
| T-54.06-05 | Denial of Service | n8n monthly execution budget (2,500) | low | accept | The whole plan consumes 0 n8n executions — no workflow is run; 54-04 measured exactly this for the same ceremony |
| T-54.06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; package-legitimacy gate not triggered |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — full Python suite green (3220 passing at phase start; the two new tests add to that count).
- `node --test tests/n8n/*.test.mjs` — full node suite green (776 at phase start). Glob form, not the directory form.
- Built JSON authored only by `python3 scripts/build_cloud_workflows.py`; node counts 26 / 39 unchanged.
- `python3 scripts/verify_live_write_safety.py` passes post-deploy.
</verification>

<success_criteria>
- WR-01: no comment in `scripts/build_cloud_workflows.py` asserts a contacts approve resolves without a write, in any of the three regions, and the corrected text ships in the deployed jsCode.
- WR-02: every `config/field_policy.yaml` `contacts` key is fetched by the two limit=1 contacts nodes; the up-to-100-record queue node is unchanged. TEST-PROVEN with synthetic candidates only — no live contacts candidate producer exists, so this plan claims no live proof of the promote branch.
- WR-03: `reviewApply.js`'s header describes a company-only enum guard and says why it is correctly inert for contacts today; a drift guard pins that reason and names what to build when it breaks. No empty table is generated.
- The deploy is disarmed, bounced, and proven by an independent re-GET; 0 n8n executions consumed.
</success_criteria>

<output>
Create `.planning/phases/54-single-pass-armed-dispatch/54-06-SUMMARY.md` when done
</output>
