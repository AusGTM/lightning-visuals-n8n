---
phase: 44-sj-3-dispatch-gate-drain-cap
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - config/execution_budget.yaml
  - scripts/build_cloud_workflows.py
  - n8n/code/sj3DispatchGate.js
  - n8n/wf_scheduled_maintenance_cloud.json
  - tests/n8n/sj3DispatchGate.test.mjs
  - tests/test_execution_budget.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, CAP-01, CAP-02, CAP-03]

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One SJ-3 tick dispatches at most a cap derived at build time from the configured plan allowance, the configured share, and the cadence the builder is baking into SJ-3's own trigger — never a hardcoded number (CAP-01, D-10/D-11)."
    - "A record that passes the gate but exceeds the cap keeps its flag and is picked up next tick — the drain never touches it (D-09)."
    - "Every tick where the gate ran emits a named outcome plus found/permitted/declined/capped counts, distinguishable from 'found nothing to do' and never modelled as an error (GATE-02, CAP-02, D-13/D-14)."
    - "A gate-open tick with permitted rows at or under the cap dispatches all of them, in input order, with nothing swallowed or reordered (GATE-03)."
    - "A test fails if the shipped schedule's computed monthly execution floor exceeds the configured share of the plan allowance (CAP-03)."
  artifacts:
    - config/execution_budget.yaml
    - tests/test_execution_budget.py
    - "n8n/code/sj3DispatchGate.js — cap + tick-summary expansion"
    - "n8n/wf_scheduled_maintenance_cloud.json — `SJ-3 Tick Outcome` node and the baked cap"
  key_links:
    - "`config/execution_budget.yaml` -> the builder's derived cap AND the CAP-03 test AND Phase 45's ALARM-03 — one allowance, read three times, never duplicated (D-11)"
    - "`SJ-3 Dispatch Gate` -> `SJ-3 Tick Outcome` (fan-out off the gate's own output; the only node that runs on a fully gate-closed tick — GATE-02)"
    - "`_schedule_trigger`'s (field, interval) -> ticks-per-month -> cap: re-timing the trigger must move the cap, or CAP-01 is a hardcoded constant wearing a formula (D-10)"
---

<objective>
Expand the proven SJ-3 slice with the dispatch cap and the tick outcome. The cap is derived
at build time from configured budget rather than written as a literal (CAP-01), a capped tick
says how many records it found versus dispatched (CAP-02), a gate-closed tick reports a named
non-error outcome an operator can tell apart from "nothing to do" (GATE-02), and a test refuses
a shipped schedule whose monthly floor eats the budget (CAP-03).

Purpose: bound one tick's spend and make the gate's decision visible, without touching the
dispatch path Plan 01 proved.
Output: `config/execution_budget.yaml` as the single allowance source (Phase 45's ALARM-03
reads the same key), a regenerated maintenance workflow, and the CAP-03 budget test.
**Still no deploy** — Plan 03 ships gate + drain + cap together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-CONTEXT.md
@.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-RESEARCH.md
@.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-01-SUMMARY.md

Read before editing:
- `scripts/build_cloud_workflows.py:5530-5545` — `_schedule_trigger` and its per-interval
  monthly cost arithmetic (15 min = 2,880/month per trigger, hourly = 720, daily = 30)
- `scripts/build_cloud_workflows.py:6115-6125` — the existing build-time `config/*.yaml` read
  (`_COMPANY_POLICY_FIELDS`), the pattern D-11 mirrors
- `tests/test_field_policy_conformance.py:31` — the test-side mirror of that read; tests
  re-derive from the YAML rather than importing the builder's constant, so drift is visible
- `n8n/code/sj3DispatchGate.js` and `tests/n8n/sj3DispatchGate.test.mjs` (from Plan 01)
- `scripts/backfill_seed_company_scores.py:80-85`, `:197-227` — the repo's "no silent caps"
  precedent. Read it to inherit the *spirit*; its mechanism is refuse-entirely, and CAP-02
  needs truncate-and-continue, so do not try to reuse the function.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the dispatch cap from configured budget and apply it as deferral, not truncation</name>
  <files>config/execution_budget.yaml, scripts/build_cloud_workflows.py, n8n/code/sj3DispatchGate.js, tests/n8n/sj3DispatchGate.test.mjs, n8n/wf_scheduled_maintenance_cloud.json</files>
  <read_first>`scripts/build_cloud_workflows.py:5530-5545`, `:6115-6125`; `tests/test_field_policy_conformance.py:31`</read_first>
  <behavior>
    `sj3Gate(rows, opts)` gains `opts.cap` (a positive integer) and a third disposition.
    - 3 rows all permitted, cap 10: all three `sj3_dispatch: true`, none deferred.
    - 5 rows all permitted, cap 2: the first two `sj3_dispatch: true`, the remaining three
      `sj3_dispatch: false` **and** `sj3_drain: false` — deferred, not drained (D-09).
    - Mixed permitted/declined with a cap: the cap counts only permitted rows; declined rows are
      drained regardless of cap position, and a declined row never consumes cap budget.
    - Cap applies in input order, so the deferred remainder is the tail, not an arbitrary subset.
    - Every returned row carries the same `sj3_tick` summary object: `{found, permitted,
      dispatched, declined, deferred, cap, outcome}`, where `outcome` is one of `dispatched`,
      `gate_closed`, or `capped_partial`.
    - `found === permitted + declined`; `permitted === dispatched + deferred`. Assert both.
  </behavior>
  <action>
    Create `config/execution_budget.yaml` as the single home for the plan allowance (D-11): the
    monthly n8n Cloud execution allowance (2,500, the measured plan), the share of it SJ-3's
    dispatch lane may consume (default ~0.5, per D-10 — the remaining headroom is deliberately
    reserved for webhook and operator-initiated enrichment, which do not pass SJ-3 and are
    therefore not covered by this cap, D-12), and the maximum share of the allowance the shipped
    schedule's idle floor may consume (Task 3's CAP-03 threshold). Head the file with the
    measured incident it exists to prevent — 253 executions/hour flat for at least 10 hours,
    ~182,000/month against 2,500 — and a note that Phase 45's ALARM-03 reads the allowance key
    from here too, so there is one allowance rather than two that drift. Env var was rejected
    (`.env` is permission-blocked to tooling here and a committed test cannot read it); a builder
    constant was rejected because Phase 45 would then have to import from a build script.

    In `scripts/build_cloud_workflows.py`, read that YAML at module import time exactly the way
    `_COMPANY_POLICY_FIELDS` reads `field_policy.yaml` at `:6120-6121`. Derive the cap from the
    allowance, the share, and the ticks-per-month implied by the `(field, interval_value)` pair
    the builder is passing to `_schedule_trigger` for SJ-3 — reusing that function's own
    documented arithmetic (15 min = 2,880/month per trigger, hourly = 720, daily = 30). Subtract
    the tick's own execution from the per-tick budget, since GATE-01's cost model is `1 +
    dispatched`. At the shipped daily cadence with a 0.5 share this yields 40, matching D-10's
    stated expectation — put that worked number in a comment as a sanity anchor, but derive it,
    never write it. Re-timing SJ-3's trigger must move the cap; if it does not, CAP-01 is not met.

    Extend `n8n/code/sj3DispatchGate.js` per the behavior block. Deferred rows are the crux of
    D-09: a record that passes the gate but exceeds the cap is *deferred* work, not *declined*
    work, so it keeps its flag and the next tick picks it up. The drain must never touch it —
    draining overflow would be exactly the silent truncation CAP-02 forbids. Encode that as a
    comment on the deferral branch, not only in the tests.

    Bake the derived cap into the `SJ-3 Dispatch Gate` node's wrapper as a build-time constant
    and pass it into `sj3Gate`. Never compute it at n8n runtime — every other budget-ish constant
    in this builder is baked, and a runtime-computed cap would put budget arithmetic in a Code
    node nothing tests.

    Extend `tests/n8n/sj3DispatchGate.test.mjs` with the behavior block's cases, plus a wiring
    assertion that the built gate node's jsCode carries a numeric cap constant. Regenerate the
    workflow JSON through the builder.
  </action>
  <verify>
    <automated>node --test tests/n8n/sj3DispatchGate.test.mjs</automated>
  </verify>
  <done>
    `config/execution_budget.yaml` exists and is the only place the allowance appears. The gate
    node's baked cap equals allowance x share / ticks-per-month minus the tick itself, computed
    from SJ-3's actual trigger arguments. Overflow rows are deferred (`sj3_dispatch` false,
    `sj3_drain` false) and every row carries the `sj3_tick` summary with `found === permitted +
    declined` and `permitted === dispatched + deferred`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit the tick outcome — the one node that runs on a fully gate-closed tick</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_scheduled_maintenance_cloud.json, tests/n8n/sj3DispatchGate.test.mjs</files>
  <read_first>`scripts/build_cloud_workflows.py:4129-4133` and `:4178-4179` — the repo's established zero-items-stops-the-chain findings, which are exactly why this node cannot hang off the dispatch branch</read_first>
  <action>
    On a fully gate-closed tick, `SJ-3 Build Dispatch Event` returns an empty array and
    `SJ-3 Dispatch To Enrichment` never runs — which satisfies GATE-01's cost bound but leaves
    nothing downstream recording what happened. That is the "indistinguishable from failure or
    from nothing to do" outcome GATE-02 forbids.

    Add a `SJ-3 Tick Outcome` Code node fed **directly from `SJ-3 Dispatch Gate`** — a third
    consumer of the gate's single output, alongside `SJ-3 Build Dispatch Event` and
    `SJ-3 Drain Gate`. It must not sit downstream of any branch whose item count can reach zero,
    or it inherits the exact problem it exists to solve. It reads the `sj3_tick` summary off its
    first input item and returns a single item carrying the named outcome plus the counts, so a
    gate-closed tick's execution data contains one node whose output plainly says so.

    Emit a named string outcome, not counts alone: `gate_closed` when permitted is zero and found
    is non-zero, `capped_partial` when deferred is non-zero, `dispatched` otherwise. A tick where
    the search matched nothing produces no items at all through `SJ-3 Extract Rows`, so the gate
    and this node do not run — which is itself the distinguishing signal between "gate closed" and
    "found nothing to do" that GATE-02 asks for. Record that reasoning in the node's comment.

    This node is quiet and recorded, never loud (D-14): disarmed is the normal resting state, so a
    tick declining 61 records while disarmed is correct behaviour, not an incident. It must not
    throw, must not set an error status, and must not emit a warning — Phase 45's burn-rate alarm
    is the thing that should be loud. Note in the comment that this is one of D-13's two
    observation points and the ephemeral one: n8n prunes execution history at ~2,500 rows, so the
    durable evidence is the drained records' own `lv_enrichment_status`.

    Regenerate. Extend `tests/n8n/sj3DispatchGate.test.mjs` with a wiring assertion that
    `SJ-3 Tick Outcome`'s only feeder is `SJ-3 Dispatch Gate` — not `SJ-3 Build Dispatch Event`,
    not the drain branch — and that its jsCode carries all three outcome strings.
  </action>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    `SJ-3 Tick Outcome` exists, is fed only by `SJ-3 Dispatch Gate`, emits exactly one item
    carrying a named outcome (`gate_closed` / `capped_partial` / `dispatched`) plus found,
    permitted, dispatched, declined, deferred and cap, and raises no error on the gate-closed path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pin gate-open dispatch behaviour (GATE-03) and the shipped schedule's budget floor (CAP-03)</name>
  <files>tests/test_execution_budget.py, tests/n8n/sj3DispatchGate.test.mjs</files>
  <read_first>`scripts/build_cloud_workflows.py:5530-5545`; `tests/test_field_policy_conformance.py:31`; the committed `n8n/wf_*_cloud.json` schedule-trigger nodes</read_first>
  <action>
    GATE-03, in `tests/n8n/sj3DispatchGate.test.mjs`: assert that for a set of permitted rows at
    or under the cap, `sj3Gate` returns every one of them marked for dispatch, in the same order
    they arrived, with no field of the original row mutated or dropped — the gate check must not
    swallow or reorder a single dispatch inside a legitimately armed window. Include a case with
    declined rows interleaved between permitted ones, so a filter that quietly re-sorts to group
    dispositions fails. The downstream per-record refusal inside the enrichment lane is unchanged
    and stays (D-03) — the webhook and operator-initiated paths reach enrichment without passing
    SJ-3 at all, so this gate is defence in depth, not a replacement.

    CAP-03, in a new `tests/test_execution_budget.py`: compute the shipped schedule's monthly
    execution *floor* — the cost of every schedule trigger firing and finding nothing — by reading
    every `n8n-nodes-base.scheduleTrigger` node out of the committed `n8n/wf_*_cloud.json`
    artifacts, converting each node's own `(field, interval)` into ticks per month with the same
    arithmetic `_schedule_trigger` documents, and summing. Fail if that floor exceeds the
    configured share of the plan allowance from `config/execution_budget.yaml`.

    Re-derive the allowance and share from the YAML rather than importing the builder's computed
    constant, mirroring `tests/test_field_policy_conformance.py:31` — a test that imports the
    number the builder baked cannot see the builder and the config disagreeing.

    Make the failure message name each contributing trigger and its monthly cost, so a future
    re-timing shows which trigger blew the budget rather than only that something did. Anchor the
    test in a comment against the case it is designed to catch: the v0.7 schedule ran three
    sub-daily triggers whose idle floor alone was roughly 2.6x the entire allowance while doing no
    work, and nothing in the repo said so. Assert the guard is not vacuous — at least one schedule
    trigger must be found, or an artifact-shape change could make this pass by measuring nothing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_execution_budget.py -q</automated>
    <automated>node --test tests/n8n/sj3DispatchGate.test.mjs</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
    A gate-open tick's dispatch set and order are pinned by test. `tests/test_execution_budget.py`
    computes the shipped floor from the committed artifacts plus the YAML, passes on the current
    daily/monthly/weekly schedule, names offending triggers on failure, and would have failed the
    v0.7 sub-daily schedule. Both full suites are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configured budget -> baked runtime cap | A misread or missing config key silently changes how much one unattended tick may spend. |
| n8n execution data -> operator | The only in-tick record of what the gate decided, before n8n prunes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-07 | Denial of Service | Dispatch cap drifting out of budget when a trigger is re-timed | high | mitigate | Cap is derived from SJ-3's own trigger arguments at build time (Task 1) and the shipped floor is independently re-checked from the committed artifacts (Task 3). |
| T-44-08 | Repudiation | Silent truncation reading as "processed everything" | medium | mitigate | `SJ-3 Tick Outcome` emits found vs dispatched vs deferred on every tick where the gate ran (Task 2, CAP-02). |
| T-44-09 | Tampering | Deferred overflow drained, destroying pending work | high | mitigate | Deferred rows carry `sj3_drain: false` and the drain filters on that flag; pinned by the cap tests (Task 1, D-09). |
| T-44-10 | Information Disclosure | Tick outcome ephemeral (n8n prunes at ~2,500 rows) | low | accept | D-13's deliberate two-place design — the durable half is the drained records' `lv_enrichment_status`, which survives pruning. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` green (2427 baseline + Plan 01 + Plan 02 cases).
- `node --test tests/n8n/*.test.mjs` green (636 baseline + new cases).
- Changing SJ-3's `_schedule_trigger` interval and rebuilding visibly changes the baked cap.
- `git diff --stat n8n/` shows regenerated JSON only.
</verification>

<success_criteria>
- The per-tick dispatch cap is derived, not written (CAP-01, D-10/D-11).
- Overflow is deferred and never drained (D-09).
- A capped tick reports found vs dispatched (CAP-02).
- A gate-closed tick reports a named non-error outcome (GATE-02, D-13/D-14).
- A gate-open tick dispatches everything permitted, in order (GATE-03).
- The shipped schedule's monthly floor is tested against configured budget (CAP-03).
</success_criteria>

<output>
Create `.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-02-SUMMARY.md` when done.
</output>
