---
phase: 74-code-review-follow-ups-from-phase-73
plan: 04
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - tests/n8n/lib/walkWorkflow.mjs
  - tests/n8n/walkWorkflow.test.mjs
  - tests/n8n/walkerEngineFidelityV1.test.mjs
  - tests/n8n/mergeInputContract.test.mjs
  - tests/n8n/ingestCreateErrorLane.test.mjs
  - tests/n8n/ingestCarryMerge.test.mjs
  - tests/n8n/ingestWidenedFieldsFlow.test.mjs
  - tests/n8n/zoominfoLaneFlow.test.mjs
  - tests/n8n/enrichmentConvergenceMerge.test.mjs
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
  - CLAUDE.md
  - .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md
autonomous: true
requirements: [D-74-03, D-74-14, D-74-12, WR-08, D-74-10]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-74-03: the walker pads an alwaysOutputData node's output index 0 only; an empty output 1 on such a node makes no delivery."
    - "D-74-03: all six previously-red test files are green with the corrected walker and no per-node exemption."
    - "D-74-14: under the corrected walker, a research-error row on the enrichment lane leaves no Build Response Merge stage undrained and no real row unreported."
    - "D-74-14: the enrichment fix adds no second producer to any existing input, so nothing can double-fire under v1."
    - "WR-08: walkWorkflow.mjs's JSDoc describes the contract the file's own code actually implements for a continueErrorOutput node stub."
    - "D-74-12: the MN-01 question is answered from frozen runData or explicitly left open with its trigger; never closed on a resolves_phase match."
  artifacts:
    - tests/n8n/lib/walkWorkflow.mjs
    - n8n/wf_enrichment_cloud.json
    - CLAUDE.md
  key_links:
    - "walkWorkflow.mjs's propagate() -> every offline graph proof this repo has; a wrong padding rule makes every such proof optimistic."
    - "the corrected walker -> the research-error branch of wf_enrichment_cloud.json -> Build Response Merge -> the rows an operator gets back."
    - "tests/n8n/fixtures/frozen/exec_12522.runData.json (from 74-01) -> the fidelity test pinning the index-0 rule."
---

<objective>
Correct the offline engine model so it pads only what the real engine pads (D-74-03), fix the
one enrichment-lane consumer that the corrected model exposes (D-74-14), and record both in
CLAUDE.md section 13.0.3 with their tags. WR-08's stale JSDoc rides along in the same edit.
D-74-12's MN-01 search runs over the now-frozen runData.

Purpose: `propagate()` currently pads whichever branch of an `alwaysOutputData` node happens to
be empty. The real engine pads output 0 only. Every offline proof this repo has rests on that
rule, so the correction is the phase's highest-leverage change — and it turns six test files red
until the one graph that actually depended on the wrong rule is fixed.

Output: a corrected walker, one regenerated `wf_enrichment_cloud.json`, six green files, a
fidelity test pinned against `exec_12522`, two section 13.0.3 rows, and an MN-01 verdict.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-CONTEXT.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-RESEARCH.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-PATTERNS.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-01-SUMMARY.md
@tests/n8n/lib/walkWorkflow.mjs
@tests/n8n/walkerEngineFidelityV1.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The walker pads output 0 only — corrected end to end, ingest files green</name>
  <files>tests/n8n/lib/walkWorkflow.mjs, tests/n8n/walkWorkflow.test.mjs, tests/n8n/ingestCreateErrorLane.test.mjs, tests/n8n/ingestCarryMerge.test.mjs, tests/n8n/ingestWidenedFieldsFlow.test.mjs</files>
  <read_first>
    - `tests/n8n/lib/walkWorkflow.mjs` — the `propagate` function and its `alwaysOutputData`
      branch; the call site that invokes it once per output branch; the `drainedOnce` MN-01 cap
      and the stall pass that emits `merge_pending_runs_undrained` and
      `merge_fired_with_unfilled_input`; and the JSDoc block WR-08 names (the sentence about a
      plain-array stub on a `continueErrorOutput` node, versus what the code at the
      stub-normalisation site actually does).
    - `74-CONTEXT.md` D-74-03 and D-74-01 (ruled) — in particular the recorded per-node run and
      item counts for execution `12522` and the sentence about the MN-01 cap.
    - `74-RESEARCH.md` section "Walker's current (wrong) behaviour" and the quoted upstream
      `ensureAlwaysOutputData` body (the guard on output 0 and the hardcoded write target).
    - `73-REVIEW.md` findings CR-01 and WR-08.
  </read_first>
  <behavior>
    - An `alwaysOutputData` node whose output 0 is empty delivers one padded item on output 0.
    - The same node with a NON-empty output 0 and an empty output 1 delivers nothing on output 1.
    - A node without `alwaysOutputData` is unaffected on every output index.
    - Given the ingest graph and a batch where every create succeeds, `Create Carry Merge`
      receives deliveries on inputs 0 and 1 only, fires exactly one end-of-run drain run, and is
      NOT reported as a stall.
    - The walker's own unit test file and the three ingest flow test files are green with no
      per-node exemption anywhere in `walkWorkflow.mjs`.
  </behavior>
  <action>
    Gate the `alwaysOutputData` padding in `propagate` on the output index being zero; the index
    is already a parameter of that function and already passed by the call site, so no new
    plumbing is needed. Add the rule's own rationale to the surrounding comment, citing the
    upstream engine symbol `ensureAlwaysOutputData` (file and symbol only — this repo does not
    vendor that source) and execution `12522` as the live observation. Do NOT add a per-node
    exemption list: D-74-03's amendment rules there is none. Correct the stale JSDoc sentence
    WR-08 names in the same edit, so it describes the contract the file's own stub-normalisation
    code implements. Then run the walker against the frozen `exec_12522` graph shape and check
    whether the existing MN-01 `drainedOnce` cap already models `Create Carry Merge`'s single
    end-of-run drain run: if it does, change nothing and record that in the SUMMARY; if the
    corrected padding makes the cap report that single run as `merge_pending_runs_undrained`,
    adjust the cap so a merge whose pending run is its FIRST drain is fired rather than reported
    — the adjustment is conditional on what the walker actually reports, not assumed. Add unit
    cases in `tests/n8n/walkWorkflow.test.mjs` for the four padding behaviors above. Bring
    `tests/n8n/ingestCreateErrorLane.test.mjs`, `tests/n8n/ingestCarryMerge.test.mjs` and
    `tests/n8n/ingestWidenedFieldsFlow.test.mjs` green, adjusting their expectations only where
    the OLD expectation encoded the wrong padding rule — never by relaxing an assertion that
    still holds.
  </action>
  <acceptance_criteria>
    - Source: the padding condition in `propagate` tests the output index; a grep for the
      function name returns exactly one definition.
    - Source: the padding guard's condition references only the output index and the node's own
      always-output flag — it contains no node-name comparison and reads no per-node list, shown
      by quoting the guard's full condition in the commit body.
    - Behavior: an `alwaysOutputData` node with a non-empty output 0 and an empty output 1
      produces no delivery on output 1.
    - Behavior: walking the committed ingest graph with an all-successful create batch reports
      no stall entry naming `Create Carry Merge`.
    - Test: `tests/n8n/walkWorkflow.test.mjs`, `tests/n8n/ingestCreateErrorLane.test.mjs`,
      `tests/n8n/ingestCarryMerge.test.mjs` and `tests/n8n/ingestWidenedFieldsFlow.test.mjs`
      are all green.
    - Test: the new padding cases were seen RED before the one-line gate landed.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/walkWorkflow.test.mjs tests/n8n/ingestCreateErrorLane.test.mjs tests/n8n/ingestCarryMerge.test.mjs tests/n8n/ingestWidenedFieldsFlow.test.mjs tests/n8n/mergeInputContract.test.mjs tests/n8n/executionOrderV1.test.mjs</automated>
    <fails_when>`node --test` prints a `# fail` count of 1 or higher, or `# tests` for `walkWorkflow.test.mjs` is not greater than its pre-change count (the four padding cases must be added).</fails_when>
  </verify>
  <done>`propagate` pads output index 0 only, WR-08's JSDoc matches the code, the MN-01 cap's treatment of the single drain run is settled against the frozen fixture, and the walker plus the three ingest flow files are green with no exemption.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the enrichment lane's research-error branch graph-side (D-74-14)</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/n8n/zoominfoLaneFlow.test.mjs, tests/n8n/enrichmentConvergenceMerge.test.mjs, tests/n8n/mergeInputContract.test.mjs (registry only, if the new pair requires it)</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` — grep for `IF Research Errored`, `Validate Research
      Output`, `Build Research Failure Response`, `Research Carry Merge`,
      `set_always_output_data`, `_add_starved_lane_sentinel`, and the existing
      `Companies Research None Needed Sentinel` call site (the nearest analog, which covers the
      disjoint "no research at all" scenario).
    - `74-CONTEXT.md` D-74-14 in full, including the Claude's-Discretion line on the mechanism.
    - `74-RESEARCH.md` "Don't Hand-Roll" table — the rule that a new merge-input producer uses
      `_add_starved_lane_sentinel` and never a hand-wired Code node, and the rule that a merge
      input index is derived, never hardcoded.
    - `CLAUDE.md` section 13.0.3 — the v1 rows on Merge starvation, end-of-run drain, and
      "a zero-item output is not a delivery under v1".
  </read_first>
  <action>
    First reproduce the defect offline: drive the corrected walker over the committed
    `n8n/wf_enrichment_cloud.json` with a stub in which every research row errors, and capture
    which Merge inputs are left unfilled and which real rows never reach `Build Response`.
    Record the trace shape in the commit body — that trace, not a guess, is what the fix's
    targets are derived from. Then add ONE `_add_starved_lane_sentinel` call in the cloud
    enrichment build only (the local-live harness has no such node — the builder's own comment
    at the local sentinel block says so; confirm before assuming). Source it from the node that
    carries the full research item set entering the routing IF — `Research Carry Merge`, whose
    presence in the generated JSON must be confirmed by walking `connections`, not assumed.
    Give it the universally-quantified form of the IF's own predicate, so it emits its single
    marker exactly when every row errored, which is exactly when the IF's output 1 is empty.
    Target Merge inputs ONLY, taken from the captured trace, with each index derived through the
    existing index-deriving helper — never `Validate Research Output` itself, because a Code node
    with two producers double-fires under v1. Write the mutual-exclusion argument into the
    builder comment beside the call: the sentinel fires only when output 1 is empty, the real
    chain fires only when it is not, and the existing no-research-needed sentinel covers a
    disjoint scenario. Leave `set_always_output_data` on the routing IF in place — output 0's
    padding is the mechanism the failure-response node's own marker branch already depends on.
    Regenerate with `scripts/build_cloud_workflows.py`, commit the regenerated
    `n8n/wf_enrichment_cloud.json`, and record the node count before and after (it is 287 at the
    start of this phase). Add a walker case driving the research-error branch, asserting no
    `merge_pending_runs_undrained` and that every errored row reaches `Build Response` carrying
    its research-failed action. Bring `tests/n8n/zoominfoLaneFlow.test.mjs` and
    `tests/n8n/enrichmentConvergenceMerge.test.mjs` green.
  </action>
  <acceptance_criteria>
    - Source: the new sentinel is created by the existing starved-lane helper; a grep for a
      hand-built Code node on this lane returns none added by this task.
    - Source: no merge input index is written as a literal integer in the new call site; the
      index comes from the deriving helper.
    - Behavior: walking the regenerated enrichment graph with an all-errored research stub yields
      an empty `merge_pending_runs_undrained` list and every errored row present in
      `Build Response`'s output.
    - Behavior: walking the same graph with a no-error research stub is unchanged from before
      this task (no new delivery, no double fire) — assert the marker-emitting failure-response
      branch still produces exactly one sentinel marker.
    - Behavior: every declared Merge input in the regenerated graph still has at most one
      producer per lane-scenario; no input gains a second producer that can fire in the same
      scenario as the first.
    - Source: `tests/n8n/mergeInputContract.test.mjs`'s multi-producer-tolerant registry agrees
      with the regenerated graph in BOTH directions — that test asserts the registry exactly, so
      an unregistered new pair and a stale registered one both fail. If the new sentinel makes a
      previously single-producer input multi-producer, the registry entry lands in this task.
    - Test: `tests/n8n/zoominfoLaneFlow.test.mjs` and
      `tests/n8n/enrichmentConvergenceMerge.test.mjs` are green.
    - CLI: `.venv/bin/python scripts/build_cloud_workflows.py` followed by
      `git diff --quiet -- n8n/` exits 0 (regeneration is idempotent), and the only n8n file
      changed by this task is `n8n/wf_enrichment_cloud.json`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && node --test tests/n8n/zoominfoLaneFlow.test.mjs tests/n8n/enrichmentConvergenceMerge.test.mjs tests/n8n/mergeInputContract.test.mjs tests/n8n/executionOrderV1.test.mjs</automated>
    <fails_when>`git diff --quiet -- n8n/` exits 1 after the regeneration (generated and committed disagree), or `node --test` prints a `# fail` count of 1 or higher.</fails_when>
  </verify>
  <done>The research-error branch leaves no Merge undrained and loses no row under the corrected walker; the fix is one sentinel with derived targets, regenerated once; the two enrichment test files are green; and the post-regeneration enrichment node count is written into `74-04-SUMMARY.md` under a heading named `Node counts` so 74-06 Task 1 has a value to compare the live body against.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the rule against exec_12522, record section 13.0.3, answer MN-01</name>
  <files>tests/n8n/walkerEngineFidelityV1.test.mjs, CLAUDE.md, .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md</files>
  <read_first>
    - `tests/n8n/fixtures/frozen/exec_12522.runData.json` (produced by 74-01) and
      `tests/n8n/walkerEngineFidelityV1.test.mjs` — the existing pinned-execution pattern.
    - `tests/n8n/v1RuntimeRecordings.test.mjs` — the sibling pattern for asserting recorded
      node-run shapes.
    - `CLAUDE.md` section 13.0.3, specifically the row beginning "A node fed zero items does not
      run at all" — its two-tag format is the one to copy.
    - `.planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md` in
      full, including its trigger line.
    - `74-CONTEXT.md` D-74-12 and CLAUDE.md section 31 rule 2.
  </read_first>
  <action>
    Add a fidelity case to the `walkerEngineFidelityV1.test.mjs` family pinned against
    `tests/n8n/fixtures/frozen/exec_12522.runData.json`: assert the recording shows the create
    node's output 0 padded and its output 1 making no delivery, that `Create Carry Merge` has a
    single run, and that the walker's own prediction for that graph and input agrees with the
    recording on those points. Add two rows to CLAUDE.md section 13.0.3 following the existing
    two-tag convention: one for the padding rule, tagged documented for the upstream engine
    symbol (file and symbol only) and observed live for execution `12522` with its recorded
    counts; one for the zero-rejection end-of-run drain on `Create Carry Merge`, observed live on
    `12522`, and note that the enrichment lane's research-error branch stays documented-only
    because D-74-11 does not exercise it. Then run D-74-12's MN-01 search over
    `exec_12522.runData.json`, `exec_12434.runData.json` and `exec_12449.runData.json`: look for
    any Merge node left with two partially-filled pending runs (a Merge whose runData entry holds
    two or more node runs). If found, record the answer as a section 13.0.3 row and close the
    todo with a dated resolved block. If not found — the expected outcome per 74-CONTEXT's
    plan-time probe — leave the todo open with its trigger unchanged and say so in the SUMMARY.
    Never close it on a `resolves_phase` match.
  </action>
  <acceptance_criteria>
    - Test: the new fidelity case reads `exec_12522.runData.json` and is green.
    - Source: CLAUDE.md section 13.0.3 gains exactly two rows, each carrying an explicit
      documented or observed-live tag and, for the observed-live half, the execution id.
    - Behavior: the MN-01 search reports, per fixture, the name and run count of every Merge with
      two or more runs; the SUMMARY states the verdict.
    - Source: if the search found nothing, the todo file still exists under
      `.planning/todos/pending/` with its trigger line byte-identical.
    - Both suites green at plan close.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/walkerEngineFidelityV1.test.mjs tests/n8n/v1RuntimeRecordings.test.mjs tests/n8n/walkerEngineFidelity.test.mjs && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>`node --test` prints a `# fail` count of 1 or higher on either invocation, or the fidelity file's `# tests` count is not greater than its pre-change value (the pinned case must be added).</fails_when>
  </verify>
  <done>The index-0 rule is pinned against a real recording, section 13.0.3 carries both rows with their tags, and MN-01 has an explicit verdict with its todo handled per CLAUDE.md section 31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline engine model → every graph claim in this repo | `walkWorkflow.mjs` is the only pre-deploy check on row loss; an optimistic model is a false assurance |
| generated graph → n8n Cloud | the regenerated enrichment JSON is what a later deploy ships |
| frozen runData → committed fixture | already scrubbed by 74-01; this plan only reads it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-04-01 | Denial of Service (silent row loss) | `walkWorkflow.mjs` padding whichever branch is empty | high | mitigate | D-74-03: gate padding on output index 0; pin against `exec_12522` (Tasks 1, 3) |
| T-74-04-02 | Denial of Service (silent row loss) | enrichment research-error branch leaving `Build Response Merge` undrained | high | mitigate | D-74-14: one gated sentinel with walker-derived Merge targets; regenerate once (Task 2) |
| T-74-04-03 | Tampering | a second producer on an existing input double-firing under v1 | high | mitigate | targets restricted to Merge inputs; mutual-exclusion argument written into the builder comment; a per-input producer check in the acceptance criteria (Task 2) |
| T-74-04-04 | Repudiation | a documented-only claim read later as observed | medium | mitigate | two-tag convention enforced in the section 13.0.3 rows; research-error branch explicitly left documented-only (Task 3) |
| T-74-04-05 | Repudiation | closing an open engine question on a phase-id match rather than evidence | medium | mitigate | D-74-12: verdict from frozen runData only; todo left open with its trigger otherwise (Task 3) |
| T-74-04-06 | Information Disclosure | runData printed raw into a transcript | medium | mitigate | node names, run counts and item counts only; never raw items (Tasks 1-3) |
| T-74-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is in this plan's scope; no dependency is added |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green (baseline 1303 pass / 0 fail); the six previously-red
  files green with no per-node exemption.
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/` exits 0.
- Only `n8n/wf_enrichment_cloud.json` changed among generated workflows.
- Enrichment node count before and after recorded (287 at phase start).
- Nothing deployed; zero n8n executions consumed.
</verification>

<success_criteria>
- D-74-03, D-74-14, WR-08 closed; D-74-12 answered or explicitly left open.
- Six previously-red files green in the same commit range, no exemption anywhere.
- CLAUDE.md section 13.0.3 carries both new rows with correct tags.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Note |
|---|---|---|
| index-0-gated padding in `propagate` | behaviour change | D-74-03; one-line gate, no exemption list |
| corrected WR-08 JSDoc in `walkWorkflow.mjs` | doc | rides the same edit |
| new research-errored starved-lane sentinel node | new n8n node | D-74-14; cloud enrichment build only, targets derived from the walker trace |
| regenerated `n8n/wf_enrichment_cloud.json` | generated artifact | node count delta recorded |
| new padding cases in `tests/n8n/walkWorkflow.test.mjs` | tests | four behaviors |
| new fidelity case against `exec_12522.runData.json` | test | D-74-03 pin |
| two CLAUDE.md section 13.0.3 rows | doc | documented + observed-live tags |
| MN-01 verdict | finding | D-74-12; todo closed only on evidence |

<output>
Create `.planning/phases/74-code-review-follow-ups-from-phase-73/74-04-SUMMARY.md` when done.
State the enrichment node count before and after, the MN-01 verdict, and whether the MN-01 drain
cap needed an adjustment or already modelled the single drain run.
</output>
