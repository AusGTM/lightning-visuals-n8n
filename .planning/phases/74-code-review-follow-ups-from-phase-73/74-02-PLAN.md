---
phase: 74-code-review-follow-ups-from-phase-73
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/report_enrichment.py
  - operator-claude-plugin/scripts/chunking.py
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_report_enrichment.py
  - operator-claude-plugin/tests/test_chunking.py
  - operator-claude-plugin/scripts/run_manifest.py
  - operator-claude-plugin/tests/test_run_manifest.py
autonomous: true
requirements: [WR-01, WR-02, WR-05, WR-06, D-74-10, D-74-13]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-01: the contact-upload lane's grant preview still reports its lane-invariant execution count when the chunk ceiling is missing, because that assignment no longer sits inside the try that raises."
    - "WR-02: executions_projection_basis and providers are taken per-lane from the estimate, not from a single unconditional module constant."
    - "WR-05: the backfill ledger is keyed by (lane, id); an id that is ambiguous across lanes is skipped, never silently overwritten by the last lane."
    - "WR-06: dispatch_and_recover returns excluded_marker_count in its result dict instead of discarding it, emitted only when non-zero."
    - "D-74-13: the match-recovery bound is operator-configurable with a test pinning the resolution at both ends, and the unchecked count is readable from dispatch_and_recover's result dict (or already from the run manifest); no renderer module is touched."
    - "D-74-10: each of the four findings in this plan lands with its own test; no accept-with-reason rows."
  artifacts:
    - operator-claude-plugin/scripts/write_grant.py
    - operator-claude-plugin/scripts/report_enrichment.py
    - operator-claude-plugin/scripts/chunking.py
  key_links:
    - "write_grant.py's grant-preview text -> the operator's consent decision before any dispatch."
    - "chunking.dispatch_and_recover's returned dict -> every caller that builds what the operator reads after a batch; this plan changes the dict, never a renderer."
    - "report_enrichment.backfill_missing_identity's ledger -> per-row identity in the durable written-records ledger."
---

<objective>
Close the four operator-plugin findings the ROADMAP pre-specified: WR-01 and WR-02 in
`write_grant.py`, WR-05 in `report_enrichment.py`, WR-06 plus the folded D-74-13 question in
`chunking.py`. Each lands with its own test (D-74-10).

Purpose: all four are silent-wrong-answer bugs in the surfaces an operator reads before
consenting to a batch and after one settles — a grant preview that omits its execution count,
a basis string that describes the wrong lane, a ledger that lets one lane overwrite another,
and a marker count that is computed and thrown away.

Output: four fixed functions, four tests, no behaviour change to any n8n graph.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-CONTEXT.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-RESEARCH.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-PATTERNS.md
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-REVIEW.md
@operator-claude-plugin/scripts/write_grant.py
@operator-claude-plugin/scripts/report_enrichment.py
@operator-claude-plugin/scripts/chunking.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Grant-preview execution basis, proven end to end on the contact-upload lane (WR-01, WR-02)</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/write_grant.py` — grep for `EXECUTIONS_BASIS` and for the
      `try` block that calls `chunking.chunk_ceiling`; the review's line numbers are stale, the
      symbols are not.
    - `73-REVIEW.md` findings WR-01 and WR-02 in full, including their proposed fixes.
    - `.planning/ROADMAP.md` Phase 74 entry — the roadmap fixes the shape ("lane-invariant
      executions hoisted out of the try; per-lane basis and providers taken from the estimate").
    - `operator-claude-plugin/tests/test_write_grant.py` — THREE contact-upload-lane cases
      already exist (their names begin `test_envelope_contact_upload_lane_...` and
      `test_envelope_contact_upload_figure_...`). 74-CONTEXT's plan-time note that there is "no
      coverage" refers to the MISSING-CEILING combination, not to the lane. Read all three
      first: extend the nearest one if it already builds the preview you need, and add a new
      case only for the combination none of them covers. Do not create a fourth case that
      duplicates an existing one.
  </read_first>
  <behavior>
    - Contact-upload lane, chunk ceiling present: the preview reports one execution and a basis
      string describing the contact-upload lane.
    - Contact-upload lane, chunk ceiling MISSING (the configuration that makes `chunk_ceiling`
      raise): the preview still reports one execution, and the chunk-count sentence is omitted
      rather than rendered with a fabricated or stale figure.
    - Enrich-records lane: the basis string and the providers list come from that lane's
      estimate, not from the contact-upload values.
    - No lane silently reports an execution count of zero or a basis describing a different
      lane.
  </behavior>
  <action>
    Hoist the lane-invariant execution-count assignment out of the `try` that can raise on a
    missing chunk ceiling, so a configuration gap removes only the chunk-count sentence and
    never the execution count itself (WR-01). Replace the unconditional module-level basis
    constant read with a per-lane value taken from the estimate the function already computes,
    and do the same for the providers list (WR-02) — exactly the shape the ROADMAP fixes and
    D-74-10 restates. Suppress the "at N chunk(s) of at most M record(s)" render line for the
    contact-upload lane when the ceiling is unavailable, rather than printing a figure the
    file's own comment already documents as not that lane's real POST count. Add a new test
    case in `operator-claude-plugin/tests/test_write_grant.py` covering the contact-upload lane
    with the ceiling present and with it absent, asserting the execution count survives both
    and that the basis text names the right lane. Add a second case asserting the enrich-records
    lane's basis and providers differ from the contact-upload lane's.
  </action>
  <acceptance_criteria>
    - Behavior: with the chunk-ceiling config key removed, building a contact-upload grant
      preview returns an execution count of 1 and does not raise.
    - Source: the three pre-existing contact-upload cases still exist with their original names;
      no duplicate of any of them was added.
    - Behavior: the returned basis value for the contact-upload lane differs from the value
      returned for the enrich-records lane.
    - Source: `/usr/bin/grep -v "^#" operator-claude-plugin/scripts/write_grant.py | /usr/bin/grep -c "EXECUTIONS_BASIS"`
      is lower than its pre-change value (the unconditional use is gone); record both numbers in
      the commit body.
    - Test: the two new cases in `test_write_grant.py` were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_write_grant_guardrails.py operator-claude-plugin/tests/test_write_grant_surface.py operator-claude-plugin/tests/test_headless_grant_boundary.py</automated>
    <fails_when>pytest exits non-zero, or the run reports fewer collected tests in `test_write_grant.py` than before the change (a new case must be added, not a rename).</fails_when>
  </verify>
  <done>A contact-upload grant preview reports its execution count with or without a chunk ceiling, the basis and providers are per-lane, and two new tests pin both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ledger keyed by (lane, id), ambiguous ids skipped (WR-05)</name>
  <files>operator-claude-plugin/scripts/report_enrichment.py, operator-claude-plugin/tests/test_report_enrichment.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/report_enrichment.py` — grep for `_ACTION_LANE_ORDER`,
      `backfill_missing_identity` and the ledger dict built inside the lane loop.
    - `73-REVIEW.md` finding WR-05 and its proposed fix.
    - `74-CONTEXT.md` D-74-10's stated shape: "ledger keyed by `(lane, id)` with ambiguous ids
      skipped".
    - `operator-claude-plugin/tests/test_report_enrichment.py` — the existing backfill cases.
  </read_first>
  <behavior>
    - Two lanes both carrying the same ledger id with different payloads: neither payload is
      applied; the id is skipped and counted, not last-lane-wins.
    - One lane carrying an id no other lane carries: backfilled exactly as today.
    - An id ambiguous across lanes does not change the function's existing
      `excluded_marker_count` semantics — ambiguity and marker exclusion are separate counts.
  </behavior>
  <action>
    Key the ledger built in `backfill_missing_identity` by the tuple of lane and id rather than
    by id alone, so a lane later in `_ACTION_LANE_ORDER` can no longer silently overwrite an
    earlier lane's entry. When the same id appears under two different lanes with differing
    payloads, skip it — apply neither — per D-74-10's stated shape; ambiguity is not a merge.
    Keep the function pure and keep its existing return tuple shape. Add tests in
    `operator-claude-plugin/tests/test_report_enrichment.py` for the ambiguous-across-lanes case
    (assert neither payload applied) and for the unambiguous case (assert unchanged behaviour).
  </action>
  <acceptance_criteria>
    - Behavior: with the same id present under two lanes carrying different payloads, the
      backfilled rows carry neither payload.
    - Behavior: with the id present under one lane only, the backfilled row carries that lane's
      payload exactly as before this change.
    - Test: both new cases in `test_report_enrichment.py` were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_run_report.py operator-claude-plugin/tests/test_written_records.py</automated>
    <fails_when>pytest exits non-zero, or `test_report_enrichment.py` collects no new cases relative to the pre-change count.</fails_when>
  </verify>
  <done>An id ambiguous across lanes is skipped rather than overwritten, unambiguous backfill is unchanged, and two tests pin both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep the excluded-marker count and parametrise the recovery bound (WR-06, D-74-13)</name>
  <files>operator-claude-plugin/scripts/chunking.py, operator-claude-plugin/tests/test_chunking.py, operator-claude-plugin/scripts/run_manifest.py (only if the manifest does not already carry the unchecked count), operator-claude-plugin/tests/test_run_manifest.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/chunking.py` — grep for `dispatch_and_recover`, for the
      tuple unpack that currently discards the marker count, for the returned dict, for
      `resolve_bound_seconds` and for the ceiling key constant.
    - `operator-claude-plugin/scripts/watch.py` — the default bound constant
      `resolve_bound_seconds` falls back to.
    - `73-REVIEW.md` finding WR-06.
    - `74-CONTEXT.md` D-74-13, and `74-RESEARCH.md` section "D-74-13 — the recovery-bound
      question, current numbers" (the only timing evidence that exists is stress attempt 3's
      aggregate 17-of-48-unchecked figure; no per-chunk trace exists to research further).
    - `.planning/todos/pending/2026-09-17-stage-d-match-chunk-unchecked-rate.md` — its trigger
      line, which must NOT be closed by this plan.
    - **Where the unchecked count lives, checked at plan time:** the `unchecked` bucket is
      produced in `preingest.py` and carried by `match_state.py`, `held_queue.py` and
      `run_manifest.py`. It appears in NO renderer (`run_report.py`, `report.py`,
      `render_text.py`, `render_dashboard.py` all lack it) and in `chunking.py` only inside a
      docstring. Read `run_manifest.py`'s `unchecked` handling FIRST: if the manifest already
      carries the count, D-74-13's surfacing half is a no-op and this task shrinks to WR-06 plus
      a SUMMARY sentence saying so.
  </read_first>
  <behavior>
    - `dispatch_and_recover` returns the excluded-marker count in its result dict instead of
      discarding it.
    - A caller reading that dict sees the count only when it is non-zero, so the usual zero case
      adds nothing to what an operator reads.
    - The recovery bound resolves from the operator-facing config override when present and
      from the module default when absent; the resolution is pinned by a test at both ends.
    - The unchecked count for a match pass is readable from the same returned dict (or already
      readable from the run manifest, in which case nothing is added).
  </behavior>
  <action>
    Stop discarding the second element of the tuple that `dispatch_and_recover` currently
    unpacks, and add it to the returned dict beside the existing failures key (WR-06), emitting
    the key only when the count is non-zero so the usual zero case is unchanged for every
    existing caller. For D-74-13: read
    `resolve_bound_seconds` against the default bound in `watch.py` and against stress attempt
    3's recorded outcome (48 rows, 17 unchecked, a match chunk that did not settle inside the
    bound); confirm whether the config override key is already honoured end to end, and if it is
    only half-wired, finish wiring it and raise the default only if the evidence supports a
    specific number — otherwise leave the default and expose the override, saying so in the
    SUMMARY. Add a test pinning the override-present and override-absent resolutions. For the
    unchecked count, surface it through `dispatch_and_recover`'s OWN returned dict — the same
    channel the excluded-marker count uses above — and through `run_manifest.py` only if the
    manifest does not already carry it. Do NOT add the count to any renderer module
    (`run_report.py`, `report.py`, `render_text.py`, `render_dashboard.py`): none of those files
    belongs to any plan in this wave and touching one would create an undeclared conflict. If
    the count genuinely cannot be surfaced without a renderer edit, stop and raise it in the
    SUMMARY rather than reaching outside this plan's declared files. Do NOT close
    `.planning/todos/pending/2026-09-17-stage-d-match-chunk-unchecked-rate.md` — its trigger is
    the next live Stage D run and no such run happens this phase; record that in the SUMMARY
    per CLAUDE.md section 31 rule 2.
  </action>
  <acceptance_criteria>
    - Behavior: `dispatch_and_recover`'s returned dict carries the excluded-marker count key.
    - Behavior: a result whose excluded-marker count is zero carries no such key; a result
      with a non-zero count carries it.
    - Source: no renderer module (`run_report.py`, `report.py`, `render_text.py`,
      `render_dashboard.py`) appears in this plan's diff.
    - Behavior: with the override config key set, the resolved bound equals the override; with
      it absent, it equals the module default.
    - Source: the pending Stage D todo file still exists under `.planning/todos/pending/` after
      this task, with its trigger line unchanged.
    - Test: the new cases in `test_chunking.py` were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_chunking.py operator-claude-plugin/tests/test_run_manifest.py operator-claude-plugin/tests/test_run_report.py && test -f .planning/todos/pending/2026-09-17-stage-d-match-chunk-unchecked-rate.md</automated>
    <fails_when>pytest exits non-zero, or the `test -f` returns 1 because the Stage D todo was moved or deleted (its trigger has not fired).</fails_when>
  </verify>
  <done>The excluded-marker count is returned and conditionally reported, the recovery bound's resolution is pinned at both ends, the unchecked count is surfaced, and the Stage D todo stays open with its trigger intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → operator | the grant preview is the text an operator consents against; an understated cost is a consent defect, not a cosmetic one |
| n8n execution result → durable ledger | `report_enrichment` and `chunking` write the record the operator later audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-02-01 | Repudiation | `write_grant.py` grant preview omitting the execution count on a config gap | medium | mitigate | WR-01: hoist the lane-invariant assignment out of the raising `try` (Task 1) |
| T-74-02-02 | Spoofing (of provenance) | `write_grant.py` reporting another lane's basis/providers | medium | mitigate | WR-02: per-lane basis and providers from the estimate (Task 1) |
| T-74-02-03 | Tampering | `report_enrichment.py` last-lane-wins ledger overwrite | high | mitigate | WR-05: key by (lane, id); skip ambiguous ids rather than merging (Task 2) |
| T-74-02-04 | Repudiation | a silently dropped excluded-marker count hiding rows from the audit | medium | mitigate | WR-06: return and conditionally report the count (Task 3) |
| T-74-02-05 | Denial of Service (of the report channel) | a match chunk that does not settle inside a fixed bound, leaving rows unchecked | medium | mitigate | D-74-13: expose the bound as an operator-facing override and surface the unchecked count (Task 3) |
| T-74-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is in this plan's scope; no dependency is added |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/`
  green.
- Full Python suite green (baseline 5132 passed / 160 skipped).
- No `n8n/wf_*.json` diff from this plan.
- The Stage D todo remains pending with its trigger unchanged.
</verification>

<success_criteria>
- WR-01, WR-02, WR-05, WR-06 each closed with a test that was seen red first.
- D-74-13's read-and-parametrise half done; its todo trigger left open.
- No graph regeneration, no deploy, zero n8n executions.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Note |
|---|---|---|
| per-lane basis/providers in `write_grant.py` | behaviour change | WR-02 |
| hoisted execution-count assignment in `write_grant.py` | behaviour change | WR-01 |
| `(lane, id)`-keyed ledger in `report_enrichment.py` | behaviour change | WR-05; ambiguous ids skipped |
| excluded-marker count in `dispatch_and_recover`'s result | new returned key | WR-06 |
| configurable recovery bound + unchecked count in the result dict | behaviour change | D-74-13; renderer modules deliberately untouched |
| new cases in `test_write_grant.py`, `test_report_enrichment.py`, `test_chunking.py` | tests | one per finding, D-74-10 |

<output>
Create `.planning/phases/74-code-review-follow-ups-from-phase-73/74-02-SUMMARY.md` when done.
State explicitly whether the recovery-bound default was changed or only exposed, and that the
Stage D todo stays open with its trigger.
</output>
