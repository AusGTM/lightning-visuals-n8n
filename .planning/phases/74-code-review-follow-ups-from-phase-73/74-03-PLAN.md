---
phase: 74-code-review-follow-ups-from-phase-73
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/csv_dedupe.py
  - operator-claude-plugin/scripts/preview.py
  - operator-claude-plugin/scripts/review_decision.py
  - operator-claude-plugin/tests/test_csv_dedupe.py
  - operator-claude-plugin/tests/test_preview_rendering.py
  - operator-claude-plugin/tests/test_review_decision.py
autonomous: true
requirements: [WR-03, WR-04, WR-09, WR-11, WR-12, D-74-10]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-11: a CSV row shorter than the header is padded, not truncated, so its trailing columns survive dedupe as empty values."
    - "WR-12: the dedupe output and report paths are derived from the input's full path, so an input outside the working directory does not write its outputs somewhere else."
    - "WR-03: the dedupe CLI resolves the column mapping through the one canonical config-gate reader preview.py already uses, not a second independent resolver."
    - "WR-04: a collapsed-block file that fails to parse raises an operator-visible error instead of silently becoming an absent block."
    - "WR-09: the review-intent comparison distinguishes an absent key from a key present with a different value."
    - "D-74-10: each of the five findings in this plan lands with its own test; no accept-with-reason rows."
  artifacts:
    - operator-claude-plugin/scripts/csv_dedupe.py
    - operator-claude-plugin/scripts/preview.py
    - operator-claude-plugin/scripts/review_decision.py
  key_links:
    - "csv_dedupe's row canonicalisation -> the deduped CSV the operator then uploads -> every downstream identity join."
    - "preview.py's collapsed-block read -> the preview an operator consents against."
    - "review_decision's leg-1 comparison -> whether a held record's approval is applied or refused."
---

<objective>
Close the five remaining operator-plugin warnings: WR-11, WR-12 and WR-03 in `csv_dedupe.py`,
WR-04 in `preview.py`, WR-09 in `review_decision.py`. Each lands with its own test (D-74-10).

Purpose: three of these five silently produce a wrong answer rather than an error — a truncated
row, an output written beside the wrong file, and a parse failure collapsing to "no block". The
other two make an operator-facing decision on an incomplete comparison.

Output: five fixed functions, five tests, no n8n graph change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-CONTEXT.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-RESEARCH.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-PATTERNS.md
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-REVIEW.md
@operator-claude-plugin/scripts/csv_dedupe.py
@operator-claude-plugin/scripts/preview.py
@operator-claude-plugin/scripts/review_decision.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A short CSV row survives dedupe end to end (WR-11, WR-12)</name>
  <files>operator-claude-plugin/scripts/csv_dedupe.py, operator-claude-plugin/tests/test_csv_dedupe.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/csv_dedupe.py` — grep for the row-canonicalisation helper
      and for `apply_dedupe`; the review's line numbers are stale, the symbols are not.
    - `73-REVIEW.md` findings WR-11 and WR-12, with their proposed fixes.
    - `operator-claude-plugin/tests/test_csv_dedupe.py` — the existing round-trip cases, for the
      shape the new ones should follow.
    - `74-PATTERNS.md` section for `csv_dedupe.py` — the padding idiom and the full-path
      construction rule.
  </read_first>
  <behavior>
    - A row with fewer fields than the header keeps every header column, with the missing
      trailing columns as empty strings, rather than losing them to a zip that stops at the
      shorter sequence.
    - A row with MORE fields than the header is handled by the same rule without raising, and
      the overflow is not silently folded into the last column.
    - Running dedupe on an input file given by an absolute path outside the working directory
      writes its output and report beside that input, not beside a same-stem path in the working
      directory.
    - An input whose stem collides with an unrelated file elsewhere does not overwrite it.
  </behavior>
  <action>
    Replace the zip-based pairing in the row-canonicalisation helper with an index walk over the
    header that reads each row position when present and substitutes an empty string when it is
    not (WR-11) — the exact fix `73-REVIEW.md` supplies. Construct the dedupe output path and
    the report path from the input's full resolved path rather than from its stem alone (WR-12),
    so both land beside the input. Add cases in
    `operator-claude-plugin/tests/test_csv_dedupe.py`: a short row round-tripping with its
    trailing columns intact, and a dedupe run over an input in a temporary directory asserting
    both written paths sit in that directory.
  </action>
  <acceptance_criteria>
    - Behavior: canonicalising a row one field shorter than the header returns a mapping with
      every header key present, the last one holding an empty string.
    - Behavior: running dedupe against an input inside a pytest temporary directory produces an
      output file and a report file whose parents both equal that directory.
    - Test: both new cases in `test_csv_dedupe.py` were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_csv_dedupe.py</automated>
    <fails_when>pytest exits non-zero, or `test_csv_dedupe.py` collects no more tests than it did before the change.</fails_when>
  </verify>
  <done>A short row keeps its columns through dedupe, both output paths derive from the input's full path, and two tests pin it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One canonical column-mapping reader; a parse failure is an error (WR-03, WR-04)</name>
  <files>operator-claude-plugin/scripts/csv_dedupe.py, operator-claude-plugin/scripts/preview.py, operator-claude-plugin/tests/test_csv_dedupe.py, operator-claude-plugin/tests/test_preview_rendering.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/preview.py` — grep for `config_gate` and for the collapsed
      block argument handling near its entry point; read the existing try/except shape verbatim
      before editing it.
    - `operator-claude-plugin/scripts/csv_dedupe.py` — its entry point and the two calls that
      currently pass no mapping path.
    - `73-REVIEW.md` findings WR-03 and WR-04.
    - `74-PATTERNS.md` — "Config-gate resolution, one canonical reader": mirror `preview.py`,
      do not build a third resolver.
  </read_first>
  <behavior>
    - Running the dedupe CLI with a column mapping configured resolves and uses it, matching what
      the preview CLI resolves for the same configuration.
    - Running it with no mapping configured behaves exactly as today.
    - A collapsed-block file containing invalid JSON causes a visible, named failure rather than
      an absent block.
    - A collapsed-block file that is simply absent still yields the absent-block path, unchanged.
  </behavior>
  <action>
    In the dedupe CLI entry point, resolve the column mapping path through the same config-gate
    reader `preview.py` already calls, and pass it into both the propose and apply calls (WR-03).
    Import or call the existing reader; do not add a second resolver for the same config key —
    that duplication is the drift WR-03 itself flags. In `preview.py`, narrow the broad exception
    around the collapsed-block read so a genuine parse failure surfaces with the offending path
    named, while a missing file keeps its current absent-block behaviour (WR-04). Add a case in
    `test_csv_dedupe.py` asserting the CLI resolves the configured mapping, and a case in
    `operator-claude-plugin/tests/test_preview_rendering.py` asserting an invalid collapsed-block
    file produces a visible failure naming the path while an absent file does not.
  </action>
  <acceptance_criteria>
    - Source: `/usr/bin/grep -v "^#" operator-claude-plugin/scripts/csv_dedupe.py | /usr/bin/grep -c "config_gate"`
      returns 1 or more.
    - Behavior: with a column mapping configured, the dedupe CLI resolves the same path the
      preview CLI resolves for the same configuration.
    - Behavior: invalid JSON in a collapsed-block file raises a failure that names the path;
      an absent collapsed-block file yields the absent-block result with no failure.
    - Test: both new cases were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_csv_dedupe.py operator-claude-plugin/tests/test_preview_rendering.py operator-claude-plugin/tests/test_preview_enrichment.py operator-claude-plugin/tests/test_preview_empty_input.py</automated>
    <fails_when>pytest exits non-zero, or the source grep above returns 0 (the canonical reader was not wired into the dedupe CLI).</fails_when>
  </verify>
  <done>The dedupe CLI resolves its column mapping through the one canonical reader, a collapsed-block parse failure is visible, and both are pinned by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Absence is not a value in the review-intent comparison (WR-09)</name>
  <files>operator-claude-plugin/scripts/review_decision.py, operator-claude-plugin/tests/test_review_decision.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/review_decision.py` — grep for the HubSpot-text
      normalisation helper and for the leg-1 key comparison; confirm the block is the one
      `73-REVIEW.md` describes and was not altered by the unrelated boolean-stringify commit of
      2026-09-18.
    - `73-REVIEW.md` finding WR-09 and its proposed fix.
    - `operator-claude-plugin/tests/test_review_decision.py` — the existing leg-1 cases.
  </read_first>
  <behavior>
    - A key present in the intended payload and absent from the would-write payload is reported
      as mismatched, not as agreeing-because-both-normalise-to-empty.
    - A key present in both with the same normalised value is not reported as mismatched.
    - A key present in both with different values is reported as mismatched exactly as today.
    - A key absent from both is not reported at all.
  </behavior>
  <action>
    Change the leg-1 comparison so presence is tested before value equality: a key missing from
    either mapping is a mismatch rather than a normalised-empty match, and the mismatch reason
    distinguishes absence from a differing value so the operator-facing text names which case
    fired. Keep the existing normalisation helper as the value comparator for the
    both-present case. Add cases in `operator-claude-plugin/tests/test_review_decision.py` for
    each of the four behaviors above.
  </action>
  <acceptance_criteria>
    - Behavior: a key present only in the intended payload appears in the mismatch list with an
      absence reason.
    - Behavior: a key present in both with equal normalised values does not appear.
    - Behavior: a key absent from both does not appear.
    - Test: the new cases in `test_review_decision.py` were seen RED before the fix.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_review_decision.py</automated>
    <fails_when>pytest exits non-zero, or `test_review_decision.py` collects no more tests than before the change.</fails_when>
  </verify>
  <done>The leg-1 comparison distinguishes absence from a differing value, the reason names which fired, and four cases pin it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator spreadsheet → plugin | untrusted, ragged CSV from a human-maintained file crosses here |
| plugin → local filesystem | dedupe writes derived files; a wrong path is an overwrite of something it does not own |
| held record → HubSpot write | `review_decision`'s comparison decides whether an approval is applied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-03-01 | Tampering | `csv_dedupe.py` row canonicalisation truncating a short row | high | mitigate | WR-11: index walk over the header with empty-string padding (Task 1) |
| T-74-03-02 | Tampering | `csv_dedupe.py` writing outputs beside a same-stem path it does not own | medium | mitigate | WR-12: derive both paths from the input's full resolved path (Task 1) |
| T-74-03-03 | Spoofing (of configuration) | a second, drifting column-mapping resolver in the dedupe CLI | medium | mitigate | WR-03: reuse the one canonical config-gate reader (Task 2) |
| T-74-03-04 | Repudiation | `preview.py` swallowing a collapsed-block parse failure into an absent block | medium | mitigate | WR-04: narrow the exception; name the offending path (Task 2) |
| T-74-03-05 | Elevation of Privilege (of an unintended write) | `review_decision.py` treating an absent key as agreeing, letting an unintended field through | high | mitigate | WR-09: presence tested before value equality; reason names which case fired (Task 3) |
| T-74-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is in this plan's scope; no dependency is added |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/`
  green.
- Full Python suite green (baseline 5132 passed / 160 skipped).
- No `n8n/wf_*.json` diff from this plan.
</verification>

<success_criteria>
- WR-03, WR-04, WR-09, WR-11, WR-12 each closed with a test that was seen red first.
- No second config-gate resolver introduced anywhere.
- Zero n8n executions, nothing deployed.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Note |
|---|---|---|
| padded row canonicalisation in `csv_dedupe.py` | behaviour change | WR-11 |
| full-path output/report construction in `csv_dedupe.py` | behaviour change | WR-12 |
| config-gate column-mapping resolution in the dedupe CLI | behaviour change | WR-03; reuses `preview.py`'s reader |
| narrowed collapsed-block exception in `preview.py` | behaviour change | WR-04 |
| presence-aware leg-1 comparison in `review_decision.py` | behaviour change | WR-09 |
| new cases in `test_csv_dedupe.py`, `test_preview_rendering.py`, `test_review_decision.py` | tests | one per finding, D-74-10 |

<output>
Create `.planning/phases/74-code-review-follow-ups-from-phase-73/74-03-SUMMARY.md` when done.
</output>
