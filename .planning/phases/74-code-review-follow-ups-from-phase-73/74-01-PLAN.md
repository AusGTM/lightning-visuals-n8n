---
phase: 74-code-review-follow-ups-from-phase-73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/freeze_execution_rundata.py
  - tests/n8n/frozenFixtureSecrets.test.mjs
  - tests/n8n/fixtures/frozen/README.md
  - tests/n8n/fixtures/frozen/exec_12434.runData.json
  - tests/n8n/fixtures/frozen/exec_12449.runData.json
  - tests/n8n/fixtures/frozen/exec_12354.runData.json
  - tests/n8n/fixtures/frozen/exec_12355.runData.json
  - tests/n8n/fixtures/frozen/exec_12356.runData.json
  - tests/n8n/fixtures/frozen/exec_12357.runData.json
  - tests/n8n/fixtures/frozen/exec_12358.runData.json
  - tests/n8n/fixtures/frozen/exec_12522.runData.json
autonomous: true
requirements: [D-74-07, D-74-08, D-74-09, D-74-03]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-74-07: the freezer replaces any headers/error/request/options/config key at any depth of a node run, including run.error, not only run.data.main[*][*].json.headers."
    - "D-74-08: none of the 7 committed runData fixtures carries an unredacted JWT body or an unredacted shared-secret value after this plan."
    - "D-74-09: a guard test over tests/n8n/fixtures/frozen/ refuses secret VALUE shapes and does not trip on the header NAME appearing inside a frozen workflow body's jsCode."
    - "D-74-03 (fixture half): tests/n8n/fixtures/frozen/exec_12522.runData.json exists, produced by the widened freezer, and no raw unscrubbed copy of it is committed."
  artifacts:
    - scripts/freeze_execution_rundata.py
    - tests/n8n/frozenFixtureSecrets.test.mjs
    - tests/n8n/fixtures/frozen/exec_12522.runData.json
    - tests/n8n/fixtures/frozen/README.md
  key_links:
    - "freeze_execution_rundata.py's single scrub chokepoint -> build_full_fixture and build_excerpt (both callers) -> every fixture byte that reaches git."
    - "the widened scrub -> exec_12434/exec_12449 -> operator-claude-plugin/tests/test_run_report_enrich_account.py's reconciliation assertions (must stay green)."
    - "exec_12522.runData.json -> 74-05's fidelity test and MN-01 search (this plan is their blocking dependency)."
---

<objective>
Close CR-04 by widening the freeze tool's redaction to a non-enumerating key scrub over the
whole node run (D-74-07), re-redacting the 7 committed runData fixtures in place (D-74-08),
adding a value-shape guard test over the frozen directory (D-74-09), then freezing execution
`12522` through the widened tool so 74-05 has its fidelity fixture (D-74-03, fixture half).

Purpose: the current scrubber only reaches `run["data"]["main"][branch][item]["json"]["headers"]`.
Two committed fixtures carry live-shaped ZoomInfo OAuth JWT bodies right now, and the tool that
produced them would produce more. This plan widens the chokepoint so the next freeze is safe by
construction, then proves it on the existing bytes.

Output: a widened scrub, 7 rewritten fixtures, a new guard test, a README rule line, and the
`exec_12522.runData.json` fixture 74-05 depends on.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-CONTEXT.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-RESEARCH.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-PATTERNS.md
@scripts/freeze_execution_rundata.py
@tests/n8n/fixtures/frozen/README.md
@tests/n8n/v1RuntimeRecordings.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Widen the freezer's scrub end to end, proven on one leaking fixture</name>
  <files>scripts/freeze_execution_rundata.py, tests/n8n/frozenFixtureSecrets.test.mjs, tests/n8n/fixtures/frozen/exec_12434.runData.json</files>
  <read_first>
    - `scripts/freeze_execution_rundata.py` in full: the `_redact_headers` function, the
      `REDACTED_PLACEHOLDER` constant, and both callers `build_full_fixture` / `build_excerpt`.
    - `74-CONTEXT.md` section "Fixture redaction guard", decisions D-74-07 and D-74-09.
    - `74-RESEARCH.md` section "CR-04 / D-74-07 / D-74-08 / D-74-09 — the freezer's scrub scope",
      which quotes the current narrow body verbatim.
    - `tests/n8n/v1RuntimeRecordings.test.mjs` lines 22-31 — the existing value-shape refusal
      test, the precedent this plan's guard generalizes rather than duplicates.
  </read_first>
  <behavior>
    - A node run whose `error` key sits as a sibling of `data` (not inside `data.main`) comes
      back with that value replaced by the placeholder.
    - An item carrying a bearer value at `json.error.request.headers.Authorization` has the
      whole `error` value replaced at the first matching key, so nothing below it survives.
    - An `options` or `config` key at any depth is replaced.
    - A dict with no sensitive key round-trips byte-identical.
    - A non-dict node-run entry (list, string, None) is returned unchanged and does not raise.
    - The caller's input dict is never mutated (the deep-copy contract the current function
      already keeps).
  </behavior>
  <action>
    Replace `_redact_headers` in `scripts/freeze_execution_rundata.py` with a recursive
    `_scrub` that walks every dict and list at every depth of an entire node-run entry and
    replaces the value of any key named in a new module-level `_SENSITIVE_KEYS` tuple holding
    exactly the five names D-74-07 specifies, using the existing `REDACTED_PLACEHOLDER`. Apply
    it per node run to the whole `run` dict, not only to its `data.main` subtree — that
    widening is the point of D-74-07 and is what reaches the node-run-level error object. Keep
    the deep-copy-first contract and the dict return type so both existing callers need no
    change beyond the renamed symbol. Replace on first match and do not recurse into the
    replaced subtree. Extend the function docstring to state the new scope and to carry
    forward the module's existing argument for wholesale replacement over enumeration. Then
    re-run the widened tool over the already-committed
    `tests/n8n/fixtures/frozen/exec_12434.runData.json` in place, through the freezer's own
    re-scrub path — never a hand edit of the JSON — and commit the rewritten bytes. Add
    `tests/n8n/frozenFixtureSecrets.test.mjs`, scoped in this task to that one fixture: it
    reads the raw file text and refuses four value shapes named in D-74-09 — a HubSpot
    private-app token prefix followed by a value, a bearer scheme followed by a
    non-placeholder value, the webhook shared-secret header key followed by any value other
    than the placeholder string, and a JWT body. Per D-74-09 the guard refuses VALUES, never
    key names. Add unit tests for the behaviors above in whichever Python test file already
    covers this script; grep for the script name under `operator-claude-plugin/tests/` and
    `tests/` first and create one beside the nearest sibling only if none exists.
  </action>
  <acceptance_criteria>
    - Source: `/usr/bin/grep -c '_SENSITIVE_KEYS' scripts/freeze_execution_rundata.py` returns
      2 or more (the definition and at least one use).
    - Source: `/usr/bin/grep -c 'def _scrub' scripts/freeze_execution_rundata.py` returns 1.
    - Behavior: a node-run dict whose top level carries both an error object and a data object
      comes back with the error value equal to the placeholder constant.
    - Test: `node --test tests/n8n/frozenFixtureSecrets.test.mjs` passes with
      `exec_12434.runData.json` in scope.
    - Test: the guard was seen RED against the pre-rewrite bytes before the rewrite; record the
      red run's assertion message in the commit body.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider -k freeze_execution && node --test tests/n8n/frozenFixtureSecrets.test.mjs</automated>
    <fails_when>pytest exits non-zero, or reports `no tests ran` (the scrub unit tests must exist and be collected), or `node --test` prints a `# fail` count of 1 or higher.</fails_when>
  </verify>
  <done>The scrub replaces the five key names at any depth of a whole node run, `exec_12434.runData.json` carries no JWT-shaped value, and the guard test is green on that one file after having been red on the pre-rewrite bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-redact the remaining 6 fixtures, widen the guard to the directory, document the rule</name>
  <files>tests/n8n/frozenFixtureSecrets.test.mjs, tests/n8n/fixtures/frozen/README.md, tests/n8n/fixtures/frozen/exec_12449.runData.json, tests/n8n/fixtures/frozen/exec_12354.runData.json, tests/n8n/fixtures/frozen/exec_12355.runData.json, tests/n8n/fixtures/frozen/exec_12356.runData.json, tests/n8n/fixtures/frozen/exec_12357.runData.json, tests/n8n/fixtures/frozen/exec_12358.runData.json</files>
  <read_first>
    - `tests/n8n/fixtures/frozen/README.md`, the Redaction rule section (currently documents the
      narrow header-only rule).
    - `74-RESEARCH.md` section "The 7 fixtures D-74-08 re-redacts" and Pitfall 4 (the
      `test_run_report_enrich_account.py` collision risk).
    - `operator-claude-plugin/tests/test_run_report_enrich_account.py` — which node outputs it
      actually asserts on.
    - `tests/n8n/walkerEngineFidelity.test.mjs` and `tests/n8n/v1RuntimeRecordings.test.mjs` —
      the other readers of these fixtures.
  </read_first>
  <action>
    Re-run the widened freezer over the remaining 6 committed runData fixtures in place
    (`exec_12449`, `exec_12354`, `exec_12355`, `exec_12356`, `exec_12357`, `exec_12358`), plus
    `run_6891d018-decide-and-response.excerpt.json` if it is produced by the same tool, and
    commit the rewritten bytes. No path exemptions — that is D-74-08's explicit instruction.
    Immediately afterwards run `operator-claude-plugin/tests/test_run_report_enrich_account.py`:
    this is Pitfall 4, because the widened scrub touches far more of `exec_12434` and
    `exec_12449` than the JWTs alone, and a collision with that test's assertions must surface
    here rather than in a later wave. If it does collide, do not add a path exemption — name the
    key the test needs and raise it as a blocker in the SUMMARY. Widen
    `tests/n8n/frozenFixtureSecrets.test.mjs` from the single file to every JSON file under
    `tests/n8n/fixtures/frozen/`, and have the test report its own in-scope file count so the
    four frozen workflow-body fixtures are provably covered — those carry the shared-secret
    header NAME inside node jsCode and must pass, since the guard refuses values only. Add a
    line to the README's Redaction rule section naming the widened scope (the five key names,
    at any depth, applied per node run) and stating that error fixtures lose their message text
    by design.
  </action>
  <acceptance_criteria>
    - Behavior: a recursive text search for a JWT body prefix across every `.runData.json` file
      under `tests/n8n/fixtures/frozen/` returns no file.
    - Behavior: every remaining occurrence of the shared-secret header key inside a
      `.runData.json` file is followed by the placeholder string and nothing else.
    - Test: `node --test tests/n8n/frozenFixtureSecrets.test.mjs` is green over the whole
      directory and its reported in-scope file count equals the number of JSON files there.
    - Test: `operator-claude-plugin/tests/test_run_report_enrich_account.py` is green against
      the rewritten bytes.
    - Test: `tests/n8n/v1RuntimeRecordings.test.mjs` and `tests/n8n/walkerEngineFidelity.test.mjs`
      are green against the rewritten bytes.
    - Source: the README's Redaction rule section names all five scrubbed key names.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/frozenFixtureSecrets.test.mjs tests/n8n/v1RuntimeRecordings.test.mjs tests/n8n/walkerEngineFidelity.test.mjs && .venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_run_report_enrich_account.py</automated>
    <fails_when>`node --test` prints a `# fail` count of 1 or higher, or pytest exits non-zero, or the guard test's own reported in-scope file count is lower than the number of JSON files in the frozen directory.</fails_when>
  </verify>
  <done>All 7 runData fixtures are rewritten through the widened freezer, the guard is green over the whole directory including the workflow-body fixtures, the three existing consumer test files stay green, and the README records the widened rule.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze execution 12522 through the widened freezer</name>
  <files>tests/n8n/fixtures/frozen/exec_12522.runData.json</files>
  <read_first>
    - `scripts/freeze_execution_rundata.py`'s CLI entry point and its own dotenv load.
    - `74-RESEARCH.md` section "D-74-03's frozen-fixture target does not exist yet" and
      Pitfall 3 (the credential channel).
    - `74-CONTEXT.md` D-74-01's recorded per-node run and item counts for execution `12522` —
      the numbers the frozen fixture must agree with.
    - `.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md` lines 116-131 — the
      execution's provenance (run id, 46 rows sent, 21 created).
  </read_first>
  <precondition>`N8N_URL` and `N8N_API_KEY` are present in the repo's `.env`; the freeze is a read-only GET against the n8n executions API and consumes zero n8n executions.</precondition>
  <action>
    Freeze execution `12522` into `tests/n8n/fixtures/frozen/exec_12522.runData.json` by
    invoking `scripts/freeze_execution_rundata.py` with that execution id through an in-process
    dotenv driver written into the session scratchpad and never committed to the repo — the
    pattern recorded in `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md`
    lines 203-219: a scratchpad Python file that calls python-dotenv's loader against the
    resolved `.env` path in its own process, then dispatches to the script's entry point. Do not
    read, print or echo `.env`, and do not source it from a shell. The freezer used must be the
    D-74-07-widened one from Task 1 — freezing through the old narrow scrubber and re-scrubbing
    afterwards is not acceptable, because the raw bytes would have touched disk inside the repo.
    Do not copy the raw unscrubbed runData that already sits in the session scratchpad into the
    repo under any name. After the freeze, check the fixture against `74-CONTEXT.md`'s recorded
    counts: `HubSpot Create` output item counts 21 and 0, a single `Create Carry Merge` run
    emitting 42, one `Build Association Request Merge` run of 22, `HubSpot Associate Company` of
    22, one `Ingest Merge Response` run with all 6 inputs, and `Build Ingest Response` of 46.
    Print node names, run counts and item counts only — never raw items. Re-run the D-74-09
    guard over the directory with the new file in it.
  </action>
  <acceptance_criteria>
    - Artifact: `tests/n8n/fixtures/frozen/exec_12522.runData.json` exists and parses as JSON.
    - Behavior: the fixture's `HubSpot Create` entry shows two output branches with item counts
      21 and 0 respectively.
    - Behavior: the fixture's `Create Carry Merge` entry is a single node run emitting 42 items.
    - Behavior: the fixture's `Build Ingest Response` entry emits 46 items.
    - Test: `node --test tests/n8n/frozenFixtureSecrets.test.mjs` is green with the new file in
      scope.
    - CLI: the freeze consumed zero n8n executions (read-only GET); state this in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/frozenFixtureSecrets.test.mjs && node --test tests/n8n/walkerEngineFidelityV1.test.mjs</automated>
    <fails_when>the guard file prints a `# fail` count of 1 or higher, or `tests/n8n/fixtures/frozen/exec_12522.runData.json` is absent so the guard reports a lower in-scope file count than the directory holds.</fails_when>
  </verify>
  <done>`exec_12522.runData.json` is committed, matches every per-node count recorded in D-74-01, carries no secret value shape, and cost zero n8n executions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n Cloud executions API → local disk | runData fetched over the network is written into a git-tracked directory; anything not scrubbed becomes a committed secret |
| git-tracked fixture → public history | once committed, a leaked value cannot be recalled by rewriting the working tree |
| `.env` → process environment | credentials are loaded in-process only; never echoed, never sourced from a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01-01 | Information Disclosure | `scripts/freeze_execution_rundata.py` narrow scrub | high | mitigate | D-74-07: non-enumerating `_scrub` over the whole node run, five key names at any depth (Task 1) |
| T-74-01-02 | Information Disclosure | committed ZoomInfo OAuth JWT bodies in `exec_12434`/`exec_12449` | high | mitigate | D-74-08: re-redact in place through the widened freezer and commit the rewritten bytes (Tasks 1-2). Git history is not rewritten — accepted per D-74-08, tokens have a 24h lifetime |
| T-74-01-03 | Information Disclosure | a future freeze reintroducing the gap | medium | mitigate | D-74-09 guard test over the whole frozen directory, refusing four value shapes (Tasks 1-2) |
| T-74-01-04 | Information Disclosure | the raw unscrubbed `12522` runData in the session scratchpad | high | mitigate | Task 3 forbids copying it into the repo; the only path into git is the widened freezer |
| T-74-01-05 | Information Disclosure | credentials echoed to the transcript while freezing | medium | mitigate | in-process dotenv driver, never `source .env`, never printing env values (Task 3) |
| T-74-01-06 | Tampering | over-broad scrub blanking a field a consumer test asserts on | medium | mitigate | Pitfall 4 check: run `test_run_report_enrich_account.py` immediately after re-redaction; no path exemptions permitted (Task 2) |
| T-74-74-SC | Tampering | npm/pip/cargo installs | low | accept | No package install of any kind is in this plan's scope; no dependency is added |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green (baseline 1303 pass / 0 fail).
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` green (baseline 5132 passed /
  160 skipped).
- No `.runData.json` under `tests/n8n/fixtures/frozen/` carries a JWT body or a non-placeholder
  shared-secret value.
- `tests/n8n/fixtures/frozen/exec_12522.runData.json` exists and agrees with D-74-01's counts.
- Zero n8n executions consumed by this plan.
</verification>

<success_criteria>
- D-74-07, D-74-08, D-74-09 closed with a test each.
- D-74-03's fixture half delivered, unblocking 74-05.
- Both suites green; nothing deployed, nothing armed, zero executions spent.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Note |
|---|---|---|
| `_scrub` + `_SENSITIVE_KEYS` in `scripts/freeze_execution_rundata.py` | helper + constant | replaces `_redact_headers`; both existing callers unchanged |
| `tests/n8n/frozenFixtureSecrets.test.mjs` | new guard test | D-74-09; refuses four value shapes over the whole frozen directory |
| `tests/n8n/fixtures/frozen/exec_12522.runData.json` | new fixture | D-74-03; blocking dependency of 74-05 |
| 7 rewritten `.runData.json` fixtures | data | D-74-08; re-redacted in place, no path exemptions |
| `tests/n8n/fixtures/frozen/README.md` Redaction rule line | doc | records the widened scope |

<output>
Create `.planning/phases/74-code-review-follow-ups-from-phase-73/74-01-SUMMARY.md` when done.
State explicitly: zero n8n executions consumed; whether the widened scrub collided with any
consumer test; and that the scratchpad raw runData was not committed.
</output>
