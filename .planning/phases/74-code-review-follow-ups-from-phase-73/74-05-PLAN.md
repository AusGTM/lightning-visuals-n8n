---
phase: 74-code-review-follow-ups-from-phase-73
plan: 05
type: execute
wave: 3
depends_on: ["74-04"]
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/wf_contact_ingest_cloud.json
  - tests/n8n/mergeInputContract.test.mjs
  - n8n/code/pairCreateOutcome.js
  - operator-claude-plugin/scripts/written_records.py
  - tests/n8n/pairCreateOutcome.test.mjs
  - tests/n8n/ingestCreateErrorLane.test.mjs
  - tests/n8n/ingestMixedBatch.test.mjs
  - operator-claude-plugin/tests/test_written_records.py
  - CLAUDE.md
autonomous: true
requirements: [D-74-01, D-74-02, D-74-04, D-74-05, D-74-06, WR-10, D-74-10]
user_setup: []

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-74-04: an item arriving from the create node's error edge carries an explicit stamp, and pairCreateOutcome tests that stamp before any shape test, so classification no longer depends on a guessed error-item shape."
    - "D-74-05: the hand-written error-item stub is kept and tagged UNOBSERVED in the register walkWorkflow.mjs already uses, and a test proves classification survives stripping the invented field."
    - "D-74-06: a create with no joined response reaches the operator as create_unconfirmed, mapped to FAILED, with its reason passed through verbatim so the two sub-cases stay distinguishable."
    - "D-74-01 (ruled B'): Create Carry Merge keeps its three inputs and the create node keeps alwaysOutputData; only the stamp node is added on the error edge; the zero-rejection drain is recorded as observed live on 12522."
    - "D-74-02: Ingest Merge Response's create-failure input has ONE gated starved-lane sentinel whose condition is a disjunction (writes not permitted, OR zero create-routed rows), so neither an all-update batch nor a disarmed batch carrying create rows leaves that input drain-only, and the single emission cannot double-fire."
    - "WR-10: the build-time JS constant extractor behaves correctly for the case 73-REVIEW.md names, with a test."
  artifacts:
    - n8n/wf_contact_ingest_cloud.json
    - n8n/code/pairCreateOutcome.js
    - operator-claude-plugin/scripts/written_records.py
  key_links:
    - "the create node's error edge -> the stamp node -> pairCreateOutcome's first classification test -> which rows become create_failed."
    - "Build Create Failure Row -> Ingest Merge Response input for the create-failure lane -> the per-row report the operator reads."
    - "create_unconfirmed -> written_records.ACTION_TO_OUTCOME -> the durable ledger outcome (FAILED, never a fabricated success)."
---

<objective>
Land the create-error lane cluster as ONE splice against ONE regenerated
`wf_contact_ingest_cloud.json`: the explicit error stamp and its stamp-first classification
(D-74-04, D-74-05), the unconfirmed-create outcome reaching the operator (D-74-06), the gated
sentinel on the create-failure input (D-74-02), the ruled-B' comment correction for the
always-output-data reliance (D-74-01), and WR-10's unrelated extractor fix riding the same
regeneration.

Purpose: today a create that is neither confirmed success nor HubSpot rejection is dropped by
both of its only two routes and reaches the operator still carrying its pre-write action. And
the one thing that decides whether an error item is an error item is a guess about its shape.
Both are silent-wrong-answer defects on the write path.

Output: one stamp node, one sentinel, a stamp-first classifier, a `create_unconfirmed` outcome,
and a single regenerated ingest workflow.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-CONTEXT.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-RESEARCH.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-PATTERNS.md
@.planning/phases/74-code-review-follow-ups-from-phase-73/74-04-SUMMARY.md
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-REVIEW.md
@n8n/code/pairCreateOutcome.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: An explicit stamp decides the error branch, end to end (D-74-04, D-74-05)</name>
  <files>scripts/build_cloud_workflows.py, n8n/code/pairCreateOutcome.js, tests/n8n/pairCreateOutcome.test.mjs, tests/n8n/ingestCreateErrorLane.test.mjs</files>
  <read_first>
    - `n8n/code/pairCreateOutcome.js` in full — the module header's shape-only rationale and the
      classification ladder (the carried-row test, the success-response test, and the else arm).
    - `scripts/build_cloud_workflows.py` — grep for the line appending the create node's error
      output to the carry merge and for the always-output-data call beside it; read the whole
      surrounding comment block (it is the one CR-01 quotes).
    - `74-CONTEXT.md` D-74-04, D-74-05 and the ruled D-74-01.
    - `74-RESEARCH.md` Pitfall 5 — the stamp must be applied by a node that KNOWS it is on the
      error branch, never inferred inside the pure function.
    - `tests/n8n/ingestCreateErrorLane.test.mjs` — the hand-written stub, and the
      `tests/n8n/pairCreateOutcome.test.mjs` cases.
    - `tests/n8n/lib/walkWorkflow.mjs`'s existing UNOBSERVED register comment (the one for the
      Phase 70 rule) — the exact wording register D-74-05 asks to reuse.
  </read_first>
  <behavior>
    - An item carrying the stamp is classified as an error outcome even when it also carries a
      non-empty action field (the stamp beats the carried-row shape test).
    - An item carrying the stamp is classified as an error outcome even when it also carries an
      id (the stamp beats the success-response shape test).
    - An item with no stamp classifies exactly as it does today: carried row, then success
      response, then error.
    - With the invented email field stripped from the hand-written error stub, the row still
      classifies as a create failure.
  </behavior>
  <action>
    Add one Code node on the create node's error output in `scripts/build_cloud_workflows.py`,
    between that output and the carry merge input it currently feeds directly, that stamps every
    item passing through it with an explicit boolean error marker and changes nothing else about
    the item. Keep the carry merge's three inputs and keep `alwaysOutputData` on the create node
    — D-74-01 as ruled (B') makes both correct, because that flag pads output 0, which the
    all-rejected case needs. Rewrite the surrounding comment block to say what the engine
    actually does: output 0 is padded, the error-edge input is simply absent on a zero-rejection
    batch, and the merge completes via the single v1 end-of-run drain, observed live on execution
    `12522`. In `n8n/code/pairCreateOutcome.js`, test the stamp FIRST in the classification loop,
    ahead of the carried-row test, and route a stamped item to the error outcome. Update the
    module header to describe the stamp as the primary signal with shape as fallback. Tag the
    hand-written stub in `tests/n8n/ingestCreateErrorLane.test.mjs` with an UNOBSERVED comment in
    the register `walkWorkflow.mjs` already uses, naming the open shape question, citing D-73-19
    and pointing at CLAUDE.md section 13.0.3 (D-74-05). Add cases to
    `tests/n8n/pairCreateOutcome.test.mjs` for the four behaviors above, including the
    stripped-field case that proves correctness rests on the stamp and not on the guess.
  </action>
  <acceptance_criteria>
    - Source: the stamp is set by a builder-authored Code node on the error edge; a grep of
      `n8n/code/pairCreateOutcome.js` shows the stamp is only READ there, never written.
    - Source: in the generated ingest workflow, the create node's output 1 connects to the new
      stamp node, and the stamp node connects to the carry merge input the error edge used to
      feed directly — verified by walking `connections`, not by reading the builder alone.
    - Source: the create node still carries `alwaysOutputData` and the carry merge still declares
      three inputs.
    - Behavior: a stamped item carrying an action field classifies as an error outcome.
    - Behavior: a stamped item carrying an id classifies as an error outcome.
    - Behavior: the stub with its email field removed still yields a create failure.
    - Test: the new cases in `tests/n8n/pairCreateOutcome.test.mjs` were seen RED before the
      classifier change.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && node --test tests/n8n/pairCreateOutcome.test.mjs tests/n8n/ingestCreateErrorLane.test.mjs tests/n8n/ingestCarryMerge.test.mjs</automated>
    <fails_when>`node --test` prints a `# fail` count of 1 or higher, or `tests/n8n/pairCreateOutcome.test.mjs` reports no more `# tests` than before the change.</fails_when>
  </verify>
  <done>Classification of the error branch is decided by an explicit stamp applied where the topology is known, the stub is tagged UNOBSERVED, and a stripped-field test proves the guess is no longer load-bearing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: An unconfirmed create reaches the operator as FAILED (D-74-06)</name>
  <files>scripts/build_cloud_workflows.py, operator-claude-plugin/scripts/written_records.py, tests/n8n/ingestCreateErrorLane.test.mjs, tests/n8n/ingestMixedBatch.test.mjs, operator-claude-plugin/tests/test_written_records.py</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` — the create-failure row constant (its filter and its
      zero-rejection marker branch) and the ingest response constant (the decided-snapshot read,
      the email-keyed failure join, and the action/outcome ternary).
    - `n8n/code/pairCreateOutcome.js` — the three outcomes it can emit besides success, and what
      each item carries.
    - `74-RESEARCH.md` section "CR-03 / D-74-06", especially the CORRECTED trace: the review's
      own fix snippet filters an empty set because neither of the pair node's two consumers lets
      such an item through, so the fix belongs one hop earlier, in the create-failure row
      constant.
    - `operator-claude-plugin/scripts/written_records.py` — the action-to-outcome table and the
      existing create-failure entry, plus the all-outcomes frozenset.
    - `74-CONTEXT.md` D-74-06.
  </read_first>
  <behavior>
    - A row whose create produced no joined response reaches the response as an unconfirmed
      create, carrying a reason naming that no create response joined to it.
    - A row whose create was refused for ambiguity reaches the response as an unconfirmed create,
      carrying the ambiguity reason verbatim, so the two sub-cases stay distinguishable.
    - A row whose create was rejected by HubSpot still reaches the response as a create failure,
      exactly as today.
    - A batch whose only create outcomes are unconfirmed emits those rows rather than the
      zero-rejection marker.
    - The plugin ledger maps the unconfirmed action to the failed outcome, never to any
      success-shaped outcome.
  </behavior>
  <action>
    Widen the create-failure row constant's filter from "outcome equals error" to "outcome is not
    success", so unconfirmed and refused items are admitted instead of being swallowed by the
    association consumer's own drop rule. Stamp the emitted row's action and outcome
    conditionally: the existing create-failure value when the outcome was an error, and a new
    unconfirmed-create value otherwise, passing the pair node's own reason through verbatim and
    falling back to the no-response-joined text when it is absent (D-74-06). Move the
    zero-rejection marker branch's condition from "no error items" to "no non-success items" in
    the same edit — 74-RESEARCH flags this as the interaction that would otherwise emit a marker
    on a batch that now has a real row to produce. In the ingest response constant, join these
    rows by email exactly as the existing failure join already does, and extend the
    action/outcome ternary with the third branch; no filter on the pair node's outcome field is
    needed there, because the row arrives pre-classified. Add the unconfirmed-create key to
    `operator-claude-plugin/scripts/written_records.py`'s action-to-outcome table beside the
    existing create-failure entry, mapping to the existing failed outcome constant — no new
    outcome constant is needed. Add tests: graph-side cases in
    `tests/n8n/ingestCreateErrorLane.test.mjs` and `tests/n8n/ingestMixedBatch.test.mjs` for each
    of the first four behaviors, and a case in
    `operator-claude-plugin/tests/test_written_records.py` for the mapping.
  </action>
  <acceptance_criteria>
    - Behavior: walking the regenerated ingest graph with a create whose response never joined
      yields a response row whose action is the unconfirmed-create value and whose reason names
      that no create response joined.
    - Behavior: the refused sub-case carries its own reason text, different from the
      no-response-joined text.
    - Behavior: a HubSpot-rejected create still yields the create-failure action.
    - Behavior: a batch with only unconfirmed outcomes emits rows and no marker.
    - Behavior: the plugin's action-to-outcome table resolves the unconfirmed action to the
      failed outcome.
    - Source: the unconfirmed outcome value is absent from any success-shaped mapping in
      `written_records.py`.
    - Test: all new cases were seen RED before their fixes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && node --test tests/n8n/ingestCreateErrorLane.test.mjs tests/n8n/ingestMixedBatch.test.mjs tests/n8n/ingestResponseRowId.test.mjs && .venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_written_records.py operator-claude-plugin/tests/test_run_report.py operator-claude-plugin/tests/test_run_manifest.py</automated>
    <fails_when>`node --test` prints a `# fail` count of 1 or higher, or pytest exits non-zero, or `test_written_records.py` collects no more tests than before the change.</fails_when>
  </verify>
  <done>An unconfirmed create is reported to the operator as such, with its sub-case reason intact, and lands in the durable ledger as failed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gated sentinel on the create-failure input, WR-10, one regeneration (D-74-02, WR-10, D-74-01)</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_contact_ingest_cloud.json, tests/n8n/mergeInputContract.test.mjs (registry only, if the new pair requires it), CLAUDE.md</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` — the starved-lane sentinel helper's signature and
      docstring; the association lane sentinel call site, read for its COMPOSITION IDIOM ONLY
      (how the write-safety gate constant is prefixed onto a condition) and NOT as a shape to
      copy, because that sentinel is sourced from the decided-row node and keyed on the UPDATE
      lane — a different predicate from the one this task needs; the merge-input-index helper;
      and the append call whose return value for the create-failure row is currently discarded.
    - `operator-claude-plugin/scripts/n8n_arming.py`'s write-safety setter — confirm it rewrites
      by scanning every node's jsCode for the constant declaration rather than from an
      enumerated node list, because that is what makes composing the gate into a NEW node safe.
    - `74-CONTEXT.md` D-74-02 and `74-RESEARCH.md` section "WR-07 — confirmed empirically",
      which lists every producer on every input of the ingest response merge.
    - `73-REVIEW.md` finding WR-10 and its proposed fix for the build-time constant extractor.
    - `CLAUDE.md` section 13.0.1 and section 13.0.3.
  </read_first>
  <behavior>
    - An all-update batch (zero create-routed rows) delivers a single marker to the
      create-failure input, so the ingest response merge completes with every input delivered.
    - A batch containing create-routed rows with writes NOT permitted also delivers the marker,
      because the gate refuses, the pass-through emits nothing, the carry merge never fires and
      the create-failure row node never runs. This case is asserted by its own walker run, not
      inferred from the all-update case.
    - A batch containing create-routed rows with writes permitted does NOT deliver the marker;
      the real create-failure row producer is the only delivery on that input.
    - No input of the ingest response merge ever receives two deliveries in the same scenario.
  </behavior>
  <action>
    Add exactly ONE starved-lane sentinel node feeding the ingest response merge's create-failure
    input, built through the existing helper. Its condition is a DISJUNCTION evaluated inside
    that single node, with a single marker emission, so it can never fire twice: emit the marker
    when writes are not permitted (the write-safety gate constant, prefixed onto the condition
    using the composition idiom the association lane sentinel demonstrates — that sentinel's own
    predicate is about the update lane and is NOT the shape to copy), OR when the decided row set
    contains zero create-routed rows (D-74-02's literal predicate). The first disjunct is what
    covers the disarmed-batch-with-create-rows case, which D-74-02's bare predicate would leave
    drain-only: in that case the gate refuses, the pass-through emits nothing, the carry merge
    never fires, and the create-failure row node never runs. Composing the gate is safe because
    the arming setter rewrites the constant by scanning every node's jsCode rather than from an
    enumerated node list — confirm that before relying on it, and if it turns out to be
    list-driven, register the new node there or fall back to the bare predicate and record the
    residual in the SUMMARY. Source the sentinel from the pre-write decided-row node the lane's
    three other sentinels already use. Derive the target input index by capturing the return
    value of the append call that currently discards it — never write the index as a literal.
    Write the mutual-exclusion argument into the comment beside the call: the two disjuncts and
    the real producer's own firing condition partition every batch shape, so the input receives
    exactly one delivery in each. If the ingest response merge's create-failure input now has two
    declared producers, check whether `tests/n8n/mergeInputContract.test.mjs`'s
    multi-producer-tolerant registry must name this (workflow, merge) pair — that test asserts
    the registry in BOTH directions, so an unregistered new pair and a stale registered one both
    fail. Separately fix
    WR-10 in the build-time JS constant extractor with the review's own fix and add a test for
    the case it names. Then regenerate once with `scripts/build_cloud_workflows.py` and commit
    `n8n/wf_contact_ingest_cloud.json`; record the node count before and after (98 at the start
    of this phase). Add a CLAUDE.md section 13.0.1 note for the new stamp node and the sentinel,
    and the section 13.0.3 row D-74-01 asks for if 74-04 did not already add it — do not
    duplicate a row that already exists.
  </action>
  <acceptance_criteria>
    - Source: in the generated ingest workflow, the create-failure input of the ingest response
      merge has exactly two producers: the real create-failure row node and the new sentinel.
    - Source: the sentinel's target index is not a literal integer in the builder source.
    - Behavior: walking the regenerated graph with an all-update batch yields no stall entry for
      the ingest response merge and a marker on the create-failure input.
    - Behavior: walking it with a permitted create batch yields exactly one delivery on that
      input, from the real producer.
    - Behavior: walking it with a DISARMED batch that contains create-routed rows yields the
      marker on that input and no stall entry for the ingest response merge.
    - Source: `tests/n8n/mergeInputContract.test.mjs`'s multi-producer-tolerant registry agrees
      with the regenerated graph in both directions (no unregistered new pair, no stale entry).
    - Behavior: the WR-10 case the review names now returns the correct extraction, pinned by a
      test.
    - CLI: `.venv/bin/python scripts/build_cloud_workflows.py` then `git diff --quiet -- n8n/`
      exits 0, and `n8n/wf_contact_ingest_cloud.json` is the only generated workflow this plan
      changed.
    - Source: CLAUDE.md gains no duplicate of a section 13.0.3 row 74-04 already added.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && node --test tests/n8n/*.test.mjs && .venv/bin/python -m pytest -q --tb=short -p no:cacheprovider</automated>
    <fails_when>`git diff --quiet -- n8n/` exits 1 after regeneration, or `node --test` prints a `# fail` count of 1 or higher, or pytest exits non-zero.</fails_when>
  </verify>
  <done>The create-failure input is fed on every batch shape that produces no create-failure row, WR-10 is fixed with a test, the ingest workflow is regenerated exactly once, and the node-count delta is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot create response/error → the graph | an untrusted, shape-unverified payload crosses here; only named scalar fields may be read from it |
| graph → operator-facing per-row report | the ingest response is what an operator treats as the record of what happened |
| graph → durable ledger | `written_records`'s outcome mapping is the audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-05-01 | Spoofing | classification of an error item resting on a guessed shape | high | mitigate | D-74-04: explicit stamp applied on the error edge, tested first (Task 1) |
| T-74-05-02 | Repudiation | a create with no joined response reported to the operator still carrying its pre-write action | high | mitigate | D-74-06: unconfirmed-create action, reason passed through verbatim, mapped to failed (Task 2) |
| T-74-05-03 | Information Disclosure | serializing a raw HubSpot error object (which can carry the outbound request's auth header) into a row or fixture | high | mitigate | keep the existing read-at-most-a-few-named-scalars rule in the create-failure row constant; the new stamp node adds one boolean and copies nothing else (Tasks 1-2) |
| T-74-05-04 | Denial of Service (report channel) | the create-failure input starving on an all-update batch | medium | mitigate | D-74-02: gated sentinel with a derived target index (Task 3) |
| T-74-05-05 | Tampering | a sentinel composed with the write-safety gate that the arming setter does not rewrite, dropping rows on a real armed batch | high | mitigate | Task 3 requires confirming the setter is scan-driven before relying on the composition, with a named fallback otherwise |
| T-74-05-06 | Repudiation | a documented-only lane claim read later as observed | medium | mitigate | the create-error lane stays documented-only per D-73-19; the UNOBSERVED tag names the open question (Task 1) |
| T-74-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is in this plan's scope; no dependency is added |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green; `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` green.
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/` exits 0.
- Only `n8n/wf_contact_ingest_cloud.json` changed among generated workflows (the local ingest
  workflow has no create nodes and must not move).
- Ingest node count before and after recorded (98 at phase start).
- Nothing deployed; zero n8n executions consumed.
</verification>

<success_criteria>
- D-74-01 (as ruled), D-74-02, D-74-04, D-74-05, D-74-06 and WR-10 closed, each with a test.
- Exactly one regeneration of the ingest workflow for the whole cluster.
- The create-error lane's shape question stays open and explicitly tagged.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Note |
|---|---|---|
| create-error stamp Code node | new n8n node | D-74-04; on the create node's error edge |
| create-failure lane starved sentinel | new n8n node | D-74-02; target index derived, not literal |
| stamp-first classification in `pairCreateOutcome.js` | behaviour change | stamp read only, never written there |
| `create_unconfirmed` action value | new value | D-74-06; emitted by the create-failure row constant |
| `create_unconfirmed` entry in `ACTION_TO_OUTCOME` | new mapping | maps to the existing failed outcome |
| UNOBSERVED tag on the hand-written error stub | doc | D-74-05 |
| regenerated `n8n/wf_contact_ingest_cloud.json` | generated artifact | node count delta recorded |
| WR-10 extractor fix + test | behaviour change | rides the same regeneration |

<output>
Create `.planning/phases/74-code-review-follow-ups-from-phase-73/74-05-SUMMARY.md` when done.
State the ingest node count before and after, and whether the sentinel composes the write-safety
gate or fell back to the literal predicate (with the residual named if so).
</output>
