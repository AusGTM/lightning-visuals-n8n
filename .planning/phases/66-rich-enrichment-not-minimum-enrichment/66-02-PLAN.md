---
phase: 66-rich-enrichment-not-minimum-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - .planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md
  - tests/n8n/fieldProducerMatrix.test.mjs
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_review_decision_cloud.json
autonomous: true
requirements: [RICH-02, RICH-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-66-07 / RICH-02 / RICH-05: for every policy-promotable field on BOTH lanes, the matrix names which provider or research branch emits a candidate, what the policy class does with it, whether the property is fetched onto existingRecord, and whether the gate chases it."
    - "D-66-07: a field with a policy entry and no producer at all is reported by name and fails a test, so a second lv_linkedin_url is caught by the suite rather than discovered live."
    - "D-66-01 companies half: ENRICH_CO_GATE's REQUIRED is derived from the matrix, never from intuition — a field that cannot be promoted to canonical, or that no branch produces, is excluded with its reason recorded."
    - "D-66-04: widening the companies gate does not change which conflicts are judged. The Judge Gate and Merge Company nodes' conflict-watch lists are byte-unchanged."
    - "D-66-08: no min_confidence and no class moves on either lane. config/field_policy.yaml has a zero-line diff."
    - "CLAUDE.md 29.1: the numberofemployees exception is not widened — the gate may chase it, the merge still admits only an already-numeric provider value at fill_blank_only."
  artifacts:
    - .planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md
    - tests/n8n/fieldProducerMatrix.test.mjs
    - scripts/build_cloud_workflows.py
  key_links:
    - "config/field_policy.yaml keys -> normalizeProviders _push field keys (plus the two PN-1 renames in the ENRICH_MERGE wrapper) -> the gate REQUIRED lists -> the two search property CSVs. The matrix is the join of those four, and a hole in any column is a defect."
    - "ENRICH_CO_GATE's REQUIRED and CONFLICT_WATCH / MATERIAL_CONFLICT_GROUPS are different constants in different nodes. Widening one must leave the other untouched."
---

<objective>
Produce the producer/consumer matrix for both lanes, then derive the companies gate's chased-field
list from it.

Purpose: D-66-07 asks for the matrix precisely because `lv_linkedin_url`'s missing producer was
found by doing this audit by hand. RICH-02 additionally requires that the gate lists be DERIVED
from the matrix rather than chosen by intuition — this plan makes that derivation mechanical and
keeps it honest with a test.

Output: `66-COVERAGE.md` (the human table, both lanes), `tests/n8n/fieldProducerMatrix.test.mjs`
(the machine gate that catches the next producerless field), a widened `ENRICH_CO_GATE` required
list, a widened companies search property list, and regenerated `n8n/wf_*.json`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-CONTEXT.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-PATTERNS.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-01-SUMMARY.md
</context>

<notes_for_the_executor>
- This plan depends on 66-01 having landed. It reads the contacts lane in its POST-66-01 state
  and treats it as the worked example the companies lane is measured against.
- **API-coverage detector: ran, `detected: false`.** No coverage gate fires for this phase.
  `66-COVERAGE.md` is written because D-66-07 and RICH-02 ask for it, not to satisfy a gate.
- **No RESEARCH.md and no VALIDATION.md exist for this phase**; both were skipped by operator
  decision. Verification is derived from 66-CONTEXT.md's own obligations.
- **D-66-10 parity: vacuous, evidence recorded in 66-01.** `grep -nE "REQUIRED[[:space:]]*=" src/*.py`
  returns only `src/judge.py:41` `JUDGE_OUTPUT_REQUIRED` (the judge's output-key list from
  `config/escalation_policy.yaml`), which is not a chased-field gate. Nothing in `src/` moves in
  this commit. Do not invent a Python edit.
</notes_for_the_executor>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The producer/consumer matrix, both lanes — as a test first, then as a document</name>

  <read_first>
    - `config/field_policy.yaml` — the `contacts` block (12 keys) and the `companies` block
      (17 keys), reading each key's `class` and `promote_to_canonical`.
    - `n8n/code/normalizeProviders.js` — every `_push` call site in all six branches
      (`lushaCandidates`, `apolloCandidates`, `zoominfoCandidates`, each with a contacts arm and
      a companies arm). Grep with `grep -nF '_push(out,'` to enumerate them, then read the
      surrounding branches for the ones that pass a variable field name rather than a literal.
    - `scripts/build_cloud_workflows.py` — the two PN-1 rename sites in `ENRICH_MERGE` (search
      `canonicalizeLinkedin(winners.` and `winners.persona_group`); `ENRICH_GATE`'s and
      `ENRICH_CO_GATE`'s `REQUIRED` arrays; `ENRICH_CONTACT_SEARCH_PROPERTIES_CSV`;
      `ENRICH_COMPANY_SEARCH_PROPERTIES_CSV`; and `HS_SEARCH_BODY_EXPR` (the narrower LOCAL-LIVE
      sibling 66-01 Task 2 deliberately left alone).
    - `n8n/code/webResearch.js` — which company fields the Claude web-research lane produces.
      This is the producer for the ICP fields no provider branch emits.
    - `n8n/code/mergeCompanies.js` — `DEFAULT_COMPANY_POLICY`, read-only, for parity against the
      YAML.
    - `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-01-SUMMARY.md`.
    - CLAUDE.md sections 29 and 29.1 — the never-write-automatically list and the ONE scoped
      exception.
  </read_first>

  <files>tests/n8n/fieldProducerMatrix.test.mjs, .planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md</files>

  <behavior>
    New file `tests/n8n/fieldProducerMatrix.test.mjs`, written and run before the document:
    - It DERIVES its inputs, never hardcodes them. Read the policy key sets from
      `config/field_policy.yaml`; read the emitted `_push` field-key literals from
      `n8n/code/normalizeProviders.js` source text; read the two PN-1 rename mappings from
      `scripts/build_cloud_workflows.py` source text; read each lane's required list and each
      lane's search property list from the regenerated `n8n/wf_enrichment_cloud.json` node
      parameters.
    - Assertion 1, the D-66-07 gate: for each lane, every policy key that is
      `promote_to_canonical: true` and is not a `score_output` or `veto_output` class has at
      least one producer — either a `_push` under that key, or a `_push` under a key the PN-1
      rename maps onto it, or the company research lane. Any key with none is reported BY NAME in
      the failure message. Seed the file with the currently-known exceptions as an explicit
      allow-list constant with a one-line reason each, so a NEW hole fails while a known,
      recorded one does not.
    - Assertion 2, the fetch gate: every member of each lane's required list appears in that
      lane's search property list. This is the generalisation of the non-clobber hole 66-01
      Task 2 closed for contacts.
    - Assertion 3, the chase gate: every policy key that has a producer AND is
      `promote_to_canonical: true` AND is not a recomputed output class appears in that lane's
      required list, or in the allow-list with a reason.
    - Assertion 4, the seam gate: for each PN-1 renamed field, the unprefixed key is what
      `normalizeProviders.js` pushes and the prefixed key is what the policy and the search
      property list use. A push under the prefixed spelling is a failure — that is the silent
      no-op D-66-03 names.
  </behavior>

  <action>
    Build the matrix as a derived test first, then render the human document from what the test
    measured. Per D-66-07, RICH-02 and RICH-05.

    Write `tests/n8n/fieldProducerMatrix.test.mjs` implementing the four assertions in
    `<behavior>`. Derive every input; a hardcoded copy of any of the four lists would drift and
    would defeat the point of the matrix. For the source-text reads, parse the `_push` field-key
    literals with a regex over the file text and the required list out of the regenerated node's
    `jsCode` — this is the same read-the-generated-artifact discipline 66-01 Task 2 used, and it
    proves what the workflow will actually do rather than what the Python intends.

    Expect assertion 1 to fail on first run for the companies lane and use the failure output as
    the audit result. Record each genuine hole in the allow-list constant with a one-line reason,
    and record it again in the document. Two are already known and must appear there with these
    reasons: the ZoomInfo LinkedIn output field is unprobed and every member of that output-field
    list must be valid or the whole batch request returns 400, so it is pending-probe with
    `scripts/probe_zoominfo_location_fields.mjs` named as the precedent; and the companies
    `domain` field has a policy entry, a create-seed provenance stamp and no candidate source at
    all, which is an open todo (`.planning/todos/pending/2026-09-04-company-domain-has-no-candidate-source.md`)
    that 66-CONTEXT.md's `<deferred>` block reviewed and deliberately did NOT fold into this
    phase. Surface it, cite the todo, do not fix it here.

    Then write `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md` with
    one table per lane and these columns: field, policy class, `promote_to_canonical`, producing
    branches (naming each provider or the research lane, or the word none), the pushed key if it
    differs from the policy key, fetched onto `existingRecord` yes/no, chased by the gate yes/no,
    and a notes column carrying the reason for any no. Add a short preamble stating the three
    spellings a single field can carry and which lane owns each — the pushed key, the HubSpot
    property key, and the identity-matching key — because that seam is what made
    `lv_linkedin_url` invisible for as long as it was. Add a row noting `HS_SEARCH_BODY_EXPR` as
    a narrower LOCAL-LIVE sibling of the contacts search list, deliberately not widened.

    Change no production code in this task. The test may fail against the companies lane at the
    end of it; Task 2 is what makes it pass.
  </action>

  <verify>
    <automated>node --test tests/n8n/fieldProducerMatrix.test.mjs</automated>
    <fails_when>non-zero exit for a reason other than a companies-lane assertion the allow-list has not yet recorded; at the end of THIS task a companies chase-gate failure is the expected, documented state and Task 2 closes it</fails_when>
    <automated>test -s .planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md</automated>
    <fails_when>non-zero exit, meaning the coverage document is absent or empty</fails_when>
    <automated>grep -c "|" .planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md</automated>
    <fails_when>a count below 29 — fewer table rows than the 12 contacts keys plus the 17 companies keys, meaning a field was omitted from the matrix</fails_when>
  </verify>

  <acceptance_criteria>
    - `tests/n8n/fieldProducerMatrix.test.mjs` exists and contains no hardcoded copy of the
      twelve contacts keys or the seventeen companies keys — it reads them from
      `config/field_policy.yaml` at runtime. Verify by asserting the test file reads that YAML
      path.
    - `66-COVERAGE.md` contains one row per key for both lanes, with a producing-branch cell for
      every row.
    - `66-COVERAGE.md` names the ZoomInfo LinkedIn pending-probe row and the companies `domain`
      producerless row, each with its reason and citation.
    - `git diff --exit-code -- config/field_policy.yaml n8n/code/ scripts/build_cloud_workflows.py`
      exits 0 — this task changes no production code.
  </acceptance_criteria>

  <done>
    Both lanes are audited, every promotable field's producer (or its absence, with a reason) is
    recorded in a document and asserted by a test, and the companies required list Task 2 will
    write is now derivable rather than guessed.
  </done>

  <reversibility rating="reversible">
    A new test file and a new document. Neither changes runtime behaviour.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the companies gate from the matrix, and prove the conflict guard did not move</name>

  <read_first>
    - `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-COVERAGE.md` — Task 1's
      output. This is the input to the derivation; do not re-derive by intuition.
    - `scripts/build_cloud_workflows.py` — the `ENRICH_CO_GATE` wrapper in full (search
      `ENRICH_CO_GATE = inline(`), including the `RECOMPUTE_REQUESTED` block and the two
      recompute action mappings immediately below the required list; and
      `ENRICH_COMPANY_SEARCH_PROPERTIES_CSV` with its long comment block about properties that
      were omitted and the spurious vetoes that followed.
    - `scripts/build_cloud_workflows.py` — `CONFLICT_WATCH` and `MATERIAL_CONFLICT_GROUPS`
      (search `CONFLICT_WATCH`), READ-ONLY. These are a different mechanism in different nodes.
    - `n8n/code/providerConflict.js` and `n8n/code/mergeCompanies.js` — READ-ONLY, for the
      58-06 material-conflict suppression this task must not perturb.
    - CLAUDE.md sections 15.0, 29 and 29.1, and 13.0.
    - `tests/n8n/materialConflictNoVetoFlip.test.mjs` and `tests/n8n/providerConflict.test.mjs` —
      the existing regression proof for the conflict guard.
  </read_first>

  <files>scripts/build_cloud_workflows.py, tests/n8n/fieldProducerMatrix.test.mjs, tests/n8n/materialConflictNoVetoFlip.test.mjs, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json, n8n/wf_scheduled_maintenance_cloud.json, n8n/wf_backend_status_cloud.json, n8n/wf_review_decision_cloud.json</files>

  <behavior>
    - Task 1's chase-gate assertion passes for the companies lane, with only allow-listed
      exclusions remaining.
    - Task 1's fetch-gate assertion passes for the companies lane: every new required member is
      requested by the regenerated company search node.
    - A new assertion in `tests/n8n/materialConflictNoVetoFlip.test.mjs` (or a sibling) captures
      the `jsCode` of the `Judge Gate` and `Merge Company` nodes from the regenerated
      `n8n/wf_enrichment_cloud.json` and asserts the conflict-watch field lists inside them are
      exactly what they were before this plan — a byte-level equality against a literal snapshot
      recorded in the test, so a stray edit to the wrong constant fails loudly.
    - `decideAction` against a company record holding every newly required field returns action
      `skip`; with one blanked, that field appears in `missingFields`.
  </behavior>

  <action>
    Derive `ENRICH_CO_GATE`'s `REQUIRED` from `66-COVERAGE.md` using this exclusion rule, and put
    the rule itself in the comment above the list so a future reader can re-derive it. Per
    D-66-01's companies half and RICH-05.

    Include a `config/field_policy.yaml` `companies` key only when all three hold: it is
    `promote_to_canonical: true`; its class is neither `score_output` nor `veto_output`; and
    `66-COVERAGE.md` records at least one producing branch for it. Everything else is excluded
    with its reason named in the comment. The exclusions this rule produces, and why each matters:
    `domain` is `manual_protected` with `promote_to_canonical: false` and, per the matrix, has no
    candidate source at all — including it would mark every company permanently incomplete;
    `annualrevenue` is `review_required` / `stage_only` / `promote_to_canonical: false`, so the
    pipeline can never fill it canonically and every company without a human-entered value would
    gate to enrich forever, on every scheduled tick — that is the 2026-08-09 execution-runaway
    shape and is the single most important exclusion in this task; and the two `veto_output`
    fields are recomputed from current inputs by `Decide Company Action`, never chased, which is
    the same reason no ICP score field appears in the contacts list either.

    Leave `POLICY` unchanged: the two existing TTL entries stay, and no new `stale_after_days` is
    added anywhere (the D-66-09 discipline applies to both lanes).

    Add every newly required member that is not already present to
    `ENRICH_COMPANY_SEARCH_PROPERTIES_CSV`, in the same commit and for the same reason that
    constant's own comment block already records twice: a property omitted there reads as absent
    on `existingRecord`, which both makes the gate report it permanently missing and turns
    `mergeCompanies`'s non-clobber comparison into a permit. From the matrix, expect
    `lv_revenue_band` and `lv_employee_band` to be the additions; confirm against the matrix
    rather than trusting this sentence.

    Do not touch `CONFLICT_WATCH` or `MATERIAL_CONFLICT_GROUPS` (D-66-04). They live in
    `ENRICH_MERGE_CO` and the `Judge Gate` node, not in `ENRICH_CO_GATE`, and the two mechanisms
    answer different questions: the gate decides what to chase, the conflict lists decide what to
    adjudicate. Add the snapshot assertion described in `<behavior>` so that separation is
    machine-checked rather than asserted in prose.

    Do not touch the `RECOMPUTE_REQUESTED` block or either recompute action mapping immediately
    below the required list. Widening the list changes which records gate to `enrich`; the
    skip-to-enrich and create-to-refused recompute mappings are a request-level override on top of
    that and must keep their exact current behaviour (CLAUDE.md section 13.0).

    On `numberofemployees`: the matrix will show it promotable with producers, so the rule admits
    it to the required list. Record in the comment that this chases the field and changes nothing
    about what the merge will accept — CLAUDE.md section 29.1's exception stays exactly as scoped,
    `fill_blank_only` with already-numeric provider values only and no band parsing. Chasing is
    not widening the exception, and no other field on section 29's list is touched.

    Regenerate with `.venv/bin/python scripts/build_cloud_workflows.py`. Never hand-edit any
    `n8n/wf_*.json`.
  </action>

  <verify>
    <automated>node --test tests/n8n/fieldProducerMatrix.test.mjs</automated>
    <fails_when>non-zero exit, or a "fail" count above 0 — any promotable, produced companies field still absent from the required list without an allow-list reason</fails_when>
    <automated>node --test tests/n8n/materialConflictNoVetoFlip.test.mjs tests/n8n/providerConflict.test.mjs tests/n8n/companyRecomputeLaneFlow.test.mjs</automated>
    <fails_when>non-zero exit, or a "fail" count above 0 — the conflict guard or the recompute lane moved</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, a "fail" count above 0, or a "pass" count below the post-66-01 baseline recorded in 66-01-SUMMARY.md</fails_when>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py</automated>
    <fails_when>non-zero exit, or fewer than 8 "wrote n8n/..." lines in the output</fails_when>
    <automated>.venv/bin/python -m pytest -x -q</automated>
    <fails_when>non-zero exit, or a non-zero failure count in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -c "import json,re; wf=json.load(open('n8n/wf_enrichment_cloud.json')); n=[x for x in wf['nodes'] if x['name']=='Company Gate'][0]; arr=re.search(r'const REQUIRED\s*=\s*\[(.*?)\]', n['parameters']['jsCode'], re.S).group(1); print(sorted(re.findall(r'\"([a-z_]+)\"', arr)))"` prints exactly the derived list from `66-COVERAGE.md`, and that printed list matches the matrix test's own derivation. **This check parses ONLY the bracketed array literal**, never a character window, so an exclusion named in the surrounding comment can neither satisfy nor break it — the exclusions are asserted by the matrix test's allow-list instead, which reads the same array.
    - Every member of that printed list also appears in the regenerated company search node's
      property list — asserted by Task 1's fetch-gate assertion, which now passes for companies.
    - The `POLICY` object literal in the regenerated `Company Gate` node still has exactly two
      keys.
    - `git diff --exit-code -- n8n/code/providerConflict.js n8n/code/mergeCompanies.js` exits 0.
    - `git diff --exit-code -- config/field_policy.yaml` exits 0 (D-66-08).
    - The conflict-watch snapshot assertion in the regression test passes against the regenerated
      JSON.
    - `node --test tests/n8n/*.test.mjs` reports 0 fail.
  </acceptance_criteria>

  <done>
    The companies gate chases every field the matrix shows is promotable and produced, excludes
    the rest with a written reason, fetches everything it chases, and the 58-06 conflict guard
    and the 47.5 recompute lane are proven byte-unchanged.
  </done>

  <reversibility rating="costly">
    The list edit itself reverts in one commit. What does not revert is provider spend on a
    scheduled companies tick: a widened list moves records from `skip` to `enrich`, and the
    scheduled poller runs unattended once armed. This is the reason the `annualrevenue` and
    `domain` exclusions are load-bearing rather than tidy — including either would make the
    enrich state permanent for nearly every company. Nothing is armed and this plan deploys
    nothing, so the irreversible edge is deferred to a future armed run.
  </reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Symbol / path | Where |
|---|---|---|
| New document | `66-COVERAGE.md` — the producer/consumer matrix, both lanes, one row per policy key | `.planning/phases/66-rich-enrichment-not-minimum-enrichment/` |
| New test file | `tests/n8n/fieldProducerMatrix.test.mjs` — four derived assertions (producer, fetch, chase, PN-1 seam) plus a reasoned allow-list constant | repo |
| New `REQUIRED` members (companies) | derived from the matrix; expected to add `industry`, `numberofemployees`, `lv_revenue_band`, `lv_employee_band`, `lv_country_region_normalized`, `country`, `city`, `lv_content_type`, `lv_sponsorship_reliant`, `lv_is_hardware_vendor`, `lv_is_gambling_operator` to the existing `lv_org_type`, `lv_produces_content` — confirm against the matrix, not this sentence | `ENRICH_CO_GATE` wrapper, `scripts/build_cloud_workflows.py` |
| Recorded exclusions | `domain`, `annualrevenue`, `lv_anti_icp_flag`, `lv_anti_icp_reason`, each with its reason in the wrapper comment and in the matrix | same |
| New fetched properties (companies) | expected `lv_revenue_band`, `lv_employee_band` in `ENRICH_COMPANY_SEARCH_PROPERTIES_CSV` | `scripts/build_cloud_workflows.py` |
| New regression assertion | a byte-level snapshot of the conflict-watch lists inside the regenerated `Judge Gate` and `Merge Company` nodes | `tests/n8n/materialConflictNoVetoFlip.test.mjs` |
| Regenerated (never hand-edited) | all eight `n8n/wf_*.json` | repo |

**New `config/field_policy.yaml` entries: NONE** on either lane. Both policy blocks already carry
every key. Zero-line diff (D-66-08).

**Deployment: none.** Adds to the undeployed delta CLAUDE.md section 13.0.2 records.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| companies gate `REQUIRED` -> scheduled provider calls | the widened list changes which companies the unattended poller sends to paid providers. |
| provider/research response -> company canonical properties | more chased fields means more values reaching `mergeCompanies`. |
| gate constant -> conflict-watch constant | two adjacent wrapper constants in the same generated file; an edit to the wrong one changes what gets adjudicated. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-66-07 | Denial of Service | `ENRICH_CO_GATE` required list on the scheduled poller | high | mitigate | A required field the pipeline can never fill canonically (`annualrevenue`: `review_required` / `promote_to_canonical: false`) or that has no producer (`domain`) makes every affected company permanently `enrich`, on every scheduled tick — the 2026-08-09 execution-runaway shape. Mitigated by the three-part exclusion rule, which is written into the wrapper comment and machine-checked by the matrix test's chase-gate assertion, not left to judgement. |
| T-66-08 | Elevation of Privilege | `mergeCompanies` non-clobber on newly required fields | high | mitigate | A property absent from `ENRICH_COMPANY_SEARCH_PROPERTIES_CSV` reads absent on `existingRecord`, so the `fill_blank_only` guard permits an overwrite it should refuse. That constant's own comment records this defect class firing twice already. Mitigated by adding every new required member to the CSV in the same commit, asserted by the matrix test's fetch-gate assertion. |
| T-66-09 | Tampering | 58-06 material-conflict suppression | high | mitigate | `CONFLICT_WATCH` / `MATERIAL_CONFLICT_GROUPS` sit near `ENRICH_CO_GATE` in the same generated file, and a mis-aimed edit would change which cross-provider conflicts are adjudicated before a hard veto can fire — the exact defect execution 11983 exposed. Mitigated by a byte-level snapshot assertion over the regenerated `Judge Gate` and `Merge Company` node code, plus a `git diff --exit-code` on the two conflict modules. |
| T-66-10 | Elevation of Privilege | CLAUDE.md 29.1's `numberofemployees` exception | medium | mitigate | Chasing the field must not be mistaken for widening the exception. Mitigated by leaving `config/field_policy.yaml` and `mergeCompanies.js`'s policy table byte-unchanged (asserted by `git diff --exit-code`), so the field stays `fill_blank_only` admitting already-numeric provider values only, with no band parsing. No other field on section 29's list is chased. |
| T-66-11 | Repudiation | the matrix as an audit artifact | low | mitigate | A hand-written matrix drifts from the code the moment either moves. Mitigated by deriving every column at test runtime from the YAML, the normalizer source and the regenerated JSON, so a divergence fails the suite rather than silently ageing. |
| T-66-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package. No manifest or lockfile is modified. |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` reports 0 fail, including the new matrix test with the
  companies lane passing.
- `.venv/bin/python -m pytest -x -q` reports 0 failures (regression only).
- `git diff --exit-code -- config/field_policy.yaml n8n/code/providerConflict.js n8n/code/mergeCompanies.js`
  exits 0.
- `66-COVERAGE.md` exists with one row per policy key on both lanes.
</verification>

<success_criteria>
- Both lanes are audited and the audit is machine-enforced.
- The companies required list is derived from the matrix by a written rule, with every exclusion
  reasoned.
- Every chased companies field is fetched.
- The conflict guard and the recompute lane are proven byte-unchanged.
- No threshold or class moved on either lane.
</success_criteria>

<output>
Create `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-02-SUMMARY.md` when done.
Record: the final companies required list and each exclusion's reason; every producerless field
the matrix surfaced (including the companies `domain` todo, cited not fixed); and the post-plan
`node --test` pass count.
</output>
