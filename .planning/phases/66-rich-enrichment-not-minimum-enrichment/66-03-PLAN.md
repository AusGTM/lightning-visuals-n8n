---
phase: 66-rich-enrichment-not-minimum-enrichment
plan: 03
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified:
  - scripts/build_cloud_workflows.py
  - operator-claude-plugin/scripts/report_enrichment.py
  - tests/n8n/outcomeContractFlow.test.mjs
  - operator-claude-plugin/tests/test_report_enrichment.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_review_decision_cloud.json
autonomous: true
requirements: [RICH-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-66-06 / RICH-01: in the operator's enrichment report, a contact row that ends the run holding both a phone number and an email is distinguishable from a row holding an email alone. Today the two look identical."
    - "D-66-05: a phone-less row is FLAGGED, never held. No gate, refusal, hold or write-path branch reads the completeness signal — it is report-only."
    - "D-66-05: the signal reflects the post-run state — the existing record plus anything this run promoted — not merely what a provider returned."
    - "The outcome contract version is NOT bumped. The client refuses any version outside its known set, and a backend stamping a version the installed plugin has not learned would refuse every row rather than degrade."
    - "D-66-08: no threshold, class or drop path moves. config/field_policy.yaml has a zero-line diff."
  artifacts:
    - scripts/build_cloud_workflows.py
    - operator-claude-plugin/scripts/report_enrichment.py
  key_links:
    - "ENRICH_BUILD_RESPONSE (the one convergence point every terminal reaches) -> the per-row response body -> report_enrichment._build_row_report -> the operator's rendered report. Both the executions-API path and the synchronous-body path route through that one function."
    - "row.existingRecord plus row.merge.canonicalPatch is the post-run state of email, phone and mobilephone."
---

<objective>
Make phone-and-email completeness visible in the operator's enrichment report, without holding
anything.

Purpose: the operator's stated target is a phone number AND an email, with email-only as the
fallback (2026-09-04). Today nothing in the report expresses it, and a phone-less contact is
indistinguishable from a complete one. D-66-05 settles the hold-or-flag question as FLAG.

Output: a per-row completeness marker computed at the backend's single response convergence
point, a batch-level count, and the renderer that surfaces both — all additive, none of it in a
write path.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-CONTEXT.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-01-SUMMARY.md
@.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-02-SUMMARY.md
</context>

<notes_for_the_executor>
- **Scope note against 66-CONTEXT.md.** Its `<code_context>` says this phase touches no plugin
  skill and no `suggest_contacts.py`. That holds: the renderer this plan edits is
  `operator-claude-plugin/scripts/report_enrichment.py`, a plugin SCRIPT, not a skill, and it is
  the only place the enrichment report's per-row shape is computed. Emitting a field from n8n
  that nothing renders would not satisfy D-66-06's "visible in the report", so the renderer edit
  is in bounds and necessary. No skill file, no `suggest_contacts.py`, no `preingest.py`.
- **Phase 68's rule applies here in advance:** a report reports, it does not halt. Nothing in
  this plan may block, prompt or refuse.
- **No RESEARCH.md and no VALIDATION.md** exist for this phase; both were skipped by operator
  decision. Verification is derived from 66-CONTEXT.md's own obligations.
- **D-66-10 parity: vacuous**, evidence recorded in 66-01. No `src/` edit.
- Plugin tests run from the plugin directory with the repo venv:
  `cd operator-claude-plugin && ../.venv/bin/python -m pytest tests/ -q`. Measured 2026-09-05:
  `tests/test_report_enrichment.py` alone reports 49 passed.
</notes_for_the_executor>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp per-row contactability at the backend's single response convergence point</name>

  <read_first>
    - `scripts/build_cloud_workflows.py` — the `ENRICH_BUILD_RESPONSE` wrapper in full (search
      `ENRICH_BUILD_RESPONSE = inline(`), including the comment explaining why the per-row
      outcome projection is computed at this one node rather than duplicated per terminal, and
      the comment recording the version bump from 1 to 2 and the client-parser coupling it
      created.
    - `n8n/code/mergeContacts.js` — the `mergeContacts` return shape (`canonicalPatch`,
      `provenance`, `cacheKeys`, `decisions`) and which of the twelve fields can appear in
      `canonicalPatch`.
    - `operator-claude-plugin/scripts/preingest.py` lines 45 to 95 — the known-version frozen set
      and the refusal branch. READ-ONLY. This is the reason the version is not bumped.
    - `tests/n8n/outcomeContractFlow.test.mjs` — the existing assertions on this node's
      projection, and the shape to extend.
    - `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-CONTEXT.md` D-66-05 and
      D-66-06.
  </read_first>

  <files>scripts/build_cloud_workflows.py, tests/n8n/outcomeContractFlow.test.mjs, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json, n8n/wf_scheduled_maintenance_cloud.json, n8n/wf_backend_status_cloud.json, n8n/wf_review_decision_cloud.json</files>

  <behavior>
    Extend `tests/n8n/outcomeContractFlow.test.mjs`, RED before GREEN:
    - A contacts row whose post-run state carries an email and a mobile number stamps a
      completeness value meaning both; the same row with the phone fields blank stamps the
      email-only value; a row with neither stamps a third, distinct value.
    - Either phone field satisfies the phone half — a landline alone counts, a mobile alone
      counts. The account cannot buy a verified direct dial at all (that provider field is
      unentitled), so demanding a mobile specifically would make the marker unreachable.
    - The post-run state is the union of the existing record and this run's promoted patch: a row
      whose email was blank and was promoted THIS RUN reads as having an email.
    - A companies row stamps the absence value explicitly rather than omitting the key, matching
      the sibling field in this projection that already stamps absence as an explicit null.
    - `outcome_contract_version` is UNCHANGED by this task. Assert its exact current value so a
      later accidental bump fails here.
  </behavior>

  <action>
    In `scripts/build_cloud_workflows.py`'s `ENRICH_BUILD_RESPONSE` wrapper, add one per-row key
    to the returned projection carrying contactability. Compute it from the union of
    `row.existingRecord` and `row.merge` and `row.merge.canonicalPatch` — the post-run state, not
    the raw provider return — so a value this run promoted counts and a value the run merely
    staged does not. Give it three distinct states, one for both an email and a phone, one for an
    email with no phone, and one for neither, plus the explicit absence value for a row that is
    not a contact. Follow this node's existing discipline exactly: absence is stamped explicitly,
    never a missing key, so a companies row and a contacts row that lost a field cannot look
    alike to a parser. Also add a small object naming which of the three field names satisfied
    each half, so the operator can see WHICH number was found rather than only that one was.

    Treat either phone field as satisfying the phone half. Record in a comment that this is
    deliberate and why: the verified-direct-dial provider field is unentitled on this account, so
    the ceiling this account can buy is a landline (often a switchboard) plus a mobile, and
    requiring a mobile specifically would make the complete state largely unreachable (D-66-05).

    **Do NOT bump `OUTCOME_CONTRACT_VERSION`,** and record the reason at the constant. The client
    parser (`operator-claude-plugin/scripts/preingest.py`) refuses outright any version outside a
    frozen known set rather than degrading, and the plugin's installed marketplace clone does not
    self-refresh — so a backend stamping a version the installed plugin has not learned would
    refuse every row, not lose a field. Additive keys at the current version have no such failure
    mode: an unknown extra key is simply not read. This is a deliberate, recorded departure from
    the precedent that node's own comment sets for its previous additive field, and the departure
    is what keeps this phase from acquiring a plugin-version dependency it does not need.

    **Nothing may branch on this value.** It is stamped and rendered, and no gate, hold, refusal
    or write decision reads it (D-66-05). Say so in the comment so a later reader does not
    "helpfully" wire it into a gate.

    Regenerate with `.venv/bin/python scripts/build_cloud_workflows.py`. Never hand-edit any
    `n8n/wf_*.json` (D-66-10).
  </action>

  <verify>
    <automated>node --test tests/n8n/outcomeContractFlow.test.mjs</automated>
    <fails_when>non-zero exit, or a "fail" count above 0</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, a "fail" count above 0, or a "pass" count below the post-66-02 baseline recorded in 66-02-SUMMARY.md</fails_when>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py</automated>
    <fails_when>non-zero exit, or fewer than 8 "wrote n8n/..." lines in the output</fails_when>
    <automated>.venv/bin/python -c "import json,sys; wf=json.load(open('n8n/wf_enrichment_cloud.json')); n=[x for x in wf['nodes'] if x['name']=='Build Response'][0]; c=n['parameters']['jsCode']; sys.exit(0 if 'OUTCOME_CONTRACT_VERSION = 2' in c else 1)"</automated>
    <fails_when>exit code 1, meaning the outcome contract version moved away from the value the installed plugin knows</fails_when>
  </verify>

  <acceptance_criteria>
    - The regenerated `Build Response` node's `jsCode` contains the new per-row completeness key.
      Assert with a positive read of the node parameters, not a source grep.
    - `.venv/bin/python -c "import json; wf=json.load(open('n8n/wf_enrichment_cloud.json')); n=[x for x in wf['nodes'] if x['name']=='Build Response'][0]; print('OUTCOME_CONTRACT_VERSION = 2' in n['parameters']['jsCode'])"` prints True.
    - `git diff --exit-code -- operator-claude-plugin/scripts/preingest.py` exits 0 — the client
      version set is untouched by this task.
    - `git diff --exit-code -- config/field_policy.yaml n8n/code/mergeContacts.js` exits 0.
    - `node --test tests/n8n/*.test.mjs` reports 0 fail.
  </acceptance_criteria>

  <done>
    Every contacts row in an enrichment response carries a three-state contactability value
    computed from its post-run state, companies rows carry an explicit absence value, the outcome
    contract version is unchanged, and nothing in the workflow branches on the new value.
  </done>

  <reversibility rating="reversible">
    An additive key on a response projection at an unchanged contract version. Removing it cannot
    break a client that never required it.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render completeness in the operator's report — per row and as a batch count</name>

  <read_first>
    - `operator-claude-plugin/scripts/report_enrichment.py` — the whole file, especially
      `_build_row_report`, `_match_info_for_row` (the model for reading an optional nested value
      and returning a pair of Nones when it is absent), `build_sync_report` (the second caller of
      `_build_row_report`), `enrichment_row_ledger`, `_empty_counts`, `_empty_review_counts` and
      `build_enrichment_report`.
    - `operator-claude-plugin/tests/test_report_enrichment.py` — every existing assertion on the
      row-report shape, and in particular the ICP-scoping guard test that scans the built report
      object for a banned literal substring. Read that guard before choosing any key or value
      name, so the new names cannot trip a ban that has nothing to do with this change.
    - `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-CONTEXT.md` D-66-05 and
      D-66-06, and the `Claude's Discretion` item about how completeness is rendered.
  </read_first>

  <files>operator-claude-plugin/scripts/report_enrichment.py, operator-claude-plugin/tests/test_report_enrichment.py</files>

  <behavior>
    Extend `operator-claude-plugin/tests/test_report_enrichment.py`, RED before GREEN:
    - `_build_row_report` on a row carrying the backend's completeness value surfaces it as a
      top-level key on the returned row dict, alongside the existing outcome, review-state and
      match keys.
    - A row from a backend that never stamped the value (an older deployed workflow, which is the
      current live state) surfaces None rather than raising and rather than inferring a false
      negative. This mirrors the module's existing rule that a review-state is never inferred
      false when the backend is silent.
    - `build_sync_report` produces rows carrying the same key — one edit in `_build_row_report`
      must serve both callers, not two implementations.
    - The report object gains a batch-level count of contacts rows in each completeness state,
      and that count is absent-tolerant: rows the backend did not stamp are counted as unknown,
      never folded into either the complete or the partial bucket.
    - `build_enrichment_report` still never raises for a malformed row, a missing node or a
      non-mapping payload — assert with a malformed-input case, matching the module's stated
      never-raise contract.
    - The existing ICP-scoping guard test still passes against the report object.
  </behavior>

  <action>
    Add one key to `_build_row_report`'s returned dict carrying the backend's per-row
    completeness value, read defensively in the same shape `_match_info_for_row` already uses for
    an optional nested value: a non-dict or absent source yields None, never an exception and
    never an inferred negative. Because `build_sync_report` also routes through
    `_build_row_report`, that single edit serves both the executions-API path and the synchronous
    response path — do not add a second implementation.

    Add a batch-level tally to the report object alongside the existing outcome and review counts,
    following `_empty_counts` and `_empty_review_counts` as the shape to copy. Give it a bucket
    per completeness state plus an explicit unknown bucket for rows the backend did not stamp,
    and count contacts rows only. Per D-66-06 the operator's stated target is what this makes
    legible: how many rows ended the run with both a phone number and an email, versus how many
    have an email alone.

    Per D-66-05, this is a FLAG and nothing else. Nothing in this module may hold, filter, refuse,
    prompt or reorder on the new value, and no caller is given a reason to. Record that in the
    docstring or a comment beside the tally, together with the reason the operator ruled this way:
    direct dials are far scarcer than emails and the verified-direct-dial provider field is
    unentitled on this account, so holding every phone-less row would hold most rows. Also note
    Phase 68's rule, which applies to this surface already — a report reports, it does not halt.

    Before choosing the key and bucket names, check them against the ICP-scoping guard test that
    scans the built report object for a banned literal substring, and pick names that do not trip
    it. That guard exists for a reason unrelated to this change and must keep passing unmodified.

    Change no n8n code and no `preingest.py` in this task.
  </action>

  <verify>
    <automated>cd operator-claude-plugin && ../.venv/bin/python -m pytest tests/test_report_enrichment.py -q</automated>
    <fails_when>non-zero exit, or a non-zero failed count in the summary line, or a passed count below the 49 measured on 2026-09-05</fails_when>
    <automated>cd operator-claude-plugin && ../.venv/bin/python -m pytest tests/ -q</automated>
    <fails_when>non-zero exit, or a non-zero failed count in the summary line</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or a "fail" count above 0 — regression only; this task edits no n8n code</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "def _build_row_report" operator-claude-plugin/scripts/report_enrichment.py`
      prints 1 — the edit is in the one shared function, not duplicated into `build_sync_report`.
    - The report object returned by `build_enrichment_report` contains the batch-level
      completeness tally with an unknown bucket.
    - The existing ICP-scoping guard test in `operator-claude-plugin/tests/test_report_enrichment.py`
      passes unmodified — verify by confirming that test's own source is unchanged with
      `git diff -- operator-claude-plugin/tests/test_report_enrichment.py` showing only added
      assertions, no edits inside that test function.
    - `git diff --exit-code -- operator-claude-plugin/scripts/preingest.py operator-claude-plugin/skills/`
      exits 0 — no skill and no parser is touched.
    - `git diff --exit-code -- n8n/ scripts/` exits 0 for this task's own commit.
  </acceptance_criteria>

  <done>
    An operator reading the enrichment report can tell, per row and as a batch count, which
    contacts ended the run with both a phone number and an email and which have an email alone —
    and nothing anywhere holds, filters or refuses on that distinction.
  </done>

  <reversibility rating="reversible">
    An additive report key and an additive count, both absent-tolerant. Reverting removes a
    column from a rendered report and changes no behaviour.
  </reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Symbol / path | Where |
|---|---|---|
| New per-row response key | a three-state contactability value plus a small object naming which field satisfied each half, computed from the post-run state | `ENRICH_BUILD_RESPONSE`, `scripts/build_cloud_workflows.py` |
| New report row key | the same value surfaced on `_build_row_report`'s returned dict, serving both the executions-API and synchronous paths | `operator-claude-plugin/scripts/report_enrichment.py` |
| New batch tally | a count per completeness state, contacts only, with an explicit unknown bucket | same |
| Changed test files | `tests/n8n/outcomeContractFlow.test.mjs`, `operator-claude-plugin/tests/test_report_enrichment.py` | repo |
| Regenerated (never hand-edited) | all eight `n8n/wf_*.json` | repo |

**Deliberately NOT produced: an `OUTCOME_CONTRACT_VERSION` bump.** The client refuses an unknown
version outright and the installed marketplace clone does not self-refresh, so a bump would refuse
every row rather than degrade. Additive keys at the current version cannot break the parser.
Recorded at the constant.

**New `config/field_policy.yaml` entries: NONE. New gate `REQUIRED` members: NONE.** This plan
adds no chased field; it makes the result of 66-01's and 66-02's chasing legible.

**Deployment: none.** Adds to the undeployed delta CLAUDE.md section 13.0.2 records.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| backend response body -> plugin report parser | the plugin reads a body it does not control and must not raise on a shape it does not recognise. |
| report surface -> operator decision | what the report says shapes what the operator sends, so a false "complete" is worse than an honest unknown. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-66-12 | Denial of Service | outcome contract version coupling | high | mitigate | The client refuses any version outside a frozen known set rather than degrading, and the installed plugin is a marketplace clone that does not self-refresh. A backend stamping an unlearned version would refuse EVERY row — a total pipeline outage, not a lost field. Mitigated by adding keys at the unchanged version and pinning that version with an assertion over the regenerated node, so a later accidental bump fails the suite here. |
| T-66-13 | Spoofing | a falsely-complete row | medium | mitigate | A row the backend never stamped must not read as complete or as partial. Mitigated by surfacing None for an unstamped row and giving the batch tally an explicit unknown bucket — the same never-infer-false-when-the-backend-is-silent rule this module already applies to review-state. |
| T-66-14 | Denial of Service | report parsing of a malformed row | medium | mitigate | `build_enrichment_report` promises never to raise. Mitigated by reading the new value with the same defensive non-dict guard `_match_info_for_row` uses, and by a malformed-input assertion in the test. |
| T-66-15 | Elevation of Privilege | completeness used as a gate | medium | mitigate | D-66-05 rules that a phone-less row is flagged, never held; a later reader wiring the flag into a hold would silently change send behaviour and would have made Phase 69's decline store a dependency of this phase. Mitigated by stamping and rendering only, with the no-branching rule recorded at both the emission and the render site, and by an acceptance criterion asserting no skill file is touched. |
| T-66-16 | Information Disclosure | naming which field satisfied each half | low | accept | The object names field names already present in the same response body and adds no new datum. No phone number or email value is added to the report surface by this plan. |
| T-66-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package. No manifest or lockfile is modified. |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` reports 0 fail with no regression against 66-02's recorded
  pass count.
- `cd operator-claude-plugin && ../.venv/bin/python -m pytest tests/ -q` reports 0 failures.
- `.venv/bin/python -m pytest -x -q` (repo root) reports 0 failures.
- `git diff --exit-code -- config/field_policy.yaml operator-claude-plugin/scripts/preingest.py operator-claude-plugin/skills/`
  exits 0.
</verification>

<success_criteria>
- A contacts row ending the run with both a phone number and an email is distinguishable in the
  report from one holding an email alone, per row and in a batch count.
- A row the backend did not stamp reads as unknown, never as either bucket.
- The outcome contract version is unchanged and pinned by an assertion.
- Nothing holds, filters or refuses on the new value.
</success_criteria>

<output>
Create `.planning/phases/66-rich-enrichment-not-minimum-enrichment/66-03-SUMMARY.md` when done.
Record: the exact key and bucket names chosen; the no-version-bump decision and its reason; the
confirmation that no skill file and no parser was touched; and the final pass counts for both
suites.
</output>
