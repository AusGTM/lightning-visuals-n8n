---
phase: 53-operator-openable-write-grant
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_write_grant_guardrails.py
autonomous: true
requirements: [GRANT-01, GRANT-02, GRANT-04, GRANT-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening a grant computes and returns its envelope — record count, worst-case provider credits per provider, worst-case Anthropic dollars, projected n8n executions, and the configured monthly execution allowance — before any yes is asked for (GRANT-02, disclosure half)."
    - "The envelope's figures carry the date the rate table was measured and how old it is, and an unreadable provider balance reads as unconfirmed rather than as headroom."
    - "The grant's envelope IS its ceiling: the numbers shown at open are the numbers the grant is bound to, and the plan says in the operator's own words that this figure discloses rather than constrains (D-53-02)."
    - "A grant closes on batch completion, ceiling breach, operator revocation, session end and unhandled error; each close names its reason and each is reported (GRANT-04)."
    - "GRANT-04's 'each expiry disarms the backend' is satisfied concretely: on the two guardrail-B paths that have just observed or inferred live writes, the close ATTEMPTS a disarm and reports its verdict. On completion, revocation and session end it is vacuously satisfied — under per-send windows no window is open at close time — and the plan says so rather than leaving it reading as skipped."
    - "A revoked grant refuses the NEXT SEND (GRANT-05, re-scoped by the operator 2026-08-25). A dispatch_plan already running completes its remaining chunks under the arm it opened with — chunking.dispatch_plan loops internally and never consults the grant — so a batch chunked at 2 records can run many chunks before a revoke bites."
    - "GUARDRAIL A: opening a grant reads the live write-safety state first and refuses to open when it finds writes already armed, naming the workflow, the flags and the record allowlist it found, and offering a disarm (D-53-03 proposed guardrail)."
    - "GUARDRAIL B: two consecutive disarm failures, or a pre-flight read showing writes still live at the start of the next send, closes the grant and blocks further sends; one isolated disarm failure does not (D-53-04 proposed guardrail)."
  prohibitions:
    - "No expiry is written into any deployed workflow and no watchdog job is created — both were offered and declined (53-CONTEXT deferred ideas)."
    - "No admin-set maximum ceiling is introduced; the computed worst case is the only ceiling (D-53-02)."
    - "No wall-clock timeout closes a grant; the grant is scoped to a batch, never to a duration (GRANT-03)."
    - "A ceiling breach does not silently continue and does not silently reduce the batch; it closes the grant and reports."
    - "Guardrail A never disarms on its own initiative; it names what it found and offers, and the operator decides (D-53-03 mandates offer-only). Guardrail B is the opposite case and DOES disarm — the distinction is that guardrail A found a state it did not create, while guardrail B is closing a window its own run opened."
    - "No claim is made anywhere that revocation stops a chunk of an already-running dispatch; chunking.dispatch_plan has no grant-aware loop and none is added here."
    - "Neither guardrail is reachable by an environment variable, a config toggle or a phrase that turns it off."
  artifacts:
    - operator-claude-plugin/tests/test_write_grant_guardrails.py
  key_links:
    - "write_grant.envelope reuses cost_guard.load_rates / estimate_batch / fetch_balances / compare and chunking.plan_chunks — no second cost model exists."
    - "The allowance figure is read through n8n_read.EXECUTION_ALLOWANCE_KEY, the same key n8n_cadence.check_budget_floor and sweep_read's burn-rate alarm read."
    - "Guardrail A reads through n8n_read.read_write_safety over n8n_arming.DISPATCH_FLAGS — the shipped reader, never a second regex."
    - "Guardrail B's counter lives on the grant dict, so it round-trips through the conversation and needs no process to survive."
    - "Guardrail B's two closes call n8n_arming.disarm, which is ungated by design — closing with a disarm adds no new authority anywhere."
---

<objective>
Give the grant its envelope, its lifetime and its two guardrails: the arithmetic an operator reads
before saying yes, the five ways a grant ends, revocation that bites at the next chunk, and the two
defences 53-CONTEXT.md asked the planner to surface rather than assume.

Purpose: D-53-02 makes the computed worst case the grant's ceiling and records plainly that its job
is disclosure, not constraint. D-53-03 and D-53-04 each accept a real risk and each propose one cheap
defence against it — a session-start read that refuses to open over an already-armed backend, and a
bound on how far a run may march while the disarm state is unknown. Both are built here.

Output: `write_grant.envelope`, `revoke`, `close_grant` reasons, `check_before_send`,
`record_send_outcome`, the guardrail-A preflight that fills 53-01's seam, and
`tests/test_write_grant_guardrails.py`.

Scope note: the ceiling that REFUSES before starting, against a sampled remaining monthly allowance,
is Phase 57 and is not built here. This plan's ceiling discloses.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.1-REQUIREMENTS.md
@.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md
@.planning/phases/53-operator-openable-write-grant/53-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The envelope — the arithmetic the operator reads before the yes</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/cost_guard.py` (lines 100-260) — `estimate_batch`, `fetch_balances`, `compare`, and the tri-state that branches on readability before magnitude
    - `operator-claude-plugin/scripts/preview_enrichment.py` (lines 73-135, 246-360) — `records_block`, `cost_block`, `assemble_preview`: the register and table shape an operator already reads
    - `operator-claude-plugin/scripts/chunking.py` (lines 93-200) — `chunk_ceiling`, `plan_chunks`, `ChunkPlan`
    - `operator-claude-plugin/scripts/n8n_read.py` — `EXECUTION_ALLOWANCE_KEY` and where it is defined
    - `operator-claude-plugin/scripts/control_actions.py` (lines 150-185) — `plan_action`'s `arm_dispatch` consequence sentence, the register 53-CONTEXT names as the right one to extend
    - `operator-claude-plugin/config/operator.local.example.json` — `max_records_per_chunk` and its provenance note, `n8n_monthly_execution_allowance`
    - `.planning/milestones/v1.1-REQUIREMENTS.md` — GRANT-02's four figures, verbatim
  </read_first>
  <behavior>
    - Given a batch of N record ids and a resolved provider list, the envelope reports the record
      count, one worst-case credit figure per provider, a worst-case Anthropic dollar figure, a
      projected execution count, and the configured monthly allowance.
    - Given a provider whose balance could not be read, the envelope reports that provider's
      headroom as unconfirmed — never as sufficient and never as zero.
    - Given a rate table, the envelope carries its measured-on date and its age in days.
    - Given a config with no allowance key, the envelope reports the allowance as unconfigured and
      still returns every other figure — an absent allowance degrades one line, not the whole open.
    - The consequence sentence names what turns on, bounded to what, what turns it off, and what
      happens if turning it off fails.
    - The envelope's figures are attached to the returned grant unchanged, so what was shown and what
      the grant is bound to are the same object.
  </behavior>
  <action>
Add `envelope(config, *, object_type, record_ids, record_domains, providers, transport=None)` to
`write_grant.py`, returning a dict of figures plus a rendered block. Build it entirely out of what
already exists: `cost_guard.load_rates`, `cost_guard.estimate_batch`, `cost_guard.fetch_balances`
and `cost_guard.compare` for the credits and dollars; `chunking.chunk_ceiling` and
`chunking.plan_chunks` for the chunk count. Do not write a second cost model — the one in
`cost_guard` is dated, measured and already over-states on purpose (Lusha at its first-time rate),
and a second one would drift from it silently.

Project executions honestly and label the projection with its basis rather than presenting it as a
measurement: one webhook execution per chunk, plus one sub-execution per record, because the
enrichment workflow has no batching node and fans out per record. Say in the returned dict which
part is measured and which part is projected. Read the plan allowance through
`n8n_read.EXECUTION_ALLOWANCE_KEY` rather than spelling the key again — `n8n_cadence.check_budget_floor`
already hoisted it there for exactly this reason.

State the remaining-allowance gap in the block itself, in one sentence: the projection is against
the plan's configured monthly allowance, not against what is left of it this month, because n8n
exposes no usage endpoint to an API key and sampling the remainder is Phase 57's work. An operator
who reads a share-of-allowance figure will otherwise assume it accounts for what the schedulers
have already spent.

Write the D-53-02 sentence into the block in the operator's own register, not only into a docstring:
these numbers are the grant's ceiling and they describe what this batch can cost — they do not
prevent it, because a ceiling derived from the batch cannot block anything the batch already
implies. An operator who wants a smaller ceiling names a smaller batch. This is the honest form of
the decision and hiding it would let the figure read as a guard it is not.

Compose the consequence sentence by extending the register `control_actions.plan_action` already
produces for `arm_dispatch`, per 53-CONTEXT's `<specifics>`: what turns on (live writes on the named
lanes' workflows), bounded to what (exactly this record set, and creates included or not), what
turns it off (each send's own disarm, and closing the grant), and what happens if turning it off
fails (the send is reported as a disarm failure, the run does not silently continue past two of
them, and an admin must check n8n). Name every lane the grant covers, individually. Under D-53-05 a grant may span both the
enrichment and the contacts lanes, and when it does the consequence sentence must say so in the same
breath as saying the HubSpot write is being authorized before the enriched preview exists — that is
the protection the operator traded away and the sentence they read at the yes is the last place it
can be stated. Do not shorten two named lanes into a collective phrase; the operator is approving
two distinct write surfaces and must see both.

Wire the envelope into `open_grant`: compute it before the grant dict is built, attach it under
`envelope`, and return it in the refusal path too when the open is refused for another reason, so an
operator who is refused still learns what the batch would have cost.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_cost_guard.py operator-claude-plugin/tests/test_preview_enrichment.py -q</automated>
  </verify>
  <done>Opening a grant returns record count, per-provider worst-case credits, worst-case Anthropic dollars, projected executions and the configured allowance, each labelled measured or projected; an unreadable balance reads unconfirmed; the rate table's age is shown; the disclosure-not-constraint sentence and the remaining-allowance gap are both in the block an operator reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lifetime and revocation — the five ways a grant ends, and the next chunk</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/n8n_arming.py` (lines 345-452) — `disarm`'s outcomes, `DISARM_FAILED`, `armed_window.__exit__`'s both-failed chaining
    - `operator-claude-plugin/scripts/chunking.py` (lines 236-300) — `dispatch_plan`'s per-chunk loop and `failed_batch`: read the loop and confirm for yourself that it never consults a grant, which is why GRANT-05's boundary is the send
    - `operator-claude-plugin/scripts/write_grant.py` — 53-01's `close_grant`, `covers`, and the grant dict's `closed_reason` field
    - `.planning/milestones/v1.1-REQUIREMENTS.md` — GRANT-04 and GRANT-05, verbatim
  </read_first>
  <behavior>
    - A grant closed for each of the five reasons carries that reason by name, and the reason set is
      exactly: batch completion, ceiling breach, operator revocation, session end, unhandled error.
    - `check_before_send` on a closed grant refuses and names the closing reason; on an open grant
      whose record set does not cover the send, refuses and names the offending records.
    - A revocation stops the NEXT SEND: the next `check_before_send` refuses, so no further arm is
      attempted. A revocation partway through a running `dispatch_plan` does NOT stop that
      dispatch's remaining chunks, and the test asserts that directly rather than pretending
      otherwise.
    - `record_send_outcome` given a verified disarm resets the consecutive-failure counter to zero.
    - `record_send_outcome` given a ceiling breach closes the grant rather than continuing.
    - Closing a grant makes no network call and does not disarm — there is no window open at close
      time under per-send windows.
  </behavior>
  <action>
Add the five close reasons as a frozen set of named constants on `write_grant.py`, and make
`close_grant` refuse an unrecognised reason rather than accepting a free-text string. A close reason
that can be anything is a close reason nobody can report on, and GRANT-04 requires each expiry to be
reported.

Add `check_before_send(grant, *, lane, workflow_id, record_ids, record_domains)` as the single
question every send asks: it composes 53-01's `covers` with the state check and returns None or a
refusal naming what failed.

**GRANT-05's boundary is the SEND, not the chunk — verified against `chunking.py` lines 236-283 and
re-scoped by the operator on 2026-08-25.** `dispatch_plan` loops over every chunk internally and
never consults the grant; there is no per-chunk hook and none is added here. So a revocation takes
effect when the next send asks `check_before_send`, and a dispatch already running completes its
remaining chunks under the arm it opened with. Write that in the docstring in those words, including
the consequence an operator has to live with: at `max_records_per_chunk` of 2, a 40-record send is
20 chunks, all of which run after a revoke that arrived at chunk three. Anyone who wants
chunk-granular revocation has to make `dispatch_plan` grant-aware, which is a change to the shared
dispatch loop every lane uses and is not this phase's.

Do not write the drafted two-hand-calls test. Calling `check_before_send` twice by hand and
asserting the second refuses proves only that a revoked grant refuses — it would pass while GRANT-05
was entirely unimplemented. Test what is actually true instead: drive a real `dispatch_plan` over a
multi-chunk plan with a revocation applied midway and assert every chunk still went, then assert the
following `check_before_send` refuses. A test that documents the limitation is worth more than one
that decorates it.

Add `record_send_outcome(grant, outcome)`, a pure function returning an updated grant copy. It reads
the disarm verdict out of a send's result, resets `consecutive_disarm_failures` to zero on a verified
disarm, increments it on a failure, and closes the grant on a ceiling breach. Task 3 adds the
two-failure rule on top of the counter this establishes. Keep it pure and return a new dict: the
grant round-trips through the conversation between turns, so any state that mutates in place is
state that is lost.

State where GRANT-04's disarm clause bites and where it does not, so neither reads as skipped. On
completion, revocation and session end there is no window open at close time — every send disarmed
its own — so the clause is vacuously satisfied and `close_grant` correctly makes no call. The two
paths where it is NOT vacuous are guardrail B's, and Task 3 makes those disarm.

Handle session end and unhandled error honestly rather than pretending at a mechanism. Both are
`close_grant` calls the caller makes, and there is no process to make them for a session that dies
mid-turn. Say that in the module docstring, next to D-53-03's accepted risk, and point at
guardrail A as the thing that catches the case where nobody made the call — that is the whole reason
the guardrail exists and a reader who cannot connect the two will read the gap as an oversight.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_chunking.py -q</automated>
  </verify>
  <done>The five close reasons are named constants and an unrecognised reason is refused; a closed grant refuses the next SEND by name; a real multi-chunk `dispatch_plan` with a mid-run revocation is shown to complete every chunk, and the docstring says so with the 2-records-per-chunk consequence; the disarm-failure counter resets on a verified disarm; GRANT-04's disarm clause is stated as vacuous on three paths and deferred to Task 3 on the other two; the session-end gap is stated and linked to guardrail A.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The two guardrails — refuse to open over an armed backend, and bound the disarm unknown</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant_guardrails.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/n8n_read.py` (lines 384-423) — `read_write_safety`'s return shape, including `disagreement` and `nodes`
    - `operator-claude-plugin/scripts/status.py` (lines 108-140) — `describe_workflow` and `WRITE_SAFETY_FLAGS`, the existing live read to mirror rather than duplicate
    - `operator-claude-plugin/scripts/n8n_arming.py` (lines 180-200, 345-400) — `DISPATCH_FLAGS`, `disarm`'s ungated contract and its outcome dict
    - `operator-claude-plugin/scripts/write_grant.py` — 53-01's `preflight` seam on `open_grant`
    - `.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md` — the two PROPOSED GUARDRAIL paragraphs under D-53-03 and D-53-04, verbatim
  </read_first>
  <behavior>
    - GUARDRAIL A: given a live workflow whose record-writes flag reads enabled, `open_grant`
      refuses, and the refusal names the workflow, every dispatch flag it read and their values, and
      the record allowlist currently in force.
    - GUARDRAIL A: the refusal offers a disarm as the next step and does not perform one.
    - GUARDRAIL A: given a live workflow reading disarmed on every dispatch flag, the open proceeds.
    - GUARDRAIL A: given a workflow that cannot be read, the open refuses — an unreadable
      write-safety state is not a disarmed one.
    - GUARDRAIL A: given declaring nodes that disagree with each other, the open refuses and says so.
    - GUARDRAIL B: one disarm failure followed by a verified disarm leaves the grant open and no
      extra disarm is attempted.
    - GUARDRAIL B: two consecutive disarm failures close the grant, ATTEMPT a disarm, and carry that
      disarm's verdict on the close; the next `check_before_send` refuses.
    - GUARDRAIL B: a pre-flight read before a send that finds writes still live closes the grant,
      ATTEMPTS a disarm, carries its verdict, and refuses that send, whatever the counter reads.
    - GUARDRAIL B: when the closing disarm itself fails, the close still happens and the close
      carries the failed verdict — a failed disarm never leaves the grant open.
    - GUARDRAIL A still performs no disarm on any path, and a test asserts its transport log holds
      reads only.
  </behavior>
  <action>
Fill 53-01's `preflight` seam with guardrail A. Add `read_live_write_state(config, workflow_ids,
transport)` reading each covered lane's workflow once through `n8n_read.get_workflow` and mapping
`n8n_read.read_write_safety` over `n8n_arming.DISPATCH_FLAGS` — the shipped reader, never a second
declaration regex. `status.describe_workflow` reads only two of the four flags and does not return
the allowlists, which is why this is its own narrow read rather than a call into it; note that in a
comment so the next reader does not undo it as duplication.

Treat three states as refusing to open: any write-enabling flag reading enabled, a workflow that
could not be read at all, and declaring nodes that disagree. The middle one matters most and is the
one a hurried implementation gets wrong: an unreadable write-safety state is not evidence of a
disarmed backend, and the whole point of this guardrail is that it fires exactly when something is
already wrong.

Write the refusal to name what was found, in the register the operator can act on: the workflow's
name and id, each flag and its value, and the record ids and domains currently allowlisted. Offer
the disarm as the operator's next step and do not take it. Guardrail A exists to turn D-53-03's
accepted risk from silent into loud — a guardrail that silently repaired the state would remove the
evidence that a previous session died armed, which is the one signal telling the operator the
client-held design is costing them.

Say in the docstring what this guardrail does and does not prove. It proves nothing about a session
that died between an arm and a disarm inside the same turn and was then cleaned up by something
else; it proves that at THIS open, on the lanes this grant covers, writes were off. It is the only cheap defence
available under a client-held grant, and D-53-03 recorded that plainly.

Then add guardrail B on top of Task 2's counter. Extend `record_send_outcome` so a second
consecutive disarm failure closes the grant with its own named reason, and add
`preflight_before_send(grant, config, lane, transport=None)` performing the same live read against
that send's lane workflow before a send and closing the grant when it finds writes still live. Both paths
refuse the next send through Task 2's `check_before_send`, so there is exactly one place a send is
refused.

**Both of those two paths ATTEMPT a disarm as part of closing, and record its verdict on the closed
grant.** This is where GRANT-04's "each expiry disarms the backend" actually bites. Each of these
two paths closes having just observed live writes (the pre-flight read) or having twice failed to
turn them off (the counter) — closing and walking away would leave a live record-scoped allowlist
behind at exactly the moment the system knows one is there, which is the failure the whole ceremony
exists to prevent. Call `n8n_arming.disarm`, which is ungated by design, so this adds no authority
anywhere; carry the verdict on the close so the operator reads whether it worked; and close the
grant either way — a failed closing disarm must never be a reason to leave the grant open, because
that would let the run continue over exactly the state that triggered the guardrail.

Keep guardrail A offer-only, in contrast, and write the contrast down where both live. D-53-03
mandates that it names what it found and offers rather than acting, and the reason is asymmetric:
guardrail A found a state it did not create and whose evidence the operator needs to see, while
guardrail B is closing a window its own run opened and is responsible for. Add a test asserting
guardrail A's transport log contains reads only, so a later edit cannot quietly make it act.

Keep D-53-04's chosen behaviour intact: ONE failed disarm fails that send only and the session
continues. The bound is on the second, not the first. Write that distinction into the docstring with
the reason from 53-CONTEXT — a transient blip must not abort a long run, and an unbounded march of
writes over an unknown backend state must not be possible either.

Create `operator-claude-plugin/tests/test_write_grant_guardrails.py` as its own file rather than
folding these into `test_write_grant.py`. These two tests are the phase's proposed defences and the
operator asked for them by name; a separate file is where a reviewer looks for them, and it mirrors
the repository's own habit of keeping a distinct safety contract in a distinct file
(`test_enrich_before_ingest_skill_contract.py` states that reasoning for itself).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant_guardrails.py operator-claude-plugin/tests/test_write_grant.py -q</automated>
  </verify>
  <done>An open over a live-armed backend refuses and names the workflow, the flags, their values and the allowlist, offering a disarm without taking it, with a reads-only transport log; an unreadable or disagreeing state also refuses; one disarm failure leaves the grant open; two consecutive failures and a pre-flight read finding writes live each close the grant AND attempt a disarm AND carry its verdict, closing even when that disarm fails; both guardrails live in their own test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Previous session's leftover state → this session's grant | A session that died armed leaves a live record-scoped allowlist; guardrail A is the only thing that looks |
| Send outcome → grant state | The disarm verdict decides whether the run may continue; a misread verdict is an unbounded march of writes |
| Rate table and balances → the operator's yes | The envelope is what consent is given against; an over-confident figure buys consent to something else |
| Config allowance key → the projected-executions line | An absent or wrong allowance must degrade one line, never fabricate headroom |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-07 | Elevation of Privilege | opening over an already-armed backend | high | mitigate | Guardrail A refuses to open when any dispatch flag reads enabled, when the workflow is unreadable, or when declaring nodes disagree; pinned by four tests |
| T-53-08 | Denial of Service | unbounded run over unknown disarm state | high | mitigate | Guardrail B closes the grant on two consecutive disarm failures or on a pre-flight read finding writes live; one failure deliberately does not, per D-53-04 |
| T-53-08b | Elevation of Privilege | a grant closed while writes are still live | critical | mitigate | Guardrail B's two closes call the ungated `n8n_arming.disarm` and carry its verdict on the close; the grant closes even when that disarm fails, so a live allowlist is never left behind unreported |
| T-53-09 | Spoofing | an unreadable balance read as headroom | medium | mitigate | `cost_guard.compare`'s tri-state is reused unchanged; unconfirmed is its own answer and is pinned by a test |
| T-53-10 | Repudiation | a ceiling figure that reads as a guard | medium | mitigate | The disclosure-not-constraint sentence is in the operator-facing block, not only in a docstring; the remaining-allowance gap is stated in the same block |
| T-53-11 | Tampering | a revocation that does not bite | high | mitigate | Revocation sets grant state and `check_before_send` refuses the next SEND. `dispatch_plan` has no grant-aware loop, so a running dispatch finishes its chunks — the limitation is tested and documented rather than claimed away (operator re-scope, 2026-08-25) |
| T-53-12 | Elevation of Privilege | a guardrail switched off | high | mitigate | Neither guardrail reads an environment variable, a config key or a phrase; a test asserts no toggle exists in either path |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

<flagged_assumptions>
- **The projected execution count is a projection, not a measurement.** One webhook execution per
  chunk plus one sub-execution per record follows from the enrichment workflow having no batching
  node, which is documented in `max_records_per_chunk`'s own provenance note, but nobody has counted
  executions for a multi-chunk grant end to end. It is labelled as projected in the returned dict and
  should be measured in Phase 54, where the cost model is being corrected anyway.
- **Guardrail A cannot see a lane the grant does not name.** It reads only the workflows the grant
  covers. A backend armed on a lane outside this grant stays unnoticed. Widening it to every workflow
  the API key can see is a one-line change against `n8n_read.list_workflows` and was left out because
  a refusal citing an unrelated workflow would train the operator to override it. Flagged rather than
  silently scoped.
- **GRANT-05's send boundary is a real reduction in what revocation buys, and the operator chose
  it.** At `max_records_per_chunk` of 2, a 40-record send runs 20 chunks under one arm and a revoke
  arriving at chunk three stops none of them. Chunk-granular revocation needs `dispatch_plan` to
  become grant-aware, which changes the shared dispatch loop every lane in this plugin uses. The
  requirement text in `.planning/milestones/v1.1-REQUIREMENTS.md` is updated to say send boundary so
  the requirement and the build agree.
- **"Ceiling breach" is a close reason with no producer in this phase.** Nothing in Phase 53
  measures spend as it happens, so the reason exists and is testable but is only reached by a caller
  that supplies it. Phase 57 is what makes it fire on its own. Named here so its emptiness is
  deliberate rather than a missed wire.
</flagged_assumptions>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0.
- `.venv/bin/python -m pytest` exits 0 (full Python suite).
- `node --test tests/n8n/*.test.mjs` exits 0.
- `git status --porcelain` shows no modification under `n8n/` or to
  `operator-claude-plugin/scripts/scheduled_arm.py`.
- `operator-claude-plugin/tests/test_control_arming.py`, `test_control_flag_parity.py` and
  `test_scheduled_arm.py` are unmodified by this plan.
</verification>

<success_criteria>
- The operator reads real arithmetic before saying yes, and the figure they read is the figure the
  grant is bound to.
- The plan says plainly, where the operator will see it, that this ceiling discloses and does not
  constrain, and that the remaining monthly allowance is not yet sampled.
- A grant ends in exactly five named ways and each is reported.
- Revocation bites at the next chunk with no interrupt mechanism.
- Both proposed guardrails exist as working code with their own test file — neither was assumed away.
</success_criteria>

<output>
Create `.planning/phases/53-operator-openable-write-grant/53-02-SUMMARY.md` when done.
</output>
</content>
