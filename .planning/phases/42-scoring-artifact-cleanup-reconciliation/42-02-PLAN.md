---
phase: 42-scoring-artifact-cleanup-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - config/hubspot_properties.yaml
  - tests/test_hubspot_properties_config.py
  - .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json
autonomous: false
requirements: [CLEAN-01]

user_setup:
  - service: hubspot
    why: "One live read-only re-run of the drift checker to prove the expanded yaml reconciles, plus a `sync_hubspot_properties.py` dry-run to prove the expansion cannot cause a portal write. `.env` is permission-blocked, so both are operator-pasted `!` commands."
    env_vars:
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: ".env (loaded by `load_dotenv()` inside the operator command)"
      - name: HUBSPOT_PORTAL_ID
        source: ".env (must equal 22617666)"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "config/hubspot_properties.yaml declares every lv_* and scoring property the system depends on that actually exists live, with type, fieldType, groupName, and enum option values copied from the committed phase42-pre snapshot (D-04, D-05)."
    - "No yaml entry exists for any property the portal does not have — the five design-only names are recorded as documented gaps in the drift report, not invented into the manifest (F2)."
    - "The four offline tests whose premises D-04 inverts are updated with their protective intent preserved and restated, not deleted (F3)."
    - "A live `sync_hubspot_properties.py` dry-run proposes zero property creates and zero group creates against the expanded manifest, proving the expansion cannot cause a portal mutation (D-05)."
    - "The drift checker re-run exits 0 apart from documented gaps, cosmetic-only entries, and the accepted lv_icp_tier divergence."
  artifacts:
    - config/hubspot_properties.yaml
    - tests/test_hubspot_properties_config.py
    - .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json
  key_links:
    - "Every new yaml entry's shape comes from config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json — the committed live snapshot from plan 01 — and from no other source."
    - "New entries whose live groupName is a HubSpot-native group set groupName accordingly, but the yaml's own `groups:` list is NOT extended, so `compute_group_diff` in scripts/sync_hubspot_properties.py never proposes creating a native group."
    - "The expanded yaml becomes plan 03's secondary do-not-archive oracle: a property declared here is by definition not an orphan."
---

<objective>
Expand `config/hubspot_properties.yaml` from a partial create-only manifest into the full mirror
D-04 specifies, and update the four offline tests whose premises that inversion breaks (F3).
Then prove the result two ways: the drift checker reconciles, and the only tool that can write
properties to the portal proposes nothing.

Direction is fixed by D-05: **live wins, the yaml catches up.** This plan performs zero portal
mutations. The only live calls it makes are read-only — one drift-checker re-run and one
`sync_hubspot_properties.py` dry-run, which by default writes nothing and additionally requires
two env keys it will not be given.

**F2 — the load-bearing rule of this plan.** Five of the ten names D-04 lists as missing
(`lv_icp_confidence`, `lv_recommended_motion`, `lv_icp_scored_at`, `lv_icp_scoring_version`,
`lv_named_account_priority`) trace exclusively to CLAUDE.md's superseded local-MVP design and
have no live-creation evidence anywhere in the repo; one is a documented live 404. A property
that does not exist live cannot be mirrored, and writing a yaml entry for it would fabricate
schema that the very next drift run reports as `fabricated_entry`. Plan 01's committed snapshot
answers existence per name. **Present live → mirror it. Absent live → no entry, ever; it lands
in the drift report as a documented gap.** Never write an entry from the design list.

**F1 reminder — nothing in this plan archives anything.** The eleven do-not-archive properties
and six flows named in `42-01-PLAN.md`'s objective stay live and enabled. `39-DECISION.md:107-112`
is superseded prose; `42-CONTEXT.md` D-01 is authoritative.

**n8n independence.** Do NOT run `scripts/build_cloud_workflows.py` or
`scripts/deploy_n8n_workflows.py`. Importing constants from `build_cloud_workflows` as a module
(as several existing tests do) is fine; invoking its generation path rebakes the deployed
workflow write-safety constants and would silently close Phase 41's live import arm window.

Purpose: make ROADMAP SC2's "zero drift between the config file and HubSpot's actual schema" a
statement with a defined and provable meaning, which it does not have against a partial file.
Output: an expanded manifest, an updated test suite that still protects what it was written to
protect, and a committed reconciled drift report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-CONTEXT.md
@.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-RESEARCH.md
@.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-01-SUMMARY.md
@config/hubspot_properties.yaml
@tests/test_hubspot_properties_config.py
@scripts/sync_hubspot_properties.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the manifest to a full mirror, sourced only from the committed live snapshot</name>
  <files>config/hubspot_properties.yaml</files>
  <precondition>`config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json` and its contacts sibling exist and are committed — plan 01 Task 1 produced them. This plan reads live shape from those files and from no other source.</precondition>
  <read_first>
    - `config/hubspot_properties.yaml` — the file being expanded. Note the two top-level object sections, each with a `groups:` list and a `properties:` list, and the six per-property keys: `name`, `label`, `type`, `fieldType`, `groupName`, `options`. Lines 6-35 show the enumeration shape with a populated `options` list; lines 150-163 the bool shape; lines 192-197 the string shape with `options: []`.
    - `config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json` — the live oracle. Each entry under `results` carries `name`, `label`, `type`, `fieldType`, `groupName`, `options`, `hubspotDefined`, `calculated`, and `calculationFormula` where applicable.
    - `.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json` — the per-name status list from plan 01; `missing_from_yaml` names the entries to add, `documented_gap` names the ones that must NOT be added.
    - `scripts/sync_hubspot_properties.py` lines 72-98 — `compute_property_diff` and `compute_group_diff`, the only portal-writing consumer of this file. Understanding that a live-existing property is never proposed for creation is what makes this expansion safe.
  </read_first>
  <reversibility rating="reversible">A version-controlled config file; `git revert` restores the partial manifest at zero cost.</reversibility>
  <action>
Expand the `companies.properties` list in `config/hubspot_properties.yaml` to cover the D-04
scope. Work strictly from the two committed inputs: take the candidate name list from the
`missing_from_yaml` entries of `drift-report-phase42-pre.json`, and take every field value for
each entry from the matching `results[]` object in
`config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json`.

For each candidate name:

If the drift report status is `missing_from_yaml`, add an entry carrying exactly the six keys
the file already uses — `name`, `label`, `type`, `fieldType`, `groupName`, `options` — each
value copied verbatim from the live snapshot. Where the live property has no options, write
`options: []` to match the existing convention. Do not add a seventh key: `calculated`,
`calculationFormula`, `hubspotDefined`, `displayOrder`, and `description` are live-API fields
the manifest's shape does not carry, and the fit-score formula's fidelity is already pinned
independently by `tests/test_flow_rubric_conformance.py`.

If the drift report status is `documented_gap`, add nothing. These are the design-only names.
They are recorded in the drift report and in this plan's summary as declared in the superseded
local-MVP design and never implemented live — out of scope for a reconciliation that performs
no portal mutation.

Two live values must be copied, not guessed:

`groupName`. The ICP output properties live under a HubSpot-native group, not under the yaml's
own `lv_enrichment` group. Copy each entry's live `groupName` exactly. Whatever that value is,
**do not add it to the yaml's `groups:` list** if it is a native HubSpot group — that list
exists solely to tell `compute_group_diff` which groups this project's own tooling would need
to create, and declaring a native group there is the one path by which this expansion could
cause a portal write. The five `*_score` properties' live group was unknown before plan 01 and
must be read from the snapshot, not assumed to match the ICP outputs'.

`options`. Copy the live option list, preserving each option's `label`, `value`, `displayOrder`,
and `hidden`. Drop the live-only `description` key, which the manifest shape does not carry and
which D-06 scopes out of the comparison. For enumerations, the option `value` set is the thing
the drift comparator checks, so an inexact copy here is the failure this task most plausibly
produces. After writing, verify each new enumeration entry's value set against the snapshot with
a parsed assertion rather than by eye.

Pre-existing entries are in scope for correction, not only for reading. If
`drift-report-phase42-pre.json` reports `enum_mismatch` or `type_mismatch` on any entry that was
already in the manifest before this task, correct that entry from the live snapshot in this same
task — D-05 makes live the winner and the yaml the thing that moves, and the task's own verify
asserts equality across every declared entry, not only the newly added ones.

Do not touch the `contacts` section unless the drift report shows a `missing_from_yaml` entry
for a contact property. D-04 scopes the expansion to company scoring properties; HubSpot-native
firmographic fields stay out entirely.

Record in the plan summary: the final company and contact property counts, the names added, and
the names deliberately not added with the live evidence for each.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json,yaml
cfg=yaml.safe_load(open('config/hubspot_properties.yaml'))
snap=json.load(open('config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json'))
live={p['name']:p for p in snap['results']}
declared={p['name']:p for p in cfg['companies']['properties']}
missing_live=[n for n in declared if n not in live]
assert not missing_live, f'yaml declares properties absent from the live snapshot: {missing_live}'
for n,d in declared.items():
    l=live[n]
    assert d['type']==l['type'] and d['fieldType']==l['fieldType'], (n,d['type'],d['fieldType'],l['type'],l['fieldType'])
    assert d['groupName']==l['groupName'], (n,d['groupName'],l['groupName'])
    assert {str(o['value']) for o in (d['options'] or [])}=={str(o['value']) for o in (l.get('options') or [])}, n
native=[g for g in cfg['companies']['groups'] if g['name'] not in ('lv_enrichment',)]
assert not native, f'yaml groups list gained a non-project group: {native}'
print('companies mirrored:',len(declared),'contacts:',len(cfg['contacts']['properties']))
"</automated>
  </verify>
  <acceptance_criteria>
    - Every property declared in `config/hubspot_properties.yaml` for companies exists in `portal-schema-companies-phase42-pre.json`'s `results[].name` set — zero fabricated entries.
    - For every declared company property, `type`, `fieldType`, and `groupName` equal the live snapshot's values, and the enum option `value` sets are equal.
    - None of `lv_icp_confidence`, `lv_recommended_motion`, `lv_icp_scored_at`, `lv_icp_scoring_version`, `lv_named_account_priority` appear in the yaml unless the plan-01 snapshot shows them live; the plan summary states which live-existence answer applied to each.
    - `config/hubspot_properties.yaml`'s `companies.groups` list still contains exactly the one project-owned group it had before; no HubSpot-native group was added to it.
    - Each added entry carries exactly the six keys already used by the file — no `calculated` / `calculationFormula` / `description` / `hubspotDefined` keys were introduced.
  </acceptance_criteria>
  <done>The manifest mirrors live for every in-scope company property that exists live, and declares nothing that does not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the four offline guards whose premises D-04 inverts, preserving each guard's intent</name>
  <files>tests/test_hubspot_properties_config.py</files>
  <read_first>
    - `tests/test_hubspot_properties_config.py` — the file being edited, in full. The four tests to change are at lines 45-51, 54-59, 95-101, and 104-118.
    - `config/hubspot_properties.yaml` — as expanded by Task 1; the new entries are what break the four guards.
    - `.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-RESEARCH.md` section "What Consumes the yaml" — the enumeration of which guards break and why.
    - `scripts/sync_hubspot_properties.py` lines 96-98 — `compute_group_diff`, which is the concrete reason the group-declaration guard exists at all.
  </read_first>
  <behavior>
    - The naming guard still fails when a property name outside a named, justified exemption set lacks the project namespace prefix.
    - The type/fieldType guard still fails on a pair HubSpot's Properties API would reject.
    - The group guard still fails when a property references a group that is neither declared in the yaml nor a named HubSpot-native group.
    - The count guard still fails when the manifest's length changes without a deliberate edit.
    - No guard silently accepts an entry the portal does not have.
  </behavior>
  <action>
Update `tests/test_hubspot_properties_config.py`. Four guards break under the expanded manifest.
Each gets a scoped, deliberate change with the guard's protective intent restated in a comment —
this is a documented supersession of the file's original create-only contract, mirroring how
`42-CONTEXT.md` documents D-01's supersession of ROADMAP SC1. **No assertion is simply deleted.**

Guard one, the namespace-prefix rule at lines 45-51. It was protecting against a
wrongly-namespaced property being invented in this manifest and then created live under a name
that collides with HubSpot's own or another integration's. Extend the existing
`_PN1_EXEMPT_NAMES` set with the five component score names, and add a comment recording that
these five were created directly against the portal during Phase 40's remediation with names
chosen by the flows that write them, that they are the live scoring engine's terms rather than
project-namespaced enriched fields, and that the manifest is now mirroring an existing live
name rather than proposing a new one. The rule still fires for any name outside the exemption
set.

Guard two, the type/fieldType pair rule at lines 54-59. It was protecting against a pair the
Properties API would reject at create time, which is a live 400 nobody sees until a migration
runs. Add the pair used by the live calculated fit-score property to
`VALID_TYPE_FIELDTYPE_PAIRS`, with a comment recording that this is a calculated-property pair
proven live in this portal and read from the plan-01 snapshot. Every other pair stays rejected.

Guard three, the group-declaration rule at lines 95-101. It was protecting against a typo'd
`groupName` that `compute_group_diff` would then try to create as a brand-new group on the
portal. That protection is still needed and still fires — but a HubSpot-native group is a valid
`groupName` that must never be declared in the yaml's own `groups:` list, so the rule needs a
second accepted source. Introduce a module-level frozenset of native group names accepted
without declaration, populate it from the live `groupName` values the plan-01 snapshot shows for
the newly-mirrored properties, and assert `groupName` is in the declared set or in that
frozenset. Comment records that a native group must never enter `groups:` because
`compute_group_diff` would then propose creating it, and that this frozenset is the deliberate
alternative.

Guard four, the exact-count tripwire at lines 104-118. This test exists to catch accidental
manifest drift and is expected to need its numbers changed by any deliberate expansion. Update
both counts to the manifest's new lengths. Separately, the sibling test at lines 112-118 asserts
the *absence* of two properties D-04 requires present, and its docstring states the plan
explicitly omits them. That premise is what D-04 overturns. Replace the test body rather than
deleting the test: assert both names are now present AND that each one's declared shape matches
the live snapshot. The docstring records that the original create-only premise is superseded by
`42-CONTEXT.md` D-04, that the manifest is now a full mirror, and that the replacement assertion
protects the stronger property — presence with live-matching shape — instead of mere absence.

If the expansion breaks a fifth guard not named here, fix it the same way: state what it was
protecting, and show the new form still protects it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_hubspot_properties_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_hubspot_properties_config.py -x -q` passes with no test count reduction versus the pre-edit file.
    - `.venv/bin/python -c "
import re
src=open('tests/test_hubspot_properties_config.py').read()
assert src.count('def test_')>=14, src.count('def test_')
assert 'def test_every_property_name_is_lv_prefixed' in src
assert 'def test_every_type_fieldtype_pair_is_valid' in src
assert 'def test_every_groupname_is_a_declared_group' in src
assert 'def test_exact_counts_guard_against_manifest_drift' in src
print('all four guards still present')"` passes — the guards were amended, not removed.
    - The count assertions equal the actual lengths of the expanded lists (confirmed by the test passing, not by a number hardcoded in this plan).
    - A module-level frozenset of natively-defined group names exists and the group guard consults it, while `config/hubspot_properties.yaml`'s own `groups:` list is unchanged.
    - Each of the four amended guards carries a comment naming what it protects and why the new form still protects it.
  </acceptance_criteria>
  <done>All four amended guards pass, each still fails on the condition it was written to catch, and each records its supersession rationale in-file.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the expansion reconciles live and cannot cause a portal write</name>
  <files>.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json</files>
  <precondition>Live credentials for portal 22617666 are available to the operator's shell via `.env`; Claude cannot read `.env`, so both live commands in this task are operator-pasted.</precondition>
  <read_first>
    - `scripts/check_schema_drift.py` — the tool being re-run; its `--out` argument and its 0/1/2 exit-code semantics.
    - `scripts/sync_hubspot_properties.py` lines 50-61 and 216-250 — the two-key write gate (`DRY_RUN=false` AND `ALLOW_HUBSPOT_PROPERTY_WRITES=true`) and the default dry-run report path; this is the only consumer of the yaml with a portal write path, and this task's whole point is showing it proposes nothing.
    - `.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json` — the before state to compare the new report against.
  </read_first>
  <action>
Re-run the drift checker live and capture the reconciled report, then run the property sync tool
in its default dry-run mode to prove the expanded manifest cannot cause a portal mutation.

Hand the operator these two commands, in order, and have them paste the output back.

The reconciled drift check:

`!.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['check_schema_drift.py', '--out', '.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json']; runpy.run_path('scripts/check_schema_drift.py', run_name='__main__')"`

The sync dry-run. Neither write key is set, so the tool prints its diff and returns without
writing — this is its documented default, not a flag being trusted:

`!.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['sync_hubspot_properties.py']; runpy.run_path('scripts/sync_hubspot_properties.py', run_name='__main__')"`

The reconciled report must show zero entries with status `missing_from_yaml`, `fabricated_entry`,
`enum_mismatch`, or `type_mismatch`, and must still show `do_not_archive.ok` true. Statuses
`cosmetic_only` and `documented_gap`, and the seeded accepted divergence, are expected and do
not fail the run. If the exit code is not 0, do not adjust the report or the comparator — fix
the yaml entry the report names, since D-05 makes live the winner and the yaml the thing that
moves.

The sync dry-run output must report zero properties to create and zero groups to create for both
object types. A non-empty property-create list means the yaml declares something the portal does
not have, which is the fabricated-entry failure in a second, independent form. A non-empty
group-create list means a native group leaked into the yaml's `groups:` list — remove it.

Commit the reconciled report. Record both outputs verbatim in the plan summary; the sync
dry-run's zero-creates line is this phase's evidence that D-05's no-portal-mutation guarantee
survived the expansion.

Finish by running the full offline suite and confirming no regression.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/python -c "
import json
r=json.load(open('.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json'))
assert r['do_not_archive']['ok'] is True, r['do_not_archive']
bad=[p for p in r['properties'] if p['status'] in ('missing_from_yaml','fabricated_entry','enum_mismatch','type_mismatch')]
assert not bad, bad
assert r['exit_code']==0, r['exit_code']
print('reconciled clean; documented gaps:',[p['name'] for p in r['properties'] if p['status']=='documented_gap'])"</automated>
  </verify>
  <acceptance_criteria>
    - `drift-report-phase42-reconciled.json` exists, is committed, and has `exit_code == 0`.
    - That report contains zero properties with status `missing_from_yaml`, `fabricated_entry`, `enum_mismatch`, or `type_mismatch`, and `do_not_archive.ok == true`.
    - The `sync_hubspot_properties.py` dry-run output shows `Properties to create (0): []` and `Groups to create: []` for both `companies` and `contacts`, and both outputs are quoted verbatim in the plan summary.
    - `.venv/bin/python -m pytest -q` reports zero failures and zero errors.
    - `git status --porcelain n8n/` is empty — no workflow content was regenerated.
    - Every name the reconciled report lists as `documented_gap` is recorded in the plan summary with the live evidence that it does not exist.
  </acceptance_criteria>
  <done>The expanded manifest reconciles clean against the live portal, and the only tool that could write properties proposes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/hubspot_properties.yaml` → `scripts/sync_hubspot_properties.py` → portal | The one path by which a config-file edit can become a live portal mutation. This plan's Task 3 exists to prove that path stays inert. |
| Claude/script → HubSpot portal 22617666 | Crossed twice in this plan, GET only. |
| Committed live snapshot → yaml entry | The only sanctioned source of live shape; the superseded CLAUDE.md design list is explicitly not a source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Tampering | Yaml entry fabricated from the superseded design list | high | mitigate | Task 1's automated verify asserts every declared company property exists in the committed live snapshot; the drift comparator independently classifies any such entry as `fabricated_entry` and returns exit 1; Task 3 asserts zero. |
| T-42-08 | Elevation of Privilege | Native group declared in the yaml's `groups:` list, causing `compute_group_diff` to propose creating a portal group | high | mitigate | Task 1's verify asserts the `groups:` list is unchanged; Task 2's group guard accepts native groups via a separate frozenset rather than by declaration; Task 3 asserts the sync dry-run proposes zero group creates. |
| T-42-09 | Tampering (silent) | A guard test weakened rather than amended, hiding a real defect class | medium | mitigate | Task 2's acceptance criteria assert all four guard function names still exist and the test count did not shrink; each amended guard must carry an in-file comment naming what it still protects. |
| T-42-10 | Denial of Service (of Phase 41) | n8n workflow content rebake | high | mitigate | This plan never invokes the builder or deployer; Task 3 asserts `git status --porcelain n8n/` is empty. |
| T-42-11 | Information Disclosure | Live property bodies committed to git | low | accept | The property schema is not secret and contains no credentials; `_assert_no_secrets()` in the drift script already covers the one place a portal response body is serialized to disk. |

No package-manager installs occur in this plan, so no supply-chain row applies.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` green.
- `drift-report-phase42-reconciled.json` committed with `exit_code == 0` and `do_not_archive.ok == true`.
- `sync_hubspot_properties.py` dry-run shows zero property creates and zero group creates for both object types.
- `git status --porcelain n8n/` empty.
</verification>

<success_criteria>
- `config/hubspot_properties.yaml` mirrors live for every in-scope property that exists live, at
  D-06 depth (existence plus enum option values), and declares nothing the portal lacks.
- The five design-only names are recorded as documented gaps with live evidence, not invented
  into the manifest (F2 resolved).
- All four broken offline guards are amended with their protective intent preserved and stated
  in-file, none deleted (F3 resolved).
- The expansion is proven incapable of causing a portal write (D-05).
</success_criteria>

<output>
Create `.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (plan 02)

**Files modified**
- `config/hubspot_properties.yaml` — companies `properties` list expanded to the D-04 mirror;
  `groups` list unchanged
- `tests/test_hubspot_properties_config.py` — four guards amended

**Files created**
- `.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-reconciled.json`

**Symbols in `tests/test_hubspot_properties_config.py`**
- `_PN1_EXEMPT_NAMES` — extended with the five component score property names
- `VALID_TYPE_FIELDTYPE_PAIRS` — extended with the live calculated-property pair
- a new module-level frozenset of natively-defined group names accepted without yaml declaration
- `test_every_property_name_is_lv_prefixed`, `test_every_type_fieldtype_pair_is_valid`,
  `test_every_groupname_is_a_declared_group`, `test_exact_counts_guard_against_manifest_drift` —
  amended, all four still present
- `test_lv_org_type_and_lv_produces_content_not_listed_for_creation` — body replaced with a
  presence-plus-live-shape assertion; docstring records the D-04 supersession