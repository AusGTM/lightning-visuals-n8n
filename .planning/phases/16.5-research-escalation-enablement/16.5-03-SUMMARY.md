---
plan: 16.5-03
status: blocked
completed: 2026-07-28
blocker: "BUG 10 — n8n HubSpot node company:search returns an item with null json"
---

# Phase 16.5 Plan 03 — LIVE companies run — BLOCKED by a new live-only bug

Plan 03 exists to verify the **literal** `bd682a2` (`Merge Company` on the companies branch), one
step beyond ROADMAP criterion 7. It was attempted with the enabled build and is **blocked**. The
companies research lane remains **live-unverified**.

## Pre-flight — GO

Company `9604614548` (Melbourne Racing Club, `mrc.racing.com`), fetched read-only:
`lv_org_type` BLANK, `lv_produces_content` BLANK, `lv_enrichment_provenance` BLANK,
`hs_lastmodifieddate` `2026-07-28T03:42:15.843Z` (zero-write oracle).

Companies gate is `orgUnresolved || contentBlank`; `lv_org_type` blank ⇒ **GO**.

## What happened

Execution 10, `status: success`, zero error nodes — but only **18** nodes ran, terminating at
`Normalize + Score Company`. `Research Trigger Gate`, `Claude Web Research` and `Merge Company`
never executed.

Trace:

| node | observation |
|---|---|
| `HubSpot Company Fetch By Id` | 1 output item whose **`json` is `null`** |
| `Adapt Company Fetch By Id` | `fetch_diagnostic: "error: unrecognized response shape"`, `lookup_failed: true`, empty `existingRecord`, `identity_keys {domain:null, companyName:null}` |
| `Company Gate` | `gate.action: "create"` (reason "no existing record") → **overridden to `skip`** by the `lookup_failed` guard |
| `Normalize + Score Company` | reads `$('Company Gate').all().filter(action !== "skip")` → **0 rows** → emits 0 items |
| everything downstream | never runs |

## BUG 10 — `company:search` returns null json

The failure is in the node, not the API and not our inputs:

- `Build Company Identity` produced the correct `object_id` `'9604614548'`.
- The identical search issued directly against HubSpot — same `hs_object_id EQ` filter, same
  14-property list read out of the built node — returns **HTTP 200, `total:1`**, with the real
  record (`domain: mrc.racing.com`, `annualrevenue: 206078000`, …).
- With `onError` temporarily cleared, the node **still did not throw**: `status: success`, no error
  node, no message. It simply emits an item with `json: null`.
- The contacts twin (`HubSpot Fetch By Id`, structurally identical config, `resource: contact`)
  works correctly and returns `{archived, createdAt, id, properties, updatedAt, url}`.

So the difference is `resource: company` in n8n's own HubSpot node implementation. Note that
`HubSpot Company Search` (the pre-existing companies search) shares this shape and has **also never
run live** — every canary to date fired contact events only, so it is likely affected identically.

**Candidate fix**, already scoped by 16.4's research and requiring no new adapter work: replace the
two companies search nodes with credential-bound `httpRequest` POSTs to
`/crm/v3/objects/companies/search`. `n8n/code/adaptFetchById.js` already parses that response shape
(its unit tests cover the single-object and search-envelope forms), so the change is confined to the
node type/parameters. This needs its own planned phase — it was NOT hand-patched here.

## Safety — verified, nothing left armed

- Write nodes executed: **NONE**.
- Company `9604614548` `hs_lastmodifieddate` **unchanged** (`2026-07-28T03:42:15.843Z`),
  `lv_org_type` still null.
- Contact 201 `lastmodifieddate` **unchanged** (`2026-07-18T01:14:03.751Z`).
- Deployment restored from committed source: both flags read back `['false','false']`,
  `onError: continueRegularOutput` restored on **both** fetch nodes, workflow still active.
- Credits unchanged.

## Correction recorded

An intermediate check in this session reported `Merge Company` "merge is null? True" and briefly
looked like a reproduction of `bd682a2`. It was not: `Merge Company` never executed, and the helper
returned `None` for an absent node, which the check conflated with a null merge. The real finding is
the truncation above. Recorded because the distinction matters — an absent node and a null merge
have completely different causes.
