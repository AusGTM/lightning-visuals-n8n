---
phase: 51-backfill-pipeline-credit-sizing-dry-run
plan: 03
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - scripts/scored_population_snapshot.py
  - tests/test_scored_population_snapshot.py
  - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json
  - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md
  - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-VALIDATION.md
autonomous: false
requirements: [SAFE-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "51-BEFORE-SNAPSHOT.json records every already-scored company by id, in ascending id order, with its full scoring input, component, veto and derived-tier property values as read live — the read-only baseline the milestone's closing diff is taken against."
    - "The snapshot's population is derived live by the same HAS_PROPERTY(lv_icp_fit_score) definition three existing scripts already share, via the imported select_scored_population, never a fourth inline definition."
    - "The snapshot run refuses rather than truncates: if the live scored population exceeds one search page, the run raises naming the page limit instead of writing a partial baseline."
    - "The snapshot's recorded population count and the never-scored population count in 51-SIZING.md are disjoint sets whose relationship to the portal total is stated in the artifact."
    - "COVERAGE.md decides every enumerated ZoomInfo GTM and HubSpot CRM v3 capability as INTEGRATE or OPT-OUT, and every OPT-OUT carries a one-line reason."
    - "51-VALIDATION.md's per-task verification map names an automated command for every task in plans 01-03, with no three consecutive tasks lacking one."
    - "The phase ends with the operator's explicit recorded approval of the committed dry-run artifacts; no write window in this milestone opens before it."
  prohibitions:
    - "The snapshot script performs no HubSpot write of any kind; it reads and serializes only."
    - "No n8n execution, webhook POST or recompute request is issued anywhere in this plan."
    - "The already-scored population is not modified, re-scored or re-enriched by this phase."
    - "The approval checkpoint is never auto-approved; the phase does not advance on an assumed go-ahead."
    - "No task reads the repository dotenv file through a shell command or a file-read tool."
  artifacts:
    - scripts/scored_population_snapshot.py
    - tests/test_scored_population_snapshot.py
    - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json
    - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md
    - .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-VALIDATION.md
  key_links:
    - "scored_population_snapshot imports select_scored_population from scripts.rescore_population — the same population definition, not a fourth copy."
    - "51-BEFORE-SNAPSHOT.json is the baseline Phase 52's closing diff reads; its id set and property list are its contract."
    - "The approval checkpoint gates Phase 52 structurally: Phase 51 has no write path, so approval cannot be bypassed by an in-phase write."
---

<objective>
Close Phase 51: capture the read-only before-snapshot of the already-scored companies that the
milestone's closing safety diff will be taken against, decide the external-API coverage matrix,
fill the phase validation contract, and stop at the operator's explicit approval of every committed
dry-run artifact.

Purpose: the already-scored population is deliberately excluded from this milestone's write set
(D-01), and "we did not touch them" is only provable against a baseline captured before anything
was written. Capturing it here — in the phase that structurally cannot write — is what makes the
baseline trustworthy.

Output: `scripts/scored_population_snapshot.py`, `51-BEFORE-SNAPSHOT.json`, `COVERAGE.md`, a filled
`51-VALIDATION.md`, and a recorded operator approval.

Scope note: the closing re-verification against this baseline, and the D-06 staged execution it
guards, are Phase 52's work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-RESEARCH.md
@.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-02-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Artifact | Symbols / contents |
|---|---|
| `scripts/scored_population_snapshot.py` | `EXPECTED_PORTAL_ID`, `SNAPSHOT_PROPS`, `_has_credentials()`, `_portal_ok()`, `capture_snapshot()`, `main()`, CLI flag `--out` |
| `tests/test_scored_population_snapshot.py` | `test_snapshot_shape_and_ordering`, `test_snapshot_refuses_truncated_population`, `test_snapshot_uses_shared_population_definition`, `test_snapshot_is_read_only` |
| `.planning/phases/51-.../51-BEFORE-SNAPSHOT.json` | ascending-id records of every already-scored company with all `SNAPSHOT_PROPS` values, plus run metadata |
| `.planning/phases/51-.../COVERAGE.md` | ZoomInfo GTM + HubSpot CRM v3 capability matrix, every row decided |
| `.planning/phases/51-.../51-VALIDATION.md` | filled test-infrastructure, sampling-rate, per-task verification map and sign-off |

Created by plan 01: `scripts/zoominfo_company_client.py`, `scripts/backfill_dry_run.py`,
`tests/test_zoominfo_company_client.py`, `tests/test_backfill_dry_run.py`, `51-TRACER-DRYRUN.json`.
Created by plan 02: `51-SIZING.md`, `51-DRYRUN-PREDICTIONS.json`, `51-SKIP-LOG.json`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the read-only before-snapshot of the already-scored population</name>
  <files>scripts/scored_population_snapshot.py, tests/test_scored_population_snapshot.py, .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json</files>
  <precondition>`HUBSPOT_PRIVATE_APP_TOKEN` and `HUBSPOT_PORTAL_ID` resolve from the repo-root dotenv via `load_dotenv()` inside the Python process, and `HUBSPOT_PORTAL_ID` equals `22617666`.</precondition>
  <read_first>
    - `scripts/rescore_population.py` (lines 95-145) — `select_scored_population`, `POPULATION_SEARCH_LIMIT` and the refuse-rather-than-truncate contract to import rather than restate
    - `scripts/canary_record_snapshot.py` (lines 60-180) — `_has_credentials`, `_portal_ok`, `_capture_record`, `build_snapshot`, `_write_snapshot`: the existing snapshot shape to mirror
    - `src/hubspot_client.py` (lines 16-24, 119-129) — `get_record`, `search_records`
    - `config/hubspot_properties.yaml` — the `lv_icp_tier_derived` entry and the five numeric component entries, for the exact live property names
    - `scripts/check_tier_derived_parity.py` (lines 1-60) — the D-07 parity gate's own property list, so the snapshot captures everything that gate reads
    - `.planning/MILESTONE-CONTEXT.md` — D-01 verbatim (why the scored population is excluded)
  </read_first>
  <behavior>
    - Given a mocked scored population of three ids, the snapshot writes three records in ascending
      id order, each carrying every name in `SNAPSHOT_PROPS`.
    - Given a mocked search whose reported total exceeds the returned page, the run raises rather
      than writing a partial baseline.
    - No code path in the module reaches `patch_record`, `batch_update_companies` or
      `create_record`.
  </behavior>
  <action>
Create `scripts/scored_population_snapshot.py`, a read-only snapshot driver. Copy the header
discipline of `scripts/canary_record_snapshot.py`: repo root on `sys.path`,
`EXPECTED_PORTAL_ID = "22617666"` hard-coded with no env override, `_has_credentials()` and
`_portal_ok()` asserted before any network call, a clean printed skip returning 0 when credentials
are absent and a printed refusal returning 1 on a portal mismatch. Docstring states the module is
read-only, must be run from the repo root, and carries the standard `load_dotenv()` +
`runpy.run_path` operator invocation.

Import `select_scored_population` from `scripts.rescore_population` — do NOT write a fourth inline
`HAS_PROPERTY(lv_icp_fit_score)` search. That function already carries the refuse-rather-than-
truncate guard this snapshot needs: if the live scored population has grown past one page, a
partial baseline is worse than no baseline, because Phase 52's closing diff would silently pass
over the records it omitted.

`SNAPSHOT_PROPS` is the explicit property list, in this order: the six scoring inputs
(`lv_org_type`, `lv_produces_content`, `lv_country_region_normalized`, `lv_revenue_band`,
`lv_is_gambling_operator`, `lv_is_hardware_vendor`), the five component scores (`org_type_score`,
`geography_score`, `annual_revenue_score`, `produces_content_score`, `gambling_score`), the veto
pair (`lv_anti_icp_flag`, `lv_anti_icp_flag_num`) and `lv_anti_icp_reason`, the two calculated
outputs (`lv_icp_fit_score`, `lv_icp_tier_derived`), plus `name` and `domain` for human
readability. Reading a calculated property is free and safe — the exclusion in D-01 is on writing,
not reading — and Phase 52's closing diff needs the derived tier in the baseline to prove it did
not move.

`capture_snapshot() -> dict` returns `{"captured_at":..., "portal_id_verified":...,
"population_definition": "HAS_PROPERTY(lv_icp_fit_score)", "population_count": len(ids),
"records": [...]}` with `records` sorted by ascending numeric id, each entry
`{"id":..., "properties": {name: value for every name in SNAPSHOT_PROPS}}`. `main()` parses `--out`
and writes pretty-printed JSON with sorted keys so two captures of an unchanged portal produce a
line-comparable diff.

Then create `tests/test_scored_population_snapshot.py` in the plain-pytest style of
`tests/test_icp_scoring.py`. `test_snapshot_shape_and_ordering` monkeypatches the population
selection and `get_record` and asserts ascending-id ordering and that every record carries every
name in `SNAPSHOT_PROPS`. `test_snapshot_refuses_truncated_population` monkeypatches
`search_records` to report a total larger than the returned page and asserts the imported selection
raises with the page limit named, and that no snapshot file was written.
`test_snapshot_uses_shared_population_definition` asserts object identity:
`scored_population_snapshot.select_scored_population is
scripts.rescore_population.select_scored_population`. `test_snapshot_is_read_only` asserts by
source inspection that the module's text contains none of the three HubSpot mutation function
names, so a future edit cannot quietly add a write path to a file whose whole purpose is being a
trustworthy baseline.

Finally run it live from the repo root with the documented invocation and `--out` at
`.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json`, and commit.
This issues HubSpot read calls only: one search plus one read per scored record. Record the
captured count in the summary and state its relationship to the never-scored count in
`51-SIZING.md` — the two populations are disjoint by construction (one has the fit score, the other
does not), and stating both together makes an accidental overlap visible immediately.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_scored_population_snapshot.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_scored_population_snapshot.py -x` exits 0
    - `grep -c 'from scripts.rescore_population import select_scored_population' scripts/scored_population_snapshot.py` returns 1
    - `grep -c 'patch_record\|batch_update_companies\|create_record' scripts/scored_population_snapshot.py` returns 0
    - `grep -c 'EXPECTED_PORTAL_ID = "22617666"' scripts/scored_population_snapshot.py` returns 1
    - `.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json` exists, parses as JSON, and its `population_count` is a non-zero integer equal to `len(records)`
    - The artifact's `records` ids are in ascending numeric order and every record's `properties` contains all eighteen `SNAPSHOT_PROPS` names (6 scoring inputs + 5 component scores + `lv_anti_icp_flag` + `lv_anti_icp_flag_num` + `lv_anti_icp_reason` + `lv_icp_fit_score` + `lv_icp_tier_derived` + `name` + `domain`)
    - `grep -c 'HAS_PROPERTY' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json` returns 1 or more (the population definition is stated, not just its result)
    - `grep -ci 'bearer\|pat-na' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json` returns 0
    - `git log --oneline -1 -- .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-BEFORE-SNAPSHOT.json` shows a commit
  </acceptance_criteria>
  <done>Every already-scored company's full property state is committed, read-only, in ascending id order, as the baseline the milestone's closing safety diff is taken against — captured in a phase with no write path, so it cannot have been influenced by a write.</done>
  <reversibility rating="reversible">Read-only HubSpot calls; new files only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Reconcile the API coverage matrix and validation contract against what was actually built</name>
  <files>.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md, .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/50-derived-tier-property/COVERAGE.md` — the exact matrix format and opt-out reason style to copy
    - `.planning/phases/49-re-score-strategy-reporting/49-VALIDATION.md` — a filled validation contract to model
    - `.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-RESEARCH.md` — the "Validation Architecture" section's framework table, requirements-to-test map and sampling rate
    - `.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-01-PLAN.md` and `51-02-PLAN.md` — every task's automated verify command, to transcribe into the map
    - `scripts/build_cloud_workflows.py` (lines 1670-1760) — the ZoomInfo GTM company surface actually reachable, for enumerating capabilities honestly
  </read_first>
  <action>
Both `COVERAGE.md` and `51-VALIDATION.md` were WRITTEN AT PLAN TIME and are already committed —
`COVERAGE.md` because the api-coverage gate requires a decided matrix before the plan is finalized,
and `51-VALIDATION.md` because it is the sampling contract waves 1 and 2 execute against. This task
reconciles both against what was actually built; it does not author them from scratch.

For `COVERAGE.md`: re-read every row against the code as shipped. If plans 01-02 reached a ZoomInfo
GTM or HubSpot CRM v3 capability the matrix marks `OPT-OUT`, that is either a scope leak to fix in
the code or a decision to flip to `INTEGRATE` with a reason — resolve it explicitly, never leave the
matrix disagreeing with the source. Confirm in particular that no HubSpot write capability moved to
`INTEGRATE` (they are all opted out with "Phase 52; Phase 51 is dry-run only"), and that the
pagination note at the foot still describes the shipped `search_records` usage. Cross-check by
grepping the three new scripts for every endpoint path the matrix names.

For `51-VALIDATION.md`: verify each of the nine per-task rows' automated command actually runs and
passes against the built tests, and correct any command whose test name drifted during
implementation. Flip each row's Status column from pending to green as its command passes. Update
the estimated-runtime figures with the real measured times. Leave `status: draft` and
`nyquist_compliant: false` — the plan writes the contract, `/gsd-validate-phase` §6 signs it.

If either file needed no change, say so explicitly in the summary rather than silently reporting
the task done — "the as-built matched the plan-time matrix" is the finding, and it is worth stating.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md && grep -c 'OPT-OUT' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md` exists and contains both a ZoomInfo GTM section and a HubSpot CRM v3 section
    - Every table row in COVERAGE.md carries either `INTEGRATE` or `OPT-OUT`, and no row is blank or marked TBD
    - `grep -c 'OPT-OUT |$' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md` returns 0 (no OPT-OUT row has an empty reason cell)
    - `grep -c 'Phase 52' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/COVERAGE.md` returns 1 or more (the write-surface opt-outs name their destination phase)
    - `51-VALIDATION.md` contains no remaining `{` placeholder braces
    - `51-VALIDATION.md`'s per-task map has one row per task in plans 01-03 (nine rows) and every row names an automated command (the checkpoint row excepted, which points at Manual-Only Verifications)
    - `grep -c 'node --test tests/n8n/\*.test.mjs' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-VALIDATION.md` returns 1 or more (glob form, not directory form)
    - `grep -c 'wave_0_complete: true' .planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-VALIDATION.md` returns 1
    - Every automated command in the per-task map has been run and passes; each row's Status column reads green
    - Every endpoint path named in `COVERAGE.md` as `INTEGRATE` is reachable in `scripts/zoominfo_company_client.py`, `scripts/backfill_dry_run.py` or `scripts/scored_population_snapshot.py`, and no path marked `OPT-OUT` appears in any of the three
  </acceptance_criteria>
  <done>The plan-time coverage matrix and validation contract have been reconciled against the shipped code, every automated command in the map has been run and passes, and any divergence between the matrix and the source has been resolved explicitly rather than left standing.</done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator approval of the dry-run artifacts — the phase exit gate</name>
  <what-built>
Phase 51 produced a zero-write backfill pipeline and five committed artifacts:

1. `51-TRACER-DRYRUN.json` — the one-record live proof, with the ZoomInfo per-match credit cost
   measured by bracketing the call with two balance reads (replacing the inferred 1.08 figure).
2. `51-SIZING.md` — live credit balance, measured per-match cost, derived cap, live never-scored
   population count via `NOT_HAS_PROPERTY(lv_icp_fit_score)`, chosen sample size, and the Anthropic
   research-cost estimate labelled as a prior-pipeline figure not measured for this call pattern.
3. `51-DRYRUN-PREDICTIONS.json` — one row per matched sample company: the exact PATCH payload (six
   `lv_*` scoring inputs plus `org_type_score`, `geography_score`, `annual_revenue_score`,
   `produces_content_score`, `gambling_score`, `lv_anti_icp_flag_num`) and its pre-registered
   predicted `lv_icp_tier_derived`, derived from the oracle's score and veto flag via the live
   four-branch formula.
4. `51-SKIP-LOG.json` — one reasoned entry per company ZoomInfo could not match, carrying no
   payload.
5. `51-BEFORE-SNAPSHOT.json` — the read-only baseline of every already-scored company, for the
   milestone's closing safety diff.

Zero HubSpot writes and zero n8n executions occurred. Credits spent: the tracer's one plus the
sample's, both inside the cap recorded in `51-SIZING.md`.
  </what-built>
  <how-to-verify>
1. Open `51-SIZING.md`. Confirm the credit cap is sized against a balance you find plausible, that
   the never-scored population count is close enough to the ~646 estimate to be believable (and if
   not, that the divergence is noted rather than reconciled away), and that the Anthropic figure
   reads as an estimate.
2. Open `51-DRYRUN-PREDICTIONS.json`. Spot-check three rows against the companies you know:
   - Does `lv_revenue_band` look right in DOLLARS? A company you know to be mid-sized must not read
     one band low — that is the thousands-conversion landmine.
   - Does `lv_country_region_normalized` read `AU`/`NZ`/`Other`, or is it absent? An absent region
     is correct for a company ZoomInfo gave no country for; a `D` tier on such a record is a defect.
   - Is `predicted_tier` one of A/B/C/D/Unscored, and does it match your intuition for that account?
3. Open `51-SKIP-LOG.json`. Confirm each reason is specific ("no domain", "ZoomInfo no match") and
   that you agree those companies should stay blank-tiered rather than being scored on guesses.
4. Decide the one open question carried forward: should there be a THIRD disposition beyond
   "predicted" and "skipped" — for example a company ZoomInfo matched but for which no scoring
   input at all could be resolved? Today such a record lands in predictions with a mostly-empty
   payload. Say whether that is right or whether it should be skipped instead.
5. Confirm you approve proceeding to Phase 52, which will open armed, record-scoped HubSpot write
   windows against exactly this predicted set, staged 1 → 5 → 25 → chunked remainder (D-06).
  </how-to-verify>
  <resume-signal>Type "approved" to open Phase 52, or describe what must change in the dry run first. Also answer the third-disposition question in step 4.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot CRM v3 API → Python process | Read-only responses forming the safety baseline; portal identity must be asserted before the data is trusted |
| Python process → committed baseline artifact | The integrity surface: a wrong or partial baseline makes the milestone's closing safety proof vacuous |
| Operator judgement → phase advance | The approval gate; the only non-automated control in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-12 | Tampering | `scored_population_snapshot` | high | mitigate | Population definition imported from `scripts.rescore_population`, never restated; its refuse-rather-than-truncate guard raises rather than writing a partial baseline; module asserted write-free by a source-inspection test so no future edit can add a mutation path |
| T-51-13 | Tampering | wrong-portal baseline | high | mitigate | `EXPECTED_PORTAL_ID = "22617666"` hard-coded, asserted before any network call; the artifact records `portal_id_verified` |
| T-51-14 | Elevation of Privilege | phase advance without approval | high | mitigate | The approval task is `gate="blocking"` and the plan is `autonomous: false`; Phase 51 has no write path at all, so approval cannot be bypassed by an in-phase write, and Phase 52's ROADMAP entry names this approval as its own precondition |
| T-51-15 | Information Disclosure | baseline artifact | medium | mitigate | Artifact serializes only company property values; no header, token or credential enters the serialized structure; acceptance criteria grep for credential markers |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this phase; `requirements.txt` is unchanged and no package-manager task exists |
</threat_model>

<flagged_assumptions>
- **SAFE-04 is Phase 52's requirement id, not this plan's.** This plan captures the baseline SAFE-04
  is verified against; the verification itself happens at Phase 52's close. Named in prose so the
  handoff is explicit rather than implied by an id appearing in the wrong phase.
- **The unresolved FILL-04 `unclassified` edge probe is put to the operator** in step 4 of the
  approval checkpoint. It was not auto-resolved to a backstop marker and was not dropped.
- **Pagination is a Phase 52 prerequisite.** `src/hubspot_client.search_records` has no
  `after`-cursor loop. It is sufficient for Phase 51 (a count plus one bounded page) and for this
  snapshot (the scored population fits one page today, and the imported guard raises if it stops
  fitting). Phase 52's chunked remainder needs the full never-scored id list and must build
  pagination first.
</flagged_assumptions>

<verification>
- `.venv/bin/python -m pytest` exits 0 (full Python suite).
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form only).
- All five phase artifacts exist, are committed, and contain no credential material.
- `git status --porcelain` shows no modification to `src/icp_scoring.py`,
  `scripts/rescore_population.py`, `scripts/backfill_seed_company_scores.py`,
  `config/icp_scoring.yaml` or any `n8n/` file.
- `git log --oneline -20 -- src/ scripts/ config/` shows no commit in this phase touching a
  HubSpot write path.
</verification>

<success_criteria>
- The already-scored population's full property state is committed as a read-only baseline captured
  before any write existed in the milestone.
- Every external-API capability is explicitly decided in `COVERAGE.md` with a reason for each
  subtraction.
- `51-VALIDATION.md` names an automated command for every task in the phase.
- The operator has explicitly approved the dry-run artifacts, and has ruled on the one carried-
  forward open question. Phase 52 does not open until that approval is recorded.
- Zero HubSpot writes and zero n8n executions occurred across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/51-backfill-pipeline-credit-sizing-dry-run/51-03-SUMMARY.md` when done.
</output>
