---
phase: 50-derived-tier-property
plan: 03
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - tests/test_tier_formula_pin.py
  - scripts/check_tier_derived_parity.py
  - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md
autonomous: true
requirements: [TIER-01, TIER-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07's gate is decided from evidence, not assertion: lv_icp_tier_derived matches lv_icp_tier on all 66 scored companies with zero mismatches, EXCEPT the 4 known stuck records where the derived value MUST differ (B, not the stale C). A mismatch anywhere else is a defect."
    - "The comparison across all 66 is committed as an evidence artifact rather than a test (D-17 item 4), so the gate decision stays auditable after the fact."
    - "The live calculationFormula is pinned key-by-key against config/icp_scoring.yaml's tier_rules, so the failure message names exactly which threshold moved (D-17 item 1)."
    - "The derived ladder carries exactly five values - D, A, B, C, Unscored. No sixth label is added, even though a string property makes it structurally free; PARITY-01 stays a documented accepted divergence (D-09)."
    - "TIER-01 boundary, pinned offline: 70 -> A, 69 -> B, 40 -> B, 39 -> C, 15 -> C, 14 -> Unscored - each threshold and one step either side."
    - "TIER-03 adjacency: a company carrying lv_anti_icp_flag true AND a score at or above 70 derives D, not A - the veto branch precedes every score branch in the ladder."
    - "TIER-03 empty: the parity run refuses on an empty population rather than reporting zero mismatches as a pass, and the rendered distributions sum exactly to the population count recorded alongside them."
    - "TIER-03 ordering: rows and distributions sort deterministically by record id, so a re-render against unchanged data is byte-identical."
    - "The 4 stuck records are verified fixed by read-back and by nothing else - no PATCH, no event, no enrolment, no workflow run (D-10), and each is cross-referenced to its WINDOWS.md id 9-12."
    - "The operator-facing result is a before/after tier census in Phase 49's three-point distribution format, against the pre-registered expectation of an identical distribution except 4 records moving C to B; any other movement is a defect signal, not a narrative (D-19)."
    - "If D-04's fallback fired, the roughly 646 never-enriched companies flipping blank to Unscored are disclosed in the census in Phase 49's unmet-truth style, as a deliberate consequence rather than a surprise."
  artifacts:
    - tests/test_tier_formula_pin.py
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md
  key_links:
    - "config/icp_scoring.yaml tier_rules -> tests/test_tier_formula_pin.py's PINNED literal -> the live calculationFormula on lv_icp_tier_derived"
    - "scripts/check_tier_derived_parity.py -> 50-TIER-PARITY-EVIDENCE.md -> Plan 04's checkpoint:decision, which is unanswerable without it"
    - "scripts/build_rescore_report.py's distribution renderer -> the D-19 census section of the same artifact"
  prohibitions:
    - "No company record is written by this plan; the parity run and the census are reads."
    - "The gate verdict is never rendered as a pass when any row outside the 4 known stuck ids mismatches."
    - "A stale parity capture is never reused; the population is re-derived live on every run."
---

<objective>
Decide **D-07**'s gate from evidence and give the operator the before/after census **D-19** asks
for, backed by an offline pin that makes a silent threshold drift impossible.

Purpose: **D-06**'s archive and **D-08**'s WF1 shutdown are both gated on this. If the gate fails,
Plan 04's decision checkpoint closes the phase with the derived property live and the old enum
still present — a coherent partial state, not a cleanup failure.
Output: a formula pin test, and one committed artifact carrying both the 66-company parity verdict
and the operator-facing tier census.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-derived-tier-property/50-CONTEXT.md
@.planning/phases/50-derived-tier-property/50-PATTERNS.md
@.planning/phases/50-derived-tier-property/50-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

| Symbol | Kind |
|---|---|
| `tests/test_tier_formula_pin.py` | new test module (D-17 item 1) |
| `PINNED_TIER_LADDER` | new module-level constant in that test |
| `assert_tier_formula_pinned` | new function in that test |
| `--census` | new CLI flag on `scripts/check_tier_derived_parity.py` |
| `.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md` | new evidence artifact |

Produced by other plans: `scripts/check_tier_null_propagation.py`,
`scripts/check_tier_derived_parity.py`, `scripts/sweep_tier_dependents.py`,
`tests/test_tier_derived_tools.py`, `tests/test_sweep_tier_dependents.py`,
`docs/OPERATOR-TIER-ROLLBACK.md`, `ALLOW_TIER_NULL_PROBE`, `RETIRED_FLOW_IDS`,
`lv_icp_tier_derived`, `50-NULL-PROBE.json`, `50-DEPENDENTS-SWEEP.md`, `50-ROLLBACK-DRILL.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-03-01: Pin the shipped formula against tier_rules (D-17 item 1)</name>
  <files>tests/test_tier_formula_pin.py</files>
  <read_first>
    - tests/test_rubric_change_guard.py (full file — the exact structure to copy: module docstring, ROOT/RUBRIC_PATH constants, PINNED_* dict literal, `_diff_keys`, the assert helper, and the three-test shape)
    - config/icp_scoring.yaml lines 50-90 (`tier_rules` — the source of truth being pinned against)
    - config/hubspot_properties.yaml (the `lv_icp_tier_derived` declaration written in Plan 01)
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json (the settled variant)
    - .planning/TIER-DERIVATION-SPIKE-2026-08-13.md (Round 1's BigDecimal finding, Round 2's accepted ladder)
  </read_first>
  <behavior>
    - `test_pinned_ladder_matches_current_config`: the pinned ladder's per-tier lower bounds equal
      `config/icp_scoring.yaml`'s `tier_rules` min_score values, key by key — A at 70, B at 40,
      C at 15 — and `D` is the hard-veto tier while `Unscored` is the missing-inputs tier.
    - `test_pinned_ladder_matches_declared_formula`: the declared `calculationFormula` in
      `config/hubspot_properties.yaml` for `lv_icp_tier_derived` is byte-identical to the literal
      recorded in `50-NULL-PROBE.json`, and parses back to the same per-tier bounds.
    - `test_mutated_ladder_fails_the_guard`: parametrized mutations each raise — A moved to 65,
      B moved to 45, C moved to 20, the D veto branch removed, the `Unscored` else-branch removed,
      a sixth label introduced, and the veto branch demoted below the score branches.
    - `test_boolean_guard_shape_is_numeric`: the pinned formula's veto guard compares numerically
      (`coalesce(lv_anti_icp_flag, 0) = 1`); a bare-boolean variant and a `coalesce(..., false)`
      variant each raise, because both are live-proven 400s (spike Round 1).
    - `test_sentinel_collision_is_behaviour_preserving`: under the coalesced variant a score of
      exactly -1 and a null score both derive `Unscored`, which is the same value WF1 produces for
      either, so the sentinel collision changes no observable outcome.
    - `test_failure_message_names_the_reverification_obligation`: the raised message contains the
      pointer to re-running `scripts/check_tier_derived_parity.py`.
  </behavior>
  <action>
Create `tests/test_tier_formula_pin.py`, copying `tests/test_rubric_change_guard.py`'s structure
verbatim and adapting only the pinned literal and the comparison target. Offline only: no network,
no HubSpot credentials.

The module docstring names **D-17 item 1** and states why re-baselining requires a real action —
re-running `scripts/check_tier_derived_parity.py` and refreshing
`50-TIER-PARITY-EVIDENCE.md`, not merely editing the pinned literal. Keep the `ROOT`/config-path
constants via `Path(__file__).resolve().parent.parent`, the `_diff_keys(pinned, actual)` key-by-key
helper "so the failure message can name exactly what moved," and rename the assert helper to
`assert_tier_formula_pinned`.

`PINNED_TIER_LADDER` pins the per-tier lower bounds and the branch order, sourced from
`config/icp_scoring.yaml`'s `tier_rules`: A at 70, B at 40, C at 15, `D` as the hard-veto branch
that precedes every score branch, and `Unscored` as the terminal else. Exactly five labels per
**D-09** — the sixth label that `config/icp_scoring.yaml`'s `recommended_motion` map names is
deliberately not added here, PARITY-01 remains an accepted divergence, and one of the parametrized
mutation cases proves the guard rejects its introduction. Write the pinned formula literal so the
**D-03** and **D-04** variants differ in exactly one substitution, making the swap a one-line edit
if the probe forced the fallback.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tier_formula_pin.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_tier_formula_pin.py -x` exits 0.
    - Every mutation case listed in `<behavior>` is parametrized and each raises `AssertionError`.
    - A pytest case asserts the declared `calculationFormula` equals `50-NULL-PROBE.json`'s recorded literal.
    - A pytest case asserts the pinned ladder's A/B/C lower bounds equal `config/icp_scoring.yaml`'s `tier_rules` `min_score` values for A, B and C.
    - A pytest case asserts the veto branch's position in the pinned ladder precedes every score branch.
    - A pytest case asserts the raised failure message contains `check_tier_derived_parity`.
    - The test module makes no network call and passes with no HubSpot credentials set.
  </acceptance_criteria>
  <done>A silent threshold or branch-order drift in the live formula is now impossible without a red test that names exactly what moved.</done>
  <reversibility rating="reversible">A new test file.</reversibility>
</task>

<task type="auto">
  <name>Task 50-03-02: Run D-07's parity gate across all 66 scored companies</name>
  <files>.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md</files>
  <read_first>
    - scripts/check_tier_derived_parity.py (built in Plan 01)
    - scripts/rescore_population.py lines 1-60 (population re-derived live every invocation, never trusted from a stale snapshot)
    - .planning/WINDOWS.md ids 9-12 (the 4 stuck records)
    - .planning/phases/49-re-score-strategy-reporting/49-RESCORE-REPORT.md (the report conventions this matches)
  </read_first>
  <action>
Run the parity comparator live and read-only across the full scored population, re-derived at run
time via the `HAS_PROPERTY(lv_icp_fit_score)` search — never reused from a stale capture:
`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['check_tier_derived_parity.py', '--out', '.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md']; runpy.run_path('scripts/check_tier_derived_parity.py', run_name='__main__')"`

Apply **D-07**'s gate exactly: `lv_icp_tier_derived` matches `lv_icp_tier` on all 66 scored
companies with zero mismatches, **except** the 4 known stuck records `9605273630`, `9604738976`,
`17696004613`, `19100977027`, where the derived value **must differ** — `B`, not the stale `C`. A
mismatch anywhere else is a defect, not a rounding difference. A stuck record that does *not*
differ is equally a defect, because it means the derivation did not take. Render the verdict as a
pass only when both conditions hold; render a failure naming the offending rows otherwise.

Cross-reference each of the 4 to its `WINDOWS.md` id (9, 10, 11, 12) in the artifact, and state
explicitly that each was verified by read-back and by nothing else — no PATCH, no event, no
enrolment, no workflow run (**D-10**) — and that **D-16** declares zero company write windows for
this phase, so any company write appearing here would be a disclosed deviation requiring
justification.

The report states up front what fraction of the portal the scored population represents, sorts
rows deterministically by record id, and carries an explicit "what this does not say" limits
block, matching `49-RESCORE-REPORT.md`'s conventions. This is **D-17 item 4**'s evidence artifact
— evidence, not a pytest assertion — so the gate decision remains auditable after the fact.

If the live population count is not 66, do not silently proceed: record the live count, state the
divergence from the recorded expectation, and carry it into Plan 04's decision context.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv=['check_tier_derived_parity.py','--out','/tmp/50-parity-recheck.md']; runpy.run_path('scripts/check_tier_derived_parity.py', run_name='__main__')" && test -s .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md && echo "parity artifact ok"</automated>
  </verify>
  <acceptance_criteria>
    - `50-TIER-PARITY-EVIDENCE.md` exists, is non-empty, and is committed.
    - The artifact records the live-derived population count and the date it was derived.
    - The artifact classifies every row as a match, an expected mismatch, or a defect, with no row left unclassified.
    - All four of `9605273630`, `9604738976`, `17696004613`, `19100977027` are classified as expected mismatches reading live `C` and derived `B`, each cross-referenced to its `WINDOWS.md` id.
    - Zero rows are classified as defects, or the artifact renders a failure verdict naming each one.
    - Rows are sorted by record id and a re-run against unchanged data produces a byte-identical row section.
    - The artifact carries a "what this does not say" limits block.
  </acceptance_criteria>
  <done>D-07's gate has an evidence-backed verdict that Plan 04's decision checkpoint can be answered from.</done>
  <reversibility rating="reversible">A read-only run producing a regenerable report; no portal state changes.</reversibility>
</task>

<task type="auto">
  <name>Task 50-03-03: Render the operator-facing before/after tier census (D-19)</name>
  <files>scripts/check_tier_derived_parity.py, .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md</files>
  <read_first>
    - scripts/build_rescore_report.py (the three-point distribution renderer and its sums-to-population invariant)
    - .planning/phases/49-re-score-strategy-reporting/49-RESCORE-REPORT.md (the format D-19 asks to reuse)
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md (written by Task 50-03-02)
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json (whether D-04's fallback fired)
  </read_first>
  <action>
Add a `--census` mode to `scripts/check_tier_derived_parity.py` that renders a before/after tier
census into the same `50-TIER-PARITY-EVIDENCE.md` artifact, reusing
`scripts/build_rescore_report.py`'s three-point distribution renderer rather than writing a second
one. "Before" is the `lv_icp_tier` distribution; "after" is the `lv_icp_tier_derived`
distribution; both are read live in the same run so they cannot drift apart.

**D-19** pre-registers the expected result and it is stark: an **identical distribution except 4
records moving C to B**. Render that expectation in the artifact alongside the actual, and state
plainly that any other movement is a defect signal rather than a narrative to explain. Carry over
`build_rescore_report.py`'s invariants: each distribution sums exactly to the population count
recorded beside it and the renderer raises rather than silently rendering on a mismatch; sort
deterministically by record id so re-renders are reproducible; state up front what fraction of the
portal the population represents; carry a "what this does not say" limits block.

Conditional disclosure: if `50-NULL-PROBE.json` records that **D-04**'s fallback fired, the census
must also report the never-scored population — roughly 646 companies — flipping blank to
`Unscored`, with its live-counted actual, framed in Phase 49's unmet-truth style as a **disclosed
deliberate consequence** of the forced fallback, not as an unexplained distribution change. If the
probe settled on **D-03**'s uncoalesced variant, state instead that never-scored companies keep
today's blank tier and that the count of blank-tier companies is unchanged. **D-19** requires no
separately published artifact for this phase; this section is the operator-facing result.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tier_derived_tools.py tests/test_tier_formula_pin.py -x && .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv=['check_tier_derived_parity.py','--census','--out','/tmp/50-census-recheck.md']; runpy.run_path('scripts/check_tier_derived_parity.py', run_name='__main__')" && echo "census ok"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_tier_derived_parity.py --help` lists a `--census` option.
    - `50-TIER-PARITY-EVIDENCE.md` contains a before-and-after tier distribution, each summing exactly to the population count printed beside it.
    - The artifact states the pre-registered expectation (identical distribution except 4 records moving C to B) next to the actual result.
    - A pytest case asserts the census renderer raises when a distribution does not sum to its stated population count.
    - The artifact states which null variant shipped and, when the fallback fired, reports the live-counted number of companies flipping blank to `Unscored`.
    - The artifact carries a "what this does not say" limits block.
  </acceptance_criteria>
  <done>The operator has a before/after census in the format they already read, against an expectation registered before the result was known.</done>
  <reversibility rating="reversible">A regenerable report section.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scripts → HubSpot API | a live private-app bearer token crosses here |
| parity/census output → committed git artifact | company ids and firmographic-derived tiers are committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-11 | Information Disclosure | `50-TIER-PARITY-EVIDENCE.md` | high | mitigate | `_assert_no_secrets` applied to the rendered text before writing; the artifact carries company ids, names and tiers only — no contact records, no emails, no phone numbers |
| T-50-12 | Tampering | a stale parity capture reused as the gate's evidence | high | mitigate | the population is re-derived live on every invocation via `HAS_PROPERTY(lv_icp_fit_score)`; the artifact records the derivation timestamp and the live count, and the verify step re-runs the comparator independently |
| T-50-13 | Repudiation | a gate verdict that cannot be reconstructed later | medium | mitigate | D-17 item 4 requires the full 66-row comparison committed as evidence rather than collapsed into a pass/fail assertion |
| T-50-14 | Tampering | a threshold silently moved in the live formula | high | mitigate | `tests/test_tier_formula_pin.py` pins the ladder key-by-key against `config/icp_scoring.yaml`, with parametrized mutation cases proving the guard has teeth |
| T-50-15 | Denial of Service | a defect-free-looking report produced from an empty population | medium | mitigate | the renderer raises on an empty population and on any distribution that does not sum to its stated count, rather than rendering zero mismatches as a pass |
| T-50-SC | Tampering | npm/pip/cargo installs | high | accept | no package-manager install occurs in this phase (RESEARCH § Standard Stack: no `pip install` / `npm install` required), so no package-legitimacy audit is applicable |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_tier_formula_pin.py -x` exits 0
- `.venv/bin/python -m pytest -q` exits 0
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form — directory form is broken on node 24)
- `50-TIER-PARITY-EVIDENCE.md` is committed carrying both the D-07 verdict and the D-19 census
- Zero rows classified as defects, or a failure verdict naming each one
</verification>

<success_criteria>
D-07's gate has an auditable, evidence-backed verdict over the live 66; the ladder is pinned
offline against `tier_rules`; the operator has a before/after census against a pre-registered
expectation.
</success_criteria>

<output>
Create `.planning/phases/50-derived-tier-property/50-03-SUMMARY.md` when done
</output>
