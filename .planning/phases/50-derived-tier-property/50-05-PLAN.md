---
phase: 50-derived-tier-property
plan: 05
type: execute
wave: 4
depends_on: [50-04]
files_modified:
  - scripts/check_schema_drift.py
  - tests/test_check_schema_drift.py
  - config/hubspot_properties.yaml
  - config/hubspot_flows/lv_icp_tier-property.after.json
  - config/hubspot_flows/lv_icp_tier_derived-property.after.json
  - config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json
  - .planning/phases/50-derived-tier-property/50-RETIREMENT-RECORD.md
autonomous: false
requirements: [TIER-01, TIER-03]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Retirement is the LAST gated step: the derived property is already live and proven, dependents are enumerated, and the rollback mechanism is proven, before anything is archived (D-06)."
    - "If D-07's gate failed, the phase closes with lv_icp_tier_derived live and the old enum still present — a coherent partial state, explicitly not a failure to clean up (D-06)."
    - "WF1 4625147345 is switched off with its definition kept — not deleted, and not left running alongside the derived property, because two writers disagreeing is worse than either alone (D-08)."
    - "scripts/check_schema_drift.py stops reporting this phase's own deliberate success as portal damage: lv_icp_tier leaves DO_NOT_ARCHIVE_COMPANY_PROPERTIES, and 4625147345 moves into a RETIRED_FLOW_IDS structure whose invariant is live-AND-disabled, so damage now means deleted OR re-enabled (D-17 item 2)."
    - "That guard edit lands in the SAME COMMIT as the archive and the WF1 disable, so no schema-drift run between them can report exit code 2 for a deliberate, correct state."
    - "config/hubspot_properties.yaml and the config/hubspot_flows snapshots reflect the post-retirement portal, refreshed from live read-backs rather than hand-edited (D-17 item 3)."
    - "D-15's rename is treated as researched and unproven, not assumed: HubSpot internal names are immutable after creation and archived-name reuse needs a permanent delete rather than a soft archive, so the shipped outcome is the operator's stated fallback — keep lv_icp_tier_derived permanently and change only its display label to 'ICP Tier'."
    - "If a portal dependent cannot be migrated, the phase stops and brings it to the operator rather than forcing the retirement through or silently keeping the old property alive (D-11)."
    - "Every archive and disable is verified by an independent re-read, never by the mutating call's own response body."
  artifacts:
    - scripts/check_schema_drift.py
    - tests/test_check_schema_drift.py
    - config/hubspot_properties.yaml
    - config/hubspot_flows/lv_icp_tier-property.after.json
    - config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json
    - .planning/phases/50-derived-tier-property/50-RETIREMENT-RECORD.md
  key_links:
    - "DELETE /crm/v3/properties/companies/lv_icp_tier -> the refreshed lv_icp_tier-property.after.json -> check_schema_drift.py's edited DO_NOT_ARCHIVE_COMPANY_PROPERTIES"
    - "PUT /automation/v4/flows/4625147345 isEnabled false -> the refreshed 4625147345-wf1-set-icp-tier.after.json -> RETIRED_FLOW_IDS' live-AND-disabled invariant"
    - "50-TIER-PARITY-EVIDENCE.md's verdict + 50-DEPENDENTS-SWEEP.md + 50-ROLLBACK-DRILL.md -> the decision checkpoint that authorises all of the above"
  prohibitions:
    - "WF1 4625147345 is not deleted."
    - "The archive does not run unless the decision checkpoint authorised it."
    - "No company record is written by this plan."
    - "A rename attempt is not made unless the operator selects that option at the decision checkpoint."
    - "The guard edit is not committed separately from the live archive and disable it exists to accommodate."
---

<objective>
Take **D-06**'s one irreversible act — archiving `lv_icp_tier` — and **D-08**'s WF1 shutdown, both
only after an explicit operator decision, and land them in the same commit as the guard edits that
keep the repo's drift comparator truthful afterwards.

Purpose: retirement is the last gated step by design. Everything before it is reversible; this is
not. If **D-07**'s gate failed, the honest outcome is to stop here with the derived property live
and the old enum still present — **D-06** calls that a coherent partial state, not a cleanup
failure, and the decision checkpoint offers it as a first-class option.
Output: WF1 off with its definition kept, `lv_icp_tier` archived, and a drift comparator that
reports the new state as correct rather than as damage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-derived-tier-property/50-CONTEXT.md
@.planning/phases/50-derived-tier-property/50-RESEARCH.md
@.planning/phases/50-derived-tier-property/50-03-SUMMARY.md
@.planning/phases/50-derived-tier-property/50-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

| Symbol | Kind |
|---|---|
| `RETIRED_FLOW_IDS` | new module-level structure in `scripts/check_schema_drift.py` |
| `.planning/phases/50-derived-tier-property/50-RETIREMENT-RECORD.md` | new evidence artifact |
| refreshed `config/hubspot_flows/lv_icp_tier-property.after.json` | updated snapshot |
| refreshed `config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json` | updated snapshot |

Produced by other plans: `scripts/check_tier_null_propagation.py`,
`scripts/check_tier_derived_parity.py`, `scripts/sweep_tier_dependents.py`,
`tests/test_tier_derived_tools.py`, `tests/test_sweep_tier_dependents.py`,
`tests/test_tier_formula_pin.py`, `docs/OPERATOR-TIER-ROLLBACK.md`, `ALLOW_TIER_NULL_PROBE`,
`lv_icp_tier_derived`, `50-NULL-PROBE.json`, `50-DEPENDENTS-SWEEP.md`,
`50-TIER-PARITY-EVIDENCE.md`, `50-ROLLBACK-DRILL.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 50-05-01: Authorise retirement — the phase's one irreversible act (D-06, D-07, D-11, D-15)</name>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md (D-07's verdict — read this first, the decision turns on it)
    - .planning/phases/50-derived-tier-property/50-DEPENDENTS-SWEEP.md (both halves, including the dated pre-cutover re-run)
    - .planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md (proof the rollback mechanism works)
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json (the RESEARCH Q6 finding — whether the API DELETE is a soft archive)
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-06, D-07, D-11, D-15
  </read_first>
  <decision>Whether to retire `lv_icp_tier` — archive the old enum and switch WF1 `4625147345` off — now that the derived property is live and proven.</decision>
  <context>
Read the four artifacts above before deciding. Between them they answer every question this
decision turns on.

**What D-07's gate says.** The gate is: `lv_icp_tier_derived` matches `lv_icp_tier` on all 66
scored companies with zero mismatches, **except** the 4 known stuck records `9605273630`,
`9604738976`, `17696004613`, `19100977027`, where the derived value **must differ** — `B`, not the
stale `C`. A mismatch anywhere else is a defect, not a rounding difference.
`50-TIER-PARITY-EVIDENCE.md` carries the verdict and the full 66-row comparison behind it.

**What happens if you proceed.** In one window: `PUT /automation/v4/flows/4625147345` sets
`isEnabled: false` — WF1's definition is kept, not deleted (**D-08**), so re-enabling it is one
action. Then `DELETE /crm/v3/properties/companies/lv_icp_tier` archives the old enum. In the same
commit, `scripts/check_schema_drift.py`'s do-not-archive structures are edited so the comparator
stops reading this phase's own success as portal damage.

**What this costs.** Zero n8n executions, zero Anthropic calls, zero provider credits, zero
company-record writes. Two HubSpot schema calls plus their verification re-reads.

**Reversibility — this is the phase's single irreversible act.** Archiving a HubSpot property
cannot be undone by re-creating it under the same internal name, and every portal dependent still
pointed at it breaks at that moment. `50-DEPENDENTS-SWEEP.md`'s pre-cutover re-run is your list of
what those dependents are. WF1's shutdown, by contrast, is fully reversible — its definition is
kept and `docs/OPERATOR-TIER-ROLLBACK.md` step 1 restores it.

**D-11 applies here.** If the sweep flagged any dependent that cannot be migrated, that is not a
detail to work around — it is the reason this checkpoint exists. Do not force the retirement
through, and do not silently keep the old property alive without saying so.

**On the name (D-15).** The intent was to rename `lv_icp_tier_derived` back to `lv_icp_tier` after
retirement. That was flagged as unproven and has now been researched: HubSpot internal names are
immutable after creation, and reusing an archived name requires a *permanent* delete rather than
the soft archive this step performs — so the rename route is very likely unreachable, and it is a
separate, higher-risk action that should not share a window with this one. **D-15**'s own stated
fallback is to keep `lv_icp_tier_derived` permanently and change only its display label to "ICP
Tier", which costs nothing and is what option `retire-and-relabel` does.
  </context>
  <options>
    <option id="retire-and-relabel">
      <name>Proceed: WF1 off, archive `lv_icp_tier`, relabel the derived property to "ICP Tier" (D-15's fallback)</name>
      <pros>Closes the phase completely; removes the second writer so two engines cannot disagree; the operator-facing label reads correctly with no rename risk; WF1 remains one action from restoration.</pros>
      <cons>The archive is irreversible; any dependent missed by the sweep breaks at that moment; the internal name `lv_icp_tier_derived` becomes the permanent survivor.</cons>
    </option>
    <option id="keep-both">
      <name>Stop here: derived property live, old enum still present (D-06's coherent partial state)</name>
      <pros>Nothing irreversible happens; the 4 stuck records are already fixed; the phase's core delivery stands. Explicitly sanctioned by D-06 when the gate fails.</pros>
      <cons>Two tier properties coexist and WF1 keeps writing the stale one, so the operator must know which to read; the retirement carries into a later phase.</cons>
    </option>
    <option id="retire-and-attempt-rename">
      <name>Proceed, then additionally attempt the D-15 rename to `lv_icp_tier`</name>
      <pros>Restores the canonical internal name if it works.</pros>
      <cons>One-way and researched-as-unlikely: it requires a permanent delete of the archived property rather than a soft archive, past the point where the 90-day restore window would help. RESEARCH recommends against sharing a window with this step. Not the default.</cons>
    </option>
  </options>
  <resume-signal>Select: retire-and-relabel, keep-both, or retire-and-attempt-rename.</resume-signal>
  <acceptance_criteria>
    - The selected option is recorded verbatim in `50-RETIREMENT-RECORD.md` with the date and the D-07 verdict it was taken against.
    - If `keep-both` is selected, Task 50-05-02 does not run and the phase seals with the partial state disclosed as a deliberate D-06 outcome rather than as an unmet truth.
    - If any dependent was flagged unmigratable in `50-DEPENDENTS-SWEEP.md`, the decision record states how it was resolved.
  </acceptance_criteria>
  <done>The operator has authorised or declined the irreversible act, against evidence rather than assertion.</done>
  <reversibility rating="reversible">The decision itself changes nothing; it authorises the next task.</reversibility>
</task>

<task type="auto">
  <name>Task 50-05-02: Retire lv_icp_tier and WF1, with the guard edits in the same commit</name>
  <files>scripts/check_schema_drift.py, tests/test_check_schema_drift.py, config/hubspot_properties.yaml, config/hubspot_flows/lv_icp_tier-property.after.json, config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json, config/hubspot_flows/lv_icp_tier_derived-property.after.json, .planning/phases/50-derived-tier-property/50-RETIREMENT-RECORD.md</files>
  <precondition>Task 50-05-01 selected `retire-and-relabel` or `retire-and-attempt-rename`. The operator has run the two armed commands given in this task's action; arming a live schema mutation is an operator action in this repo. If `keep-both` was selected, this task does not run.</precondition>
  <read_first>
    - scripts/check_schema_drift.py lines 60-140 (`DO_NOT_ARCHIVE_COMPANY_PROPERTIES`, `DO_NOT_ARCHIVE_FLOW_IDS`, `ACCEPTED_DIVERGENCES` and its `PARITY-01-tier-label` entry)
    - scripts/check_schema_drift.py lines 215-250 (`exit_code_for` and `_compute_do_not_archive` — the `ok` conjunction that cannot currently express "kept but deliberately off")
    - tests/test_check_schema_drift.py (the offline pin for those structures)
    - scripts/put_hubspot_flow.py and scripts/fetch_hubspot_flow.py (the live PUT mechanism and `archive_flow`'s before/after snapshot convention)
    - scripts/rollback_property_migration.py lines 110-140 (`_get_property_live` 404-tolerant GET, `_archive_property_live` DELETE)
    - config/hubspot_properties.yaml (the `lv_icp_tier` declaration at roughly line 405)
    - .planning/phases/50-derived-tier-property/50-RETIREMENT-RECORD.md (the authorised option)
  </read_first>
  <action>
Make every repo edit in the working tree first, then run the two armed live mutations, then commit
everything as **one commit**. Pitfall 2 is the reason: `exit_code_for()` returns `2` — its most
severe class, documented as "the live scoring engine itself is damaged" — the moment either
`lv_icp_tier` is archived or WF1's `isEnabled` flips to `false`, because both names sit in
structures written in Phase 42 with a "never archived by anything" invariant baked in. A commit
that separates the guard edit from the actions it accommodates leaves a window in which the
comparator reports this phase's own deliberate success as portal damage.

**Guard edits (D-17 item 2).** Remove `"lv_icp_tier"` from `DO_NOT_ARCHIVE_COMPANY_PROPERTIES`.
Move `"4625147345": "wf1-set-icp-tier"` out of `DO_NOT_ARCHIVE_FLOW_IDS` into a new module-level
`RETIRED_FLOW_IDS` structure, and extend `_compute_do_not_archive` so a retired flow's invariant is
**live AND disabled** — damage now means the flow was deleted **or** re-enabled, which is exactly
**D-08**'s "not deleted, not left running alongside the derived property" expressed as a
machine-checked condition. The current `f["live"] and f["is_enabled"]` conjunction cannot express
"kept but deliberately off"; this gives that state its own truth value. Update the
`PARITY-01-tier-label` entry in `ACCEPTED_DIVERGENCES`, which pins the archived property's
five-value enum and goes stale the moment it is archived — restate it against
`lv_icp_tier_derived`'s five-label ladder, since **D-09** keeps PARITY-01 an accepted divergence
rather than closing it here. Extend `tests/test_check_schema_drift.py` to pin the new structure
offline: a retired flow that is live-and-disabled is `ok`; the same flow deleted is not; the same
flow re-enabled is not.

**Config edits (D-17 item 3).** Remove the `lv_icp_tier` declaration from
`config/hubspot_properties.yaml` so `scripts/sync_hubspot_properties.py` does not propose
re-creating the property it just archived.

**Armed live mutation 1 — WF1 off (D-08).** The operator runs the `put_hubspot_flow.py` invocation
that sets `isEnabled: false` on `4625147345`, keeping the definition. Verify by an independent
re-read via `scripts/fetch_hubspot_flow.py`, never from the PUT's own response body, and refresh
`config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json` from that read-back.

**Armed live mutation 2 — archive the enum (D-06).** The operator runs the armed
`DELETE /crm/v3/properties/companies/lv_icp_tier` call, gated behind the repo's paired
`DRY_RUN=false` plus its property-write allow-key. Verify gone by a 404 re-read
(`_get_property_live(...) is None`), and confirm against
`GET /crm/v3/properties/companies?archived=true` that it landed in the archived state the null
probe already established for this API — RESEARCH Q6 was answered there precisely so this step
would not be the first time the question was asked. Refresh
`config/hubspot_flows/lv_icp_tier-property.after.json` to record the archived state.

**Relabel (D-15's fallback).** PATCH `lv_icp_tier_derived`'s `label` to "ICP Tier" — a label change
only; the internal name stays `lv_icp_tier_derived` permanently, which is the operator's chosen
fallback. Refresh `config/hubspot_flows/lv_icp_tier_derived-property.after.json` from the
read-back. Only if the operator selected `retire-and-attempt-rename` at the decision checkpoint,
additionally attempt the rename route documented there; otherwise do not.

Record the whole window in `50-RETIREMENT-RECORD.md`: the authorised option, both live calls with
their status codes, both verification re-reads, the relabel, and the `check_schema_drift.py` exit
code after the fact.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_check_schema_drift.py tests/test_tier_formula_pin.py tests/test_tier_derived_tools.py -x && .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/check_schema_drift.py', run_name='__main__')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_schema_drift.py` exits 0 after the retirement, or exits non-zero for a reason named and explained in `50-RETIREMENT-RECORD.md`.
    - `scripts/check_schema_drift.py` defines a `RETIRED_FLOW_IDS` structure containing `4625147345`.
    - A pytest case asserts a retired flow that is live and disabled computes `ok` as true.
    - A pytest case asserts a retired flow that is absent computes `ok` as false.
    - A pytest case asserts a retired flow that is live and enabled computes `ok` as false.
    - `GET /automation/v4/flows/4625147345` returns 200 with `isEnabled` false, and the refreshed snapshot records that read-back.
    - `GET /crm/v3/properties/companies/lv_icp_tier` returns 404, and the property appears in the archived-properties listing.
    - `GET /crm/v3/properties/companies/lv_icp_tier_derived` returns 200 with `label` equal to `ICP Tier`.
    - `config/hubspot_properties.yaml` declares `lv_icp_tier_derived` and no longer declares the archived enum, and a `sync_hubspot_properties.py` dry-run proposes zero property creates.
    - `git show --stat HEAD` shows the `check_schema_drift.py` edit and the refreshed flow/property snapshots in one commit.
    - `50-RETIREMENT-RECORD.md` records both live calls with status codes and both verification re-reads.
  </acceptance_criteria>
  <done>WF1 is off with its definition kept, the old enum is archived, and the drift comparator reports the new state as correct.</done>
  <reversibility rating="one-way">Archiving `lv_icp_tier` cannot be undone by re-creating it under the same internal name, and every portal dependent still pointed at it breaks at that moment. This is the phase's single irreversible act and sits directly behind Task 50-05-01's decision gate. WF1's shutdown alone is reversible — its definition is kept.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scripts → HubSpot API | a live private-app bearer token crosses here, authorising irreversible schema mutation |
| env → two-key write gate | `DRY_RUN` plus the property-write allow-key decide whether the archive executes |
| operator decision → irreversible act | the checkpoint is the only thing standing between the plan and an unrecoverable archive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-21 | Tampering | irreversible archive of `lv_icp_tier` executed without authority | high | mitigate | `checkpoint:decision` immediately precedes the one-way task, and the task's `<precondition>` refuses to run unless a proceed option was selected; the archive is additionally gated by the repo's paired `DRY_RUN=false` plus property-write allow-key |
| T-50-22 | Denial of Service | portal dependents silently breaking at archive time | high | mitigate | `50-DEPENDENTS-SWEEP.md`'s pre-cutover re-run enumerates them, and D-11 escalates anything unmigratable to the operator before the decision is taken |
| T-50-23 | Tampering | the drift comparator reporting deliberate success as live-engine damage | high | mitigate | `RETIRED_FLOW_IDS` gives "kept but deliberately off" its own truth value; the edit lands in the same commit as the actions, pinned offline by three new `tests/test_check_schema_drift.py` cases |
| T-50-24 | Repudiation | an archive whose outcome cannot be reconstructed later | medium | mitigate | `50-RETIREMENT-RECORD.md` records both live calls with status codes, both independent verification re-reads, and the post-hoc drift exit code |
| T-50-25 | Tampering | verifying a mutation from its own response body | high | mitigate | every archive and disable is verified by an independent re-read — a 404 GET for the property, a fresh flow GET for WF1 — never from the mutating call's response |
| T-50-26 | Tampering | wrong-portal execution of an irreversible call | high | mitigate | portal guard asserted before any call in every schema-touching script (`HUBSPOT_EXPECTED_PORTAL_ID`, default `22617666`), refusing with exit 1 and zero calls made |
| T-50-27 | Information Disclosure | refreshed snapshots and `50-RETIREMENT-RECORD.md` | high | mitigate | `_assert_no_secrets` applied to every live-read payload before it is written to a committed path |
| T-50-SC | Tampering | npm/pip/cargo installs | high | accept | no package-manager install occurs in this phase (RESEARCH § Standard Stack: no `pip install` / `npm install` required), so no package-legitimacy audit is applicable |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` exits 0
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form — directory form is broken on node 24)
- `.venv/bin/python scripts/check_schema_drift.py` exits 0, or non-zero for a documented, expected reason
- WF1 `4625147345` reads `isEnabled: false` and still exists
- `lv_icp_tier` returns 404 and appears in the archived-properties listing
- `lv_icp_tier_derived` reads `label: ICP Tier`
- One commit carries the guard edits, the config edits and the refreshed snapshots together
</verification>

<success_criteria>
The old enum is retired and WF1 is off with its definition kept — or, if the gate failed, the phase
closes in D-06's sanctioned partial state with that disclosed. Either way the repo's guards tell
the truth about the portal.
</success_criteria>

<output>
Create `.planning/phases/50-derived-tier-property/50-05-SUMMARY.md` when done
</output>
