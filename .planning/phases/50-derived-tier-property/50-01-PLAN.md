---
phase: 50-derived-tier-property
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_tier_null_propagation.py
  - scripts/check_tier_derived_parity.py
  - scripts/apply_fit_score_formula.py
  - tests/test_tier_derived_tools.py
  - config/hubspot_properties.yaml
  - config/hubspot_flows/lv_icp_tier_derived-property.before.json
  - config/hubspot_flows/lv_icp_tier_derived-property.after.json
  - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json
autonomous: false
requirements: [TIER-01, TIER-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-05's live probe answers RESEARCH Q4 against live HubSpot before any formula variant is committed: whether a null term in an UNTAKEN conditional branch blanks the whole calculated property."
    - "The settled variant is recorded as an explicit choice in 50-NULL-PROBE.json — D-03's uncoalesced ladder if the probe reads a value, D-04's coalesce(lv_icp_fit_score, -1) if it reads blank."
    - "lv_icp_tier_derived exists live as a calculated string property whose calculationFormula is byte-identical to the settled variant (D-01's one-property scope lift, D-14's name)."
    - "The 4 stuck records 9605273630, 9604738976, 17696004613, 19100977027 read B on lv_icp_tier_derived — with no event, no enrolment, no workflow run and no PATCH to any of them (D-10)."
    - "TIER-01 boundary: a score of exactly 70 derives A, 69 derives B, exactly 40 derives B, 39 derives C, exactly 15 derives C, 14 derives Unscored — the ladder uses >= lower bounds only."
    - "TIER-01 precision: lv_anti_icp_flag is compared numerically via coalesce(lv_anti_icp_flag, 0) = 1 because HubSpot booleans arrive in formula-land as BigDecimal; a bare boolean reference and coalesce(..., false) both 400 (spike Round 1) and neither shape ships."
    - "TIER-01 precision: scores are integer sums of integer rubric weights, so the ladder needs no rounding, truncation or tie-breaking contract — equality at a threshold is exact."
    - "TIER-02 boundary: a company whose lv_icp_fit_score is null reads exactly the value recorded in 50-NULL-PROBE.json and no other value."
    - "TIER-02 adjacency: under D-04's fallback the -1 sentinel is indistinguishable from a real score of -1, and both derive Unscored — the same value WF1 produces for either, so the collision is behaviour-preserving."
    - "The probe touches no company record from the live population: every company, numeric property and calculated property it creates is one it created itself in the same run and archives in a finally block (D-05, D-16)."
    - "RESEARCH Q6 is answered as a side effect: the probe records whether its archived disposables reappear under GET /crm/v3/properties/companies?archived=true, confirming DELETE is a soft archive before D-06 ever runs."
  artifacts:
    - scripts/check_tier_null_propagation.py
    - scripts/check_tier_derived_parity.py
    - tests/test_tier_derived_tools.py
    - config/hubspot_properties.yaml
    - config/hubspot_flows/lv_icp_tier_derived-property.before.json
    - config/hubspot_flows/lv_icp_tier_derived-property.after.json
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json
  key_links:
    - "config/hubspot_properties.yaml's lv_icp_tier_derived declaration -> scripts/sync_hubspot_properties.py::_create_property_live, which POSTs the declaration verbatim (body = dict(prop))"
    - "50-NULL-PROBE.json's settled variant -> the calculationFormula literal in config/hubspot_properties.yaml -> the live property"
    - "scripts/apply_fit_score_formula.py --property lv_icp_tier_derived -> config/hubspot_flows/lv_icp_tier_derived-property.after.json as formula source of truth"
  prohibitions:
    - "No script in this plan issues a PATCH, POST or DELETE against any company record in the live population."
    - "None of the 4 stuck records 9605273630, 9604738976, 17696004613, 19100977027 is written by anything in this plan."
    - "The new property is not created under any internal name other than lv_icp_tier_derived."
    - "No formula string uses the UI-editor bracket-ref grammar; the Properties API grammar is statement-form."
    - "No sixth tier label is added to the derived ladder."
---

<objective>
Settle TIER-02's null question against live records, then land `lv_icp_tier_derived` as a live
calculated string property and prove end-to-end that the 4 stuck records read the tier their
score already implies — with no event, no enrolment, no workflow run and no record write.

This is the phase's tracer. Per **D-02** it closes v0.9's own disclosed debt (`WINDOWS.md`
ids 9–12) rather than opening v1.0.

Tasks 1–3 are `50-VALIDATION.md`'s designated Wave-0 infrastructure; their output — the settled
formula literal — is the tracer task's input. Probe-before-create is contractually forced:
TIER-02 requires the null question be answered against live records *before* the choice is
committed, and creating the real property with an unsettled variant commits it.

Purpose: remove the property-change-event dependency that makes `lv_icp_tier` go stale against a
score that is already correct.
Output: two permanent scripts, one offline test module, a live calculated property, and the
committed probe evidence that settled its formula.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-derived-tier-property/50-CONTEXT.md
@.planning/phases/50-derived-tier-property/50-PATTERNS.md
@.planning/TIER-DERIVATION-SPIKE-2026-08-13.md
</context>

<artifacts_this_phase_produces>
Symbols created by **this plan** (the phase-wide list is repeated in each plan for the executor):

| Symbol | Kind |
|---|---|
| `scripts/check_tier_null_propagation.py` | new script (D-05's two-key-gated live probe) |
| `scripts/check_tier_derived_parity.py` | new script (D-07's gate + D-17 item 4 renderer) |
| `tests/test_tier_derived_tools.py` | new test module |
| `ALLOW_TIER_NULL_PROBE` | new env var (probe's dedicated allow-key) |
| `DRY_RUN` | existing repo-wide env var (probe's second key) |
| `lv_icp_tier_derived` | new HubSpot company property (internal name) |
| `--property` | new CLI flag on `scripts/apply_fit_score_formula.py` |
| `--ids` | new CLI flag on `scripts/check_tier_derived_parity.py` |
| `--out` | new CLI flag on both new scripts |
| `config/hubspot_flows/lv_icp_tier_derived-property.before.json` | new snapshot |
| `config/hubspot_flows/lv_icp_tier_derived-property.after.json` | new snapshot |
| `.planning/phases/50-derived-tier-property/50-NULL-PROBE.json` | new evidence artifact |

Produced by later plans: `scripts/sweep_tier_dependents.py`, `tests/test_sweep_tier_dependents.py`,
`tests/test_tier_formula_pin.py`, `docs/OPERATOR-TIER-ROLLBACK.md`, `RETIRED_FLOW_IDS`,
`50-DEPENDENTS-SWEEP.md`, `50-TIER-PARITY-EVIDENCE.md`, `50-ROLLBACK-DRILL.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-01-01: Build the null probe and the parity comparator</name>
  <files>scripts/check_tier_null_propagation.py, scripts/check_tier_derived_parity.py, tests/test_tier_derived_tools.py</files>
  <read_first>
    - scripts/sync_hubspot_properties.py (two-key gate `_writes_allowed()`, portal guard, `_has_credentials()` skip-to-exit-0, `_create_property_live`'s `body = dict(prop)` pass-through)
    - scripts/rollback_property_migration.py lines 110-140 (`_get_property_live` 404-tolerant GET, `_archive_property_live` DELETE)
    - scripts/derive_orphan_candidates.py lines 280-335 (`archive_property`, re-check-immediately-before-mutation discipline)
    - scripts/check_schema_drift.py lines 55-150 (portal guard, `_assert_no_secrets`, dotenv-wrapper docstring idiom)
    - scripts/rescore_population.py lines 1-60 (population re-derived live every invocation; dotenv-wrapper docstring with `sys.argv` pre-seed)
    - scripts/build_rescore_report.py (deterministic markdown renderer; distribution-sums-to-population invariant)
    - .planning/phases/50-derived-tier-property/50-PATTERNS.md (composite analog assignments for both scripts)
    - .planning/TIER-DERIVATION-SPIKE-2026-08-13.md (the accepted ladder and the BigDecimal finding)
  </read_first>
  <behavior>
    Offline-testable pure functions (these are what `tests/test_tier_derived_tools.py` pins):
    - `derived_tier(score, anti_icp_flag)` -> the expected ladder value. Cases: (70, False)->"A";
      (69, False)->"B"; (40, False)->"B"; (39, False)->"C"; (15, False)->"C"; (14, False)->"Unscored";
      (85, True)->"D" (veto precedes score); (-1, False)->"Unscored"; (None, False)->the sentinel
      branch under the settled variant.
    - `classify_probe_result(read_back_value)` -> "uncoalesced_ok" when a value is returned,
      "null_propagates" when blank/None.
    - `classify_row(record_id, live_tier, derived_tier, known_stuck_ids)` -> one of "match",
      "expected_mismatch" (id in the 4 stuck ids AND live=="C" AND derived=="B"), "defect".
    - `_writes_allowed()` -> True only when `DRY_RUN` is exactly "false" AND
      `ALLOW_TIER_NULL_PROBE` is exactly "true"; every other combination is False.
    - `render_parity_markdown(rows, population_count)` -> raises when the row count and the
      recorded population count disagree, and raises on an empty population rather than
      reporting zero mismatches as a pass.
  </behavior>
  <action>
Create `scripts/check_tier_null_propagation.py` — D-05's fresh, two-key-gated live probe. Gate on
`DRY_RUN=false` AND its own dedicated allow-key `ALLOW_TIER_NULL_PROBE=true`, copying
`sync_hubspot_properties.py::_writes_allowed()`'s exact-string idiom; do not reuse
`ALLOW_HUBSPOT_PROPERTY_WRITES`, which is scoped to that migration. Assert the portal guard
(`HUBSPOT_EXPECTED_PORTAL_ID` default `22617666`) before any call, and `_has_credentials()`
skip-to-exit-0 when the token is absent.

Probe steps, per RESEARCH Q4's minimal test: create a disposable numeric company property standing
in for `lv_icp_fit_score`; create a disposable calculated string property whose
`calculationFormula` is the spike's accepted ladder with that stand-in substituted for
`lv_icp_fit_score`; create a disposable company with the stand-in numeric property left null
(never set — not zero); read the calculated property back on that disposable company. A blank
read means null propagates through an untaken branch, which forces **D-04**'s
`coalesce(lv_icp_fit_score, -1)`; a returned value means **D-03**'s uncoalesced ladder holds.
Disclosure required in the module docstring: the disposable company is self-created and
self-deleted inside the same run and is therefore not a population write under **D-16** — no
company record from the live population is read or written.

Teardown in a `finally` block: archive both disposable properties and delete the disposable
company, then verify each is gone by re-read (`_get_property_live(...) is None`, company GET 404).
Immediately after teardown, issue `GET /crm/v3/properties/companies?archived=true` and record
whether the two disposables appear there — this answers RESEARCH Q6 (is the API `DELETE` a soft
archive?) at zero extra cost, before **D-06**'s irreversible archive is ever authorised.

Write the result to `--out` (default
`.planning/phases/50-derived-tier-property/50-NULL-PROBE.json`) carrying: the read-back value, the
`classify_probe_result` verdict, the settled variant id (`uncoalesced` or `coalesced_minus_one`),
the exact `calculationFormula` literal that must ship, the teardown 404 confirmations, the Q6
archived-list finding, and a UTC timestamp. Apply `_assert_no_secrets` to the serialized output
before writing.

Create `scripts/check_tier_derived_parity.py` — read-only, never issues a write of any kind.
Re-derive the scored population live on every invocation using the same
`HAS_PROPERTY(lv_icp_fit_score)` search shape `scripts/rescore_population.py` uses; never trust a
stale local snapshot. Read `lv_icp_tier`, `lv_icp_tier_derived`, `lv_icp_fit_score` and
`lv_anti_icp_flag` per company, classify each row via `classify_row`, and render a deterministic
markdown report sorted by record id. Accept `--ids` (comma-separated) to restrict the run to named
records — the tracer task uses this — and `--out` for the report path. Hard-code the 4 known stuck
ids `9605273630`, `9604738976`, `17696004613`, `19100977027` as the `known_stuck_ids` set; a row
is `expected_mismatch` only when it is one of those AND live reads `C` AND derived reads `B`.

Both scripts carry the repo's dotenv-wrapper invocation in their docstring verbatim (`.env` is
Read/Bash permission-blocked), copying `scripts/check_schema_drift.py`'s `sys.argv` pre-seed shape.

Create `tests/test_tier_derived_tools.py` pinning every pure function listed in `<behavior>`,
offline, with no network and no HubSpot credentials. Include the boundary cases at 70/69, 40/39,
15/14, the veto-precedes-score case, and the `-1` sentinel case. Also assert the gate refuses
every env combination except both keys set.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tier_derived_tools.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_tier_derived_tools.py -x` exits 0.
    - `.venv/bin/python scripts/check_tier_null_propagation.py` with no credentials prints a skip line and exits 0.
    - `.venv/bin/python scripts/check_tier_null_propagation.py` with credentials and neither key set prints a dry-run refusal and makes no write call.
    - A pytest case asserts `derived_tier(70, False) == "A"` and `derived_tier(69, False) == "B"`.
    - A pytest case asserts `derived_tier(40, False) == "B"` and `derived_tier(39, False) == "C"`.
    - A pytest case asserts `derived_tier(15, False) == "C"` and `derived_tier(14, False) == "Unscored"`.
    - A pytest case asserts `derived_tier(85, True) == "D"`.
    - A pytest case asserts `derived_tier(-1, False) == "Unscored"`.
    - A pytest case asserts `classify_row("9605273630", "C", "B", KNOWN_STUCK) == "expected_mismatch"` and `classify_row("9605273630", "C", "C", KNOWN_STUCK) == "defect"`.
    - A pytest case asserts `render_parity_markdown([], 0)` raises.
    - A pytest case asserts two consecutive `render_parity_markdown` calls on the same rows return byte-identical strings.
  </acceptance_criteria>
  <done>Both scripts exist, are disarmed-by-default, and every pure function they expose is pinned by a passing offline test module.</done>
  <reversibility rating="reversible">New files only; deleting them restores the prior state exactly.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 50-01-02: Run the armed null probe (D-05)</name>
  <files>.planning/phases/50-derived-tier-property/50-NULL-PROBE.json</files>
  <read_first>
    - scripts/check_tier_null_propagation.py (built in Task 50-01-01)
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-03, D-04, D-05, D-16
  </read_first>
  <action>
Hand the operator the literal armed invocation below and halt. Arming a live write is an operator
action in this repo — the executor cannot set `DRY_RUN=false` or `ALLOW_TIER_NULL_PROBE=true`.
When the operator returns, read `50-NULL-PROBE.json`, confirm it names a settled variant
(`uncoalesced` per **D-03**, or `coalesced_minus_one` per **D-04**'s forced fallback), and carry
that variant's `calculationFormula` literal forward into Task 50-01-03 unchanged. **D-04** is
explicit that a blank result does not stop for a checkpoint and does not abandon derivation — it
ships the coalesced variant and discloses the roughly 646 never-enriched companies flipping blank
to `Unscored`, which Plan 03's census renders.
  </action>
  <what-built>
A permanent, two-key-gated live probe that answers the one question the spike could not settle
from syntax: does a null term inside an **untaken** conditional branch blank the whole calculated
property? The probe creates its own disposable numeric property, disposable calculated property
and disposable company, reads one value back, then archives and deletes all three in a `finally`
block and confirms each is gone. It reads and writes no company record from the live population
(**D-16**).
  </what-built>
  <instructions>
Arming a live write is an operator action in this repo — the executor cannot set the keys. Run
exactly this, from the repo root:

`ALLOW_TIER_NULL_PROBE=true DRY_RUN=false .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/check_tier_null_propagation.py', run_name='__main__')"`

Then confirm, from the printed output:
1. The portal guard passed (portal `22617666`).
2. The probe printed the value it read back for the disposable company.
3. Teardown printed a gone-confirmation for both disposable properties and the disposable company.
4. `.planning/phases/50-derived-tier-property/50-NULL-PROBE.json` now exists and names a settled
   variant — `uncoalesced` (D-03 holds) or `coalesced_minus_one` (D-04 fires).

If the probe exits non-zero mid-run, confirm the `finally` teardown still ran and that no
disposable was left behind before re-running.
  </instructions>
  <verification>
    <automated>.venv/bin/python -c "import json; d=json.load(open('.planning/phases/50-derived-tier-property/50-NULL-PROBE.json')); assert d['settled_variant'] in ('uncoalesced','coalesced_minus_one'), d['settled_variant']; assert d['calculation_formula'].strip(), d; assert d['teardown']['all_gone'] is True, d['teardown']; print('probe evidence ok:', d['settled_variant'])"</automated>
  </verification>
  <resume-signal>Paste the probe output and the settled variant, or describe the failure.</resume-signal>
  <acceptance_criteria>
    - `50-NULL-PROBE.json` exists and its `settled_variant` is exactly `uncoalesced` or `coalesced_minus_one`.
    - `50-NULL-PROBE.json` carries the exact `calculationFormula` literal that must ship.
    - `50-NULL-PROBE.json` records the teardown gone-confirmations and the RESEARCH Q6 archived-list finding.
    - No company id from the live population appears anywhere in `50-NULL-PROBE.json`.
  </acceptance_criteria>
  <done>TIER-02's null question is answered against live records and the answer is committed as evidence, before any formula variant is committed to a permanent property.</done>
  <reversibility rating="reversible">Every object the probe creates is destroyed by its own teardown; the portal ends the run in the state it started.</reversibility>
</task>

<task type="auto">
  <name>Task 50-01-03: Declare lv_icp_tier_derived and generalize the formula applier</name>
  <files>config/hubspot_properties.yaml, scripts/apply_fit_score_formula.py, config/hubspot_flows/lv_icp_tier_derived-property.before.json</files>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json (the settled variant — this task's only input for the formula literal)
    - config/hubspot_properties.yaml lines 405-445 (the `lv_icp_tier` and `lv_icp_fit_score` declarations — the shape to mirror)
    - config/hubspot_flows/lv_icp_fit_score-property.after.json (the live-captured calculated-property template)
    - scripts/apply_fit_score_formula.py (full file — the formula PATCH tool being generalized)
    - scripts/sync_hubspot_properties.py lines 126-135 (`_create_property_live` passes the YAML declaration through verbatim)
  </read_first>
  <action>
Declare `lv_icp_tier_derived` in `config/hubspot_properties.yaml` under `companies.properties`,
adjacent to the existing `lv_icp_tier` entry. Mirror `lv_icp_fit_score`'s declaration exactly
except: `type: string`, `label: ICP Tier (Derived)`, `groupName: companyinformation`,
`fieldType: calculation_equation`, `options: []`, and a `calculationFormula` key carrying the
literal recorded in `50-NULL-PROBE.json`. Under **D-03** that literal is
`if coalesce(lv_anti_icp_flag, 0) = 1 then "D" elseif lv_icp_fit_score >= 70 then "A" elseif lv_icp_fit_score >= 40 then "B" elseif lv_icp_fit_score >= 15 then "C" else "Unscored"`;
under **D-04**'s forced fallback both bare `lv_icp_fit_score` references become
`coalesce(lv_icp_fit_score, -1)`. Take the literal from the probe artifact, never from this plan.
The name is `lv_icp_tier_derived` per **D-14** — the operator chose it explicitly over the
alternative and it is not to be substituted. This is the one property **D-01**'s scope lift
authorises; declare nothing else. The ladder carries exactly five values per **D-09** —
PARITY-01 stays an accepted divergence and no additional label is introduced here.

`scripts/sync_hubspot_properties.py::_create_property_live` POSTs the declaration verbatim
(`body = dict(prop)`), so the `calculationFormula` key rides along with the create. No change to
that script is needed. If HubSpot rejects `calculationFormula` at create time (RESEARCH assumption
A1 — a wrong shape 400s loudly rather than creating a broken property), the fallback is to create
the property without that key and PATCH the formula on afterwards with the generalized applier
below; record which path was taken in the task summary.

Generalize `scripts/apply_fit_score_formula.py` to take `--property NAME`, defaulting to
`lv_icp_fit_score` so the existing documented invocation keeps working byte-for-byte. Derive both
the archive path (`config/hubspot_flows/{NAME}-property.after.json`) and the property URL from that
argument. Keep its existing single-key `ALLOW_FORMULA_WRITE` gate exactly as it is — adding a
second key here would break the documented `lv_icp_fit_score` invocation, and **D-05**'s two-key
requirement is scoped to the null probe, not to this pre-existing tool.

Create `config/hubspot_flows/lv_icp_tier_derived-property.before.json` recording the pre-create
state — the property does not exist yet, so the file records that absence explicitly (an object
carrying the property name, `"exists": false`, and the UTC timestamp of the check), matching the
before/after snapshot pairing convention already used for `lv_icp_tier-property.*.json`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tier_derived_tools.py -x && .venv/bin/python -c "import yaml,sys; d=yaml.safe_load(open('config/hubspot_properties.yaml')); p=[x for x in d['companies']['properties'] if x['name']=='lv_icp_tier_derived']; assert len(p)==1, p; assert p[0]['type']=='string' and p[0]['fieldType']=='calculation_equation', p[0]; assert p[0]['calculationFormula'].strip(), p[0]; print('declaration ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config/hubspot_properties.yaml` contains exactly one `companies` property whose `name` is `lv_icp_tier_derived`, with `type: string` and `fieldType: calculation_equation`.
    - That declaration's `calculationFormula` is byte-identical to `50-NULL-PROBE.json`'s recorded literal.
    - `.venv/bin/python scripts/apply_fit_score_formula.py --help` lists a `--property` option whose default is `lv_icp_fit_score`.
    - `.venv/bin/python scripts/apply_fit_score_formula.py` with no arguments still resolves the archive path `config/hubspot_flows/lv_icp_fit_score-property.after.json`.
    - `config/hubspot_flows/lv_icp_tier_derived-property.before.json` exists and parses as JSON recording the property as not yet existing.
    - The existing test suite still passes: `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The property is fully declared with the settled formula and the repo's formula-apply tool can address it, with zero live calls made.</done>
  <reversibility rating="reversible">Config and script edits only; `git revert` restores the prior state.</reversibility>
</task>

<task type="tracer">
  <name>Task 50-01-04: End-to-end — create lv_icp_tier_derived live and read B off the 4 stuck records</name>
  <files>config/hubspot_flows/lv_icp_tier_derived-property.after.json</files>
  <precondition>The operator has run the armed create command given in this task's action, and `GET /crm/v3/properties/companies/lv_icp_tier_derived` returns 200. Arming a live schema write is an operator action in this repo; the executor halts and requests it if the property is absent.</precondition>
  <read_first>
    - config/hubspot_properties.yaml (the declaration written in Task 50-01-03)
    - .planning/phases/50-derived-tier-property/50-NULL-PROBE.json (the settled variant)
    - scripts/check_tier_derived_parity.py (built in Task 50-01-01)
    - scripts/sync_hubspot_properties.py lines 216-255 (`main()` — dry-run report, portal guard, partial-failure exit)
    - config/hubspot_flows/lv_icp_fit_score-property.after.json (the after-snapshot shape to mirror)
    - .planning/WINDOWS.md ids 9-12 (the 4 stuck records and their root cause)
  </read_first>
  <action>
Wire one path end to end: YAML declaration → HubSpot Properties API create → HubSpot's calculation
engine → live read-back of real company records → committed evidence.

First run the create disarmed and confirm the dry-run report names exactly one property pending
creation, `lv_icp_tier_derived`, and nothing else:
`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/sync_hubspot_properties.py', run_name='__main__')"`.
If the report names any other pending create, stop — **D-01**'s lift authorises one property only.

The armed create is the operator command named in this task's `<precondition>`:
`DRY_RUN=false ALLOW_HUBSPOT_PROPERTY_WRITES=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/sync_hubspot_properties.py', run_name='__main__')"`.
It exits non-zero on a partial failure, and it appends the confirmed create to an undo manifest
under `config/hubspot_migration/`.

Once the property is live: read it back with a direct GET and write
`config/hubspot_flows/lv_icp_tier_derived-property.after.json` from that response, mirroring
`lv_icp_fit_score-property.after.json`'s shape. Assert the live `calculationFormula` is
byte-identical to the YAML declaration and to `50-NULL-PROBE.json`'s literal — the property is the
formula's source of truth from here, and `scripts/apply_fit_score_formula.py --property lv_icp_tier_derived`
is the repeatable path back into line if it ever drifts. Apply `_assert_no_secrets` to the
snapshot before writing it.

Then prove the tracer: run
`.venv/bin/python scripts/check_tier_derived_parity.py --ids 9605273630,9604738976,17696004613,19100977027`
and confirm all four read `B` on `lv_icp_tier_derived` while `lv_icp_tier` still reads `C`. That
difference is **D-07**'s one expected class of mismatch and **D-10**'s whole delivery: their score
was already correct at 45, so the derived tier lands them on `B` the moment the property exists —
no event, no enrolment, no workflow run, no PATCH. Verify by reading them back and by nothing
else; per **D-16** any write to a company record here is a deviation requiring justification, not
a budgeted allowance. Also read back one never-scored company (blank `lv_icp_fit_score`) and
confirm its derived value matches the semantics `50-NULL-PROBE.json` recorded — blank under
**D-03**, `Unscored` under **D-04**.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/check_tier_derived_parity.py --ids 9605273630,9604738976,17696004613,19100977027 --out /tmp/50-tracer-parity.md && .venv/bin/python -c "import json; a=json.load(open('config/hubspot_flows/lv_icp_tier_derived-property.after.json')); n=json.load(open('.planning/phases/50-derived-tier-property/50-NULL-PROBE.json')); assert a['name']=='lv_icp_tier_derived', a['name']; assert a['calculated'] is True, a; assert a['type']=='string', a['type']; assert a['calculationFormula']==n['calculation_formula'], (a['calculationFormula'], n['calculation_formula']); print('tracer ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /crm/v3/properties/companies/lv_icp_tier_derived` returns 200 with `calculated: true` and `type: string`.
    - The live `calculationFormula` is byte-identical to the literal in `50-NULL-PROBE.json`.
    - `config/hubspot_flows/lv_icp_tier_derived-property.after.json` exists, parses, and carries no `Authorization` value and no bearer token.
    - The parity run over the 4 named ids reports all four as `expected_mismatch` (live `C`, derived `B`) and reports zero rows classified `defect`.
    - A never-scored company's derived value equals the value `50-NULL-PROBE.json` records for a null score.
    - The `config/hubspot_migration/` undo manifest from the create run names `lv_icp_tier_derived` and no other property.
  </acceptance_criteria>
  <done>The derived tier works end to end on real records: the 4 stuck records read the tier their score implies, produced by HubSpot's calculation engine with no workflow in the path.</done>
  <reversibility rating="costly">A created HubSpot property cannot be un-created, only archived, and archived internal names are generally not reusable (D-01's own reversibility note). The formula itself is PATCHable, so a wrong variant is cheap to correct; the property's existence is not.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scripts → HubSpot API | a live private-app bearer token crosses here; every call can mutate portal schema |
| script output → committed git artifacts | probe/snapshot JSON is committed and could carry credentials or PII |
| env → write gate | `DRY_RUN` / `ALLOW_TIER_NULL_PROBE` decide whether a run mutates the live portal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Information Disclosure | `50-NULL-PROBE.json`, `lv_icp_tier_derived-property.after.json` | high | mitigate | `_assert_no_secrets` (copied verbatim from `scripts/snapshot_hubspot_schema.py:78-83`) applied to every serialized payload before it is written to a committed path |
| T-50-02 | Tampering | `scripts/check_tier_null_propagation.py` live writes | high | mitigate | two-key gate — a write is refused unless `DRY_RUN=false` AND `ALLOW_TIER_NULL_PROBE=true`, both exact-string; disarmed is the default and is asserted by `tests/test_tier_derived_tools.py` |
| T-50-03 | Tampering | wrong-portal execution | high | mitigate | portal guard asserted before any API call in both new scripts (`HUBSPOT_EXPECTED_PORTAL_ID`, default `22617666`), refusing with exit 1 and zero calls made |
| T-50-04 | Denial of Service | disposable objects left behind by a mid-run failure | medium | mitigate | teardown in a `finally` block, each deletion confirmed by a 404 re-read; the probe reports the confirmation rather than assuming it |
| T-50-05 | Tampering | silent wrong-value computation from boolean/BigDecimal coercion | high | mitigate | `coalesce(lv_anti_icp_flag, 0) = 1` is the only accepted veto-guard shape (spike Round 1: a bare boolean and a `false` second argument both 400); pinned offline by `derived_tier(85, True) == "D"` and by Plan 03's formula pin test |
| T-50-06 | Elevation of Privilege | `lv_icp_tier_derived` becomes writable by any pipeline | low | accept | HubSpot sets `readOnlyValue: true` on calculated properties; nothing — including a corrective script — can PATCH the value. This is a positive integrity control, not a risk to mitigate |
| T-50-SC | Tampering | npm/pip/cargo installs | high | accept | no package-manager install occurs in this phase — RESEARCH § Standard Stack: "no `pip install` / `npm install` required" — so no package-legitimacy audit is applicable |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_tier_derived_tools.py -x` exits 0
- `.venv/bin/python -m pytest -q` exits 0 (no existing test regressed by the `apply_fit_score_formula.py` generalization)
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form — directory form is broken on node 24)
- `50-NULL-PROBE.json` records a settled variant and the literal that shipped
- The 4 stuck records read `B` on `lv_icp_tier_derived` with `lv_icp_tier` still `C`
</verification>

<success_criteria>
TIER-02's null semantics are a recorded choice backed by a live probe; `lv_icp_tier_derived` is
live with the settled formula; the 4 stuck records read `B` with zero writes to them.
</success_criteria>

<output>
Create `.planning/phases/50-derived-tier-property/50-01-SUMMARY.md` when done
</output>
