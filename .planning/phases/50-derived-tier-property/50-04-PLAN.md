---
phase: 50-derived-tier-property
plan: 04
type: execute
wave: 3
depends_on: [50-02, 50-03]
files_modified:
  - docs/OPERATOR-TIER-ROLLBACK.md
  - .planning/phases/50-derived-tier-property/50-DEPENDENTS-SWEEP.md
  - .planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md
autonomous: false
requirements: [TIER-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-18's forced re-enrolment mechanism is NAMED and PROVEN before WF1 is switched off, not after — this plan lands entirely ahead of Plan 05's WF1-off task."
    - "The runbook does not stop at 'toggle WF1 back on'. Re-enabling alone re-grades nothing, because a value-identical record fires no property-change event and re-enrolment needs an event to re-enrol on."
    - "RESEARCH Q1's negative is recorded rather than papered over: HubSpot's Automation v4 API exposes no enrolment endpoint of any kind, and the one legacy v2 endpoint is contacts-only and deprecated, so no API call can force a company back through WF1."
    - "Phase 47.5's request-level recompute POST is documented as NOT transferring: it drives n8n's own veto lane and never touches HubSpot's native Automation platform, so it cannot re-enrol a HubSpot-native flow."
    - "Exactly two real mechanisms are named, with the primary chosen: portal-UI manual enrolment (requires WF1 ON) as primary, and an armed, capped, disarm-verified perturb-then-restore double-write on the trigger property as the documented fallback."
    - "The perturb-then-restore fallback is labelled a deliberate D-16 deviation requiring justification — two real company writes per record, emergency path only, never the happy path."
    - "The drill subject is a correctly-tiered company that is NOT one of the 4 stuck records, because a workflow run on those would violate the phase's own 'no event, no enrolment, no workflow run' success criterion and would contaminate D-07's expected-mismatch set."
    - "The drill's proof is WF1's own execution history showing the manual enrolment completed, with the record's tier value unchanged — a value-identical outcome, so the drill demonstrates the mechanism without re-tiering anything."
    - "D-13's sweep is re-run immediately before cutover so anything added to the portal since Plan 02's first run is caught, and the operator confirms the API-blind manual half is still current."
  artifacts:
    - docs/OPERATOR-TIER-ROLLBACK.md
    - .planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md
  key_links:
    - "docs/OPERATOR-TIER-ROLLBACK.md -> PUT /automation/v4/flows/4625147345 with isEnabled true (rollback step 1) -> portal-UI manual enrolment (rollback step 2)"
    - "50-ROLLBACK-DRILL.md -> Plan 05's checkpoint:decision context; the decision cannot be taken without a proven rollback"
    - "scripts/sweep_tier_dependents.py re-run -> the pre-cutover section of 50-DEPENDENTS-SWEEP.md -> the same decision context"
  prohibitions:
    - "The rollback runbook never claims a re-enabled WF1 converges on its own."
    - "The drill is never performed against any of the 4 stuck records 9605273630, 9604738976, 17696004613, 19100977027."
    - "The perturb-then-restore fallback is never exercised in this plan; it is documented only."
    - "WF1 is not switched off by this plan."
---

<objective>
Name **D-18**'s forced re-enrolment mechanism and prove it works — while WF1 is still on, which is
the only window in which the primary mechanism can be proven at all — and refresh **D-13**'s
dependent sweep immediately before cutover.

Purpose: this is the phase's crux. A rollback that says "re-enable WF1" and stops there does
nothing for any record whose values are already correct, which is by definition every record the
derived property computed correctly. **D-18** rejects "re-enable and let it converge naturally"
explicitly, and requires the forced mechanism be named and proven *before* WF1 is switched off.
Plan 05 cannot run until this one has.
Output: an operator rollback runbook, a live drill record, and a fresh pre-cutover dependent sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-derived-tier-property/50-CONTEXT.md
@.planning/phases/50-derived-tier-property/50-RESEARCH.md
@.planning/phases/50-derived-tier-property/50-02-SUMMARY.md
@.planning/phases/50-derived-tier-property/50-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

| Symbol | Kind |
|---|---|
| `docs/OPERATOR-TIER-ROLLBACK.md` | new runbook (D-18) |
| `.planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md` | new evidence artifact |
| pre-cutover section of `50-DEPENDENTS-SWEEP.md` | refreshed D-13 sweep |

Produced by other plans: `scripts/check_tier_null_propagation.py`,
`scripts/check_tier_derived_parity.py`, `scripts/sweep_tier_dependents.py`,
`tests/test_tier_derived_tools.py`, `tests/test_sweep_tier_dependents.py`,
`tests/test_tier_formula_pin.py`, `ALLOW_TIER_NULL_PROBE`, `RETIRED_FLOW_IDS`,
`lv_icp_tier_derived`, `50-NULL-PROBE.json`, `50-TIER-PARITY-EVIDENCE.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 50-04-01: Write the rollback runbook and refresh the pre-cutover sweep</name>
  <files>docs/OPERATOR-TIER-ROLLBACK.md, .planning/phases/50-derived-tier-property/50-DEPENDENTS-SWEEP.md</files>
  <read_first>
    - docs/OPERATOR-VETO-REFRESH.md (full file — the runbook conventions this matches: numbered operator steps, literal commands, what-it-costs and what-it-does-not-change sections)
    - docs/OPERATOR-RESCORE.md (the arm/disarm write-window discipline and disclosure style)
    - .planning/phases/50-derived-tier-property/50-RESEARCH.md § Open Questions Q1 and § Common Pitfalls Pitfall 1
    - scripts/put_hubspot_flow.py and scripts/fetch_hubspot_flow.py (the live-proven `PUT /automation/v4/flows/{id}` mechanism and its snapshot pairing)
    - scripts/remediate_veto_companies.py lines 590-630 (`post_webhook_event` — the Phase 47.5 precedent, and precisely what it does not reach)
    - config/hubspot_flows/4625147345-wf1-set-icp-tier.after.json (WF1's enrollment criteria)
    - .planning/phases/50-derived-tier-property/50-DEPENDENTS-SWEEP.md (Plan 02's first run)
  </read_first>
  <action>
Create `docs/OPERATOR-TIER-ROLLBACK.md`, matching `docs/OPERATOR-VETO-REFRESH.md`'s conventions —
numbered operator steps, literal commands, an explicit what-it-costs section and a what-it-does-not-change
section.

**Step 1 — re-enable WF1.** `PUT /automation/v4/flows/4625147345` with `isEnabled: true`, via
`scripts/put_hubspot_flow.py`, restoring the state archived in
`config/hubspot_flows/4625147345-wf1-set-icp-tier.before.json`. Per **D-08** the definition is
never deleted, so this is a one-action restore.

**Step 2 — force the re-grade. The runbook must not stop at step 1.** State the reason in the
runbook itself: a value-identical write fires no property-change event, `shouldReEnroll: true`
notwithstanding, so a re-enabled WF1 has nothing to re-enrol on and re-grades nothing. Record
RESEARCH Q1's negative as a finding, not as a gap: HubSpot's Automation v4 API documents
create/read/update/delete for flow *definitions* and exposes **no enrolment endpoint of any kind**;
the one legacy v2 enrolment endpoint is contacts-only by path and flagged for deprecation, so it
cannot reach a company. Record explicitly that Phase 47.5's request-level `recompute: true` POST
does **not** transfer: it drives n8n's own veto-recompute lane, which PATCHes veto fields directly
and never calls HubSpot's Automation platform, so it has no code path that could re-enrol a
HubSpot-native flow.

Name exactly two real mechanisms and choose the primary:
- **Primary — portal-UI manual enrolment.** A human selects records on the companies index page or
  inside WF1 and enrols them. Requires WF1 to be **on**, which step 1 has just done. This is the
  mechanism Task 50-04-02 proves.
- **Fallback — armed, capped, disarm-verified perturb-then-restore double-write** on the trigger
  property (`lv_icp_fit_score` or `lv_anti_icp_flag`) for the specific records needing a re-grade:
  write a different value, let WF1 enrol, then restore. Label it plainly as a deliberate **D-16**
  deviation requiring justification — two real company writes per record — available only on the
  emergency path, never the happy path, and under the same armed/capped/disarm-and-read-back
  discipline as Phases 47–49. Do not exercise it here; document it.

Do not invent a third path. The repo's own history is full of clever workarounds that broke
silently; a mechanism with no live-proven precedent in this portal does not belong in a rollback
runbook.

Then re-run **D-13**'s sweep immediately before cutover, as that decision requires, and append the
result to `50-DEPENDENTS-SWEEP.md` as a dated pre-cutover section rather than overwriting Plan 02's
first run:
`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['sweep_tier_dependents.py', '--out', '/tmp/50-sweep-precutover.md']; runpy.run_path('scripts/sweep_tier_dependents.py', run_name='__main__')"`
State whether the scripted findings changed since the first run; a new dependent appearing here is
exactly what the re-run exists to catch.
  </action>
  <verify>
    <automated>test -s docs/OPERATOR-TIER-ROLLBACK.md && grep -q '4625147345' docs/OPERATOR-TIER-ROLLBACK.md && grep -q 'Step 2' docs/OPERATOR-TIER-ROLLBACK.md && .venv/bin/python -m pytest tests/test_sweep_tier_dependents.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `docs/OPERATOR-TIER-ROLLBACK.md` exists and contains a numbered step 1 naming `PUT /automation/v4/flows/4625147345` with `isEnabled: true`.
    - It contains a numbered step 2 that names both mechanisms and marks portal-UI manual enrolment as the primary.
    - It states, in its own text, that a value-identical write fires no property-change event and therefore a re-enabled WF1 re-grades nothing on its own.
    - It records that Automation v4 exposes no enrolment endpoint and that the legacy v2 endpoint is contacts-only.
    - It records that Phase 47.5's `recompute: true` POST does not reach a HubSpot-native flow.
    - It labels the perturb-then-restore fallback as a D-16 deviation requiring justification, emergency-path only.
    - `50-DEPENDENTS-SWEEP.md` carries a dated pre-cutover section alongside Plan 02's first run, and states whether the scripted findings changed.
  </acceptance_criteria>
  <done>The rollback mechanism is named, its impossible alternatives are recorded as findings, and the dependent inventory is current as of immediately before cutover.</done>
  <reversibility rating="reversible">Documentation and a read-only sweep run.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 50-04-02: Prove the rollback mechanism live, while WF1 is still on (D-18)</name>
  <files>.planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md</files>
  <action>
Select the drill subject from `50-TIER-PARITY-EVIDENCE.md` — any company classified as a **match**,
and none of `9605273630`, `9604738976`, `17696004613`, `19100977027`. Hand the operator the portal
steps below and halt; manual enrolment has no API (RESEARCH Q1) and is a human-only portal action.
When the operator returns, write `50-ROLLBACK-DRILL.md` recording the subject's id and name, its
tier before and after (which must be identical), WF1's execution-history evidence that the
enrolment completed, the date, and the disclosure that this value-identical WF1-authored write is a
noted **D-16** deviation justified by **D-18**'s requirement that the mechanism be proven rather
than asserted. If the operator reports that HubSpot refused the enrolment, record that as a
material finding and mark `docs/OPERATOR-TIER-ROLLBACK.md`'s primary mechanism unavailable, leaving
the perturb-then-restore fallback as the only path — then carry that into Plan 05's decision
context rather than proceeding as if the rollback were proven.
  </action>
  <read_first>
    - docs/OPERATOR-TIER-ROLLBACK.md (written in Task 50-04-01)
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md (to pick a drill subject that is a confirmed match, not one of the 4 stuck records)
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-10, D-16, D-18
  </read_first>
  <what-built>
A rollback runbook whose step 2 names portal-UI manual enrolment as the primary forced-re-grade
mechanism. **D-18** requires that mechanism be *proven*, not just named, and it can only be proven
while WF1 is **on** — HubSpot refuses manual enrolment into a workflow that is switched off. This
is therefore the last moment the proof is obtainable, and it must land before Plan 05 switches WF1
off.
  </what-built>
  <instructions>
Pick the drill subject from `50-TIER-PARITY-EVIDENCE.md`: any company classified as a **match** —
its `lv_icp_tier` and `lv_icp_tier_derived` already agree. It **must not** be one of
`9605273630`, `9604738976`, `17696004613`, `19100977027`. Running a workflow on those would break
this phase's own success criterion that they are fixed with no event, no enrolment and no workflow
run, and would contaminate D-07's expected-mismatch set.

Because the subject's tier is already correct, WF1 will recompute and write the same value — a
value-identical outcome. The drill proves the *mechanism*, not a data change.

In the HubSpot portal (`22617666`):
1. Confirm WF1 `4625147345` ("WF1 Set ICP Tier based on ICP Score") is still **on**.
2. Note the subject's current `lv_icp_tier` value before you start.
3. Manually enrol that one company into WF1 (companies index page, or from inside the workflow).
4. Open WF1's execution history and confirm the enrolment completed for that record.
5. Re-read the subject's `lv_icp_tier` and confirm it is unchanged.

Report back: the subject's id and name, its tier before and after, and what WF1's execution history
shows. If HubSpot refuses the manual enrolment, say so — that is a material finding, because it
would mean the primary mechanism does not exist and the runbook's fallback becomes the only path.
  </instructions>
  <verification>
    <automated>test -s .planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md && .venv/bin/python -c "
import re,sys
d=open('.planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md').read()
stuck={'9605273630','9604738976','17696004613','19100977027'}
found=set(re.findall(r'\b\d{9,12}\b', d))
subj=found-stuck
assert subj, 'no drill subject id recorded'
assert not (found &amp; stuck), f'drill subject must not be a stuck record: {found &amp; stuck}'
assert 'checked' in d.lower() or 'date' in d.lower(), 'drill date not recorded'
print('drill evidence ok:', sorted(subj))"</automated>
  </verification>
  <resume-signal>Report the drill subject, the before/after tier, and the execution-history evidence — or describe the refusal.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/50-derived-tier-property/50-ROLLBACK-DRILL.md` exists, is committed, and names the drill subject's id and company name.
    - The drill subject is recorded as a `match` row in `50-TIER-PARITY-EVIDENCE.md` and is none of the 4 stuck ids.
    - The artifact records the subject's tier before and after the drill and states they are identical.
    - The artifact records WF1's execution-history evidence that the manual enrolment completed.
    - The artifact discloses the drill's WF1-authored, value-identical company write as a noted D-16 deviation with its justification.
    - The artifact states the drill was performed while WF1 was still enabled, with the date.
  </acceptance_criteria>
  <done>D-18's forced re-enrolment mechanism is proven live, before WF1 is switched off, so the rollback path is real rather than asserted.</done>
  <reversibility rating="reversible">A value-identical workflow run; the record ends in the state it started.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → HubSpot portal UI | a human with workflow permissions enrols a record manually |
| repo scripts → HubSpot API | a live private-app bearer token crosses here (sweep re-run) |
| drill/runbook output → committed git artifacts | company ids and workflow ids are committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-16 | Tampering | the rollback drill re-tiering a real record | high | mitigate | drill subject is constrained to a confirmed `match` row, so WF1 recomputes and writes the same value; before/after tier is recorded and asserted identical |
| T-50-17 | Tampering | the drill contaminating D-07's evidence | high | mitigate | the 4 stuck ids are excluded by an explicit acceptance criterion; a workflow run on any of them would both violate D-10 and change an expected-mismatch row into a match |
| T-50-18 | Repudiation | a rollback path asserted but never exercised | high | mitigate | D-18 requires proof; the drill is performed live while WF1 is on and its execution-history evidence is committed to `50-ROLLBACK-DRILL.md` |
| T-50-19 | Information Disclosure | `50-ROLLBACK-DRILL.md`, `OPERATOR-TIER-ROLLBACK.md` | high | mitigate | artifacts record company ids, names, workflow ids and tier values only; the sweep re-run applies `_assert_no_secrets` before writing, and no runbook step embeds a token |
| T-50-20 | Elevation of Privilege | manual enrolment requires elevated portal permissions | low | accept | HubSpot's own portal permission model gates this; it is HubSpot's control, not this repo's, and needs no new code |
| T-50-SC | Tampering | npm/pip/cargo installs | high | accept | no package-manager install occurs in this phase (RESEARCH § Standard Stack: no `pip install` / `npm install` required), so no package-legitimacy audit is applicable |
</threat_model>

<verification>
- `docs/OPERATOR-TIER-ROLLBACK.md` is committed with both steps and both mechanisms
- `50-ROLLBACK-DRILL.md` is committed with live execution-history evidence
- `50-DEPENDENTS-SWEEP.md` carries a dated pre-cutover section
- `.venv/bin/python -m pytest -q` exits 0
- WF1 `4625147345` is still enabled at the end of this plan
</verification>

<success_criteria>
D-18's rollback mechanism is named, its impossible alternatives recorded, and the primary mechanism
proven live while WF1 is still on — before anything is switched off.
</success_criteria>

<output>
Create `.planning/phases/50-derived-tier-property/50-04-SUMMARY.md` when done
</output>
