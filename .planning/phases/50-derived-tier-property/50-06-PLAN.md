---
phase: 50-derived-tier-property
plan: 06
type: execute
wave: 5
depends_on: [50-03]
files_modified:
  - config/hubspot_properties.yaml
  - config/hubspot_flows/lv_anti_icp_flag_num-property.before.json
  - config/hubspot_flows/lv_anti_icp_flag_num-property.after.json
  - config/hubspot_flows/lv_icp_tier_derived-property.after.json
  - scripts/backfill_anti_icp_flag_num.py
  - scripts/build_cloud_workflows.py
  - scripts/check_tier_derived_parity.py
  - scripts/check_schema_drift.py
  - src/icp_scoring.py
  - n8n/wf_enrichment_cloud.json
  - tests/test_backfill_anti_icp_flag_num.py
  - tests/test_icp_scoring.py
  - tests/test_tier_formula_pin.py
  - tests/test_tier_derived_tools.py
  - tests/test_flow_rubric_conformance.py
  - tests/test_check_schema_drift.py
  - tests/n8n/antiIcpFlagMirror.test.mjs
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
  - .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md
  - .planning/phases/50-derived-tier-property/50-MIRROR-BACKFILL.md
  - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md
autonomous: false
requirements: [TIER-01, TIER-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "D-20: `lv_anti_icp_flag_num` exists live as a pipeline-written numeric 0/1 property. It is NOT calculated — `calculation_equation` reads only numeric properties, so there is no numeric upstream a formula could read, which is precisely why the mirror is required rather than preferred."
    - "D-20: the veto is computed ONCE per engine and serialized TWICE from that single computed value. `lv_anti_icp_flag` and `lv_anti_icp_flag_num` are two serializations of one derivation, not two derivations — the same architecture `produces_content_score` already uses to pre-translate a boolean into a number for `lv_icp_fit_score`."
    - "D-20: drift control is structural, not asserted. A behavioural test in the n8n engine (executing the built `Decide Company Action` node over every veto trigger) and an offline test in the Python oracle both fail loudly if the boolean and its mirror ever disagree, and a population-level agreement check surfaces any live record where they diverge."
    - "D-20: both engines land in ONE commit, per CLAUDE.md §10.3.1's Phase 46 parity rule, and `n8n/wf_enrichment_cloud.json` is regenerated by `scripts/build_cloud_workflows.py` — never hand-edited."
    - "D-21: the shipped `lv_icp_tier_derived` formula references `lv_icp_fit_score` bare. D-04 fired on a race, not a finding; the ~646 never-enriched companies flipped blank→`Unscored` are flipped back."
    - "D-21: `50-NULL-PROBE.json` stays committed byte-unaltered as historical evidence of what was believed at the time. The correction of record lives in `50-CONTEXT.md`'s amendment block and in this plan's own artifacts."
    - "D-22: every verdict taken from a calculated property's value is polled to a populated value (or a fixed ≥3 min ceiling). A single immediate read-back is not evidence — that is the process defect that produced D-04."
    - "D-16: exactly ONE company-write deviation is spent — backfilling `lv_anti_icp_flag_num` onto the live-derived set of `lv_anti_icp_flag=true` companies. It is named, justified, capped, armed, scoped by a live-derived id list, confirmed at a checkpoint before it writes, and disclosed in the SUMMARY. No other company write occurs anywhere in this plan."
    - "D-07's gate is re-run and its verdict stated plainly. The pre-registered expectation is 61 match / 4 expected_mismatch / 1 defect — a still-RED gate that keeps D-06 and D-08 blocked is an acceptable outcome, not something to force green."
    - "D-09: exactly five ladder values — `D`, `A`, `B`, `C`, `Unscored`. The sixth label `Needs Review` is not added; PARITY-01 stays a documented accepted divergence."
    - "D-19: the census is appended as a dated post-correction section, so 50-03's original FAIL stays visible alongside the new result."
  artifacts:
    - config/hubspot_flows/lv_anti_icp_flag_num-property.after.json
    - scripts/backfill_anti_icp_flag_num.py
    - tests/n8n/antiIcpFlagMirror.test.mjs
    - .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md
    - .planning/phases/50-derived-tier-property/50-MIRROR-BACKFILL.md
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md
  key_links:
    - "`Decide Company Action`'s single `vetoReasons.length > 0` derivation -> both `lv_anti_icp_flag` and `lv_anti_icp_flag_num` in the same PATCH body -> `lv_icp_tier_derived`'s veto guard -> tier `D`"
    - "`src/icp_scoring.py`'s computed `anti_icp_flag` -> the one serializer emitting both property values -> the offline oracle test"
    - "`scripts/backfill_anti_icp_flag_num.py` (armed, capped, live-derived id list) -> the 6 historically-vetoed companies -> `50-MIRROR-BACKFILL.md`"
    - "the uncoalesced formula -> `scripts/apply_fit_score_formula.py --property lv_icp_tier_derived` -> the ~646 never-enriched companies reading blank again"
    - "`scripts/check_tier_derived_parity.py`'s mirror-agreement check -> the appended section of `50-TIER-PARITY-EVIDENCE.md` -> Plan 04/05's decision context"
  prohibitions:
    - "`KNOWN_STUCK_IDS` is never extended. Coffs Harbour Racing Club (14752488879) stays a defect row and a WINDOWS.md id-14 entry; absorbing it would make the gate pass by redefinition."
    - "`50-NULL-PROBE.json` is never edited, in whole or in part."
    - "`n8n/wf_enrichment_cloud.json` is never hand-edited — it is regenerated by `scripts/build_cloud_workflows.py`."
    - "The backfill never writes any property other than `lv_anti_icp_flag_num`, and never touches a record outside the live-derived vetoed set confirmed at the checkpoint."
    - "`scripts/check_tier_derived_parity.py` gains no `requests.{post,patch,delete}` call of any kind — its zero-write guarantee is load-bearing."
    - "`lv_icp_tier` is not archived and WF1 (4625147345) is not switched off by this plan — that stays 50-05's gated work."
    - "The gate is not re-run against a hand-picked subset to produce a friendlier verdict; the population is re-derived live across all scored companies."
---

<objective>
Correct the two live findings that Plan 03's D-07 gate exposed: `lv_icp_tier_derived`'s veto guard
has never fired (it reads a `booleancheckbox`, and `calculation_equation` reads only numeric
properties), and D-04's coalesce was adopted on a race rather than a finding.

Purpose: `lv_icp_tier_derived` is currently WORSE than the stale enum for the 6 vetoed companies —
records correctly excluded on `lv_icp_tier` read a workable tier on the derived property. That
single fact blocks Plan 04 and Plan 05. This plan lands **D-20**'s numeric mirror, **D-21**'s
uncoalesced formula, and the drift control that keeps the mirror honest, then re-runs the gate and
states the verdict without softening it.
Output: `lv_anti_icp_flag_num` live and written by both engines, a corrected formula, one disclosed
**D-16** backfill deviation, and a refreshed D-07 verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-derived-tier-property/50-CONTEXT.md
@.planning/phases/50-derived-tier-property/50-PATTERNS.md
@.planning/phases/50-derived-tier-property/50-01-SUMMARY.md
@.planning/phases/50-derived-tier-property/50-03-SUMMARY.md
@.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by **this plan**:

| Symbol | Kind |
|---|---|
| `lv_anti_icp_flag_num` | new HubSpot company property (number, 0/1, pipeline-written, NOT calculated) — D-20 |
| `config/hubspot_flows/lv_anti_icp_flag_num-property.{before,after}.json` | new snapshot pair |
| `scripts/backfill_anti_icp_flag_num.py` | new armed, capped backfill script |
| `ALLOW_ANTI_ICP_MIRROR_BACKFILL` | new write allow-key (paired with `DRY_RUN=false`) |
| `MAX_BACKFILL_RECORDS` | new cap constant in the backfill script |
| `anti_icp_flag_properties()` | new serializer in `src/icp_scoring.py` — one derivation, two serializations |
| `flagIsSet` | new single-derivation local in `Decide Company Action` (`scripts/build_cloud_workflows.py`) |
| `mirror_disagrees()` | new pure predicate in `scripts/check_tier_derived_parity.py` |
| `render_mirror_section()` | new evidence section renderer in `scripts/check_tier_derived_parity.py` |
| `tests/n8n/antiIcpFlagMirror.test.mjs` | new behavioural drift test (n8n engine) |
| `tests/test_backfill_anti_icp_flag_num.py` | new offline test for the backfill script |
| `.planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md` | new read-only scope artifact (backfill blast radius) |
| `.planning/phases/50-derived-tier-property/50-MIRROR-BACKFILL.md` | new D-16 deviation record |

Produced by earlier plans and only *modified* here: `lv_icp_tier_derived`'s `calculationFormula`,
`scripts/check_tier_derived_parity.py`, `tests/test_tier_formula_pin.py`,
`tests/test_tier_derived_tools.py`, `50-TIER-PARITY-EVIDENCE.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 50-06-01: Declare and create lv_anti_icp_flag_num, and report the backfill blast radius (D-20)</name>
  <files>config/hubspot_properties.yaml, config/hubspot_flows/lv_anti_icp_flag_num-property.before.json, config/hubspot_flows/lv_anti_icp_flag_num-property.after.json, .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md</files>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md, the 2026-08-14 amendment block (D-20, D-21, D-22) — the contract for this whole plan
    - config/hubspot_properties.yaml around the `lv_icp_tier_derived` declaration and the five numeric score components (`org_type_score`, `geography_score`, `annual_revenue_score`, `produces_content_score`, `gambling_score`) — the template: a plain `number`/`number` pipeline-written component, NOT a `calculation_equation`
    - scripts/sync_hubspot_properties.py lines 1-90 (`_writes_allowed`, `_portal_ok`, `_has_credentials`, the per-property POST design note and the undo-manifest contract)
    - config/hubspot_flows/lv_icp_fit_score-property.after.json (the snapshot shape to mirror)
    - scripts/check_tier_derived_parity.py lines 40-100 (population selection, `FETCH_PROPS`)
    - .planning/phases/50-derived-tier-property/50-01-SUMMARY.md § Files Created/Modified (the before/after snapshot + undo-manifest convention this repeats)
  </read_first>
  <action>
Declare `lv_anti_icp_flag_num` in `config/hubspot_properties.yaml` alongside the five existing
numeric score components, in the same companies block: `type: number`, `fieldType: number`,
`groupName: companyinformation`, `options: []`, label "Anti ICP Flag (Numeric)". It carries **no**
`calculationFormula` — per **D-20** it is pipeline-written, because `calculation_equation` reads only
numeric properties and there is no numeric upstream a formula could read. Place it immediately after
`lv_anti_icp_flag` so the pair reads as a pair.

Write `config/hubspot_flows/lv_anti_icp_flag_num-property.before.json` recording the pre-create
absence (a live GET returning 404), matching 50-01's convention for `lv_icp_tier_derived`.

Then create it live. Run first disarmed to confirm the diff proposes exactly one create and nothing
else, then armed:
`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/sync_hubspot_properties.py', run_name='__main__')"`
then
`DRY_RUN=false ALLOW_HUBSPOT_PROPERTY_WRITES=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/sync_hubspot_properties.py', run_name='__main__')"`
If the disarmed diff proposes more than one create, stop and report — the declaration file has
drifted from the portal in some other way and that is a separate finding, not something to push
through. Snapshot the live read-back into `lv_anti_icp_flag_num-property.after.json`, applying the
repo's `_assert_no_secrets` idiom before writing.

Finally, produce `50-MIRROR-SCOPE.md` — a **read-only** report establishing the exact blast radius
the next task's checkpoint authorises. Search companies live for `lv_anti_icp_flag EQ true` and list
every hit: record id, name, current `lv_icp_tier`, `lv_icp_fit_score`, `lv_icp_tier_derived`. Do not
assume the count is 6 — 50-03's evidence found 6 within the 66-company *scored* population, and the
live authoritative set is whatever this search returns. State the count, state that the backfill will
write exactly one property (`lv_anti_icp_flag_num`) to exactly these records and nothing else, and
state that records whose flag is `false` or unset are deliberately NOT written: a null mirror and a
`0` mirror are indistinguishable to the veto guard, so writing them would spend company writes for no
behavioural change.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import yaml
d=yaml.safe_load(open('config/hubspot_properties.yaml'))
p=[x for x in d['companies']['properties'] if x['name']=='lv_anti_icp_flag_num']
assert len(p)==1, 'lv_anti_icp_flag_num not declared exactly once'
assert p[0]['type']=='number' and p[0]['fieldType']=='number', p[0]
assert 'calculationFormula' not in p[0], 'the mirror must be pipeline-written, not calculated (D-20)'
print('declared ok:', p[0])" && test -s .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - `config/hubspot_properties.yaml` declares `lv_anti_icp_flag_num` exactly once, as `type: number` / `fieldType: number`, with no `calculationFormula` key.
    - A disarmed `sync_hubspot_properties.py` run, executed BEFORE the armed one, proposed exactly one property create.
    - `GET /crm/v3/properties/companies/lv_anti_icp_flag_num` returns 200 with `calculated` false, and that read-back is captured in `config/hubspot_flows/lv_anti_icp_flag_num-property.after.json`.
    - `config/hubspot_flows/lv_anti_icp_flag_num-property.before.json` records the pre-create 404.
    - An undo manifest was written naming exactly one property.
    - `50-MIRROR-SCOPE.md` lists every live `lv_anti_icp_flag=true` company by id and name, states the total count, and states that exactly one property will be written to exactly those records.
    - `50-MIRROR-SCOPE.md` records that no HubSpot write of any kind was issued to a company record by this task.
  </acceptance_criteria>
  <done>The mirror property exists live, is declared in the repo, and the exact set of records the next write window would touch is enumerated and committed.</done>
  <reversibility rating="costly">Per D-20, a created HubSpot property cannot be un-created — only soft-archived, and archived internal names are not reusable (proven by 50-NULL-PROBE.json's `calculated_property_reappears` finding). The declaration and snapshots are plain text reverts; the live property is not.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 50-06-02: Authorise the phase's one D-16 company-write deviation</name>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md (written by Task 50-06-01 — the exact id list and count)
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-16 and § D-20
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md § SEVERITY callout (what these records currently read)
  </read_first>
  <decision>Whether to spend the phase's first and only D-16 company-write deviation: writing `lv_anti_icp_flag_num` to the companies enumerated in `50-MIRROR-SCOPE.md`.</decision>
  <context>
**D-16** declares zero company write windows for this phase. That declaration was made on the
premise that a calculated property computes itself and **D-10** needs no PATCH. **D-20** breaks that
premise: `calculation_equation` reads only numeric properties, so the veto branch cannot be expressed
at all without a numeric mirror, and a mirror on a historically-vetoed record can only get there by
being written. This is therefore a deviation requiring justification — exactly what **D-16** says to
do with one — not a budgeted allowance.

**What gets written.** One property, `lv_anti_icp_flag_num`, set to `1`, on exactly the records
`50-MIRROR-SCOPE.md` enumerates (every live company whose `lv_anti_icp_flag` is already `true`).
The value is copied from a property the record already carries — it is not a re-derivation, so no
record's veto status can change as a result of the write itself.

**What does not get written.** Nothing else. No canonical field, no score, no tier, no status, no
record outside that list. Records whose flag is `false` or unset are not written: a null mirror and a
`0` mirror are indistinguishable to the veto guard.

**What it costs.** One HubSpot PATCH per record. Zero n8n executions, zero Anthropic calls, zero
provider credits.

**What happens if you decline.** The 6 historically-vetoed companies keep reading a workable
score-based tier on `lv_icp_tier_derived` — Simtech LED and Jam TV read `B`, Sportsbet and Big Screen
Video read `C` — which is the defect **D-07**'s gate caught and the reason Plan 04 and Plan 05 are
blocked. The mirror would then only populate as each record is next enriched by the pipeline, which
is the daily poller at best and never for a record with complete inputs (§13.0's skip lane).

**Reversibility.** Fully reversible in effect: the mirror is a derived value the pipeline recomputes
on every run, so a wrong write self-corrects on the record's next enrichment. The property itself
already exists at this point regardless of this decision.
  </context>
  <options>
    <option id="backfill-scoped">
      <name>Authorise: write `lv_anti_icp_flag_num` to exactly the ids in `50-MIRROR-SCOPE.md`</name>
      <pros>The veto branch becomes reachable for the records it exists to protect; D-07's gate can be re-run against a corrected property; the deviation is one property on a named, capped, live-derived set.</pros>
      <cons>Spends the phase's zero-write declaration; must be disclosed in the SUMMARY as a deviation, not as budgeted work.</cons>
    </option>
    <option id="no-backfill">
      <name>Decline: ship the mirror and the formula, let the pipeline populate it naturally</name>
      <pros>D-16's zero-write posture is preserved literally.</pros>
      <cons>The 6 vetoed records stay mis-tiered on the derived property for an unbounded period — a record with complete inputs is skipped by the poller entirely (§13.0), so "naturally" may mean never. D-07's gate cannot pass and Plans 04/05 stay blocked.</cons>
    </option>
    <option id="backfill-plus-recompute">
      <name>Authorise the backfill AND additionally force a full pipeline recompute on those records</name>
      <pros>Every veto field is re-derived from current inputs by the real engine rather than copied.</pros>
      <cons>Far larger blast radius — a recompute writes many properties per record and needs an armed, record-scoped n8n allowlist; it re-opens questions this plan does not need to answer. Not the default.</cons>
    </option>
  </options>
  <resume-signal>Select: backfill-scoped, no-backfill, or backfill-plus-recompute.</resume-signal>
  <acceptance_criteria>
    - The selected option and the exact authorised id list are recorded verbatim in `50-MIRROR-BACKFILL.md` with the date.
    - If `no-backfill` is selected, Task 50-06-03's backfill step does not run; the formula change still lands and the gate is still re-run, with the un-backfilled records disclosed as a known, decided-upon residual.
    - No company write has occurred before this checkpoint resolves.
  </acceptance_criteria>
  <done>The operator has authorised or declined the phase's one D-16 deviation, against an enumerated id list rather than an estimate.</done>
  <reversibility rating="reversible">The decision itself writes nothing.</reversibility>
</task>

<task type="tracer">
  <name>Task 50-06-03: End-to-end — backfill the mirror, ship the corrected formula, poll one record to D (D-16, D-20, D-21, D-22)</name>
  <files>scripts/backfill_anti_icp_flag_num.py, tests/test_backfill_anti_icp_flag_num.py, config/hubspot_properties.yaml, config/hubspot_flows/lv_icp_tier_derived-property.after.json, tests/test_tier_formula_pin.py, .planning/phases/50-derived-tier-property/50-MIRROR-BACKFILL.md</files>
  <precondition>Task 50-06-02 selected `backfill-scoped` or `backfill-plus-recompute`, and `50-MIRROR-SCOPE.md` carries the authorised id list. If `no-backfill` was selected, the backfill half of this task does not run and the formula half proceeds unchanged.</precondition>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-MIRROR-SCOPE.md and 50-MIRROR-BACKFILL.md (the authorised scope)
    - scripts/rescore_population.py lines 1-140 and 260-300 (`_writes_allowed`, `_portal_ok`, `assert_payload_scope`, `_settle_one`/`settle_population` — the armed-window, payload-scope-assertion and poll idioms this script copies)
    - scripts/apply_fit_score_formula.py (full file — the archive-is-source-of-truth contract, the `--property` flag, the `ALLOW_FORMULA_WRITE` gate, and the independent re-read verification)
    - config/hubspot_flows/lv_icp_tier_derived-property.after.json (the live-canonicalized formula text this task replaces)
    - tests/test_tier_formula_pin.py (full file — `PINNED_VETO_GUARD`, `_SCORE_BRANCH_RE`, the mutation cases and `test_sentinel_collision_is_behaviour_preserving`, all of which this task must update)
    - .planning/phases/50-derived-tier-property/50-01-SUMMARY.md § Deviations (HubSpot canonicalizes stored formula text on create/update — the trap this task must not fall into)
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-21 and § D-22
  </read_first>
  <action>
This is the thin end-to-end slice: mirror value on a record → corrected formula → the derived tier
that record reads. Prove it on one record before any of Task 50-06-04's breadth is built.

**Ordering, and why.** The backfill runs BEFORE the formula change, and the engine wiring
(Task 50-06-04) lands AFTER the formula. That ordering opens no regression window: the veto guard
does not fire today for any record, so an un-backfilled record behaves after this task exactly as it
behaves now, while a backfilled record becomes correct. Do not "fix" the ordering by moving the
engine wiring earlier — the engine change is a large diff and belongs in its own commit with its
drift tests.

**Step 1 — build `scripts/backfill_anti_icp_flag_num.py`, disarmed and tested first.**
Follow the repo's script idioms exactly, copied from `rescore_population.py`: `_has_credentials()`
skip-to-exit-0, `_portal_ok()` portal guard asserted before any call, a two-key write gate
(`DRY_RUN=false` AND a new `ALLOW_ANTI_ICP_MIRROR_BACKFILL=true` — its own key, never another
script's), and the dotenv-wrapper invocation block in the module docstring. Modes: `--plan` (default,
prints the exact PATCH bodies it would issue and exits 0, zero network writes) and `--execute`.

Its target set is re-derived LIVE on every invocation via a `lv_anti_icp_flag EQ true` company
search — never read from `50-MIRROR-SCOPE.md` or any local snapshot, so a stale artifact cannot widen
the blast radius. Add `MAX_BACKFILL_RECORDS = 10` and refuse the whole run with a non-zero exit if the
live set exceeds it; a cap that has to be raised by an edit is a cap that gets noticed. Add a
payload-scope assertion in the shape of `rescore_population.py::assert_payload_scope`: every PATCH
body must contain exactly the one key `lv_anti_icp_flag_num` and nothing else, raising rather than
sending if it does not. Verify each write by an independent per-record re-read, never from the PATCH
response body.

Pin it offline in `tests/test_backfill_anti_icp_flag_num.py`: the payload-scope assertion rejects a
body carrying a second key; the cap refuses a set of 11; the gate is closed unless both env keys are
set; the value written for a `true` flag is `1`; and a record whose flag is not `true` is never
included in the target set.

**Step 2 — run the backfill armed.** Disarmed `--plan` first, confirm the printed ids match the
checkpoint-authorised list exactly, then:
`DRY_RUN=false ALLOW_ANTI_ICP_MIRROR_BACKFILL=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['backfill_anti_icp_flag_num.py', '--execute']; runpy.run_path('scripts/backfill_anti_icp_flag_num.py', run_name='__main__')"`
Record the whole window in `50-MIRROR-BACKFILL.md`: the authorised option, the live-derived id list,
each PATCH with its status code, each verification re-read, and the explicit statement that this is
the phase's one **D-16** deviation with its justification.

**Step 3 — ship the corrected formula.** The new ladder keeps the veto branch positionally first and
keeps exactly five labels (**D-09**). Its guard reads the numeric mirror instead of the boolean, in the
same `coalesce(<numeric>, 0) = 1` shape the live property already proves HubSpot accepts. Its three
score comparisons reference `lv_icp_fit_score` bare, per **D-21** — the shipped formula must end up
with exactly one `coalesce(` call in total, the veto guard's.

Update the declaration in `config/hubspot_properties.yaml` to the new submitted literal, then set
`config/hubspot_flows/lv_icp_tier_derived-property.after.json`'s `calculationFormula` to that same
literal — `apply_fit_score_formula.py` treats the archive as the source of truth and only pushes it.
Then:
`ALLOW_FORMULA_WRITE=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['apply_fit_score_formula.py', '--property', 'lv_icp_tier_derived']; runpy.run_path('scripts/apply_fit_score_formula.py', run_name='__main__')"`

Expect the script's own re-read verification to report a mismatch and exit 1 even on a successful
PATCH: HubSpot canonicalizes stored formula text (50-01's disclosed finding — the spelling of the
equality operator, quote style, and inserted line breaks all change). That is not a failure. Refresh
`lv_icp_tier_derived-property.after.json` from the live read-back so the archive records the true
stored text, then re-run the same command and confirm it now reports the property in sync and exits 0.
Do not chase byte-identity against the submitted literal — that trap is what 50-01 flagged forward.

**Step 4 — update the formula pin (D-17 item 1) in the same commit as the formula.**
In `tests/test_tier_formula_pin.py`: repoint `PINNED_VETO_GUARD` at the mirror; widen
`_SCORE_BRANCH_RE` if needed so it still parses bare score references (it already accepts both
forms); update every mutation case whose fixture text embeds the old wrapped score references, so the
mutations still mutate a formula that exists. Replace
`test_sentinel_collision_is_behaviour_preserving` with a D-21 test asserting the shipped ladder is
uncoalesced on the score — assert the declared formula's total `coalesce(` count is exactly 1, and
carry D-21's rationale in the docstring. And in
`test_pinned_ladder_matches_declared_formula`, replace the byte-identity assertion against
`50-NULL-PROBE.json` with a D-21 divergence assertion: the declared formula must now DIFFER from the
frozen probe literal, and the docstring must state why. `50-NULL-PROBE.json` is not edited — it is
historical evidence of what was believed at the time, and the correction of record lives in
`50-CONTEXT.md`'s amendment block.

**Step 5 — poll, never single-read (D-22).** Applying the formula triggers a portal-wide recompute.
Poll `lv_icp_tier_derived` to a populated value with a ≥3 min ceiling on three controls, and record
each in `50-MIRROR-BACKFILL.md`:
- Simtech LED `18047161864` — backfilled, `lv_anti_icp_flag=true` — must settle to `D`.
- Rockhampton Jockey Club `9604732795` — a `match` row from `50-TIER-PARITY-EVIDENCE.md` — must
  still read `B`, proving the uncoalesce did not disturb a scored record.
- Any never-scored company (one from the `NOT_HAS_PROPERTY(lv_icp_fit_score)` set 50-01 sized at 646)
  — must read blank again, not `"Unscored"`. This is **D-21**'s un-flip, and a single immediate read
  is not evidence of it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backfill_anti_icp_flag_num.py tests/test_tier_formula_pin.py -x && .venv/bin/python -c "
import re, yaml
d=yaml.safe_load(open('config/hubspot_properties.yaml'))
f=[x for x in d['companies']['properties'] if x['name']=='lv_icp_tier_derived'][0]['calculationFormula']
assert f.count('coalesce')==1, f'D-21: exactly one coalesce (the veto guard) expected: {f}'
assert 'lv_anti_icp_flag_num' in f, 'veto guard must read the numeric mirror (D-20)'
labels=re.findall(r'[\x22\x27]([A-Za-z]+)[\x22\x27]', f)
assert labels==['D','A','B','C','Unscored'], f'D-09 ladder must be veto-first, five labels: {labels}'
print('formula ok:', f)" && .venv/bin/python -c "
from dotenv import load_dotenv; load_dotenv()
import sys, time; sys.path.insert(0,'.')
from src.hubspot_client import get_record
deadline = time.time() + 200
props = {}
while time.time() < deadline:
    props = get_record('companies','18047161864',['lv_icp_tier_derived','lv_anti_icp_flag','lv_anti_icp_flag_num'])['properties']
    if props.get('lv_icp_tier_derived') == 'D':
        print('D-22 poll settled:', props); sys.exit(0)
    time.sleep(10)
print('NOT SETTLED after ceiling:', props); sys.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/backfill_anti_icp_flag_num.py` exists, is disarmed by default, gates on both `DRY_RUN=false` and `ALLOW_ANTI_ICP_MIRROR_BACKFILL=true`, asserts the portal id before any call, and refuses a target set larger than `MAX_BACKFILL_RECORDS`.
    - A `--plan` run was executed before the armed run, and its printed id list matches Task 50-06-02's authorised list exactly.
    - Every PATCH body issued by the backfill carried exactly one key, `lv_anti_icp_flag_num`, proven by the payload-scope assertion and recorded in `50-MIRROR-BACKFILL.md`.
    - Each backfilled record's value is confirmed by an independent per-record re-read, not from the PATCH response body.
    - `tests/test_backfill_anti_icp_flag_num.py` passes and includes cases for the payload-scope rejection, the cap refusal, the closed gate, and the exclusion of non-`true` records.
    - The declared `calculationFormula` for `lv_icp_tier_derived` contains exactly one `coalesce(` call, names `lv_anti_icp_flag_num` in its opening `if` clause, and carries exactly the five D-09 labels.
    - `scripts/apply_fit_score_formula.py --property lv_icp_tier_derived` reports the property in sync and exits 0 on a re-run after the archive was refreshed from the live read-back.
    - `tests/test_tier_formula_pin.py` passes, its veto-guard pin names the mirror, and it carries a D-21 test asserting the shipped ladder is uncoalesced on the score.
    - `50-NULL-PROBE.json` is byte-identical to its state before this plan (`git diff` reports no change to it).
    - `50-MIRROR-BACKFILL.md` records all three D-22 polls with their elapsed time or poll count: Simtech LED `18047161864` settling to `D`, Rockhampton `9604732795` still `B`, and a named never-scored company reading blank.
  </acceptance_criteria>
  <done>One backfilled, vetoed company reads `D` on `lv_icp_tier_derived` end to end — mirror written, formula corrected, value polled — and a never-scored company reads blank again.</done>
  <reversibility rating="costly">The formula is a one-command revert, but the ~646 never-enriched companies will have been visibly re-labelled twice by then, and the backfill's company writes are disclosed rather than undone. No irreversible act occurs.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 50-06-04: Both engines emit the mirror from one derivation, with drift control (D-20)</name>
  <files>src/icp_scoring.py, scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/test_icp_scoring.py, tests/n8n/antiIcpFlagMirror.test.mjs, scripts/check_tier_derived_parity.py, tests/test_tier_derived_tools.py, tests/test_flow_rubric_conformance.py, scripts/check_schema_drift.py, tests/test_check_schema_drift.py</files>
  <read_first>
    - CLAUDE.md §10.3.1 (the OR predicate and the Phase 46 two-engine parity rule: both engines change in ONE commit, and `n8n/wf_enrichment_cloud.json` is never hand-edited)
    - src/icp_scoring.py lines 106-166 (the veto block, `anti_icp_flag`, and `ICPScoreResult`)
    - src/merge_policy.py lines 346-360 (Approach C — the canonical WRITE of the ICP outputs was REMOVED in Phase 15; Python writes none of these fields to HubSpot)
    - scripts/build_cloud_workflows.py around the `Decide Company Action` veto block (`vetoReasons`, the `properties.lv_anti_icp_flag` assignment and the D-04/P4 string-literal rule immediately below it)
    - tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs lines 1-60 (`loadNodeJsCode`/`runCodeNode` — the harness that executes the BUILT node code, reused verbatim)
    - tests/test_icp_scoring.py (the offline oracle test module this extends)
    - tests/test_flow_rubric_conformance.py around `VETO_PROPERTY_NAMES` and `test_no_archived_flow_writes_veto_properties`
    - scripts/check_schema_drift.py `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` and `_compute_do_not_archive`
    - scripts/check_tier_derived_parity.py `FETCH_PROPS`, `build_rows`, `render_evidence_markdown`
    - .planning/phases/49-re-score-strategy-reporting/49-DEPLOY-PROOF.md (§3 deploy, §4 bounce — the exact deploy-and-bounce procedure and its proof standard)
    - docs/OPERATOR-VETO-REFRESH.md § Deploy mechanics (a bare PUT stores but does not reload; the bounce is mandatory)
  </read_first>
  <behavior>
    - Given a veto fires by any trigger (non-ANZ region, `lv_produces_content` false, `lv_is_hardware_vendor` true, or `lv_org_type` hardware_vendor): the emitted `lv_anti_icp_flag` is the string `"true"` AND `lv_anti_icp_flag_num` is the string `"1"`.
    - Given no veto fires: `lv_anti_icp_flag` is `"false"` AND `lv_anti_icp_flag_num` is `"0"`.
    - No input produces any other combination of the two — the pair is never half-set, in either engine.
    - Population check: a live record whose flag reads `true` while its mirror is null or not 1 is reported as a divergence; a record whose flag is `false`/unset with a null mirror is NOT reported (that is the normal, un-backfilled state for roughly 60 of the 66 scored companies).
  </behavior>
  <action>
**The Python nuance, stated up front so it is not re-discovered.** `src/merge_policy.py`'s Approach C
removed the canonical write of the ICP outputs in Phase 15 — the Python engine writes none of these
fields to HubSpot; the n8n `Decide Company Action` node is the sole live writer, and
`src/icp_scoring.py` is the **oracle** the parity tests assert live state against. Do not go looking
for a Python write path, and do not re-add one. What Python gains is the serializer, so the oracle
knows what a correct record looks like for both properties.

**Python side.** Add one serializer to `src/icp_scoring.py` — `anti_icp_flag_properties(flag)`,
returning both `lv_anti_icp_flag` and `lv_anti_icp_flag_num` from the single boolean argument, using
the string values the pipeline PATCHes (the repo's D-04/P4 rule: string literals in HubSpot bodies,
never bare booleans). Per **D-20** this is one derivation and two serializations; the computed
`anti_icp_flag` in `compute_icp_score` stays the single source. Pin it offline in
`tests/test_icp_scoring.py`: both branches agree, no branch produces a half-set pair, and the veto
computed by `compute_icp_score` for a vetoed fixture and a non-vetoed fixture round-trips through the
serializer consistently.

**n8n side.** In `scripts/build_cloud_workflows.py`'s `Decide Company Action`, replace the inline
`vetoReasons.length > 0` test with a single named local — `const flagIsSet = vetoReasons.length > 0;`
— and assign BOTH properties from `flagIsSet`, adjacent, in the same block. That adjacency is the
structural guarantee the operator's objection asked for: there is one predicate, and a future edit
that changes it changes both. Then regenerate the workflow:
`.venv/bin/python scripts/build_cloud_workflows.py`
Never hand-edit `n8n/wf_enrichment_cloud.json`.

**Drift control, behavioural not asserted.** Add `tests/n8n/antiIcpFlagMirror.test.mjs`, reusing
`decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs`'s `loadNodeJsCode`/`runCodeNode` harness
verbatim so it executes the BUILT node code rather than grepping it. Drive a matrix covering each
veto trigger independently, several triggers together, and the clean no-veto case, and assert in
every case that the two emitted values are a matching pair. A test that only checks the veto case
would pass a build that hardcodes the mirror; the no-veto case is what gives it teeth.

**Population-level agreement check.** In `scripts/check_tier_derived_parity.py`: add
`lv_anti_icp_flag_num` to `FETCH_PROPS`, add a pure `mirror_disagrees(flag, num)` predicate, and add
a `render_mirror_section()` that lists any diverging record in the evidence artifact. Get the
predicate's null handling right or it is worthless: treat the flag as set only when it reads `true`,
treat the mirror as set only when it parses to 1 (guarding None and empty string before any numeric
coercion), and report a divergence only when those two differ. Flag `false`-or-unset with a null
mirror is agreement, not divergence — that is the expected state for most of the population and
reporting it would drown the real signal. Flag `true` with a null mirror is the dangerous state and
must be reported. Pin both directions in `tests/test_tier_derived_tools.py`. This script issues no
write of any kind and must not gain one.

**Two existing guards learn about the mirror.** Add `lv_anti_icp_flag_num` to
`VETO_PROPERTY_NAMES` in `tests/test_flow_rubric_conformance.py`, so no future HubSpot-native flow can
become a second writer of it — that is §13.0's single-writer rule machine-checked for the mirror. Add
it to `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` in `scripts/check_schema_drift.py` and extend the offline
pin in `tests/test_check_schema_drift.py`: archiving the mirror would make the veto guard fall
through to the score ladder again, which is exactly the defect this plan exists to close. Note that
`scripts/check_schema_drift.py` is also edited by Plan 05 — these are sequential edits to the same
file, not parallel ones.

**Deploy and bounce.** The repo must not claim a fix that production does not have; a stored PUT
alone never reloads a running workflow. Follow `49-DEPLOY-PROOF.md`'s procedure exactly: deploy with
`DRY_RUN=false ALLOW_N8N_DEPLOY=true .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/deploy_n8n_workflows.py', run_name='__main__')"`,
then bounce (deactivate, verify, reactivate, verify) the `LV Enrichment (Cloud template)` workflow
`950HPb7a1GgSAIyZ`. This deploy arms no write allowlist. If an unarmed recompute POST against Simtech
LED `18047161864` (§13.0's request-level `recompute: true`, which returns `write_blocked` when
nothing is armed) exposes the running node's derived properties in its response or execution data,
use it as the running-content proof; if it does not surface them, say so and fall back to the stored
read-back plus the offline behavioural test rather than claiming a proof you did not obtain.

**One commit.** The Python serializer, the builder change, the regenerated workflow JSON, both drift
tests and both guard edits land together, per CLAUDE.md §10.3.1's parity rule.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && node -e "
const fs=require('fs');
const wf=JSON.parse(fs.readFileSync('n8n/wf_enrichment_cloud.json','utf8'));
const js=wf.nodes.find(n=>n.name==='Decide Company Action').parameters.jsCode;
const uses=(js.match(/flagIsSet/g)||[]).length;
if(uses<3) throw new Error('expected one derivation + two serializations from flagIsSet, found '+uses+' references');
if(!js.includes('lv_anti_icp_flag_num')) throw new Error('built node does not emit the numeric mirror');
console.log('built node ok: flagIsSet references =',uses);
" && node --test tests/n8n/*.test.mjs && .venv/bin/python -m pytest tests/test_icp_scoring.py tests/test_tier_derived_tools.py tests/test_flow_rubric_conformance.py tests/test_check_schema_drift.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/icp_scoring.py` exposes one serializer producing both property values from a single boolean argument, and `tests/test_icp_scoring.py` pins both branches plus the round-trip from `compute_icp_score`.
    - The built `Decide Company Action` node in `n8n/wf_enrichment_cloud.json` derives the veto once into a single named local and assigns both `lv_anti_icp_flag` and `lv_anti_icp_flag_num` from it.
    - `n8n/wf_enrichment_cloud.json` was regenerated by `.venv/bin/python scripts/build_cloud_workflows.py` and not hand-edited; re-running the builder after the commit leaves the file unchanged.
    - `tests/n8n/antiIcpFlagMirror.test.mjs` executes the built node code and asserts the pair agrees for: each veto trigger alone, multiple triggers together, and the clean no-veto case.
    - `node --test tests/n8n/*.test.mjs` exits 0.
    - `scripts/check_tier_derived_parity.py` fetches `lv_anti_icp_flag_num` and exposes `mirror_disagrees`, pinned in `tests/test_tier_derived_tools.py` for all four combinations: flag true + mirror 1 (agree), flag true + mirror null (divergence), flag false + mirror null (agree), flag false + mirror 1 (divergence).
    - `scripts/check_tier_derived_parity.py` still issues no `requests.{post,patch,delete}` call anywhere in the module, verified by reading it.
    - `VETO_PROPERTY_NAMES` in `tests/test_flow_rubric_conformance.py` includes the mirror, and the suite passes.
    - `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` in `scripts/check_schema_drift.py` includes the mirror, with the offline pin in `tests/test_check_schema_drift.py` updated to match.
    - The deploy and bounce are recorded with invocation, status codes and the post-bounce verification; if the running-content proof could not be obtained from an unarmed recompute POST, that is stated rather than claimed.
    - `git show --stat HEAD` shows the Python serializer, the builder change, the regenerated workflow JSON, both drift tests and both guard edits in ONE commit.
  </acceptance_criteria>
  <done>Both engines emit the mirror from a single derivation, the running n8n instance carries the change, and a future edit that updates one side without the other fails a test in both engines.</done>
  <reversibility rating="reversible">Code and a workflow redeploy; the previous workflow body is recoverable from git and redeployable by the same command.</reversibility>
</task>

<task type="auto">
  <name>Task 50-06-05: Re-run D-07's gate and the D-19 census, append the verdict, amend the ledgers</name>
  <files>.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - .planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md (full file — the FAIL verdict and census this appends to, and the exact row-level baseline the expectation below is derived from)
    - scripts/check_tier_derived_parity.py (`main`, `--census`, `--out`, `_append_or_write`, `KNOWN_STUCK_IDS`)
    - .planning/phases/50-derived-tier-property/50-03-SUMMARY.md § Decisions Made (why Coffs Harbour was NOT folded into the exception list)
    - .planning/WINDOWS.md ids 13 and 14
    - .planning/REQUIREMENTS.md § Tier Derivation, § Out of Scope (the D-01 amendment's dated, additive style), and the TIER-01/TIER-02 traceability rows
    - .planning/phases/50-derived-tier-property/50-CONTEXT.md § D-07, § D-09, § D-19, § D-20, § D-21
  </read_first>
  <action>
**Pre-registered expectation, stated before the run so the report is a test and not a narrative.**
Derived from 50-03's committed 66-row table plus this plan's corrections: 61 match, 4
expected_mismatch (the pre-registered stuck ids), 1 defect — Coffs Harbour Racing Club
`14752488879`. Census: before `A 9 / B 41 / C 7 / D 6 / Unscored 3`, after
`A 9 / B 45 / C 4 / D 6 / Unscored 2`. Any deviation from those numbers is a NEW finding and must be
investigated and named, not absorbed.

Re-run the gate and the census live, twice, and confirm the two runs are byte-identical before
treating either as established — 50-03 set that discipline to rule out settling lag, and a formula
PATCH triggers a portal-wide recompute, so a single read here would violate **D-22** in spirit:
`.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['check_tier_derived_parity.py', '--out', '/tmp/50-06-gate.md']; runpy.run_path('scripts/check_tier_derived_parity.py', run_name='__main__')"`
and the same with `--census`.

**Append, do not regenerate.** Add the result to `50-TIER-PARITY-EVIDENCE.md` as a dated
post-correction section so 50-03's original FAIL stays visible above it — the repo's
amendment-not-edit ethos, the same reason `50-NULL-PROBE.json` and `50-CONTEXT.md`'s D-20/D-21 block
exist. Include the mirror-agreement section from Task 50-06-04, the pre-registered expectation
alongside the actual result, and the never-scored population re-counted after **D-21**'s un-flip
(it should now read blank, not `"Unscored"` — state the count and the sampled evidence).

**State the verdict plainly.** If one defect row remains, the gate is FAIL: **D-06** (retire
`lv_icp_tier`) and **D-08** (switch off WF1) stay blocked, which is an acceptable outcome and not
something to force green. Say what the residual defect is and what its polarity is: Coffs Harbour is
a case where the derived property is CORRECT (`C`, matching its score of 25) and the stale enum is
wrong — the opposite polarity of the veto-guard defect this plan fixed, and evidence FOR the derived
property rather than against it. Whether to extend the gate's exception list is Plan 04's and Plan
05's operator decision, taken against this evidence; it is not this plan's to take, and
`KNOWN_STUCK_IDS` is not edited here.

**Ledgers.** In `.planning/WINDOWS.md`, close id 13 (the veto-guard failure) with the commit that
fixed it and the evidence reference; leave id 14 (Coffs Harbour) open, since nothing in this plan
addresses it. In `.planning/REQUIREMENTS.md`, make three dated, additive amendments in the
established style: extend the § Out of Scope no-new-properties bullet with **D-20**'s second-property
lift, stating that it is forced rather than preferred and why (only numeric properties are readable by
`calculation_equation`, so the veto branch is otherwise inexpressible), and that everything else in
that bullet still stands; amend TIER-02's traceability row, which currently records the coalesced
variant and the 646-record flip as settled fact and is falsified by **D-21**; and update TIER-01's
traceability row to state exactly what the re-run gate now shows, marking it complete only if the
gate's own text supports it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import re
p='.planning/phases/50-derived-tier-property/50-TIER-PARITY-EVIDENCE.md'
d=open(p).read()
assert d.count('D-07 VERDICT')>=2, 'the post-correction verdict must be APPENDED, leaving the original visible'
assert '14752488879' in d, 'the residual defect row must be named'
for i in ('9605273630','9604738976','17696004613','19100977027'):
    assert i in d, f'pre-registered stuck id {i} missing'
print('evidence artifact ok')" && .venv/bin/python -c "
import subprocess
out=subprocess.run(['git','diff','--name-only','HEAD'],capture_output=True,text=True).stdout
assert '50-NULL-PROBE.json' not in out, 'D-21: the null probe is historical evidence and must not be edited'
print('null probe untouched')" && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `50-TIER-PARITY-EVIDENCE.md` carries a dated post-correction section appended below 50-03's original verdict, with both verdicts readable in the same file.
    - The appended section states the pre-registered expectation (61 match / 4 expected_mismatch / 1 defect; census after `A 9 / B 45 / C 4 / D 6 / Unscored 2`) alongside the actual live result, and names any deviation as a new finding.
    - The gate was run twice live and the two outputs are recorded as byte-identical.
    - The appended section carries the mirror-agreement result: every live `lv_anti_icp_flag=true` record also carries the mirror, or every exception is named.
    - The never-scored population is re-counted post-D-21 and reported as reading blank rather than `"Unscored"`, with at least one sampled record named.
    - `KNOWN_STUCK_IDS` in `scripts/check_tier_derived_parity.py` is unchanged and still contains exactly the four pre-registered ids.
    - `.planning/WINDOWS.md` id 13 is closed with a commit reference; id 14 remains open.
    - `.planning/REQUIREMENTS.md` § Out of Scope carries a dated D-20 extension of the D-01 lift, and the TIER-01 and TIER-02 traceability rows reflect the post-D-21 reality.
    - `git diff` shows no change to `50-NULL-PROBE.json`.
    - `.venv/bin/python -m pytest -q` exits 0 and `node --test tests/n8n/*.test.mjs` exits 0.
  </acceptance_criteria>
  <done>D-07's gate has a current, auditable verdict taken against the corrected property, and the requirement and window ledgers tell the truth about what is fixed and what is not.</done>
  <reversibility rating="reversible">Read-only live runs plus documentation amendments.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scripts → HubSpot Properties API | a live private-app bearer token crosses here to create a property and PATCH a formula |
| repo scripts → HubSpot CRM API | the backfill's company PATCHes cross here — the phase's one authorised company-write deviation |
| env → two-key write gates | `DRY_RUN` plus a per-script allow-key decide whether any of the four armed steps execute |
| repo → n8n Cloud API | the deploy PUT and the bounce cross here, changing what production actually runs |
| live-read payloads → committed artifacts | company ids, names, tier values and property JSON are written to git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-28 | Tampering | the backfill widening beyond its authorised scope | high | mitigate | target set re-derived live every invocation (never from `50-MIRROR-SCOPE.md`), `MAX_BACKFILL_RECORDS` refuses a set over 10, a payload-scope assertion refuses any body carrying a key other than `lv_anti_icp_flag_num`, and `checkpoint:decision` authorises the id list before any write |
| T-50-29 | Tampering | the two veto serializations drifting apart in a future edit | high | mitigate | one named local derives the veto once and both assignments read it; a behavioural n8n test executes the built node over every trigger, an offline Python test pins the oracle serializer, and a population-level agreement check surfaces live divergence |
| T-50-30 | Tampering | a HubSpot-native flow silently becoming a second writer of the mirror | high | mitigate | `lv_anti_icp_flag_num` added to `VETO_PROPERTY_NAMES`, so `test_no_archived_flow_writes_veto_properties` fails on any archived flow that writes it |
| T-50-31 | Denial of Service | the mirror being archived, silently disabling the veto branch again | high | mitigate | added to `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` with an offline pin, so `check_schema_drift.py` reports its absence as damage |
| T-50-32 | Repudiation | a verdict taken from an unsettled calculated read | high | mitigate | D-22 polling with a ≥3 min ceiling on every calculated read used for a verdict, the gate re-run twice byte-identically, and every poll's elapsed time recorded in the evidence artifact |
| T-50-33 | Tampering | a stored-but-not-running n8n deploy | high | mitigate | mandatory bounce per `49-DEPLOY-PROOF.md`, with post-bounce verification; a stored read-back alone is explicitly not accepted as proof |
| T-50-34 | Tampering | verifying a mutation from its own response body | high | mitigate | every property create, formula PATCH and backfill PATCH is verified by an independent re-read |
| T-50-35 | Tampering | wrong-portal execution | high | mitigate | `_portal_ok()` asserted before any call in every schema- or record-touching script, refusing with a non-zero exit and zero calls made |
| T-50-36 | Information Disclosure | committed snapshots and evidence artifacts | high | mitigate | `_assert_no_secrets` applied to every live-read payload before it is written to a committed path; no invocation in this plan embeds a token |
| T-50-37 | Repudiation | the gate being made to pass by redefining its exception list | high | mitigate | `KNOWN_STUCK_IDS` is a stated prohibition and an acceptance criterion; the residual defect is named and left to Plan 04/05's operator decision |
| T-50-SC | Tampering | npm/pip/cargo installs | high | accept | no package-manager install occurs in this plan — no new dependency is added, so no package-legitimacy audit is applicable |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` exits 0
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form — the directory form is broken on node 24)
- `GET /crm/v3/properties/companies/lv_anti_icp_flag_num` returns 200 with `calculated` false
- `scripts/apply_fit_score_formula.py --property lv_icp_tier_derived` reports in sync and exits 0
- Simtech LED `18047161864` reads `lv_icp_tier_derived` = `D`, confirmed by a polled read
- A named never-scored company reads a blank `lv_icp_tier_derived`, confirmed by a polled read
- `50-TIER-PARITY-EVIDENCE.md` carries both 50-03's verdict and this plan's dated post-correction verdict
- `git diff` reports no change to `50-NULL-PROBE.json`
- `lv_icp_tier` still exists and WF1 `4625147345` is still enabled at the end of this plan
</verification>

<success_criteria>
`lv_icp_tier_derived`'s veto branch fires for the records it exists to protect, both engines emit the
numeric mirror from one derivation with drift control that fails loudly, the ~646 never-enriched
companies read blank again, and D-07's gate has a current verdict stated honestly — whatever that
verdict is.
</success_criteria>

<output>
Create `.planning/phases/50-derived-tier-property/50-06-SUMMARY.md` when done
</output>