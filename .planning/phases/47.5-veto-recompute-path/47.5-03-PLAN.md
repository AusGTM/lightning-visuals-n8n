---
phase: 47.5-veto-recompute-path
plan: 03
type: execute
wave: 3
depends_on: ["47.5-02"]
files_modified:
  - tests/test_scoring_parity.py
  - tests/scoring_fixtures.py
  - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md
autonomous: false
requirements: [RECOMP-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tests/test_scoring_parity.py::test_veto_clear_after_correction passes live with all four of its assert statements BYTE-IDENTICAL to what they are today: lines 451, 464, 465 and 466. Only trigger and wait lines change."
    - "The test is made HARDER, not easier: the disposable is stamped with both lv_*_verified_at values before leg 2 so its gate verdict is genuinely skip — without that the test would pass on an unstamped record and prove only that a trigger exists, not that a COMPLETE record can be recomputed."
    - "Both legs are triggered by a D-18 recompute POST. The lv_enrichment_requested + SJ-3 trigger is removed because SJ-3 is DAILY (CLAUDE.md 19.0 as-built), and the first leg previously had no trigger at all — settle() only polls and sends nothing."
    - "The 900-second settle timeout is gone; waiting is bounded and the test's own asserts remain the verdict, never a helper's assertion."
    - "This is armed window #1 of TWO in this phase, and it allowlists a DOMAIN, not ids — the disposable's id does not exist until the test creates it, and a test never arms."
    - "The armed domain matches zero real companies. A read-only portal search proves it returns zero rows before arming and returns zero rows after the run."
    - "The window is disarmed afterward and the disarm's own independent re-read is quoted verbatim."
    - "No record other than the test's own disposable company is writable during the window."
    - "There is ONE arming surface, not Phase 47's two: create_record / patch_record / delete_record carry no environment gate of their own, so the test's direct writes are gated by RUN_LIVE_PARITY plus the portal assertion, and n8n's overlay is armed only for the veto PATCH the pipeline performs."
  artifacts:
    - tests/test_scoring_parity.py
    - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md
  key_links:
    - "disposable_company(domain=...) -> HubSpot Company Search domain EQ -> existingRecord.hs_object_id -> _writeSafetyAllows(enrich, id, domain) matching TEST_RECORD_DOMAINS"
    - "recompute POST -> IF Company Recompute true -> Decide Company Action -> HubSpot Company Update -> lv_anti_icp_flag"
    - "flag cleared -> HubSpot flow WF1 enrollment on the flag -> lv_icp_tier off D"
  prohibitions:
    - "Never weaken, delete, reorder or reword any assert statement in test_veto_clear_after_correction. A test adjusted to match behaviour is the worst possible outcome of this phase."
    - "Never touch the four sibling @live veto tests in this plan — they are Phase 49 scope."
    - "Never arm by id. The disposable does not exist at arming time, and arming a real id here would put a production record inside a window that exists for a throwaway."
    - "Claude never sets ALLOW_N8N_ARM or any arming variable — operator-only, per-shell."
    - "Never gate the disarm on the arming variable."
    - "Never attempt to fix scripts/run_scoring_parity.py's population sweep."
    - "Never claim from this test alone that a frozen COMPLETE record can be recomputed unless the verified_at stamps were actually written — the frozen-record proof of record is plan 02's Simtech LED execution."
---

<objective>
Turn `test_veto_clear_after_correction` green, live, with its assertions untouched — the
honest acceptance bar this phase inherited. Armed window #1: one throwaway company, a domain
allowlist, one open and one close.

Purpose: plan 02 proved the lane runs. This proves the lane produces the OUTCOME the project
has been unable to produce since Phase 40-07: a corrected input clears a veto on demand.

Output: a green live run recorded in `47.5-A-LIVE-PROOF.md`, with the disarm re-read quoted
verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47.5-veto-recompute-path/47.5-RESEARCH.md
@.planning/phases/47.5-veto-recompute-path/47.5-01-SUMMARY.md
@.planning/phases/47.5-veto-recompute-path/47.5-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the test a trigger and a bounded wait — and nothing else</name>
  <files>tests/test_scoring_parity.py, tests/scoring_fixtures.py</files>
  <read_first>
    - tests/test_scoring_parity.py :440-466 (the whole test; the four asserts are lines 451, 464, 465, 466)
    - tests/scoring_fixtures.py :91-129 (disposable_company and settle — settle returns as soon as two consecutive reads AGREE)
    - scripts/remediate_veto_companies.py :490-545 (settle_and_assert's predicate shape — the pattern to mirror, not to import)
    - .planning/phases/47.5-veto-recompute-path/47.5-01-SUMMARY.md (the recompute POST helper's signature)
  </read_first>
  <behavior>
    - `settle_until(company_id, prop, predicate, timeout, interval)` returns the last observed value once the predicate holds, or after the timeout — it never raises, so the test's own asserts stay the verdict.
    - Leg 1: patch the veto-setting inputs, wait for the record to be searchable by domain, POST a recompute event, wait until the flag reads a non-null value, then assert.
    - Leg 2: patch the corrected region, stamp both verified_at properties, POST a recompute event, wait until the flag reads "false", wait for the tier to leave D, then assert.
    - The four assert statements are byte-identical to today's.
  </behavior>
  <action>
Three edits, all of them trigger or wait mechanics.

1. `tests/scoring_fixtures.py` — add `settle_until(company_id, prop, predicate, timeout=180,
   interval=5)`: poll `get_record` until `predicate(value)` is true or the timeout elapses,
   return `(value, elapsed)`. Do NOT raise on timeout. Today's `settle` returns as soon as
   two consecutive reads agree, which on a fresh disposable means it returns a stable `None`
   in about five seconds while the pipeline is still running — the reason a trigger alone
   would not turn this test green. Also add
   `wait_until_searchable(domain, timeout=90, interval=5)` returning the number of matching
   companies: a freshly created record is not immediately visible to the HubSpot search API
   (the ~20s new-record index lag already recorded as WINDOWS.md id 6), and the enrichment
   workflow finds a domain-carrying event by search.

2. `tests/test_scoring_parity.py::test_veto_clear_after_correction` — change only these:
   - `with disposable_company() as company_id:` gains
     `domain="zz-scoring-test-delete-me.example"`. This is what makes the record
     allowlistable by domain at arming time; the id does not exist until this line runs.
   - Before each POST, call `wait_until_searchable(...)` and require exactly one match.
   - Replace the leg-1 `settle(...)` with: a recompute POST via the plan-01 helper, carrying
     both the object id and the domain, then `settle_until(..., lambda v: v is not None)`.
     Leg 1 has never had a trigger of any kind — `settle` only reads.
   - Replace the leg-2 `patch_record(..., {"lv_enrichment_requested": "true"})` and the
     `settle(..., timeout=900, interval=15)` with: a patch stamping
     `lv_org_type_verified_at` and `lv_produces_content_verified_at` to now (these two
     exist live — D-21 narrowed the stamp set to exactly these), a recompute POST, then
     `settle_until(..., lambda v: v == "false")` and a second
     `settle_until(..., "lv_icp_tier", lambda v: v != "D")` before the tier assert.
     Stamping is what makes the record genuinely complete and fresh, so the gate returns
     skip and the test exercises the frozen case this phase exists to fix. It makes the bar
     HARDER; the old 900-second timeout existed only because it was waiting on a daily
     poller that would have hit the same gate anyway.
   - Update the stale comment block at lines 453-459 to describe the recompute trigger. The
     comment is not an assertion.
   - Leave lines 451, 464, 465 and 466 exactly as they are, character for character.

3. Run the offline tier to prove nothing else broke. The test itself stays `@live`-gated and
   does not run here.

If any of the four asserts turns out to be unsatisfiable against the deployed system, STOP
and put it to the operator as a named correction with evidence — the way Phase 47 recorded
its six. Do not edit the assert.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q && test "$(git diff HEAD -- tests/test_scoring_parity.py | grep -cE '^[-+][[:space:]]*assert ')" = "0"</automated>
  </verify>
  <done>The offline suite is green, and the diff of `tests/test_scoring_parity.py` contains zero added or removed lines that begin with `assert` — the four assertions are byte-identical. Comment lines begin with `#` and cannot trip that gate, so rewriting the stale comment block is free. Run the gate BEFORE committing the test edit, or `HEAD` moves under it and it goes vacuous. Both legs now carry a recompute trigger and a bounded, non-raising wait.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Armed window #1 — operator arms the throwaway domain, runs the test, disarms</name>
  <what-built>The recompute lane is live and proven to run (plan 02). The acceptance test now carries a trigger and a bounded wait, with its assertions untouched.</what-built>
  <reversibility rating="reversible">The only record written is a throwaway the test creates and deletes in a `finally`. Undo is the teardown that already runs.</reversibility>
  <action>
Run the read-only pre-arm domain search yourself and report it. Then halt and hand the
operator the arm, run and disarm commands verbatim — `ALLOW_N8N_ARM` is operator-only and
per-shell, never set by Claude. Wait for the pytest exit status and the disarm `observed`
block before Task 3. If the pre-arm search returns anything other than zero rows, do not hand
over the arm command at all: report the refuse-to-arm condition instead.
  </action>
  <instructions>
**Pre-arm, read-only.** Confirm the allowlisted domain matches nothing real:

```
.venv/bin/python -c "
from dotenv import load_dotenv; load_dotenv()
from src.hubspot_client import search_records
r=search_records('companies',[{'propertyName':'domain','operator':'EQ','value':'zz-scoring-test-delete-me.example'}],['name'],limit=100)
print('MATCHES:', r.get('total'), [x['id'] for x in r.get('results',[])])
"
```

Anything other than `MATCHES: 0` is a refuse-to-arm condition — a leaked disposable from an
earlier run would become writable. Delete it first, then re-check.

**Arm — domains, not ids. ONE surface, and this is the whole of it.**

Phase 47 armed two surfaces because its writes went through the remediation script's own
gate as well as n8n's. This window does not. The test creates, patches and deletes its
disposable by calling `create_record` / `patch_record` / `delete_record` directly with
`dry_run=False`; those functions carry no environment gate of their own — the caller is the
gate, and here the caller's gates are `RUN_LIVE_PARITY=true` plus
`scoring_fixtures._portal_ok()`, which refuses to create anything unless `HUBSPOT_PORTAL_ID`
is the expected portal. So the only surface to arm is n8n's, and it is armed for the veto
PATCH the pipeline performs, not for the test's own writes. `june_run_arm.py` pins
`allow_create=False`, which is correct and unrelated: the n8n lane must never create, and the
disposable is created by the test process, not by the pipeline.

! ALLOW_N8N_ARM=true .venv/bin/python scripts/june_run_arm.py --domains zz-scoring-test-delete-me.example

**Run the acceptance test:**

! RUN_LIVE_PARITY=true .venv/bin/python -m pytest tests/test_scoring_parity.py::test_veto_clear_after_correction -q

**Disarm, always, pass or fail:**

! .venv/bin/python scripts/june_run_arm.py --disarm

Disarm is not gated on the arming variable, so it works from any shell. Capture its
`observed` block — that is `n8n_arming.disarm`'s own independent re-read, not an echo.

**Post-run, read-only.** Re-run the domain search above (expect `MATCHES: 0` again — the
context manager deletes the disposable in a `finally`) and run
`assert_no_disposables_survive()`.

If the test fails, do not touch its asserts. Report which assert failed with the observed
value, and stop.
  </instructions>
  <verification>The pre-arm and post-run domain searches both return zero rows; pytest exits 0; the disarm `observed` block reads ALLOW_HUBSPOT_RECORD_WRITES "false" with both allowlists empty; `assert_no_disposables_survive()` passes.</verification>
  <resume-signal>Type "approved" with the pytest exit status and the disarm `observed` block, or paste the failing assert and its observed value.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the window and state exactly what it proved</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md</files>
  <read_first>
    - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md (plan 02's deploy, bounce and disarmed-execution sections)
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md section "Window accounting" (the disclosure standard this file matches)
  </read_first>
  <action>
Append an "Armed window #1 — acceptance test" section to `47.5-A-LIVE-PROOF.md` recording:
the pre-arm domain search result, the arm outcome, the pytest result, the disarm `observed`
block quoted verbatim, and the post-run domain search.

State plainly what the green test proves and what it does not. It proves a corrected input
clears a veto on demand, end to end, including the HubSpot tier derivation. Whether it also
proves the frozen-COMPLETE case depends on the verified_at stamps actually landing — if they
did, say so; if HubSpot rejected either stamp, say that instead and point at plan 02's
Simtech LED execution as the frozen-record proof of record. Do not blur the two.

Record the window count honestly: this is window #1 of two, opened once and closed once.
Phase 47's must_have #1 failed because five windows were reported as one; this phase states
two up front.
  </action>
  <verify>
    <automated>grep -c "Armed window #1" .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md</automated>
  </verify>
  <done>The window is recorded end to end with its disarm re-read quoted verbatim, and the file distinguishes what the acceptance test proved from what plan 02's execution proved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> n8n arming overlay | `ALLOW_N8N_ARM` operator-only, per-shell; disarm ungated |
| armed n8n workflow -> HubSpot | `TEST_RECORD_DOMAINS` allowlist of exactly one throwaway domain |
| test process -> HubSpot | direct create/patch/delete of a `ZZ-SCORING-TEST-DELETE-ME-*` company, portal-asserted before creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47.5-11 | Elevation of privilege | a domain allowlist matching a real company | critical | mitigate | Read-only pre-arm search must return zero rows; anything else is a refuse-to-arm condition, checked again after the run |
| T-47.5-12 | Tampering | a leaked disposable from an earlier run becoming writable | high | mitigate | Same zero-row pre-arm check, plus `assert_no_disposables_survive()` after |
| T-47.5-13 | Repudiation | a window reported as closed but still open | critical | mitigate | Disarm's independent re-read is quoted verbatim; disarm is never gated on the arming variable so it always works |
| T-47.5-14 | Tampering | an assertion quietly relaxed to make the run green | critical | mitigate | Task 1's automated verify greps for all four assert lines byte-identical; a relaxation is a named, operator-approved correction or it does not happen |
| T-47.5-15 | Denial of service | the test hanging on a wait that never resolves | low | mitigate | Every wait is bounded and non-raising; the asserts are the verdict |
| T-47.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` green offline; the four asserts present byte-identical.
- `RUN_LIVE_PARITY=true ... ::test_veto_clear_after_correction` exits 0 inside the armed window.
- The disarm `observed` block shows `ALLOW_HUBSPOT_RECORD_WRITES: "false"` and both allowlists empty.
- The throwaway-domain search returns zero rows before and after.
</verification>

<success_criteria>
- The acceptance test this phase inherited is green live, with its assertions untouched.
- Exactly one arm and one disarm, both recorded, the disarm proven by independent re-read.
- The record of what was proven distinguishes the on-demand clear from the frozen-complete case.
</success_criteria>

<output>
Create `.planning/phases/47.5-veto-recompute-path/47.5-03-SUMMARY.md` when done.
</output>
