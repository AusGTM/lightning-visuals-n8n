---
phase: 47.5-veto-recompute-path
plan: 04
type: execute
wave: 3
depends_on: ["47.5-01"]
files_modified:
  - .planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md
  - .planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json
autonomous: false
requirements: [RECOMP-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Nothing is written to HubSpot in this plan. Every live call is a read."
    - "The operating-presence claim for each candidate is RESEARCHED with evidence URLs and a quoted supporting line, not asserted. The todo that raised this work states plainly that its claims come from general knowledge."
    - "D-V6's bar is applied as written: a substantive local operating entity — subsidiary, office, staff, production operations. Explicitly NOT headquarters, NOT 'sells into ANZ', NOT 'has ANZ customers', NOT a reseller or licensee."
    - "Jam TV 17317850381 is CORRECT and is not a candidate. It is the Italian broadcaster jamtv.it and D-23 requires its veto to persist. It appears in the before-snapshot for completeness and in no write set."
    - "Entain 10024564084 cannot move on a region flip: lv_produces_content reads false, which fires a second veto independently. Stated up front so nobody discovers it inside a window."
    - "The operator decides each flip explicitly, naming the exact enum value to write, checked against the LIVE enum options rather than a value copied from CLAUDE.md."
    - "Evidence lives in 47.5-B-EVIDENCE.md and the run report. It is NOT stamped into HubSpot: no evidence or verified_at property exists for the region field and creating properties is out of scope for this milestone."
    - "The before-snapshot captures all four records' region, org_type, produces_content, score, tier, flag and reason, so window #2 has a diff to be judged against."
  artifacts:
    - .planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md
    - .planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json
  key_links:
    - "D-V6 operating-presence test -> researched evidence URLs -> operator decision -> the exact lv_country_region_normalized enum value plan 06 writes"
    - "lv_country_region_normalized -> Decide Company Action's _regionKey -> the non-ANZ veto predicate"
  prohibitions:
    - "Never PATCH anything in this plan — not the region, not a stamp, not a note."
    - "Never PATCH lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier at any point in this phase (D-07). Change inputs and let the derived chain settle."
    - "Never touch Jam TV 17317850381."
    - "Never re-touch any of Phase 47's 17 records. Phase 47 closed them; the only overlap this workstream has with that set is Jam TV, which is not a candidate."
    - "Never create a HubSpot property to hold this evidence."
    - "Never treat a headquarters address, a distributor, a licensee or an events-sold-here relationship as operating presence."
    - "Never let the pipeline's own research lane derive the region for these records — its normalizer maps only Australia and New Zealand to AU/NZ and everything else to Other, so a multinational with an ANZ operating entity will never normalize correctly on its own. This is a human D-V6 call informed by evidence."
---

<objective>
Establish, with evidence, whether Ironman `17317184159` and GRAVITY MEDIA `15860277364` meet
D-V6's ANZ operating-presence bar, and get an explicit operator decision on the exact value
to write for each. Read-only throughout.

Purpose: Ironman scores 70 — Tier A material, a content-producing governing body — and is
suppressed entirely by a geography call made on a Tampa headquarters, which D-V6 replaced
with an operating-presence test. That it qualifies is currently an assertion from general
knowledge. This plan turns it into evidence, or refutes it.

Output: `47.5-B-EVIDENCE.md` (per-record evidence with URLs and quoted lines) and
`47.5-B-BEFORE.json` (the four-row before-snapshot plan 06 diffs against).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47.5-veto-recompute-path/47.5-CONTEXT.md
@.planning/phases/47.5-veto-recompute-path/47.5-RESEARCH.md
@.planning/todos/pending/2026-08-12-d-v6-reexamine-the-four-remaining-non-anz-vetoes.md
@.planning/decisions/2026-08-12-org-type-venue-and-normalization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Before-snapshot of the four records, and the live enum options</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json</files>
  <read_first>
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md section "VETO-03 — operator confirmation" (the four-row census this snapshot re-verifies)
    - .planning/phases/47-veto-remediation/47-BEFORE.json (the snapshot shape to mirror)
    - .planning/todos/pending/2026-08-12-d-v6-reexamine-the-four-remaining-non-anz-vetoes.md (its Evidence section carries the exact read-only census command)
  </read_first>
  <action>
Read-only. Use the dotenv driver form — `.env` is permission-blocked to direct reads, so load
it through `dotenv` inside the command rather than reading the file.

1. Re-run the portal-wide census that defines this work-list: companies whose
   `lv_anti_icp_reason` contains the non-ANZ token. Confirm it still returns exactly four
   rows and the same four ids. A different set means the work-list moved under the plan and
   Task 3's decision would be made against stale facts — STOP and report instead.
2. For each of the four capture `name`, `domain`, `lv_country_region_normalized`,
   `lv_org_type`, `lv_produces_content`, `lv_is_hardware_vendor`, `lv_icp_fit_score`,
   `lv_icp_tier`, `lv_anti_icp_flag`, `lv_anti_icp_reason`, `lv_org_type_verified_at` and
   `lv_produces_content_verified_at`. Write them to `47.5-B-BEFORE.json`, mirroring the
   shape of `47-BEFORE.json`.
3. Read the LIVE enum options for `lv_country_region_normalized` from the CRM properties API
   and record them in the same file. Task 3's decision must name a value that exists in the
   portal, never one copied from CLAUDE.md section 5.1 — that document is a design target,
   not an inventory, and its own section 4.0 says so.
4. Note per record whether both required fields are present with both stamps inside the
   180-day TTL, which is the condition under which the gate skips the record. Three of the
   four are in that state; that is why plan 01 exists.

Assert in the file, from the fetched data rather than from memory, that Entain carries
`lv_produces_content` false and therefore an independent second veto.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json;d=json.load(open('.planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json'));rows=d.get('records',d) if isinstance(d,dict) else d;ids={str(r['id']) for r in rows};assert ids=={'17317850381','17317184159','15860277364','10024564084'},ids;print('OK',len(rows))"</automated>
  </verify>
  <done>`47.5-B-BEFORE.json` holds all four records with the named properties, the live enum options, and a data-derived statement of Entain's second veto. The census still returns exactly those four ids.</done>
</task>

<task type="auto">
  <name>Task 2: Research ANZ operating presence for the two candidates, with evidence URLs</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md</files>
  <read_first>
    - .planning/decisions/2026-08-12-org-type-venue-and-normalization.md (D-V6's exact wording — quote the bright line into the evidence file rather than paraphrasing it)
    - .planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json (each record's domain, which anchors the search)
  </read_first>
  <action>
Research two companies against one question each. Do it directly with web search and page
fetches — this is a judgement supported by sources, not a pipeline job, and routing it
through the deployed research lane would spend an Anthropic call and re-stamp the record's
metadata for a field the lane cannot normalize correctly anyway.

For **Ironman `17317184159`** and **GRAVITY MEDIA `15860277364`**, answer: does a substantive
local operating entity exist in Australia or New Zealand — a subsidiary, an office, staff, or
production operations?

For each record write, in `47.5-B-EVIDENCE.md`:
- the question, and D-V6's bright line quoted from the decision record;
- every source consulted as a URL, with the specific line or fact it supports quoted;
- a verdict of `meets`, `does_not_meet` or `insufficient_evidence`;
- the exact region enum value that verdict implies, chosen from the live options captured in
  Task 1 — `ANZ` where operations span both countries, `AU` or `NZ` where they do not;
- what would change the verdict, so a later reader can re-open it cheaply.

Rules that decide the verdict:
- A headquarters address is not operating presence. Neither is selling into the region,
  having customers there, a distributor, a licensee, or a franchised event.
- Absence of evidence is `insufficient_evidence`, never `does_not_meet` dressed up — and
  `insufficient_evidence` means the record is not in plan 06's write set.
- A source that is the company's own site is acceptable and is often the best evidence for
  an operating entity; say which claims rest on self-description.

Also record, without researching them:
- **Entain `10024564084`** — out of the write set. Its second veto (`lv_produces_content`
  false) holds whatever its region says, so a flip changes nothing about its tier. State it
  with the snapshot value as the citation.
- **Jam TV `17317850381`** — correct as it stands, D-23, not a candidate, not to be touched.
  </action>
  <verify>
    <automated>.venv/bin/python -c "t=open('.planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md').read();assert '17317184159' in t and '15860277364' in t and '10024564084' in t and '17317850381' in t;assert t.count('http')>=4,'evidence URLs missing';assert any(v in t for v in ('meets','does_not_meet','insufficient_evidence'));print('OK')"</automated>
  </verify>
  <done>Both candidates have a verdict backed by cited URLs with quoted supporting lines and an implied enum value; Entain and Jam TV are recorded as out of the write set with their reasons.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Operator decides the D-V6 flips</name>
  <decision>For each candidate, does the researched evidence meet D-V6's operating-presence bar, and what exact value of `lv_country_region_normalized` should plan 06 write?</decision>
  <context>
D-V6 redefined this field as ANZ operating presence rather than headquarters. All four
records' regions were set under the old headquarters reading. Ironman scores 70 — Tier A
material — and is suppressed by that call alone; Gravity Media is the same shape at 50.
Entain will not move whatever is decided, and Jam TV is correct and stays.

The evidence for each verdict is in `47.5-B-EVIDENCE.md`, with URLs. Read it before deciding.
Expected effects if a flip lands, stated as predictions to verify and not as assertions —
Phase 47 recorded that an oracle tier cannot be asserted against a live tier before the run:
Ironman 70 to 80, likely Tier A; Gravity Media 50 to 60, likely Tier B.

Whatever is decided, plan 06 writes ONLY the region. The flag, reason, score and tier are
never patched (D-07); they settle from the recompute.
  </context>
  <options>
    <option id="flip-both">
      <name>Flip both Ironman and Gravity Media to their evidenced value</name>
      <pros>Releases the highest-value suppressed record in the portal; applies D-V6 as written</pros>
      <cons>Two records in window #2 rather than one; both verdicts rest on the evidence gathered in Task 2</cons>
    </option>
    <option id="flip-ironman-only">
      <name>Flip Ironman only</name>
      <pros>Smallest window that captures most of the value; Gravity Media can be revisited with better evidence</pros>
      <cons>Leaves a second known-stale headquarters call standing</cons>
    </option>
    <option id="flip-neither">
      <name>Flip neither — the evidence does not clear D-V6's bar</name>
      <pros>The bright line stays bright; nothing is written on a thin case</pros>
      <cons>Window #2 then carries workstream C alone, and Ironman stays suppressed</cons>
    </option>
    <option id="other-value">
      <name>Flip, but to a different enum value than the evidence file proposes</name>
      <pros>The operator owns the AU / NZ / ANZ call; only ANZ covers operations in both</pros>
      <cons>Must still be one of the live enum options captured in Task 1</cons>
    </option>
  </options>
  <resume-signal>Name the write set as id plus exact enum value per record, e.g. "17317184159 -> ANZ, 15860277364 -> none". Record the decision and its reasoning in `47.5-B-EVIDENCE.md` before continuing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web -> evidence file | Researched claims are untrusted until cited; every verdict carries its source URL |
| this plan -> HubSpot | read-only; no arming variable is set and no write path is exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47.5-16 | Tampering | a flip decided on an unresearched assertion | high | mitigate | Task 2 requires a cited URL and a quoted line per verdict; `insufficient_evidence` keeps the record out of the write set |
| T-47.5-17 | Spoofing | a marketing page describing a licensee or franchised event as a local operation | medium | mitigate | D-V6's exclusions are restated as verdict rules; self-described claims are labelled as such in the evidence file |
| T-47.5-18 | Elevation of privilege | an accidental write from a read-only plan | high | mitigate | No arming variable is set anywhere in this plan and the n8n write surface stays disarmed; prohibitions name it |
| T-47.5-19 | Repudiation | a decision recorded without its reasoning | medium | mitigate | The checkpoint's resume signal requires the write set and its reasoning to be written into the evidence file before the phase continues |
| T-47.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages |
</threat_model>

<verification>
- `47.5-B-BEFORE.json` holds exactly the four census ids plus the live enum options.
- `47.5-B-EVIDENCE.md` carries a cited verdict per candidate and an explicit out-of-set note for Entain and Jam TV.
- `git status` shows no change to any script, workflow or test — this plan writes only planning artifacts.

</verification>

<success_criteria>
- Each candidate has an evidence-backed D-V6 verdict rather than an assertion.
- The operator has named the exact write set for plan 06, as id plus enum value.
- Nothing was written to HubSpot.
</success_criteria>

<output>
Create `.planning/phases/47.5-veto-recompute-path/47.5-04-SUMMARY.md` when done.
</output>
