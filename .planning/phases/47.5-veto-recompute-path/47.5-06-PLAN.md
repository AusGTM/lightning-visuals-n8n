---
phase: 47.5-veto-recompute-path
plan: 06
type: execute
wave: 5
depends_on: ["47.5-03", "47.5-04", "47.5-05"]
files_modified:
  - .planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md
  - .planning/phases/47.5-veto-recompute-path/47.5-AFTER.json
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [RECOMP-03, RECOMP-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "This is armed window #2 of TWO in this phase, and it is ONE window: one arm, one run, one disarm. Phase 47 needed five because six of its plan's assertions were unsatisfiable against the deployed system; every assertion in this plan has already been proven live by plans 02, 03 and 05 before the window opens."
    - "The allowlist is pinned to at most THREE ids, exactly the ones the recorded decisions name: 17317184159 and 15860277364 if plan 04's decision includes them, and 18047161864 if plan 05's decision is retroactive. Nothing else is writable."
    - "Entain 10024564084 is NEVER in the allowlist. Its second veto (lv_produces_content false) means a region flip cannot move it, so writing to it would spend a touch for no change."
    - "Jam TV 17317850381 is NEVER in the allowlist and is not read-modified. D-23: its veto is correct and must persist."
    - "Only lv_country_region_normalized is PATCHed, and only for records the decision names. Simtech LED gets NO patch at all — only a recompute POST, because its inputs are already right and only the rule changed."
    - "D-07 holds absolutely: lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score and lv_icp_tier are never PATCHed. They settle from the recompute and are read back, never written."
    - "D-01 touch-once: every record in the write set is written exactly once. A repeat touch is a disclosed deviation, not a silent retry."
    - "Every record's outcome is verified by an independent read-back after the recompute settles, and the per-id before/after table is recorded."
    - "The window is disarmed afterward and n8n_arming.disarm's own independent re-read is quoted verbatim, as Phase 47 did."
    - "A pre-arm, read-only guard runs BEFORE arming: the portal-wide non-ANZ census, the current state of every id in the write set, and a per-record prediction. Any id in the census that is not in the write set and not accounted for in writing is a refuse-to-arm condition."
    - "Predicted score and tier movements are recorded as predictions to verify, never asserted — Phase 47 recorded that an oracle tier cannot be asserted against a live tier before the run, and oracle-versus-live parity is Phase 49's scope. The veto flag and reason ARE hard assertions."
    - "Cost is reported after the run: n8n executions consumed, provider credits at zero, Anthropic calls at zero — the recompute lane makes none."
  artifacts:
    - .planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md
    - .planning/phases/47.5-veto-recompute-path/47.5-AFTER.json
  key_links:
    - "recorded decision -> pinned allowlist -> june_run_arm.py --ids -> n8n TEST_RECORD_IDS -> _writeSafetyAllows"
    - "PATCH lv_country_region_normalized -> recompute POST -> Decide Company Action -> lv_anti_icp_flag / lv_anti_icp_reason -> HubSpot flow WF1 -> lv_icp_tier"
    - "june_run_arm.py --disarm -> n8n_arming.disarm's independent re-read -> the confirmed-closed window"
  prohibitions:
    - "Never PATCH lv_anti_icp_flag, lv_anti_icp_reason, lv_icp_fit_score or lv_icp_tier (D-07)."
    - "Never write to Entain 10024564084 or Jam TV 17317850381."
    - "Never re-touch any of Phase 47's other 17 records. The only one this window may touch is Simtech LED 18047161864, and only because plan 05's rule change is what makes its recompute necessary."
    - "Never blank a field to force the gate to enrich. That workaround is exactly what this phase removed; if a record will not recompute, stop and report."
    - "Claude never sets ALLOW_N8N_ARM or ALLOW_VETO_REMEDIATION — operator-only, per-shell."
    - "Never gate the disarm on the arming variable."
    - "Never re-open the window to fix a discovered assertion. Record the miss, disarm, and put the correction to the operator — a second window is a disclosed deviation, not a routine step."
    - "Never create a HubSpot property."
    - "Never attempt to fix scripts/run_scoring_parity.py's population sweep — red by design until Phase 49."
---

<objective>
Open the phase's second and last armed window, write at most three records, prove each
outcome by independent read-back, close the window and prove it closed.

Purpose: workstreams B and C are decisions that have already been made and a mechanism that
has already been proven. This is the small, bounded write those decisions imply — the thing
Phase 47 could not do for these records because the gate skipped them.

Output: `47.5-RUN-REPORT.md` with the per-id table, the window accounting and the cost
actuals; `47.5-AFTER.json`; and the RECOMP requirement rows ticked with pointers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47.5-veto-recompute-path/47.5-CONTEXT.md
@.planning/phases/47.5-veto-recompute-path/47.5-02-SUMMARY.md
@.planning/phases/47.5-veto-recompute-path/47.5-03-SUMMARY.md
@.planning/phases/47.5-veto-recompute-path/47.5-04-SUMMARY.md
@.planning/phases/47.5-veto-recompute-path/47.5-05-SUMMARY.md
@.planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md
@.planning/phases/47.5-veto-recompute-path/47.5-C-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-arm guard — the write set, the census, and a per-record prediction</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md</files>
  <read_first>
    - .planning/phases/47.5-veto-recompute-path/47.5-B-EVIDENCE.md (the decided write set, id plus exact enum value)
    - .planning/phases/47.5-veto-recompute-path/47.5-C-DECISION.md (whether Simtech is in scope, and the decided predicate)
    - .planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json (the before-snapshot to diff against)
    - .planning/phases/47-veto-remediation/47-RUN-REPORT.md section "Pre-flight VETO-03 guard" (the precedent, including the D-23 near-miss that only the pre-arm read caught)
  </read_first>
  <action>
Read-only, before anything is armed. Write the results into a new `47.5-RUN-REPORT.md` as its
pre-arm section.

1. Derive the write set from the two recorded decisions — never from this plan's prose or
   from memory. Each entry is an id, the action (`patch_region_then_recompute` or
   `recompute_only`), and for a region write the exact enum value. If the two decision files
   disagree with each other or name an id not in the four-row census, STOP.
2. Confirm the write set has at most three ids and contains neither Entain nor Jam TV.
3. Re-read each id's current state live and diff it against `47.5-B-BEFORE.json`. A record
   whose state moved since plan 04 is a stop-and-report, not a proceed.
4. Re-run VETO-03's portal-wide bar read-only: a non-ANZ veto reason ANDed with a blank
   `lv_country_region_normalized`. It returned zero at the end of Phase 47 and must still
   return zero. Any id it returns that is not in the write set and not explained in writing
   is a refuse-to-arm condition — it means the bar cannot pass however well this window runs.
5. Predict, per record, what the recompute will derive: the veto reason set, the flag, and
   the expected score and tier movement. Mark the flag and reason as assertions and the score
   and tier as predictions to verify. Where plan 05's change is in scope, predict Simtech's
   veto firing and its tier moving to D — a correct outcome, not a regression.
6. State the expected cost: one n8n execution per POST, zero provider credits, zero Anthropic
   calls. The recompute lane calls neither, and a bare-array D-18 body enables no providers
   in any case.
  </action>
  <verify>
    <automated>.venv/bin/python -c "t=open('.planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md').read();assert '10024564084' in t and '17317850381' in t, 'the two excluded ids must be named as excluded';assert 'MATCHES' in t or 'census' in t.lower();print('OK')"</automated>
  </verify>
  <done>The write set is derived from the recorded decisions, capped at three ids, excludes Entain and Jam TV by name, matches the before-snapshot, and the VETO-03 census result plus a per-record prediction are recorded before anything is armed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Armed window #2 — arm, write, recompute, read back, disarm</name>
  <what-built>The recompute lane is live and proven (plans 02 and 03). The hardware-veto decision is live and bounced (plan 05). The write set and its predictions are recorded and guarded (Task 1).</what-built>
  <reversibility rating="costly">These are production CRM records. Undo means a second window against records already written, which is the sprawl Phase 47 paid for — flagged, not blocked, because Task 1's pre-arm guard makes every outcome known before the window opens and only one property is written per record.</reversibility>
  <action>
Halt and hand the operator the arm command, built from Task 1's write set, verbatim. Claude
never sets `ALLOW_N8N_ARM` or `ALLOW_VETO_REMEDIATION` — operator-only, per-shell. Then drive
the per-record sequence below in the stated order, and hand back the disarm command at the
end whether the run succeeded or not. Disarm is never gated on the arming variable, so it
always works. Do not re-arm to retry anything.
  </action>
  <instructions>
**Arm exactly the ids in Task 1's write set — no others:**

! ALLOW_N8N_ARM=true .venv/bin/python scripts/june_run_arm.py --ids &lt;ids from the write set&gt;

**Per record, in this order** (the order matters — Phase 47 proved that reading a derived
value before the trigger asserts something the system cannot satisfy):

1. If the record's action is `patch_region_then_recompute`: PATCH **only**
   `lv_country_region_normalized` with the decided enum value. Nothing else. Simtech LED gets
   no patch — its inputs are already correct and only the rule changed.
2. POST one D-18 event carrying the recompute intent, via
   `post_webhook_event(..., recompute=True)`. It now waits 300 seconds by default; a
   client-side read timeout is not evidence of failure either way — the read-back is.
3. Wait for `lv_anti_icp_flag` to settle, then read the record back independently and record
   flag, reason, score and tier.
4. Then, and only then, wait for `lv_icp_tier`.

**Assertions** (hard): the flag and the reason match Task 1's prediction for that record.
**Predictions** (recorded, not asserted): score and tier.

**Disarm, always, pass or fail:**

! .venv/bin/python scripts/june_run_arm.py --disarm

Capture the `observed` block verbatim — it is `n8n_arming.disarm`'s own independent re-read,
not an echo of the request.

**If anything does not behave as Task 1 predicted, stop.** Do not blank a field to force the
gate, do not re-arm to try again, and do not adjust a prediction after the fact. Disarm,
record what happened, and put the correction to the operator. Phase 47's five windows are the
cost of doing otherwise.
  </instructions>
  <verification>Every written record reads back with the flag and reason Task 1 predicted; the disarm `observed` block reads ALLOW_HUBSPOT_RECORD_WRITES "false" with both allowlists empty; no id outside the write set changed.</verification>
  <resume-signal>Type "approved" with the per-id read-back values and the disarm `observed` block, or describe what stopped the run.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: After-snapshot, run report, requirement rows</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-AFTER.json, .planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json (the diff source)
    - .planning/phases/47-veto-remediation/47-04-SUMMARY.md (the disclosure standard: misses stated plainly, not softened)
  </read_first>
  <action>
Write `47.5-AFTER.json` — the same four records the before-snapshot holds, in the same shape,
read fresh — and append to `47.5-RUN-REPORT.md`:

- **Per-id outcome table**: before and after score, tier, flag and reason, plus region,
  org_type and the classification of each row. Jam TV and Entain appear as untouched rows so
  the file shows the whole census, not only what moved.
- **Window accounting**: how many arm/disarm cycles actually happened, which records were
  touched and how many times, and the disarm `observed` block quoted verbatim. If it was more
  than one cycle, say so as plainly as Phase 47's run report did — a report that says "the
  window" when there were two reads as one ceremony.
- **Cost actuals** against Task 1's estimate: n8n executions consumed against the 2,500/month
  allowance, provider credits, Anthropic calls.
- **VETO-03 re-check**: the portal-wide census after the run, and the new count of records
  carrying a non-ANZ veto. Jam TV must still be among them.
- **What is not claimed**: anything the run recorded as a prediction rather than an assertion,
  and anything Phase 49 owns.

Then tick the RECOMP requirement rows in `.planning/REQUIREMENTS.md` with concrete pointers —
the test name or the report section that evidences each — using a scoped `Edit`, never a
whole-file write. Leave COVER-01 and COVER-02 alone: they are Phase 48's, and this phase does
not touch them.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json;a=json.load(open('.planning/phases/47.5-veto-recompute-path/47.5-AFTER.json'));b=json.load(open('.planning/phases/47.5-veto-recompute-path/47.5-B-BEFORE.json'))
ra=a.get('records',a) if isinstance(a,dict) else a; rb=b.get('records',b) if isinstance(b,dict) else b
assert {str(r['id']) for r in ra}=={str(r['id']) for r in rb}, 'after-snapshot must cover the same four records'
jam=[r for r in ra if str(r['id'])=='17317850381'][0]
assert str(jam.get('lv_anti_icp_flag','')).lower()=='true', 'D-23: Jam TV must still be vetoed'
print('OK')"</automated>
  </verify>
  <done>The after-snapshot covers the same four records, Jam TV is still vetoed, the run report carries the per-id table, honest window accounting, cost actuals and the VETO-03 re-check, and the RECOMP rows are ticked with pointers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> n8n arming overlay | `ALLOW_N8N_ARM` operator-only, per-shell; disarm ungated |
| armed n8n workflow -> HubSpot | `TEST_RECORD_IDS` pinned to at most three ids |
| this plan -> production CRM records | real, customer-facing records; every write is one property on one record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47.5-25 | Elevation of privilege | an allowlist wider than the decided write set | critical | mitigate | Task 1 derives the set from the recorded decisions and caps it at three; the arm command takes exactly those ids; the pre-arm census refuses on any unaccounted id |
| T-47.5-26 | Tampering | clearing a CORRECT veto | critical | mitigate | Jam TV and Entain are excluded by name in the allowlist and in the prohibitions; Task 3's automated check fails if Jam TV's flag is not still true. This is the D-23 near-miss Phase 47 caught only at the pre-arm read |
| T-47.5-27 | Tampering | a direct write to a derived field | high | mitigate | D-07 prohibition; only `lv_country_region_normalized` is patched, and Simtech is patched not at all |
| T-47.5-28 | Repudiation | a window reported as closed but still open | critical | mitigate | The disarm's independent re-read is quoted verbatim; disarm is never gated on the arming variable |
| T-47.5-29 | Repudiation | five windows reported as one | high | mitigate | Window accounting is a named section of the run report with the cycle count stated plainly, following Phase 47's disclosure |
| T-47.5-30 | Tampering | degrading a record's data to force a recompute | high | mitigate | Prohibited outright — removing the need for that workaround is this phase's whole purpose; a record that will not recompute is a stop-and-report |
| T-47.5-31 | Denial of service | execution budget burn | low | accept | At most three POSTs, one execution each, against a 2,500/month allowance; recorded in the cost actuals |
| T-47.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages |
</threat_model>

<verification>
- `47.5-AFTER.json` covers the same four ids as the before-snapshot; Jam TV still carries its veto.
- Each written record's flag and reason match the pre-arm prediction, verified by independent read-back.
- The disarm `observed` block shows `ALLOW_HUBSPOT_RECORD_WRITES: "false"` and both allowlists empty.
- `.venv/bin/python -m pytest` and `node --test tests/n8n/*.test.mjs` are green after the run, with no test edited to accommodate it.

</verification>

<success_criteria>
- At most three records written, each exactly once, inside one arm and one disarm.
- Every veto outcome asserted by independent read-back; score and tier recorded as predictions.
- The window is proven closed by an independent re-read quoted verbatim.
- The run report states any miss plainly rather than softening it.
</success_criteria>

<output>
Create `.planning/phases/47.5-veto-recompute-path/47.5-06-SUMMARY.md` when done.
</output>
