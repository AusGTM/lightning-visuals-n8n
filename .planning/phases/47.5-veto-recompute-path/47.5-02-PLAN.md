---
phase: 47.5-veto-recompute-path
plan: 02
type: execute
wave: 2
depends_on: ["47.5-01"]
files_modified:
  - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md
autonomous: false
requirements: [RECOMP-01, RECOMP-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The changed workflow is deployed AND bounced. A bare PUT updates stored content only; the running instance keeps the old graph until a deactivate/activate bracket forces it to reload."
    - "Verification reads the RUNNING behaviour — a live execution with includeData — never a GET read-back of stored JSON, which proves only what was stored."
    - "The disarmed live POST proves Decide Company Action ran, and that Normalize + Score Company did NOT. Both signals together discharge assumption A1: n8n's multi-inbound dispatch did not fire the false lane as well."
    - "The disarmed POST writes nothing: ALLOW_HUBSPOT_RECORD_WRITES reads false and both allowlists read empty before it is sent, and Decide emits write_blocked."
    - "The verification record is Simtech LED 18047161864 — complete, frozen, and its recompute currently derives a cleared veto, so even an impossible write would change nothing."
    - "A second POST without the recompute intent, against the same record, terminates at Build Response carrying the gate reason — the RECOMP-02 observability proof."
    - "Neither POST runs a provider node, the Claude Web Research node or the Judge Call node — proven from the execution's own node list, not inferred."
  artifacts:
    - .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md
  key_links:
    - "deploy_n8n_workflows.py PUT -> stored content only -> n8n_control.set_active(False) then (True) -> running instance reloads"
    - "disarmed D-18 POST with recompute -> live execution -> executions API includeData -> resultData.runData node list"
    - "Decide Company Action -> _writeSafetyAllows(enrich, id, domain) with both allowlists empty -> action write_blocked -> no PATCH"
  prohibitions:
    - "Never treat a GET read-back of the stored workflow as proof that the running graph changed."
    - "Never read compute_workflow_diff's output as a verdict — credentials, webhookId and executeWorkflow placeholders always differ (project memory: n8n-deploy-diff-noise.md)."
    - "Never arm any write surface in this plan. Both POSTs are disarmed and must be proven disarmed BEFORE they are sent."
    - "Claude never sets ALLOW_N8N_DEPLOY, DRY_RUN=false, ALLOW_N8N_ARM or ALLOW_VETO_REMEDIATION — arming is operator-only and per-shell."
    - "Never touch any of Phase 47's 17 records beyond this read-only, write-blocked verification POST at Simtech LED."
    - "Never re-run the verification against a record whose recompute would derive a NEW veto — the point is a proof that cannot change data even if every gate failed at once."
  reversibility_note: "The deploy is reversible by redeploying the previous built JSON and bouncing again; nothing is written to HubSpot in this plan."
---

<objective>
Get the recompute lane into the RUNNING n8n instance and prove, from a live execution, that
a complete record now reaches Decide Company Action and that a skipped record now reports
itself — with every write surface disarmed.

Purpose: an offline-green build proves nothing about n8n. Phase 47 learned the expensive
version of this lesson; plan 03 must not discover a stale graph inside an armed window.

Output: `47.5-A-LIVE-PROOF.md` — the deploy record, the bounce read-backs, and the two
execution node lists quoted verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47.5-veto-recompute-path/47.5-RESEARCH.md
@.planning/phases/47.5-veto-recompute-path/47.5-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator deploys the built workflow (two-key gate) and reports the outcome</name>
  <what-built>Plan 01's recompute lane, built into `n8n/wf_enrichment_cloud.json` and committed. Offline tests green on both tiers.</what-built>
  <action>
Halt and hand the operator the two commands below verbatim. Claude runs neither:
`ALLOW_N8N_DEPLOY` and `DRY_RUN=false` are operator-only and per-shell, a standing project
rule rather than a preference. Wait for the pasted stdout and exit codes before Task 2. Do
not bounce here — the bounce is Task 2, and it must follow a confirmed successful PUT.
  </action>
  <instructions>
Run the dry-run first, then the live deploy, from an operator shell:

! .venv/bin/python scripts/deploy_n8n_workflows.py

! DRY_RUN=false ALLOW_N8N_DEPLOY=true .venv/bin/python scripts/deploy_n8n_workflows.py

The dry-run writes nothing. Its diff is known to be noisy — credentials, `webhookId` and
`executeWorkflow` placeholders always differ — so read it for the presence of the two new
IF nodes, not as a pass/fail verdict.

The live command PUTs stored content and deliberately does NOT activate: that script's own
docstring says activation is a separate operator-runbook step. Report the per-workflow
status codes.
  </instructions>
  <verification>The live command returns a success status for "LV Enrichment (Cloud template)" (workflow id 950HPb7a1GgSAIyZ), and the dry-run output mentions both new IF node names. The diff itself is not a verdict — credentials, webhookId and executeWorkflow placeholders always differ.</verification>
  <resume-signal>Paste the deploy outcome (status codes per workflow), or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bounce the workflow so the running instance reloads the changed graph</name>
  <files>.planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md</files>
  <precondition>Task 1's live deploy returned success for "LV Enrichment (Cloud template)" (workflow id 950HPb7a1GgSAIyZ). Without it this task bounces the OLD graph and every later signal is a false negative.</precondition>
  <read_first>
    - operator-claude-plugin/scripts/n8n_control.py :245-276 (set_active — GET, POST, then an INDEPENDENT second GET that decides the verdict)
    - operator-claude-plugin/scripts/config_gate.py :98-110 (the loader)
    - project memory: n8n-stored-vs-running-content.md
  </read_first>
  <action>
Bounce the enrichment workflow with `n8n_control.set_active`, twice — `False` then `True`.
`deploy_n8n_workflows.py` never activates and `apply_mutation` refuses structural changes,
so neither of them can bounce a newly-deployed graph; `set_active` is the named vehicle and
its verdict comes from an independent second GET rather than the mutation's own echo.

Driver form (read-only apart from the two activation POSTs, which touch no record):

```
.venv/bin/python -c "
import sys; sys.path.insert(0,'operator-claude-plugin/scripts')
import config_gate, n8n_control
cfg = config_gate.load_config()
for want in (False, True):
    print(n8n_control.set_active('950HPb7a1GgSAIyZ', want, cfg))
"
```

**Correcting 47.5-RESEARCH.md:** that document's driver snippet calls `config_gate.load()`.
The function is `config_gate.load_config()`. Use the real name.

Open `47.5-A-LIVE-PROOF.md` and record both `set_active` results verbatim, including each
independent read-back. Note in the file that the first arm after a deploy re-bounces anyway
(`n8n_arming.arm` does its own deactivate/PUT/activate bracket) — this explicit bounce
exists because Task 3 runs disarmed, before any arm, and would otherwise test the old graph.
  </action>
  <verify>
    <automated>grep -c 'set_active\|"active": true' .planning/phases/47.5-veto-recompute-path/47.5-A-LIVE-PROOF.md</automated>
  </verify>
  <done>Both bounce results are recorded verbatim in `47.5-A-LIVE-PROOF.md`, each carrying its own independent read-back, and the workflow ends active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Two disarmed live POSTs at Simtech LED, read back from the executions API</name>
  <what-built>The recompute lane is deployed and the running instance has reloaded it.</what-built>
  <how-to-verify>
**Before sending anything**, confirm both write surfaces are shut, and record the output:

```
.venv/bin/python scripts/june_run_arm.py --disarm
```

Its `observed` block is an independent re-read. `ALLOW_HUBSPOT_RECORD_WRITES` must read
`"false"` and `TEST_RECORD_IDS` / `TEST_RECORD_DOMAINS` must read empty. Disarm is never
gated on the arming variable, so this is always safe to run.

Then send two POSTs at **Simtech LED `18047161864`** — chosen because it is complete and
frozen (it is the record that revealed this defect), and because its recompute currently
derives a CLEARED veto, so even an impossible write could not change its data:

1. With the recompute intent, via `post_webhook_event(company_id, True, cfg, recompute=True)`.
2. Without it, via `post_webhook_event(company_id, True, cfg)`.

**Pass `armed=True` on both, and understand why that is not a contradiction.** That parameter
has no default and raises `NotArmedError` before any network call when falsy — it is the
REMEDIATION SCRIPT's own client-side ceremony flag, the one Phase 47 paired with
`ALLOW_VETO_REMEDIATION`. It gates whether this process is willing to send a POST. It has
nothing to do with whether n8n is willing to write. The disarm that matters here is
server-side: `ALLOW_HUBSPOT_RECORD_WRITES` reads false and both allowlists read empty, which
is what the pre-POST re-read above proves and what makes Decide emit `write_blocked`. Passing
`armed=False` would simply mean no POST is sent and nothing is verified.

Read each execution back with `includeData=true` and check:

| Signal | POST 1 (recompute) | POST 2 (no intent) |
|---|---|---|
| `Decide Company Action` in `resultData.runData` | present | absent |
| `Normalize + Score Company` in `runData` | **absent** | absent |
| provider nodes / `Claude Web Research` / `Judge Call` | absent | absent |
| `Decide` emits `action` | `write_blocked` | n/a |
| run reaches `Respond to Webhook` | yes | yes |
| response body carries `action: "skip"` + gate reason | n/a | yes |

`Normalize + Score Company` being ABSENT from POST 1 is the load-bearing signal: it proves
the false lane did not also fire, which is the multi-inbound dispatch question
47.5-RESEARCH.md could not answer offline (assumption A1). If it IS present, stop — the
lanes are not mutually exclusive and plan 03 must not run.

`write_blocked` is the correct outcome, not a failure: it proves the lane would have PATCHed
and that the write-safety gate is engaged.

Record everything in `47.5-A-LIVE-PROOF.md`: the pre-POST disarm block, both execution ids,
both node lists, the two response bodies, and durations. Today's skip signature is 19 nodes
/ 2.4 s (execution 11846); a run that reaches Decide is materially longer and ends at
`Respond to Webhook`.
  </how-to-verify>
  <resume-signal>Type "approved" with both execution ids, or describe which signal failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> n8n Public API | `ALLOW_N8N_DEPLOY` + `DRY_RUN=false` two-key gate; API key from the plugin config, never printed |
| n8n running graph -> HubSpot | disarmed for the whole plan; `_writeSafetyAllows` denies on an empty allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47.5-06 | Tampering | deploying an unproven graph to a live workflow | high | mitigate | Plan 01's offline tiers are green before the deploy; the deploy is reversible by redeploying the prior built JSON and bouncing again; no record write is possible while disarmed |
| T-47.5-07 | Repudiation | a stored read-back mistaken for proof the running graph changed | high | mitigate | Verification is a live execution read through the executions API, never a workflow GET. Named as a prohibition |
| T-47.5-08 | Elevation of privilege | a verification POST that unexpectedly writes | high | mitigate | Both surfaces are proven disarmed by an independent re-read BEFORE the POST; the target record's recompute derives no change even if every gate failed at once |
| T-47.5-09 | Denial of service | the deploy bouncing an active production workflow | medium | accept | The bounce is seconds long, the workflow is webhook- and schedule-triggered, and the daily poller's window is 24h wide. Accepted rather than mitigated |
| T-47.5-10 | Information disclosure | secrets in deploy or execution output | medium | mitigate | Neither the API key nor the webhook secret is ever echoed into `47.5-A-LIVE-PROOF.md`; quote node lists and actions only |
| T-47.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages |
</threat_model>

<verification>
- `47.5-A-LIVE-PROOF.md` contains: the deploy outcome, both bounce read-backs, the pre-POST disarm block quoted verbatim, both execution ids with their node lists, and both response bodies.
- The recompute execution shows `Decide Company Action` present and `Normalize + Score Company` absent.
- No HubSpot property on `18047161864` changed — confirmed by a read before and after.
</verification>

<success_criteria>
- The running instance carries the recompute lane, proven by execution rather than by read-back.
- A complete record reaches the sole veto writer on demand, disarmed, at zero provider and zero Anthropic cost.
- A complete record without the intent reports its skip to the caller.
- Assumption A1 (multi-inbound dispatch) is discharged with evidence, or the phase stops here.
</success_criteria>

<output>
Create `.planning/phases/47.5-veto-recompute-path/47.5-02-SUMMARY.md` when done.
</output>
