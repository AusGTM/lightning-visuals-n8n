---
phase: 47.5-veto-recompute-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
  - scripts/remediate_veto_companies.py
  - scripts/june_run_arm.py
  - tests/n8n/companyRecomputeLaneFlow.test.mjs
  - tests/test_cloud_companies_branch.py
  - tests/test_remediate_veto_companies.py
  - tests/test_june_run_arm.py
autonomous: true
requirements: [RECOMP-01, RECOMP-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A company whose enrichment inputs are complete (Company Gate returns action skip) reaches Decide Company Action when — and only when — the request carries the recompute intent, and the veto fields it emits are derived from existingRecord with no merge present."
    - "The recompute lane runs zero provider nodes, zero research nodes and zero Anthropic nodes: it leaves Company Gate and arrives at Decide Company Action with nothing in between."
    - "Decide Company Action remains the ONLY node in the built workflow that assigns lv_anti_icp_flag / lv_anti_icp_reason — exactly one assignment site, proven by a count gate over the built JSON, not by inspection."
    - "A recompute request for a record that does not resolve to an existing company is REFUSED (BUG-19 shape): no create branch, no seeded domain/name, an explicit terminal action string in the response."
    - "A skipped record is observable: without the recompute intent, a complete record terminates at Build Response carrying its gate reason, instead of the silent 200 with no body it returns today (RECOMP-02)."
    - "The recompute intent is NOT a value of `mode`. isReturnOnly() in n8n/code/matchProposal.js treats every non-write mode as return-only, so a mode-borne intent would report success and write nothing."
    - "The recompute predicate is REQUEST-level, read by node name from Parse HubSpot Event with .first(), so the two lanes of IF Company Recompute are mutually exclusive for a whole execution by construction."
    - "A mixed 2-row batch (one skip, one enrich) without the recompute intent scores the enrich row against ITS OWN provider responses — the paired-index defect is closed by construction because skipped rows no longer reach Build Company Requests."
    - "n8n/wf_enrichment_cloud.json is BUILT by scripts/build_cloud_workflows.py and committed alongside the script change. It is never hand-edited."
    - "post_webhook_event can carry the recompute intent and no longer hardcodes a 30-second read timeout — the 300s correction Phase 47 made only inside 47-armed-driver.py lands in the script itself."
    - "june_run_arm.py can arm a DOMAIN allowlist, not ids alone — n8n_arming.arm_for_dispatch already takes record_domains and the wrapper never exposed it. Plan 03 cannot arm a not-yet-created disposable by id."
  artifacts:
    - scripts/build_cloud_workflows.py
    - n8n/wf_enrichment_cloud.json
    - tests/n8n/companyRecomputeLaneFlow.test.mjs
  key_links:
    - "D-18 POST event `recompute: true` -> Parse HubSpot Event normalized boolean -> IF Company Recompute (request-level, by node name) -> Decide Company Action"
    - "Company Gate action skip -> under recompute -> enrich -> Decide's IF Company Enrich -> HubSpot Company Update PATCH"
    - "Company Gate action create -> under recompute -> recompute_refused -> falls through both write IFs -> Build Response"
    - "Company Gate action skip -> without recompute -> IF Company Skip true -> Build Response -> Respond to Webhook"
  prohibitions:
    - "Never hand-edit n8n/wf_enrichment_cloud.json — build it."
    - "Never add a second node that assigns the veto flag or the veto reason. Decide Company Action stays the sole writer."
    - "Never carry the recompute intent in `mode`."
    - "Never route the recompute lane through Merge Company — it would re-stamp *_verified_at and re-enter the D-20 clobber race this lane exists to avoid."
    - "Never change the non-ANZ predicate, the no-content predicate or D-16's reason strings in this plan. Plan 01 changes reachability, never a verdict."
    - "Never make the recompute predicate per-row — request-level is what keeps the two lanes mutually exclusive."
    - "Never deploy in this plan. Build and test only; plan 02 owns deploy, bounce and live verification."
    - "Never attempt to fix scripts/run_scoring_parity.py's population sweep — red by design until Phase 49."
---

<objective>
Make a veto recompute reachable for a record whose inputs are already complete, on demand,
at zero provider/research/Anthropic cost, and make a skipped record stop being silently
successful. Offline only: build script, built workflow, tests, and the two operator-facing
helpers plan 03 needs.

Purpose: this is workstream A's mechanism. Nothing in workstreams B or C can be written
until this lands and is proven — changing a record's region while the gate still skips it
does nothing to its flag, which is the exact trap Phase 47 hit.

Output: two new IF nodes and their edges in the companies branch, a gate that flips skip to
enrich under the recompute intent and refuses create, a recompute-capable webhook helper, a
domain-capable arm wrapper, and the regression tests that pin all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47.5-veto-recompute-path/47.5-CONTEXT.md
@.planning/phases/47.5-veto-recompute-path/47.5-RESEARCH.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end recompute lane — one complete record, one path, no providers</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/n8n/companyRecomputeLaneFlow.test.mjs</files>
  <read_first>
    - scripts/build_cloud_workflows.py :1834-1851 (ENRICH_CO_GATE — the gate wrapper that calls decideAction)
    - scripts/build_cloud_workflows.py :1875-1914 (ENRICH_NORMALIZE_SCORE_CO — the skip filter and the paired-index read)
    - scripts/build_cloud_workflows.py :2750-2883 (ENRICH_DECIDE_CO_CLOUD — the veto derivation, the create-seed branch, _writeSafetyAllows)
    - scripts/build_cloud_workflows.py :2918-2938 (_if_bool_expr_node) and :2992-2996 (_provider_enabled_expr — the by-node-name idiom)
    - scripts/build_cloud_workflows.py :3620-3705 (ENRICH_PARSE_EVENT_CLOUD — where the row is built and `...event` is spread)
    - scripts/build_cloud_workflows.py :4578-4585 (companies node emission around Company Gate) and :4946-4998 (companies connections block)
    - tests/n8n/researchChainRowFlow.test.mjs (the chain-flow test template this task copies)
    - tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs (already proves the merge-free existingRecord derivation this lane depends on)
  </read_first>
  <behavior>
    - Given a request whose first event carries `recompute: true` and a company whose gate verdict is skip, the row arrives at Decide Company Action and Decide emits action `enrich` plus both veto fields derived from existingRecord.
    - Given the same company and NO recompute intent, the row terminates at Build Response carrying `action: "skip"` and its gate reason, and never reaches Build Company Requests.
    - Given a recompute request whose gate verdict is create, the gate emits `recompute_refused` and Decide seeds neither domain nor name.
    - Given a recompute request whose gate verdict is already `enrich` (a record with a present-but-unstamped input, which the gate reads as stale), the row ALSO takes the recompute lane and reaches Decide without touching providers or research. The intent is request-level, so the verdict only decides what Decide does with the row, never which lane carries it. Plan 03's acceptance test depends on this.
    - The recompute row's journey contains no provider node, no research node and no judge node.
  </behavior>
  <action>
Add the request-level recompute lane to the companies branch of `build_enrichment_cloud()`.
Four coordinated edits in `scripts/build_cloud_workflows.py`, then rebuild.

1. `ENRICH_PARSE_EVENT_CLOUD` — in the object each event maps to, add an explicit
   `recompute` key placed AFTER the `...event` spread so a caller-injected raw value can
   never shadow it, computed strictly as `event.recompute === true`. Anything that is not a
   real JSON boolean true normalizes to false; that direction is fail-closed and costs
   nothing. Comment why the placement is after the spread (companies has
   `entry_strip_markers=False`, so caller-supplied row properties are not stripped on this
   branch) and why this is not carried in `mode` (per V5 in 47.5-RESEARCH.md's security
   table, and see the prohibition in this plan's frontmatter).

2. `ENRICH_CO_GATE` — after `decideAction(...)` resolves `action`, read the request-level
   intent by node name, `$('Parse HubSpot Event').first().json.recompute === true`, and
   apply exactly two mappings when it is set: `skip` becomes `enrich` (so Decide's enrich IF
   fires the PATCH), and `create` becomes `recompute_refused` with `gate.reason` rewritten to
   say a recompute was requested for a company that did not resolve. `enrich` is untouched.
   Leave `gate` itself otherwise intact — the reason string is what makes the outcome
   readable in the response. Note in a comment that a Code node cannot call `$()` on a node
   that has not run; Parse HubSpot Event is the root and always has.

3. Two new IF nodes, emitted next to `Company Gate` on a free row so no existing node
   position moves:
   - `IF Company Recompute` via `_if_bool_expr_node`, expression
     `$('Parse HubSpot Event').first().json.recompute === true`. `.first()` — never `.item` —
     is what makes this a whole-request decision, which is what keeps the two lanes mutually
     exclusive per execution and preserves the property every existing multi-inbound
     convergence in this graph relies on.
   - `IF Company Skip` via `_if_bool_expr_node`, expression `$json.action === "skip"`. Bare
     `$json` is correct here and only here: the immediate upstream is a Code node, with no
     HTTP hop in between that could have replaced the item.

4. Connections — replace the single `Company Gate -> Build Company Requests` edge (currently
   emitted by the `chain([...])` call in the companies connections block) with:
   `Company Gate -> IF Company Recompute`;
   `IF Company Recompute` true -> `Decide Company Action`, false -> `IF Company Skip`;
   `IF Company Skip` true -> `Build Response`, false -> `Build Company Requests`.
   Both `chain([...])` calls that currently terminate at `Company Gate` keep their edges;
   only the outgoing edge changes. The skip terminal is a direct port of the contacts
   branch's existing `Skip (NoOp) -> Build Response` edge.

   Leave `ENRICH_NORMALIZE_SCORE_CO`'s existing skip filter line in place. It becomes a
   no-op because a skipped row can no longer reach `Build Company Requests`, and that is
   precisely what closes the paired-index defect — the provider arrays and the filtered rows
   now align by construction. Add a comment saying it is retained as defence in depth and is
   no longer load-bearing.

Do NOT touch Decide's veto derivation block: with `row.merge` absent, `properties` is `{}`
and the three `??` fall through to `existingRecord`, which is the whole reason a merge-free
lane works. Do NOT route the lane through `Merge Company`.

Rebuild with `.venv/bin/python scripts/build_cloud_workflows.py` and commit the regenerated
`n8n/wf_enrichment_cloud.json` alongside the script change.

Write `tests/n8n/companyRecomputeLaneFlow.test.mjs` on the `researchChainRowFlow.test.mjs`
template — evaluate the emitted `jsCode` of the real committed workflow across the hop
sequence with faked `$()` node lookups. Cover the four behaviours above. Model the request
level correctly: the fake `$('Parse HubSpot Event').first()` returns the request's first
event row.
  </action>
  <verify>
    <automated>node --test tests/n8n/companyRecomputeLaneFlow.test.mjs && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>A complete record reaches Decide Company Action under the recompute intent and emits both veto fields from existingRecord; the same record without the intent terminates at Build Response with its gate reason; a create verdict under the intent is refused; no provider, research or judge node appears on the recompute path. `node --test tests/n8n/*.test.mjs` is green with no pre-existing test weakened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural guards — the built graph, and exactly one veto writer</name>
  <files>tests/test_cloud_companies_branch.py</files>
  <read_first>
    - tests/test_cloud_companies_branch.py (existing BFS-reachability and Decide-veto assertions — extend, never replace)
    - tests/test_n8n_org_type_absence.py (its regex flags only a term adjacent to `: <number>`; nothing in this plan is numeric, so it must stay green untouched)
  </read_first>
  <behavior>
    - The built workflow contains nodes named `IF Company Recompute` and `IF Company Skip`, both BFS-reachable from the Webhook Trigger.
    - The five edges named in Task 1 exist exactly as wired, and `Company Gate` no longer has a direct edge to `Build Company Requests`.
    - Across every node's jsCode in the built workflow, the number of assignment sites for the veto flag property is exactly one, and that node is `Decide Company Action`.
    - The recompute lane's path from `IF Company Recompute` true to `Decide Company Action` is a single edge — no intermediate node.
  </behavior>
  <action>
Extend `tests/test_cloud_companies_branch.py` with the four assertions above. Follow the
file's existing `_load()` / `_reachable_from()` helpers rather than adding new ones.

For the single-writer assertion use a POSITIVE count gate: scan every node's `jsCode` in the
built JSON for the assignment shape (the property name followed by optional whitespace, `=`,
and not `==`), assert the total is 1, and assert the owning node's name. Do not express this
as a zero-hit search over any file whose comments this plan authors — a comment mentioning
the property would then invalidate its own gate.

Assert the edge set explicitly, by reading `connections` and comparing the target lists for
`Company Gate`, `IF Company Recompute` and `IF Company Skip` against the expected wiring.
An edge-count assertion alone would pass on the wrong targets.

Add a docstring on each new test naming the defect it guards, in the style the file already
uses, so a later reader knows why it exists.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cloud_companies_branch.py tests/test_n8n_org_type_absence.py -q</automated>
  </verify>
  <done>Both new nodes are present and reachable, the five edges are asserted by target not by count, the veto-writer count gate reads exactly one and names Decide Company Action, and the Phase 46 org-type absence guard is still green with no edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Operator helpers — a recompute-capable POST and a domain-capable arm</name>
  <files>scripts/remediate_veto_companies.py, scripts/june_run_arm.py, tests/test_remediate_veto_companies.py, tests/test_june_run_arm.py</files>
  <read_first>
    - scripts/remediate_veto_companies.py :557-590 (build_webhook_event and post_webhook_event — note the hardcoded read timeout)
    - .planning/phases/47-veto-remediation/47-armed-driver.py :60-80 (the _PatientTransport wrapper Phase 47 used instead of fixing the script)
    - scripts/june_run_arm.py :47-77 (arm(), which passes record_domains=[] unconditionally) and :100-125 (the argparse block)
    - operator-claude-plugin/scripts/n8n_arming.py :264-300 (arm_for_dispatch already accepts record_domains)
  </read_first>
  <behavior>
    - `build_webhook_event(id, recompute=True)` emits the D-18 array element plus a real JSON boolean `recompute: true`; with the default it emits the byte-identical event shape it emits today, with no recompute key at all.
    - `build_webhook_event(id, domain="x.example")` additionally carries `domain`, and omits the key entirely when it is None. A domain-carrying event routes through `HubSpot Company Search` (domain EQ) rather than the bare-event fetch-by-id lane, which is what populates `identity_keys.domain` so `_writeSafetyAllows` can match a DOMAIN allowlist — the only allowlist plan 03 can arm for a company that does not exist yet.
    - `post_webhook_event(..., recompute=True)` sends that body and uses a read timeout long enough for a lane that reaches Decide, defaulting to 300 seconds and overridable per call.
    - `june_run_arm.py --domains a.example` arms a domain allowlist; `--ids` alone and `--domains` alone each work; both empty is still the existing refusal.
    - The disarm path is not gated on the arming variable and is unchanged by this task.
  </behavior>
  <action>
Two small, separately-testable helper changes. Neither performs a live call in this plan.

`scripts/remediate_veto_companies.py`:
- Give `build_webhook_event` a `recompute: bool = False` keyword and a `domain: str | None =
  None` keyword. Add each key to the emitted dict ONLY when it is set — an always-present
  false or null key changes the event body shape and would break any byte-shape assertion on
  the existing payload.
- Give `post_webhook_event` matching `recompute: bool = False` and `domain: str | None = None`
  keywords threaded into `build_webhook_event`, and replace the hardcoded 30-second read timeout with a
  `timeout: float = 300` keyword. **Correcting 47.5-RESEARCH.md:** that document states the
  300s correction already lives in this function. It does not — it lives only in
  `.planning/phases/47-veto-remediation/47-armed-driver.py`'s transport wrapper, which is
  why Phase 47 burned a window on a read timeout against a run n8n had already completed.
  Land it in the script.
- Leave the `armed` positional and its `NotArmedError` exactly as they are.

`scripts/june_run_arm.py`:
- Add a `--domains` argument mirroring `--ids`, parsed by the same `_parse_ids` splitter,
  and pass it through to `n8n_arming.arm_for_dispatch`'s existing `record_domains` parameter
  — the library has always accepted it; only this wrapper hid it.
- Widen the empty-allowlist refusal to fire only when ids AND domains are both empty, and
  keep its wording: an empty allowlist denies every write and would look like a successful
  arm.
- `allow_create` stays false. Do not touch `disarm()`.

Add offline tests for each behaviour above with an injected transport / fake config, in the
style the existing tests for these two scripts already use. No network call in any test.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_remediate_veto_companies.py tests/test_june_run_arm.py -q</automated>
  </verify>
  <done>The webhook helper can carry the recompute intent and waits 300s by default; the default event body is unchanged; the arm wrapper can arm a domain allowlist and still refuses when both allowlists are empty; the disarm path is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> n8n webhook | The D-18 POST body is caller-supplied and header-authenticated (`X-Enrichment-Secret`) at the trigger, before any node runs |
| webhook row -> HubSpot PATCH | `_writeSafetyAllows` plus the `TEST_RECORD_IDS` / `TEST_RECORD_DOMAINS` allowlist gate every write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47.5-01 | Elevation of privilege | the caller-supplied `recompute` flag | medium | mitigate | The flag changes routing, never authorisation. Every PATCH still passes `_writeSafetyAllows`, which is disarmed by default and allowlisted when armed. Asserted by the structural test that the lane's terminal is Decide, not the PATCH node |
| T-47.5-02 | Tampering | caller-injected row properties on the companies branch (`entry_strip_markers=False`) | medium | mitigate | `recompute` is normalized in Parse HubSpot Event AFTER the `...event` spread and read by node name from that root node, so a mid-chain injected property cannot flip the lane |
| T-47.5-03 | Spoofing / junk data | recompute request that resolves to no existing company | high | mitigate | The gate maps `create` to `recompute_refused` under the intent; Decide's seed branch is gated on `action === "create"` and never fires. Pinned by a Task 1 test (BUG-19 shape) |
| T-47.5-04 | Repudiation | a request that silently does nothing | medium | mitigate | `IF Company Skip` terminates skipped rows at `Build Response` carrying the gate reason, replacing today's bare 200 (RECOMP-02) |
| T-47.5-05 | Denial of service | a recompute request that fans out to providers or Anthropic | low | mitigate | The lane bypasses the provider chain, the research chain and the judge outright; asserted by the Task 1 path test |
| T-47.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages. No package-manager command appears in any task |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` — green, 658 pre-existing tests plus the new file, none weakened.
- `.venv/bin/python -m pytest` — green, no pre-existing test edited to accommodate this change.
- `git diff --stat n8n/wf_enrichment_cloud.json` shows the built file changed in the same commit as the build script, and `git diff scripts/run_scoring_parity.py` is empty.
</verification>

<success_criteria>
- A complete record reaches Decide Company Action under the recompute intent, and Decide emits both veto fields derived from `existingRecord` with no merge present.
- The same record without the intent terminates observably at Build Response.
- A recompute request that resolves to no company is refused with a named terminal action.
- Exactly one node in the built workflow assigns the veto fields.
- The two operator helpers plan 03 depends on exist and are tested offline.
</success_criteria>

<output>
Create `.planning/phases/47.5-veto-recompute-path/47.5-01-SUMMARY.md` when done.
</output>
