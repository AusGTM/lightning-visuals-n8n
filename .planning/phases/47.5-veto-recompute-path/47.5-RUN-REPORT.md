# 47.5-RUN-REPORT.md — armed window #2

Phase 47.5, plan 06. Window #2 of the **two** this phase declared up front. Phase 47 needed
five windows for one plan; the correction was declaring the count before opening any, and this
is the declared second and last.

---

# Part 1 — Pre-arm guard (read-only, BEFORE anything was armed)

**Nothing armed. Nothing written to HubSpot. Every HubSpot call in this part was a GET or a
search POST; the one webhook POST was fired against a provably disarmed instance and is
accounted for below.**

## 1.1 The write set, derived from the recorded decisions

Derived from the two decision files, not from the plan's prose and not from memory:

| id | name | action | value | source of the decision |
|---|---|---|---|---|
| `17317184159` | Ironman | `patch_region_then_recompute` | `lv_country_region_normalized = "ANZ"` | `47.5-B-EVIDENCE.md` § Operator decision — 2026-08-12 |
| `15860277364` | GRAVITY MEDIA | `patch_region_then_recompute` | `lv_country_region_normalized = "ANZ"` | `47.5-B-EVIDENCE.md` § Operator decision — 2026-08-12 |
| `18047161864` | Simtech LED | `recompute_only` | *(no input write at all)* | `47.5-C-DECISION.md` § Consequence for the write window |

**Three ids — the cap. The two decision files agree and neither names an id outside it.**

**Excluded by name, and never in the allowlist:**

- **Entain `10024564084`** — cannot move. Its `lv_produces_content` reads `"false"` live, which
  fires the no-content hard veto independently of geography; its reason string already carries
  two vetoes. A region flip would spend a touch and leave it at Tier D.
- **Jam TV `17317850381`** — correct as it stands (D-23). Not researched, not a candidate, not
  read-modified. Its veto must persist.

`ANZ` for Gravity Media rests on **Australian operating presence alone** — its New Zealand leg
is UNPROVEN and recorded as such in `47.5-B-EVIDENCE.md`. Per D-V6 the label denotes the
multinational-with-local-operations pattern, not a count of countries.

## 1.2 Live state, and the diff against the before-snapshot

Read live 2026-08-12, immediately before arming:

| id | name | region | org_type | hw_bool | score | tier | flag | reason | hs_lastmodified |
|---|---|---|---|---|---|---|---|---|---|
| `17317184159` | Ironman | `Other` | `governing_body_league` | `null` | 70 | D | `true` | `Non-ANZ geography` | 2026-08-09T23:13:42.149Z |
| `15860277364` | GRAVITY MEDIA | `Other` | `broadcaster` | `null` | 50 | D | `true` | `Non-ANZ geography` | 2026-08-09T23:13:39.445Z |
| `18047161864` | Simtech LED | `AU` | `hardware_vendor` | `null` | 40 | **B** | **`false`** | `""` | 2026-08-12T01:55:43.556Z |
| `10024564084` | Entain *(excluded)* | `Other` | `gambling_operator` | `null` | -70 | D | `true` | `Non-ANZ geography; No broadcast or streaming content` | 2026-08-09T23:13:31.865Z |
| `17317850381` | Jam TV *(excluded)* | `Other` | `null` | `null` | 20 | D | `true` | `Non-ANZ geography` | 2026-08-12T01:42:45.648Z |

**Diff vs `47.5-B-BEFORE.json`: MATCHES on every field the snapshot holds, for all four census
records.** Region, org_type, produces_content, hardware boolean, score, tier, flag, reason and
both `_verified_at` stamps are unchanged since plan 04's capture. No record moved under the
plan; the pre-arm read is a confirmation, not a discovery.

**Stated rather than glossed: Simtech LED has NO row in `47.5-B-BEFORE.json`.** That snapshot
holds the four-record non-ANZ census, and Simtech is not in it (it carries no veto at all
today — that is the entire point of workstream C). Its diff source is therefore the live-state
table in `47.5-WINDOW2-PREARM.md`, written before arming: `AU / hardware_vendor / null / 40 /
B / false / ""`. **The live read above is byte-identical to it.**

## 1.3 The two portal-wide queries — different queries, labelled distinctly

**(a) The non-ANZ veto census** — `lv_anti_icp_reason CONTAINS_TOKEN "Non-ANZ"`:

```
4
  10024564084 Entain          region Other
  15860277364 GRAVITY MEDIA   region Other
  17317184159 Ironman         region Other
  17317850381 Jam TV          region Other
```

Exactly the four ids the before-snapshot names. **Every id it returns is accounted for**: two
are in the write set, two are excluded in writing above. No unaccounted id — the refuse-to-arm
condition does not apply.

**(b) The VETO-03 bar** — a non-ANZ veto reason ANDed with a **blank**
`lv_country_region_normalized` (`NOT_HAS_PROPERTY`):

```
[]   -> 0 rows
```

**Zero, as at the end of Phase 47.** VETO-03 stays met going into the window.

## 1.4 Abort conditions (`47.5-WINDOW2-PREARM.md`) — all four checked

| # | Condition | Result |
|---|---|---|
| 1 | Deployed `Decide Company Action` lacks `orgType === "hardware_vendor"` | **Does not apply — proven on the RUNNING graph, see 1.5** |
| 2 | `lv_anti_icp_reason` changed on any of the three | **Does not apply** — `Non-ANZ geography`, `Non-ANZ geography`, `""`, all unchanged (1.2) |
| 3 | Entain or Jam TV in the allowlist | **Does not apply** — allowlist is the three ids of 1.1 and is asserted at arm time |
| 4 | Allowlist empty | **Does not apply** — checked live at arm time; an empty allowlist denies every write and reads as a successful arm |

## 1.5 Abort #1 turned from trust into evidence — one DISARMED rehearsal POST

A GET of the workflow proves only what is **stored**; stored ≠ running. The orchestrator's
deploy + bounce record (`47.5-C-DECISION.md` § Deploy record) establishes the reload. To make
abort #1 evidence about the *running* graph rather than an inference, one disarmed recompute
POST was fired at Simtech — the same record and the same disarmed pattern plan 02 proved
writes nothing.

**Stored read-back** of `Decide Company Action`, verbatim:

```js
if (isHardwareVendor === true || orgType === "hardware_vendor") vetoReasons.push("Hardware/AV/LED vendor, not sports-media buyer");
```

with `active: true`, `updatedAt 2026-08-12T06:57:21.032Z`, and the write-safety literals read
out of the same body:

```js
const ALLOW_HUBSPOT_RECORD_WRITES = "false";
const TEST_RECORD_IDS = "";
const TEST_RECORD_DOMAINS = "";
```

**Live execution `11858`**, 20 nodes, node-level `runData` (not `status: "success"` —
47-BLOCKED.md):

| Signal | Value |
|---|---|
| `Company Gate` verdict | `skip` — "all required fields present, fresh and valid" |
| `IF Company Recompute` | present — the recompute lane fired |
| `Decide Company Action` action | **`write_blocked`** |
| `Decide` derived properties | `lv_anti_icp_flag: "true"`, `lv_anti_icp_reason: "Hardware/AV/LED vendor, not sports-media buyer"` |
| provider / research / judge / merge nodes | **none** (only the three always-run credit-probe IFs) |
| HubSpot before vs after | **byte-identical**, `hs_lastmodifieddate` still `2026-08-12T01:55:43.556Z` |

**This is the decisive pre-arm fact.** The running graph derives the hardware veto for Simtech
today, from `lv_org_type` alone with `lv_is_hardware_vendor` still `null` — the OR predicate is
live, not merely deployed — and `write_blocked` proves the lane reached the PATCH and the empty
allowlist denied it. Simtech's armed-window outcome is therefore pre-proven at node level; the
window only has to let the same derivation through the gate.

## 1.6 Predictions — assertions vs predictions

Computed with `src.icp_scoring.compute_icp_score` against each record's **live** properties
with the proposed write applied. The oracle already carries workstream C's OR predicate
(commit `f817ec5`).

| id | write | flag **(ASSERTION)** | reason **(ASSERTION)** | score *(prediction)* | tier *(prediction)* |
|---|---|---|---|---|---|
| `17317184159` Ironman | region → `ANZ` | `false` | `""` (no veto) | 70 → **80** | D → **A** |
| `15860277364` GRAVITY MEDIA | region → `ANZ` | `false` | `""` (no veto) | 50 → **60** | D → **B** |
| `18047161864` Simtech LED | *(none — recompute only)* | `true` | `Hardware/AV/LED vendor, not sports-media buyer` | 40 → **40** | B → **D** |

**Flag and reason are hard assertions.** Score and tier are recorded predictions, not
assertions: Phase 47 recorded that an oracle tier cannot be asserted against a live tier before
the run, and oracle-vs-live parity is Phase 49's scope. Window #1's D-16 is the same caution —
its tier assertion passed without being evidence of a transition.

**Simtech moving to Tier D is a correct outcome, not a regression.** It is also the only row
that exercises the new recompute lane with **no input change at all** — the purest test of
workstream A. If it does not move, the lane did not do its job.

## 1.7 Expected cost

| Item | Expected |
|---|---|
| n8n executions | **3** — one POST per record, one execution each, against the 2,500/month allowance (plus the 1 disarmed rehearsal in 1.5, already spent) |
| Provider credits (Lusha / Apollo / ZoomInfo) | **0** — the recompute lane reaches `Decide Company Action` directly; execution 11858's node list contains no provider node |
| Anthropic calls (Haiku / Sonnet) | **0** — no research, judge or merge node on the lane |
| HubSpot writes | **2 PATCHes** (`lv_country_region_normalized` only) + whatever the pipeline's own single writer emits |

A bare-array D-18 body enables no providers in any case.

## 1.8 Verdict

**Cleared to arm.** Write set of three, derived from the decisions and capped; both excluded
ids named; live state matches the snapshot on every field; census fully accounted for; the
VETO-03 bar still zero; all four abort conditions checked, with #1 proven against the running
graph rather than assumed.

<!-- gsd:write-continue -->
