---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 06
type: execute
wave: 3
depends_on: ["73.1-04", "73.1-05"]
autonomous: true
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/code/matchProposal.js
  - operator-claude-plugin/scripts/backend_status.py
  - operator-claude-plugin/scripts/config_gate.py
  - operator-claude-plugin/config/operator.local.example.json
  - operator-claude-plugin/skills/initialize/SKILL.md
  - operator-claude-plugin/skills/backend-status/SKILL.md
  - tests/n8n/matchProposal.test.mjs
  - tests/n8n/mobilephoneLaneFlow.test.mjs
  - tests/n8n/backendStatusResponse.test.mjs
  - operator-claude-plugin/tests/test_config_gate.py
  - operator-claude-plugin/tests/test_status_all_workflows.py
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_review_decision_cloud.json
  - n8n/wf_scheduled_maintenance_cloud.json
requirements: [D-16a, D-16a-i, D-16d, D-14c]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-16a: the contact ingest lane gains an identity ladder for MATCHING — email, then linkedin_url via `hs_linkedin_url` EQ, then mobilephone — reusing the enrichment lane's `IF Linkedin Searchable` to `HubSpot Linkedin Search` to `Adapt Linkedin Search` idiom, regenerated by the builder and never hand-edited; a match updates and no match creates."
    - "D-16a-i: `phone` is identity for CREATE only and is NEVER a match rung — a landline is often a company switchboard shared by every contact there, and an EQ match on it would update the wrong person."
    - "D-16a-i: `mobilephone` matches on EQ only when there is EXACTLY ONE hit; more than one hit routes to `review` and never updates, and the same single-hit rule applies to the LinkedIn rung."
    - "D-16d: a created emailless contact lands like any other — associated to its company under the single verdict taken at `HubSpot Update Write Gate`, one `written_records` entry, and one report row naming which identity key resolved it."
    - "D-14c: `wf_backend_status_cloud` reads the HubSpot account with the n8n credential and returns `portalId`, proving the n8n credential's own portal; `initialize` and `backend-status` compare it to `hubspot_portal_id`, which stays OPTIONAL in `operator.local.json` so the guard runs only when it is set."
    - "D-14a: the backend read proves the n8n credential's portal and nothing else — an outside tool still proves itself by its own lookup and must never be allowed to trust this answer."
  artifacts:
    - "n8n nodes `IF Mobilephone Searchable`, `HubSpot Mobilephone Search`, `Adapt Mobilephone Search` and their carry Merge and starved-lane sentinel in the ingest lane"
    - "matchProposal.js `laneOf` mobilephone rung and `summarizeMatch`'s mobilephone arm"
    - "n8n node `HubSpot Account Info` in wf_backend_status_cloud and the `portalId` field on its response"
    - "config_gate portal comparison reading the backend's portalId against hubspot_portal_id"
    - "tests/n8n/mobilephoneLaneFlow.test.mjs — the walker test for the new rung"
  key_links:
    - "laneOf's ordering -> the ingest lane's IF chain order: the ladder's priority exists in two places and must agree, or a row with both a linkedin_url and a mobilephone resolves differently in the proposal than in the graph."
    - "every new Merge input -> its starved-lane sentinel: under v1 a Merge whose input never receives a delivery never fires and its downstream terminates silently with status success."
    - "backend portalId -> config_gate comparison -> initialize/backend-status prose: the guard only runs when hubspot_portal_id is set, so an unset value must read as not-checked, never as matched."
---

<objective>
Give the contact ingest lane the identity ladder D-16a specifies — email, then LinkedIn, then
mobilephone, each single-hit only — with `phone` usable for CREATE and never for matching
(D-16a-i); and make the backend prove which HubSpot portal its own credential is on (D-14c).

Purpose: plan 05 made an emailless row admissible. Without this lane change that row reaches a
lane that can only search by email, so it always creates — including when the person is already
in the portal under a LinkedIn URL or a mobile number.

Output: three new nodes plus a carry Merge and a sentinel on the ingest lane, a mobilephone arm
in `matchProposal.js`, a HubSpot account read on the backend-status lane returning `portalId`, and
regenerated JSON.

Why `depends_on` names 73.1-04 and 73.1-05: 04 is a FILE dependency, not a logic one — both plans
edit `initialize/SKILL.md` and `backend-status/SKILL.md`, and 04 inserts the shared CRM-access
block into them before Task 3 adds its portal line. 05 is a file dependency too: it regenerates
every `n8n/wf_*.json`, and two plans writing generated JSON in one wave would race.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-PATTERNS.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-05-SUMMARY.md
@n8n/code/matchProposal.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the mobilephone lane in matchProposal.js, single-hit only</name>
  <files>n8n/code/matchProposal.js, tests/n8n/matchProposal.test.mjs</files>
  <read_first>
    - n8n/code/matchProposal.js in full — the module header, `laneOf` (lines 37-51), `tokenize`/`sharesToken`, the verified-hits helper the linkedin lane uses, and `summarizeMatch`'s four arms with the Phase 61 REVIEW-C4 comment explaining why linkedin is a DEDICATED three-outcome arm and not joined to the two-outcome email arm
    - tests/n8n/matchProposal.test.mjs — the existing linkedin-lane cardinality tests, which are the shape to copy
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-16a, D-16a-i
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-PATTERNS.md § matchProposal.js
  </read_first>
  <behavior>
    - Test 1: `laneOf` returns `mobilephone` for a row whose `identity_keys` carry a mobilephone and no email and no linkedin_url.
    - Test 2: `laneOf` still returns `email` when an email is present, `linkedin` when a linkedin_url is present and no email, and `fetch_by_id` when an object_id is present and no email — the existing order is unchanged and mobilephone sits BELOW linkedin.
    - Test 3: `laneOf` NEVER returns a phone-only lane — a row carrying only `phone` and a name returns `name`, and a row carrying only `phone` returns `none`. `phone` is create-only identity (D-16a-i).
    - Test 4: `summarizeMatch` with `lane === "mobilephone"` and one candidate returns `tier: "high"`, `auto: true`, a reason naming mobilephone, and no candidates.
    - Test 5: with more than one candidate it returns `auto: false` and a `medium` tier with the candidates attached — never a pick.
    - Test 6: with zero candidates it returns `tier: "none"`, `auto: false`, `searched, no hit`.
    - Test 7: the linkedin arm's three outcomes are byte-identical to today — this addition does not touch them.
  </behavior>
  <action>
Add `mobilephone` to `laneOf`'s ranking chain, BETWEEN the `linkedin` rung and the `name` rung,
reading `identity_keys.mobilephone` through the same `trimmedOrValue` helper the other keys use.
Do NOT add a `phone` rung anywhere in `laneOf`: D-16a-i makes `phone` identity for CREATE only,
and a landline is frequently a company switchboard shared by every contact at that company, so an
EQ match on it would update the wrong person. Put that reason in a comment at the rung, naming
D-16a-i and the 2026-09-18 observation that a provider row carried a stray unrelated number.

Add a `lane === "mobilephone"` arm to `summarizeMatch`, sibling to the linkedin arm and identical
in shape: exactly one candidate is a high-tier auto match; more than one is a medium-tier
non-pick carrying the candidates; zero is `searched, no hit`. Substitute the reason strings to
name mobilephone. Do not join it to the linkedin arm with an `||` — the Phase 61 REVIEW-C4
comment explains why each cardinality-reading lane gets its own arm, and a shared arm would make
a future divergence invisible.

Like the linkedin lane, the candidates this arm reads are ALREADY-VERIFIED hits supplied by the
caller, not an unverified proposal; say so in a comment so nobody later feeds it a raw search
response.

Write all seven tests in `tests/n8n/matchProposal.test.mjs` first and record the RED output.
  </action>
  <acceptance_criteria>
    - `laneOf` returns `mobilephone` only when there is no email and no linkedin_url, and returns it in preference to `name`.
    - `/usr/bin/grep -v '^ *//' n8n/code/matchProposal.js | /usr/bin/grep -c 'identityKeys.phone'` returns 0 — `phone` is never read by `laneOf`.
    - `summarizeMatch` has a dedicated `lane === "mobilephone"` arm with three outcomes, not an `||` joined to linkedin.
    - Tests 1 through 7 exist by name in `tests/n8n/matchProposal.test.mjs` and pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/matchProposal.test.mjs tests/n8n/linkedinLaneFlow.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP summary reports a `# fail` count greater than 0</fails_when>
  </verify>
  <done>A mobilephone-keyed row has a lane, matches only on a single hit, and a phone-only row can never match anything.</done>
  <reversibility rating="reversible">A pure JS module plus tests; the graph change is Task 2.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the ingest lane's mobilephone rung — nodes, carry Merge, sentinel, regeneration</name>
  <files>scripts/build_cloud_workflows.py, tests/n8n/mobilephoneLaneFlow.test.mjs, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py — the enrichment lane's `IF Linkedin Searchable`, `HubSpot Linkedin Search`, `Adapt Linkedin Search` subgraph and its carry-Merge splice (search those three node names), `ENRICH_ADAPT_LINKEDIN_SEARCH`
    - scripts/build_cloud_workflows.py `build_cloud()` — the ingest match chain around `HubSpot Search by Email`, `Adapt Search Results`, `Resolve Identity`, `HubSpot Contact History` and `Merge Contacts`, and the `splice_carry_merge_after` helper with its `merge_name` argument
    - scripts/build_cloud_workflows.py — search for `Sentinel Gate` and read two existing examples in full, including how a gated sentinel is wired so it delivers a marker only when its own lane is genuinely absent (D-70-23)
    - scripts/build_cloud_workflows.py `assert_merge_input_contract`, `assert_no_by_name_reads`, `assert_no_self_dispatch`, `assert_execution_order_v1`, `_assert_generation_contracts`
    - tests/n8n/lib/walkWorkflow.mjs and tests/n8n/ingestCarryMerge.test.mjs — the walker API and an existing ingest-lane flow test to copy
    - CLAUDE.md section 13.0.3 — the v1 Merge rules: a Merge whose input never receives a delivery never fires and its downstream terminates silently with status `success`; a node that ran and emitted zero items is NOT a delivery under v1
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-16a, D-16a-i, D-16d
  </read_first>
  <behavior>
    - Test 1 (walker, cloud): a batch of one row carrying only firstname, lastname and mobilephone walks from the webhook to a decided outcome with zero stalled nodes and zero Merges that fired with an unfilled input.
    - Test 2 (walker): a batch mixing an email row, a linkedin row, a mobilephone row and a row with none of them walks clean and every row reaches `Build Ingest Response`.
    - Test 3: a mobilephone search returning exactly one hit produces an `update` action for that row.
    - Test 4: a mobilephone search returning two hits produces a `review` action naming multiple matches, and no update.
    - Test 5: a mobilephone search returning zero hits falls through to `create`.
    - Test 6: a row carrying a `phone` and no mobilephone is never routed into the mobilephone search at all — it goes to `create` and the phone value is carried onto the created contact.
    - Test 7: the decided row names which identity key resolved it, and that value reaches `Build Ingest Response` (D-16d).
    - Test 8: the generated `wf_contact_ingest_cloud.json` passes every generation contract, contains zero `executeWorkflow` dispatch nodes, and every Merge in it has at most 10 inputs.
  </behavior>
  <action>
Add the mobilephone rung to `build_cloud()`'s match chain in `scripts/build_cloud_workflows.py`,
copying the enrichment lane's LinkedIn-rung idiom in FOUR parts, in this order after the existing
email search and the linkedin rung plan 05's identity widening makes reachable:

One, an `IF Mobilephone Searchable` node gating on the row carrying a usable mobilephone and no
higher-priority key — the same predicate shape `IF Linkedin Searchable` uses.

Two, a `HubSpot Mobilephone Search` HTTP node posting to the contacts search endpoint with a
single `filterGroups` filter, `propertyName` `mobilephone`, operator `EQ`. This property is
EQ-searchable on this portal, verified live 2026-09-18 by a read-only probe (RESEARCH.md Open
Question 2, resolved) — mark that fact `[observed live]` in the node comment and mark the MATCH
QUALITY question, whether a real number normalises identically on both sides, as still open and
`[documented]` only.

Three, an `Adapt Mobilephone Search` Code node turning the response into the already-verified
candidate array `summarizeMatch`'s mobilephone arm expects, applying the D-16a-i cardinality rule
by handing the arm the candidate list and letting it decide — never deciding the 0/1/many
question a second time in the adapter.

Four, a carry Merge spliced immediately after the search node using `splice_carry_merge_after`
with an explicit `merge_name`, so no downstream node ever does a by-name read of the search node
— `assert_no_by_name_reads` fails generation otherwise.

Every branch of this rung that can legitimately emit zero items needs its own gated starved-lane
sentinel, per the Phase 70 idiom: the lane is skipped entirely for a row with an email, and the
search legitimately returns zero hits. Under v1 a Merge whose input never receives any delivery
never fires and its downstream terminates silently reporting success, and a node that ran and
emitted zero items is not a delivery — so a missing sentinel here silently drops the row from the
response rather than failing. Use a GATED sentinel (D-70-23), not an unconditional one, so the
marker is delivered only when its lane is genuinely absent and the lane never double-fires.

Keep the association rule intact: ONE verdict is taken at `HubSpot Update Write Gate` and covers
both the update and the association it implies (D-70-15), and `Associate Lane Sentinel`
duplicates that gate's predicate for Merge plumbing — do not add a second gate for the new rung,
and do not let an update take a different verdict because it arrived by a different rung.

Carry the resolving identity key onto the decided row so `Build Ingest Response` can report it
(D-16d) — one field naming the rung that resolved the row, using the same lane vocabulary
`laneOf` returns so the two never diverge.

Mirror the change into `build_local()` where the local ingest replica has the corresponding
chain, so the two do not diverge; if the local replica has no equivalent chain, say so explicitly
in the SUMMARY rather than inventing one.

Write `tests/n8n/mobilephoneLaneFlow.test.mjs` with the eight behaviours first, using
`tests/n8n/lib/walkWorkflow.mjs` and copying `tests/n8n/ingestCarryMerge.test.mjs`'s setup.
Record the RED output. Then regenerate with `.venv/bin/python scripts/build_cloud_workflows.py`.
Never hand-edit any `n8n/wf_*.json`.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'IF Mobilephone Searchable' n8n/wf_contact_ingest_cloud.json` returns at least 1.
    - `/usr/bin/grep -c 'HubSpot Mobilephone Search' n8n/wf_contact_ingest_cloud.json` returns at least 1.
    - A carry Merge exists whose producer is `HubSpot Mobilephone Search`, and no node's jsCode contains a by-name read of that search node.
    - Every Merge in the regenerated `wf_contact_ingest_cloud.json` declares at most 10 inputs.
    - `.venv/bin/python -c "import json;d=json.load(open('n8n/wf_contact_ingest_cloud.json'));print(sum(1 for n in d['nodes'] if n['type']=='n8n-nodes-base.executeWorkflow'))"` prints `0`.
    - `.venv/bin/python -c "import json;print(json.load(open('n8n/wf_contact_ingest_cloud.json'))['settings']['executionOrder'])"` prints `v1`.
    - The node count of `n8n/wf_contact_ingest_cloud.json` before and after is recorded in the SUMMARY.
    - Tests 1 through 8 exist in `tests/n8n/mobilephoneLaneFlow.test.mjs` and pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/mobilephoneLaneFlow.test.mjs tests/n8n/ingestCarryMerge.test.mjs tests/n8n/ingestMixedBatch.test.mjs tests/n8n/mergeInputContract.test.mjs tests/n8n/executionOrderV1.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP summary reports a `# fail` count greater than 0</fails_when>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -c "import subprocess,sys; out=subprocess.run(['git','status','--porcelain'],capture_output=True,text=True,check=True).stdout; stray=[l for l in out.splitlines() if l[:2].strip()=='M' and 'n8n/wf_' not in l]; print('STRAY',stray); sys.exit(1 if stray else 0)"</automated>
    <fails_when>non-zero exit, or the printed `STRAY` list is not empty</fails_when>
  </verify>
  <done>An emailless mobilephone-keyed row is matched when exactly one contact holds that number, held for review when two do, created when none does, and a landline never matches anything.</done>
  <reversibility rating="costly">New search hops, a carry Merge and sentinels on the ingest graph under the Phase 70 idiom; reverting means regenerating and redeploying the lane, and the live instance would be a generation behind until the operator does.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the backend proves its own portal</name>
  <files>scripts/build_cloud_workflows.py, operator-claude-plugin/scripts/backend_status.py, operator-claude-plugin/scripts/config_gate.py, operator-claude-plugin/config/operator.local.example.json, operator-claude-plugin/skills/initialize/SKILL.md, operator-claude-plugin/skills/backend-status/SKILL.md, tests/n8n/backendStatusResponse.test.mjs, operator-claude-plugin/tests/test_config_gate.py, n8n/wf_backend_status_cloud.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py `build_backend_status_cloud()` in full — the straight-line chain, its deliberate non-fan-out rationale, `ENRICH_STATUS_CREDIT_REQUEST`, `ENRICH_STATUS_BUILD_RESPONSE`, the four HubSpot count searches, `Build Status` and `Respond to Webhook`, and the `onError: continueRegularOutput` discipline every probe node uses
    - operator-claude-plugin/scripts/backend_status.py in full (60 lines) — `STATUS_PATH`, `status_target`, `_unavailable`, `fetch_backend_status` and how the response body is parsed into `{available, reason, data}`
    - operator-claude-plugin/scripts/config_gate.py — the existing `hubspot_portal_id` read and what it is currently used for (a clickable link, not a guard), and the module's refusal vocabulary
    - operator-claude-plugin/config/operator.local.example.json — the existing `hubspot_portal_id` key and its comment
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md Assumption A4 (RESOLVED — `GET /account-info/v3/details` returns 200 with `portalId: 22617666`) and Pattern 4's caveat that the local `.env` token and the n8n-side credential are DIFFERENT app registrations
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-14c and D-14a
  </read_first>
  <behavior>
    - Test 1: the backend-status response body carries a `portalId` field.
    - Test 2: the account-info probe node uses `onError: continueRegularOutput` like every other probe on that chain, so a failing probe passes an error item along the same single chain instead of stopping it, and the responder still fires.
    - Test 3: when the probe fails, `portalId` is present and null-valued, never absent — an absent key and an unknown portal must not read the same.
    - Test 4: `config_gate`'s comparison returns a not-checked verdict when `hubspot_portal_id` is unset — the guard runs only when the operator set it.
    - Test 5: matching ids return a matched verdict; differing ids return a mismatch verdict naming BOTH ids.
    - Test 6: a null backend `portalId` with a set `hubspot_portal_id` returns an unknown verdict, never a match and never a mismatch.
    - Test 7 (walker): the regenerated `wf_backend_status_cloud.json` walks clean under v1 with the new node in the chain, and its Merge input contract holds.
  </behavior>
  <action>
Add one HubSpot read to `build_backend_status_cloud()`'s straight-line chain: a `HubSpot Account
Info` HTTP node issuing `GET /account-info/v3/details` with the same bound HubSpot credential the
lane's existing count searches use. That endpoint and its `portalId` field are verified — a live
`GET` returned 200 with `portalId: 22617666` on 2026-09-18 — so mark the node's comment
`[observed live]` for the endpoint and schema, and mark `[documented]` only the claim that the
n8n-side credential (a different app registration from the local token that ran the probe) has
`account-info` scope. That scope question is settled by the operator's disarmed live proof in the
final plan, not here.

Place the node on the existing chain, never as a fan-out — the lane's own docstring explains that
a single chain is what guarantees exactly one item reaches the responder with every probe already
run. Give it `onError: continueRegularOutput` and have it read nothing off its incoming item,
exactly like every other probe on this chain, so a 403 on the scope passes an error item along
rather than stopping the status read.

Extend `ENRICH_STATUS_BUILD_RESPONSE` (or the `Build Status` node that composes the final body,
whichever composes the outermost object) to carry `portalId` as a top-level field: the id when the
probe succeeded, and `null` when it did not. Never omit the key — an absent key and an unknown
portal must not read the same to the plugin.

In `operator-claude-plugin/scripts/backend_status.py`, surface `portalId` through the existing
`{available, reason, data}` shape without changing that shape. Keep the never-echo-exception-text
rule intact.

In `operator-claude-plugin/scripts/config_gate.py`, add a pure comparison returning one of three
verdicts — not-checked when `hubspot_portal_id` is unset, unknown when the backend returned a
null `portalId`, and matched or mismatched otherwise — with the mismatch naming BOTH ids. Keep
`hubspot_portal_id` OPTIONAL in `operator.local.json` per D-14c, and update
`operator.local.example.json`'s comment for that key to say it is optional and that setting it
turns on a portal guard against the backend's own reported portal.

Add the comparison's result to `initialize`'s setup report and `backend-status`'s status output as
one line each. Both must state what the check does and does not prove: it proves the N8N
CREDENTIAL's portal only. An outside HubSpot tool still proves ITSELF by its own lookup (D-14a)
and must never be allowed to trust this answer — say that in both skills' text, alongside the
shared CRM-access block plan 04 already put there.

Write the seven tests first and record the RED output. Regenerate at the end of the task.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'account-info/v3/details' n8n/wf_backend_status_cloud.json` returns at least 1.
    - `/usr/bin/grep -c 'portalId' n8n/wf_backend_status_cloud.json` returns at least 1.
    - The account-info node's `onError` is `continueRegularOutput`.
    - `config_gate`'s comparison returns three distinct verdicts and the mismatch message contains both ids.
    - `operator.local.example.json`'s `hubspot_portal_id` comment states the key is optional.
    - Both `initialize/SKILL.md` and `backend-status/SKILL.md` state that the check proves the n8n credential's portal only.
    - Tests 1 through 7 exist by name and pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/n8n/backendStatusResponse.test.mjs tests/n8n/backendStatus.test.mjs tests/n8n/backendStatusCredits.test.mjs tests/n8n/creditsSummaryUnderV1.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP summary reports a `# fail` count greater than 0</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_config_gate.py operator-claude-plugin/tests/test_status_all_workflows.py operator-claude-plugin/tests/test_status_skill.py operator-claude-plugin/tests/test_status_unknown.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/ && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears, or the TAP summary reports a `# fail` count greater than 0</fails_when>
  </verify>
  <done>The backend reports which portal its own credential is on, the plugin compares it when the operator set one, and neither pretends to prove an outside tool's portal.</done>
  <reversibility rating="costly">A node added to a deployed workflow's chain; reverting needs a regeneration and a redeploy, and the live instance is a generation behind until the operator runs it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a CSV or provider-supplied phone number -> a HubSpot contact search filter | An attacker-or-error-supplied number decides which existing contact gets updated. |
| n8n HubSpot credential -> a portal | The credential decides whose CRM the lane writes to; nothing in the graph currently states which portal that is. |
| new Merge inputs -> the response | Under v1, a starved Merge silently drops rows while reporting success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-19 | Tampering | A shared landline matching many contacts, updating the wrong person | high | mitigate | D-16a-i: `phone` is never a match rung; `laneOf` reads no `phone` key and Task 1 Test 3 pins it. |
| T-73.1-20 | Tampering | A mobilephone shared by two contacts silently updating one of them | high | mitigate | The single-hit rule: more than one hit routes to `review` and never updates; Task 1 Test 5 and Task 2 Test 4 pin it. |
| T-73.1-21 | Spoofing | The n8n credential bound to a different portal than the operator believes | high | mitigate | D-14c's live account read returning `portalId`, compared by `config_gate` when the operator set one. Both skills state it proves the n8n credential only. |
| T-73.1-22 | Denial of Service | A starved Merge on the new rung silently dropping a row while the execution reports success | high | mitigate | A gated starved-lane sentinel on every branch that can legitimately emit zero items; Task 2 Tests 1, 2 and 8 walk the graph under v1. |
| T-73.1-23 | Elevation of Privilege | A new dispatch node re-introducing the 2026-09-10 self-dispatch runaway | critical | mitigate | `assert_no_self_dispatch` runs at generation time; the acceptance criteria assert zero `executeWorkflow` nodes in the regenerated ingest JSON. |
| T-73.1-24 | Information Disclosure | A frozen runData fixture carrying `x-enrichment-secret` or a discovered person's number | high | mitigate | No fixture is frozen from a live execution in this plan; all tests are offline and synthetic. The live proof in the final plan carries the redaction rule. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 06)

- n8n nodes: `IF Mobilephone Searchable`, `HubSpot Mobilephone Search`, `Adapt Mobilephone Search`, their carry Merge and gated starved-lane sentinel (ingest lane)
- n8n node: `HubSpot Account Info` (backend-status lane) and the `portalId` field on that lane's response
- `matchProposal.js` `laneOf` mobilephone rung; `summarizeMatch` mobilephone arm
- `tests/n8n/mobilephoneLaneFlow.test.mjs` — new walker test file
- `config_gate` portal comparison with three verdicts (not-checked, unknown, matched/mismatched)
- `operator.local.example.json` `hubspot_portal_id` comment stating the key is optional and gate-enabling
</artifacts_produced>

<verification>
- Both suites green; the builder is idempotent.
- The regenerated ingest and backend-status JSON carry `settings.executionOrder: "v1"` and zero `executeWorkflow` nodes.
- Node counts before and after are recorded in the SUMMARY for the operator's deploy step.
</verification>

<success_criteria>
- A mobilephone-keyed row matches on exactly one hit, reviews on more, creates on none.
- A landline never matches.
- The backend reports its own portal and the plugin compares it when configured.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-06-SUMMARY.md` when done.
</output>
