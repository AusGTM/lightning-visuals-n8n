---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 03
subsystem: contact-discovery
tags: [python, pytest, tdd, cost-guard, pricing-disclosure]

# Dependency graph
requires:
  - phase: 62-suggest-the-contacts-nobody-named
    provides: cost_guard.py's tri-state (no_rows/unmeasured/measured) disclosure
      convention (research_line, suggestion_line) that this plan's discovery_line
      follows rather than reinventing
provides:
  - "cost_rates.json gains three honestly-null per-provider search rates
    (zoominfo_contact_search, apollo_people_search, lusha_prospecting_search),
    each citing the D-12 plan-time probe and forbidding a scaled-from-another-rate
    guess"
  - "cost_guard.DISCOVERY_SEARCH_RATE_KEYS -- ordered provider->rate-key map
    mirroring the D-11 waterfall order (zoominfo, apollo, lusha)"
  - "cost_guard.discovery_line(gap_company_count, per_company_cap, rates) -- the
    D-13a disclosure builder: gap-company count, each provider's rate or
    \"unknown\", the one n8n execution, and the stage-2 reveal ceiling"
affects: ["73.1-08", "73.1-09"]

# Actuals (#2632)
actuals:
  tokens: 3341
  tasks: 2
  commits: 3
  plan_head_before: 0dd6f0874d5821f02956db35f526aef37220b813

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "discovery_line follows the exact tri-state branch order research_line and
      suggestion_line already established (no_rows checked before any rate is
      consulted; a null rate renders 'unknown', never $0) rather than inventing a
      fourth vocabulary for the same idea."
    - "Per-provider dicts carry their own rate/state/citation alongside a fragment
      string, so a caller can render the whole line OR inspect one provider's
      state without re-parsing the sentence."

key-files:
  created: []
  modified:
    - operator-claude-plugin/config/cost_rates.json
    - operator-claude-plugin/scripts/cost_guard.py
    - operator-claude-plugin/tests/test_cost_guard.py

key-decisions:
  - "Apollo's citation records unreadability from two angles rather than one: its own
    apollo_per_match citation (usage endpoint returns rate limits, not a depleting
    credit pool) AND the separate provider credit-check endpoint's 403 for this
    account's non-master key (memory: provider-credit-check-endpoints.md) -- so the
    citation is grounded in two independently-recorded facts, not a restatement of
    the same one."
  - "The disclosure sentence uses 'not yet measured' rather than a second 'unknown'
    for the D-13 override clause, so the word 'unknown' appears exactly once per
    unknown-rate provider (three times with the shipped table) and never picks up a
    fourth, unrelated occurrence that would break Test 3's exact count."
  - "Per-provider search-rate arithmetic renders with %g formatting (e.g. '6 × 1 = 6
    credits') rather than fixed 2-decimal formatting, because a probed rate is as
    likely to be a clean integer as a decimal and the existing research_line/
    suggestion_line convention already uses the format that fits the number, not a
    fixed one."
  - "Whole-round state is binary (measured only when EVERY provider's rate is known,
    else unmeasured) rather than suggestion_line's three-way measured/partial/
    unmeasured, per the plan's own explicit instruction: 'a round with one known and
    two unknown rates is unmeasured, because the total genuinely is.'"

requirements-completed: [D-13, D-13a]

coverage:
  - id: D1
    description: "cost_rates.json gains zoominfo_contact_search, apollo_people_search
      and lusha_prospecting_search, all value: null, each citation naming the D-12
      probe (scripts/probe_provider_discovery.py) and forbidding a scaled guess;
      Apollo's citation additionally records its balance is unreadable on this
      account. measured_on is unchanged (no figure was actually measured)."
    requirement: D-13
    verification:
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py -- full suite green
          (no test asserts the new rate entries directly, but load_rates()'s schema
          check and every existing cost_guard test still pass against the widened
          rates table)"
        status: pass
      - kind: other
        ref: "acceptance-criteria commands from 73.1-03-PLAN.md Task 1 (python
          value-is-None check, four-key shape check, probe-script + 'do not'
          citation grep, DISCOVERY_SEARCH_RATE_KEYS order check, measured_on grep)"
        status: pass
    human_judgment: false
  - id: D2
    description: "cost_guard.discovery_line(gap_company_count, per_company_cap,
      rates) prices a discovery round: no_rows before any rate is consulted, a
      provider list with per-provider rate/state/citation, exactly one n8n
      execution, and a stage-2 reveal ceiling computed from
      SUGGESTION_STAGE2_RATE_KEY -- never a second literal for the same spend. The
      rendered line names the gap count, each provider's rate or 'unknown', the
      one execution, and the ceiling, and never renders $0 or a bare 'tier'."
    requirement: D-13a
    verification:
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_zero_gap_companies_yields_no_rows"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_all_rates_unknown_is_the_shipped_state"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_never_renders_zero_and_names_unknown_per_provider"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_names_gap_count_one_execution_and_stage2_ceiling"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_a_known_rate_renders_arithmetically"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_stage2_ceiling_follows_the_stage2_rate_key"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_cost_guard.py::test_discovery_line_never_mentions_tier"
        status: pass
    human_judgment: false

# Metrics
duration: 15min
completed: 2026-09-18
status: complete
---

# Phase 73.1 Plan 03: Discovery Round Pricing Summary

**Three honestly-null per-provider search rates plus `cost_guard.discovery_line()`, a D-13a disclosure builder that shows gap-company count, per-provider rate or "unknown", one n8n execution, and the stage-2 reveal ceiling — never a $0.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-18T11:33:00+10:00
- **Completed:** 2026-09-18T11:35:26+10:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `cost_rates.json` carries three new search-rate keys, all `value: null`, each citation naming the D-12 plan-time probe (`scripts/probe_provider_discovery.py`) and explicitly forbidding a guess scaled from another rate; Apollo's citation additionally records why its balance is unreadable on this account (rate-limit-only usage endpoint plus a 403 on the separate credit-check endpoint for this account's non-master key).
- `cost_guard.DISCOVERY_SEARCH_RATE_KEYS` maps each of the three providers to its rate key in the D-11 waterfall's own order (zoominfo, apollo, lusha).
- `cost_guard.discovery_line()` prices a discovery round with the same tri-state discipline `research_line`/`suggestion_line` already established: zero gap companies short-circuits to `no_rows` before any rate is read; each provider renders its rate or the word "unknown" (never `$0` or `0 credits`); the round is always exactly one n8n execution (D-08); the stage-2 reveal ceiling reuses `SUGGESTION_STAGE2_RATE_KEY` (the same contacts rate `suggestion_line` already uses).
- Seven tests, written first and confirmed RED (`AttributeError: module 'cost_guard' has no attribute 'discovery_line'`), then GREEN after implementation. Full plugin suite green: 3143 passed, 5 skipped, no regressions.

## Task Commits

Each task was committed atomically:

1. **Task 1: three honestly-unknown search rates** - `570b97ca` (feat)
2. **Task 2: discovery_line — RED** - `f4aa7373` (test)
2. **Task 2: discovery_line — GREEN** - `61894f6c` (feat)

_TDD task (Task 2): RED then GREEN. No REFACTOR commit — the first working implementation needed only a one-word wording fix (caught by Test 3 itself before commit), not a structural cleanup._

## Files Created/Modified
- `operator-claude-plugin/config/cost_rates.json` - three new null-valued search-rate entries
- `operator-claude-plugin/scripts/cost_guard.py` - `DISCOVERY_SEARCH_RATE_KEYS` constant and `discovery_line()` function
- `operator-claude-plugin/tests/test_cost_guard.py` - seven new tests for `discovery_line`

## Decisions Made
- Apollo's unreadability citation cites two independent facts (its own `apollo_per_match` note about the usage endpoint, plus the separate credit-check endpoint's 403 for this account's non-master key) rather than one restated fact.
- The D-13 override sentence in the rendered line uses "not yet measured" instead of a second "unknown", so the word "unknown" appears exactly once per unknown-rate provider (Test 3's exact-count assertion).
- Per-provider arithmetic renders with `%g` formatting ("6 × 1 = 6 credits") to match whatever precision a future probed rate actually has, rather than a fixed 2-decimal format.
- Whole-round `state` is binary (`measured` only when every provider's rate is known, else `unmeasured`) — no `partial` state, per the plan's explicit instruction, unlike `suggestion_line`'s three-way state.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Initial `discovery_line()` implementation used the word "unknown" in its D-13 disclosure sentence in addition to the three per-provider "unknown" occurrences, causing Test 3's exact-count assertion (`line.count("unknown") == 3`) to see 4. Fixed by rewording that sentence to "not yet measured" before committing GREEN — caught by the test itself, not a silent gap.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `discovery_line()` is a pure function with no call site yet — plan 08 wires it into the skill's disclosure step, exactly as its `<reversibility>` note anticipated.
- The three search rates stay `null` until the D-12 plan-time probe (`scripts/probe_provider_discovery.py`, not yet written) measures them and the operator rules on the reveal-in-lane question.
- No blockers for 73.1-08/09.

## Self-Check: PASSED

All key files found on disk (cost_rates.json, cost_guard.py, test_cost_guard.py). All
three task commits (570b97ca, f4aa7373, 61894f6c) found in git log. Full plugin suite
green (3143 passed, 5 skipped) at HEAD.

---
*Phase: 73.1-provider-backed-contact-discovery-as-source-tier-2*
*Completed: 2026-09-18*
