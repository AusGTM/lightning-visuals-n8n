---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 09
type: execute
wave: 6
depends_on: ["73.1-08"]
autonomous: false
files_modified:
  - scripts/probe_provider_discovery.py
  - tests/test_probe_provider_discovery.py
  - operator-claude-plugin/config/cost_rates.json
  - n8n/code/discoverySearch.js
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json
  - scripts/bounce_n8n_workflows.py
  - CLAUDE.md
requirements: [D-12, D-13, D-11b, D-06, D-14c]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-12: the operator rules on whether the discovery lane stays SEARCH-ONLY or may reveal in-lane, on the evidence of a plan-time live probe — one search call per provider against a known company, with a provider balance read before and after; Apollo's balance is unreadable (403) so its search is priced from vendor documentation only."
    - "D-12: the probe SPENDS real provider credit and is therefore the operator's step, run once, deliberately, with the result recorded; authoring the script is offline work and happens before the operator sits down."
    - "D-13: whatever the probe measures is written back into `cost_rates.json` as a dated, cited figure — and a rate the probe could not measure stays `null`, never a guess."
    - "D-06: the sixth workflow's first deploy is a CREATE, not a PUT — it has no live id until the operator creates it — and the id is then recorded in `scripts/bounce_n8n_workflows.py`'s map before any bounce."
    - "D-11b: the end-of-phase live sample measures the provider filter's hit against the classifier's verdict on a small set, so the operator can rule afterwards on whether to trust the provider's title match; nothing about that is decided in this phase."
    - "D-14c: the n8n-side credential's `account-info` scope is confirmed by the live disarmed read, upgrading the backend portal proof from [documented] to [observed live]."
    - "every live-behaviour claim this phase makes is tagged [documented] or [observed live] per CLAUDE.md section 13.0.3, and nothing is armed — no write flag is flipped anywhere in this plan."
  artifacts:
    - "scripts/probe_provider_discovery.py — the one-shot, credit-spending D-12 probe"
    - ".planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json — the recorded probe result and the operator's ruling"
    - ".planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md — the live disarmed proof record"
    - "the sixth workflow's live id recorded in scripts/bounce_n8n_workflows.py"
  key_links:
    - "the probe's measured rates -> cost_rates.json -> cost_guard.discovery_line: what the operator is shown next round is exactly what the probe measured, or still unknown."
    - "the probe's endpoint findings -> n8n/code/discoverySearch.js: every [ASSUMED] request body is isolated in that one module so the correction is a file, not a graph rewrite."
---

<objective>
Close the one question this phase could not answer offline: which provider search endpoints
actually exist on this account and what a search costs (D-12), and prove the new lane on the real
engine, disarmed.

Purpose: five of the phase's assumptions trace to the same gap — no provider search-by-role
endpoint has ever been called in this repository. The probe resolves them all and the operator
rules on the result. The live proof is the only place this phase's Merge and sentinel claims stop
being `[documented]`.

Output: a probe script that exists before the operator sits down, a recorded verdict, corrected
rates and endpoints, the sixth workflow live and disarmed with its id recorded, and a UAT record.

This plan is `autonomous: false`. Tasks 2 and 3 are `blocking-human` and are deliberately the LAST
wave so the eight offline plans before them run uninterrupted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-SUMMARY.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-08-SUMMARY.md
@docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: author the probe offline — it spends nothing until the operator runs it</name>
  <files>scripts/probe_provider_discovery.py, tests/test_probe_provider_discovery.py</files>
  <read_first>
    - scripts/probe_scoring_tool_availability.py lines 1-90 — the in-process dotenv idiom and the one-shot probe script shape this repo already uses
    - scripts/check_provider_credits.py — how a provider balance is read, and the auth quirks per provider
    - scripts/provider_registry.py — each provider's `credit` entry: Lusha `GET /v3/account/usage` with the `api_key` header reading `credits.remaining`; ZoomInfo `GET /gtm/data/v1/users/usage`, which needs `Accept: application/vnd.api+json` or returns 406; Apollo `POST /api/v1/usage_stats/api_usage_stats`, which returns rate limits and not a credit pool
    - n8n/code/discoverySearch.js — the three `[ASSUMED]` endpoint constants and request shapes this probe tests
    - memory `provider-credit-check-endpoints` and memory `lusha-v3-measured-pricing` — the balance-read quirks and the fact that a Lusha flat-rate assumption was already contradicted once by a real 7-credit reveal
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-12, D-13 and the Claude's Discretion note on provider search endpoints
  </read_first>
  <behavior>
    - Test 1: with a stub transport, the probe reads each provider's balance, issues exactly ONE search call per provider, then reads each balance again — three searches total, never more.
    - Test 2: the probe refuses to run at all unless an explicit opt-in environment variable is set, so an accidental invocation cannot spend credit.
    - Test 3: the probe never prints a provider key, an Authorization header or a full request object — only the endpoint, the HTTP status, the balance before and after, and the delta.
    - Test 4: a provider whose balance is unreadable — Apollo, 403 — is reported as unreadable with its documented price cited, never as a zero delta.
    - Test 5: a provider whose search endpoint returns 404 or 400 is reported as endpoint-absent with the status and the response's own message, and that provider's rate stays `null`.
    - Test 6: the probe writes a machine-readable verdict document carrying, per provider, the endpoint tried, the status, the balance before and after, the delta, whether any person was returned, and whether the response carried reveal fields such as an email or a phone.
    - Test 7: no test in this file makes a network call — the autouse `no_network` fixture holds.
  </behavior>
  <action>
Write `scripts/probe_provider_discovery.py` as a one-shot, operator-run probe. It is authored here
and RUN in Task 2; this task spends nothing.

For each provider in the D-11 order, it reads the balance through that provider's known credit
endpoint, issues exactly ONE search call against `n8n/code/discoverySearch.js`'s `[ASSUMED]`
endpoint for a known company (a company the operator names, defaulting to one with a real domain
and staff the round already knows about), then reads the balance again. Three searches total,
never more — the probe must not loop, retry or paginate.

Require an explicit opt-in environment variable before it will run. Every other guard in this repo
is structural, and a script whose only protection is that nobody types its name is not guarded.

Never print a key, a header or a whole request or response object. Print the endpoint, the status,
the balance before, the balance after, the delta, whether any person came back, and whether the
response carried reveal fields. A raw provider error object can carry the outbound request's own
Authorization header, which is why the ingest lane already reads at most a message, description
and status code off one — follow the same rule here.

Handle the three providers' known quirks explicitly rather than generically: ZoomInfo needs
`Accept: application/vnd.api+json` or answers 406; Apollo's usage endpoint returns rate limits and
not a depleting credit pool, so its balance is UNREADABLE and its search can only be priced from
vendor documentation, which the probe must report as unreadable and never as a zero delta; Lusha's
pricing has already been contradicted once by a real execution, so the probe reports what it
measured and nothing inferred.

Write the verdict to
`.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json`
carrying, per provider: the endpoint tried, the HTTP status, balance before and after, the delta,
whether people were returned, whether reveal fields were present, and a free-text note. Leave a
top-level `operator_ruling` field null for Task 2 to fill.

Write the seven tests in `tests/test_probe_provider_discovery.py` first with a stub transport and
record the RED output.
  </action>
  <acceptance_criteria>
    - `scripts/probe_provider_discovery.py` exists and refuses to run without its opt-in environment variable.
    - It issues at most one search call per provider, proved by a stub-transport call count.
    - `/usr/bin/grep -c 'Authorization\|api_key' scripts/probe_provider_discovery.py` counts only header CONSTRUCTION sites, and no print or log statement takes a header or a raw response object as an argument.
    - The verdict document's shape includes, per provider, the endpoint, status, balance before, balance after, delta, people-returned flag and reveal-fields flag, plus a top-level `operator_ruling` key initialised to null.
    - Tests 1 through 7 exist by name and pass with no network access.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_probe_provider_discovery.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -c "import ast,sys; ast.parse(open('scripts/probe_provider_discovery.py').read()); print('PARSE_OK')"</automated>
    <fails_when>the output is not exactly `PARSE_OK`, or the command exits non-zero</fails_when>
  </verify>
  <done>The probe exists, is guarded, spends nothing until deliberately run, and cannot print a secret.</done>
  <reversibility rating="reversible">A standalone script and its offline test.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: run the D-12 probe and take the operator's ruling</name>
  <files>.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json, operator-claude-plugin/config/cost_rates.json, n8n/code/discoverySearch.js</files>
  <read_first>
    - scripts/probe_provider_discovery.py — the script written in Task 1, including its opt-in variable and what it prints
    - operator-claude-plugin/config/cost_rates.json — the three `null` search-rate entries and the top-level note that `null` means genuinely unknown and `0` means a real measured zero
    - n8n/code/discoverySearch.js — the three `[ASSUMED]` endpoint constants that this ruling either confirms or corrects
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md Assumptions Log A1, A2, A3, A5 — the five assumptions this probe resolves
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-12
  </read_first>
  <what-built>`scripts/probe_provider_discovery.py` (plan 09 Task 1) — a one-shot, opt-in-guarded probe that reads each provider's balance, issues exactly ONE search call per provider against the `[ASSUMED]` endpoints in `n8n/code/discoverySearch.js`, reads each balance again, and writes a machine-readable verdict. It has not been run; it spends nothing until it is.</what-built>
  <resume-signal>Type the ruling — `search-only` or `reveal-in-lane` — or describe what the probe result changes. Nothing proceeds until the ruling is recorded in `73.1-D12-VERDICT.json`.</resume-signal>
  <how-to-verify>
**This task spends real provider credit. It is the operator's step.**

Present to the operator, before anything runs: that the probe issues exactly three search calls,
one per provider, against a company they name; that it reads each provider's balance before and
after; that Apollo's balance is unreadable on this account so its cost can only come from vendor
documentation; and that the whole point is to answer whether the discovery lane stays search-only
or may reveal in-lane, which is a decision nobody in this repo has authority to take for them.

Run `scripts/probe_provider_discovery.py` with its opt-in variable set. Read the verdict document
back and present it plainly, per provider: did the endpoint exist, what did it cost, did it return
people without revealing their contact details.

Ask the operator to rule on D-12: search-only, or reveal in-lane. Record the answer verbatim in
the verdict document's `operator_ruling` field along with the date.

Then apply the ruling's consequences, and only those:

If a provider's search rate was measured, write it into `operator-claude-plugin/config/cost_rates.json`
as a real value with the date, the execution or probe reference, and a citation naming what was
measured. A rate the probe could not measure stays `null` — a figure taken from vendor
documentation is a documentation figure and must be labelled as one, never as measured.

If an endpoint differed from `discoverySearch.js`'s `[ASSUMED]` constant, correct that module and
re-run its offline tests and the walker test. If an endpoint does NOT exist on this account, drop
that provider from the waterfall rather than leaving a node that will 404 every round, and record
the drop.

If the operator rules REVEAL IN-LANE, do NOT implement it in this phase. It is a scope change to a
lane whose whole read-only guarantee is asserted at generation time by `assert_no_write_nodes` and
whose cost model was disclosed to the operator as search-only. Record the ruling, open a triaged
`kind: design` todo naming the decision, and say plainly that the next phase implements it.

Nothing is armed by this task. No write flag is flipped.
  </how-to-verify>
  <acceptance_criteria>
    - `73.1-D12-VERDICT.json` exists, has a non-null `operator_ruling` with a date, and carries a per-provider result for all three providers.
    - Any rate written into `cost_rates.json` carries a date and a citation naming the probe, and any rate not measured is still `null`.
    - No rate taken from vendor documentation is labelled `measured`.
    - Any corrected endpoint in `discoverySearch.js` is accompanied by a green offline test run.
    - If the ruling is reveal-in-lane, a triaged `kind: design` todo exists and no reveal code was written in this phase.
    - No `ALLOW_*` write flag was changed anywhere.
  </acceptance_criteria>
  <verify>
    <human-check>The operator confirms: they saw the three search calls priced before they ran; the verdict document matches what they were shown; their ruling is recorded verbatim; and any rate now in `cost_rates.json` is one the probe actually measured, with anything unmeasured still reading unknown.</human-check>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_cost_guard.py && node --test tests/n8n/discoverySearch.test.mjs tests/n8n/suggestDiscoveryLane.test.mjs</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears, or the TAP summary reports a `# fail` count greater than 0</fails_when>
  </verify>
  <done>The operator has ruled on D-12 on real evidence, and every rate the round will disclose is either measured or still honestly unknown.</done>
  <precondition>Provider API keys for Lusha, ZoomInfo and Apollo are present in the local environment the probe reads, and the operator is present to rule on the result.</precondition>
  <reversibility rating="one-way">The probe spends credit that cannot be returned, and the operator's D-12 ruling sets the lane's cost model for every later round. No separate `checkpoint:decision` precedes it: this task IS a `gate="blocking-human"` checkpoint, so the human confirms before the door is walked through, and a preceding decision checkpoint would ask the same person the same question twice.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: create the sixth workflow live, disarmed, and prove the lane once</name>
  <files>scripts/bounce_n8n_workflows.py, .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md, CLAUDE.md</files>
  <read_first>
    - scripts/deploy_n8n_workflows.py — `_load_local_workflows` globbing `wf_*_cloud.json`, `compute_workflow_diff` matching on the workflow's top-level `name`, `_create_workflow_live` versus `_update_workflow_live`, `bind_credentials`, `NODE_CREDENTIAL_MAP`, and the `DRY_RUN` / `ALLOW_N8N_DEPLOY` gate in `_writes_allowed`
    - scripts/bounce_n8n_workflows.py — the `WORKFLOWS` file-to-live-id map and the unset sixth entry plan 07 added
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md — the live supervised pattern to follow
    - CLAUDE.md section 13.0.2's deployment-state paragraphs and section 13.0.3's platform-facts table — the tagging discipline and the row format to append to
    - memory `n8n-stored-vs-running-content` — a bare PUT never reloads a running workflow, so a bounce is required after every deploy
    - memory `n8n-rundata-carries-webhook-secret` — the Webhook Trigger's runData embeds `x-enrichment-secret` and the caller IP verbatim; redact the headers object before freezing any fixture, because a token-shape scan misses it
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-SUMMARY.md — the node count the deploy should read back
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-06, D-11b, D-14c
  </read_first>
  <what-built>Six committed cloud workflows, five of them already live and one — `n8n/wf_suggest_discovery_cloud.json` (`LV Suggest Discovery (Cloud template)`, webhook path `hubspot/suggest/discover`) — never deployed, with no live id and an unset placeholder in `scripts/bounce_n8n_workflows.py`'s `WORKFLOWS` map. Every write-safety flag in every committed body reads `false`. The plugin side (`suggest_discovery.py`, the new skill step, the cost line) is committed and released.</what-built>
  <resume-signal>Type `approved` once the six read-backs, the one-execution proof and the two-minute burst watch are all clean — or describe what differed. If any write-safety flag reads anything but `false`, say so and stop.</resume-signal>
  <how-to-verify>
**This task deploys to the live n8n instance. It is the operator's step. Nothing is armed.**

Deploy. The sixth workflow has NO live id, so its first deploy is a CREATE, not a PUT —
`compute_workflow_diff` matches on the top-level `name` and will find no match. Deploy all six
disarmed, then bounce: a bare PUT never reloads a running workflow, so a deploy without a bounce
leaves the live instance running the old content while the read-back reports the new.

Record the created workflow's live id into `scripts/bounce_n8n_workflows.py`'s `WORKFLOWS` map,
replacing the unset placeholder, and re-run the bounce so all six are covered.

Read back and record, for all six: the live node count against the committed count, `active`,
`settings.executionOrder`, and every write-safety flag. Every flag must read `false`. If any reads
otherwise, stop and say so — do not proceed to the proof send.

Prove the lane once, disarmed. Send ONE POST to `hubspot/suggest/discover` carrying a small set of
companies with at least one `gap: true` and one `gap: false`. Read the response body AND the
settled runData. Confirm: every company appears exactly once; `num_associated_contacts` is echoed
for all of them including the non-gap one (D-09); the non-gap company triggered zero provider
calls; no Merge starved and no company's echo row vanished; and the execution count consumed is
exactly one. Watch for a burst of executions for two minutes after the send — the 2026-09-10
runaway produced 135 child executions in six minutes, and although this lane has zero
`executeWorkflow` nodes and a generation-time assertion against them, the mechanism behind that
incident was never isolated.

Confirm D-14c's residual scope question while the instance is up: call the backend-status endpoint
and read `portalId`. A real id confirms the n8n-side credential has `account-info` scope and
upgrades that claim from `[documented]` to `[observed live]`; a null with a 403 means the scope is
not granted and must be recorded as such, not quietly ignored.

Take D-11b's live sample. For the gap companies the lane searched, record for a small sample how
many provider rung-1 hits `role_classify.classify_title` also placed, and how many it dropped.
This is EVIDENCE FOR A LATER RULING, not a change: trusting the provider's title match instead of
the classifier is explicitly not shipped in this phase and the operator rules on it afterwards.

Write `73.1-UAT.md` recording every read-back, the execution ids, the response body shape, the
sample, and the portal read. Tag every claim `[documented]` or `[observed live]` per CLAUDE.md
section 13.0.3, and append a deployment-state paragraph to CLAUDE.md section 13.0.2 in the same
register the existing ones use: what is committed, what is live, what the node counts are, and
that nothing is armed. If a new engine behaviour was observed, add a row to section 13.0.3's table
with its execution id.

Redact before freezing anything. If a runData fixture is frozen from this execution, remove the
Webhook Trigger's headers object entirely — it carries `x-enrichment-secret` and the caller IP
verbatim, and a token-shape scan does not catch either. Remove any discovered person's contact
details too.

Nothing is armed. No `ALLOW_*` flag is flipped. No HubSpot record is written.
  </how-to-verify>
  <acceptance_criteria>
    - All six cloud workflows are live, `active`, with `settings.executionOrder` reading `v1` and every write-safety flag reading `false`.
    - `scripts/bounce_n8n_workflows.py`'s `WORKFLOWS` map has six entries, all with real live ids, and the bounce covered all six.
    - The live node count of `wf_suggest_discovery_cloud` matches the committed count recorded in `73.1-07-SUMMARY.md`.
    - One disarmed POST produced exactly one execution, and no further execution appeared in the two minutes after it.
    - The response body carried every sent company exactly once, each with its `num_associated_contacts`.
    - The backend-status `portalId` read is recorded, and the `account-info` scope claim is tagged `[observed live]` or recorded as not granted.
    - `73.1-UAT.md` exists with every claim tagged `[documented]` or `[observed live]`.
    - CLAUDE.md section 13.0.2 carries a new deployment-state paragraph stating what is live and that nothing is armed.
    - Any frozen fixture has no `x-enrichment-secret`, no caller IP and no discovered person's contact details.
  </acceptance_criteria>
  <verify>
    <human-check>The operator confirms: six workflows live and active, every write flag false, the discovery POST produced exactly one execution with every company echoed once, no execution burst followed, and no HubSpot record was written.</human-check>
    <automated>/usr/bin/grep -c 'x-enrichment-secret' tests/n8n/fixtures/frozen/*.json 2>/dev/null | /usr/bin/grep -v ':0$' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`, meaning at least one frozen fixture still carries the webhook secret</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/ && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears, or the TAP summary reports a `# fail` count greater than 0</fails_when>
  </verify>
  <done>The sixth workflow is live and disarmed, one POST proved the lane end to end on the real engine, and every claim in this phase's record is tagged for what it actually is.</done>
  <precondition>`N8N_URL`, `N8N_API_KEY` and the deploy opt-in variables are set for the live instance, the operator is present, and plan 09 Task 2's ruling has been recorded so `discoverySearch.js` is not deployed with a known-wrong endpoint.</precondition>
  <reversibility rating="one-way">A live CREATE mints a workflow id this repo then records and depends on; the execution budget consumed cannot be returned. No separate `checkpoint:decision` precedes it: this task IS a `gate="blocking-human"` checkpoint and the operator performs the deploy themselves, so the confirmation and the action are the same step.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local environment -> provider APIs | The probe reads real provider keys and spends real credit. |
| committed JSON -> the live n8n instance | A deploy changes what runs against the operator's real CRM credential. |
| live runData -> a committed fixture | runData embeds the webhook secret and the caller IP verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-37 | Information Disclosure | A provider key or an Authorization header printed by the probe | high | mitigate | The probe prints endpoint, status, balances and deltas only; no print takes a header or a raw response object; Task 1 Test 3 pins it. |
| T-73.1-38 | Information Disclosure | `x-enrichment-secret` and the caller IP frozen into a committed fixture from the live execution | high | mitigate | The headers object is removed entirely before freezing, per memory `n8n-rundata-carries-webhook-secret`; a token-shape scan does not catch it, so the removal is structural. A verify command greps every frozen fixture. |
| T-73.1-39 | Denial of Service | An execution burst consuming the Starter monthly budget, as on 2026-09-10 | critical | mitigate | Zero `executeWorkflow` nodes asserted at generation time, plus a two-minute burst watch after the proof send; the mechanism behind the 2026-09-10 incident was never isolated, so the watch is not redundant. |
| T-73.1-40 | Elevation of Privilege | A deploy that arms writes while the operator believes it is disarmed | critical | mitigate | Every write-safety flag is read back as `false` on all six workflows BEFORE the proof send, and the task stops if any reads otherwise. |
| T-73.1-41 | Tampering | Deploying `discoverySearch.js` with an endpoint the probe has already shown to be wrong | medium | mitigate | Task 3's precondition requires Task 2's ruling to be recorded first, and Task 2 corrects the module before Task 3 deploys. |
| T-73.1-42 | Repudiation | A live claim recorded without saying whether it was observed or merely documented | medium | mitigate | CLAUDE.md section 13.0.3's tagging discipline applied to every claim in `73.1-UAT.md`, with execution ids. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 09)

- `scripts/probe_provider_discovery.py` — new one-shot credit-spending probe, opt-in guarded
- `tests/test_probe_provider_discovery.py` — new offline test
- `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json` — probe result plus the operator's `operator_ruling`
- `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md` — the live disarmed proof record
- the sixth workflow's live id in `scripts/bounce_n8n_workflows.py`'s `WORKFLOWS` map
- a new deployment-state paragraph in CLAUDE.md section 13.0.2, and any new row in section 13.0.3's platform-facts table
</artifacts_produced>

<verification>
- Both suites green after any endpoint correction.
- Six workflows live, active, `executionOrder: v1`, every write flag `false`.
- No frozen fixture carries the webhook secret.
</verification>

<success_criteria>
- The operator has ruled on D-12 on measured evidence.
- The discovery lane has run once on the real engine, disarmed, echoing every company exactly once in one execution.
- Nothing is armed and no HubSpot record was written.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md` when done.
</output>
