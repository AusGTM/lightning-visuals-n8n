---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - operator-claude-plugin/config/source_allowlist.yaml
  - operator-claude-plugin/scripts/search_fallback.py
  - operator-claude-plugin/scripts/suggest_contacts.py
  - operator-claude-plugin/tests/test_source_tier_parity.py
  - operator-claude-plugin/tests/test_search_fallback.py
  - operator-claude-plugin/tests/test_suggest_contacts.py
requirements: [D-01, D-02, D-03, D-04, D-05, D-05a]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: ranks 1 and 2 are sendable and ranks 3 and 4 are held — `STRONG_TIERS` stays `(1, 2)` with tier 2's MEANING changed from LinkedIn to provider discovery, `KNOWN_TIERS` becomes `(1, 2, 3, 4)`, `LISTED_TIERS` becomes `(3, 4)`, and LinkedIn (now rank 3) flips from sendable to held."
    - "D-01: the absence of a match is represented by `_tier_of`'s pre-existing `(None, None)` rejection shape — NO new tier-5 integer and no new sentinel value is introduced, because the pipeline already has one."
    - "D-01: tier 2 is STAMPED by the discovery adapter and is never matched by host — `config/source_allowlist.yaml` lists no tier-2 entry at all, so `_tier_of` can never mint rank 2 from a URL."
    - "D-05a: one parity test pins ALL FOUR tier constants against the allowlist — `STRONG_TIERS`, `KNOWN_TIERS`, `LISTED_TIERS` (`search_fallback.py`) and `SEARCH_SOURCE_TIERS` (`suggest_contacts.py`) — deriving each from the allowlist rather than restating literals."
    - "D-05: provider-discovered records do NOT bypass the tier gate — they reuse the gated `suggest_contacts_web_search` provenance input with `source_tier: 2`, so `hold_weak_sources`' existing input check needs zero change and cannot fail open on them."
    - "D-05: a rank-2 record with an unrelated email is still held by `partition_for_dispatch`, and a rank-3 or rank-4 record is never sendable however confident the waterfall was."
    - "D-02: `partition_for_dispatch`'s email-domain relation is unchanged for a provider-found person — after stage 2 reveals an email, a personal or unrelated domain is held exactly as today."
    - "D-03: a person found on the company page (rank 1) AND by a provider (rank 2) is ONE row stamped with the LOWEST rank's provenance and locator, with the other hit recorded as corroboration inside the provenance entry; the dedupe key is `linkedin_url` when both carry one, else casefolded firstname+lastname+company."
    - "D-04: the row's `jobtitle` is the winning source's title and the other source's title lives in provenance, never on the row."
  artifacts:
    - "operator-claude-plugin/config/source_allowlist.yaml at version lv-source-allowlist-v2 with LinkedIn at tier 3 and industry/media at tier 4"
    - "operator-claude-plugin/tests/test_source_tier_parity.py — the D-05a four-constant parity test"
    - "suggest_contacts.dedupe_discovered() — the D-03/D-04 one-person-not-two fold"
    - "suggest_contacts.DISCOVERY_LOCATOR_BY_PROVIDER — the synthetic locator strings for a provider hit"
  key_links:
    - "config/source_allowlist.yaml tier numbers -> search_fallback.LISTED_TIERS/KNOWN_TIERS -> suggest_contacts.SEARCH_SOURCE_TIERS: the parity test is the only thing holding these three in step."
    - "synthesise_rows(source_tier=2) -> provenance.input == search_fallback.SEARCH_INPUT -> hold_weak_sources' gate: reusing the literal is what makes the gate see provider rows at all."
---

<objective>
Re-tier the source ranking so provider discovery is rank 2, LinkedIn is rank 3 and industry
media is rank 4 (D-01), pin all four tier constants against the allowlist with one parity test
(D-05a), close the tier gate against provider records without weakening it (D-05/D-02), and
give a provider-found person the provenance and dedupe behaviour D-03/D-04 require.

Purpose: every other plan in this phase hangs off this numbering. Until the constants, the
allowlist and the gate agree, a provider-discovered person either cannot be stamped at all
(`synthesise_rows` raises) or slips past the send gate unjudged.

Output: a re-tiered allowlist at `v2`, four constants in agreement, a parity test that fails on
any future drift, and the `dedupe_discovered` fold that makes one person one row.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-PATTERNS.md
@operator-claude-plugin/config/source_allowlist.yaml
@operator-claude-plugin/scripts/search_fallback.py
</context>

<assumption_delta_decision>
**Noun:** the source rank of a discovered person.
**Signal:** `pluralization` — a second discovery source class (provider search) joins the
website ladder, and the existing rank-2 slot changes occupant.
**Decision:** `promote` — provider discovery takes rank 2 outright and LinkedIn is demoted to
rank 3, rather than being added alongside as a new rank. Rationale: the operator's ruling is a
re-ranking of trust, not an addition of a parallel axis; keeping LinkedIn at 2 and inventing a
rank 2.5 would give the codebase two numbering schemes for one question.
**Invariant this implies:** `2 not in {entry["tier"] for entry in allowlist["tiers"]}` — rank 2
is adapter-stamped only and can never be minted from a matched host. Encoded as an assertion in
`test_source_tier_parity.py` (Task 1).
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: the four-constant parity test RED, then the re-tier that turns it GREEN</name>
  <files>operator-claude-plugin/tests/test_source_tier_parity.py, operator-claude-plugin/config/source_allowlist.yaml, operator-claude-plugin/scripts/search_fallback.py, operator-claude-plugin/scripts/suggest_contacts.py</files>
  <read_first>
    - operator-claude-plugin/config/source_allowlist.yaml (the full header comment block and both tier entries — the header states the tier semantics that are about to change)
    - operator-claude-plugin/scripts/search_fallback.py lines 40-70 (MAX_FALLBACK_SEARCHES, SEARCH_INPUT, LADDER_INPUT, STRONG_TIERS, KNOWN_TIERS, LISTED_TIERS, SOURCE_TIER_HOLD_CODE) and lines 280-310 (`_host_matches`, `_tier_of`)
    - operator-claude-plugin/scripts/suggest_contacts.py line 492 (`SEARCH_SOURCE_TIERS`) and the `synthesise_rows` docstring and body that follow it
    - tests/n8n/columnMapIdentityParity.test.mjs — the parity-test idiom this new Python test follows (derive one side from the other, never restate literals on both sides)
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md § "Pitfall 2" (the explicit RED-first ordering note for this exact change)
  </read_first>
  <behavior>
    - Test 1 `test_listed_tiers_are_exactly_the_allowlist_tiers`: loads `source_allowlist.yaml`, builds `tuple(sorted({entry["tier"] for entry in doc["tiers"]}))`, asserts it equals `search_fallback.LISTED_TIERS`.
    - Test 2 `test_known_tiers_are_the_computed_ranks_plus_the_listed_ones`: asserts `search_fallback.KNOWN_TIERS == (1, 2) + search_fallback.LISTED_TIERS`.
    - Test 3 `test_search_source_tiers_matches_known_tiers`: asserts `suggest_contacts.SEARCH_SOURCE_TIERS == search_fallback.KNOWN_TIERS`, so every stampable rank is a rank the gate can read.
    - Test 4 `test_strong_tiers_are_the_two_computed_ranks`: asserts `search_fallback.STRONG_TIERS == (1, 2)` and that every member of `STRONG_TIERS` is in `KNOWN_TIERS`.
    - Test 5 `test_rank_two_is_never_listed_as_a_host`: asserts `2 not in {entry["tier"] for entry in doc["tiers"]}`, because rank 2 is stamped by the discovery adapter and must never be mintable from a matched host by `_tier_of`.
    - Test 6 `test_allowlist_version_records_the_renumbering`: asserts the allowlist's `version` string is `lv-source-allowlist-v2`, so a future reader diffing an old document knows the numbers changed meaning.
  </behavior>
  <action>
Write `operator-claude-plugin/tests/test_source_tier_parity.py` FIRST with the six tests above and run it — it must fail RED against today's values (`LISTED_TIERS = (2, 3)`, `KNOWN_TIERS = (1, 2, 3)`, `SEARCH_SOURCE_TIERS = (1, 2, 3)`, allowlist tiers `{2, 3}`, version `lv-source-allowlist-v1`). Record the failing output in the SUMMARY.

Then make it green. In `operator-claude-plugin/config/source_allowlist.yaml`: renumber the `linkedin.com` entry from `tier: 2` to `tier: 3` keeping its label "LinkedIn"; renumber the racing/sport-industry entry from `tier: 3` to `tier: 4` keeping its label and every `[ASSUMED]` host comment byte-identical; bump `version` to `lv-source-allowlist-v2`. Rewrite the header comment block that currently reads "tier 1 -- the company's OWN host ... tier 4 -- the ABSENCE of a match" to state the new four-rank scheme: rank 1 the company's own host (computed at rank time), rank 2 provider discovery (STAMPED by the discovery adapter, never listed here and never matched by host), rank 3 LinkedIn, rank 4 industry bodies and media, and the absence of a match is a REJECTION (`_tier_of` returns `(None, None)`) — not a bucket and not a fifth number. Rewrite the trailing paragraph that currently says "Tier 3 is admitted but NEVER sendable (D-5sd-05)" to name ranks 3 AND 4 as admitted-but-never-sendable, and add one sentence saying LinkedIn moved from sendable to held by operator ruling 2026-09-18 (D-01).

In `operator-claude-plugin/scripts/search_fallback.py`: leave `STRONG_TIERS = (1, 2)` as the same tuple but rewrite its comment — tier 1 is the company's own host and tier 2 is now provider discovery, both first-party or provider-attested in a way a third-party mention is not; set `KNOWN_TIERS = (1, 2, 3, 4)` and `LISTED_TIERS = (3, 4)`. Update `_tier_of`'s docstring where it says a hit on the company's own `board.example.com` "would fall to tier 4 and be thrown away" — under the new numbering an unlisted host is rejected outright, so name the rejection rather than a number.

In `operator-claude-plugin/scripts/suggest_contacts.py`: set `SEARCH_SOURCE_TIERS = (1, 2, 3, 4)` and extend `synthesise_rows`'s docstring to say rank 2 is the provider-discovery rank stamped by the discovery adapter (never derived from a URL) and rank 4 is the renamed industry/media rank that `rank_results` now returns.

Do NOT touch `hold_weak_sources`' `provenance.get("input") != SEARCH_INPUT` guard in this task — D-05's chosen resolution (Task 2) is to reuse the same input literal, so that line stays exactly as it is.
  </action>
  <acceptance_criteria>
    - `operator-claude-plugin/tests/test_source_tier_parity.py` exists and contains all six test function names listed in `<behavior>`.
    - The test file derives `LISTED_TIERS`/`KNOWN_TIERS`/`SEARCH_SOURCE_TIERS` from the allowlist document and from each other; it contains no literal tuple `(3, 4)` or `(1, 2, 3, 4)` on the asserted side.
    - `/usr/bin/grep -c 'tier: 2' operator-claude-plugin/config/source_allowlist.yaml` returns 0.
    - `/usr/bin/grep -c 'lv-source-allowlist-v2' operator-claude-plugin/config/source_allowlist.yaml` returns 1.
    - `search_fallback.KNOWN_TIERS == (1, 2, 3, 4)`, `search_fallback.LISTED_TIERS == (3, 4)`, `search_fallback.STRONG_TIERS == (1, 2)`, `suggest_contacts.SEARCH_SOURCE_TIERS == (1, 2, 3, 4)`.
    - The SUMMARY records the RED output observed before the re-tier.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_source_tier_parity.py</automated>
    <fails_when>non-zero exit, or the pytest summary line reports any count other than `6 passed`</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_search_fallback.py operator-claude-plugin/tests/test_suggest_contacts.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>The four tier constants and the allowlist agree on the new numbering, one parity test proves it, and rank 2 is provably unlistable as a host.</done>
  <reversibility rating="reversible">Constants, a YAML config and a test — revert is one commit; no durable state stores a tier number that is re-derived on read (RESEARCH.md Runtime State Inventory).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the send gate under the new numbering — LinkedIn held, rank 2 still email-gated</name>
  <files>operator-claude-plugin/scripts/search_fallback.py, operator-claude-plugin/tests/test_search_fallback.py, operator-claude-plugin/tests/test_suggest_contacts.py</files>
  <read_first>
    - operator-claude-plugin/scripts/search_fallback.py `hold_weak_sources` in full (the docstring's fail-open paragraph and the whole loop, including both reason-prose branches)
    - operator-claude-plugin/scripts/suggest_contacts.py `partition_for_dispatch` in full (its `extraction.hold_emailless` first pass, the `email_domain_relation` second pass and `_RELATION_REASON_CODES`)
    - operator-claude-plugin/tests/test_suggest_contacts.py — the existing `test_the_tier_gate_and_the_waterfall_gate_are_independent_and_both_must_hold` test, which is the one to extend
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md § "Pattern 1" and § "Pattern 2" (the two options D-05 leaves open and why option b is chosen)
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-02 and D-05
  </read_first>
  <behavior>
    - Test A: a record whose provenance is `{input: SEARCH_INPUT, source_tier: 2, locator: <provider locator>}` and whose row is in `sendable` stays in `sendable` after `hold_weak_sources` — rank 2 is strong.
    - Test B: the SAME record, when `partition_for_dispatch` has already held it for an unrelated email, stays held — its existing hold entry stands and is not overwritten (D-02/D-05).
    - Test C: a record stamped `source_tier: 3` (LinkedIn under the new numbering) is moved out of `sendable` into `held`, and its reason names LinkedIn as a self-attested profile whose role can be stale, NOT the pre-existing "not this company's own site or LinkedIn" sentence, which is now false.
    - Test D: a record stamped `source_tier: 4` (industry/media) is held with the third-party-source reason naming its locator.
    - Test E: every operator-visible reason string produced by this function contains the word `rank` and does not contain the word `tier` (the D-10b guard).
  </behavior>
  <action>
Resolve D-05 by REUSE, not by widening the gate: provider-discovered records will be stamped with the existing gated `provenance.input` literal plus `source_tier: 2` (Task 3 does the stamping). `hold_weak_sources`' `if provenance.get("input") != SEARCH_INPUT: continue` line therefore needs NO change and must not be edited — record that choice and its reason in the SUMMARY, because the alternative (widening the `!=` check to a second input value) is the one a later reader will assume was taken.

Split `hold_weak_sources`' single `tier in KNOWN_TIERS` reason branch into two, because rank 3 and rank 4 are now different claims about the world. For rank 3, write a LinkedIn-specific reason: the person is named by a self-maintained profile, which is current about the person but says nothing about this company's own record of them, so the row is held for the operator to judge rather than sent — and say plainly that this rank was sendable before the 2026-09-18 ruling and is not any more. For rank 4, keep the existing third-party-source wording, updating the number in the text and dropping the clause "not this company's own site or LinkedIn", which no longer describes rank 4's complement. Keep `SOURCE_TIER_HOLD_CODE` unchanged — one hold code still covers both, and the reason prose is what differentiates them.

Keep every operator-visible string saying "rank" and never "tier"; the internal names (`source_tier`, `STRONG_TIERS`) are unchanged.

Extend `test_the_tier_gate_and_the_waterfall_gate_are_independent_and_both_must_hold` in `operator-claude-plugin/tests/test_suggest_contacts.py` so it drives the real `hold_weak_sources` with a rank-2 record carrying an unrelated email and asserts the row is STILL held — the two gates independent, both holding (D-02). Add tests A through E to `operator-claude-plugin/tests/test_search_fallback.py`. Write the tests before the prose change and record the RED output for Test C (which today produces the false LinkedIn sentence).
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'provenance.get("input") != SEARCH_INPUT' operator-claude-plugin/scripts/search_fallback.py` returns 1 — the gate line is unmodified.
    - `hold_weak_sources` contains two distinct reason branches keyed on the record's rank, one naming LinkedIn and one naming a third-party source.
    - `/usr/bin/grep -v '^ *#' operator-claude-plugin/scripts/search_fallback.py | /usr/bin/grep -c "not this company's own site or LinkedIn"` returns 0.
    - Tests A–E exist by name in `operator-claude-plugin/tests/test_search_fallback.py` and pass.
    - `test_the_tier_gate_and_the_waterfall_gate_are_independent_and_both_must_hold` asserts a rank-2 unrelated-email row is held.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_search_fallback.py operator-claude-plugin/tests/test_suggest_contacts.py operator-claude-plugin/tests/test_report_enrichment.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>A rank-2 row with an unrelated email is still held, a rank-3 LinkedIn row is held with a reason that is true of LinkedIn, and a rank-4 row is held as a third-party mention.</done>
  <reversibility rating="reversible">Reason prose and tests; no stored value changes shape.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: provider provenance and the one-person-not-two fold</name>
  <files>operator-claude-plugin/scripts/suggest_contacts.py, operator-claude-plugin/tests/test_suggest_contacts.py</files>
  <read_first>
    - operator-claude-plugin/scripts/suggest_contacts.py `synthesise_rows` in full, including the docstring paragraph "WHY THE TIER RIDES provenance AND NOT THE ROW", and `_normalize_name` and `select_people`'s existing dedupe
    - operator-claude-plugin/scripts/extraction.py — `validate`'s provenance check (it requires only `input` and `locator` to be present) and `canonical_props()`
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-03, D-04 and the "Claude's Discretion" bullet on the provenance `input` value and corroboration storage
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-RESEARCH.md § "Pattern 2" (the locator problem — a provider hit has no fetched URL) and § "Anti-Patterns to Avoid" (a rank-3/4 corroboration must never upgrade a row's effective rank)
  </read_first>
  <behavior>
    - Test 1: `synthesise_rows(company, people, DISCOVERY_LOCATOR_BY_PROVIDER["apollo"], cap, source_tier=2)` produces records whose provenance is `{input: "suggest_contacts_web_search", locator: "apollo people search", source_tier: 2, provider: "apollo_search"}` and whose rows carry only canonical props.
    - Test 2: `dedupe_discovered` given the same person from a rank-1 ladder hit and a rank-2 provider hit returns ONE record, carrying the rank-1 provenance and locator, with the rank-2 hit recorded under `provenance["corroboration"]` naming the provider and its own title.
    - Test 3: the surviving row's `jobtitle` is the rank-1 (winning) source's title; the rank-2 title appears only inside the corroboration entry and never on the row (D-04).
    - Test 4: dedupe keys on `linkedin_url` when BOTH records carry one, and on casefolded firstname+lastname+company otherwise — two people with the same name at different companies are not merged.
    - Test 5: a rank-1 record corroborated by a rank-3 or rank-4 hit keeps `source_tier: 1`; a rank-3 record corroborated by a rank-4 hit keeps `source_tier: 3` and is still held — corroboration never lowers (improves) a row's rank.
    - Test 6: `dedupe_discovered` on records with no duplicates returns them unchanged and in input order.
  </behavior>
  <action>
Add `DISCOVERY_LOCATOR_BY_PROVIDER` to `operator-claude-plugin/scripts/suggest_contacts.py`, mapping `zoominfo`, `apollo` and `lusha` to the synthetic locator strings `"zoominfo contact search"`, `"apollo people search"` and `"lusha prospecting search"`. A provider hit has no fetched page, and `locator` is free text that `extraction.validate` only checks for presence — this names the provider and the endpoint in the one field that already travels with the row's provenance.

Extend `synthesise_rows` to accept an optional `provider` keyword. When given (only the discovery adapter gives it), add a sibling `provider` key to the provenance dict carrying `f"{provider}_search"` — the roadmap's own vocabulary. The key rides `provenance`, never the row: `synthesise_rows` already asserts every row key is in `extraction.canonical_props()` and `write_dispatch_csv` raises on a non-canonical key, so a row key here would be a dispatch-time refusal. Keep the existing `source_tier` validation and its `ValueError` exactly as they are.

Add `dedupe_discovered(records)` to the same module. It folds records that name the same person, keyed by `linkedin_url` when both records carry one and otherwise by the casefolded, whitespace-collapsed `firstname + lastname + company` — reuse the module's existing `_normalize_name` rather than writing a second normaliser. The surviving record is the one whose `provenance["source_tier"]` is LOWEST (a missing `source_tier` means a ladder hit and sorts as rank 1); it keeps its own provenance, locator and `jobtitle` untouched. Every losing hit is appended to the survivor's `provenance["corroboration"]` list as a dict naming that hit's `source_tier`, `locator`, `provider` when present, and its own `jobtitle`. Corroboration is recorded only — it never changes the survivor's `source_tier`, so a rank-3 row corroborated by a rank-4 hit is still rank 3 and still held, and a rank-4 corroboration can never upgrade anything.

Call `dedupe_discovered` BEFORE `hold_weak_sources`, never instead of it: a merged row still passes through both the email-relatedness gate and the tier gate. State that ordering in the function's docstring.

Write all six tests in `operator-claude-plugin/tests/test_suggest_contacts.py` first and record the RED output.
  </action>
  <acceptance_criteria>
    - `suggest_contacts.DISCOVERY_LOCATOR_BY_PROVIDER` exists with exactly the three keys `zoominfo`, `apollo`, `lusha`.
    - `suggest_contacts.dedupe_discovered` exists and its docstring states that it runs BEFORE `hold_weak_sources` and never replaces it.
    - A provider-stamped record's `provenance` has keys `input`, `locator`, `source_tier`, `provider` and its `row` has no key outside `extraction.canonical_props()`.
    - Tests 1–6 exist by name and pass.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import suggest_contacts as s; print(sorted(s.DISCOVERY_LOCATOR_BY_PROVIDER))"` prints `['apollo', 'lusha', 'zoominfo']`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_suggest_contacts.py operator-claude-plugin/tests/test_source_tier_parity.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>A provider-found person is stamped rank 2 with a provider-naming locator and provenance key, and a person found twice is one row keeping the better source's title and provenance.</done>
  <reversibility rating="reversible">New function plus an optional keyword; no existing call site changes shape.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider search response -> plugin `records` | A provider-supplied person's name and title become a row that may later be written to HubSpot; the provider is not a trusted authority on who works where. |
| allowlist YAML -> `_tier_of` | A host string in a committed config decides whether a person is sendable without review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-01 | Elevation of Privilege | `hold_weak_sources` fail-open on an unrecognised `provenance.input` | high | mitigate | D-05 by reuse: provider records carry the SAME gated input literal, so the existing check sees them. Task 2 pins that the gate line is unmodified and Task 2's tests drive real provider-shaped records through it. |
| T-73.1-02 | Spoofing | A host listed at tier 2 in the allowlist would let `_tier_of` mint the adapter-only rank from any matched URL | high | mitigate | Task 1 Test 5 asserts `2 not in {allowlist tiers}`; the header comment states rank 2 is stamped only. |
| T-73.1-03 | Tampering | A rank-3/4 corroboration silently upgrading a row's effective rank past the send gate | high | mitigate | `dedupe_discovered` records corroboration without touching `source_tier`; Task 3 Test 5 pins it. |
| T-73.1-04 | Information Disclosure | A provider-supplied person's PII (title, name) landing in a committed test fixture | medium | mitigate | Tests use the synthetic pickleball fixture names already in `tests/demo-adversarial/`, never a live provider response. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package; RESEARCH.md records "Package Legitimacy Audit: not applicable — no external packages are installed by this phase." |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 01)

- `operator-claude-plugin/tests/test_source_tier_parity.py` — new file, six tests
- `operator-claude-plugin/config/source_allowlist.yaml` `version: "lv-source-allowlist-v2"` — new value
- `suggest_contacts.DISCOVERY_LOCATOR_BY_PROVIDER` — new module constant (keys `zoominfo`, `apollo`, `lusha`)
- `suggest_contacts.dedupe_discovered(records)` — new function
- `suggest_contacts.synthesise_rows(..., provider=None)` — new optional keyword
- `provenance["provider"]` and `provenance["corroboration"]` — new provenance keys (never row keys)
- constants changed: `search_fallback.KNOWN_TIERS` -> `(1, 2, 3, 4)`, `search_fallback.LISTED_TIERS` -> `(3, 4)`, `suggest_contacts.SEARCH_SOURCE_TIERS` -> `(1, 2, 3, 4)`
</artifacts_produced>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/` is green.
- The four tier constants are derivable from `source_allowlist.yaml` and the parity test proves it.
- No operator-visible string produced by this plan's code contains the word `tier`.
</verification>

<success_criteria>
- All six parity tests pass and would fail against the pre-change constants.
- A rank-2 record with an unrelated email is held; a rank-3 or rank-4 record is never sendable.
- One person found twice is one row with the lower rank's provenance and the winning title.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-01-SUMMARY.md` when done.
</output>
