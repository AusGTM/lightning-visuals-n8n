---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 11
type: execute
wave: 2
gap_closure: true
depends_on: ["73.1-10"]
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/wf_suggest_discovery_cloud.json
  - n8n/code/discoverySearch.js
  - scripts/probe_provider_discovery.py
  - tests/n8n/suggestDiscoveryLane.test.mjs
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md
autonomous: true
requirements: [D-11, D-11a, D-11c]

estimate:
  tokens: 140000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-11 / headline goal: a gap:true company sent to the discovery lane returns at least one provider-discovered person — a live 200 from /gtm/data/v1/contacts/search inside n8n with a real people list, or a genuine total:0 at status 200, for a domain independently confirmed at 1094 results via Python."
    - "The discovery lane's ZoomInfo search never reuses a token minted by a previous execution — it mints once per execution, so the stale/rejected-token failure class behind the 12668/12669 401s cannot recur."
    - "The same live execution that proves D-11 also proves D-11a: rung 1's diagnostics report the chosen families' own member count as titles_used with titles_dropped 0."
  artifacts:
    - "scripts/build_cloud_workflows.py::_discovery_zoom_search_gate_js — mint-per-execution token gate, discovery lane only"
    - "scripts/probe_provider_discovery.py --token-replay — offline mint-A/mint-B/replay-A evidence, zero n8n executions"
    - ".planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json"
    - ".planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md — dated gap-closure addendum with execution ids"
  key_links:
    - "ZoomInfo Search Token Gate -> IF ZoomInfo Search Needs Mint (true) -> ZoomInfo Search Mint -> Carry Merge -> Cache Token -> ZoomInfo Search Rung1"
---

<objective>
Close Gap 1 (D-11). Three live disarmed executions returned zero people. `12666` hit a real,
now-fixed 400 (PFAPI0006, the 757-char title join). `12668` and `12669` both returned 401 on a
token CACHED by `12666` 27 minutes earlier and reused — a token whose claims (issuer, audience,
client id, 17 scopes including `api:data:contact`, 24h lifetime) are byte-identical to one minted
locally by `scripts/check_provider_credits.py`, which returns 200 / `totalResults: 1094` on the
same endpoint and domain the same hour. Expiry, scope/entitlement and client identity are ruled
out by that evidence and are NOT re-litigated here.

Two hypotheses remain. (a) request construction — `buildUrl()` puts pagination in the URL query
string, a shape no other live-proven lane here sends. (b) token lifecycle — a token rejected
server-side before its own expiry claim, e.g. a later mint invalidating an earlier one (local
Python mints did happen during diagnosis). **A malformed query string yields 400, not 401** — so
(b) fits the observed status better AND is testable offline at zero n8n cost.

Purpose: adopt first the fix that is correct under EITHER hypothesis (this lane runs one
execution per round, so a cross-execution token cache buys nothing and carries an entire failure
class), gather offline evidence saying which hypothesis held, then spend ONE live execution that
proves D-11 and D-11a together.

Output: a mint-per-execution token gate, an offline replay verdict, and a dated UAT addendum
carrying execution ids and the read-back `search_diagnostics`.

**Reversibility:** `costly` — task 3 spends live n8n executions against a Starter 2.5K/month
budget. Hard ceiling: 3 executions on `VJJBZ2oJ0079MSzG`. Nothing is armed at any point; this lane
has zero HubSpot write nodes and zero `ALLOW_*` flags, so there is no arming window to open.

**Spec-less probe fallback skipped (visible, recorded):** this phase has no requirement IDs to
probe and no `-SPEC.md`; plans are keyed on `73.1-CONTEXT.md` decision ids.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-VERIFICATION.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md
@n8n/code/zoominfoToken.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Mint per execution on the discovery lane — the whole token path, end to end</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_suggest_discovery_cloud.json, tests/n8n/suggestDiscoveryLane.test.mjs</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` lines 5791-5870 — `_zoom_split_gate_js` (SHARED with the enrich lane: do not modify it) and `_zoom_split_cache_js`
    - `scripts/build_cloud_workflows.py` lines 12296-12335 — the discovery lane's gate/IF/Mint/Cache wiring, the carry-merge splice, and `if_direct_edges`
    - `n8n/code/zoominfoToken.js` — the pure cache helpers and `isAuthError`/`extractErrorStatus`
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md` section 5a and its orchestrator addendum — what `12666`/`12668`/`12669` actually showed
    - `tests/n8n/suggestDiscoveryLane.test.mjs` lines 17-70 — `loadWf()`, `zeroPeopleHttpStubs()` (the Mint node is already stubbed there)
  </read_first>
  <action>
Add `_discovery_zoom_search_gate_js()` to `scripts/build_cloud_workflows.py` and use it at the
discovery lane's `ZoomInfo Search Token Gate` (line ~12298) in place of `_zoom_split_gate_js`.
Leave `_zoom_split_gate_js` byte-identical — it is the enrich lane's gate and is live-proven.

The new body is secret-free and cache-free: map every input row to itself plus
`zoom_needs_mint: true` and `zoom_token: null`, consulting no cross-run store and importing
nothing from `zoominfoToken.js`. Its docstring records WHY: this lane runs exactly one execution
per round (D-08), so a cross-execution token cache saves at most one free OAuth call and in
exchange carries the whole stale/rejected-token failure class that produced the `12668`/`12669`
401s. The enrich lane keeps its cache — it makes many calls per execution and its tradeoff differs.

This changes no edge and no node count. `IF ZoomInfo Search Needs Mint` simply always takes its
true output — `ZoomInfo Search Mint` then `ZoomInfo Search Mint Carry Merge` then `ZoomInfo Search
Cache Token` then `ZoomInfo Search Rung1` — which is exactly the path execution `12666` took live
and reached ZoomInfo on. The false output into `Rung1` becomes unreachable; `Rung1` is a plain Code
node with two producer edges, not a Merge, so an unreachable producer starves nothing (CLAUDE.md
§13.0.3's Merge-starvation rule governs Merges only and does not apply here).

Leave `ZoomInfo Search Cache Token` alone: it still persists the freshly-minted token to the
cross-run store, which nothing now reads. That is one dead write in shared code, cheaper than a
second variant of the cache node — record it as a one-line note in the SUMMARY.

Add one node-scoped assertion to `tests/n8n/suggestDiscoveryLane.test.mjs`: load the workflow with
the file's existing `loadWf()`, find the node named `ZoomInfo Search Token Gate`, and assert on its
`parameters.jsCode`. Node-scoped, never a whole-file grep. Then regenerate; never hand-edit the JSON.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/ && echo REGEN_CLEAN</automated>
    <fails_when>non-zero exit, or "REGEN_CLEAN" absent from the output (a second regeneration produced a different file)</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "fail 0"</fails_when>
    <automated>git diff --quiet n8n/wf_enrichment_cloud.json && echo ENRICH_UNTOUCHED</automated>
    <fails_when>"ENRICH_UNTOUCHED" absent from the output — the shared enrich gate was modified, which this task forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c '_discovery_zoom_search_gate_js' scripts/build_cloud_workflows.py` returns a value `>= 2` (definition plus call site)
    - In `n8n/wf_suggest_discovery_cloud.json`, the node named `ZoomInfo Search Token Gate` has `parameters.jsCode` containing `zoom_needs_mint: true`
    - In `n8n/wf_suggest_discovery_cloud.json`, that same node's `parameters.jsCode` does NOT contain the substring `needsMint(`
    - `git diff --quiet n8n/wf_enrichment_cloud.json` exits 0 and `git diff --quiet n8n/wf_enrichment_local_live.json` exits 0
    - `n8n/wf_suggest_discovery_cloud.json` has exactly 26 nodes, zero nodes of type `n8n-nodes-base.executeWorkflow`, zero nodes whose URL mentions `api.hubapi.com`, and `settings.executionOrder` equal to `"v1"`
    - `node --test tests/n8n/*.test.mjs` exits 0 with `fail 0`
  </acceptance_criteria>
  <done>Every discovery execution mints its own ZoomInfo token; no execution can reuse a token another execution minted; graph, node count and the enrich lane are unchanged.</done>
  <reversibility rating="reversible">A gate-body swap on one lane — revert the single call site and regenerate.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Offline token-replay evidence — does a later mint invalidate an earlier token?</name>
  <files>scripts/probe_provider_discovery.py, .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json</files>
  <read_first>
    - `scripts/probe_provider_discovery.py` (356 lines) — `_node_eval`, `_search_request`, `_search_url`, `_search_headers`, `probe_provider`, `main`; it already derives ZoomInfo's URL and body from `n8n/code/discoverySearch.js` rather than holding a second copy
    - `scripts/check_provider_credits.py` — `_mint_zoominfo_token`, the mint whose token returns 200 live
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md` — the four Python replays that returned 200 / `totalResults: 1094` for tennis.com.au
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md` "Issues Encountered" item 1 — the `.env` secret-file guard and the scratchpad `run_with_env.py` driver idiom that works around it
    - `CLAUDE.md` §31 — todo triage: `kind:` plus the field that kind requires
  </read_first>
  <action>
Add a `--token-replay` mode to `scripts/probe_provider_discovery.py`. Zero n8n executions, zero
HubSpot calls, zero ZoomInfo credits (search is measured at 0 on this account, D-13):

1. Mint token A via `check_provider_credits._mint_zoominfo_token()`; POST the search for
   `tennis.com.au` with A; record the numeric HTTP status and `meta.totalResults`.
2. Mint token B the same way; record only that a second mint succeeded.
3. Replay the SAME search with token A again; record its numeric status.
4. Replay with token B as the control; record its numeric status.

Build the URL and body through the module the way the existing `_search_url`/`_search_request`
already do — never a hand-written second copy. The verdict is
`prior_token_invalidated_by_later_mint`, true when step 1 is 200 and step 3 is 401.

Write `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json`
carrying an ISO timestamp, the domain, each step's numeric status and total, the boolean verdict,
and each token's issued-at and expiry claims as integers. **Never write a token value, never write
any other claim, never print a token to stdout.** Decode the two integer claims from the payload
segment with the stdlib (`base64` + `json`), adding no dependency.

The `.env` file is unreadable by the shell's secret-file guard (still in force from 73.1-09), so
drive this through a scratchpad python driver that calls `load_dotenv()` in its own process — the
`run_with_env.py` idiom 73.1-09-SUMMARY.md records — rather than exporting the credential into the
shell. The driver lives in the session scratchpad, never in the repo.

Then handle the ENRICH-lane consequence, CONDITIONALLY on the verdict — CLAUDE.md §31 rule 1 says
a residual with no open question is a SUMMARY sentence, not a todo:

- Verdict `true`: file ONE todo under `.planning/todos/pending/` with `kind: design` and a
  `decision_needed:` line. It is a design decision, not a question — the observation is already in
  hand; what is open is whether the enrich lane should keep its 24-hour cross-execution cache when
  a later mint (backend-status's own mint node, or any local probe) silently invalidates it. Do NOT
  change the enrich lane here: its leaf already clears its cache on a 401 and self-heals on the
  next run, so this is an efficiency decision for the operator, never a blind fix.
- Verdict `false`: file NO todo. State in the SUMMARY that the enrich lane's cache is unaffected
  and why.
  
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider; echo "EXIT=$?"</automated>
    <fails_when>the output does not end with "EXIT=0", or the summary line reports any count under "failed" or "error"</fails_when>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json').read_text()); print('VERDICT', d['prior_token_invalidated_by_later_mint'], d['steps'][0]['status'], d['steps'][2]['status'])"</automated>
    <fails_when>non-zero exit (a missing file or a missing key), or the output does not begin with "VERDICT " followed by a boolean</fails_when>
    <automated>.venv/bin/python scripts/todo_triage.py</automated>
    <fails_when>non-zero exit — an untriaged pending todo, or one missing the field its `kind` requires</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'token-replay' scripts/probe_provider_discovery.py` returns a value `>= 1`
    - `/usr/bin/grep -c 'eyJ' .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json` returns `0` — no bearer token, in whole or in part, is ever written to the verdict file
    - `/usr/bin/grep -rc 'client_secret' .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json` returns `0`
    - The verdict JSON has keys `prior_token_invalidated_by_later_mint` (boolean), `domain`, `generated_at`, and a `steps` array of length 4 whose entries each carry a numeric or null `status`
    - Conditional on the verdict: if `prior_token_invalidated_by_later_mint` is `true`, exactly ONE new file exists under `.planning/todos/pending/` relative to the pre-task state and it declares `kind: design` with a `decision_needed:` line; if `false`, ZERO new files exist there and the SUMMARY states why the enrich lane is unaffected
    - `.venv/bin/python scripts/todo_triage.py` exits 0
    - `git status --porcelain n8n/` prints nothing — this task changes no workflow
  </acceptance_criteria>
  <done>The repo carries a dated, secret-free verdict saying whether a later mint invalidates an earlier ZoomInfo token, obtained without spending a single n8n execution, and the enrich-lane consequence is triaged as an open question.</done>
</task>

<task type="auto">
  <name>Task 3: One live disarmed round that proves D-11 and D-11a together</name>
  <files>.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md, n8n/code/discoverySearch.js, scripts/build_cloud_workflows.py, n8n/wf_suggest_discovery_cloud.json</files>
  <precondition>The n8n Cloud API is reachable and the repo `.env` loads through a scratchpad dotenv driver; workflow `VJJBZ2oJ0079MSzG` is active and on the committed body. If the API is unreachable or the credential does not load, halt and report rather than redeploying blind.</precondition>
  <read_first>
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md` — the exact deploy, bounce, proof-send and execution-listing invocations, and the scratchpad-driver workaround
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md` section 5a — the format of the existing dated addenda, and the redaction discipline
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-TOKEN-REPLAY-VERDICT.json` — task 2's verdict; it selects this task's branch
    - `scripts/deploy_n8n_workflows.py` and `scripts/bounce_n8n_workflows.py` — the `--only` scoping and the `WORKFLOWS` id map
    - `operator-claude-plugin/config/role_vocabulary.yaml` — the family labels to name in the proof envelope
    - `CLAUDE.md` §13.0.3 — the platform facts and the `[documented]` vs `[observed live]` tagging rule
  </read_first>
  <action>
Deploy scoped and disarmed, bounce, then send ONE POST. Ceiling for the whole task: **3
executions on `VJJBZ2oJ0079MSzG`**. Never arm anything. Never deploy any other workflow — use
`--only wf_suggest_discovery_cloud.json`. Never touch the other five live workflows.

**Send 1 — the discriminator.** A disarmed POST to `hubspot/suggest/discover` carrying a
client-minted `run_id`, `per_company_cap`, and two companies: one `gap: true` for `tennis.com.au`
(independently confirmed at 1094 results via Python), one `gap: false` control. Put
`role_families` on the gap company naming FOUR labels — `CEO`, `Chair`, `Executive Officer` and
`Board Chair` — so one execution proves both gaps at once: a two-label selection risks a genuine
zero that tells you nothing, while these four include D-11c's own families and a family
tennis.com.au plausibly staffs. Read `search_diagnostics` off the response body, and read the
execution id from the executions API.

**Branch on what comes back.**

- `rung1.status` is `200`: D-11 is closed. Confirm `rung1.titles_used` equals the member count of
  the four chosen families and `rung1.titles_dropped` is `0` — that is D-11a proven live, not just
  offline. Record and stop. One execution spent.
- `rung1.status` is `401` AND task 2's verdict is `true` AND `ZoomInfo Search Mint` appears in this
  execution's runData: a freshly-minted token was rejected. A query-string defect cannot produce a
  401, so the pagination variant would be two executions spent on a hypothesis the status already
  excludes. STOP. Write it up as an account-side escalation (a documented gap, per the
  VERIFICATION report's own "a THIRD 401 on a confirmed-fresh mint" instruction) and file a todo
  in §31 form. One execution spent.
- `rung1.status` is `401` AND task 2's verdict is `false`: hypothesis (a) is back in play. Change
  `buildUrl("zoominfo", ...)` to return the bare endpoint and pass the pagination through the
  request options instead — `qs: { "page[size]": n, "page[number]": 1 }` on the
  `this.helpers.httpRequest` call in the rung leaf — regenerate, redeploy scoped, bounce, send
  again (execution 2). If that also 401s, send once more with no pagination at all (execution 3).
  Record every variant's own raw status via the existing `search_diagnostics` keys. A third 401
  ends it as an account-side escalation, not a fourth code fix.
- Any other status (`400`, `403`, `429`, `5xx`): record it verbatim with the vendor pointer if the
  body carries one. A `400` with a JSON:API pointer is directly actionable and needs no further
  live send to diagnose.

**Redaction, non-negotiable.** This lane threads the bearer token along the row as `zoom_token`, so
its runData carries a live credential on EVERY item — on top of the Webhook Trigger's
`x-enrichment-secret` and caller IP that CLAUDE.md already warns about. Do not freeze this lane's
runData into `tests/n8n/fixtures/frozen/`. Do not paste raw runData into the UAT. Quote only
`search_diagnostics`, `company_id`, `num_associated_contacts`, person names and titles, and the
execution id.

Append a dated gap-closure section to `73.1-UAT.md` recording: each execution id, each rung's
status/total/error, `titles_used` and `titles_dropped`, the people returned (or a genuine zero),
the D-11 and D-11a verdicts, and the live node count / `active` / `executionOrder` read back for
the discovery workflow only. Tag each claim `[observed live]` or `[documented]` per §13.0.3.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import re,pathlib; t=pathlib.Path('.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md').read_text(); assert 'gap closure' in t.lower(), 'no gap-closure addendum'; m=max(int(x) for x in re.findall(r'\b12\d{3}\b', t)); assert m >= 12670, f'highest execution id {m} predates this task; the UAT already carried 12666-12669 before it ran'; print('ADDENDUM_OK', m)"</automated>
    <fails_when>non-zero exit, or "ADDENDUM_OK" absent. The `>= 12670` floor is deliberate: the UAT already names `12666`-`12669`, so a bare "some execution id is present" check would pass before this task ran at all. If the live instance's execution counter has moved well past 12670 by the time this runs, raise the floor to the pre-send maximum read from the executions API rather than lowering it</fails_when>
    <automated>/usr/bin/grep -cE '"(zoom_token|x-enrichment-secret)" *:' .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md; echo "RC=$?"</automated>
    <fails_when>a count above 0 on the first line — a JSON object carrying a bearer token or the webhook secret header was pasted into the UAT. `RC=1` with count 0 is the passing case (grep exits 1 on no match). The pattern deliberately requires the quoted-key-plus-colon shape so prose naming either field, including this plan's own redaction rule, cannot trip it</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "fail 0"</fails_when>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/ && echo REGEN_CLEAN</automated>
    <fails_when>non-zero exit, or "REGEN_CLEAN" absent — a variant edit was left un-regenerated or the JSON was hand-edited</fails_when>
  </verify>
  <acceptance_criteria>
    - `73.1-UAT.md` gained a dated gap-closure section naming at least one n8n execution id and reporting `rung1` status, total, `titles_used` and `titles_dropped` verbatim from `search_diagnostics`
    - The total number of new executions listed for workflow `VJJBZ2oJ0079MSzG` during this task is `<= 3`
    - `/usr/bin/grep -c 'eyJ' .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md` returns `0`
    - `git status --porcelain tests/n8n/fixtures/frozen/` prints nothing — no runData from this lane was frozen
    - `scripts/bounce_n8n_workflows.py` reports the discovery workflow `active` with `settings.executionOrder` equal to `"v1"`, and reports every HubSpot write-safety flag `false` on the four workflows that carry one
    - Exactly one of these holds and is stated explicitly in the addendum: (a) `rung1.status` is `200` and D-11 is recorded closed; or (b) the account-side escalation is recorded as a documented gap with a `kind: question` todo filed in §31 form
    - `.venv/bin/python scripts/todo_triage.py` exits 0
    - `git diff --quiet requirements.txt package.json` exits 0 — no dependency was added (T-73.1-11-SC)
  </acceptance_criteria>
  <done>The repo records, from a live disarmed execution, either a 200 from ZoomInfo's search inside n8n (closing D-11, with `titles_dropped: 0` closing D-11a live) or a reasoned account-side escalation with its evidence — and never more than three executions were spent reaching that answer.</done>
  <reversibility rating="costly">Spends live n8n executions from a 2.5K/month budget; the ceiling is 3 and every send is disarmed and read-only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n runData / response body → this repo's files | the discovery row carries a live bearer token as `zoom_token`; the Webhook Trigger's runData carries `x-enrichment-secret` and the caller IP |
| local process → ZoomInfo OAuth endpoint | task 2 mints real credentials from `.env` |
| committed workflow JSON → n8n Cloud | task 3 deploys a generated body to a live instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-11-01 | Information Disclosure | `zoom_token` on every discovery row, present in runData and in any pasted excerpt | high | mitigate | Task 3 forbids freezing this lane's runData and forbids pasting raw runData; only `search_diagnostics`, ids, names and titles may be quoted. Enforced by three grep gates on the UAT (`zoom_token`, `x-enrichment-secret`, the token prefix) and a clean-tree check on `tests/n8n/fixtures/frozen/`. |
| T-73.1-11-02 | Information Disclosure | task 2's verdict file recording token claims | high | mitigate | Only the two integer time claims are written; the grep gates assert zero token-prefix and zero `client_secret` occurrences in the verdict file. |
| T-73.1-11-03 | Spoofing | the ZoomInfo credential read from `.env` | medium | mitigate | Driven through an in-process dotenv scratchpad driver that never exports the credential into the shell and never echoes it; the driver lives outside the repo and is deleted with the session. |
| T-73.1-11-04 | Denial of Service | self-dispatch / execution runaway on a live deploy (CLAUDE.md §13.0.2's 2026-09-10 incident) | high | mitigate | `assert_no_self_dispatch` keeps passing; task 1's acceptance asserts zero `n8n-nodes-base.executeWorkflow` nodes; task 3 caps the round at 3 executions and deploys `--only` the discovery workflow. |
| T-73.1-11-05 | Elevation of Privilege | an unintended HubSpot write during the live round | high | mitigate | This lane has zero HubSpot nodes (asserted: zero nodes whose URL mentions `api.hubapi.com`) and zero `ALLOW_*` flags; nothing is armed, and the bounce script's flag read-back is an acceptance criterion. |
| T-73.1-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan: the claim decoder uses the stdlib (`base64`, `json`) and `probe_provider_discovery.py` already imports `requests`. Verified, not asserted — `git diff --quiet requirements.txt package.json` must exit 0 at plan end. No `## Package Legitimacy Audit` is required because no package manager is invoked. |
</threat_model>

<verification>
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/` — deterministic regeneration, nothing hand-edited.
- `node --test tests/n8n/*.test.mjs` — walker suite green including the discovery lane's 26-node, zero-stalled-Merge and v1 assertions.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` — Python suite green.
- `.venv/bin/python scripts/todo_triage.py` — every pending todo triaged per CLAUDE.md §31.
- `73.1-UAT.md` carries a dated gap-closure addendum with execution ids and zero secret material.

**Deployment state after this plan:** the discovery workflow `VJJBZ2oJ0079MSzG` is redeployed,
bounced, `active`, `executionOrder: v1`, and **disarmed**. The other five cloud workflows are
untouched. Nothing is armed anywhere.
</verification>

<success_criteria>
The discovery lane mints its own token every execution, so the `12668`/`12669` failure class cannot
recur. The repo carries offline evidence of whether a later mint invalidates an earlier token.
One live disarmed execution either returns a ZoomInfo 200 — closing D-11, and closing D-11a live
via `titles_dropped: 0` on the chosen families — or produces a reasoned account-side escalation
with its evidence recorded and triaged. No more than three executions spent; no secret written to
any file; nothing armed.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Note |
|---|---|---|
| `scripts/build_cloud_workflows.py::_discovery_zoom_search_gate_js` | new function | mint-per-execution gate; discovery lane only, enrich lane's gate untouched |
| `scripts/probe_provider_discovery.py --token-replay` | new CLI mode | mint A, mint B, replay A; zero n8n executions, zero credits |
| `73.1-TOKEN-REPLAY-VERDICT.json` | new artifact | key `prior_token_invalidated_by_later_mint`, 4-step status record, integer time claims only |
| `73.1-UAT.md` gap-closure addendum | appended section | execution ids, `search_diagnostics`, D-11 / D-11a verdicts |
| `tests/n8n/suggestDiscoveryLane.test.mjs` | 1 new test | node-scoped assertion on the token gate's jsCode |
| one `.planning/todos/pending/` entry | new todo | `kind: question` — enrich-lane cache consequence if the replay verdict is true |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-11-SUMMARY.md` when done.
</output>
