---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - operator-claude-plugin/config/role_vocabulary.yaml
  - operator-claude-plugin/tests/test_role_vocabulary.py
requirements: [D-11c]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11c: the role vocabulary gains the two families the 2026-09-18 pickleball round dropped — an Executive Officer family (Executive Officer, approximately CEO) and a Board Chair family (Board Chair / Chairwoman / Chairman, approximately President) — under the same `evidenced: false` / `source: generic_fallback` discipline the file already carries, so D-62-07 stays AMENDED and no member claims evidence it does not have."
    - "D-11c: the bare-grade-noun structural test is unchanged and still passes — `Executive Officer` is admitted as a two-token member, never the bare noun `officer`."
    - "D-11c: `role_classify.classify_title` is untouched code — contiguous-token, longest-wins, never fuzzy; this plan is a vocabulary diff plus tests only."
    - "D-11b: `classify_title` still gates every candidate title and stamps `role_family`; a title it cannot place is still dropped `role_not_selected`, so the new families widen what can be placed without widening how placing works."
  artifacts:
    - "operator-claude-plugin/config/role_vocabulary.yaml at version lv-role-vocabulary-v4 with the Executive Officer and Board Chair families"
    - "test_no_member_string_appears_in_two_families — the new structural guard against a member duplicated across families"
  key_links:
    - "role_vocabulary.yaml families -> role_classify.classify_title's longest-wins tie-break: a member duplicated across two families makes the winner depend on YAML order, which the classifier's own docstring promises it never does."
---

<objective>
Add the two role families the 2026-09-18 pickleball round dropped on the floor — Brendan Lee
(Executive Officer) and Jen Ramamurthy (Board Chairwoman) both failed `classify_title` and were
dropped `role_not_selected` while Liam Oke (Marketing & Communication Coordinator) classified.

Purpose: provider discovery is worthless if the classifier cannot place the titles AU sporting
bodies actually use. This is the cheapest half of the pickleball fix and has no dependency on
any other slice.

Output: two new families in `role_vocabulary.yaml` at `v4`, and a new structural guard that a
member string never appears in two families — the failure mode this specific addition invites.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@operator-claude-plugin/config/role_vocabulary.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the duplicate-member guard, seen RED by the naive addition</name>
  <files>operator-claude-plugin/tests/test_role_vocabulary.py, operator-claude-plugin/config/role_vocabulary.yaml</files>
  <read_first>
    - operator-claude-plugin/config/role_vocabulary.yaml in full — all 20 families and their members, especially the existing `Chair` family (members `Chairman`, `Chairperson`, `Deputy Chairman`, `Vice Chairman`) and `CEO` (member `CEO`)
    - operator-claude-plugin/scripts/role_classify.py `_tokenize`, `_contains_run` and `classify_title` — the longest-wins rule and its promise that "the result never depends on YAML ordering"
    - operator-claude-plugin/tests/test_role_vocabulary.py — `FORBIDDEN_BARE_GRADE_NOUNS` and `test_shipped_vocabulary_has_no_bare_grade_noun_members`, the structural test that must still pass
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-11c and § Specific Ideas (the three PA titles)
  </read_first>
  <behavior>
    - New test `test_no_member_string_appears_in_two_families`: tokenises every member of every family and asserts no tokenised member sequence occurs under two different labels. The failure message must name the offending member and both labels.
    - RED demonstration: `Chairman` is already a member of the existing `Chair` family. A naive reading of D-11c ("Board Chair / Chairwoman / Chairman") that copies `Chairman` into the new Board Chair family makes the new test fail, and makes `classify_title`'s longest-wins tie-break resolve on YAML order, which its own docstring says it never does.
  </behavior>
  <action>
Write `test_no_member_string_appears_in_two_families` in `operator-claude-plugin/tests/test_role_vocabulary.py` FIRST. It loads the shipped vocabulary through `role_classify.load_families()`, tokenises each member with `role_classify._tokenize`, and asserts no tokenised sequence appears under two labels, naming the member and both labels in the assertion message.

Then demonstrate the guard RED exactly as D-11c's literal wording invites: temporarily add a `Board Chair` family whose members include `Chairman` — a member the `Chair` family already carries — and run the test. Record the RED output in the SUMMARY. This is the RED-first proof the project rule requires for a guard that would otherwise pass on day one.

Remove the duplicate `Chairman` member and leave the file otherwise untouched by this task; Task 2 lands the real families. State in the SUMMARY that `Chairman` stays in the existing `Chair` family alone — it is already covered there, and D-11c's listing of it is a description of the family's SCOPE, not an instruction to duplicate the string.
  </action>
  <acceptance_criteria>
    - `test_no_member_string_appears_in_two_families` exists in `operator-claude-plugin/tests/test_role_vocabulary.py` and passes against the shipped vocabulary.
    - The test's assertion message names both the member and both family labels.
    - The SUMMARY records the RED output produced by the temporary duplicate `Chairman` member.
    - `operator-claude-plugin/config/role_vocabulary.yaml` is byte-identical to its pre-task state at the end of this task.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_role_vocabulary.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>git diff --quiet -- operator-claude-plugin/config/role_vocabulary.yaml; echo "VOCAB_UNCHANGED=$?"</automated>
    <fails_when>the echoed line is not exactly `VOCAB_UNCHANGED=0`</fails_when>
  </verify>
  <done>A member string duplicated across two families now fails a test, and that test has been seen failing.</done>
  <reversibility rating="reversible">A test file addition.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the Executive Officer and Board Chair families</name>
  <files>operator-claude-plugin/config/role_vocabulary.yaml, operator-claude-plugin/tests/test_role_vocabulary.py</files>
  <read_first>
    - operator-claude-plugin/config/role_vocabulary.yaml — the file header (`version`, `built_on`, `source: generic_fallback`, `evidenced: false`, `top_n`, `distinct_titles_sampled`) and the exact per-family key shape (`label`, `recurrence`, `evidenced`, `members`)
    - operator-claude-plugin/scripts/role_classify.py `chosen_families` — it validates the operator's selection against `label` strings, so labels are operator-visible text
    - operator-claude-plugin/tests/test_role_vocabulary.py — the existing classify assertions to copy the shape of (e.g. `test_double_encoded_title_classifies_to_its_real_shipped_family`)
  </read_first>
  <behavior>
    - `classify_title("Executive Officer", families)` returns `"Executive Officer"`.
    - `classify_title("Chief Executive Officer", families)` returns `"Executive Officer"` — the two-token member is the longest contiguous run present, and D-11c's own gloss is "Executive Officer approximately CEO", so this is the intended answer, pinned rather than left to chance.
    - `classify_title("Board Chairwoman", families)` returns `"Board Chair"`.
    - `classify_title("Chairwoman", families)` returns `"Board Chair"`.
    - `classify_title("Board Chair", families)` returns `"Board Chair"`.
    - `classify_title("Chairman", families)` still returns `"Chair"` — unchanged by this addition.
    - `classify_title("Marketing & Communication Coordinator", families)` still returns its existing family — the PA title that already worked is not disturbed.
    - `test_shipped_vocabulary_has_no_bare_grade_noun_members` still passes.
    - `test_no_member_string_appears_in_two_families` still passes.
  </behavior>
  <action>
Add two families to `operator-claude-plugin/config/role_vocabulary.yaml`, each carrying `recurrence: 0` and `evidenced: false` exactly like every existing family — the file's `source: generic_fallback` / `evidenced: false` discipline is what keeps D-62-07's AMENDED state true, and a family claiming evidence it does not have would break it.

Family one: `label: Executive Officer`, members `Executive Officer`. Two tokens, so the bare grade noun `officer` is never a member and the existing structural test stays green.

Family two: `label: Board Chair`, members `Board Chair`, `Board Chairwoman`, `Chairwoman`. Deliberately NOT `Chairman` or `Chairperson` — both already live in the existing `Chair` family, and Task 1's guard now fails on a duplicate. `Chairwoman` is the actual gap the pickleball round hit (Jen Ramamurthy, Board Chairwoman); `Chair` covers the masculine and neutral forms already.

Use human-readable `label` values, not D-11c's snake_case identifiers `executive_officer` / `board_chair`: `role_classify.chosen_families` validates the operator's once-per-batch selection against these labels and `role_classify`'s prompt builder prints them to the operator, so a label is operator-visible text and the file's whole existing convention is title case. Record that discretion call in the SUMMARY, naming D-11c's identifiers as the decision ids.

Bump the file's `version` from `lv-role-vocabulary-v3` to `lv-role-vocabulary-v4` and set `built_on` to the implementation date — the same discipline the file already uses whenever its shape changes materially. Leave `top_n`, `distinct_titles_sampled`, `source` and the top-level `evidenced` untouched.

Add the nine classify assertions listed in `<behavior>` to `operator-claude-plugin/tests/test_role_vocabulary.py`, written before the YAML change so the new-family ones are seen RED.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c '^- label: Executive Officer' operator-claude-plugin/config/role_vocabulary.yaml` returns 1.
    - `/usr/bin/grep -c '^- label: Board Chair' operator-claude-plugin/config/role_vocabulary.yaml` returns 1.
    - `/usr/bin/grep -c 'lv-role-vocabulary-v4' operator-claude-plugin/config/role_vocabulary.yaml` returns 1.
    - The Board Chair family's members are exactly `Board Chair`, `Board Chairwoman`, `Chairwoman` — it contains no `Chairman` and no `Chairperson`.
    - Both new families carry `evidenced: false` and `recurrence: 0`.
    - All nine classify assertions in `<behavior>` pass, including the three unchanged-behaviour ones.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_role_vocabulary.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>Brendan Lee's Executive Officer and Jen Ramamurthy's Board Chairwoman both classify, Liam Oke's title still classifies as before, and no member is duplicated across families.</done>
  <reversibility rating="reversible">A YAML diff plus tests; the classifier is untouched code.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role vocabulary YAML -> `classify_title` | A member string decides whether an unknown person's title is placed and therefore whether they enter the round at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-05 | Tampering | An over-broad member (a bare grade noun such as `officer`) matching nearly every person on a board page | high | mitigate | Both new families use multi-token members; the existing `test_shipped_vocabulary_has_no_bare_grade_noun_members` is re-run unchanged and must stay green. |
| T-73.1-06 | Repudiation | A member duplicated across families making the classified family depend on YAML order, so a round cannot be explained after the fact | medium | mitigate | `test_no_member_string_appears_in_two_families` (Task 1), seen RED first against a duplicate `Chairman`. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package; RESEARCH.md records no external packages are installed by this phase. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

- `role_vocabulary.yaml` family `Executive Officer` (members: `Executive Officer`)
- `role_vocabulary.yaml` family `Board Chair` (members: `Board Chair`, `Board Chairwoman`, `Chairwoman`)
- `role_vocabulary.yaml` `version: lv-role-vocabulary-v4`
- `test_no_member_string_appears_in_two_families` — new structural guard in `operator-claude-plugin/tests/test_role_vocabulary.py`
</artifacts_produced>

<verification>
- The full plugin suite is green.
- `role_classify.py` has no diff — this plan changes vocabulary, not code.
</verification>

<success_criteria>
- The three 2026-09-18 pickleball titles all classify: Executive Officer, Board Chairwoman, Marketing & Communication Coordinator.
- No member string appears under two labels, proved by a test that has been seen failing.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-02-SUMMARY.md` when done.
</output>
