# Phase 73.1: Provider-backed contact discovery as source tier 2 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-18
**Phase:** 73.1-provider-backed-contact-discovery-as-source-tier-2
**Areas discussed:** Sendability per tier, Lane shape + response, When discovery runs, Cost + what spends, Portal pin (UAT fold), Landing script (UAT fold), Session grant (UAT fold), Emailless create (UAT fold)

Before the areas were chosen the operator asked that four issues from the 2026-09-18
pickleball UAT be folded in: a HubSpot connector on the wrong portal, the `.get()` crash,
grants re-asked within one session, and (surfaced from the same log) a LinkedIn+mobile person
held for lack of an email. All four were accepted as areas; the landing script was later
parked (see below). One pending todo was folded (eligibility not reconstructable from runData).

---

## Sendability per tier

| Option | Description | Selected |
|--------|-------------|----------|
| 1-2 sendable, 3-4 held | Provider = current employment record; LinkedIn self-attested; industry historical. LinkedIn flips to held. | ✓ |
| 1-3 sendable, 4 held | Keeps LinkedIn sendable as today. | |
| 1-2 sendable, 3 if provider confirms | Two-source rule for LinkedIn. | |

**User's choice:** 1-2 sendable, 3-4 held.
**Follow-ups:** email-domain rule unchanged for provider rows (vs relax) — unchanged; dedupe page+provider — lowest rank wins, others noted (vs provider wins / you decide); title on disagreement — winning source's title (vs provider always / you decide).

---

## Lane shape + response

| Option | Description | Selected |
|--------|-------------|----------|
| New 6th workflow, own webhook | Read-only, no write gate, own execution count. | ✓ |
| Extend enrichment lane | Stamp discovered people on Build Response rows. | |
| Extend contact ingest lane | Discover mode on a lane that writes. | |

**User's choice:** New 6th workflow.
**Notes:** Operator asked whether a response body without `run_id` loses state in n8n on failure. Answer given: `run_id` is client-minted and lands in runData regardless of channel; the channel only decides how the plugin reads results; sync body has a webhook timeout risk. Reformulated options: Hybrid sync body + runData fallback (✓) / Ack + runData only / Sync body only. POST shape: one per round (✓) vs per company. Eligibility echo: plugin sends, lane echoes (✓) / lane re-reads HubSpot / you decide. After advisor review the operator widened the POST to every eligible company with a `gap` flag (✓) rather than gap-only with a partial fold.

---

## When discovery runs

| Option | Description | Selected |
|--------|-------------|----------|
| Ladder first, providers fill the gap | Providers only where ladder found fewer than cap. | ✓ |
| Providers always, in parallel | One POST for every eligible company. | |
| Providers only when ladder found zero | | |

**User's choice:** Ladder first, gap fill.
**Notes:** Waterfall stop rule — operator asked whether an existing enrich sequencing exists; found `enrichment.FULL_WATERFALL = [zoominfo, apollo, lusha]` and `provider_priority.yaml` default; operator chose that convention (✓) over ZoomInfo > Lusha > Apollo or cheapest-first. Role filtering — operator rejected "provider filter then local classifier always" as too conservative; chose provider filter first, unfiltered+local only on zero (✓), confirmed on re-ask. Whether rung-1 results still pass `role_classify`: "test with small sample before trusting" — recorded as classifier gates by default, live sample measured at UAT, switch is a later ruling. Result cap 10 (✓) vs 25 / you decide.

---

## Cost + what spends

| Option | Description | Selected |
|--------|-------------|----------|
| Search only; reveal stays in stage 2 | Lane spends 0 credits by design. | |
| Reveal in-lane | Lane reveals the capped people directly. | |

**User's choice:** Neither yet — "Test whether spend is required per provider for search first and validate then return test result and I make ruling." Recorded as a research-step probe with the ruling to follow.
**Follow-ups:** disclosure line shape — you decide (✓); unknown-rate provider — proceed, report after (✓) vs skip / ask per round; per-round ceiling — reuse `MAX_FALLBACK_SEARCHES=3` per company (✓) vs 3 × gap at grant / you decide.

---

## Portal pin (UAT fold)

| Option | Description | Selected |
|--------|-------------|----------|
| Never write; read only if portal matches | | |
| Never use any outside HubSpot tool | | |
| Allow writes if portal matches | Connector writes ok after portal proof. | ✓ |

**User's choice:** Allow writes if portal matches.
**Notes:** Proof — backend status echoes portalId (✓) vs config only / you decide. `hubspot_portal_id` — optional, guard only when set (✓) vs required. Placement — shared skill block + test (✓). Operator asked "Is this the whole grant thing?" when shown the connector-write follow-up; explained that connector writes bypass covers()/arming/written_records/association; operator chose: grant covers ALL write paths (✓) vs grant-checked only / portal match only. Advisor review added the outside-tool proof step (look up a known `hs_object_id`) since backend-status proves only the n8n credential's portal.

---

## Landing script (UAT fold) — PARKED

| Option | Description | Selected |
|--------|-------------|----------|
| Promote the skill block to a shipped script | `scripts/suggest_land.py` | |
| Skill rule only | | |
| Both | | |

**User's choice:** "We can skip this until it becomes repeated." Scope and test questions skipped likewise.
**Notes:** Operator asked whether the crash came from an improvised script and whether the skill would paper over it. Finding: the crash was in session-written `scratch/sc_land.py`; `SKILL.md:484-693` already ships the correct stage-2 block and the held routing; the session bypassed both. Deferred with trigger "repeat observed".

---

## Session grant (UAT fold)

| Option | Description | Selected |
|--------|-------------|----------|
| Whole session: all lanes, proposal's domains | | ✓ |
| Whole session, any domain | Blanket. | |
| Session, but per lane | | |

**User's choice:** Whole session, proposal's domains.
**Notes:** Uncovered domain mid-session — auto-widen, state it (✓) vs ask once / refuse. Operator asked "What is meant by triple open?" — explained the UAT's three `open_grant` calls (`session batch`, `PA landing`, `PA ingest`) where the first covered everything. Second `open_grant` while one is open — refused, open grant named (✓) vs allowed+warned. Ask wording — state scope + revocation (✓).

---

## Emailless create (UAT fold)

| Option | Description | Selected |
|--------|-------------|----------|
| Copy the enrichment lane's LinkedIn search idiom | Email first, then linkedin. | |
| Defer to a later phase | | |

**User's choice:** "Test ladder mechanism, using multiple properties i.e. email > linkedin > mobile (test if available) > other (test what is available)." Recorded as an identity ladder with a research-step searchable-property probe.
**Follow-ups:** sendable emailless rows — any row with phone or mobile too (✓) vs linkedin only; domain rule — applies only when an email exists (✓); landing — same as email rows (✓). After advisor review: `phone` = identity for CREATE only, never a match rung; `mobilephone` single-hit match (✓) vs phone matches single-hit / probe decides; identity groups = name + number (✓) vs bare number.

---

## Claude's Discretion

- Disclosure line wording and `cost_rates.json` key names.
- Tier-5 vs sentinel for "absence of a match".
- How the tier gate recognises provider records.
- Provenance `input` value and corroboration storage for provider hits.
- Webhook path, node names, sentinel/Merge layout; local replica for the discovery lane.
- Which search endpoints each provider exposes (researcher).

## Deferred Ideas

- Landing script promotion + offline e2e test (trigger: fault repeats).
- Trusting provider title match over `role_classify` (after live sample).
- Reveal-in-lane (after the spend probe and ruling).
- Correcting a company's recorded domain after a cross-host 301 (out of scope).
