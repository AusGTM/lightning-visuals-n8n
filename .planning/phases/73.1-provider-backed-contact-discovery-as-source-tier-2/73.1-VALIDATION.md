---
phase: "73.1"
slug: "provider-backed-contact-discovery-as-source-tier-2"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-18"
---

# Phase 73.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (`.venv/bin/python -m pytest`) + Node native test runner (`node --test`) |
| **Config file** | none at repo root; `tests/conftest.py` and `operator-claude-plugin/tests/conftest.py` provide autouse `no_network` / `no_durable_writes` fixtures |
| **Quick run command** | `.venv/bin/python -m pytest -q -k "search_fallback or suggest_contacts or role_vocabulary or extraction or cost_guard"` |
| **Full suite command** | `.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs` (glob form required — directory form broken on node 24) |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run the targeted `-k` pytest command for the files touched, plus `node --test tests/n8n/*.test.mjs` whenever `n8n/code/*.js` or `scripts/build_cloud_workflows.py` changes
- **After every plan wave:** Run `.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs`, then `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/` (builder determinism)
- **Before `/gsd-verify-work`:** Full suite must be green; builder determinism clean
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | — | D-01/D-05a | — | tier constants match allowlist tiers, all 4 constants | unit | `.venv/bin/python -m pytest -q -k test_tier_constants_match_allowlist` | ❌ W0 | ⬜ pending |
| TBD | — | — | D-05 | — | rank-2 unrelated email held; rank-3/4 never sendable | unit | `.venv/bin/python -m pytest -q -k test_the_tier_gate_and_the_waterfall_gate_are_independent_and_both_must_hold` | ✅ extend | ⬜ pending |
| TBD | — | — | D-11c | — | `Executive Officer`, `Board Chair`/`Chairwoman`/`Chairman` classify; no bare-grade-noun collision | unit | `.venv/bin/python -m pytest -q operator-claude-plugin/tests/test_role_vocabulary.py` | ✅ extend | ⬜ pending |
| TBD | — | — | D-16b | — | YAML/JS parity for 2 new name+number identity groups | unit | `node --test tests/n8n/columnMapIdentityParity.test.mjs` | ✅ extend | ⬜ pending |
| TBD | — | — | D-16a-i | — | mobilephone rung 0/1/>1 hit handling mirrors linkedin lane | unit | `node --test tests/n8n/*.test.mjs` (new file) | ❌ W0 | ⬜ pending |
| TBD | — | — | D-06/D-09 | — | discovery workflow walks clean under v1; echoes `num_associated_contacts` per eligible company; zero `executeWorkflow` nodes | walker | `node --test tests/n8n/*.test.mjs` (new file) | ❌ W0 | ⬜ pending |
| TBD | — | — | D-13 | — | unknown-rate provider proceeds, reports after; disclosure never renders unmeasured as `$0` | unit | `.venv/bin/python -m pytest -q -k cost_guard` | ✅ extend | ⬜ pending |
| TBD | — | — | D-12 | — | provider search endpoint existence/cost | manual (disarmed probe) | `scripts/probe_*_discovery.py` (one-shot, credit-spending) | ❌ new script | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Python test pinning all four tier constants against `config/source_allowlist.yaml` (no existing file covers this — grep zero hits)
- [ ] `.mjs` walker test for the new discovery workflow (no `n8n/wf_*discovery*.json` exists until `build_cloud_workflows.py` gains the builder)
- [ ] Read `operator-claude-plugin/tests/test_cost_guard.py` coverage of `SUGGESTION_RATE_KEY` / `suggestion_line` before extending for D-13

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Provider search endpoints exist and their per-result cost | D-12 | credit-spending live call, operator-ruled | run the one-shot probe script disarmed, record measured cost, operator rules on rate before discovery lane ships |
| Discovery lane end-to-end on n8n Cloud | phase gate | live disarmed proof, `blocking-human` | deploy + bounce disarmed (operator), one disarmed send, read runData; back-loaded to end-of-phase UAT |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
