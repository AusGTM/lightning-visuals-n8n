---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 04
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - operator-claude-plugin/config/crm_access_block.md
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/written_records.py
  - operator-claude-plugin/skills/backend-control/SKILL.md
  - operator-claude-plugin/skills/backend-status/SKILL.md
  - operator-claude-plugin/skills/backend-sweep/SKILL.md
  - operator-claude-plugin/skills/contact-upload/SKILL.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/skills/initialize/SKILL.md
  - operator-claude-plugin/skills/loss-reason-report/SKILL.md
  - operator-claude-plugin/skills/review-triage/SKILL.md
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - operator-claude-plugin/skills/suggestion-declines/SKILL.md
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_crm_access_block.py
  - operator-claude-plugin/tests/test_written_records.py
requirements: [D-14a, D-14b, D-14d, D-15a, D-15b, D-15c, D-15d]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-15a: the first yes covers the whole session — all lanes and the domains and ids named in that proposal — and every later send goes through `covers()` on that one grant, instead of the three separate grants the 2026-09-18 UAT session opened where the first already covered everything."
    - "D-15b: a send on a domain or id the open grant does not cover AUTO-WIDENS the grant and states the widening to the operator — no question asked, never a second grant — and the widened set is re-checked against the execution ceiling so a widening that breaches it still refuses in code, not in prose."
    - "D-15c: a second `open_grant` while one is open is REFUSED, naming the already-open grant's label — a structural stop for the triple-open the UAT session produced."
    - "D-15d: the ask states scope and revocation in one sentence from the shared block, and `CLOSED_REVOKED` stays the revoke path."
    - "D-14a: any HubSpot tool outside the plugin lanes — the claude.ai HubSpot connector, MCP, the `hs` CLI — may be used for reads AND writes only after THAT TOOL itself proves it is on portal 22617666 by looking up a record the plugin knows exists, and a zero-result lookup stops the step and says so."
    - "D-14a: the portal rule is SKILL-TEXT enforced by a grep test, never a runtime code gate and never a hook — CONTEXT.md forbids inventing one."
    - "D-14b: a connector write on the matching portal runs under the SAME session grant as every other write path — `write_grant.covers()` is checked before it, its outcome is appended to `written_records` with `source: connector`, and a created contact is associated to its company in the same step."
    - "D-14d: the CRM-access rule lives in ONE shared skill block included verbatim by every skill, pinned by a test that greps each SKILL.md for it."
  artifacts:
    - "operator-claude-plugin/config/crm_access_block.md — the single canonical CRM-access block"
    - "operator-claude-plugin/tests/test_crm_access_block.py — the D-14d grep test over all 11 SKILL.md files"
    - "write_grant.widen() and write_grant.SECOND_GRANT_REFUSED — the D-15b/D-15c mechanics"
    - "written_records entry key `source` — the D-14b connector attribution"
  key_links:
    - "open_grant(existing_grant=...) -> the skill's held grant variable: nothing persists a grant between sends, so the refusal can only work if every skill passes the grant it is holding — the grep test is what makes that call convention uniform."
    - "widen() -> ceiling_verdict: a widened record set costs more executions, and SAFE-01..05 require a ceiling to stay a refusal in code."
---

<objective>
Fold in the two governance failures the 2026-09-18 pickleball UAT surfaced: the session opened
three write grants where the first already covered everything (D-15), and a HubSpot connector on
a DIFFERENT portal returned zero results for contacts the plugin had just created (D-14).

Purpose: both are cross-cutting rules that every lane in this phase's new discovery round will
also be subject to. Landing them before the discovery lane exists means the new lane inherits
them rather than needing a retrofit.

Output: `open_grant` refuses a second open grant by name, a new `widen()` auto-widens an open
grant within the execution ceiling, one shared CRM-access block appears verbatim in all eleven
SKILL.md files, and `written_records` can attribute a write to the connector.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: one grant per session — refuse the second, widen the first</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py `open_grant` (line ~1296) in full, `close_grant`, `covers` (line ~1362) in full including its GRANT-03 refusal text and the Phase 61 REVIEW-11 comment about same-run creates, `ceiling_verdict` (line ~332), `allowance_headroom` (line ~222), the `CEILING_OK` / `CEILING_OVER` / `CEILING_UNKNOWN` constants, `CLOSED_REVOKED` and the CLOSE_REASONS block, and `plan_grant`'s returned proposal keys including `label`
    - operator-claude-plugin/tests/test_write_grant.py — especially `test_covers_admits_a_same_run_create_via_the_domain_named_at_grant_time`, the test whose behaviour must not change
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-15a, D-15b, D-15c, D-15d
    - .planning/ROADMAP.md § the SAFE-01..SAFE-05 binding paragraph (ceilings stay refusals in code, not prose)
  </read_first>
  <behavior>
    - Test 1: `open_grant(proposal, "yes", config, existing_grant=None)` behaves exactly as today — every existing test in `test_write_grant.py` passes unmodified.
    - Test 2: `open_grant(proposal, "yes", config, existing_grant=<an OPEN grant>)` returns a refusal whose message contains the open grant's `label` and the words `already open`, and instructs the caller to use `covers()` or widen. No grant is returned.
    - Test 3: `open_grant(proposal, "yes", config, existing_grant=<a CLOSED grant>)` opens normally — a closed grant is not an open one.
    - Test 4: `widen(grant, record_ids=[...], record_domains=[...], ceiling=None)` returns a grant whose `record_ids`/`record_domains` are the union of the old and new sets, deduplicated and order-stable, plus a `statement` naming exactly what was added.
    - Test 5: `widen` never mutates its input grant — the original's record sets are unchanged after the call.
    - Test 6: `widen(..., ceiling={"verdict": CEILING_OVER, ...})` returns a REFUSAL, not a widened grant, naming the ceiling as the reason.
    - Test 7: `widen(..., ceiling={"verdict": CEILING_UNKNOWN, ...})` widens — D-57-02 is explicit that an unknown verdict proceeds rather than refuses.
    - Test 8: `widen` refuses a grant that is not OPEN, and refuses when the caller passes a lane or workflow id (it takes neither) — widening is scoped to ids and domains only, because a lane or workflow mismatch is a different question and stays a hard `covers()` refusal.
    - Test 9: after `widen`, `covers(widened, lane=..., workflow_id=..., record_ids=<the new ids>, record_domains=<the new domains>)` returns `None`.
  </behavior>
  <action>
Add an `existing_grant=None` keyword to `write_grant.open_grant`. When it is a dict whose
`kind` is `KIND` and whose `state` is `OPEN`, return a refusal naming that grant's `label` and
saying it is already open, and pointing the caller at `covers()` or `widen()` — the D-15c
structural stop. Place the check AFTER the authority and confirmation checks so an unauthorised
or unconfirmed caller still gets the refusal it gets today, and BEFORE the proposal-shape check
so a caller holding an open grant is told about it even if their second proposal is malformed.
Name the refusal constant `SECOND_GRANT_REFUSED` and use it as a stable marker the skills and
the test can assert on. The keyword defaults to `None`, so every existing call site keeps
today's behaviour and no existing test changes.

Add `widen(grant, *, record_ids, record_domains, ceiling=None)` to the same module. It returns
either a NEW grant dict (a deep copy, never a mutation — the module's own `close_grant`
convention) with `record_ids` and `record_domains` set to the order-stable deduplicated union of
the old and new values and a `statement` string naming exactly which ids and domains were added,
or a refusal. It refuses when the grant is not a `KIND` dict, when its state is not `OPEN`, and
when `ceiling` carries `CEILING_OVER` — the last one is the load-bearing case: a widened record
set projects more executions, and the SAFE-01..05 binding says a ceiling stays a refusal in
code. `CEILING_UNKNOWN` and `CEILING_OK` both widen, because D-57-02 is explicit that an unknown
verdict proceeds. `widen` takes no `lane` and no `workflow_id` and must not accept one: a lane or
workflow mismatch is a scope question `covers()` already refuses on, and auto-widening one would
let a grant on one lane authorise arming another lane's workflow.

Update `covers()`'s ids/domains refusal text: it currently ends "widening it needs a new grant —
a grant's record set is what bounds it (GRANT-03)", which D-15b overrides. Rewrite that clause to
say the send is outside the grant's current record set and the caller is expected to widen the
open grant (naming `widen`) rather than open a second one, and that widening is stated to the
operator, not asked. Keep the refusal's `outside_record_ids` / `outside_record_domains` fields
exactly as they are — the skills key their widen call off them. Change NOTHING about the lane
and workflow_id refusal branches. Record in the SUMMARY that this is a deliberate amendment of
GRANT-03's "needs a new grant" clause by operator ruling D-15b, with GRANT-03's bounding
intention preserved by the ceiling re-check inside `widen`.

Write all nine tests in `operator-claude-plugin/tests/test_write_grant.py` first and record the
RED output.
  </action>
  <acceptance_criteria>
    - `write_grant.open_grant` accepts `existing_grant` as a keyword-only argument defaulting to `None`.
    - `write_grant.SECOND_GRANT_REFUSED` exists and appears in the second-open refusal's payload.
    - `write_grant.widen` exists with signature `(grant, *, record_ids, record_domains, ceiling=None)` and accepts no `lane` or `workflow_id`.
    - `widen` returns a deep copy; the input grant's `record_ids` list object is unchanged and not the same object as the output's.
    - `/usr/bin/grep -v '^ *#' operator-claude-plugin/scripts/write_grant.py | /usr/bin/grep -c 'widening it needs a new grant'` returns 0.
    - Tests 1–9 exist by name and pass; every pre-existing test in `test_write_grant.py` passes unmodified.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_write_grant_guardrails.py operator-claude-plugin/tests/test_write_grant_surface.py operator-claude-plugin/tests/test_headless_grant_boundary.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>A second grant cannot be opened while one is open, and a send outside the open grant's record set widens it within the ceiling instead of asking again.</done>
  <reversibility rating="costly">`covers()`'s refusal text is the operator-facing contract for GRANT-03 and is quoted in several skills; reverting means re-auditing every skill's grant step. Rated costly rather than reversible for that reason, not for the code.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one shared CRM-access block in all eleven skills, pinned by a grep test</name>
  <files>operator-claude-plugin/config/crm_access_block.md, operator-claude-plugin/tests/test_crm_access_block.py, operator-claude-plugin/skills/backend-control/SKILL.md, operator-claude-plugin/skills/backend-status/SKILL.md, operator-claude-plugin/skills/backend-sweep/SKILL.md, operator-claude-plugin/skills/contact-upload/SKILL.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/skills/initialize/SKILL.md, operator-claude-plugin/skills/loss-reason-report/SKILL.md, operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/skills/suggestion-declines/SKILL.md</files>
  <read_first>
    - all eleven `operator-claude-plugin/skills/*/SKILL.md` front-matter blocks and their first section, to choose one insertion point that reads naturally in every one of them
    - operator-claude-plugin/tests/test_mandatory_report_call_sites.py — the existing idiom for a test that greps every SKILL.md for a required string; copy its file-discovery and failure-message shape rather than inventing a second one
    - operator-claude-plugin/skills/contact-upload/SKILL.md around line 395 and enrich-records/SKILL.md around line 322 — the existing `open_grant` call sites whose convention the block must describe
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-14a, D-14b, D-14d, D-15d and § Specific Ideas (portal 22617666, contacts 353752261074 / 353732581840)
  </read_first>
  <behavior>
    - Test 1 `test_every_skill_carries_the_crm_access_block`: discovers every `operator-claude-plugin/skills/*/SKILL.md`, reads `operator-claude-plugin/config/crm_access_block.md`, and asserts every non-blank line of the shared file appears in each SKILL.md. The failure message names the skill and the first missing line.
    - Test 2 `test_the_shared_block_states_the_portal_rule`: asserts the shared file contains the portal id `22617666`, the word `stop`, and the phrase naming the lookup proof.
    - Test 3 `test_the_shared_block_states_the_session_grant_rule`: asserts it contains `covers(`, the word `widen`, the word `revoke`, and states the one-yes-per-session scope in one sentence.
    - Test 4 `test_no_skill_carries_a_second_copy_of_the_rule`: asserts the block's opening marker line appears exactly once per SKILL.md, so a future edit cannot leave two divergent copies.
    - Test 5 `test_the_shared_block_lives_outside_skills`: asserts the canonical block is at `operator-claude-plugin/config/crm_access_block.md` and that `operator-claude-plugin/skills/` contains no directory without a `SKILL.md` — the plugin loader enumerates skill directories, so a source file parked under `skills/` risks being read as a broken skill.
  </behavior>
  <action>
Markdown has no include mechanism, so D-14d's "one shared block" is implemented as one canonical
source file plus a grep test that every skill carries it verbatim — that is what D-14d's own
wording ("pinned by a test that greps each SKILL.md for it") describes.

Create `operator-claude-plugin/config/crm_access_block.md` holding the canonical block, opened and
closed by stable marker lines (an HTML comment naming the rule and its decision ids) so the test
can count occurrences. It goes in `config/`, NOT in `skills/_shared/`: `config/` already ships with
the plugin and is where every other cross-skill source lives (`column_mapping.yaml`,
`role_vocabulary.yaml`, `cost_rates.json`), whereas `skills/` is enumerated by directory and a
subdirectory with no `SKILL.md` is a shape the loader was never asked to tolerate. The block states, in operator-facing prose:

The portal rule (D-14a). Every HubSpot tool OUTSIDE this plugin's own lanes — the claude.ai
HubSpot connector, an MCP HubSpot server, the `hs` CLI — must prove IT is on portal `22617666`
before its first call in a session, read or write. The proof is that tool looking up, through
itself, a record this plugin knows exists: an `hs_object_id` from `written_records`, or the
configured sentinel id. Zero results means the wrong portal: stop the step and say so, naming the
portal the plugin expects and the record that was not found. Do not retry, do not fall back to a
plugin lane silently, and do not treat a zero result as "the record does not exist". Record that
the 2026-09-18 session's connector was on a different portal and returned zero results for
contacts the plugin had just created, which is the failure this rule exists to stop.

The connector-write rule (D-14b). A write through such a tool, once its portal is proven, is
under the SAME session grant as every other write path: call `write_grant.covers()` before it,
append the outcome to `written_records` with `source: connector`, and associate a created contact
to its company in the same step, exactly as the ingest lane does. One yes governs all paths.

The session-grant rule (D-15a/b/c/d). The first yes covers the whole session — every lane, and
the domains and ids named in that proposal. Every later send goes through `covers()` on that one
grant. A send outside its record set widens the grant with `widen()` and STATES the widening; it
never asks again and never opens a second grant. A second `open_grant` while one is open is
refused by the code, naming the open grant. The ask itself is one sentence: this yes covers every
send this session across the named lanes for the named domains, widening as new domains appear,
and the operator may say revoke at any time (`CLOSED_REVOKED`).

State explicitly inside the block that these are rules the assistant follows when reading the
skill — there is no runtime hook and no code gate enforcing the portal proof, and CONTEXT.md
D-14a forbids inventing one.

Insert the block verbatim into all eleven `SKILL.md` files at a consistent position: immediately
after the YAML front matter and before the skill's first section heading, so it is read before
any step runs. Do not reword it per skill — the test compares line by line.

Write `operator-claude-plugin/tests/test_crm_access_block.py` with the five tests above BEFORE
inserting the block into any skill, and record the RED output (eleven missing).
  </action>
  <acceptance_criteria>
    - `operator-claude-plugin/config/crm_access_block.md` exists, and `operator-claude-plugin/skills/` contains no directory lacking a `SKILL.md`.
    - `/usr/bin/grep -lc "22617666" operator-claude-plugin/skills/*/SKILL.md | wc -l` reports 11.
    - The block's opening marker line appears exactly once in each of the eleven SKILL.md files.
    - The block names `write_grant.covers()`, `widen`, `written_records`, `source: connector` and `CLOSED_REVOKED`.
    - The block states that the portal rule is skill text with no runtime hook.
    - Tests 1–5 exist by name in `operator-claude-plugin/tests/test_crm_access_block.py` and pass.
    - The SUMMARY records the RED output observed before insertion.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_crm_access_block.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py operator-claude-plugin/tests/test_plugin_manifest.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>/usr/bin/grep -c "22617666" operator-claude-plugin/skills/suggest-contacts/SKILL.md operator-claude-plugin/skills/initialize/SKILL.md operator-claude-plugin/skills/backend-sweep/SKILL.md</automated>
    <fails_when>any of the three lines reports a count of 0</fails_when>
  </verify>
  <done>Every skill carries the same CRM-access rule, and a test fails the moment one of them drifts or loses it.</done>
  <reversibility rating="reversible">Skill text plus a grep test.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: attribute a connector write in written_records</name>
  <files>operator-claude-plugin/scripts/written_records.py, operator-claude-plugin/tests/test_written_records.py</files>
  <read_first>
    - operator-claude-plugin/scripts/written_records.py — the two entry-building sites (around lines 352-372 and 435-455), the exact seven-key entry shape (`object_type`, `action`, `hs_object_id`, `outcome`, `reason`, `row_id`, `association`), the `_looks_forbidden` scan every key and value passes through, `ALL_OUTCOMES`, `ACTION_TO_OUTCOME` and `append_chunk`
    - operator-claude-plugin/tests/test_written_records.py — the existing entry-shape assertions, which will see an eighth key
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-14b and D-16d
  </read_first>
  <behavior>
    - Test 1: an entry built from a normal lane response carries `source` with the default value naming the backend lane, so the key is never absent and a reader never has to distinguish "no source" from "unknown source".
    - Test 2: an entry built with `source="connector"` carries it verbatim and every other key is unchanged.
    - Test 3: `source` is validated against a closed vocabulary — an unknown value raises `WrittenRecordsError` rather than being stored, matching the module's existing register for a value nobody can report on.
    - Test 4: the new key passes the `_looks_forbidden` scan like every other key and value.
    - Test 5: a durable document written before this change (seven keys, no `source`) still loads without error and reads as the default source — old documents are not invalidated.
  </behavior>
  <action>
Add `source` as an eighth key on the `written_records` entry shape at BOTH entry-building sites.
Define a closed vocabulary constant — `SOURCE_LANE` for a write that went through a plugin
backend lane (the default for every existing call site, so no caller changes) and
`SOURCE_CONNECTOR` for a write made through an outside HubSpot tool under D-14b — plus an
`ALL_SOURCES` frozenset, following the module's own existing `ALL_OUTCOMES` pattern exactly. An
unrecognised value raises `WrittenRecordsError` in the same register the module already uses for
a close reason nobody can report on.

Thread the value through as a keyword defaulting to `SOURCE_LANE` so every existing call site is
byte-identical in behaviour. Run the new key and value through the existing `_looks_forbidden`
scan along with the other seven — do not exempt it.

Make the loader tolerant of a seven-key entry: a document written before this change has no
`source` and must read as `SOURCE_LANE`, not raise and not read as unknown. There is no version
marker on these documents, so tolerance is the only available answer; state that in the SUMMARY.

Write the five tests first and record the RED output.
  </action>
  <acceptance_criteria>
    - `written_records.SOURCE_LANE`, `written_records.SOURCE_CONNECTOR` and `written_records.ALL_SOURCES` exist.
    - Both entry-building sites produce a `source` key; an entry built with no explicit source carries `SOURCE_LANE`.
    - An entry built with an unrecognised source raises `WrittenRecordsError`.
    - A seven-key entry loaded from a pre-change document reads as `SOURCE_LANE`.
    - Tests 1–5 exist by name and pass.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_written_records.py operator-claude-plugin/tests/test_run_report.py operator-claude-plugin/tests/test_run_manifest.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>A write made through the HubSpot connector can be recorded and reported as such, and an old document still reads.</done>
  <reversibility rating="costly">`written_records` documents are durable and already written on disk from prior rounds; adding a key is additive but the loader tolerance is a compatibility contract that a revert would break for any document written in the meantime.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outside HubSpot tool (connector / MCP / `hs` CLI) -> the operator's CRM | A tool the plugin does not control performs reads and writes against what may be a different portal entirely. |
| operator's one yes -> every later send in the session | A single authorisation now governs sends whose record sets were not all known when it was given. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-10 | Spoofing | An outside HubSpot tool silently on the wrong portal, so reads return zero and writes land in a stranger's CRM | high | mitigate | D-14a's per-tool lookup proof against a known `hs_object_id`, stated in the shared block in every skill and pinned by `test_crm_access_block.py`. Observed live 2026-09-18. |
| T-73.1-11 | Elevation of Privilege | A connector write bypassing the session grant because it is "not a lane" | high | mitigate | D-14b: the shared block requires `covers()` before any connector write and a `written_records` entry with `source: connector` after it; Task 3 makes that attribution representable. |
| T-73.1-12 | Elevation of Privilege | Auto-widening a grant onto a lane or workflow the operator never authorised | high | mitigate | `widen()` accepts neither `lane` nor `workflow_id`; those stay hard `covers()` refusals. Task 1 Test 8 pins it. |
| T-73.1-13 | Denial of Service | An auto-widened grant pushing the batch past the monthly execution ceiling with no refusal | high | mitigate | `widen(ceiling=...)` refuses on `CEILING_OVER`, keeping SAFE-01..05's "ceilings stay refusals in code" true; Test 6 pins it. |
| T-73.1-14 | Repudiation | A write that cannot be attributed to the path that made it | medium | mitigate | Closed `source` vocabulary on the `written_records` entry, validated on write. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 04)

- `operator-claude-plugin/config/crm_access_block.md` — new canonical block file
- `operator-claude-plugin/tests/test_crm_access_block.py` — new test, five tests
- `write_grant.open_grant(..., existing_grant=None)` — new keyword-only argument
- `write_grant.SECOND_GRANT_REFUSED` — new refusal marker constant
- `write_grant.widen(grant, *, record_ids, record_domains, ceiling=None)` — new function
- `written_records.SOURCE_LANE`, `written_records.SOURCE_CONNECTOR`, `written_records.ALL_SOURCES` — new constants
- `written_records` entry key `source` — new eighth key
</artifacts_produced>

<verification>
- The full plugin suite is green.
- All eleven SKILL.md files carry the shared block verbatim, proved line by line.
- No runtime hook was added for the portal rule.
</verification>

<success_criteria>
- A second `open_grant` while one is open is refused by name.
- A send outside the open grant's record set widens it and states the widening, unless the ceiling refuses.
- Every skill carries the same portal and session-grant rule.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-04-SUMMARY.md` when done.
</output>
