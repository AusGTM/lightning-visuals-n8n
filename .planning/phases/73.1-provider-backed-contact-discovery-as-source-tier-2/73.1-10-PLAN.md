---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 10
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - n8n/code/discoverySearch.js
  - scripts/build_cloud_workflows.py
  - n8n/wf_suggest_discovery_cloud.json
  - tests/n8n/discoverySearch.test.mjs
  - tests/n8n/suggestDiscoveryLane.test.mjs
autonomous: true
requirements: [D-11a, D-11c]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11a: rung 1's provider-side title filter is built from the round's CHOSEN role families' member strings, so an operator-selected family is what narrows the search."
    - "D-11c's four member strings — Executive Officer, Board Chair, Board Chairwoman, Chairwoman — survive into rung 1's jobTitle body whenever their families are in the round's selection (they are dropped today by the greedy 500-char fold over the full build-time vocabulary)."
  artifacts:
    - "n8n/code/discoverySearch.js::titlesForFamilies — pure family-map filter, exported"
    - "scripts/build_cloud_workflows.py::_discovery_role_family_map — label→members map, replaces _discovery_role_titles"
    - "n8n/wf_suggest_discovery_cloud.json — regenerated, rung-1 leaf filters at runtime"
    - "tests/n8n/discoverySearch.test.mjs — D-11c survival test"
    - "tests/n8n/suggestDiscoveryLane.test.mjs — generated-artifact assertion on the rung-1 node"
  key_links:
    - "SKILL.md's discovery_body role_families → Parse Discovery Request row → ZoomInfo Search Rung1 → buildRequest jobTitle"
---

<objective>
Close Gap 2 (D-11a). `role_families` is ALREADY on the wire — `operator-claude-plugin/skills/suggest-contacts/SKILL.md`
line 458 puts `"role_families": chosen_families` on every company in the D-08 envelope. The lane
throws it away: `ENRICH_PARSE_DISCOVERY_REQUEST` never carries it onto the row, and
`_discovery_zoom_search_leaf_js("rung1")` inlines the FULL build-time vocabulary, which
`capRoleTitles` then folds greedily to 500 chars, dropping the last 14 of 42 titles — exactly
D-11c's four new members, the roles this phase exists to catch.

Purpose: make D-11a true by threading the round's selection through to the rung-1 body, so the
cap operates on the operator's chosen subset (normally far under 500 chars) instead of silently
truncating the full vocabulary.
Output: a runtime family filter in the shared pure module, a label→members map emitted by the
builder, a regenerated workflow, and two tests that pin D-11c's members.

**No plugin change.** The plugin already sends the field; this is n8n-side only, so
`operator-claude-plugin/.claude-plugin/plugin.json` stays at `0.51.1` and no CHANGELOG entry is
needed. Task 2's acceptance criteria prove that rather than asserting it.

**Spec-less probe fallback skipped (visible, recorded):** this phase has no requirement IDs to
probe and no `-SPEC.md`; plans are keyed on `73.1-CONTEXT.md` decision ids.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-VERIFICATION.md
@n8n/code/discoverySearch.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thread role_families end-to-end — chosen families narrow rung 1</name>
  <files>n8n/code/discoverySearch.js, scripts/build_cloud_workflows.py, n8n/wf_suggest_discovery_cloud.json</files>
  <read_first>
    - `n8n/code/discoverySearch.js` (whole file, 158 lines) — `capRoleTitles`, `buildRequest`, `ZOOMINFO_JOBTITLE_MAX`, the `module.exports` list
    - `scripts/build_cloud_workflows.py` lines 11990-12135 — `_discovery_role_titles`, `ENRICH_PARSE_DISCOVERY_REQUEST`, `_discovery_zoom_search_leaf_js`
    - `operator-claude-plugin/config/role_vocabulary.yaml` — the 21 families; the last two (`Executive Officer`, `Board Chair`) carry D-11c's four members
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md` lines 447-465 — the envelope that already carries `role_families` (family LABELS, from `role_classify.chosen_families`, operator order preserved)
    - `operator-claude-plugin/scripts/role_classify.py` lines 106-119 — `chosen_families` validates labels and returns `list(labels)`
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md` — D-11a, D-11c
  </read_first>
  <behavior>
    - `titlesForFamilies(map, ["Executive Officer", "Board Chair"])` returns exactly the 4 member strings of those two families, in chosen order, deduped.
    - `titlesForFamilies(map, [])` and `titlesForFamilies(map, null)` return every member in map order — the current build-time behaviour, preserved as the fallback for a caller that omits the field.
    - A label not present in the map contributes nothing and does not throw (the plugin already validates labels; the lane must not be the second validator).
    - `buildRequest("zoominfo", {domain, roleTitles: <the 4 D-11c members>})` yields a `jobTitle` containing all four strings and `capRoleTitles(...).dropped === 0`.
    - Rung 2's body is unchanged: no `jobTitle` key at all.
  </behavior>
  <action>
Four edits, one slice, module → builder → generated artifact.

1. `n8n/code/discoverySearch.js`: add a pure `titlesForFamilies(familyMap, chosen)` beside
`capRoleTitles`. `familyMap` is a plain object of label → array of member strings. When `chosen`
is a non-empty array, walk it in order and append each named family's members, skipping a label
the map does not carry and skipping a member already collected; otherwise return every member in
map insertion order. Export it in the existing `module.exports` list. Keep it dependency-free —
this file is `inline()`d verbatim into Code nodes.

2. `scripts/build_cloud_workflows.py`: replace `_discovery_role_titles()` (line ~12005) with
`_discovery_role_family_map()` returning an ordered dict of `family["label"] → family["members"]`
read from `operator-claude-plugin/config/role_vocabulary.yaml` at build time. Delete the old
function — it has exactly one caller. Update its docstring: the map is build-time config, the
SELECTION is per-request, which is what D-11a asked for; record that the 73.1-07 discretion this
replaces is now retired.

3. `ENRICH_PARSE_DISCOVERY_REQUEST`: carry the field onto the row with the envelope-plus-
per-company fallback idiom this wrapper already uses for `run_id` and `per_company_cap` — add an
`ENVELOPE_ROLE_FAMILIES` const read off the envelope and emit
`role_families: (c && c.role_families) ?? ENVELOPE_ROLE_FAMILIES ?? null` in the returned json.

4. `_discovery_zoom_search_leaf_js(rung)`: for rung 1, emit the family map as a
`ROLE_FAMILY_MAP` const (`json.dumps` of `_discovery_role_family_map()`) instead of the flat
`ROLE_TITLES` list, and compute the titles PER ROW inside the existing loop —
`titlesForFamilies(ROLE_FAMILY_MAP, row.role_families)` — then `capRoleTitles` that result and
pass it as `roleTitles` to `buildRequest`. The `_titleCap` computation currently sits outside the
loop; it is per-row now, so move it in, keeping the existing `__USED_KEY__`/`__DROPPED_KEY__`
stamps pointing at that row's own cap result. Rung 2 keeps its empty-list short-circuit exactly
as today — an empty map yields an empty title list and `buildRequest` emits no title-filter key.

Regenerate with `scripts/build_cloud_workflows.py`. Do not hand-edit `n8n/wf_suggest_discovery_cloud.json`.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/ && echo REGEN_CLEAN</automated>
    <fails_when>non-zero exit, or "REGEN_CLEAN" absent from the output (a second regeneration produced a different file — the builder is not deterministic)</fails_when>
    <automated>node --test tests/n8n/discoverySearch.test.mjs tests/n8n/suggestDiscoveryLane.test.mjs</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "fail 0"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node -e 'const m=require("./n8n/code/discoverySearch.js"); console.log(typeof m.titlesForFamilies)'` prints `function`
    - `/usr/bin/grep -c '_discovery_role_titles' scripts/build_cloud_workflows.py` returns `0`
    - `/usr/bin/grep -c '_discovery_role_family_map' scripts/build_cloud_workflows.py` returns a value `>= 2` (definition plus at least one call site)
    - In `n8n/wf_suggest_discovery_cloud.json`, the node named `Parse Discovery Request` has `parameters.jsCode` containing the string `role_families`
    - In `n8n/wf_suggest_discovery_cloud.json`, the node named `ZoomInfo Search Rung1` has `parameters.jsCode` containing all of `ROLE_FAMILY_MAP`, `titlesForFamilies`, `row.role_families`, `Board Chairwoman`, `Executive Officer`
    - In `n8n/wf_suggest_discovery_cloud.json`, the node named `ZoomInfo Search Rung2` has `parameters.jsCode` containing `ROLE_FAMILY_MAP` set to an empty object (rung 2 stays unfiltered)
    - `n8n/wf_suggest_discovery_cloud.json` still has exactly 26 nodes and `settings.executionOrder` equal to `"v1"`
  </acceptance_criteria>
  <done>A round carrying `role_families: ["Executive Officer", "Board Chair"]` produces a rung-1 jobTitle built from exactly those two families' members, and the generated workflow proves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin D-11c's survival and the fallback, then run the full suites</name>
  <files>tests/n8n/discoverySearch.test.mjs, tests/n8n/suggestDiscoveryLane.test.mjs</files>
  <read_first>
    - `tests/n8n/discoverySearch.test.mjs` (137 lines) — existing `capRoleTitles` arithmetic tests and the "unfiltered body must carry no title-filter key" test
    - `tests/n8n/suggestDiscoveryLane.test.mjs` lines 17-70 — `loadWf()`, `zeroPeopleCodeStubs()`, `run()`; note the leaves are STUBBED, so the walker never executes the real filter — the generated-artifact assertion is the check that matters in this file
    - `n8n/code/discoverySearch.js` after Task 1
    - `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-VERIFICATION.md` — Gap 2's `missing` list asks for exactly this test
  </read_first>
  <behavior>
    - Test: `titlesForFamilies` over the SHIPPED `role_vocabulary.yaml` with `["Executive Officer", "Board Chair"]` yields a list whose `capRoleTitles(list, 500)` reports `dropped === 0`, and `buildRequest("zoominfo", {domain, roleTitles: list}).data.attributes.jobTitle` contains each of `Executive Officer`, `Board Chair`, `Board Chairwoman`, `Chairwoman`.
    - Test: an absent/empty selection falls back to every member in map order — pinned as a deliberate, visible choice, not an accident.
    - Test: an unknown label contributes nothing and does not throw.
    - Test (lane file): the generated `ZoomInfo Search Rung1` node reads `role_families` off the row and carries the family map.
  </behavior>
  <action>
Add the tests above to `tests/n8n/discoverySearch.test.mjs`, loading the real shipped vocabulary
from `operator-claude-plugin/config/role_vocabulary.yaml` rather than a hand-written fixture — the
regression this closes is specifically that the SHIPPED vocabulary's last-declared families get
cut, so a fixture would not catch a reordering regression. Parse the YAML without adding a
dependency: the file is flat enough that a small reader over the `- label:` / `  - ` lines is
enough, or reuse whatever this suite's siblings already do for YAML (check
`tests/n8n/columnMapIdentityParity.test.mjs` first — if it already has a reader, reuse it rather
than writing a second one).

Add the generated-artifact assertion to `tests/n8n/suggestDiscoveryLane.test.mjs`: load the
workflow with the file's existing `loadWf()`, find the node named `ZoomInfo Search Rung1`, and
assert on its `parameters.jsCode` — node-scoped, never a whole-file grep (the vocabulary strings
legitimately appear in more than one node).

Then run both full suites and the builder determinism check. Do not modify any test under
`operator-claude-plugin/tests/` — no plugin file changes in this plan.
  </action>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "fail 0"</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider; echo "EXIT=$?"</automated>
    <fails_when>the output does not end with "EXIT=0", or the summary line reports any count under "failed" or "error"</fails_when>
    <automated>git diff --quiet operator-claude-plugin/ && echo PLUGIN_UNTOUCHED</automated>
    <fails_when>"PLUGIN_UNTOUCHED" absent from the output — a plugin file changed, which would require a plugin.json version bump and a CHANGELOG entry this plan deliberately does not make</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'Board Chairwoman' tests/n8n/discoverySearch.test.mjs` returns a value `>= 1`
    - `/usr/bin/grep -c 'titlesForFamilies' tests/n8n/discoverySearch.test.mjs` returns a value `>= 3`
    - `/usr/bin/grep -c 'ZoomInfo Search Rung1' tests/n8n/suggestDiscoveryLane.test.mjs` returns a value `>= 1`
    - `node --test tests/n8n/*.test.mjs` exits 0 with `fail 0`
    - `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` exits 0
    - `git status --porcelain operator-claude-plugin/` prints nothing
    - `operator-claude-plugin/.claude-plugin/plugin.json` still reads version `0.51.1`
    - `git diff --quiet requirements.txt package.json` exits 0 — no dependency was added (T-73.1-10-SC)
  </acceptance_criteria>
  <done>The four D-11c member strings are pinned by a test that reads the shipped vocabulary; the full n8n and Python suites are green; the plugin is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator round → discovery webhook | `role_families` is now operator-influenced input reaching a provider request body |
| generated workflow JSON → git | build-time content is committed and later deployed to n8n Cloud |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-10-01 | Tampering | `role_families` from the request body reaching ZoomInfo's `jobTitle` | medium | mitigate | `titlesForFamilies` is a lookup, never a pass-through: a label selects members from the build-time map or contributes nothing. An arbitrary string from the request can never reach the provider body. Pinned by the unknown-label test in Task 2. |
| T-73.1-10-02 | Denial of Service | oversized `role_families` inflating the request | low | mitigate | `capRoleTitles(…, ZOOMINFO_JOBTITLE_MAX)` still bounds the joined string below 500 chars after filtering — the cap is retained, not removed. |
| T-73.1-10-03 | Information Disclosure | the role vocabulary embedded in the committed workflow JSON | low | accept | Already the case before this plan; the vocabulary is non-sensitive generic job titles under `evidenced: false`. |
| T-73.1-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan: the YAML reader reuses the existing suite helper or the stdlib, and `discoverySearch.js` stays dependency-free by contract (it is `inline()`d into Code nodes, which cannot resolve a module). Verified, not asserted — `git diff --quiet requirements.txt package.json` must exit 0 at plan end. No `## Package Legitimacy Audit` is required because no package manager is invoked. |
</threat_model>

<verification>
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/` — regeneration is deterministic and the committed JSON matches.
- `node --test tests/n8n/*.test.mjs` — walker suite green, including the discovery lane's zero-stalled-Merge and 26-node assertions.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` — Python suite green.
- `git status --porcelain operator-claude-plugin/` empty — the no-bump claim is proven, not asserted.
</verification>

<success_criteria>
D-11a is true: rung 1's `jobTitle` is built from the round's chosen family labels. D-11c's four
member strings reach the provider body under a selection naming their families, pinned by a test
that reads the shipped vocabulary. Zero plugin changes. 26 nodes, `executionOrder: v1`, zero write
nodes, zero `executeWorkflow` nodes — all unchanged.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Note |
|---|---|---|
| `n8n/code/discoverySearch.js::titlesForFamilies` | new exported function | pure; family-map filter, chosen order, deduped |
| `scripts/build_cloud_workflows.py::_discovery_role_family_map` | new function | replaces `_discovery_role_titles` (deleted) |
| `ROLE_FAMILY_MAP` | new const in the generated rung-1 leaf | build-time label→members map |
| `role_families` | new key on the discovery row | emitted by `Parse Discovery Request` |
| `_zoominfo_rung1_titles_used` / `_titles_dropped` | existing keys, now per-row | were computed once per node, now once per company |
| `tests/n8n/discoverySearch.test.mjs` | 3 new tests | D-11c survival, fallback, unknown label |
| `tests/n8n/suggestDiscoveryLane.test.mjs` | 1 new test | node-scoped generated-artifact assertion |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-10-SUMMARY.md` when done.
</output>
