---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - operator-claude-plugin/config/cost_rates.json
  - operator-claude-plugin/scripts/cost_guard.py
  - operator-claude-plugin/tests/test_cost_guard.py
requirements: [D-13, D-13a]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-13: `cost_rates.json` gains one search-rate key per provider — `zoominfo_contact_search`, `apollo_people_search`, `lusha_prospecting_search` — each shipping `value: null` with its own dated citation, because no provider search call has ever been made in this repository and a guess would read as a measurement."
    - "D-13: a provider whose search rate is `null` at round time PROCEEDS and reports the measured cost after, overriding the roadmap's states-cost-before-spending line for that provider only; a provider whose rate is known is disclosed up front."
    - "D-13: an unknown rate renders as unknown and never as zero — `null` means genuinely unknown and `0` means a real measured zero, and the two stay different answers exactly as `cost_rates.json`'s own note already requires."
    - "D-13a: the discovery disclosure line shows the gap-company count, each provider's search rate or the word unknown, the one n8n execution the round costs, and the stage-2 reveal ceiling computed as the contact rate × (per-company cap × gap companies)."
    - "D-12: the disclosure is written for a SEARCH-ONLY lane — zero credits are attributed to the discovery lane itself and the reveal ceiling stays attributed to stage 2's existing enrich path — per CONTEXT.md's own stated assumption pending the operator's ruling on the plan-time probe."
  artifacts:
    - "cost_guard.DISCOVERY_SEARCH_RATE_KEYS — the provider-name to rate-key map"
    - "cost_guard.discovery_line(gap_company_count, per_company_cap, rates) — the D-13a disclosure builder"
    - "three new null-valued entries in operator-claude-plugin/config/cost_rates.json"
  key_links:
    - "cost_rates.json null value -> _rate_entry -> discovery_line's `unmeasured` branch: the whole never-render-unknown-as-zero rule lives in that one branch and is what D-13 turns on."
    - "discovery_line's stage-2 ceiling -> SUGGESTION_STAGE2_RATE_KEY: the reveal ceiling must use the SAME contacts rate suggestion_line already uses, never a second number for the same spend."
---

<objective>
Price the discovery round: three new per-provider search-rate keys that ship honestly unknown,
and one disclosure builder that shows the gap-company count, each provider's rate or "unknown",
the single n8n execution, and the stage-2 reveal ceiling (D-13/D-13a).

Purpose: the operator is being asked to authorise a round that spends on endpoints nobody in
this repo has ever called. D-13 permits proceeding with an unknown rate — it does not permit
implying the rate is zero, which is exactly what a missing key would do.

Output: `cost_rates.json` with three `null` search rates carrying their own citations, and
`cost_guard.discovery_line()` following the existing tri-state `research_line`/`suggestion_line`
convention rather than inventing a second vocabulary for the same idea.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@operator-claude-plugin/config/cost_rates.json
@operator-claude-plugin/scripts/cost_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: three honestly-unknown search rates</name>
  <files>operator-claude-plugin/config/cost_rates.json, operator-claude-plugin/scripts/cost_guard.py</files>
  <read_first>
    - operator-claude-plugin/config/cost_rates.json in full — the top-level `note` stating that `null` means genuinely unknown and `0` means a real measured zero, and the exact five-key entry shape (`value`, `unit`, `citation`, `confidence`)
    - operator-claude-plugin/scripts/cost_guard.py lines 38-90 — `PROVIDERS`, `PROVIDER_RATE_KEYS`, `ANTHROPIC_RATE_KEY`, `RESEARCH_RATE_KEY`, `SUGGESTION_RATE_KEY`, `SUGGESTION_STAGE2_RATE_KEY`, `MAX_FETCHES_PER_COMPANY`, `load_rates`, `_rate_entry`
    - the existing `company_domain_research` and `suggestion_stage1_discovery` entries — both ship `value: null` with a citation that explains WHY no figure exists and forbids scaling another rate to fill it in; copy that register
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-12 and D-13
  </read_first>
  <action>
Add three entries to `operator-claude-plugin/config/cost_rates.json`'s `rates` object, keyed
`zoominfo_contact_search`, `apollo_people_search` and `lusha_prospecting_search`. Each carries
`value: null`, `unit: "credits/search"`, a `citation` and a `confidence`. Key names are Claude's
discretion under D-13a and are chosen to name the provider and the endpoint class, matching the
existing `<provider>_<thing>` convention of `lusha_companies_match` and `zoominfo_per_match`.

Each citation must say, in the same register as `suggestion_stage1_discovery`'s: that no
provider search-by-role endpoint has ever been called in this repository; that the endpoint
itself is `[ASSUMED]` from vendor documentation and has not been probed on this account; that
the figure is to be measured by the D-12 plan-time probe (name `scripts/probe_provider_discovery.py`)
and the operator's ruling; and that another rate must NOT be scaled to fill it in. Apollo's
citation must additionally record that Apollo's balance is unreadable on this account (403 on
the credit endpoint, per `cost_rates.json`'s existing `apollo_per_match` citation), so its
search can only ever be priced from vendor documentation, never from a before/after balance
read. Each `confidence` value is the string `unknown -- no provider search endpoint has been called on this account; do not substitute a guess`.

Do NOT change the top-level `version` or `measured_on` — no figure was measured by this task,
and bumping `measured_on` would claim one was.

In `operator-claude-plugin/scripts/cost_guard.py`, add `DISCOVERY_SEARCH_RATE_KEYS` as an
ordered mapping from provider name to rate key in the D-11 waterfall order — `zoominfo`, then
`apollo`, then `lusha` — with a comment stating that the order is the waterfall's own
(`enrichment.FULL_WATERFALL` / `config/provider_priority.yaml` default) and is not re-derived
here. Add no other code in this task.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import json;d=json.load(open('operator-claude-plugin/config/cost_rates.json'))['rates'];print([k for k in ('zoominfo_contact_search','apollo_people_search','lusha_prospecting_search') if d.get(k,{}).get('value',0) is None])"` prints all three key names.
    - Each of the three new entries has all four keys `value`, `unit`, `citation`, `confidence`.
    - Each new `citation` names `scripts/probe_provider_discovery.py` and contains the words `do not` (the no-scaling prohibition).
    - The Apollo entry's citation records that Apollo's balance is unreadable on this account.
    - `cost_guard.DISCOVERY_SEARCH_RATE_KEYS` lists providers in the order `zoominfo`, `apollo`, `lusha`.
    - `/usr/bin/grep -c '"measured_on": "2026-07-30"' operator-claude-plugin/config/cost_rates.json` returns 1 — unchanged.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_cost_guard.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -c "import json; json.load(open('operator-claude-plugin/config/cost_rates.json')); print('JSON_OK')"</automated>
    <fails_when>the output is not exactly `JSON_OK`, or the command exits non-zero</fails_when>
  </verify>
  <done>Three provider search rates exist, all genuinely unknown, each explaining why and forbidding a substituted guess.</done>
  <reversibility rating="reversible">Config keys plus one constant; no consumer exists yet.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: discovery_line — gap count, per-provider rate or unknown, one execution, stage-2 ceiling</name>
  <files>operator-claude-plugin/scripts/cost_guard.py, operator-claude-plugin/tests/test_cost_guard.py</files>
  <read_first>
    - operator-claude-plugin/scripts/cost_guard.py `research_line` and `suggestion_line` in full — the tri-state `no_rows` / `unmeasured` / `measured` branch order, the returned dict's key set, and `suggestion_line`'s rule that the rendered line must name BOTH components and label the figure a ceiling
    - operator-claude-plugin/tests/test_cost_guard.py — the existing `suggestion_line` and `research_line` tests, for the assertion style to copy
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-13, D-13a and D-12's search-only assumption
    - operator-claude-plugin/tests/test_report_enrichment.py — the D-10b guard banning the word `tier` from operator-visible strings
  </read_first>
  <behavior>
    - Test 1 zero gap companies: `discovery_line(0, 2, rates)` returns `state: "no_rows"`, `cost` figures `None`, and a line saying no company needs provider discovery. Checked FIRST, before any rate is consulted — the same branch order `research_line` uses.
    - Test 2 all rates unknown (the shipped state): `discovery_line(6, 2, rates)` returns `state: "unmeasured"`, `known: False`, a `providers` list of three entries each with `rate: None` and `state: "unmeasured"`, `executions: 1`, `stage2_contact_ceiling: 12`, and a `stage2_credit_ceiling` DERIVED in the test as `12 * rates["rates"][SUGGESTION_STAGE2_RATE_KEY]["value"]` — never a literal. That rate is documented as a worst case observed once, not a flat price, and plan 09 Task 2 rewrites `cost_rates.json` with the probe's own measurements; a hard-coded product would break in wave 6 for a reason that has nothing to do with this function.
    - Test 3 the rendered line never says `$0`, never says `0 credits`, and contains the word `unknown` once per unknown-rate provider.
    - Test 4 the rendered line contains the gap-company count, the literal `1 n8n execution`, and the stage-2 ceiling figure.
    - Test 5 a known rate renders arithmetically: with `zoominfo_contact_search` patched to `1`, that provider's part shows `6 × 1 = 6 credits` and its `state` is `"measured"` while the other two stay `unmeasured` — a mixed round is the expected state, not an error.
    - Test 6 the stage-2 credit ceiling is computed from `SUGGESTION_STAGE2_RATE_KEY`, proved by patching that rate to a different number and asserting the ceiling follows it.
    - Test 7 the rendered line contains no occurrence of the word `tier`.
  </behavior>
  <action>
Add `discovery_line(gap_company_count, per_company_cap, rates)` to
`operator-claude-plugin/scripts/cost_guard.py`, immediately after `suggestion_line` and modelled
on it rather than on anything new.

Branch order is `research_line`'s, unchanged in spirit: zero gap companies first (no company
needs provider discovery — a different kind of nothing from an unknown rate), then the rate
question per provider.

Per provider, read its key from `DISCOVERY_SEARCH_RATE_KEYS` through the existing `_rate_entry`
helper and produce an entry carrying `provider`, `rate_key`, `rate`, `unit`, `confidence`,
`citation`, `state` (`measured` or `unmeasured`) and its own rendered fragment. A `null` rate
renders the word `unknown`; a known rate renders `gap_count × rate = total` in that provider's
unit. The whole-round `state` is `measured` only when EVERY provider's rate is known, and
`known` mirrors it — a round with one known and two unknown rates is `unmeasured`, because the
total genuinely is.

`executions` is the integer `1` with a comment naming D-08: one POST per round carrying every
eligible company means exactly one n8n execution regardless of company count. Do not compute it
from the company count.

`stage2_contact_ceiling` is `gap_company_count * per_company_cap` and `stage2_credit_ceiling` is
that times `SUGGESTION_STAGE2_RATE_KEY`'s value, or `None` when that rate is null — the SAME
contacts rate `suggestion_line` already uses, read through `_rate_entry`, never a second literal
for the same spend. Label it a ceiling in the rendered text: stage 2 can only reveal people
discovery actually found.

The rendered `line` names all four things D-13a requires in one passage — the gap-company count,
each provider's search rate or `unknown`, the one n8n execution, and the stage-2 reveal ceiling
— and states that the discovery search itself spends no HubSpot write and opens no arming
window (D-06, read-only lane). Say plainly, in the line, that a provider whose rate is unknown
proceeds and reports its measured cost after the round (D-13) — the operator is authorising that
override and must read it. Keep every operator-visible string saying `rank` and never `tier`.

Write all seven tests in `operator-claude-plugin/tests/test_cost_guard.py` first and record the
RED output.
  </action>
  <acceptance_criteria>
    - `cost_guard.discovery_line` exists with the signature `(gap_company_count, per_company_cap, rates)`.
    - Its returned dict has at least the keys `gap_company_count`, `per_company_cap`, `providers`, `executions`, `stage2_contact_ceiling`, `stage2_credit_ceiling`, `state`, `known`, `line`.
    - `discovery_line(0, 2, rates)["state"] == "no_rows"`.
    - With the shipped `cost_rates.json`, `discovery_line(6, 2, rates)["executions"] == 1` and `["stage2_contact_ceiling"] == 12`, and `["stage2_credit_ceiling"]` equals `12 * rates["rates"][cost_guard.SUGGESTION_STAGE2_RATE_KEY]["value"]` computed in the assertion, with no numeric literal for that product anywhere in the test.
    - `"$0" not in discovery_line(6, 2, rates)["line"]` and `"tier" not in discovery_line(6, 2, rates)["line"].lower()`.
    - Tests 1–7 exist by name in `operator-claude-plugin/tests/test_cost_guard.py` and pass.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_cost_guard.py operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_disclosure_audit.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
  </verify>
  <done>A round can state what discovery will cost, showing unknown where it is unknown, and the operator reads the D-13 override before authorising it.</done>
  <reversibility rating="reversible">A new pure function with no call site yet; plan 08 wires it into the skill.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cost_rates.json` -> the operator's authorisation decision | A rate figure shown to the operator is the basis on which they authorise spend they cannot themselves observe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-07 | Repudiation | An unmeasured search rate rendered as `$0`, so the operator authorises unbounded spend believing it free | high | mitigate | Three entries ship `value: null`; `discovery_line`'s `unmeasured` branch renders the word `unknown`; Test 3 asserts `$0` never appears in the rendered line. |
| T-73.1-08 | Tampering | A guessed figure copied from another provider's rate to make the line look complete | medium | mitigate | Each citation carries the explicit no-scaling prohibition, copied from the existing `suggestion_stage1_discovery` entry's register; the acceptance criteria grep for it. |
| T-73.1-09 | Information Disclosure | A citation embedding an account identifier or key while explaining the 403 balance read | medium | mitigate | Citations name endpoints and HTTP statuses only, never a key, account id or header value. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 03)

- `cost_rates.json` keys `zoominfo_contact_search`, `apollo_people_search`, `lusha_prospecting_search` (all `value: null`, unit `credits/search`)
- `cost_guard.DISCOVERY_SEARCH_RATE_KEYS` — new ordered provider-to-key map
- `cost_guard.discovery_line(gap_company_count, per_company_cap, rates)` — new function
</artifacts_produced>

<verification>
- The full plugin suite is green.
- With the shipped rate table, `discovery_line` renders three `unknown` figures and no dollar or credit zero.
</verification>

<success_criteria>
- Three provider search rates exist and are honestly null.
- One disclosure line shows gap count, per-provider rate or unknown, one n8n execution and the stage-2 ceiling.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-03-SUMMARY.md` when done.
</output>
