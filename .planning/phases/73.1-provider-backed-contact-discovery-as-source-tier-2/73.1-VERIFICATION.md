---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
verified: 2026-09-18T12:43:57Z
status: gaps_found
score: 29/31 D-ids verified (2 FAILED: D-11, D-11a); 1 additional goal-level truth (not a D-id) also FAILED for the same root cause
covered_files:
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-01-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-01-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-02-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-02-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-03-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-03-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-04-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-04-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-05-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-05-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-06-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-06-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-08-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-08-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-PLAN.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-09-SUMMARY.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-D12-VERDICT.json
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-UAT.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-VALIDATION.md
  - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/COVERAGE.md
  - n8n/code/columnMap.js
  - n8n/code/discoverySearch.js
  - n8n/code/matchProposal.js
  - n8n/wf_backend_status_cloud.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_suggest_discovery_cloud.json
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/config/column_mapping.yaml
  - operator-claude-plugin/config/cost_rates.json
  - operator-claude-plugin/config/crm_access_block.md
  - operator-claude-plugin/config/operator.local.example.json
  - operator-claude-plugin/config/role_vocabulary.yaml
  - operator-claude-plugin/config/source_allowlist.yaml
  - operator-claude-plugin/scripts/config_gate.py
  - operator-claude-plugin/scripts/cost_guard.py
  - operator-claude-plugin/scripts/extraction.py
  - operator-claude-plugin/scripts/role_classify.py
  - operator-claude-plugin/scripts/search_fallback.py
  - operator-claude-plugin/scripts/suggest_contacts.py
  - operator-claude-plugin/scripts/suggest_discovery.py
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/written_records.py
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - scripts/bounce_n8n_workflows.py
  - scripts/build_cloud_workflows.py
  - scripts/deploy_n8n_workflows.py
  - scripts/probe_provider_discovery.py
covered_digest: "v1:sha256:2b2efa70b616664be7e1c349f619682ad48a75e2a9f9c9f3766dd4fc0ba007dc"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "A gap:true company sent to the discovery lane returns at least one provider-discovered person (the phase's own headline goal: \"suggest-contacts discovers people through the enrichment providers, not only by crawling the company's own website\"). D-11: the ZoomInfo waterfall search succeeds and returns candidates."
    status: failed
    reason: >
      Every live attempt to date (three disarmed executions: 12666, 12668, 12669) returned
      zero people for every gap company. The first attempt (12666) had a real bug (ZoomInfo's
      own PFAPI0006: a 757-char jobTitle OR-join exceeds its 500-char cap, swallowed by a
      try/catch into an indistinguishable empty result) which was fixed in-session
      (73.1-09 follow-through, commit 4736fbd8) and confirmed arithmetically correct
      (titles_used=28). But the two subsequent live proof sends made AFTER that fix
      (12668, 12669) both returned 401 UNAUTHORIZED on BOTH rungs, for a token whose claims
      (issuer, audience, client id, scope list including api:data:contact, 24h lifetime) are
      byte-identical to a token minted locally by scripts/check_provider_credits.py from the
      same .env credential, which itself returns 200/totalResults:1094 for the same endpoint
      and domain in four separate Python replays the same hour. The UAT's own "orchestrator
      addendum" therefore rules out token expiry, scope/entitlement gap, and client identity
      as the cause, leaving unresolved something about how n8n's Code-node
      this.helpers.httpRequest issues this specific request (query-string pagination + a
      JSON:API body, a combination no other lane in this repo sends) versus how Python's
      requests library issues the identical call. A cache-clear-on-401 fix was added
      (commit f0af2055) so the NEXT execution re-mints, but no execution with a fresh mint
      has yet been observed to succeed — the UAT explicitly records this as "genuinely
      unresolved" and recommends one further diagnostic execution that was not run in this
      phase (budget exhausted). This is negative live evidence, not merely untested code —
      the capability has been exercised and has failed every time.
    artifacts:
      - path: "n8n/code/discoverySearch.js"
        issue: "buildUrl() puts ZoomInfo's pagination in the query string (a shape no other live-proven lane in this repo uses); the true cause of the live 401 was never isolated"
      - path: "scripts/build_cloud_workflows.py"
        issue: "_discovery_zoom_search_leaf_js emits the Code-node leaf that issues the failing request via this.helpers.httpRequest; the isAuthError-clears-cache fix (mirroring the enrich lane) has not yet been confirmed to produce a successful re-mint-and-search"
    missing:
      - "One diagnostic live execution that moves ZoomInfo's pagination from the URL query string into request options (qs), and a second with no pagination at all, each persisting the raw HTTP status — per the UAT orchestrator addendum's own recommended next step"
      - "A live 200 response from /gtm/data/v1/contacts/search inside n8n, with a real people list, for a known-good domain (e.g. tennis.com.au, independently confirmed at 1094 results via Python the same day)"
  - truth: "D-11a: rung 1's provider-side title filter is built from the round's CHOSEN role families' member strings, so an operator-selected family is what narrows the search."
    status: failed
    reason: >
      `scripts/build_cloud_workflows.py::_discovery_role_titles()` reads EVERY family in
      `role_vocabulary.yaml` at BUILD time, not the request body's `role_families` field —
      the builder's own docstring self-documents this as a discretion, not a bug, but it
      means rung 1 never narrows to what the operator actually selected for the round.
      Worse: `discoverySearch.js`'s `capRoleTitles()` is a naive greedy stop-before-500-chars
      fold over that full, un-selected list in declaration order. Reimplementing the exact
      algorithm and running it against the real 42-title vocabulary (verified live in this
      session, not merely read from a comment) confirms 14 of 42 titles are silently
      dropped, and the dropped set is EXACTLY the four members of the two families D-11c
      added THIS PHASE to catch the pickleball round's missed people: "Executive Officer",
      "Board Chair", "Board Chairwoman", "Chairwoman" (only "Chairman" survives the cap).
      Rung 1 can therefore never title-match the very roles (Brendan Lee, Executive
      Officer; Jen Ramamurthy, Board Chairwoman) that motivated this phase. Rung 2
      (unfiltered top-10, D-11a's own stated fallback) fires "only when rung 1 returns
      zero" — so a company where rung 1 gets ANY hit under one of the 28 surviving titles
      never reaches rung 2 at all, silently missing an Executive Officer or Board Chair who
      was present in ZoomInfo's data. This is a self-inflicted logic defect, found by
      static inspection and a deterministic offline replay of the shipped code (zero live
      calls), independent of the 401 issue above.
    artifacts:
      - path: "scripts/build_cloud_workflows.py"
        issue: "_discovery_role_titles() (line ~12005) reads the full vocabulary at build time, never the request's per-round role_families; capRoleTitles' greedy fold (inlined via discoverySearch.js) drops later-declared families first with no relevance weighting"
      - path: "n8n/code/discoverySearch.js"
        issue: "capRoleTitles() has no awareness of which families are new/important; a title's survival depends only on its position in role_vocabulary.yaml"
    missing:
      - "Either thread the round's actual role_families selection through to rung 1 (so the cap only ever has to fit the operator's chosen subset, which is normally far under 500 chars), or reorder/prioritize the cap so newly-added or explicitly-requested families are never silently dropped"
      - "A test asserting D-11c's own new members survive the cap (none currently exists — the offline suite pins arithmetic correctness of the cap, not which titles survive)"
deferred: []
human_verification:
  - test: "D-11b: run the discovery lane live against a company where rung 1 (or rung 2, once the 401 above is fixed) returns real hits, then compare the provider's own title match against role_classify.classify_title's verdict on the same titles."
    expected: "A sample large enough for the operator to rule on whether the provider's own title match can be trusted going forward, or whether classify_title must keep gating every result."
    why_human: "This is an explicit, deferred operator ruling (D-11b) that depends on live evidence the phase's own 0/0 sample (executions 12666/12668/12669, blocked by the Gap-1 401) could not produce. Not a code check — a data-driven policy decision."
---

# Phase 73.1: Provider-backed contact discovery as source tier 2 — Verification Report

**Phase Goal:** suggest-contacts discovers people through the enrichment providers, not only by
crawling the company's own website. Provider discovery is source tier 2; LinkedIn moves to tier
3; industry bodies and media move to tier 4; tier 1 (company's own host) unchanged. Discovery is
a backend lane built in `scripts/build_cloud_workflows.py`; provenance carries
`source: <provider>_search`; identity ladder widened per D-16; role vocabulary gains
`executive_officer` and `board_chair`; cost guard prices discovery per company (D-13); offline
tests plus one live disarmed proof at end-of-phase UAT; plugin release with CHANGELOG.

**Verified:** 2026-09-18T12:43:57Z
**Status:** gaps_found
**Re-verification:** No — initial verification

**Requirement basis:** ROADMAP.md records `Requirements: TBD` for this phase; per the verifier
prompt's instruction, the 31 decision IDs in `73.1-CONTEXT.md` (D-01 … D-16d) are treated as the
requirement set. `grep -c "Phase 73.1" .planning/REQUIREMENTS.md` → 0 (confirmed: no orphaned
REQUIREMENTS.md mappings exist for this phase).

## Goal Achievement

### Observable Truths

The phase's own headline claim is checked first, separately from the 31 D-ids, because no
individual plan's `must_haves.truths` states it as bluntly as the ROADMAP goal does.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 0 | A `gap: true` company sent to the discovery lane returns ≥1 provider-discovered person | ✗ FAILED | Three live disarmed executions (`12666`, `12668`, `12669`), zero people returned on every attempt. See Gap 1. |

### D-ids (31 total, from 73.1-CONTEXT.md)

| # | D-id | Status | Evidence |
|---|------|--------|----------|
| 1 | D-01 | ✓ VERIFIED | `search_fallback.py`: `STRONG_TIERS=(1,2)`, `KNOWN_TIERS=(1,2,3,4)`, `LISTED_TIERS=(3,4)`; `source_allowlist.yaml` v2, no tier-2 host entry; `(None,None)` rejection shape unchanged (no tier-5 sentinel added); `test_source_tier_parity.py`/`test_search_fallback.py`/`test_suggest_contacts.py` green (272 tests) |
| 2 | D-02 | ✓ VERIFIED | Email-domain relation unchanged for a provider-found person; same test run above green |
| 3 | D-03 | ✓ VERIFIED | `suggest_contacts.dedupe_discovered()` present, lowest-rank-wins, `linkedin_url` else casefolded name+company key; docstring + code read directly |
| 4 | D-04 | ✓ VERIFIED | Winning source's title on the row, loser's title recorded in `corroboration[].jobtitle` only — read directly in `dedupe_discovered` |
| 5 | D-05 | ✓ VERIFIED | Discovered records reuse `search_fallback.SEARCH_INPUT` provenance input with `source_tier`; gate unmodified |
| 6 | D-05a | ✓ VERIFIED | `operator-claude-plugin/tests/test_source_tier_parity.py` exists and passes, pinning all 4 constants |
| 7 | D-06 | ✓ VERIFIED (observed live) | 6th workflow `LV Suggest Discovery (Cloud template)` id `VJJBZ2oJ0079MSzG`, live, active, v1, 26 nodes (confirmed by direct read of `n8n/wf_suggest_discovery_cloud.json`: 26 nodes, zero `executeWorkflow`, zero HubSpot write nodes, `assert_no_write_nodes`/`assert_no_self_dispatch` present in the builder); zero `ALLOW_*` literals on this workflow (UAT §4) |
| 8 | D-07 | ✓ VERIFIED | Hybrid response confirmed live (§5, synchronous body, `available=True, complete=True`); durable `run_id`-keyed recovery path reuses existing `watch.recover_async_dispatch` unchanged (code read + `test_suggest_discovery.py` passing) |
| 9 | D-08 | ✓ VERIFIED (observed live) | Exactly one execution (`12666`) for a 3-company batch; body/runData both carry every company keyed by `company_id` |
| 10 | D-09 | ✓ VERIFIED (observed live) | `num_associated_contacts` echoed for all 3 companies (0, 3, 0) in both response body and runData |
| 11 | D-10 | ✓ VERIFIED (observed live) | `IF Company Is Gap` split `[2,1]`; non-gap company's leg touched zero provider nodes |
| 12 | D-11 | ✗ FAILED | See Gap 1 — waterfall stop-condition and provider order exist in code (ZoomInfo-only, per the D-12 amendment below), but the search itself has never succeeded live |
| 13 | D-11a | ✗ FAILED | See Gap 2 — filter is build-time/all-families, not per-round; the cap drops this phase's own new families |
| 14 | D-11b | ✓ VERIFIED (code); ruling pending | `role_classify.classify_title` still gates every result, untouched — confirmed by direct code read; operator ruling itself blocked on Gap 1 (0/0 sample) — see Human Verification |
| 15 | D-11c | ✓ VERIFIED | `role_vocabulary.yaml` v4 carries `executive_officer`/`board_chair` families; `test_role_vocabulary.py` green including the no-duplicate-member structural guard |
| 16 | D-12 | ✓ VERIFIED | No reveal fields anywhere in `discoverySearch.js`; zero credit spent on reveal by design; D-12 probe rounds 1-4 measured live, operator ruled ZoomInfo-only search-only, applied in `73.1-D12-VERDICT.json` with full consequence list |
| 17 | D-13 | ✓ VERIFIED | `cost_rates.json`: `zoominfo_contact_search` measured 0 (dated, cited), `lusha_prospecting_search` measured 1/request (dated, cited, provider now dropped from lane), `apollo_people_search` stays `null` with an unmeasurable-403 citation |
| 18 | D-13a | ✓ VERIFIED | `cost_guard.discovery_line` present, tests green (`test_cost_guard.py`) |
| 19 | D-14a | ✓ VERIFIED | `crm_access_block.md` states the portal-proof rule; `test_crm_access_block.py` green |
| 20 | D-14b | ✓ VERIFIED | `written_records.SOURCE_CONNECTOR = "connector"`; `crm_access_block.md` states association-in-same-step; `test_written_records.py` green |
| 21 | D-14c | ✓ VERIFIED (observed live) | UAT §7: `backend_status.fetch_backend_status` → `portalId=22617666`, matches plugin config |
| 22 | D-14d | ✓ VERIFIED | `test_crm_access_block.py` greps all SKILL.md files for the shared block (confirmed present in backend-control, backend-status, and others read directly) |
| 23 | D-15a | ✓ VERIFIED | `write_grant.py` tests green (`test_write_grant.py`, `test_write_grant_guardrails.py`, `test_write_grant_surface.py`, `test_headless_grant_boundary.py`) |
| 24 | D-15b | ✓ VERIFIED | `write_grant.widen()` present; same test run |
| 25 | D-15c | ✓ VERIFIED | `write_grant.SECOND_GRANT_REFUSED` present; same test run |
| 26 | D-15d | ✓ VERIFIED | Shared block states scope/revocation; `CLOSED_REVOKED` unchanged |
| 27 | D-16a | ✓ VERIFIED (observed live) | `wf_contact_ingest_cloud.json`: `IF Mobilephone Searchable`/`HubSpot Mobilephone Search`/`Adapt Mobilephone Search` + carry Merge + sentinel present; live node count 98 matches committed; deployed and bounced 2026-09-18 |
| 28 | D-16a-i | ✓ VERIFIED | `matchProposal.js`: dedicated `mobilephone` arm, single-hit-only rule, never joined to `linkedin`'s arm; `phone` never referenced in `laneOf` |
| 29 | D-16b | ✓ VERIFIED | `column_mapping.yaml`/`operator-claude-plugin/config/column_mapping.yaml`: 5 `required_identity.any_of` groups incl. the two name+number groups; parity test present |
| 30 | D-16c | ✓ VERIFIED | `test_suggest_contacts.py`/`test_source_tier_parity.py` green covering the email-only domain-relation gate |
| 31 | D-16d | ✓ VERIFIED | `matchProposal.js` reasons name the resolving lane (`"matched by mobilephone"`, `"matched by linkedin"`); `written_records` tests green |

**Score:** 29/31 D-ids verified (D-11, D-11a FAILED); plus 1 goal-level truth (not itself a D-id)
also FAILED for the same root cause as D-11.

### D-11 amendment note (not a gap)

D-11's originally-stated "ZoomInfo then Apollo then Lusha" waterfall order was legitimately
superseded within this same phase by the D-12 operator ruling (`73.1-D12-VERDICT.json`,
2026-09-18, verbatim: "ZoomInfo retained as tier-2 source, others (Apollo/Lusha) dropped for
search phase. Full waterfall only used on enrich."), applied in plan 09 with measured live
evidence (Apollo obfuscates last name; Lusha's preview carries no name/title and costs 1
credit/request even on zero results). The narrowing itself is correctly implemented — zero
Apollo/Lusha nodes remain in the generated workflow, zero Apollo/Lusha branches remain in
`discoverySearch.js` — and is NOT counted as part of D-11's FAILED status. D-11 is marked FAILED
solely because the (now ZoomInfo-only) search has never succeeded live — see Gap 1.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `operator-claude-plugin/config/source_allowlist.yaml` | v2, tiers 3/4 | ✓ VERIFIED | version string confirmed `lv-source-allowlist-v2` |
| `operator-claude-plugin/tests/test_source_tier_parity.py` | 4-constant parity test | ✓ VERIFIED | exists, passes |
| `operator-claude-plugin/config/role_vocabulary.yaml` | v4, new families | ✓ VERIFIED | version string confirmed `lv-role-vocabulary-v4` |
| `operator-claude-plugin/config/crm_access_block.md` | shared CRM-access block | ✓ VERIFIED | exists, referenced by 11 skills |
| `scripts/build_cloud_workflows.py::build_suggest_discovery_cloud()` | 6th workflow builder | ✓ VERIFIED | generates `n8n/wf_suggest_discovery_cloud.json`, 26 nodes, deterministic (`git diff --quiet n8n/` clean after regen) |
| `n8n/wf_suggest_discovery_cloud.json` | new, generated | ✓ VERIFIED | 26 nodes, zero write nodes, zero `executeWorkflow`, `executionOrder: v1` — matches live (`VJJBZ2oJ0079MSzG`) |
| `n8n/code/discoverySearch.js` | request builders/normalizers | ✓ VERIFIED (structure); ✗ operational (see Gap 1) | ZoomInfo-only per D-12; request never succeeds live |
| `operator-claude-plugin/scripts/suggest_discovery.py` | discovery client | ✓ VERIFIED | `test_suggest_discovery.py` green |
| `operator-claude-plugin/.claude-plugin/plugin.json` | version bump | ✓ VERIFIED | `0.51.1`, CHANGELOG entries for 0.51.0 (tier renumbering) and 0.51.1 (D-12 ruling) present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `config/source_allowlist.yaml` tiers | `search_fallback` constants | `test_source_tier_parity.py` | ✓ WIRED | parity test derives from allowlist, passes |
| `synthesise_rows(source_tier=2)` | `hold_weak_sources` gate | `search_fallback.SEARCH_INPUT` | ✓ WIRED | reused literal confirmed in code |
| `role_vocabulary.yaml` families | `role_classify.classify_title` | longest-wins tie-break | ✓ WIRED | structural no-duplicate test passes |
| `cost_rates.json` null/measured | `cost_guard.discovery_line` | `_rate_entry` | ✓ WIRED | tests green |
| discovery lane response body | plugin `run_id` recovery | `watch.recover_async_dispatch` | ✓ WIRED (offline); not separately exercised live (fast path always won in all 3 live sends) | — |
| `search_fallback.MAX_FALLBACK_SEARCHES` | builder-emitted per-company ceiling | generation-time constant | ✓ WIRED | `MAX_FALLBACK_SEARCHES = 3` confirmed |
| discovery lane's response body | plugin's `run_id` recovery | client-minted `run_id` | ✓ WIRED | `run_state.new_run_id()` used in the live proof send |
| `_discovery_role_titles()` (full vocab, build-time) | rung-1 request body | `capRoleTitles` greedy fold | ✗ BROKEN | see Gap 2 — not per-round, drops this phase's own new families |
| ZoomInfo Search Rung1/Rung2 leaves | ZoomInfo `/contacts/search` | `this.helpers.httpRequest` | ✗ BROKEN | see Gap 1 — 401 on every live attempt after the title-cap fix |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full Python suite | `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` | 5131 passed, 160 skipped | ✓ PASS |
| Full n8n walker suite | `node --test tests/n8n/*.test.mjs` | 1285 pass, 0 fail | ✓ PASS |
| Builder determinism | `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet n8n/` | clean, no diff | ✓ PASS |
| Discovery lane structure | direct read of `n8n/wf_suggest_discovery_cloud.json` | 26 nodes, 0 write nodes, 0 `executeWorkflow`, `executionOrder: v1` | ✓ PASS |
| Rung-1 title cap (offline replay of shipped `capRoleTitles` logic against the real 42-title vocabulary) | one-off `python3` script reimplementing the greedy fold | 28 used / 14 dropped, dropped set includes all 4 of D-11c's new members | ✗ FAIL (Gap 2) |
| Redaction gate | `grep -c 'x-enrichment-secret' tests/n8n/fixtures/frozen/*.json \| grep -v ':0$'` | 5 pre-existing Phase-70 fixtures still leak an (old, superseded) secret | ℹ️ INFO — pre-existing, confirmed not this phase's own commit (`git log --grep="73\.1"` touches none of `exec_1235{4..8}.runData.json`); not fixed here per UAT §9's own reasoning (fixing risks breaking two pinned byte-exact tests) |

### Requirements Coverage

No `.planning/REQUIREMENTS.md` IDs are mapped to this phase (`grep -c "Phase 73.1" REQUIREMENTS.md` → 0, confirmed — zero orphans). Coverage is by the 31 CONTEXT.md decision IDs, tabulated above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD`/`FIXME`/`XXX` debt-marker scan over every file touched by a `73\.1`-grep commit (121 files) | ℹ️ INFO | All 8 word-boundary hits are the pre-existing, explained "Requirements: TBD" convention (no requirement IDs mapped, decision-IDs used instead) — not unfinished-code markers. Zero real debt markers found. |
| `scripts/build_cloud_workflows.py` | ~12005-12019 | `_discovery_role_titles()` reads full vocabulary at build time, contradicting D-11a's "chosen role families" language | ⚠️ WARNING (promoted to Gap 2 — see above) | Rung 1 never narrows per-round; new families get silently dropped by the cap |
| `n8n/code/discoverySearch.js` | `buildUrl()` | Pagination in query string, unique among this repo's live-proven provider lanes | 🛑 BLOCKER (Gap 1) | Search 401s live, root cause unresolved |

### Human Verification Required

1. **D-11b operator ruling on provider-title trust.** Blocked entirely on Gap 1 — 0 rung-1 hits were returned in all 3 live attempts, so there is no evidence yet to rule on. Not a code check.

### Secret-exposure note (carried forward from 73.1-UAT.md, not a code gap)

`73.1-UAT.md`'s own "Issues Encountered" §1 records that the live `n8n_api_key` and
`webhook_secret` were printed into that session's tool output (never written to a file, never
committed) and recommends rotating both. This verification session did not re-expose either
value and takes no position beyond surfacing the UAT's own recommendation — it is an operational
follow-up, not a phase-goal gap.

### Gaps Summary

The phase built a complete, correctly-wired, read-only 6th cloud workflow with clean routing,
gap-only search dispatch, one-execution-per-round batching, full company echo, and zero write/
self-dispatch risk — all of that is `[observed live]` and works. What has never worked, in three
separate live disarmed attempts spanning two code fixes, is the one thing the phase exists to
deliver: an actual ZoomInfo search returning a real person. The first attempt hit a real,
now-fixed bug (title string over ZoomInfo's 500-char cap, silently swallowed as an empty
result). The two attempts after that fix both hit a 401 whose cause the phase's own UAT
explicitly could not isolate (expiry, scope, and client-identity are all ruled out by direct
evidence) and explicitly recommends one more diagnostic step that was not performed. Separately,
and independently of the 401, static inspection found that the newly-added Executive
Officer/Board Chair role families — the exact roles this phase exists to catch — are silently
dropped from rung 1's title filter by an un-prioritized 500-char cap over the full vocabulary,
because the cap operates on the full build-time vocabulary rather than the round's actual
selection. Both are real, closeable gaps; neither is a wiring or contract failure — the
plumbing around the broken search is itself sound.

---

*Verified: 2026-09-18T12:43:57Z*
*Verifier: Claude (gsd-verifier)*
