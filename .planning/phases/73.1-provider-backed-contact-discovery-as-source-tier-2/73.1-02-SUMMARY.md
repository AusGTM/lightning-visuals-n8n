---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 02
subsystem: contact-discovery
tags: [python, pytest, tdd, role-classification, vocabulary]

# Dependency graph
requires:
  - phase: 62-suggest-the-contacts-nobody-named
    provides: role_classify.classify_title's contiguous-token, longest-wins matcher and
      the role_vocabulary.yaml family/member shape this plan adds two families to
provides:
  - "role_vocabulary.yaml at lv-role-vocabulary-v4 with an Executive Officer family
    (member: Executive Officer) and a Board Chair family (members: Board Chair, Board
    Chairwoman, Chairwoman)"
  - "test_no_member_string_appears_in_two_families -- a permanent structural guard
    generalizing the existing single-token-only D-rf1-03 check to members of any
    token length"
affects: []

# Actuals (#2632)
actuals:
  tokens: 1615
  tasks: 2
  commits: 2
  plan_head_before: 7c2b8a579dbc13349a7827b9c2a70c304cfb7a44

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Duplicate-member guard as a permanent structural test rather than a one-off
      check -- tokenises every member of every family and asserts no tokenised
      sequence occurs under two labels, so any FUTURE vocabulary edit that
      accidentally re-adds an already-covered member (not just a bare single-token
      one) fails loudly instead of silently depending on YAML order."

key-files:
  created: []
  modified:
    - operator-claude-plugin/config/role_vocabulary.yaml
    - operator-claude-plugin/tests/test_role_vocabulary.py

key-decisions:
  - "Board Chair family deliberately excludes Chairman and Chairperson, both of which
    D-11c's own prose lists (\"Board Chair / Chairwoman / Chairman\"). Both strings
    already live in the existing Chair family; D-11c's listing is read as a
    description of the family's real-world SCOPE (who counts as a board chair), not
    an instruction to duplicate the member string. Chairman stays in Chair alone --
    it is already covered there."
  - "Labels are human-readable title case (\"Executive Officer\", \"Board Chair\"),
    not D-11c's snake_case identifiers (executive_officer, board_chair).
    role_classify.chosen_families validates the operator's once-per-batch selection
    against these labels and the prompt builder prints them to the operator, so a
    label is operator-visible text -- and every one of the file's existing 20
    families already uses title case."
  - "The new duplicate-member guard (Task 1) generalizes rather than duplicates the
    pre-existing test_no_single_token_member_belongs_to_two_families (D-rf1-03): that
    test only ever tokenised single-token members. A two-token member re-added
    across families (exactly what a naive D-11c reading invites with `Chairman`,
    which happens to be single-token, or a hypothetical two-token duplicate) needed
    a check keyed on the full tokenised member string, not token length."

requirements-completed: [D-11c]

coverage:
  - id: D1
    description: "role_vocabulary.yaml gains the Executive Officer family (member:
      Executive Officer) and the Board Chair family (members: Board Chair, Board
      Chairwoman, Chairwoman) at version lv-role-vocabulary-v4, both evidenced: false
      / recurrence: 0 matching every other family in the file."
    requirement: D-11c
    verification:
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_executive_officer_classifies_to_its_own_family"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_chief_executive_officer_classifies_to_executive_officer_via_longest_run"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_board_chairwoman_classifies_to_board_chair"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_bare_chairwoman_classifies_to_board_chair"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_board_chair_classifies_to_board_chair"
        status: pass
    human_judgment: false
  - id: D2
    description: "Unchanged-behaviour guarantee: Chairman still classifies to Chair,
      Marketing & Communication Coordinator still classifies to Marketing (Liam Oke's
      already-working title is not disturbed), and the bare-grade-noun structural
      test stays green."
    requirement: D-11c
    verification:
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_chairman_still_classifies_to_chair_unchanged"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_marketing_and_communication_coordinator_still_classifies_unchanged"
        status: pass
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_shipped_vocabulary_has_no_bare_grade_noun_members"
        status: pass
    human_judgment: false
  - id: D3
    description: "test_no_member_string_appears_in_two_families -- a new permanent
      structural guard that no member string (of any token length) appears under two
      family labels, seen RED against a temporary duplicate Chairman member in a
      naive Board Chair family before the real families were added."
    requirement: D-11c
    verification:
      - kind: unit
        ref: "operator-claude-plugin/tests/test_role_vocabulary.py::test_no_member_string_appears_in_two_families"
        status: pass
    human_judgment: false

# Metrics
duration: 12min
completed: 2026-09-18
status: complete
---

# Phase 73.1 Plan 02: Executive Officer and Board Chair role families Summary

**role_vocabulary.yaml v4 adds an Executive Officer family and a Board Chairwoman-covering
Board Chair family (deliberately not duplicating Chairman/Chairperson, both already in
`Chair`), guarded by a new permanent test that no member string can appear under two
family labels.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-09-18
- **Completed:** 2026-09-18
- **Tasks:** 2 (each RED-then-GREEN, per plan's `tdd="true"`)
- **Files modified:** 2

## Accomplishments

- Added `test_no_member_string_appears_in_two_families` to `test_role_vocabulary.py`:
  tokenises every member of every family and asserts no tokenised sequence occurs
  under two labels, naming the offending member and both labels on failure. Seen RED
  by temporarily appending a naive `Board Chair` family carrying the already-used
  member `Chairman` (`{'chairman': ['Board Chair', 'Chair']}`), then the YAML was
  restored byte-identical (`git diff --quiet` confirmed) before the real families
  landed.
- Added the `Executive Officer` family (member: `Executive Officer`) and the
  `Board Chair` family (members: `Board Chair`, `Board Chairwoman`, `Chairwoman`) to
  `role_vocabulary.yaml`, bumping `version` to `lv-role-vocabulary-v4` and `built_on`
  to `2026-09-18`. Both carry `evidenced: false` / `recurrence: 0`, matching the
  file's existing discipline — `D-62-07` stays AMENDED.
- Nine classify assertions added and proven: five genuinely RED against the
  pre-change vocabulary (`Executive Officer`, `Chief Executive Officer`, `Board
  Chairwoman`, `Chairwoman`, `Board Chair` all classified to `None`), then GREEN
  after the YAML change; the other four (Chairman unchanged, PA title unchanged, and
  the two pre-existing structural tests) never moved.

## Task Commits

Each task followed RED-then-GREEN (`tdd="true"` on both):

1. **Task 1: the duplicate-member guard, seen RED by the naive addition**
   - `10219abe` `test(73.1-02)`: added `test_no_member_string_appears_in_two_families`;
     RED demonstrated against a temporary duplicate `Chairman` member (not committed),
     then YAML restored byte-identical before commit.
2. **Task 2: the Executive Officer and Board Chair families**
   - Nine classify assertions added first — ran RED (5 of 9 failed: the new-family
     ones) against the pre-change YAML.
   - `d46ad9d6` `feat(73.1-02)`: `role_vocabulary.yaml` v3 → v4 with both families;
     all nine assertions GREEN; full plugin suite 3136 passed / 5 skipped; `node --test
     tests/n8n/*.test.mjs` 1218 pass / 0 fail, zero n8n diff.

**Plan metadata:** this SUMMARY's own commit (follows).

## Files Created/Modified

- `operator-claude-plugin/config/role_vocabulary.yaml` - two new families
  (`Executive Officer`, `Board Chair`), version bumped to `lv-role-vocabulary-v4`.
- `operator-claude-plugin/tests/test_role_vocabulary.py` - one new structural guard
  (`test_no_member_string_appears_in_two_families`) plus seven new classify
  assertions (five new-family, two unchanged-behaviour) covering the two new
  families.

## Decisions Made

- **Board Chair excludes Chairman/Chairperson.** D-11c's own prose lists "Board
  Chair / Chairwoman / Chairman", but `Chairman` and `Chairperson` already live in
  the pre-existing `Chair` family. Read D-11c's listing as describing the family's
  real-world SCOPE, not an instruction to duplicate the string — duplicating either
  would trip Task 1's new guard and make `classify_title`'s tie-break depend on
  YAML order, exactly what the guard exists to prevent.
- **Human-readable title-case labels, not D-11c's snake_case identifiers.**
  `role_classify.chosen_families` validates the operator's selection against these
  labels and `role_classify`'s prompt builder prints them to the operator; every one
  of the file's 20 pre-existing families already uses title case.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`D-11c` closed. `role_classify.py` has zero diff — the classifier itself is untouched,
confirmed by `git diff --stat`. No dependency for later 73.1 plans is created or
consumed here; this plan was independent of plan 01's work (`depends_on: []`).

## Self-Check: PASSED

- `operator-claude-plugin/config/role_vocabulary.yaml` contains `lv-role-vocabulary-v4`: FOUND
- `Executive Officer` family present (`grep -c '^- label: Executive Officer'` = 1): FOUND
- `Board Chair` family present (`grep -c '^- label: Board Chair'` = 1): FOUND
- Board Chair members are exactly `Board Chair`, `Board Chairwoman`, `Chairwoman`
  (no `Chairman`, no `Chairperson`): FOUND
- Commits `10219abe`, `d46ad9d6` present in `git log --oneline --all`: FOUND
- `role_classify.py` diff: none (`git diff --stat` empty): FOUND
- Full plugin suite: 3136 passed / 5 skipped. `node --test tests/n8n/*.test.mjs`:
  1218 pass / 0 fail, zero `n8n/` diff.

---
*Phase: 73.1-provider-backed-contact-discovery-as-source-tier-2*
*Completed: 2026-09-18*
