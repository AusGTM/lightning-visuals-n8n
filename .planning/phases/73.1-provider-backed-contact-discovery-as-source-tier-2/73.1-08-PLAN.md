---
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
plan: 08
type: execute
wave: 5
depends_on: ["73.1-07", "73.1-03", "73.1-01"]
autonomous: true
files_modified:
  - operator-claude-plugin/scripts/suggest_discovery.py
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - operator-claude-plugin/tests/test_suggest_discovery.py
  - operator-claude-plugin/tests/test_suggest_contacts.py
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
requirements: [D-07, D-08, D-09, D-10, D-11c, D-13a, D-01, D-03, D-04, D-16d]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-10: the WEBSITE LADDER RUNS FIRST and providers fill the gap — the plugin computes the gap set after the ladder, as the companies where the ladder found fewer than `per_company_cap` people, and sends one POST for the whole round."
    - "D-08: the POST body is `{run_id, companies: [{company_id, name, domain, role_families, num_associated_contacts, gap}]}` carrying EVERY eligible company with its `gap` boolean, not just the gap set, so one execution answers for the whole round."
    - "D-07: the response channel is hybrid — the synchronous data body is the fast path and settled runData keyed on the client-minted `run_id` is the durable path, recovered through `watch.recover_async_dispatch`'s existing `workflow_name`/`response_node` overrides with zero change to `watch.py`."
    - "D-07: `run_id` comes from `run_state.new_run_id()`, the sole existing mint point, never a fresh ad hoc uuid call, so the round can be tracked alongside an existing batch's bookkeeping."
    - "D-09: `num_associated_contacts` is sent by the plugin (it already has it from the enrichment lane's anonymous response) and read back beside `company_id` for every eligible company."
    - "D-01: the skill's step 5 to 9 prose is renumbered for the new ranks — rank 2 is provider discovery and is sendable, rank 3 is LinkedIn and is now HELD where it used to be sendable, rank 4 is industry and media and stays held — and every operator-visible string says rank, never tier."
    - "D-03: a person found on the company page and by a provider is presented as ONE person with the better source named, and D-04: the row shows the winning source's title."
    - "D-13a: the round states the discovery cost before spending — gap-company count, per-provider search rate or unknown, the one n8n execution, and the stage-2 reveal ceiling — and states that a provider whose rate is unknown proceeds and reports after (D-13)."
    - "D-11c: the operator's role-family selection can now include the Executive Officer and Board Chair families, and those labels are what travels to the lane as `role_families`."
    - "D-16d: the report names which identity key resolved each created contact."
  artifacts:
    - "operator-claude-plugin/scripts/suggest_discovery.py — DISCOVERY_PATH, discovery_target(config), fetch_discovery(config, body, transport=...)"
    - "operator-claude-plugin/tests/test_suggest_discovery.py"
    - "the new discovery step in suggest-contacts/SKILL.md between the ladder step and the filter/synthesise step"
    - "operator-claude-plugin/.claude-plugin/plugin.json version bump and the matching CHANGELOG entry"
  key_links:
    - "the gap set computed after the ladder -> the POST's `gap` booleans -> the lane's gap IF: D-10's ordering is expressed in the plugin, and the lane trusts it; a plugin that computed the gap wrong would spend provider credit on companies the ladder already covered."
    - "run_state.new_run_id() -> the POST body -> runData recovery: the durable path only works because the id is minted client-side before the send."
    - "cost_guard.discovery_line -> the skill's disclosure step -> write_grant: the operator authorises what the line states."
---

<objective>
Wire the plugin side of the discovery round: a client module that POSTs the D-08 envelope and
recovers from runData on a partial body (D-07), the skill step that computes the gap set after the
ladder and merges provider people back into the round (D-10), the renumbered rank prose (D-01),
the cost disclosure (D-13a), and the plugin release.

Purpose: the lane built in plan 07 has no caller until this lands. This is also where every
earlier plan becomes visible to the operator — the new ranks, the new families, the new cost line
and the identity key that resolved a create.

Output: `suggest_discovery.py` following the `backend_status.py`/`review_queue.py` four-part
shape, a rewritten step sequence in `suggest-contacts/SKILL.md`, and a version bump with its
CHANGELOG cut.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-PATTERNS.md
@.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-SUMMARY.md
@operator-claude-plugin/scripts/backend_status.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the discovery client — one POST, one durable fallback, no secret in any message</name>
  <files>operator-claude-plugin/scripts/suggest_discovery.py, operator-claude-plugin/tests/test_suggest_discovery.py</files>
  <read_first>
    - operator-claude-plugin/scripts/backend_status.py in full (60 lines) — the four-part shape to copy: a `<NAME>_PATH` constant, a `<name>_target(config)` builder that never includes the secret, a `_unavailable(reason)` degrade helper, and a `fetch_<name>(config, transport=requests.post)` doing a plain `json=` POST with the `X-Enrichment-Secret` header and the never-echo-exception-text rule
    - operator-claude-plugin/scripts/review_queue.py lines 74-105 — the same shape with a non-empty body, which is the closer analog here
    - operator-claude-plugin/scripts/dispatch.py's first 42 lines — the MULTIPART CSV shape, read so it is NOT copied: it is purpose-built for the row-set upload and is the wrong transport for a JSON envelope
    - operator-claude-plugin/scripts/watch.py `recover_async_dispatch` (lines ~540-571) — its `workflow_id`, `workflow_name`, `echo_node`, `response_node` keyword overrides and its refusal to fall back to time-proximity correlation
    - operator-claude-plugin/scripts/run_state.py `new_run_id` (line ~224) — the sole mint point
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-07-SUMMARY.md — the response node's literal name, the workflow's name, and the exact response contract plan 07 recorded
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-07, D-08, D-09
  </read_first>
  <behavior>
    - Test 1: `discovery_target(config)` returns the n8n URL joined to `webhook/hubspot/suggest/discover`, with a trailing slash on `n8n_url` handled, and never contains the secret.
    - Test 2: `fetch_discovery` with no `webhook_secret` in config returns the unavailable shape and makes NO transport call.
    - Test 3: the POST is `json=`, never multipart, and carries the `X-Enrichment-Secret` header.
    - Test 4: a transport exception returns `{available: False, reason: "endpoint_unreachable", data: None}` and the returned reason contains nothing from the exception's own text.
    - Test 5: a non-2xx status returns the unavailable shape naming the status class, not the body.
    - Test 6: a well-formed 200 returns the parsed `{run_id, companies}` under `data`.
    - Test 7: a 200 whose body is partial — missing `companies`, or carrying fewer companies than were sent — returns a shape that marks the body incomplete rather than pretending it is complete, so the caller knows to take the durable path.
    - Test 8: the module exposes the workflow name and response node literals as constants so the caller passes them to `recover_async_dispatch` rather than hard-coding them at the call site.
    - Test 9: `operator-claude-plugin/scripts/watch.py` is not imported by this module and is not modified — recovery is the caller's step, using the existing function.
  </behavior>
  <action>
Create `operator-claude-plugin/scripts/suggest_discovery.py` copying `backend_status.py`'s exact
four-part shape: `DISCOVERY_PATH = "webhook/hubspot/suggest/discover"`, `DEFAULT_TIMEOUT`,
`discovery_target(config)`, `_unavailable(reason)`, and `fetch_discovery(config, body, transport=requests.post)`.
Do NOT build this as a variant of `dispatch.py` — that module's only exported call is a multipart
POST carrying a CSV, purpose-built for the row-set upload, and a JSON envelope is a different
transport.

Keep the two disciplines that module states about itself: the target URL never includes the
secret, and an exception's text is never echoed because it can carry request headers. Degrade to
`{available, reason, data}` on any failure — never raise, never return a blank-but-successful
mapping.

Add a partial-body verdict. D-07 makes the body the fast path and runData the durable path, so the
client must be able to say the body was incomplete rather than silently returning fewer companies
than were sent. Compare the returned company count against the sent count and mark the result
incomplete when they differ or `companies` is absent, carrying whatever did arrive.

Export the workflow name and response node literals plan 07 recorded as module constants, so the
caller hands them to `recover_async_dispatch` as `workflow_name` and `response_node`. Do not
import or modify `watch.py`: `recover_async_dispatch` already generalises through those keyword
overrides, and recovery belongs at the call site where the run bookkeeping lives.

Write the nine tests first with a stub transport and record the RED output. Make no network call
in any test — the autouse `no_network` fixture is already in place.
  </action>
  <acceptance_criteria>
    - `operator-claude-plugin/scripts/suggest_discovery.py` defines `DISCOVERY_PATH`, `discovery_target`, `fetch_discovery` and the two workflow/response-node literals.
    - `/usr/bin/grep -c 'files=\|multipart' operator-claude-plugin/scripts/suggest_discovery.py` returns 0.
    - `/usr/bin/grep -c 'import watch\|from watch' operator-claude-plugin/scripts/suggest_discovery.py` returns 0.
    - `discovery_target({"n8n_url": "https://x.n8n.cloud/", "webhook_secret": "s"})` contains no occurrence of `s` as a secret value and ends with the discovery path.
    - A partial body is reported as incomplete, carrying what arrived.
    - Tests 1 through 9 exist by name and pass.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_suggest_discovery.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>git diff --quiet -- operator-claude-plugin/scripts/watch.py operator-claude-plugin/scripts/dispatch.py; echo "TRANSPORTS_UNCHANGED=$?"</automated>
    <fails_when>the echoed line is not exactly `TRANSPORTS_UNCHANGED=0`</fails_when>
  </verify>
  <done>The plugin can POST the discovery envelope, knows when the body came back partial, and recovery needs no change to `watch.py`.</done>
  <reversibility rating="reversible">A new module with one call site, added in Task 2.</reversibility>
</task>

<task type="auto">
  <name>Task 2: the skill — ladder first, gap set, one POST, new ranks, cost stated</name>
  <files>operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_suggest_contacts.py</files>
  <read_first>
    - operator-claude-plugin/skills/suggest-contacts/SKILL.md in full from the Steps heading to the end of step 9 — in particular step 5's ladder block around lines 295-335 (which carries the rank 1/2/3 prose and the `--no-ladder` branch), step 6's filter-and-synthesise block, step 7's stage 2, step 8's partition and `hold_weak_sources` block, and step 9's report
    - operator-claude-plugin/skills/suggest-contacts/SKILL.md around line 201 — the existing `write_grant.open_grant(proposal, "yes", config)` call site, which plan 04 changed the contract of
    - operator-claude-plugin/skills/suggest-contacts/SKILL.md around lines 484-693 — the stage-2 block that the 2026-09-18 session BYPASSED with an improvised script; it is correct and is not the crash site, so it is not rewritten here
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-01-SUMMARY.md, 73.1-03-SUMMARY.md, 73.1-07-SUMMARY.md — the constants, the disclosure function and the response contract this step text must match
    - operator-claude-plugin/tests/test_report_enrichment.py — the D-10b guard banning the word `tier` from operator-visible strings
    - .planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-CONTEXT.md D-01, D-03, D-04, D-07, D-08, D-09, D-10, D-11, D-11a, D-11b, D-11c, D-13, D-13a, D-16d
  </read_first>
  <action>
Renumber every rank reference in steps 5 to 9. Rank 1 is the company's own host, unchanged. Rank
2 is now PROVIDER DISCOVERY and is sendable — it is stamped by the discovery adapter and never
matched from a URL. Rank 3 is LinkedIn and is now HELD; call the change out explicitly in the step
text, because a reader who knows the old numbering will otherwise assume a rank-2 LinkedIn hit is
still sendable. Rank 4 is industry bodies and media and stays held. An unmatched host is still
rejected outright and contributes nothing — there is no rank 5. Keep every operator-visible string
saying rank and never tier.

Add a NEW step between the ladder step and the filter-and-synthesise step, carrying D-10's
ordering explicitly: the website ladder runs FIRST for every company, and only then is the gap set
computed as the companies where the ladder found fewer than `per_company_cap` people. State the
2026-09-18 pickleball measurement — six of seven would have gone to providers, Pickleball
Australia would not — so the reader can sanity-check their own round's gap count.

The step mints `run_id` with `run_state.new_run_id()` and sends ONE POST through
`suggest_discovery.fetch_discovery` carrying EVERY eligible company with `company_id`, `name`,
`domain`, `role_families` (the operator's chosen family labels, which can now include Executive
Officer and Board Chair), `num_associated_contacts` (which the round already has from the
enrichment lane's anonymous response — never re-read from HubSpot) and the `gap` boolean, plus
`per_company_cap` at the envelope level. One POST, one execution, every company echoed.

Handle the two channels. The body is the fast path. When `fetch_discovery` reports the body
incomplete or unavailable, recover the same people from settled runData with
`watch.recover_async_dispatch`, passing the workflow name and response node literals
`suggest_discovery` exports. A miss is `recovered: False` and is reported as such — never
back-filled by time proximity and never silently treated as zero people found.

Merge the provider people into the round before `mint_row_ids`: each provider person becomes rows
through `synthesise_rows(..., source_tier=2, provider=<provider>)` with the synthetic locator from
`DISCOVERY_LOCATOR_BY_PROVIDER`, then `dedupe_discovered` folds anyone found both on the company
page and by a provider into one person carrying the better source's provenance and title (D-03,
D-04). Say in the step text that the merged row still passes BOTH gates afterwards — the
email-relatedness pass and the source-rank pass — and that a rank-3 or rank-4 corroboration never
upgrades a row.

Gate every provider result through `role_classify.classify_title` exactly as ladder results are
gated (D-11b): the lane does not classify, a title the classifier cannot place is dropped
`role_not_selected` as today, and trusting the provider's own title match is NOT shipped — the
end-of-phase live sample is what the operator rules on.

State the cost before spending, from `cost_guard.discovery_line(gap_company_count, per_company_cap, rates)`:
the gap-company count, each provider's search rate or the word unknown, the one n8n execution and
the stage-2 reveal ceiling, plus the sentence that a provider whose rate is unknown proceeds and
reports the measured cost after (D-13). Place it where the round's existing cost disclosure is,
not as a second competing disclosure.

Update the grant step for plan 04's contract: the FIRST yes covers the whole session; a later
send outside its record set calls `write_grant.widen` and STATES the widening rather than asking
again; a second `open_grant` while one is open is refused by the code. Pass the held grant as
`existing_grant` on any later `open_grant` call so the refusal can fire at all — nothing persists
a grant between sends, so the call convention is what makes D-15c work.

Name who computes the ceiling for a widening, because `widen(ceiling=None)` widens unconditionally
and an omitted argument would make D-15b a silent bypass of SAFE-01..05. BEFORE calling `widen`,
the step recomputes the projection over the WIDENED record set exactly the way the round computed
it at grant time — `write_grant.envelope`, then `allowance_headroom`, then `ceiling_verdict` — and
passes that verdict in. A `CEILING_OVER` verdict refuses the widening and is reported; an unknown
verdict proceeds, per D-57-02. Say in the step text that a widening whose ceiling was never
computed is not a widening this round performs.

Extend step 9's report: name which identity key resolved each created contact (D-16d), and
present a person found by both the page and a provider as ONE person naming the better source —
never as two rows and never as a merged row whose title came from the weaker source.

Do NOT rewrite the stage-2 block at lines 484-693. It was already correct on 2026-09-18 and was
bypassed by an improvised session script; rewriting it now would change the thing that was not
broken. Instead, open a triaged todo per CLAUDE.md section 31 — `kind: question`, `trigger:` a
second session improvises the stage-2 pipeline instead of running the skill block, `owner:`
operator — recording the deferred promotion of that block to `scripts/suggest_land.py`.

Add or extend skill-contract assertions in `operator-claude-plugin/tests/test_suggest_contacts.py`
so the step text is pinned where it is load-bearing: the ladder-before-providers ordering, the
one-POST rule, the `new_run_id` mint point, and that no operator-visible line in the skill uses
the word tier.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'rank 2' operator-claude-plugin/skills/suggest-contacts/SKILL.md` returns at least 1 and the surrounding text names provider discovery, not LinkedIn.
    - `/usr/bin/grep -c 'rank 3' operator-claude-plugin/skills/suggest-contacts/SKILL.md` returns at least 1 and the surrounding text names LinkedIn as held.
    - The skill names `suggest_discovery.fetch_discovery`, `run_state.new_run_id`, `watch.recover_async_dispatch`, `dedupe_discovered`, `cost_guard.discovery_line`, `write_grant.widen`, `write_grant.ceiling_verdict` and `existing_grant`.
    - The skill states that the ceiling verdict is recomputed over the widened record set and passed to `widen` before any widening happens.
    - The skill states that the website ladder runs first and providers fill the gap.
    - The skill states that a provider whose search rate is unknown proceeds and reports after.
    - The stage-2 block at lines 484-693 is unchanged; the deferred promotion is a triaged todo carrying `kind:`, `trigger:` and `owner:`.
    - The new todo file exists under `.planning/todos/pending/` and passes `scripts/todo_triage.py`.
    - No operator-visible line in the skill contains the word `tier`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_suggest_contacts.py operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_crm_access_block.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python scripts/todo_triage.py</automated>
    <fails_when>non-zero exit, or the output names any untriaged pending todo</fails_when>
  </verify>
  <done>The skill runs the ladder first, computes the gap set, sends one POST, merges and dedupes the provider people, and states the cost with unknowns shown as unknown.</done>
  <reversibility rating="reversible">Skill text and its contract tests.</reversibility>
</task>

<task type="auto">
  <name>Task 3: the plugin release</name>
  <files>operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/tests/test_plugin_manifest.py</files>
  <read_first>
    - operator-claude-plugin/CHANGELOG.md — the tail and the release checklist it carries, and the format of the most recent entries
    - operator-claude-plugin/.claude-plugin/plugin.json — current version `0.50.2`
    - operator-claude-plugin/tests/test_plugin_manifest.py — what it asserts about the manifest and the CHANGELOG
    - every SUMMARY written by plans 01 through 07 in this phase — the CHANGELOG entry must describe what actually shipped, not what was planned
  </read_first>
  <action>
Cut one CHANGELOG entry for this phase and bump `operator-claude-plugin/.claude-plugin/plugin.json`'s
version in the SAME commit. A greyed-out Update button in the marketplace means an unbumped
version, so the bump is not optional bookkeeping.

The entry must name, in operator-facing language: that discovery now goes through the enrichment
providers and not only the company's own website; that the source ranks were renumbered so
provider discovery is rank 2 and LinkedIn moved to rank 3 and is now HELD where it used to be
sendable — this is a BEHAVIOUR CHANGE an existing operator will notice and must be called out as
one, not buried; that two role families were added; that a person with a LinkedIn URL or a mobile
number and no email can now be loaded and matched; that a landline is never used to match an
existing contact; that the first yes now covers the whole session and widens itself rather than
asking again; that any HubSpot tool outside the plugin must prove its portal before use; and that
a provider whose search rate is not yet measured proceeds and reports the cost after.

Also record what did NOT ship: the discovery lane is search-only pending the operator's ruling on
the plan-time probe, and trusting a provider's own title match instead of the classifier is
deferred to a live sample.

Follow the CHANGELOG's own release checklist, including pushing master if the checklist says the
marketplace clone fetches from origin — the clone does not self-refresh.
  </action>
  <acceptance_criteria>
    - `operator-claude-plugin/.claude-plugin/plugin.json`'s `version` is greater than `0.50.2`.
    - The CHANGELOG's newest entry names that version.
    - The entry calls the LinkedIn sendable-to-held change out explicitly as a behaviour change.
    - The entry records the search-only scope and the deferred title-match question.
    - The version bump and the CHANGELOG entry are in the same commit.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_plugin_manifest.py</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears in the output</fails_when>
    <automated>.venv/bin/python -c "import json;v=json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version'];print('VERSION',v);import sys;sys.exit(0 if tuple(int(p) for p in v.split('.')) > (0,50,2) else 1)"</automated>
    <fails_when>non-zero exit, or the printed VERSION is `0.50.2` or lower</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/ && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or any line beginning with `FAILED` appears, or the TAP summary reports a `# fail` count greater than 0</fails_when>
  </verify>
  <done>The plugin is releasable, the version is bumped, and an operator reading the CHANGELOG learns that LinkedIn stopped being sendable.</done>
  <reversibility rating="reversible">A version string and a CHANGELOG entry.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> the discovery webhook | The shared secret crosses this boundary on every round. |
| the lane's response -> the round's `records` | Provider-supplied people become rows that can reach a write path. |
| the cost line -> the operator's authorisation | What the line says is what the operator agrees to spend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73.1-32 | Information Disclosure | The webhook secret echoed in a transport exception message or a target URL | high | mitigate | `suggest_discovery.py` copies `backend_status.py`'s two rules verbatim: the target never includes the secret and the exception's text is never echoed. Tests 1 and 4 pin both. |
| T-73.1-33 | Spoofing | Provider-supplied people entering the round without passing the gates a ladder person passes | high | mitigate | The step text routes every provider person through `synthesise_rows(source_tier=2)`, `classify_title`, `partition_for_dispatch` and `hold_weak_sources` — the same two gates, both of which must hold. |
| T-73.1-34 | Repudiation | A partial response body silently read as "no people found", losing spend the operator paid for | medium | mitigate | Task 1's partial-body verdict plus the runData durable path; a recovery miss is reported as `recovered: False`, never back-filled. |
| T-73.1-35 | Elevation of Privilege | A second grant opened mid-round because the skill never passes the grant it holds | high | mitigate | The skill passes `existing_grant` on any later `open_grant`; without that call convention D-15c's refusal cannot fire, since nothing persists a grant. |
| T-73.1-36 | Information Disclosure | A discovered person's details written into the CHANGELOG or a committed test fixture | medium | mitigate | The CHANGELOG describes behaviour, not people; tests use synthetic fixtures. |
| T-73.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 08)

- `operator-claude-plugin/scripts/suggest_discovery.py` — `DISCOVERY_PATH`, `DEFAULT_TIMEOUT`, `discovery_target(config)`, `_unavailable(reason)`, `fetch_discovery(config, body, transport=requests.post)`, and the workflow-name and response-node literals
- `operator-claude-plugin/tests/test_suggest_discovery.py` — new test file
- a new discovery step in `operator-claude-plugin/skills/suggest-contacts/SKILL.md` between the ladder step and the filter/synthesise step
- a triaged `kind: question` todo deferring the promotion of the stage-2 block to `scripts/suggest_land.py`
- `operator-claude-plugin/.claude-plugin/plugin.json` version bump plus its CHANGELOG entry
</artifacts_produced>

<verification>
- Both suites green.
- `watch.py` and `dispatch.py` have no diff.
- `scripts/todo_triage.py` reports no untriaged pending todo.
</verification>

<success_criteria>
- One POST per round reaches the lane and its people are merged, deduped and gated exactly as ladder people are.
- The operator reads the gap count, the per-provider rate or unknown, the one execution and the stage-2 ceiling before authorising.
- The CHANGELOG tells an existing operator that LinkedIn stopped being sendable.
</success_criteria>

<output>
Create `.planning/phases/73.1-provider-backed-contact-discovery-as-source-tier-2/73.1-08-SUMMARY.md` when done.
</output>
