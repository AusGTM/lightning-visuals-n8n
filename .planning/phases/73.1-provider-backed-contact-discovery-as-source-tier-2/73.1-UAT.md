---
status: testing
phase: 73.1-provider-backed-contact-discovery-as-source-tier-2
source: [73.1-VERIFICATION.md]
started: 2026-09-18T15:02:14Z
updated: 2026-09-19T02:22:06Z
---

# Phase 73.1 Plan 09 Task 3 — Live Disarmed Proof Record

Operator approved (verbatim 2026-09-18): "approved" — the seven-step checkpoint (dry-run
deploy, live disarmed deploy, id recording + bounce, full flag read-back, one proof send,
burst watch, portal read). Every claim below is tagged `[documented]` or `[observed live]`
per CLAUDE.md section 13.0.3. Nothing was armed. No HubSpot record was written.

## 1. Dry-run deploy `[observed live]`

`DRY_RUN=true ALLOW_N8N_DEPLOY=false` via an in-process dotenv driver (the shell classifier
blocks `. ./.env`; per memory `env-file-permission-blocked` / `n8n-deploy-permission-blocked`,
a python driver that `load_dotenv()`s in its own process and never echoes a value is the
established path).

```
Workflows to create: ['LV Suggest Discovery (Cloud template)']
Workflows to update: ['LV Backend Status (Cloud template)', 'LV Contact Ingest (Cloud template)',
                       'LV Enrichment (Cloud template)', 'LV Review Decision (Cloud)',
                       'LV Scheduled Maintenance (Cloud)']
DRY RUN (default) — no writes will be made.
```

Exactly one CREATE, five updates (matching HEAD's known undeployed changes to Backend
Status D-14c and Contact Ingest's identity ladder — `git log --since=2026-09-13` on those
two files showed real commits since the 2026-09-13 live level). No `ENABLE_BAKED_FLAGS:`
line — the deploy-time overlay was not requested.

## 2. Live disarmed deploy `[observed live]`

`DRY_RUN=false ALLOW_N8N_DEPLOY=true`, same driver:

```
created workflow LV Suggest Discovery (Cloud template) (200)
updated workflow LV Backend Status (Cloud template) (200)
updated workflow LV Contact Ingest (Cloud template) (200)
updated workflow LV Enrichment (Cloud template) (200)
updated workflow LV Review Decision (Cloud) (200)
updated workflow LV Scheduled Maintenance (Cloud) (200)
```

All six at 200. New live id for the sixth workflow, read back via `GET /workflows`:
**`VJJBZ2oJ0079MSzG`** (created inactive, per n8n's default for a new workflow).

## 3. Id recorded + bounce `[observed live]`

`scripts/bounce_n8n_workflows.py`'s `WORKFLOWS` map updated (commit `9e358254`), then all
six bounced (deactivate -> activate -> read back):

| workflow | id | active | live nodes | committed nodes | write flags (script's own check) | execution order |
|---|---|---|---|---|---|---|
| LV Backend Status (Cloud template) | `Cj83mOgrIm59oxcX` | True | 33 | 33 | `-` | v1 |
| LV Contact Ingest (Cloud template) | `AwbBeShdPgV48eiY` | True | 98 | 98 | `false` | v1 |
| LV Enrichment (Cloud template) | `950HPb7a1GgSAIyZ` | True | 287 | 287 | `false` | v1 |
| LV Review Decision (Cloud) | `WBJwoZOo63wzeP69` | True | 55 | 55 | `false` | v1 |
| LV Scheduled Maintenance (Cloud) | `1fXPuIabz3RsAHgn` | True | 43 | 43 | `false` | v1 |
| LV Suggest Discovery (Cloud template) | `VJJBZ2oJ0079MSzG` | True | 26 | 26 | `-` | v1 |

Script verdict: `OK — all active, node counts match, write flags false, execution order v1.`
The discovery lane's node count (26) matches `73.1-07-SUMMARY.md`'s committed count. Node
counts for Backend Status (33, up from the Phase 72 gate's 30) and Contact Ingest (98, up
from 78) reflect real commits landed since the last live level (D-14c/D-14a and the
identity-ladder plans, `3e6ffbe1`/`5035e26d`/`7887e010`) — not a discrepancy, the deploy in
step 2 above is exactly what pushed them live.

## 4. Full write-safety flag scan — every `ALLOW_*` literal, all six `[observed live]`

The bounce script's own check is scoped to `ALLOW_HUBSPOT_RECORD_WRITES` /
`ALLOW_HUBSPOT_CREATE`. A separate, broader scan regexed every `ALLOW_[A-Z_]+ = "<value>"`
literal across every node's flattened `parameters` (jsCode, jsonBody, Set-node values) on
all six live bodies:

```
LV Backend Status (Cloud template): NO ALLOW_* LITERALS FOUND
LV Contact Ingest (Cloud template): ALLOW_HUBSPOT_CREATE=['false'], ALLOW_HUBSPOT_RECORD_WRITES=['false'], ALLOW_HUBSPOT_REVIEW_WRITES=['false']
LV Enrichment (Cloud template): ALLOW_HUBSPOT_CREATE=['false'], ALLOW_HUBSPOT_RECORD_WRITES=['false'], ALLOW_HUBSPOT_REVIEW_WRITES=['false'], ALLOW_JUDGE_ESCALATION=['true'], ALLOW_WEB_RESEARCH=['false', 'true']
LV Review Decision (Cloud): ALLOW_HUBSPOT_CREATE=['false'], ALLOW_HUBSPOT_RECORD_WRITES=['false'], ALLOW_HUBSPOT_REVIEW_WRITES=['false']
LV Scheduled Maintenance (Cloud): ALLOW_HUBSPOT_CREATE=['false'], ALLOW_HUBSPOT_RECORD_WRITES=['false'], ALLOW_HUBSPOT_REVIEW_WRITES=['false']
LV Suggest Discovery (Cloud template): NO ALLOW_* LITERALS FOUND
```

**Every HubSpot write-safety flag** (`ALLOW_HUBSPOT_RECORD_WRITES`, `ALLOW_HUBSPOT_CREATE`,
`ALLOW_HUBSPOT_REVIEW_WRITES`) reads `false` everywhere it appears. `ALLOW_WEB_RESEARCH`
and `ALLOW_JUDGE_ESCALATION` on the Enrichment workflow are a **different category**,
confirmed against the committed `n8n/wf_enrichment_cloud.json`: `const ALLOW_WEB_RESEARCH =
true;` is a pre-existing literal inside the "Research Trigger Gate" Code node (contact
research-escalation gate, CLAUDE.md section 21.1's `ALLOW_WEB_RESEARCH`/
`ALLOW_JUDGE_ESCALATION` global kill switches) — it gates whether the enrichment pipeline
may call Claude web research / escalate to the judge model, never a HubSpot write. It is
NOT one of `bounce_n8n_workflows.py`'s own `WRITE_FLAGS` tuple, is unchanged by this
deploy, and is unrelated to this plan (the discovery lane sends no traffic to Enrichment).
Recorded here for completeness rather than silently omitted. Discovery and Backend Status
carry **zero** `ALLOW_*` literals of any kind — the read-only design has no flag to arm.

## 5. One proof send `[observed live]`

Envelope built per the `suggest-contacts` skill's own documented D-08 shape (`{run_id,
per_company_cap, companies: [{company_id, name, domain, role_families,
num_associated_contacts, gap}]}`), sent via `suggest_discovery.fetch_discovery` with the
plugin's local config (portal `22617666`).

- `run_id`: `feed8a77d9b0409dbe1575aec4bc945f` (minted via `run_state.new_run_id()`)
- Companies sent: `proof-tennis` (tennis.com.au, gap=true, num_associated_contacts=0),
  `proof-nongap` (hubspot.com, gap=false, num_associated_contacts=3), `proof-pickleball`
  (pickleballaustralia.org.au, gap=true, num_associated_contacts=0)
- Execution consumed: **exactly one** — `12666` on `VJJBZ2oJ0079MSzG`, `status=success`,
  started `2026-09-18T11:53:15.867Z`. Global executions list immediately before the send
  topped out at `12665` (an unrelated Backend Status execution); immediately after, only
  `12666` was new.

**Response body (synchronous fast path, no runData recovery needed):**
`available=True`, `complete=True`, `companies_echoed=3`. Every sent company appeared
exactly once, each with its `num_associated_contacts` echoed (0, 3, 0 respectively). Note:
`Build Discovery Response`'s own jsCode (read from the committed node) deliberately never
echoes `gap` back — only `company_id`, `num_associated_contacts`, `people` — so a null
`gap` in the response is the node's documented shape, not data loss.

**runData per-node item counts (`12666`, redacted — no name/contact field printed, no
Webhook Trigger item inspected at all):**

```
Discovery Webhook Trigger:                         [1]
Parse Discovery Request:                            [3]
IF Company Is Gap:                                  [2, 1]   (2 gap, 1 non-gap — correct split)
IF ZoomInfo Eligible:                                [2, 0]   (both gap rows eligible)
ZoomInfo Search Rung1:                               [2]
IF ZoomInfo Rung1 Empty:                             [2, 0]   (both rung-1 filtered searches empty)
ZoomInfo Search Rung2:                               [2]
Adapt ZoomInfo People:                               [2]
Discovery Waterfall Complete:                        [2]
IF Company Is Gap -> ... Pass-Through (non-gap leg): [1]      (the hubspot.com row, zero provider nodes touched)
Discovery Response Merge:                            [3]      (2 waterfall + 1 passthrough = 3, no starvation, no duplication)
Build Discovery Response:                            [1]
Respond to Webhook:                                  [1]
All Gap Sentinel / All Non-Gap Sentinel / Zero Companies Sentinel:  [0] each (no edge-case marker fired — correct for a mixed 2-gap/1-non-gap batch)
```

Confirms: every company echoed once (D-09), the non-gap company triggered **zero**
provider-search nodes (`IF ZoomInfo Eligible`'s eligible branch carried only the 2 gap
rows), no Merge starved, no company's echo row vanished, and the response and runData
agree on shape.

**Notable observation, not a stop condition — recorded as evidence, not chased further
live.** Both gap companies returned **zero people** at both ZoomInfo rungs: rung-1
(filtered on the 42-title `ROLE_TITLES` list — corrected from an earlier "46-title" figure
in this paragraph, see section 5a) was empty for both (`IF ZoomInfo Rung1
Empty` split `[2, 0]`), and rung-2 (unfiltered fallback, same request shape the D-12 probe
validated) was *also* empty for both — including tennis.com.au, which the D-12 probe
(round 4, same day, ~1h45m earlier) measured at `meta.totalResults: 1094` unfiltered. The
ZoomInfo token mint succeeded (a real ~2.2KB JWT was cached and reused across both rungs),
so authentication was not the failure mode. By design, `normalizeResponse()` cannot
distinguish a genuine zero-match response from a swallowed HTTP error — `ZoomInfo Search
Rung1`/`Rung2`'s own try/catch converts any exception to `{error: ...}`, and
`normalizeResponse` reads `Array.isArray(raw.data)` on that shape as `false`, silently
returning `[]` either way; the raw HTTP status is never persisted onto the item. No
`executionStatus` on any node read anything but `success`, so no exception surfaced at the
node level either way. This was **not re-tested** to avoid a second live search (the D-11
per-company provider-attempt ceiling and general parsimony about spending more provider
budget just to confirm a diagnostic gap) and does not block any of the three operator stop
conditions (no non-`false` write flag, exactly one CREATE, no burst). Recommend a
follow-up: either persist the raw HTTP status onto the item for diagnosability, or accept
that a genuine provider-side flake is indistinguishable from empty-by-design in this
lane's current telemetry.

## 6. Burst watch `[observed live]`

Global executions list polled 3x at ~25s intervals starting at `11:56:02Z` (send was
`11:53:15Z`), then re-checked again at `11:57:00Z` after the backend-status read below —
**over 3.5 minutes total, zero additional executions on the discovery workflow.** The one
new execution seen at the final check (`12667`) is the backend-status lane's own execution
from step 7 below, on a different workflow id (`Cj83mOgrIm59oxcX`), correctly attributable
and not a discovery burst.

## 7. Portal read (D-14c) `[observed live]`

Run after the burst watch, deliberately, so its own execution does not confound the watch.
`backend_status.fetch_backend_status(cfg)`: `available=True`, `portalId=22617666` —
matches the plugin config's `hubspot_portal_id`. **D-14c upgraded from `[documented]` to
`[observed live]`**: the n8n-side credential's `account-info` scope is confirmed granted.

## 8. D-11b sample (evidence for a later ruling, not a change)

For the gap companies searched in this proof, zero ZoomInfo rung-1 hits were returned (see
section 5's notable observation), so there were zero titles available to run through
`role_classify.classify_title` — the sample is **0 hits / 0 classified / 0 dropped**. This
is a legitimate result for this specific round, not a null result from a broken step; it
simply means this round produced no D-11b evidence. A future round with non-empty rung-1
hits is needed before D-11b's operator ruling (trust the provider's title match vs. run the
classifier) has anything substantive to weigh.

## 9. Redaction

No runData fixture was frozen from this session's execution (`12666`) — freezing is
optional per the plan and nothing here needed pinning. The redaction verify command was
still run against the repo's existing frozen fixtures:

```
/usr/bin/grep -c 'x-enrichment-secret' tests/n8n/fixtures/frozen/*.json | /usr/bin/grep -v ':0$' | wc -l
-> 5
```

**This is a pre-existing condition, not introduced by this task.** Five fixtures from
Phase 70 (`exec_12354.runData.json` through `exec_12358.runData.json`, 2026-09-10, pinned
by `tests/n8n/v1RuntimeRecordings.test.mjs` / `tests/n8n/walkerEngineFidelityV1.test.mjs`)
still carry an un-redacted Webhook Trigger `headers` object. Checked (without printing the
value) whether the CURRENT live `webhook_secret` appears in one of them: **it does not** —
the leaked value is an old, already-superseded secret, not the credential currently bound
to the live workflows. Out of scope for this task to fix (redacting risks breaking the two
pinned tests' exact byte-content assertions, and this task created none of these files);
logged as a broken-windows defect for a follow-up redaction pass (`gsd_run query windows
append` was attempted but `gsd-tools.cjs` was not resolvable in this checkout — recorded
here and in the SUMMARY instead).

## 10. Automated verify

```
.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/
-> 3209 passed, 10 skipped

node --test tests/n8n/*.test.mjs
-> tests 1280, pass 1280, fail 0
```

Both green. The task's own redaction grep (section 9) is the one automated check in this
task's `<verify>` block that does **not** currently pass, for the pre-existing reason
recorded above.

## Issues Encountered (carried into the plan SUMMARY)

1. **Secret exposure in this session's own transcript.** Early in Task 3, `cat
   operator-claude-plugin/config/operator.local.json` was run to inspect its shape and
   printed the live `n8n_api_key` and `webhook_secret` into tool output. Neither value was
   written to any file or committed. **Recommend rotating both values** (the n8n API key
   and the webhook shared secret) out of caution, since they are now present in this
   session's transcript.
2. **Pre-existing frozen-fixture secret leak** — section 9 above. Confirmed not the
   current live secret; still worth a follow-up redaction pass.
3. **Zero-people anomaly** — section 5 above. Evidence recorded, not fixed, not blocking.
   Follow-through below (section 5a).

---

## 5a. Task 3 follow-through — isolating and fixing the zero-people anomaly

Rule 3 deviation (blocking issue for the just-shipped lane), same disarmed scope the
operator already approved. Isolated offline first (zero live executions), fixed, then two
further live disarmed proof sends — within the "≤2 further proof executions" budget for
this follow-through, counting section 5's `12666` as the baseline.

### Isolation (offline, zero credits — ZoomInfo search is measured 0 credits per §5's own
### note, and the isolation itself made no live call at all)

Four zero-credit Python replays of the lane's exact `buildRequest`/`buildUrl` shapes (done
by the orchestrator before this task spawned) found:

- **Rung 1 could never have succeeded as built.** `_discovery_role_titles()`'s 42-title
  list (measured; corrects section 5's "46-title" figure above) OR-joins to **757 chars**.
  ZoomInfo's own 400 (`PFAPI0006`, pointer `/data/attributes/jobTitle`) reads *"Input
  parameter 'jobTitle' must be less than 500 characters."* The leaf's try/catch converted
  that 400 into a swallowed `{error}`, and `normalizeResponse` read it as `[]` — identical
  to a genuine zero-match response.
- **Rung 2's cause was not isolated offline** — the identical unfiltered request returned
  `5/1094` from Python but `[]` from n8n, with no status/error persisted anywhere in
  runData to explain the gap.

### Fix 1 — the 500-char cap (commit `4736fbd8`)

`n8n/code/discoverySearch.js`: `ZOOMINFO_JOBTITLE_MAX = 500` and `capRoleTitles()`, a
greedy stop-before-cap OR-join that never splits a title mid-string, applied inside
`buildRequest` itself so no caller can bypass it. Pinned in `discoverySearch.test.mjs`.
Against the real 42-title list: **28 titles used, 14 dropped, joined length 496** (first
dropped title: `Trackwork Supervisor` — earlier families in `role_vocabulary.yaml` survive
the cap, later ones do not; a discretion worth naming, not a ruling).

### Fix 2 — per-rung diagnostics (commit `4736fbd8`, refined `9a14237b`)

`ZoomInfo Search Rung1`/`Rung2` now stamp `_zoominfo_{rung}_status/_error/_total` (reusing
`zoominfoToken.js`'s `extractErrorStatus`, the same idiom the enrich-lane ZoomInfo hops
already use) plus rung 1's own `_titles_used`/`_titles_dropped`. `Adapt ZoomInfo People`
folds these into one `search_diagnostics` object per company and deletes the scratch keys;
`Build Discovery Response` passes it through. A `200` with no readable
`res.meta.totalResults` records `unexpected-shape:<keys or typeof>` rather than a bare
null, so a response-shape problem is distinguishable from a genuine zero-match without a
second live call.

### Live disarmed redeploy 1 + proof send 1 `[observed live]`

Scoped deploy: `deploy_n8n_workflows.main(["--only", "wf_suggest_discovery_cloud.json"])`
— dry-run showed exactly one update (`LV Suggest Discovery (Cloud template)`), zero
creates; the real disarmed deploy then updated that one workflow at 200; the standard
six-workflow bounce read back all six `active`, node counts matching committed JSON,
every `ALLOW_*` flag absent/`false`, `executionOrder: v1` — `OK` verdict.

One proof send, same 3 companies as section 5's `12666` (`proof-tennis` tennis.com.au,
`proof-nongap` hubspot.com non-gap, `proof-pickleball` pickleballaustralia.org.au),
`per_company_cap: 5`, via `suggest_discovery.fetch_discovery`:

- **Execution `12668`** (2026-09-18T12:20:14Z), `status: success`.
- `proof-nongap`: `search_diagnostics: null` (never entered the search lane — correct).
- `proof-tennis` / `proof-pickleball`: **both rungs now `status: 401`**,
  `error: "Request failed with status code 401"`. Rung 1's `titles_used: 28,
  titles_dropped: 14` — confirms the cap fix landed and is arithmetically correct.
- runData confirmed `ZoomInfo Search Mint` / `ZoomInfo Search Cache Token` **did not run**
  — the leaf reused the token cached by execution `12666` (27 minutes earlier).
  `needsMint()` judged it still fresh (correctly — see the JWT check below, this was not
  a TTL bug). The cap fix surfaced a SECOND, independent defect the swallowed-error
  design had been hiding: **whatever the true cause of the 401, the search leaf had no
  `isAuthError`-clears-cache path at all** (the enrich-lane leaves
  `_zoom_split_enrich_contacts_js`/`_zoom_split_enrich_companies_js` already have this
  exact path), so a rejected token, once cached, would have been retried forever with no
  recovery mechanism regardless of why it was rejected.

### Fix 3 — clear the cached token on 401 (commit `f0af2055`)

Mirrors the enrich lane precedent exactly: on `isAuthError(extractErrorStatus(e))`,
`delete sd.zoominfo` (workflow static data) so the **next** execution re-mints. A cloud
split-code-node cannot mint inline (no credential access there — only the credential-bound
Mint HTTP node can) so this self-heals across runs, not within one, the same accepted
limitation the enrich lane already carries.

### Live disarmed redeploy 2 + proof send 2 `[observed live]`

Same scoped dry-run → real deploy → six-workflow bounce sequence, both clean (`OK`
verdict, all six active/v1/flags-false). Same 3-company envelope re-sent:

- **Execution `12669`** (2026-09-18T12:23:30Z), `status: success`.
- Still both rungs `401` for both gap companies — expected: `12669` itself is the run that
  first carries the fix, and the fix only clears the cache for a run's OWN 401s so the
  *next* run re-mints; `12669` was still handed the same stale token `12668` was.
- **Confirmed the fix fired without spending a third live send**: a plain read-only
  `GET /workflows/VJJBZ2oJ0079MSzG` (zero executions, zero cost) read back
  `staticData.global: {}` — the cached ZoomInfo token was deleted, exactly as `f0af2055`'s
  `delete sd.zoominfo` predicts. The next real execution of this lane (an operator send,
  or a future proof) will see `zoom_needs_mint: true`, trigger a genuine fresh mint via the
  credential-bound Mint node, and use a valid token.
- **Burst watch**: 3 checks over ~75s after `12669`, zero new executions — no burst.

### JWT `exp` check — the TTL-expiry explanation is RULED OUT `[observed live]`

Before writing up root cause, decoded (offline, read-only, zero executions) the `exp`/`iat`
claims of the JWT `12666` cached — never the token itself, never any other claim:

```
iat = 1789732398 (2026-09-18T11:53:18Z)
exp = 1789818798 (2026-09-19T11:53:18Z)
lifetime_seconds = 86400   # 24 hours
```

The token was minted at `11:53:18Z` with a **24-hour** lifetime. Both `12668` (`12:20:14Z`,
+27 min) and `12669` (`12:23:30Z`, +30 min) rejected it **more than 23 hours before its own
claimed expiry**. `needsMint()`'s cache decision was correct given this JWT's real `exp` —
this was never a TTL-underestimate bug. The earlier "root cause" draft of this section
(before this check) attributed the 401 to token expiry; that attribution was wrong and has
been corrected below.

### Root cause, summarized — genuinely unresolved

Two independent defects were found; only one is confirmed fixed:

1. **Rung 1's title filter could never have succeeded** — a vendor 400 (`PFAPI0006`)
   masked as an empty result by the swallowed try/catch. **Fixed and confirmed arithmetic
   (`titles_used: 28`)**, though not yet confirmed against a real `200` (see below).
2. **The search leaf had no recovery path for a rejected token** — fixed by mirroring the
   enrich lane's `isAuthError`-clears-cache precedent. This causes the *next* execution to
   re-mint. **What is NOT confirmed: whether a freshly-minted token will succeed against
   `/contacts/search` at all.** The JWT check above rules out simple expiry as the 401's
   cause; the true cause is unresolved — candidates include a scope/product-entitlement
   difference between ZoomInfo's Enrich API (which this account's credential is confirmed
   to work against, live, elsewhere in this codebase) and its Search API (used only by
   this lane), or something specific to this credential/account that a re-mint will not
   change. If the true cause is a scope/entitlement gap, the next execution's fresh mint
   will re-mint successfully (the OAuth grant itself works) but the SUBSEQUENT search call
   may 401 again for the same underlying reason — the cache-clear fix would then be
   correctly recovering the token lifecycle while never actually resolving the visible
   symptom. This was not distinguishable from the available evidence without spending a
   third additional live execution, which this follow-through's budget does not cover.
   Whether `12666`'s original (pre-diagnostics) zero-people result hit the same 401, or a
   genuine zero-match, or something else entirely, cannot be determined retroactively.

**Recommended next step** (not performed here, outside today's budget): when the discovery
lane is next invoked live (an operator `suggest-contacts` run, or a dedicated follow-up),
read `search_diagnostics` off the response. A `200` with a real `people` list or a genuine
`total: 0` closes this out. A THIRD `401` — this time on a token confirmed fresh via
`ZoomInfo Search Mint` appearing in that execution's runData — would confirm a scope/
entitlement gap on this ZoomInfo account's Search API access, which needs an account-side
escalation, not another code fix.

**Orchestrator addendum (2026-09-18, read-only, zero executions) `[observed live]`.** The
token n8n minted in `12666` and reused in `12668`/`12669` was decoded from runData (claims
only, never the value): `iss` Okta `oauth2/default`, `aud api://default`, `cid` prefix `0oa1`,
`ziApplicationName: GTM API`, 24 h lifetime, and `scp` = the SAME 17-scope list (including
`api:data:contact`) as a token minted locally by `scripts/check_provider_credits.py` from the
repo `.env` — which returned 200 / `totalResults: 1094` on `/gtm/data/v1/contacts/search` in
four Python replays the same hour (query string with raw AND `%5B`-encoded brackets both
200). **Entitlement/scope and client identity are therefore ruled out** alongside expiry. What
remains is something about how the n8n Code node's `this.helpers.httpRequest` issues THIS
request (a query-string URL plus a JSON:API body — a combination no other lane in this repo
sends; the live-proven enrich lane has no query string) versus how Python `requests` issues
it. The decisive next step is one diagnostic execution inside n8n that tries the same search
with the pagination moved from the URL into the request options (`qs`), and with no
pagination at all, and persists each status. Rung 1's `PFAPI0006` cap fix is real regardless
(`titles_used: 28`), but its effect cannot be observed until the 401 is gone.

**Not verified live in this follow-through**: an execution that actually reaches ZoomInfo
with a freshly-minted token AND the corrected sub-500-char rung-1 body, to confirm
`tennis.com.au` returns real people. That is the next real send on this lane — either the
operator's next `suggest-contacts` invocation, or a follow-up proof task, whichever comes
first. Not chased further here, per the "≤2 further proof executions" budget for this
follow-through and the same parsimony section 5's original note states about live provider
spend.

---

## 5b. Plan 11 Task 3 gap closure — D-11 closed live, D-11a closed live (2026-09-18)

Follows directly from section 5a. Two live disarmed executions, both `[observed live]`,
against the redeployed `LV Suggest Discovery (Cloud template)` (`VJJBZ2oJ0079MSzG`), within
the plan's 3-execution ceiling (2 spent, 1 unused).

### Precondition + deploy 1 + bounce `[observed live]`

Read-only precheck confirmed the workflow reachable, `active`, 26 nodes, `executionOrder:
v1`, pre-send max execution id `12669` on this workflow. Scoped dry-run deploy
(`--only wf_suggest_discovery_cloud.json`) showed exactly one update, zero creates; the real
scoped deploy then updated that one workflow at 200. The standard six-workflow bounce read
back all six `active`, node counts matching committed JSON (33/98/287/55/43/26), every
`ALLOW_HUBSPOT_RECORD_WRITES`/`ALLOW_HUBSPOT_CREATE` reading `false` or absent, all six
`executionOrder: v1` — `OK` verdict.

### Send 1 — the discriminator — execution `12670` `[observed live]`

One disarmed POST (`hubspot/suggest/discover`) carrying a client-minted `run_id`,
`per_company_cap: 5`, and two companies: `73.1-11-proof-gap` (`gap: true`,
`domain: tennis.com.au`, `role_families: ["CEO", "Chair", "Executive Officer", "Board Chair"]`)
and `73.1-11-proof-nongap` (`gap: false`, `hubspot.com`).

- `73.1-11-proof-nongap`: `search_diagnostics: null` (never entered the search lane — correct).
- `73.1-11-proof-gap`: `search_diagnostics.rung1 = { status: 400, total: null, error: "Request
  failed with status code 400", titles_used: 9, titles_dropped: 0 }`. `rung2.status` also `400`.
- `ZoomInfo Search Mint` ran (1 item) — task 1's mint-per-execution gate fired; this was a
  freshly-minted token, not a reused one, so the `12668`/`12669` stale-token failure class did
  NOT recur here.
- `titles_used: 9, titles_dropped: 0` matches the offline-computed expectation for the four
  chosen families' deduped member set (`CEO`×1, `Chair`×4, `Executive Officer`×1,
  `Board Chair`×3 = 9 members, OR-joined to 136 chars, well under the 500-char cap) — **D-11a
  is closed live by this execution alone**, independent of what rung 1's HTTP status turned
  out to be.
- The leaf's own `catch` block (as shipped before this task) discards the vendor's response
  body, keeping only `e.message` — no JSON:API pointer was available from n8n's own runData
  for this 400.

### Offline diagnostic (zero n8n executions, zero cost) — the 400 isolated to n8n's own request construction, not the account

A direct Python replay of the byte-identical request (same URL, same query string, same
JSON:API body built from `discoverySearch.js`'s own `buildRequest`/`buildUrl`) with a
freshly-minted token returned **200 / `totalResults: 10`**, with five real named people at
Tennis Australia (`Chief Executive Officer` × 5) — falsifying an account-side explanation:
same account, same credential grant, same request shape, succeeded outside n8n. This isolates
the discrepancy to how n8n's `this.helpers.httpRequest` (axios) constructs/sends THIS specific
request (a query string appended to the URL, a shape no other live-proven lane in this repo
sends) versus how Python's `requests` sends the identical shape — exactly the "orchestrator
addendum" hypothesis recorded in section 5a, now confirmed as the live cause of execution
`12670`'s 400, not the 401/token-lifecycle question section 5a and Task 2 investigated (that
question is independently answered — see Task 2's `73.1-TOKEN-REPLAY-VERDICT.json` — but does
not explain this execution, since 400 ≠ 401 and the token here was freshly minted this run).

**Deviation (Rule 1 — bug, auto-fixed inline).** The plan's own branch table maps a
request-construction fix to the "`rung1.status` is 401 AND verdict false" branch; this
execution returned 400, which the branch table did not explicitly anticipate. The plan's own
objective already states the reasoning that resolves it: *"A malformed query string yields
400, not 401"* — the 400 observed here is exactly that predicted signature. Applied the same
fix the 401 branch prescribes: `buildUrl` now returns the bare ZoomInfo endpoint with no query
string; pagination moved to a new `buildQuery` helper, passed via the `httpRequest` node's own
`qs` option (`n8n/code/discoverySearch.js`, `scripts/build_cloud_workflows.py`). Also added
`extractErrorDetail(e)` (JSON:API `code`/`title`/`detail`/`source.pointer` only, never the raw
body) so a future 4xx captures the vendor's own pointer instead of discarding it — the gap
that left `12670` without a vendor pointer to record. `scripts/probe_provider_discovery.py`'s
`_search_url`/`_replay_search` updated to match (bare URL + `params=`), plus its own offline
test and `tests/n8n/discoverySearch.test.mjs`. Regenerated, redeployed scoped, bounced (see
below), before spending execution 2 — full offline suites (`node --test tests/n8n/*.test.mjs`,
`.venv/bin/python -m pytest`) green before the redeploy.

### Deploy 2 (the fix) + bounce `[observed live]`

Same scoped dry-run → real deploy → six-workflow bounce sequence as above, both clean (one
update, zero creates; `OK` verdict, all six active/v1/flags-false/node-counts-matching).

### Send 2 — execution `12671` `[observed live]` — D-11 closed

Same 2-company envelope re-sent (`run_id` freshly minted). Result:

- `73.1-11-proof-gap`: `search_diagnostics.rung1 = { status: 200, total: 10, error: null,
  titles_used: 9, titles_dropped: 0 }`. `rung2: null` (rung 1 had results, rung 2 never ran —
  `ZoomInfo Search Rung2` is absent from this execution's node list).
- Five real people returned, all at the gap company: **Tania Tandora**, **Cameron Pearson**,
  **Olivia Birkett**, **Mark La Brooy**, **Debbie Sterrey** — each `jobtitle: "Chief Executive
  Officer"`, `provider: "zoominfo"`.
- `ZoomInfo Search Mint` ran (1 item) — a second independent freshly-minted token, confirming
  Task 1's mint-per-execution gate holds across repeated sends.
- Burst watch: re-listed executions immediately after send 2, zero new executions beyond
  `12671` — no runaway.

### D-11 / D-11a verdicts

**D-11: CLOSED.** A live 200 from `/gtm/data/v1/contacts/search` inside n8n, execution
`12671`, with a real people list (5 named contacts at Tennis Australia) for a domain
independently confirmed at a much larger unfiltered total via Python (execution `12670`'s
offline replay and 73.1-D12-VERDICT's own round-4 probe both corroborate the domain is real
and searchable). `rung1.total: 10` under the D-11c-narrowed 4-family filter, consistent with
the unfiltered ~1094-total domain being real and the filter genuinely narrowing it.

**D-11a: CLOSED, live, on BOTH executions.** `titles_used: 9` equals the deduped member count
of the four chosen families (`CEO`, `Chair`, `Executive Officer`, `Board Chair`) exactly;
`titles_dropped: 0` on both `12670` and `12671`. D-11a does not depend on rung 1's HTTP
status — the title-cap arithmetic runs before the HTTP call — so it was proven live by the
FIRST execution (`12670`) and reconfirmed by the second.

### Live read-back, discovery workflow only, at the end of this task `[observed live]`

`VJJBZ2oJ0079MSzG` (`LV Suggest Discovery (Cloud template)`): `active: true`, 26 nodes
(matches committed JSON), `executionOrder: v1`. No `ALLOW_*` flag of any kind exists on this
lane (zero HubSpot write nodes, D-06) — nothing to arm, nothing armed. Total executions
spent this task: **2 of the 3-execution ceiling** (`12670`, `12671`); the third was not
needed.

### Deferred (not this task's scope)

Whether `titlesForFamilies`'s deliberate no-op-on-unknown-label behaviour (73.1-10) should
ever surface a warning to the operator, and whether the enrich lane's 24h token cache should
be dropped given Task 2's `prior_token_invalidated_by_later_mint: true` finding, are both
recorded as their own `kind: design`/pre-existing todos, not decided here.

---
*Phase: 73.1-provider-backed-contact-discovery-as-source-tier-2*
*Task 3 completed: 2026-09-18*
*Task 3 follow-through (73.1-09) completed: 2026-09-18*
*Plan 11 Task 3 gap closure completed: 2026-09-18*

### CR-01 discriminating live proof — execution `12675` `[observed live]` — verify-work Test 1, 2026-09-19

The code-review blocker CR-01 (73.1-REVIEW.md) predicted that a round with N `gap: true`
companies would fire N `ZoomInfo Search Mint` calls in one burst, each invalidating the last
(73.1-TOKEN-REPLAY-VERDICT.json), leaving every company but the last with a 401. The fix
(`executeOnce: true` on the mint node + a `combineAll` carry merge, commit `96770d6d`) was
deployed scoped and bounced disarmed on 2026-09-19 but had only been reasoned about from n8n's
documentation. This send is the discriminator.

Pre-send read-only checks (zero executions): live body `updatedAt 2026-09-18T14:48:08.954Z`,
26 nodes, `active`, `executionOrder: v1`; `ZoomInfo Search Mint` reads `executeOnce: true`,
`ZoomInfo Search Mint Carry Merge` reads `mode: combine`, `combineBy: combineAll`. Pre-send max
execution id on `VJJBZ2oJ0079MSzG`: `12671`.

One disarmed POST (`hubspot/suggest/discover`), client-minted `run_id`, `per_company_cap: 5`,
THREE companies — `73.1-cr01-gap-a` (`gap: true`, `tennis.com.au`), `73.1-cr01-gap-b`
(`gap: true`, `afl.com.au`), both with `role_families: ["CEO", "Chair", "Executive Officer",
"Board Chair"]`, and `73.1-cr01-nongap` (`gap: false`, `hubspot.com`) as the control.

Result — execution `12675`, `status: success`, one execution consumed (the last of plan 11's
3-execution budget):

- `73.1-cr01-gap-a`: `search_diagnostics.rung1 = { status: 200, total: 10, error: null,
  titles_used: 9, titles_dropped: 0, unknown_role_families: [] }`, `rung2: null`; the same five
  Tennis Australia people as execution `12671`.
- `73.1-cr01-gap-b`: `search_diagnostics.rung1 = { status: 200, total: 4, error: null,
  titles_used: 9, titles_dropped: 0, unknown_role_families: [] }`, `rung2: null`; four people
  at the AFL (titles: `Chief Executive Officer`, `Vice Chairman of the VFL Tribunal`,
  `Executive Assistant To Chief Executive Officer`, `Assistant To the Chief Executive Officer`).
- `73.1-cr01-nongap`: `search_diagnostics: null`, `people: []` — never entered the search lane.
- **No 200/401 mix.** Both gap companies 200.
- runData (node names and item counts only; no item ever printed): `ZoomInfo Search Token Gate`
  1 run × 2 items → `IF ZoomInfo Search Needs Mint` 1 × 2 → **`ZoomInfo Search Mint` 1 run × 1
  item** → `ZoomInfo Search Mint Carry Merge` 1 × 2 → `ZoomInfo Search Cache Token` 1 × 2 →
  `ZoomInfo Search Rung1` 1 × 2 → `Adapt ZoomInfo People` 1 × 2 → `Discovery Response Merge`
  1 × 3 → `Build Discovery Response` 1 × 1. Every sentinel 0 items. Exactly ONE mint for two
  gap rows; the carry merge broadcast that one token onto both rows.

**Verdict: PASS.** `executeOnce` + `combineAll` on this engine upgrade from `[documented]` to
`[observed live]`. CR-01's batch failure mode is gone on the live lane.

Burst watch `[observed live]`: executions re-listed at `02:22:22Z` (113 s after the send
started) — `12675` is still the newest; zero executions followed it. No runaway.

Secret hygiene: the response body and runData excerpt above were grep-gated for `zoom_token`
and `x-enrichment-secret` before recording (0 hits); no token value, header value or raw
runData item appears in this file.

## Current Test

number: 2
name: D-11b operator ruling on provider-title trust
expected: |
  With live rung-1 hits now available (executions 12671 and 12675, 9 real people), compare
  ZoomInfo's title match against `role_classify.classify_title` on the same titles and record a
  durable ruling (CONTEXT.md amendment or new decision id) on whether `classify_title` may relax
  for ZoomInfo-search-sourced candidates.
awaiting: user response

## Tests

### 1. CR-01 discriminating live proof — 2+ gap-company disarmed send
expected: Every gap company's `search_diagnostics.rung1.status == 200`; `ZoomInfo Search Mint` ran exactly once in runData. Lane `VJJBZ2oJ0079MSzG` is deployed + bounced with the fix (2026-09-19, see 73.1-REVIEW-FIX.md deploy record); zero write nodes, nothing armed; 1 of plan 11's 3-execution budget remains.
result: pass
evidence: execution 12675 (2026-09-19T02:20:29Z) — gap-a tennis.com.au rung1 200/total 10, gap-b afl.com.au rung1 200/total 4, control null; `ZoomInfo Search Mint` runs=1 item_counts=[1] for 2 gap rows; see the "CR-01 discriminating live proof" section above

### 2. D-11b operator ruling on provider-title trust
expected: With live rung-1 hits now available (execution 12671, 5 real people), compare ZoomInfo's title match against `role_classify.classify_title` on the same titles and record a durable ruling (CONTEXT.md amendment or new decision id) on whether `classify_title` may relax for ZoomInfo-search-sourced candidates.
result: [pending]

## Summary

total: 2
passed: 1
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
