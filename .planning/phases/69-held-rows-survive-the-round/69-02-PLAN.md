---
phase: 69-held-rows-survive-the-round
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - operator-claude-plugin/tests/test_suggest_contacts_composition.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
  - .planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md
autonomous: true
requirements: [HELD-01, HELD-03]

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Step 8's held half no longer names `held_queue.build_entry` / `held_queue.save` / `run_manifest.save`; it routes every partition-declined row into `suggestion_declines`, and the skill and the code now agree on where partition holds go (HELD-01, D-69-02)."
    - "The held routing is EXECUTABLE code in a fenced python block, not prose — the same rigor `enrich-before-ingest`'s own held-row block already carries — and its call sequence is registered in `test_skill_sequence_coverage.py`'s `COVERED` in the same commit, pointing at a composition test that DRIVES the join end to end."
    - "A decline that cannot be keyed — no company id on the round's own `rounds[]` entry, or an incomplete name — is reported to the operator as unkeyable and named individually; it is never silently dropped (D-69-04)."
    - "A decline the store would refuse to persist (an arming-, secret- or token-shaped string on the row) is pre-checked with `suggestion_declines.first_refusal` before it enters the map, so one odd value costs that one decline and not the whole batch's save (D-69-01's validate-before-write, applied at the caller)."
    - "The round MERGES into the loaded document — `suggestion_declines.load()` first, this run's entries added, one `save()` — so an earlier run's deferred entries survive this round (D-69-03)."
    - "Step 9's report names, in one end-of-run batch, BOTH this run's declines AND the deferred backlog from earlier runs, read off `suggestion_declines.partition_by_run` — not a per-company halt, and no cause causes a stop (D-69-05, Phase 68's standing rule)."
    - "A round that never dispatched (`records` empty, so no `run_id` and no `held`) still shows the backlog: the step-9 surface reads the store outside the `if records:` guard and calls `partition_by_run(declines, None)`, which puts everything in the backlog half rather than inventing a run handle."
    - "The brief `.planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md` is folded: its front matter carries `resolves_phase: 69`, and its candidate fix (3) — mapping a partition code onto a match-gate code at the boundary — is implemented nowhere."
    - "FLAGGED ASSUMPTION (probe item HELD-01, category `unclassified`, never backstop-resolved): the probe could not classify HELD-01, so this plan assumes \"the skill and the code agree\" is satisfied by (a) step 8's prose and its executable fence both naming `suggestion_declines`, and (b) a composition test proving the documented sequence runs without raising `HeldQueueError`. If the operator meant a wider agreement — every skill's every held path, or a static cross-check between prose and code — this plan under-delivers and the gap is here, named."
  artifacts:
    - "operator-claude-plugin/skills/suggest-contacts/SKILL.md — step 8's rewritten held half, step 9's decline + backlog report"
    - "operator-claude-plugin/tests/test_suggest_contacts_composition.py — two new composition tests"
    - "operator-claude-plugin/tests/test_skill_sequence_coverage.py — two new `COVERED` entries"
  key_links:
    - "Step 8's held fence -> `suggestion_declines.load` -> `suggest_contacts.company_id_for_index` -> `suggestion_declines.entry_key` -> `suggestion_declines.first_refusal` -> `suggestion_declines.build_entry` -> `suggestion_declines.save` -> `suggestion_declines.partition_by_run`."
    - "`entry[\"index\"]` indexes `records` — the original index both `partition_for_dispatch` and `search_fallback.hold_weak_sources` stamp and re-sort by — so `records[entry[\"index\"]][\"provenance\"]` is that decline's own provenance."
    - "Step 9's empty-records fence -> `suggestion_declines.load` -> `suggestion_declines.partition_by_run(declines, None)`."
  prohibitions:
    - "`confidence.assess`, `held_queue.build_entry` and `held_queue.save` appear nowhere on the partition-hold path in `suggest-contacts/SKILL.md`."
    - "No partition reason code is translated, aliased, or mapped onto a member of `confidence.ALL_HOLD_CODES` at the step-8 boundary — the brief's rejected fix (3)."
    - "The held routing never keys on `row_id`."
    - "Step 9's decline surface never halts the round, never asks a per-company question, and never blocks the report on an operator answer."
    - "Nothing in this plan writes state to HubSpot, arms anything, spends a provider credit, or dispatches."
    - "No file under `n8n/` and no line of `scripts/build_cloud_workflows.py` is touched."
    - "`suggest-contacts/SKILL.md` gains neither of the two substrings D-10b forbids (the pair `test_autonomy_switch_prose.py::test_no_icp_or_tier_substring_anywhere_in_the_file` already asserts for this file) — in particular the search-fallback hold-code CONSTANT is never named in a fence; write the string value or read it off the entry."
    - "`test_skill_sequence_coverage.py`'s `GRANDFATHERED_UNCOVERED` gains no entry and `MAX_GRANDFATHERED` is not raised."
    - "Step 8's existing `COVERED` tuple for the main round pipeline is not edited — the held routing lands in its OWN fence, so the big tuple's identity is unchanged."
---

<objective>
Make the skill and the code agree. Rewrite `suggest-contacts/SKILL.md` step 8's held half so a
partition-declined person is routed into `suggestion_declines` — as executable code, driven by
a composition test — and widen step 9's report so the operator sees this run's declines and the
deferred backlog in one end-of-run batch.

Purpose: HELD-01's live defect. Step 8 tells the round to call `held_queue.build_entry`;
`held_queue.save` raises `HeldQueueError` on every partition code, by design. The code is
right and the skill is wrong; this is where the skill changes.

Output: a rewritten step 8 held half, a widened step 9 report, two composition tests, two
`COVERED` registrations, and the brief folded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-held-rows-survive-the-round/69-CONTEXT.md
@.planning/phases/69-held-rows-survive-the-round/69-RESEARCH.md
@.planning/phases/69-held-rows-survive-the-round/69-PATTERNS.md
@.planning/phases/69-held-rows-survive-the-round/69-01-SUMMARY.md

@operator-claude-plugin/skills/suggest-contacts/SKILL.md
@operator-claude-plugin/scripts/suggestion_declines.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Step 8's held half — one declined person, from the partition to the store, in code</name>
  <files>operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_suggest_contacts_composition.py, operator-claude-plugin/tests/test_skill_sequence_coverage.py</files>
  <read_first>
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md:432-442` (the defective prose sentence naming `confidence.assess()` / `held_queue.build_entry()` / `run_manifest.save()`) and `:454-639` (step 8's existing single fence — the one whose identity must NOT change)
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md:597-639` (the `rounds.append({...  "start", "count" ...})` structure and the terminal classify loop the new fence sits after)
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md:661-691` (the held-row block whose SHAPE is copied — load, build, merge into the map, save — never its `confidence`/`held_queue` vocabulary)
    - `operator-claude-plugin/scripts/suggestion_declines.py` (as built by plan 01 — `load`, `entry_key`, `first_refusal`, `build_entry`, `save`, `partition_by_run`)
    - `operator-claude-plugin/scripts/suggest_contacts.py` — `company_id_for_index`, `name_key`, `partition_for_dispatch`
    - `operator-claude-plugin/scripts/search_fallback.py:358-403` (`hold_weak_sources` — confirms the merged `held` keeps each record's ORIGINAL index and is re-sorted by it)
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:80-200` (`parse_calls`/`sequences_in` — identity is per-FENCE, so a new fence is a new identity) and `:390-419` (the existing `suggest-contacts` tuple, left untouched)
    - `operator-claude-plugin/tests/test_suggest_contacts_composition.py:77-285` (`test_the_documented_round_pipeline_drives_its_real_joins_end_to_end` — the fixture idiom to reuse) and `:33-40` (`_company_row`, which carries no `id` today)
  </read_first>
  <behavior>
    RED first, verified by reading pytest's own failure output (no `gsd-tools check
    tdd-red-evidence` in this install):
    - `test_the_documented_step_8_held_routing_persists_a_declined_person`: build two eligible
      company rows that DO carry an `id`, drive the documented round to a `records` /
      `rounds` / `held` state through the real `partition_for_dispatch`, then run step 8's new
      held sequence exactly as the fence documents it — `load` (empty), resolve `company_id`,
      `entry_key`, `first_refusal`, `build_entry`, `save(path=tmp)` — and assert: the saved
      map has one entry per held row; each entry's `reason_code` is the code the partition
      actually stamped; each `company_id` is the owning company's `id`, not the other
      company's; the whole sequence raises no `HeldQueueError` and no `SuggestionDeclineError`.
    - `test_a_second_round_merges_into_the_first_rounds_declines`: save a first run's map, then
      run a second round whose `held` contains one NEW person and one person already stored
      from run 1, loading before merging. Assert the file afterwards holds three entries (not
      two, not four), the re-found person's entry carries the SECOND run's `run_id`, and the
      un-rediscovered first-run entry is still present byte-for-byte in its `row`.
    - `test_a_decline_with_no_company_id_is_reported_unkeyable_and_never_dropped`: a round
      whose company row carries no `id` yields `entry_key(...) is None`; assert the saved map
      is empty AND that the documented fence's `unkeyable` list carries that held entry, so
      the report has something to name.
    - `test_step_8_held_routing_never_calls_held_queue`: parse `suggest-contacts/SKILL.md`'s
      python fences with the module's own `extract_python_blocks`/`parse_calls` helpers and
      assert no extracted call is `held_queue.build_entry`, `held_queue.save` or
      `confidence.assess`.
  </behavior>
  <action>
    In `operator-claude-plugin/skills/suggest-contacts/SKILL.md` step 8:

    (1) Replace the sentence at :440-442 ("The held half is handled exactly as
    `enrich-before-ingest/SKILL.md`'s own held-row path: ...") with prose that states the
    correction and why it is a correction, not a widening: a suggestion-round partition code
    answers "identified fine, declined to send", which is a different question from the
    match-gate vocabulary `held_queue` validates against, so a decline goes to its own store
    — `suggestion_declines` — and `held_queue`'s refusal on these codes stays correct
    behaviour (D-69-02, HELD-01). Name the live case in one line: the Roma Turf Club round
    held two real committee members correctly and the only record of them was a chat message.

    (2) Add a NEW fenced python block immediately after the existing step-8 fence — never
    inside it, so the existing `COVERED` tuple's identity is unchanged. The fence runs INSIDE
    the `if records:` branch, after the terminal classify loop, over `held`, `records`,
    `rounds` and the step-5-sourced `run_id`, and does exactly this, in order: import
    `suggestion_declines`; `declines = suggestion_declines.load()`; bind two empty lists
    `unkeyable` and `unstorable`; loop the held entries, resolving
    `company_id = suggest_contacts.company_id_for_index(rounds, entry["index"])`, then
    `key = suggestion_declines.entry_key(company_id, entry["row"])`, appending to `unkeyable`
    and continuing when `key` is `None`; build the candidate with
    `suggestion_declines.build_entry(entry["row"], entry["reason_code"], entry["reason"],
    run_id, company_id, records[entry["index"]]["provenance"])`; pre-check it with
    `suggestion_declines.first_refusal(key, candidate)`, appending to `unstorable` with the
    returned sentence and continuing when it is not `None`; otherwise `declines[key] =
    candidate`. After the loop, call `suggestion_declines.save(declines)` only when this
    round added at least one entry, then
    `batch = suggestion_declines.partition_by_run(declines, run_id)` — the structure step 9
    renders. No `while`, no `sleep`, no second save.

    (3) Add short prose under the fence stating the three ceilings plainly: the pre-check
    exists so one odd string costs one decline instead of the whole batch's save; an
    `unkeyable` or `unstorable` decline is named individually in step 9's report and is never
    silently dropped; and the save merges into what `load()` returned, so an earlier run's
    deferred entries survive this round.

    In `operator-claude-plugin/tests/test_suggest_contacts_composition.py`, add the four tests
    in `<behavior>`, reusing the file's existing `_company_row` idiom (extended with an `id`
    where the test needs one) and passing an explicit `path=` under `tmp_path` to every store
    call. In `operator-claude-plugin/tests/test_skill_sequence_coverage.py`, add ONE new
    `COVERED` entry keyed `("suggest-contacts", (<the new fence's extracted call tuple, in
    source order>))` mapped to
    `"test_suggest_contacts_composition.py::test_the_documented_step_8_held_routing_persists_a_declined_person"`,
    with a comment stating this is a NEW fence (the pre-existing round-pipeline tuple is
    untouched) and why the routing left `held_queue`. Read the tuple off the suite's own
    failure message rather than hand-deriving it. Commit RED then GREEN.
  </action>
  <acceptance_criteria>
    `operator-claude-plugin/skills/suggest-contacts/SKILL.md` contains no occurrence of
    `held_queue.` or `confidence.assess` anywhere in the file (asserted by
    `test_step_8_held_routing_never_calls_held_queue`, which parses the fences, plus a plain
    substring assertion over the file text for the prose sentence). The suite
    `test_skill_sequence_coverage.py` passes with no `UNREGISTERED SKILL SEQUENCE` message,
    `GRANDFATHERED_UNCOVERED == {}` and `MAX_GRANDFATHERED == 0` unchanged. The composition
    test that drives the sequence completes without raising `held_queue.HeldQueueError`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts_composition.py operator-claude-plugin/tests/test_skill_sequence_coverage.py -q</automated>
    <fails_when>pytest exits non-zero, or its output contains the string `UNREGISTERED SKILL SEQUENCE` (a fence whose tuple was not registered), or a `held_queue.HeldQueueError` traceback (the routing still reaches the wrong store).</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_autonomy_switch_prose.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py operator-claude-plugin/tests/test_implicit_approval_contract.py operator-claude-plugin/tests/test_interrupt_semantics.py operator-claude-plugin/tests/test_disclosure_audit.py -q</automated>
    <fails_when>pytest exits non-zero — in particular a failure from `test_no_icp_or_tier_substring_anywhere_in_the_file[suggest-contacts]` (a forbidden substring entered the file), from `test_suggest_contacts_step_3_is_split_not_collapsed`, or from `test_suggest_contacts_run_id_is_sourced_not_minted` (the step-8 edit disturbed the run handle).</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>The command prints any line at all.</fails_when>
  </verify>
  <done>A partition-declined person produced by the real `partition_for_dispatch` is persisted by the sequence step 8 documents, driven by a registered composition test, with no call into `held_queue` anywhere on the path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Step 9 reports this run's declines AND the backlog — including when nothing dispatched</name>
  <files>operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_suggest_contacts_composition.py, operator-claude-plugin/tests/test_skill_sequence_coverage.py, .planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md</files>
  <read_first>
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md:640-709` (step 9's whole report — the five-reason-code grouping the decline surface sits beside, the mandatory `run_report` fence, and the "When `records` is empty, there is no run and no report" paragraph the backlog surface must survive)
    - `operator-claude-plugin/scripts/suggestion_declines.py` — `load`, `partition_by_run`, `classify_read`
    - `.planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md` (the brief in full — its candidate fix (3) is the one implemented nowhere)
    - `.planning/todos/completed/2026-08-10-runtime-cadence-has-no-budget-floor.md:1-8` (the `resolves_phase` front-matter convention)
    - `operator-claude-plugin/tests/test_disclosure_audit.py:1-30` (Phase 68's standing rule: a statement of fact the operator cannot act on differently is not a decision point — this surface is a report, never a halt)
  </read_first>
  <behavior>
    RED first, verified by reading pytest's own failure output:
    - `test_the_documented_empty_records_path_still_reads_the_backlog`: with a store already
      holding two entries from an earlier run and a round in which every company finds nobody
      (so `records` is empty and there is no `run_id`), drive step 9's documented backlog
      fence — `suggestion_declines.load()` then `partition_by_run(declines, None)` — and
      assert `this_run` is empty, `backlog` has both entries, and nothing raises. Register its
      nodeid in `COVERED`.
    - `test_partition_by_run_splits_this_rounds_declines_from_the_backlog`: one store holding
      entries from run A and run B; `partition_by_run(entries, "run-B")` puts exactly run B's
      in `this_run` and run A's in `backlog`, and the two halves reunite to the input.
    - A plain assertion over `suggest-contacts/SKILL.md`'s text that step 9's decline section
      names the store's three read states (`absent` / `parseable` / `anomalous`) so an
      unreadable store is reported honestly rather than as "nothing to drain".
  </behavior>
  <action>
    In `operator-claude-plugin/skills/suggest-contacts/SKILL.md` step 9, immediately after the
    existing five-reason-code grouping paragraph and BEFORE the mandatory end-of-run account
    fence:

    (1) Add a "What was held, and what is still waiting" subsection. It renders `batch`
    (bound by task 1's fence): under "held this round", one line per entry in
    `batch["this_run"]` — the person's name, the company, the `reason_code` in the operator's
    own words using step 9's existing five-code vocabulary, and the entry's prose `reason`;
    under "still waiting from earlier rounds", the same per entry in `batch["backlog"]`, each
    also naming the run it was declined in. Then the two exception lists task 1 built:
    `unkeyable` (named individually, with the sentence "this one could not be stored — the
    round had no HubSpot company id for it") and `unstorable` (named individually with the
    sentence `first_refusal` returned). State plainly, in the operator's words, that this is
    one end-of-run batch and not a per-company halt, that no answer is required here, and
    that the round is over either way (D-69-05, Phase 68's standing rule).

    (2) Amend the "When `records` is empty, there is no run and no report" paragraph so it
    stays true AND stops hiding the backlog: keep its refusal to invent a run handle verbatim,
    then add that the deferred backlog is still shown, read straight from the store with no
    run to compare against. Add a second fenced python block there containing exactly
    `declines = suggestion_declines.load()` then
    `batch = suggestion_declines.partition_by_run(declines, None)` — two calls, one result
    flowing into the next, so it is a real sequence and gets a real `COVERED` entry rather
    than a contrived two-fence split.

    (3) Add one sentence stating what the surface says when the store cannot be read: call
    `suggestion_declines.classify_read()` and, on `anomalous`, say the store exists and could
    not be read rather than reporting an empty backlog — an empty report over an unreadable
    file tells the operator there was nothing to look at when there may be plenty.

    In `operator-claude-plugin/tests/test_suggest_contacts_composition.py` add the tests in
    `<behavior>`; in `operator-claude-plugin/tests/test_skill_sequence_coverage.py` add the
    second `COVERED` entry for the empty-records fence, reading its tuple off the suite's own
    failure message.

    Fold the brief: add `resolves_phase: 69` to the front matter of
    `.planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md`
    (leave the file in `pending/`; phase completion moves it), and append a short "Resolution"
    section stating which of its three candidate fixes was taken — (2), its own durable-store
    option — that (1) report-only was rejected because the holds must survive the round, and
    that (3), mapping a partition code onto a match-gate code at the boundary, is implemented
    nowhere. Commit RED then GREEN.
  </action>
  <acceptance_criteria>
    `.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts_composition.py
    operator-claude-plugin/tests/test_skill_sequence_coverage.py -q` passes. The todo file's
    front matter contains the line `resolves_phase: 69`. `suggest-contacts/SKILL.md` step 9
    contains the literal words `still waiting from earlier rounds` and names all three of
    `absent`, `parseable`, `anomalous`. Step 9's `build_run_report` fence is unchanged and
    still the only one in the file (`test_mandatory_report_call_sites.py` green).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts_composition.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py operator-claude-plugin/tests/test_disclosure_audit.py -q</automated>
    <fails_when>pytest exits non-zero, or its output contains `UNREGISTERED SKILL SEQUENCE`, or a failure from `test_exactly_one_build_run_report_call[suggest-contacts]` (the step-9 edit disturbed the mandatory report fence), or from `test_audit_table_covers_every_skill_on_disk` (a skill directory appeared or vanished, which this plan does not do).</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
    <fails_when>The summary reports any failures, or fewer than 2750 passed — the Phase 68 baseline is 2750 passed / 5 skipped and this plan only adds tests.</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>The command prints any line at all.</fails_when>
  </verify>
  <done>The operator sees, in one end-of-run batch, every person this round declined and every person still waiting from earlier rounds — including on a round that dispatched nothing — and the brief is folded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| round data -> operator's durable filesystem | Step 8's new fence is the write site: a held row's provider-derived email, crawled job title and company name cross into a file that outlives the session. |
| stored entries -> operator's screen | Step 9 renders entries from earlier runs, written by code that may since have changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Information Disclosure | step 8's held fence | high | mitigate | The fence pre-checks every candidate with `suggestion_declines.first_refusal` before it enters the map, and `save` re-validates independently before writing — the caller-side check is a convenience, never the control. A refused candidate is reported as `unstorable`, naming the offending string, never written. |
| T-69-07 | Repudiation | a decline that cannot be keyed | medium | mitigate | An unkeyable decline is appended to `unkeyable` and named individually in step 9's report — the one failure mode that would silently recreate the Roma defect inside the fix for it. |
| T-69-08 | Denial of Service (of the operator's attention) | step 9's decline surface | low | accept | The backlog accumulates with no retention or pruning (deferred by CONTEXT). Accepted for this phase: real backlog sizes have not been observed yet, and the surface is a report that never halts. |
| T-69-09 | Information Disclosure | step 9 rendering a stored `provenance` | low | mitigate | Render the locator only. The provenance of a search-sourced person carries a key whose NAME contains one of the two substrings D-10b forbids in a skill body; the surface never names that key, and `test_no_icp_or_tier_substring_anywhere_in_the_file[suggest-contacts]` is the standing check. |
| T-69-10 | Tampering | package installs | n/a | accept | No package is installed by this plan. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — 2750+ passed, 0 failed.
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q` — no `UNREGISTERED SKILL SEQUENCE`, `MAX_GRANDFATHERED` still 0.
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_disclosure_audit.py operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status -q` — the two standing ratchets. This plan touches no plugin script, so the poll/loop scan is a no-change assertion; it runs anyway.
- `git status --porcelain -- n8n/ scripts/build_cloud_workflows.py` — prints nothing.
- `git diff` over `operator-claude-plugin/scripts/held_queue.py` and `operator-claude-plugin/scripts/confidence.py` is empty.
</verification>

<success_criteria>
Running the documented step-8 sequence over a real `partition_for_dispatch` result persists
every declined person it can key, names every one it cannot, and step 9 shows the operator
this round's declines beside the backlog — with no call into `held_queue` anywhere on the path.
</success_criteria>

## Artifacts this phase produces (plan 02's share)

**Changed files**
- `operator-claude-plugin/skills/suggest-contacts/SKILL.md` — step 8's held-half prose replaced;
  one NEW python fence in step 8 (the held routing); step 9's "What was held, and what is still
  waiting" subsection; one NEW python fence in step 9 (the empty-records backlog read); the
  empty-records paragraph amended.
- `operator-claude-plugin/tests/test_suggest_contacts_composition.py` — new tests:
  `test_the_documented_step_8_held_routing_persists_a_declined_person`,
  `test_a_second_round_merges_into_the_first_rounds_declines`,
  `test_a_decline_with_no_company_id_is_reported_unkeyable_and_never_dropped`,
  `test_step_8_held_routing_never_calls_held_queue`,
  `test_the_documented_empty_records_path_still_reads_the_backlog`,
  `test_partition_by_run_splits_this_rounds_declines_from_the_backlog`
- `operator-claude-plugin/tests/test_skill_sequence_coverage.py` — two new `COVERED` entries
- `.planning/todos/pending/2026-09-04-skill-step8-routes-holds-into-a-queue-that-refuses-them.md`
  — `resolves_phase: 69` plus a Resolution section

<output>
Create `.planning/phases/69-held-rows-survive-the-round/69-02-SUMMARY.md` when done.
</output>
