---
phase: 69-held-rows-survive-the-round
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/suggestion_declines.py
  - operator-claude-plugin/scripts/suggest_contacts.py
  - operator-claude-plugin/tests/test_suggestion_declines.py
autonomous: false
requirements: [HELD-02, HELD-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ---- goal-backward observable truths -------------------------------------------
    - "A person `partition_for_dispatch` declined to send has a durable home on the operator's machine: `suggestion_declines.json`, a sibling of `held_queue.json` in the same `durable_paths` directory, written 0600 (D-69-01)."
    - "The store ACCUMULATES: a save merges into the loaded document and never overwrites another run's entries; every entry carries its OWN `run_id`, and the document carries no run id and no timestamp of its own (D-69-03)."
    - "An entry is keyed by `company_id` + the normalised first+last name key, so the same person re-found in a later round updates one entry instead of creating a second; `row_id` is neither the key nor a persisted row field (D-69-04)."
    - "`confidence.ALL_HOLD_CODES` gains no member, and `suggestion_declines.py` neither imports `confidence` nor names any of its codes; a test asserts the two vocabularies are disjoint sets (D-69-02, HELD-02)."
    - "`held_queue.save`'s `HeldQueueError` on `no_email` / `email_domain_mismatch` is untouched — the store is new, the refusal stays (D-69-02)."
    - "A save validates every entry BEFORE it writes anything, so a refused save leaves the previous file byte-identical (D-69-01)."
    - "A `defer` leaves the map unchanged; a `delete` removes the entry and records nothing in its place — no tombstone, no suppression key (D-69-06, D-69-07)."
    - "The store's decisions are pure: `partition_by_run` and `apply_action` take a map and return a map, and the module performs no HTTP call, no model call, and no `while` loop or `sleep`."
    # ---- spec-less probe fallback: HELD-03 (4 items, all auto-resolved) -------------
    - "HELD-03/empty: an absent or empty store loads as `{}`; `partition_by_run({}, run_id)` returns both halves empty and raises nothing; a round with zero declines writes no file at all and leaves an existing file byte-identical."
    - "HELD-03/encoding: the on-disk key is `str(company_id)` + `::` + normalised-first + `|` + normalised-last; the file is UTF-8 JSON written with `ensure_ascii=False`; a HubSpot numeric id can contain no `::` and `suggest_contacts.name_key`'s casefolded, whitespace-collapsed output is compared as an exact Python string (code points, no Unicode normalisation)."
    - "HELD-03/idempotency: saving the same map twice produces byte-identical file content (no document-level stamp exists to differ); the same person re-found in a later round replaces the value at the existing composite key and the entry count does not grow."
    - { statement: "HELD-03/concurrency: whole-document overwrite via `durable_paths._atomic_write_0600` (temp file in the target directory, then rename) means an interrupted write leaves either the whole old document or the whole new one, never a partial; two concurrent operators are NOT defended — single-operator, last writer wins, no lock, stated in the module docstring with a `ponytail:` comment naming the ceiling.", verification: backstop }
    # ---- spec-less probe fallback: HELD-02 (unclassified -> flagged assumption) ------
    - "FLAGGED ASSUMPTION (probe item HELD-02, category `unclassified`, never backstop-resolved): the probe could not classify HELD-02, so this plan assumes its testable content is exactly the two-set disjointness plus the absence of a `confidence` import in the new module. If the operator meant something wider by \"a test pins that disjointness as deliberate\", this plan under-delivers and the gap is here, named."
  artifacts:
    - operator-claude-plugin/scripts/suggestion_declines.py
    - operator-claude-plugin/tests/test_suggestion_declines.py
    - "operator-claude-plugin/scripts/suggest_contacts.py — `PARTITION_REASON_CODES`, `name_key`, `company_id_for_index`"
  key_links:
    - "`suggestion_declines.queue_path()` resolves through `durable_paths.resolve_state_path().parent` — the ONE resolution rule, never a second."
    - "`suggestion_declines.save()` writes through `durable_paths._atomic_write_0600` — never a bespoke open/rename."
    - "`suggestion_declines.entry_key()` calls `suggest_contacts.name_key` — the same key `select_people`'s D-62-18 dedupe already trusts."
    - "`suggestion_declines.first_refusal()` validates `reason_code` against `suggest_contacts.PARTITION_REASON_CODES` — never `confidence.ALL_HOLD_CODES`."
    - "`suggestion_declines.ROW_FIELD_ALLOWLIST` is pinned equal to `set(extraction.canonical_props())` by test, so what the store persists is exactly what `contact-upload` accepts."
  prohibitions:
    - "`confidence.ALL_HOLD_CODES` is never widened, added to, re-ordered, or imported into `suggestion_declines.py`."
    - "No partition reason code is ever mapped onto a match-gate hold code at any boundary."
    - "`held_queue.save`'s `HeldQueueError` on a partition reason code is never removed, softened, caught-and-ignored, or routed around."
    - "`row_id` is never the store's key and never appears on a persisted row."
    - "The store never persists a grant, secret, token, API key, or arming-shaped field or value."
    - "`delete` never writes a tombstone, a suppression key, or a do-not-suggest marker."
    - "Nothing in this plan writes state to HubSpot, arms anything, or spends a provider credit."
    - "No file under `n8n/` and no line of `scripts/build_cloud_workflows.py` is touched."
    - "No script added or changed by this plan contains a `while` loop, an `import time`, an `import sched`, or a `sleep()` call."
    - "`suggest_contacts.py` gains no `search_fallback` import — its own purity tests pin that import list."
    # Canon-security prohibitions (no secret in a log line, no `eval`, no shell
    # interpolation, no credential in a commit) are project canon and dropped here; the
    # forbidden-name refusal above is this phase's own instance of that canon.
---

<objective>
Give a correctly-held person somewhere durable to live. Build `suggestion_declines.py` — a
sibling of `held_queue.json` in the same durable directory, with its OWN closed reason-code
vocabulary, its own composite key, and a document that accumulates across runs — plus the
`suggest_contacts.py` half of that contract: the named `PARTITION_REASON_CODES` frozenset,
the promoted `name_key`, and the `rounds[]`-range lookup that supplies a decline's
`company_id`.

Purpose: after the live Roma Turf Club round the only record of two correctly-held committee
members was a chat message. This plan is where that stops being true.

Output: one new script module, one new test module, three new symbols in `suggest_contacts.py`,
and a corrected `partition_for_dispatch` docstring.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-held-rows-survive-the-round/69-CONTEXT.md
@.planning/phases/69-held-rows-survive-the-round/69-RESEARCH.md
@.planning/phases/69-held-rows-survive-the-round/69-PATTERNS.md

@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/scripts/durable_paths.py
@operator-claude-plugin/tests/test_held_queue.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way multi-run document shape before it is written</name>
  <read_first>
    - `.planning/phases/69-held-rows-survive-the-round/69-CONTEXT.md` § D-69-01, D-69-03
    - `operator-claude-plugin/scripts/held_queue.py:186-224` (the single-`run_id` document this deliberately diverges from)
  </read_first>
  <reversibility rating="one-way">D-69-03 moves run scope from the document to the entry. Once operator machines hold a multi-run `suggestion_declines.json`, reverting to a single-run document discards every deferred entry that has not been drained. D-69-01 is separately rated `costly` (a second file on operator machines; merging it back into `held_queue` later is a migration of live operator state) and is confirmed by the same gate.</reversibility>
  <decision>Write the accumulating, multi-run `suggestion_declines.json` document now — the shape D-69-03 locks — knowing it is one-way.</decision>
  <context>
    This is a CONFIRM gate, not a menu. D-69-03 and D-69-01 are LOCKED operator decisions from
    `/gsd-discuss-phase`; the checkpoint exists because the first save is the point of no
    return, not because the decision is open. Two consequences the operator should hear once,
    plainly, before any code writes the file:

    ONE-WAY (D-69-03): from the first save onward, `suggestion_declines.json` on the operator's
    machine carries entries from more than one run. `held_queue.json`'s shape — one `run_id`
    stamped on the whole document, a new run overwriting it — cannot represent that. Moving
    back to it later silently discards every deferred entry that has not been drained. The
    deferred backlog IS the phase's product, so the fallback loses exactly what was built.

    COSTLY (D-69-01): this is a NEW file beside `held_queue.json` in the operator's durable
    state directory, not a section inside it. Folding the two together later is a migration of
    live operator state, not a code change.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — write the multi-run document (D-69-03 as locked)</name>
      <pros>A declined person survives a run boundary, which is the whole phase. Each entry carries its own `run_id`, so `partition_by_run` can show "this round" beside "still waiting" in one batch (D-69-05). A new run merges; it never overwrites.</pros>
      <cons>One-way once written: reverting to a single-run document discards un-drained deferred entries. A second durable file on operator machines; merging it into `held_queue` later is a state migration.</cons>
    </option>
    <option id="stop">
      <name>Stop the plan and re-open the decision with the operator</name>
      <pros>Nothing is written; D-69-01/D-69-03 can be revisited before any operator machine holds the file.</pros>
      <cons>The phase does not proceed. Do NOT substitute a report-only design here — that is the brief's candidate fix (1), which CONTEXT.md already rejected on the grounds that the holds must survive the round.</cons>
    </option>
  </options>
  <action>
    State the decision, the context, and the two options above to the operator in one short
    block. Ask for an explicit go-ahead and wait. Do not proceed to Task 2 without one. On
    `stop`, halt the plan and report; do not substitute a report-only design. Record the
    operator's answer verbatim in `69-01-SUMMARY.md` § Decisions Made.
  </action>
  <acceptance_criteria>
    The operator has given an explicit affirmative naming the multi-run document, quoted
    verbatim in `69-01-SUMMARY.md` § Decisions Made. No file under `operator-claude-plugin/`
    is modified by this task.
  </acceptance_criteria>
  <verify>
    <human-check>The operator answered the confirm gate and the answer is quoted in the summary.</human-check>
  </verify>
  <resume-signal>Reply "proceed" (or a plain yes naming the multi-run document) to continue; reply "stop" to halt the plan.</resume-signal>
  <done>An explicit go-ahead is recorded, or the plan is stopped.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One declined person, end to end — held entry to disk and back</name>
  <files>operator-claude-plugin/scripts/suggest_contacts.py, operator-claude-plugin/scripts/suggestion_declines.py, operator-claude-plugin/tests/test_suggestion_declines.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/held_queue.py` (whole file — the shape being copied: `queue_path`, `_looks_forbidden`, `_first_forbidden`, `ROW_FIELD_ALLOWLIST`, `save`'s validate-before-write loop, `load`'s degrade-whole)
    - `operator-claude-plugin/scripts/durable_paths.py` — `resolve_state_path`, `_atomic_write_0600`
    - `operator-claude-plugin/scripts/suggest_contacts.py:161-215` (`_name_key`, `_normalize_name`, `select_people`), `:678-682` (`_RELATION_REASON_CODES`), `:829-897` (`partition_for_dispatch` and its closing docstring paragraph)
    - `operator-claude-plugin/scripts/confidence.py:74-77` (`ALL_HOLD_CODES` — read to stay disjoint from it, never to import it)
    - `operator-claude-plugin/scripts/search_fallback.py:64` (`SOURCE_TIER_HOLD_CODE`) and `:358-403` (`hold_weak_sources` — confirms a held entry's `index` is the record's ORIGINAL index)
    - `operator-claude-plugin/tests/test_held_queue.py:1-60` (the `_point_at_a_fake_durable_home` isolation idiom and the explicit-`path=` idiom)
    - `operator-claude-plugin/tests/conftest.py` (`no_durable_writes`, `no_network` — both autouse)
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md:503-508` and `:597-608` (the `rounds[]` `{start, count}` structure `company_id_for_index` reads, and `c.get("id")` as the company-id convention)
  </read_first>
  <behavior>
    RED first — write these before the implementation and read pytest's own failure output
    to confirm they fail for the stated reason (there is no `gsd-tools check tdd-red-evidence`
    in this install):
    - The end-to-end slice: build two company rows and two people, run the REAL
      `suggest_contacts.partition_for_dispatch` over synthesised rows with a company-domain
      map that holds both (one with no email, one with a stranger's domain), take its `held`
      entries, resolve each one's `company_id` through `suggest_contacts.company_id_for_index`
      against a hand-built `rounds` list, key it with `suggestion_declines.entry_key`, build
      it with `suggestion_declines.build_entry`, `save` to an explicit `path=` under
      `tmp_path`, and `load` it back — the loaded map equals what was saved, has two entries,
      and each entry's `reason_code` is the code `partition_for_dispatch` actually stamped.
    - HELD-02: `set(suggest_contacts.PARTITION_REASON_CODES) & set(confidence.ALL_HOLD_CODES)`
      is empty, and each set is non-empty (so the assertion cannot pass vacuously).
    - The vocabulary is pinned to its three real sources: `"no_email"` is a member,
      `set(suggest_contacts._RELATION_REASON_CODES.values())` is a subset, and
      `search_fallback.SOURCE_TIER_HOLD_CODE` is a member — the TEST imports
      `search_fallback`, the module under test does not.
    - `suggestion_declines` does not import `confidence`: assert the module's source
      contains no `confidence` import by parsing it with `ast` and inspecting
      `ast.Import`/`ast.ImportFrom` names.
    - `save` writes at mode 0600.
    - A persisted row carries no `row_id` and no key outside `ROW_FIELD_ALLOWLIST`, even when
      the input row carries both.
    - `entry_key` returns `None` — never a key with `None` in it — when the company id is
      missing or when either half of the name is blank.
    - `company_id_for_index` returns the owning company's `id` for an index inside a round's
      `{start, count}` range and `None` for an index outside every range.
  </behavior>
  <action>
    In `operator-claude-plugin/scripts/suggest_contacts.py`:
    (1) Add module-level `PARTITION_REASON_CODES = frozenset({"no_email",
    "email_domain_freemail", "email_domain_mismatch", "company_domain_unknown",
    "search_source_not_strong"})` as a LITERAL, with a comment stating it is the mirror of
    `confidence.ALL_HOLD_CODES` for the other question and that its members are pinned to
    their three real sources by test rather than by import — this module gains no
    `search_fallback` import (its own purity tests pin that list) and no `confidence` import.
    (2) Rename `_name_key` to `name_key` (public) and update its three internal call sites at
    lines 190, 196 and 365 plus the docstring mention at line 275; the key becomes a
    cross-module contract under D-69-04, so it stops being private. Add no alias.
    (3) Add `company_id_for_index(rounds, index)`: walk `rounds`, return
    `entry["company"].get("id")` for the first entry whose `entry["start"] <= index <
    entry["start"] + entry["count"]`, else `None`. Docstring states it reads the SAME
    `{start, count}` ranges the terminal `round_outcome` classify already slices, and that a
    `None` return means the decline is unkeyable and must be reported as such, never dropped.
    (4) Correct `partition_for_dispatch`'s closing docstring paragraph: the sentence claiming
    "the held-row path downstream (`confidence.assess()` -> `held_queue.build_entry()`) is
    unchanged" is now false. Replace it with: these codes are the closed
    `PARTITION_REASON_CODES` vocabulary and route to `suggestion_declines`;
    `confidence.ALL_HOLD_CODES` is still not widened by them, and `held_queue.save`'s refusal
    on them stays correct behaviour.

    Create `operator-claude-plugin/scripts/suggestion_declines.py`. Module docstring states:
    the plugin's next persisted artifact, a SIBLING of `held_queue.json` and never a section
    inside it (D-69-01); the document accumulates and carries NO run id and NO timestamp of
    its own because run scope lives on the entry (D-69-03); the forbidden-name refusal is
    REIMPLEMENTED, not imported, per `held_queue.py:68-70`'s own stated discipline; and a
    `ponytail:` comment naming the concurrency ceiling — single operator, whole-document
    overwrite, last writer wins, no lock, per-key locking only if two operators ever share a
    machine. Module contents:
    - `QUEUE_FILENAME = "suggestion_declines.json"`, `ENTRIES_FIELD = "entries"`,
      `KEY_SEPARATOR = "::"`, `NAME_SEPARATOR = "|"`.
    - `ROW_FIELD_ALLOWLIST = enrichment.MATCH_LOOKUP_KEYS + ("jobtitle", "phone",
      "company_id")` — deliberately NOT `held_queue`'s, and deliberately excluding `row_id`
      (D-69-04; `preingest.build_rows_spec` also refuses a row that already carries one).
    - `_FORBIDDEN_NAME_MARKERS`, `_looks_forbidden`, `_first_forbidden` — copied verbatim
      from `held_queue.py:101-144`.
    - `ABSENT`/`PARSEABLE`/`ANOMALOUS` string constants (no `ANOTHER_RUN`: the document is
      multi-run by design, so there is no wrong run to detect at document level).
    - `class SuggestionDeclineError(Exception)`.
    - `queue_path()` -> `durable_paths.resolve_state_path().parent / QUEUE_FILENAME`,
      resolved fresh every call.
    - `entry_key(company_id, row)` -> the composite string, or `None` when `company_id` is
      `None`/empty or `suggest_contacts.name_key(row)` is `None`. Uses `str(company_id)`.
      Docstring names the separator contract and its ceiling: a `|` inside a normalised name
      is not defended, because the key is an INDEX and the entry carries `company_id` and the
      row's own name fields as the record of truth.
    - `build_entry(row, reason_code, reason, run_id, company_id, provenance=None)` -> a dict
      with `reason_code`, `reason`, `run_id`, `company_id` (as `str`), `row` (allowlisted),
      `provenance` (a plain dict copy, default `{}`), and `recorded_at` (an ISO-8601 UTC
      stamp). The stamp lives on the ENTRY, never on the document, so re-saving an unchanged
      map is byte-identical.
    - `first_refusal(key, entry)` -> a human sentence naming what is wrong, or `None`.
      Checks, in order: the key is forbidden-shaped; `entry` is not a dict; `reason_code` is
      not in `suggest_contacts.PARTITION_REASON_CODES`; `run_id` is not a non-empty string;
      `_first_forbidden` finds an offender in `row`, in `provenance`, or in `reason`. Public
      because the caller pre-checks with it (see plan 02) so one odd company name cannot cost
      a whole batch's save.
    - `save(entries, path=None)` -> loops `first_refusal` over every item and raises
      `SuggestionDeclineError` on the first non-`None` BEFORE writing anything, then writes
      `{ENTRIES_FIELD: dict(entries)}` — no run id, no `saved_at` — through
      `durable_paths._atomic_write_0600`, serialised with `json.dumps(..., sort_keys=True,
      ensure_ascii=False)` so identical content serialises identically.
    - `_validated_entries(document)` and `load(path=None)` — mirror `held_queue.py:227-257`
      exactly, with `reason_code in suggest_contacts.PARTITION_REASON_CODES` and a
      non-empty-string `run_id` as the per-entry usability checks; every anomaly degrades
      whole to `{}`.

    Create `operator-claude-plugin/tests/test_suggestion_declines.py` with the tests in
    `<behavior>`, isolating the way `test_held_queue.py` does (explicit `path=` for most, the
    `CLAUDE_PLUGIN_DATA` fake durable home for the location tests). Commit RED first
    (`test(69-01): ...`), then GREEN (`feat(69-01): ...`).
  </action>
  <acceptance_criteria>
    `set(suggest_contacts.PARTITION_REASON_CODES) & set(confidence.ALL_HOLD_CODES) == set()`
    with both sets non-empty. `suggestion_declines.py`'s AST carries no `confidence` import
    name. `queue_path().parent == held_queue.queue_path().parent` and
    `queue_path().name == "suggestion_declines.json"`. A saved file's mode is `0o600`.
    A persisted entry's `row` has no `row_id` key and no key outside `ROW_FIELD_ALLOWLIST`.
    `entry_key(None, row) is None` and `entry_key("123", {"firstname": "Amy"}) is None`.
    The end-to-end test's loaded map equals the saved map and has one entry per held row.
    `.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggest_contacts.py -q`
    still passes after the `_name_key` rename.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggestion_declines.py operator-claude-plugin/tests/test_held_queue.py operator-claude-plugin/tests/test_suggest_contacts.py -q</automated>
    <fails_when>pytest exits non-zero, or its summary line reports any number other than `0 failed` / omits a `passed` count — in particular a `NameError: name '_name_key' is not defined` from a missed rename call site, or an `AssertionError` naming a non-empty intersection between `PARTITION_REASON_CODES` and `ALL_HOLD_CODES`.</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>The command prints any line at all — one line of output means an n8n artifact or the builder was touched, which this phase forbids.</fails_when>
  </verify>
  <done>A real `partition_for_dispatch` held entry is keyed, built, saved 0600 and loaded back identical, in one test; `PARTITION_REASON_CODES` exists and is proven disjoint from `ALL_HOLD_CODES`; `suggestion_declines.py` provably does not import `confidence`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The store's lifecycle — read honestly, accumulate, apply an action</name>
  <files>operator-claude-plugin/scripts/suggestion_declines.py, operator-claude-plugin/tests/test_suggestion_declines.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/held_queue.py:260-279` (`classify_read`'s probe-before-degrade shape; `ANOTHER_RUN` is the state this store does NOT have)
    - `operator-claude-plugin/tests/test_held_queue.py:185-290` (the refused-save-leaves-the-previous-file test and the `classify_read` tests to mirror)
    - `operator-claude-plugin/scripts/extraction.py:162-167` (`canonical_props` — the allowlist non-drift pin)
    - `.planning/phases/69-held-rows-survive-the-round/69-CONTEXT.md` § D-69-05, D-69-06, D-69-07
  </read_first>
  <behavior>
    RED first, verified by reading pytest's own failure output:
    - `classify_read` on a missing file is `ABSENT`; on a good file `PARSEABLE`; on malformed
      JSON `ANOMALOUS`; on a document whose one entry carries an unknown `reason_code`
      `ANOMALOUS`. There is no state for "a different run" — a document holding entries from
      two different `run_id`s classifies `PARSEABLE`.
    - `load` still degrades whole to `{}` for every case `classify_read` calls `ANOMALOUS`.
    - `partition_by_run(entries, run_id)` returns `{"this_run": {...}, "backlog": {...}}`,
      splitting on each entry's own `run_id`; the two halves are disjoint and together equal
      `entries`. With `run_id=None` everything is backlog and `this_run` is empty. With `{}`
      both halves are empty and nothing raises.
    - `apply_action(entries, key, "defer")` returns a map equal to the input, and the input
      map object is not mutated.
    - `apply_action(entries, key, "delete")` returns a map with that key gone and NO new key
      added anywhere — asserted by comparing key sets, so a tombstone under any name fails.
    - `apply_action(entries, key, "send")` removes the key (a landed person is not a
      standing decline); `"export"` leaves the map unchanged (the export is a copy, not a
      move).
    - `apply_action` raises `SuggestionDeclineError` on an action outside `DRAIN_ACTIONS` and
      on a key absent from the map.
    - Idempotency: `save(m, path)` twice produces byte-identical file bytes; re-keying the
      same person from a later run with a new `run_id` replaces the value at the existing key
      and `len(load(path))` does not grow.
    - Refused save leaves the previous file byte-identical: save a good map, then attempt a
      save whose one entry carries a forbidden-shaped value, assert `SuggestionDeclineError`
      and that the file's bytes are unchanged.
    - Allowlist non-drift: `set(suggestion_declines.ROW_FIELD_ALLOWLIST) ==
      set(extraction.canonical_props())` and `"row_id" not in ROW_FIELD_ALLOWLIST`.
  </behavior>
  <action>
    Add to `operator-claude-plugin/scripts/suggestion_declines.py`:
    - `classify_read(path=None)` returning `ABSENT` / `PARSEABLE` / `ANOMALOUS`, mirroring
      `held_queue.classify_read`'s probe-then-degrade shape with the `expected_run_id`
      parameter and the `ANOTHER_RUN` branch both absent; docstring states why: D-69-03 makes
      the document inherently multi-run, so a "wrong run" is not a document-level fact — an
      individual entry's `run_id` is still informative and is what `partition_by_run` reads.
    - `partition_by_run(entries, run_id)` -> `{"this_run": ..., "backlog": ...}`, pure, no
      I/O. `run_id=None` puts everything in `backlog` — the end-of-round surface for a round
      that never dispatched has no run handle to compare against and must not invent one.
    - `DRAIN_ACTIONS = ("send", "defer", "delete", "export")` and
      `apply_action(entries, key, action)` -> a NEW dict; `"delete"` and `"send"` remove the
      key, `"defer"` and `"export"` return an equal copy, anything else raises. Docstring
      states the two ceilings plainly: `"send"` is the caller's promise that the dispatch
      already succeeded (this function cannot see a transport), and `"delete"` writes nothing
      in the entry's place — no tombstone, no suppression key, so a later round that
      rediscovers the person re-queues them (D-69-07).
    Extend `operator-claude-plugin/tests/test_suggestion_declines.py` with the tests in
    `<behavior>`. Commit RED then GREEN.
  </action>
  <acceptance_criteria>
    `.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggestion_declines.py -q`
    passes with a non-zero test count. `apply_action(entries, key, "delete")`'s returned key
    set is exactly the input key set minus `{key}` — asserted by set equality, not by
    membership, so any added marker fails. Two consecutive `save` calls with the same map
    produce equal `path.read_bytes()`. `set(ROW_FIELD_ALLOWLIST) ==
    set(extraction.canonical_props())` holds.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggestion_declines.py -q</automated>
    <fails_when>pytest exits non-zero, or reports `no tests ran`, or its output contains a `failed` count above zero — in particular an `AssertionError` on the byte-identical re-save (which would mean a document-level timestamp survived) or on the delete key-set equality (which would mean a tombstone was written).</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_disclosure_audit.py -q</automated>
    <fails_when>pytest exits non-zero — in particular a failure naming `suggestion_declines.py` in the poll/sleep/loop scan, or an `UNREGISTERED SKILL SEQUENCE` message (no SKILL.md is edited by this plan, so either is a real regression).</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
    <fails_when>The summary reports fewer than 2750 passed, or any failures — the Phase 68 baseline is 2750 passed / 5 skipped and this plan only adds tests.</fails_when>
  </verify>
  <done>The store reads honestly (three states), accumulates across runs, and its two pure decision functions return maps a drain can act on — all under test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| round data → operator's durable filesystem | A held row's contents (provider-derived email, crawled job title, company name) crosses into a file that persists across sessions and plugin updates. |
| stored entry → a later run's decisions | An entry written by one run is read and acted on by a later one, possibly weeks later, with no re-verification of the data that produced it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Information Disclosure | `suggestion_declines.save` | high | mitigate | `ROW_FIELD_ALLOWLIST` (canonical contact props only, `row_id` excluded) is the first line; `_first_forbidden` over `row`/`provenance`/`reason` is the second, reimplemented verbatim from `held_queue.py:101-144`, never imported. A save that would persist an arming grant, secret, token or API key raises `SuggestionDeclineError` and writes nothing. |
| T-69-02 | Tampering | the durable file | medium | mitigate | `durable_paths._atomic_write_0600` — mode 0600, temp file in the target directory then rename, so an interrupted write leaves either the whole old document or the whole new one. Pinned by a 0600-mode test and a refused-save-leaves-previous-bytes test. |
| T-69-03 | Spoofing (of identity, not of the file) | a stale entry drained after a long defer | low | accept | The store accumulates by design (D-69-03); an entry's email was gated once by `partition_for_dispatch` and time has passed. Accepted because a drained `send` (plan 03) re-enters `extraction.validate()` and every gate a normal send clears — no NEW gate is skipped, and a still-bad email is still held by the pre-existing machinery. |
| T-69-04 | Elevation of Privilege | `apply_action(..., "send")` | medium | mitigate | This function removes a key and nothing else — it takes no transport, opens no grant, and cannot itself send. The authority lives entirely in plan 03's re-entry of `enrich-before-ingest/SKILL.md` steps 5 and 7. |
| T-69-05 | Tampering | package installs | n/a | accept | This plan installs no npm/pip/cargo package — every import is Python stdlib or an existing module in `operator-claude-plugin/scripts/`. No legitimacy audit applies. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — 2750+ passed, 0 failed.
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_suggestion_declines.py -q` — the new module's own suite.
- `git status --porcelain -- n8n/ scripts/build_cloud_workflows.py` — prints nothing.
- `confidence.ALL_HOLD_CODES` is byte-identical to its pre-plan value (`git diff` over `operator-claude-plugin/scripts/confidence.py` is empty).
- `held_queue.py` is byte-identical to its pre-plan value.
</verification>

<success_criteria>
A partition-declined person survives a process exit: keyed by company and name, stamped with
the run that declined them, readable back, and disjoint by construction from the match-gate
vocabulary that would have refused them.
</success_criteria>

## Artifacts this phase produces (plan 01's share)

**New files**
- `operator-claude-plugin/scripts/suggestion_declines.py`
- `operator-claude-plugin/tests/test_suggestion_declines.py`

**New symbols in `operator-claude-plugin/scripts/suggestion_declines.py`**
- Constants: `QUEUE_FILENAME`, `ENTRIES_FIELD`, `KEY_SEPARATOR`, `NAME_SEPARATOR`,
  `ROW_FIELD_ALLOWLIST`, `_FORBIDDEN_NAME_MARKERS`, `ABSENT`, `PARSEABLE`, `ANOMALOUS`,
  `DRAIN_ACTIONS`
- Exception: `SuggestionDeclineError`
- Functions: `_looks_forbidden`, `_first_forbidden`, `queue_path`, `entry_key`, `build_entry`,
  `first_refusal`, `save`, `_validated_entries`, `load`, `classify_read`, `partition_by_run`,
  `apply_action`

**New / changed symbols in `operator-claude-plugin/scripts/suggest_contacts.py`**
- `PARTITION_REASON_CODES` (new frozenset)
- `name_key` (renamed from `_name_key`, now public)
- `company_id_for_index` (new)
- `partition_for_dispatch` — docstring corrected only, behaviour unchanged

<output>
Create `.planning/phases/69-held-rows-survive-the-round/69-01-SUMMARY.md` when done.
</output>
