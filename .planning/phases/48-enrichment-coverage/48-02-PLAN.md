---
phase: 48-enrichment-coverage
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
  - tests/n8n/researchErrorGateFlow.test.mjs
autonomous: true
requirements: [COVER-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An error-shaped payload leaving the Claude Web Research HTTP node is routed to a failure terminal and never reaches Validate Research Output, Merge Company or Decide Company Action (D-04)."
    - "The failure terminal's response row carries the same identity/action/gate shape every other terminal produces, recovered from the pre-HTTP row by node name — not a bare Anthropic error blob."
    - "A healthy research response is routed down the unchanged lane, so the gate is additive and no existing behaviour is altered."
    - "The committed n8n/wf_enrichment_cloud.json is regenerated from the builder, never hand-edited, and the offline test evaluates the node's REAL emitted expression rather than a hand-copied string."
  prohibitions:
    - statement: "n8n/wf_*.json is never hand-edited; every workflow change originates in scripts/build_cloud_workflows.py and is regenerated."
      status: required
      verification: "git diff --stat for this plan shows n8n/wf_enrichment_cloud.json changed in the same commit as scripts/build_cloud_workflows.py, and the file is byte-reproducible by re-running the builder with no further edits"
    - statement: "No second writer of the derived veto fields is introduced; Decide Company Action remains the sole writer."
      status: required
      verification: "The dot-anchored assignment count in the built workflow is unchanged from before this plan — the existing single-veto-writer guard test stays green"
    - statement: "The new gate reads bare $json and adds no $() node-name lookup to any code path shared across the three workflows that share ENRICH_CO_GATE."
      status: required
      verification: "The IF node's leftValue contains no '$(' substring; the one node-name lookup that does exist is inside the new Code node and is wrapped in the repo's try/catch idiom"
  artifacts:
    - scripts/build_cloud_workflows.py
    - n8n/wf_enrichment_cloud.json
    - tests/n8n/researchErrorGateFlow.test.mjs
  key_links:
    - "Claude Web Research → IF Research Errored → (true) Build Research Failure Response → Build Response; (false) Validate Research Output — the single existing edge at scripts/build_cloud_workflows.py:5085 is replaced, not duplicated."
    - "Build Research Failure Response recovers its row from 'Build Research Request' using the identical try/catch idiom Validate Research Output already uses at :2360-2362."
---

<objective>
Land D-04: a gate immediately after `Claude Web Research` in the CLOUD enrichment workflow that
detects an error-shaped payload and routes it to a stated-reason failure terminal instead of
into the merge/normalize chain. Treat "research returned an error object" as unknown, never as
data — the same prefer-unknown-over-guessing rule `docs/WEB-RESEARCH-SPEC.md` already states for
the model's own output.

Purpose: this closes the folded todo (`2026-08-12-n8n-swallows-anthropic-credit-failure.md`).
A `400 invalid_request_error` from Anthropic — credit exhaustion being the live case — does not
fail the execution today: `status: success`, zero node-level errors, and the error object
carried downstream as data (live exec `11833`).

Output: two new nodes in the built cloud workflow, the regenerated JSON, and an offline
structural test that drives the node's real emitted expression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-PATTERNS.md
@.planning/todos/pending/2026-08-12-n8n-swallows-anthropic-credit-failure.md
</context>

## Artifacts this phase produces

New symbols introduced by this plan (excluded from drift verification — they do not exist
before it): the n8n node names `IF Research Errored` and `Build Research Failure Response`, the
row field value `action: "research_failed"`, and the test file
`tests/n8n/researchErrorGateFlow.test.mjs`. The full phase-wide list is in 48-01-PLAN.md.

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | D-04 is folded into COVER-01 because an errored research call silently becoming an "unmatched" candidate is indistinguishable from real coverage. This plan assumes structural presence + offline expression evaluation is the achievable bar; the gate's live *firing* cannot be induced this phase (no way to force an Anthropic 400 on demand) and is explicitly not claimed. |
| COVER-02 | `unclassified` → **unresolved** | Not carried by this plan — it is carried by 48-03/48-05/48-06. Recorded here so the row is not silently dropped from any plan's view. |

<tasks>

<task type="auto">
  <name>Task 1: Build the IF Research Errored gate and its failure terminal in the cloud lane</name>
  <files>scripts/build_cloud_workflows.py</files>
  <read_first>
    - scripts/build_cloud_workflows.py — read `_if_bool_expr_node` (:2977-2997), `code_node` (:459), `_http_node`'s docstring and its `on_error` default (:3337-3360), the `Claude Web Research` cloud build (:4770-4775), the `IF Company Recompute`/`IF Company Skip` construction (:4690-4697) and wiring (:5060-5069), the single existing edge to replace (:5084-5085), `Validate Research Output`'s pre-HTTP recovery idiom (:2360-2362), and `ENRICH_BUILD_RESPONSE` (:3956-3970)
    - .planning/phases/48-enrichment-coverage/48-PATTERNS.md (§ "scripts/build_cloud_workflows.py (MODIFIED — D-04 gate)")
    - n8n/code/webResearch.js (`researchCandidateFromHttpItem`, :154-194 — READ ONLY, its existing guard is correct and this gate sits upstream of it)
  </read_first>
  <action>
    In the CLOUD enrichment workflow builder only (the call site around lines 4770-4775 / 5084-5085
    that produces `n8n/wf_enrichment_cloud.json`), insert two nodes between the existing
    `Claude Web Research` HTTP node and `Validate Research Output`.

    Node 1, built with `_if_bool_expr_node` — name `IF Research Errored`, expression
    `!!$json.error || !Array.isArray($json.content)`, positioned on the same row as
    `Claude Web Research`. A bare `$json` read is correct here and only here: the immediate
    upstream IS the HTTP node, so nothing can have replaced the item in between. This is the same
    reasoning `IF Company Skip`'s own comment gives for its bare read, and it is why the gate adds
    no `$()` lookup to the code shared by the three workflows that share `ENRICH_CO_GATE`.

    Node 2, built with `code_node` — name `Build Research Failure Response`. It recovers the
    pre-HTTP row by node name from `Build Research Request` using the identical try/catch idiom
    `Validate Research Output` already uses, indexed the same way, and returns
    `{ ...row, action: "research_failed", gate: { reason: <the Anthropic error message, or a
    fixed fallback string when absent> } }`. This node exists because the true lane cannot wire
    straight to `Build Response` the way `IF Company Skip` does: its input row is the raw HTTP
    output, carrying none of the identity, action or gate fields that `Build Response`'s
    pass-through-plus-credits shape assumes.

    Rewire, replacing the single existing edge — not duplicating it:
      `Claude Web Research` → `IF Research Errored`
      `IF Research Errored` true (output index 0) → `Build Research Failure Response`
      `IF Research Errored` false (output index 1) → `Validate Research Output` (unchanged)
      `Build Research Failure Response` → `Build Response`

    Scope decision to record in the summary: the second `Claude Web Research` build site around
    line 3214, which feeds `wf_enrichment_local_live.json`, is deliberately left unchanged — it
    is a local dev harness with mocked HubSpot, it is never deployed, and D-04's stated target is
    the production lane. Record this as a knowing divergence, not an oversight.

    Do not touch `n8n/code/webResearch.js`. Its existing error guard is correct; this gate is
    defence in depth sitting upstream of it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,sys;ast.parse(open('scripts/build_cloud_workflows.py').read());print('parse ok')"</automated>
    <automated>grep -c 'IF Research Errored' scripts/build_cloud_workflows.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IF Research Errored' scripts/build_cloud_workflows.py` is at least 3 (node construction, true-lane wiring, and the replaced `Claude Web Research` edge).
    - `grep -c 'Build Research Failure Response' scripts/build_cloud_workflows.py` is at least 3.
    - `grep -n '"Claude Web Research": {"main"' scripts/build_cloud_workflows.py` shows the cloud-site edge now targets `IF Research Errored`, and the local_live site edge (near line 3285) still targets `Claude Web Research` → `Validate Research Output` unchanged.
    - The new IF node's expression string contains no `$(` substring.
    - The new Code node's body contains the substring `catch` and the node name `Build Research Request`.
    - `.venv/bin/python -c "import ast;ast.parse(open('scripts/build_cloud_workflows.py').read())"` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-04 changes the sealed enrichment lane's control flow; undoing it needs another rebuild plus an operator deploy and bounce. Flagged only — no checkpoint, per CONTEXT.md's own rating.</reversibility>
  <done>Both nodes are built and wired in the cloud lane's builder, the single pre-existing edge is replaced rather than duplicated, and the local_live site is knowingly unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Regenerate the committed workflow JSON from the builder</name>
  <files>n8n/wf_enrichment_cloud.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py (`main()` at :7125 — confirm which output paths it writes)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (the constraints-table row "Never hand-edit n8n/wf_*.json")
  </read_first>
  <action>
    Run `.venv/bin/python scripts/build_cloud_workflows.py` and commit every regenerated
    `n8n/*.json` the run touches, in the same commit as the builder edit. This step is BLOCKING:
    the committed JSON is build output, and a builder edit without a rebuild produces exactly the
    false-positive this repo has been bitten by before — offline tests pass against a stale
    artifact while nothing downstream has changed.

    Then confirm the two new nodes are present in `n8n/wf_enrichment_cloud.json` and that the
    connection map matches the four edges specified in Task 1. If the builder writes other
    workflow files whose content is unchanged, that is expected; do not revert them by hand.

    Do not deploy. Deploying and bouncing is the ONE operator ceremony declared in D-06 and is
    plan 48-04's checkpoint — Claude must not set `ALLOW_N8N_DEPLOY` under any framing, and the
    Phase 47.5 waiver expired with that phase.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -c "
import json
wf=json.load(open('n8n/wf_enrichment_cloud.json'))
names={n['name'] for n in wf['nodes']}
assert 'IF Research Errored' in names, 'gate node missing'
assert 'Build Research Failure Response' in names, 'failure terminal missing'
c=wf['connections']
assert c['Claude Web Research']['main'][0][0]['node']=='IF Research Errored'
g=c['IF Research Errored']['main']
assert g[0][0]['node']=='Build Research Failure Response'
assert g[1][0]['node']=='Validate Research Output'
assert c['Build Research Failure Response']['main'][0][0]['node']=='Build Response'
print('gate wired ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The inline verification script above prints `gate wired ok` and exits 0.
    - `git status --porcelain n8n/` shows `wf_enrichment_cloud.json` modified, and the builder script is staged in the same commit.
    - Re-running `.venv/bin/python scripts/build_cloud_workflows.py` a second time leaves `git diff --stat n8n/` empty — the output is reproducible from the builder alone, proving no hand edit crept in.
    - `n8n/wf_enrichment_local_live.json` shows no `IF Research Errored` node (the knowing scope decision from Task 1).
  </acceptance_criteria>
  <done>The committed cloud workflow JSON carries both new nodes and the four specified edges, regenerated by the builder and byte-reproducible from it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline structural test driving the gate's REAL emitted expression</name>
  <files>tests/n8n/researchErrorGateFlow.test.mjs</files>
  <read_first>
    - tests/n8n/companyRecomputeLaneFlow.test.mjs (the full harness pattern to copy — `loadWorkflow`, `makeCtx`, the faked `$()` lookups, and how it evaluates an IF node's real leftValue)
    - n8n/wf_enrichment_cloud.json (the emitted `IF Research Errored` and `Build Research Failure Response` nodes, after Task 2)
    - .planning/todos/pending/2026-08-12-n8n-swallows-anthropic-credit-failure.md (the live error payload shape from exec 11833)
  </read_first>
  <behavior>
    - Loading `n8n/wf_enrichment_cloud.json` and evaluating `IF Research Errored`'s real `leftValue` (with the `={{ ... }}` wrapper stripped) against the live-observed error shape — an object whose `error.message` begins with `400 - ` and names `invalid_request_error` — returns `true`.
    - Evaluating the same real expression against a healthy shape (`{content: [{type: "text", text: "..."}]}`) returns `false`.
    - Evaluating it against a degenerate shape (`{}`) returns `true` — fail closed.
    - Running `Build Research Failure Response`'s real `jsCode` through the faked-`$()` harness, with `Build Research Request` supplying a row carrying company identity, returns one item whose `action` is `research_failed`, whose `gate.reason` is the Anthropic error message, and which still carries the identity fields from the recovered pre-HTTP row.
    - Running the same `jsCode` when `$('Build Research Request')` throws returns an item that still carries `action: "research_failed"` — the try/catch fails closed rather than propagating.
  </behavior>
  <action>
    Create `tests/n8n/researchErrorGateFlow.test.mjs` modelled directly on
    `tests/n8n/companyRecomputeLaneFlow.test.mjs`: `node:test` + `node:assert/strict`, load the
    committed workflow by path, index nodes by name, and build the same `makeCtx` harness with
    faked `$()` node lookups.

    Extract the IF node's expression from the loaded JSON and evaluate it with `new Function` —
    do not hand-copy the expression as a string literal in the test. A hand-copied expression
    asserts the routing the test exists to pin, which is the exact failure the recompute-lane
    test's own header warns about.

    Carry the same header note that test carries: this executes the repo's own committed jsCode
    and expressions over a fixed, in-repo list of node names; no external or untrusted input is
    ever interpolated into the function body.

    Also assert the recompute lane still routes correctly after the node insertion — the existing
    `tests/n8n/companyRecomputeLaneFlow.test.mjs` must stay green, proving no node rename or
    connection-map collision was introduced.
  </action>
  <verify>
    <automated>node --test tests/n8n/researchErrorGateFlow.test.mjs</automated>
    <automated>node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/n8n/researchErrorGateFlow.test.mjs` exits 0 with at least 5 assertions covering the five behaviours listed above.
    - `grep -c 'wf_enrichment_cloud.json' tests/n8n/researchErrorGateFlow.test.mjs` is at least 1 and the test contains no literal copy of the expression string `!!$json.error` outside a comment — the expression under test is read from the workflow JSON.
    - `node --test tests/n8n/*.test.mjs` exits 0 (glob form; the directory form is broken on node 24), including the pre-existing `companyRecomputeLaneFlow.test.mjs`.
    - `.venv/bin/python -m pytest` exits 0.
  </acceptance_criteria>
  <done>The gate's real emitted expression and the failure terminal's real jsCode are both proven offline against a genuine error payload, a healthy payload, and a degenerate payload, with the recompute lane's existing test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → `Claude Web Research` HTTP node | An untrusted upstream response crosses into the enrichment lane; `on_error="continueRegularOutput"` means a 400 arrives as data, not as a failure |
| `Claude Web Research` → merge/normalize chain | The point where a non-response can be mistaken for coverage |
| builder source → deployed workflow | Build output is committed; a hand edit would diverge the artifact from its source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03 | Spoofing | `Claude Web Research` → `Validate Research Output` | high | mitigate | `IF Research Errored` routes any error-shaped or non-array-content payload to a stated-reason terminal before the merge chain sees it (D-04) |
| T-48-06 | Repudiation | failure terminal response shape | medium | mitigate | The failure row carries recovered company identity plus `action: "research_failed"` and a gate reason, so a failed research call is attributable in the execution log rather than silently indistinguishable from a genuine no-match |
| T-48-07 | Tampering | committed `n8n/wf_*.json` | high | mitigate | Never hand-edited; regenerated by the builder and asserted byte-reproducible by a second build producing an empty diff |
| T-48-08 | Denial of Service | `ENRICH_CO_GATE` shared across three workflows | medium | mitigate | The gate adds no `$()` lookup to shared code; the one node-name read lives in a workflow-local Code node and is try/catch guarded, failing closed |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` exits 0
- `.venv/bin/python -m pytest` exits 0
- A second builder run leaves `git diff --stat n8n/` empty
- Nothing deployed, nothing armed, zero n8n executions attributable to this plan
</verification>

<success_criteria>
- `IF Research Errored` and `Build Research Failure Response` exist in the committed cloud workflow with the four specified edges
- The gate's real expression returns true on the live-observed error shape, false on a healthy shape, true on a degenerate shape
- The failure terminal's row carries recovered identity, `action: "research_failed"`, and the error message as its gate reason
- The pre-existing recompute-lane test is still green
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-02-SUMMARY.md` when done, recording the
knowing scope decision to leave the `wf_enrichment_local_live.json` build site unchanged, and
stating plainly that the gate is built and tested but NOT yet running — the deploy is 48-04.
</output>
