---
phase: 48-enrichment-coverage
plan: 04
type: execute
wave: 4
depends_on: ["48-02", "48-03"]
files_modified:
  - .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md
autonomous: false
requirements: [COVER-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The D-04 gate is present in the RUNNING n8n instance, proven by a live execution's own embedded workflow node list — not by re-reading the JSON that was PUT."
    - "Exactly one deploy and one bounce were performed, matching the D-06 declaration; the operator performed both and Claude performed neither."
    - "The proof execution wrote nothing: it ran disarmed and its outcome states the write was blocked."
    - "The run record states plainly that the gate's live FIRING is not proven this phase, and why — no execution in this phase traverses the research branch, and an Anthropic 400 cannot be induced on demand."
  prohibitions:
    - statement: "Claude never sets ALLOW_N8N_DEPLOY, never runs the deploy with DRY_RUN=false, and never performs the activation toggle."
      status: required
      verification: "Every deploy/bounce command in this plan appears only inside a blocking checkpoint's operator instructions; the plan's own automated verifies are read-only GETs and a disarmed POST"
    - statement: "A stored read-back of the PUT body is never accepted as proof that the running instance changed."
      status: required
      verification: "The proof artifact cites an execution id and that execution's own node list, not a GET of the stored workflow definition"
    - statement: "The proof POST carries the recompute intent as a request-level boolean, never as a mode value."
      status: required
      verification: "The POST is made via post_webhook_event(..., recompute=True); no mode string is used to carry intent"
  artifacts:
    - .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md
  key_links:
    - "The recompute lane routes Company Gate → IF Company Recompute → Decide Company Action, bypassing providers, research, judge and merge — so the proof POST costs 0 provider credits and 0 Anthropic calls."
    - "n8n executionStatus success does not mean the run succeeded; judge by node-level runData."
---

<objective>
Spend the phase's ONE declared deploy and bounce, and prove the D-04 gate is live in the running
instance rather than merely stored.

Purpose: a bare PUT stores JSON without reloading a running workflow — n8n keeps executing the
old in-memory code. This is the trap that has cost this project real time before, and the only
honest proof is an artefact produced by the running instance itself.

Output: `48-DEPLOY-PROOF.md` citing a live execution id and that execution's own node list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-HANDOVER.md
@.planning/phases/47.5-veto-recompute-path/47.5-C-DECISION.md
</context>

## Artifacts this phase produces

New artefact from this plan: `.planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md`. The
n8n node names it proves live (`IF Research Errored`, `Build Research Failure Response`) are
created by plan 48-02. Full phase-wide list in 48-01-PLAN.md.

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | This plan assumes structural presence in the running instance is the achievable proof bar for D-04. The gate's live firing on a genuine erroring Anthropic call is NOT proven this phase and is not claimed: no execution in Phase 48 traverses the research branch (the armed window is recompute-only, Racing NSW's research is standalone Python), and there is no supported way to force an Anthropic 400 on demand. `48-VALIDATION.md`'s row "IF Research Errored fires on a real erroring Anthropic call" is therefore recorded as unachievable-this-phase rather than attempted. |
| COVER-02 | `unclassified` → **unresolved** | Carried by 48-03/48-05/48-06. Recorded here so the row is not dropped from any plan's view. The one proof execution this plan spends must appear in the actual-vs-estimate comparison. |

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-deploy baseline and prepare the exact operator invocations</name>
  <files>.planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md</files>
  <read_first>
    - scripts/deploy_n8n_workflows.py (its module docstring's two-key gate at :8-25, `_writes_allowed` :235, and the note that activation is a separate operator-runbook step not performed by the script)
    - n8n/wf_enrichment_cloud.json (the node list plan 48-02 produced)
    - .planning/phases/47.5-veto-recompute-path/47.5-C-DECISION.md (§ "Deploy record" — the shape this proof should take)
  </read_first>
  <action>
    Perform a read-only GET of the deployed `LV Enrichment (Cloud template)` workflow
    (`950HPb7a1GgSAIyZ`) and record its current node names and count as the pre-deploy baseline.
    Confirm `IF Research Errored` is ABSENT from it — that absence is what makes the post-deploy
    presence meaningful.

    Run the deploy script in its default dry-run mode to capture the diff it would apply, and
    record that output. Note the known diff noise this repo has documented — credential ids,
    `webhookId`, and the executeWorkflow placeholder always differ — so a reviewer does not read
    it as a real change.

    Open `48-DEPLOY-PROOF.md` with: the baseline node list and count, the dry-run diff, and the
    two exact commands the operator will run in Task 2, written out in full and ready to paste.
    Both live reads use the dotenv form with an ABSOLUTE `.env` path.

    Do not set `ALLOW_N8N_DEPLOY`. Do not run the deploy with `DRY_RUN=false`. Do not toggle
    activation. Each of those is the operator's, in Task 2, and the Phase 47.5 waiver that once
    delegated them to Claude expired with that phase.
  </action>
  <verify>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md && grep -c 'ALLOW_N8N_DEPLOY' .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - `48-DEPLOY-PROOF.md` records the pre-deploy live node count and explicitly states that `IF Research Errored` was absent, with the timestamp of the GET.
    - The document contains the two operator commands in full, including the single-invocation form that sets both `DRY_RUN=false` and `ALLOW_N8N_DEPLOY=true`, and the deactivate-then-reactivate bounce step.
    - The dry-run diff output is captured verbatim, with the known-noise fields named so they are not misread as changes.
    - No command executed by this task set `ALLOW_N8N_DEPLOY` or `DRY_RUN=false`; the plan summary states this explicitly.
  </acceptance_criteria>
  <done>The baseline is captured, the absence of the gate in the running instance is on record, and the operator has a ready-to-paste pair of commands.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator deploys and bounces — the ONE declared deploy of D-06</name>
  <action>
    HALT and hand off. The operator runs the deploy and the bounce themselves, using the two
    commands Task 1 wrote into `48-DEPLOY-PROOF.md`. Claude executes nothing in this task: it must
    not set `ALLOW_N8N_DEPLOY`, must not run the deploy script with `DRY_RUN=false`, and must not
    toggle activation. Wait for the resume signal, then proceed to Task 3.
  </action>
  <what-built>
    Plan 48-02 built the D-04 research-error gate into `scripts/build_cloud_workflows.py` and
    regenerated `n8n/wf_enrichment_cloud.json`. Two new nodes — `IF Research Errored` and
    `Build Research Failure Response` — sit between `Claude Web Research` and
    `Validate Research Output`. Offline tests are green against the real emitted expression.
    Nothing is deployed. Task 1 has captured the pre-deploy baseline and written both commands
    into `48-DEPLOY-PROOF.md`.
  </what-built>
  <instructions>
    Run the two commands recorded in `.planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md`:

    1. The deploy, with both keys set in ONE invocation — the script's gate requires
       `DRY_RUN=false` AND `ALLOW_N8N_DEPLOY=true` together, and setting them in separate shells
       will silently produce another dry run.
    2. The bounce: deactivate the enrichment workflow, then reactivate it. Without this, n8n keeps
       running the old in-memory code and the deploy is invisible to every execution.

    Then tell Claude the deploy is done. Claude will prove the running instance actually reloaded,
    in Task 3, using a live execution rather than a read-back.

    This is the ONE deploy and bounce declared in D-06 for this phase. If a second becomes
    necessary, that is a disclosure obligation in the run report, not a silent event.
  </instructions>
  <verification>
    Task 3 verifies this checkpoint, and it is the only acceptable verification: one disarmed
    recompute POST, then a read of that execution's OWN embedded workflow node list showing
    `IF Research Errored` present. A GET of the stored workflow definition proves only that
    storage changed, not that the running instance reloaded, and is not accepted here.
  </verification>
  <resume-signal>Type "deployed" once both the deploy and the bounce have completed, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Prove the RUNNING instance carries the gate, using one disarmed execution</name>
  <files>.planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md</files>
  <precondition>Task 2's checkpoint returned "deployed" — the operator has completed both the deploy and the deactivate-then-reactivate bounce.</precondition>
  <read_first>
    - scripts/remediate_veto_companies.py (`post_webhook_event` :596 — its `armed` parameter has no default and its read timeout defaults to 300s)
    - .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md (the baseline from Task 1)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (Traps 1, 2, 3 and 6)
  </read_first>
  <action>
    Fire exactly ONE disarmed recompute POST for Jam TV `17317850381` via
    `post_webhook_event(..., recompute=True)`, then read the resulting execution back through the
    n8n API and record, in `48-DEPLOY-PROOF.md`: the execution id, its node count, its node-level
    `runData` keys, and — the load-bearing assertion — that the execution's OWN embedded workflow
    node list contains `IF Research Errored` and `Build Research Failure Response`. That list is
    produced by the running instance at execution time, which is why it proves what a GET of the
    stored definition cannot.

    Budget note to carry into the run report: this proof costs 1 n8n execution beyond the 5
    recompute POSTs plan 48-05 will spend, and 0 provider credits and 0 Anthropic calls, because
    the recompute lane routes straight from `Company Gate` to `Decide Company Action` and never
    touches providers, research, judge or merge.

    Judge the run by node-level `runData`, never by execution status — a node can receive a 400
    and pass the error object downstream as data with `status: "success"` and zero node-level
    errors. Check the duration too: a healthy company recompute is 20 nodes disarmed and 21
    armed, and runs 10-37 seconds; roughly 2 seconds means the chain died early.

    If the POST times out client-side, do NOT retry. n8n completes server-side; read the
    execution back first and confirm what actually happened. Retrying on a timeout is how a
    record gets touched twice.

    Expected outcome, disarmed: the write is blocked. That is success for this task — the point
    is that the lane ran and the new nodes were loaded, not that anything was written.

    Finally, record in the same document the honest limitation: no execution in this phase
    traverses the research branch, so the gate's live FIRING on a genuine Anthropic error is not
    proven here. Structural presence in the running instance plus plan 48-02's offline expression
    test is the bar this phase meets, and the run report must say so rather than imply more.
  </action>
  <verify>
    <automated>grep -cE 'IF Research Errored' .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md</automated>
    <automated>grep -cE 'execution' .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - `48-DEPLOY-PROOF.md` names a specific execution id created after the bounce, and records that execution's own node list containing both new node names.
    - The document records the execution's node count and duration, and states whether they fall in the healthy 20-node / 10-37 second range.
    - The document records the execution's outcome as write-blocked, confirming it ran disarmed.
    - The document contains an explicit statement that the gate's live firing is NOT proven this phase, with the reason.
    - Exactly one POST was made; the document states the count, and no retry occurred.
    - `git log --oneline -1 -- n8n/wf_enrichment_cloud.json` shows the workflow JSON was committed by plan 48-02 and not modified by this plan.
  </acceptance_criteria>
  <done>The running instance is proven to carry the D-04 gate by an execution it produced itself, the proof cost one disarmed execution and nothing else, and the limits of the proof are stated rather than glossed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build artifact → deployed n8n instance | A PUT changes what production runs |
| Claude → deploy/activation authority | Deliberately severed this phase; the operator holds it |
| n8n execution API → proof artifact | The only trustworthy evidence of what is actually running |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-11 | Elevation of Privilege | `scripts/deploy_n8n_workflows.py` two-key gate | high | mitigate | Claude never sets either key; the deploy and the bounce are a blocking operator checkpoint, and the plan's own commands are read-only or disarmed |
| T-48-12 | Repudiation | "deployed" claimed without a running-instance change | high | mitigate | Proof is an execution's own embedded node list, produced after the bounce; a stored read-back is explicitly rejected as evidence |
| T-48-13 | Tampering | a retry after a client timeout touching a record twice | medium | mitigate | No retry on timeout; read the execution back first. The POST is disarmed regardless, so no write is possible |
| T-48-14 | Information Disclosure | `.env` contents in logs | medium | mitigate | `.env` is Read/Bash permission-blocked; live reads go through the dotenv form with an absolute path and no secret is printed |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `48-DEPLOY-PROOF.md` committed, citing a post-bounce execution id and that execution's node list
- Exactly 1 deploy, 1 bounce, 1 disarmed execution attributable to this plan
- 0 provider credits, 0 Anthropic calls, 0 HubSpot writes
- `node --test tests/n8n/*.test.mjs` and `.venv/bin/python -m pytest` both still exit 0
</verification>

<success_criteria>
- The gate is live in the running instance, proven by the instance itself
- The D-06 deploy declaration is honoured, or any excess is disclosed
- The limits of the proof are recorded, not implied away
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-04-SUMMARY.md` when done, recording the deploy
and bounce as operator-performed, the proof execution id, and the one extra n8n execution to be
reconciled against the estimate in 48-06.
</output>
