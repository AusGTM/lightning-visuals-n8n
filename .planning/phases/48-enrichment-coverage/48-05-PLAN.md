---
phase: 48-enrichment-coverage
plan: 05
type: execute
wave: 5
depends_on: ["48-03", "48-04"]
files_modified:
  - scripts/enrich_coverage_companies.py
  - tests/test_enrich_coverage_companies.py
  - .planning/phases/48-enrichment-coverage/48-BEFORE.json
  - .planning/phases/48-enrichment-coverage/48-ARM-RECORD.md
  - .planning/phases/48-enrichment-coverage/48-AFTER.json
autonomous: false
requirements: [COVER-01, COVER-02]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every record in the live-derived population ends the window with either a real lv_org_type or the D-03 marker — unknown plus a non-empty review reason — and no record is left blank (COVER-01)."
    - "The writes happened inside exactly ONE armed window, opened once and closed once, capped at 5 records, matching the D-06 declaration; any excess is disclosed in the run report rather than passing silently."
    - "Both arming surfaces were armed together and disarmed together, and the disarm was confirmed by an independent re-read rather than by re-reading what was written."
    - "The allowlist was asserted non-empty AND exactly the intended id set by the driver at arm time, not by eyeball — an empty allowlist denies every write while still reporting armed."
    - "Jam TV 17317850381 still carries its geographic veto after the recompute settles; the broadcaster write adds base points and cannot clear a non-ANZ veto (D-23)."
    - "No derived scoring field was ever PATCHed by this driver; every score, tier, flag and reason change observed came from the n8n Decide Company Action node settling after the input write (project D-07)."
    - "Each record was touched exactly once — Editix is the only region/org-type overlap and its research resolved neither, so it took one write and no region PATCH (D-08)."
  prohibitions:
    - statement: "The driver never issues a PATCH containing any of the four derived scoring fields owned by Decide Company Action."
      status: required
      verification: "grep -vE '^\\s*(#|\")' scripts/enrich_coverage_companies.py | grep -cE '\"lv_anti_icp_(flag|reason)\"|\"lv_icp_(fit_score|tier)\"' reads 0, and every PATCH body captured in 48-AFTER.json contains only org-type, review-reason and verified-at keys"
    - statement: "No second writer of the veto fields is introduced anywhere in this phase."
      status: required
      verification: "Decide Company Action remains the sole writer; the existing dot-anchored single-writer guard test stays green"
    - statement: "Claude never arms either surface; both arming commands are operator-run."
      status: required
      verification: "Every arming command appears only inside a blocking checkpoint's operator instructions"
    - statement: "A client-side POST timeout is never retried without first reading the execution back."
      status: required
      verification: "The run record shows one POST per record and, for any timeout, an execution read-back before any further action"
    - statement: "No region property is written to any record in this population."
      status: required
      verification: "No PATCH body captured in 48-AFTER.json contains a key matching country_region"
  artifacts:
    - .planning/phases/48-enrichment-coverage/48-BEFORE.json
    - .planning/phases/48-enrichment-coverage/48-AFTER.json
  key_links:
    - "Two independent arming surfaces guard two independent write paths: the driver's own env-key gate guards the direct HubSpot PATCH, and june_run_arm.py's n8n-side allowlist guards Decide Company Action → HubSpot Company Update. Both must be armed together or the recompute leg reports success and writes nothing."
    - "post_webhook_event(..., recompute=True) carries the intent as a request-level boolean — a mode-borne intent is treated as return-only and would report success while writing nothing."
    - "Writing lv_org_type completes the record, so Company Gate will say skip on every future trigger; the recompute lane is the only way the derived chain settles afterwards (D-09)."
---

<objective>
Spend the phase's ONE declared armed write window: write `lv_org_type` (or the D-03 marker) to
every record in the live-derived population, fire one recompute POST per written record so the
derived scoring chain settles, read the result back independently, and close both arming
surfaces.

Purpose: this is the plan that actually satisfies COVER-01 for Phase 48's share of the
population, and it is the only plan in the phase that writes anything. Everything before it
exists to make this window short, capped, and fully prepared.

Output: `48-BEFORE.json`, `48-AFTER.json`, and five records that are no longer blank.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-VALIDATION.md
@.planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md
</context>

## Artifacts this phase produces

New artefacts from this plan: `48-BEFORE.json`, `48-AFTER.json`, and the driver functions
`assert_allowlist_exact()` and `run_coverage_window()`. Full phase-wide list in 48-01-PLAN.md.

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | This plan assumes Phase 48's share of COVER-01 is the live-derived blank-`lv_org_type` population as of the write date — expected to be the 5 ids, subject to Task 1's re-derivation. It does NOT claim full COVER-01 closure: Phase 47 covers 17 records and neither phase closes the requirement alone (REQUIREMENTS.md, D-02 split). |
| COVER-02 | `unclassified` → **unresolved** | This plan assumes the after-half of COVER-02 is satisfied by capturing actual n8n executions, provider credits and Anthropic spend during the window, for 48-06 to reconcile against the ex-ante estimate. |

<tasks>

<task type="auto">
  <name>Task 1: Pre-arm — re-derive live, snapshot before, dry-run the exact five payloads</name>
  <files>scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py, .planning/phases/48-enrichment-coverage/48-BEFORE.json</files>
  <read_first>
    - scripts/enrich_coverage_companies.py (current state — `derive_population`, `reconcile_population`, `ORG_TYPE_DECISIONS` with all 5 entries after plan 03, `build_coverage_patch`, `coverage_writes_allowed`)
    - scripts/june_run_arm.py (its module docstring at :12-30 — the `ALLOW_N8N_ARM` gate, `--ids`, `--domains`, and that `--disarm` is deliberately NOT gated so a window can always be closed)
    - scripts/remediate_veto_companies.py (`verify_post_run` :688, `settle_veto` :537, `settle_and_assert` :492 — reuse these rather than writing a parallel poller)
    - .planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md (§ pre-arm guard — the shape this task's guard should take)
  </read_first>
  <action>
    Re-derive the population live AGAIN, at write time, and reconcile it against
    `COVERAGE_COMPANY_ID_ORDER`. Plan 01's derivation is evidence, not a guarantee — a record
    could have been enriched by another path since. If `reconcile_population` reports drift, stop
    and disclose; do not silently widen or narrow the write set.

    Write `48-BEFORE.json`: for each id, the current `lv_org_type`, `lv_enrichment_review_reason`,
    `lv_icp_fit_score`, `lv_icp_tier`, `lv_anti_icp_flag`, `lv_anti_icp_reason`,
    `lv_country_region_normalized`, and `hs_lastmodifieddate`. The last of these is what lets a
    later reader confirm which records the window actually touched.

    Add `assert_allowlist_exact(expected_ids)` to the driver: it reads the deployed workflow's
    write-safety allowlist and raises unless it is non-empty AND its id set equals `expected_ids`
    exactly. An empty allowlist arms successfully and denies every write while still reporting
    armed — that is the trap this function exists to make impossible to walk into. Cover it with
    a unit test for three cases: empty, superset, and exact.

    Add `run_coverage_window(ids, armed, ...)` as the single entry point for Task 3, structured so
    the window is opened and closed within one invocation. It must call `coverage_writes_allowed()`
    and `assert_allowlist_exact()` before its first write, and it must never trim its id set.

    Produce and record the five exact dry-run PATCH payloads. Each contains only `lv_org_type`,
    `lv_org_type_verified_at`, and — for Editix `17317381378` alone — `lv_enrichment_review_reason`.
    The concrete write set, per CONTEXT.md's mapping table and D-05:
      `17317850381` Jam TV → `broadcaster`
      `20538284384` Waikato Racing Club → `individual_club_team`
      `20943964946` The Rumble → `content_producer`
      `17317381378` Editix → `unknown` plus its non-empty review reason (one write, no region key)
      `15008671672` Racing NSW → the value plan 48-03 resolved and validated
    Every value is checked against the imported `VALID_ORG_TYPES` before the payload is built — an
    out-of-vocabulary value 400s and fails the whole batch, so it must never reach the wire.

    Then open `.planning/phases/48-enrichment-coverage/48-ARM-RECORD.md` — the durable pre-arm
    record — containing the write-time re-derivation result, the five dry-run payloads verbatim,
    and the two operator arming commands written out in full and ready to paste: the driver's own
    two-key form, and the `june_run_arm.py --ids` form with the five ids listed explicitly. Task 3
    appends the arm-time allowlist assertion, the per-record outcomes and the disarm evidence to
    the same file, so the whole window has one durable home rather than living only in a summary
    written after the fact.

    Arm nothing. Write nothing. Every live read here goes through the dotenv form with an ABSOLUTE
    `.env` path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k "allowlist or window" -x</automated>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-BEFORE.json && .venv/bin/python -c "
import json;d=json.load(open('.planning/phases/48-enrichment-coverage/48-BEFORE.json'));print(len(d));assert len(d)>=1"</automated>
  </verify>
  <acceptance_criteria>
    - `48-BEFORE.json` exists with one entry per id in the live-reconciled set, each carrying the seven properties named above plus `hs_lastmodifieddate`.
    - The plan summary records the write-time re-derivation result and states explicitly whether it matched plan 01's derivation and CONTEXT.md's 2026-08-12 snapshot.
    - Three unit tests cover `assert_allowlist_exact`: raises on empty, raises on a superset, passes on the exact set.
    - A unit test asserts `run_coverage_window` raises before its first write when `coverage_writes_allowed()` is false.
    - `48-ARM-RECORD.md` exists and contains the five dry-run payloads verbatim plus both operator arming commands in full, with the five ids listed explicitly in the `--ids` form.
    - None of the five payloads contains a key matching `country_region`, and none contains any of the four derived scoring field names.
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0 and nothing was armed or written.
  </acceptance_criteria>
  <done>The population is re-derived at write time, the before-state is captured, the allowlist guard exists and is tested, the five payloads are known exactly, and the operator has ready-to-paste arming commands.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator arms BOTH surfaces — the ONE declared write window of D-06</name>
  <read_first>
    - .planning/phases/48-enrichment-coverage/48-ARM-RECORD.md and 48-BEFORE.json (Task 1's output — the five payloads and the two ready-to-paste arming commands)
    - scripts/june_run_arm.py (module docstring :12-30 — the n8n-side allowlist, `--ids`, and the ungated `--disarm`)
  </read_first>
  <action>
    HALT and hand off. The operator opens both arming surfaces themselves, using the two commands
    Task 1 printed. Claude executes nothing in this task: it must not set the driver's arm key and
    must not run the n8n-side arming helper. Wait for the resume signal, then proceed to Task 3,
    whose first act is to assert in code that the allowlist is non-empty and exactly the five ids.
  </action>
  <what-built>
    The driver can now write the five payloads and fire one recompute POST per record. Task 1 has
    captured `48-BEFORE.json`, re-derived the population at write time, and printed both arming
    commands. Nothing is armed; the driver refuses every write path until both surfaces are open.
  </what-built>
  <instructions>
    There are TWO independent arming surfaces guarding TWO independent write paths. Both must be
    open or the run half-works in a way that reports success:

    1. The driver's own two-key gate guards the direct HubSpot PATCH that writes `lv_org_type`.
       Set both keys in the SAME shell invocation as the run command in Task 3 — the run is
       per-shell, so nothing persists afterwards.
    2. `scripts/june_run_arm.py --ids <the five ids>` guards the n8n side: the write-safety
       allowlist that lets `Decide Company Action` reach `HubSpot Company Update`. Without it the
       recompute POSTs derive the correct values and return write-blocked, which is exactly what
       happened on a prior phase's execution 11858.

    Run the two commands printed in the 48-05 pre-arm record. Then tell Claude the window is open.
    Claude will assert, in code and before its first write, that the allowlist is non-empty and
    contains exactly those five ids — an empty allowlist arms successfully and blocks everything
    while still reporting armed.

    This is the ONE armed write window declared in D-06, capped at 5 records. Phase 47 needed five
    arm/disarm cycles against a declaration of one and had to disclose it; Phase 47.5's correction
    was declaring the count up front and using exactly that many. If a second window becomes
    necessary here, it is a disclosure obligation in the run report, not a silent event.

    Claude will close both surfaces at the end of Task 3 and prove the closure with an independent
    re-read.
  </instructions>
  <verification>
    Task 3's first act verifies this checkpoint: `assert_allowlist_exact` reads the deployed
    workflow's write-safety allowlist and raises unless it is non-empty AND its id set equals the
    five ids exactly. An empty allowlist arms successfully and denies every write while still
    reporting armed, so an eyeball confirmation is not accepted here. If it raises, the window is
    closed again and the checkpoint is re-presented rather than worked around.
  </verification>
  <acceptance_criteria>
    - `48-05-SUMMARY.md` records that both arming surfaces were operator-opened, with the resume signal received, and that Claude armed neither.
    - `assert_allowlist_exact` passed against exactly the five ids before the first write; the summary records the id set it observed.
    - The armed-window count is recorded as 1 opened and 1 closed, or any excess is disclosed against the D-06 declaration and its record cap of 5.
  </acceptance_criteria>
  <resume-signal>Type "armed" once both surfaces are open, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The armed window — write, recompute, settle, read back, disarm</name>
  <files>scripts/enrich_coverage_companies.py, .planning/phases/48-enrichment-coverage/48-AFTER.json, .planning/phases/48-enrichment-coverage/48-ARM-RECORD.md</files>
  <precondition>Task 2's checkpoint returned "armed" — the operator has opened both the driver's env-key gate and the n8n-side allowlist for exactly the five ids.</precondition>
  <read_first>
    - scripts/enrich_coverage_companies.py (`run_coverage_window`, `assert_allowlist_exact` from Task 1)
    - scripts/remediate_veto_companies.py (`post_webhook_event` :596, `settle_veto` :537, `settle_and_assert` :492, `verify_post_run` :688)
    - .planning/phases/48-enrichment-coverage/48-BEFORE.json
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (Traps 1, 2, 4 and 6; the specifics section on Jam TV and Waikato)
  </read_first>
  <action>
    Run the window as one invocation, so it cannot be left half-open across a task boundary.

    Order of operations. First, `assert_allowlist_exact` against the five ids — stop if it raises.
    Then, per record, in `COVERAGE_COMPANY_ID_ORDER`: PATCH the payload Task 1 recorded; fire one
    `post_webhook_event(..., recompute=True)`; poll until the derived chain settles using the
    existing settle helpers rather than a new poller. One POST per record, no more. Writing
    `lv_org_type` completes the record, so the gate would say skip on every future trigger and the
    sole writer of the veto fields would never run again — the recompute intent is what routes it
    through anyway, and it is carried as a request-level boolean, never as a mode value.

    On a client-side POST timeout: do not retry. n8n completes server-side and the 300-second
    default already accounts for a slow run. Read the execution back first and record what
    actually happened; retrying on a timeout is how a record gets touched twice.

    Judge every execution by node-level `runData`, never by execution status — a node can receive
    a 400 and pass the error object downstream as data with a successful-looking status and zero
    node-level errors. Check duration as a cheap tell: a healthy company recompute is 20 nodes
    disarmed and 21 armed, running 10-37 seconds; roughly 2 seconds means the chain died early.

    Then read every record back independently and write `48-AFTER.json` with the same property set
    as the before-snapshot, plus each record's execution id and node count. Four assertions the
    read-back must carry, each recorded as an observed value and not as a prediction:
      Jam TV `17317850381` STILL carries its veto and its reason still names non-ANZ geography.
        Its veto is geographic and its region is Other; a broadcaster classification adds base
        points and cannot clear it. Confirm it is still present, not merely that the write landed.
      Editix `17317381378` reads `unknown` with a non-empty review reason, and `coverage_state`
        classifies it attempted-unresolved rather than never-attempted. Its blank region maps to
        an unknown region key rather than a non-ANZ one, so no spurious geography veto can fire.
      Waikato `20538284384`'s gambling-operator boolean changes nothing — gambling is a graduated
        deduction and the graduated-deduction table has been empty since Phase 46. Record this
        explicitly so a future reader does not misread the boolean as a veto trigger.
      If Racing NSW `15008671672` resolved to a hardware-vendor classification it now acquires a
        hard veto and lands Tier D through the OR predicate. That is correct and intended; report
        it, do not suppress or work around it.

    Every score, tier, flag and reason difference between before and after must be attributable to
    the n8n node settling after an input write. This driver writes inputs only.

    Finally, close both surfaces in the same invocation: run the n8n-side disarm — which is
    deliberately ungated so a window can always be closed — and let the per-shell env keys lapse.
    Then prove the closure with an INDEPENDENT re-read: fetch the workflow's write-safety state
    fresh and record that both allowlists are empty and the workflow is still active. Re-reading
    what was just written proves nothing; the read must be a separate fetch.
  </action>
  <verify>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-AFTER.json && .venv/bin/python -c "
import json
b=json.load(open('.planning/phases/48-enrichment-coverage/48-BEFORE.json'))
a=json.load(open('.planning/phases/48-enrichment-coverage/48-AFTER.json'))
assert set(a)==set(b), 'before/after id sets differ'
for cid,row in a.items():
    p=row.get('properties',row)
    assert (p.get('lv_org_type') or '').strip(), f'{cid} still blank'
    if p.get('lv_org_type')=='unknown':
        assert (p.get('lv_enrichment_review_reason') or '').strip(), f'{cid} unknown with no reason'
jam=a['17317850381']; jp=jam.get('properties',jam)
assert str(jp.get('lv_anti_icp_flag')).lower()=='true', 'Jam TV veto lost'
print('coverage + jam tv veto assertions ok')"</automated>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `48-AFTER.json` exists with the same id set as `48-BEFORE.json`; every record has a non-blank `lv_org_type`, and every record reading `unknown` has a non-empty `lv_enrichment_review_reason`.
    - The inline assertion script above prints `coverage + jam tv veto assertions ok` and exits 0.
    - `48-AFTER.json` records, per record, the execution id and node count of its recompute run; the summary states each duration and whether it fell in the healthy range.
    - Exactly one PATCH and exactly one recompute POST per record; the summary states the counts and, if any exceeded one, discloses it against the D-06 declaration.
    - The summary records the observed before-and-after `lv_icp_fit_score`, `lv_icp_tier`, `lv_anti_icp_flag` and `lv_anti_icp_reason` for all five records, and states that every difference came from the n8n node settling rather than from a PATCH by this driver.
    - The disarm is confirmed by an independent fetch showing both allowlists empty and the workflow still active, and that fetch is recorded separately from the disarm command's own output.
    - `grep -vE '^\s*(#|")' scripts/enrich_coverage_companies.py | grep -cE '"lv_anti_icp_(flag|reason)"|"lv_icp_(fit_score|tier)"'` reads 0.
    - `.venv/bin/python -m pytest` and `node --test tests/n8n/*.test.mjs` both exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Each write is a property value, undone by clearing the property; the derived chain re-settles on the next recompute. D-03's semantics are the costly part and were rated in plan 01.</reversibility>
  <done>Every record in the population carries a real org type or the marker, the derived chain has settled and been read back independently, Jam TV's veto survived, and both arming surfaces are closed and proven closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → HubSpot CRM v3 | Real records are modified; an out-of-vocabulary enum value fails the whole batch |
| driver → n8n webhook → HubSpot | A second, indirect write path reached only when the n8n-side allowlist is open |
| operator shell → both arming surfaces | Two independent gates; either one closed silently degrades the run |
| n8n execution log → read-back assertions | The only trustworthy account of what the pipeline actually did |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | five PATCH payloads | high | mitigate | Every value validated against the imported 9-tuple before the payload is built; a bad value raises locally and never reaches the wire |
| T-48-04 | Elevation of Privilege | two arming surfaces | high | mitigate | Both operator-armed in a blocking checkpoint; `assert_allowlist_exact` proves non-empty and exact before the first write; both closed in the same invocation and proven closed by an independent fetch |
| T-48-15 | Tampering | duplicate write from a retried timeout | high | mitigate | No retry on client timeout; read the execution back first. One PATCH and one POST per record, counted and disclosed |
| T-48-16 | Repudiation | a scoring change attributed to the wrong writer | medium | mitigate | The driver writes inputs only; the derived-field negative grep is an acceptance criterion, and every before/after difference is attributed to the n8n node in the record |
| T-48-17 | Denial of Service | window left open after the run | high | mitigate | Disarm runs in the same invocation as the write, is ungated by design so it can always execute, and is proven by an independent re-read |
| T-48-18 | Information Disclosure | `.env` contents in logs | medium | mitigate | `.env` is permission-blocked; live work uses the dotenv form with an absolute path and prints no secret |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `48-BEFORE.json` and `48-AFTER.json` both committed, same id set
- Every record non-blank; every `unknown` accompanied by a non-empty reason
- Jam TV's veto still present, asserted from the read-back
- One arm, one disarm, disarm proven by an independent fetch
- `.venv/bin/python -m pytest` and `node --test tests/n8n/*.test.mjs` both exit 0
- Actual n8n executions, provider credits and Anthropic calls recorded for 48-06's reconciliation
</verification>

<success_criteria>
- Phase 48's share of the blank-org-type population is closed: real value or marker, none left blank
- Exactly one armed window, capped at 5, opened once and closed once — or the excess disclosed
- No derived scoring field PATCHed by this driver
- Each record touched exactly once
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-05-SUMMARY.md` when done, recording per-record
before and after values, execution ids, node counts, durations, the arm and disarm evidence, and
the actual cost figures 48-06 will reconcile against the estimate.
</output>
