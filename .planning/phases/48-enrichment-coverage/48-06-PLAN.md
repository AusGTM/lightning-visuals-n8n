---
phase: 48-enrichment-coverage
plan: 06
type: execute
wave: 6
depends_on: ["48-05"]
files_modified:
  - .planning/phases/48-enrichment-coverage/48-RUN-REPORT.md
  - .planning/decisions/2026-08-12-org-type-venue-and-normalization.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [COVER-01, COVER-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The actual execution, provider-credit and Anthropic cost of the run is reported against the pre-run estimate, line by line, with any variance named rather than averaged away (COVER-02)."
    - "The window accounting states how many deploys, bounces and armed windows were actually spent against D-06's declaration of one each, and discloses any excess explicitly."
    - "The D-02 amendment is written as a dated block on the LOCKED decision file, recording that the population was examined and the venue option was not spent — the deferral is on the record, not silently dropped."
    - "Neither COVER-01 nor COVER-02 is marked fully closed by Phase 48 alone; the requirements update records Phase 48's share and cites the Phase 47 + 48 split."
  prohibitions:
    - statement: "No enum-option PATCH is made to lv_org_type; venue is deferred and this plan only records the deferral."
      status: required
      verification: "No invocation of scripts/sync_hubspot_properties.py appears in this plan, and the live lv_org_type option count is unchanged at 9"
    - statement: "The LOCKED decision file's original text is not edited in place; the amendment is a new dated block."
      status: required
      verification: "git diff of the decision file shows added lines forming a dated block and no deletion or rewording of pre-existing decision text"
    - statement: "run_scoring_parity.py's population sweep is not 'fixed'; it is RED BY DESIGN until Phase 49."
      status: required
      verification: "git diff --stat shows scripts/run_scoring_parity.py unchanged by this phase"
    - statement: "The report does not claim the D-04 gate's live firing was proven."
      status: required
      verification: "The report's D-04 section states the proof bar met (structural presence in the running instance plus an offline expression test) and names what was not proven"
  artifacts:
    - .planning/phases/48-enrichment-coverage/48-RUN-REPORT.md
  key_links:
    - "The report reconciles against 48-COST-ESTIMATE.md's captured figures, so the ex-ante and ex-post numbers sit in one comparison rather than two unlinked documents."
    - "Plain-language tier-distribution narration stays Phase 49's deliverable (RESCORE-03); Phase 48 records the numbers only."
---

<objective>
Close the phase's evidence loop: report the actual cost against the pre-run estimate, account for
the windows spent against the D-06 declaration, amend the LOCKED venue decision with the dated
block D-02 obliges, and update the requirements traceability to Phase 48's share without claiming
closure that belongs jointly to Phase 47.

Purpose: COVER-02's second half is "reported after". A run whose cost is estimated and then never
reconciled has satisfied half a requirement. The window accounting is the same discipline applied
to operator ceremonies rather than money.

Output: `48-RUN-REPORT.md`, a dated amendment block, and updated traceability rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md
</context>

## Artifacts this phase produces

New artefact from this plan: `.planning/phases/48-enrichment-coverage/48-RUN-REPORT.md`, plus a
dated amendment block appended to the existing decision file. Full phase-wide list in
48-01-PLAN.md.

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | This plan assumes COVER-01's Phase 48 share is satisfied when the live-derived blank population is zero and every un-enrichable record carries a distinguishable marker. It records that judgement as a claim with evidence, and explicitly does not tick the requirement as fully closed — that needs Phase 47's 17 records counted alongside. |
| COVER-02 | `unclassified` → **unresolved** | This plan assumes COVER-02's Phase 48 share is satisfied by the estimate-before / report-after / refuse-rather-than-truncate triple, with the refusal path proven by test rather than by having fired. |

<tasks>

<task type="auto">
  <name>Task 1: Write 48-RUN-REPORT.md — actuals against the estimate, and the window accounting</name>
  <files>.planning/phases/48-enrichment-coverage/48-RUN-REPORT.md</files>
  <read_first>
    - .planning/phases/47.5-veto-recompute-path/47.5-RUN-REPORT.md (the shape to follow — pre-arm guard, per-record outcomes, cost actuals, window accounting)
    - .planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md (the ex-ante figures to reconcile against)
    - .planning/phases/48-enrichment-coverage/48-BEFORE.json and 48-AFTER.json
    - .planning/phases/48-enrichment-coverage/48-DEPLOY-PROOF.md
    - .planning/phases/48-enrichment-coverage/48-POPULATION.json
  </read_first>
  <action>
    Write `48-RUN-REPORT.md` with these sections, all sourced from the committed artefacts rather
    than from recollection:

    Population. The live count and id list from `48-POPULATION.json` with its derivation date, the
    write-time re-derivation from plan 05, and whether either differed from CONTEXT.md's
    2026-08-12 snapshot of 66 scored and 5 blank.

    Per-record outcomes. For each id: before and after `lv_org_type`,
    `lv_enrichment_review_reason`, `lv_icp_fit_score`, `lv_icp_tier`, `lv_anti_icp_flag`,
    `lv_anti_icp_reason`; the execution id, node count and duration; and the basis for the enum
    decision. Three of these need a named note: Jam TV's veto is geographic and survived the
    broadcaster write as D-23 requires; Editix reads the marker and is attempted-unresolved rather
    than never-attempted; Waikato's gambling-operator boolean changed nothing because the
    graduated-deduction table has been empty since Phase 46, and a future reader must not misread
    it as a veto trigger. If Racing NSW resolved to a hardware-vendor classification, report the
    resulting hard veto and Tier D plainly as a correct consequence of the OR predicate.

    Cost actuals against the estimate. A table with the estimated and actual figures side by side
    for n8n executions, provider credits and Anthropic spend, and a named reason for any variance.
    The estimate projected six n8n executions — five recompute POSTs plus one disarmed
    deploy-proof execution — one research call, zero provider credits, and roughly seven cents.
    Record the Lusha balance before and after to evidence the zero.

    Window accounting. Deploys, bounces and armed windows actually spent, against D-06's
    declaration of one each and a record cap of five. If the actual exceeded the declaration in any
    row, say so in that row — this is a disclosure obligation, and the precedent this phase is
    correcting is a prior phase that spent five arm/disarm cycles against a declaration of one.

    D-04 status. State the proof bar that was met — the gate is present in the running instance,
    proven by an execution's own node list, and its routing logic is proven offline against a
    genuine error payload, a healthy payload and a degenerate payload — and state plainly what was
    not proven: the gate's live firing on a real erroring Anthropic call, because no execution in
    this phase traverses the research branch and an Anthropic error cannot be induced on demand.

    Requirements status. Phase 48's share of COVER-01 and COVER-02, explicitly not claiming full
    closure. Neither Phase 47 nor Phase 48 closes either requirement alone.

    Deliberately not narrated here: the plain-language before-and-after tier distribution. Phase 48
    records the numbers; Phase 49 narrates them under RESCORE-03.
  </action>
  <verify>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-RUN-REPORT.md && grep -cE 'Window accounting|window accounting' .planning/phases/48-enrichment-coverage/48-RUN-REPORT.md</automated>
    <automated>.venv/bin/python -c "
import re,pathlib
t=pathlib.Path('.planning/phases/48-enrichment-coverage/48-RUN-REPORT.md').read_text()
for tok in ['15008671672','17317381378','17317850381','20538284384','20943964946']:
    assert tok in t, f'missing record {tok}'
print('all five records reported')"</automated>
  </verify>
  <acceptance_criteria>
    - `48-RUN-REPORT.md` names all five company ids and prints their before and after values for the six properties listed.
    - It carries a cost table with estimated and actual columns for n8n executions, provider credits and Anthropic spend, and a named reason for every non-zero variance.
    - It carries a window-accounting section stating deploys, bounces and armed windows spent against the declaration of one each and a cap of five.
    - It states explicitly that the D-04 gate's live firing was not proven, with the reason.
    - It states explicitly that neither COVER-01 nor COVER-02 is fully closed by Phase 48 alone.
    - It records the Lusha balance before and after, evidencing zero provider credits drawn.
  </acceptance_criteria>
  <done>The phase's cost, outcomes and ceremonies are on the record, reconciled against what was projected, with every gap named rather than smoothed over.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the LOCKED venue decision with a dated deferral block (D-02)</name>
  <files>.planning/decisions/2026-08-12-org-type-venue-and-normalization.md</files>
  <read_first>
    - .planning/decisions/2026-08-12-org-type-venue-and-normalization.md (read the existing dated correction block at the top — this is the house style to copy, and it is where the "no portal work is required" clause was already corrected)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (D-02, and the population table showing which record was the nearest miss)
    - .planning/phases/48-enrichment-coverage/48-AFTER.json (the values actually written, evidencing that none needed the deferred option)
  </read_first>
  <action>
    Append a new dated block to the decision file in the same style as the correction block already
    at its top. Never edit the original decision text in place — the house style is additive dated
    amendments, so the decision's history stays readable.

    The block records: that the LOCKED decision named Phase 48 as the implementing phase; that
    Phase 48 examined the whole live population and found no record whose evidence maps to the
    deferred option; that Waikato Racing Club `20538284384` was the nearest miss as a racecourse
    and event centre, and mapped better to `individual_club_team`, which is what was written; that
    adding the option is a HubSpot enum-option PATCH and therefore portal schema work with its own
    arming, which this phase did not spend; and that the option should be revisited when a record's
    evidence actually demands it.

    Do not run `scripts/sync_hubspot_properties.py`. Do not PATCH the property's options. The live
    enumeration must still hold exactly nine options when this task is done.
  </action>
  <verify>
    <automated>grep -c '2026-08' .planning/decisions/2026-08-12-org-type-venue-and-normalization.md</automated>
    <automated>git diff --numstat .planning/decisions/2026-08-12-org-type-venue-and-normalization.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` for the decision file shows added lines and zero deleted lines — the amendment is additive.
    - The new block carries a date, names the phase that examined the population, names Waikato `20538284384` as the nearest miss and the value written instead, and states that adding the option is portal schema work requiring its own arming.
    - A live re-list of the `lv_org_type` property shows exactly 9 options, unchanged.
    - No invocation of `scripts/sync_hubspot_properties.py` appears anywhere in this plan's execution record.
  </acceptance_criteria>
  <reversibility rating="reversible">Per D-02: deferring costs nothing; a future phase adds the option when a record demands it.</reversibility>
  <done>The deferral is recorded on the LOCKED decision as a dated amendment, with the evidence that the population was examined, and the portal enumeration is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update requirements traceability to Phase 48's share, without claiming closure</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (COVER-01 at :98-105, COVER-02 at :106-112, the traceability rows at :176-177, and the split note at :184-186)
    - .planning/phases/48-enrichment-coverage/48-RUN-REPORT.md (the evidence the update cites)
  </read_first>
  <action>
    Update the two COVER traceability rows with a pointer to the evidence rather than a bare status
    flip. Each row should name what Phase 48 contributed — the live-derived population, the records
    written, the marker applied, the estimate-and-report pair — and cite `48-RUN-REPORT.md`.

    Do not mark either requirement fully complete on Phase 48's evidence alone. The scope amendment
    already in the file records that Phase 47 covers its 17 records and Phase 48 the remainder, and
    that neither phase may be closed claiming full coverage of COVER-01 or COVER-02 on its own.
    Whether the pair is now jointly satisfied is a judgement that reads both phases' evidence
    together; state Phase 48's half and leave the joint call to whoever seals the milestone.

    Use scoped edits to the specific rows and lines. Do not rewrite the file.
  </action>
  <verify>
    <automated>grep -nE '^\| COVER-0[12] \|' .planning/REQUIREMENTS.md</automated>
    <automated>git diff --numstat .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Both COVER traceability rows cite `48-RUN-REPORT.md` and name Phase 48's specific contribution.
    - Neither row reads a status that asserts full closure on Phase 48's evidence alone.
    - `git diff .planning/REQUIREMENTS.md` touches only the COVER rows and, at most, the adjacent COVER requirement bodies — no unrelated requirement text is modified.
    - The existing Phase 47 + 48 split note at the end of the file is present and unmodified.
  </acceptance_criteria>
  <done>Traceability points at the evidence, Phase 48's share is stated precisely, and the joint-closure judgement is left where it belongs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run artefacts → run report | The report must derive from committed evidence, not from recollection |
| LOCKED decision file → amendment | An in-place edit would erase decision history |
| requirements traceability → milestone seal | An over-claimed status propagates into the milestone judgement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-19 | Repudiation | run report figures | high | mitigate | Every figure is sourced from a committed artefact named in `read_first`, and variance is reported per line rather than netted |
| T-48-20 | Tampering | LOCKED decision file | medium | mitigate | Additive dated block only; the numstat check asserts zero deleted lines |
| T-48-21 | Repudiation | requirement status over-claim | high | mitigate | Neither COVER row may assert full closure on this phase's evidence; the existing split note is asserted present and unmodified |
| T-48-22 | Tampering | the deliberately-red parity sweep | medium | mitigate | `scripts/run_scoring_parity.py` asserted unchanged by this phase; it is red by design until Phase 49 |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `48-RUN-REPORT.md` committed, naming all five records and carrying the cost and window tables
- Decision file amended additively; live `lv_org_type` still has exactly 9 options
- REQUIREMENTS.md edits scoped to the COVER rows, no closure over-claim
- `.venv/bin/python -m pytest` and `node --test tests/n8n/*.test.mjs` both exit 0
- `git diff --stat` shows `scripts/run_scoring_parity.py` unchanged
</verification>

<success_criteria>
- Actual cost reported against the estimate with variance named
- Window accounting stated against the D-06 declaration, with any excess disclosed
- The venue deferral recorded as a dated amendment on the LOCKED decision
- Phase 48's share of COVER-01/COVER-02 traced without claiming joint closure
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-06-SUMMARY.md` when done, stating the final
population count, the actual-versus-estimated cost, the window count spent, and whether any
disclosure obligation was triggered.
</output>
