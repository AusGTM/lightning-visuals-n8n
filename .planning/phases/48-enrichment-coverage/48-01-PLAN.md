---
phase: 48-enrichment-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enrich_coverage_companies.py
  - tests/test_enrich_coverage_companies.py
  - .planning/phases/48-enrichment-coverage/48-POPULATION.json
autonomous: true
requirements: [COVER-01, COVER-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The driver resolves each of the 5 coverage ids to exactly one of the 9 live lv_org_type enum options, or to the D-03 un-enrichable marker, with no keyword guessing anywhere in the code path (D-01, D-05)."
    - "A record marked un-enrichable carries a non-empty review reason, so its state is machine-distinguishable from a record whose lv_org_type was never attempted (D-03, COVER-01)."
    - "The driver refuses whole rather than trimming the id set when a projected cost exceeds either budget (COVER-02)."
    - "No write of any kind can occur from this plan: every network write path raises before its first request unless two independent operator keys are set (D-06)."
    - "The live population is re-derived through the HubSpot Search API and stamped with the date it was read, and a divergence from the 5-id literal set is disclosed rather than silently absorbed."
  prohibitions:
    - statement: "The driver never issues a PATCH containing any of the four derived scoring fields owned by the n8n Decide Company Action node (project D-07)."
      status: required
      verification: "grep -vE '^\\s*(#|\")' scripts/enrich_coverage_companies.py | grep -cE '\"lv_anti_icp_(flag|reason)\"|\"lv_icp_(fit_score|tier)\"' reads 0"
    - statement: "No free-text-to-enum keyword or regex mapper exists; the mapping is a literal per-record table authored from CONTEXT.md (D-01)."
      status: required
      verification: "The mapping module exposes a dict/tuple literal keyed by company id; no re.search / .lower().find / substring scan over research free text appears in the mapping code path"
    - statement: "The over-budget path refuses the whole run; no code path trims, slices or partially processes the id set (COVER-02)."
      status: required
      verification: "Unit test asserts BudgetRefused is raised on a synthetic over-budget estimate and that no id list shorter than the input is ever returned"
  artifacts:
    - scripts/enrich_coverage_companies.py
    - tests/test_enrich_coverage_companies.py
    - .planning/phases/48-enrichment-coverage/48-POPULATION.json
  key_links:
    - "VALID_ORG_TYPES is IMPORTED from scripts/remediate_veto_companies.py, never re-declared — a second 9-tuple can drift from the live portal enum."
    - "refuse_if_over_budget() is imported and called against the Phase-48-shaped estimate dict, not against estimate_cost()'s Phase-47-shaped output."
    - "post_webhook_event()'s armed parameter has no default; the driver passes it explicitly and never defaults it true."
---

<objective>
Build the Phase 48 coverage driver and its offline test suite: live population re-derivation,
the per-record enum mapping authored in CONTEXT.md (D-01/D-05), the D-03 un-enrichable marker,
the Phase-48-shaped cost estimate, and the refuse-whole budget gate — all of it dry-run,
unarmed, and provable without a single write.

Purpose: every later plan in this phase (the paid research call, the deploy, the armed window,
the run report) consumes this driver. Proving the whole spine end-to-end on one record before
expanding is what stops a mapping or arming defect from being discovered inside an armed
window.

Output: `scripts/enrich_coverage_companies.py`, `tests/test_enrich_coverage_companies.py`,
`.planning/phases/48-enrichment-coverage/48-POPULATION.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-PATTERNS.md
@.planning/phases/48-enrichment-coverage/48-HANDOVER.md
</context>

## Artifacts this phase produces

Symbols and paths created by Phase 48 (excluded from drift verification — none of these exist
in the codebase before this phase):

- `scripts/enrich_coverage_companies.py` — the Phase 48 driver
- `tests/test_enrich_coverage_companies.py` — its offline test suite
- `COVERAGE_COMPANY_ID_ORDER`, `ORG_TYPE_DECISIONS`, `UNENRICHABLE_REASONS` — module constants
- `resolve_coverage_ids()`, `derive_population()`, `decide_org_type()`, `build_coverage_patch()`,
  `estimate_phase48_cost()`, `coverage_writes_allowed()` — module functions
- `ALLOW_ENRICH_COVERAGE` — the driver's own arm key (new; deliberately NOT `ALLOW_VETO_REMEDIATION`)
- `IF Research Errored`, `Build Research Failure Response` — new n8n node names (plan 02)
- `RACING_NSW_ORG_TYPE_SYSTEM` — one-off enum-constrained research prompt (plan 03)
- `tests/n8n/researchErrorGateFlow.test.mjs` (plan 02)
- `.planning/phases/48-enrichment-coverage/48-POPULATION.json`, `48-COST-ESTIMATE.md`,
  `48-RESEARCH-RACING-NSW.json`, `48-BEFORE.json`, `48-AFTER.json`, `48-RUN-REPORT.md`

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | The deterministic edge probe could not classify COVER-01, so no backstop verification was auto-attached. This plan assumes the coverage bar is met by the per-record decision table plus the D-03 marker, and that "distinguishable from never attempted" is satisfied by `blank` vs `unknown` + non-empty review reason. If a reviewer reads COVER-01 as requiring a dedicated status property, that is NOT what this phase builds (D-03, no new properties). |
| COVER-02 | `unclassified` → **unresolved** | Likewise unclassified. This plan assumes "estimated before the run and reported after, against the 2,500/month n8n allowance and the current Lusha balance" is satisfied by a code-produced estimate dict + a Lusha balance read, and that "refused, not truncated" is satisfied by an exception that aborts the whole run. Neither Phase 47 nor Phase 48 closes COVER-01/COVER-02 alone (REQUIREMENTS.md D-02 split). |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end coverage slice for ONE record — Jam TV 17317850381, dry-run</name>
  <files>scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py</files>
  <read_first>
    - scripts/remediate_veto_companies.py (the analog to extend — read `resolve_pinned_ids` :202, `_writes_allowed` :249, `post_webhook_event` :596, `estimate_cost` :650, `refuse_if_over_budget` :674, `VALID_ORG_TYPES` :153, `N8N_EXECUTION_BUDGET_MONTH` :167, `ANTHROPIC_PER_RECORD_ESTIMATE_USD` :647, the classes at :184-199)
    - src/hubspot_client.py (`search_records` :119, `batch_update_companies` :88, `get_record` :16)
    - tests/test_veto_remediation_report.py (the offline network-refusing test style to mirror)
    - .planning/phases/48-enrichment-coverage/48-PATTERNS.md (§ "scripts/enrich_coverage_companies.py")
    - .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json (key `17317850381`)
  </read_first>
  <action>
    Create `scripts/enrich_coverage_companies.py` carrying the complete Phase 48 spine, wired
    end to end, exercised for exactly one record: Jam TV `17317850381`.

    Import — never re-declare — `VALID_ORG_TYPES`, `N8N_EXECUTION_BUDGET_MONTH`,
    `ANTHROPIC_PER_RECORD_ESTIMATE_USD`, `refuse_if_over_budget`, `post_webhook_event`,
    `BudgetRefused`, `NotArmedError`, `PinRefused` from `scripts.remediate_veto_companies`
    (repo-root `sys.path` insert, same idiom as `tests/test_veto_remediation_report.py`).

    Module docstring states the same boundary the analog's does: this script writes only
    `lv_org_type`, `lv_enrichment_review_reason` and `lv_org_type_verified_at`; the four
    derived scoring fields are recomputed by the n8n `Decide Company Action` node and are
    never in any patch this script builds (project D-07).

    Layers this tracer must wire, in order, for the single id:
    1. `COVERAGE_COMPANY_ID_ORDER` — a 5-tuple literal in CONTEXT.md's table order:
       `"15008671672"` (Racing NSW), `"17317381378"` (Editix), `"17317850381"` (Jam TV),
       `"20538284384"` (Waikato Racing Club), `"20943964946"` (The Rumble). Plus
       `COVERAGE_COMPANY_IDS = frozenset(...)` and `resolve_coverage_ids(requested)` raising
       `PinRefused` naming the offending id before any HubSpot or n8n call, returning ids
       sorted into tuple order — the `resolve_pinned_ids` pattern, new membership.
    2. `derive_population(searcher=search_records)` — calls the searcher with filters
       `[{"propertyName": "lv_icp_fit_score", "operator": "HAS_PROPERTY"},
       {"propertyName": "lv_org_type", "operator": "NOT_HAS_PROPERTY"}]` and properties
       `["hs_object_id", "name", "lv_org_type", "lv_icp_fit_score", "lv_icp_tier",
       "lv_country_region_normalized", "lv_anti_icp_flag"]`; returns
       `{"derived_at": <UTC ISO date>, "count": n, "ids": [...], "rows": [...]}`. In this task
       it is exercised with an injected fake searcher only.
    3. `ORG_TYPE_DECISIONS` — a literal dict keyed by company id, value
       `{"org_type": <enum>, "basis": <one-line prose citing the evidence>}`. Task 1 populates
       one entry: `"17317850381": {"org_type": "broadcaster", "basis": "47-RESEARCH-RESULTS.json
       matched=true confidence=85, 'Media company / Web television broadcaster'"}`.
    4. `decide_org_type(company_id, research)` — returns the table's decision. It reads the
       captured research only to assert the evidence it cites is present (`matched`,
       `confidence`, `data.lv_org_type`); it must not derive the enum value from that free
       text. There is no regex, no substring scan, no `.lower()` keyword match anywhere in
       this function — the value comes from the literal table (D-01, 48-RESEARCH.md
       "Don't Hand-Roll" row 3).
    5. `build_coverage_patch(company_id, decision, now_iso)` — returns a dict carrying exactly
       `lv_org_type`, `lv_org_type_verified_at`, and (only when the decision is the D-03
       marker) `lv_enrichment_review_reason`. It raises `ValueError` naming the id and the
       value when `decision["org_type"]` is not in the imported `VALID_ORG_TYPES` — the
       out-of-vocabulary write fails the whole batch, so it must never reach the wire.
    6. `estimate_phase48_cost(research_ids, written_ids, proof_executions=0)` — returns
       `{"web_research_calls": len(research_ids), "n8n_executions": len(written_ids) +
       proof_executions, "n8n_budget_month": N8N_EXECUTION_BUDGET_MONTH, "lusha_credits": 0,
       "lusha_credits_note": <D-01 wording>, "anthropic_estimate_usd":
       round(len(research_ids) * ANTHROPIC_PER_RECORD_ESTIMATE_USD, 4)}`. Racing NSW's
       research is a direct Anthropic call costing zero n8n executions; the n8n executions are
       exactly the D-09 recompute POSTs, one per written record, plus any disarmed
       proof-of-deploy execution the phase declares up front (`proof_executions`) so that
       execution is budgeted rather than discovered afterwards.
    7. `coverage_writes_allowed()` — the two-key gate, `DRY_RUN` defaulting `"true"` and
       `ALLOW_ENRICH_COVERAGE` defaulting `"false"`; both must flip for it to return True.
       Do not reuse the literal name `ALLOW_VETO_REMEDIATION`.
    8. `main()` with argparse `--ids`, `--dry-run/--no-dry-run`, `--population-out`. In
       dry-run it prints the derived population, the decision, the estimate dict, and the exact
       PATCH payload it would send, and returns 0 without any write.

    Racing NSW's slot stays absent from `ORG_TYPE_DECISIONS` in this task; `decide_org_type`
    raises a named `PendingResearch` error for an id with no entry, which is the correct
    tracer-time behaviour for a record whose evidence has not been bought yet.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k tracer -x</automated>
    <automated>DRY_RUN=true .venv/bin/python scripts/enrich_coverage_companies.py --ids 17317850381 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k tracer -x` exits 0 with at least one test that has monkeypatched `requests.post` and `requests.patch` to raise, proving the whole slice runs with zero network calls.
    - The dry-run CLI prints a PATCH payload whose keys are exactly `lv_org_type`, `lv_org_type_verified_at` and whose `lv_org_type` value is the literal string `broadcaster`.
    - `grep -c 'from scripts.remediate_veto_companies import' scripts/enrich_coverage_companies.py` is at least 1, and `grep -c 'VALID_ORG_TYPES = ' scripts/enrich_coverage_companies.py` reads 0 (imported, not re-declared).
    - A test asserts `build_coverage_patch` raises `ValueError` for a synthetic decision whose `org_type` is `"venue"` (not one of the 9 live options).
    - A test asserts `post_webhook_event(..., armed=False, ...)` raises `NotArmedError` before any transport call, using a transport double that raises `AssertionError` if invoked.
    - `grep -vE '^\s*(#|")' scripts/enrich_coverage_companies.py | grep -cE '"lv_anti_icp_(flag|reason)"|"lv_icp_(fit_score|tier)"'` reads 0.
  </acceptance_criteria>
  <reversibility rating="reversible">D-01's mapping is a per-record value write, undone by clearing the property; no schema or contract change.</reversibility>
  <done>One record travels the whole spine — live-search seam, captured evidence, literal decision, enum validation, cost estimate, budget gate, dry-run payload, unarmed-POST refusal — with a green offline test and zero writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the decision table to all 5 records, including the D-03 un-enrichable marker</name>
  <files>scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py</files>
  <read_first>
    - scripts/enrich_coverage_companies.py (current state after Task 1)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (§ "The per-record mapping table this phase implements", D-03, D-05, D-08)
    - .planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json (keys `17317381378`, `20538284384`, `20943964946`)
  </read_first>
  <behavior>
    - `decide_org_type("17317850381", ...)` returns `broadcaster`.
    - `decide_org_type("20538284384", ...)` returns `individual_club_team`.
    - `decide_org_type("20943964946", ...)` returns `content_producer` (D-05: it partners with Skate Australia, it does not govern the sport).
    - `decide_org_type("17317381378", ...)` returns the D-03 marker: `unknown` plus a non-empty reason.
    - `decide_org_type("15008671672", ...)` raises `PendingResearch` until plan 03 supplies the researched value.
    - `build_coverage_patch` for Editix emits `lv_enrichment_review_reason` with non-empty text; for the other three it emits no reason key at all.
    - A helper `coverage_state(record_properties)` classifies a record as `never_attempted` (blank `lv_org_type`), `attempted_unresolved` (`unknown` + non-empty reason) or `resolved` (any other valid enum value) — the machine-readable form of D-03's semantics.
  </behavior>
  <action>
    Extend `ORG_TYPE_DECISIONS` to the four already-evidenced records with the exact enum values
    and bases from CONTEXT.md's mapping table:
      `17317850381` Jam TV → `broadcaster` (research: "Media company / Web television
        broadcaster", matched=true, confidence 85)
      `20538284384` Waikato Racing Club → `individual_club_team` (research: "Racing Club /
        Sports Organization", matched=true, confidence 85)
      `20943964946` The Rumble / Pacific Action Sports → `content_producer` per D-05 (research
        "Event organizer / Sports league operator", confidence 92, but the same evidence names
        Skate Australia as the sport's body and The Rumble as a partner; it produces and
        broadcasts content, it does not govern)
      `17317381378` Editix → the D-03 un-enrichable marker: `unknown` plus a reason drawn from
        its own captured evidence (matched=false, confidence 5, every data field null, searches
        for the domain returned an XML editor, an AI book-editing tool and a media software
        vendor — no company matching the name+domain).

    Add `UNENRICHABLE_REASONS`, a literal dict keyed by company id holding that reason text, so
    the reason is authored data rather than generated prose.

    Add `coverage_state(record_properties)` implementing D-03's three-state semantics. Per D-08
    Editix takes exactly one write — it is the only record with both a blank region and a blank
    org type, and its research resolved neither, so no region property appears in any patch this
    driver builds.

    Write the tests first (the `<behavior>` list above), each loading its record from
    `47-RESEARCH-RESULTS.json` by id rather than from an inline fixture, so the test fails if
    the captured evidence ever changes shape.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k "mapping or marker" -x</automated>
  </verify>
  <acceptance_criteria>
    - Four tests assert the four literal enum values above, each loading its research entry from `.planning/phases/47-veto-remediation/47-RESEARCH-RESULTS.json` by company id.
    - A test asserts `coverage_state({"lv_org_type": ""})` is `never_attempted`, `coverage_state({"lv_org_type": "unknown", "lv_enrichment_review_reason": "..."} )` is `attempted_unresolved`, and that the two are not equal — COVER-01's distinguishability bar, asserted rather than described.
    - A test asserts `build_coverage_patch` for `17317381378` contains a `lv_enrichment_review_reason` whose value is non-empty, and that the patches for the other three contain no key matching `review_reason`.
    - A test asserts no patch built by this driver contains any key containing `country_region` (D-08: no record in this population needs a region PATCH).
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-03's blank-vs-`unknown` semantics become the query contract every future coverage sweep keys on; changing it later means re-interpreting every record written here. Flagged only — no checkpoint, per CONTEXT.md's own rating.</reversibility>
  <done>All four already-evidenced records map to their CONTEXT.md enum value, Editix carries the un-enrichable marker with a non-empty reason, and the three coverage states are machine-distinguishable and asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Live population re-derivation, date-stamped, with drift disclosure</name>
  <files>scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py, .planning/phases/48-enrichment-coverage/48-POPULATION.json</files>
  <read_first>
    - scripts/enrich_coverage_companies.py (current state after Task 2)
    - src/hubspot_client.py (`search_records` :119 — the filter/properties/limit signature)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (§ "The population", and the constraints-table row "Re-derive the population live, stamp the date")
  </read_first>
  <action>
    Run `derive_population()` against the live portal and write the result to
    `.planning/phases/48-enrichment-coverage/48-POPULATION.json` with a `derived_at` UTC
    timestamp, the count, the id list, and the per-row properties fetched.

    `.env` is Read/Bash permission-blocked. Drive the live read through the dotenv form with an
    ABSOLUTE path — `.venv/bin/python -c "from dotenv import load_dotenv;
    load_dotenv('/Users/robertli/Desktop/consulting/lightning-visuals/lv-n8n-poc/.env'); ..."` —
    because a bare `load_dotenv()` resolves relative to the calling file and every HubSpot read
    then 401s.

    Add `reconcile_population(derived, literal=COVERAGE_COMPANY_ID_ORDER)` returning
    `{"expected": [...], "derived": [...], "missing": [...], "unexpected": [...], "drift": bool}`.
    Its contract: it never mutates either set and never narrows the run — when `drift` is true
    the driver prints the reconciliation and exits non-zero, so a moved population is an operator
    decision, not something the driver absorbs. CONTEXT.md's snapshot (66 scored, 5 blank
    `lv_org_type`, read 2026-08-12) is the expectation; a different live count is a finding to
    disclose in the run report, not a reason to edit the literal tuple silently.

    If credentials are absent, the existing `_has_credentials()`-style degradation applies:
    print `skipped`, return 0, and record in the summary that `48-POPULATION.json` was not
    produced — do not fabricate a snapshot.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k "population or reconcile" -x</automated>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-POPULATION.json && .venv/bin/python -c "import json;d=json.load(open('.planning/phases/48-enrichment-coverage/48-POPULATION.json'));assert d['derived_at'];assert isinstance(d['ids'],list);print(d['derived_at'],d['count'])"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/48-enrichment-coverage/48-POPULATION.json` exists, is non-empty, and carries a `derived_at` value whose date component is the date the executor ran it.
    - A unit test injects a fake searcher and asserts `derive_population` passes exactly the two filters `lv_icp_fit_score`/`HAS_PROPERTY` and `lv_org_type`/`NOT_HAS_PROPERTY`.
    - A unit test asserts `reconcile_population` returns `drift: true` with a populated `missing` list when the derived ids omit one of the five, and that the returned `expected` list still has 5 entries — proving it never narrows the run.
    - The plan summary records the live count and id list actually observed, alongside CONTEXT.md's 2026-08-12 figures, and states explicitly whether they match.
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` and `node --test tests/n8n/*.test.mjs` both exit 0.
  </acceptance_criteria>
  <done>The population is re-derived from the live portal, stamped with today's date, committed as evidence, and reconciled against the 5-id literal set with any drift disclosed rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → HubSpot CRM v3 | An enum-valued property is written; an out-of-vocabulary value 400s and fails the whole batch |
| driver → n8n webhook | A request-level flag crosses into a workflow that can PATCH real records |
| 47-RESEARCH-RESULTS.json → driver | Untrusted-by-provenance free text (model output) crosses into a value that will be written to the CRM |
| operator shell env → driver | Two independent env keys gate every write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | `build_coverage_patch` → HubSpot PATCH | high | mitigate | Validate every value against the imported `VALID_ORG_TYPES` and raise before the wire; unit test asserts a non-option value raises |
| T-48-02 | Denial of Service (budget) | `estimate_phase48_cost` / `refuse_if_over_budget` | medium | mitigate | Refuse whole; no trimming path exists; unit test asserts the raise on a synthetic over-budget dict |
| T-48-04 | Elevation of Privilege | `coverage_writes_allowed` two-key gate | high | mitigate | Both `DRY_RUN=false` and `ALLOW_ENRICH_COVERAGE=true` required; default-deny; `post_webhook_event`'s `armed` has no default |
| T-48-05 | Spoofing | model free text promoted to a CRM enum value | medium | mitigate | The enum comes from a literal authored table, never derived from the free text (D-01); no keyword mapper exists |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages in any ecosystem this phase (48-RESEARCH.md § Package Legitimacy Audit); no install task exists, so the legitimacy gate has nothing to audit |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0
- `.venv/bin/python -m pytest` full suite exits 0 (`run_scoring_parity.py`'s population sweep is RED BY DESIGN until Phase 49 and is NOT part of this bar)
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form; the directory form is broken on node 24)
- `48-POPULATION.json` committed with a same-day `derived_at`
- Zero HubSpot writes and zero n8n executions attributable to this plan
</verification>

<success_criteria>
- The driver resolves all 5 coverage ids offline: 4 to a literal enum decision, 1 to `PendingResearch`
- Editix's un-enrichable state is asserted machine-distinguishable from a never-attempted record
- The budget gate raises rather than trims, proven by test
- Both write surfaces are default-deny and proven to raise unarmed
- The live population is re-derived and date-stamped
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-01-SUMMARY.md` when done, recording the live
population count and ids observed, and whether they matched CONTEXT.md's 2026-08-12 snapshot.
</output>
