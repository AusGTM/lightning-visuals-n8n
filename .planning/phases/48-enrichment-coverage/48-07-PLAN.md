---
phase: 48-enrichment-coverage
plan: 07
type: execute
wave: 3
depends_on: ["48-03"]
files_modified:
  - scripts/enrich_coverage_companies.py
  - config/taxonomy.yaml
  - src/taxonomy.py
  - src/web_research.py
  - tests/test_enrich_coverage_companies.py
  - tests/test_taxonomy_conformance.py
  - tests/test_web_research_spec.py
  - docs/WEB-RESEARCH-SPEC.md
  - .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md
autonomous: true
requirements: [COVER-01]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Racing NSW 15008671672 resolves to governing_body_league, and the divergence from the captured research's returned value is recorded as data on the decision entry rather than as prose, so a later reader can see the classification was overridden and why (D-01)."
    - "The correction costs nothing: it is an offline re-decision over evidence already paid for in 48-03, with no research call, no provider call and no HubSpot write anywhere in this plan (D-01)."
    - "Every one of the 9 lv_org_type options carries a semantic definition in config/taxonomy.yaml, and both research prompts in src/web_research.py render that definition set from the taxonomy rather than restating it, so a definition cannot exist in one place and be missing from the other."
    - "The regulator and governing_body_league definitions state the commercial-control discriminator and name QRIC and Racing NSW as the two anchor cases, so statutory origin can no longer be mistaken for the test."
    - "A regulator classification arriving alongside evidence of broadcast output and/or sponsorship reliance is flagged as incoherent and routed to review or refusal; it is never silently rewritten to another value, because an automatic flip is its own guess (D-03, AT-2 precedent)."
    - "The docs/WEB-RESEARCH-SPEC.md §9 golden-set expectation for Racing NSW is an executable test that fails if the classification or the tier outcome regresses."
  prohibitions:
    - statement: "The captured research artifact 48-RESEARCH-RACING-NSW.json is verbatim evidence and is never edited, re-fetched or regenerated by this plan; the correction is an override layered over it."
      status: required
      verification: "git diff --exit-code -- .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json exits 0 at the end of every task"
    - statement: "The coherence guard never rewrites an incoherent org_type to a different enum value; it only flags, refuses, or routes to the D-03 marker."
      status: required
      verification: "A test feeds the verbatim captured artifact to resolve_racing_nsw_decision and asserts the returned org_type is the unknown marker with a non-empty reason — specifically NOT governing_body_league, which only the authored override table supplies"
    - statement: "The driver never issues a PATCH containing any of the four derived scoring fields owned by the n8n Decide Company Action node (project D-07)."
      status: required
      verification: "grep -vE '^\\s*(#|\")' scripts/enrich_coverage_companies.py | grep -cE '\"lv_anti_icp_(flag|reason)\"|\"lv_icp_(fit_score|tier)\"' reads 0"
    - statement: "No n8n workflow JSON is rebuilt, hand-edited, deployed or bounced, and no arming surface is opened; this plan is entirely offline."
      status: required
      verification: "git status shows no change under n8n/, and no invocation of scripts/build_cloud_workflows.py, scripts/deploy_n8n_workflows.py or scripts/june_run_arm.py appears in any task"
    - statement: "No free-text-to-enum keyword or regex mapper is introduced; the corrected value comes from the authored literal table, as every other entry does (D-01)."
      status: required
      verification: "The Racing NSW entry remains a literal dict value in ORG_TYPE_DECISIONS; no substring scan, regex or .lower() keyword match over research free text derives it"
  artifacts:
    - config/taxonomy.yaml
    - src/taxonomy.py
    - src/web_research.py
    - scripts/enrich_coverage_companies.py
    - .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md
  key_links:
    - "config/taxonomy.yaml is the single source of truth; the definitions block reaches both prompts through src/taxonomy, never by being retyped into src/web_research.py — two copies of a discriminator are two things that drift."
    - "gen_taxonomy_js.render() reads only synonyms, requires_evidence, implies_content and the key list — it does NOT read the new definition key, so n8n/code/taxonomy.generated.js stays byte-identical and the currency test stays green with no regeneration and no rebuild."
    - "The coherence check is one pure function in src/taxonomy.py with two call sites (validate_research_output and the driver's refusal path), not two implementations."
    - "The corrected value and the guard compose: the guard refuses to promote the captured regulator answer, and the authored override table — not the guard — supplies governing_body_league."
---

<objective>
Correct Racing NSW `15008671672` from `regulator` to `governing_body_league` offline over
already-paid evidence, put the discriminator that would have prevented the misclassification
into the taxonomy where both research prompts read it, add a coherence guard that makes the
contradiction machine-visible without guessing a replacement value, and turn the
already-written `docs/WEB-RESEARCH-SPEC.md` §9 golden-set expectation into an executable test.

Purpose: `governing_body_league` scores +40 and `regulator` scores −20, so this is a Tier A
versus Tier C decision on a live prospect account. 48-05's armed write window must not spend
its one window writing the stale value — which is why 48-05 now depends on this plan.

This plan is inserted mid-execution into an already-running phase. Its `wave: 3` matches
48-03 (already complete, so there is no parallelism hazard); ordering is enforced by the
`depends_on` edge and by 48-05's amended `depends_on`, not by the wave number.

Output: a corrected decision table entry carrying its own override record, 9 org-type
definitions in `config/taxonomy.yaml`, both prompts rendering them, a coherence guard with
tests both ways, and a golden-set regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-PATTERNS.md
@.planning/phases/48-enrichment-coverage/48-03-SUMMARY.md
@.planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json
</context>

## Artifacts this phase produces

New symbols and paths introduced by THIS plan (the phase-wide list lives in 48-01-PLAN.md):

- `RESEARCH_PATH_OVERRIDES` — per-id captured-research path map in `scripts/enrich_coverage_companies.py`
- `override_of` / `override_rationale` — the two new `ORG_TYPE_DECISIONS` entry keys recording an operator-reviewed divergence from a returned research value
- `definition:` — new per-value key on every `org_types` entry in `config/taxonomy.yaml`
- `ORG_TYPE_DEFINITIONS`, `org_type_definitions_block()`, `org_type_coherence_flags()`,
  `INCOHERENT_WITH_REGULATOR` — new in `src/taxonomy.py`
- `coherence_flags` — new additive key on `validate_research_output()`'s return dict
- `TX-10`, `AT-4` — new requirement ids in `docs/WEB-RESEARCH-SPEC.md`
- Test names: `test_racing_nsw_decision_is_governing_body_league_overriding_the_returned_value`,
  `test_racing_nsw_captured_artifact_is_unedited_and_the_override_is_recorded`,
  `test_tx10_every_org_type_has_a_definition_and_both_prompts_render_them`,
  `test_guard_captured_racing_nsw_artifact_trips_the_coherence_flags`,
  `test_guard_never_flips_an_incoherent_regulator_to_another_value`,
  `test_guard_qric_shaped_regulator_is_coherent_and_unflagged`,
  `test_at4_golden_racing_nsw_governing_body_league_scores_tier_a_or_b`
- `.planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md`

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | This plan assumes COVER-01's bar is about *which* value a covered record receives as much as *whether* it receives one, so correcting a wrong classification before the write window is in scope. It does NOT claim COVER-01 closure: Phase 47 covers 17 records, Phase 48 the rest, and neither closes the requirement alone (REQUIREMENTS.md, D-02 split). |
| COVER-02 | `unclassified` → **unresolved** | This plan assumes it consumes zero budget on every axis and therefore does not change the ex-ante estimate 48-03 committed: no research call, no provider credit, no n8n execution, no HubSpot write. If a reviewer reads COVER-02 as requiring every plan to restate an estimate, that is not what this plan does — it states a zero and leaves `48-COST-ESTIMATE.md` untouched for 48-06 to reconcile. |

<tasks>

<task type="tracer">
  <name>Task 1: Correct Racing NSW end-to-end — override the returned value, offline, zero spend</name>
  <files>scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py</files>
  <read_first>
    - scripts/enrich_coverage_companies.py (`ORG_TYPE_DECISIONS` :76-125 — the Racing NSW entry at :85-94; `_load_captured_research` :299; `decide_org_type` :306; `main` :426-476)
    - .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json (the verbatim captured evidence — read it, never edit it)
    - tests/test_enrich_coverage_companies.py (:69-77 `test_tracer_racing_nsw_decision_no_longer_pending_after_task_3`, :180-184 `test_marker_racing_nsw_is_resolved_not_pending`, :273-277 `test_racing_nsw_captured_artifact_exists_and_is_the_fifth_decision`)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (D-01 — the offline remap over already-paid evidence; the "one paid call, already made" boundary)
    - config/icp_scoring.yaml (`base_score.org_type` — the +40 vs −20 the correction turns on)
  </read_first>
  <action>
    Two changes to `scripts/enrich_coverage_companies.py`, both offline. Make no research call,
    no provider call, no HubSpot call: this is D-01's pattern, an offline re-decision over
    evidence the phase already paid for. The phase declared exactly one paid call and it has
    been made — there is no second call in this plan under any circumstance.

    FIRST, fix the captured-research loader, which is a live blocker for 48-05. Racing NSW is
    not one of the 17 keys in `47-RESEARCH-RESULTS.json`, so `_load_captured_research` returns
    None for it and `decide_org_type` raises an uncaught ValueError. Confirmed: the full five-id
    dry-run today exits 1 with a traceback, which means 48-05 Task 1 cannot produce the five
    payloads it is required to record. Add `RESEARCH_PATH_OVERRIDES`, a literal per-id map
    pointing `15008671672` at `.planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json`,
    and teach the loader that an override file IS the research dict itself rather than a
    dict keyed by company id (the two artifacts have different shapes — say so in the comment).
    Fix it in the loader, not at the call site: every caller of the loader routes through one
    guard rather than each growing its own special case.

    SECOND, replace the Racing NSW entry in `ORG_TYPE_DECISIONS`. It becomes
    `org_type: "governing_body_league"`, and it gains two new keys that make the divergence
    from the returned value data rather than buried prose:
      `override_of: "regulator"` — the value the captured call actually returned
      `override_rationale: <the text below>`
    Keep `basis` pointing at the same captured artifact and its confidence, as every other
    entry does.

    The rationale text, stated so the executor does not have to reconstruct it. Operator
    review on 2026-08-13 rejected the returned `regulator`. The discriminator is COMMERCIAL
    CONTROL of the sport, not statutory origin — statutory origin is useless as a test because
    both a pure regulator and a governing body can be creatures of an Act. The test: does a
    DIFFERENT body hold the commercial functions (race programme and calendar, prizemoney,
    media rights, sponsorship)? If yes, this org is a pure regulator. If this org holds them
    itself, it is a governing body, even where it also carries statutory regulatory powers.
    QRIC is the pure-regulator anchor — integrity, licensing and stewards only, with Racing
    Queensland holding the commercial functions after Queensland split them out in 2016.
    Racing NSW is the governing-body anchor: it sets the race calendar, distributes
    prizemoney, collects Race Fields fees, runs its own streaming and is Tabcorp-sponsored.
    Every one of those commercial facts is already stated in the captured artifact's own
    `evidence.evidence_summary` — cite it, do not re-fetch it. Two independent repo artifacts
    already agree: `config/taxonomy.yaml` lists "racing authority" and "controlling body"
    among `governing_body_league`'s synonyms, and `docs/WEB-RESEARCH-SPEC.md` §9 already names
    Racing NSW → `governing_body_league` + Tier A/B as a golden-set acceptance case.

    Then bring the two stale tests in `tests/test_enrich_coverage_companies.py` up to date.
    `test_tracer_racing_nsw_decision_no_longer_pending_after_task_3` asserts the literal
    `regulator` — flip it to the corrected value and to the recorded override.
    `test_racing_nsw_captured_artifact_exists_and_is_the_fifth_decision` asserts the artifact's
    value EQUALS the decision's value, which is now deliberately false — rewrite it as the
    divergence-is-recorded assertion described in the acceptance criteria. Leave
    `test_marker_racing_nsw_is_resolved_not_pending` alone; it asserts membership in the valid
    enum set and stays green.

    Do not edit `48-RESEARCH-RACING-NSW.json`. It is verbatim evidence; the whole correction is
    an override layered over it, and an edited artifact would destroy the only record of what
    the paid call actually said.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x</automated>
    <automated>DRY_RUN=true .venv/bin/python scripts/enrich_coverage_companies.py --dry-run</automated>
    <automated>git diff --exit-code -- .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json</automated>
  </verify>
  <acceptance_criteria>
    - `DRY_RUN=true .venv/bin/python scripts/enrich_coverage_companies.py --dry-run` (all five ids, no credentials) exits 0 and prints a Racing NSW patch whose org-type value is the literal string `governing_body_league`. This is a genuine red-to-green: the same command exits 1 with a ValueError traceback before this task.
    - A test asserts `ORG_TYPE_DECISIONS["15008671672"]["org_type"] == "governing_body_league"`, `["override_of"] == "regulator"`, and that `override_rationale` is non-empty and mentions both anchor organisations by name.
    - The rewritten artifact test asserts the captured JSON still reads `regulator` for its org type AND that the decision entry differs from it AND that `override_of` equals the artifact's value — the divergence is recorded, not accidental.
    - A test asserts `_load_captured_research("15008671672")` returns a dict carrying `matched` and `confidence` (proving the override path resolves the 48-shaped artifact), while `_load_captured_research("17317850381")` still resolves through the 47-keyed file.
    - `git diff --exit-code -- .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json` exits 0.
    - `grep -vE '^\s*(#|")' scripts/enrich_coverage_companies.py | grep -cE '"lv_anti_icp_(flag|reason)"|"lv_icp_(fit_score|tier)"'` reads 0.
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0, and no network call was made by any test in the module.
  </acceptance_criteria>
  <reversibility rating="reversible">The correction is a literal table value plus two recorded metadata keys; reverting is a one-line edit, and nothing has been written to HubSpot at this point. The loader fix is additive.</reversibility>
  <done>Racing NSW resolves to `governing_body_league` with its divergence from the returned value recorded as data, the five-id dry-run runs clean end to end, and the paid evidence file is byte-identical to what 48-03 captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the vocabulary where the model actually sees it — taxonomy definitions, rendered into both prompts</name>
  <files>config/taxonomy.yaml, src/taxonomy.py, src/web_research.py, tests/test_taxonomy_conformance.py, docs/WEB-RESEARCH-SPEC.md, .planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md</files>
  <read_first>
    - config/taxonomy.yaml (the header's derived-representation list, and all 9 `org_types` entries)
    - src/taxonomy.py (:20-40 module-level loads, `EVIDENCE_GATED_ORG_TYPES` :37 — the pattern a derived constant follows)
    - src/web_research.py (`RESEARCH_SYSTEM` :31-54, `RACING_NSW_ORG_TYPE_SYSTEM` :63-92)
    - scripts/gen_taxonomy_js.py (`render()` :50-98 — confirm for yourself which keys it reads)
    - tests/test_judge_spec.py (:68-105 `_block` and `test_prompt_parity_vendor_flags` — the source-text slice this task must not break)
    - tests/test_taxonomy_conformance.py (the TX-numbered test style to extend)
    - docs/WEB-RESEARCH-SPEC.md §2 (TX-1..TX-9 and "Adding a value later")
  </read_first>
  <behavior>
    - `src.taxonomy.ORG_TYPE_DEFINITIONS` has one non-empty entry for each of the 9 org types.
    - `org_type_definitions_block()` returns a deterministic single string listing every option with its definition, in `config/taxonomy.yaml` key order.
    - `RESEARCH_SYSTEM` and `RACING_NSW_ORG_TYPE_SYSTEM` both contain the rendered block.
    - The `regulator` definition names QRIC; the `governing_body_league` definition names Racing NSW; both state the commercial-control test.
    - `gen_taxonomy_js.render()` output is unchanged by the taxonomy edit, so `n8n/code/taxonomy.generated.js` needs no regeneration.
    - Every existing taxonomy conformance test (TX-1..TX-9) and `test_prompt_parity_vendor_flags` stay green.
  </behavior>
  <action>
    The root cause of the misclassification is that both prompts enumerate the 9 enum VALUES
    and define none of them, so the taxonomy's semantic content never reaches the model and it
    keys on whatever the source text emphasises — here, statutory origin. Fix it at the single
    source of truth, not in a prompt string.

    Add a `definition:` key to ALL NINE `org_types` entries in `config/taxonomy.yaml` — one
    line each. All nine, not just the two in dispute: the defect is an absent definition set,
    and a half-defined vocabulary invites the model to treat the defined pair as special.
    The two load-bearing ones, written out so they are not paraphrased away:

      governing_body_league — holds commercial control of a sport or competition: sets the
      calendar or race programme, distributes prizemoney, and holds media rights and
      sponsorship, whether or not it ALSO carries statutory regulatory powers. Anchor example:
      Racing NSW, a statutory body that nonetheless programmes racing, distributes prizemoney,
      collects Race Fields fees and runs its own streaming.

      regulator — integrity, licensing and stewarding ONLY, where a DIFFERENT body holds the
      commercial functions (calendar, prizemoney, media rights, sponsorship). Anchor example:
      QRIC, where Racing Queensland holds the commercial functions. Statutory origin is NOT
      the test — both a pure regulator and a governing body can be created by an Act.

    The other seven get a plain one-line definition in the same voice; they are not in dispute,
    so keep them short.

    In `src/taxonomy.py`, derive `ORG_TYPE_DEFINITIONS` from `ORG_TYPES` the same way
    `EVIDENCE_GATED_ORG_TYPES` is derived, and add `org_type_definitions_block()` returning one
    deterministic string. In `src/web_research.py`, have both prompt constants render that
    block instead of restating any of it — a discriminator that exists in two hand-maintained
    copies is two things that drift, which is the same reasoning the repo already applies to
    its two scoring engines. Preserve three source-text facts while doing it: the literal
    `RESEARCH_SYSTEM = (` opening marker, the `"lv_org_type":<str>` schema fragment, and the
    two hard-veto field names — a source-slicing parity test and an existing 48-03 test read
    them out of the file text. Do not enum-constrain `RESEARCH_SYSTEM`; that prompt is
    parity-tracked against the production lane and constraining it is not this plan's job.

    Importing `src.taxonomy` into `src.web_research` makes the module CWD-sensitive, because
    the taxonomy loads by relative path — the same convention `src/icp_scoring.py` already
    uses. Run every verify command from the repo root.

    Add TX-10 to `tests/test_taxonomy_conformance.py`: every org type has a non-empty
    definition, both prompts contain the rendered block, and the regulator and
    governing_body_league definitions each name their anchor organisation. Record TX-10 in
    `docs/WEB-RESEARCH-SPEC.md` §2 as a dated addition, in the house dated-amendment style.

    `gen_taxonomy_js.render()` reads only the key list, synonyms, `requires_evidence` and
    `implies_content` — it does not read `definition`, so the generated JS is byte-identical,
    the currency test stays green, and NOTHING needs regenerating. Do not run the workflow
    builder and do not touch anything under `n8n/`: a rebuild here would create deploy-diff
    noise against an instance whose one declared deploy belongs to 48-04.

    That leaves the live n8n research prompt still definition-free — it builds its
    `allowed_org_types` list from the generated taxonomy but has no definitions to inject.
    Injecting them there is blocked three ways: the frozen jsCode fixture is re-baselined only
    by an explicit reviewed act, deploys are operator-only, and D-06 declares exactly one
    deploy which 48-04 owns. Do not do it here and do not leave it implicit — write
    `.planning/todos/pending/2026-08-13-n8n-research-prompt-lacks-org-type-definitions.md` in
    the house todo format, naming the builder function that would carry the injection, the
    frozen-fixture rebaseline it requires, and the fact that the Python and production prompts
    are knowingly divergent until it lands. Note the same divergence in the spec §2 amendment.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_taxonomy_conformance.py tests/test_judge_spec.py tests/test_web_research_spec.py -x</automated>
    <automated>.venv/bin/python -c "from src.taxonomy import ORG_TYPE_DEFINITIONS, org_type_definitions_block; assert len(ORG_TYPE_DEFINITIONS)==9 and all(v.strip() for v in ORG_TYPE_DEFINITIONS.values()); b=org_type_definitions_block(); assert 'QRIC' in b and 'Racing NSW' in b; from src.web_research import RESEARCH_SYSTEM, RACING_NSW_ORG_TYPE_SYSTEM; assert b in RESEARCH_SYSTEM and b in RACING_NSW_ORG_TYPE_SYSTEM; print('definitions wired into both prompts')"</automated>
    <automated>git diff --exit-code -- n8n/</automated>
  </verify>
  <acceptance_criteria>
    - All 9 `org_types` entries in `config/taxonomy.yaml` carry a non-empty `definition`, asserted by TX-10 rather than by inspection.
    - TX-10 asserts the rendered definitions block appears in BOTH `RESEARCH_SYSTEM` and `RACING_NSW_ORG_TYPE_SYSTEM`, and that the regulator definition names QRIC while the governing_body_league definition names Racing NSW.
    - `.venv/bin/python -c "import gen_taxonomy_js"`-equivalent currency check passes unchanged: `tests/test_taxonomy_conformance.py::test_taxonomy_generated_js_currency` is green with no regeneration, and `git diff --exit-code -- n8n/` exits 0.
    - `tests/test_judge_spec.py::test_prompt_parity_vendor_flags` and `tests/test_enrich_coverage_companies.py::test_racing_nsw_prompt_lists_all_9_options_and_the_unknown_instruction` both stay green — the source-text slice and the schema fragment survived.
    - The todo file exists, names the builder function that would carry the n8n-side injection and the frozen-fixture rebaseline it requires, and states that the Python and production prompts are knowingly divergent until then.
    - `docs/WEB-RESEARCH-SPEC.md` §2 carries a dated block recording TX-10 and the known divergence.
    - `.venv/bin/python -m pytest` (full suite) exits 0, excluding `run_scoring_parity.py`'s population sweep which is red by design until Phase 49.
  </acceptance_criteria>
  <reversibility rating="reversible">Definitions are additive YAML plus a derived constant; removing them restores the previous prompt strings exactly. No generated artifact, no workflow and no deployed instance is touched.</reversibility>
  <done>The commercial-control discriminator lives in the taxonomy, both Python prompts render it from there, a conformance test enforces the set, the generated JS is untouched, and the un-fixed production lane is captured as a named todo rather than forgotten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard the contradiction and make the golden case executable</name>
  <files>src/taxonomy.py, scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py, tests/test_web_research_spec.py, docs/WEB-RESEARCH-SPEC.md</files>
  <read_first>
    - src/taxonomy.py (`validate_research_output` :114-160 — where AT-2's off-vocabulary rule already lives and what its return shape is)
    - scripts/enrich_coverage_companies.py (`resolve_racing_nsw_decision` :165-217 — the three existing D-03 refusal conditions this task extends to four)
    - tests/test_web_research_spec.py (:160-220 — the `score()` helper style and the existing §9 golden-set section)
    - src/icp_scoring.py (:55-140 — org points, content points, geography key, tier thresholds)
    - docs/WEB-RESEARCH-SPEC.md §9 (the golden-set table and AT-1..AT-3)
    - .planning/phases/48-enrichment-coverage/48-CONTEXT.md (D-03 — the marker path an incoherent classification routes to)
  </read_first>
  <behavior>
    - `org_type_coherence_flags({"lv_org_type": "regulator", "lv_produces_content": True})` returns a non-empty list naming the contradicting signal.
    - The same for `lv_sponsorship_reliant: True`, and both flags together when both are set.
    - A QRIC-shaped regulator (`lv_produces_content` null or False, `lv_sponsorship_reliant` null) returns an empty list — the guard does not fire on an honest regulator.
    - A non-regulator org type with either signal returns an empty list — the guard is scoped to the one contradiction it understands.
    - The verbatim captured `48-RESEARCH-RACING-NSW.json` returns a non-empty list.
    - `validate_research_output` gains a `coherence_flags` key and sets `needs_review` True when it is non-empty, leaving `data["lv_org_type"]` exactly as normalization left it.
    - `resolve_racing_nsw_decision` on the verbatim captured artifact returns the D-03 marker with a non-empty reason — not `regulator`, and NOT `governing_body_league`.
    - `compute_icp_score` with `governing_body_league` + content true + AU yields a tier in {A, B} and no anti-ICP flag; the same signals with `regulator` do not.
  </behavior>
  <action>
    A `regulator` classification means integrity and licensing only, with the commercial
    functions held elsewhere. A body with no commercial control has no broadcast product to
    sell and no sponsorship inventory — so `regulator` arriving beside evidence of content
    output and sponsorship reliance is not a low-confidence answer, it is an internally
    inconsistent one. The captured Racing NSW artifact carries exactly that shape: a regulator
    classification alongside produces-content true, sponsorship-reliant true and
    sports-media-fit true. That incoherence is mechanically checkable, and nothing checked it.

    Add `org_type_coherence_flags(data)` to `src/taxonomy.py` as a pure function returning a
    list of human-readable reason strings, empty when coherent. Drive it from a module-level
    `INCOHERENT_WITH_REGULATOR` tuple naming the two signals rather than open-coding them, and
    normalize the incoming org type through the existing normalizer so a synonym form is
    caught too. It reads; it never mutates.

    Wire it at both call sites — one implementation, two callers, per the repo's own
    fix-once-where-all-callers-route-through rule:
      `validate_research_output` gains an additive `coherence_flags` key and ORs the flags into
      its existing `needs_review`. Additive only: every existing key keeps its meaning and
      every existing test in `tests/test_web_research_spec.py` must stay green.
      `resolve_racing_nsw_decision` gains a FOURTH refusal condition alongside its three
      existing ones (out-of-vocabulary value, bare unknown, missing evidence URL): flags
      present means refuse to promote, return the D-03 `unknown` marker with a reason naming
      the contradicting signals.

    The guard does NOT rewrite an incoherent value to a different enum option. Flipping
    `regulator` to `governing_body_league` automatically would be the system making the same
    class of guess that produced the error, and the repo's prefer-unknown-over-guessing rule
    and AT-2's precedent both say a value the pipeline cannot justify becomes the marker, not a
    substitute. The corrected Racing NSW value comes from Task 1's authored override table and
    from nowhere else — assert that separation directly, per the acceptance criteria below.

    Then make the golden case executable. `docs/WEB-RESEARCH-SPEC.md` §9 has asserted since it
    was written that Racing NSW maps to `governing_body_league` with content true and lands
    Tier A/B — and nothing ran it, which is why a regulator classification with a Tier C-shaped
    score passed through unremarked. Add AT-4 to §9 and its test to the existing §9 section of
    `tests/test_web_research_spec.py`, using `compute_icp_score` against explicit synthetic
    signals rather than any live read, per AT-3. The arithmetic, so a wrong assertion is
    obvious: `governing_body_league` 40 + content 20 + AU geography 10 = 70, which is exactly
    the Tier A threshold. Include the `regulator` negative control in the same test: −20 + 20 +
    10 = 10, below the Tier C floor, so it scores Unscored — a test that passes for both values
    is not testing anything. Pin the decision table itself in the same test so a future edit to
    `ORG_TYPE_DECISIONS` cannot silently re-break the golden case.

    Nothing in this task writes to HubSpot, calls a provider, calls Anthropic, or touches the
    n8n lane.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_web_research_spec.py tests/test_enrich_coverage_companies.py -x</automated>
    <automated>.venv/bin/python -m pytest</automated>
    <automated>node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - A test loads `48-RESEARCH-RACING-NSW.json` verbatim, passes it to `org_type_coherence_flags`, and asserts the returned list is non-empty and names both the content and sponsorship signals — the contradiction that shipped is now mechanically detected.
    - A test passes the same verbatim artifact to `resolve_racing_nsw_decision` and asserts the returned org type is `unknown` with a non-empty reason, and explicitly asserts it is NOT `governing_body_league` — proving the guard refuses rather than flips, and that the corrected value comes only from the authored table.
    - A QRIC-shaped negative control (regulator, content null, sponsorship null) asserts an empty flag list, and a non-regulator org type carrying both signals also asserts empty.
    - A test asserts `validate_research_output` on an incoherent payload returns `needs_review` True with non-empty `coherence_flags`, and that `data["lv_org_type"]` is still `regulator` — flagged, not rewritten.
    - AT-4's test asserts `compute_icp_score` with `governing_body_league`, content true and AU gives a tier in {"A","B"} with the anti-ICP flag false, asserts the `regulator` negative control's tier is NOT in {"A","B"}, and asserts `ORG_TYPE_DECISIONS["15008671672"]["org_type"] == "governing_body_league"`.
    - `docs/WEB-RESEARCH-SPEC.md` §9 lists AT-4 and points at the test that now enforces the Racing NSW golden row.
    - `.venv/bin/python -m pytest` exits 0 (the population sweep in `run_scoring_parity.py` is red by design until Phase 49 and is not part of this bar) and `node --test tests/n8n/*.test.mjs` exits 0.
    - `git diff --exit-code -- .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json` and `git diff --exit-code -- n8n/` both exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">The guard is an additive pure function plus one additive return key and one additional refusal branch; removing it restores the prior behaviour exactly. It changes no scoring predicate, so the two-engines-one-commit parity rule is not triggered — see the threat-model note.</reversibility>
  <done>The contradiction that produced the wrong classification is detected by a test against the real captured artifact, the guard surfaces it without guessing a replacement, and the golden-set expectation that should have caught it is executable and discriminating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured research artifact → decision table | Model output already on disk crosses into a value 48-05 will write to the CRM |
| config/taxonomy.yaml → both research prompts | The vocabulary's semantics cross into what the model is told; a definition present in one prompt and absent from the other is a silent divergence |
| operator judgement → ORG_TYPE_DECISIONS | A human override of a machine classification enters the write path and must remain attributable |
| src/taxonomy.py → every Python research consumer | An additive return key changes a shared contract used by the smoke script and the spec suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-19 | Repudiation | the corrected Racing NSW entry | high | mitigate | The divergence is recorded as data (`override_of` + `override_rationale`) and asserted by test; the captured artifact stays byte-identical under `git diff --exit-code`, so what the paid call actually said remains recoverable |
| T-48-20 | Tampering | evidence rewritten to match the conclusion | high | mitigate | Prohibition row plus a `git diff --exit-code` acceptance criterion on the artifact in every task; the correction may only ever be an override layered over verbatim evidence |
| T-48-21 | Spoofing | a guard that "fixes" a classification by guessing a replacement | high | mitigate | The guard only flags/refuses; a test asserts the refusal path returns the marker and specifically not the corrected value, so the corrected value provably comes from the authored table alone |
| T-48-22 | Tampering | an unintended rebuild or deploy of the enrichment lane | high | mitigate | No builder, deploy or arming script is invoked; `git diff --exit-code -- n8n/` is an acceptance criterion; the generated taxonomy JS is provably unaffected because the generator does not read the new key |
| T-48-23 | Denial of Service | a shared-contract change breaking other research consumers | medium | mitigate | `coherence_flags` is additive-only and the full suite green is the acceptance bar, covering the spec suite and the smoke script's consumer |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages in any ecosystem this phase (48-RESEARCH.md § Package Legitimacy Audit); no install task exists, so the legitimacy gate has nothing to audit |

**Parity-rule note (Phase 46):** nothing in this plan changes a scoring predicate. The
taxonomy gains a documentation key the scorer never reads, and the coherence guard is
research-validation control flow, not a veto or a base-score rule. The two engines
(`src/icp_scoring.py` and the builder's `Decide Company Action` node) are untouched and remain
byte-identical to each other, so the two-engines-one-commit rule is not triggered.
</threat_model>

<verification>
- `.venv/bin/python -m pytest` exits 0 (`run_scoring_parity.py`'s population sweep is RED BY DESIGN until Phase 49 and is NOT part of this bar)
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form; the directory form is broken on node 24)
- `DRY_RUN=true .venv/bin/python scripts/enrich_coverage_companies.py --dry-run` exits 0 for all five ids and prints `governing_body_league` for Racing NSW
- `git diff --exit-code -- .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json` exits 0
- `git diff --exit-code -- n8n/` exits 0
- Zero HubSpot writes, zero provider credits, zero Anthropic calls, zero n8n executions attributable to this plan
</verification>

<success_criteria>
- Racing NSW resolves to `governing_body_league` with its override of the returned `regulator` recorded as data
- The commercial-control discriminator lives in `config/taxonomy.yaml` and reaches both Python prompts from there
- An incoherent `regulator` classification is flagged and refused, never auto-flipped
- The §9 golden-set expectation for Racing NSW is executable and discriminating
- The phase's budget is unchanged: this plan spends nothing on any axis
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-07-SUMMARY.md` when done, recording the
corrected value and its override record, the definitions added, the guard's disposition and
why it refuses rather than flips, the golden-test arithmetic actually observed, and an
explicit statement that no research call, provider call, HubSpot write, rebuild, deploy or
arming action occurred.
</output>
