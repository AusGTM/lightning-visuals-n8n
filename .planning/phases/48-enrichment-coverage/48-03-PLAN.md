---
phase: 48-enrichment-coverage
plan: 03
type: execute
wave: 3
depends_on: ["48-01"]
files_modified:
  - .planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md
  - .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json
  - src/web_research.py
  - scripts/enrich_coverage_companies.py
  - tests/test_enrich_coverage_companies.py
autonomous: false
requirements: [COVER-01, COVER-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The cost estimate exists BEFORE any paid call is made, is produced by a live call to estimate_phase48_cost() rather than hand-derived prose, and states the projection against both the 2,500/month n8n allowance and the current Lusha balance (COVER-02)."
    - "The operator sees the estimate and the D-06 window declaration and approves the spend as a separable decision before the one paid research call is made."
    - "Racing NSW's fresh research is constrained to the 9 live lv_org_type options at the prompt, so its output is mappable without guessing (D-01)."
    - "An out-of-vocabulary or unevidenced Racing NSW result falls back to the D-03 un-enrichable marker rather than being force-fitted to an enum value."
  prohibitions:
    - statement: "The shared, parity-tracked production research prompt is not edited; the enum constraint lives in a separate one-off prompt used only by this call."
      status: required
      verification: "git diff src/web_research.py shows an added module-level constant and no modification to the existing RESEARCH_SYSTEM string body"
    - statement: "No provider-waterfall call is made; zero Lusha, Apollo or ZoomInfo credits are drawn (D-01)."
      status: required
      verification: "The Lusha balance read before and after this plan is unchanged, recorded in 48-COST-ESTIMATE.md and the run report"
    - statement: "The estimate document's figures are not typed by hand; they are the captured stdout of a live function call."
      status: required
      verification: "48-COST-ESTIMATE.md embeds the exact invocation and its verbatim output, following 47-COST-ESTIMATE.md's structure"
  artifacts:
    - .planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md
    - .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json
  key_links:
    - "refuse_if_over_budget() is called against the Phase-48-shaped dict from estimate_phase48_cost(), not against estimate_cost()'s Phase-47-shaped output — the latter charges one n8n execution per id on an execution model that does not hold here."
    - "The Racing NSW decision, once resolved, is written into ORG_TYPE_DECISIONS so plan 05's write leg reads it from the same literal table as the other four."
---

<objective>
Satisfy COVER-02's ex-ante half and buy the phase's only paid work: produce the cost estimate
from code, put it in front of the operator as a separable spend decision alongside the D-06
window declaration, then make one enum-constrained research call for Racing NSW `15008671672`
and fold its answer into the decision table.

Purpose: the ROADMAP frames this phase's spend as "a separable spend decision the operator
approves on its own terms". This plan is that decision point, and it precedes every paid or
armed action in the phase.

Output: `48-COST-ESTIMATE.md`, `48-RESEARCH-RACING-NSW.json`, and a fifth entry in
`ORG_TYPE_DECISIONS`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-enrichment-coverage/48-CONTEXT.md
@.planning/phases/48-enrichment-coverage/48-PATTERNS.md
@.planning/phases/47-veto-remediation/47-COST-ESTIMATE.md
</context>

## Artifacts this phase produces

New symbols introduced by this plan: `RACING_NSW_ORG_TYPE_SYSTEM` (or an equivalently named
one-off system prompt constant in `src/web_research.py`), the fifth `ORG_TYPE_DECISIONS` entry,
and the two documents named in `files_modified`. Full phase-wide list in 48-01-PLAN.md.

## Flagged assumptions (spec-less edge probe, both rows UNRESOLVED — not dropped)

| requirement | probe status | assumption being carried |
|---|---|---|
| COVER-01 | `unclassified` → **unresolved** | This plan assumes one enum-constrained research call is sufficient attempt for Racing NSW, and that a result the model cannot evidence is honestly recorded as `unknown` + reason rather than retried until it produces a value. |
| COVER-02 | `unclassified` → **unresolved** | This plan assumes the operator checkpoint plus a code-produced estimate document satisfies "estimated before the run", and that the Lusha balance line is satisfied by reading the live balance even though zero credits will be drawn. |
| A1 (48-RESEARCH.md) | assumption, not probe | The Anthropic account has credit for the one call. No code in this repo pre-checks Anthropic balance (Open Question 2); the pre-call check is the operator's, and a credit failure now surfaces as a hard failure of this task rather than as garbage data. |

<tasks>

<task type="auto">
  <name>Task 1: Produce 48-COST-ESTIMATE.md from a live estimate_phase48_cost() call</name>
  <files>.planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md, scripts/enrich_coverage_companies.py, tests/test_enrich_coverage_companies.py</files>
  <read_first>
    - .planning/phases/47-veto-remediation/47-COST-ESTIMATE.md (the document structure to copy almost verbatim — this is the shape COVER-02 wants)
    - scripts/enrich_coverage_companies.py (`estimate_phase48_cost` from plan 01)
    - scripts/remediate_veto_companies.py (`refuse_if_over_budget` :674, `N8N_EXECUTION_BUDGET_MONTH` :167, `ANTHROPIC_PER_RECORD_ESTIMATE_USD` :647)
    - scripts/check_provider_credits.py (`_extract_lusha` :67-75 reads `credits.remaining`; ZoomInfo needs the JSON:API accept header or 406s; this account's Apollo key is non-master and 403s, degrading to null)
    - .planning/phases/48-enrichment-coverage/48-POPULATION.json (plan 01's live re-derivation)
  </read_first>
  <action>
    Write `.planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md` following
    `47-COST-ESTIMATE.md`'s structure, with every figure captured from a live call rather than
    typed. Embed the exact invocation and its verbatim stdout, the way the Phase 47 document
    does, so the document and the function can never silently drift.

    The projection to capture, for `research_ids=["15008671672"]`, `written_ids=` the ids plan
    01's reconciliation accepted (expected 5), and `proof_executions=1`:
    `web_research_calls: 1`, `n8n_executions: 6`, `n8n_budget_month: 2500`,
    `lusha_credits: 0`, `anthropic_estimate_usd: 0.0686`. The n8n executions are the five D-09
    recompute POSTs plus the one disarmed proof-of-deploy execution plan 48-04 spends to prove
    the running instance reloaded — budgeted here rather than discovered afterwards. Racing
    NSW's research is a direct Anthropic call through `src/web_research.py` and costs zero n8n
    executions, and the four already-researched records cost zero research calls because their
    evidence was paid for in Phase 47 and is on disk.

    Read the current Lusha balance live via `scripts/check_provider_credits.py` and record the
    number with the date and time it was read — COVER-02's wording is "against ... the current
    Lusha balance", which the zero-credit projection does not excuse. Record ZoomInfo and Apollo
    outcomes too, including their known degradations, so a null is legible as a known condition
    rather than a failure.

    The document must state the D-06 window declaration explicitly: 1 operator deploy and bounce,
    1 armed write window, record cap 5; and that exceeding the declaration is a disclosure
    obligation in the run report, not a silent event.

    Add a unit test asserting `refuse_if_over_budget` raises `BudgetRefused` when handed a
    synthetic estimate whose projected executions exceed the monthly budget, and returns the id
    list unmodified — same length, same order — when within budget. This is the code path
    COVER-02 requires to exist even though it will not fire at n=5.

    `.env` is Read/Bash permission-blocked: drive every live read through the dotenv form with an
    ABSOLUTE path, because a bare `load_dotenv()` resolves relative to the calling file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k budget -x</automated>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md && grep -c 'n8n_budget_month' .planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `48-COST-ESTIMATE.md` exists and contains the literal keys `web_research_calls`, `n8n_executions`, `n8n_budget_month`, `lusha_credits`, `anthropic_estimate_usd` inside a captured-output block, together with the exact command that produced them.
    - The captured `n8n_executions` figure is 6 (five recompute POSTs plus one disarmed deploy-proof execution), and the document explains that breakdown in one line.
    - The document records a Lusha `remaining` figure with the timestamp it was read, or an explicit named failure reason if the endpoint did not answer.
    - The document states the D-06 declaration verbatim: 1 deploy and bounce, 1 armed write window, record cap 5.
    - A test asserts `refuse_if_over_budget` raises on a synthetic over-budget estimate, and a second test asserts the returned id list is identical in length and order to the input when within budget.
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0.
  </acceptance_criteria>
  <done>The ex-ante estimate exists as a committed document whose figures are captured stdout, the Lusha balance is recorded live, and the refuse-whole path is proven by test.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator approves the separable spend and the window declaration</name>
  <decision>Approve Phase 48's spend and window plan, or send it back.</decision>
  <context>
    Read `.planning/phases/48-enrichment-coverage/48-COST-ESTIMATE.md`. The projection is one
    paid Anthropic research call (~$0.07 order-of-magnitude, against the measured $0.0686/record
    floor), five n8n executions against a 2,500/month allowance, and zero provider credits — the
    ROADMAP's literal "full provider waterfall per record" is rejected by D-01 because providers
    do not return `lv_org_type` at all and would spend Lusha and ZoomInfo credits on
    firmographics this phase does not need.

    Approving here authorises exactly three things, in this order: (1) one enum-constrained
    Anthropic research call for Racing NSW `15008671672`, made in the next task; (2) one deploy
    and bounce of the enrichment workflow, in plan 48-04; (3) one armed write window over at most
    5 records, in plan 48-05. Nothing else. Claude does not arm and does not deploy — both Phase
    47.5 waivers expired with that phase, and each of (2) and (3) has its own checkpoint asking
    you to run the command yourself.

    Before approving (1), confirm the Anthropic account has credit. No code in this repo checks
    it, and a credit failure is the failure mode most likely to poison a research-touching run:
    an n8n `status: "success"` can carry a 400 downstream as data. That specific hole is what
    plan 48-02 just closed at the lane; the standalone Python call this task makes will fail
    loudly instead.
  </context>
  <options>
    <option id="approve-as-estimated">
      <name>Approve as estimated</name>
      <pros>Unblocks the phase at the projected cost; the estimate is code-produced and the window count is declared up front, which is the correction Phase 47.5 made to Phase 47's five-window overrun</pros>
      <cons>Commits ~$0.07 and five n8n executions</cons>
    </option>
    <option id="skip-racing-nsw">
      <name>Approve the writes but skip the paid research call</name>
      <pros>Zero Anthropic spend; the four already-evidenced records still get their org type</pros>
      <cons>Racing NSW `15008671672` would have to be recorded `unknown` + reason without ever being attempted, which is precisely the state D-03's marker is meant to distinguish itself FROM — it would misrepresent an unattempted record as an attempted one</cons>
    </option>
    <option id="defer">
      <name>Defer the whole phase</name>
      <pros>Spends nothing now</pros>
      <cons>COVER-01/COVER-02 stay open; plan 48-02's gate stays built but undeployed</cons>
    </option>
  </options>
  <resume-signal>Select: approve-as-estimated, skip-racing-nsw, or defer. If approving, confirm Anthropic credit is available.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: One enum-constrained research call for Racing NSW, captured and mapped</name>
  <files>src/web_research.py, scripts/enrich_coverage_companies.py, .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json, tests/test_enrich_coverage_companies.py</files>
  <precondition>Task 2 returned `approve-as-estimated`, `ANTHROPIC_API_KEY` is present in the repo `.env`, and the operator has confirmed the account has credit.</precondition>
  <read_first>
    - src/web_research.py (read `RESEARCH_SYSTEM` at :31-54 — note its `"lv_org_type":<str>` open-string schema fragment, and the existing prefer-unknown-over-guessing sentence — and `claude_web_research()`'s call surface and return shape)
    - scripts/enrich_coverage_companies.py (`ORG_TYPE_DECISIONS`, `decide_org_type`, `build_coverage_patch` from plan 01)
    - .planning/phases/48-enrichment-coverage/48-PATTERNS.md (§ "src/web_research.py (possibly MODIFIED)")
    - scripts/remediate_veto_companies.py (`VALID_ORG_TYPES` :153 — the 9 options the output must land inside)
  </read_first>
  <action>
    Add a new module-level system-prompt constant to `src/web_research.py` — e.g.
    `RACING_NSW_ORG_TYPE_SYSTEM` — used only by this call. Leave the shared `RESEARCH_SYSTEM`
    untouched: it is kept in parity with the production n8n research prompt, and editing it would
    change the schema every future caller sees for a one-record need.

    The one-off prompt replaces the open-string org-type schema fragment with an explicit
    enumerated constraint over exactly the 9 live options — `governing_body_league`,
    `content_producer`, `broadcaster`, `individual_club_team`, `regulator`, `gambling_operator`,
    `hardware_vendor`, `other`, `unknown` — and adds a sentence instructing the model to answer
    `unknown` rather than invent a value outside that set, mirroring the prefer-unknown sentence
    already present for other fields. It must still require an evidence URL for the org-type
    classification.

    Make the call for Racing NSW `15008671672` with `USE_MOCK_WEB_RESEARCH=false`, driven through
    the dotenv form with an ABSOLUTE `.env` path. Capture the RAW response verbatim to
    `.planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json` before any parsing, so
    the evidence survives independently of how it is interpreted.

    Validate the captured output shape against what `claude_web_research()` actually returns
    today — it is a richer, `entity_resolution`-carrying shape than the flatter records in
    `47-RESEARCH-RESULTS.json`, so do not assume the older fixture's shape.

    Then fold the answer into `ORG_TYPE_DECISIONS` as the fifth entry, with the returned enum
    value and a `basis` citing the evidence URL. Three refusal conditions, each landing on the
    D-03 marker (`unknown` + a reason in `UNENRICHABLE_REASONS`) rather than a forced value:
    the returned value is not one of the 9 options; the model returned `unknown`; or the
    org-type classification carries no evidence URL.

    Note for the write plan, not to be acted on here: Racing NSW is a governing body for
    thoroughbred racing in NSW, so `governing_body_league` is the likely outcome — but the value
    written is whatever the call returns and validates, never a value assumed at plan time. If
    the call returns `hardware_vendor`, that record acquires a hard veto and lands Tier D by the
    OR predicate, which is correct and must be reported rather than suppressed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -k racing_nsw -x</automated>
    <automated>test -s .planning/phases/48-enrichment-coverage/48-RESEARCH-RACING-NSW.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/web_research.py` gains one new module-level prompt constant; `git diff src/web_research.py` shows no change inside the existing `RESEARCH_SYSTEM` string body.
    - The new prompt constant contains all 9 enum option literals and an instruction to answer `unknown` rather than invent a value outside the set.
    - `48-RESEARCH-RACING-NSW.json` exists, is non-empty, and is the raw captured response including its evidence URLs.
    - `ORG_TYPE_DECISIONS` has 5 entries, and `decide_org_type("15008671672", ...)` no longer raises `PendingResearch`.
    - A test asserts that a synthetic research response carrying an out-of-vocabulary org-type string routes to the D-03 marker with a non-empty reason, and never reaches `build_coverage_patch` with that string.
    - A test asserts that a synthetic response with a valid enum value but no evidence URL for the classification also routes to the D-03 marker.
    - `.venv/bin/python -m pytest tests/test_enrich_coverage_companies.py -x` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Per D-01: a value write per record, undone by clearing the property. The prompt addition is additive and leaves the shared prompt untouched.</reversibility>
  <done>Racing NSW has been attempted once, its raw evidence is committed, and its decision is either a validated enum value with an evidence URL or the D-03 marker — with the fallback proven by test rather than asserted in prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → driver | Model output crosses into a value destined for a CRM enum property |
| operator decision → paid call | Money is spent only after an explicit approval |
| provider credit APIs → estimate document | Read-only balance reads, no enrichment call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | Racing NSW research output → `lv_org_type` | high | mitigate | Enum-constrained prompt, plus post-hoc validation against the imported 9-tuple, plus a D-03 fallback for any out-of-vocabulary or unevidenced result |
| T-48-02 | Denial of Service (budget) | pre-run estimate | medium | mitigate | Estimate produced and reviewed before the first paid call; refuse-whole path proven by test |
| T-48-09 | Information Disclosure | committed raw research artifact | low | accept | The captured response contains public web-research findings about a company and no credentials or personal data; committing it is the evidence trail COVER-01 needs |
| T-48-10 | Repudiation | spend without a recorded decision | medium | mitigate | The blocking checkpoint records the operator's selection, and the estimate document is committed before the call is made |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `48-COST-ESTIMATE.md` and `48-RESEARCH-RACING-NSW.json` both committed
- `.venv/bin/python -m pytest` exits 0
- `node --test tests/n8n/*.test.mjs` exits 0
- Exactly one Anthropic research call attributable to this plan; zero provider credits drawn, confirmed by an unchanged Lusha balance
- Nothing deployed, nothing armed
</verification>

<success_criteria>
- The estimate exists before the spend, produced by code, covering both budgets
- The operator approved the spend as a separable decision, on the record
- Racing NSW attempted exactly once with an enum-constrained prompt
- `ORG_TYPE_DECISIONS` holds all 5 records
</success_criteria>

<output>
Create `.planning/phases/48-enrichment-coverage/48-03-SUMMARY.md` when done, recording the
operator's selection, the actual Anthropic call count and observed cost, the Lusha balance before
and after, and the enum value Racing NSW resolved to (or the reason it landed on the D-03 marker).
</output>
