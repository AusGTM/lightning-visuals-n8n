---
phase: 39-path-decision-fit-score-verification
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - .planning/phases/39-path-decision-fit-score-verification/evidence/account_info_response.json
  - .planning/phases/39-path-decision-fit-score-verification/evidence/properties_probe_response.json
  - .planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md
  - .planning/phases/39-path-decision-fit-score-verification/COVERAGE.md
autonomous: false
requirements: [DECIDE-01]
user_setup:
  - service: hubspot
    why: "The lead-scoring-tool availability question is answerable only inside the portal UI (D-01); no public API exposes hub-tier or product entitlement."
    env_vars:
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: "already in .env — the operator runs the probe command; the agent never reads .env"
      - name: HUBSPOT_PORTAL_ID
        source: "already in .env — must equal 22617666 or the probe refuses"
    dashboard_config:
      - task: "Walk Settings → Account & Billing → Products & Add-ons, then open the lead scoring tool, and screenshot whichever outcome occurs"
        location: "app-ap1.hubspot.com (portal 22617666) — note the ap1 host, not app.hubspot.com"

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The availability verdict rests on a dated in-portal observation by the operator, never on API silence alone."
    - "Raw API responses from portal 22617666 exist on disk as re-checkable files, not as prose summarizing them."
    - "A future reader can tell, from the evidence alone, which HubSpot API surfaces this phase deliberately did not touch and why."
  artifacts:
    - .planning/phases/39-path-decision-fit-score-verification/evidence/account_info_response.json
    - .planning/phases/39-path-decision-fit-score-verification/evidence/properties_probe_response.json
    - .planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md
    - .planning/phases/39-path-decision-fit-score-verification/COVERAGE.md
  key_links:
    - "VERIFICATION-NOTE.md links every claim it makes to a named file in evidence/ — no unsourced assertion."
    - "The availability verdict recorded here is the gate that plan 39-03's live probe and plan 39-04's decision both read."
  prohibitions:
    - "An availability verdict is never recorded from API evidence alone; a portal observation must back it."
    - "A properties-API 400 or an empty score-property list is never recorded as tier-negative evidence."
    - "No browser-automation tool drives the portal walkthrough — the operator drives it (D-01)."
    - "No evidence file is committed containing an Authorization header value or any token substring."
    - "Custom equation properties are not recorded as the fallback; D-06 supersedes ROADMAP criterion 3."
---

<objective>
Gather the load-bearing availability evidence and write it down so it survives a HubSpot
packaging change: run the API ladder live against portal 22617666, have the operator do the
in-portal walkthrough that the API provably cannot substitute for, then write the dated
attestation (D-02) and the API coverage matrix.

Purpose: This plan produces the availability half of the D-05 gate. Everything downstream —
whether the latency probe can even run, and which path 39-DECISION.md records — reads the
verdict written here.
Output: two raw API response files, operator screenshots, `evidence/VERIFICATION-NOTE.md`,
and `COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md
@.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md
@.planning/phases/39-path-decision-fit-score-verification/39-01-SUMMARY.md
@scripts/probe_scoring_tool_availability.py
</context>

## Artifacts this plan produces

- `.planning/phases/39-path-decision-fit-score-verification/evidence/account_info_response.json`
- `.planning/phases/39-path-decision-fit-score-verification/evidence/properties_probe_response.json`
- `.planning/phases/39-path-decision-fit-score-verification/evidence/portal_walkthrough_<YYYY-MM-DD>-*.png` (operator-supplied)
- `.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md`
- `.planning/phases/39-path-decision-fit-score-verification/COVERAGE.md`

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the availability probe live against portal 22617666</name>
  <what-built>
`scripts/probe_scoring_tool_availability.py` (plan 39-01) — a GET-only probe that refuses any
portal other than 22617666 and exits 0 with no network call when credentials are absent. It
performs two reads (`GET /account-info/v3/details`, `GET /crm/v3/properties/companies`) and
writes both raw responses into the phase `evidence/` directory.

The agent cannot run it: `.env` is permission-blocked to Read and to Bash in this session, so
the token can only be loaded in-process by a command the operator runs.
  </what-built>
  <how-to-verify>
Run this from the repo root (the leading `!` is the operator-invocation convention for
`.env`-dependent commands in this repo):

    .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/probe_scoring_tool_availability.py', run_name='__main__')"

Expected: two `GET` summary lines printed, exit status 0, and two new files under
`.planning/phases/39-path-decision-fit-score-verification/evidence/`.

Report back:
1. The full printed output (it contains no token by construction — confirm that visually).
2. Whether the run printed `REFUSED` (means `HUBSPOT_PORTAL_ID` in `.env` is not 22617666 —
   stop and report, do not edit `.env` to work around it).
3. Whether either probe returned a 403 — if so, paste the response body, because HubSpot 403
   bodies name the missing scope and the operator will need to grant it in the private app
   before plan 39-03 can run.
  </how-to-verify>
  <read_first>
    - `scripts/probe_scoring_tool_availability.py` — the module docstring carries the exact command
    - `.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md` "Open Questions" #2 (write-scope uncertainty and the 403-names-the-scope fallback)
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/account_info_response.json` exists and its `body.portalId` is `22617666`.
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/account_info_response.json` contains `"has_tier_field"` with a boolean value and a `probed_at_utc` timestamp.
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/properties_probe_response.json` exists and contains a `calculation_score_properties_found` key.
    - `grep -ril 'pat-na1\|Bearer ' .planning/phases/39-path-decision-fit-score-verification/evidence/` produces no output.
  </acceptance_criteria>
  <resume-signal>Paste the command output, then type "probe done" or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-portal lead-scoring-tool walkthrough (the authoritative check)</name>
  <what-built>
Nothing new — this is the step only the operator can perform. RESEARCH.md establishes that
no public HubSpot API exposes hub-tier or product entitlement, and that the property type the
lead-scoring tool creates cannot be created via the properties API on any tier. The API
evidence from the previous task is therefore supporting/negative only; the portal walkthrough
is the sole authoritative source for the availability verdict (D-01, Pitfall 1).
  </what-built>
  <how-to-verify>
In a browser, signed into portal **22617666** on **app-ap1.hubspot.com** (not
`app.hubspot.com` — this portal is ap1, HANDOVER §9):

1. Settings → Account &amp; Billing → Products &amp; Add-ons. Screenshot the full list of
   subscriptions/add-ons showing on this portal.
2. Navigate to the lead scoring tool and attempt to reach the score-builder UI.
3. Screenshot whichever outcome actually occurs. RESEARCH.md Open Question 1 flags that this
   could be either shape and the plan does not assume which:
   - the builder renders and offers a **company** score of type **fit** → capture the
     object-type selector and the score-type selector in the same shot; or
   - an upsell/paywall screen appears → capture the exact upsell wording; or
   - the entry point is absent entirely → capture the settings navigation showing where it
     would be, and say so in words.
4. If the builder renders: note whether "company" appears as a selectable object for a **fit**
   score specifically, not just for contacts. Contact-based scores are Marketing-Hub-only per
   HANDOVER §8; the company/fit combination is the actual question.

Save the screenshots into
`.planning/phases/39-path-decision-fit-score-verification/evidence/` named
`portal_walkthrough_<YYYY-MM-DD>-<step>.png`.

Report back, in words: available or not available, and which of the three outcome shapes you
saw. Do not use a browser-automation tool for this — D-01 assigns this step to you
deliberately.
  </how-to-verify>
  <read_first>
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` (D-01, D-02, and the click-path in "Specific Ideas")
    - `.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md` "Open Questions" #1
    - `HANDOVER-2026-08-06-icp-scoring.md` §8 (blocking item) and §9 (ap1 host, `--account=22617666`)
  </read_first>
  <acceptance_criteria>
    - At least two files matching `portal_walkthrough_*.png` exist in `.planning/phases/39-path-decision-fit-score-verification/evidence/`.
    - The operator has stated in words one of: "available", "not available".
    - The operator has stated which outcome shape occurred (builder rendered / paywall shown / entry point absent).
    - If "available": the operator has confirmed whether **company** is selectable for a **fit** score specifically.
  </acceptance_criteria>
  <resume-signal>Type "available" or "not available", then the outcome shape you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the dated attestation and the API coverage matrix</name>
  <files>.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md, .planning/phases/39-path-decision-fit-score-verification/COVERAGE.md</files>
  <read_first>
    - the two evidence JSON files written by Task 1 of this plan (read the actual values; do not paraphrase from expectation)
    - the operator's Task 2 report in this plan's execution log
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` (D-02 attestation requirements, D-06 fallback)
    - `.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md` "Common Pitfalls" 1–3 (the exact ways this note could record a false verdict)
  </read_first>
  <precondition>Both checkpoints in this plan have been answered; the two evidence JSON files and at least two `portal_walkthrough_*.png` files exist on disk.</precondition>
  <action>
Write `evidence/VERIFICATION-NOTE.md` — the D-02 attestation. It must be re-checkable by
someone who was not present, after HubSpot changes its packaging. Sections:

- **Header:** verification date, portal ID 22617666, portal host `app-ap1.hubspot.com`, region
  ap1, the git SHA at time of verification, and who performed the portal walkthrough.
- **Verdict:** company fit-score availability on Sales Hub Pro — available or not available —
  in one sentence, followed immediately by the named evidence file(s) that establish it.
- **API evidence (supporting/negative only):** for each of the two probes, the endpoint, the
  HTTP status actually observed, and the specific finding, each citing its evidence filename.
  State explicitly, in the note itself, that neither API result can establish availability:
  the account-info schema carries portal identity and locale only, and a score property
  appears in the properties listing only after the operator builds a scoring model in the UI,
  so an empty listing is inconclusive. Anyone re-reading this note must not be able to mistake
  API silence for a negative verdict.
- **Portal evidence (authoritative):** which of the three outcome shapes occurred, what the
  operator observed regarding the company + fit combination specifically, and the screenshot
  filenames.
- **Gate status:** the availability half of D-05, resolved. If available, note that the D-04
  recalc gate is still open and is measured in plan 39-04. If not available, note that D-06
  pre-commits the path to fix-the-four-workflow-chain-in-place with no second decision round,
  and that custom equation properties remain rejected — this supersedes ROADMAP criterion 3's
  equation-property wording.
- **Re-check procedure:** the exact probe command and the exact portal click-path, so this
  note can be re-run against a future portal state.
- **Assumptions carried:** name RESEARCH.md A1 (third-party "real-time" recalc claims are
  marketing copy, not an SLA) and A2 (the properties-create failure is believed
  portal-agnostic but was not reproduced live), and state that neither was relied upon for
  the verdict.

Then write `COVERAGE.md` — the API coverage matrix, a canonical markdown table with columns
`Capability | Endpoint | Decision | Reason`, `Decision` being `INTEGRATE` or `OPT-OUT`, and
every `OPT-OUT` carrying a one-line reason. This is a read-mostly probe phase, so most
CRM-write surface is honestly opted out. Rows to enumerate, at minimum:

INTEGRATE: account info read (`GET /account-info/v3/details`); company property list read
(`GET /crm/v3/properties/companies`); single property read
(`GET /crm/v3/properties/companies/{name}`); company create
(`POST /crm/v3/objects/companies`, disposable record only); company read
(`GET /crm/v3/objects/companies/{id}`); company patch
(`PATCH /crm/v3/objects/companies/{id}`, disposable record only); company delete
(`DELETE /crm/v3/objects/companies/{id}`, teardown).

OPT-OUT with reasons: property create (`POST /crm/v3/properties/companies`) — the
`calculation_score` field type is HubSpot-defined and read-only on every tier, so the call's
failure carries no tier signal; property update/archive — no schema mutation in a probe
phase; company search (`POST /crm/v3/objects/companies/search`) — the probe targets one known
id, no population query; batch company endpoints — single-record probe, batching adds no
signal; flows read (`GET /automation/v4/flows/{id}`) — the four-workflow chain is Phase 40/42
scope and its definitions are already archived; contacts and deals objects — phase scope is
company fit score only; lists — no population work; notes and associations — no audit-note
writing in a probe phase; owners and pipelines — unrelated to scoring availability; webhooks
and subscriptions — no event-subscription change; a dedicated lead-scoring API — none is
published (HubSpot's own APIs-by-tier reference has zero scoring entries), and that absence
is itself recorded evidence for this phase.

Finally, review both evidence JSON files before committing and confirm no `Authorization`
header value or token substring leaked into either.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md &amp;&amp; test -f .planning/phases/39-path-decision-fit-score-verification/COVERAGE.md &amp;&amp; grep -q '22617666' .planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md &amp;&amp; grep -c 'INTEGRATE\|OPT-OUT' .planning/phases/39-path-decision-fit-score-verification/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md` exists and contains the literal `22617666`, the literal `app-ap1.hubspot.com`, and an ISO date.
    - VERIFICATION-NOTE.md names, by filename, every evidence file it draws a claim from — `grep -c 'account_info_response.json\|properties_probe_response.json\|portal_walkthrough_'` outputs 3 or more.
    - VERIFICATION-NOTE.md contains a one-sentence verdict line beginning with `Verdict:`.
    - `.planning/phases/39-path-decision-fit-score-verification/COVERAGE.md` contains a markdown table whose header row is `| Capability | Endpoint | Decision | Reason |`.
    - `grep -c 'OPT-OUT' COVERAGE.md` outputs 10 or more, and every such line also contains an em-dash or `—`-introduced reason (no bare OPT-OUT row).
    - `grep -c 'INTEGRATE' COVERAGE.md` outputs 7 or more.
    - `grep -ril 'Bearer \|pat-na1' .planning/phases/39-path-decision-fit-score-verification/evidence/` produces no output.
  </acceptance_criteria>
  <done>A reader who was not present can reconstruct the availability verdict from named files, tell which API surfaces were deliberately skipped and why, and re-run the check against a future portal state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → HubSpot portal UI | The only channel that can answer the availability question; its output is screenshots, which are unverifiable by the agent. |
| evidence/ → committed git history | Raw API response bodies and portal screenshots become permanent public-repo history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05 | Information Disclosure | evidence JSON + screenshots committed to git | high | mitigate | Task 3 reviews both JSON files for `Authorization`/token substrings before commit, backed by a grep acceptance criterion; the operator is told screenshots go to public history so they can crop credentials out of the browser chrome. |
| T-39-06 | Repudiation | availability verdict with no traceable source | high | mitigate | VERIFICATION-NOTE.md is required to name the evidence file behind every claim, with date, git SHA, and portal ID; asserted by a filename-citation grep count. |
| T-39-07 | Spoofing | verdict recorded against the wrong portal | medium | mitigate | The note records portal ID and `app-ap1.hubspot.com` host; the probe itself refuses any portal other than 22617666, so the API half cannot be sourced from elsewhere. |
| T-39-08 | Tampering | operator "fixes" a portal-guard refusal by editing `.env` | medium | mitigate | The checkpoint instruction explicitly says to stop and report on `REFUSED` rather than edit `.env`. |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Both evidence JSON files present, portal-stamped, token-free.
- Two or more portal screenshots present.
- VERIFICATION-NOTE.md and COVERAGE.md present and well-formed.
</verification>

<success_criteria>
- The availability half of the D-05 gate is resolved and recorded with named evidence.
- The record states explicitly that API silence is not a negative verdict.
- COVERAGE.md is the honest subtraction record for this phase's HubSpot API surface.
</success_criteria>

<output>
Create `.planning/phases/39-path-decision-fit-score-verification/39-02-SUMMARY.md` when done.
</output>