---
status: complete
phase: 16.6-companies-search-transport-fix
source: [16.6-VERIFICATION.md gaps 1 and 2]
started: 2026-07-29T17:15:00+10:00
updated: 2026-07-29T17:15:00+10:00
---

## Current Test

[testing complete]

## Tests

### 1. All six company:search nodes retrieve real records live
expected: |
  ROADMAP criterion 1. The verifier refused to generalize from one live proof, correctly:
  this project found ten live-only defects that an offline-green suite did not catch.
result: pass
source: automated
evidence: |
  COMPLETE — 6/6 executed inside n8n, all on 2026-07-29. Splitting the criterion's
  two distinct risks:

  (a) EXECUTED INSIDE n8n — 6 of 6:
        HubSpot Company Fetch By Id    execution 12  {total:1, results:[9604614548]}
        HubSpot Company Search         execution 19  total:1, Merge Company non-null
        SJ-3 Search (requested poller) execution 23  scheduled tick, total=0 (correct)
        Review Search (approved=true)  execution 24  scheduled tick, total=0 (correct)
        SJ-1 Search (input-gap scan)   execution 29  FIRST HOURLY TICK — total=712 live
                                       through n8n (the read-only replay's exact number),
                                       100 rows extracted, and SJ-1 Set Requested Write
                                       Gate emitted 0 items: ALL 100 DENIED on the empty
                                       allowlist. The 712-company flag cascade did not
                                       start — fail-closed proven at scale, exactly the
                                       activate-allowlist-empty sequence this UAT's
                                       operational warning prescribed.
        SJ-2 Search (stale refresh)    execution 33  total=0 (correct — nothing verified
                                       >180d ago). Its monthly cadence would not have
                                       ticked for weeks, so it was exercised by a
                                       temporary disarmed deploy shortening the trigger
                                       to 2 minutes, capturing the tick, and restoring
                                       the committed build — monthly cadence read back
                                       confirmed, workflow still active. Zero writes:
                                       SJ-2 Set Requested sits behind a denying gate and
                                       the scan matched nothing.

  (b) FILTER CONTRACT ACCEPTED LIVE — 6 of 6. On 2026-07-29 each remaining node's ACTUAL
  committed jsonBody was replayed verbatim against POST /crm/v3/objects/companies/search
  (read-only; a query endpoint):

        SJ-1 Search   lv_org_type NOT_HAS_PROPERTY OR =unknown
                      OR lv_produces_content NOT_HAS_PROPERTY   -> 200  total=712
        SJ-2 Search   lv_org_type_verified_at LT cutoff
                      OR lv_produces_content_verified_at LT     -> 200  total=0
        SJ-3 Search   lv_enrichment_requested EQ true
                      AND lv_enrichment_status NEQ running      -> 200  total=0
        Review Search lv_enrichment_review_approved EQ true     -> 200  total=0

  This matters because an invalid filter property 400s rather than silently returning
  empty (measured in 16.4-UAT.md), so a 200 is positive evidence the contract is accepted,
  not merely an absence of error. SJ-1 additionally RETRIEVED 712 real company records —
  a genuine live retrieval, just not one routed through n8n.

  No residual. Every one of the six nodes has now retrieved (or correctly
  zero-matched) real records live, executing inside n8n, with read-back evidence.

### 2. State the answer: do company:create / company:update share BUG 10's defect?
expected: |
  Criterion 5 / 16.6-CONTEXT.md's explicit open question, which the phase never answered
  in a direct sentence.
result: pass
source: automated
evidence: |
  Answered in 16.6-CRITERION-5-ANSWER.md, from upstream source rather than the RCA's
  summary. `companyOperations` = create/delete/get/getAll/getRecentlyCreatedUpdated/
  searchByDomain/update. create and update both exist; search does not, and that single
  absence is all of BUG 10 — an operation with a dispatch branch cannot fail that way.

  Asking the general question found a second instance: `Dedupe Set Needs Review` ran
  `contact:update`, which does not exist (contacts have upsert). BUG 18 — same mechanism,
  same invisibility, same workflow as the six nodes BUG 10 fixed. Never live. Fixed, and
  the class is now closed by tests/test_hubspot_native_operation_validity.py.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
blocked: 0
skipped: 0

## Gaps

[none — test 1 is a prerequisite gate, not a code defect]

## Operational warning found while testing (READ BEFORE ACTIVATING)

**SJ-1's input-gap scan matches 712 companies right now.** Its branch flags matches with
`lv_enrichment_requested=true`, which SJ-3 then polls and dispatches into enrichment. On an
hourly cadence with `limit: 100`, activating LV Scheduled Maintenance without care would
start walking 712 records through the full provider waterfall — real Lusha/ZoomInfo/Apollo
credits and real Anthropic spend, on a schedule, unattended.

The fail-closed design does hold: `SJ-1 Set Requested` sits behind a BUG 15 write-safety
gate, so with an empty allowlist it flags nothing and the cascade never starts. That is the
safe way to activate first — switch it on with the allowlist empty, confirm the searches run
and the gates deny, then widen deliberately.

This is a live-cost observation, not a defect, and it was not visible before the searches
were run against real data.
