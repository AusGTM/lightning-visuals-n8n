---
phase: 57-ceilings-refusal-before-start-and-post-run-proof
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - operator-claude-plugin/scripts/written_records.py
  - operator-claude-plugin/scripts/report_enrichment.py
  - scripts/build_cloud_workflows.py
  - n8n/wf_contact_ingest_cloud.json
  - operator-claude-plugin/tests/test_written_records.py
  - operator-claude-plugin/tests/test_report_enrichment.py
  - tests/n8n/ingestResponseRowId.test.mjs
  - .planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-DISCUSSION-LOG.md
autonomous: false
requirements: [AFTER-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`written_records.classify_item({'action': 'write_blocked', ...})` yields the outcome word `gated`, distinct from `written` — a record that would have been written never reads as a completed one (AFTER-03)."
    - "`classify_item` maps every one of the ten `action` values the backend actually emits: `create`, `update`, `enrich`, `write_blocked`, `review`, `needs_match_review`, `research_failed`, `recompute_refused`, `skip`, `proposed`. No value falls through to a default."
    - "`enrich` with an `hs_object_id` is treated exactly as `update` is — it is in `WRITE_ACTIONS` and had no row in D-57-03's table, and it is never dropped and never falls through to a default. WHICH word the two share is Task 1's checkpoint: `written` under option-a, `write_attempted` under option-b (where only a create's echoed id is terminal evidence) and under option-c."
    - "`create` with no id in the response still maps to `created_id_unknown` and no id is ever fabricated (D-57-03: stays as-is)."
    - "`skip` and `proposed` map to `no_action`, so a clean run stops reading as though half of it did not land."
    - "Every entry `classify_item` builds carries `row_id` and `association`, both defaulting to None when the response item does not supply them — the join key AFTER-01 needs for exactly the held and gated rows that have no `hs_object_id`."
    - "An entry written before this widening is read back BYTE-UNCHANGED — `load()` never rewrites an old document — and every reader reaches the new fields through `.get()`, so a missing `row_id` reads as `None` at the reader and joins as unjoinable, never as an error and never as a partial-trust guess (REVIEW-57-M10: pick one contract and state it; this is the one)."
    - "`report_enrichment` and `written_records` render the same backend `action` with the same outcome word — there is exactly one action-to-outcome vocabulary in the client, not two."
    - "The shared vocabulary lives in a PURE `written_records.outcome_for_action(action, hs_object_id=None)`; `classify_item` keeps its raising persistence validation and `report_enrichment` calls only the pure function, so the never-raise report contract at `report_enrichment.py:285-304` is preserved (REVIEW-57-H, `classify_item` raises at `written_records.py:100-115` and `:150-190`)."
    - "`Build Ingest Response` returns `row_id` on every row it emits, so a lane whose rows carried `row_id` into the backend can name an ingest-lane held row individually even when it has no `contact_id`."
    - statement: "The pair pipeline's FINAL ingest dispatch does NOT carry `row_id` — `extraction.strip_row_id` removes it at `enrich-before-ingest/SKILL.md:639` before the CSV is written — so that leg's rows come back with `row_id: None`. This is named as an open join gap in the plan and in the end-of-run report, never papered over (REVIEW-57-H7)."
      verification: backstop
  artifacts:
    - operator-claude-plugin/scripts/written_records.py
    - operator-claude-plugin/scripts/report_enrichment.py
    - scripts/build_cloud_workflows.py
    - n8n/wf_contact_ingest_cloud.json
    - tests/n8n/ingestResponseRowId.test.mjs
  key_links:
    - "`written_records.classify_item` -> `report_enrichment._outcome_for_row` — the two client-side readers of the same backend fact; PATTERNS confirms there is no third. If they drift again, an operator reads two different words for one event depending on which surface they open."
    - "`Decide Action`'s `row.row_id` -> `Build Ingest Response`'s returned json -> `classify_item`'s entry -> the AFTER-01 join. Break any hop and held ingest rows become unnameable."
  prohibitions:
    - statement: "Never fabricate an `hs_object_id` to make a report tidier; a create whose response carried no id keeps its own distinct word."
      status: flagged-unverified
      verification: none
    - statement: "A `write_blocked` row must never render with the same operator-facing word as a written row, on any surface."
      status: flagged-unverified
      verification: none
    - statement: "No PII field (`email` and every other contact identifier) may enter a written-records entry, whatever else this widening adds."
      status: flagged-unverified
      verification: none
    - statement: "`n8n/wf_contact_ingest_cloud.json` must never be hand-edited; it is regenerated by `scripts/build_cloud_workflows.py`."
      status: flagged-unverified
      verification: none
    - statement: "This plan does NOT deploy. The regenerated ingest workflow is committed and pinned by test; deploying it is deferred to 57-05's phase-gate checkpoint, because a live change should not land before the phase's own safety proposition is complete (REVIEW-57, gpt-5-6-sol: 'Deployment in Plan 57-02 before the full phase safety gate is complete')."
      status: flagged-unverified
      verification: none
---

<objective>
Make the run's ledger say what actually happened to each row. Today
`written_records.classify_item` collapses every non-write action into one word,
`not_written`, and the concrete consequence is AFTER-03's exact case: a `write_blocked` row —
one the backend would have written, and which an operator can recover by opening a grant and
re-sending — is indistinguishable from a failure. The other consequence is the mirror image:
`skip` and `proposed` are successes, and they currently make a clean run read as though half of
it did not land.

Purpose: AFTER-03, and the join key AFTER-01 cannot be built without.

Output: one widened outcome vocabulary, used by both client-side readers, with `row_id` and
`association` captured on every entry and reaching the ingest lane's response.

## The two explicit resolutions this plan owes the phase

**1. `enrich` — the tenth action.** The backend emits ten distinct `action` values, not the
nine D-57-03's table accounts for. `enrich` is a genuine write (it is in
`written_records.WRITE_ACTIONS`) and has no row in the operator's table. It is placed on
`written`, exactly as `update` is, because that is what it is. It is not dropped and it does not
fall through to a default.

**A seventh outcome word is added, and this plan is declaring it rather than smuggling it.**
`update` or `enrich` whose response carried no `hs_object_id` has nowhere honest to go: it is
not `written` (constraint: "written" must never be inferred), and `created_id_unknown` is
literally named for creates and D-57-03 says it stays as-is. That row gets
`written_id_unknown`. This EXTENDS the operator's six-outcome table by one word for a case the
backend can in principle emit; the operator action for it is "open this row's record and
confirm", which is distinct from every other word's action, so it earns its place by the same
test the other six are held to.

**2. The two-vocabulary divergence is reconciled here, not deferred.** PATTERNS grepped the
whole repo and confirmed exactly TWO client-side surfaces map an `action` to an operator-facing
word: `written_records.classify_item`/`WRITE_ACTIONS`, and
`report_enrichment._ACTION_TO_OUTCOME`. The second maps only 6 of 10 values — `update`,
`review`, `research_failed` and `recompute_refused` all render as `"unknown"` today, uncovered
by any test — and uses different words for overlapping concepts (`"blocked"` where D-57-03 says
`gated`). Leaving them divergent is not an option; CLAUDE.md section 13.0.1 records what a
second copy of one rule costs, and closed exactly that class of gap by refusing to duplicate.
`report_enrichment` imports `written_records`' outcome constants and delegates to
`classify_item`. It does not keep a second table.

The per-module `_FORBIDDEN_NAME_MARKERS` anti-DRY convention is NOT weakened by that import: it
covers the forbidden-name tuple specifically (each module carries its own copy so a change to
one cannot silently weaken another), and says nothing about an outcome vocabulary that must, by
definition, be identical everywhere.

**What `report_enrichment` imports is a PURE function, not `classify_item`** (revised after
review). `classify_item` raises `WrittenRecordsError` for a malformed item and for a
forbidden-named value (`written_records.py:100-115`, `:150-190`), while
`build_enrichment_report` promises never to raise (`report_enrichment.py:285-304`). Delegating
the report path straight to `classify_item` would import persistence validation into a never-raise
surface. So the mapping is extracted into `outcome_for_action(action, hs_object_id=None)` — pure,
raising nothing, returning a word — and BOTH callers use it: `classify_item` for the durable
entry it also validates, `_outcome_for_row` for the report it must not fail.

## The two review findings this plan does NOT close, stated where the executor will see them

**1. `row_id` does not survive the pair pipeline's final ingest leg (REVIEW-57-H7).**
`extraction.strip_row_id(sendable_rows)` at `enrich-before-ingest/SKILL.md:639` deliberately
removes the key before `write_dispatch_csv`, because `row_id` is a plugin-internal join key and
STRUCT-01 refuses a row carrying a non-canonical column. That is a 2026-08-29 operator ruling,
reaffirmed in `extraction.strip_row_id`'s own docstring, which explicitly considered and rejected
exempting `row_id` from the canonical check.

So Task 4's addition of `row_id` to `Build Ingest Response` closes the join for every lane whose
rows carry `row_id` into the backend, and does NOT close it for the pair pipeline's final ingest
dispatch, where the value the node echoes will be `null`. Adding it back would need one of two
things this plan will not do: carrying `row_id` beside the rows rather than inside them (named in
that same docstring as "the better end state but too wide a change for this fix"), or joining the
ingest response to the client's rows by `email` — which would put a contact identifier into the
durable ledger and breach this plan's own third prohibition. **Deferred with rationale**, not
silently: 57-05's report marks pair-pipeline ingest rows as joined by `hs_object_id` where one is
present and UNJOINABLE otherwise, and names the gap in its `gaps` section rather than implying a
complete join.

**2. Whether `written` is provable at all is Task 1's checkpoint (REVIEW-57-H6).** Every `action`
this module maps originates in a DECISION node that runs before the HubSpot HTTP write, and for
an update `hs_object_id` is known before the PATCH — so a failed terminal write can still classify
as `written`. That collides with CONTEXT's own rule that "'Written' must never be inferred",
while D-57-03 locks the word `written`. A locked decision against a locked principle is an
operator call, not a planner's, so it is a checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-CONTEXT.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-RESEARCH.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Can the ledger say `written` at all, when nothing proves the write landed?</name>
  <read_first>
    - .planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-CONTEXT.md (D-57-03's outcome table, which locks the word `written`; and `<specifics>`, which states "'Written' must never be inferred"; and `<deferred>`, which keeps the post-write confirmation out of scope)
    - scripts/build_cloud_workflows.py (lines 465-520 — `Build Ingest Response`, where `contactId` falls back to `row.hs_object_id` when no write response was joined; lines 1690-1715 and 3414-3441 — the two decision nodes, which run BEFORE the HubSpot write)
    - operator-claude-plugin/scripts/written_records.py (lines 38-52 — why `created_id_unknown` exists, and the no-fabrication discipline this question is the general case of)
  </read_first>
  <decision>Whether a `create`/`update`/`enrich` row whose response carried an `hs_object_id` is reported to the operator as `written`, or as an honest intermediate that does not claim the HubSpot write succeeded.</decision>
  <context>
The evidence, verified in source this session. `action` comes from a decision node that runs
before the HubSpot HTTP write. For a CREATE, the id in the response is echoed back from the
create call, so an id present is real evidence a record now exists. For an UPDATE or an ENRICH,
`hs_object_id` was already known before the PATCH — `Build Ingest Response` even falls back to
`row.hs_object_id` when no write response could be joined — so an id present proves only that the
write was PERMITTED and ATTEMPTED, never that it landed.

D-57-03 locks the word `written` for both. CONTEXT's `<specifics>` says "'Written' must never be
inferred". Both are the operator's. Restoring the post-write confirmation that would settle it is
explicitly deferred (`<deferred>`, scoped out in 59-01 and still out), so this phase cannot get
the evidence — it can only choose what to say without it.

This matters more than a word: `written` is the outcome an operator does NOTHING about. If an
update's PATCH failed and the row reads `written`, that row is silently lost — the exact
confusion AFTER-03 exists to prevent, arriving from the other direction.

Whatever is chosen, three things do not change: no id is ever fabricated,
`created_id_unknown` stays as-is (D-57-03), and `write_blocked` keeps its own recoverable word.
The choice is costly-to-reverse either way — these words go to a persisted on-disk artifact.
  </context>
  <options>
    <option id="option-a">
      <name>Keep `written` as D-57-03 locked it; disclose what it rests on</name>
      <pros>D-57-03 is honoured verbatim; the operator's six-word table is unchanged; the persisted vocabulary matches what the decision said. 57-05's report states once, in the spend/evidence line, that `written` means the backend decided to write and the response carried an id, and that terminal confirmation is not available in this milestone.</pros>
      <cons>The strongest review finding stays open: a failed update reads as a completed one, and the disclosure is a sentence in a report rather than a distinction in the data. `<specifics>`'s rule is not satisfied.</cons>
    </option>
    <option id="option-b">
      <name>Split by what the evidence actually supports: `written` for a create whose id was echoed back, `write_attempted` for an update/enrich whose id was known beforehand (recommended)</name>
      <pros>Every word is true. It extends the table by one, exactly as this plan already proposes to do for `written_id_unknown`, and by the same test — the operator action differs ("nothing" versus "spot-check this record if it matters"). It is implementable today from data the backend already emits, with no new backend call.</pros>
      <cons>A seventh and eighth word for the operator to learn; the persisted vocabulary diverges from D-57-03's table, so the decision record must be amended. Some rows an operator previously read as done now read as probably-done, which is more accurate and less comfortable.</cons>
    </option>
    <option id="option-c">
      <name>`write_attempted` for all three write actions until a post-write confirmation exists</name>
      <pros>Maximally honest and simplest to reason about — no row claims a landed write anywhere in the system until something proves one.</pros>
      <cons>Discards real evidence for creates, where the echoed id IS terminal proof. Makes a clean run read as uncertain, which is the same "a clean run reads as half-landed" failure D-57-03 set out to fix.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c. If option-b or option-c, D-57-03's table is amended in `57-DISCUSSION-LOG.md` in the same task that implements it.

**Ownership (M-2), scoped to what this plan can actually reach.** `57-DISCUSSION-LOG.md` is the
phase's one home for CHECKPOINT RULINGS, and this plan — 57-02 — owns the file for the rulings
taken in WAVES 1 AND 2: its own Task 1 ruling, and 57-04's Task 2 run/decline ruling (wave 1,
so it is already taken by the time this plan executes). It cannot own the whole phase's rulings
and does not claim to: 57-05 Task 4 is a checkpoint in WAVE 3, after this plan has finished, so
that task records its own entry and carries `57-DISCUSSION-LOG.md` in its own `files_modified`.
57-01 Task 2's ruling is recorded by 57-01 Task 3, which already edits this file for the D-57-00
supersession entry. Three writers, in wave order, each named — never two at once. The other half of M-2 lives in 57-03, which owns `.planning/STATE.md` as the
decision-record for its own one-way ruling; the two files have distinct owners and distinct
content so no wave has two writers of the same record.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the outcome vocabulary, extract the pure mapping, and capture the join keys</name>
  <files>operator-claude-plugin/scripts/written_records.py, operator-claude-plugin/tests/test_written_records.py, .planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-DISCUSSION-LOG.md</files>
  <read_first>
    - operator-claude-plugin/scripts/written_records.py (the whole file, 323 lines — module docstring's three-outcome contract at 38-52, `WRITE_ACTIONS` at 96, `_FORBIDDEN_NAME_MARKERS` at 100-103, `classify_item` at 131-192, `_entries_from_document` at 207-219, `append_chunk` at 241-288)
    - scripts/build_cloud_workflows.py (lines 1699-1715 and 3434-3444 — the two decision nodes' returned json, which is where `row_id` comes from; lines 494-519 — `Build Ingest Response`, which supplies `association`, `company_id` and `contact_id`)
    - operator-claude-plugin/tests/test_written_records.py (the existing `_patch_durable_dir` fixture idiom and every test that pins `NOT_WRITTEN`)
  </read_first>
  <behavior>
    Write these first and watch them fail.

    - `classify_item({"action": "write_blocked"})["outcome"] == written_records.GATED`.
    - `classify_item({"action": "review"})["outcome"] == written_records.HELD` and the same
      for `needs_match_review`.
    - `classify_item({"action": "research_failed"})["outcome"] == written_records.FAILED` and
      the same for `recompute_refused`.
    - `classify_item({"action": "skip"})["outcome"] == written_records.NO_ACTION` and the same
      for `proposed`.
    - The write-action branch, per Task 1's SELECTED option — write these three tests to
      match the selection, not all three variants:
      **option-a:** `create`/`update`/`enrich` with an `hs_object_id` all yield
      `written_records.WRITTEN`.
      **option-b:** `create` with an `hs_object_id` yields `WRITTEN` (the id was echoed back
      by the create call, which is terminal evidence); `update` and `enrich` with an
      `hs_object_id` yield `written_records.WRITE_ATTEMPTED` (the id was known before the
      PATCH, so it proves nothing about the write landing).
      **option-c:** all three yield `WRITE_ATTEMPTED`.
    - `classify_item({"action": "create"})["outcome"] == written_records.CREATED_ID_UNKNOWN`
      (no id supplied) — unchanged by the selection; D-57-03 keeps this word as-is.
    - `classify_item({"action": "enrich"})["outcome"] ==
      written_records.WRITTEN_ID_UNKNOWN` (no id supplied), and the same for `update` —
      unchanged by the selection, since a missing id is the same fact under all three.
    - An unrecognised action (for example `"quokka"`) maps to `written_records.FAILED` and its
      `reason` is preserved — an action this module has never seen is something to look at,
      never a silent success.
    - Every one of the ten real action values is exercised by a parametrised test. The
      ten-value set is NOT hard-coded in the consumer test — that would be circular and
      would not notice an eleventh action (REVIEW-57-M). Instead the test EXTRACTS the
      action literals from `scripts/build_cloud_workflows.py` (regex over
      `action: "<literal>"` and `action = "<literal>"` in the decision-node jsCode
      constants) and asserts the extracted set equals
      `set(ACTION_TO_OUTCOME) | WRITE_ACTIONS`. A new action added to the builder fails
      this test in the client, which is the point.
    - `outcome_for_action("write_blocked")` returns the same word `classify_item` puts on
      the entry, and `outcome_for_action` NEVER raises — not for `None`, not for a
      non-string, not for an unknown value. Its unknown-value answer is the same
      `FAILED`-shaped word.
    - `classify_item({"action": "review", "row_id": "r7", "association": "not_attempted"})`
      carries `row_id == "r7"` and `association == "not_attempted"` on the entry, with
      `hs_object_id` None.
    - `classify_item({"action": "skip"})` carries `row_id` None and `association` None — the
      keys are always present, never absent, so a reader cannot mistake a missing key for a
      different shape.
    - `classify_item` still raises `WrittenRecordsError` on a non-dict item and still raises on
      a forbidden-named value, with the new keys included in the scan.
    - An `email` key on the input item never appears on the entry.
    - `load(path=...)` over a document whose entries predate this widening returns those
      entries BYTE-UNCHANGED — no key is injected on load — and `entry.get("row_id")` on
      them yields None. One contract, asserted both ways (REVIEW-57-M10).
  </behavior>
  <action>
Widen `written_records.py` per D-57-03, AFTER-03 and Task 1's selected option.

FIRST, honour Task 1's decision. If option-b was selected, the write-action branch below
distinguishes a `create` whose response echoed an id (`written`) from an `update`/`enrich` whose
id was known before the write (`write_attempted`), and D-57-03's table is amended in
`57-DISCUSSION-LOG.md` in this same task. If option-a was selected, keep `written` for all three
and add the one-sentence evidence disclosure to the module docstring so 57-05 can render it. If
option-c, every write action maps to `write_attempted`. Everything else in this task is unchanged
by the choice.

Add outcome constants beside the existing `WRITTEN`, `CREATED_ID_UNKNOWN` and `NOT_WRITTEN`:
`GATED = "gated"`, `HELD = "held"`, `FAILED = "failed"`, `NO_ACTION = "no_action"`,
`WRITTEN_ID_UNKNOWN = "written_id_unknown"`. Keep `NOT_WRITTEN` defined and exported as a
deprecated alias with a comment saying it is no longer produced by `classify_item`, so an old
on-disk entry carrying it is still a recognisable word to a reader rather than a mystery
string. Add `ALL_OUTCOMES` as a frozenset of the seven produced words.

Add a module-level `ACTION_TO_OUTCOME` mapping for the six non-write actions:
`write_blocked` to `GATED`; `review` and `needs_match_review` to `HELD`; `research_failed` and
`recompute_refused` to `FAILED`; `skip` and `proposed` to `NO_ACTION`. Write actions
(`WRITE_ACTIONS`, unchanged) are resolved by the id branch, not by this table. An action in
neither set resolves to `FAILED` — a value this module has never seen is a thing to look at,
and calling it a no-action would be a silent success.

Extract the mapping into a PURE module-level function before wiring anything to it:

`outcome_for_action(action, hs_object_id=None) -> str` — no I/O, no validation, no raise, for
ANY input. A `WRITE_ACTIONS` action with a truthy `hs_object_id` resolves per Task 1's decision;
without one it is `CREATED_ID_UNKNOWN` when the action is `create` and `WRITTEN_ID_UNKNOWN`
otherwise. Everything else reads `ACTION_TO_OUTCOME` with `FAILED` as the fallback. This function
is the single vocabulary; `classify_item` calls it, `report_enrichment` calls it, and nothing
re-derives it. Separating it from `classify_item` is what lets the never-raise report path reuse
the vocabulary without inheriting persistence validation (REVIEW-57-H, `report_enrichment.py:285-304`).

Rewrite `classify_item`'s branch to call `outcome_for_action(item.get("action"),
item.get("hs_object_id"))`. `classify_item` keeps everything else it does — the non-dict raise,
the forbidden-name scan, the entry construction — unchanged.

Add `"row_id": item.get("row_id")` and `"association": item.get("association")` to the entry
dict, before the forbidden-name scan so both are scanned like every other field. `row_id` is
the join key AFTER-01 needs and is verified present on the same response item this function
already reads (`scripts/build_cloud_workflows.py:1705,3439`); `association` is what AFTER-01's
"association outcome" is read from (`Build Ingest Response`, one of `associated`,
`not_confirmed`, `not_attempted`, `none`). Neither is PII. Do NOT add `email`, `contact_id` or
any other identifier the module docstring already excludes.

Rewrite the module docstring's outcome section to describe the seven words and what an operator
does about each, and record there that `written_id_unknown` extends D-57-03's six-word table by
one, why, and that `created_id_unknown` was left alone because it is already on disk. Add
`row_id` and `association` to the schema line. State the read-side rule for old entries: a
missing `row_id` is `None` and unjoinable, the same absent-field discipline every sibling
artifact already applies — no schema version field is introduced, because `_entries_from_document`
already degrades a whole document rather than half-trusting one.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_written_records.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_written_records.py -q` passes, including every behaviour above.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_written_records.py -k gated -q` selects at least one test and passes.
    - `ALL_OUTCOMES` equals the set Task 1's checkpoint selected, asserted by an executable check written to match that selection:
      **option-a** → `frozenset({'written','created_id_unknown','written_id_unknown','gated','held','failed','no_action'})`;
      **option-b** → the same seven plus `'write_attempted'` (eight);
      **option-c** → the seven with `'written'` replaced by `'write_attempted'`.
      Write the `.venv/bin/python -c "... assert written_records.ALL_OUTCOMES == frozenset({...})"` one-liner against the selected set; do not carry all three into the repo.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import written_records as w; e=w.classify_item({'action':'write_blocked','row_id':'r1'}); assert e['outcome']=='gated' and e['row_id']=='r1' and e['association'] is None and 'email' not in e"` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import written_records as w; assert set(w.ACTION_TO_OUTCOME) | w.WRITE_ACTIONS == {'create','update','enrich','write_blocked','review','needs_match_review','research_failed','recompute_refused','skip','proposed'}"` exits 0.
    - The builder-derived action test passes, and demonstrably fails when a fake eleventh `action: "quokka"` literal is temporarily added to a decision-node constant in `scripts/build_cloud_workflows.py` — the executor verifies this by making the edit, watching the test go red, and reverting it. A test that cannot fail is not a test.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import written_records as w; [w.outcome_for_action(v) for v in (None, 7, '', 'quokka')]"` exits 0 — the pure function never raises.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
  </acceptance_criteria>
  <reversibility rating="costly">Implements D-57-03. `written_records` entries are a persisted on-disk artifact; changing these words later means migrating existing per-run files or teaching every reader two vocabularies.</reversibility>
  <done>Every backend action maps to exactly one of seven outcome words, `enrich` is placed on `written`, gated is distinct from written, and every entry carries `row_id` and `association`.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse report_enrichment onto the one vocabulary</name>
  <files>operator-claude-plugin/scripts/report_enrichment.py, operator-claude-plugin/tests/test_report_enrichment.py</files>
  <read_first>
    - operator-claude-plugin/scripts/report_enrichment.py (lines 1-80 for `_ACTION_TO_OUTCOME`, `SUCCESS_OUTCOMES`, `_OUTCOME_REASON`, `_empty_counts`; 121-185 for `_outcome_for_row` and `_build_row_report`; 245-337 for `build_sync_report` and `build_enrichment_report`)
    - operator-claude-plugin/scripts/written_records.py (the constants and `classify_item` written in Task 1)
    - operator-claude-plugin/tests/test_report_enrichment.py (every test pinning `created`, `enriched`, `blocked`, `skipped`, `previewed` or `unknown` — all of them change in this edit)
  </read_first>
  <action>
Delete `report_enrichment._ACTION_TO_OUTCOME` and have `_outcome_for_row` delegate to
`written_records.outcome_for_action(row.get("action"), row.get("hs_object_id"))`, per D-57-03 and
AFTER-03. **Not `classify_item`** — that was the first pass's design and cross-AI review caught
why it is wrong: `classify_item` raises for a malformed item and for a forbidden-named value
(`written_records.py:100-115`, `:150-190`), while `build_enrichment_report` promises never to
raise (`report_enrichment.py:285-304`). Delegating to it would import persistence validation into
a never-raise surface and turn a malformed row into a report that fails instead of a report that
says the row was malformed. `outcome_for_action` is pure and total by construction.

Import the outcome constants from `written_records` rather than restating them — the anti-DRY
convention in this codebase covers `_FORBIDDEN_NAME_MARKERS` only, and an outcome vocabulary that
must be identical everywhere is the opposite case.

Add a test that `build_enrichment_report` over a row set containing a malformed row and a row
carrying a forbidden-named value still returns a report rather than raising — the regression this
delegation choice exists to prevent.

Re-key `_empty_counts()` on `written_records.ALL_OUTCOMES` — derived, never restated, so it
follows Task 1's selection automatically. Replace `SUCCESS_OUTCOMES` with the landed-or-nothing-
to-do set for that selection: `{WRITTEN, NO_ACTION}` under option-a, and additionally
`WRITE_ATTEMPTED` under option-b or option-c (a write the backend was allowed to make is not a
failing row; the uncertainty is carried by the word, not by the bucket). `skip` and `proposed`
are successes and
must stop inflating `failing_rows`, which is exactly the "a clean run reads as half-landed"
defect D-57-03 names. Keep each row's raw `action` on `_build_row_report`'s output so a renderer
can still say how many of the written rows were creates and how many were enriches; the counts
are by outcome, the detail is by action.

Re-key `_OUTCOME_REASON` on the new words and add the four that had no entry. Write the `gated`
reason as the recoverable instruction AFTER-03 asks for: this row would have been written, and
opening a grant and re-sending it will write it. Keep the existing accurate explanations for
skip and for the look-only pass, moved to their new keys. Add a `failed` reason naming retry or
fixing the input, a `written_id_unknown` reason naming "open the record and confirm", and a
`created_id_unknown` reason stating the id is unrecoverable and is never fabricated.

Add tests covering the four actions that previously rendered as `"unknown"` (`update`,
`review`, `research_failed`, `recompute_refused`), and a test asserting the two modules agree:
for each of the ten action values, `report_enrichment._outcome_for_row({"action": a})` equals
`written_records.classify_item({"action": a})["outcome"]` — the two READERS compared, both
resolved through the one pure function.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_enrichment.py -q</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: _ACTION_TO_OUTCOME -->
    - `grep -c "_ACTION_TO_OUTCOME" operator-claude-plugin/scripts/report_enrichment.py` returns 0. The identifier is named in the action because the action is to delete it; it is a symbol name, not comment prose that could satisfy the grep by accident.
    - `grep -c "written_records" operator-claude-plugin/scripts/report_enrichment.py` is at least 2 (import and use).
    <!-- planner-discipline-allow: classify_item -->
    - `grep -c "classify_item" operator-claude-plugin/scripts/report_enrichment.py` returns 0 — the never-raise report path reaches the vocabulary only through the pure function. The identifier is named in the action because the action is to keep it out of this module; it is a symbol name, and the explanation of why belongs in this plan rather than in a code comment that would satisfy the grep by accident.
    - A test asserts `build_enrichment_report` returns a report (does not raise) over a row set containing a malformed row and a forbidden-named value.
    - A parametrised test asserts the two modules return the same outcome word for all ten action values, and fails if either drifts.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import report_enrichment as r, written_records as w; assert set(r._empty_counts()) == set(w.ALL_OUTCOMES)"` exits 0 — the counts are keyed on whatever vocabulary Task 1's checkpoint selected, derived rather than restated, so this criterion holds under all three options.
    - A test asserts a `skip` row does not appear in `build_enrichment_report(...)["failing_rows"]`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
  </acceptance_criteria>
  <reversibility rating="costly">Same persisted vocabulary as Task 1; the operator-facing words change on a second surface.</reversibility>
  <done>There is one action-to-outcome vocabulary in the client, both surfaces render the same word for the same backend action, and the four previously-`unknown` actions are covered by tests.</done>
</task>

<task type="auto">
  <name>Task 4: Carry row_id through Build Ingest Response and regenerate — no deploy in this plan</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_contact_ingest_cloud.json, tests/n8n/ingestResponseRowId.test.mjs</files>
  <read_first>
    - scripts/build_cloud_workflows.py (lines 465-520 — `BUILD_INGEST_RESPONSE`'s explicit field list, which is where a field not named dies; lines 1690-1715 — `Decide Action`'s returned json, which already emits `row_id: row.row_id ?? null`)
    - operator-claude-plugin/scripts/extraction.py (`strip_row_id` at 876-895 — the 2026-08-29 operator ruling that puts the strip at the CSV boundary, and its own note that carrying `row_id` beside the rows is the better end state but too wide a change)
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md (lines 628-650 — the step that calls `strip_row_id` before `write_dispatch_csv`)
    - CLAUDE.md section 13.0.1 (the ingest lane's association contract and the by-value join `Build Association Request` already does)
    - tests/n8n/ (an existing `*.test.mjs` for the assertion idiom against the generated workflow JSON)
  </read_first>
  <action>
Add `row_id: row.row_id ?? null` to the object `BUILD_INGEST_RESPONSE` returns, alongside the
existing `action`, `outcome`, `contact_id`, `hs_object_id`, `email`, `company_id`,
`company_match`, `association`, `reason` and `email_status`. This is required by AFTER-01 and
consumed by the entry field Task 2 added: the node's return is an EXPLICIT field list, so a
field not named there dies at that boundary, and `row.row_id` is available because `Decide
Action` already emits it (`build_cloud_workflows.py:1699`).

**What this closes, and what it does not — state both in the SUMMARY, not just here
(REVIEW-57-H7).** It closes the join for every lane whose rows carry `row_id` into the backend,
and for the enrichment lane, whose `Build Response` spreads the whole row. It does NOT close the
pair pipeline's FINAL ingest dispatch: `extraction.strip_row_id` removes the key before
`write_dispatch_csv` (`enrich-before-ingest/SKILL.md:639`), so the node will echo `null` for
those rows. Adding a field to the response cannot restore a value the request never carried.
Do not describe this task as closing AFTER-01's join. Restoring that leg needs either carrying
`row_id` beside the rows — rejected 2026-08-29 as too wide, and still out of scope here — or
joining by `email`, which this plan's own third prohibition forbids from reaching the durable
ledger. 57-05's report names the leg as unjoinable rather than implying otherwise.

Regenerate `n8n/wf_contact_ingest_cloud.json` with `scripts/build_cloud_workflows.py`. Never
hand-edit the JSON (CLAUDE.md sections 13.0.1 and 13.0.2).

Add `tests/n8n/ingestResponseRowId.test.mjs` asserting the generated ingest workflow's
`Build Ingest Response` node's jsCode names `row_id`, and that the node still names every one
of the fields it emitted before — a field list is exactly the shape that loses things silently.

**DO NOT DEPLOY.** The first pass deployed and bounced the regenerated workflow here; cross-AI
review flagged landing a live change before the phase's own safety proposition is complete. The
regenerated JSON is committed and pinned by test in this plan; deploying it is a task in 57-05,
behind the phase-gate checkpoint, alongside the first-live-run authorisation. Nothing in this
plan touches the live instance.
  </action>
  <verify>
    <automated>node --test tests/n8n/ingestResponseRowId.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "row_id" n8n/wf_contact_ingest_cloud.json` is at least 1.
    - `node --test tests/n8n/*.test.mjs` passes in full (glob form only — the directory form is broken on node 24).
    - `.venv/bin/python -m pytest tests -q -k workflow` passes (the repo's existing generated-JSON parity tests).
    - `git diff --stat n8n/wf_contact_ingest_cloud.json` shows the file changed, and `git diff n8n/wf_contact_ingest_cloud.json` contains no edit outside the `Build Ingest Response` node's jsCode.
    - `git diff --stat` for this task shows no change under `.planning/` and no deploy artifact — this task made no live call.
    - The SUMMARY states, in its own words, that the pair pipeline's final ingest leg still returns `row_id: null` because `strip_row_id` removes it, and that this is a known open gap rather than a defect introduced here.
  </acceptance_criteria>
  <reversibility rating="reversible">One additive field on a response object; removing it restores the previous shape exactly, and no persisted artifact depends on its presence (a missing `row_id` reads as None). No deployment, so nothing to roll back on the instance.</reversibility>
  <done>The ingest lane's per-row response carries `row_id` in the generated workflow, a node test pins it, the pair-pipeline join gap is named rather than claimed closed, and the live instance is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| backend response body to durable artifact | untrusted JSON crossing into a 0600 file on the operator's disk |
| plugin to n8n Cloud REST API | NOT CROSSED BY THIS PLAN after review — the deploy moved to 57-05's phase gate; this plan makes no live call at all |
| pre-write decision action to operator-facing outcome word | the boundary Task 1's checkpoint is about: what the backend DECIDED is not proof of what HubSpot DID |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-57-06 | Information disclosure | a secret or arming grant reaching disk via a widened entry | high | mitigate | `row_id` and `association` are added BEFORE the existing `_FORBIDDEN_NAME_MARKERS` scan, so both are checked exactly like every other field; `email` and every other PII field stay excluded by the module's own docstring rule |
| T-57-07 | Repudiation | a gated row reported as a completed one, so an operator believes a write landed | critical | mitigate | `gated` is its own word on both client surfaces, pinned by a cross-module agreement test over all ten action values |
| T-57-08 | Tampering | a hand-edited workflow JSON diverging from its builder | high | mitigate | Task 4 regenerates through `scripts/build_cloud_workflows.py` and an acceptance criterion asserts the diff touches only the one node |
| T-57-09 | Elevation of privilege | a redeploy that silently leaves live writes enabled | critical | mitigate | The redeploy is REMOVED from this plan (REVIEW-57) and deferred to 57-05's phase gate, where the read-back assertion that every write-safety flag reads false lives. An acceptance criterion here asserts this plan made no live call |
| T-57-09a | Repudiation | a row reported `written` on the strength of a pre-write decision plus a pre-known id, so an operator does nothing about a write that failed | critical | mitigate | Task 1's blocking checkpoint decides the word; whichever option is selected, the evidence it rests on is stated in the module docstring and rendered by 57-05, and no id is ever fabricated |
| T-57-09b | Repudiation | AFTER-01's join claimed complete while the pair pipeline's ingest leg returns `row_id: null` | high | mitigate | Named in `must_haves`, in the objective, in Task 4's action and in the required SUMMARY sentence; 57-05 marks those rows unjoinable and lists the leg in `gaps` |
| T-57-10 | Tampering | package-manager installs | low | accept | This plan installs no package in any ecosystem |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `.venv/bin/python -m pytest -q` green.
- `node --test tests/n8n/*.test.mjs` green.
- No live n8n call, no deploy, no armed window, no provider credit spent by anything in this plan.
</verification>

<success_criteria>
An operator reading either client report surface sees the same word for the same backend action,
a gated row is visibly recoverable rather than failed, skipped rows read as successes, and every
row — including a held one with no HubSpot id — carries the key the end-of-run report will join
on.
</success_criteria>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):

- `written_records.GATED`, `written_records.HELD`, `written_records.FAILED`,
  `written_records.NO_ACTION`, `written_records.WRITTEN_ID_UNKNOWN`, and — if Task 1's checkpoint
  selects option-b or option-c — `written_records.WRITE_ATTEMPTED`
- `written_records.ALL_OUTCOMES`, `written_records.ACTION_TO_OUTCOME`
- `written_records.outcome_for_action(action, hs_object_id=None)` — the pure, total, never-raising
  single vocabulary both client readers resolve through
- the `row_id` and `association` fields on every `written_records.classify_item` entry
- `row_id` on `BUILD_INGEST_RESPONSE`'s returned json in `scripts/build_cloud_workflows.py`
- `tests/n8n/ingestResponseRowId.test.mjs`
- if Task 1 selects option-b or option-c: the amendment to D-57-03's table in
  `.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-DISCUSSION-LOG.md`

Removed by this plan: `report_enrichment._ACTION_TO_OUTCOME` (folded into the one vocabulary).

NOT produced by this plan, contrary to the first pass: any deployed change. The regenerated
`n8n/wf_contact_ingest_cloud.json` is committed here and DEPLOYED in 57-05, behind the phase
gate.

Created elsewhere in this phase: `write_grant.allowance_headroom` / `ceiling_verdict` /
`record_dispatch_outcome` / `CLOSED_UNHANDLED_ERROR` / `split_for_allowance`,
`chunking.CeilingStop` / `projected_spend` / the generalised `failed_batch`,
`n8n_read.executions_in_window`'s `max_pages` and `listing_exhausted`,
`preingest.MergeResult.dispatch_outcome` (57-01, 57-03); `remainder_queue.py` and its whole
surface (57-03); `scripts/prove_zoominfo_balance.py` and `ALLOW_ZOOMINFO_BALANCE_PROBE` (57-04);
`operator-claude-plugin/scripts/run_report.py` with `build_run_report` and `record_audit`,
`written_records.classify_read` and `run_manifest.classify_read` (57-05). Plan 57-01 carries the
same list in full and the phase's requirement-coverage map.

## Flagged Assumptions (specless probe fallback)

No SPEC file exists; `## Edge Coverage` and `## Prohibitions` are absent. All four probe rows
came back `unclassified` and therefore `unresolved`. Carried by this plan:

| # | Requirement | Probe status | Disposition here |
|---|---|---|---|
| 3 | AFTER-03 | unclassified / unresolved | Edges derived by hand into `must_haves.truths` above: a create whose response carried no id, a write action whose response carried no id, an unrecognised eleventh action, an entry written before the widening. Assumption: `report_enrichment`'s 4-of-10 gap is a genuine defect, not a deliberate boundary pinned elsewhere (RESEARCH Assumption A2) — the executor confirms by reading `test_report_enrichment.py` before deleting the table. |
| 1, 2, 4 | RUN-05, AFTER-01, G-4 | unclassified / unresolved | Carried by plans 57-01, 57-05, 57-04 respectively; listed in full in 57-01. |

<output>
Create `.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-02-SUMMARY.md` when done
</output>
