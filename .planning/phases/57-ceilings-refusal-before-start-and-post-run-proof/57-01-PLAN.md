---
phase: 57-ceilings-refusal-before-start-and-post-run-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/chunking.py
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_chunking.py
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A batch whose projected executions exceed the sampled remaining monthly allowance is refused by `write_grant.plan_grant()` BEFORE anything is armed or sent, and the refusal carries the arithmetic (projection, sampled spend, remaining, shortfall)."
    - "A ceiling verdict of `unknown` — an unconfigured allowance, an unsampleable executions list, or a truncated sample — does NOT refuse; it proceeds and names the blind spot (D-57-02: a guard that always fires is indistinguishable from a feature that is off)."
    - "`chunking.dispatch_plan` given an `execution_ceiling` stops sending once the running projected tally reaches it, records the never-attempted chunks on a NEW `DispatchOutcome.ceiling_stop` field, and returns normally — the run completes rather than aborting (D-57-01)."
    - "A dispatch that stopped on the ceiling closes the grant through `write_grant.record_dispatch_outcome`, and the resulting grant's close reason is `write_grant.CLOSED_CEILING_BREACH` — reached from a real `dispatch_plan()` call, not from a hand-built `DispatchOutcome` (D-57-01, Pitfall 1)."
    - "A ceiling stop is never reported as a chunk failure: the stopped run's `failed_batch` and every `ChunkResult.ok` are unchanged by the stop (Pitfall 5)."
    - "`dispatch_plan` called without `execution_ceiling` sends the byte-identical envelope and produces a `DispatchOutcome` whose every pre-existing field is unchanged, with `ceiling_stop` None."
    - statement: "The month-to-date sample is read through `n8n_read.executions_in_window` and its `covers_full_window`/`truncated_by_page_cap` tri-state is carried into the verdict, never discarded (Pitfall 4)."
      verification: backstop
  artifacts:
    - operator-claude-plugin/scripts/write_grant.py
    - operator-claude-plugin/scripts/chunking.py
    - operator-claude-plugin/tests/test_write_grant.py
    - operator-claude-plugin/tests/test_chunking.py
  key_links:
    - "`plan_grant()` -> `allowance_headroom()` -> `n8n_read.executions_in_window` — the sample RUN-05's refusal rests on; break it and the refusal silently degrades to `unknown` forever."
    - "`dispatch_plan()` -> `DispatchOutcome.ceiling_stop` -> `write_grant.record_dispatch_outcome()` -> `record_send_outcome()` — the producer chain for a consumer that has had zero production callers since it was written."
    - "Both lane SKILL.md dispatch steps -> `record_dispatch_outcome` — the production call site; without it the chain above exists only in tests."
  prohibitions:
    - statement: "No task in this plan may arm a live write, open an armed window against the real instance, or spend a provider credit."
      status: flagged-unverified
      verification: none
    - statement: "A truncated or non-covering `executions_in_window` sample must never be read as though it were a complete month-to-date count."
      status: flagged-unverified
      verification: none
    - statement: "A deliberate ceiling stop must never be represented by adding chunks to `failed_batch` or by setting `ChunkResult.ok = False`."
      status: flagged-unverified
      verification: none
    - statement: "No parameter added to `chunking.dispatch_plan` may be named `grant`."
      status: flagged-unverified
      verification: none
---

<objective>
Make the ceiling refuse. Today `write_grant.py` says so in its own words, and this plan is
what overturns it — D-53-02, at `write_grant.py:113-154`, states that the envelope's figures
"describe what this batch can cost" but that "the ceiling is computed FROM the batch you
named, so it cannot block anything that batch already implies," and names the fix as
"Phase 57's". This is that phase.

This is the phase's TRACER: one over-ceiling batch travelling the whole vertical — sample the
month-to-date spend, compute a verdict, refuse before starting, and (for a batch that does
start) stop mid-run on a running tally, close the grant with `ceiling_breach`, and return a
completed run. Every later plan expands out from this proven slice.

Purpose: RUN-05, and the producer that `write_grant.record_send_outcome`'s `ceiling_breach`
branch has been waiting for since it was written. Verified this session:
`record_send_outcome(` has ZERO production callers — the only invocation outside
`write_grant.py` itself is `test_write_grant.py:1507`.

Output: a pre-flight refusal with arithmetic, a mid-run stop that is not a failure, and a
grant that closes for the right reason from a real dispatch.

## Decisions taken here (Claude's Discretion, per CONTEXT.md)

**Where the ceiling check lives: the CLIENT, at both ends, and nowhere else.** No backend
change, no workflow JSON regeneration, no deploy. Evidence: RESEARCH's Architectural
Responsibility Map — the backend has no concept of "this batch's ceiling" and no cross-chunk
state, while `chunking.dispatch_plan` is the only place in the system that sees every chunk of
one run in sequence.

**How a breach is detected: a running tally inside `dispatch_plan`'s existing loop**, checked
at each chunk boundary. Rejected: post-chunk reconciliation between separate `dispatch_plan`
calls, which would require restructuring `preingest.py:713`, `scheduled_arm.py:234` and both
lane SKILL.md runbooks for a benefit the tally already gets. Also considered and rejected: a
re-sample of `executions_in_window` per chunk — at `max_records_per_chunk` of 2 that is ~170
extra cross-workflow GETs on a 340-record batch, against a sample that is laggy and
instance-wide. Maximum overshoot of the tally is therefore exactly one chunk.

**When the mid-run breach is actually reachable — state this plainly, it is not a defect.**
Within a single `dispatch_plan` call under an un-overridden grant the tally cannot diverge
from the pre-flight projection, because both are computed from the same fixed plan. The breach
is reachable when the budget is consumed across MORE than that one projection: (a) the operator
overrode the pre-flight refusal, (b) several `dispatch_plan` calls run under one grant — the
pair pipeline's match, enrich and ingest passes, and `preingest.rerequest_unanswered`'s
re-request, (c) the allowance was `unknown` at grant open and became sampleable later, (d) a
remainder run resumes against an allowance a previous run has since eaten into. The ceiling
handed to each call is therefore the grant's remaining allowance MINUS what this grant has
already spent, not this call's own projection.

**The projection over-states, deliberately, and that is disclosed.** `EXECUTIONS_BASIS`
(`chunk_count + record_count`) was measured by P-10 to over-state a real 2-record chunk by
roughly 3x (projected 3, the executions list showed 1), and nothing found suggests it ever
under-projects. Used as a refusal trigger, over-stating can only refuse too early — never let
an over-budget batch through. That direction is chosen on purpose and is named in the
operator-facing arithmetic, not left implicit.

**Evidence tags carried, never upgraded** (CLAUDE.md §13.0.3): the Starter plan's 2,500
executions/month is `[documented]`; the over-statement factor is `[measured]` from execution
`11950`; the executions API list is NOT the billing quota, so every figure here is what the
API *listed*.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-CONTEXT.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-RESEARCH.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-PATTERNS.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One over-ceiling batch, end to end — refused before start, stopped mid-run, grant closed</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/scripts/chunking.py, operator-claude-plugin/tests/test_write_grant.py, operator-claude-plugin/tests/test_chunking.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py (lines 111-345 for the envelope and the D-53-02 text; 411-520 for `plan_grant`'s frozen call order; 660-705 for the five close reasons; 899-949 for `record_send_outcome`)
    - operator-claude-plugin/scripts/chunking.py (lines 87-155 for `ChunkResult`/`DispatchOutcome`; 317-451 for the dispatch loop; 494-517 for `failed_batch`)
    - operator-claude-plugin/scripts/n8n_read.py (lines 62-76 for `EXECUTION_ALLOWANCE_KEY`, `EXECUTIONS_WINDOW_PAGE_LIMIT`, `MAX_EXECUTION_PAGES`; 258-382 for `executions_in_window`'s full return shape)
    - operator-claude-plugin/scripts/n8n_cadence.py (lines 452-491 — `check_budget_floor`, the refuse-with-arithmetic analog)
    - operator-claude-plugin/scripts/run_state.py (lines 168-181 for `SPEND_BASIS`; 368-378 for `spend_against_ceiling`)
    - operator-claude-plugin/tests/test_write_grant.py (lines 1440-1533 — the pinned no-`grant`-parameter test and the existing ceiling-breach unit test)
    - operator-claude-plugin/tests/conftest.py (the `stub_module_transport_factory`, `no_network` and `no_durable_writes` fixtures every plugin test runs under)
  </read_first>
  <behavior>
    Write these tests FIRST, in this order, and watch each fail before implementing.

    - Characterization (green today, must stay green): a 3-chunk `chunking.dispatch_plan()`
      call with no `execution_ceiling` attempts all 3 chunks, and `outcome.ceiling_stop` is
      None. This is the known baseline the later stop is legible against.
    - `write_grant.allowance_headroom(config, transport=...)` against a stubbed
      `executions_in_window` returning `covers_full_window: True`: reports
      `remaining_sampled == allowance - count_in_window` and `sampled: True`.
    - Same, with `covers_full_window: False` / `truncated_by_page_cap: True`: reports
      `sampled: False` and a `reason` naming the truncation. `remaining_sampled` is None,
      never a number derived from a partial count.
    - Same, with `EXECUTION_ALLOWANCE_KEY` absent from config: `allowance` None,
      `sampled: False`, `reason` names the missing key.
    - `write_grant.ceiling_verdict(figures, headroom)` is pure and returns `CEILING_OVER`
      when `projected_executions > remaining_sampled`, `CEILING_OK` when it is not, and
      `CEILING_UNKNOWN` whenever `headroom["sampled"]` is False.
    - `write_grant.plan_grant()` — the REAL function an operator's request reaches — over a
      record set whose projection exceeds the sampled remainder returns
      `outcome == write_grant.REFUSED`, carries `ceiling` with the four numbers, and carries
      `envelope`. Nothing is armed: the stubbed transport records no arming call.
    - `plan_grant()` with `override=True` over the SAME record set returns a proposal
      (`kind == PROPOSAL_KIND`) whose `ceiling` carries `overridden: True`.
    - `plan_grant()` with an unconfigured allowance returns a proposal, NOT a refusal, and
      its `ceiling["verdict"]` is `CEILING_UNKNOWN`.
    - Mid-run, driving the real loop: a 3-chunk `dispatch_plan(..., execution_ceiling=N)`
      with `stub_module_transport_factory` (mirror
      `test_a_revocation_midway_does_not_stop_a_running_dispatch`'s 3-chunk idiom) where N is
      reached after chunk 2 — the transport is called for chunks 0 and 1 and NOT for chunk 2;
      `outcome.ceiling_stop.chunk_index == 1`; `ceiling_stop.unsent_chunks` is the third
      chunk; `ceiling_stop.remainder` is a `failed_batch`-shaped spec naming exactly the
      unsent records; `outcome.failed_batch` is None; every `ChunkResult.ok` is True.
    - The producer, from that same real dispatch: passing that outcome to
      `write_grant.record_dispatch_outcome(grant, outcome, config)` returns a grant whose
      close reason is `write_grant.CLOSED_CEILING_BREACH`. Assert `record_send_outcome` was
      REACHED as a consequence of the dispatch outcome, not called directly with a
      hand-built dict.
    - `record_dispatch_outcome` with an outcome whose `ceiling_stop` is None leaves the
      grant open.
  </behavior>
  <action>
In `write_grant.py`, add three names and one adapter, all implementing RUN-05 and D-57-01.

`allowance_headroom(config, *, transport=None, now=None) -> dict` — samples this calendar
month to date by calling `n8n_read.executions_in_window(config, transport=..., now=now,
window_hours=<hours elapsed since 00:00 UTC on the 1st of `now`'s month>)`. Returns keys
`allowance`, `spent_sampled`, `remaining_sampled`, `covers_full_window`,
`truncated_by_page_cap`, `observed_span_hours`, `sampled`, `reason`. `sampled` is True only
when the allowance key is a positive int AND `executions_in_window` returned a dict AND
`covers_full_window` is True AND `truncated_by_page_cap` is False; in every other case
`sampled` is False, `remaining_sampled` is None, and `reason` names which of those failed.
Never compute a remainder from a partial count (Pitfall 4). Cite the page cap honestly:
`MAX_EXECUTION_PAGES` x `EXECUTIONS_WINDOW_PAGE_LIMIT` is 1,000 executions across ALL
workflows on the instance, so a busy month can truncate.

`ceiling_verdict(figures, headroom) -> dict` — pure, no I/O. Constants `CEILING_OK = "ok"`,
`CEILING_OVER = "over"`, `CEILING_UNKNOWN = "unknown"`. Returns `verdict`,
`projected_executions` (from `figures["projected_executions"]`), `allowance`,
`spent_sampled`, `remaining_sampled`, `shortfall` (projection minus remainder, or None),
`basis`, `reason`. `CEILING_UNKNOWN` whenever `headroom["sampled"]` is False or
`figures["projected_executions"]` is None. `CEILING_OVER` only when both are real numbers and
the projection strictly exceeds the remainder.

`plan_grant(...)` gains a keyword-only `override=False`. AFTER the existing envelope
computation and BEFORE guardrail A's `preflight` call, compute the headroom and the verdict,
attach the verdict to the returned proposal under key `ceiling`, and — when the verdict is
`CEILING_OVER` and `override` is falsey — return `_refusal(...)` carrying `envelope=figures`
and `ceiling=<verdict>`. When `override` is truthy on a `CEILING_OVER` verdict, proceed and set
`overridden: True` on the attached ceiling dict, mirroring `n8n_cadence.check_budget_floor`'s
own override key. The refusal text states the four numbers, names the over-stating bias as
deliberate, and says the batch can be reduced or the refusal overridden.

DELIBERATE DIVERGENCE FROM THE ANALOG, RECORD IT IN A COMMENT AT THE BRANCH: `check_budget_floor`
refuses FIRST and unconditionally on a missing config key. This function does NOT. A
`CEILING_UNKNOWN` verdict proceeds with the blind spot disclosed, per D-57-02 — two of three
provider balances already read `unknown` and refusing on unknown would block essentially every
run today, which makes the guard indistinguishable from the feature being switched off. Only
`CEILING_OVER` refuses. `envelope()`'s own established contract for a missing allowance key
("one missing line, not a reason to refuse") is preserved by this choice, not contradicted.

`record_dispatch_outcome(grant, outcome, config=None, *, disarm=None, transport=None)` — the
adapter that turns a `chunking.DispatchOutcome` into the `record_send_outcome` call D-57-01
requires. Builds `{"ceiling_breach": outcome.ceiling_stop is not None, "disarm": disarm}` and
delegates to the EXISTING `record_send_outcome`. It adds no second close path and no second
outcome vocabulary; it exists so the producer is reachable from a pytest-driven dispatch
rather than only from SKILL.md prose (Pitfall 1). Do not put the `ceiling_breach` computation
in the skills.

In `chunking.py`, add the mid-run stop.

`dispatch_plan` gains a keyword-only `execution_ceiling=None` — NEVER a parameter named
`grant`, which `test_write_grant.py:1455-1463` pins as unavailable for an unrelated reason
(GRANT-05's chunk-granularity limit) and whose failure would be confusing to debug. Default
None means today's behaviour, byte-identical envelope, byte-identical `DispatchOutcome` with
`ceiling_stop` None; `preingest.py:713` and `scheduled_arm.py:234` therefore need no change at
all and must not be edited.

Add `@dataclass(frozen=True) class CeilingStop` with fields `chunk_index`,
`projected_executions`, `execution_ceiling`, `unsent_chunks` (tuple), `remainder` (dict or
None), `reason` (str). Add `ceiling_stop: CeilingStop = None` to `DispatchOutcome`, defaulting
None so every existing caller branches on presence exactly as it already does for
`failed_batch`. Document on the dataclass, mirroring `written_records_failures`' own D-59-10
precedent, that this is a SEPARATE field on purpose: a deliberate budget stop is categorically
not a recovered-from failure, so it must never flip `ChunkResult.ok`, never add a chunk to
`failed_chunks`, and never appear in `failed_batch` (Pitfall 5).

The tally: inside the existing `for index, chunk in enumerate(plan.chunks)` loop, immediately
after the `append_chunk` guard block and before the `if reason is not None` line, accumulate
`sent_rows` from each chunk's `plan.row_counts[index]` when it is an int, and compute
`projected = (index + 1) + sent_rows` — the SAME `write_grant.EXECUTIONS_BASIS` formula
`run_state.spend_against_ceiling` already reuses, never a second one. When `execution_ceiling`
is an int, `projected >= execution_ceiling`, and unsent chunks remain, build a `CeilingStop`
whose `unsent_chunks` is `tuple(plan.chunks[index + 1:])` and whose `remainder` is
`failed_batch(list(plan.chunks[index + 1:]))`, then `break`. Build `DispatchOutcome` from the
already-accumulated lists exactly as the natural-completion path does.

The one case the tally cannot compute: a backend-resolved list spec, where `plan.row_counts`
is `chunking.UNKNOWN`. That plan is a single chunk by construction, so there is nothing to stop
mid-run — skip the tally and leave `ceiling_stop` None. State this in the docstring rather than
guessing a record count.

Update `dispatch_plan`'s docstring: its "stays deliberately GRANT-UNAWARE" paragraph is about
REVOCATION (D-59-06/GRANT-05 — revocation bites at the next send, not mid-run) and stays true.
Say that a budget stop is a different kind of early exit, that it consults a number and never a
grant object, and that the grant-close it enables happens in the CALLER through
`write_grant.record_dispatch_outcome`, not here.

Add `projected_spend(outcome) -> int` to `chunking.py` — the chunks attempted plus the rows in
them, read off a `DispatchOutcome`, so a caller running several dispatches under one grant can
decrement its remaining allowance without re-deriving the formula.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_chunking.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_chunking.py -q` passes, including every test named in `<behavior>`.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import inspect, chunking; p=inspect.signature(chunking.dispatch_plan).parameters; assert 'execution_ceiling' in p and 'grant' not in p"` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import write_grant as w; assert w.CEILING_OK=='ok' and w.CEILING_OVER=='over' and w.CEILING_UNKNOWN=='unknown'"` exits 0.
    - `grep -c "record_send_outcome" operator-claude-plugin/scripts/write_grant.py` is at least 3 (definition, the internal breach branch, and the adapter's delegation).
    - `grep -c "execution_ceiling" operator-claude-plugin/scripts/preingest.py operator-claude-plugin/scripts/scheduled_arm.py` reports 0 for both files — neither existing caller was edited.
    - The mid-run test asserts the stubbed transport received exactly 2 POSTs for a 3-chunk plan, and that `outcome.failed_batch is None`.
    - The producer test obtains its `DispatchOutcome` from a real `chunking.dispatch_plan()` call and asserts the closed grant's reason equals `write_grant.CLOSED_CEILING_BREACH`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes (no regression in the 1701-test plugin baseline).
  </acceptance_criteria>
  <reversibility rating="costly">Implements D-57-01. The breach-hold shape is shared with D-61-07's confidence holds and 61-06's no-company holds; changing it later means re-deciding what a held row means in three producers and re-cutting the review queue's reason vocabulary.</reversibility>
  <done>An over-ceiling `plan_grant()` refuses with arithmetic and arms nothing; an under-ceilinged real `dispatch_plan()` stops after the breaching chunk, completes, and its outcome closes the grant with `ceiling_breach`; a call with no ceiling behaves exactly as it does today.</done>
</task>

<task type="auto">
  <name>Task 2: Overturn the disclosure-not-constraint block and stop hardcoding the sample as absent</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py (lines 111-155 for `EXECUTIONS_BASIS`, `_ALLOWANCE_GAP` and `_DISCLOSURE_NOT_CONSTRAINT`; 183-345 for `envelope()` and `_envelope_block()`, including the hardcoded `remaining_allowance_sampled` at line 261 and the `_headroom()` tri-state renderer at 167-174)
    - operator-claude-plugin/tests/test_write_grant.py (the existing envelope-block assertions — several pin the current sentences and must be updated in this same edit, not left to fail)
    - CLAUDE.md section 13.0.3 (the evidence tags this block must carry)
  </read_first>
  <action>
Rewrite the two module-level disclosure strings and the block they feed, per RUN-05 and D-57-02.

`_ALLOWANCE_GAP` currently states that the remainder is sampled rather than read and that the
sampling is unbuilt. Task 1 built it. Replace this constant with `_ALLOWANCE_SAMPLED`, whose
text says the remainder IS now sampled from the executions list, states that the executions API
list is not the billing quota (CLAUDE.md section 13.0.3, tagged as documented, not verified
against billing), and names the 1,000-execution page cap across all workflows as the reason a
sample can come back incomplete.

`_DISCLOSURE_NOT_CONSTRAINT` is D-53-02's text and this plan overturns it. Replace the constant
with `_CEILING_CONSTRAINT`, whose text says the projection is now compared against the sampled
remaining monthly allowance and that a batch exceeding it is refused before anything is armed;
that the comparison uses a formula measured to over-state (so it refuses early rather than
late, on purpose); and that when the remainder cannot be sampled the run proceeds with the gap
named rather than being blocked. Add a code comment at the constant recording that this
supersedes D-53-02 as of Phase 57, naming the decision it replaces and why an operator ruling is
being reversed rather than quietly contradicted.

In `envelope()`, add a keyword-only `headroom=None`. When None, call
`allowance_headroom(config, transport=transport)`; a caller that already has one (`plan_grant`
does) passes it in so the executions list is walked ONCE per grant, not twice. Add
`spent_sampled`, `remaining_sampled`, `sample_covers_full_window`,
`sample_truncated_by_page_cap` and `ceiling` (the `ceiling_verdict` result) to `figures`. Add
`"remaining_allowance": MEASURED if headroom["sampled"] else UNCONFIGURED` to `figures["basis"]`,
reusing the module's existing three basis labels rather than inventing a fourth.

In `_envelope_block()`, render one new line after the existing allowance line: the verdict, the
sampled spend, the remainder and the shortfall when over. When `headroom["sampled"]` is False,
render the blind spot in the same register `_headroom()` already uses for an unreadable provider
balance — `unconfirmed` is its own answer and must never read as headroom.

The hardcoded false literal on the `remaining_allowance_sampled` key at line 261 is the one
place `envelope()` currently asserts the sample does not exist; it must now carry
`headroom["sampled"]`.

Update every existing test in `test_write_grant.py` that pins the old sentences, and add tests
that the block names the verdict in all three states.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "do not prevent it" operator-claude-plugin/scripts/write_grant.py` returns 0.
    - `grep -c "not built yet" operator-claude-plugin/scripts/write_grant.py` returns 0.
    - `grep -c "_CEILING_CONSTRAINT" operator-claude-plugin/scripts/write_grant.py` is at least 2 (definition and use).
    - `grep -c "_ALLOWANCE_SAMPLED" operator-claude-plugin/scripts/write_grant.py` is at least 2 (definition and use).
    - A test asserts `envelope(...)["remaining_allowance_sampled"]` is True for a stubbed full-coverage sample and False for a truncated one.
    - A test asserts the rendered `block` contains the shortfall number when the verdict is `over`, and names the blind spot when the verdict is `unknown`.
    - A test asserts `plan_grant()` walks the executions list exactly once (the stubbed transport records one `/executions` GET sequence, not two).
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Text and figure plumbing only; no persisted shape depends on it.</reversibility>
  <done>The envelope block states the ceiling as a constraint with real sampled numbers, the D-53-02 sentences are gone from the source, and `remaining_allowance_sampled` reports the truth.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the ceiling and the breach into both lane runbooks</name>
  <files>operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/skills/enrich-records/SKILL.md (lines 275-345 — the dispatch step's code block and the report step that follows it)
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md (lines 355-400 — the same dispatch step, which additionally passes `run_id` and `async_ack=True`)
    - operator-claude-plugin/scripts/chunking.py (the `execution_ceiling` and `projected_spend` added in Task 1)
    - operator-claude-plugin/scripts/write_grant.py (`record_dispatch_outcome` added in Task 1)
  </read_first>
  <action>
Edit the dispatch step in BOTH lane runbooks so the ceiling reaches production, per RUN-05 and
D-57-01. Keep each edit inside the existing dispatch code block and the paragraph under it — do
not restructure the step.

Pass `execution_ceiling` to `chunking.dispatch_plan`. Its value is the grant's remaining
allowance at open MINUS what this grant has already spent across earlier dispatches in the same
session, computed with `chunking.projected_spend(<each earlier outcome>)`. The
`enrich-before-ingest` lane is the one that matters here: it runs a match pass and an enrich
pass under ONE grant, so a within-call projection alone would never see the second pass. Where
the grant's ceiling is None (verdict `unknown`), pass None — the run proceeds unbounded with the
blind spot already disclosed at grant time (D-57-02).

After the dispatch, call `write_grant.record_dispatch_outcome(grant, outcome, cfg)` and replace
the local grant handle with what it returns. When `outcome.ceiling_stop` is present, the runbook
tells the operator four things: spending stopped at the named chunk, the run completed rather
than aborting, the unsent rows are named individually and are not lost, and the grant is now
closed for `ceiling_breach` so a further send needs a fresh grant. Do not report a ceiling stop
as a chunk failure — it does not appear in `outcome.failed_batch` and every attempted chunk's
`ok` is still True.

Add a structural test in `test_write_grant.py` that reads both SKILL.md files and asserts each
contains `record_dispatch_outcome` and `execution_ceiling`, so the runbook cannot drift away
from the functions it names.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "record_dispatch_outcome" operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1.
    - `grep -c "record_dispatch_outcome" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - `grep -c "execution_ceiling" operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1.
    - `grep -c "execution_ceiling" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - `grep -c "projected_spend" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - The new structural test fails if either identifier is removed from either SKILL.md.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Runbook prose plus one structural test.</reversibility>
  <done>Both lanes pass a decrementing ceiling into every dispatch under a grant and route the outcome through the breach adapter; a test pins the runbooks to the functions they name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator conversation to grant object | the only place write authority is created; it is in-memory and never persisted (GRANT-06) |
| plugin to n8n Cloud REST API | the executions list this plan's arithmetic is built on; untrusted, laggy, cross-workflow, and not the billing quota |
| `dispatch_plan` loop to the deployed backend | the only place this plan can cause spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-57-01 | Denial of Service (of the monthly allowance) | `ceiling_verdict` false negative lets an over-budget batch through | high | mitigate | Prefer the conservative direction the `EXECUTIONS_BASIS` formula already has (measured to over-state, never under-state); a truncated sample yields `CEILING_UNKNOWN` and never a number derived from a partial count |
| T-57-02 | Repudiation | a ceiling stop reported identically to an ordinary chunk failure hides the real cause | high | mitigate | `DispatchOutcome.ceiling_stop` is its own field; the stop never sets `ChunkResult.ok = False` and never enters `failed_batch`; both runbooks say "budget stop", not "failure" |
| T-57-03 | Elevation of privilege | a ceiling parameter that could widen what a dispatch may write | critical | mitigate | `execution_ceiling` is an int and can only stop sending; the record-scoped allowlist (`covers()`) and `armed_window`'s per-send narrowing are untouched by this plan |
| T-57-04 | Information disclosure | a config value or webhook secret echoed into a refusal message | medium | mitigate | The refusal text is composed from the operator's own record spec and integers only, never from a transport response or a config value (T-25-17's rule, already enforced by `ChunkResult`'s own docstring) |
| T-57-05 | Tampering | package-manager installs | low | accept | This plan installs no package in any ecosystem; RESEARCH's Package Legitimacy Audit is not applicable |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `.venv/bin/python -m pytest -q` green (root baseline 3539 passed / 154 skipped).
- `node --test tests/n8n/*.test.mjs` green (glob form only — the directory form is broken on node 24).
- No live n8n call, no armed window, no provider credit spent by anything in this plan.
</verification>

<success_criteria>
An over-ceiling grant is refused before start with its arithmetic; an unknown ceiling proceeds
with the blind spot named; a real multi-chunk dispatch under a ceiling stops sending, completes,
and closes its grant with `ceiling_breach` through a path a pytest run actually drives.
</success_criteria>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification — these symbols do not exist yet):

- `write_grant.allowance_headroom(config, *, transport=None, now=None)`
- `write_grant.ceiling_verdict(figures, headroom)`
- `write_grant.CEILING_OK`, `write_grant.CEILING_OVER`, `write_grant.CEILING_UNKNOWN`
- `write_grant.record_dispatch_outcome(grant, outcome, config=None, *, disarm=None, transport=None)`
- `write_grant._CEILING_CONSTRAINT`, `write_grant._ALLOWANCE_SAMPLED` (replacing `_DISCLOSURE_NOT_CONSTRAINT` and `_ALLOWANCE_GAP`)
- `write_grant.plan_grant`'s new keyword-only `override` parameter and the `ceiling` key on both its proposal and its refusal
- `write_grant.envelope`'s new keyword-only `headroom` parameter and the `spent_sampled`, `remaining_sampled`, `sample_covers_full_window`, `sample_truncated_by_page_cap`, `ceiling` figures
- `chunking.CeilingStop` (dataclass: `chunk_index`, `projected_executions`, `execution_ceiling`, `unsent_chunks`, `remainder`, `reason`)
- `chunking.DispatchOutcome.ceiling_stop` (new field, default None)
- `chunking.dispatch_plan`'s new keyword-only `execution_ceiling` parameter
- `chunking.projected_spend(outcome)`

Created by later plans in this phase (named here so the whole phase's new-symbol set is in one
place): `written_records.GATED`/`HELD`/`FAILED`/`NO_ACTION`/`WRITTEN_ID_UNKNOWN` and the
`row_id`/`association` entry fields (57-02); `remainder_queue.py` and its
`build_entry`/`save`/`load`/`remainder_path`/`REASON_CEILING_BREACH`/`REASON_ALLOWANCE_SPLIT`
plus `write_grant.split_for_allowance` (57-03); `scripts/prove_zoominfo_balance.py` and its
`ALLOW_ZOOMINFO_BALANCE_PROBE` gate (57-04); `operator-claude-plugin/scripts/run_report.py` and
its `build_run_report` (57-05).

## Flagged Assumptions (specless probe fallback)

No SPEC file exists for this phase, so `## Edge Coverage` and `## Prohibitions` are absent. The
deterministic edge probe classified all four requirement rows as `unclassified` and therefore
`unresolved`. They are surfaced here, never dropped, and the concrete edges are derived by hand
into `must_haves.truths` above.

| # | Requirement | Probe status | Disposition in this phase |
|---|---|---|---|
| 1 | RUN-05 | unclassified / unresolved | Edges derived by hand: an exhausted allowance (this plan, truth 1), an unsampleable allowance (truth 2), a mid-run breach (truths 3-5). Assumption: the projection never under-states — sourced from P-10's single measured execution `11950`, not from a general law. |
| 2 | AFTER-01 | unclassified / unresolved | Edges derived in plan 57-05. Assumption: `row_id` is a sufficient join key across all four stores once 57-02 lands it on both lanes. |
| 3 | AFTER-03 | unclassified / unresolved | Edges derived in plan 57-02. Assumption: `report_enrichment.py`'s 4-of-10 vocabulary gap is a genuine defect, not a deliberate boundary with a test elsewhere (RESEARCH Assumption A2). |
| 4 | G-4 | unclassified / unresolved | Edges derived in plans 57-04 and 57-05. Assumption: the ZoomInfo `provider_error` cause is not the already-fixed missing `Accept` header, and can only be established by a live re-probe (RESEARCH Assumption A3). |

Counts balance: 4 probe-surfaced rows = 4 flagged assumptions + 7 hand-derived truths authored
into `must_haves` across the phase. Zero silent drops.

<output>
Create `.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-01-SUMMARY.md` when done
</output>
