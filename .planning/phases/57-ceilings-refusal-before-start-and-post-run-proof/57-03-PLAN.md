---
phase: 57-ceilings-refusal-before-start-and-post-run-proof
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - operator-claude-plugin/scripts/remainder_queue.py
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/chunking.py
  - operator-claude-plugin/tests/test_remainder_queue.py
  - operator-claude-plugin/tests/test_write_grant.py
autonomous: false
requirements: [RUN-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A refusal from `plan_grant()` on a `CEILING_OVER` verdict carries a concrete smaller batch: the largest record subset that fits the sampled remaining allowance, plus the remainder — RUN-05's 'offers a smaller batch'."
    - "When the remaining allowance cannot be sampled there is no split arithmetic, so no split is offered; the run proceeds with the blind spot named (D-57-02) and the operator may name a smaller batch by hand."
    - "The queued remainder holds WORK only — record ids, domains or rows in `chunking.failed_batch()`'s shape — and never a grant, an arming token, an allowlist or any other authority (D-57-05, GRANT-06)."
    - "A remainder entry whose spec carries a value or key resembling an arming grant, a secret, an API key, a token, a credential, a password, a permission or a webhook is refused rather than persisted, by this module's own copy of the forbidden-name markers."
    - "Resuming against a queued remainder with no grant open refuses exactly as any other ungranted send refuses today — the queue confers nothing."
    - "A mid-run ceiling stop writes its unsent rows to the remainder queue with reason `ceiling_breach`, so the run completes with those rows named and recoverable rather than lost (D-57-01)."
    - "The remainder store is one file per `run_id` (`remainder_queue-<run_id>.json`), read by globbing and unioning, mirroring `written_records`' D-59-09 shape — never one shared file that two concurrent writers race over."
  artifacts:
    - operator-claude-plugin/scripts/remainder_queue.py
    - operator-claude-plugin/tests/test_remainder_queue.py
  key_links:
    - "`chunking.CeilingStop.remainder` -> `remainder_queue.build_entry` -> `remainder_queue.save` — the durable half of D-57-01's 'the run completes and the operator gets one review pass'."
    - "`write_grant.split_for_allowance` -> the refusal payload -> the operator's next grant. Each split run opens its OWN grant; the queue carries no authority across that boundary (D-57-05)."
  prohibitions:
    - statement: "The remainder store must never persist a grant object, an arming token, an allowlist, or any value that would let a later run write without a fresh operator yes."
      status: flagged-unverified
      verification: none
    - statement: "The remainder store must not import another module's forbidden-name marker tuple; it carries its own copy, per this codebase's deliberate anti-DRY convention."
      status: flagged-unverified
      verification: none
    - statement: "Auto-split must never present a schedule of future runs as though the whole batch were already authorised."
      status: flagged-unverified
      verification: none
    - statement: "No task in this plan may arm a live write or spend a provider credit."
      status: flagged-unverified
      verification: none
---

<objective>
RUN-05 does not stop at refusing. It says the run "refuses before starting, with the
arithmetic, and offers a smaller batch," and D-57-04 makes that offer concrete: a batch that
would exhaust the allowance is split into affordable runs and the remainder is queued, so the
whole batch completes over time without the operator re-deciding each round.

D-57-05 is what keeps that honest, and it is a hard constraint rather than a preference:
**auto-split queues WORK, never AUTHORITY.** GRANT-06 is that a grant is never persisted and
never rehydrated, and 61-06 restated it — a resumed run gets a FRESH grant. Auto-split is the
most autonomous thing in this milestone and it schedules future spend at the moment of one yes;
the only thing that stops it becoming a standing licence is that each subsequent run is still
separately authorised.

Purpose: RUN-05's offer, D-57-04's remainder, D-57-01's durable home for the rows a mid-run
breach did not send.

Output: one new durable store, one split calculator, and the breach path wired into both.

## Decisions taken here (Claude's Discretion, per CONTEXT.md)

**The remainder lives BESIDE `held_queue.py`, not in it — and the two questions CONTEXT told
the planner to keep separate are both answered the same way, for one reason.**

CONTEXT asks separately where breach-held ROWS go (D-57-01) and where the remainder BATCH goes
(D-57-04). Both go to the same new `remainder_queue.py`, because a ceiling-breach row and a
ceiling-breach remainder are the same fact at two granularities, and splitting them across two
stores would be the second copy of one rule this codebase keeps paying for.

They are not `held_queue.py` entries. That module's `hold_code` is a validated, CLOSED
vocabulary (`confidence.ALL_HOLD_CODES`) built for per-row confidence holds, and its
`fingerprint()` requires a `preingest.Outcome`-shaped object with `.match_tier` and
`.candidate_count`. A row that was never dispatched because the budget ran out was never
assessed by `confidence.assess()` and has neither. Forcing it through would mean either adding
a hold code `assess()` never emits — breaking that module's own stated invariant — or
synthesising a fake outcome for `fingerprint()` to hash. Both are worse than a small sibling
file following the same idiom.

What IS reused, verbatim: `durable_paths._atomic_write_0600`, the per-run
`<name>-<run_id>.json` filename plus glob-and-union reader (D-59-09), the fail-loud-on-shape
discipline, and a FRESH per-module copy of `_FORBIDDEN_NAME_MARKERS` — never an import, per the
convention `written_records.py`, `held_queue.py` and `run_state.py` each already follow and each
document.

**The operator's one review pass is preserved by the report, not by the store.** D-57-01 asks
for one review pass over a run's held rows. Plan 57-05's end-of-run report presents held_queue
rows and remainder-queue rows together in one section, so the operator still reviews once. Two
stores, one surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-CONTEXT.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-RESEARCH.md
@.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-PATTERNS.md
@.planning/phases/61-autonomous-batch-runs/61-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm auto-split queues work, never authority</name>
  <read_first>
    - .planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-CONTEXT.md (the D-57-04 and D-57-05 sections, including the one-way reversibility rating and the instruction that an auto-split/GRANT-06 conflict is an operator checkpoint rather than a judgement call)
    - .planning/phases/61-autonomous-batch-runs/61-06-PLAN.md (Task 3 — GRANT-06 verbatim: a grant is never persisted, never rehydrated, and a resumed run gets a FRESH grant)
    - operator-claude-plugin/scripts/write_grant.py (the `allowance_headroom` and `ceiling_verdict` added by plan 57-01 — the arithmetic a split would be computed from, and the `sampled` flag that decides whether a split can be offered at all)
  </read_first>
  <decision>Whether a batch too large for the remaining monthly allowance is auto-split across runs with the remainder queued — knowing that each split run will still need its own separate yes.</decision>
  <context>
D-57-05 is rated ONE-WAY. Persisting authority across runs would breach GRANT-06 (a grant is
never persisted and never rehydrated), and unwinding it later means revoking a capability an
operator had already been given. That is why this is a checkpoint and not a judgement call.

What the plan proposes to build honours GRANT-06: the queue holds rows still to do, and every
run that picks them up opens a fresh grant. The tension worth stating plainly is that auto-split
is the most autonomous option in this milestone and it schedules FUTURE SPEND at the moment of
one yes. What keeps it honest is exactly the thing that also makes it less convenient: the
schedule is a plan of work, and each run's spend is separately authorised.

One arithmetic edge the operator should know before choosing: when the remaining allowance
cannot be sampled — an unconfigured allowance key, an unreadable executions list, or a sample
truncated by the 1,000-execution page cap — there is no number to split against, so no split
can be offered. In that case option A degrades to option B for that run, with the blind spot
named.

Persisting the grant so a split run needs no second yes is NOT on this list. It is not an
option; it is the thing GRANT-06 forbids.
  </context>
  <options>
    <option id="option-a">
      <name>Auto-split with a work-only remainder queue (recommended)</name>
      <pros>The whole batch completes over time without the operator re-deciding what to send each round; the unsent rows are durable and named rather than lost; the same mechanism gives a mid-run ceiling breach a home for its unsent rows.</pros>
      <cons>Each split run still needs its own yes, so the operator is not freed from the decision entirely — only from re-deciding the batch. A new persisted artifact exists that other surfaces will read, which is itself costly to remove later.</cons>
    </option>
    <option id="option-b">
      <name>No auto-split — refuse with the arithmetic and let the operator name a smaller batch</name>
      <pros>Nothing new is persisted; the refusal is the whole feature; there is no schedule of future spend to explain.</pros>
      <cons>A 340-record batch becomes the operator's arithmetic problem every time; the mid-run breach still needs somewhere durable to put its unsent rows, so a store is likely needed anyway and would arrive later with less thought.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The remainder queue — a durable store that holds work and refuses authority</name>
  <files>operator-claude-plugin/scripts/remainder_queue.py, operator-claude-plugin/tests/test_remainder_queue.py</files>
  <read_first>
    - operator-claude-plugin/scripts/held_queue.py (the whole file, 279 lines — the storage idiom, `queue_path`, `save`, `_validated_entries`, `load`, `classify_read`, and the `hold_code`/`fingerprint` schema this module deliberately does NOT copy)
    - operator-claude-plugin/scripts/written_records.py (the per-run `<name>-<run_id>.json` naming, `WRITTEN_RECORDS_GLOB`, the glob-and-union `load()`, `_entries_from_document`, `_refuses_real_durable_write_under_pytest`, and the fresh `_FORBIDDEN_NAME_MARKERS` copy)
    - operator-claude-plugin/scripts/durable_paths.py (lines 57-77 — `_atomic_write_0600`'s exact guarantee; `resolve_state_path`)
    - operator-claude-plugin/scripts/chunking.py (lines 494-517 — `failed_batch()`, the entry shape this store persists)
    - operator-claude-plugin/tests/test_written_records.py (the `_patch_durable_dir` `tmp_path` fixture idiom to reuse)
  </read_first>
  <behavior>
    Write these first and watch them fail.

    - `remainder_path("abc")` resolves into the same durable directory as
      `written_records.written_records_path` and is named `remainder_queue-abc.json`.
    - `build_entry({"record_ids": ["1","2"], "object_type": "companies"},
      remainder_queue.REASON_CEILING_BREACH)` returns an entry carrying that spec verbatim,
      the reason, and a `record_count` of 2.
    - `build_entry` raises `RemainderQueueError` when the spec carries a key or value
      resembling an arming grant, a secret, an api key, a token, a credential, a password, a
      permission or a webhook — one test per marker.
    - `save(run_id, entries)` writes a 0600 file whose document carries `run_id`, `saved_at`
      and `entries`, and returns truthy.
    - `save` returns falsey (never raises) on an `OSError`, mirroring `append_chunk`'s
      degrade-rather-than-halt rule, because this is written from inside a live dispatch.
    - `save` still RAISES `RemainderQueueError` on a forbidden-named value — a defect in the
      data, not the environment.
    - `load()` with no path globs `remainder_queue*.json`, unions the matches in sorted
      filename order, stamps each entry with its own document's `run_id`, and skips an
      unreadable or malformed file without suppressing the readable ones.
    - `load()` over a document whose `entries` is not a list, or contains a non-dict, returns
      nothing from that file — whole-document degradation, never partial trust.
    - Two different `run_id`s never resolve to the same path.
    - The authority test, pinning D-57-05: an entry built from a spec that also carried a
      `grant` key is refused, and no entry produced by `build_entry` contains any key whose
      name matches a forbidden marker.
  </behavior>
  <action>
Create `operator-claude-plugin/scripts/remainder_queue.py` — the plugin's sixth durable
artifact, implementing D-57-04's queued remainder and D-57-01's home for the rows a mid-run
breach did not send.

Module docstring, in the register the five sibling stores already use, stating: what this holds
and why it is not a `held_queue.py` entry (that module's `hold_code` is a closed
`confidence.ALL_HOLD_CODES` vocabulary and its `fingerprint()` needs a `preingest.Outcome`; a
never-dispatched row has neither); that it holds WORK and never AUTHORITY per D-57-05 and
GRANT-06, so a resumed run opens a fresh grant; that the file is per-`run_id` per D-59-09 with
the reader globbing and unioning; and that `_FORBIDDEN_NAME_MARKERS` is defined fresh here
rather than imported, per the deliberate anti-DRY convention every sibling documents.

Constants: `REMAINDER_QUEUE_GLOB = "remainder_queue*.json"`, `RUN_ID_FIELD`, `STAMP_FIELD`,
`ENTRIES_FIELD`, `REASON_CEILING_BREACH = "ceiling_breach"`,
`REASON_ALLOWANCE_SPLIT = "allowance_split"`, `ALL_REASONS` as a frozenset of the two, and a
fresh copy of the nine forbidden-name markers.

`class RemainderQueueError(Exception)`, `_looks_forbidden(value)`, and
`remainder_path(run_id) -> Path` resolving fresh on every call into
`durable_paths.resolve_state_path().parent`.

`build_entry(spec, reason, *, note=None) -> dict` — validates `reason` against `ALL_REASONS`,
validates `spec` is a dict, scans every key and every value (including nested list members) with
`_looks_forbidden` and raises on a hit, and returns
`{"spec": <dict copy>, "reason": reason, "record_count": <len of record_ids/rows/people, or
None for a backend-resolved list spec>, "note": note}`. The spec is whatever
`chunking.failed_batch()` produced, unmodified — a well-formed enrichment request by
construction, re-sendable through the identical armed dispatch path, never a second shape.

`save(run_id, entries, path=None)` — appends to whatever this run's file already held (there is
no foreign document to replace, because the path is per-run), writes through
`durable_paths._atomic_write_0600`, returns True. Returns False on `OSError`. Carries the
`PYTEST_CURRENT_TEST` real-durable-directory guard `written_records.py` already implements, for
the same reason.

`load(path=None) -> list` — with an explicit path, one file's entries or `[]`; with none, the
glob-and-union across `REMAINDER_QUEUE_GLOB`, each entry stamped with its document's `run_id`.

Do NOT add a `clear()`, a resume helper, or an index. Nothing needs them yet.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_remainder_queue.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_remainder_queue.py -q` passes, including every behaviour above.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import remainder_queue as q; assert q.remainder_path('abc').name == 'remainder_queue-abc.json'"` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import remainder_queue as q; q.build_entry({'grant':'x'}, q.REASON_CEILING_BREACH)" ; test $? -ne 0` — building an entry from a spec carrying a grant raises.
    - `grep -c "from written_records import\|from held_queue import\|from run_state import" operator-claude-plugin/scripts/remainder_queue.py` returns 0 — the forbidden-name tuple is a fresh copy, not an import.
    - `grep -c "_atomic_write_0600" operator-claude-plugin/scripts/remainder_queue.py` is at least 1 — no second atomic-write implementation.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import remainder_queue as q; e=q.build_entry({'record_ids':['1']}, q.REASON_CEILING_BREACH); assert set(e) == {'spec','reason','record_count','note'}"` exits 0 — the entry schema is this module's own, carrying no borrowed hold-queue fields.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
  </acceptance_criteria>
  <reversibility rating="one-way">Implements D-57-05. The store is the mechanism that keeps auto-split from carrying authority across runs; approved at Task 1's checkpoint.</reversibility>
  <done>A durable, 0600, per-run remainder store exists that persists re-sendable work specs, refuses anything shaped like authority, and degrades rather than halting a live dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offer the smaller batch, and give the mid-run breach its durable landing</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/scripts/chunking.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py (the `allowance_headroom`, `ceiling_verdict`, `plan_grant` refusal branch and `record_dispatch_outcome` added by plan 57-01; `envelope()` at 183-278; `_normalise` at 107)
    - operator-claude-plugin/scripts/chunking.py (`chunk_ceiling` at 157-180, `plan_chunks` at 183+, `failed_batch` at 494-517, and the `CeilingStop`/`execution_ceiling` added by plan 57-01)
    - operator-claude-plugin/scripts/remainder_queue.py (written in Task 2)
    - .planning/phases/61-autonomous-batch-runs/61-06-PLAN.md (Task 3 — GRANT-06 verbatim, "a resumed run gets a FRESH grant")
  </read_first>
  <behavior>
    Write these first and watch them fail.

    - `write_grant.split_for_allowance(...)` over a record set whose projection exceeds a
      sampled remainder returns `affordable` (a spec whose own projection is at or under the
      remainder), `remainder` (a spec holding every record not in `affordable`), and `runs`
      (how many runs of that size the whole batch would take). No record appears in both, and
      the two together are the original set in the original order.
    - Its arithmetic uses `chunking.chunk_ceiling(config)` and the same
      `chunk_count + record_count` basis, never a second formula.
    - With `headroom["sampled"]` False, `split_for_allowance` returns `affordable` None,
      `remainder` None and a `reason` naming why no split can be offered.
    - With a remainder so small that not even one record fits, it returns `affordable` None
      and a reason saying so, rather than an empty batch presented as an offer.
    - `plan_grant()` refusing on `CEILING_OVER` carries the split offer under `split_offer`,
      and the refusal text names how many records would go now and how many are queued.
    - The authority test, pinning D-57-05: nothing in the refusal payload, and nothing
      `split_for_allowance` returns, carries a grant, a workflow id allowlist, an arming flag
      or a token. Assert by name over the returned keys.
    - Driving the real dispatch: a 3-chunk `chunking.dispatch_plan(..., execution_ceiling=N)`
      that stops after chunk 2 leaves a `remainder_queue` file for that `run_id` containing
      one entry whose reason is `REASON_CEILING_BREACH` and whose spec names exactly the
      unsent records.
    - A dispatch with no ceiling writes no remainder file at all.
    - A `remainder_queue.save` failure during a ceiling stop does not raise out of
      `dispatch_plan` and does not change the returned `DispatchOutcome`'s other fields — the
      same degrade-rather-than-halt rule `written_records` bookkeeping already follows
      (D-59-10).
  </behavior>
  <action>
Add `split_for_allowance(config, *, object_type, record_ids, record_domains, headroom,
providers=None)` to `write_grant.py`, implementing D-57-04 and completing RUN-05's "offers a
smaller batch". Pure apart from reading `chunking.chunk_ceiling(config)`. Find the largest N
such that `ceil(N / chunk_ceiling) + N` is at or under `headroom["remaining_sampled"]`, then
return `{"affordable": <spec of the first N in the original order>, "remainder": <spec of the
rest>, "runs": <ceil(total / N)>, "record_ceiling_per_run": N, "reason": None}`. Specs are in
the same `{record_ids, object_type}` shape `chunking.failed_batch()` produces, so the offer is
re-sendable rather than re-derived. When `headroom["sampled"]` is False, or N works out below
1, return every spec key as None with a `reason` naming which — never an empty batch dressed as
an offer.

Attach the result to `plan_grant()`'s `CEILING_OVER` refusal under `split_offer`, and extend the
refusal text: how many records this run can carry, how many are queued, and — the sentence
D-57-05 requires and the operator-facing text must not omit — that each subsequent run opens its
OWN grant, so the queue is a plan of work and not standing permission to spend.

In `chunking.dispatch_plan`, when the ceiling tally fires, persist the stop before returning:
build a `remainder_queue.build_entry(<the CeilingStop's remainder spec>,
remainder_queue.REASON_CEILING_BREACH, note=<the chunk index it stopped at>)` and
`remainder_queue.save(run_id, [entry])`. Wrap it in the SAME shape as the existing
`written_records.append_chunk` guard: a `RemainderQueueError` or a falsey save must never raise
out of the dispatch and must never alter any other field of the returned `DispatchOutcome`
(D-59-10's rule, applied to a second bookkeeping store). Record the failure on the
`CeilingStop`'s `reason` so it is not silent.

`chunking` must not gain a dependency on the grant module — it stays grant-unaware, exactly as
its own docstring says. `remainder_queue` is a durable store rather than an authority object, so
depending on it introduces no grant awareness.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_chunking.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_chunking.py -q` passes, including every behaviour above.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'operator-claude-plugin/scripts'); import inspect, chunking; assert 'grant' not in inspect.signature(chunking.dispatch_plan).parameters"` exits 0.
    - `grep -c "import write_grant" operator-claude-plugin/scripts/chunking.py` returns 0.
    - A test asserts the refusal payload's keys contain no key whose name matches any of the nine forbidden-name markers.
    - The mid-run test reads the remainder file back off `tmp_path` and asserts its single entry's `reason` is `ceiling_breach` and its spec's record ids are exactly the unsent ones.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes.
    - `.venv/bin/python -m pytest -q` passes.
  </acceptance_criteria>
  <reversibility rating="costly">Implements D-57-04. The queued-remainder store is a new persisted artifact other surfaces will read; removing it later means re-deciding what happens to a partially-run batch.</reversibility>
  <done>An over-ceiling refusal comes with a concrete affordable batch and a named remainder, a mid-run stop leaves its unsent rows durable and re-sendable, and neither carries anything that would let a later run write without a fresh yes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| in-memory grant to durable disk | the boundary GRANT-06 forbids authority from crossing; this plan adds a new store immediately beside it |
| dispatch loop to durable disk | a bookkeeping write that must never be able to halt a live run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-57-11 | Elevation of privilege | a remainder entry carrying write authority across runs | critical | mitigate | Entries hold `chunking.failed_batch()`-shaped work specs only; `build_entry` runs a fresh forbidden-name scan over every key and value and refuses a spec carrying a grant, token, permission or arming marker; a resumed run with no grant open refuses exactly as any ungranted send does, pinned by test |
| T-57-12 | Information disclosure | an API key or webhook secret reaching a new 0600 file | high | mitigate | The same nine-marker scan, reimplemented fresh in this module per the established convention, plus `_atomic_write_0600`'s 0600 mode |
| T-57-13 | Denial of service | a remainder-queue write failure halting a live dispatch | high | mitigate | The save is wrapped in D-59-10's degrade-rather-than-halt guard: never raises out of `dispatch_plan`, never alters another `DispatchOutcome` field, and the failure is recorded on `CeilingStop.reason` rather than swallowed |
| T-57-14 | Repudiation | auto-split presented as though the whole batch were authorised by one yes | high | mitigate | The refusal text states explicitly that each subsequent run opens its own grant; pinned by an assertion on the refusal text |
| T-57-15 | Tampering | package-manager installs | low | accept | This plan installs no package in any ecosystem |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green.
- `.venv/bin/python -m pytest -q` green.
- `node --test tests/n8n/*.test.mjs` green (unchanged by this plan; run to confirm no collateral).
- No live n8n call, no armed window, no provider credit spent.
</verification>

<success_criteria>
An operator refused for budget is handed a batch that fits and a queue holding the rest; a run
stopped mid-flight leaves its unsent rows durable and named; and neither path lets a later run
spend without its own yes.
</success_criteria>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):

- `operator-claude-plugin/scripts/remainder_queue.py` and its
  `RemainderQueueError`, `remainder_path`, `build_entry`, `save`, `load`,
  `REMAINDER_QUEUE_GLOB`, `REASON_CEILING_BREACH`, `REASON_ALLOWANCE_SPLIT`, `ALL_REASONS`
- `write_grant.split_for_allowance(...)` and the `split_offer` key on a `CEILING_OVER` refusal
- the `remainder_queue` persistence branch inside `chunking.dispatch_plan`'s ceiling stop

Created elsewhere in this phase: see plan 57-01's "Artifacts this phase produces".

## Flagged Assumptions (specless probe fallback)

No SPEC file exists; `## Edge Coverage` and `## Prohibitions` are absent. All four probe rows
came back `unclassified` and therefore `unresolved`. Carried by this plan:

| # | Requirement | Probe status | Disposition here |
|---|---|---|---|
| 1 | RUN-05 | unclassified / unresolved | Edges derived by hand into `must_haves.truths` above: an allowance too small for even one record, an unsampleable allowance with no split arithmetic, a remainder-queue write that fails mid-dispatch. Assumption: the largest-N search using `chunk_count + record_count` is monotonic in N, so a linear or binary search finds a real maximum — the executor asserts monotonicity with a test over a range of N rather than assuming it. |
| 2, 3, 4 | AFTER-01, AFTER-03, G-4 | unclassified / unresolved | Carried by plans 57-05, 57-02, 57-04 respectively; listed in full in 57-01. |

<output>
Create `.planning/phases/57-ceilings-refusal-before-start-and-post-run-proof/57-03-SUMMARY.md` when done
</output>
