---
phase: 67-an-autonomy-flag-with-sensible-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/config_gate.py
  - operator-claude-plugin/config/operator.local.example.json
  - operator-claude-plugin/tests/test_autonomy_levels.py
  - operator-claude-plugin/tests/test_headless_grant_boundary.py
autonomous: false
requirements: [AUTO-01, AUTO-05]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "config_gate.autonomy_enabled(cfg, level) returns True for each of read_only / spend_no_write / write when the `autonomy` object is absent from the config, and returns False only when that level's value is something other than the JSON boolean true or absent (D-67-01, D-67-03, D-67-10). [probe AUTO-01/unclassified — explicit]"
    - "autonomy_enabled is pure and repeatable: two calls with the same config dict return the same value, and neither call writes any file, opens any socket, or mutates the config (D-67-02). [probe AUTO-02/idempotency — explicit]"
    - "operator-claude-plugin/scripts/scheduled_arm.py and operator-claude-plugin/scripts/n8n_arming.py are byte-identical to commit 238d1ab, and neither file's source names `autonomy`, `AUTONOMY_SETTINGS_KEY`, `AUTONOMY_LEVELS` or `autonomy_enabled` — ALLOW_N8N_ARM stays the sole authority for the headless and cron paths (D-67-02, AUTO-05). [probe AUTO-05/unclassified — explicit]"
    - "The three autonomy keys live under one `autonomy` object in operator.local.example.json with a sibling `_autonomy_note` that states, in the operator's own words, that an ABSENT key reads as ON — the opposite of the neighbouring allow_write_grants note — and that these keys authorise nothing on their own (D-67-01, D-67-02, D-67-03)."
    - "AUTONOMY_SETTINGS_KEY is not a member of config_gate.CAPABILITY_KEYS and is not a second copy of WRITE_GRANT_SETTINGS_KEY's identity-on-absence comparison; the read site carries a comment naming the distinction so a later reader does not 'fix' it into `is True` (D-67-02, D-67-10)."
    - "The operator has answered the AUTO-04 reversal question before any autonomy default is written to disk, and the answer is recorded verbatim in 67-01-SUMMARY.md (D-67-04, AUTO-04)."
  artifacts:
    - operator-claude-plugin/scripts/config_gate.py
    - operator-claude-plugin/config/operator.local.example.json
    - operator-claude-plugin/tests/test_autonomy_levels.py
    - operator-claude-plugin/tests/test_headless_grant_boundary.py
  key_links:
    - "operator.local.json `autonomy` object -> config_gate.load_config -> config_gate.autonomy_enabled(cfg, level) -> the batch skills' single ask-or-proceed switch site (wired in 67-02)"
    - "config_gate.autonomy_enabled -> NOT reachable from scheduled_arm.py or n8n_arming._arm_gate — pinned by AST/source scan in test_headless_grant_boundary.py"
  prohibitions:
    - "No settings-file key arms the headless or cron path; ALLOW_N8N_ARM remains the sole authority there."
    - "n8n_arming._arm_gate gains no third branch."
    - "scheduled_arm.py and n8n_arming.py are not edited, for any reason."
    - "The new keys are never added to CAPABILITY_KEYS and never compared with `is True` on absence."
    - "No plugin script gains a `while` loop, an `import time`, or a `sleep()` call."
    - "No file under n8n/ and no change to scripts/build_cloud_workflows.py."
    - "The literal substring `tier` is not introduced into any skills/*/SKILL.md body."
---

<objective>
Name the three autonomy levels as settings keys and prove they can never reach the headless
arm path.

Purpose: D-67-01 requires the three levels be independently named and independently
defaultable even while their default values agree today; D-67-02 requires that naming them
adds no authority. This plan makes the switch exist and makes its powerlessness a checked
fact. It also puts the AUTO-04 reversal to the operator FIRST — before any default lands on
disk — so opening D-61-08's unattended gate is a deliberate answered question and not a
consequence of a config default that already shipped (D-67-04).

Output: `config_gate.AUTONOMY_SETTINGS_KEY`, `config_gate.AUTONOMY_LEVELS`,
`config_gate.autonomy_enabled`, the `autonomy` object plus `_autonomy_note` in
`operator.local.example.json`, a new `tests/test_autonomy_levels.py`, and two new
assertions in `tests/test_headless_grant_boundary.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-CONTEXT.md
@.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-PATTERNS.md
@operator-claude-plugin/scripts/config_gate.py
@operator-claude-plugin/config/operator.local.example.json
@operator-claude-plugin/tests/test_headless_grant_boundary.py
</context>

<!-- The literals below are the exact strings this plan RETIRES; the executor must be
     able to find them verbatim to replace them, so each is allowlisted from the
     comment-text discipline gate (#429). -->
<!-- planner-discipline-allow: fail-closed -->

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: The reversal, and the rendering of it — two questions, answered before anything ships</name>
  <read_first>
    - `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-CONTEXT.md` — D-67-01, D-67-02, D-67-03, D-67-04, D-67-09, D-67-10
    - `.planning/ROADMAP.md` § "Standing facts" (the first live unattended credit-spending batch has NOT run) and § "Binding on all six" (SAFE-05)
    - `.planning/REQUIREMENTS.md` § AUTO — AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05
    - `operator-claude-plugin/skills/backend-control/SKILL.md` lines 95-120 — the paragraph 68-01 wrote naming Phase 67 as the opener
    - `operator-claude-plugin/scripts/config_gate.py` lines 88-121 — `WRITE_GRANT_SETTINGS_KEY` and `write_grants_enabled`, the pattern this phase deliberately does NOT copy
  </read_first>
  <decision>
Two answers are needed before any autonomy default is written to disk. **Question A (AUTO-04,
D-67-04):** confirm that recording D-67-01's "autonomy ON for all three levels" is a deliberate
reversal of the decision taken at Phase 57-05's Task 4 gate. **Question B (D-67-02 / D-67-03):**
confirm four concrete renderings of the settings keys — the key shape, what an absent key
means, what a near-miss value means, and what a malformed parent means.
  </decision>
  <context>
Neither question re-opens a locked decision. A confirms a RECORDED REVERSAL as a reversal; B
confirms a RENDERING.

**For A, the record as it stands.** At Phase 57-05's Task 4 gate the operator selected
option-a — a small, operator-SUPERVISED first live batch — with option B, authorising the
unattended credit-spending batch, on the table and not taken. The standing fact today is that
nothing is armed and the first live unattended credit-spending batch has never run. What does
NOT change either way: `allow_write_grants` still gates the interactive path, `ALLOW_N8N_ARM`
still gates the headless and cron paths, and this phase touches neither (D-67-02, AUTO-05).
SAFE-05 requires this gate stay shut unless AUTO-04 is explicitly answered — which is why this
checkpoint is `blocking-human` and is never auto-approved. The answer is recorded VERBATIM: plan
67-04 quotes it into `backend-control/SKILL.md`, so it must be quotable.

**For B, the four renderings.** (1) Key shape: one `autonomy` object holding `read_only`,
`spend_no_write`, `write` — three independently settable keys under one parent, not one flat
boolean and not three top-level keys; constants `AUTONOMY_SETTINGS_KEY` / `AUTONOMY_LEVELS` in
`config_gate.py`. (2) Absence: an absent `autonomy` object, and an absent level inside a present
one, both read ON (D-67-03, D-67-10) — deliberately the OPPOSITE of `write_grants_enabled`'s
absent-means-off, because these keys authorise nothing. (3) Near-miss and (4) malformed parent
are the two the artifacts leave genuinely open; each has its own option pair below.
  </context>
  <options>
    <option id="a-confirm">
      <name>A: confirm the reversal as stated</name>
      <pros>D-61-08's unattended gate opens by a deliberate, recorded answer, exactly as AUTO-04 and SAFE-05 require; the phase proceeds as planned.</pros>
      <cons>One-way — it crosses a line the project has deliberately not crossed; credits an unattended round spends under an unsampled ceiling are not recoverable (D-67-09).</cons>
    </option>
    <option id="a-narrow">
      <name>A: confirm with a stated narrowing</name>
      <pros>The reversal is recorded with an explicit scope the operator names (a level left off, a lane excluded), and the narrowing is written into the record alongside it.</pros>
      <cons>The narrowing must be carried through plans 02-04's prose and tests, so it changes work already sized.</cons>
    </option>
    <option id="a-halt">
      <name>A: halt the phase</name>
      <pros>Nothing is written, nothing changes posture, and 57-05 Task 4's option-a stands untouched.</pros>
      <cons>AUTO-01..AUTO-06 stay open and Phase 67 does not ship; the per-round friction the phase exists to name stays unnamed.</cons>
    </option>
    <option id="b-near-miss-asks">
      <name>B3: a near-miss value reads autonomy OFF (the round asks) — recommended</name>
      <pros>For a default-setter whose safe state is ASKING, a near miss fails toward asking; the strings `"true"`/`"false"`, `0`, `1`, `"yes"` and `null` cannot become consent by accident.</pros>
      <cons>Stricter than 67-PATTERNS.md's sketched `is not False`, so a hand-edited config using the string `"true"` silently asks rather than proceeding.</cons>
    </option>
    <option id="b-near-miss-on">
      <name>B3: a near-miss value reads autonomy ON (only the JSON boolean false turns it off)</name>
      <pros>Matches 67-PATTERNS.md's literal sketch; a config typo cannot accidentally re-introduce per-round asking.</pros>
      <cons>The string `"false"` — the single most likely hand-edit an operator makes to turn autonomy off — would read as ON, failing toward the less safe state.</cons>
    </option>
    <option id="b-malformed-off">
      <name>B4: `"autonomy": true` (a bare boolean parent) reads OFF for every level — recommended</name>
      <pros>A hand-edited config degrades to asking rather than crashing a round mid-batch; pinned by its own test.</pros>
      <cons>The operator gets asking with no explanation of why until they re-read the example config.</cons>
    </option>
    <option id="b-malformed-raises">
      <name>B4: a bare boolean parent raises `ConfigError` naming the key</name>
      <pros>The mistake is named loudly at load time rather than showing up as unexplained asking.</pros>
      <cons>A malformed default-setter key would then take down every capability that loads the config, including read-only ones that never consult autonomy.</cons>
    </option>
  </options>
  <resume-signal>Answer with one id from the A group and one id from each B group — e.g. "a-confirm, b-near-miss-asks, b-malformed-off". For A, also give the sentence you want recorded verbatim; it is quoted into `backend-control/SKILL.md` by plan 67-04. Answering `a-halt` stops the phase here.</resume-signal>
  <acceptance_criteria>
    - Both questions were asked in one message, in the order A then B, with Question A naming Phase 57-05 Task 4 option-a explicitly as the decision being reversed.
    - The operator's answer to Question A is recorded verbatim (a quotable string) in `67-01-SUMMARY.md`.
    - The operator's answer to Question B names, for each of the four renderings, either the proposed rendering or a stated alternative.
    - No file under `operator-claude-plugin/` was modified by this task.
  </acceptance_criteria>
  <reversibility rating="one-way">Opening D-61-08's unattended gate crosses a line the project has deliberately not crossed (D-67-04); shipping new write-posture defaults to other installs cannot be un-shipped (D-67-03).</reversibility>
  <verify>
    <automated>git status --porcelain -- operator-claude-plugin/</automated>
    <fails_when>prints any line — this task changes no file, so any porcelain output means an edit was made before the gate was answered</fails_when>
  </verify>
  <done>Both answers are recorded; Question A's answer is verbatim and quotable; the phase either proceeds to Task 2 or halts on the operator's word.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One settings file, three levels, one reader — and no path from any of them to the headless arm</name>
  <files>operator-claude-plugin/scripts/config_gate.py, operator-claude-plugin/config/operator.local.example.json, operator-claude-plugin/tests/test_autonomy_levels.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/config_gate.py` — lines 34-76 (`CAPABILITY_KEYS`), 88-121 (`WRITE_GRANT_SETTINGS_KEY`, `write_grants_enabled`), 130-163 (`load_config`)
    - `operator-claude-plugin/config/operator.local.example.json` — the `allow_write_grants` / `_allow_write_grants_note` sibling-key idiom and the `n8n_monthly_execution_allowance` / `_n8n_monthly_execution_allowance_note` pair
    - `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-PATTERNS.md` § `config_gate.py` and § `operator.local.example.json`
    - `operator-claude-plugin/tests/test_config_gate.py` — the `tmp_path` + `cfg_path.write_text(json.dumps(...))` fixture idiom this test file mirrors
    - `operator-claude-plugin/tests/test_write_grant.py` around line 1156 — the test asserting `WRITE_GRANT_SETTINGS_KEY not in CAPABILITY_KEYS`, the shape the new key's own separation test mirrors
    - Task 1's recorded answer to Question B (the four confirmed renderings)
  </read_first>
  <behavior>
    - A config file on disk carrying no `autonomy` key at all, loaded through `config_gate.load_config(path)`, yields `autonomy_enabled(cfg, "write") is True` — and the same for `read_only` and `spend_no_write`. This is the D-67-03/D-67-10 upgrade path: an existing install moves to the new defaults without anything being written into its file.
    - A config carrying `{"autonomy": {"write": false}}` yields False for `write` and True for the two levels it does not mention.
    - Every near-miss value for a level — the string `"false"`, the string `"true"`, `0`, `1`, `"yes"`, `null` — yields False (the round asks).
    - `{"autonomy": true}` — a bare boolean where the object belongs — yields False for all three levels and raises nothing.
    - `autonomy_enabled(cfg, "admin")` raises `ValueError` naming the unknown level; the three valid names are exactly `AUTONOMY_LEVELS`.
    - Calling `autonomy_enabled` twice on the same dict returns the same value both times and leaves the dict `==` to a pre-call copy.
    - `AUTONOMY_SETTINGS_KEY` is not a key of `CAPABILITY_KEYS` and is not any capability's required-key tuple member.
  </behavior>
  <action>
Add to `operator-claude-plugin/scripts/config_gate.py`, in its own commented section BELOW
the `WRITE_GRANT_SETTINGS_KEY` block and clearly separated from it:

- `AUTONOMY_SETTINGS_KEY = "autonomy"` and `AUTONOMY_LEVELS = ("read_only", "spend_no_write",
  "write")`.
- `autonomy_enabled(config, level)` returning a bool, implementing exactly Task 1's confirmed
  rendering: raise `ValueError` for a `level` not in `AUTONOMY_LEVELS`; read the parent object
  with `.get(AUTONOMY_SETTINGS_KEY)`; an absent parent returns True; a parent that is not a
  `dict` returns False; otherwise read the level's value and return True only when it is
  `None` (absent) or the JSON boolean `True`, and False for every other value.
- A docstring and an inline comment at the read site stating, in prose a future maintainer
  cannot misread: this is a DEFAULT-SETTER (D-67-02), layered on top of authority already
  granted by `allow_write_grants` (interactive) or `ALLOW_N8N_ARM` (headless); it authorises
  nothing by itself; do NOT copy `write_grants_enabled`'s `is True` identity-on-absence check
  onto it, because an absent key must read ON here (D-67-03, D-67-10); and the near-miss rule
  differs in DIRECTION rather than in spirit — an authority gate fails toward not-authorised,
  this one fails toward asking.

Add to `operator-claude-plugin/config/operator.local.example.json`, immediately after the
`_allow_write_grants_note` line so the two sit adjacent and the contrast is readable: an
`autonomy` object with all three levels set to `true`, and an `_autonomy_note` sibling. The
note must say: what the levels are and which functions each covers (`read_only` — status,
queue and report reads; `spend_no_write` — the match and propose lanes; `write` — ingest,
enrich-and-write and review-decision apply); that an ABSENT key or level reads as ON, unlike
`allow_write_grants` directly above it (D-67-03); that setting a level to `false` puts that
level's rounds back to asking first; that these keys are a default-setter and never a third
authority gate, so `allow_write_grants` and `ALLOW_N8N_ARM` are still both required and
neither is replaced (D-67-02); and that `backend-control`'s own arm, deploy and structural
mutations are covered by NONE of the three and always confirm and wait (D-67-12).

Create `operator-claude-plugin/tests/test_autonomy_levels.py` covering every bullet in
`<behavior>` above, written RED first. Drive at least the absent-key case through a real
`config_gate.load_config(tmp_path/...)` round trip rather than a hand-built dict, so the
tracer is settings-file-to-reader end to end and not a unit test of a pure function. Include
the `AUTONOMY_SETTINGS_KEY not in CAPABILITY_KEYS` separation test, mirroring
`test_write_grant.py`'s own precedent for the write-grant key.

Commit RED first (`test(67-01): ...`), then GREEN (`feat(67-01): ...`). Verify RED by reading
pytest's failure output — there is no `gsd-tools check tdd-red-evidence` here.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0, 'operator-claude-plugin/scripts'); import config_gate; print(config_gate.AUTONOMY_LEVELS)"` prints `('read_only', 'spend_no_write', 'write')`.
    - `autonomy_enabled({}, "write")`, `autonomy_enabled({"autonomy": {}}, "write")` and `autonomy_enabled(None, "write")` each return `True`.
    - `autonomy_enabled({"autonomy": {"write": False}}, "write")` returns `False`; the same config returns `True` for `read_only` and `spend_no_write`.
    - `autonomy_enabled({"autonomy": {"write": "false"}}, "write")`, `{"write": "true"}`, `{"write": 0}`, `{"write": 1}`, `{"write": "yes"}` and `{"write": None}` each return `False`.
    - `autonomy_enabled({"autonomy": True}, "write")` returns `False` and raises nothing.
    - `autonomy_enabled({}, "admin")` raises `ValueError` whose message contains `admin`.
    - `/usr/bin/grep -c '"autonomy"' operator-claude-plugin/config/operator.local.example.json` is at least 1, and `python3 -c "import json;d=json.load(open('operator-claude-plugin/config/operator.local.example.json'));print(sorted(d['autonomy']))"` prints `['read_only', 'spend_no_write', 'write']` with all three values `true`.
    - `/usr/bin/grep -c '_autonomy_note' operator-claude-plugin/config/operator.local.example.json` is 1, and that note's text contains both `ALLOW_N8N_ARM` and `allow_write_grants`.
    - `git diff --stat 238d1ab -- operator-claude-plugin/scripts/` lists `config_gate.py` and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_autonomy_levels.py operator-claude-plugin/tests/test_config_gate.py -q</automated>
    <fails_when>pytest exits non-zero, or reports fewer than 12 passed for test_autonomy_levels.py</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status -q</automated>
    <fails_when>exits non-zero, naming config_gate.py as a script that loops or sleeps</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>prints any line — a path under n8n/ or the workflow builder was touched</fails_when>
  </verify>
  <done>A config file with no `autonomy` key reads every level ON through the real loader; every near-miss and the malformed-parent case read OFF; the example config documents all of it beside `allow_write_grants`; both commits are on the branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin that no settings key can reach the headless arm, and retire the stale forward reference</name>
  <files>operator-claude-plugin/tests/test_headless_grant_boundary.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_headless_grant_boundary.py` — the whole file: the module docstring (which still claims Phase 67 builds a `CEILING_UNKNOWN` fail-closed condition), `_imported_names`, and the three existing tests
    - `operator-claude-plugin/scripts/n8n_arming.py` lines 170-265 — `ARM_ENV_VAR`, `DISPATCH_FLAGS`, `REVIEW_FLAGS`, and `_arm_gate`'s exactly-two branches
    - `operator-claude-plugin/scripts/scheduled_arm.py` — read only; never edited
    - `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-CONTEXT.md` — D-67-02 (the four reasons a third gate was rejected), D-67-09 (which reverses D-67-05)
  </read_first>
  <behavior>
    - `scheduled_arm.py`'s own source names none of `autonomy`, `AUTONOMY_SETTINGS_KEY`, `AUTONOMY_LEVELS`, `autonomy_enabled` — a settings key cannot reach the cron path even by string.
    - `n8n_arming.py`'s own source names none of the same four symbols — `_arm_gate` keeps exactly its two branches (with a grant; with `ALLOW_N8N_ARM`), and gains no autonomy-aware third.
    - The three pre-existing tests in the file still pass unmodified.
  </behavior>
  <action>
Extend `operator-claude-plugin/tests/test_headless_grant_boundary.py` with two new tests, in
the same source-scan style the file already uses (read the file's own text, assert a symbol is
absent — never a transitive-import claim, for the reason the existing module docstring already
gives about `n8n_arming`'s function-local `import write_grant`):

- one asserting `scheduled_arm.py`'s source contains none of the four autonomy symbol names;
- one asserting `n8n_arming.py`'s source contains none of them either, with a comment naming
  D-67-02's four recorded reasons in one line each (Claude-writable file vs unsettable env
  var; persistence asymmetry; two blast radii collapsed into one; the pinned
  `DISPATCH_FLAGS`/`REVIEW_FLAGS` separation).

Do NOT modify the three existing tests. In the same commit, correct the module docstring's
one stale sentence: it currently says the gate Phase 67 builds includes "the `CEILING_UNKNOWN`
fail-closed condition". D-67-09 reversed that — an autonomous round discloses an unknown state
and proceeds. Rewrite that clause to say what Phase 67 actually did (named the three autonomy
levels as default-setter settings keys) and that the unknown-state handling is unchanged
disclose-and-proceed, citing D-67-09. Paraphrase the retired wording; never quote the old
"fail-closed" phrasing verbatim into a note, or a later negative pin trips on this file's own
explanation of itself.

RED first: add the two tests with the symbol names deliberately mis-scoped (assert the symbols
are absent from a file that does contain them, e.g. `config_gate.py`) to observe pytest fail,
quote that output in the commit message, then correct the scope for GREEN. Two commits,
`test(67-01): ...` then `fix(67-01): ...`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_headless_grant_boundary.py -q` reports 5 passed (3 pre-existing + 2 new).
    - `/usr/bin/grep -c 'autonomy' operator-claude-plugin/scripts/scheduled_arm.py` is 0 and `/usr/bin/grep -c 'autonomy' operator-claude-plugin/scripts/n8n_arming.py` is 0.
    - `/usr/bin/grep -v '^#' operator-claude-plugin/tests/test_headless_grant_boundary.py | /usr/bin/grep -c 'fail-closed'` is 0 — the retired wording survives nowhere in the file's live prose.
    - `/usr/bin/grep -c 'D-67-09' operator-claude-plugin/tests/test_headless_grant_boundary.py` is at least 1.
    - `git diff --quiet 238d1ab -- operator-claude-plugin/scripts/scheduled_arm.py operator-claude-plugin/scripts/n8n_arming.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_headless_grant_boundary.py -q</automated>
    <fails_when>exits non-zero, or reports fewer than 5 passed</fails_when>
    <automated>git diff --quiet 238d1ab -- operator-claude-plugin/scripts/scheduled_arm.py operator-claude-plugin/scripts/n8n_arming.py</automated>
    <fails_when>exits 1 because scheduled_arm.py or n8n_arming.py differs from 238d1ab</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
    <fails_when>exits non-zero, or reports fewer than 2646 passed</fails_when>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <fails_when>exits non-zero, or reports fewer than 940 pass</fails_when>
  </verify>
  <done>Five tests pass in the boundary file; neither arming script names an autonomy symbol; both scripts are byte-identical to 238d1ab; the docstring no longer promises a fail-closed condition this phase does not build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator settings file -> plugin | `operator.local.json` is a plain JSON file the assistant can write; every value read from it crosses into decision logic |
| plugin session -> n8n arming | the only path that can enable a live HubSpot write; gated by `allow_write_grants` (interactive) and `ALLOW_N8N_ARM` (headless) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-01 | Elevation of Privilege | `config_gate.autonomy_enabled` | high | mitigate | The function returns a default-setter boolean only; it is never consulted by `_arm_gate` or `write_grant`, and Task 3 pins its symbol absent from both arming scripts. `AUTONOMY_SETTINGS_KEY` is asserted out of `CAPABILITY_KEYS`. |
| T-67-02 | Tampering | `operator.local.json` -> `scheduled_arm.py` | high | mitigate | AST/source scan in `test_headless_grant_boundary.py` asserts neither `scheduled_arm.py` nor `n8n_arming.py` names any autonomy symbol; both files pinned byte-identical to 238d1ab by `git diff --quiet`. |
| T-67-03 | Spoofing | a near-miss settings value read as consent | medium | mitigate | Only the JSON boolean `true` or an absent key reads ON; `"true"`, `1`, `"yes"` all read OFF (the round asks). Pinned by six parametrized cases in `test_autonomy_levels.py`. |
| T-67-04 | Information Disclosure | `_autonomy_note` and `ConfigError` messages | low | mitigate | The new note carries no secret and no configured value; `config_gate` already never interpolates a secret into a message (T-27-12) and this task adds no new message that reads one. |
| T-67-05 | Denial of Service | a malformed `autonomy` parent raising mid-round | low | mitigate | A non-dict parent returns `False` rather than raising, so a hand-edited config degrades to asking instead of crashing a round; pinned by its own test. |
| T-67-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package (67-RESEARCH.md § Package Legitimacy Audit); there is no install task to gate. |
</threat_model>

<artifacts>
## Artifacts this phase produces (plan 01)

**New symbols in `operator-claude-plugin/scripts/config_gate.py`:**
- `AUTONOMY_SETTINGS_KEY` — `str`, value `"autonomy"`
- `AUTONOMY_LEVELS` — `tuple[str, str, str]`, value `("read_only", "spend_no_write", "write")`
- `autonomy_enabled(config: dict, level: str) -> bool`

**New settings keys in `operator-claude-plugin/config/operator.local.example.json`:**
- `autonomy` — object, three boolean members `read_only` / `spend_no_write` / `write`, all `true`
- `_autonomy_note` — string, the sibling-note documenting absence-reads-ON and the no-authority rule

**New test file:** `operator-claude-plugin/tests/test_autonomy_levels.py`

**New tests in an existing file:** two symbol-absence tests in
`operator-claude-plugin/tests/test_headless_grant_boundary.py`; its module docstring's
fail-closed clause corrected.

**Recorded decision:** the operator's verbatim answer to the AUTO-04 reversal question, in
`67-01-SUMMARY.md` — read by plan 67-04, which writes it into `backend-control/SKILL.md`.
</artifacts>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green (baseline 2646 passed / 5 skipped, plus this plan's new tests)
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q` — ratchet green (no SKILL.md touched by this plan)
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status -q`
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_enrichment.py -q` — the D-10b vocabulary guard
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_headless_grant_boundary.py -q`
- `node --test tests/n8n/*.test.mjs` — 940 pass, untouched
- `git status --porcelain -- n8n/ scripts/build_cloud_workflows.py` — prints nothing
- `git diff --quiet 238d1ab -- operator-claude-plugin/scripts/scheduled_arm.py operator-claude-plugin/scripts/n8n_arming.py` — exits 0
</verification>

<success_criteria>
- The AUTO-04 reversal was put to the operator BEFORE any autonomy default was written, and the answer is recorded verbatim.
- Three named levels exist as settings keys; an install with no `autonomy` key reads all three ON through the real config loader.
- Every near-miss value and a malformed parent read OFF (the round asks).
- Neither arming script names an autonomy symbol and both are byte-identical to 238d1ab.
- Full plugin suite and the n8n harness are green; zero `n8n/` diff.
</success_criteria>

<output>
Create `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-01-SUMMARY.md` when done
</output>
