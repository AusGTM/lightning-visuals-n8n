---
phase: 67-an-autonomy-flag-with-sensible-defaults
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/skills/contact-upload/SKILL.md
  - operator-claude-plugin/skills/suggest-contacts/SKILL.md
  - operator-claude-plugin/tests/test_autonomy_switch_prose.py
autonomous: false
requirements: [AUTO-01, AUTO-02, AUTO-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "At each of the four batch skills, the single ask-or-proceed switch site Phase 68 left reads its own level through config_gate.autonomy_enabled — `write` at enrich-before-ingest, enrich-records and contact-upload, `spend_no_write` at suggest-contacts — and routes to the surviving two-phase ask when that level is off (AUTO-01, D-67-01, D-67-02)."
    - "On a `\"unknown\"` ceiling verdict the round states the blind spot and PROCEEDS; on an `\"over\"` verdict it relays proposal[detail] verbatim and STOPs; on a sampled verdict under the ceiling it proceeds with the ok line. No new refusal exists on `CEILING_UNKNOWN`, on an unreadable provider balance, or on a missing n8n_monthly_execution_allowance key (D-67-09, which reverses D-67-05; AUTO-03). [probe AUTO-03/boundary — explicit]"
    - statement: "No arithmetic, rounding or threshold is introduced by this plan — every ceiling and balance figure is write_grant.py's own, unchanged, and write_grant.py is byte-identical to 238d1ab."
      verification: backstop
    - "All three unknown states are named by cause in the pre-spend prose at all four skills: an unsampled or unreadable monthly ceiling, a provider balance the backend could not read (which the same envelope block already renders `unconfirmed`), and an unconfigured monthly allowance key (which is one of the causes of the first) — each disclosed, none refusing (AUTO-03, D-57-02, D-68-10)."
    - "A pre-start `\"over\"` ceiling refusal has no run to report: the relayed refusal plus the STOP is the whole account, and the affordable-subset split that would trim the batch to fit is named as unbuilt (D-67-13, D-67-08, RUN-05)."
    - "Every genuine decision point named in 68-CONTEXT.md still stops and asks under every autonomy setting: suggest-contacts' role selection, enrich-before-ingest's held-row approve/deny/pick/email vocabulary, enrich-records' undecided company-domain row, contact-upload's per-header confirmation. review-triage and backend-control are not edited at all (D-67-12, D-67-07)."
    - "The read_only level is named in config and covers status, queue and report reads, and it gates nothing today because no read-only skill asks anything — stated honestly rather than given an invented ask (D-67-01)."
  artifacts:
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
    - operator-claude-plugin/skills/enrich-records/SKILL.md
    - operator-claude-plugin/skills/contact-upload/SKILL.md
    - operator-claude-plugin/skills/suggest-contacts/SKILL.md
    - operator-claude-plugin/tests/test_autonomy_switch_prose.py
  key_links:
    - "config_gate.autonomy_enabled(config, level) -> the one ask-or-proceed switch site per skill -> Phase 68's stated path, or the pre-existing two-phase ask preserved as the interrupted path"
    - "write_grant.plan_grant's ceiling verdict -> the pre-spend disclosure sentence -> proceed on `unknown`, STOP on `over` (unchanged code, changed prose)"
  prohibitions:
    - "No refusal is added on CEILING_UNKNOWN, on an unread provider balance, or on a missing allowance key."
    - "Autonomy ON never bypasses CEILING_OVER or CapRefused; both stay refusals in code."
    - "The literal `\"unknown\"` and the phrase `not bounded by the monthly ceiling this run` survive at all four skills."
    - "The symbol `pre_spend_pause` appears exactly once per skill file; the new prose says `the pause` in words instead."
    - "No numbered step is added, removed or renumbered in any of the four files."
    - "review-triage/SKILL.md and backend-control/SKILL.md are not edited by this plan."
    - "No plugin script is edited by this plan; write_grant.py, scheduled_arm.py and n8n_arming.py stay byte-identical to 238d1ab."
    - "The literal substring `tier` is not introduced into any skills/*/SKILL.md body."
    - "No file under n8n/ and no change to scripts/build_cloud_workflows.py."
---

<objective>
Make the already-shipped proceed-without-asking behaviour readable as a setting, and replace
the three stale sentences that promise a fail-closed condition this phase deliberately does
not build.

Purpose: Phase 68 made "state the price and keep moving" unconditional for everyone. AUTO-01
requires that behaviour be expressed per level rather than as one hard-wired posture, and
AUTO-03 — as reversed by D-67-09 — requires the three unknown states be DISCLOSED and passed,
not refused. Three of the four skills still carry a forward reference saying Phase 67 will add
fail-closed conditions. That sentence is now wrong and must be retired at its own sites.

Output: one autonomy read plus one off-path sentence at each of the four batch skills, the
D-67-09 disclosure paragraph replacing the stale forward reference, and a new prose-contract
test pinning both.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-CONTEXT.md
@.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-PATTERNS.md
@.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-01-SUMMARY.md
@operator-claude-plugin/tests/test_implicit_approval_contract.py
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
</context>

<!-- The literals below are the exact strings this plan RETIRES; the executor must be
     able to find them verbatim to replace them, so each is allowlisted from the
     comment-text discipline gate (#429). -->
<!-- planner-discipline-allow: Phase 67's to -->
<!-- planner-discipline-allow: fail-closed -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the rendering of D-67-09 — what the round says when it cannot read a bound</name>
  <read_first>
    - `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-CONTEXT.md` — D-67-05 (superseded), D-67-09 (binding), D-67-13, D-67-08
    - `.planning/REQUIREMENTS.md` § AUTO — AUTO-03's rewritten text and its recorded reversal note
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` lines 292-310 — the exact pre-spend paragraph, its `"unknown"` literal, and the stale "the fail-closed conditions on an unsampled ceiling are Phase 67's to add" clause
    - `operator-claude-plugin/scripts/write_grant.py` lines 377-406 — `_headroom` and `_ceiling_line`, which already render `unconfirmed` for both an unreadable provider balance and an unsampled ceiling
    - `operator-claude-plugin/tests/test_implicit_approval_contract.py` — `test_the_unsampled_ceiling_verdict_is_named_and_disclosed`, the two literals that must survive
  </read_first>
  <decision>
Confirm the RENDERING of D-67-09 — the words an operator actually reads mid-round when a bound
cannot be read — and the two boundary cases words alone leave ambiguous: how much detail the
three unknown states get, and how the pre-start refusal's missing report is described.
  </decision>
  <context>
D-67-09 is locked and reverses D-67-05; this gate does not re-open it. Three of the four batch
skills currently end their pre-spend paragraph with "No Phase-68-only fence sits on top of that
— the fail-closed conditions on an unsampled ceiling are Phase 67's to add." That promise is now
wrong and must be retired at its own sites.

The proposed replacement says four things. **(1) The sentence:** Phase 67 adds no fence here
either — an autonomous round discloses an unknown state and proceeds, exactly as the attended
path already does (D-57-02, D-68-10); the bounds that remain are an `"over"` ceiling verdict and
`CapRefused`. **(2) The three unknowns, named by cause:** an unsampled or unreadable monthly
ceiling; a provider balance the backend could not read (which `write_grant._headroom` already
renders `unconfirmed` and never reads as headroom, and which two of three providers already hit
on this account); and an unconfigured `n8n_monthly_execution_allowance` key, which is one of the
CAUSES of the first rather than a separate fourth check. Today's prose names only the first.
**(3) The pre-start refusal (D-67-13, D-67-08 / RUN-05):** an `"over"` verdict refuses the whole
batch before anything runs, and `plan_grant` writes nothing durable on that refusal, so there is
no run and no end-of-run report for it to land in — the relayed `proposal["detail"]` plus the
STOP is the entire account, and trimming to an affordable subset is not built. (The MID-RUN
ceiling stop is a different case and is plan 67-03's.) **(4) The off-path:** when a level's
autonomy is off the round takes the pre-existing two-phase ask Phase 68 preserved verbatim, and
the pointing sentence names `autonomy.<level>` and `operator.local.json` so an operator knows how
to change it — this line is also AUTO-02's first-round notice at the round itself.
  </context>
  <options>
    <option id="as-proposed">
      <name>Ship all four as described</name>
      <pros>The stale promise is retired at its own sites; each unknown is named by the cause an operator would act on; the missing report for a pre-start refusal is a stated limitation rather than a silence.</pros>
      <cons>The pre-spend paragraph grows by roughly three sentences at four skills — more prose at the moment the operator is being asked to read least.</cons>
    </option>
    <option id="unknowns-undifferentiated">
      <name>State the three unknowns as one sentence ("some figures could not be read")</name>
      <pros>Shortest possible pre-spend line; the blind spot is still disclosed.</pros>
      <cons>Loses the cause — an unconfigured allowance key is fixed in thirty seconds, an unreadable Apollo balance is not fixable at all, and one sentence cannot tell them apart.</cons>
    </option>
    <option id="off-path-only">
      <name>Add only the off-path line, leave the existing forward reference in place</name>
      <pros>Smallest diff at the four skills; nothing pinned is disturbed.</pros>
      <cons>Ships a false promise — the prose would keep telling operators that Phase 67 adds fail-closed conditions that D-67-09 reversed and this phase does not build.</cons>
    </option>
    <option id="refusal-silent">
      <name>Leave the pre-start refusal's missing report unstated</name>
      <pros>Avoids naming an unbuilt feature (RUN-05) in operator-facing prose.</pros>
      <cons>Under autonomy an over-ceiling batch would read as a silent no-op — exactly the outcome D-67-13 says must never happen.</cons>
    </option>
  </options>
  <resume-signal>Answer with one option id, or `as-proposed` with the specific wording you want changed. The answer is recorded in `67-02-SUMMARY.md` and is what Task 2 writes.</resume-signal>
  <acceptance_criteria>
    - The question showed the exact replacement text for the stale clause, quoted the clause being replaced, and named D-67-09 as the reversal of D-67-05 by id.
    - The operator's answer names, for each of the four items, either the proposal or a stated alternative, and is recorded in `67-02-SUMMARY.md`.
    - No file under `operator-claude-plugin/` was modified by this task.
  </acceptance_criteria>
  <reversibility rating="one-way">D-67-09 accepts that credits an unattended round spends under an unsampled ceiling are not recoverable; this gate confirms the words that disclose it before they ship.</reversibility>
  <verify>
    <automated>git status --porcelain -- operator-claude-plugin/</automated>
    <fails_when>prints any line — this task changes no file</fails_when>
  </verify>
  <done>The rendering is confirmed on all four items and recorded; Task 2 has exact wording to write.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One skill, end to end — the level is read, the off-path is named, the stale promise is gone</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_autonomy_switch_prose.py</files>
  <read_first>
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` lines 276-332 — the whole implicit-open span: the "does not ask — it states" paragraph, the `plan_grant` fence, the `"unknown"` sentence, the refusal rule, the pause, `open_grant`
    - `operator-claude-plugin/tests/test_implicit_approval_contract.py` — `TARGETS`, `_normalized`, `_numbered_step_spans`, `_step`, `_open_span`, and every assertion that must keep passing
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py` lines 123-160 — `sequences_in`: only a fenced python block with TWO OR MORE scripts-module calls acquires a registry identity
    - `operator-claude-plugin/tests/test_report_enrichment.py` lines 666-681 — the case-insensitive `icp`/`tier` substring ban on every SKILL.md body
    - `operator-claude-plugin/scripts/config_gate.py` — `autonomy_enabled`'s signature as landed by 67-01
    - Task 1's recorded rendering
  </read_first>
  <behavior>
    - `enrich-before-ingest/SKILL.md`'s step 5 span contains exactly one `config_gate.autonomy_enabled(` call, passing the level string `"write"`.
    - That call sits in its own fenced python block containing exactly one scripts-module call, so `test_skill_sequence_coverage.py` acquires no new registry identity and needs no edit.
    - The autonomy read appears BEFORE `write_grant.plan_grant(` in the same span; the pre-existing `plan_grant` -> pause -> `open_grant` order is unchanged.
    - The span names `autonomy.write` and `operator.local.json` in the off-path sentence, and points at the pre-existing two-phase ask rather than describing a new one.
    - The string `Phase 67's to` no longer appears anywhere in the file.
    - The literal `"unknown"` and the phrase `not bounded by the monthly ceiling this run` both still appear in the span.
    - The file contains the symbol `pre_spend_pause` exactly once (the pre-existing call), and the new prose refers to the pause in words only.
    - Neither `icp` nor `tier` appears anywhere in the file, case-insensitively.
  </behavior>
  <action>
Edit ONLY `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md`, inside step 5's
existing span. Add nothing outside it; add no numbered step; renumber nothing.

**(a) The autonomy read.** Immediately before the "**With no write grant open, this run does
not ask — it states.**" paragraph, add a short paragraph and its own single-call python fence
binding `proceed_without_asking = config_gate.autonomy_enabled(config, "write")`. Keep the
fence to that one call — a second scripts-module call in the same fence would create a new
sequence identity the ratchet demands be registered. The paragraph states, in the operator's
register: this round proceeds without asking because the `write` level of autonomy is on, and
an admin turns it off by setting `autonomy.write` to `false` in `operator.local.json`. Say
plainly that the level authorises nothing on its own — `allow_write_grants` still gates this
interactive path and `ALLOW_N8N_ARM` still gates the scheduled and cron paths, unchanged
(D-67-02).

**(b) The off-path.** One sentence after the read: when `proceed_without_asking` is false, do
not take the stated path — take the two-phase ask this skill already documents (the same
ungranted path Phase 68 preserved), and nothing else changes. Refer to the pause in words
("the few seconds before anything is spent"), never by the symbol name, or the once-only pause
assertion fails.

**(c) The D-67-09 disclosure.** Replace the trailing clause "No Phase-68-only fence sits on top
of that — the fail-closed conditions on an unsampled ceiling are Phase 67's to add." with
Task 1's confirmed text: Phase 67 adds no fence here either (D-67-09) — an autonomous round
discloses an unknown state and proceeds, and the bounds that remain are an `"over"` ceiling
verdict and `CapRefused`. Then add the confirmed sentence naming all three unknowns by cause,
including the provider balance the backend could not read, which the same envelope block
already prints `unconfirmed` for and which is never read as headroom. Leave the sentence
carrying `"unknown"` and `not bounded by the monthly ceiling this run` intact — both are
pinned literals.

**(d) The pre-start refusal limitation (D-67-13 / RUN-05).** Extend the existing "**If
`plan_grant` refuses**" paragraph with one sentence: an `"over"` verdict refuses the whole
batch before anything starts, so there is no run and no end-of-run report for that refusal to
appear in — the relayed detail and the stop are the account — and trimming the batch to an
affordable subset is not built (name it as a stated limitation, not a defect). Do not weaken
the existing STOP wording or the `authorize_ungranted_send` sentence; both are pinned.

**(e) The test.** Create `operator-claude-plugin/tests/test_autonomy_switch_prose.py` with a
`TARGETS`-style table keyed by skill, holding this task's one entry for now (path, the level
string, the open-step numbers `(5,)`), plus helpers copied in the idiom
`test_implicit_approval_contract.py` uses (`_normalized`, `_numbered_step_spans`, `_step`) —
copied rather than imported, matching that file's own stated reason for staying separate.
Assert every bullet in `<behavior>`. Write it RED first against the unedited file.

Two commits: `test(67-02): ...` then `feat(67-02): ...`. Confirm RED by reading pytest's
failure output.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'config_gate.autonomy_enabled(config, "write")' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 1.
    - `/usr/bin/grep -c "Phase 67's to" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 0.
    - `/usr/bin/grep -c 'pre_spend_pause' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 1.
    - `/usr/bin/grep -c 'not bounded by the monthly ceiling this run' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 1.
    - `/usr/bin/grep -c 'autonomy.write' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1, and `/usr/bin/grep -c 'operator.local.json' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - `/usr/bin/grep -ic 'tier' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 0.
    - `/usr/bin/grep -c 'D-67-09' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q` passes with `test_skill_sequence_coverage.py` itself unmodified (`git diff --quiet 238d1ab -- operator-claude-plugin/tests/test_skill_sequence_coverage.py` exits 0).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_autonomy_switch_prose.py operator-claude-plugin/tests/test_implicit_approval_contract.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q</automated>
    <fails_when>exits non-zero — most likely `test_the_unsampled_ceiling_verdict_is_named_and_disclosed` or `test_pre_spend_pause_appears_exactly_once_and_never_in_a_no_pause_step` failing on the new prose</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_report_enrichment.py -q</automated>
    <fails_when>exits non-zero, printing `UNREGISTERED SKILL SEQUENCE` for enrich-before-ingest, or naming a forbidden `icp`/`tier` substring in a skill body</fails_when>
  </verify>
  <done>enrich-before-ingest reads its level, names its off-path and its settings key, discloses all three unknowns without refusing, and states the pre-start refusal limitation; both commits are on the branch and the ratchet file is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The same three edits at the remaining three skills, at their own levels</name>
  <files>operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/skills/contact-upload/SKILL.md, operator-claude-plugin/skills/suggest-contacts/SKILL.md, operator-claude-plugin/tests/test_autonomy_switch_prose.py</files>
  <read_first>
    - `operator-claude-plugin/skills/enrich-records/SKILL.md` lines 186-300 — steps 5 and 6, the split open span
    - `operator-claude-plugin/skills/contact-upload/SKILL.md` lines 230-335 — steps 4 and 5, the split open span
    - `operator-claude-plugin/skills/suggest-contacts/SKILL.md` lines 96-178 — steps 3 and 4, the split open span; note it has NO "Phase 67's to add" clause, only the shared `"unknown"` sentence at step 4
    - `operator-claude-plugin/tests/test_implicit_approval_contract.py` — `TARGETS`' `open_steps` per skill: enrich-records `(5, 6)`, contact-upload `(4, 5)`, suggest-contacts `(3, 4)`
    - `operator-claude-plugin/tests/test_disclosure_audit.py` — `PRESERVED_LITERALS` and `SUGGEST_CONTACTS_ROLE_ASK`, the genuine decision points that must survive untouched
    - Task 2's landed edit in `enrich-before-ingest/SKILL.md`, as the shape to mirror
  </read_first>
  <behavior>
    - Each of the three files' open span contains exactly one `config_gate.autonomy_enabled(` call, in its own single-call fence, before that span's `write_grant.plan_grant(` call.
    - The level string is `"write"` at enrich-records and contact-upload, and `"spend_no_write"` at suggest-contacts.
    - The string `Phase 67's to` appears in no skill file at all.
    - All four files carry the D-67-09 disclosure sentence and the three-unknowns sentence; suggest-contacts gets both appended after its existing `"unknown"` sentence at step 4.
    - Each file still contains `pre_spend_pause` exactly once, and `"unknown"` plus `not bounded by the monthly ceiling this run` in its open span.
    - Every `PRESERVED_LITERALS` entry and both suggest-contacts split literals are still present, normalized.
    - `review-triage/SKILL.md` and `backend-control/SKILL.md` are byte-identical to 238d1ab.
  </behavior>
  <action>
Apply Task 2's four edits (a)-(d) to the three remaining skills, mirroring its wording so a
reader comparing the four finds them saying the same thing — the same discipline 68-03 used for
the interrupt statement.

Per skill:
- **enrich-records** — level `"write"`. The autonomy read goes in step 5, before the
  `plan_grant` fence. Replace step 5's "Phase 67's to add" clause. The off-path sentence points
  at this skill's own ungranted two-phase ask.
- **contact-upload** — level `"write"`. The autonomy read goes in step 4, before its
  `plan_grant` fence. Replace step 4's "Phase 67's to add" clause.
- **suggest-contacts** — level `"spend_no_write"`; this lane matches and proposes and is bounded
  by provider credit rather than by a CRM write, which is why it is not the `write` level. The
  autonomy read goes in step 3, before its `plan_grant` fence. This file has no "Phase 67's to
  add" clause to replace — append the D-67-09 disclosure and the three-unknowns sentence after
  its existing `"unknown"` / `not bounded by the monthly ceiling this run` sentence at step 4.
  Do NOT touch step 3's role ask, its stated cap default of 2, or its `CapRefused` relay
  paragraph — all three are pinned genuine decision points, and the role ask in particular stays
  a real question under every autonomy setting.

In the same commit, extend `tests/test_autonomy_switch_prose.py`'s `TARGETS` to all four
skills with their own level strings and open-step tuples, parametrize every assertion over
it, and add three structural assertions: `TARGETS` has exactly four entries; every path in it
exists on disk; and the string `Phase 67's to` appears in zero files under
`operator-claude-plugin/skills/`. Also assert, positively, that `review-triage/SKILL.md`
contains no `autonomy_enabled` call and that `backend-control/SKILL.md` contains none either
— D-67-12's "in no level" recorded as a checked fact rather than an omission.

RED first: widen `TARGETS` to four before making the three prose edits, observe the three new
failures, then edit for GREEN. Two commits, `test(67-02): ...` then `feat(67-02): ...`.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'config_gate.autonomy_enabled(config, "write")' operator-claude-plugin/skills/enrich-records/SKILL.md` is 1 and the same for `contact-upload/SKILL.md`.
    - `/usr/bin/grep -c 'config_gate.autonomy_enabled(config, "spend_no_write")' operator-claude-plugin/skills/suggest-contacts/SKILL.md` is 1.
    - `/usr/bin/grep -rlc "Phase 67's to" operator-claude-plugin/skills/` returns no files.
    - `/usr/bin/grep -c 'pre_spend_pause' operator-claude-plugin/skills/enrich-records/SKILL.md` is 1, and the same for `contact-upload/SKILL.md` and `suggest-contacts/SKILL.md`.
    - `/usr/bin/grep -c 'D-67-09' operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1, and the same for `contact-upload/SKILL.md` and `suggest-contacts/SKILL.md`.
    - `/usr/bin/grep -ic 'tier' operator-claude-plugin/skills/enrich-records/SKILL.md operator-claude-plugin/skills/contact-upload/SKILL.md operator-claude-plugin/skills/suggest-contacts/SKILL.md` reports 0 for each file.
    - `git diff --quiet 238d1ab -- operator-claude-plugin/skills/review-triage/SKILL.md operator-claude-plugin/skills/backend-control/SKILL.md` exits 0.
    - `git diff --name-only 238d1ab -- operator-claude-plugin/scripts/` lists only `config_gate.py` (plan 01's change) and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_autonomy_switch_prose.py operator-claude-plugin/tests/test_implicit_approval_contract.py operator-claude-plugin/tests/test_disclosure_audit.py operator-claude-plugin/tests/test_interrupt_semantics.py operator-claude-plugin/tests/test_enrich_skill_contract.py -q</automated>
    <fails_when>exits non-zero — a preserved decision-point literal was disturbed, a pause count moved off 1, or a skill's open span lost its `"unknown"` disclosure</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status -q</automated>
    <fails_when>exits non-zero, printing `UNREGISTERED SKILL SEQUENCE` for one of the three skills, or a forbidden `icp`/`tier` substring hit</fails_when>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
    <fails_when>exits non-zero, or reports fewer than 2646 passed</fails_when>
    <automated>git status --porcelain -- n8n/ scripts/build_cloud_workflows.py</automated>
    <fails_when>prints any line</fails_when>
  </verify>
  <done>All four batch skills read their own level at the one switch site Phase 68 left, name their settings key and their off-path, disclose all three unknowns without refusing, and state the pre-start refusal limitation; review-triage and backend-control are untouched; the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator settings file -> the round's ask-or-proceed branch | a JSON value now decides whether a spend/write round stops to ask |
| plugin round -> provider credit and HubSpot writes | the spend boundary the pre-spend disclosure and the pause guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-06 | Elevation of Privilege | the autonomy read at the four switch sites | high | mitigate | The read decides only whether the round ASKS. The two authority gates are untouched and are named in the same paragraph; `plan_grant` still refuses an ungranted round, and the refusal STOP wording is pinned by `test_implicit_approval_contract.py`. |
| T-67-07 | Tampering | a disclosure edit that softens a refusal | high | mitigate | `CEILING_OVER` and `CapRefused` stay refusals in `write_grant.py`, which this plan does not open; the STOP and `authorize_ungranted_send` literals are pinned by the pre-existing contract test, re-run in every task's verify. |
| T-67-08 | Repudiation | an unknown bound passed silently | medium | mitigate | All three unknown causes are named in the pre-spend line at all four skills and pinned by `test_autonomy_switch_prose.py`; the envelope block's own `unconfirmed` rendering is unchanged. |
| T-67-09 | Information Disclosure | new operator-facing prose | low | mitigate | The added sentences name key names and file names only — no secret, no configured value, no record identifier. |
| T-67-10 | Denial of Service | a genuine decision point auto-answered | medium | mitigate | Role selection, held-row vocabulary, per-header confirmation and the undecided company-domain row are explicitly out of scope and pinned present by `test_disclosure_audit.py`, re-run in Task 3's verify. |
| T-67-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package (67-RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<artifacts>
## Artifacts this phase produces (plan 02)

**New test file:** `operator-claude-plugin/tests/test_autonomy_switch_prose.py` — with
`TARGETS` (four entries: skill name -> path, level string, open-step tuple), the copied
`_normalized` / `_numbered_step_spans` / `_step` helpers, parametrized behaviour assertions,
and three structural assertions (exact `TARGETS` length, every path exists, `Phase 67's to`
absent from every skill).

**New prose, per batch skill** (4 files): one autonomy-read paragraph plus its single-call
python fence binding `proceed_without_asking`; one off-path sentence naming
`autonomy.<level>` and `operator.local.json`; the D-67-09 disclosure sentence; the
three-unknowns sentence; the pre-start refusal limitation sentence naming the unbuilt
affordable-subset split.

**Retired string:** `Phase 67's to add` / `Phase 67's to` — removed from every skill body.

**No new script symbols.** No plugin script is edited by this plan.
</artifacts>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q` — no new registry identity required
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_sufficiency.py::test_no_plugin_script_polls_sleeps_or_loops_on_execution_status -q`
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_headless_grant_boundary.py -q`
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_enrichment.py -q` — D-10b guard
- `node --test tests/n8n/*.test.mjs` — 940 pass, untouched
- `git status --porcelain -- n8n/ scripts/build_cloud_workflows.py` — prints nothing
- `git diff --quiet 238d1ab -- operator-claude-plugin/scripts/scheduled_arm.py operator-claude-plugin/scripts/n8n_arming.py operator-claude-plugin/scripts/write_grant.py` — exits 0
- Read the four pre-spend paragraphs side by side: they must say the same thing in the same register.
</verification>

<success_criteria>
- Each of the four batch skills reads its own autonomy level once, at the one ask-or-proceed site Phase 68 left, and names how to turn it off.
- No refusal exists anywhere on `CEILING_UNKNOWN`, an unread provider balance, or a missing allowance key; all three are disclosed by cause and passed.
- `CEILING_OVER` and `CapRefused` still refuse, and the pre-start refusal's lack of a report is stated as a named limitation with RUN-05 unbuilt.
- Every genuine decision point still asks; `review-triage` and `backend-control` are byte-identical to 238d1ab.
- No plugin script changed; full suite and n8n harness green; zero `n8n/` diff.
</success_criteria>

<output>
Create `.planning/phases/67-an-autonomy-flag-with-sensible-defaults/67-02-SUMMARY.md` when done
</output>
