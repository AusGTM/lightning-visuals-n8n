---
phase: 75-config-driven-region-whitelist-and-scoring-version-staleness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/icp_scoring.yaml
  - scripts/gen_icp_scoring_js.py
  - n8n/code/icpScoring.generated.js
  - scripts/build_cloud_workflows.py
  - src/icp_scoring.py
  - tests/test_icp_scoring_generated_currency.py
  - tests/test_rubric_change_guard.py
  - tests/test_scoring_parity.py
  - tests/test_icp_scoring.py
  - tests/test_veto_remediation_report.py
  - tests/test_simulate_rubric_weights.py
  - tests/test_remediate_veto_companies.py
  - tests/test_backfill_dry_run.py
  - tests/n8n/antiIcpFlagMirror.test.mjs
  - tests/n8n/companyRecomputeLaneFlow.test.mjs
  - tests/n8n/materialConflictNoVetoFlip.test.mjs
  - tests/n8n/reviewQueueEndpoint.test.mjs
  - tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_scheduled_maintenance_cloud.json
autonomous: true
requirements: []

estimate:
  tokens: 140000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-75-05: config/icp_scoring.yaml carries regions.home = [AU, NZ, ANZ, US, GB, IE, CA, ZA, HK, SG, AE, IN] with the selection-criteria comment, and a company whose region resolves into that set earns the geography points and never fires the geography veto."
    - "D-75-01: a company with a KNOWN region outside regions.home fires the hard veto carrying the single renamed string \"Outside target regions\" — byte-identical from both engines."
    - "D-75-02: all three hard-veto reason strings are read from config/icp_scoring.yaml (hard_vetoes.<key>.reason) — zero hard-typed reason literals remain in scripts/build_cloud_workflows.py's Decide Company Action; the content and hardware VALUES are unchanged."
    - "D-75-03: config.version is bumped to an opaque new string and Decide Company Action stamps properties.lv_icp_scoring_version = VERSION on every company it decides, so an already-vetoed record is refreshed by the bump + sweep alone with no dedicated remediation script."
    - "D-75-04: every file under tests/n8n/fixtures/frozen/ and tests/stress-tests/ is byte-identical to its pre-plan content; only tests that ASSERT the reason string change."
    - "D-75-20: tests/test_rubric_change_guard.py's PINNED_BASE_SCORE is re-baselined to the home/other/unknown geography keys in this same commit, and its AssertionError message names lv_icp_scoring_version as the segmentation mechanism that now exists."
    - "A blank/absent lv_country_region_normalized resolves to region_key \"unknown\", contributes 0 geography points, and fires NO veto in either engine (2026-08-10 three-state rule)."
  artifacts:
    - scripts/gen_icp_scoring_js.py
    - n8n/code/icpScoring.generated.js
    - tests/test_icp_scoring_generated_currency.py
    - config/icp_scoring.yaml
  key_links:
    - "config/icp_scoring.yaml -> scripts/gen_icp_scoring_js.py::render() -> n8n/code/icpScoring.generated.js -> inline() into ENRICH_DECIDE_CO_CLOUD"
    - "src/icp_scoring.py::compute_icp_score and ENRICH_DECIDE_CO_CLOUD's veto block read the SAME regions.home / hard_vetoes reasons (Phase 46 parity, one commit)"
  prohibitions:
    - "[unverified] A blank or unknown region NEVER sets lv_anti_icp_flag in either engine."
    - "[unverified] An empty or missing regions.home NEVER silently degrades to veto-everything or veto-nothing — scripts/gen_icp_scoring_js.py::render() raises instead."
    - "[unverified] No file under tests/n8n/fixtures/frozen/ is rewritten by this plan."
    - "[unverified] No hand-edit of any n8n/wf_*.json — every JSON change comes from scripts/build_cloud_workflows.py."
---

<objective>
Make `config/icp_scoring.yaml` the single source of the geography rule and of all three
hard-veto reason strings, and prove it end-to-end through BOTH scoring engines in one commit.

Purpose: today the geography whitelist is three hand-typed string literals in two different
runtimes (`src/icp_scoring.py:124` and `scripts/build_cloud_workflows.py:5118`) and the veto
reason is a hard-coded literal in one of them. Expanding target markets is currently a
two-runtime code edit; after this plan it is a one-file yaml edit.

Output: `scripts/gen_icp_scoring_js.py`, `n8n/code/icpScoring.generated.js`, a re-shaped
`config/icp_scoring.yaml`, both engines reading the generated/loaded constants, a currency
test, and regenerated workflow JSON.

**Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).**
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-CONTEXT.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-RESEARCH.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-PATTERNS.md
</context>

## Artifacts this phase produces

New across the whole of Phase 75 (this plan creates the first four):

| Artifact | Kind | Plan |
|---|---|---|
| `scripts/gen_icp_scoring_js.py` (`render()`, `main()`) | new file | 01 |
| `n8n/code/icpScoring.generated.js` (`VERSION`, `REGIONS_HOME`, `REGION_ALIASES`, `HARD_VETO_REASONS`, `GEOGRAPHY_POINTS`) | new generated file | 01 |
| `tests/test_icp_scoring_generated_currency.py` | new test | 01 |
| yaml keys `regions.home`, `regions.aliases`, `hard_vetoes.outside_home_regions`, `base_score.geography.home/other/unknown`, bumped `version` | new config keys | 01 |
| `src/icp_scoring.py::regions_home()`, `::region_aliases()` | new functions | 02 |
| `tests/n8n/regionAliasParity.test.mjs` | new test | 02 |
| `config/hubspot_properties.yaml` options `GB, IE, CA, ZA, HK, SG, AE, IN`; `UK` `hidden: true`; new property `lv_icp_scoring_version` | new config entries | 02 |
| builder constant `ALLOW_HUBSPOT_RECOMPUTE_WRITES` (`WRITE_SAFETY_DEFAULTS`) | new flag | 03 |
| `_writeSafetyAllows` `"recompute"` branch; row fields `recompute_reason` | new code paths | 03 |
| `tests/n8n/companyVersionStaleRecompute.test.mjs`, `tests/n8n/sj2VersionStaleGate.test.mjs` | new tests | 03 |
| `scripts/gen_geography_flow.py` | new file | 04 |
| `scripts/sync_hubspot_properties.py::_update_property_options_live`, `compute_property_diff`'s `update` bucket | new functions | 04 |
| `tests/test_geography_flow_conformance.py`, `tests/test_recompute_flag_isolation.py` | new tests | 04 |
| live HubSpot property `lv_icp_scoring_version` + 8 new enum options + `UK` hidden | live schema | 05 |
| fresh `config/hubspot_migration/baseline/portal-schema-companies-*.json` | new snapshot | 05 |
| `.planning/phases/75-.../75-UAT.md`, `75-DEPLOY-RECORD.md` | new docs | 06 |
| `.planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md` (`kind: design`) | new todo | 06 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Config is the source of truth — yaml to generated JS to BOTH engines, end to end</name>
  <files>config/icp_scoring.yaml, scripts/gen_icp_scoring_js.py, n8n/code/icpScoring.generated.js, scripts/build_cloud_workflows.py, src/icp_scoring.py</files>
  <read_first>
    - config/icp_scoring.yaml (the file being re-shaped — current `base_score.geography` and `hard_vetoes` blocks)
    - scripts/gen_escalation_js.py (the generator precedent being copied — `json.dumps` literals, `render()`, `main()`)
    - n8n/code/escalation.generated.js (the artifact shape and DO-NOT-EDIT banner convention)
    - src/icp_scoring.py lines 70-200 (`compute_icp_score`: `region_raw`/`region`/`region_key` split at ~118-126, the three `cfg["hard_vetoes"][...]["reason"]` lookups at ~175-200)
    - scripts/build_cloud_workflows.py lines 5030-5060 (`ENRICH_DECIDE_CO_CLOUD`'s `inline(...)` argument list) and 5105-5165 (`_regionKey`, the three `vetoReasons.push(...)` literals, the `properties.lv_anti_icp_flag`/`_num`/`_reason` assignment block)
    - scripts/build_cloud_workflows.py lines 45-70 (the `import gen_escalation_js` + `(CODE / "escalation.generated.js").write_text(...)` build-step registration pair)
    - scripts/build_cloud_workflows.py lines 148-172 (`strip_module`, `inline` — why a referenced constant's defining module must be inlined FIRST)
  </read_first>
  <behavior>
    - A company with `lv_country_region_normalized = "AU"` scores 10 geography points, `region_key` is `"home"`, and fires no geography veto — identical from `src/icp_scoring.py::compute_icp_score` and from the Decide Company Action Code node.
    - A company with `lv_country_region_normalized = "DE"` scores 0 geography points, `region_key` is `"other"`, and fires exactly one geography veto carrying the string `"Outside target regions"` — identical from both engines.
    - A company with `lv_country_region_normalized` blank/absent scores 0 geography points, `region_key` is `"unknown"`, and fires NO veto — identical from both engines (the 2026-08-10 three-state rule).
    - A company with `lv_country_region_normalized = "US"` scores 10 geography points and fires no veto (the whitelist expansion is live, not just structural).
    - `scripts/gen_icp_scoring_js.py::render()` raises when `regions.home` is absent, empty, or not a list of non-empty strings, and when any `hard_vetoes.<key>.reason` is absent or empty.
  </behavior>
  <action>
Re-shape `config/icp_scoring.yaml` and make BOTH engines read it, in ONE commit (Phase 46 parity
rule, CLAUDE.md §10.3.1 — `src/icp_scoring.py` and Decide Company Action never land apart).

yaml changes, per D-75-05 / D-75-01 / D-75-02 / D-75-03:
- Bump `version` from `lv-icp-v0.1` to `lv-icp-v0.2` (an opaque bumped string — no semver
  meaning, no timestamp).
- Add a top-level `regions:` block with `home:` = the 12 codes `AU, NZ, ANZ, US, GB, IE, CA,
  ZA, HK, SG, AE, IN` and an `aliases:` mapping (see Task 2 of plan 02 for the full alias
  contents — this task seeds only the codes needed for the tracer: `australia: AU`, `au: AU`,
  `aus: AU`, `new zealand: NZ`, `nz: NZ`, `anz: ANZ`, `us: US`, `united states: US`,
  `de: Other` is NOT added — an unmapped known country must fall through to `Other`).
  Above `home:`, record the selection criteria verbatim as a yaml comment: English-speaking or
  native-English-audience market with a strong sports-broadcasting ecosystem; considered and
  NOT included: NG, KE, JM, PH.
- Replace `base_score.geography`'s five region-code keys with exactly three: `home: 10`,
  `other: 0`, `unknown: 0` (Claude's discretion default — flat, no tiering).
- Rename the `hard_vetoes.non_anz` key to `hard_vetoes.outside_home_regions` and change its
  `reason` value to `"Outside target regions"`. `hard_vetoes.no_content` and
  `hard_vetoes.hardware_vendor` keep their current keys AND their current reason VALUES
  byte-for-byte.

Create `scripts/gen_icp_scoring_js.py` mirroring `scripts/gen_escalation_js.py` exactly: same
shebang/header comment shape (including the ponytail line about `json.dumps` handling all
JS-literal escaping), `ROOT`/`sys.path.insert`, `from src.icp_scoring import load_yaml`, an
`OUT` pointing at `n8n/code/icpScoring.generated.js`, a `render() -> str` and a `main()`.
`render()` emits a DO-NOT-EDIT banner naming `config/icp_scoring.yaml` and the regenerate
command, then five `const` declarations via `json.dumps`: `VERSION` (from `cfg["version"]`),
`REGIONS_HOME` (from `cfg["regions"]["home"]`), `REGION_ALIASES` (from
`cfg["regions"]["aliases"]`), `HARD_VETO_REASONS` (a dict comprehension mapping each
`hard_vetoes` key to its `reason`), `GEOGRAPHY_POINTS` (from `cfg["base_score"]["geography"]`),
then a `module.exports` naming all five. Before emitting, `render()` asserts fail-closed
(threat T-75-01): `regions.home` is a non-empty list whose every member is a non-empty string,
and every `hard_vetoes.<key>` carries a non-empty string `reason`. An assertion message names
the offending key. Run the script once to write the checked-in artifact.

Register the generator in `scripts/build_cloud_workflows.py` beside the existing
`gen_escalation_js` pair: an `import gen_icp_scoring_js  # noqa: E402` and a
`(CODE / "icpScoring.generated.js").write_text(gen_icp_scoring_js.render())` line, placed
BEFORE any `inline(...)` call that references the new module.

Add `"icpScoring.generated.js"` as the FIRST argument of `ENRICH_DECIDE_CO_CLOUD`'s existing
`inline(...)` call (generated modules first, consumers after — the convention that call already
follows for `taxonomy.generated.js`/`hubspotEnums.generated.js`). Declaration order matters:
`inline()` concatenates stripped sources and n8n Code nodes cannot `require()` a sibling.

In `ENRICH_DECIDE_CO_CLOUD`'s JS body: rewrite `_regionKey(v)` to return `"unknown"` when `v`
is `undefined`/`null`/`""`, else `"home"` when `REGIONS_HOME.includes(v)`, else `"other"` —
keeping its whole existing comment block (the blank-region incident narrative) intact and
adding one line naming `REGIONS_HOME` as the generated source. Replace the three
`vetoReasons.push("...")` string literals with `HARD_VETO_REASONS.outside_home_regions`,
`HARD_VETO_REASONS.no_content` and `HARD_VETO_REASONS.hardware_vendor` respectively, and change
the geography predicate from `region === "non_anz"` to `region === "other"`. Leave the
`producesContent === false` and `isHardwareVendor === true || orgType === "hardware_vendor"`
predicates untouched. Immediately after the existing
`properties.lv_anti_icp_reason = ...` line, add `properties.lv_icp_scoring_version = VERSION;`
— this is the D-75-03 / D-75-20 stamp, unconditional on every company this node decides, so
the bump + sweep is the only refresh mechanism a renamed record needs.

In `src/icp_scoring.py::compute_icp_score`: replace the
`region_key = region if region in ["AU", "NZ", "ANZ"] else "non_anz"` line with a set-membership
test against `cfg["regions"]["home"]` yielding `"home"` or `"other"`; leave the
`if not region_raw: region_key = "unknown"` guard above it exactly as it is (that guard IS the
three-state rule). Change the veto branch from `if region_key == "non_anz"` to
`if region_key == "other"` and its reason lookup from `cfg["hard_vetoes"]["non_anz"]["reason"]`
to `cfg["hard_vetoes"]["outside_home_regions"]["reason"]` — note this is a yaml-KEY exposure
independent of the string rename, and a `KeyError` at score time if missed. The breakdown
component must keep `signal: "geography"` and `value: region` unchanged
(`scripts/backfill_seed_company_scores.py::compute_components` reads that component name).

Regenerate the workflow JSON with `.venv/bin/python scripts/build_cloud_workflows.py`. Do NOT
hand-edit any `n8n/wf_*.json`.
  </action>
  <reversibility rating="costly">The reason-string rename churns `lv_anti_icp_reason` once on every currently-vetoed record at its next recompute, and the version bump marks the entire scored population stale. Both are data-visible; neither is destructive.</reversibility>
  <verify>
    <automated>.venv/bin/python scripts/gen_icp_scoring_js.py && .venv/bin/python scripts/build_cloud_workflows.py && node -e "const w=require('./n8n/wf_enrichment_cloud.json'); const n=w.nodes.find(n=>n.name==='Decide Company Action'); const c=n.parameters.jsCode; const need=['const REGIONS_HOME','const HARD_VETO_REASONS','const VERSION','HARD_VETO_REASONS.outside_home_regions','lv_icp_scoring_version']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING from Decide jsCode:',miss);process.exit(1);} if(c.includes('\"Non-ANZ geography\"')){console.error('hard-typed geography reason still present');process.exit(1);} if(c.indexOf('const REGIONS_HOME')>c.indexOf('function _regionKey')){console.error('generated module inlined AFTER its consumer');process.exit(1);} console.log('decide node reads generated constants');"</automated>
    <fails_when>non-zero exit, or the output contains "MISSING from Decide jsCode", "hard-typed geography reason still present", or "generated module inlined AFTER its consumer"</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_icp_scoring.py tests/test_scoring_parity.py -k "not live"</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed", or any line contains "KeyError"</fails_when>
  </verify>
  <acceptance_criteria>
    - `n8n/code/icpScoring.generated.js` exists, carries a `GENERATED FROM config/icp_scoring.yaml — DO NOT EDIT.` banner, and declares exactly `VERSION`, `REGIONS_HOME`, `REGION_ALIASES`, `HARD_VETO_REASONS`, `GEOGRAPHY_POINTS` plus a `module.exports` naming all five.
    - `node -e "const m=require('./n8n/code/icpScoring.generated.js'); if(m.VERSION!=='lv-icp-v0.2')process.exit(1); if(m.REGIONS_HOME.length!==12)process.exit(1); if(m.HARD_VETO_REASONS.outside_home_regions!=='Outside target regions')process.exit(1); if(JSON.stringify(m.GEOGRAPHY_POINTS)!==JSON.stringify({home:10,other:0,unknown:0}))process.exit(1); console.log('ok')"` prints `ok`.
    - `/usr/bin/grep -c 'Non-ANZ geography' scripts/build_cloud_workflows.py` prints `0`.
    - `/usr/bin/grep -c 'non_anz' src/icp_scoring.py` prints `0`.
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'.');from src.icp_scoring import compute_icp_score;from src.schemas import HubSpotRecord;mk=lambda r:HubSpotRecord(object_type='companies',id='1',properties={'lv_country_region_normalized':r,'lv_org_type':'governing_body_league','lv_produces_content':True});print([ (compute_icp_score(mk(r),{}).anti_icp_flag, compute_icp_score(mk(r),{}).anti_icp_reason) for r in ['AU','US','DE',''] ])"` shows `False` for `AU` and `US`, `True` with reason `Outside target regions` for `DE`, and `False` for the blank region.
    - `scripts/gen_icp_scoring_js.py::render()` raises `AssertionError` when handed a config whose `regions.home` is `[]` (exercise it in the Task 3 currency test, not by editing the shipped yaml).
    - `git status --porcelain tests/n8n/fixtures/frozen/ tests/stress-tests/` prints nothing.
  </acceptance_criteria>
  <done>Both engines derive `home`/`other`/`unknown` from `config/icp_scoring.yaml` alone, all three veto reason strings come from the yaml through the generated module, the version stamp is written by Decide Company Action, and one whitelisted and one non-whitelisted company produce identical verdicts from both engines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename blast radius — every byte-asserting test site moves in this same commit</name>
  <files>tests/test_scoring_parity.py, tests/test_icp_scoring.py, tests/test_veto_remediation_report.py, tests/test_simulate_rubric_weights.py, tests/test_remediate_veto_companies.py, tests/test_backfill_dry_run.py, tests/n8n/antiIcpFlagMirror.test.mjs, tests/n8n/companyRecomputeLaneFlow.test.mjs, tests/n8n/materialConflictNoVetoFlip.test.mjs, tests/n8n/reviewQueueEndpoint.test.mjs, tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs</files>
  <read_first>
    - .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-RESEARCH.md § "Pitfall 3" (the exhaustive file:line list of every `"Non-ANZ geography"` and `non_anz` site, confirmed by grep)
    - tests/test_scoring_parity.py lines 55-70 and 400-450 (`HARD_VETOES = CFG["hard_vetoes"]` at ~59, and the `HARD_VETOES["non_anz"]["reason"]` KEY lookups at ~443 — a SEPARATE exposure from the string rename)
    - tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs (the test that pins the three-state rule — its assertion must stay semantically identical)
    - tests/n8n/fixtures/frozen/README.md (refresh policy: none)
  </read_first>
  <behavior>
    - Every test that asserted the literal `"Non-ANZ geography"` now asserts `"Outside target regions"` and is green.
    - Every test that looked up the yaml key `non_anz` (notably `HARD_VETOES["non_anz"]["reason"]` in `tests/test_scoring_parity.py`) now looks up `outside_home_regions` and is green.
    - `tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs` still proves a blank region fires NO veto — the assertion's MEANING is unchanged; only the string it compares against moves.
    - No file under `tests/n8n/fixtures/frozen/` or `tests/stress-tests/` is modified.
  </behavior>
  <action>
Update every asserting site named in RESEARCH.md Pitfall 3, in this same commit as Task 1
(the rename and the engines that produce it never land apart).

Two DISTINCT classes of site, both must move:
(a) STRING-value sites asserting the literal `Non-ANZ geography` — change to
    `Outside target regions`.
(b) YAML-KEY sites indexing `hard_vetoes` (or a `HARD_VETOES` alias loaded from the yaml)
    by `non_anz` — change the index to `outside_home_regions`. `tests/test_scoring_parity.py`
    around line 443 is the one CONTEXT.md's own D-75-01 list does not enumerate; find the rest
    with `/usr/bin/grep -rn "non_anz" tests/`.

Sites whose only occurrence is a TEST NAME or a COMMENT (RESEARCH.md marks several as
"test name only" / "comment reference only") may keep or update their wording at the
executor's discretion, but a test NAME containing `non_anz` should be renamed for readability;
this is cosmetic and never changes an assertion.

EXPLICITLY DO NOT TOUCH (D-75-04): `tests/n8n/fixtures/frozen/run_6891d018-decide-and-response.excerpt.json`
(lines 4608/4729 carry the old string) and anything else under `tests/n8n/fixtures/frozen/` or
`tests/stress-tests/` — these are RECORDINGS with refresh policy "none". Verified during
planning: `/usr/bin/grep -rln "run_6891d018" tests/` matches ONLY the frozen README, so no test
replays that fixture through current Decide; nothing under `tests/` goes red from leaving it
untouched. If the executor finds a consumer that DOES replay it, stop and report rather than
regenerating the recording.

Do not weaken any assertion to make it pass. If a test compares a whole veto-reason array, the
array's other members (the content and hardware strings) must remain byte-identical.
  </action>
  <reversibility rating="costly">~15 asserting sites move together; reverting means moving all of them back plus the yaml and both engines.</reversibility>
  <verify>
    <automated>/usr/bin/grep -rn "Non-ANZ geography" tests/ src/ scripts/ n8n/ --include=*.py --include=*.mjs --include=*.js | /usr/bin/grep -v "tests/n8n/fixtures/frozen/" | /usr/bin/grep -v "tests/stress-tests/" ; test $(/usr/bin/grep -rn "Non-ANZ geography" tests/ src/ scripts/ n8n/ --include=*.py --include=*.mjs --include=*.js | /usr/bin/grep -v "tests/n8n/fixtures/frozen/" | /usr/bin/grep -cv "tests/stress-tests/") -eq 0</automated>
    <fails_when>non-zero exit, or any file path is printed before the exit-code check (each printed line is a surviving non-fixture occurrence)</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_scoring_parity.py tests/test_icp_scoring.py tests/test_veto_remediation_report.py tests/test_simulate_rubric_weights.py tests/test_remediate_veto_companies.py tests/test_backfill_dry_run.py -k "not live" && node --test tests/n8n/antiIcpFlagMirror.test.mjs tests/n8n/companyRecomputeLaneFlow.test.mjs tests/n8n/materialConflictNoVetoFlip.test.mjs tests/n8n/reviewQueueEndpoint.test.mjs tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs</automated>
    <fails_when>non-zero exit, or the pytest summary reports "0 passed", or the node output reports "fail 1" or higher</fails_when>
    <automated>git status --porcelain tests/n8n/fixtures/frozen/ tests/stress-tests/</automated>
    <fails_when>any output at all — a modified recording is a D-75-04 violation</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -rn "non_anz" tests/ src/ scripts/ n8n/code/` returns no hit outside `tests/n8n/fixtures/frozen/` and `tests/stress-tests/`.
    - `tests/test_scoring_parity.py` indexes `HARD_VETOES["outside_home_regions"]` and no longer indexes `["non_anz"]`.
    - `tests/n8n/decideCompanyActionRegionFallbackNoSpuriousVeto.test.mjs` still asserts that a blank region produces an EMPTY veto-reason list (not merely a different string).
    - `git diff --stat tests/n8n/fixtures/frozen/ tests/stress-tests/` is empty.
  </acceptance_criteria>
  <done>The repo contains no non-recording occurrence of `Non-ANZ geography` or the `non_anz` yaml key, every named test file is green, and every frozen recording is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Currency guard for the generated artifact, and the rubric change guard re-baselined</name>
  <files>tests/test_icp_scoring_generated_currency.py, tests/test_rubric_change_guard.py</files>
  <read_first>
    - tests/test_judge_spec.py around line 62 (the `checked_in = (ROOT / "n8n" / "code" / "escalation.generated.js").read_text()` currency-test pattern being mirrored — read the surrounding test body for the exact comparison shape)
    - tests/test_hubspot_enums_generated_currency.py (the sibling currency test — the second instance of the same convention)
    - tests/test_rubric_change_guard.py in full (the `PINNED_BASE_SCORE` literal at ~32-58 and the `assert_rubric_pinned` AssertionError message at ~95-109 that names `lv_icp_scoring_version` as a mechanism that does not exist)
    - scripts/gen_icp_scoring_js.py (created in Task 1 — the `render()` this test calls)
  </read_first>
  <behavior>
    - The checked-in `n8n/code/icpScoring.generated.js` is byte-identical to a fresh `gen_icp_scoring_js.render()`; editing the yaml without regenerating turns this test RED.
    - `render()` raises `AssertionError` for a config whose `regions.home` is empty, whose `regions.home` contains a non-string, and whose `hard_vetoes.<key>.reason` is empty — each proved by a separate case, each naming the offending key.
    - `tests/test_rubric_change_guard.py` passes against the new `config/icp_scoring.yaml` with `PINNED_BASE_SCORE["geography"] == {"home": 10, "other": 0, "unknown": 0}`, and its mutation tests still prove the guard's teeth.
    - The guard's AssertionError message names `lv_icp_scoring_version` as the segmentation mechanism that NOW EXISTS, and names `docs/OPERATOR-RESCORE.md` for the re-score procedure.
  </behavior>
  <action>
Create `tests/test_icp_scoring_generated_currency.py` mirroring `tests/test_judge_spec.py`'s
currency idiom: read the checked-in `n8n/code/icpScoring.generated.js`, call
`gen_icp_scoring_js.render()` fresh, assert byte-equality, with a failure message naming
`.venv/bin/python scripts/gen_icp_scoring_js.py` as the fix. Add three fail-closed cases that
each call `render()` against a monkeypatched/injected config (never by editing the shipped
yaml) and assert `AssertionError`: empty `regions.home`, a non-string member in `regions.home`,
and an empty `hard_vetoes.outside_home_regions.reason`. This is threat T-75-01's control — an
empty whitelist is "veto everything" and must refuse generation rather than default.

Re-baseline `tests/test_rubric_change_guard.py` (D-75-20) in this SAME plan, never as a
standalone edit: set `PINNED_BASE_SCORE["geography"]` to `{"home": 10, "other": 0,
"unknown": 0}` and leave `org_type`, `produces_content`, `revenue_band` and
`PINNED_GRADUATED_DEDUCTIONS` byte-identical. Update the pin's dated provenance comment to name
Phase 75 and the `lv-icp-v0.2` bump. Rewrite the `assert_rubric_pinned` AssertionError message:
it currently says a re-score of the whole population is obliged "because no lv_icp_scoring_version
property exists to segment scored records by which rubric version scored them" — that sentence
is now false. The new message must say that `lv_icp_scoring_version` IS the segmentation
mechanism, that a rubric weight change obliges a `config.version` bump plus the stale sweep
(the D-75-14 operator one-shot recompute sweep and the SJ-2 monthly backstop), and must still
name `docs/OPERATOR-RESCORE.md`. Do not change `_diff_keys` or the parametrised mutation tests'
structure.

State in the task's commit message and in the plan SUMMARY, per D-75-20: the version STAMP
lands in this same commit (Task 1, `properties.lv_icp_scoring_version = VERSION` in Decide
Company Action); the SWEEP ROUTING that consumes it lands in plan 75-03. D-75-20 is satisfied
because the mechanism its message demanded now exists in the same commit as the re-baseline —
this is not a bare "make the test pass" edit.
  </action>
  <reversibility rating="reversible">Test-only edits; the pin can be moved back in one commit.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_icp_scoring_generated_currency.py tests/test_rubric_change_guard.py</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed", or fewer than 4 tests are collected from tests/test_icp_scoring_generated_currency.py</fails_when>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'tests');import test_rubric_change_guard as g; m=g.assert_rubric_pinned.__doc__ or ''; import inspect; src=inspect.getsource(g.assert_rubric_pinned); assert 'lv_icp_scoring_version' in src, 'guard message does not name lv_icp_scoring_version'; assert 'no lv_icp_scoring_version' not in src, 'guard message still claims the property does not exist'; assert 'OPERATOR-RESCORE' in src, 'guard message dropped the runbook'; print('guard message updated')"</automated>
    <fails_when>non-zero exit, or the output contains "guard message does not name", "still claims the property does not exist", or "dropped the runbook"</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the pytest summary reports any "failed", or the node output reports "fail 1" or higher</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_icp_scoring_generated_currency.py` contains at least one byte-equality test and three `pytest.raises(AssertionError)` fail-closed cases.
    - Touching `config/icp_scoring.yaml`'s `version` without re-running the generator makes `tests/test_icp_scoring_generated_currency.py` RED — the executor demonstrates this once, then restores.
    - `tests/test_rubric_change_guard.py::test_pinned_rubric_matches_current_config` passes.
    - `/usr/bin/grep -c "no lv_icp_scoring_version" tests/test_rubric_change_guard.py` prints `0`.
    - The full suite (`.venv/bin/python -m pytest -q --tb=short` and `node --test tests/n8n/*.test.mjs`) is green.
  </acceptance_criteria>
  <done>The generated artifact cannot go stale unnoticed, the generator refuses a whitelist that would veto everything, and the rubric guard is re-baselined with a message that names the mechanism this commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/icp_scoring.yaml` → both scoring engines | A config value crosses into a security-relevant decision (whether a company is vetoed). The yaml is repo-controlled, not user input, but a malformed edit silently changes every score. |
| generated JS → n8n Code node body | Generated literals are concatenated verbatim into a Code node executed on n8n Cloud. Escaping is `json.dumps`'s job, never hand-built interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Denial of Service | `scripts/gen_icp_scoring_js.py::render()` | high | mitigate | An empty/malformed `regions.home` would make every KNOWN region `other` and veto the entire population (or, if the veto key vanished, veto none). `render()` asserts `regions.home` is a non-empty list of non-empty strings and every `hard_vetoes.<key>.reason` is a non-empty string, naming the offending key; generation refuses rather than defaulting. Proved by three `pytest.raises` cases in Task 3. |
| T-75-02 | Tampering | `config/icp_scoring.yaml` scoring surface | medium | mitigate | `tests/test_rubric_change_guard.py` pins the full `base_score` surface; any unaccompanied weight change is RED and the message demands the version bump + stale sweep before re-baselining. |
| T-75-03 | Information Disclosure | `n8n/code/icpScoring.generated.js` | low | accept | The generated module carries only region codes, point values and veto reason strings — no secret, no credential, no PII. The whole file is committed by design. |
| T-75-04 | Tampering | `n8n/wf_*.json` | medium | mitigate | JSON is only ever produced by `scripts/build_cloud_workflows.py`; the plan forbids hand-edits and the Task 1 verify reads the regenerated Decide node's `jsCode` rather than trusting the committed file. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` green.
- `node --test tests/n8n/*.test.mjs` green.
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/` exits 0 AFTER the plan's own regeneration commit (proves a clean second regeneration).
- No occurrence of `Non-ANZ geography` or `non_anz` outside `tests/n8n/fixtures/frozen/` and `tests/stress-tests/`.
- `git status --porcelain tests/n8n/fixtures/frozen/ tests/stress-tests/` empty.
</verification>

<success_criteria>
One yaml edit changes the geography rule in both runtimes. A whitelisted region earns points and
no veto; a known non-whitelisted region earns 0 and one veto reading `Outside target regions`;
a blank region earns 0 and no veto. The generated artifact cannot drift, and cannot be generated
from a whitelist that would veto everything.
</success_criteria>

<output>
Create `.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-01-SUMMARY.md` when done.
Commit with plain `git commit` (not `gsd_run query commit`), message ending:
`Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
</output>
