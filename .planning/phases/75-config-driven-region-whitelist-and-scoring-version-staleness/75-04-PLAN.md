---
phase: 75-config-driven-region-whitelist-and-scoring-version-staleness
plan: 04
type: execute
wave: 4
depends_on: ["75-03"]
files_modified:
  - scripts/gen_geography_flow.py
  - config/hubspot_flows/4626722240-geography-score.after.json
  - scripts/sync_hubspot_properties.py
  - scripts/check_schema_drift.py
  - scripts/bounce_n8n_workflows.py
  - scripts/deploy_n8n_workflows.py
  - tests/test_geography_flow_conformance.py
  - tests/test_recompute_flag_isolation.py
  - tests/test_sync_hubspot_properties.py
  - tests/test_check_schema_drift.py
autonomous: true
requirements: []

estimate:
  tokens: 140000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-75-09: config/hubspot_flows/4626722240-geography-score.after.json's branch filter values are REGENERATED from config/icp_scoring.yaml's regions.home by scripts/gen_geography_flow.py and never hand-edited; Decide Company Action still does not write geography_score (Approach C unchanged)."
    - "D-75-11: yaml-vs-flow agreement is enforced twice — an offline test asserts the committed after.json branch values equal regions.home, and scripts/check_schema_drift.py compares the LIVE fetched flow's branch values against the yaml and reports ordinary drift (exit 1), while the flow's presence/enabled state stays in the exit-2 do-not-archive set."
    - "D-75-18a: ALLOW_HUBSPOT_RECOMPUTE_WRITES is absent from operator-claude-plugin/scripts/n8n_arming.py's OVERLAY_DISABLED_LITERALS, OVERLAYABLE_FLAGS and WRITE_ENABLING_FLAGS, and from scripts/deploy_n8n_workflows.py's _OVERLAY_FLAG_SPEC — pinned by a test, so disarm() and arm_for_dispatch() can never rewrite it."
    - "D-75-18b: scripts/bounce_n8n_workflows.py reads ALLOW_HUBSPOT_RECOMPUTE_WRITES and compares its LIVE value against the COMMITTED body's value rather than a hardcoded false, so the flag is verified both before and after the operator's D-75-17 flip."
    - "D-75-18c: config/execution_budget.yaml is byte-unchanged — the recompute lane rides the existing SJ-3 dispatch cap."
    - "scripts/sync_hubspot_properties.py can update an existing enumeration property's options, including setting hidden: true on an existing option, behind the same ALLOW_HUBSPOT_PROPERTY_WRITES gate and the same undo-manifest mechanism as the create path — and never deletes an option."
  artifacts:
    - scripts/gen_geography_flow.py
    - tests/test_geography_flow_conformance.py
    - tests/test_recompute_flag_isolation.py
  key_links:
    - "config/icp_scoring.yaml regions.home -> scripts/gen_geography_flow.py -> config/hubspot_flows/4626722240-geography-score.after.json -> scripts/put_hubspot_flow.py (plan 05)"
    - "scripts/check_schema_drift.py live flow fetch -> branch values vs regions.home -> exit 1 on drift"
    - "WRITE_SAFETY_DEFAULTS committed literal -> scripts/bounce_n8n_workflows.py read-back -> live body comparison"
  prohibitions:
    - "[unverified] scripts/sync_hubspot_properties.py gains NO option-delete path — the update replaces the options array and an option present live but absent from the desired set is REFUSED, never dropped."
    - "[unverified] ALLOW_HUBSPOT_RECOMPUTE_WRITES is never added to any arming, overlay or disarm set."
    - "[unverified] config/execution_budget.yaml is not modified."
    - "[unverified] No task in this plan performs a live HubSpot or n8n write, deploy, bounce or arm."
    - "[unverified] No hand-edit of config/hubspot_flows/4626722240-geography-score.after.json — it is regenerated."
---

<objective>
Teach the tooling about the two new facts this phase creates: a fourth write flag that is
deliberately true-able, and a HubSpot flow body whose branch values are derived from the yaml.

Purpose: `scripts/bounce_n8n_workflows.py` asserts every write flag reads `false` against a
hardcoded two-flag tuple — after the operator's D-75-17 flip that assertion would either fail
spuriously or, worse, pass without ever reading the flag. `scripts/sync_hubspot_properties.py`
has no update path at all, so the eight new enum options and the `UK` hide cannot be pushed.
And the geography flow's `[AU, NZ, ANZ]` branch is a second, hand-maintained copy of the
whitelist.

Output: a flow generator, an update path in the property sync tool, drift enforcement on both
sides, and flag-isolation tests. Everything offline; plan 05 pushes it live.

**Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).**
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-CONTEXT.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-RESEARCH.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-PATTERNS.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-03-SUMMARY.md
</context>

## Artifacts this plan produces

`scripts/gen_geography_flow.py` (new file); `scripts/sync_hubspot_properties.py::_update_property_options_live`
and an `update` bucket in `compute_property_diff`; a live flow-branch comparator in
`scripts/check_schema_drift.py`; `tests/test_geography_flow_conformance.py` and
`tests/test_recompute_flag_isolation.py` (new tests).
(Full phase-wide artifact table: `75-01-PLAN.md` § Artifacts this phase produces.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The geography flow body is generated from regions.home, and drift is caught on both sides</name>
  <files>scripts/gen_geography_flow.py, config/hubspot_flows/4626722240-geography-score.after.json, scripts/check_schema_drift.py, tests/test_geography_flow_conformance.py, tests/test_check_schema_drift.py</files>
  <read_first>
    - config/hubspot_flows/4626722240-geography-score.after.json (the whole body; in particular the LIST_BRANCH action's `filters[0].operation.values` array currently holding `["AU","NZ","ANZ"]` and its `property: "lv_country_region_normalized"`)
    - config/hubspot_flows/4626722240-geography-score.before.json (the rollback body plan 05 falls back to)
    - scripts/put_hubspot_flow.py and scripts/fetch_hubspot_flow.py (the PUT/GET mechanism, its two-key gating, and where the before/after JSON records live)
    - scripts/check_schema_drift.py (its exit-code contract: exit 2 for a do-not-archive violation, exit 1 for ordinary drift; `RETIRED_FLOW_IDS`, `D04_COMPANY_PROPERTY_SCOPE` and `ACCEPTED_DIVERGENCES` for the conventions a new comparator must follow)
    - tests/test_check_schema_drift.py (the existing comparator tests — the harness a new case joins)
    - src/icp_scoring.py `regions_home()` (added in plan 02 — the one loader)
  </read_first>
  <behavior>
    - `scripts/gen_geography_flow.py` reads the committed `after.json`, replaces ONLY the geography branch's `values` array with `regions.home`, and writes the file back; every other byte of the body is unchanged.
    - Running the generator twice is idempotent (second run produces no diff).
    - The generator raises if the target branch cannot be located unambiguously (zero or more than one filter on `lv_country_region_normalized`), rather than writing a guessed body.
    - `tests/test_geography_flow_conformance.py` is RED if the committed `after.json`'s branch values differ from `regions.home` in contents or order.
    - `scripts/check_schema_drift.py` reports a drift entry (contributing exit 1) when the LIVE flow's branch values differ from `regions.home`, and does not change the flow's existing do-not-archive (exit 2) treatment.
  </behavior>
  <action>
Create `scripts/gen_geography_flow.py`. It imports `regions_home` from `src.icp_scoring` (the
one loader added in plan 02 — no second YAML parser), loads
`config/hubspot_flows/4626722240-geography-score.after.json` as JSON, walks the body to find
every filter whose `property` is `lv_country_region_normalized` and whose `operation.operator`
is `IS_EQUAL_TO`, asserts EXACTLY ONE was found (naming the count in the assertion message if
not — a flow body that grew a second geography branch must fail the build, not be silently
half-updated), sets that filter's `operation.values` to `list(regions_home())`, and writes the
body back with the SAME json formatting the file already uses (read the file's existing indent
and trailing-newline convention and preserve it, so the diff is the values array alone). Expose
`render(body: dict) -> dict` as a pure function and keep `main()` as the file-I/O wrapper, so
the conformance test can exercise `render` without touching disk. Header comment on the
`gen_icp_scoring_js.py` / `gen_escalation_js.py` model: GENERATED-FROM banner, the regenerate
command, and a line stating the flow body is never hand-edited (D-75-09).

Run it once so the committed `after.json` carries the 12 `regions.home` codes. This is the body
plan 05 PUTs live.

Create `tests/test_geography_flow_conformance.py` (D-75-11a): assert the committed
`after.json`'s geography branch `values` equals `regions_home()` exactly, including order; assert
`render()` is idempotent; and assert `render()` raises when handed a body with zero matching
filters and when handed one with two. Name the regenerate command in the byte-equality failure
message.

Extend `scripts/check_schema_drift.py` (D-75-11b) with a LIVE comparison: when the script
fetches flow `4626722240`, compare its geography branch `values` against `regions_home()` and
emit an ORDINARY drift entry on mismatch — contributing to exit 1, following whatever structure
the file's existing drift entries use. Do NOT move the flow's presence/enabled state out of the
exit-2 do-not-archive set; this adds a new finding beside it, it does not reclassify the old
one. Reuse the same unambiguous-filter-location helper as the generator (import it from
`gen_geography_flow`) rather than writing a second walker. Follow the file's existing
credential-absent convention so the script still exits cleanly with no HubSpot token.

Add a case to `tests/test_check_schema_drift.py` proving the new comparator reports drift for a
synthetic fetched body whose values differ, and reports nothing when they match — offline,
against an injected body, never a live call.

RECORD IN THE SUMMARY, per CONTEXT.md's planner note: after the plan-05 PUT, HubSpot re-evaluates
this flow only when `lv_country_region_normalized` CHANGES (`shouldReEnroll: true`, event-based
on that property). The PUT alone re-scores nothing; an existing record gains its new
`geography_score` when its region is next written. This is why `geography_score` is NOT part of
the phase's recompute proof.
  </action>
  <reversibility rating="costly">The regenerated `after.json` is the body plan 05 pushes to a live HubSpot automation; `.before.json` is the rollback. Offline, this task is a one-commit revert.</reversibility>
  <verify>
    <automated>.venv/bin/python scripts/gen_geography_flow.py; GEN=$?; echo "gen_exit=$GEN"; git status --porcelain -- config/hubspot_flows/4626722240-geography-score.after.json</automated>
    <fails_when>gen_exit is non-zero, or any path is printed after the file has already been committed — a re-run of the generator produced a working-tree change, meaning the committed body is not what the yaml generates</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_geography_flow_conformance.py tests/test_check_schema_drift.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed", or fewer than 4 tests are collected from tests/test_geography_flow_conformance.py</fails_when>
    <automated>.venv/bin/python -c "import json;b=json.load(open('config/hubspot_flows/4626722240-geography-score.after.json'));s=json.dumps(b);assert '\"AU\"' in s and '\"GB\"' in s and '\"IN\"' in s, 'flow body does not carry the widened whitelist';assert 'geography_score' in s, 'flow body lost its score action';print('flow body carries regions.home')"</automated>
    <fails_when>non-zero exit, or the output contains "AssertionError", or the confirmation line is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/gen_geography_flow.py` exists, exposes a pure `render(body) -> dict` and a `main()`, and imports `regions_home` from `src.icp_scoring` (no `yaml.safe_load` of its own).
    - `config/hubspot_flows/4626722240-geography-score.after.json`'s geography branch `values` array equals the 12 codes of `regions.home` in order, and the rest of the body is unchanged: the executor records the file's byte length and its top-level key list before and after the generator run, and `tests/test_geography_flow_conformance.py`'s idempotence case proves the generator touches nothing else.
    - `config/hubspot_flows/4626722240-geography-score.before.json` is byte-unchanged.
    - `render()` raises `AssertionError` for a body with zero and for a body with two matching geography filters.
    - `scripts/check_schema_drift.py --help` (or its no-credential path) still exits 0 with no HubSpot token set.
    - The SUMMARY records the `shouldReEnroll` re-evaluation note and why `geography_score` is excluded from the phase's recompute proof.
  </acceptance_criteria>
  <done>The HubSpot flow's whitelist is derived from the yaml, cannot be hand-edited without a RED test, and a live divergence is reported as ordinary drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sync_hubspot_properties.py learns to update an existing enumeration's options</name>
  <files>scripts/sync_hubspot_properties.py, tests/test_sync_hubspot_properties.py</files>
  <read_first>
    - scripts/sync_hubspot_properties.py in full — especially `compute_property_diff` (~72-91, whose docstring states matching-name-mismatching-shape is `drift`, report-only, never auto-fixed), `_options_values`, `_create_property_live` (~126-133, the ONLY live-write path, a POST), the `_writes_allowed`-style gate reading `ALLOW_HUBSPOT_PROPERTY_WRITES`, `manifest_path` and `append_manifest_entries` (the undo mechanism)
    - tests/test_sync_hubspot_properties.py (the existing harness — how a live call is stubbed, how the dry-run report is asserted)
    - config/hubspot_properties.yaml `lv_country_region_normalized` (the desired sixteen-option state declared in plan 02)
    - .planning/phases/75-.../75-RESEARCH.md § "Pitfall 1" (the gap finding) and § "Assumptions Log" A1 (the PATCH contract is ASSUMED, not verified)
  </read_first>
  <behavior>
    - `compute_property_diff` returns a third bucket — `update` — for an existing, non-`hubspotDefined` property whose desired options are a SUPERSET of live (or differ only in an option's `hidden` value), while a property whose LIVE options contain a value absent from desired stays classified as `drift` and is never auto-fixed.
    - `_update_property_options_live` issues exactly one `PATCH /crm/v3/properties/{objectType}/{propertyName}` carrying the full desired `options` array, and is unreachable unless the same `ALLOW_HUBSPOT_PROPERTY_WRITES` + non-dry-run gate the create path uses is satisfied.
    - An update records an undo-manifest entry carrying the PRE-UPDATE options array, so the change is revertible by the same mechanism a create is.
    - The dry-run report lists `lv_country_region_normalized` under `update` (not `drift`) and `lv_icp_scoring_version` under `create`, given the plan-02 manifest and a synthetic live schema.
  </behavior>
  <action>
Add the missing update path (RESEARCH.md Pitfall 1 — this tool has no `PATCH`/`PUT` anywhere
today and `hidden` appears zero times in the file, a larger gap than CONTEXT.md's discretion item
implied).

`compute_property_diff` gains an `update` bucket beside `create` and `drift`. Classification,
in this order:
- live property absent -> `create` (unchanged).
- live `hubspotDefined` -> skipped (unchanged, belt-and-braces).
- live `type`/`fieldType` differ from desired -> `drift` (unchanged — a type change is not
  something this tool may attempt).
- options differ AND every live option VALUE is still present in the desired set -> `update`,
  carrying both the desired options and the live options (the latter for the undo manifest).
- options differ AND a live option VALUE is ABSENT from desired -> `drift`, report-only. This
  is the option-delete refusal: the tool must never drop a live option, because a HubSpot enum
  option delete is destructive and orphans every record carrying it (D-75-07's own reversibility
  note). `UK` is retained-and-hidden precisely so it stays in the desired set.
Preserve the existing docstring's wording about drift being report-only and extend it to name
the new bucket and this refusal rule.

Add `_update_property_options_live(object_type, prop)` mirroring `_create_property_live`'s
shape: same `hs_headers()`/`BASE_URL` import style, same return of `(status_code, body)`, issuing
`PATCH {BASE_URL}/crm/v3/properties/{object_type}/{prop['name']}` with a body carrying the full
desired `options` array (each option keeping `label`, `value`, `displayOrder`, `hidden`). Gate it
behind the SAME predicate the create path uses — do not introduce a second, weaker gate.

VERIFY THE CONTRACT BEFORE RELYING ON IT. RESEARCH.md flags the full-array-replace PATCH shape as
`[ASSUMED]`, never verified live. Read HubSpot's published CRM properties API documentation for
the update-a-property endpoint and confirm: the method and path; whether `options` is a full
replace or additive; and whether `hidden` is settable per option on update. Record the finding,
with its documentation source, in `75-04-SUMMARY.md`. If the documented contract differs from the
full-array-replace shape above, implement what the documentation says and note the divergence —
do not implement the assumption and hope.

Extend `tests/test_sync_hubspot_properties.py` with offline cases (no network, stubbed live
schema) for: the `update` classification of `lv_country_region_normalized` given plan 02's
manifest; the `drift` (refusal) classification when a live option is missing from desired; the
`create` classification of `lv_icp_scoring_version`; that `_update_property_options_live` is not
called when the write gate is closed; and that an update appends an undo-manifest entry carrying
the pre-update options.

This task performs NO live call. Plan 05 runs the tool against the real portal.
  </action>
  <reversibility rating="costly">The code is a one-commit revert; the live effect it enables (plan 05) is not — a HubSpot enum option cannot be deleted, only hidden, which is why the delete path is refused by construction.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_sync_hubspot_properties.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed", or fewer than 5 tests are collected</fails_when>
    <automated>/usr/bin/grep -c "requests.patch\|\.patch(" scripts/sync_hubspot_properties.py</automated>
    <fails_when>the printed count is 0 — the update path was never implemented</fails_when>
    <automated>.venv/bin/python scripts/sync_hubspot_properties.py --dry-run 2>&1 | /usr/bin/grep -i "lv_country_region_normalized\|lv_icp_scoring_version\|skipped"</automated>
    <fails_when>non-zero exit, or no line is printed — the dry-run neither named the two properties nor reported a credential-absent skip</fails_when>
  </verify>
  <acceptance_criteria>
    - `compute_property_diff` returns a dict with `create`, `update` and `drift` keys.
    - A stubbed live schema whose `lv_country_region_normalized` carries an option value absent from `config/hubspot_properties.yaml` classifies as `drift`, NOT `update` — proved by a test.
    - `_update_property_options_live` exists, issues a PATCH, and is gated by the same predicate as `_create_property_live`.
    - `/usr/bin/grep -c "delete" scripts/sync_hubspot_properties.py` shows no option-delete call path (any hit is a comment or an unrelated identifier — the executor confirms by reading each).
    - HubSpot's documented property-update contract (method, path, options replace semantics, per-option `hidden`) is recorded in `75-04-SUMMARY.md` with its source URL.
    - No live HTTP call is made by any test or by this task.
  </acceptance_criteria>
  <done>The property sync tool can add enum options and hide an existing one, behind the existing gate and undo manifest, and refuses by construction to drop a live option.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The deploy and bounce read-backs learn the fourth flag, and the arming tooling is pinned to ignore it</name>
  <files>scripts/bounce_n8n_workflows.py, scripts/deploy_n8n_workflows.py, tests/test_recompute_flag_isolation.py</files>
  <read_first>
    - scripts/bounce_n8n_workflows.py lines 25-80 — `WRITE_FLAGS = ("ALLOW_HUBSPOT_RECORD_WRITES", "ALLOW_HUBSPOT_CREATE")`, `_flag_values`, and `_row_ok`'s `all(v in ([], ["false"]) for v in flags.values())` plus its `executionOrder == "v1"` check
    - scripts/deploy_n8n_workflows.py `_OVERLAY_FLAG_SPEC` (~line 231) and `_OVERLAYABLE_FLAGS` (~245) — the three overlayable HubSpot flags and the comment explaining why `ALLOW_JUDGE_ESCALATION`/`ALLOW_WEB_RESEARCH` are deliberately absent
    - operator-claude-plugin/scripts/n8n_arming.py lines 40-70 — `OVERLAY_DISABLED_LITERALS`, `OVERLAYABLE_FLAGS`, `WRITE_ENABLING_FLAGS`, `ALLOWLIST_FLAGS`
    - scripts/build_cloud_workflows.py `WRITE_SAFETY_DEFAULTS` (the committed truth the bounce read-back compares against, extended in plan 03)
    - tests/test_deploy_flag_overlay.py and tests/test_june_run_arm.py (the existing flag-tooling test harnesses)
    - .planning/phases/75-.../75-CONTEXT.md D-75-17 and D-75-18 in full
  </read_first>
  <behavior>
    - `scripts/bounce_n8n_workflows.py::_row_ok` returns True for a live body whose `ALLOW_HUBSPOT_RECOMPUTE_WRITES` literal EQUALS the committed `WRITE_SAFETY_DEFAULTS` value, and False when it differs — in BOTH directions (live `true` while committed is `false`, and live `false` while committed is `true`).
    - `_row_ok` still returns False for any live body where `ALLOW_HUBSPOT_RECORD_WRITES` or `ALLOW_HUBSPOT_CREATE` reads `true`, unchanged.
    - `ALLOW_HUBSPOT_RECOMPUTE_WRITES` is absent from `n8n_arming.py`'s three flag sets and from `deploy_n8n_workflows.py::_OVERLAY_FLAG_SPEC`; `arm_for_dispatch` and `disarm` raise or refuse if asked to target it.
    - `config/execution_budget.yaml` is byte-unchanged.
  </behavior>
  <action>
Close D-75-18b. `scripts/bounce_n8n_workflows.py` currently hardcodes a two-flag `WRITE_FLAGS`
tuple (it does not even track `ALLOW_HUBSPOT_REVIEW_WRITES` — a pre-existing gap this phase does
not have to fix, but should not deepen) and asserts every tracked flag reads the `false` literal.
After the operator's D-75-17 flip that assertion would either fail spuriously or, if the flag
were simply left untracked, pass while reading nothing at all — a green bounce that proves
nothing about the flag that matters most.

Fix by comparing against the COMMITTED body rather than a hardcoded literal: add a second,
separate tuple (name it for what it is, e.g. `COMMITTED_TRUTH_FLAGS = ("ALLOW_HUBSPOT_RECOMPUTE_WRITES",)`)
whose members `_flag_values` also collects, and extend `_row_ok` so that each such flag's live
literal must EQUAL the same flag's literal in the committed workflow body being compared against
(the function already receives `expected_nodes` derived from that committed body — thread the
committed flag values through the same way, or read them from
`build_cloud_workflows.WRITE_SAFETY_DEFAULTS`, whichever the file's existing structure makes
smaller). The existing `WRITE_FLAGS` all-false rule, the `active is True` check, the node-count
check and the `executionOrder == "v1"` check stay exactly as they are. Add a short comment
naming D-75-17/D-75-18b and stating that this flag is deliberately allowed to read `true`
post-flip, and that the committed JSON — not a constant in this script — is the truth it is
checked against.

Surface the flag's value in the script's per-row output (security register T-75-15): an operator
scanning a routine bounce must be able to see that the standing authority is still on, weeks
later, without reading the workflow JSON.

`scripts/deploy_n8n_workflows.py`: make NO functional change — the flag must stay out of
`_OVERLAY_FLAG_SPEC` so `--enable-baked-flags`-style overlays can never target it. If that file
carries its own "every ALLOW_* literal reads false" read-back separate from the overlay spec,
extend it the same way as the bounce script; otherwise add only a comment beside
`_OVERLAY_FLAG_SPEC` naming D-75-18a and why the fourth flag is deliberately absent.

`operator-claude-plugin/scripts/n8n_arming.py`: make NO code change. D-75-18a is an EXCLUSION —
the flag must not appear in `OVERLAY_DISABLED_LITERALS`, `OVERLAYABLE_FLAGS` or
`WRITE_ENABLING_FLAGS`, so `disarm()` and `arm_for_dispatch()` never rewrite it. Because there is
no plugin code change, there is NO `operator-claude-plugin/plugin.json` version bump and no
plugin CHANGELOG entry in this plan; state that explicitly in the SUMMARY so a reviewer does not
read the absence as an oversight.

Create `tests/test_recompute_flag_isolation.py` in the ROOT `tests/` directory (not under
`operator-claude-plugin/tests/`, precisely so no plugin release is implied) asserting:
`ALLOW_HUBSPOT_RECOMPUTE_WRITES` is in `build_cloud_workflows.WRITE_SAFETY_DEFAULTS` with the
value `"false"`; it is NOT in `n8n_arming.OVERLAY_DISABLED_LITERALS`, `OVERLAYABLE_FLAGS` or
`WRITE_ENABLING_FLAGS`; it is NOT in `deploy_n8n_workflows._OVERLAY_FLAG_SPEC`; asking
`n8n_arming.arm_for_dispatch`/`disarm` to target it is refused (the existing unknown-flag path);
and `_row_ok` accepts a live `true` when the committed value is `true` and rejects it when the
committed value is `false`. Each assertion carries a message naming D-75-18 and the consequence
of the pin being removed.
  </action>
  <reversibility rating="reversible">Read-back and test changes only; no live state, no plugin release.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/test_recompute_flag_isolation.py tests/test_deploy_flag_overlay.py tests/test_june_run_arm.py</automated>
    <fails_when>non-zero exit, or the summary line reports any "failed", or fewer than 6 tests are collected from tests/test_recompute_flag_isolation.py</fails_when>
    <automated>git status --porcelain -- config/execution_budget.yaml operator-claude-plugin/</automated>
    <fails_when>any path is printed — D-75-18a is an exclusion, D-75-18c says the budget is unchanged, and no plugin code changed so no version bump is due</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider && node --test tests/n8n/*.test.mjs</automated>
    <fails_when>non-zero exit, or the pytest summary reports any "failed", or the node output reports "fail 1" or higher</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/bounce_n8n_workflows.py` reads `ALLOW_HUBSPOT_RECOMPUTE_WRITES` and compares it to the committed value, not to a hardcoded `"false"`.
    - `_row_ok`'s existing `WRITE_FLAGS` all-false rule, `active`, node-count and `executionOrder` checks are byte-unchanged.
    - The bounce script's per-row output names the recompute flag's current value.
    - `operator-claude-plugin/scripts/n8n_arming.py`, `operator-claude-plugin/.claude-plugin/plugin.json` and `config/execution_budget.yaml` are all byte-unchanged.
    - `tests/test_recompute_flag_isolation.py` lives under the root `tests/` directory and contains at least six assertions, each with a D-75-18 message.
    - `75-04-SUMMARY.md` states explicitly that no plugin code changed, therefore no plugin version bump or CHANGELOG entry is due.
  </acceptance_criteria>
  <done>A routine bounce verifies the standing flag against the committed truth in both directions and shows its value, and the arming tooling's deliberate blindness to it is pinned by test rather than left to memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/hubspot_properties.yaml` → live HubSpot property schema | The new PATCH path turns a declared manifest into a destructive-capable live schema change. |
| committed workflow JSON → live n8n body | The bounce read-back is the only routine check that live and committed agree on write authority. |
| operator's memory → the standing flag's current state | A flag that `disarm()` cannot touch must be visible, or it is forgotten while on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-15 | Elevation of Privilege | `ALLOW_HUBSPOT_RECOMPUTE_WRITES` left `true` indefinitely | high | mitigate | `bounce_n8n_workflows.py` surfaces the flag's current value in its per-row output, so every routine bounce shows an operator that the standing authority is still on. The comparison is against the committed body, so live-vs-committed divergence in EITHER direction is caught. |
| T-75-16 | Denial of Service | `_update_property_options_live` dropping a live enum option | high | mitigate | `compute_property_diff` classifies any desired set that omits a live option value as `drift` (report-only), never `update`. A HubSpot option delete is destructive and orphans records; the tool has no delete path and gains none. Proved by a dedicated refusal test. |
| T-75-17 | Tampering | HubSpot flow `4626722240` body | medium | mitigate | The body is generated from `regions.home` and never hand-edited; an offline conformance test pins the committed body and `check_schema_drift.py` reports a live divergence as ordinary drift (exit 1). The generator refuses an ambiguous body rather than guessing which branch to rewrite. |
| T-75-18 | Spoofing | a green bounce that never read the flag | medium | mitigate | `tests/test_recompute_flag_isolation.py` asserts `_row_ok` REJECTS a live `true` when the committed value is `false` — a read-back that silently ignored the flag would pass that case and is therefore caught. |
| T-75-19 | Information Disclosure | HubSpot token in a PATCH error object | medium | mitigate | `_update_property_options_live` follows `_create_property_live`'s existing convention of returning `(status_code, body)` and never echoing the raw request or its headers; the executor confirms no error path serialises the outbound request. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` green.
- `node --test tests/n8n/*.test.mjs` green.
- After this plan's commit, re-running `.venv/bin/python scripts/gen_geography_flow.py` leaves `git status --porcelain -- config/hubspot_flows/` printing nothing.
- `git status --porcelain -- config/execution_budget.yaml operator-claude-plugin/` prints nothing at any point during the plan.
- No live HubSpot or n8n call is made by any task in this plan.
</verification>

<success_criteria>
The geography flow's whitelist is derived, not duplicated. The property sync tool can add and
hide options but can never drop one. A routine bounce verifies the fourth flag against the
committed truth and shows its value. And the arming tooling's deliberate blindness to that flag
is a pinned test, not a remembered intention.
</success_criteria>

<output>
Create `.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-04-SUMMARY.md` when done.
Commit with plain `git commit` (not `gsd_run query commit`), message ending:
`Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
</output>
