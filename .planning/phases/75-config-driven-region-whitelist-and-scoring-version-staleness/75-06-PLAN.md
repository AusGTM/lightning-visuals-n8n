---
phase: 75-config-driven-region-whitelist-and-scoring-version-staleness
plan: 06
type: execute
wave: 6
depends_on: ["75-05"]
files_modified:
  - .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-UAT.md
  - .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-DEPLOY-RECORD.md
  - docs/OPERATOR-RESCORE.md
  - CLAUDE.md
  - .planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md
  - .planning/todos/pending/2026-09-20-sj2-version-stale-backstop-is-armed-only.md
autonomous: false
requirements: []
user_setup:
  - service: n8n
    why: "Disarmed deploy + bounce of the enrichment and scheduled-maintenance workflows, and two disarmed recompute proof POSTs"
    env_vars:
      - name: N8N_URL
        source: "n8n Cloud -> the instance URL"
      - name: N8N_API_KEY
        source: "n8n Cloud -> Settings -> n8n API -> the existing LV key"
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: "Needed read-only, for the population-sizing search in Task 2"

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ROADMAP exit criteria are met: the workflow JSON is regenerated, both suites are green, the enrichment and scheduled-maintenance workflows are deployed and bounced DISARMED, and one recompute proof each for a whitelisted and a non-whitelisted company ran at 0 provider credits and 2 n8n executions, with nothing armed."
    - "D-75-14: the operator-run one-shot bump sweep — a chunked recompute POST on the scripts/remediate_veto_companies.py::post_webhook_event pattern — is documented as a runbook procedure with its bounded-window rules, and is named as the intended path for the config.version bump this phase performed."
    - "D-75-17: the ALLOW_HUBSPOT_RECOMPUTE_WRITES flip to \"true\" is documented as a one-line deploy step (edit WRITE_SAFETY_DEFAULTS, regenerate, commit, deploy, bounce) taken only AFTER the first supervised bump sweep is reviewed, and is explicitly NOT part of this phase's exit gate."
    - "D-75-03: the runbook states that records carrying the old \"Non-ANZ geography\" reason are refreshed by the version bump plus sweep alone, with no dedicated armed remediation script."
    - "The population of records stamped lv_country_region_normalized = Other whose native country now falls in regions.home is SIZED by a read-only search, and the chosen path (re-enrich, not re-derivation inside the recompute lane) is recorded as a design todo with a named decision."
  artifacts:
    - .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-UAT.md
    - .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-DEPLOY-RECORD.md
    - .planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md
  key_links:
    - "committed n8n/wf_*.json -> scripts/deploy_n8n_workflows.py -> scripts/bounce_n8n_workflows.py -> live n8n Cloud"
    - "scripts/remediate_veto_companies.py::post_webhook_event(recompute=True) -> IF Company Recompute -> Decide Company Action"
  prohibitions:
    - "[unverified] Nothing is ARMED by this plan — every ALLOW_HUBSPOT_* literal reads false in the deployed bodies, including the new ALLOW_HUBSPOT_RECOMPUTE_WRITES."
    - "[unverified] No HubSpot company record is written by this plan — both recompute proofs run disarmed and are expected to return write_blocked."
    - "[unverified] No provider credit is spent — the recompute lane reaches Decide with no provider, research, judge or merge node on it."
    - "[unverified] The D-75-17 flag flip is NOT performed by this plan."
    - "[unverified] No frozen fixture is regenerated; any new freeze is additive and named as such."
---

<objective>
Close the phase against the ROADMAP's exit criteria, and write down the two operator procedures
this phase creates but deliberately does not run.

Purpose: the exit gate is a disarmed deploy and bounce plus one recompute proof each for a
whitelisted and a non-whitelisted company. The bump sweep (D-75-14) and the standing-flag flip
(D-75-17) are supervised operator steps that come AFTER this gate, and must exist as runbook
text rather than as institutional memory.

Output: `75-UAT.md`, `75-DEPLOY-RECORD.md`, runbook updates, a sized population for the
`Other`-stamped fork, and a triaged todo set.

**Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).**

## Executor fallback (D-75-10 pattern, applies to Task 1)

The executor attempts the deploy and bounce itself — the repo's own standing note is that
DISARMED deploys, activation and API reads pass the permission classifier, and only ARMING
writes do not. Nothing in this plan arms anything. If a command is nonetheless refused, the
executor stops at the checkpoint and prints it verbatim for the operator to run with `!`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-CONTEXT.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-03-SUMMARY.md
@.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-05-SUMMARY.md
</context>

## Artifacts this plan produces

`75-UAT.md`, `75-DEPLOY-RECORD.md`, runbook sections in `docs/OPERATOR-RESCORE.md`, a CLAUDE.md
as-built delta, and two pending todos (`kind: design` for the `Other`-stamped fork, `kind:
question` for the armed-only SJ-2 backstop).
(Full phase-wide artifact table: `75-01-PLAN.md` § Artifacts this phase produces.)

<tasks>

<task type="auto">
  <name>Task 1: Disarmed deploy and bounce, then one recompute proof each for a whitelisted and a non-whitelisted company</name>
  <files>.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-DEPLOY-RECORD.md, .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-UAT.md</files>
  <read_first>
    - scripts/deploy_n8n_workflows.py and scripts/bounce_n8n_workflows.py (the deploy/bounce pair as extended in plan 04 — in particular `_row_ok`'s checks and the new recompute-flag read-back)
    - scripts/remediate_veto_companies.py `post_webhook_event(..., recompute=True)` (~lines 660-700) — the recompute POST helper, its 300s read timeout and its pinned-id-set pattern
    - CLAUDE.md §13.0 (the recompute lane: what it costs, why arming is still required to PATCH, and `recompute_refused`) and §13.0.3 (the v1 platform facts and how to read runData)
    - .planning/phases/75-.../75-03-SUMMARY.md (the node counts before and after plan 03, and the named routing deviation)
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md (the house format for a deploy record and a disarmed proof)
  </read_first>
  <precondition>`N8N_URL` and `N8N_API_KEY` are set in the executing shell and resolve to the LV instance — a read-only `GET /workflows` must succeed before any PUT.</precondition>
  <action>
PRINT for the operator before deploying (the deploy is DISARMED, so this is disclosure, not an approval gate — the end-of-phase human-check below is where the operator signs off):
- the committed node count of `n8n/wf_enrichment_cloud.json` and
  `n8n/wf_scheduled_maintenance_cloud.json`, beside their pre-phase counts;
- confirmation that every `ALLOW_HUBSPOT_*` literal in both committed bodies reads `false`,
  INCLUDING the new `ALLOW_HUBSPOT_RECOMPUTE_WRITES`;
- the exact deploy and bounce commands;
- what the two proof POSTs will do and will not do.

Deploy and bounce DISARMED — the enrichment and scheduled-maintenance workflows at minimum; if
the phase's regeneration moved any other workflow body, deploy that too rather than leaving the
instance split across two generations. Record every PUT status code, every post-bounce `active`
state, every live node count, the `settings.executionOrder` read-back (must be `v1`), and the
value `bounce_n8n_workflows.py` now prints for `ALLOW_HUBSPOT_RECOMPUTE_WRITES` (must be `false`).

Then run the ROADMAP's two recompute proofs, both DISARMED, via
`scripts/remediate_veto_companies.py::post_webhook_event(..., recompute=True)` against exactly
two real companies:
  (a) ONE company whose `lv_country_region_normalized` is a `regions.home` code — expect the
      derived veto to carry NO geography reason;
  (b) ONE company whose `lv_country_region_normalized` is a KNOWN code outside `regions.home` —
      expect exactly one geography veto reason, byte-equal to `Outside target regions`.
Pick both from a read-only HubSpot search and name their ids and current property values in
`75-UAT.md` BEFORE sending, so the expected verdict is written down before it is observed.

Expected outcome for both, stated up front so a surprise is legible: the row reaches
`Decide Company Action`, the veto is DERIVED correctly, and the write returns
`write_blocked` — because `ALLOW_HUBSPOT_RECOMPUTE_WRITES` ships `false` (D-75-17) and
`ALLOW_HUBSPOT_RECORD_WRITES` is `false`. Deriving is free; writing needs the operator's later
flip. Record the n8n execution ids and confirm from the executions API that exactly TWO
executions were consumed and that no provider (`Lusha`/`Apollo`/`ZoomInfo`), research or judge
node ran on either — the ROADMAP's 0-credits/2-executions criterion.

Then WATCH for two minutes after the final POST and confirm no further executions appear. This
is the burst watch the 2026-09-10 runaway made standing practice; the deactivate-is-not-instant
behaviour in CLAUDE.md §13.0.3 means a clean instant reading is not the same as a clean watch.

Write `75-DEPLOY-RECORD.md` (deploy/bounce evidence) and `75-UAT.md` (the two proofs, with the
pre-stated expectation beside the observed result for each). If a proof's observed verdict
differs from the written-down expectation, record it as a FINDING with an id and do not smooth
it over.

Optional and additive only (D-75-04): freeze the two proof executions' runData as a NEW fixture
under `tests/n8n/fixtures/frozen/`. If done, redact first with the Phase 74 freezer — the
Webhook Trigger's runData embeds `x-enrichment-secret` and the caller IP verbatim. Never
regenerate an EXISTING frozen fixture.
  </action>
  <reversibility rating="costly">A deploy replaces the live workflow bodies. The rollback is a PUT of the previous generation; nothing is armed, so no record can be written either way.</reversibility>
  <verify>
    <human-check>The operator confirms from 75-DEPLOY-RECORD.md that both workflows are active, their live node counts equal the committed counts, `settings.executionOrder` reads `v1`, and every `ALLOW_HUBSPOT_*` literal — including `ALLOW_HUBSPOT_RECOMPUTE_WRITES` — reads `false`; and from 75-UAT.md that the whitelisted company derived no geography veto, the non-whitelisted company derived exactly `Outside target regions`, both returned `write_blocked`, exactly two executions were consumed, and no provider/research/judge node ran.</human-check>
    <automated>.venv/bin/python scripts/bounce_n8n_workflows.py; echo "bounce_exit=$?"</automated>
    <fails_when>the printed bounce_exit is non-zero — the script exits 1 while any tracked write flag is armed, or a live node count, active state or executionOrder disagrees with the committed body</fails_when>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && echo COMMITTED_IS_CURRENT</automated>
    <fails_when>non-zero exit, or the words COMMITTED_IS_CURRENT are absent — the bodies just deployed were not regenerated from current source</fails_when>
  </verify>
  <acceptance_criteria>
    - `75-DEPLOY-RECORD.md` records, per workflow: PUT status, `active`, live node count vs committed, `settings.executionOrder`, and every `ALLOW_HUBSPOT_*` literal's value.
    - `75-UAT.md` names both proof companies by HubSpot id, records their pre-send `lv_country_region_normalized` and `lv_anti_icp_reason`, states the expected verdict BEFORE the observed one, and records both n8n execution ids.
    - The non-whitelisted company's derived `lv_anti_icp_reason` is byte-equal to `Outside target regions`.
    - Both proofs returned `write_blocked`; no HubSpot company record was written.
    - Exactly two n8n executions were consumed and no provider, research or judge node ran on either.
    - The two-minute post-send burst watch showed no further executions.
    - Any freeze added under `tests/n8n/fixtures/frozen/` is NEW and redacted; `git status --porcelain` shows no modification to an existing frozen file.
  </acceptance_criteria>
  <done>The ROADMAP's exit criteria are met and evidenced: regenerated JSON, green suites, a disarmed deploy and bounce, and two zero-cost recompute proofs whose verdicts were written down before they were observed.</done>
</task>

<task type="auto">
  <name>Task 2: Size the Other-stamped population, and write the two operator procedures this phase does not run</name>
  <files>docs/OPERATOR-RESCORE.md, .planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md, .planning/todos/pending/2026-09-20-sj2-version-stale-backstop-is-armed-only.md, .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-UAT.md</files>
  <read_first>
    - docs/OPERATOR-RESCORE.md (the re-score runbook `tests/test_rubric_change_guard.py`'s message names — the procedure this phase's version bump plus sweep now IS)
    - scripts/remediate_veto_companies.py (the chunked POST helper, its bounded-window discipline and its pinned-id-set pattern — the model for the D-75-14 sweep)
    - src/hubspot_client.py `search_records` (the read-only search used for the sizing query)
    - .planning/todos/pending/2026-09-12-enrichment-lane-and-companies-branch-have-no-property-history-hop.md (the house todo front-matter format, including `kind:` and its required companion key)
    - CLAUDE.md §31 (todo triage rules: `kind:` is mandatory, `design` needs `decision_needed:`, `question` needs `trigger:` and `owner:`)
    - .planning/phases/75-.../75-CONTEXT.md § "Open items for research/planning" (the `Other`-stamped fork this task resolves, and D-75-05's flip-count sizing ask)
  </read_first>
  <behavior>
    - A read-only HubSpot search returns a COUNT of companies where `lv_country_region_normalized` equals `Other` and native `country` names a `regions.home` market, and that count is recorded.
    - A second read-only search returns a COUNT of currently-vetoed companies carrying the old `Non-ANZ geography` reason — D-75-05's flip-size ask.
    - `docs/OPERATOR-RESCORE.md` contains a D-75-14 bump-sweep procedure and a D-75-17 flag-flip procedure, each with its exact commands and its stop conditions.
    - Two pending todos exist, each with a `kind:` and its required companion key.
  </behavior>
  <action>
SIZE FIRST, read-only, no write of any kind. Run two HubSpot searches and record both counts and
their filter bodies in `75-UAT.md`:
  (1) companies where `lv_country_region_normalized` EQ `Other` AND native `country` is one of
      the `regions.home` markets — the population the whitelist expansion CANNOT rescue by
      recompute alone;
  (2) companies where `lv_anti_icp_flag` EQ `true` AND `lv_anti_icp_reason` contains `Non-ANZ` —
      D-75-05's unknown flip count, sized before any armed sweep as CONTEXT.md asks.
If credentials are unavailable, record that plainly as NOT SIZED with the exact queries written
out, rather than guessing a number.

RESOLVE THE OPEN FORK, and record the reasoning. CONTEXT.md leaves the planner two options for
population (1): re-enrich, or teach the recompute path to re-derive region from native `country`
through `REGION_ALIASES` when the stored region reads `Other`. Take RE-ENRICH. The re-derivation
option is refused because it would make the derived veto disagree with the record's own stored
`lv_country_region_normalized` — two different answers to "what region is this company" living
on one record — and because HubSpot's `geography_score` flow reads the STORED property, so the
record would still score 0 there while the pipeline's veto said otherwise. A record whose region
is wrong needs its region rewritten, which is enrichment, not recompute. File
`.planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md` with
`kind: design` and a `decision_needed:` naming the open question that remains — whether and when
to run a targeted re-enrichment pass over population (1) — carrying both measured counts.

File `.planning/todos/pending/2026-09-20-sj2-version-stale-backstop-is-armed-only.md` with
`kind: question`, `owner: operator`, and `trigger:` = the first monthly SJ-2 fire after the
D-75-17 flip, carrying plan 03's named assumption: SJ-2's version-stale dispatch keeps its
`enrich` classification, so its `lv_enrichment_requested=true` write is still allowlist-gated
and inert while disarmed.

Add TWO sections to `docs/OPERATOR-RESCORE.md`:
(a) **The bump sweep (D-75-14).** After any `config/icp_scoring.yaml` change that moves the
    scoring surface: bump `version`, regenerate, commit, deploy, bounce, then run a chunked
    recompute POST on the `scripts/remediate_veto_companies.py::post_webhook_event(...,
    recompute=True)` pattern under a bounded window. State the stop conditions: chunk size, the
    execution budget the run must stay inside (`config/execution_budget.yaml`'s SJ-3 dispatch
    cap, which this lane rides unchanged per D-75-18c), the requirement to watch for a burst
    after the final chunk, and that the scheduled SJ-2 path is the BACKSTOP not the mechanism.
    State D-75-03 plainly: records carrying the old `Non-ANZ geography` reason are refreshed by
    this sweep alone — there is no dedicated remediation script for the string, and none should
    be written.
(b) **The standing-flag flip (D-75-17).** Flipping `ALLOW_HUBSPOT_RECOMPUTE_WRITES` to `"true"`
    is: edit the literal in `WRITE_SAFETY_DEFAULTS`, regenerate, commit, deploy, bounce. It is
    taken only AFTER the first supervised bump sweep in (a) has been reviewed, and is explicitly
    NOT part of Phase 75's exit gate. Record the two facts an operator will otherwise be
    surprised by: `disarm()` does NOT disarm this flag (D-75-18a), so a kill-everything step must
    flip it back by the same deploy+bounce route; and `bounce_n8n_workflows.py` prints its
    current value on every run, which is the routine reminder that the authority is still on.
    Name the exact four properties a recompute PATCH writes.
  </action>
  <reversibility rating="reversible">Documentation and todos; the sizing searches are read-only.</reversibility>
  <verify>
    <automated>.venv/bin/python scripts/todo_triage.py</automated>
    <fails_when>non-zero exit — an untriaged pending todo (missing `kind:` or its required companion key) fails the triage script</fails_when>
    <automated>/usr/bin/grep -c "D-75-14\|D-75-17" docs/OPERATOR-RESCORE.md; /usr/bin/grep -c "ALLOW_HUBSPOT_RECOMPUTE_WRITES" docs/OPERATOR-RESCORE.md</automated>
    <fails_when>either printed count is 0 — the runbook is missing the bump-sweep procedure, the flip procedure, or the flag name an operator will search for</fails_when>
    <automated>test -f .planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md && test -f .planning/todos/pending/2026-09-20-sj2-version-stale-backstop-is-armed-only.md && /usr/bin/grep -c "decision_needed" .planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md</automated>
    <fails_when>non-zero exit, or the printed count is 0 — a `kind: design` todo without a `decision_needed:` key is untriaged under CLAUDE.md §31</fails_when>
  </verify>
  <acceptance_criteria>
    - `75-UAT.md` records both sizing counts with their exact filter bodies, or records NOT SIZED with the queries written out and the reason.
    - The `Other`-stamped fork is RESOLVED in writing as re-enrich, with the two-answers-on-one-record and `geography_score`-reads-the-stored-property reasons stated.
    - `.planning/todos/pending/2026-09-20-other-stamped-records-cannot-be-rescued-by-recompute.md` carries `kind: design` and a `decision_needed:` key.
    - `.planning/todos/pending/2026-09-20-sj2-version-stale-backstop-is-armed-only.md` carries `kind: question`, `owner:` and `trigger:`.
    - `docs/OPERATOR-RESCORE.md` names D-75-14, D-75-17, `ALLOW_HUBSPOT_RECOMPUTE_WRITES`, the fact that `disarm()` does not touch it, and the four properties a recompute PATCH writes.
    - `.venv/bin/python scripts/todo_triage.py` exits 0.
  </acceptance_criteria>
  <done>Both unmeasured populations have a number or an honest NOT SIZED, the open fork is resolved with its reasoning, and the two procedures this phase creates but does not run exist as runbook text.</done>
</task>

<task type="auto">
  <name>Task 3: Record the as-built delta and close the phase</name>
  <files>CLAUDE.md, .planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-UAT.md</files>
  <read_first>
    - CLAUDE.md §5.2 (which lists `lv_icp_scoring_version` as documented-but-never-created — now false), §10.1/§10.2 (the scoring config and tier table carrying the old `non_anz` geography keys), §10.3 (the hard-veto section naming "non-ANZ" as a veto trigger), §10.3.1 (the as-built delta format this task mirrors), §13.0/§13.0.2 (the recompute lane and the request-level flags), §19.5 (SJ-2's monthly job), §21.1 (the global kill switches)
    - .planning/phases/75-.../75-03-SUMMARY.md and 75-05-SUMMARY.md (the facts being recorded)
    - .planning/ROADMAP.md § Phase 75 (the exit criteria being ticked)
  </read_first>
  <behavior>
    - CLAUDE.md carries a Phase 75 as-built delta recording: the geography rule is now config-driven; `hard_vetoes.outside_home_regions` replaced `non_anz` and its reason string changed; all three veto reasons come from the yaml; `lv_icp_scoring_version` EXISTS live and is written by Decide Company Action; the version-stale recompute reroute and how it is wired; and the fourth `ALLOW_HUBSPOT_*` flag with its standing-arming exception.
    - Every claim in that delta carries a tag: `[observed live]` with its execution id or read-back for the things this phase actually watched, `[documented]` for the rest.
    - `.planning/ROADMAP.md`'s Phase 75 plan checkboxes are ticked and its `Plans:` count is accurate.
  </behavior>
  <action>
Add a Phase 75 as-built delta to `CLAUDE.md`, in the house style of §10.3.1 and §13.0.1 — a dated
subsection naming the decision ids, what changed, and what did NOT. It must correct four places
the document is now stale:
- §5.2's `lv_icp_scoring_version` row: no longer "documented but never created" — it exists live
  as of plan 05, tagged with the read-back date.
- §10.1's `config/icp_scoring.yaml` listing: `base_score.geography` is now `home`/`other`/
  `unknown`, `hard_vetoes.non_anz` is now `hard_vetoes.outside_home_regions` with the reason
  `Outside target regions`, and a `regions:` block carrying `home` and `aliases` is the single
  source of the whitelist. State the §4.0 discipline explicitly: §10.1's listing is the ROADMAP,
  not an inventory — re-read the yaml before relying on any key named there.
- §10.3's veto list: "non-ANZ" becomes "a KNOWN region outside `regions.home`", with the
  three-state rule restated (blank/unknown never vetoes).
- §13.0.2's request-level-flags section: the version-stale reroute is NOT a fifth request-level
  flag. It is a GATE-DERIVED marker set by `Company Gate` on a `skip` verdict, carried on
  `row.recompute` with `row.recompute_reason = "version_stale"`, and routed by the EXISTING
  `IF Company Recompute` node. Record why the literal D-75-12 IF-splice was rejected (the v1
  Merge-starvation class, §13.0.3) and that the change added ZERO nodes.
Also record §21.1's new kill switch `ALLOW_HUBSPOT_RECOMPUTE_WRITES`: what it grants (a
`recompute`-classified write, on this flag alone, no allowlist), the exact four properties that
write carries, that it ships `false`, that `disarm()` deliberately cannot touch it (D-75-18a),
and that `bounce_n8n_workflows.py` prints its value on every run.

TAG EVERY CLAIM, per §13.0.3's own rule. `[observed live]` only for what this phase watched, with
its execution id or read-back: the deploy/bounce read-back, the two recompute proofs, the live
schema and flow read-backs from plan 05. Everything else — notably the SJ-2 monthly backstop's
behaviour on a real tick, and the behaviour of the recompute lane under an ARMED flag — is
`[documented]`, because this phase deliberately armed nothing.

Complete `75-UAT.md`: one section per ROADMAP exit criterion with its evidence, and a findings
list (empty is a valid answer, stated as such). Tick `.planning/ROADMAP.md`'s Phase 75 plan
checkboxes and set its `**Plans:** N plans` line to 6.

Re-run the full suite one final time and record the pass counts in the SUMMARY.
  </action>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <verify>
    <automated>/usr/bin/grep -c "Phase 75" CLAUDE.md; /usr/bin/grep -c "outside_home_regions" CLAUDE.md; /usr/bin/grep -c "ALLOW_HUBSPOT_RECOMPUTE_WRITES" CLAUDE.md</automated>
    <fails_when>any printed count is 0 — the as-built delta is missing its phase marker, the renamed veto key, or the new kill switch</fails_when>
    <automated>/usr/bin/grep -c "no live-creation evidence\|documented but never created\|never created" CLAUDE.md</automated>
    <fails_when>the printed count is greater than 0 AND the surviving occurrence is the `lv_icp_scoring_version` row — the executor reads each hit and confirms none still claims that property is uncreated</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider && node --test tests/n8n/*.test.mjs && .venv/bin/python scripts/todo_triage.py</automated>
    <fails_when>non-zero exit, or the pytest summary reports any "failed", or the node output reports "fail 1" or higher</fails_when>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md carries a dated Phase 75 as-built delta correcting §5.2, §10.1, §10.3, §13.0.2 and §21.1 as listed, with every claim tagged `[observed live]` (with its execution id or read-back) or `[documented]`.
    - The delta states plainly that nothing was armed, that `ALLOW_HUBSPOT_RECOMPUTE_WRITES` ships `false`, and that the D-75-17 flip has NOT been performed.
    - `75-UAT.md` has one section per ROADMAP exit criterion with its evidence, plus an explicit findings list.
    - `.planning/ROADMAP.md`'s Phase 75 entry lists six plans, all checkboxes ticked, and its `**Plans:**` count reads 6.
    - The full suite is green and its pass counts are recorded in `75-06-SUMMARY.md`.
    - `.venv/bin/python scripts/todo_triage.py` exits 0.
  </acceptance_criteria>
  <done>CLAUDE.md tells the truth about the geography rule, the version property and the fourth flag; every claim is tagged by how it is known; and the phase's exit criteria each have their evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed workflow JSON → live n8n Cloud | A deploy replaces the running bodies. An armed literal in the committed JSON is armed the instant it lands. |
| proof POST → the recompute lane → HubSpot PATCH | A disarmed proof must derive without writing. The gate is the only thing between the two. |
| n8n runData → a frozen fixture in git | Webhook Trigger runData embeds the shared webhook secret and the caller IP verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-25 | Elevation of Privilege | an armed literal reaching the live instance | high | mitigate | The checkpoint requires confirming every `ALLOW_HUBSPOT_*` literal in the committed bodies reads `false` BEFORE the deploy, `bounce_n8n_workflows.py` exits 1 while any tracked flag is armed, and the acceptance criteria require the post-bounce read-back to show `false` for all four flags including the new one. |
| T-75-26 | Information Disclosure | `x-enrichment-secret` and caller IP in a frozen proof fixture | high | mitigate | The freeze is OPTIONAL and additive; if taken, it goes through the Phase 74 freezer's scrub first and the headers object is redacted — a token-shape scan alone misses this secret. No existing frozen fixture is regenerated. |
| T-75-27 | Denial of Service | an execution burst after the proof POSTs | high | mitigate | A two-minute post-send burst watch is required, not an instantaneous reading — CLAUDE.md §13.0.3 records that a deactivate is not instant and that a runaway has previously consumed 5.5% of a month's budget in six minutes. Exactly two executions is an acceptance criterion. |
| T-75-28 | Repudiation | a proof whose expectation was written after the result | medium | mitigate | `75-UAT.md` must name both companies and state the expected verdict BEFORE the send; a divergence is recorded as a FINDING with an id rather than smoothed over. |
| T-75-29 | Information Disclosure | HubSpot record data in the sizing search output | low | accept | The sizing searches record COUNTS and filter bodies, not record contents. Company firmographics are not PII and the counts are the whole point of the query. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider` green; `node --test tests/n8n/*.test.mjs` green.
- `.venv/bin/python scripts/bounce_n8n_workflows.py` exits 0 and reports `ALLOW_HUBSPOT_RECOMPUTE_WRITES` as `false`.
- `.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/` exits 0.
- `.venv/bin/python scripts/todo_triage.py` exits 0.
- `75-UAT.md` and `75-DEPLOY-RECORD.md` exist with evidence for every ROADMAP exit criterion.
- Nothing is armed; no HubSpot company record was written; no provider credit was spent.
</verification>

<success_criteria>
The phase's exit criteria are met and evidenced, not asserted: regenerated JSON, green suites, a
disarmed deploy and bounce with all four write flags reading `false`, and one zero-cost recompute
proof each for a whitelisted and a non-whitelisted company whose expected verdicts were written
down first. The two supervised procedures this phase creates but does not run — the bump sweep
and the standing-flag flip — exist as runbook text with their stop conditions.
</success_criteria>

<output>
Create `.planning/phases/75-config-driven-region-whitelist-and-scoring-version-staleness/75-06-SUMMARY.md` when done.
Commit with plain `git commit` (not `gsd_run query commit`), message ending:
`Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
</output>
