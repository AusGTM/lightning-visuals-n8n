---
phase: 71-a-held-new-person-lands-in-hubspot-with-one-reply
plan: 03
type: execute
wave: 3
depends_on: ["71-02"]
files_modified:
  - .planning/todos/pending/2026-09-11-known-company-domains-never-seeded-so-no-held-row-reads-new-person.md
  - .planning/todos/pending/2026-09-11-held-queue-row-id-is-positional-not-a-stable-identity.md
  - .planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md
  - .planning/todos/completed/2026-09-11-known-company-domains-never-seeded-so-no-held-row-reads-new-person.md
  - .planning/todos/completed/2026-09-11-held-queue-row-id-is-positional-not-a-stable-identity.md
  - .planning/todos/completed/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md
  - .planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
autonomous: false
requirements: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The three todos 71-CONTEXT.md folds into this phase are out of `.planning/todos/pending/` and in `completed/`, each carrying the D-71-NN decisions that closed it and, for the forbidden-marker one, the honest scope line naming which stores were deliberately NOT changed (§31 rule 2: a batch never ends with an untriaged residual)."
    - "`.venv/bin/python -m pytest -q tests/test_todo_triage.py` is green, and every todo still in `pending/` carries a valid `kind:` with its required companion key."
    - "Plugin `0.48.0` ships: `plugin.json`'s version string and the CHANGELOG section are bumped in the SAME commit, and the operator has the marketplace-clone refresh and Claude Code restart instructions before the gate runs (the clone never fetches on its own)."
    - "D-71-06's live gate runs ONCE, at the end of the phase, on a CSV designed to actually produce a `new_person` row: at least one row lands via a count-restating `create all N` on the batch surface, and a second such row is created cold-start through `review-triage` in a fresh sitting. Nothing was armed before this gate; every send arms and disarms individually."
    - "Grant Dewsbury — the person `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` §1d recorded as deliberately dropped because the forbidden-name marker matched his name — is in the gate's CSV and persists."
  artifacts:
    - .planning/todos/completed/2026-09-11-known-company-domains-never-seeded-so-no-held-row-reads-new-person.md
    - .planning/todos/completed/2026-09-11-held-queue-row-id-is-positional-not-a-stable-identity.md
    - .planning/todos/completed/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md
    - .planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md
    - operator-claude-plugin/.claude-plugin/plugin.json
    - operator-claude-plugin/CHANGELOG.md
    - docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
  key_links:
    - "`plugin.json`'s `version` and the CHANGELOG's cut section move in ONE commit — Claude Desktop compares only that string, and a stale marketplace clone hides even a bumped one."
    - "The gate's CSV design follows from the stamp's two sources (plan 02's `preingest.confirmed_company_domains`); without one of them present in the CSV, no row can read `new_person` live."
    - "The D-71-05 wipe (`held_queue.json` deleted) happens in the gate's clean-up, not in code."
  prohibitions:
    - "No arming anywhere except inside the D-71-06 gate's own per-send windows, each disarmed immediately after."
    - "No n8n workflow JSON edit, no deploy, no bounce — this phase touches no backend."
    - "No todo closed that this phase did not actually resolve; the forbidden-marker todo's resolution must name the stores left unchanged rather than claim them."
    - "No new hold code, no client-side company creation, no second company resolver."
---

<objective>
Close the phase honestly: triage the folded todos and the one residual this phase created, ship
`0.48.0`, and run the single live gate D-71-06 backloads to the end.

Purpose: the phase's headline claim stops being an offline test result and becomes an observation
— a real person not in HubSpot, at a company that is, landing with one reply.

Output: three todos in `completed/`, one new triaged `defect` todo, plugin `0.48.0`, a UAT CSV
spec that can actually produce the case, and the gate's recorded outcome.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-CONTEXT.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-01-SUMMARY.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-02-SUMMARY.md
@operator-claude-plugin/CHANGELOG.md
@docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage — the three folded todos out, the one residual this phase created in</name>
  <files>.planning/todos/pending/2026-09-11-known-company-domains-never-seeded-so-no-held-row-reads-new-person.md, .planning/todos/pending/2026-09-11-held-queue-row-id-is-positional-not-a-stable-identity.md, .planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md, .planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md</files>
  <read_first>
    - `CLAUDE.md` §31 — the three triage rules; rule 2 (zero-inbox at batch close) and rule 1's `kind:` contract (`defect` needs `evidence:`, `question` needs `trigger:`+`owner:`, `design` needs `decision_needed:`, `accepted` belongs in `completed/`).
    - `scripts/todo_triage.py` — `KINDS`, `validate()`, and that `triage()` scans `.planning/todos/pending/` only, so a file in `completed/` is outside the gate.
    - `tests/test_todo_triage.py` — the root-suite gate.
    - All three pending todos named in this task's `<files>`, in full — especially the forbidden-marker one's `files:` list and its "Update 2026-09-11 (quick 260911-w6o)" section.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-01-SUMMARY.md` § the Open Question 4 scope line, and `71-02-SUMMARY.md` § the Open Question 1 decision.
    - Existing files in `.planning/todos/completed/` — e.g. `2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md` — to copy the resolution-note shape.
  </read_first>
  <action>
`git mv` each of the three folded todos from `pending/` to `completed/`, keeping the filename, and
append to each a `## Resolved by Phase 71 (<date>)` section:

  - **`...known-company-domains-never-seeded...`** — closed by D-71-01..03. Name the mechanism:
    `preingest.confirmed_company_domains` collects step 2's own confirmed domains (two closed
    sources), `held_queue.build_entry` stamps them on the entry, and both surfaces derive
    `known_company_domains` through `held_queue.stamped_domains`. `classify_facet` itself is
    unchanged. Cite the covering tests by nodeid.
  - **`...held-queue-row-id-is-positional...`** — closed by D-71-04..05. Name
    `held_queue.identity_keys`/`stable_key`, the SOURCE-row derivation, the single shared
    derivation called from both the write site and `rows_to_resume`, and the D-71-05 wipe with
    `legacy_reason`'s refusal in place of a migration. Record the sharper framing RESEARCH found:
    the primary defect was a silent cross-run OVERWRITE inside `save()`, not only a failed
    resume-time lookup.
  - **`...forbidden-name-marker-whole-token-still-refuses-grant-token...`** — closed for the two
    identity-keyed stores ONLY, and the note must say exactly that. Copy plan 01 Task 3's scope
    line verbatim: fixed in `held_queue.py` (entries-map key) and `suggestion_declines.py` (key +
    allowlisted `row` payload); NOT changed in `written_records.py`, where a value scan still
    refuses a person named Grant and is deliberately left because `test_written_records.py`'s
    T-59-02 pins it as load-bearing; not applicable in `remainder_queue.py` (already key-only),
    `run_manifest.py`/`run_state.py`/`match_state.py`/`match_handoff.py` (keys are
    system-minted), or `run_report.py` (already splits key and value matchers). Also record the
    factual correction: 71-CONTEXT.md's "through the shared matcher" premise is wrong — there are
    nine deliberate reimplementations (D-69-01) and the fix is at CALL SITES, so
    `test_forbidden_marker_parity.py` is unchanged and green. Because a residue remains in
    `written_records.py`, ALSO open a successor pending todo for that one store with
    `kind: accepted`... **no** — `accepted` belongs in `completed/`, so instead state the
    `written_records.py` decision inside THIS completed note as the accepted won't-fix, with its
    reason, and open no successor. §31 rule 1's "a residual with no test and no recorded hit is a
    sentence in the SUMMARY, not a todo" applies.

Then CREATE one new pending todo,
`.planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md`,
for the residual plan 02 Task 1 deliberately left open. Frontmatter must satisfy
`scripts/todo_triage.py::validate` for `kind: defect`:

```
kind: defect
severity: minor
area: operator-plugin
evidence: operator-claude-plugin/scripts/run_manifest.py:449 (the `current is None` re-include path); operator-claude-plugin/skills/enrich-before-ingest/SKILL.md step 8 passes held_entries= but not current_outcomes=
files:
  - operator-claude-plugin/scripts/run_manifest.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
```

Body: what the fingerprint comparison would buy (not re-spending a provider credit on a held row
whose situation has not moved), why it is not wired here (it needs a fresh zero-credit match pass
per resume, which is its own scope), and what closes it.

Finally re-read every remaining file in `pending/` and confirm each still validates — do not edit
the three deferred todos 71-CONTEXT.md lists under Deferred Ideas beyond confirming they are
already triaged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_todo_triage.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output</fails_when>
    <automated>.venv/bin/python scripts/todo_triage.py --check</automated>
    <fails_when>non-zero exit, or a line beginning `INVALID` or the word `UNTRIAGED` appears in the output</fails_when>
    <automated>ls .planning/todos/pending/ | /usr/bin/grep -c "known-company-domains-never-seeded\|held-queue-row-id-is-positional\|forbidden-name-marker-whole-token"</automated>
    <fails_when>the printed count is not 0</fails_when>
  </verify>
  <acceptance_criteria>
    - All three folded todo filenames exist under `.planning/todos/completed/` and none exists under `.planning/todos/pending/`.
    - Each of the three completed files contains a `## Resolved by Phase 71` heading and cites at least one `D-71-` decision id.
    - The forbidden-marker completed file names `written_records.py` and `T-59-02` as the deliberately-unchanged store and the reason.
    - `.planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md` exists with `kind: defect` and a non-empty `evidence:`.
    - `.venv/bin/python scripts/todo_triage.py --check` exits 0.
  </acceptance_criteria>
  <done>Zero untriaged residuals; every fold is recorded with what actually closed it and what was deliberately left.</done>
</task>

<task type="auto">
  <name>Task 2: Release 0.48.0 and write the gate's CSV spec into the UAT doc</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md, docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</files>
  <read_first>
    - `operator-claude-plugin/CHANGELOG.md:1-30` (the release rule at the top) and its release checklist at the tail — four required steps, in order.
    - `operator-claude-plugin/.claude-plugin/plugin.json:4` — the `version` string, currently `0.47.0`.
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md:117-212` (§1d's mixed-batch shape and its forbidden-name warning about `jobtitle`) and `:213-245` (the second-round `contact-upload` table, including the row that records Grant Dewsbury 7101 as deliberately dropped and Jimmy Busteed row 2 with his revealed email).
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md` § Seam Map 10 (why the EXISTING §1d second-round CSV proves nothing about this phase — it goes through `contact-upload`, a straight create with no hold) and § Open Question 2 (the three CSV-design options).
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-02-SUMMARY.md` — which two stamp sources actually shipped.
  </read_first>
  <action>
**A. Cut the release.** In ONE commit: set `plugin.json`'s `version` to `0.48.0`, and turn
CHANGELOG's empty `[Unreleased]` into `## [0.48.0] - <today>` with a fresh empty `[Unreleased]`
above it. The section names, in plain operator language: a held new person now reads as a new
person on both surfaces because the run records what it already knew about their company; the
held backlog is now keyed by the person rather than by their line number, so a second run cannot
overwrite a first run's held row; a person named Grant persists; an old-format backlog file says
so instead of reading as empty. Cite `D-71-01`..`D-71-06` once. Keep the existing house style —
read the `0.47.0` section and match its shape.

**B. Write the gate's CSV spec** as a new `### 1e.` subsection in
`docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md`, immediately after §1d's second-round table. It must open
by stating why a third CSV is needed at all: §1d's second round sends Jimmy Busteed through
`contact-upload` with his email ALREADY revealed, which is a straight create with no hold — so it
exercises none of this phase. The gate needs a row that actually gets HELD on a `no_match` and
then read.

Required shape, stated as a table like §1d's:
  - **Run it through `enrich-before-ingest`, not `contact-upload`.**
  - **Row A — the new-person case.** Jimmy Busteed (ABSENT from HubSpot), company Australian Turf
    Club, **`email` column BLANK**. The waterfall reveals `jbusteed@australianturfclub.com.au`;
    `confidence.assess` holds him `no_match`; the entry is stamped and reads `new_person`.
  - **Row B — the stamp's source. Default: option (c), zero credit.** One real contact who IS
    already in HubSpot at a genuine `@australianturfclub.com.au` address, with that email in the
    row. That row matches at step 2 and its domain enters `confirmed_company_domains` under
    `step2_match`. The operator picks the person from the portal at gate time — ATC carries about
    ten such contacts (RESEARCH § Seam Map 4). **Fallback: option (a)** — add an ATC COMPANY row
    to the CSV instead, so step 2's confirmed company-row table supplies the domain under
    `step2_company_row`; note that this one spends a company enrichment credit where option (c)
    spends none. Record which was used.
  - **Row C — Grant Dewsbury 7101, restored.** §1d dropped him precisely because the marker
    refused his name. He is back in this CSV, and his persisting at all is the fold's own live
    proof.
  - **Row D — a second new-person row**, reserved for the cold-start half: do NOT include it in
    the batch-surface `create all N`, leave it held, and create it from a FRESH `review-triage`
    sitting.
  - Keep §1d's `jobtitle` warning in force for `secret`/`arm` substrings, and note that the
    `grant`/`token` markers no longer apply to a person's or company's own NAME as of `0.48.0`.

Also write the gate's clean-up, in the same subsection: hand-delete every contact created, and
delete `held_queue.json` from the plugin's durable state directory — **that deletion IS the
D-71-05 wipe**, and it is a manual step by design, not code.

No arming instruction belongs in this task — Task 3 owns every send.
  </action>
  <verify>
    <automated>/usr/bin/grep -c '"version": "0.48.0"' operator-claude-plugin/.claude-plugin/plugin.json</automated>
    <fails_when>the printed count is not 1</fails_when>
    <automated>/usr/bin/grep -c "^## \[0.48.0\]" operator-claude-plugin/CHANGELOG.md</automated>
    <fails_when>the printed count is not 1</fails_when>
    <automated>/usr/bin/grep -c "^### 1e\." docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md</automated>
    <fails_when>the printed count is not 1</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any number before `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/.claude-plugin/plugin.json` contains `"version": "0.48.0"`.
    - `operator-claude-plugin/CHANGELOG.md` contains `## [0.48.0]` and still contains a `## [Unreleased]` heading above it.
    - Both files are staged in the SAME commit (`git show --stat HEAD` lists both).
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` contains `### 1e.`, the strings `enrich-before-ingest`, `Grant Dewsbury`, `held_queue.json`, and an explicit `email` BLANK instruction for the new-person row.
    - The §1e text names which of options (c) and (a) is the default and what each costs.
    - `git status --porcelain` lists only repo paths; nothing under `~/.claude/plugins/` was edited.
  </acceptance_criteria>
  <done>0.48.0 is cut in one commit and the gate has a CSV design that can actually produce the case it is meant to observe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-71-06 — the one live gate: a held new person lands with one reply, on both surfaces</name>
  <read_first>
    - `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` §1e (written by Task 2) and §2's run script, plus its operator-scripts ruling (a driver script must go through the same durable bookkeeping and be deleted at session end).
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-CONTEXT.md` D-71-06.
    - `.planning/phases/70-.../70-DEFERRED-GATES.md` — the arming discipline every prior gate used.
    - The memory note on n8n arming: `ALLOW_N8N_ARM=true` must be set in the session shell; a bounce exits 1 while armed; a bare PUT never reloads a running workflow.
  </read_first>
  <what-built>
Plugin `0.48.0` is cut (plan 03 Task 2), carrying everything plans 01 and 02 landed: the
persist-time company-known stamp, the stable held-entry identity key, the forbidden-marker key
narrowing, the legacy-document refusal, the stable-key resume lookup, and both operator surfaces
reading `new_person` from the stamp. `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` §1e specifies the CSV
this gate runs. Nothing has been armed at any point in this phase — this task owns every send.
  </what-built>
  <how-to-verify>
**Before starting — three operator steps this checkpoint cannot do for you:**

1. Push `master` (the branch the marketplace clone tracks).
2. Refresh the marketplace clone — it never fetches on its own:
   `git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator fetch --depth=1 origin master`
   then
   `git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator reset --hard FETCH_HEAD`
3. Restart Claude Code, and confirm the installed plugin reports `0.48.0`. An un-refreshed clone
   means the gate runs the OLD code and proves nothing.

**Then run the gate, in a fresh conversation** (a grant lives only in one conversation):

**Part 1 — the batch surface.** Run `/operator-claude-plugin:enrich-before-ingest` over §1e's CSV.
Observe and record:
   - Step 2 reports the match groups; the source row supplying the stamp matches.
   - Step 5 holds the blank-email person on a `no_match` after the waterfall reveals their email.
   - **Step 6 names that person under "new person", not "needs company"** — this is the single
     observation the whole phase exists for. Record the rendered line verbatim.
   - Step 6 asks NO question. If it asks one, that is a finding, not something to answer past.
   - Reply with the count-restating `create all N` for row A only.
   - The create lands: a new HubSpot contact, associated to Australian Turf Club `9605284724`.
   - **Grant Dewsbury's row persists** (he appears wherever his row lands — not silently dropped).

**Part 2 — the cold start.** In a NEW conversation, run `/operator-claude-plugin:review-triage`.
Observe and record:
   - Row D still appears, in the ONE continuously-numbered table, **as a new person** — read from
     the queue file alone, with nothing said in this conversation first.
   - Create it from there; confirm it lands and is marked.
   - Re-open `review-triage` once more: neither created row is offered again (the settlement
     survived the run boundary under the stable key).

**Arming discipline — unchanged from every prior gate:** arm per send, disarm immediately after,
and bounce after each arm and each disarm (a bare PUT does not reload a running workflow). Nothing
in this phase is armed outside this task. Record every execution id.

**Clean-up (§1e), and it is part of the gate, not an afterthought:**
   - Hand-delete every contact created. HubSpot has no rollback.
   - Delete `held_queue.json` from the plugin's durable state directory — this is the D-71-05 wipe.
   - Delete any driver script written during the session.

**Record the outcome in `.planning/phases/71-.../71-UAT.md`**: the verbatim step-6 line, both
surfaces' verdicts, every execution id, credits spent, which §1e option supplied the stamp, and
any finding. A stuck or non-settling execution is a finding to report, never something to work
around.

  </how-to-verify>
  <resume-signal>Reply `PASS` with the evidence listed above, or `FAIL` naming what did not hold. A stuck or non-settling execution is a FAIL finding, never something to work around.</resume-signal>
  <acceptance_criteria>
    - The installed plugin reported `0.48.0` before the run (stated in the record).
    - At least one row was rendered under the new-person group at `enrich-before-ingest` step 6, quoted verbatim.
    - Step 6 asked no question.
    - One `create all N` reply landed that row in HubSpot, associated to `9605284724`.
    - A second held row was rendered as a new person, and created, from a FRESH `review-triage` sitting with no prior conversation context.
    - A third `review-triage` open did not re-offer either created row.
    - Grant Dewsbury's row persisted rather than being dropped.
    - Every send was armed individually and disarmed immediately after, with execution ids recorded.
    - Contacts hand-deleted, `held_queue.json` deleted, any driver script deleted.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-UAT.md` exists and carries the above.
  </acceptance_criteria>
  <done>The operator has answered PASS or FAIL with evidence, and `71-UAT.md` records the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → n8n Cloud arming flag | The only point in this phase where a live HubSpot write becomes possible. |
| repo `master` → marketplace clone → installed plugin | The code the gate actually exercises arrives through a clone that never self-refreshes. |
| gate run → HubSpot production records | Real contacts are created in a production portal with no rollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-13 | Elevation of Privilege | the per-send arming window in Task 3 | high | mitigate | Arming exists in exactly ONE task in this phase, is per-send, is disarmed immediately after, and is followed by a bounce (a bare PUT does not reload a running workflow). Plans 01 and 02 carry an explicit no-arming prohibition and touch no dispatch, workflow or arming file. Every execution id is recorded in `71-UAT.md`. |
| T-71-14 | Spoofing | the gate running stale code | medium | mitigate | The checkpoint's first three steps are push, clone refresh, restart, and the acceptance criteria require the installed version to be stated as `0.48.0` in the record. An un-refreshed clone silently runs the old plugin and would produce a false PASS. |
| T-71-15 | Repudiation | an unrecorded live write | medium | mitigate | Every created contact is named in `71-UAT.md` and hand-deleted in the clean-up; the record carries execution ids and credits. The clean-up is an acceptance criterion, not a courtesy. |
| T-71-16 | Information Disclosure | the new pending todo's `evidence:` field | low | mitigate | The evidence names file paths and line numbers only — no run id, no token, no portal secret. Todos are committed to the repo, so this is checked at write time. |
| T-71-17 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package in any ecosystem (RESEARCH § Package Legitimacy Audit: "Not applicable"). The `0.48.0` bump is a version string in a JSON file, not a dependency change. |
</threat_model>

<verification>
- `.venv/bin/python scripts/todo_triage.py --check` exits 0 and prints no `INVALID` line.
- `git show --stat HEAD` for the release commit lists both `plugin.json` and `CHANGELOG.md`.
- `git status --porcelain -- n8n/` prints nothing, across the whole phase.
- `71-UAT.md` exists and answers PASS or FAIL.
</verification>

<success_criteria>
- Three folded todos in `completed/` with their real resolutions; one new triaged `defect` todo; the root todo-triage gate green.
- Plugin `0.48.0` cut in one commit, with the marketplace-refresh and restart steps handed to the operator.
- `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` §1e specifies a CSV that can produce a live `new_person` row.
- The gate ran once, armed per send and disarmed after, and its outcome is recorded.
</success_criteria>

<artifacts_this_phase_produces>
Produced by THIS plan:

**Moved files (pending → completed):**
- `.planning/todos/completed/2026-09-11-known-company-domains-never-seeded-so-no-held-row-reads-new-person.md`
- `.planning/todos/completed/2026-09-11-held-queue-row-id-is-positional-not-a-stable-identity.md`
- `.planning/todos/completed/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md`

**New files:**
- `.planning/todos/pending/2026-09-12-rows-to-resume-current-outcomes-never-wired-fingerprint-branch-dead.md` (`kind: defect`)
- `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-UAT.md`

**Version string:**
- `operator-claude-plugin/.claude-plugin/plugin.json` → `"version": "0.48.0"`

**CHANGELOG section:**
- `operator-claude-plugin/CHANGELOG.md` → `## [0.48.0] - <date>`, with a fresh empty `## [Unreleased]` above it

**Documentation section:**
- `docs/OPERATOR-AUTONOMOUS-BATCH-UAT.md` → `### 1e.` (the third-round `enrich-before-ingest` CSV spec and the gate's clean-up, including the D-71-05 wipe)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-03-SUMMARY.md` when done.
Record in it: which §1e option supplied the stamp live, the verbatim step-6 new-person line, every
execution id and the credit spend, the `written_records.py` accepted won't-fix as stated in the
forbidden-marker todo's resolution, and the gate's PASS/FAIL with evidence.
</output>
