---
phase: 71-a-held-new-person-lands-in-hubspot-with-one-reply
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - operator-claude-plugin/scripts/run_manifest.py
  - operator-claude-plugin/scripts/preingest.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/skills/review-triage/SKILL.md
  - operator-claude-plugin/tests/test_run_manifest.py
  - operator-claude-plugin/tests/test_preingest_match.py
  - operator-claude-plugin/tests/test_held_facet_render_composition.py
  - operator-claude-plugin/tests/test_review_triage_facets.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
autonomous: true
requirements: []
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Jimmy Busteed reads `new_person` on BOTH surfaces — `enrich-before-ingest` step 6's ready answer and `review-triage`'s one continuously-numbered table — because both derive `known_company_domains` from the entry's own persist-time stamp (D-71-01, D-71-03) instead of a hardcoded empty set, and neither performs a lookup (D-71-02)."
    - "`run_manifest.rows_to_resume`'s `confidence_held` branch finds a prior run's held entry by `held_queue.stable_key(row)` even when this run mints a different positional `row_id` (D-71-04), and `enrich-before-ingest` step 8 actually passes `held_entries=`, so `is_settled` is live in production rather than dead code."
    - "A row whose company is confirmed this run — by step 2's own contact match (`auto_matched`, including a step-3-confirmed row) or by step 2's confirmed company-row table — carries that domain and its source word onto the held entry at persist time, with no new HubSpot read anywhere (D-71-01, D-71-02)."
    - "`review-triage` step 4c records `held_queue.record_verb` under the same stable key the entry was saved with (D-71-04), so a create the operator lands is settled rather than re-offered next sitting."
    - "The plugin still never creates a company through the ingest lane and never reproduces the absent-company downgrade client-side — that stays server-side (CLAUDE.md §13.0.1), and `review-triage` 2c's in-conversation add-a-domain route is unchanged (D-71-03)."
  artifacts:
    - operator-claude-plugin/scripts/run_manifest.py
    - operator-claude-plugin/scripts/preingest.py
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
    - operator-claude-plugin/skills/review-triage/SKILL.md
    - operator-claude-plugin/tests/test_skill_sequence_coverage.py
  key_links:
    - "`run_manifest.rows_to_resume` calls `held_queue.stable_key(row)` — the SAME function `enrich-before-ingest` step 5's write site calls, never a second derivation (RESEARCH Pitfall 2)."
    - "`enrich-before-ingest` step 5 passes `company_known=` to `held_queue.build_entry`, and step 6 reads it back through `held_queue.stamped_domains` — the stamp is the only channel between them."
    - "`test_skill_sequence_coverage.py`'s COVERED registry tuples for both skills' held-queue fences match the live call sequence after `stamped_domains` is inserted."
  prohibitions:
    - "No HubSpot read at facet time, and no second company resolver anywhere (D-71-02, CLAUDE.md §13.0.1)."
    - "No client-side company creation through the ingest lane; a `needs_company` row's handoff stays by-heading to `enrich-records`'s companies form."
    - "No new hold code and no change to `classify_facet`'s signature or body (D-70-11, D-71-03)."
    - "No scoring or prioritisation vocabulary in either SKILL.md's operator-facing prose (the D-10b ban; see the acceptance criteria's negative greps for the exact banned substrings)."
    - "No arming, no dispatch, no deploy, no live call in this plan."
---

<objective>
Make the tracer's proven data path reach both operator surfaces and the resume decision: wire the
stamp's two step-2 sources, read it back on both renders, and key the resume lookup on the stable
identity so a settled held row is never silently re-resumed.

Purpose: the headline case — Jimmy Busteed reading `new_person` and landing with one
`create all 1` reply — is true end to end, on both surfaces, across a run boundary.

Output: `preingest.confirmed_company_domains`, a stable-key `rows_to_resume`, both SKILL.md
fences, and an updated skill-sequence registry with covering tests that actually assert
`new_person`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-CONTEXT.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-PATTERNS.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-01-SUMMARY.md
@operator-claude-plugin/scripts/run_manifest.py
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/skills/review-triage/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rows_to_resume finds a prior run's held entry by stable key, and the caller decision is recorded</name>
  <files>operator-claude-plugin/scripts/run_manifest.py, operator-claude-plugin/tests/test_run_manifest.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/run_manifest.py:361-470` — `rows_to_resume` in full, especially the `CONFIDENCE_HELD` branch's `entry = held_entries.get(row_id)` at :438 and its docstring's account of the fingerprint comparison and the `260911-w6p` verb short-circuits.
    - `operator-claude-plugin/scripts/watch.py:724-762` — `resume_or_disclose`, and how `held_entries`/`current_outcomes` pass straight through.
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md:1193-1200` — the live call `watch.resume_or_disclose(rows)` with NEITHER keyword argument.
    - `operator-claude-plugin/scripts/held_queue.py` as landed by plan 01 — `stable_key`, `identity_keys`, `is_settled`, `entry_verb`.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md` § Seam Map 2 (the silent-overwrite framing), § Common Pitfalls 2, § Open Question 1.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-PATTERNS.md` § `run_manifest.py` (`rows_to_resume`).
  </read_first>
  <behavior>
    - Given a held entry saved under `held_queue.stable_key(source_row)` in run A, and a row in run B carrying the SAME identity fields but a freshly-minted, different `row_id`, `rows_to_resume` finds the entry.
    - With that entry carrying a `create` verb, run B's row is reported in `skipped`, not `to_resume`.
    - With that entry carrying a `retry` verb, run B's row is in `to_resume`.
    - The `row_id` reported inside every `skipped`/`still_held` record is still `row.get("row_id")` — the source position, unchanged.
    - A row with no identity group at all resolves through `stable_key`'s total fallback without raising.
    - Every existing positional call `rows_to_resume(rows, manifest)` behaves byte-identically.
  </behavior>
  <action>
Change ONE line of lookup and nothing else in the branch: `entry = held_entries.get(row_id)`
becomes `entry = held_entries.get(held_queue.stable_key(row))`. Call the function plan 01 landed —
do not re-derive the key here, which is exactly the two-places-must-agree failure RESEARCH
Pitfall 2 names. Extend the `CONFIDENCE_HELD` paragraph of the docstring with two sentences: the
lookup key is now the row's stable identity (D-71-04) while the REPORTED `row_id` stays the source
position (D-69-04); and the key is computed from the caller's SOURCE row, the same side
`enrich-before-ingest` step 5 computes it from.

Also decide and record RESEARCH § Open Question 1 — the DECISION is this task's, the SKILL.md edit
is Task 2's: **wire `held_entries=held_queue.load()` only; leave `current_outcomes` unwired.**
Passing `held_entries` is what makes the settled-verb short-circuit live, which is this phase's own
cross-run promise. `current_outcomes` would require a fresh zero-credit match pass whose scope
belongs to a separate gap; with it unwired the fingerprint comparison keeps taking the
`entry is None`/`current is None` re-include path, which is the money-not-a-contact trade that
branch already documents. Do NOT leave this unsaid: plan 03 files it as a triaged `defect` todo
with `evidence: operator-claude-plugin/scripts/run_manifest.py:449`.

**RED first:** write the cross-run test before the change — build `held_entries` by calling
`held_queue.stable_key(source_row)` (never a hardcoded literal, or the test agrees with itself and
passes falsely), give run B's row a different `row_id`, and observe the row land in `to_resume`
with `skipped == ()` on today's code.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_run_manifest.py operator-claude-plugin/tests/test_watch_settle_reporting.py operator-claude-plugin/tests/test_watch_bound_fallback.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output</fails_when>
    <automated>/usr/bin/grep -c "held_entries.get(held_queue.stable_key(row))" operator-claude-plugin/scripts/run_manifest.py</automated>
    <fails_when>the printed count is not 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/scripts/run_manifest.py` contains `held_entries.get(held_queue.stable_key(row))` exactly once, and no longer contains `held_entries.get(row_id)`.
    - `test_run_manifest.py` contains a test whose held-entries fixture key is built by calling `held_queue.stable_key(...)` and whose resume row carries a different `row_id`, asserting the entry is found.
    - A test asserts a `create`-verb entry puts run B's row in `skipped`, and a `retry`-verb entry puts it in `to_resume`.
    - A test asserts the `row_id` value reported inside `skipped`/`still_held` records equals the row's own `row_id`, not the stable key.
    - `/usr/bin/grep -c "def rows_to_resume(rows, manifest, \*, held_entries=None, current_outcomes=None):" operator-claude-plugin/scripts/run_manifest.py` prints 1 — the signature is unchanged.
  </acceptance_criteria>
  <done>A held entry saved in one run is found, and honoured, by the next run's resume — under a different positional row_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: enrich-before-ingest collects the confirmed domains, stamps them at persist, and reads new_person back at step 6</name>
  <files>operator-claude-plugin/scripts/preingest.py, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_preingest_match.py, operator-claude-plugin/tests/test_held_facet_render_composition.py, operator-claude-plugin/tests/test_skill_sequence_coverage.py</files>
  <read_first>
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` steps 2 (:146-215 — the company-row confirm table, `company_domain.to_envelope_spec`, `preingest.classify_matches`, `match_state.save(match_run_id, classified)`), 3 (:252-318 — `preingest.apply_match_decisions` moving a confirmed row INTO the auto-matched group), 5 (:739-780 — the ONLY live `build_entry`/`save` writer), 6 (:855-905 — the render fence and its hardcoded `known_company_domains = set()` at :869), 8 (:1193-1200 — the `watch.resume_or_disclose(rows)` call).
    - `operator-claude-plugin/scripts/preingest.py` — `classify_matches` and its five buckets (the auto-matched dict literal at :519-521, `CANDIDATE_KEYS` at :133), `apply_match_decisions`, `index_company_dependencies` (:1259-1276 — the "fold a bucket into a lookup" shape to copy), `build_rows_spec`, `parse_outcome`, `UNPARSEABLE_OUTCOME`.
    - `operator-claude-plugin/scripts/company_domain.py:198-228` — `to_envelope_spec` returns `{"companies": [{"name","domain"}|{"name"}]}`; a name-only row has NO `domain` key.
    - `operator-claude-plugin/scripts/enrichment.py` — `_clean_domain`, `FREEMAIL_DOMAINS`, `MATCH_LOOKUP_KEYS`.
    - `operator-claude-plugin/scripts/held_queue.py` as landed by plan 01 — `build_entry(..., company_known=)`, `COMPANY_KNOWN_SOURCES`, `stable_key`, `stamped_domains`.
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:555-568` — the `("enrich-before-ingest", (...))` registry entry and its covering test nodeid.
    - `operator-claude-plugin/tests/test_held_facet_render_composition.py` in full.
    - `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` — the skill-contract fence this edit must keep green.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md` § Seam Map 4 (the three candidate stamp sources, and the ordering correction that the write happens at step 5, not step 6), § Common Pitfalls 3, § Open Question 2.
  </read_first>
  <behavior>
    - `preingest.confirmed_company_domains(classified)` returns a mapping of cleaned domain to the word `step2_match` for each auto-matched entry whose row email yields a non-freemail cleaned domain.
    - A step-3-confirmed row (moved into the auto-matched group by `apply_match_decisions`) contributes its domain identically — one code path, not a second branch.
    - Passing a company spec of `{"companies": [{"name": "Australian Turf Club", "domain": "australianturfclub.com.au"}]}` also yields that domain, under the word `step2_company_row`.
    - A name-only company entry (no `domain` key) contributes nothing and does not raise.
    - A freemail address contributes nothing.
    - When both sources name the same domain, the company-row word wins.
    - Step 6's fence, driven end to end over a real saved-and-reloaded queue, puts a Jimmy-shaped entry in the new-person bucket.
  </behavior>
  <action>
**A. `preingest.confirmed_company_domains(classified, company_spec=None) -> dict[str, str]` (new).**
The zero-new-lookup collector. Both sources are already produced by step 2's ONE match call and
step 2's own confirm table — no new HubSpot read, and no dormant function wired (RESEARCH
Pitfall 3 explicitly warns `index_company_dependencies`/`assign_same_run_company_ids` have zero
skill call sites; do not wire them here):
  - the auto-matched bucket — each entry's row email, split on `@`, host through
    `enrichment._clean_domain`, dropped when empty or in `enrichment.FREEMAIL_DOMAINS`. Because
    `apply_match_decisions` moves a step-3-confirmed row INTO that bucket, the confirmed-MEDIUM
    case needs no second branch. Source word `step2_match`.
  - `company_spec` — the `{"companies": [...]}` dict `company_domain.to_envelope_spec` returns when
    the batch carried company rows; each present `domain`, cleaned the same way. A name-only entry
    has no `domain` key and contributes nothing. Source word `step2_company_row`, and it overrides
    a `step2_match` claim on the same domain.
  Both words must be members of `held_queue.COMPANY_KNOWN_SOURCES`; assert that relationship in a
  test rather than restating the literals in a third place.
  Put in the docstring, one sentence each: that this is EVIDENCE, not proof — an existing HubSpot
  contact's own email domain is not guaranteed to be that company's canonical property (the UAT's
  own ATC observation: 10 contacts at `@australianturfclub.com.au`, 4 associated) — and that being
  wrong is bounded, because the ingest lane resolves the company server-side and downgrades an
  unresolvable create to review (CLAUDE.md §13.0.1), so a false new-person reading costs one
  offered row and never a bad landing.

**B. `enrich-before-ingest/SKILL.md` step 2.** After `match_state.save(match_run_id, classified)`,
add one line to the same fence binding `confirmed_domains = preingest.confirmed_company_domains(
classified, company_spec)` — bind `company_spec` from `to_envelope_spec`'s own result where the
batch had company rows, else `None`. Add one short prose paragraph saying what it is for, that it
is a fold over data step 2 already has rather than a lookup, and naming the closed source
vocabulary. If step 3 runs, re-derive `confirmed_domains` from the re-saved classified buckets
after `apply_match_decisions`, so a step-3 confirmation counts — state that in one sentence at
step 3.

**C. `enrich-before-ingest/SKILL.md` step 5 — the persist site.** `build_entry` gains the stamp:
derive this row's own cleaned email domain from the MERGED row the same way, look it up in
`confirmed_domains`, and pass `company_known={"domain": d, "source": confirmed_domains[d]}` when
present, `None` otherwise. And change the map key: `held_entries[held_queue.stable_key(row)] =
entry` where `row` is the loop's own `unmatched_rows` item — **the SOURCE row, never
`merged_by_id.get(row_id, row)`**. Put the reason in the prose in one sentence: the resume side
(`run_manifest.rows_to_resume`) only ever sees the caller's source rows, so keying on the merged
row would give Jimmy an email-group key on the write side and a name-group key on the read side —
two keys for one person, which is the cross-run miss D-71-04 exists to close. `build_entry` still
STORES the merged row (F2-1, unchanged — the reveal is the entry's whole value).

**D. `enrich-before-ingest/SKILL.md` step 6.** Replace `known_company_domains = set()` with
`known_company_domains = held_queue.stamped_domains(held_entries)`, and replace the stale comment
with one sentence naming the stamp and D-71-01. Update the surrounding prose that currently
asserts nothing is known yet this run. Keep the render, the three groups, the one ready answer and
the by-heading create handoff exactly as they are — this is a seed change, not a change to what
the operator sees or is asked, and the ruling's "renders, never asks" discipline is untouched.

**E. `enrich-before-ingest/SKILL.md` step 8.** `watch.resume_or_disclose(rows,
held_entries=held_queue.load())`, per Task 1's recorded decision. Add one sentence saying
`current_outcomes` is deliberately still unwired and what that costs (the fingerprint comparison
stays on its re-include path).

**F. `test_skill_sequence_coverage.py`.** The step-6 fence gains a `held_queue.stamped_domains`
call, so the extracted tuple for the `("enrich-before-ingest", ...)` key changes. Do NOT guess the
new tuple — run the test, read the failure's own reported actual tuple, and set the registry entry
to exactly that, keeping the existing explanatory comment and extending it with one line naming
this phase. Then extend the covering test
(`test_held_facet_render_composition.py::test_step_6_fence_loads_the_queue_and_facets_what_it_loaded_not_a_dict_literal`)
to actually DRIVE the new sequence over a real saved-and-reloaded queue and assert the new-person
bucket — re-pinning the tuple without driving it is the failure mode this registry exists to
prevent.

**Prose constraints for this SKILL.md edit:** keep every existing heading text byte-identical,
because `review-triage` and this skill both delegate to headings by name; and honour the D-10b
vocabulary ban that `test_autonomy_switch_prose.py:220-221` enforces on sibling surfaces — the two
banned substrings are named only in this task's acceptance criteria, deliberately not restated
here.

**RED first:** extend the composition test to assert the new-person bucket before wiring D —
observe it fail with the entry landing in the needs-company bucket.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_preingest_match.py operator-claude-plugin/tests/test_held_facet_render_composition.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output</fails_when>
    <automated>/usr/bin/grep -c "known_company_domains = set()" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>/usr/bin/grep -c "def confirmed_company_domains(" operator-claude-plugin/scripts/preingest.py</automated>
    <fails_when>the printed count is not 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/scripts/preingest.py` contains `def confirmed_company_domains(`.
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` contains `held_queue.stamped_domains(held_entries)` and `held_entries[held_queue.stable_key(row)] = entry`, and contains neither `known_company_domains = set()` nor `held_entries[row_id] = entry`.
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` contains `held_entries=held_queue.load()` on the `watch.resume_or_disclose` call.
    - `/usr/bin/grep -ci "icp" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` prints 0 and `/usr/bin/grep -ci "tier" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` prints 0 (both print 0 on the file as it stands before this task — this is a no-regression gate, not a new property).
    - A test asserts every value `confirmed_company_domains` returns is a member of `held_queue.COMPANY_KNOWN_SOURCES`.
    - A test asserts a freemail address and a name-only company entry each contribute nothing.
    - `test_skill_sequence_coverage.py`'s `("enrich-before-ingest", ...)` registry tuple contains `held_queue.stamped_domains`, and its covering test in `test_held_facet_render_composition.py` asserts `held_queue.FACET_NEW_PERSON` over a queue it saved and reloaded.
  </acceptance_criteria>
  <done>Step 6 renders Jimmy as a new person, from a stamp step 5 wrote, with no lookup anywhere in the path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: review-triage reads the same stamp on a cold start and settles the create under the stable key</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/tests/test_review_triage_facets.py, operator-claude-plugin/tests/test_skill_sequence_coverage.py</files>
  <read_first>
    - `operator-claude-plugin/skills/review-triage/SKILL.md` steps 2b (:83-132 — the read/bucket fence and its `known_company_domains = set()` at :98), 2c (:134-180 — the one continuously-numbered table and the closed provenance vocabulary for adding a domain in-conversation), 4a (:290-305 — the CSV build and `created_by_row_id`), 4b (:306-320 — the by-heading dispatch), 4c (:321-375 — the confirm-then-mark fence and the ONLY live `held_queue.record_verb` call site).
    - `operator-claude-plugin/scripts/held_queue.py` as landed by plan 01 — `stamped_domains`, `stable_key`, `record_verb`, `legacy_reason`, `classify_read`'s four answers.
    - `operator-claude-plugin/tests/test_review_triage_facets.py` in full — its `_entry()` helper, its Jimmy/Katie fixtures, and `test_one_held_new_person_end_to_end_read_render_create_confirm_mark`, the covering test for all three of this skill's registry entries.
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:525-555` — the three `("review-triage", ...)` registry entries.
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md` § Seam Map 1 (the `skip`/`drop`/`retry` wiring question, assumption A3), § Seam Map 3.
    - `CLAUDE.md` §13.0.1 — the ONE association implementation and the server-side absent-company downgrade this skill must not reproduce.
  </read_first>
  <behavior>
    - On a cold start with no prior conversation, a stamped Jimmy-shaped entry loaded from disk lands in the new-person bucket of `by_facet`.
    - A domain the operator names in-conversation still ADDS to the set on top of the stamps (2c's existing route, unchanged).
    - An unstamped entry at an unknown domain still reads needs-company — the safe direction is preserved.
    - After the create confirm, `held_queue.record_verb` is called with the entry's own stable key, and reloading shows the entry settled.
    - A legacy-shaped queue makes step 2b say the wipe sentence rather than rendering an empty table.
  </behavior>
  <action>
**A. Step 2b.** Replace `known_company_domains = set()` with
`known_company_domains = held_queue.stamped_domains(held_entries)`. Rewrite the surrounding prose
paragraph that currently states "With nothing yet confirmed this sitting, every entry reads
needs-company" — that is no longer true and is the exact sentence that made the cold start
useless. The replacement must say: the set starts from what each entry recorded at persist time
(D-71-01), and 2c's in-conversation route still ADDS to it from the same closed provenance
vocabulary already named there (`hubspot_lookup`, `operator_statement`, `provider_result`,
`same_row_derivation`). Keep unchanged, verbatim: the "performs no HubSpot search of its own"
paragraph and its `write_grant.py` reasoning (D-71-02 is the same refusal), and the anomalous /
absent distinction.

**B. Step 2b's anomalous branch.** Extend it by one sentence: when `held_state` is anomalous, call
`held_queue.legacy_reason()` (plan 01) and say ITS sentence when it returns one, instead of the
generic unreadable line — that is how the operator learns the D-71-05 wipe is what is needed
rather than believing the backlog is empty or broken.

**C. Step 4a and 4c — the key.** `created_by_row_id` is keyed today by whatever `rid` 2c's table
carried, which after plan 01 IS the stable key (2b iterates `held_queue.load()`'s own keys, so it
is already key-agnostic). Confirm that by reading, and make it explicit in the prose in one
sentence: the identifier carried from 2c through 4a into 4c's `held_queue.record_verb` call is the
entry's stable key (D-71-04), never a positional row id, and the `row_id` that `build_rows_spec`
mints for the confirm pass is a different, throwaway thing. Do not rename the local variable if
that would churn the fence — add the sentence and keep the code shape.

**D. RESEARCH assumption A3 — settle it by reading, not by assuming.** Steps 5-8's prose names
`skip`/`drop`/`retry` in the answer vocabulary but this research found no `held_queue.record_verb`
call site for them. Read those steps; if the calls are genuinely absent, add the missing
`record_verb` call in the SAME idiom 4c already uses (one call per decided row, after the
decision, never on a hope) so the four verbs are symmetric — a `skip` that is not recorded is a
row the operator is asked about again next sitting, which is the same friction this phase exists
to remove. If they are present, say so in the SUMMARY and change nothing.

**E. `test_skill_sequence_coverage.py`.** The 2b fence gains `held_queue.stamped_domains`, so the
`("review-triage", ...)` read/bucket tuple changes. Same instruction as Task 2: run the test, read
the reported actual tuple, set the registry to exactly that. If step D adds a `record_verb` call to
a step that had no registered fence, register it too, with its own covering test. Extend
`test_review_triage_facets.py::test_one_held_new_person_end_to_end_read_render_create_confirm_mark`
to drive a COLD START — save the stamped queue to a temp path, load it fresh with no prior
conversation state, and assert the new-person bucket and the post-mark settled read.

**Prose constraints:** keep every heading byte-identical (this skill is delegated to BY heading
from `enrich-before-ingest` step 6 and delegates onward to `contact-upload` by heading); honour the
same D-10b vocabulary ban named in this task's acceptance criteria; and do not add any
client-side company creation or any client-side absent-company downgrade — the needs-company
handoff stays by-heading to `enrich-records`'s companies form exactly as 4c already writes it.

**RED first:** extend the composition test's cold-start assertion before wiring A — observe the
entry land in the needs-company bucket.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_review_triage_facets.py operator-claude-plugin/tests/test_skill_sequence_coverage.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any number before `failed`, or reports fewer than 3006 passed</fails_when>
    <automated>/usr/bin/grep -c "known_company_domains = set()" operator-claude-plugin/skills/review-triage/SKILL.md</automated>
    <fails_when>the printed count is not 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/skills/review-triage/SKILL.md` contains `held_queue.stamped_domains(held_entries)` and `held_queue.legacy_reason`, and contains no `known_company_domains = set()`.
    - `/usr/bin/grep -ci "icp" operator-claude-plugin/skills/review-triage/SKILL.md` prints 0 and `/usr/bin/grep -ci "tier" operator-claude-plugin/skills/review-triage/SKILL.md` prints 0.
    - `/usr/bin/grep -c "^#" operator-claude-plugin/skills/review-triage/SKILL.md` prints 3 — the same count the file carries before this task, so no heading line was added or removed.
    - `test_skill_sequence_coverage.py`'s read/bucket `("review-triage", ...)` tuple contains `held_queue.stamped_domains`.
    - `test_review_triage_facets.py::test_one_held_new_person_end_to_end_read_render_create_confirm_mark` saves a stamped queue to a temp path, loads it fresh, asserts `held_queue.FACET_NEW_PERSON`, and asserts the entry is settled after the mark.
    - The SUMMARY states whether `skip`/`drop`/`retry` already had `record_verb` call sites, and what was done.
    - Full plugin suite exits 0 with at least 3006 passed and no `failed` count.
  </acceptance_criteria>
  <done>A fresh `review-triage` sitting, with no prior conversation, reads Jimmy as a new person from the queue alone and settles his create under the stable key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step-2 match/company-confirm results → the persisted `company_known` stamp | Backend-derived and operator-confirmed values cross into a durable entry field that later governs what the operator is offered. |
| `held_queue.json` on disk → both operator-facing renders | A stamp written by a prior run drives a create offer in a later sitting, possibly in a different conversation. |
| the plugin → HubSpot / n8n | Deliberately NOT crossed by this plan: no lookup, no dispatch, no arming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-07 | Spoofing | the `company_known` stamp as a create authority | medium | mitigate | The stamp is EVIDENCE, not authority: it only moves a row from the needs-company render group to the new-person render group. The create still needs the operator's own count-restating reply, still travels the ONE ingest dispatch under a review-lane grant, and the company is still resolved server-side with an unresolvable create downgraded to review (CLAUDE.md §13.0.1). A wrong stamp therefore costs one offered row, never a bad landing. Asserted by the `confirmed_company_domains` docstring and its tests. |
| T-71-08 | Elevation of Privilege | a widened `known_company_domains` seed becoming a second company resolver | high | mitigate | Both sources are folds over data step 2 already holds; `preingest.confirmed_company_domains` performs no network call and takes no config. `review-triage` 2b's "performs no HubSpot search of its own" paragraph and its `write_grant.py` reasoning are kept verbatim. D-71-02's rejected lookup is not introduced. Verified by the plan's prohibitions and by `confirmed_company_domains` having no client/session parameter. |
| T-71-09 | Information Disclosure | operator-facing prose in two SKILL.md files | low | mitigate | The D-10b vocabulary ban is enforced by the negative greps in both tasks' acceptance criteria; the two banned substrings are absent from both files today, so the gate is a no-regression check with a known-zero baseline. |
| T-71-10 | Tampering | `rows_to_resume`'s changed lookup silently skipping a row that was never settled | medium | mitigate | The change is a lookup key only; every disposition rule, the fingerprint comparison, and the reported `row_id` are untouched. A key miss still falls to the `entry is None` re-include path — the money-not-a-contact direction, never a silent skip. Pinned by the cross-run test and by a test asserting the reported `row_id` is still the source position. |
| T-71-11 | Repudiation | a create recorded against the wrong entry | medium | mitigate | `record_verb` raises `HeldQueueError` when the key is not in the loaded queue (never silently creating an entry), and 4c marks only rows the independent re-read actually found. Task 3's cold-start composition test asserts the settled read after the mark. |
| T-71-12 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package in any ecosystem (RESEARCH § Package Legitimacy Audit: "Not applicable"). No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/` exits 0 with at least 3006 passed and no `failed` count.
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form; the directory form is broken on node 24).
- `git status --porcelain -- n8n/` prints nothing — this plan touches no workflow JSON and no generator.
- `git status --porcelain` lists only repo paths; no edit was made under `~/.claude/plugins/` (edits are in the repo's `operator-claude-plugin/`, never an install mirror).
</verification>

<success_criteria>
- `preingest.confirmed_company_domains` exists, is called from `enrich-before-ingest` step 2, and its values are `held_queue.COMPANY_KNOWN_SOURCES` members.
- Both SKILL.md surfaces derive `known_company_domains` from `held_queue.stamped_domains`; neither contains a hardcoded empty set.
- `held_entries[held_queue.stable_key(row)] = entry` is the step-5 write, keyed on the SOURCE row.
- `run_manifest.rows_to_resume` looks up by `held_queue.stable_key(row)`, and step 8 passes `held_entries=`.
- Both skills' registry tuples and their covering tests drive `stamped_domains` and assert `FACET_NEW_PERSON`.
- Full plugin suite and the n8n suite both green.
</success_criteria>

<artifacts_this_phase_produces>
Produced by THIS plan:

**Functions (new):**
- `preingest.confirmed_company_domains(classified, company_spec=None) -> dict[str, str]`

**Call-site changes (existing symbols, new arguments/keys):**
- `run_manifest.rows_to_resume` — `CONFIDENCE_HELD` lookup is now `held_entries.get(held_queue.stable_key(row))`
- `enrich-before-ingest/SKILL.md` step 5 — `held_queue.build_entry(..., company_known=...)`, `held_entries[held_queue.stable_key(row)] = entry`
- `enrich-before-ingest/SKILL.md` step 6 and `review-triage/SKILL.md` step 2b — `known_company_domains = held_queue.stamped_domains(held_entries)`
- `enrich-before-ingest/SKILL.md` step 8 — `watch.resume_or_disclose(rows, held_entries=held_queue.load())`
- `review-triage/SKILL.md` step 2b — `held_queue.legacy_reason()` on the anomalous branch

**Registry changes:**
- `operator-claude-plugin/tests/test_skill_sequence_coverage.py` — the `("enrich-before-ingest", ...)` and `("review-triage", ...)` held-queue read/bucket tuples gain `held_queue.stamped_domains` (exact tuples read from the test's own failure output, not guessed)

**New test cases (in existing files — no new test file):**
- `test_run_manifest.py` — cross-run stable-key lookup, verb short-circuits, reported-row_id invariant
- `test_preingest_match.py` — `confirmed_company_domains` over both sources, freemail and name-only exclusions, the `COMPANY_KNOWN_SOURCES` membership assertion
- `test_held_facet_render_composition.py` — the step-6 fence driven over a saved-and-reloaded queue, asserting `FACET_NEW_PERSON`
- `test_review_triage_facets.py` — the cold-start read/render/create/confirm/mark composition
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-02-SUMMARY.md` when done.
Record in it: the Open Question 1 decision and its cost sentence; the exact registry tuples before
and after, and that they were read from the test's own output rather than guessed; and the answer
to RESEARCH assumption A3 (whether `skip`/`drop`/`retry` already had `record_verb` call sites).
</output>
