---
phase: 71-a-held-new-person-lands-in-hubspot-with-one-reply
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/held_queue.py
  - operator-claude-plugin/scripts/suggestion_declines.py
  - operator-claude-plugin/tests/test_held_queue.py
  - operator-claude-plugin/tests/test_held_queue_facets.py
  - operator-claude-plugin/tests/test_suggestion_declines.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
autonomous: false
requirements: []
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `no_match` held entry built from a Jimmy-Busteed-shaped row (revealed email at a company already in HubSpot) carries a persist-time company-known stamp (D-71-01) and, loaded on a fresh sitting from disk, reads `held_queue.FACET_NEW_PERSON` — the exact case quick batch 260911-w6n left false."
    - "The `held_queue.json` entries map is keyed by the row's satisfied `required_identity.any_of` group, normalised (D-71-04), derived by ONE function both the write site and the read site call — a second run's own positional `row-1` can no longer silently overwrite a prior run's held row-1 entry (D-69-03 accumulate contract, D-69-04 `row_id` is never a key)."
    - "A held entry for a person whose normalised identity key contains a whole-token forbidden marker (Grant Dewsbury, a company named Token) persists; a key that is NOT the entry's own derived identity and IS marker-shaped (`webhook_secret`, an arming-grant token value) is still refused, writing nothing."
    - "A legacy `row-N`-keyed `held_queue.json` document (the live run `a254d1e` shape) is refused — never silently read as an empty queue — with a one-line reason naming the D-71-05 wipe."
    - "`classify_facet`'s signature, its purity, and `confidence.ALL_HOLD_CODES` are unchanged (D-71-02, D-71-03, D-70-11): `known_company_domains` stays an ARGUMENT the caller derives from the stamp, and the default empty set still means `needs_company` for every usable-email entry."
  artifacts:
    - operator-claude-plugin/scripts/held_queue.py
    - operator-claude-plugin/scripts/suggestion_declines.py
    - operator-claude-plugin/tests/test_held_queue.py
    - operator-claude-plugin/tests/test_held_queue_facets.py
    - operator-claude-plugin/tests/test_suggestion_declines.py
  key_links:
    - "`held_queue.identity_keys(row)` is called from BOTH `held_queue.save()`'s key-exemption check and (plan 02) `run_manifest.rows_to_resume`'s lookup — never re-derived independently in two places."
    - "`held_queue.stable_key(row)` is derived from `config/column_mapping.yaml`'s `required_identity.any_of` order and `suggest_contacts.name_key` — never a new normalisation rule."
    - "`held_queue.stamped_domains(entries)` is the ONE derivation both skill surfaces (plan 02) call to turn stamps into `known_company_domains`."
  prohibitions:
    - "No new hold code — `confidence.ALL_HOLD_CODES` stays closed (D-70-11); a facet is a read, never a verdict."
    - "No client-side company creation through the ingest lane, and no second company-resolution implementation anywhere (CLAUDE.md §13.0.1) — this plan performs no HubSpot read of any kind."
    - "No arming, no deploy, no n8n write, no live call in this plan (D-71-06 backloads the only live gate to plan 03)."
    - "No migration/lazy-rekey code for the on-disk queue (D-71-05 is a wipe, not a migration)."
    - "No edit to `_looks_forbidden` itself in any of the nine stores — the narrowing is at the CALL SITE, so `test_forbidden_marker_parity.py` needs no change and stays green unmodified."
---

<objective>
Land the one cross-item contract quick batch 260911-w6n split across four parallel plans and
therefore left false end to end: the persist-time company-known stamp (D-71-01..03), the stable
held-entry identity (D-71-04..05), and the forbidden-marker key narrowing the rekey newly makes
mandatory — in ONE plan, because they are one seam.

Purpose: `held_queue.json` becomes a store whose key means a PERSON rather than a position in
this run's spreadsheet, and whose entries carry the fact that answers "is this person's company
already in HubSpot?" without any reader performing a lookup.

Output: `held_queue.identity_keys`/`stable_key`/`stamped_domains`, a validated `company_known`
stamp on the entry schema, a call-site key exemption in the two identity-keyed stores, and a
legacy-document refusal — all pinned by RED-first tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-CONTEXT.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-RESEARCH.md
@.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-PATTERNS.md
@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/scripts/suggestion_declines.py
</context>

<assumption_delta_decision>
**Noun:** held-entry identity — the row's satisfied `required_identity.any_of` group, normalised.
**Decision:** `promote`. The stable identity key becomes the PRIMARY key of the `held_queue.json`
entries map; the positional `row_id` is DEMOTED to a carried source-position detail (it stays on
`entry["row"]["row_id"]` via `ROW_FIELD_ALLOWLIST`, and stays the value reported in
`rows_to_resume`'s `skipped`/`still_held`/`to_resume` dicts — see plan 02).
**Rationale:** D-69-03 makes the document accumulate across runs, and under accumulation a
per-batch positional key is not a weak key but a destructive one — run B's own `row-1` overwrites
run A's held `row-1` inside the very `save()` that was meant to merge them.
**Invariant test (added by Task 2):** every held entry round-trips save → load → `record_verb` →
`is_settled` under its stable key across two distinct `run_id`s, with a different positional
`row_id` minted on the second run.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Confirm the serialised stable-key shape before it becomes published on-disk schema</name>
  <read_first>
    - `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-CONTEXT.md` — D-71-04 (rated **one-way**: "a published on-disk schema on operator machines; changing it again means another migration") and its "Claude's Discretion: exact normalisation of the stable key and its serialisation in the JSON map".
    - `operator-claude-plugin/scripts/suggestion_declines.py:57-58` (`KEY_SEPARATOR = "::"`, `NAME_SEPARATOR = "|"`) and `:141-156` (`entry_key`'s own separator contract docstring).
    - `config/column_mapping.yaml:60-64` — the three `required_identity.any_of` groups, in order.
  </read_first>
  <decision>The literal serialisation of the held-entry stable key — the strings that become dict keys inside `held_queue.json` on operator machines.</decision>
  <context>
D-71-04 is rated **one-way** in 71-CONTEXT.md ("a published on-disk schema on operator machines;
changing it again means another migration"). Everything else in this phase is reversible; this is
not. After the D-71-06 gate there is no migration path back — only a second wipe. 71-CONTEXT.md
leaves the exact normalisation and serialisation to Claude's Discretion, so this is the one
discretion item that deserves an operator's eye before it lands.
  </context>
  <options>
    <option id="prefixed">
      <name>Group-prefixed (recommended)</name>
      <pros>
        `email::jbusteed@australianturfclub.com.au`, `name::grant|dewsbury|darwin turf club`,
        `linkedin::linkedin.com/in/some-person`, and the total fallback
        `source-position::row-3`. Separators mirror `suggestion_declines.py`'s already-shipped
        `KEY_SEPARATOR = "::"` / `NAME_SEPARATOR = "|"`, so the two sibling stores read alike.
        The prefix makes the map self-describing and collision-free between groups (a `company`
        column literally holding an email cannot collide with the email group), and guarantees a
        stable key can never be produced as, or mistaken for, a legacy `row-N` key by Task 3's
        detector.
      </pros>
      <cons>Keys are longer and less pretty to read by eye in the raw JSON.</cons>
    </option>
    <option id="bare">
      <name>Bare value, no group prefix</name>
      <pros>Shorter keys; the email group reads as a plain address.</pros>
      <cons>
        Two groups can collide on one string; nothing distinguishes a stable key from a future
        positional one, so Task 3's legacy detector would need a second, weaker signal.
      </cons>
    </option>
  </options>
  <resume-signal>Reply `prefixed`, `bare`, or name a different shape. Approving is approving an on-disk format; the only path away from it afterwards is the D-71-05 wipe.</resume-signal>
  <acceptance_criteria>
    - The operator has answered with either an approval of the four literal forms above or a named replacement.
    - The approved form is recorded verbatim in the plan SUMMARY before Task 2 starts.
  </acceptance_criteria>
  <done>The serialised key shape is confirmed by the operator and recorded.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end — a stamped Jimmy-shaped entry, saved under its stable key, reads new_person on a fresh load</name>
  <files>operator-claude-plugin/scripts/held_queue.py, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_held_queue.py, operator-claude-plugin/tests/test_held_queue_facets.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/held_queue.py` in full — especially `ROW_FIELD_ALLOWLIST` (line 136), `_looks_forbidden`/`_first_forbidden`/`_first_forbidden_key` (199-249), `build_entry` (278), `classify_facet` (290-368), `record_verb` (396), `save` (446-500), `_validated_entries` (502-526), `load` (528), `classify_read` (543).
    - `operator-claude-plugin/scripts/suggest_contacts.py:161-176` — `name_key`, PUBLIC per D-69-04, and `_normalize_name`'s exact rule (`" ".join(str(value or "").strip().casefold().split())`).
    - `operator-claude-plugin/scripts/enrichment.py:79` — `MATCH_LOOKUP_KEYS = ("email", "firstname", "lastname", "company", "linkedin_url")`, and `_clean_domain`/`FREEMAIL_DOMAINS` (the ONE domain guard, already imported by `classify_facet`).
    - `config/column_mapping.yaml:60-64` — the `required_identity.any_of` priority order.
    - `operator-claude-plugin/scripts/suggestion_declines.py:1-38` (the accumulate/per-entry-run_id store docstring to copy the framing from) and `:216-245` (`save`'s validate-then-atomic-write shape).
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md:739-780` — step 5, the ONLY live production writer: `held_entries[row_id] = entry` at :766 and the `unmatched_rows` loop it sits in.
    - `.planning/phases/71-.../71-RESEARCH.md` § Seam Map 2, 3, 6, 7 and § Common Pitfalls 1-2.
    - `.planning/phases/71-.../71-PATTERNS.md` § `held_queue.py` and § Shared Patterns.
    - `operator-claude-plugin/tests/test_forbidden_marker_parity.py` — its `MUST_REFUSE` tuple includes `held_queue._FORBIDDEN_NAME_MARKERS` themselves, so `_looks_forbidden("grant")` MUST keep returning True. The matcher is not what changes.
  </read_first>
  <behavior>
    - `identity_keys({"email": "JBusteed@AustralianTurfClub.com.au ", ...})` returns a tuple whose first member is the email-group key, lowercased and stripped.
    - `identity_keys({"firstname": "Grant", "lastname": "Dewsbury", "company": "Darwin Turf Club"})` returns the name-group key `"name::grant|dewsbury|darwin turf club"`.
    - `identity_keys` returns EVERY satisfied group, in `required_identity.any_of` order; `stable_key(row)` is its first member.
    - `stable_key({})` returns the `source-position::` fallback built from `row["row_id"]`, and a row with no `row_id` either still yields a total, non-`row-N` string.
    - A `build_entry(..., company_known={"domain": "australianturfclub.com.au", "source": "step2_match"})` entry round-trips through `save` → `load` unchanged.
    - `stamped_domains(entries)` returns the set of cleaned domains carried by the entries' stamps, and `{}` for an entries map with no stamps.
    - `classify_facet(entry, stamped_domains({key: entry}))` returns `FACET_NEW_PERSON` for the Jimmy shape; the SAME entry with `known_company_domains=frozenset()` still returns `FACET_NEEDS_COMPANY`.
    - `save()` refuses an entries map keyed `{"webhook_secret": entry}` (raises `HeldQueueError`, writes nothing) and ACCEPTS `{"name::grant|dewsbury|darwin turf club": entry}` when that key is in `identity_keys(entry["row"])`.
    - A linkedin-only row for Grant Dewsbury (`linkedin_url` set, no email, no name pair) derives a key whose tokenised form contains the `grant` marker, and STILL persists — because that key is in `identity_keys(entry["row"])`.
    - Cross-run invariant: save under run A's `run_id`, load, `record_verb(stable_key, VERB_CREATE, run_id_B)`, reload → `is_settled` is True, and a fresh row with a DIFFERENT positional `row_id` but the same identity derives the same stable key.
  </behavior>
  <action>
Land the whole data path for ONE person, end to end, in `held_queue.py` plus its tests. This is
the tracer: it wires the store layer, the stamp, the key, and the facet read for a single row,
and everything after it in this phase is expansion out from this proven slice.

**A. `identity_keys(row) -> tuple[str, ...]` (new, PUBLIC).** Returns every satisfied
`required_identity.any_of` group's normalised key, in that file's own priority order
(email, then firstname+lastname+company, then linkedin_url), using the serialisation Task 1
approved. Normalisation rules, each named here so nothing is reinvented:
  - email group: `row["email"]` stripped and case-folded; skip the group when it does not strip
    to a non-empty string containing exactly one `@` with both halves non-empty (the same
    usability test `classify_facet` step 1 already applies — mirror its rule, do not import a
    private name).
  - name group: `suggest_contacts.name_key(row)` for the two halves (PUBLIC per D-69-04, never
    re-normalise a name independently), joined with `NAME_SEPARATOR` to a `company` normalised by
    the SAME rule `suggest_contacts._normalize_name` applies, restated locally rather than
    reaching into another module's private name (`run_manifest._present`'s own docstring is the
    precedent for restating rather than importing). Skip the group when `name_key` is `None` or
    the normalised company is empty.
  - linkedin group: `row["linkedin_url"]` stripped and case-folded; skip when empty.
  Declare `KEY_SEPARATOR`/`NAME_SEPARATOR` as module constants mirroring
  `suggestion_declines.py:57-58`, and a `SOURCE_POSITION_PREFIX` for the fallback.

**B. `stable_key(row) -> str` (new, PUBLIC, TOTAL).** `identity_keys(row)[0]` when non-empty,
else `f"{SOURCE_POSITION_PREFIX}{KEY_SEPARATOR}{row.get('row_id')}"`. Docstring must state, in
one sentence each: that it is the ONE derivation both the write site and `run_manifest.
rows_to_resume`'s read site call (never re-derived — RESEARCH Pitfall 2), that the fallback is
deliberately prefixed so it can never be produced as, or mistaken for, a legacy `row-N` key, and
that **it is computed from the SOURCE row, not the merged row** — the merged row's revealed email
would put Jimmy in the email group while his source row (blank email) is in the name group, and
two keys for one person is exactly the cross-run miss D-71-04 exists to close.

**C. The `company_known` stamp (D-71-01..03).** Add an OPTIONAL `company_known=None` keyword to
`build_entry`, written onto the entry as a dict `{"domain": <cleaned>, "source": <word>}` only
when supplied. Declare a closed vocabulary `COMPANY_KNOWN_SOURCES = ("step2_match",
"step2_company_row")` and validate it in BOTH `save()` and `_validated_entries()` exactly the way
`status.verb` is already validated (absent is valid; present must be a dict whose `domain` is a
non-empty string and whose `source` is in the closed tuple — anything else refuses / degrades the
whole document). Clean the domain through `enrichment._clean_domain` at build time so every
reader gets one already-normalised form. Do NOT add a same-run-create source — plan 02's step-2
sources are the only two this phase wires, and the vocabulary being closed is what makes a third
one a deliberate future widening rather than a silent one.

**D. `stamped_domains(entries) -> set[str]` (new, PUBLIC).** The ONE derivation both skill
surfaces call in plan 02 to turn an entries map into `known_company_domains` — the same
"one filter every later report calls, instead of each growing its own" rule `open_entries`'s own
docstring already states. Returns the set of non-empty `entry["company_known"]["domain"]` values,
tolerant of a non-dict entry or a missing stamp. `classify_facet` itself is NOT touched: its
signature, its purity, its decision table and its default-empty-set safe direction all stand
(D-71-02, D-71-03).

**E. `save()`'s entries-map key check — the call-site narrowing.** Today
`held_queue.py:453-459` runs `_looks_forbidden(row_id)` on every dict key. Replace that with:
exempt the key from the marker scan when `key in identity_keys(entry.get("row") or {})`; keep
`_looks_forbidden(key)` and the identical refusal message for every other key. Rationale to put
in the code comment, in one sentence: a key that IS the entry's own derived identity is a
projection of `ROW_FIELD_ALLOWLIST`-filtered fields, which `_allowlisted_row` already closed and
which `260911-w6o` already exempted from value scanning for exactly this reason — whereas a key
that is not the entry's own identity is an arbitrary caller-supplied string and keeps the
original refusal. `_looks_forbidden` itself is NOT edited: `test_forbidden_marker_parity.py`'s
`MUST_REFUSE` tuple pins `_looks_forbidden("grant") is True` and must stay green untouched.
Note the asymmetry in the docstring: `identity_keys` is computed from `entry["row"]` (the MERGED
row, which `build_entry` stored) while the KEY was derived from the SOURCE row — merged is a
superset (the waterfall fills blanks, it does not replace a present `firstname`/`lastname`/
`company`/`email`), so a source-derived key is always in the merged row's `identity_keys`.
Two consequences to state in the docstring rather than leave for a reader to discover:
the exemption is EXACT MEMBERSHIP, so `identity_keys` must return byte-identical strings to the
ones `stable_key` hands the write site (a linkedin-only Grant Dewsbury key tokenises to contain
`grant`, and is exempt only because it is literally in the tuple); and `stable_key`'s
`source-position::` fallback is NOT in `identity_keys` (that function returns only SATISFIED
groups), so a no-identity row's key goes through the full marker scan — correct, since it is
built from a system-minted `row_id` and can never legitimately carry a person's name.

**F. Update the module docstring** to D-69-03's accumulate framing, copying `suggestion_declines.
py:1-38`'s own wording ("The document ACCUMULATES; each entry carries its OWN `run_id`") adjusted
for the fact that `held_queue`'s document-level `run_id` field is NOT removed here — `record_verb`
already documents that it names the run that LAST TOUCHED the file.

**G. Move the step-5 write key, in THIS task, not plan 02.** In
`enrich-before-ingest/SKILL.md` step 5's fence, change `held_entries[row_id] = entry` to
`held_entries[held_queue.stable_key(row)] = entry`, where `row` is the loop's own
`unmatched_rows` item — **the SOURCE row, never `merged_by_id.get(row_id, row)`**. Add one prose
sentence giving the reason: the resume side (`run_manifest.rows_to_resume`, plan 02) only ever
sees the caller's source rows, so keying on the merged row would give a revealed-email person an
email-group key on the write side and a name-group key on the read side — two keys for one person,
which is the cross-run miss D-71-04 exists to close. `build_entry` still STORES the merged row
(F2-1, unchanged).

This line belongs here and not in plan 02 because Task 3 lands the `^row-\d+$` refusal in
`_validated_entries`: if the only production writer were still emitting `row-N` keys between the
two plans, every queue it wrote would be refused by the next `load()`. The stamp itself
(`company_known=`) is NOT added here — it needs plan 02's `confirmed_company_domains` — so this
task changes the KEY only and leaves `build_entry`'s call arguments alone.

**RED first, in this order, each observed failing before its fix:** (1) the Jimmy composition test
(`KeyError`/`TypeError` — no stamp, no `stamped_domains`); (2) the `{"webhook_secret": entry}` /
`{"name::grant|dewsbury|darwin turf club": entry}` pair (the second raises `HeldQueueError` on
today's code — this is Pitfall 1's RED half, expect it); (3) the cross-run `record_verb` invariant.
Do NOT renumber or rewrite the existing `row-1`/`row-2` fixtures in these two files yet — `save()`
has never rejected a `row-N` key and `load()` does not yet either, so every existing test keeps
passing through this task; the fixture migration belongs with Task 3's legacy detector, which is
what makes them fail.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_held_queue.py operator-claude-plugin/tests/test_held_queue_facets.py operator-claude-plugin/tests/test_forbidden_marker_parity.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output, or the summary line reports fewer passing tests than before this task's new cases were added</fails_when>
    <automated>/usr/bin/grep -c "def identity_keys(\|def stable_key(\|def stamped_domains(" operator-claude-plugin/scripts/held_queue.py</automated>
    <fails_when>the printed count is less than 3</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/scripts/held_queue.py` contains `def identity_keys(`, `def stable_key(`, and `def stamped_domains(`.
    - `held_queue.py` contains `COMPANY_KNOWN_SOURCES = ("step2_match", "step2_company_row")` and validates it in both `save()` and `_validated_entries()`.
    - `/usr/bin/grep -c "def classify_facet(entry, known_company_domains=frozenset()):" operator-claude-plugin/scripts/held_queue.py` prints 1, and `/usr/bin/grep -c "known_cleaned = {enrichment._clean_domain(d) for d in known_company_domains}" operator-claude-plugin/scripts/held_queue.py` prints 1 — `classify_facet`'s body is intact.
    - `/usr/bin/grep -c "^_FORBIDDEN_NAME_MARKERS = (" operator-claude-plugin/scripts/held_queue.py` prints 1 and `/usr/bin/grep -c "^def _looks_forbidden(value) -> bool:" operator-claude-plugin/scripts/held_queue.py` prints 1 — the matcher itself is not edited.
    - A test in `test_held_queue.py` asserts `held_queue.save(run_id, {"webhook_secret": entry})` raises `HeldQueueError`.
    - A test in `test_held_queue.py` asserts a Grant-Dewsbury-shaped stable key persists and reloads.
    - A test in `test_held_queue_facets.py` drives save → load → `stamped_domains` → `classify_facet` and asserts `held_queue.FACET_NEW_PERSON` for the Jimmy shape, and `FACET_NEEDS_COMPANY` for the same entry with `frozenset()`.
    - A test asserts the cross-run round-trip: save under run A, `record_verb(stable_key, VERB_CREATE, run_B)`, reload, `is_settled(...) is True`.
    - A test asserts a linkedin-only Grant-Dewsbury row persists (the exemption is exact membership in `identity_keys`).
    - `/usr/bin/grep -c "held_entries\[held_queue.stable_key(row)\] = entry" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` prints 1, and `/usr/bin/grep -c "held_entries\[row_id\] = entry" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` prints 0.
    - `git status --porcelain -- operator-claude-plugin/tests/test_forbidden_marker_parity.py` prints nothing, and the parity test passes.
  </acceptance_criteria>
  <done>A Jimmy-shaped held entry is stamped at build, saved under its stable identity key, reloaded on a fresh read, and reads `new_person` — and a Grant-named person persists while a marker-shaped non-identity key is still refused.</done>
  <reversibility rating="one-way">The serialised stable key becomes published on-disk schema on operator machines (D-71-04); Task 1's checkpoint precedes it. The `company_known` stamp is rated `costly` by D-71-01 — a schema field every reader validates, but removable without a migration since it is optional.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The already-live half of the forbidden-marker fold, and the legacy-document refusal</name>
  <files>operator-claude-plugin/scripts/suggestion_declines.py, operator-claude-plugin/scripts/held_queue.py, operator-claude-plugin/tests/test_suggestion_declines.py, operator-claude-plugin/tests/test_held_queue.py, operator-claude-plugin/tests/test_held_queue_facets.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/suggestion_declines.py:104-132` (`_first_forbidden`), `:141-156` (`entry_key`), `:158-172` (`build_entry`, `ROW_FIELD_ALLOWLIST`), `:174-218` (`first_refusal` — its key scan at 184 and its `_first_forbidden(entry.get("row"))` value scan at 207).
    - `operator-claude-plugin/scripts/held_queue.py:226-249` (`_first_forbidden_key`, the `260911-w6o` narrowing to copy) and `:502-526` (`_validated_entries`) and `:543-568` (`classify_read`'s four answers).
    - `.planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md` — its `files:` list and its "Update 2026-09-11 (quick 260911-w6o)" section naming which six stores are still unfixed.
    - `operator-claude-plugin/scripts/run_report.py:990-1000` — `held_queue.classify_read()`'s only script-side consumer, and `_add_gap`'s handling of its answer.
    - `operator-claude-plugin/scripts/remainder_queue.py:155-210` — already key-only, so nothing to do there; read it to confirm rather than assume.
    - `.planning/phases/71-.../71-RESEARCH.md` § Open Question 3 and § Open Question 4.
  </read_first>
  <behavior>
    - `suggestion_declines.save({entry_key("9605284724", {"firstname": "Grant", "lastname": "Dewsbury"}): entry})` persists instead of raising.
    - `suggestion_declines.first_refusal("webhook_secret", entry)` still returns a refusal sentence.
    - `suggestion_declines.first_refusal(key, entry)` still refuses an entry whose `provenance` or `reason` carries a marker (both keep full key-and-value scanning).
    - `held_queue.load()` on a document whose entries map holds any `^row-\d+$` key returns `{}`.
    - `held_queue.classify_read()` on that same document returns `held_queue.ANOMALOUS`.
    - `held_queue.legacy_reason(path)` returns the one-line wipe sentence for that document and `None` for a stable-keyed one.
    - A stable-keyed document still reads `PARSEABLE`.
  </behavior>
  <action>
**A. `suggestion_declines.py` — the same call-site narrowing, for the store that already has the
live defect.** `entry_key` has ALWAYS produced name-shaped keys (`"9605284724::grant|dewsbury"`),
and `first_refusal` has always run `_looks_forbidden` on them, so this store refuses a decline for
a person named Grant today, in production. Apply the identical rule Task 2 applied to
`held_queue.save()`: exempt the key from the marker scan when it equals
`entry_key(entry.get("company_id"), entry.get("row") or {})`; keep the refusal for every other
key. Additionally narrow `first_refusal`'s `_first_forbidden(entry.get("row"))` to a
`_first_forbidden_key`-shaped scan, reimplemented in this module (never imported — D-69-01's
stated anti-DRY discipline, which `test_forbidden_marker_parity.py` is what keeps honest), for
the same reason `260911-w6o` gave `held_queue`: `ROW_FIELD_ALLOWLIST` already closes the field set,
so the value scan's only live effect was refusing a person's own name. `provenance` and `reason`
keep FULL key-and-value scanning, unchanged.

**Record the scope line explicitly in the code comment and in the SUMMARY** (this is the honest
answer to RESEARCH § Open Question 4, and it corrects a factual premise): 71-CONTEXT.md's Folded
Todos note says every store takes "the same fix through the shared matcher" — **there is no shared
matcher.** There are nine deliberate reimplementations (D-69-01), pinned behaviourally identical
by `test_forbidden_marker_parity.py`, and the fix is at CALL SITES, not in the matcher, so
"moving all copies together" does not apply. The defect can only fire in a store whose KEY or
allowlisted row payload is derived from a person's or company's own name. That is
`held_queue` (Task 2) and `suggestion_declines` (here). `remainder_queue` is already key-only;
`run_manifest` and `run_state` key on a system-minted `row_id` and a closed verdict vocabulary;
`run_report` already splits key and value matchers; `match_state`/`match_handoff` key on a run id.
`written_records` DOES scan a written record's property values and so can still refuse a person
named Grant — that one is left deliberately alone because `test_written_records.py`'s T-59-02 pins
that value refusal as load-bearing, and narrowing it is a separate judgement with its own
regression cost. Say exactly this in plan 03's todo resolution; do not claim the todo is closed
for the other five.

**B. The D-71-05 legacy-document refusal, in `held_queue.py`.** CONTEXT.md leaves the signal to
discretion; **choose the key pattern `^row-\d+$`** and say why in the docstring: the stamp field
is legitimately absent on a `needs_company` entry, so stamp-presence is not a legacy signal, while
Task 2's prefixed serialisation makes `row-N` a shape `stable_key` can never produce — including
its own `source-position::` fallback. Implement as: a module-level `_LEGACY_KEY = re.compile(r"^row-\d+$")`;
`_validated_entries()` returns `None` when ANY entries-map key matches it (so `load()` degrades to
`{}` and `classify_read()` answers `ANOMALOUS` through the paths both already have — do NOT add a
fifth `classify_read` answer, `run_report.py:995` and both SKILL.md consumers read the closed
four-word vocabulary); plus a new `legacy_reason(path=None) -> str | None` probe returning ONE
sentence naming the D-71-05 wipe (e.g. "this queue was written under the pre-Phase-71 positional
keys; delete held_queue.json — the next run repopulates it under the stable identity key") and
`None` otherwise, so a caller can say the honest thing instead of "unreadable".

**C. Migrate the `row-1`/`row-2` fixtures.** This is the task where the ~30 existing literal
`row-1`/`row-2` entries-map keys in `test_held_queue.py` and `test_held_queue_facets.py` start
failing (`load()` now refuses them) — that IS the RED. Rewrite each fixture to build its key by
calling `held_queue.stable_key(row)` rather than hardcoding a new literal, so a test can never
pass falsely by agreeing with itself on a key the production code does not derive (RESEARCH
Pitfall 2's warning sign). Keep ONE deliberate legacy fixture, keyed `"row-1"` literally, as the
input to the new `ANOMALOUS`/`legacy_reason` tests.

**RED first:** write the Grant-key `suggestion_declines.save` test and the legacy-document
`classify_read` test before their fixes and observe both fail (`SuggestionDeclineError` raised;
`classify_read` returning `PARSEABLE`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_held_queue.py operator-claude-plugin/tests/test_held_queue_facets.py operator-claude-plugin/tests/test_suggestion_declines.py operator-claude-plugin/tests/test_suggestion_declines_skill.py operator-claude-plugin/tests/test_forbidden_marker_parity.py</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` appears in the output</fails_when>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/</automated>
    <fails_when>non-zero exit, or the summary line reports any number before `failed`, or reports fewer than 3006 passed</fails_when>
    <automated>/usr/bin/grep -c "^row-" operator-claude-plugin/scripts/held_queue.py</automated>
    <fails_when>the printed count is 0 — the `^row-\d+$` legacy pattern must be present in the module</fails_when>
  </verify>
  <acceptance_criteria>
    - `operator-claude-plugin/scripts/held_queue.py` contains `def legacy_reason(` and a compiled pattern whose source is `^row-\d+$`.
    - `/usr/bin/grep -c "^ABSENT = \|^PARSEABLE = \|^ANOMALOUS = \|^ANOTHER_RUN = " operator-claude-plugin/scripts/held_queue.py` prints 4 — `classify_read`'s vocabulary gained no fifth member.
    - A test asserts `held_queue.classify_read(path=<a {"row-1": entry} document>) == held_queue.ANOMALOUS` and `held_queue.load(path=<same>) == {}`.
    - A test asserts `held_queue.legacy_reason(<that path>)` is a non-empty string containing `held_queue.json`, and is `None` for a stable-keyed document.
    - `suggestion_declines.py` contains a `_first_forbidden_key`-shaped function and its `first_refusal` calls it for `row` while still calling `_first_forbidden` for `provenance`.
    - A test asserts a decline entry for firstname `Grant` persists, and another asserts `first_refusal("webhook_secret", entry)` still returns a refusal sentence.
    - No entries-map key literal `"row-1"`/`"row-2"` remains in `test_held_queue.py` / `test_held_queue_facets.py` except inside the deliberate legacy-document fixtures.
    - `git status --porcelain -- operator-claude-plugin/tests/test_forbidden_marker_parity.py operator-claude-plugin/tests/test_written_records.py` prints nothing.
  </acceptance_criteria>
  <done>A person named Grant persists in both identity-keyed stores; a marker-shaped non-identity key is still refused; and the live `a254d1e`-shaped queue reads `ANOMALOUS` with a sentence naming the wipe instead of silently reading as empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator conversation → `held_queue.json` on disk | Operator-supplied spreadsheet values and waterfall-revealed values cross into a durable, 0600 file under `durable_paths.resolve_state_path().parent`. |
| `held_queue.json` on disk → a later sitting's render | A document written by a prior run (or hand-edited, or left over from a prior schema) is read back and rendered to the operator as fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Information Disclosure | `held_queue.save()` entries-map key, after the D-71-04 rekey | high | mitigate | The key exemption is scoped to `key in identity_keys(entry["row"])` ONLY. A caller-supplied key that is not the entry's own derived identity keeps the full `_looks_forbidden` refusal, so an arming grant token or webhook secret smuggled in as a key still raises `HeldQueueError` and writes nothing. RED test: `save(run_id, {"webhook_secret": entry})` raises (Task 2). `_looks_forbidden` itself is not edited, so `test_forbidden_marker_parity.py`'s `MUST_REFUSE` invariant stands unmodified. |
| T-71-02 | Information Disclosure | the new `company_known` stamp on the entry schema | medium | mitigate | Closed vocabulary `COMPANY_KNOWN_SOURCES` validated in both `save()` and `_validated_entries()`; `domain` is passed through `enrichment._clean_domain` at build time. No free-form string reaches disk through this field, so it cannot become a smuggling channel past the allowlist. |
| T-71-03 | Tampering | the on-disk `held_queue.json` document itself | medium | mitigate | Unchanged and inherited: `durable_paths._atomic_write_0600` (0600 mode, atomic replace) is the only writer; `save()` validates EVERY entry before anything is written, so a refusal leaves the previous document byte-untouched. This plan adds validation (the stamp vocabulary, the legacy key pattern) and removes none. |
| T-71-04 | Spoofing | a legacy or foreign-shaped document read as current state | medium | mitigate | `_validated_entries()` returns `None` on any `^row-\d+$` key, so `load()` degrades whole to `{}` and `classify_read()` answers `ANOMALOUS`; `legacy_reason()` gives the caller the honest sentence. A stale positional-keyed queue can no longer be rendered as if it were this schema's data (D-71-05). |
| T-71-05 | Elevation of Privilege | any write path in this plan | high | mitigate | This plan performs no HubSpot read or write, no n8n dispatch, no arming and no deploy. `<must_haves.prohibitions>` states it; the only live gate in this phase is plan 03's D-71-06 checkpoint. Verified by the plan's own `files_modified` containing no dispatch, arming, or workflow file. |
| T-71-06 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package in any ecosystem (RESEARCH § Package Legitimacy Audit: "Not applicable"). No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/` exits 0 with no `failed` count and at least 3006 passed.
- `git status --porcelain -- operator-claude-plugin/tests/test_forbidden_marker_parity.py operator-claude-plugin/tests/test_written_records.py` prints nothing.
- `/usr/bin/grep -c "^_FORBIDDEN_NAME_MARKERS = (" operator-claude-plugin/scripts/held_queue.py` prints 1 and `classify_facet`'s `def` line is present unchanged.
- No file under `n8n/` is modified (`git status --porcelain -- n8n/` prints nothing).
</verification>

<success_criteria>
- `held_queue.identity_keys`, `held_queue.stable_key`, `held_queue.stamped_domains` and `held_queue.legacy_reason` exist and are called, not merely defined.
- A Jimmy-shaped stamped entry, saved and loaded from disk, reads `FACET_NEW_PERSON`.
- A Grant-Dewsbury-shaped key persists in both `held_queue` and `suggestion_declines`; `{"webhook_secret": entry}` does not.
- A `row-N`-keyed document reads `ANOMALOUS` with a wipe-naming reason.
- Full plugin suite green; `test_forbidden_marker_parity.py` green and unmodified.
</success_criteria>

<artifacts_this_phase_produces>
Produced by THIS plan (see plans 02 and 03 for the rest of the phase):

**Functions (new, all in `operator-claude-plugin/scripts/held_queue.py`):**
- `held_queue.identity_keys(row) -> tuple[str, ...]`
- `held_queue.stable_key(row) -> str`
- `held_queue.stamped_domains(entries) -> set[str]`
- `held_queue.legacy_reason(path=None) -> str | None`

**Functions (new, in `operator-claude-plugin/scripts/suggestion_declines.py`):**
- `suggestion_declines._first_forbidden_key(value)` (module-local reimplementation, D-69-01)

**Constants (new, `held_queue.py`):**
- `held_queue.KEY_SEPARATOR = "::"`
- `held_queue.NAME_SEPARATOR = "|"`
- `held_queue.SOURCE_POSITION_PREFIX` (the total fallback's group prefix)
- `held_queue.COMPANY_KNOWN_SOURCES = ("step2_match", "step2_company_row")`
- `held_queue._LEGACY_KEY` (compiled `^row-\d+$`)

**Entry-schema fields (new, on-disk):**
- `entry["company_known"] = {"domain": <cleaned domain>, "source": <one of COMPANY_KNOWN_SOURCES>}` — optional

**Signature changes:**
- `held_queue.build_entry(row, hold_code, reason, outcome, observed_signals=None, company_known=None)`

**New test cases (in existing files — no new test file):**
- `operator-claude-plugin/tests/test_held_queue.py` — identity-key derivation, the `webhook_secret` refusal, the Grant-key acceptance, the cross-run `record_verb` invariant, the legacy-document `ANOMALOUS`/`legacy_reason` pair
- `operator-claude-plugin/tests/test_held_queue_facets.py` — the save → load → `stamped_domains` → `classify_facet` Jimmy composition
- `operator-claude-plugin/tests/test_suggestion_declines.py` — the Grant-key acceptance and the `webhook_secret` refusal
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/71-a-held-new-person-lands-in-hubspot-with-one-reply/71-01-SUMMARY.md` when done.
Record in it: the operator-approved serialised key shape (Task 1) verbatim, the RED observations
for each of the five RED-first tests, and the Open Question 4 scope line exactly as stated in
Task 3 (which stores are fixed, which are deliberately not, and the correction to CONTEXT.md's
"shared matcher" premise).
</output>
