---
phase: 59-frictionless-write-path
plan: 03
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
  - operator-claude-plugin/README.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
autonomous: true
requirements: [D-59-07]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-59-07: at grant-open time the operator reads one plain, non-blocking statement of fact — this grant enables enrichment and writes to HubSpot — instead of the long pre-emptive warning D-53-05 settled on."
    - "D-59-07: the same rendering tells the operator where the post-run list of what was actually written will be, so the retired warning is replaced by something actionable rather than by silence."
    - "The retirement is a RECORDED edit, not a deletion: the reason and the date 2026-08-28 are written into every file whose text changed, and into the test bodies re-pointed alongside them."
    - "D-59-06's already-true run-to-completion statement in the same paragraph survives — a revoke refuses the next send and does not stop a dispatch already running."
  artifacts:
    - "write_grant._consequence's two-lane branch, rewritten"
    - "operator-claude-plugin/skills/enrich-before-ingest/SKILL.md step-6/7 disclosure paragraph, rewritten"
    - "operator-claude-plugin/README.md two-lane grant bullet, rewritten"
  key_links:
    - "write_grant._consequence -> the consequence string the operator reads at the yes (the ONE rendering pinned by test_write_grant.py, per its own docstring)"
    - "the rewritten text -> written_records.written_records_path() from 59-01, named so the replacement is concrete rather than a promise"
    - "every rewritten passage -> its pinning test, re-pointed in the SAME commit with the reason in the test body"
---

<objective>
D-59-07 half (a): retire D-53-05's long pre-emptive two-lane disclosure as operator-facing
text, replace it with a plain non-blocking statement of fact plus a pointer to 59-01's post-run
written-records list, and re-point every test that pins the retired wording — in the same
commit, with the reason recorded in the files themselves.

Purpose: 53-04 described the retired sentence as the whole of what the operator got in exchange
for the ordering protection D-53-05 traded away — a warning nobody could act on until after the
fact anyway. D-59-07 replaces it with something that CAN be acted on: a concrete list of the
records that actually landed, which HubSpot lets the operator amend after the write. This plan
is the half that changes what the operator reads; 59-01 already built the half that produces
the list.

Output: rewritten text at four surfaces (grant consequence, skill, README, and the two test
files pinning them), with recorded-edit discipline applied at each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-frictionless-write-path/59-CONTEXT.md
@.planning/phases/59-frictionless-write-path/59-RESEARCH.md
@.planning/phases/59-frictionless-write-path/59-01-SUMMARY.md
@operator-claude-plugin/scripts/write_grant.py
</context>

<planner_assumptions>
1. **Four surfaces carry the retired text, and all four are enumerated below.** Research
   located them; this plan does not ask the executor to go hunting. If a FIFTH surface turns up
   (a `grep` for the retired phrasing across `operator-claude-plugin/` finds something not on
   the list), it is amended with the same discipline and reported in the SUMMARY — never left
   inconsistent with the other four.
2. **PLANNER DECISION — `write_grant.py`'s LANES module comment (lines 71-80) is HISTORY and is
   AMENDED, not rewritten.** D-59-07 retires the sentence *as operator-facing text*. That
   comment is not operator-facing: it is the code's own record of why a previous phase's
   protection was deliberately traded, and deleting it would erase the reasoning a future
   reader needs. It gets a dated amendment pointing at the replacement. This is why the
   negative check on the retired wording is scoped to the RENDERED consequence string (asserted
   by test) rather than to a whole-file grep of `write_grant.py`.
3. **PLANNER REVERSIBILITY RATING — `costly`, not `one-way`.** The rewritten consequence is a
   published operator-facing contract with pinning tests, so reverting means another recorded
   edit across four surfaces and a version bump. It is not one-way: no migration, no data
   format, nothing outside this repo depends on the wording. No `checkpoint:decision` is
   inserted. Flagged here per the reversibility gate.
4. **Spec-less probe fallback did not run** (no requirement IDs for this phase); `must_haves`
   are derived from D-59-07's own text. Assumption-delta scan returned `detected: false`.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the grant-time consequence, and re-point its pinning test in the same commit</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py lines 55-95 (the LANES map and the module comment recording D-53-05's traded protection) and lines 336-380 (`_consequence` in full, including the `if len(lane_names) > 1:` branch at 362-373)
    - operator-claude-plugin/tests/test_write_grant.py lines 1001-1045 — the three tests in the "at-the-yes disclosure (D-53-05)" block; read all three, not just the one that changes
    - operator-claude-plugin/scripts/written_records.py (as written by 59-01) — specifically `written_records_path()`, so the replacement text names a real path rather than a promise
    - .planning/phases/59-frictionless-write-path/59-CONTEXT.md § D-59-07 — the exact scope of what is retired and what replaces it
  </read_first>
  <behavior>
    - A two-lane grant proposal's `consequence` states plainly and non-blockingly that the grant enables enrichment and writes to HubSpot
    - The same `consequence` tells the operator that the records actually written will be listed after the run, and where
    - The two-lane `consequence` no longer carries the retired pre-emptive warning
    - Both lanes are still NAMED INDIVIDUALLY, and both workflow names still appear — unchanged
    - The full arm-dispatch register (what turns on, bounded to what, what turns it off, what happens if turning it off fails) is unchanged
    - A single-lane grant's `consequence` still claims no two-lane trade
  </behavior>
  <action>
Rewrite `_consequence`'s multi-lane branch in `operator-claude-plugin/scripts/write_grant.py`
(the `if len(lane_names) > 1:` block, currently lines 362-373) and re-point its pinning test in
`operator-claude-plugin/tests/test_write_grant.py` in the SAME commit.

WHAT THE NEW TEXT MUST SAY, and nothing more:
- That this grant enables enrichment and writes to HubSpot across both named lanes. One plain
  sentence, a statement of fact. Non-blocking — it informs, it does not warn, it does not ask,
  and it must not read as a caution the operator has to weigh.
- That after the run, the records actually written are listed, and where that list is
  (`written_records.written_records_path()`'s filename, `written_records.json`, in the plugin's
  durable state directory) — so the operator can open those records in HubSpot and amend them.
- Nothing else. Specifically, do NOT restate the retired ordering-trade argument in softer
  words. D-59-07 retires the ARGUMENT, not just its phrasing; a shorter version of the same
  warning is a quiet non-compliance, not a compression.

WHAT MUST NOT CHANGE in `_consequence`:
- The `per_lane` construction that names every lane individually. Its docstring's reason
  (collapsing two write surfaces into a collective phrase hides one behind the other) still
  holds and this plan does not touch it.
- The whole of the base `sentence` — the record/domain bound, the create inclusion/exclusion,
  the per-send open-and-close window, and the disarm-failure escalation. Those four are the
  arm-dispatch register `test_the_consequence_carries_the_arm_dispatch_register_in_full` pins,
  and they are outside D-59-07's scope.

RECORDED-EDIT DISCIPLINE, applied in two places in this file:
- At the rewritten branch, leave a dated comment: D-59-07, operator, 2026-08-28, stating that
  the pre-emptive warning D-53-05 installed here was retired as operator-facing text and what
  replaced it (the post-run written-records list), and that the retirement was a deliberate
  operator decision rather than a simplification.
- At the LANES module comment (lines 71-80), APPEND a dated amendment rather than editing the
  existing prose. That comment is the code's own history of why a previous phase's protection
  was traded and why; it stays readable. The amendment says the operator-facing half of it was
  retired on 2026-08-28 under D-59-07 and points at `_consequence` and at 59-01's artifact.
  Do not delete the historical paragraph.

RE-POINTING THE PINNING TEST — `test_write_grant.py`:
- `test_a_two_lane_grant_names_both_lanes_and_states_the_preview_trade` (line 1003) carries the
  assertion at line 1018 pinning the retired wording. Rename the test for what it now asserts
  (for example `test_a_two_lane_grant_names_both_lanes_and_points_at_the_written_records_list`),
  replace that one assertion with two: a positive assertion on the new plain statement, and a
  NEGATIVE assertion that the retired wording is gone from the consequence. Keep every other
  assertion in that test exactly as it is (both lane names, both workflow names). Put a
  RECORDED EDIT docstring on it in the style this file already uses for D-53-05 edits
  (`test_enrich_before_ingest_skill_contract.py:156-171` is the canonical example of the style):
  what the pin used to mean, that D-59-07 retired it on 2026-08-28 at the operator's decision,
  and what the pin holds now. The negative assertion is what makes this a re-point rather than
  a weakening — the retired sentence cannot come back unnoticed.
- `test_a_single_lane_grant_claims_no_preview_trade_that_is_not_happening` (line 1021) MUST
  stay green and MUST NOT be edited. Its line-1028 negative assertion becomes trivially true
  once the two-lane sentence no longer mentions the preview at all — that is correct, not a
  dead test, and it still guards a real property (a single-lane grant must not claim a two-lane
  trade). Add nothing to it. Note in the SUMMARY that it was deliberately left alone so a later
  reader does not read its now-easy pass as rot.
- `test_the_consequence_carries_the_arm_dispatch_register_in_full` (line 1031) must pass
  unmodified. If it fails, the base sentence was changed and that is out of scope — revert that
  part rather than adjusting the test.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_write_grant_surface.py operator-claude-plugin/tests/test_write_grant_guardrails.py -x</automated>
  </verify>
  <acceptance_criteria>
    - A test in `operator-claude-plugin/tests/test_write_grant.py` asserts `"BEFORE the enriched preview exists" not in consequence` for a two-lane proposal
    - The same test still asserts `"enrichment lane" in consequence`, `"contacts lane" in consequence`, and both `write_grant.LANES[...]` workflow names
    - The same test's docstring contains `D-59-07` and `2026-08-28`
    - `grep -c "written_records.json" operator-claude-plugin/scripts/write_grant.py` is at least 1
    - `grep -c "D-59-07" operator-claude-plugin/scripts/write_grant.py` is at least 2 (the rewritten branch and the LANES amendment)
    - `git diff` shows NO change inside the body of `test_a_single_lane_grant_claims_no_preview_trade_that_is_not_happening` or `test_the_consequence_carries_the_arm_dispatch_register_in_full`
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py -x` exits 0
  </acceptance_criteria>
  <done>The operator reads a plain non-blocking statement plus a pointer to the written-records list at the yes; the retired wording is pinned out by a negative assertion; the edit is dated and reasoned in the source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the same disclosure in SKILL.md and README, re-pointing the skill contract test with it</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py, operator-claude-plugin/README.md</files>
  <read_first>
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md lines 236-270 — the paragraph that changes and the two around it that do not
    - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py lines 1-60 (the module header and the constants it pins on) and lines 155-232 (`test_no_combined_or_third_arming_phrase_appears` and `test_the_ingest_arm_heading_is_strictly_after_the_enriched_preview_heading`, whose final assertion at line 226 is the one being re-pointed)
    - operator-claude-plugin/README.md lines 350-368 — the two-lane grant bullet
    - operator-claude-plugin/scripts/write_grant.py (as rewritten by Task 1) — the skill and the README must say the same thing the consequence says, in the same terms
  </read_first>
  <behavior>
    - SKILL.md's grant paragraph states plainly that a two-lane grant enables enrichment and writes to HubSpot, and that the records actually written are listed after the run
    - SKILL.md keeps its existing, still-true statement that revoking refuses the next send and does not stop a dispatch already running
    - The heading-ordering property (`ingest_offset > preview_offset`) still holds and is still asserted
    - README's two-lane bullet says the same thing as SKILL.md and the consequence
    - The retired wording appears in neither SKILL.md nor README
  </behavior>
  <action>
Rewrite the two-lane disclosure paragraph in
`operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` (currently lines 248-256) and the
matching bullet in `operator-claude-plugin/README.md` (currently around line 360), and
re-point `test_the_ingest_arm_heading_is_strictly_after_the_enriched_preview_heading`'s final
assertion in `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` (line
226) in the SAME commit.

SKILL.md — what the rewritten paragraph says:
- One plain, non-blocking sentence: a grant covering both lanes enables enrichment and writes
  to HubSpot, said at the yes.
- Then: after the run, the records actually written are listed (naming
  `written_records.json` in the plugin's durable state directory), so the operator can open
  them in HubSpot and amend them.
- KEEP, verbatim in substance, the paragraph's existing revocation sentences: revoking refuses
  the next send and does not stop a dispatch already running, so a revoke arriving mid-dispatch
  still lets every remaining chunk go out. That statement is true, is reinforced rather than
  retired by D-59-06, and 59-04 will echo it at session start. Do not remove it here.
- KEEP the "A grant removes the question, not the safety" paragraph at lines 258-261 and the
  F2 paragraph at 263-269 unchanged. They are not D-59-07's subject.
- Add a dated recorded-edit note in the file: D-59-07, operator, 2026-08-28 — the pre-emptive
  warning this paragraph carried was retired as operator-facing text and replaced by the
  post-run list; the trade D-53-05 made is unchanged, what the operator receives for it is not.
  Reference D-53-05 by id so the chain is followable.

README.md — rewrite the two-lane bullet to say the same thing in the same terms, with the same
dated note. The three bullets around it (the cost figure discloses rather than prevents, the
revocation-refuses-the-next-send bullet, and the grant-lives-in-the-conversation bullet) are
unchanged — the revocation bullet in particular is D-59-06's own statement and stays.

TEST RE-POINT — `test_enrich_before_ingest_skill_contract.py`:
- The final assertion of
  `test_the_ingest_arm_heading_is_strictly_after_the_enriched_preview_heading` (line 226) pins
  the retired wording in SKILL.md. Replace it with a positive assertion on the new plain
  statement plus a negative assertion that the retired wording is gone. Keep the two `find()`
  guards and the `ingest_offset > preview_offset` comparison exactly as they are — that
  ordering property is still true on the ungranted path and is not this plan's subject.
- Extend the test's existing RECORDED EDIT docstring (do not replace it — it already records
  D-53-05's edit, and a chain of two recorded edits is the point of the discipline) with a
  second dated paragraph: D-59-07, 2026-08-28, what was retired, what replaced it, and that the
  negative assertion exists so the retired sentence cannot return unnoticed.
- `test_no_combined_or_third_arming_phrase_appears` (line 155) must pass unmodified. Its
  assertion that the skill names both lanes in those words is untouched by this change.
- If any other test in this file fails after the SKILL.md edit, read its docstring before
  touching it — several pin properties this plan does not change, and adjusting one of those to
  accommodate a rewrite would be exactly the quiet weakening the recorded-edit discipline
  exists to prevent.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py operator-claude-plugin/tests/test_enrich_skill_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "authorized before the enriched preview exists" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is 0
    - `grep -c "authorized before the enriched preview exists" operator-claude-plugin/README.md` is 0
    - `grep -c "written_records.json" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1
    - `grep -c "written_records.json" operator-claude-plugin/README.md` is at least 1
    - `grep -c "D-59-07" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1
    - `grep -c "D-59-07" operator-claude-plugin/README.md` is at least 1
    - `grep -c "does not stop a dispatch already running" operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1 — the D-59-06 revocation statement survives
    - `test_the_ingest_arm_heading_is_strictly_after_the_enriched_preview_heading` still contains `ingest_offset > preview_offset` and its docstring contains both `D-53-05` and `D-59-07`
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -x` exits 0
  </acceptance_criteria>
  <done>All three operator-facing surfaces say the same plain thing and point at the same list; every pinning test is re-pointed rather than relaxed; both recorded edits are dated in-file.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep for a fifth surface, then bump the plugin version and CHANGELOG</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    - operator-claude-plugin/.claude-plugin/plugin.json (version will be 0.21.0 after 59-01)
    - operator-claude-plugin/CHANGELOG.md (top ~60 lines — match the existing entry format; note the existing entry around line 283 that documents D-53-05's original disclosure, which this release supersedes)
  </read_first>
  <action>
First, sweep `operator-claude-plugin/` for any surface still carrying the retired wording that
Tasks 1-2 did not reach, EXCLUDING `CHANGELOG.md` (a changelog records what a past release
said and is never rewritten) and excluding the historical LANES code comment Task 1
deliberately preserved. Run a grep for the retired phrasing across `skills/`, `scripts/`,
`README.md` and `USAGE.md`. Any hit outside those two exclusions is amended with the same
recorded-edit discipline Task 1 and Task 2 used, and named in the SUMMARY. `USAGE.md` line 60
is a known adjacent mention of the enriched preview that is NOT the retired sentence — read it
before deciding whether it needs to change; if it does not, say so in the SUMMARY rather than
leaving the reader to wonder whether it was checked.

Then set `version` in `operator-claude-plugin/.claude-plugin/plugin.json` to `0.22.0` and add a
matching CHANGELOG entry at the top, in the same commit as Tasks 1-2.

The entry names: D-59-07's retirement of the pre-emptive two-lane disclosure as operator-facing
text; the plain non-blocking statement plus written-records pointer that replaced it; the four
surfaces changed; that every pinning test was re-pointed with a negative assertion rather than
relaxed; and that the D-53-05 trade itself is UNCHANGED — one grant still spans both lanes, the
record set is still bounded, what moved is only what the operator is told in exchange. Do not
let the entry read as though a protection was removed; nothing about the grant's reach changed
in this release.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "authorized before the enriched preview exists" operator-claude-plugin/skills operator-claude-plugin/README.md operator-claude-plugin/USAGE.md` returns no matches
    - `grep '"version"' operator-claude-plugin/.claude-plugin/plugin.json` contains `0.22.0`
    - `grep -c "0.22.0" operator-claude-plugin/CHANGELOG.md` is at least 1
    - `grep -c "D-59-07" operator-claude-plugin/CHANGELOG.md` is at least 1
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
    - The SUMMARY states whether `USAGE.md` needed a change and why
  </acceptance_criteria>
  <done>No operator-facing surface still carries the retired wording; version 0.22.0 shipped with a CHANGELOG entry that does not overstate what changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → operator (the consequence string read at the yes) | This is the last place a consent decision can be informed; what it says or omits determines what the operator actually agreed to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-11 | Repudiation | a disclosure quietly weakened, leaving the operator's consent based on less than they think | high | mitigate | The retirement is a recorded edit at every surface (dated D-59-07 / 2026-08-28 in `write_grant.py`, `SKILL.md`, `README.md` and both re-pointed test docstrings), the historical LANES comment is preserved rather than deleted, and each re-pointed test gains a NEGATIVE assertion so the retired sentence cannot silently return or be silently replaced by a softer variant. |
| T-59-12 | Spoofing | the replacement text promising a written-records list that does not exist | high | mitigate | `depends_on: ["59-01"]` — the artifact ships first. Task 1's `read_first` requires reading `written_records.py`'s real `written_records_path()` so the text names the actual filename, and the acceptance criteria grep for `written_records.json` at all three surfaces. |
| T-59-13 | Tampering | the arm-dispatch register (bounds, disarm-failure escalation) altered while editing adjacent prose | high | mitigate | `test_the_consequence_carries_the_arm_dispatch_register_in_full` must pass UNMODIFIED, and the acceptance criteria assert `git diff` shows no change inside its body or inside the single-lane test's body. Any failure there is treated as out-of-scope drift to revert, never as a test to adjust. |
| T-59-14 | Information Disclosure | the consequence string echoing config values or credentials | low | accept | `_consequence` takes only lane names, id/domain counts and a boolean; it reads no config value. `ChunkResult`'s own docstring records the related concern (a relayed transport exception echoing request headers) for a different surface. No change here widens what the string can carry. |
| T-59-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan — it is prose and test edits only. `59-RESEARCH.md` § Package Legitimacy Audit records "not applicable — this phase installs no external packages". |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths this PLAN creates or renames (new — not pre-existing surface):

- a renamed test in `operator-claude-plugin/tests/test_write_grant.py`
  (`test_a_two_lane_grant_names_both_lanes_and_states_the_preview_trade` →
  a name describing the written-records pointer)
- new operator-facing text at three surfaces naming `written_records.json`
- dated recorded-edit notes carrying the token `D-59-07` in `write_grant.py`,
  `skills/enrich-before-ingest/SKILL.md`, `README.md`, and two test docstrings
- plugin version `0.22.0`

Nothing else in this plan is a new symbol; the rest is edits to existing text.
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — full plugin suite green
- `.venv/bin/python -m pytest -q` — full root suite green (untouched by this plan; a failure is
  pre-existing and must be reported, not absorbed)
- No n8n code touched, so `node --test tests/n8n/*.test.mjs` is not required by this plan
</verification>

<success_criteria>
- The two-lane grant consequence is a plain, non-blocking statement of fact plus a pointer to
  the post-run written-records list
- The retired wording is absent from every operator-facing surface and pinned out by a negative
  assertion at both test sites
- Both re-pointed tests carry a dated recorded-edit docstring; no pinning test was relaxed
- The single-lane test and the arm-dispatch-register test are byte-identical
- Plugin version 0.22.0 with a CHANGELOG entry that does not overstate the change
</success_criteria>

<output>
Create `.planning/phases/59-frictionless-write-path/59-03-SUMMARY.md` when done
</output>
