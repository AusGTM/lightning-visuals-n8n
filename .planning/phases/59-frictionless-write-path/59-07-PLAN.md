---
phase: 59-frictionless-write-path
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: []
files_modified:
  - operator-claude-plugin/scripts/chunking.py
  - operator-claude-plugin/tests/test_chunking.py
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GAP 1 (D-59-08): a people/companies spec that fails GATE-02..GATE-05 is driven through the real integration path — plan_chunks then dispatch_plan — and the operator-visible ChunkResult carries the gate's own specific message, not a generic placeholder."
    - "GAP 1 (D-59-08): that same ChunkResult carries the gate's structured resolvable tuple, so the skill relaying outcome.results can render the add-X-Y-or-Z guidance the gate built."
    - "D-59-08: refuse-to-propose, never refuse-to-guess — nothing in this plan resolves a value; it only stops discarding a proposal the gate already built."
    - "59-GATE-INVENTORY.md's GATE-02..GATE-05 Owner cells and its closing CLOSED paragraph state what was actually delivered, including the integration point this plan repairs."
  artifacts:
    - "operator-claude-plugin/scripts/chunking.py — ChunkResult gains a resolvable field; the RecordSpecError handler binds the exception."
    - "operator-claude-plugin/tests/test_chunking.py — an integration test driving plan_chunks -> dispatch_plan for a GATE-02 spec."
    - "operator-claude-plugin/skills/enrich-records/SKILL.md — step 9 relays resolvable."
    - "operator-claude-plugin/skills/enrich-before-ingest/SKILL.md — its dispatch section relays resolvable."
  key_links:
    - "enrichment.RecordSpecError.resolvable -> ChunkResult.resolvable -> outcome.results -> the two SKILL.md relay steps. This is the one severed link 59-06 built the payload for."
    - "GATE-01 (extraction.py -> preview -> contact-upload/SKILL.md) and GATE-06 (enrich-records step 7 -> plan_grant) are UNTOUCHED — both already reach the operator on independent paths."
---

<objective>
Close gap 1 of `59-VERIFICATION.md`: GATE-02 through GATE-05 build a resolve-and-propose
payload that never reaches the operator, because the one call site
`enrichment.build_envelope` is invoked from in shipped code throws it away.

Purpose: D-59-08 is an operator ruling that refusal becomes the last resort. 59-06 built the
payload and unit-tested it against `build_envelope` directly. The dispatch path both shipped
skills document discards it, so on the lane the ruling's own origin scenario sits on, the
operator still hits a dead end.

Output: `ChunkResult` carries the gate's real message and its `resolvable` tuple; both skills
relay it; an integration test drives the whole path; the gate inventory stops claiming more
than was delivered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-frictionless-write-path/59-VERIFICATION.md
@.planning/phases/59-frictionless-write-path/59-REVIEW.md
@.planning/phases/59-frictionless-write-path/59-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry the gate's own refusal and its resolvable tuple through dispatch_plan to outcome.results</name>

  <read_first>
    - `operator-claude-plugin/scripts/chunking.py` — the whole file. Specifically: the
      `ChunkResult` dataclass and its docstring (the T-25-17 sentence about carrying nothing
      from the config), and `dispatch_plan`'s per-chunk `except` clauses.
    - `operator-claude-plugin/scripts/enrichment.py` — `RecordSpecError.__init__` and its
      docstring (the closed-vocabulary validation at construction), plus the four raise sites
      that pass `resolvable=`.
    - `operator-claude-plugin/scripts/resolution_sources.py` — the closed vocabulary. Do not
      add a source to it; this task carries what is already validated.
    - `operator-claude-plugin/tests/test_chunking.py` — the existing fixture and transport-stub
      conventions this test must follow rather than invent.
    - `operator-claude-plugin/tests/test_enrichment_envelope.py` around the `.resolvable`
      tests — they prove what `build_envelope` produces; this task proves it survives the trip.
  </read_first>

  <behavior>
    - Test 1 (the integration test that would have caught this gap): build a people spec whose
      single row is a named person with no email, no `linkedin_url` and no lastname+company —
      GATE-02's own example — via `chunking.plan_chunks`, then run
      `chunking.dispatch_plan` over it with a stub transport. Assert the resulting
      `outcome.results[0].reason` is the gate's own message (assert on a distinctive
      substring of it, e.g. `"LinkedIn profile URL"`), and assert
      `outcome.results[0].resolvable` is a non-empty tuple whose every entry has `field`,
      `sources` and `detail` keys.
    - Test 2: assert the stub transport was never called for that chunk — the refusal happens
      before any send, and this test must not accidentally start proving a network round trip.
    - Test 3: a chunk that fails for a transport reason (not a spec reason) still carries
      `resolvable == ()`, so a caller can iterate the field unconditionally on every result.
    - Test 4: `outcome.failed_batch` still carries the refused chunk, unchanged — D-13's
      re-send contract is not altered by this plan.
  </behavior>

  <files>operator-claude-plugin/scripts/chunking.py, operator-claude-plugin/tests/test_chunking.py</files>

  <action>
    Add a `resolvable` field to the `ChunkResult` dataclass, defaulting to an empty tuple —
    never `None` — mirroring `enrichment.RecordSpecError.resolvable` exactly so a caller
    iterates it unconditionally on every result including successes.

    Bind the exception in `dispatch_plan`'s `enrichment.RecordSpecError` handler (`as e`) and
    build that chunk's `ChunkResult` from `str(e)` and `getattr(e, "resolvable", ())` instead
    of the fixed placeholder string presently at `chunking.py:314`. Delete that placeholder;
    do not keep it as a fallback for an empty `resolvable`, because a `RecordSpecError` always
    carries a message and a message the gate wrote is strictly better than one this module
    invents. Leave `failed_chunks.append(chunk)` and the `continue` exactly as they are.

    Resolve the documented tension this creates, in the code, rather than leaving the next
    reader to trip over it: `ChunkResult`'s docstring records that it deliberately carries
    nothing from the config because a relayed transport exception's text can echo request
    headers (T-25-17). Threading a `RecordSpecError` message onto it is not a breach of that
    rule and the docstring must say why — a `RecordSpecError` is raised by `build_envelope`
    BEFORE any request is built, and every one of the four GATE-02..GATE-05 messages is
    composed from the operator's own record spec, never from transport, response or config
    values. Amend the docstring to state that distinction: transport-sourced text stays
    excluded, spec-sourced refusal text is admitted, and name T-25-17 so the original
    constraint is visibly honoured rather than silently overridden. Keep the `DispatchError`
    handler's fixed reason string as it is — that one IS transport-sourced.

    Write the four behaviors above into `test_chunking.py`, following that file's existing
    fixture and stub-transport conventions. The point of Test 1 is that it goes through
    `plan_chunks` and `dispatch_plan` rather than calling `build_envelope` directly — a test
    that calls `build_envelope` repeats the exact blind spot that let this ship.

    Do not touch `enrichment.py`, `extraction.py`, `resolution_sources.py`, `preview.py` or
    `write_grant.py` in this task. GATE-01 and GATE-06 already reach the operator on
    independent paths and are out of scope.
  </action>

  <acceptance_criteria>
    - `grep -c 'this chunk could not be turned into a request' operator-claude-plugin/scripts/chunking.py` returns 0.
    - `grep -n 'except enrichment.RecordSpecError as e:' operator-claude-plugin/scripts/chunking.py` returns exactly one line.
    - `grep -n 'resolvable' operator-claude-plugin/scripts/chunking.py` shows the field declared on `ChunkResult` with an empty-tuple default and referenced in the handler.
    - `grep -n 'T-25-17' operator-claude-plugin/scripts/chunking.py` still matches — the original constraint is amended in place, not deleted.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py -q` passes, and the four new tests are present by name in the run.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` reports at least 1678 passed (the pre-existing baseline) plus the new tests, 0 failed.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/lightning-visuals/lv-n8n-poc && .venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py -q</automated>
  </verify>

  <done>A GATE-02 spec driven through `plan_chunks` -> `dispatch_plan` yields an `outcome.results[0]` carrying the gate's own message and a non-empty `resolvable` tuple, proven by a test that uses the integration path and not `build_envelope`.</done>
</task>

<task type="auto">
  <name>Task 2: Both skills relay the resolvable payload, and release the plugin at 0.26.0</name>

  <read_first>
    - `operator-claude-plugin/skills/enrich-records/SKILL.md` — step 9, the reporting step
      that instructs relaying a failed chunk's reason as recorded.
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` — its dispatch section,
      the paragraph following the `dispatch_plan` snippet that says a failing chunk is skipped
      and the rest continue.
    - `operator-claude-plugin/tests/test_enrich_skill_contract.py` and
      `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` — these pin
      SKILL.md text; find every assertion covering the paragraphs you are about to edit before
      editing them.
    - `operator-claude-plugin/CHANGELOG.md` — the release checklist at the bottom, and the
      0.25.0 entry for the entry shape to follow.
    - `operator-claude-plugin/.claude-plugin/plugin.json` — currently 0.25.0.
    - `.planning/phases/59-frictionless-write-path/59-CONTEXT.md` § D-59-08, for the
      resolve-then-propose vocabulary the relay wording must match.
  </read_first>

  <files>operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_enrich_skill_contract.py, operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>

  <action>
    In `enrich-records/SKILL.md` step 9, extend the failed-chunk reporting instruction: when a
    failed chunk's `resolvable` tuple is non-empty, relay each entry's `detail` to the
    operator as a proposal of what would resolve the row, name which of the four
    `resolution_sources` vocabulary values each entry claims, and offer the resolution rather
    than reporting the row as refused. State explicitly that Claude proposes and the operator
    confirms — a resolvable entry is never silently acted on, and no value is invented to
    satisfy the gate (D-59-08's own line: the change is refuse-to-propose, never
    refuse-to-guess).

    Make the equivalent edit in `enrich-before-ingest/SKILL.md`'s dispatch section, in that
    file's own voice, so the two lanes do not diverge.

    Cite D-59-08 and the gap-closure date in both edits. Where either edit supersedes existing
    wording, use the recorded-edit discipline already used across this phase's surfaces —
    dated, reason stated, never a silent deletion — and PARAPHRASE the superseded phrasing in
    the note rather than reproducing it verbatim, so the note does not trip a pin written
    against that phrasing.

    Update any assertion in the two skill-contract test files that pins the paragraphs you
    changed, in this same commit, with the reason in the test body.

    Bump `.claude-plugin/plugin.json` to `0.26.0` and add the matching `CHANGELOG.md` entry
    describing the severed payload and its repair.

    Fold Task 1's work into THIS commit. Task 1 touches `operator-claude-plugin/` files, so a
    standalone commit for it would carry no bump and no CHANGELOG entry, violating the
    per-commit release rule. Either leave Task 1's changes staged and uncommitted until this
    task, or fold its commit in with `git commit --amend` on the immediately preceding commit
    (equivalently `git reset --soft HEAD~1` then recommit). Do NOT reach for interactive rebase
    — `git rebase -i` is unavailable in this environment. Task 1, Task 2, the bump and the
    CHANGELOG land as ONE commit; Task 3's planning-document edit stays separate.
  </action>

  <acceptance_criteria>
    - `grep -c resolvable operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1.
    - `grep -c resolvable operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` is at least 1.
    - `grep -c '"version": "0.26.0"' operator-claude-plugin/.claude-plugin/plugin.json` returns 1.
    - `grep -c '0.26.0' operator-claude-plugin/CHANGELOG.md` is at least 1.
    - `git show --stat HEAD` for this task's commit lists `plugin.json` and `CHANGELOG.md` together with the skill files.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` passes with 0 failures.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/lightning-visuals/lv-n8n-poc && .venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>

  <done>Both skills instruct Claude to relay the resolvable proposal for a refused chunk; the plugin is released at 0.26.0 with the CHANGELOG entry in the same commit.</done>
</task>

<task type="auto">
  <name>Task 3: Correct 59-GATE-INVENTORY.md's GATE-02..GATE-05 delivery claims</name>

  <read_first>
    - `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` — the whole file. Two
      places overstate delivery: the Owner cells of the GATE-02, GATE-03, GATE-04 and GATE-05
      rows, and the closing `## Unplanned items` paragraph asserting the inventory is closed
      and every CONVERT gate converted.
    - `.planning/phases/59-frictionless-write-path/59-VERIFICATION.md` — the gap-1 entry, for
      the exact claim being corrected.
  </read_first>

  <files>.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md</files>

  <action>
    Amend the four Owner cells so each states that 59-06 built the payload at the
    `RecordSpecError` construction site and this plan (59-07) carried it through
    `chunking.dispatch_plan` to `outcome.results` and into both skills' relay steps — a gate
    is CONVERTED only when the payload reaches the operator, and until this plan it did not.

    Amend the closing paragraph the same way: it may state the inventory is closed only on the
    strength of the delivered integration path, and it must name that GATE-02..GATE-05's
    delivery was completed in gap closure rather than in 59-06 alone.

    Keep GATE-01's and GATE-06's rows exactly as they are — both were verified as genuinely
    reaching the operator on independent paths.

    This is a planning document, not plugin code: commit it SEPARATELY from Task 2, with no
    version bump and no CHANGELOG entry.
  </action>

  <acceptance_criteria>
    - `grep -c '59-07' .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` is at least 5 (four Owner cells plus the closing paragraph).
    - `grep -n 'GATE-01' .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` shows its row's Owner cell unchanged from HEAD~ (`git diff` on this file shows no GATE-01 or GATE-06 row edit).
    - This task's commit touches only `59-GATE-INVENTORY.md`; `git show --stat` lists no file under `operator-claude-plugin/`.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/lightning-visuals/lv-n8n-poc && grep -c '59-07' .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md</automated>
  </verify>

  <done>The inventory's CONVERTED claim for GATE-02..GATE-05 matches what is now shipped, and GATE-01/GATE-06 are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n backend -> `dispatch_plan` | Untrusted response text crosses here; already handled by the fixed `DispatchError` reason string, which this plan does not change. |
| operator's record spec -> `build_envelope` -> `RecordSpecError` message -> operator-visible `outcome.results` | The new path this plan opens. Text originates in the operator's own input, not the network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-07-01 | Information disclosure | `ChunkResult.reason` now carrying `str(e)` | medium | mitigate | Only `RecordSpecError` is bound, and it is raised before a request exists — its message cannot contain a header, a webhook secret or a config value. `DispatchError`'s fixed string is left alone; the docstring records the distinction so a later widening to transport exceptions is a visible decision. |
| T-59-07-02 | Tampering | `resolvable` sources laundered past the closed vocabulary | high | mitigate | Nothing in this plan constructs a `resolvable` entry; `RecordSpecError.__init__` validates every `sources` member against `resolution_sources.RESOLUTION_SOURCES` at construction and this plan only reads the already-validated tuple. |
| T-59-07-03 | Elevation of privilege | A skill acting on a proposal without the operator | high | mitigate | Both SKILL.md edits state the operator confirms; no code path applies a resolvable entry. |
| T-59-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — no legitimacy audit required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — root suite, baseline 3285 passed / 154 skipped, 0 failed.
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — baseline 1678 passed / 5 skipped plus the new tests, 0 failed.
- `node --test tests/n8n/*.test.mjs` — 776 pass / 0 fail, untouched by this plan.
</verification>

<success_criteria>
A people spec that fails GATE-02, driven through `plan_chunks` -> `dispatch_plan`, produces an
operator-visible result carrying the gate's own message and its `resolvable` tuple; both skills
instruct relaying it as a proposal; the gate inventory's delivery claim matches reality; the
plugin is at 0.26.0 with its CHANGELOG entry in the same commit.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / path | New or changed |
|---|---|
| `chunking.ChunkResult.resolvable` | NEW field, default `()` |
| `chunking.dispatch_plan`'s `except enrichment.RecordSpecError as e:` | CHANGED — binds and propagates |
| `operator-claude-plugin/tests/test_chunking.py` | CHANGED — 4 new integration tests |
| `operator-claude-plugin/skills/enrich-records/SKILL.md` step 9 | CHANGED |
| `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` dispatch section | CHANGED |
| `operator-claude-plugin/.claude-plugin/plugin.json` | 0.25.0 -> 0.26.0 |
| `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` | CHANGED — 4 Owner cells + closing paragraph |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/59-frictionless-write-path/59-07-SUMMARY.md` when done.
</output>
