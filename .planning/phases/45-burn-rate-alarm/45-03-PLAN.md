---
phase: 45-burn-rate-alarm
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - tests/test_execution_budget_drift.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ALARM-03, LOOK-01, FLOOR-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A repo-side test fails when the plugin's example config allowance stops equalling `config/execution_budget.yaml`'s `monthly_execution_allowance` — the mechanical half of ALARM-03's one-logical-allowance rule (D-04)."
    - "The same test fails when the plugin's schedule-floor share stops equalling that file's `idle_floor_max_share`, so the runtime floor and CAP-03's build-time guard cannot judge against different ceilings."
    - "The same test fails when `n8n_cadence.TICKS_PER_MONTH` stops agreeing with `tests/test_execution_budget.py`'s table on their shared keys — the 30-day month is not computed a third way."
    - "Every key the drift test reads is read by DIRECT INDEXING on both sides: a missing key fails the test rather than defaulting to a value that would make the comparison vacuously pass."
    - "The plugin version is 0.13.0 and the CHANGELOG entry naming the three new config keys is in the same commit as that bump."
    - "The CHANGELOG states plainly that an existing operator config lacks the new keys, so the first sweep after upgrading fires `burn_rate_not_configured` by design until an admin adds them."
    - "All six phase requirement IDs (ALARM-01..04, LOOK-01, FLOOR-01) carry a Complete status with a concrete test or artifact pointer in REQUIREMENTS.md's traceability table."
    - "All three suites are green at phase close: repo pytest, plugin pytest, and the node suite."
  artifacts:
    - tests/test_execution_budget_drift.py
    - operator-claude-plugin/.claude-plugin/plugin.json
    - operator-claude-plugin/CHANGELOG.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "operator.local.example.json -> config/execution_budget.yaml: the drift test is the ONLY thing connecting them. 45-CONTEXT.md D-04 deliberately refused a runtime filesystem dependency, so without this test the two numbers can silently disagree and the alarm quietly watches the wrong ceiling."
    - "plugin.json version bump -> marketplace clone refresh: project memory records that a same-version reinstall deletes operator.local.json and that the marketplace clone does not refresh on reinstall. The bump is what makes the release installable at all."
  prohibitions:
    - statement: "No key in the drift test is read with a defaulting accessor; a missing key must fail the test rather than pass it vacuously."
      status: flagged-unverified
    - statement: "No real secret value is introduced into the committed example config alongside the new numeric keys."
      status: flagged-unverified
    - statement: "The plugin version is never shipped unbumped alongside a CHANGELOG entry."
      status: flagged-unverified
---

<objective>
Pin the two numbers that can silently drift, ship the release, and close the phase's
traceability.

Purpose: 45-CONTEXT.md D-04 chose a plugin config key over a runtime read of the backend
repo, precisely so the unattended sweep gains no filesystem dependency on a checkout that
could move. The cost of that choice is two copies of the same number. This plan pays that
cost the way D-04 said it would be paid: a repo-side test that fails when they disagree.
It also ships the plugin (a plugin change without a version bump is not installable —
project memory records the greyed-out Update button and the stale marketplace clone) and
marks the six requirement rows this phase closes.

Output: `tests/test_execution_budget_drift.py`, plugin version 0.13.0 with its CHANGELOG
entry, and a REQUIREMENTS.md traceability table with no Pending rows for Phase 45.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-burn-rate-alarm/45-CONTEXT.md
@.planning/phases/45-burn-rate-alarm/45-01-SUMMARY.md
@.planning/phases/45-burn-rate-alarm/45-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan (new):

**`tests/test_execution_budget_drift.py`** — new test module. Contains
`test_plugin_example_allowance_matches_the_budget_file`,
`test_plugin_example_floor_share_matches_the_budget_file`, and
`test_cadence_ticks_per_month_agrees_with_the_budget_guard` (exact names at the
executor's discretion; the three assertions are not).

**`operator-claude-plugin/.claude-plugin/plugin.json`** — `version` moves 0.12.0 to
0.13.0.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The drift test — two numbers, one source of truth, one place they can disagree</name>
  <files>tests/test_execution_budget_drift.py</files>
  <read_first>
    - tests/test_execution_budget.py (the whole file — its header comment stating the re-derive-never-import philosophy, its `ROOT` resolution, its direct-indexing comment at the allowance read, and its `TICKS_PER_MONTH` table which this new test pins `n8n_cadence`'s against)
    - config/execution_budget.yaml (the whole file — `monthly_execution_allowance` 2500, `idle_floor_max_share` 0.25, and the comment block already naming Phase 45 ALARM-03 as a reader)
    - operator-claude-plugin/config/operator.local.example.json (as left by 45-01 — the three new keys and their `_..._note` siblings)
    - operator-claude-plugin/scripts/n8n_cadence.py (as left by 45-02 — `TICKS_PER_MONTH` and how it derives from `n8n_read`'s constants)
    - operator-claude-plugin/tests/conftest.py (lines 1-20 — WHY plugin modules import as flat names and why a `scripts` package import from the repo root collides with the repo's own `scripts/`; the sys.path insert in this new test must do the same thing deliberately)
    - operator-claude-plugin/tests/test_no_backend_imports.py (the import boundary that is guarded — plugin-to-backend. Backend-to-plugin, which this test performs, is the unguarded direction and has precedent in scripts/june_run_arm.py)
  </read_first>
  <behavior>
    - With the committed files as they stand, all three assertions pass.
    - Editing the example config's allowance to 2000 fails the first test with a message naming both files.
    - Deleting `monthly_execution_allowance` from the YAML raises a KeyError rather than passing.
    - Deleting `n8n_monthly_execution_allowance` from the example JSON raises a KeyError rather than passing.
    - The comparison treats 2500 and 2500.0 as equal but "2500" (a string) as a failure.
  </behavior>
  <action>
Create `tests/test_execution_budget_drift.py`, modelled directly on
`tests/test_execution_budget.py` — same `ROOT` resolution, same module-docstring-states-
the-incident style, same direct-indexing discipline.

Assertion one: load `config/execution_budget.yaml` with `yaml.safe_load` and
`operator-claude-plugin/config/operator.local.example.json` with `json.loads`. DIRECT-INDEX
`monthly_execution_allowance` on the first and `n8n_monthly_execution_allowance` on the
second — a missing key must raise, not default, mirroring the T-44-07 comment already in
the sibling test. Assert both values are numeric (an `int` or `float`, explicitly not a
`str`, because a quoted number in JSON would compare unequal in a confusing way rather
than an obvious one), then assert they are equal by value. The failure message names both
file paths, both key names, and both values, and says which file is the source of truth
(the YAML) and which mirrors it (the plugin example).

Assertion two: the same comparison for `idle_floor_max_share` against
`n8n_schedule_floor_max_share`. Its failure message must additionally say WHY they must
match: the runtime cadence floor (45-02) judges a schedule against the plugin's share
while `tests/test_execution_budget.py` judges the committed schedule against the YAML's,
so a divergence means the plugin permits a cadence the build then rejects, or refuses one
the build allows.

Assertion three: insert `operator-claude-plugin/scripts` at the front of `sys.path`
(guarded so a repeated insert is harmless) and import `n8n_cadence`. Import
`tests.test_execution_budget`'s `TICKS_PER_MONTH` — `tests/` carries an `__init__.py`, so
the package import resolves from the repo root without a path hack. Assert that for every
key present in BOTH tables the values are equal. Do not require the key sets to be
identical: `n8n_cadence` deliberately carries a `seconds` row the build-time guard has no
need of, and requiring identity would fail on a difference that is correct. State that in
a comment so a later reader does not "fix" it into an identity assertion. The failure
message says the 30-day month has been computed a third way and names the disagreeing
key.

Add a non-vacuity assertion in the third test, mirroring the sibling test's own
non-vacuity guard: the shared key set must be non-empty, so a rename on either side
cannot make the comparison pass by comparing nothing.

Note in the module docstring that the plugin import is the backend-to-plugin direction,
which `test_no_backend_imports.py` does not guard and `scripts/june_run_arm.py` already
takes, and that `n8n_cadence` performs no I/O at import time (its transitive imports are
`copy`, `re`, `json`, `requests`, `n8n_control`, `n8n_read` — all inert on import).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_execution_budget_drift.py tests/test_execution_budget.py -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_execution_budget_drift.py -q` passes with exactly 3 tests.
    - Temporarily changing the example config's `n8n_monthly_execution_allowance` to 2000 makes test one fail, and its failure message contains both `config/execution_budget.yaml` and `operator.local.example.json`. Revert the edit after checking.
    - Temporarily changing `n8n_schedule_floor_max_share` to 0.5 makes test two fail. Revert after checking.
    - `grep -n "\.get(" tests/test_execution_budget_drift.py` returns no line that reads one of the four pinned budget keys — every one is read by direct indexing.
    - `.venv/bin/python -m pytest -q` reports 2562 collected (the 2559 baseline plus these 3) with 0 failures.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` still reports 0 failures — importing a plugin module from the repo suite did not disturb the plugin suite.
  </acceptance_criteria>
  <done>The two numbers D-04 deliberately duplicated cannot drift unnoticed, and the 30-day month is not computed a third way.</done>
</task>

<task type="auto">
  <name>Task 2: Ship it — version 0.13.0 and a CHANGELOG entry that names the admin's upgrade step</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    - operator-claude-plugin/.claude-plugin/plugin.json (the current `version` 0.12.0)
    - operator-claude-plugin/CHANGELOG.md (lines 1-80 — the header's rule that releasing means bumping plugin.json's version in the SAME change, and the format of the most recent released section, which the new section must match exactly)
    - operator-claude-plugin/tests/test_plugin_manifest.py (whatever it asserts about the manifest — the bump must not break it)
    - .planning/phases/45-burn-rate-alarm/45-01-SUMMARY.md and 45-02-SUMMARY.md (what actually shipped, so the entry describes the delivered behaviour rather than the plan's intent)
    - operator-claude-plugin/config/operator.local.example.json (as left by 45-01 — the three new key names the entry must list verbatim)
  </read_first>
  <action>
Bump `operator-claude-plugin/.claude-plugin/plugin.json`'s `version` from `0.12.0` to
`0.13.0` — a minor bump, because this release adds operator-visible behaviour (a new
sweep condition and a new refusal on an existing action) without removing or renaming any
existing surface.

Add a CHANGELOG section for 0.13.0 in the file's existing format, covering: the burn-rate
alarm and its three condition outcomes; the time-windowed execution lookback replacing the
fixed 100-row page, including that an already-fixed failure now ages out and an in-flight
run does not; the workflow-name resolution in notices; and the cadence budget floor with
its single-shot override phrase.

The entry MUST carry an explicit upgrade step, stated as an action an admin has to take
rather than buried in a feature description: an existing `operator.local.json` predates
these keys and therefore lacks them, so the FIRST sweep after upgrading will fire
`burn_rate_not_configured` — by design, not as a bug — until
`n8n_monthly_execution_allowance` and `n8n_schedule_floor_max_share` are added, and the
cadence action will refuse every schedule change until then for the same reason. Name all
three new keys verbatim, give the values they must currently carry (2500, 0.25, 1.0), and
say that the first two must match `config/execution_budget.yaml` in the backend repo,
which `tests/test_execution_budget_drift.py` now enforces.

Say plainly, in the same entry, that the alarm ships INERT: no cron or launchd schedule is
installed by this release, so the sweep only fires when it is invoked. Scheduling it is an
admin action on the operator's machine. This is the roadmap's stated accepted limit and it
must not be discovered later as a surprise.
  </action>
  <verify>
    <automated>python3 -c "import json;print(json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version'])"</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The first verify command prints `0.13.0`.
    - `grep -c "0.13.0" operator-claude-plugin/CHANGELOG.md` returns at least 1.
    - `grep -c "n8n_monthly_execution_allowance" operator-claude-plugin/CHANGELOG.md`, the same for `n8n_schedule_floor_max_share`, and the same for `burn_rate_alarm_threshold` each return at least 1.
    - `grep -c "burn_rate_not_configured" operator-claude-plugin/CHANGELOG.md` returns at least 1 — the by-design first-sweep notice is stated, not left to be discovered.
    - The 0.13.0 section states that no cron or launchd schedule is installed by this release — verified by reading it.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -q` passes.
  </acceptance_criteria>
  <done>The plugin is installable at a new version, and an admin reading the changelog knows exactly what to add to their config and what they will see if they do not.</done>
</task>

<task type="auto">
  <name>Task 3: Close the six requirement rows against real pointers, on three green suites</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Burn-Rate Alarm and Cadence Budget Floor sections and the traceability table, as amended during Phase 45 planning — six rows currently Pending)
    - .planning/phases/45-burn-rate-alarm/45-01-SUMMARY.md and 45-02-SUMMARY.md (the actual test names and file paths that become the pointers)
    - .planning/ROADMAP.md (Phase 45's six success criteria, which the closing statuses must actually satisfy)
    - .planning/phases/45-burn-rate-alarm/45-CONTEXT.md (the roadmap's ships-inert note — a requirement closed by unit test against synthetic history is closed honestly here, and the status text should say so rather than imply a live scheduled fire)
  </read_first>
  <action>
Run all three suites first — the pointers must reference tests that actually pass right
now, not tests that passed when their own plan closed.

Then mark ALARM-01, ALARM-02, ALARM-03, ALARM-04, LOOK-01 and FLOOR-01 Complete in
REQUIREMENTS.md's traceability table, each with a CONCRETE pointer in the format the
Phase 44 rows already use — a test file plus the specific test name, or a file path plus
what it asserts. Tick the corresponding checkbox on each requirement's own bullet in the
body.

Where a requirement is closed by unit test against synthetic execution history rather
than by observed live behaviour, say so IN the status text. Phase 45 ships inert by the
roadmap's own note: no cron is installed, so no requirement here can claim a live
scheduled fire, and a status that reads as though one occurred would be the first false
claim in a phase whose entire subject is not making false claims. The honest formulation
is a Complete status naming the test and the fixture shape it exercises.

Update the coverage line at the foot of the table if the totals moved.

Do not mark anything Complete whose test does not currently pass. If a suite is red, stop
and report rather than closing the row.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <automated>grep -c "Phase 45 | Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` reports 0 failures (2562 collected: the 2559 baseline plus Task 1's 3).
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` reports 0 failures and a collected count at or above 1291.
    - `node --test tests/n8n/*.test.mjs` reports 656 passing, 0 failing.
    - The fourth verify command prints `0` — no Phase 45 row is still Pending.
    - Each of the six rows' Status cell contains a `.py` path, and for the ALARM rows the text states that closure is by test against synthetic execution history rather than a live scheduled fire.
    - `grep -c "\[ \] \*\*ALARM-" .planning/REQUIREMENTS.md` returns `0` and the same for `FLOOR-` and `LOOK-` — every body checkbox is ticked.
  </acceptance_criteria>
  <done>Six requirement rows closed against passing tests, with the ships-inert limit stated rather than papered over, on three green suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo YAML + plugin JSON → test assertions | Two committed configuration artifacts cross into an equality check that is the only thing preventing a silent budget divergence. |
| plugin scripts directory → repo test process | A repo-side test inserts a plugin path on `sys.path` and imports a plugin module. |
| committed example config → git history | The example config is public in the repository and gains new keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-13 | Tampering | drift test key reads | high | mitigate | Every pinned key is read by direct indexing on both sides, so a rename or deletion raises KeyError instead of defaulting into a vacuously passing comparison; the third test additionally asserts the shared key set is non-empty. |
| T-45-14 | Information Disclosure | new keys in the committed example config | medium | mitigate | The two new numeric keys carry a plan size and a share — no secret. The file's existing convention (placeholder strings, never real values, for `webhook_secret` and `n8n_api_key`) is untouched, and the drift test reads only the numeric keys. |
| T-45-15 | Tampering | `sys.path` insert + plugin import in a repo test | medium | mitigate | The insert names exactly `operator-claude-plugin/scripts` resolved from `ROOT`, and the imported module's full transitive import set (`copy`, `re`, `json`, `requests`, `n8n_control`, `n8n_read`) performs no I/O at import time. The plugin suite is re-run as an acceptance criterion to prove no cross-contamination. |
| T-45-16 | Repudiation | shipping an unbumped plugin | medium | mitigate | The version bump and the CHANGELOG entry land in the same task and the same commit; project memory records that a same-version reinstall deletes `operator.local.json` and that the marketplace clone does not refresh without a bump. |
| T-45-17 | Spoofing | a requirement marked Complete against a test that does not run | high | mitigate | Task 3 runs all three suites BEFORE editing any status, and its acceptance criteria assert 0 failures on each; a status may not be written for a red test. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. `yaml` and `pytest` are already present and already used by the sibling budget test. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — repo suite green at 2562 collected.
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — plugin suite green.
- `node --test tests/n8n/*.test.mjs` — 656 passing.
- `python3 -c "import json;print(json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version'])"` prints `0.13.0`.
</verification>

<success_criteria>
- The plugin's allowance and floor share cannot diverge from `config/execution_budget.yaml` without a test failing.
- `n8n_cadence.TICKS_PER_MONTH` cannot diverge from the build-time budget guard's table.
- The plugin ships at 0.13.0 with a changelog entry that names the admin's upgrade step and the ships-inert limit.
- No Phase 45 requirement row is Pending, and every Complete row points at a test that passes.
</success_criteria>

<output>
Create `.planning/phases/45-burn-rate-alarm/45-03-SUMMARY.md` when done.
</output>
