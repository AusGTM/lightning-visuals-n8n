---
phase: 73-ga-fix-list-from-stress-attempt-2
plan: 07
type: execute
wave: 7
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05", "73-06"]
autonomous: false
files_modified:
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - tests/stress-tests/RUNBOOK.md
  - .planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md
requirements: [F-A6, F-A5, F-E1, F-B7, F-B3, F-A3r]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-73-18: ONE deploy — every changed cloud JSON is regenerated once, then the OPERATOR deploys and bounces disarmed, resets the portal, and runs attempt 3 Stages A to F. No staged ingest-first deploy, and nothing is armed, deployed or bounced from Claude."
    - "Regeneration from the committed tree is idempotent: the builder leaves n8n/ with zero git diff before the operator is handed the gate."
    - "Both suites are at or above their HEAD baselines (pytest 4952 passed / 154 skipped; node 1170 pass / 0 fail) before the operator is handed the gate."
    - "Every generated body reads disarmed: settings.executionOrder is v1 and every live-write flag is false, verified on the committed JSON before deploy and read back from the live instance after."
    - "The plugin ships as 0.50.0 with a CHANGELOG entry in the same commit as the last plugin change, and is NOT pushed — the operator pushes."
    - "CLAUDE.md §31 zero-inbox holds: every residual opened during this phase is triaged as defect with evidence, question with a trigger and owner, design with a decision needed, or accepted in completed."
    - "Attempt 3's Stage A and Stage E pass where attempt 2 failed or partialled."
  artifacts:
    - "operator-claude-plugin/.claude-plugin/plugin.json at 0.50.0 with its CHANGELOG entry"
    - ".planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md — the operator's gate record"
    - "tests/stress-tests/RUNBOOK.md — the known-gap note corrected for the rows this phase fixed"
  key_links:
    - "committed n8n/wf_*.json -> scripts/deploy_n8n_workflows.py PUT -> scripts/bounce_n8n_workflows.py reload -> the running instance the attempt-3 stages exercise"
    - "scripts/uat_reset.py --snapshot then --execute -> a portal reset to the pre-UAT record set -> Stage A"
  prohibitions:
    - "Never arm n8n writes, never deploy, never bounce from Claude — Task 3 hands the operator the commands and records what they report (D-73-18)."
    - "Never hand-edit n8n/wf_*.json — regenerate via scripts/build_cloud_workflows.py and leave zero git diff."
    - "Never push the plugin release — the operator pushes."
    - "Never record an observed-live tag for the create-error lane; per D-73-19 it is proven offline only and F-A5 removes attempt 3's natural trigger."
    - "Never engineer a test-only dedupe bypass in the plugin to manufacture a live create conflict (D-73-19)."
---

<objective>
Hand the operator one gate, at the end, with everything already proven offline.

Six plans changed the builder, three shared JS engines and five plugin modules. This plan makes
the tree deployable and idempotent, ships the plugin release, and then stops: the deploy, the
bounce, the portal reset and stress attempt 3 are the operator's, per D-73-18 and the standing
rule that Claude never arms, deploys or bounces.

One thing this gate deliberately does NOT claim: F-A6's create-error lane is proven offline only.
F-A5's dedupe removes the very within-batch conflict that triggered it in attempt 2, so attempt 3
cannot reproduce it on demand, and D-73-19 forbids engineering a trigger. Do not let a green
attempt 3 be read as live proof of that lane.

Purpose: one deploy, one reset, one run, one record. Output: a released plugin, a gate record, and
a phase that can be verified.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-CONTEXT.md
@tests/stress-tests/RUNBOOK.md
@tests/stress-tests/SESSION-2026-09-15.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-flight — idempotent regeneration, both suites, disarmed read, zero-inbox triage</name>
  <files>.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md</files>
  <read_first>
    - the six SUMMARY files 73-01 through 73-06 (what changed, and each plan's recorded node counts and verdicts)
    - CLAUDE.md §31 — the three todo triage rules, including zero-inbox at batch close
    - scripts/verify_live_write_safety.py's module docstring — what disarmed means on a generated body
    - tests/stress-tests/RUNBOOK.md's pre-flight section — the exact checks the operator repeats live
  </read_first>
  <action>
    No production code changes here. If any check fails, STOP and report — the failure belongs to
    whichever plan owns it, not to this one.

    1. Regenerate from the committed tree and prove idempotency: the builder must leave `n8n/`
       with no git diff.
    2. Record the node count of every generated workflow and compare against the counts the six
       SUMMARYs recorded. Any count this plan cannot explain from a named change is a stop.
    3. Confirm every generated body carries the v1 execution order and that no live-write flag is
       set true anywhere in the generated JSON.
    4. Run both suites in full and record the numbers against the HEAD baselines.
    5. Run the todo triage over everything this phase opened and take one decision per residual —
       fix now, keep as question or design with its trigger or decision named, or accept into
       completed. The phase does not reach the operator with an untriaged pending todo.
    6. Start `73-UAT.md` with a pre-flight section recording items 1 to 5.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -c "import subprocess,sys; out=subprocess.run(['git','status','--porcelain','--','n8n/'],capture_output=True,text=True,check=True).stdout; print('N8N_DIFF',repr(out)); sys.exit(1 if out.strip() else 0)"</automated>
    <fails_when>non-zero exit, or N8N_DIFF printing anything other than an empty string — regeneration is not idempotent against the committed tree</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -c "import json,glob,sys; bad=[p for p in glob.glob('n8n/wf_*.json') if json.load(open(p)).get('settings',{}).get('executionOrder')!='v1']; print('NON_V1:',bad); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or NON_V1 naming any file — a generated body would run on the legacy engine, whose empty-item push is the whole Phase 70 failure class</fails_when>
  </verify>
  <verify>
    <automated>/usr/bin/grep -c 'ALLOW_HUBSPOT_RECORD_WRITES = \"true\"' n8n/wf_contact_ingest_cloud.json n8n/wf_enrichment_cloud.json</automated>
    <fails_when>any count other than 0 — a generated body is armed and must not be deployed</fails_when>
  </verify>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs 2>&1 | tail -5 && .venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/ 2>&1 | tail -5</automated>
    <fails_when>either summary line reports a non-zero failure count, or the pass counts fall below the HEAD baselines of 1170 node and 4952 pytest</fails_when>
  </verify>
  <acceptance_criteria>
    - Regeneration leaves `n8n/` with zero git diff.
    - Every generated workflow's node count is recorded in `73-UAT.md` and reconciles with the SUMMARYs.
    - Every generated body carries the v1 execution order and zero armed write flags.
    - Both suites are at or above the HEAD baselines, with the numbers recorded.
    - `.planning/todos/pending/` contains no untriaged file from this phase.
  </acceptance_criteria>
  <done>The tree is deployable and the numbers are written down.</done>
</task>

<task type="auto">
  <name>Task 2: release the plugin as 0.50.0 and correct the RUNBOOK's known-gap note</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md, tests/stress-tests/RUNBOOK.md</files>
  <read_first>
    - operator-claude-plugin/CHANGELOG.md — the entry format of the last few releases and the version currently shipped
    - operator-claude-plugin/.claude-plugin/plugin.json — the version field
    - the 73-01 through 73-06 SUMMARYs — the operator-visible behaviour changes to describe
    - tests/stress-tests/RUNBOOK.md's restart procedure step 4 — its note that the companies CSV keeps three known-gap rows "until fixed", which is now only partly true
    - repo memory `plugin-release-requires-version-bump` — a greyed-out update button means an unbumped manifest or a stale marketplace clone; the clone fetches from the pushed branch
  </read_first>
  <action>
    Bump `plugin.json` to 0.50.0 and add the matching CHANGELOG entry in the SAME commit. The entry
    describes what an operator will notice, in their words: duplicate spreadsheet rows now collapse
    before the send and are named in the preview; a company website that is a freemail address is
    refused with the reason; a company already in the portal under a www. domain is now found
    instead of duplicated; a name-only company says what to supply; approving a held record no
    longer fails on a multi-value field; the run report now shows enrich and update outcomes; and
    the cost estimate is priced per lane. Call out the behaviour change an existing operator could
    be surprised by — a duplicate row is now dropped rather than sent — since that changes what
    their row count means.

    Correct RUNBOOK restart step 4: two of the three known-gap rows are fixed by this phase and now
    EXERCISE the fixes rather than documenting gaps; only the name-plus-TLD case stays a documented
    known gap (D-73-07). Keep all three rows in the CSV.

    Do NOT push. The operator pushes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_plugin_manifest.py</automated>
    <fails_when>non-zero exit, or output containing "failed" — the manifest version or its shape is invalid</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -c "import json,sys; v=json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version']; print('VERSION',v); sys.exit(0 if v=='0.50.0' else 1)" && /usr/bin/grep -c '0.50.0' operator-claude-plugin/CHANGELOG.md</automated>
    <fails_when>non-zero exit, or a CHANGELOG count of 0 — the manifest and the changelog disagree about the release, which is exactly the state that produces a greyed-out update button</fails_when>
  </verify>
  <acceptance_criteria>
    - `plugin.json` reads 0.50.0 and `CHANGELOG.md` carries a 0.50.0 entry, both in one commit with the last plugin change.
    - The entry names the duplicate-row drop as an operator-visible behaviour change.
    - RUNBOOK restart step 4 distinguishes the rows this phase fixed from the one that remains a known gap.
    - Nothing was pushed.
  </acceptance_criteria>
  <done>The release is cut and the runbook tells the truth about its own CSV.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Seven plans' worth of backend and plugin fixes, all proven offline: the ingest create-error
    containment and its identity-based pairing, the CSV duplicate collapse, the review-approve
    array serialization, the www.-tolerant company domain match with its no-domain sentinel and
    its name-only review reasons, the freemail refusal in both engines, the 400 ms ingest search
    throttle, the run-report enrich accounting, and the per-lane cost envelope. The tree is
    regenerated, idempotent, green on both suites, and every generated body reads disarmed on the
    v1 engine. The plugin is cut as 0.50.0 and NOT pushed. Nothing has been deployed, bounced or
    armed — that is this checkpoint.
  </what-built>
  <how-to-verify>
    Run the five numbered steps in the action below, in order, from your own shell, and stop at
    the first failure. Each command is given in the environment-loading form because the
    environment file is permission-blocked to Claude.
  </how-to-verify>
  <resume-signal>Reply "approved" once attempt 3 Stages A to F are recorded in 73-UAT.md, or describe what failed and at which stage.</resume-signal>
  <name>Task 3: OPERATOR GATE — deploy and bounce disarmed, reset, run attempt 3 Stages A to F</name>
  <read_first>
    - tests/stress-tests/RUNBOOK.md — the pre-flight section, Stages A to F, the stop conditions, and the restart procedure
    - .planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md — Task 1's pre-flight record, which the operator's live read-back is compared against
    - repo memory `n8n-stored-vs-running-content` — a bare PUT never reloads a running workflow; bounce after every deploy, and a read-back proves only what is STORED until the bounce lands
  </read_first>
  <action>
    STOP. Everything from here is the operator's, run by the operator, from their own shell. Claude
    does not deploy, does not bounce, does not arm, and does not run any of the stages. Present the
    commands, then wait, then record what comes back in `73-UAT.md`.

    Every command below is handed over in the environment-loading form this repo uses for anything
    that needs a credential, because the environment file is permission-blocked to Claude.

    **1 — Deploy (disarmed).** Both keys are required; the deploy script defaults to a dry run, and
    a dry run prints the table and makes no request at all.
    ```
    ! set -a; . ./.env; set +a; DRY_RUN=false ALLOW_N8N_DEPLOY=true python3 scripts/deploy_n8n_workflows.py
    ```
    Expect a PUT per changed workflow, each 200.

    **2 — Bounce.** A PUT alone never reloads a running workflow.
    ```
    ! set -a; . ./.env; set +a; python3 scripts/bounce_n8n_workflows.py
    ```
    It exits non-zero while anything is armed. Expect all five active.

    **3 — Read back, disarmed.**
    ```
    ! set -a; . ./.env; set +a; python3 scripts/verify_live_write_safety.py
    ```
    Record, for each of the five workflows: the live node count, the execution-order setting, and
    the write-flag reading. Compare the node counts against Task 1's record — a mismatch means the
    bounce did not land, and is a stop.

    **4 — Snapshot, then reset.** The snapshot is mandatory and the reset refuses without it.
    ```
    ! set -a; . ./.env; set +a; python3 scripts/uat_reset.py --snapshot --companies-csv tests/stress-tests/uat-stress-companies-2026-09-14.csv
    ```
    Then the reset's dry run, then its execute, per the runbook's close-out steps.

    **5 — Attempt 3, Stages A to F**, per RUNBOOK.md, recording for each stage the run id, the
    execution range, the outcome counts, and the disarm check at the stage boundary.

    Record in `73-UAT.md`, per stage: what passed, what did not, and the evidence. Stage A and
    Stage E are the two that failed or partialled in attempt 2 and are the ones this phase is
    judged on. Also record explicitly, as a limitation rather than a result: the create-error lane
    was NOT exercised, because this phase's own duplicate collapse removes the conflict that used
    to trigger it — it stays proven offline only.

    If anything reads armed at any point, or a stage hits a runbook stop condition, STOP and record
    it. Do not continue to the next stage.
  </action>
  <verify>
    <human-check>
      The operator confirms, and 73-UAT.md records:
      - five workflows deployed and bounced, all active, all disarmed, live node counts equal to Task 1's record, execution order v1 on every body
      - the portal snapshot taken and the reset executed, with the post-reset check clean
      - Stage A: the contacts batch completes with no aborted execution, duplicate rows reported as collapsed in the preview, and every created contact associated to a company
      - Stage B: no company duplicated for a www.-stored portal record, the freemail row refused with its reason, the name-only row held for review with a reason naming what to supply
      - Stage E: a held record with a multi-value field approves successfully
      - Stage F: the status read reports real Lusha and ZoomInfo balances and Apollo as unknown
      - the run report accounts every outcome, with none unaccounted
      - nothing armed at any stage boundary, and the final execution delta recorded
    </human-check>
  </verify>
  <acceptance_criteria>
    - `73-UAT.md` carries a per-stage record with run ids, execution ranges and outcome counts.
    - The live read-back's node counts and disarmed state are recorded and match Task 1's pre-flight record.
    - Stage A and Stage E are recorded as passed, or their failures are recorded with evidence and the phase stops there.
    - The create-error lane's non-exercise is recorded as a stated limitation, not omitted.
    - No armed reading appears anywhere in the record.
  </acceptance_criteria>
  <done>The operator has deployed, reset, run attempt 3 and recorded it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo JSON → the running n8n Cloud instance | A generated body becomes the code a credential-bearing runtime executes |
| disarmed instance → armed write window | The only boundary at which this system can change CRM data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-07-01 | Elevation of Privilege | deploying a body that is armed | high | mitigate | Task 1 greps the generated JSON for an armed write flag and fails on any hit; the bounce script exits non-zero while armed; the operator's read-back records the flag state per workflow before any stage runs |
| T-73-07-02 | Tampering | deploying a body that is not what the builder produces | high | mitigate | Task 1's idempotent-regeneration check must leave a zero diff before the operator is handed the gate |
| T-73-07-03 | Tampering | a stored PUT that never reloads, so the stages exercise the OLD graph | high | mitigate | Bounce after every deploy, and compare live node counts against the pre-flight record — a mismatch is a stop, not a note |
| T-73-07-04 | Repudiation | a green attempt 3 read as live proof of the create-error lane it never exercised | medium | mitigate | The limitation is a required field of the gate record, and the plan's prohibitions forbid an observed-live tag for that lane |
| T-73-07-05 | Information Disclosure | a credential echoed into the gate record | medium | mitigate | Every credential-bearing command is handed to the operator in the environment-loading form and never run by Claude; only outcomes are recorded, never command environments |
</threat_model>

<verification>
- `.venv/bin/python scripts/build_cloud_workflows.py` leaves `n8n/` with zero git diff.
- `node --test tests/n8n/*.test.mjs` at or above 1170 pass / 0 fail.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` at or above 4952 passed / 154 skipped.
- The operator's live read-back records five active, disarmed workflows on the v1 engine with the pre-flight node counts.
</verification>

<success_criteria>
- The tree is idempotent, green and disarmed before the gate opens.
- The plugin ships as 0.50.0 with its CHANGELOG entry, unpushed.
- The operator's attempt-3 record exists, with Stage A and Stage E passing where attempt 2 did not.
- The create-error lane's offline-only status is stated, not implied.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind |
|---|---|
| `operator-claude-plugin/.claude-plugin/plugin.json` at 0.50.0 | release |
| `operator-claude-plugin/CHANGELOG.md` 0.50.0 entry | release notes |
| `.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md` | new gate record |
| corrected known-gap note in `tests/stress-tests/RUNBOOK.md` | doc |

<output>
Create `.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-07-SUMMARY.md` when done.
</output>
