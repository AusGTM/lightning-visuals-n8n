---
phase: 73-ga-fix-list-from-stress-attempt-2
plan: 05
type: execute
wave: 5
depends_on: ["73-04"]
autonomous: true
files_modified:
  - scripts/build_cloud_workflows.py
  - operator-claude-plugin/scripts/backend_status.py
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/cost_guard.py
  - tests/n8n/backendStatusCredits.test.mjs
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_cost_guard.py
  - n8n/wf_backend_status_cloud.json
requirements: [F-B6, F-A1, F-A2, F-B1]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-73-17: the backend-status lane reports Lusha credits.remaining and the ZoomInfo GTM balance from the already-provisioned credentials, and reports Apollo as unknown rather than not_configured — the Apollo key is not a master key and answers 403."
    - "D-73-17: the plugin's spend guard bounds on the Lusha and ZoomInfo numbers and reports Apollo as unknown; an unknown provider is reported, never silently treated as free."
    - "The F-B6 root cause is named with evidence — an offline walk of the committed status graph either reproduces not_configured (a wiring gap, fixed here) or reports real balances (a deploy-parity gap, carried to the operator gate)."
    - "D-73-16: plan_grant and envelope take a real lane, not just a display label; contact-upload is priced at zero provider credits and one execution per POST plus any association hop, companies at the measured 2 Lusha credits per company, and enrich-before-ingest keeps the contact rates."
    - "The contact-upload zero-cost figure comes from the SAME estimator preview.py already uses for that lane, so the two call sites can no longer price the same send differently."
    - "The rate table stays dated and deliberately over-stating within a lane — no rate is revised downward in this plan."
  artifacts:
    - "tests/n8n/backendStatusCredits.test.mjs — the offline walk of the status graph's credit lane"
    - "the lane parameter on write_grant.plan_grant / envelope and its two lane branches"
    - "the F-B6 verdict, recorded in the SUMMARY as wiring or deploy-parity with the evidence that settled it"
  key_links:
    - "Status Credit Request -> Lusha Usage / Apollo Usage / the ZoomInfo usage subgraph -> Adapt nodes -> Collect Credits -> Build Status -> backend_status.py -> sweep_conditions quota state"
    - "plan_grant(lane=...) -> envelope -> cost_guard.estimate_batch / preview_enrichment.zero_cost_estimate -> the figures the operator reads before the yes"
  prohibitions:
    - "Never arm n8n writes, never deploy, never bounce from Claude — the operator's end-of-phase gate owns all three (D-73-18)."
    - "Never hand-edit n8n/wf_*.json — regenerate via scripts/build_cloud_workflows.py and leave zero git diff."
    - "Never add new probe nodes to the status lane — it already probes all three providers; diagnose before writing anything (RESEARCH Pitfall 4)."
    - "Never report an unreadable provider balance as zero or as free — unknown is a distinct, reported state."
    - "Never lower a rate in the dated table; over-stating within a lane is the safe direction for a guard."
---

<objective>
Two low-severity findings that both cost the operator trust in the numbers.

F-B6: the status lane reports every provider as not configured, while the enrichment lane's own
credit probe — the same probe code, the same credentials — read Lusha 3708 remaining and ZoomInfo
9358 in the same stress session. Research (Pitfall 4, Assumption A2) says the status graph already
has all three probe nodes fully wired, so this is a wiring gap or a deploy-parity gap, not missing
code. The live instance has already been caught one generation behind once this session (the
throttle fix, F-S2). Diagnose offline before writing anything.

F-A1/A2/B1: the cost envelope prices a provider-free contact upload as if it called providers, and
prices a companies batch at Lusha's contact rate. `plan_grant` already receives a label; nothing
branches on it.

Purpose: a guard whose numbers are wrong in the operator's favour is worse than no guard. Output:
real balances, honest per-lane arithmetic, and a named root cause for the status lane.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-CONTEXT.md
@.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-PATTERNS.md
@tests/stress-tests/SESSION-2026-09-15.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: diagnose the status lane offline, then fix or classify (F-B6)</name>
  <files>tests/n8n/backendStatusCredits.test.mjs, scripts/build_cloud_workflows.py, operator-claude-plugin/scripts/backend_status.py, n8n/wf_backend_status_cloud.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py `build_backend_status_cloud` — the whole function, node by node, with its own docstring naming the probe chain
    - scripts/build_cloud_workflows.py `ENRICH_STATUS_CREDIT_REQUEST` (its comment states it probes all three providers unconditionally, so there is no not-configured case for it to represent), `_credit_http_node`, `_credit_adapt_js`
    - scripts/build_cloud_workflows.py `build_enrichment_cloud`'s own credit branch (`Lusha Usage` / `Apollo Usage` / the ZoomInfo subgraph / `Collect Credits`) — the SAME probe code, working, on a sibling lane; the difference between the two wirings is the thing to find
    - operator-claude-plugin/scripts/backend_status.py and sweep_conditions.py — the three quota states and the fourth `{state: unknown, reason: not_configured}` shape D-22 recorded live
    - tests/n8n/backendStatus.test.mjs and backendStatusResponse.test.mjs — existing coverage
    - repo memory `provider-credit-check-endpoints` — Lusha `credits.remaining` works; ZoomInfo GTM needs the JSON:API Accept header or answers 406; Apollo's key is not a master key and answers 403
  </read_first>
  <action>
    Diagnose first, per RESEARCH Pitfall 4. Do NOT add probe nodes.

    1. Write `tests/n8n/backendStatusCredits.test.mjs`: walk the COMMITTED
       `n8n/wf_backend_status_cloud.json` with HTTP stubs that mirror the live provider answers
       the enrichment lane read in the same stress session — a Lusha body carrying a remaining
       count, a ZoomInfo body carrying a balance, an Apollo 403 — and assert the status response
       carries the two real numbers and reports Apollo as unknown, not as not configured.
    2. Run it and record the verdict in the SUMMARY:
       - RED offline means the wiring is genuinely broken in the committed graph (the likely
         suspects are the carry-merge / sentinel chain between the credit request and the status
         builder, under the v1 rules in CLAUDE.md §13.0.3: a zero-item output is not a delivery,
         and a starved Merge input silently terminates its downstream). Fix the wiring here with
         the existing helpers — never a hand-wired edge — and regenerate.
       - GREEN offline means the committed graph is already correct and the live instance is a
         generation behind, exactly as F-S2 caught the throttle fix. In that case change NO graph
         code: record the classification, and state in the SUMMARY that the proof is the operator
         gate's post-deploy status read (plan 73-07 already performs it).
    3. Either way, close the plugin half of D-73-17: the spend guard bounds on the Lusha and
       ZoomInfo numbers and reports Apollo as unknown. An unreadable provider must never be
       rendered as zero or as free. If `backend_status.py` already does this correctly, add the
       assertion that pins it rather than changing code.
  </action>
  <verify>
    <automated>node --test tests/n8n/backendStatusCredits.test.mjs tests/n8n/backendStatus.test.mjs tests/n8n/backendStatusResponse.test.mjs tests/n8n/creditsSummaryUnderV1.test.mjs</automated>
    <fails_when>non-zero exit, or output containing "fail 1" or higher — the status lane still reports a configured provider as not configured, or an existing status assertion regressed</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_status_unknown.py operator-claude-plugin/tests/test_status_all_workflows.py operator-claude-plugin/tests/test_status_skill.py</automated>
    <fails_when>non-zero exit, or output containing "failed" — an unreadable provider balance is being rendered as a number, or the unknown state stopped being reported</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/n8n/backendStatusCredits.test.mjs` exists, walks the committed status graph, and asserts two real balances plus Apollo unknown.
    - The SUMMARY names the verdict — wiring gap or deploy-parity gap — and quotes the walk output that settled it.
    - If the verdict was deploy-parity, `scripts/build_cloud_workflows.py` is unmodified by this task and `n8n/` has zero diff.
    - If the verdict was a wiring gap, the fix used the existing merge/sentinel helpers and regeneration leaves `n8n/` clean.
    - No new probe node was added in either case.
    - A test pins that an unreadable provider is reported as unknown and never as zero or free.
  </acceptance_criteria>
  <done>The status lane's balance behaviour is proven offline and its root cause is named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: price the grant by lane, not by blanket object type (D-73-16)</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/scripts/cost_guard.py, operator-claude-plugin/tests/test_write_grant.py, operator-claude-plugin/tests/test_cost_guard.py</files>
  <behavior>
    - A contact-upload grant reports zero provider credits and one execution per POST, plus the association hop's own count when the lane makes one — never a chunk-derived execution count.
    - A companies grant prices Lusha at the measured companies-match rate, not the contacts first-time-enrich rate.
    - An enrich-before-ingest grant keeps the contact rates it has today.
    - The contact-upload figure equals the figure the preview already produces for the identical send — the two call sites cannot disagree.
    - A lane the estimator does not know refuses loudly rather than falling through to a default price.
    - Every figure keeps its existing basis marker (measured, projected, unconfigured).
  </behavior>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py `plan_grant` and `envelope` (the `label` parameter that is display-only today, and the execution-count arithmetic)
    - operator-claude-plugin/scripts/cost_guard.py `PROVIDERS`, `PROVIDER_RATE_KEYS`, `estimate_batch` — the per-object-type rates that already exist, and the ponytail note on deliberate over-statement
    - operator-claude-plugin/scripts/preview_enrichment.py `zero_cost_estimate`, `TABULAR_COST_REASON` and preview.py's `tabular_cost_block` — the already-correct sibling estimator for the contact-upload lane; reuse it rather than reproducing its math
    - operator-claude-plugin/scripts/write_grant.py's `LANES` mapping — the lane names that already exist server-side
    - .planning/phases/73-ga-fix-list-from-stress-attempt-2/73-RESEARCH.md Pitfall 5
  </read_first>
  <action>
    Extend `test_write_grant.py` / `test_cost_guard.py` FIRST with the two cases D-73-16 names — a
    contact-upload grant that must price at zero provider credits and one execution, and a
    companies grant that must price Lusha at the companies-match rate — and observe them RED.

    Then give `plan_grant`/`envelope` a real `lane` parameter, resolved against the lane names the
    module already knows, and branch the estimate on it:
    - contact-upload routes to the SAME zero-cost estimator the preview already uses, and to a
      per-POST execution count rather than the chunk-derived one;
    - companies routes to the existing companies rate key — confirm it actually reaches
      `estimate_batch` un-overridden rather than being replaced upstream, and say so in the SUMMARY;
    - enrich-before-ingest keeps the contact rates unchanged.
    An unrecognised lane refuses; it never falls through to a default. Keep `label` as the display
    string it already is — the lane is a separate, meaningful argument.

    Do not revise any rate downward. The table stays dated and deliberately over-stating.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_cost_guard.py operator-claude-plugin/tests/test_write_grant_guardrails.py operator-claude-plugin/tests/test_write_grant_surface.py</automated>
    <fails_when>non-zero exit, or output containing "failed" — a lane is still priced with the wrong rate, an unknown lane fell through to a default, or a guardrail regressed</fails_when>
  </verify>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider operator-claude-plugin/tests/test_preview_enrichment.py operator-claude-plugin/tests/test_headless_grant_boundary.py</automated>
    <fails_when>non-zero exit, or output containing "failed" — the grant and the preview now disagree about the same contact-upload send</fails_when>
  </verify>
  <acceptance_criteria>
    - Both new cases were observed RED before the change (quoted in the SUMMARY).
    - `plan_grant` and `envelope` accept a `lane` argument and branch on it; `label` remains display-only.
    - A test asserts the contact-upload grant figure equals the preview's figure for the identical send.
    - A test asserts an unrecognised lane refuses rather than defaulting.
    - No rate value in the dated table was lowered; the SUMMARY states whether the companies rate key reaches `estimate_batch` un-overridden.
  </acceptance_criteria>
  <done>Each lane is priced with its own measured rate and its own execution model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider credit APIs → status lane → operator | Third-party balance responses become the numbers a spend decision rests on |
| cost envelope → operator's yes | The arithmetic an operator approves a paid batch on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-05-01 | Information Disclosure | provider credential handling on the status lane | medium | mitigate | No credential value is added to a node output, a log line, or a test fixture; the probe nodes stay credential-bound exactly as they are and no new probe node is added |
| T-73-05-02 | Repudiation | an unreadable balance reported as zero or free | high | mitigate | Unknown stays a distinct, reported state, pinned by a test; the spend guard bounds only on the providers that actually answered |
| T-73-05-03 | Tampering | under-stated cost leading to an approved batch that overspends | high | mitigate | Rates stay deliberately over-stating within a lane, no rate is lowered, and the contact-upload figure is asserted equal to the preview's figure so the two call sites cannot diverge again |
| T-73-05-04 | Spoofing | a third-party error body parsed as a balance | medium | mitigate | The Apollo 403 case is an explicit test input and must produce unknown, never a number |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` at or above the 1170 pass / 0 fail HEAD baseline.
- `.venv/bin/python -m pytest -q --tb=short -p no:cacheprovider tests/ operator-claude-plugin/tests/` at or above 4952/154.
- `.venv/bin/python scripts/build_cloud_workflows.py` leaves `n8n/` with zero git diff.
</verification>

<success_criteria>
- The status lane's balance behaviour is proven offline and its root cause named.
- An unreadable provider is reported as unknown, never as free.
- Each lane's grant carries its own rate and execution model, agreeing with the preview.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind |
|---|---|
| `tests/n8n/backendStatusCredits.test.mjs` | new test file |
| `lane` parameter on `plan_grant` / `envelope` | new function argument |
| the per-lane branches in the cost envelope | code change |
| the F-B6 verdict (wiring vs deploy-parity) | finding recorded in the SUMMARY |

<output>
Create `.planning/phases/73-ga-fix-list-from-stress-attempt-2/73-05-SUMMARY.md` when done.
</output>
