---
task: 260829-lg3-close-the-five-grandfathered-skill-md-co
type: quick
mode: full
created: 2026-08-29
autonomous: true
files_modified:
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_chunking.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
production_code_changes: none

must_haves:
  truths:
    - "GRANDFATHERED_UNCOVERED is empty and MAX_GRANDFATHERED is 0 after Task 3 lands."
    - "Each of the five closed identities has a COVERED entry whose nodeid resolves to a real test that drives the SPECIFIC named join with a real value flowing between the calls -- not the calls invoked independently of each other."
    - "The full plugin suite passes (baseline 1721 passed / 5 skipped, plus the tests added here), with zero existing assertions weakened, deleted, or reworded, and zero live network/HubSpot/Anthropic/provider calls -- stub transports only."
    - "Each of the three commits bumps .claude-plugin/plugin.json's version and adds a matching CHANGELOG.md entry in the SAME commit (0.28.3 -> 0.28.4 -> 0.28.5 -> 0.28.6)."
    - "The three registries (COVERED, NOT_A_PIPELINE, GRANDFATHERED_UNCOVERED) stay pairwise disjoint and their union stays exactly equal to the live-extracted SKILL.md identity set at every commit."
  artifacts:
    - operator-claude-plugin/tests/test_write_grant.py
    - operator-claude-plugin/tests/test_chunking.py
    - operator-claude-plugin/tests/test_skill_sequence_coverage.py
    - operator-claude-plugin/.claude-plugin/plugin.json
    - operator-claude-plugin/CHANGELOG.md
  key_links:
    - "write_grant.authorize_send/authorize_ungranted_send's returned {workflow_id, grant} feeds directly into n8n_arming.armed_window(...), whose OPEN window scopes the dispatch.dispatch / chunking.dispatch_plan call made inside its `with` body."
    - "chunking.chunk_ceiling(cfg[, key=...])'s REAL config-derived return feeds chunking.plan_chunks(spec, ceiling) as the `ceiling` argument -- never a literal int."
    - "chunking.dispatch_plan(...)'s DispatchOutcome.responses, flattened per-chunk, feeds preingest.merge_enriched(rows, responses) directly."
    - "preingest.match_batch(...)'s MatchOutcome.responses and .unchecked_row_ids feed preingest.classify_matches(rows, response, unchecked_row_ids=...) directly."
    - "enrichment.resolve_providers(None, cfg)'s real resolved provider list feeds chunking.dispatch_plan(plan, providers, ...), visible in the stub transport's recorded POST body."
---

<objective>
Close all five entries in `GRANDFATHERED_UNCOVERED`
(`operator-claude-plugin/tests/test_skill_sequence_coverage.py`) by writing composition tests
that actually drive each entry's SPECIFIC named undriven join -- not the same calls invoked in
isolation -- then moving each identity to `COVERED` with the covering test's nodeid and
decrementing `MAX_GRANDFATHERED` in the same commit as its test.

Purpose: the guard shipped in 260829-hjm is a ratchet, not a clean bill of health -- it passes
today only because these five are grandfathered with a written, specific reason each. This is
P2 of `.planning/HANDOVER-2026-08-29-backlog.md`.

Output: `MAX_GRANDFATHERED == 0`, `GRANDFATHERED_UNCOVERED == {}`, five new `COVERED` entries
across three composition tests, three commits each bumping `plugin.json` + `CHANGELOG.md`.

**Zero production-code changes.** Every change is test-file additions plus release metadata.
No file under `operator-claude-plugin/scripts/` is touched. No `skills/*/SKILL.md` is edited --
the census is a set-equality over live-extracted call tuples, and editing a documented block
would churn a tuple and orphan a registry key, failing the suite by a different mechanism than
the one this task is closing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260829-lg3-close-the-five-grandfathered-skill-md-co/260829-lg3-CONTEXT.md
@.planning/quick/260829-lg3-close-the-five-grandfathered-skill-md-co/260829-lg3-RESEARCH.md
@operator-claude-plugin/tests/test_skill_sequence_coverage.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This task adds pytest test functions and edits two data dicts + release metadata. It never imports, executes, or writes anything under `operator-claude-plugin/scripts/`, and the autouse `no_network`/`no_durable_writes` fixtures (`conftest.py:587-638`) are never bypassed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lg3-01 | Tampering | `COVERED` registry entries | medium | mitigate | `test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink` (existing, unmodified) fails if a nodeid is stale or its test is refactored out from under it -- a structural guard against a false coverage claim, not left to review alone. |
| T-lg3-02 | Repudiation | "coverage" claimed without real dataflow | high | mitigate | Each new test's own assertions are written to fail if the join is faked (see each task's `<verify>` / acceptance criteria) -- a scripted response value must appear, transformed, in the final assertion, or the test does not pass. |
| T-lg3-03 | Information Disclosure | none — no new external call, no new secret handled | n/a | accept | No new transport, credential, or endpoint is introduced; every call in every new test goes through an existing stub. |
| T-lg3-SC | Tampering | package installs | n/a | accept | No `pip`/`npm`/`cargo` install is introduced by this task — zero new dependencies. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: close #1 (contact-upload) and #4 (enrich-before-ingest) — one shared authorize -> armed_window -> dispatch.dispatch test</name>

  <read_first>
    - `operator-claude-plugin/tests/test_write_grant.py:1-141` -- module helpers `_base_workflow`,
      `_armed_workflow`, `_workflow_list`, `_plan_reads`, `_proposal`, `_open`, `granting_config`
      fixture, and the tracer test `test_a_send_arms_under_an_opened_grant_with_no_environment_variable_set`
      (lines 116-141) -- the grant-present pattern to extend.
    - `operator-claude-plugin/tests/test_write_grant.py:296-329` --
      `test_authorize_ungranted_send_arms_with_the_same_guardrails_a_standing_grant_gets` -- the
      ungranted pattern to extend; its body is `with n8n_arming.armed_window(...) as window: pass`
      -- this is the exact one-call-short gap named in the registry's reason for entries #1/#4.
    - `operator-claude-plugin/scripts/write_grant.py:729-828` -- `authorize_send` and
      `authorize_ungranted_send` signatures and their shared 5-key return shape
      (`{armed, workflow_id, grant, refusal, detail}`).
    - `operator-claude-plugin/scripts/dispatch.py:58-118` -- `dispatch(file_path, armed, config,
      transport=requests.post, *, run_id=None)`. `armed` has NO default. Returns
      `{"body", "run_id", "written_records_failures"}`.
    - `operator-claude-plugin/scripts/n8n_arming.py:475-513` -- `armed_window` is a context
      manager; `__enter__` raises `ArmingRefused` unless ARMED, `__exit__` always disarms.
    - `operator-claude-plugin/tests/conftest.py:135-167` (`_StubTransport` /
      `stub_transport` / `stub_post_transport_factory`, plain-callable) and `:213-263`
      (`_StubModuleTransport` / `stub_module_transport_factory`, `.get`/`.post`/`.put`) --
      the two INCOMPATIBLE transport shapes. `armed_window(..., transport=X)` needs the
      module-shaped one; `dispatch.dispatch(..., transport=Y)` calls `Y(url, headers=,
      files=, timeout=)` directly and needs the plain-callable one. Mixing them raises
      `TypeError` immediately.
    - `operator-claude-plugin/tests/test_dispatch_multipart.py:73-95` --
      `test_armed_dispatch_calls_the_stub_exactly_once_with_the_deployed_contract` -- the
      exact `dispatch.dispatch` return-shape assertions to mirror.
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:209-227, 262-274` -- the
      two `GRANDFATHERED_UNCOVERED` entries this task closes, and their exact reason text
      (both name TWO separate undriven gaps: the ungranted branch stops one call short of
      `dispatch.dispatch`, AND no test chains the grant-present `authorize_send` branch into
      `armed_window`+dispatch at all -- both branches must be driven, not just one).
  </read_first>

  <files>
    operator-claude-plugin/tests/test_write_grant.py,
    operator-claude-plugin/tests/test_skill_sequence_coverage.py,
    operator-claude-plugin/.claude-plugin/plugin.json,
    operator-claude-plugin/CHANGELOG.md
  </files>

  <action>
    In `test_write_grant.py`, add `import dispatch` at the top (alongside the existing
    `import n8n_arming` / `import write_grant`) and add ONE new test function, placed after
    `test_authorize_ungranted_send_arms_with_the_same_guardrails_a_standing_grant_gets`, that
    drives BOTH branches the registry's reason names as separately undriven, per D-01 of
    260829-lg3-CONTEXT.md (one shared test, two COVERED entries):

    Branch 1 (grant-present `authorize_send`): build a standing grant exactly as
    `test_a_send_arms_under_an_opened_grant_with_no_environment_variable_set` does (its own
    `_open(granting_config, transport)` call against a `stub_module_transport_factory([...])`
    instance scripted with that SAME test's 14-entry sequence -- `_workflow_list()`,
    `_base_workflow()`, then the arm/verify/disarm entries verbatim). Then call
    `write_grant.authorize_send(grant, lane="enrichment", record_ids=[RECORD_ID],
    record_domains=[])`, assert `decision["armed"] is True`, then open
    `n8n_arming.armed_window(decision["workflow_id"], [RECORD_ID], [], False, granting_config,
    transport=<that same 14-entry transport instance>, grant=decision["grant"])` and, INSIDE the
    `with` body (replacing what would be a bare `pass`), call `dispatch.dispatch(str(sample_csv),
    True, granting_config, transport=stub_transport)`. Assert the window armed and disarmed
    (`window.arm_result["outcome"] == n8n_arming.ARMED`,
    `window.disarm_result["outcome"] == n8n_arming.DISARMED`), and assert on the dispatch
    result: `result["run_id"]` is truthy and `len(stub_transport.calls) == 1`.

    Branch 2 (`authorize_ungranted_send`, no standing grant): mirror
    `test_authorize_ungranted_send_arms_with_the_same_guardrails_a_standing_grant_gets` exactly
    -- a FRESH `stub_module_transport_factory([...])` instance with that test's own 14-entry
    scripted sequence (a fresh instance is required: Gotcha 4, a scripted queue is drained on
    use and cannot be reused across the two branches) -- call
    `write_grant.authorize_ungranted_send(granting_config, lane="enrichment",
    object_type="companies", record_ids=[RECORD_ID], record_domains=[], allow_create=False,
    label="this send", transport=<the fresh instance>)`, assert `decision["armed"] is True`,
    open `armed_window(decision["workflow_id"], [RECORD_ID], [], False, granting_config,
    transport=<the same fresh instance>, grant=decision["grant"])`, and inside its body call
    `dispatch.dispatch(str(sample_csv), True, granting_config, transport=stub_transport)` again
    (the SAME `stub_transport` fixture instance is safe to reuse across both branches here,
    unlike the module-shaped one -- it has no scripted `responses` list, so it is not a
    draining queue; it just always answers the default accepted body and appends to `.calls`).
    Assert the window armed/disarmed and `len(stub_transport.calls) == 2` at the end (one call
    per branch, on the one shared `stub_transport`).

    Fixture parameters this new test needs: `granting_config`, `stub_module_transport_factory`,
    `stub_transport`, `sample_csv` (all already defined in `conftest.py` / this file).

    Give the test a short docstring naming the two registry entries it closes and citing the
    honesty rule: neither branch's `armed_window` body is left as `pass` any more.

    In `test_skill_sequence_coverage.py`: find the `GRANDFATHERED_UNCOVERED` entry whose key
    skill is `"contact-upload"` (reason text starting "the authorize_ungranted_send ->
    armed_window join IS driven") and the entry whose key skill is `"enrich-before-ingest"`
    with the IDENTICAL call tuple (reason text starting "same tuple as contact-upload's block").
    Cut BOTH entries out of `GRANDFATHERED_UNCOVERED` verbatim (do not retype the tuples --
    move the exact key) and paste them into `COVERED`, each mapped to the new test's nodeid:
    `"test_write_grant.py::<new_test_name>"`. Decrement `MAX_GRANDFATHERED` from `5` to `3`.

    Bump `operator-claude-plugin/.claude-plugin/plugin.json`'s `version` from `"0.28.3"` to
    `"0.28.4"` and add a matching `## [0.28.4] - 2026-08-29` section to
    `operator-claude-plugin/CHANGELOG.md` (under the still-empty `## [Unreleased]` heading)
    describing the closed entries, in the SAME commit as the test + registry edit.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_skill_sequence_coverage.py -q</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>

  <acceptance_criteria>
    1. The new test function fails if either `with armed_window(...):` body is reverted to
       `pass` -- verify this by temporarily reverting one branch's body to `pass` and confirming
       the test fails (missing `result`/`result2` reference or the call-count assertion), then
       restoring it. This is the concrete proof the "verify would fail on independently-called
       units" bar is met -- record the observed failure text in the task's SUMMARY.
    2. `len(GRANDFATHERED_UNCOVERED) == 3` and `MAX_GRANDFATHERED == 3` after this task.
    3. Both new `COVERED` entries' identities are BYTE-IDENTICAL to the tuples they were cut
       from (no retyped tuple) — verified by `test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink`
       and `test_no_new_or_orphaned_sequence_exists_in_the_live_corpus` passing unmodified.
    4. `plugin.json` reads `"version": "0.28.4"` and `CHANGELOG.md` has a matching `0.28.4`
       entry, landed in the same commit as the test + registry edit.
    5. Zero files under `operator-claude-plugin/scripts/`, `skills/`, `n8n/`, or repo-root
       `scripts/` are touched.
  </acceptance_criteria>

  <done>
    A single new test in `test_write_grant.py` drives the grant-present `authorize_send`
    branch and the ungranted `authorize_ungranted_send` branch, each ending in its OWN
    `armed_window`-scoped `dispatch.dispatch` call with a real result asserted on. Both
    `GRANDFATHERED_UNCOVERED` entries sharing this call tuple are now `COVERED` by that one
    nodeid; `MAX_GRANDFATHERED` is `3`.
  </done>
</task>

<task type="auto">
  <name>Task 2: close #3 (enrich-before-ingest waterfall + merge_enriched) and #7 (enrich-records waterfall) — two new tests in test_chunking.py</name>

  <read_first>
    - `operator-claude-plugin/tests/test_chunking.py:1-130` -- existing imports, `ids()`/`spec()`
      helpers, `CONFIG_EXAMPLE` path constant (already defined here), and the ceiling tests.
    - `operator-claude-plugin/tests/test_chunking.py:806-868` -- `PROVIDERS` module constant and
      `test_enrichment_and_contacts_writes_from_the_same_run_share_one_file`, the nearest
      existing precedent for `dispatch_plan` with a real stub transport -- it bypasses
      `resolve_providers`/`chunk_ceiling`/authorize/`armed_window` entirely, which is exactly
      the registry's named gap for #7.
    - `operator-claude-plugin/tests/test_write_grant.py:28-59, 94-97` -- `WORKFLOW_ID`,
      `_base_workflow(...)`, `_armed_workflow(...)`, `_workflow_list()`, and the
      `granting_config` fixture. These must be DUPLICATED (not cross-imported -- sibling test
      modules importing each other is fragile under pytest's default import mode) into
      `test_chunking.py`, verbatim, since this file needs the identical arming scaffolding
      Task 1 used. `_plan_reads`/`_proposal`/`_open` do NOT need duplicating -- inline the two
      `write_grant.plan_grant(...)` + `write_grant.open_grant(...)` calls directly where noted
      below.
    - `operator-claude-plugin/scripts/enrichment.py:133-175` -- `resolve_providers(override,
      config)`, pure, `override=None` reads `config[DEFAULT_PROVIDER_SELECTION_KEY]` or
      defaults to `FULL_WATERFALL` (a real non-empty list either way).
    - `operator-claude-plugin/scripts/enrichment.py:229-300, 480-496` -- `build_envelope`'s
      `rows` branch (`{"providers": [...], "mode": "propose", "events": [{"row_id":...,
      ...match keys...}]}`) and `record_ids` branch (`{"providers": [...], "events":
      [{"objectId": str(id), "objectType": object_type}, ...]}`) -- what actually lands in the
      stub transport's recorded POST body; assert against these shapes to prove the real
      chunked spec and the real resolved provider list both flowed through.
    - `operator-claude-plugin/scripts/chunking.py:155-178, 181-275, 315-417` --
      `chunk_ceiling(config, key=CEILING_KEY)` (no fallback, raises on a missing/bad key),
      `plan_chunks(spec, ceiling)` (pure), `dispatch_plan(plan, providers, armed, config,
      transport=requests, *, run_id=None)` -- MODULE-shaped transport (wrapped internally by
      `_StatusCapturingTransport`, calls `.post`), returns `DispatchOutcome(results,
      failed_batch, responses, run_id, written_records_failures)`. `.responses` is ONE RAW
      BODY PER CHUNK.
    - `operator-claude-plugin/config/operator.local.example.json:24` -- `"max_records_per_chunk":
      2"` -- do not load this whole file (its `n8n_url`/`webhook_secret` are placeholder
      strings); merge just this one real integer value into the fixture config, e.g.
      `cfg = {**granting_config, "max_records_per_chunk": 2}`, so `chunking.chunk_ceiling(cfg)`
      returns the REAL configured value rather than a literal passed straight to `plan_chunks`.
    - `operator-claude-plugin/scripts/preingest.py:51-84` -- `build_rows_spec(rows) ->
      {"rows": [{...,"row_id": "row-N"}], "object_type": "contacts"}`.
    - `operator-claude-plugin/scripts/preingest.py:483-591` -- `merge_enriched(rows,
      responses)`. `responses` must ALREADY be flat (one dict per row) -- flatten
      `dispatch_plan(...).responses` first with:
      `[item for body in outcome.responses for item in (body if isinstance(body, list) else [body])]`.
      A non-flat item raises `MergeError`.
    - `operator-claude-plugin/tests/test_preingest_merge.py:32-37` -- the `_response(row_id,
      properties=None)` shape to script as the `dispatch_plan` transport's POST response body
      (a JSON list of these dicts, one per row in the chunk).
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:228-243, 276-291` -- the
      two `GRANDFATHERED_UNCOVERED` entries this task closes and their reason text. Neither
      reason separately calls out both authorize branches as individually gapped (unlike
      Task 1's entries) -- each just says "no test chains ... end to end" as one gap, so ONE
      authorize branch per test honestly closes each entry (per the honesty rule: close the
      SPECIFIC gap the reason names, do not invent a stricter unstated bar). Use the
      grant-present `authorize_send` branch for #3 and the `authorize_ungranted_send` branch
      for #7 -- deliberate diversity, not required by either reason individually.
  </read_first>

  <files>
    operator-claude-plugin/tests/test_chunking.py,
    operator-claude-plugin/tests/test_skill_sequence_coverage.py,
    operator-claude-plugin/.claude-plugin/plugin.json,
    operator-claude-plugin/CHANGELOG.md
  </files>

  <action>
    At the top of `test_chunking.py`, add `import n8n_arming`, `import write_grant`,
    `import preingest`, and `import config_gate` (none of these six names —
    `n8n_arming`/`write_grant`/`preingest`/`config_gate`/`WORKFLOW_ID`/`RECORD_ID` — currently
    exist in this file; confirmed by grep before writing this plan, so this is additive, not a
    rename). Duplicate, verbatim, from `test_write_grant.py`: the module constants
    `WORKFLOW_ID = "wf-enrichment-1"` and `RECORD_ID = "12345"`, the `_base_workflow(record_writes=...,
    create=..., ids=..., domains=...)` and `_armed_workflow(ids=...)` helper functions, the
    `_workflow_list()` helper, and a local `granting_config` fixture built off `fake_config`
    exactly as `test_write_grant.py`'s is (`{**fake_config, config_gate.WRITE_GRANT_SETTINGS_KEY:
    True}`).

    Add test function 1 (closes #3, `enrich-before-ingest`): build a two-row rows spec via
    `preingest.build_rows_spec([{"firstname": "First0", ...}, {"firstname": "First1", ...}])`,
    a config `cfg = {**granting_config, "max_records_per_chunk": 2}`, resolve
    `providers = enrichment.resolve_providers(None, cfg)` (the REAL waterfall, not a literal
    list -- unlike `PROVIDERS` used elsewhere in this file), compute
    `ceiling = chunking.chunk_ceiling(cfg)` and `plan = chunking.plan_chunks(row_spec,
    ceiling)` (2 rows at ceiling 2 -> exactly 1 chunk). Open a standing grant inline: `proposal
    = write_grant.plan_grant(granting_config, lanes=["enrichment"], object_type="companies",
    record_ids=[RECORD_ID], record_domains=[], allow_create=False, label="test",
    transport=<a stub_module_transport_factory scripted with the 14-entry open+arm+disarm
    sequence duplicated from Task 1's read_first>)` then `grant = write_grant.open_grant(proposal,
    "yes", granting_config)`, then `decision = write_grant.authorize_send(grant,
    lane="enrichment", record_ids=[RECORD_ID], record_domains=[])`, assert
    `decision["armed"] is True`. Open `n8n_arming.armed_window(decision["workflow_id"],
    [RECORD_ID], [], False, granting_config, transport=<the SAME 14-entry transport instance>,
    grant=decision["grant"])` and inside its body call `outcome = chunking.dispatch_plan(plan,
    providers, True, cfg, transport=<a SEPARATE, freshly-built stub_module_transport_factory
    scripted with exactly ONE response: a JSON list of two `_response(row_id, properties)`-shaped
    dicts, one per row_id in `row_spec["rows"]`, each carrying a distinct `properties` value
    e.g. `{"email": f"{row_id}@example.com"}`>)`. After the `with` block, flatten
    `outcome.responses` with the idiom above and call `merge_report =
    preingest.merge_enriched(row_spec["rows"], flattened)`. Assert the merged row for EACH
    `row_id` carries exactly the scripted `email` value (proving the specific scripted value
    genuinely flowed dispatch_plan -> flatten -> merge_enriched, not a hand-built response), and
    assert `len(dispatch_transport.calls) == 1` (module-shaped transport's own `.calls` list).

    Add test function 2 (closes #7, `enrich-records`): spec `{"record_ids": ["100", "200"],
    "object_type": "companies"}`, same `cfg`/`ceiling`/`plan` pattern (record_ids-shaped this
    time, per Gotcha 4 -- `enrich-records` never uses a rows spec since it targets records
    already in HubSpot), `providers = enrichment.resolve_providers(None, cfg)`. Call
    `decision = write_grant.authorize_ungranted_send(granting_config, lane="enrichment",
    object_type="companies", record_ids=[RECORD_ID], record_domains=[], allow_create=False,
    label="this send", transport=<a fresh 14-entry stub_module_transport_factory instance>)`,
    assert `decision["armed"] is True`, open `armed_window(decision["workflow_id"],
    [RECORD_ID], [], False, granting_config, transport=<the same 14-entry instance>,
    grant=decision["grant"])` and inside its body call `outcome = chunking.dispatch_plan(plan,
    providers, True, cfg, transport=<a SEPARATE fresh stub_module_transport_factory with no
    scripted responses -- default accepted body is fine, this test only needs to prove the
    request shape, not a merge>)`. After the block, assert on the transport's OWN
    `.calls[0]["json"]` (the module-shaped stub's recorded POST kwargs): `["events"] ==
    [{"objectId": "100", "objectType": "companies"}, {"objectId": "200", "objectType":
    "companies"}]` (proving `plan_chunks`'s real chunked record_ids reached the wire) and
    `["providers"] == providers` (proving `resolve_providers`'s real return, not a literal,
    reached the wire). No `merge_enriched` call in this test -- #7's tuple omits it.

    In `test_skill_sequence_coverage.py`: cut the `GRANDFATHERED_UNCOVERED` entry keyed
    `("enrich-before-ingest", (... "preingest.merge_enriched"))` (reason starting "no test
    chains resolve_providers -> plan_chunks/chunk_ceiling ...") and the entry keyed
    `("enrich-records", (...))` (reason starting "no test chains resolve_providers,
    chunk_ceiling, ..."), verbatim, into `COVERED`, mapped respectively to
    `"test_chunking.py::<test_function_1_name>"` and `"test_chunking.py::<test_function_2_name>"`.
    Decrement `MAX_GRANDFATHERED` from `3` to `1`.

    Bump `plugin.json`'s `version` from `"0.28.4"` to `"0.28.5"` and add a matching
    `## [0.28.5] - 2026-08-29` CHANGELOG entry, in the SAME commit.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py operator-claude-plugin/tests/test_skill_sequence_coverage.py -q</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>

  <acceptance_criteria>
    1. Test 1 fails if the `dispatch_plan` transport's scripted `email` value is changed to a
       different string without also changing the assertion -- confirm this by temporarily
       editing the scripted value and observing the merge assertion fail, then restoring it;
       this is the concrete proof of real dataflow (record the observed failure in SUMMARY).
    2. Test 2's `["providers"]` assertion fails if `resolve_providers`'s call is replaced with
       a literal list that happens to differ from the real resolved default -- confirm by
       temporarily hardcoding `providers = ["lusha"]` (assuming the real default differs) and
       observing the assertion fail, then restoring the real call.
    3. `len(GRANDFATHERED_UNCOVERED) == 1` and `MAX_GRANDFATHERED == 1` after this task.
    4. `test_registries_have_no_orphaned_keys`, `test_the_three_registries_are_pairwise_disjoint`,
       and `test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink`
       all pass unmodified.
    5. `plugin.json` reads `"0.28.5"` with a matching CHANGELOG entry in the same commit.
  </acceptance_criteria>

  <done>
    Two new tests in `test_chunking.py` each chain `resolve_providers -> plan_chunks/
    chunk_ceiling -> authorize(_send|_ungranted_send) -> armed_window -> dispatch_plan`, one
    continuing into `merge_enriched` with the scripted response value provably flowing through.
    Both `GRANDFATHERED_UNCOVERED` entries for this waterfall are now `COVERED`;
    `MAX_GRANDFATHERED` is `1`.
  </done>
</task>

<task type="auto">
  <name>Task 3: close #2 (enrich-before-ingest match ceiling) — the real match-ceiling return flowing into match_batch/classify_matches</name>

  <read_first>
    - `operator-claude-plugin/tests/test_chunking.py:106-118` --
      `test_chunk_ceiling_reads_the_match_key_and_it_is_larger_than_the_write_ceiling`. LEAVE
      THIS TEST UNCHANGED -- it is the isolated-ceiling unit test the registry's own reason
      cites as insufficient BY ITSELF; the new composition test in this task is ADDITIVE,
      placed immediately after it, not a replacement or an edit to its body or assertions.
    - `operator-claude-plugin/tests/test_run_manifest.py:435-473` --
      `test_a_resume_re_requests_only_rows_that_still_needed_work`, the near-miss the registry
      names: it calls `chunking.plan_chunks(spec, ceiling=5)` with a LITERAL `5` at line 441,
      never `chunk_ceiling`'s real return, and it uses the WRITE ceiling shape, not the match
      key. Also LEAVE THIS TEST UNCHANGED -- Task 3 adds a new, separate test rather than
      editing this one in place, so none of its existing assertions are touched or put at risk.
    - `operator-claude-plugin/tests/test_run_manifest.py:415-422` -- `_match_item(row_id, tier,
      hs_object_id=None)` helper shape -- duplicate this (6 lines) into `test_chunking.py`.
    - `operator-claude-plugin/scripts/chunking.py:155-178` -- `chunk_ceiling(config,
      key=CEILING_KEY)`; passing `key="max_rows_per_match_request"` reads that key with the
      identical no-fallback rule, naming that key (not `max_records_per_chunk`) in any
      refusal.
    - `operator-claude-plugin/config/operator.local.example.json:28` --
      `"max_rows_per_match_request": 20` -- merge just this real integer into `fake_config`
      (e.g. `cfg = {**fake_config, "max_rows_per_match_request": <value read from
      json.loads(CONFIG_EXAMPLE.read_text())>}`), same pattern as Task 2, so the ceiling
      actually read is the REAL configured value, never a literal `20` typed into the test.
    - `operator-claude-plugin/scripts/preingest.py:181-233` -- `match_batch(plan, config,
      transport=requests.post)`. ATTRIBUTE-shaped transport (bare callable, `fetch_matches`'s
      own contract) -- use `stub_post_transport_factory`, NOT `stub_module_transport_factory`
      (Gotcha: mixing the two transport families raises `TypeError`).
    - `operator-claude-plugin/scripts/preingest.py:236-334` -- `classify_matches(rows,
      response, unchecked_row_ids=None) -> {"auto_matched", "proposed", "unmatched",
      "unchecked", "unknown_response_row_ids"}`. `response` (i.e. `outcome.responses`) is
      ALREADY flat here -- `match_batch` does its own per-chunk extend -- no flatten idiom
      needed, unlike Task 2's `dispatch_plan`/`merge_enriched` pairing.
    - `operator-claude-plugin/scripts/preingest.py:51-84` -- `build_rows_spec(rows)`, mints
      `row_id` as `"row-1"`, `"row-2"`, ... The spec fed to `plan_chunks` for this chain MUST
      be the `"rows"` shape (never `"record_ids"`) -- `match_batch` reads `row["row_id"]` off
      `chunk.get("rows", [])`.
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py:228-243` -- the last
      remaining `GRANDFATHERED_UNCOVERED` entry (skill `"enrich-before-ingest"`, tuple ending
      in `preingest.classify_matches`) and its reason text.
  </read_first>

  <files>
    operator-claude-plugin/tests/test_chunking.py,
    operator-claude-plugin/tests/test_skill_sequence_coverage.py,
    operator-claude-plugin/.claude-plugin/plugin.json,
    operator-claude-plugin/CHANGELOG.md
  </files>

  <action>
    In `test_chunking.py`, add a local `_match_item(row_id, tier, hs_object_id=None)` helper
    duplicated from `test_run_manifest.py:415-422`. Add ONE new test, placed immediately after
    `test_chunk_ceiling_reads_the_match_key_and_it_is_larger_than_the_write_ceiling`, that: reads
    `real_match_ceiling = json.loads(CONFIG_EXAMPLE.read_text())["max_rows_per_match_request"]`
    (using the `CONFIG_EXAMPLE` constant already defined at the top of this file), builds
    `cfg = {**fake_config, "max_rows_per_match_request": real_match_ceiling}`, builds a
    three-row spec via `preingest.build_rows_spec([...three plain dict rows...])`, computes
    `ceiling = chunking.chunk_ceiling(cfg, key="max_rows_per_match_request")` and asserts
    `ceiling == real_match_ceiling` (proving the value came from the real config path, not a
    hardcoded `20`), then `plan = chunking.plan_chunks(row_spec, ceiling)` (3 rows well under a
    ceiling of 20 -> exactly 1 chunk). Script a `stub_post_transport_factory(responses=[[
    _match_item("row-1", "high", hs_object_id="111"), _match_item("row-2", "none"),
    _match_item("row-3", "medium")]])` and call `outcome = preingest.match_batch(plan, cfg,
    transport=<that stub>)`. Call `classified = preingest.classify_matches(row_spec["rows"],
    outcome.responses, unchecked_row_ids=outcome.unchecked_row_ids)`. Assert
    `{entry["row_id"] for entry in classified["auto_matched"]} == {"row-1"}`,
    `{entry["row_id"] for entry in classified["unmatched"]} == {"row-2"}`, and
    `{entry["row_id"] for entry in classified["proposed"]} == {"row-3"}` -- this three-way
    tier split is what proves `match_batch`'s real response and `plan_chunks`'s real
    ceiling-derived chunking both reached `classify_matches` correctly, not merely that the
    two functions were each called once.

    In `test_skill_sequence_coverage.py`: cut the one remaining `GRANDFATHERED_UNCOVERED`
    entry (skill `"enrich-before-ingest"`, tuple `config_gate.load_config,
    chunking.plan_chunks, chunking.chunk_ceiling, preingest.match_batch,
    preingest.classify_matches`) verbatim into `COVERED`, mapped to
    `"test_chunking.py::<new_test_name>"`. Set `GRANDFATHERED_UNCOVERED = {}` (an empty dict,
    the literal end state) and `MAX_GRANDFATHERED = 0`.

    Bump `plugin.json`'s `version` from `"0.28.5"` to `"0.28.6"` and add a matching
    `## [0.28.6] - 2026-08-29` CHANGELOG entry noting all five grandfathered entries are now
    closed, in the SAME commit.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py operator-claude-plugin/tests/test_skill_sequence_coverage.py -q</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>

  <acceptance_criteria>
    1. `test_chunk_ceiling_reads_the_match_key_and_it_is_larger_than_the_write_ceiling` and
       `test_a_resume_re_requests_only_rows_that_still_needed_work` are BYTE-IDENTICAL to
       their pre-task state -- `git diff` on `test_run_manifest.py` shows no changes at all,
       and `git diff` on `test_chunking.py` shows only additive hunks (no removed or modified
       existing line).
    2. Source-level, not behavioral (a hardcoded `ceiling=20` would coincidentally still pass
       today, since the real value IS 20 -- a runtime break-and-restore check proves nothing
       here). Instead: `grep -nE "plan_chunks\(row_spec, *[0-9]+\)" operator-claude-plugin/tests/test_chunking.py`
       must match NOTHING for the new test -- the only value `plan_chunks` is called with for
       this test is the `ceiling` variable, itself assigned from
       `chunking.chunk_ceiling(cfg, key="max_rows_per_match_request")`'s return, and
       `real_match_ceiling` is read via `json.loads(CONFIG_EXAMPLE.read_text())` at runtime,
       never typed as a literal `20` anywhere in the new test's body. Confirm both by reading
       the committed test source, and record the grep's (empty) output in SUMMARY.
    3. `GRANDFATHERED_UNCOVERED == {}` and `MAX_GRANDFATHERED == 0`.
    4. `test_grandfathered_count_is_within_its_shrink_only_ceiling`,
       `test_the_three_registries_are_pairwise_disjoint`,
       `test_no_new_or_orphaned_sequence_exists_in_the_live_corpus`, and
       `test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink` all
       pass unmodified.
    5. `plugin.json` reads `"0.28.6"` with a matching CHANGELOG entry in the same commit.
    6. Full suite: `.venv/bin/python -m pytest operator-claude-plugin/tests -q` reports
       1721 + 4 = **1725 passed / 5 skipped, 0 failed** (1 test from Task 1, 2 from Task 2, 1
       from Task 3 — adjust the expected count only if Task 1/2 added a different number of
       test functions than planned, and note the actual count in SUMMARY).
  </acceptance_criteria>

  <done>
    `GRANDFATHERED_UNCOVERED` is empty and `MAX_GRANDFATHERED` is `0`. All five originally
    grandfathered composition gaps are now `COVERED` by tests that drive their named join with
    real, provably-flowing data -- verified by the falsifiability checks recorded in each
    task's acceptance criteria, not merely by the mechanical staleness guard.
  </done>
</task>

</tasks>

<verification>
Run after Task 3 (the full end state):

```
.venv/bin/python -m pytest operator-claude-plugin/tests -q
```
Expected: 1721 baseline + however many test functions the three tasks actually added
(planned: 4 — 1 + 2 + 1), 5 skipped, **0 failed**. Never bare `python -m pytest`.

```
.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q -v
```
Expected: every test in this file passes, including
`test_no_new_or_orphaned_sequence_exists_in_the_live_corpus`,
`test_registries_have_no_orphaned_keys`, `test_the_three_registries_are_pairwise_disjoint`,
`test_grandfathered_count_is_within_its_shrink_only_ceiling` (now `0 <= 0`), and
`test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink`.

```
grep -c "GRANDFATHERED_UNCOVERED = {}" operator-claude-plugin/tests/test_skill_sequence_coverage.py
grep -n "MAX_GRANDFATHERED = 0" operator-claude-plugin/tests/test_skill_sequence_coverage.py
```
Both must be non-empty/present.

```
.venv/bin/python -m pytest -q
```
Root suite. Baseline 3328 passed / 154 skipped; expect the same +4 delta if root collection
includes the plugin tests (verify once with
`.venv/bin/python -m pytest --collect-only -q | grep -c test_skill_sequence_coverage`,
non-zero means it does) — a moved count from the plugin tests added here is expected, not a
regression.

```
node --test tests/n8n/*.test.mjs
```
Expected: unchanged pass count — no file under `n8n/` or `tests/n8n/` is touched by this task.

Git: confirm three separate commits exist (one per task), each touching only the files named
in that task's `<files>`, each with `plugin.json` and `CHANGELOG.md` changed alongside the
test/registry edit.
</verification>

<success_criteria>
- `GRANDFATHERED_UNCOVERED` is the empty dict and `MAX_GRANDFATHERED` is `0`.
- All five originally-grandfathered identities have a `COVERED` entry pointing at a real test
  in the plugin suite, and each such test's own assertions would fail if the named join's
  result did not actually flow from the earlier call into the later one (demonstrated per-task
  via the temporary-break-and-restore checks in each task's acceptance criteria).
- No existing test's assertions were weakened, deleted, or reworded anywhere in the suite.
- No live n8n/HubSpot/Anthropic/provider call occurred at any point — stub transports only,
  `no_network`/`no_durable_writes` untouched.
- No `skills/*/SKILL.md` file was edited.
- Three commits, each bumping `plugin.json` and adding a `CHANGELOG.md` entry in the same
  commit: `0.28.3 -> 0.28.4 -> 0.28.5 -> 0.28.6`.
</success_criteria>

<output>
Create `.planning/quick/260829-lg3-close-the-five-grandfathered-skill-md-co/260829-lg3-SUMMARY.md`
when done, recording: the actual new test function names and their nodeids as registered in
`COVERED`, the falsifiability-check results demanded by each task's acceptance criteria (the
observed failure text when a join was temporarily broken, then confirmation it was restored),
and the final full-suite pass count.
</output>
