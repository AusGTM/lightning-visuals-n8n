---
phase: quick-260911-ao1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_cloud_workflows.py
  - tests/n8n/mergeInputContract.test.mjs
  - .planning/todos/completed/2026-09-11-merge-input-contract-allows-many-producers-per-input.md   # NEW (git mv target)
  - .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md            # NEW
files_deleted:
  - .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md     # git mv -> completed/
autonomous: true
requirements: [MIC-01, MIC-02, MIC-03]   # quick-task-local handles, NOT ROADMAP requirement IDs.
                                         # MIC-01 -> Task 1 (rule 5 + tolerant allowlist in the builder,
                                         #   RED first, zero JSON diff).
                                         # MIC-02 -> Task 2 (rule 5 mirrored in mergeInputContract.test.mjs
                                         #   with an exact-both-directions census, RED first).
                                         # MIC-03 -> Task 3 (todo closed; MN-01 + NF-MJ-01 carried forward
                                         #   still open).
                                         # All three trace to
                                         # .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md
                                         # § "Operator ruling 2026-09-11".

estimate:
  tokens: 70000
  raw_tokens: 70000        # factor 1.0, applied: false, sample_count 0 (estimate-calibration)
  tasks: 3
  confidence: low          # derived from sample_count 0, not self-rated

must_haves:
  truths:
    - "`assert_merge_input_contract` refuses to generate a workflow containing a Merge input with more than one producer edge unless that Merge is named in an explicit tolerant-allowlist carrying a reason string."
    - "The allowlist is keyed on the pair (workflow BODY name, Merge node name) — the keying precedent `_SELF_DISPATCH_EXEMPTIONS` set (WR-08) — so a Merge name borrowed by another workflow does not inherit the tolerance."
    - "`Decide Company Action Merge` is on the allowlist with the operator's own 2026-09-11 reason: the consumer filters markers, so only marker-only runs multi-fire."
    - "`Collect Credits` is admitted by the same allowlist, its reason citing the mutual-exclusivity finding already proven GREEN by `tests/n8n/creditsSummaryUnderV1.test.mjs`."
    - "Rule 5 is provably live: with the allowlist emptied the builder raises, and a synthetic two-producer Merge whose name is off the list raises too."
    - "`tests/n8n/mergeInputContract.test.mjs` mirrors rule 5 and its allowlist over every committed `n8n/wf_*.json`, and asserts the mirrored list is EXACTLY the census of multi-producer Merges in both directions."
    - "Regenerating every `n8n/wf_*.json` through the builder's own entrypoint produces zero diff — no graph change, nothing deployed, nothing armed."
    - "MN-01 and NF-MJ-01 remain open, carried forward into their own pending todo, not closed by this task."
  artifacts:
    - scripts/build_cloud_workflows.py
    - tests/n8n/mergeInputContract.test.mjs
    - .planning/todos/completed/2026-09-11-merge-input-contract-allows-many-producers-per-input.md
    - .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md
  key_links:
    - "rule 5 in `assert_merge_input_contract` <-> the mirrored rule in `structuralViolations` in mergeInputContract.test.mjs — the same 16 (workflow, Merge) pairs on both sides"
    - "`_assert_generation_contracts` -> `assert_merge_input_contract` -> every `main()` write site (composition point UNCHANGED)"
---

<objective>
Close the operator's Option A ruling on
`.planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md`:
`assert_merge_input_contract` gains rule 5 — a Merge input with more than one producer edge
is a build-time violation unless the Merge is named in an explicit tolerant-allowlist
carrying a reason string.

Purpose: freeze today's multi-producer census. Under n8n v1 a Merge input fed by two producers
that are not mutually exclusive can open a second pending run that the end-of-run drain fires
(observed live on `Decide Company Action Merge`, executions 12354/12355/12356). The builder
cannot prove exclusivity statically, so the ruling is: refuse by default, admit by name with a
written reason. Any NEW multi-producer input on a Merge that is not on the list stops
generation.

Output: rule 5 + the allowlist constant in `scripts/build_cloud_workflows.py`, the same rule
mirrored in `tests/n8n/mergeInputContract.test.mjs`, all eight `n8n/wf_*.json` regenerated with
ZERO diff, and the todo closed with MN-01/NF-MJ-01 carried forward still open.

Scope fence: the ONLY function this task may edit in `scripts/build_cloud_workflows.py` is
`assert_merge_input_contract` (plus the new module-level allowlist constant beside it). Sibling
batch item 260911-anv edits the same file's Judge Gate jsCode — do not touch any node builder,
any jsCode string, any connection, or `main()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md
@tests/n8n/mergeInputContract.test.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: rule 5 + tolerant allowlist in the builder, RED first, zero JSON diff (MIC-01)</name>
  <files>scripts/build_cloud_workflows.py</files>
  <read_first>
    - `scripts/build_cloud_workflows.py` lines ~11224-11300: `_MERGE_INPUT_MAX`,
      `_MERGE_INPUT_SENTINEL_RE`, `assert_merge_input_contract` (rules 1-4; its single pass over
      `wf["connections"]` already visits every producer edge into every Merge input and builds
      `fed_inputs` — extend that same pass).
    - `scripts/build_cloud_workflows.py` lines ~11300-11312: `_SELF_DISPATCH_EXEMPTIONS` — copy
      its keying discipline verbatim (keyed on the workflow BODY's own `wf["name"]`, never the
      caller-supplied file label, WR-08 2026-09-10) and its comment style.
    - `scripts/build_cloud_workflows.py` `_assert_generation_contracts` and `main()` — read only
      to confirm the `name` argument threaded through them is a FILE LABEL (`"wf_enrichment_cloud"`),
      which is exactly why the allowlist keys on `wf["name"]` instead. Both stay UNCHANGED.
  </read_first>
  <action>
Add a module-level constant beside `_SELF_DISPATCH_EXEMPTIONS`, named
`_MERGE_MULTI_PRODUCER_TOLERANT`: a dict mapping the tuple `(workflow_body_name,
merge_node_name)` to a reason string. Key on `wf.get("name")` for the same stated reason
`_SELF_DISPATCH_EXEMPTIONS` gives — a Merge name borrowed by another build must not inherit
the tolerance.

Extend `assert_merge_input_contract` with rule 5: for every Merge, for every declared input
index, if that input has more than one producer edge and the pair `(wf.get("name"),
merge_name)` is absent from `_MERGE_MULTI_PRODUCER_TOLERANT`, append a violation naming the
workflow, the Merge, the input index and every producer feeding it, sorted, so the message
tells the reader exactly what has to be justified. Reuse the existing connections pass: change
`fed_inputs`'s value type from a set of indices to a mapping index -> list of producer names so
rules 4 and 5 both read it, and keep rule 4's "no producer at all" message byte-identical.
Tolerance is Merge-level, per the ruling's wording ("the Merge is named"), not per-input.

Update the docstring: replace the "Deliberately NOT enforced here: that every Merge input has
exactly ONE producer" paragraph with rule 5's description, stating that the builder cannot prove
exclusivity statically and therefore refuses by default and admits by written reason, and citing
the operator ruling of 2026-09-11 recorded in the todo.

RED FIRST, and capture it in a form that shows the WHOLE census: `main()` raises on the first
violating workflow and stops, so it would only ever show you two Merges. With
`_MERGE_MULTI_PRODUCER_TOLERANT` empty, call `assert_merge_input_contract` directly in a loop
over all eight committed `n8n/wf_*.json`, collecting each `ValueError`. Importing the module has
no side effects (`main()` is `__name__`-guarded), but it DOES need `scripts/` on `sys.path` —
`sys.path.insert(0, "scripts")` before `exec_module`, or its `import gen_taxonomy_js` fails. Quote that combined output verbatim
in the SUMMARY. It is the proof rule 5 bites, and it is the authority for the census below. Only
then populate the allowlist.

Populate with EXACTLY the 16 pairs the RED run reports. This census was taken from the committed
JSON on 2026-09-11 — reconcile it against the RED output, do not trust this list blind:
  ("LV Contact Ingest (Cloud template)", "Build Association Request Merge")
  ("LV Contact Ingest (Cloud template)", "Ingest Merge Response")
  ("LV Enrichment (Cloud template)", "Build Response Merge Stage 1")
  ("LV Enrichment (Cloud template)", "Build Response Merge Stage 2")
  ("LV Enrichment (Cloud template)", "Build Response Merge Stage 3")
  ("LV Enrichment (Cloud template)", "Collect Credits")
  ("LV Enrichment (Cloud template)", "Company Gate Merge")
  ("LV Enrichment (Cloud template)", "Decide Company Action Merge")
  ("LV Enrichment (Cloud template)", "Enrichment Gate Merge")
  ("LV Enrichment (Cloud template)", "Merge Company Fan-In")
  ("LV Enrichment (Cloud template)", "Merge Winners Fan-In")
  ("LV Enrichment (local LIVE)", "Merge Company Fan-In")
  ("LV Enrichment (local LIVE)", "Merge Winners Fan-In")
  ("LV Review Decision (Cloud)", "Build Review Response Merge")
  ("LV Review Decision (Cloud)", "Review Extract Record Merge")
  ("LV Review Decision (Cloud)", "Review Queue Rows Merge")

Reason strings — three families, and NONE may invent a safety claim this repo has not
established (self-authored acceptance rationales are forbidden):
  a. `Decide Company Action Merge` — the operator's own 2026-09-11 wording: the consumer filters
     markers, so only marker-only runs multi-fire; observed live on executions 12354/12355/12356
     and pinned by `tests/n8n/walkerEngineFidelityV1.test.mjs`.
  b. `Collect Credits` — the todo's own finding: the two producers sharing each input are
     mutually exclusive by construction (a provider is either enabled or not, so exactly one of
     the pair ever runs), proven GREEN by `tests/n8n/creditsSummaryUnderV1.test.mjs`.
  c. the other fourteen — state the STRUCTURAL fact and the EVIDENCE STATUS, nothing more:
     D-70-23 gated sentinels share each input with the real producer, and the entry is admitted
     by the 2026-09-11 census under the operator's Option A ruling, which is NOT a proof of
     safety. Be precise about what evidence exists per lane rather than claiming none exists
     anywhere: the enrichment-lane Merges have v1 recordings frozen at
     `tests/n8n/fixtures/frozen/exec_1235{4,5,6}.runData.json` and the ingest-lane Merges at
     `exec_1235{7,8}` — cite those ids and say no recording in this repo shows THAT Merge
     multi-firing; only the review-decision and local-live entries have no recording at all, and
     those say so. Do not write "safe", "harmless" or "proven" for any of the fourteen.

Then regenerate every workflow through the builder's own entrypoint and prove zero diff. The
JSON must not change: this is an assertion-only change.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && echo ZERO_JSON_DIFF && .venv/bin/python -c "
import sys; sys.path.insert(0, 'scripts')
import importlib.util as u
s = u.spec_from_file_location('bcw', 'scripts/build_cloud_workflows.py')
m = u.module_from_spec(s); s.loader.exec_module(m)
assert len(m._MERGE_MULTI_PRODUCER_TOLERANT) == 16, len(m._MERGE_MULTI_PRODUCER_TOLERANT)
wf = {'name': 'Synthetic Not On The List',
      'nodes': [{'name': 'M', 'type': 'n8n-nodes-base.merge', 'parameters': {'numberInputs': 2}},
                {'name': 'A', 'type': 'n8n-nodes-base.noOp'},
                {'name': 'B', 'type': 'n8n-nodes-base.noOp'},
                {'name': 'C', 'type': 'n8n-nodes-base.noOp'}],
      'connections': {'A': {'main': [[{'node': 'M', 'index': 0}]]},
                      'B': {'main': [[{'node': 'M', 'index': 0}]]},
                      'C': {'main': [[{'node': 'M', 'index': 1}]]}}}
try:
    m.assert_merge_input_contract(wf, 'synthetic')
except ValueError as e:
    assert 'M' in str(e), e
    print('RULE5_FIRES_ON_SYNTHETIC')
else:
    raise SystemExit('RULE5_DID_NOT_FIRE')
"</automated>
  </verify>
  <done>The builder runs clean and rewrites all eight `n8n/wf_*.json` byte-identically (`git diff --quiet -- n8n/` exits 0); the allowlist holds exactly 16 entries; a synthetic Merge with two producers into input 0 whose workflow/Merge pair is off the list raises `ValueError`. With the allowlist emptied, a direct loop over all eight committed JSONs raises for every violating workflow, and that combined RED output is quoted in the SUMMARY. No node builder, jsCode string, connection or `main()` line was edited.</done>
</task>

<task type="auto">
  <name>Task 2: mirror rule 5 in mergeInputContract.test.mjs, RED first (MIC-02)</name>
  <files>tests/n8n/mergeInputContract.test.mjs</files>
  <read_first>
    - `tests/n8n/mergeInputContract.test.mjs` in full — especially the header block's
      "Deliberately NOT asserted" paragraph (lines ~37-47), which this task supersedes and must
      REPLACE rather than leave contradicting the new rule; `structuralViolations`; the `PENDING`
      exact-both-directions test, which is the pattern the new census test copies.
    - The `WALKER_TRIGGER_TYPES` comment in the same file — the house precedent for mirroring a
      constant that cannot be imported, and the phrasing to reuse.
  </read_first>
  <action>
Mirror the builder's rule 5. Add a `MULTI_PRODUCER_TOLERANT` structure keyed on the same pair
(`wf.name`, Merge node name) with the same reason strings as Task 1's Python constant, and a
comment saying it mirrors `_MERGE_MULTI_PRODUCER_TOLERANT` in `scripts/build_cloud_workflows.py`
(not importable across languages, so mirrored — the same reason the file already gives for
`WALKER_TRIGGER_TYPES`).

Give `structuralViolations` a fifth returned bucket, `multiProducer`, computed from the same
connections walk it already does: an input with more than one producer whose Merge is not in the
mirrored allowlist. Include it in `isEmpty`. `PENDING` stays `[]` and its exact-both-directions
test stays untouched — with the allowlist correctly populated every committed workflow still
satisfies the contract.

Add one new test asserting the mirrored allowlist is EXACTLY the census of (workflow name, Merge
name) pairs that actually have a multi-producer input across every committed `n8n/wf_*.json`, in
BOTH directions — an entry for a Merge that no longer has a shared input fails as loudly as a
missing one. This is what keeps the two mirrored lists from rotting without a cross-language
parser: adding an edge without an entry fails here, removing an entry without removing the edge
fails in the builder.

RED FIRST: run the file with `MULTI_PRODUCER_TOLERANT` empty and confirm it fails, listing the
multi-producer inputs. Quote that failure in the SUMMARY. Then populate.

Rewrite the header paragraph that currently instructs future readers NOT to tighten this file
into a one-producer-per-input rule: replace it with what rule 5 now does, why the tolerance is an
explicit named list rather than a static exclusivity prover (the builder cannot tell a safe share
from an unsafe one — only a replay can), and the operator ruling of 2026-09-11 that decided it.
Leave the four existing rule descriptions above it as they are.
  </action>
  <verify>
    <automated>node --test tests/n8n/mergeInputContract.test.mjs && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>`mergeInputContract.test.mjs` passes with the new `multiProducer` bucket and the census test, and the full n8n node suite passes (glob form — the directory form is broken on node 24), `creditsSummaryUnderV1.test.mjs` and `walkerEngineFidelityV1.test.mjs` included and unmodified. With `MULTI_PRODUCER_TOLERANT` emptied the file fails listing the multi-producer inputs, and that RED output is quoted in the SUMMARY. The header no longer tells readers not to assert this rule.</done>
</task>

<task type="auto">
  <name>Task 3: close the todo, carry MN-01 and NF-MJ-01 forward still open (MIC-03)</name>
  <files>.planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md, .planning/todos/completed/2026-09-11-merge-input-contract-allows-many-producers-per-input.md, .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md</files>
  <read_first>
    - The todo's "Open question (MN-01...)" and "Open question (NF-MJ-01...)" sections — both are
      carried forward VERBATIM, not summarised.
    - `.planning/todos/completed/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md`
      frontmatter — the shape a completed todo carries, and the precedent for a completed todo
      that spawned this one's open questions.
  </read_first>
  <action>
Create `.planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md`
carrying BOTH still-open questions verbatim: MN-01 (does the engine drain a SECOND pending run of
the same Merge at end of run?) and NF-MJ-01 (the walker's BL-02 grouping rule is consistent with
12354-12356 but was not isolated by them). Frontmatter in the house shape: `created`,
`updated: 2026-09-11`, a title naming both, `area: n8n-tests`, `severity: major`, `files:` the
walker and its test. Add one line at the top saying these were carried out of the
merge-input-contract todo when quick task 260911-ao1 closed it, and name where each is already
pinned (`tests/n8n/walkWorkflow.test.mjs`).

Then move the merge-input-contract todo from `pending/` to `completed/` with `git mv`, appending
a "Resolved 2026-09-11 (quick task 260911-ao1)" section recording: rule 5 shipped in
`assert_merge_input_contract` with a 16-entry tolerant allowlist keyed on (workflow body name,
Merge name); mirrored in `tests/n8n/mergeInputContract.test.mjs` with an exact-both-directions
census test; zero `n8n/` JSON diff, no graph change, nothing deployed, nothing armed; fourteen of
the sixteen entries admitted by census with multi-fire unobserved on that Merge, two with cited
evidence; MN-01 and NF-MJ-01 NOT closed, carried into the new pending todo by name.
  </action>
  <verify>
    <automated>test ! -e .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md && ls .planning/todos/completed/2026-09-11-merge-input-contract-allows-many-producers-per-input.md && /usr/bin/grep -q 'MN-01' .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md && /usr/bin/grep -q 'NF-MJ-01' .planning/todos/pending/2026-09-11-merge-multi-run-drain-and-grouping-unobserved.md && echo TODO_CARRIED</automated>
  </verify>
  <done>The merge-input-contract todo lives in `completed/` with a Resolved section; a new pending todo carries MN-01 and NF-MJ-01 forward verbatim and neither is marked resolved anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator -> committed `n8n/wf_*.json` | the only thing this task changes is a refusal; a wrong refusal blocks builds, a missing refusal ships a multi-firing Merge |
| committed JSON -> live n8n Cloud | NOT crossed by this task: no deploy, no bounce, no arming |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ao1-01 | Tampering | `n8n/wf_*.json` regenerated by the builder | high | mitigate | `git diff --quiet -- n8n/` in Task 1's verify: any graph change at all fails the task. Committed and live are level as of Gate 10/11/12 — an unnoticed diff would silently desync them. |
| T-ao1-02 | Tampering | sibling batch item 260911-anv edits the same file | medium | mitigate | Scope fence in the objective: only `assert_merge_input_contract` and the new constant. Task 1's `done` requires no jsCode/connection/`main()` edit. |
| T-ao1-03 | Repudiation | allowlist reason strings | medium | mitigate | Task 1 forbids "safe"/"harmless"/"proven" for the fourteen census entries; each states structural fact plus the frozen recordings that exist for its lane, so a later reader cannot mistake census admission for a safety proof. |
| T-ao1-04 | Spoofing | a Merge name reused in another workflow | medium | mitigate | Allowlist keyed on the (workflow BODY name, Merge name) PAIR, following `_SELF_DISPATCH_EXEMPTIONS`/WR-08 — a borrowed name inherits nothing. |
| T-ao1-05 | Denial of Service | future builds | low | accept | Rule 5 is default-refuse: a legitimate new multi-producer Merge stops generation until someone writes a reason. That is the ruling's intent. |
| T-ao1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this task — nothing to audit. |
</threat_model>

<verification>
1. `.venv/bin/python scripts/build_cloud_workflows.py` exits 0 and `git diff --quiet -- n8n/` exits 0.
2. Rule 5 is proven live, not merely present: the allowlist holds 16 entries and a synthetic off-list two-producer Merge raises.
3. `node --test tests/n8n/*.test.mjs` passes in full (`creditsSummaryUnderV1.test.mjs`, `walkerEngineFidelityV1.test.mjs` and `walkWorkflow.test.mjs` all stay green — this task changes no walker behaviour).
4. Both RED observations (builder looped over all eight JSONs with an empty allowlist; test file with an empty mirrored list) are quoted in the SUMMARY.
5. `git diff --stat` shows only `scripts/build_cloud_workflows.py`, `tests/n8n/mergeInputContract.test.mjs` and the three `.planning/todos/` paths.
6. Nothing deployed, nothing bounced, nothing armed.
</verification>

<success_criteria>
- Rule 5 refuses a multi-producer Merge input by default and admits by named, reasoned exemption.
- The same 16 pairs appear on both sides of the Python/JS mirror, and a census test pins them exactly in both directions.
- Zero `n8n/` JSON diff.
- MN-01 and NF-MJ-01 are still open, in their own pending todo.
</success_criteria>

<output>
Create `.planning/quick/260911-ao1-todo-2026-09-11-merge-input-contract-allows-many-producers-p/260911-ao1-SUMMARY.md` when done, quoting both RED outputs.
</output>
