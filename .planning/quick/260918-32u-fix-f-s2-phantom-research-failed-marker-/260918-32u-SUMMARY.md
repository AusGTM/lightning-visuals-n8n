---
phase: quick-260918-32u
plan: 01
subsystem: n8n-enrichment
tags: [f-s2, phantom-row, sentinel-marker, always-output-data, phase-73-uat]
requires:
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
provides:
  - "zero research_failed rows on a research-succeeded batch"
affects:
  - operator-claude-plugin/scripts/written_records.py (consumer — unchanged, no longer fed phantoms)
tech-stack:
  added: []
  patterns:
    - "reserved SENTINEL_MARKER_KEY via .replace(\"__SENTINEL_MARKER_KEY__\", …) on the r-string"
key-files:
  created:
    - .planning/todos/pending/2026-09-18-racing-clubs-researched-produces-content-false.md
  modified:
    - scripts/build_cloud_workflows.py
    - n8n/wf_enrichment_cloud.json
    - tests/n8n/researchErrorGateFlow.test.mjs
    - .planning/todos/completed/2026-09-17-phantom-research-failed-marker-row.md
    - .planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md
decisions:
  - "The todo's fence-parse premise is FALSE — verified live, no parser or parity change made"
  - "alwaysOutputData on IF Research Errored retained; only the marker's shape changed"
metrics:
  duration: ~25m
  completed: 2026-09-18
status: complete
actuals:
  tokens: 6000
  tasks: 2
  commits: 2
plan_head_before: 79be82fab989c88fe27b734006ae11653b3bc343
---

# Quick Task 260918-32u: Fix F-S2 — the phantom `research_failed` marker row

The empty-input case of `Build Research Failure Response` now emits the reserved sentinel
marker instead of stamping a fabricated `action: "research_failed"` outcome, so a batch where
every research call succeeds returns zero failure rows to the caller.

## What happened

**Task 1 (tracer, TDD) — commit `a3f2dc51`.** Two tests added to
`tests/n8n/researchErrorGateFlow.test.mjs` and **seen RED first** (`_gsd_sentinel_marker`
undefined; phantom survived the filter 1 !== 0). Then one guard inside the
`Build Research Failure Response` jsCode in `scripts/build_cloud_workflows.py` (~:7583): a
zero-key `merged` returns `{ json: { _gsd_sentinel_marker: true } }`, the key injected by
`.replace("__SENTINEL_MARKER_KEY__", SENTINEL_MARKER_KEY)` — the same idiom as :786/:799,
never a hand-typed literal. Regenerated with `.venv/bin/python scripts/build_cloud_workflows.py`.

Blast radius proved node-by-name against `git show HEAD:n8n/wf_enrichment_cloud.json`, not by
eyeballing the raw diff:

- 287 nodes before and after, `settings.executionOrder: "v1"`, node-name set equal,
  `connections` byte-equal.
- **Exactly one node's `parameters` differ: `Build Research Failure Response`.**
- `wf_enrichment_cloud.json` is the only `n8n/wf_*.json` that changed (it is also the only one
  containing the node — `/usr/bin/grep -l` confirms). No sibling leak.
- The `code_node` rewrite (`_OLD_MARKER_FILTER_JS` → `_NEW_MARKER_FILTER_JS`) did **not** touch
  the new `Object.keys(merged).length === 0` guard — confirmed in the emitted jsCode.

No edit at `Filter Build Response Rows` or `Build Response` was needed: that rewrite already
makes every emitted Code node's filter drop the reserved key. `set_always_output_data(nodes,
["IF Research Errored"])` is untouched — removing it would starve `Build Response Merge
Stage 2` input 1 under v1. No pre-existing test went red at any point.

**Task 2 — commit `6437ce75`.** The F-S2 record corrected in all four places it was stated:
the defect todo (rewritten in place, `git mv`'d to
`.planning/todos/completed/2026-09-17-phantom-research-failed-marker-row.md`, `src/web_research.py`
dropped from `files:` as never implicated), the `73-UAT.md` findings row, the § Stage B bullet,
and the Verdict phrase. `git diff` on that file is **exactly three hunks**. One new
`kind: question` todo opened for the research-quality residual.

## Deviations from Plan

**1. [Process — staging slip, no code defect] Task 2's first commit landed with only the
staged rename.** The `git add`
listed the pre-`git mv` pending path, which no longer existed; `git add` fails the whole
invocation on a bad pathspec, so nothing else staged and the commit captured a 0-change
rename. Caught immediately by `git diff --cached --stat`, restaged by correct path and
`--amend`ed. Final `6437ce75` carries all four paths (132 insertions / 50 deletions) and the
two trailer lines. No bad commit was pushed.

**2. [Rule 1 – Bug] The new todo's `title:` broke `todo_triage.py`.** It contains
`lv_produces_content: false`, which unquoted YAML reads as a nested mapping —
`yaml.scanner.ScannerError`, not a validation failure. Quoted the title; `--check` exits 0.

## Verification

| Check | Result |
| --- | --- |
| `node --test tests/n8n/*.test.mjs` | 1218 pass / 0 fail |
| `.venv/bin/python -m pytest tests/test_cloud_companies_branch.py tests/test_remaining_credits_response.py -q` | 48 passed |
| `.venv/bin/python -m pytest tests/test_todo_triage.py -q` | 2 passed |
| `.venv/bin/python scripts/todo_triage.py --check` | exit 0 |
| Tracer feedback gate (Task 1 `<verify>` re-run post-regeneration) | green, expanded |

Both pre-existing `Build Research Failure Response` tests (the pre-HTTP row recovery and the
fail-closed case) pass **unmodified** — `LIVE_ERROR_PAYLOAD` has one key, not zero, so the new
guard cannot reach it. No existing test in the file was edited.

Final `git status --porcelain -- n8n/ scripts/ .planning/todos/ .planning/phases/` is empty.
The tree's other dirt (`.gitignore`, `tests/demo-run/*`, the untracked CSV/sh files, this
plan's own directory) belongs to another process and was never staged.

## Task 3 — notes only, nothing acted on

1. **The committed `n8n/wf_enrichment_cloud.json` is now AHEAD of the live n8n Cloud
   instance.** Deploy + bounce is the operator's step and is never performed from Claude. No
   live n8n or HubSpot call of any kind was made here; nothing armed, nothing deployed.
2. **`73-UAT.md` and the moved todo are both in `73-VERIFICATION.md`'s `covered_files`, so the
   covered digest is now stale.** Deliberately **not** resealed — the phase-close tail reseals
   once. **Read directly from `73-VERIFICATION.md:6` rather than assumed:** `covered_files`
   lists `73-UAT.md` and the **old pending path**
   `.planning/todos/pending/2026-09-17-web-research-validator-rejects-fenced-json.md`, which no
   longer exists. The reseal must therefore **repoint that entry** to
   `.planning/todos/completed/2026-09-17-phantom-research-failed-marker-row.md` before
   recomputing, or `computeCoveredDigest` will hit a missing file. `scripts/build_cloud_workflows.py`
   and `n8n/wf_enrichment_cloud.json` are also in the list and also changed.
3. The sibling quick task **260918-322** regenerates the same workflow set; this task
   regenerated on top of its committed state (HEAD `79be82fa`, plugin 0.50.1), and only the
   enrichment body moved.
4. **F-S6's "tied to F-S2's research failure" attribution in `73-UAT.md` (~:242) is now
   stale** — a different finding's reasoning, deliberately left untouched per the plan.

## Known Stubs

None.

## Self-Check: PASSED

- `n8n/wf_enrichment_cloud.json` — FOUND (287 nodes, `executionOrder: v1`)
- `.planning/todos/completed/2026-09-17-phantom-research-failed-marker-row.md` — FOUND
- `.planning/todos/pending/2026-09-18-racing-clubs-researched-produces-content-false.md` — FOUND
- `.planning/todos/pending/2026-09-17-web-research-validator-rejects-fenced-json.md` — ABSENT (expected)
- Commit `a3f2dc51` — FOUND
- Commit `6437ce75` — FOUND
