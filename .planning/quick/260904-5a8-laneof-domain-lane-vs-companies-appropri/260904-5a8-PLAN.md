---
phase: quick-260904-5a8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/code/matchProposal.js
  - scripts/build_cloud_workflows.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - n8n/wf_review_decision_cloud.json
  - tests/n8n/matchProposal.test.mjs
  - .planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md
autonomous: true
requirements: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A companies row's response `match.reason` names how a company identity is actually resolved (domain, then exact name) instead of listing contacts identity keys."
    - "Every contacts row's `match` verdict is byte-identical to today's — same tier, same auto, same reason string, for every one of the five existing lane values."
    - "No company row's `match.tier` changes value, so no downstream confidence/hold verdict changes."
    - "62-11-DIAGNOSIS.md's remaining-exposure list names the ad-hoc run-0 read trap and records execution 12103 as the second live occurrence."
  artifacts:
    - n8n/code/matchProposal.js
    - scripts/build_cloud_workflows.py
    - n8n/wf_enrichment_cloud.json
    - tests/n8n/matchProposal.test.mjs
    - .planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md
  key_links:
    - "`ENRICH_DECIDE_CO_CLOUD`'s `match:` line is the SINGLE emission site of a companies match verdict — `Build Response` only mentions summarizeMatch in a comment."
    - "`n8n/wf_*.json` is generated; the change reaches the workflow only via scripts/build_cloud_workflows.py."
---

<objective>
Fix one false sentence and write one note.

A companies row's response body says `match.reason: "no searchable identity — the row has
no email, object id, or name+company pair"`. That is false about a company that has a
perfectly good domain, and it is contacts vocabulary on a companies lane. It sent today's
investigation down the wrong path (a claimed `Merge Company` row-drop that does not
exist). It has been recorded and left unfixed twice before —
`47.5-02-SUMMARY.md` ("Recorded, not fixed") and `58-SPIKE-VERDICT.md`.

Purpose: stop the message from misdirecting the next investigator, and close the
run-0 reader trap's documentation gap in the same sitting.
Output: one new `summarizeMatch` arm, one changed call-site line, regenerated `n8n/*.json`,
two tests, and a note in 62-11-DIAGNOSIS.md.

**OFFLINE ONLY.** No HubSpot read or write, no Anthropic call, no provider credit, no n8n
deploy, no arming.
</objective>

<premise_corrections>
Three premises in the task brief were checked against the source this session and are
WRONG. The brief pre-authorized taking the other option if that happened. It has happened;
this plan takes it. Do not re-derive from the brief's version.

**C1 — `laneOf` is NEVER called on the companies branch, so adding a `domain` lane to it
fixes nothing observable.** Only `Build Identity` (contacts) stamps `lane`
(`scripts/build_cloud_workflows.py:1278`). `Build Company Identity`
(`build_cloud_workflows.py:2124-2141`) stamps `identity_keys` and no `lane`. Walked every
node reachable from `Build Company Identity` in the committed
`n8n/wf_enrichment_cloud.json` connections (44 nodes) — not one assigns `lane`. Therefore
`row.lane` is `undefined` at `Decide Company Action`, and its
`summarizeMatch({ lane: row.lane })` (`build_cloud_workflows.py:3490`) hits the final
fallthrough by way of `undefined`, not by way of `"none"`. `Adapt Company Search`
(`build_cloud_workflows.py:2166-2203`) sets no `match` either, so `row.match ??` never
short-circuits.

**C2 — `lane` IS a routing key.** The brief says it is consumed only by `summarizeMatch`.
It is not. In the committed `n8n/wf_enrichment_cloud.json`: `IF Has Email`,
`IF Name Searchable` and `IF Linkedin Searchable` each test
`$('Build Identity').item.json.lane === "<literal>"`, and `Adapt Search`,
`Adapt Fetch By Id`, `Adapt Name Search` and `Adapt Linkedin Search` each `.filter()` on
the same field. The module's own docstring warning about a row routed to one lane and
filtered into another is live, not hypothetical. This plan adds no lane value to any row,
so the warning is not engaged — but do not repeat the false premise.

**C3 — contacts rows DO carry `identity_keys.domain`.** `build_cloud_workflows.py:1263`
sets `domain: row.domain || (email ? email.split("@")[1] : null)`. `resolveIdentity.js`
having no `domain` says nothing about `Build Identity`, which is a different function. The
brief's "no contact row can reach a new domain arm" was never provable.

**Consequence:** no `laneOf` change. The fix lands at the one place the false sentence is
emitted.
</premise_corrections>

<gating_boundary>
**`match.tier` gates. `match.reason` does not.** `preingest.py:96-114` parses any response
item's `match.tier` into an `Outcome`; `confidence.py:133-151` then turns tier `"unknown"`
into a HOLD and a non-unknown tier into a different verdict; `held_queue.py:155-163`
fingerprints on `match_tier`.

Live company responses carry `match.tier: "unknown"` today — directly observed in
`47.5-A-LIVE-PROOF.md:224`, `48-DEPLOY-PROOF.md:295`, `49-DEPLOY-PROOF.md:108` and
`58-SPIKE-VERDICT.md:26`. **The new arm keeps `tier: "unknown"` and `auto: false`, and does
NOT read `existingRecord`.** Reporting only. Any change that made a companies row report
`tier: "high"` would flip a hold to a send — that is the gating this must not start.
</gating_boundary>

<context>
@.planning/STATE.md
@n8n/code/matchProposal.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One arm, one call-site literal, regenerate, pin both directions</name>
  <files>n8n/code/matchProposal.js, scripts/build_cloud_workflows.py, tests/n8n/matchProposal.test.mjs, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local.json, n8n/wf_enrichment_local_live.json, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json, n8n/wf_review_decision_cloud.json</files>
  <read_first>n8n/code/matchProposal.js:174-217 (summarizeMatch, whole function); scripts/build_cloud_workflows.py:3341-3346 and :3482-3494 (ENRICH_DECIDE_CO_CLOUD's inline list and its `match:` line); tests/n8n/matchProposal.test.mjs:358-419 (the existing summarizeMatch pins, including :407-412 which passes lane "none" explicitly)</read_first>
  <behavior>
    - `summarizeMatch({ lane: "company" })` → `{ tier: "unknown", auto: false, candidates: [] }` and a reason naming domain (and exact company name) resolution.
    - That same reason must NOT contain "email", "object id" or "name+company" — it is the contacts vocabulary the defect was made of.
    - `summarizeMatch({ lane: "company", lookupFailed: true })` still returns the lookupFailed verdict — the failure arm runs first and is unchanged.
    - `summarizeMatch({ lane: "none" })` is byte-identical to today, reason string included.
    - The built `Decide Company Action` jsCode contains the company-lane literal call.
  </behavior>
  <action>
Do NOT touch `laneOf`. Do NOT add a lane value to any row. See premise_corrections C1-C3
for why.

**(a) `n8n/code/matchProposal.js`** — add ONE arm to `summarizeMatch`, placed after the
`name` arm and before the final `lane "none"` fallthrough (position is cosmetic; the arms
are mutually exclusive equality tests). Keep `tier: "unknown"`, `auto: false`,
`candidates: []` per gating_boundary — the companies lane resolves identity in HubSpot
nodes upstream and this module genuinely did not look, so "we could not look" is the
correct tier, not a hedge. The reason sentence should say the company identity resolves by
domain, then exact company name, upstream, and point the reader at the row's own
`hs_object_id`/`action` for the outcome. Add a short comment recording that `"company"` is
only ever a CALL-SITE LITERAL: `laneOf` never returns it and no row ever carries it, so it
cannot reach the four `lane ===` routing IFs and adapters named in C2. Update
`summarizeMatch`'s own header comment line to note the lane values it now accepts.

While in the fallthrough's comment: it currently reads "lane `none` (or anything
unrecognized)". Leave the RETURNED STRING alone (a test and four archived proof documents
quote it) — this arm is now reached by `undefined` only in practice, and saying so in the
comment is free.

**(b) `scripts/build_cloud_workflows.py`** — in `ENRICH_DECIDE_CO_CLOUD` (constant starts
:3341), change the `match:` line at :3490 from `row.match ?? summarizeMatch({ lane:
row.lane })` to pass the company literal instead of `row.lane`. Keep the `row.match ??`
guard. Do not pass `existingRecord` or `lookup_failed`. Leave `ENRICH_DECIDE_CLOUD`'s
contacts twin at :1738 completely alone — `row.lane` there is always a real `laneOf`
string.

Also at :3457-3462 there is a comment reading "the companies branch has no match lane
(Task 1's is contacts-only), so no companies row can ever carry a medium tier". Its
conclusion stays true (this arm never returns `medium`) but its premise is now half-stale.
Amend it in place to say the companies branch's match verdict is a fixed non-medium
company-lane summary, so there is still nothing to demote.

**(c) Regenerate.** `n8n/*.json` is GENERATED — never hand-edit.

FIRST, before editing anything: run `scripts/build_cloud_workflows.py` on the clean tree
and confirm `git diff --stat -- n8n/` is EMPTY. A pre-existing diff means builder/JSON
drift — stop and report it, do not fold it into this commit. (62-11 and 62-12 both closed
at zero `n8n/` diff, so this should pass.)

Then make the edits, re-run the builder, and commit every workflow JSON it rewrites.

**Expected diff shape — read this before you panic at the size.** `matchProposal.js` is
inlined verbatim into every node that uses it, so a one-line module edit rewrites the
`jsCode` of ~10 nodes in `wf_enrichment_cloud.json` alone (`Parse HubSpot Event`,
`Build Identity`, `Adapt Search`, `Enrichment Gate`, `Adapt Fetch By Id`,
`Adapt Name Search`, `Adapt Linkedin Search`, `Decide Action`, `Decide Company Action`).
Commit whatever the builder actually rewrites — expected to be the three enrichment
variants (`wf_enrichment_cloud`, `wf_enrichment_local`, `wf_enrichment_local_live`) only.
**A zero diff on `wf_contact_ingest_*.json` and `wf_review_decision_cloud.json` is
expected, not a failure** — a `lane ===` scan across all five workflows this session found
matchProposal's body in the enrichment variants only. That is mechanical and expected.
What must NOT change: `wf_enrichment_cloud.json` stays at **123 nodes and 112 connection
keys**, and only ONE node — `Decide Company Action` — gains the call-site literal. Do not
hand-trim the diff.

**(d) Tests** in `tests/n8n/matchProposal.test.mjs`, alongside the existing summarizeMatch
pins. The brief asked for both directions; with no new `laneOf` lane the two directions
are:
  - *New behaviour:* the three `behavior` assertions above, including the explicit
    negative that the company reason does not carry the contacts vocabulary.
  - *Unchanged behaviour:* the existing lane pins at :358-419 already cover every one of
    the five `laneOf` values and the `"none"` reason string. They must pass UNMODIFIED —
    name that as the evidence rather than duplicating them. Add nothing that edits an
    existing assertion.

Add one built-code assertion that `Decide Company Action`'s emitted jsCode carries the
company-lane literal call. `tests/test_enrichment_lane_dedup.py:88,111` is the precedent
for asserting substrings in built node code — put it wherever matches that file's existing
convention.

**(e) Parity + blast-radius check (do this, report the answer, do not skip).** Grep
`tests/n8n/`, `tests/`, and `operator-claude-plugin/tests/` for anything pinning
`summarizeMatch`'s output for a companies row, and confirm no Python module mirrors
`summarizeMatch` (CLAUDE.md Phase 46 parity rule). Expected answer, stated here so a
DIFFERENT answer is a stop-and-report: `laneOf` is the only mirrored piece
(`operator-claude-plugin/tests/test_rows_envelope_contract.py:12-20`) and it is untouched;
`report_enrichment.py:198-203` displays whatever `reason` arrives without inspecting it.
Reporting-only, no shared predicate, no second engine, no `src/icp_scoring.py` change.

**No plugin change and no version bump.** `operator-claude-plugin/` needs no code edit —
say so explicitly in the commit body. Do NOT touch
`operator-claude-plugin/.claude-plugin/plugin.json` or its CHANGELOG; the orchestrator cuts
any release.

**Deployment state.** This commits regenerated JSON WITHOUT deploying (offline constraint),
so the committed JSON is again ahead of the live n8n instance. Note that in the commit body
using CLAUDE.md §13.0.2's amendment wording as the precedent. Do not deploy, do not bounce,
do not arm.
  </action>
  <verify>
    <automated>node --test tests/n8n/*.test.mjs</automated>
    <automated>python3 -c "import json;d=json.load(open('n8n/wf_enrichment_cloud.json'));assert len(d['nodes'])==123,len(d['nodes']);assert len(d['connections'])==112,len(d['connections']);c=[n['name'] for n in d['nodes'] if 'summarizeMatch({ lane: row.lane })' in (n['parameters'].get('jsCode') or '')];assert c==['Decide Action'],c;print('ok',c)"</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
    <automated>cd operator-claude-plugin && python -m pytest tests/ -q</automated>
  </verify>
  <done>
`node --test tests/n8n/*.test.mjs` is green at 867 + the new assertions, 0 fail. Root
pytest and the plugin suite are at or above their stated baselines (1747 passed/149
skipped; 2365 passed/5 skipped) with zero new failures — record the actual numbers. The
node-count check prints exactly one remaining `row.lane` caller, `Decide Action` (the
contacts twin); `Decide Company Action` is absent from that list (the command asserts it, and fails if
not). No file under
`operator-claude-plugin/` is modified. Nothing deployed, nothing armed, zero network calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Note the run-0 reader trap in 62-11-DIAGNOSIS.md's remaining-exposure list</name>
  <files>.planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md</files>
  <read_first>.planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md:198-225 (the "Remaining exposure" section — all four existing bullets); operator-claude-plugin/scripts/report.py:81-105 (`all_node_items`, the helper the note points at)</read_first>
  <action>
Amend the `## Remaining exposure` section. This is a documentation-only task — no code, no
test, no regenerated artifact.

**Add one bullet** naming the trap concretely:
  - The shape: a hand-rolled `runData[node][0]["data"]["main"][0]` read — indexing run 0
    directly instead of concatenating every run.
  - The fix: `operator-claude-plugin/scripts/report.py::all_node_items(run_data,
    node_name)`, which concatenates every run in order and tolerates an absent node, a
    non-list run collection and a malformed run entry as `[]`.
  - The occurrence: an ad-hoc probe on **execution 12103** (2026-09-04), three executions
    after `12096`/`12098`. This is the **second** live occurrence of the same trap. Say
    plainly what it cost: the probe read `Merge Company` run 0, saw 1 of 2 items, and the
    resulting report claimed `Merge Company` was collapsing two null-id company rows and
    silently dropping a record. It is not — `Merge Company` is a single
    `$input.all().map(...)` with no filter, group, Set, Map or slice, so it structurally
    cannot emit fewer items than it receives. An investigation was spent on a node that had
    done nothing wrong. The reader defect is the whole cost of the trap; state it that way.

**Upgrade the existing `Merge Company` bullet** (currently at :208-213). It says "this plan
has no live evidence it ever splits". Execution 12103 IS that evidence: `Merge Company` ran
more than once and run 0 carried a strict subset. Rewrite the bullet to record that the
structural mirror is now an OBSERVED split, cite 12103, and keep the existing note that
`enrichment_row_ledger`'s fix already covers it (it reads by node name, not by lane). Keep
the bullet in place — do not move or renumber the others.

Leave every other bullet, and every other section of the file, untouched. Do NOT edit
`62-UAT.md` or `62-VERIFICATION.md` — both were finalized today.

**No executable guard.** A repo-wide scan for hand-rolled run-0 indexing was considered and
is declined, with the cost named: `report.contact_row_ledger` and
`report._write_node_items` are SHIPPED `runs[0]` readers that 62-11 deliberately left
untouched (named in this same section), so any such scan flags them on day one and needs an
allowlist — which is the linting framework the operator ruled out. The note stands alone.
Record that one-sentence rationale in the plan's commit body, not in the diagnosis file.
  </action>
  <verify>
    <automated>grep -c '12103' .planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md</automated>
    <automated>grep -c 'all_node_items' .planning/phases/62-suggest-the-contacts-nobody-named/62-11-DIAGNOSIS.md</automated>
    <automated>test -z "$(git diff --name-only -- .planning/phases/62-suggest-the-contacts-nobody-named/ | grep -v 62-11-DIAGNOSIS.md)"</automated>
  </verify>
  <done>
`## Remaining exposure` carries a new bullet naming the run-0 idiom, `all_node_items` as
the replacement, and execution 12103 as the second occurrence with the false `Merge
Company` conclusion it produced. The pre-existing `Merge Company` bullet no longer claims
there is no live evidence of a split. The first two greps return non-zero counts; the third
confirms no other file under `.planning/phases/62-*/` was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n → operator plugin (response body) | The only surface this plan changes: a `match.reason` STRING inside the response envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5a8-01 | Elevation of Privilege | `summarizeMatch` company arm → `confidence.assess` | high | mitigate | The arm keeps `tier: "unknown"`/`auto: false` and never reads `existingRecord` — the live-observed value, so no hold-to-send flip is possible. Pinned by Task 1's behaviour assertions. See gating_boundary. |
| T-5a8-02 | Tampering | regenerated `n8n/wf_*.json` | medium | mitigate | Node count 123 / connection keys 112 asserted in `<verify>`; only one node gains the literal. Regenerated by the builder, never hand-edited. |
| T-5a8-03 | Information Disclosure | new reason string | low | accept | Static prose naming resolution strategy — no record data, no identity value, no credential is interpolated into it. |

No package-manager install task exists in this plan, so no legitimacy gate applies.
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` — 867 baseline + new, 0 fail (glob form; directory form is broken on node 24).
- `.venv/bin/python -m pytest -q` — at or above 1747 passed / 149 skipped.
- `cd operator-claude-plugin && python -m pytest tests/ -q` — at or above 2365 passed / 5 skipped. (`cd` persists between bash calls in this harness.)
- `git status` shows no modified file under `operator-claude-plugin/`.
- Zero network calls of any kind across both tasks.
</verification>

<success_criteria>
A companies row's `match.reason` names domain-then-name resolution instead of contacts
identity keys; every contacts lane verdict including the `"none"` reason string is
unchanged; no `match.tier` value changes anywhere; the regenerated workflow JSON is
committed at 123 nodes; and 62-11-DIAGNOSIS.md records both the run-0 trap's second
occurrence and the now-observed `Merge Company` split.
</success_criteria>

<output>
Create `.planning/quick/260904-5a8-laneof-domain-lane-vs-companies-appropri/260904-5a8-SUMMARY.md` when done.
Record in it: the three corrected premises (C1-C3) with the file:line evidence, the actual
suite numbers, the parity-check answer from Task 1(e), and the explicit statement that no
plugin code changed and no version was bumped.
</output>
