---
phase: quick-260911-w6r
plan: 01
type: execute
wave: 1
depends_on:
  - 260911-w6o
  - 260911-w6p
  - 260911-w6q
files_modified:
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
  - operator-claude-plugin/tests/test_held_facet_render_composition.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
  - .planning/todos/completed/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md
files_deleted:
  - .planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md
autonomous: true
requirements: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`enrich-before-ingest/SKILL.md` step 6 states in its own words that it asks the operator NOTHING about held rows — the UAT F4 shape (`how do you want to handle batch 1's two held rows?`) is named as the rejected behaviour, so a later reader cannot restore it as helpfulness."
    - "Step 6 renders this run's held rows through the facet classifier 260911-w6p shipped on `held_queue.py` — new-people rows named individually with what the waterfall revealed, needs-a-company rows named separately, and nothing-found rows carried as one parked count line, never as a per-person block."
    - "Step 6 carries ONE ready answer, on one line, offering BOTH routes and defaulting to neither: a count-restating `create all N` reply in this conversation, or the same work later in `/operator-claude-plugin:review-triage`. `create all` never appears bare — always with a trailing count or named scope, the same rule step 3's `approve all` already obeys."
    - "The run NEVER blocks on that answer: step 6 says plainly that silence is a valid outcome, the batch proceeds to step 7 and the end-of-run report regardless, and the held rows stay in the queue for a later sitting."
    - "An in-conversation `create all N` dispatches through review-triage's OWN create block — quoted BY HEADING TEXT, never reproduced — so the association rule and the create fences have exactly one implementation, in `review-triage/SKILL.md`, the same by-reference discipline this skill already applies to `contact-upload` steps 6-10."
    - "Every review-triage heading this skill quotes actually exists in `skills/review-triage/SKILL.md` — asserted against the target file, not just against this one, so a stale quote fails a test instead of sending a reader to a heading that is gone."
    - "The create runs under the STANDING grant opened at step 5 and never widens it: if `write_grant.covers()` refuses the row (the F8 shape — an enriched domain the step-2 confirmation table never named), the skill relays the refusal exactly as review-triage step 7 relays `grant_not_authorized` and routes the operator to the second route, where review-triage opens its own review-lane grant over exactly those records. No widening, no re-scoping, no second standing grant."
    - "The step-5 sentences that describe a review pass positioned after the report step, and the paragraph recording the open gap resolved by this batch, are corrected — the skill no longer promises a pass that does not exist, and no longer points at a todo this batch closes."
    - "Step 9's end-of-run text restates the same facet counts and the same one-line ready answer the operator saw at step 6, so a batch read only at the end still carries the route to work its held rows."
    - "Any new documented call sequence of two-or-more scripts-module calls added to a SKILL.md python fence is registered in `test_skill_sequence_coverage.py` — `COVERED` pointing at a real test whose own source names the sequence's sink, or a reasoned `NOT_A_PIPELINE` entry. `MAX_GRANDFATHERED` does not rise."
    - "`operator-claude-plugin/.claude-plugin/plugin.json` reads `0.47.0` and `CHANGELOG.md` carries ONE `## [0.47.0]` section for that release, naming quick batch `260911-w6n`, all four items `260911-w6o`/`260911-w6p`/`260911-w6q`/`260911-w6r` and the operator ruling of 2026-09-11 — written from the three sibling SUMMARY files, never from this plan's guess at what they shipped."
    - "Nothing under `operator-claude-plugin/scripts/` and nothing under `n8n/` is modified by this plan — the facet classifier and the create path are the siblings' code; this item ships prose, tests and the release."
    - "No live HubSpot, n8n or provider call is made, and nothing is armed."
  artifacts:
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
    - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
    - operator-claude-plugin/tests/test_skill_sequence_coverage.py
    - operator-claude-plugin/.claude-plugin/plugin.json
    - operator-claude-plugin/CHANGELOG.md
  key_links:
    - "step 5's `held_queue.build_entry` persist (260911-w6o's enriched row) -> step 6's facet read (260911-w6p's classifier) -> step 6's rendered new-people block — the reveal that reached disk is the reveal the operator reads."
    - "step 6's `create all N` reply -> review-triage's create block, quoted by heading -> the contact-ingest lane under the standing grant — ONE create implementation (260911-w6q's), referenced, never copied."
    - "`write_grant.covers()` <-> step 2's confirmed-domain table <-> step 6's refusal relay — the grant's scope is fixed at open time and the second route, not a widening, is the answer to a row it does not name."
    - "`test_skill_sequence_coverage.py::test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink` <-> whichever test claims the new facet-read sequence — the registry entry is only as true as the test it points at."
---

<objective>
Close F2-4, the last item of the F2 ruling: make `enrich-before-ingest` step 6 SHOW the
operator what their held rows are and hand them a ready one-line answer, instead of asking a
question, and make step 9's end-of-run text true. Then cut plugin `0.47.0` with one CHANGELOG
section covering the whole F2 chain.

Purpose: the 2026-09-11 UAT run `a254d1ed…` recorded F4 — step 6 asked "how do you want to
handle batch 1's two held rows?" on a lane whose own documentation says it does not ask at
step 6. The operator's F2 ruling of 2026-09-11 rejected both an in-flow approve question (that
is F4) and an export-to-`contact-upload` round trip (three prompts to land one person), and
chose instead: facet the holds at read time, render the ready answer, never block, and let the
create land through the ingest lane under a review-lane grant. Items `260911-w6o` (the enriched
row reaches disk), `260911-w6p` (the facet classifier and the durable verbs) and `260911-w6q`
(review-triage reads both queues and owns the create) build the machinery. This item is the
batch's face: what the operator actually reads at the end of a run, and the release that ships
all four.

Output: edits to three regions of `enrich-before-ingest/SKILL.md` (the step-5 correction, step
6, step 9), new assertions in the skill-contract test, a sequence-coverage registry entry for
any new fence, `0.47.0` in `plugin.json`, one CHANGELOG section, and the F2 todo moved to
`completed/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@.planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md
@.planning/UAT-autonomous-batch-2026-09-09.md
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/skills/review-triage/SKILL.md
@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
@operator-claude-plugin/tests/test_skill_sequence_coverage.py
@operator-claude-plugin/CHANGELOG.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Step 6 shows the holds and hands over a ready answer — and never asks</name>
  <files>operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md</files>
  <read_first>
    Read these BEFORE editing, because three of them shipped in this same batch and their
    real names are not knowable from this plan:
    - `operator-claude-plugin/scripts/held_queue.py` as `260911-w6p` left it — the facet
      constant names, the classifier function's real signature and return shape, and the
      durable verb names. Use those names verbatim; do not invent a parallel spelling.
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` step 5 as `260911-w6o`
      left it — the persist block now writes the merged row, so the facet read at step 6 has
      something to classify.
    - `operator-claude-plugin/skills/review-triage/SKILL.md` as `260911-w6q` left it — the
      create step's exact heading text, its create fence's function names and their order.
    - `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` lines 46-120
      (`_normalized`, `_numbered_step_spans`, `_step_span`, `CONTACT_UPLOAD_HANDOFF_HEADINGS`)
      and lines 315-333, 399-443, 509-533 — the four idioms this task mirrors: quote-by-heading,
      do-not-reproduce-the-body, no-bare-all-without-a-count, and the never-asks pin.
  </read_first>
  <behavior>
    Write these assertions FIRST, append them to
    `tests/test_enrich_before_ingest_skill_contract.py` under their own banner comment naming
    UAT F4 and the 2026-09-11 ruling, and observe them RED against the unedited SKILL.md
    before writing a word of prose:

    - Step 6's span states the flow asks the operator nothing about held rows, and names the
      recorded question shape as the rejected behaviour (mirror the `d-53-06` /
      `ask for nothing here` idiom at line 509 — locate a literal sentence, not a keyword).
    - Step 6's span names every facet the shipped classifier defines (read the constants off
      `held_queue.py`, parametrise over them — never a hand-typed list that can drift).
    - Step 6's span carries the ready answer with BOTH routes on it: the `create all` reply and
      the `/operator-claude-plugin:review-triage` slash form.
    - Every occurrence of `create all` in the document is followed by a digit — a direct mirror
      of `test_bare_approve_all_never_appears_without_a_trailing_count_or_scope` and its `\s*\d`
      regex, reused unchanged, written as its own function rather than by widening that one.
    - Step 6's span states the run does not wait for the answer and reaches step 7 regardless.
    - Step 6's span quotes review-triage's create heading text, AND that same text is asserted
      present in `skills/review-triage/SKILL.md` itself — the cross-file half the existing
      contact-upload pin does not have, so a heading renamed there fails here.
    - Step 6's span does NOT reproduce review-triage's create mechanics: assert the absence of
      a distinctive literal from review-triage's own create fence (pick one when you read that
      file — a function-call line or a per-record ritual sentence), mirroring
      `test_the_skill_does_not_reproduce_contact_upload_step_bodies`.
    - Step 6's span states the create runs under the standing grant, and that a row the grant
      does not cover is relayed as refused and routed to the second route rather than having
      the grant widened. Assert both halves — the refusal relay AND the no-widening sentence.
    - A negative pin on the corrected step-5 text: the sentence promising a review pass
      positioned after the report step is gone, and the paragraph that pointed at the now-closed
      pending todo is gone (locate each by a short distinctive fragment read off the current
      file — put the fragment in the TEST, never in this plan or a shell command).
    - Step 9's span restates the facet counts and carries the same ready answer.
  </behavior>
  <action>
    RED first, then GREEN. After the assertions above are failing, edit exactly three regions of
    `skills/enrich-before-ingest/SKILL.md` and nothing else.

    Region A — the step-5 correction (surgical, three sentences). Step 5 currently promises a
    review pass positioned after the report step; no such pass exists. It also carries a
    paragraph recording the open design gap and pointing at the pending todo, and a sentence
    telling the reader what an approval on a held row means — written before the ruling and
    contradicted by it. Replace those with the ruling's own shape: a held row is FACETED and
    SHOWN at step 6, a new person is created only by an explicit count-restating reply or in
    review-triage, and the vocabulary for the ambiguous-candidate holds is untouched. Keep this
    edit inside the paragraphs that follow the persist block — `260911-w6o`'s hunk is the
    `held_queue.build_entry` call roughly 25 lines upstream, and leaving that distance intact is
    what keeps the merge clean. Do not renumber steps, do not move the persist block.

    Region B — step 6. Add, after the existing preview paragraph and before step 7: a short
    python fence that loads the queue and classifies it with the shipped facet function, and the
    prose that renders it. The prose does three things and no more. (1) It names the new-people
    rows individually, by person, with what the waterfall revealed — the whole point of
    `260911-w6o` was that this data now reaches disk, so read it from the entry, never re-derive
    it. Needs-a-company rows are named separately with what is missing; nothing-found rows are a
    single parked count line, since there is nothing to show per person. (2) It states the ready
    answer once, on one line, offering the in-conversation count-restating reply and the
    review-triage route with equal weight and defaulting to neither. Spell that reply with a
    CONCRETE digit example the way step 3 already spells its own batch answer — the count is
    restated as an actual number, never as a placeholder letter — because the mirrored test
    reuses step 3's digit regex unchanged and a placeholder would fail it. (3) It states, in the
    skill's own voice, that this is a rendering and not a question: no `AskUserQuestion`, no
    stop-and-wait, silence is a valid outcome, and the batch continues to step 7 and the report
    whatever the operator does or does not say. Say why, naming the recorded run and the ruling,
    so a later reader cannot restore the question as a courtesy.

    Then the create route, BY REFERENCE. If the operator does reply in this conversation with a
    count-restating create, do not build a create here: quote review-triage's create step by its
    exact heading text and say to run that block against these entries — same functions, same
    order, same per-record confirmation, under the grant already open. This is the identical
    by-reference discipline this skill already applies to `contact-upload` steps 6-10 and the
    identical reason CLAUDE.md 13.0.1 gives for refusing rather than duplicating the association
    rule: one implementation, in one file. Do not re-type review-triage's fences.

    State the grant boundary explicitly in that same paragraph. The create runs under the
    standing grant opened at step 5, whose scope was fixed at open time from step 2's confirmed
    domains. A new person whose enriched domain that table never named is not covered — the F8
    shape from the recorded run — and the answer is to relay the refusal in the operator's terms,
    exactly as review-triage relays its own grant refusal, and take the second route, where
    review-triage opens a review-lane grant over exactly those records. The grant is never
    widened here and no second standing grant is opened.

    Region C — step 9. After the report block is rendered, restate the facet counts and the same
    one-line ready answer, so an operator who reads only the end of a long run still has the
    route. Keep it to a few lines: it is a restatement, not a second rendering, and it must not
    contradict what `run_report.py` already prints in its own Held rows section.

    Constraints that apply to all three regions. Nothing under `scripts/` changes — the
    classifier, the verbs and the create path are the siblings' code. Any code inside a fence is
    executable Python bound to real names, never angle-bracket placeholders, because
    `test_skill_sequence_coverage.py` parses these blocks and `test_write_grant.py` compiles some
    of them. Prefer the shortest edit that satisfies the behaviour block: this skill is already
    1283 lines and every sentence added here is a sentence a future reader must reconcile.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q && git diff --quiet operator-claude-plugin/scripts/ n8n/</automated>
  </verify>
  <done>
    The new assertions were observed RED before the SKILL.md edit and are green after it; the
    whole skill-contract file passes; `scripts/` and `n8n/` are untouched. Step 6 renders the
    facets and the ready answer, names no question, and reaches the create only by quoting
    review-triage's heading.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register the new fence with the sequence-coverage ratchet</name>
  <files>operator-claude-plugin/tests/test_skill_sequence_coverage.py, operator-claude-plugin/tests/test_held_facet_render_composition.py</files>
  <read_first>
    `operator-claude-plugin/tests/test_skill_sequence_coverage.py` — the module docstring, the
    `COVERED` / `NOT_A_PIPELINE` / `GRANDFATHERED_UNCOVERED` registries and their three
    validating tests, in particular
    `test_every_covered_nodeid_resolves_to_a_real_test_mentioning_the_sequences_sink`: a
    `COVERED` nodeid must resolve to a test function that exists AND whose own source text names
    the sequence's sink function. Also read whichever test `260911-w6p` wrote over the recorded
    `a254d1e` entries — that is the first candidate to point at.
  </read_first>
  <action>
    Task 1's step-6 fence adds a documented sequence of two-or-more scripts-module calls (the
    queue load joined to the facet classify), which the ratchet extracts automatically and fails
    on until it is registered. Register it honestly.

    Preferred, and try this first: if `260911-w6p`'s recorded-entries test genuinely drives the
    join — loads the queue and classifies what it loaded, rather than classifying a dict literal
    — add a `COVERED` entry keyed `("enrich-before-ingest", (<the extracted call tuple>,))`
    pointing at that test's nodeid, with a comment naming this batch and why that test is the
    real cover. Confirm the sink function name appears in that test's source before committing to
    it; the registry validator checks exactly that.

    Otherwise, and only otherwise, write one small composition test in the NEW file
    `operator-claude-plugin/tests/test_held_facet_render_composition.py` that drives the
    documented sequence end to end over the two recorded entries (the needs-a-company one and the
    new-person one) and asserts the counts the step-6 rendering depends on. New file rather than
    an addition to `test_held_queue.py` on purpose: that file is `260911-w6p`'s and a second
    writer in the same merge buys a conflict for nothing. Point the `COVERED` entry at it.

    Do not add a `GRANDFATHERED_UNCOVERED` entry and do not raise `MAX_GRANDFATHERED` — it is 0
    and shrink-only. `NOT_A_PIPELINE` is wrong here: the classify consumes the load's result,
    which is the definition the registry uses.

    If Task 1's fence turned out to contain only one scripts-module call, the ratchet extracts no
    identity and this task's registry edit is correctly a no-op — say so in the summary and skip
    the new file rather than manufacturing an entry.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_skill_sequence_coverage.py -q</automated>
  </verify>
  <done>
    The sequence-coverage suite is green with no new grandfathered entry, and any `COVERED`
    nodeid added resolves to a real test whose source names the sequence's sink.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cut 0.47.0, one CHANGELOG section for F2-1..F2-4, close the todo</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md, .planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md, .planning/todos/completed/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md</files>
  <read_first>
    Read the three sibling summaries BEFORE writing a line of the CHANGELOG — this section
    describes what THEY shipped, and this plan does not know it:
    `.planning/quick/260911-w6o-*/260911-w6o-SUMMARY.md`,
    `.planning/quick/260911-w6p-*/260911-w6p-SUMMARY.md`,
    `.planning/quick/260911-w6q-*/260911-w6q-SUMMARY.md`.
    Also read `operator-claude-plugin/CHANGELOG.md` lines 1-60 — the release-discipline
    blockquote at the top and the `## [0.46.0]` section, which is the format to match exactly
    (batch id, item ids, Keep-a-Changelog subheadings).
    And read `scripts/todo_triage.py` plus `tests/test_todo_triage.py` before moving the todo —
    the validator scans `pending/` only, so the move itself is what closes it, but read the rules
    rather than assuming that.
  </read_first>
  <action>
    Set `version` to `0.47.0` in `operator-claude-plugin/.claude-plugin/plugin.json`. That string
    is the only thing Claude Desktop compares, so an unbumped manifest greys out the Update
    button no matter what shipped — bump it in the SAME commit as the CHANGELOG entry, which is
    what the file's own release blockquote requires. Nothing else in the plugin pins the version
    (checked: `plugin.json` is the sole non-history occurrence of the previous string), so this is
    a one-line change.

    Add ONE `## [0.47.0] - 2026-09-11` section directly beneath `## [Unreleased]`. Open it with a
    line naming quick batch `260911-w6n` and all four item ids — `260911-w6o`, `260911-w6p`,
    `260911-w6q`, `260911-w6r` — and the operator ruling of 2026-09-11 that shaped them: holds
    faceted at read time, review-triage reading both queues, the create landing through the
    ingest lane under a review-lane grant, and the batch rendering the ready answer rather than
    asking. Say what was rejected and why, in one clause — an in-flow approve question (the
    recorded F4) and an export-to-`contact-upload` round trip — because the rejected options are
    what make the shipped shape legible six months from now. Then one bullet per item under the
    right Keep-a-Changelog subheading, each written from that item's own SUMMARY, each naming its
    quick id, and each saying what an operator can now do that they could not before. Do not
    write a bullet from this plan's description of a sibling; if a summary is missing or reports a
    partial landing, describe what actually landed and say so plainly rather than claiming the
    whole.

    Then close the F2 todo, per CLAUDE.md section 31 rule 2 — zero-inbox at batch close, and this
    item is last in the chain. `git mv` the pending todo to `.planning/todos/completed/` and
    append a short resolution note under its existing Ruling section: resolved by batch
    `260911-w6n`, one line per item id saying which half each closed, and the plugin version it
    shipped in. Correct the Ruling's own pointer, which names the earlier dropped batch's item
    ids — those plans were never written and pointing a future reader at them wastes their time.
    Do not open a replacement todo: if this batch left a genuine residual, it belongs in the
    summary as a sentence, or as a properly triaged todo carrying `kind:` and its evidence or
    trigger — never an untriaged file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ tests/test_todo_triage.py -q && git diff --quiet n8n/ && test "$(.venv/bin/python -c 'import json;print(json.load(open("operator-claude-plugin/.claude-plugin/plugin.json"))["version"])')" = 0.47.0 && test "$(/usr/bin/grep -c '^## \[0\.47\.0\]' operator-claude-plugin/CHANGELOG.md)" = 1 && test ! -e .planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md</automated>
  </verify>
  <done>
    The whole plugin suite and the n8n suite are green, the manifest reads `0.47.0`, the CHANGELOG
    carries exactly one `0.47.0` section naming the batch, all four item ids and the ruling, and
    the F2 todo has moved to `completed/` with its resolution recorded and its stale pointer
    corrected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator conversation -> create dispatch | A free-text reply at step 6 becomes authorization to create a person in HubSpot. This is the only new boundary this item opens. |
| standing grant -> record scope | The grant's `record_ids`/`record_domains` were fixed at step 5 from step 2's confirmed domains; a held row's enriched domain may fall outside it. |
| local `held_queue.json` -> rendered prose | Entry content (now carrying the enriched reveal) is read off disk and shown to the operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w6r-01 | Elevation of Privilege | step 6's create reply | high | mitigate | The reply must restate the count (`create all N`); Task 1 pins that `create all` never appears bare, mirroring the existing `approve all` rule that exists because a scopeless "all" once approved the wrong candidate. An affirmative that answers nothing is not consent, and the create still runs review-triage's own per-record confirmation — quoted, not replaced. |
| T-w6r-02 | Elevation of Privilege | `write_grant.covers()` at the create | high | mitigate | Task 1 states the grant is never widened for an uncovered row: the refusal is relayed as review-triage relays `grant_not_authorized`, and the route is a fresh review-lane grant over exactly those records. Pinned by a contract assertion on both halves (refusal relay AND no-widening sentence). |
| T-w6r-03 | Tampering | review-triage create mechanics | medium | mitigate | The create is referenced by heading, never reproduced, so the association and confirmation rules keep exactly one implementation (CLAUDE.md 13.0.1's own reasoning). A do-not-reproduce assertion plus a cross-file heading-existence assertion make a drifted copy or a stale quote fail a test. |
| T-w6r-04 | Information Disclosure | held-queue content rendered at step 6 | low | accept | `ROW_FIELD_ALLOWLIST` and the forbidden-name scan already bound what reaches disk; this item renders what is already there to the operator who owns it, and adds no new field. |
| T-w6r-05 | Denial of Service | step 6 blocking on an answer | medium | mitigate | The never-block property is the ruling's own requirement and is pinned: silence is a valid outcome and the batch reaches step 7 and the end-of-run report regardless. |

No package-manager install task exists in this plan (no npm/pip/cargo install), so no supply-chain row applies and no Package Legitimacy Audit is required.
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` is green.
- `git diff --quiet operator-claude-plugin/scripts/ n8n/` — this item changes no production script and no workflow JSON, so the n8n suite has nothing new to cover. Run it (`node --test tests/n8n/*.test.mjs` — the glob form; the directory form is broken on node 24) only if that diff is non-empty, which would itself be a scope error.
- `.venv/bin/python -m pytest tests/test_todo_triage.py -q` is green after the todo move.
- No live HubSpot, n8n or provider call was made; nothing was armed; no deploy, no bounce.
</verification>

<success_criteria>
- Step 6 shows this run's held rows by facet and hands over one ready answer offering both routes, and asks nothing.
- The run never waits on that answer.
- An in-conversation create dispatches through review-triage's own create block, quoted by heading, under the standing grant, refusing rather than widening when the grant does not cover the row.
- The step-5 text no longer promises a review pass that does not exist or points at a closed todo; step 9 restates the facets and the answer.
- The sequence-coverage ratchet is satisfied without a new grandfathered entry.
- `plugin.json` is `0.47.0` and one CHANGELOG section names batch `260911-w6n`, items `260911-w6o`..`260911-w6r` and the ruling.
- The F2 todo is in `completed/` with its resolution recorded.
</success_criteria>

<output>
Create `.planning/quick/260911-w6r-f2-4-operator-claude-plugin-skills-enrich-before-ingest-skil/260911-w6r-SUMMARY.md` when done.
</output>
