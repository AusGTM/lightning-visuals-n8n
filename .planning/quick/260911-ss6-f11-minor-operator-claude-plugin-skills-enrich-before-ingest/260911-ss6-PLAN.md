---
phase: quick-260911-ss6
plan: 01
type: execute
wave: 1
depends_on:
  - 260911-ss4
  - 260911-ss5
files_modified:
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
autonomous: true
requirements: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`enrich-before-ingest/SKILL.md` carries a `write_grant.close_grant(...)` call site, positioned AFTER its `build_run_report(` call site, so the operator reads the end-of-run report first and the grant's close second."
    - "The healthy completion close uses `write_grant.CLOSED_BATCH_COMPLETE`, and the fence reaches it on EVERY exit of the flow — a batch that was sent, a batch the operator declined at step 6/7, and a batch with nothing sendable at all (the UAT's own F2 shape, which is precisely the run that left a grant unclosed)."
    - "A grant already closed by step 7's ceiling-breach or crash arm is NEVER re-closed: the fence tests `grant.get(\"state\") == write_grant.OPEN` first, because `close_grant` does not inspect state and a plain re-close would overwrite `closed_reason` (the same hazard `revoke_grant`'s docstring names)."
    - "The skill names `write_grant.CLOSED_SESSION_END` for a sitting that ends with the grant still open, keeps it distinct from `revoke_grant` (an operator saying stop is a revocation, never relabelled session end), and states D-53-03's accepted gap — a session that dies mid-turn makes no close, and Guardrail A (the next session refusing to open over an armed backend) is the designed counterpart."
    - "No durable grant store is invented: the skill's existing `never written to disk` claim survives untouched and the new prose does not contradict it."
    - "Every `write_grant.CLOSED_*` constant the SKILL names exists on the module and is a member of `write_grant.CLOSE_REASONS` — pinned structurally, so a typo'd constant fails a test rather than raising `ValueError` mid-run in front of the operator."
    - "No production script changes: nothing under `operator-claude-plugin/scripts/` is modified, confirmed by `git diff --quiet` in Task 2's own verify — `close_grant`, `CLOSED_BATCH_COMPLETE` and `CLOSED_SESSION_END` already exist and already validate their reason."
  artifacts:
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
    - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
  key_links:
    - "SKILL.md step 10's close fence <-> `write_grant.close_grant` <-> `write_grant.CLOSE_REASONS` — one close vocabulary, validated at the function, pinned at the skill."
    - "SKILL.md step 7's `record_dispatch_outcome(..., reason=CLOSED_CEILING_BREACH | CLOSED_UNHANDLED_ERROR)` <-> step 10's `state == OPEN` guard — the two close paths that already exist are what the new guard protects from relabelling."
    - "`write_grant.py`'s SESSION END AND UNHANDLED ERROR ARE CALLER-MADE CLOSES comment (D-53-03) <-> step 10's accepted-gap paragraph <-> Guardrail A's dirty-backend refusal at the next open."
---

<objective>
Give `enrich-before-ingest` the grant close it never had: a `write_grant.close_grant` call
site at the end of the run (`CLOSED_BATCH_COMPLETE`), plus the `CLOSED_SESSION_END` guidance
for a sitting that ends with a grant still open — and one test that pins both so the call
site cannot silently disappear again.

Purpose: UAT `.planning/UAT-autonomous-batch-2026-09-09.md` F11. The skill already closes the
grant on two UNHAPPY paths — step 7 closes with `CLOSED_CEILING_BREACH` on a pre-call ceiling
stop and with `CLOSED_UNHANDLED_ERROR` from its `finally` arm. The healthy path closes
nowhere, so a run that completes normally — or, as on 2026-09-11, a run where nothing was
sendable at all — leaves the grant open until the Python value dies with its fence. The
skill's own sentence at step 7 already claims a grant "ends on completion, revocation,
session end, error or a ceiling breach"; two of those five had no call site. This closes
them, without inventing the durable grant store D-53-03 deliberately refused.

Output: one new numbered step 10 in `enrich-before-ingest/SKILL.md`, and four assertions
appended to `tests/test_enrich_before_ingest_skill_contract.py`. No script change, no
CHANGELOG entry, no version bump.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/scripts/write_grant.py
@operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pin the close, then write it — RED before GREEN</name>
  <files>operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md</files>
  <read_first>
    - `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` lines 92-116
      (`_text`, `_normalized`, `_numbered_step_spans`) and lines 441-505 (the existing
      grant-lifecycle prose pins — `test_the_skill_states_the_grant_never_outlives_its_turn_
      and_arms_no_other_lane`, `test_the_skill_says_what_a_grant_does_not_remove`,
      `test_the_skill_says_revocation_bites_at_the_next_send_not_mid_dispatch`). The new
      assertions join this group and reuse `_text`/`_normalized` verbatim.
    - `operator-claude-plugin/scripts/write_grant.py` lines 1248-1262 (`close_grant` — returns
      a COPY, validates the reason, makes no network call, does NOT inspect state), 1338-1350
      (the five `CLOSED_*` constants, `GRANT_04_REASONS`, `CLOSE_REASONS`), 1352-1385 (the
      comment block on where GRANT-04's disarm actually bites, and the caller-made-close /
      Guardrail A paragraph — D-53-03), and 1390-1412 (`revoke_grant`: idempotent,
      reason-preserving, and the stated reason a plain re-close would overwrite `closed_reason`).
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` lines 920-948 (step 7's
      existing two closes, via `record_dispatch_outcome(..., reason=...)`), lines 1022-1030
      (the sentence claiming a grant ends on completion / revocation / session end / error /
      ceiling breach, and the `never written to disk` clause that must survive), and lines
      1103-1172 (step 9, the last numbered step, ending the file).
    - `operator-claude-plugin/skills/backend-control/SKILL.md` lines 150-155 (the one existing
      close-a-grant paragraph in the plugin — the wording to mirror, not re-invent).
  </read_first>
  <behavior>
    Four assertions, appended to the grant-lifecycle group in
    `test_enrich_before_ingest_skill_contract.py`. Observe them RED against the unmodified
    SKILL.md before touching it, then make them GREEN with the step-10 edit.

    - Position, not step number: the SKILL text contains `write_grant.close_grant(` and its
      index is GREATER than the LAST index of `build_run_report(` — the report is what the
      operator reads, the close comes after it. The LAST occurrence matters: step 7 already
      mentions `build_run_report(..., outcomes=...)` inside a fence comment (line ~932), so a
      first-occurrence locator would compare against step 7 and prove nothing. Position-based
      so a sibling item reflowing step 9 cannot break it.
    - Reasons: the text names `write_grant.CLOSED_BATCH_COMPLETE` and
      `write_grant.CLOSED_SESSION_END`.
    - The re-close guard, on two literals rather than a paraphrase: the raw SKILL text
      contains the fence's own state test, `grant.get("state") == write_grant.OPEN` (asserted
      on raw text, never through `_normalized`, which strips the markers code needs), and the
      normalized body contains the prose phrase `never closed a second time`.
    - Structural: every `CLOSED_*` constant name the SKILL mentions resolves on the imported
      `write_grant` module AND is a member of `write_grant.CLOSE_REASONS` — the same shape as
      this file's existing `test_every_script_the_skill_names_exists_on_disk`.
  </behavior>
  <action>
    FIRST, tests only. Append four functions to the grant-lifecycle group in
    `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py`, named
    `test_the_skill_closes_the_grant_after_the_end_of_run_report`,
    `test_the_skill_names_batch_complete_and_session_end_as_its_two_closes`,
    `test_the_skill_never_re_closes_a_grant_another_path_already_closed`, and
    `test_every_close_reason_the_skill_names_is_a_real_close_reason`. Reuse the module's own
    `_text()` / `_normalized()` helpers; add `import write_grant` beside the file's existing
    imports for the fourth. Locate the close by `text.index("write_grant.close_grant(") >
    text.rindex("build_run_report(")` — the LAST occurrence of the report call, because step 7
    names it in a fence comment too, and never by step number. Derive the constant names in the
    fourth with a regex over the SKILL text for `write_grant.CLOSED_[A-Z_]+`, assert the set is
    non-empty, then assert each resolves via `getattr(write_grant, name)` and that its VALUE is
    in `write_grant.CLOSE_REASONS`. Every wording assertion goes through `_normalized(...)`
    lower-cased, matching the neighbours.

    Run the file and RECORD the failures. Paste the failing assertion lines into the SUMMARY as
    the RED evidence before editing SKILL.md. If any of the four passes against the unmodified
    skill, stop and report — it would mean the assertion is pinning something else.

    THEN the skill. Append ONE new top-level numbered step `10. **` at the END of
    `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md`, after step 9's closing
    paragraph. Do not renumber, reword or reflow any existing step — a sibling batch item is
    editing step 9's report fence in the same file. Do not touch `plugin.json` or any
    CHANGELOG: the version bump that names F11 belongs to batch item `260911-ss7`.

    The new step is headed as closing the grant, and states that every run of this flow ends
    with a recorded close. It carries ONE python fence importing `write_grant`, guarding on the
    grant being present and its `state` still equal to `write_grant.OPEN`, calling
    `write_grant.close_grant(grant, write_grant.CLOSED_BATCH_COMPLETE)`, rebinding `grant` to
    the returned copy, and carrying a comment that the close reason word is said out loud to
    the operator. Match the file's existing fence idiom (the `if grant is not None` form step 5
    already uses).

    The prose around it says, in the plugin's own register:

    1. This step is reached on EVERY exit — a batch that was sent, a batch declined at step 6
       or step 7, and a batch with nothing sendable at all. The last of those is the run F11
       was found on, and it is the one most likely to be skipped by a reader who thinks a close
       belongs only after a write.
    2. Three states, three readings. Already closed: step 7's ceiling-breach and crash arms got
       there first; leave it alone and report the reason IT recorded. Open, from the standing
       grant opened at step 5: close it here and name the reason word. Open, but only the
       single-use grant `authorize_ungranted_send` built for one send: closing is harmless,
       but do not announce it as though a standing grant had been open all along.
    3. Why the state test is load-bearing, not defensive noise: `close_grant` does not inspect
       state, so a grant that another path already closed is `never closed a second time` here —
       write that phrase verbatim, it is what the test greps — because a second close overwrites
       `closed_reason`, and a grant closed for a ceiling breach or an unhandled error re-reading
       as ordinary batch completion misreports the one close the operator most needs to read
       correctly. This is the same reason `revoke_grant` returns an already-closed grant
       unchanged rather than re-closing it. The fence's guard is written exactly as
       `grant.get("state") == write_grant.OPEN`, which is the other literal the test greps.
    4. Closing arms nothing and disarms nothing by itself — each send already disarmed its own
       window. Mirror `backend-control/SKILL.md`'s sentence rather than writing a second one.
    5. `write_grant.CLOSED_SESSION_END` is for a sitting that ends with the grant still open
       and no further batch coming: close it before the conversation ends and say so. Keep it
       distinct from the two neighbours it is easily confused with — an operator saying stop is
       `revoke_grant` (idempotent, reason-preserving) and is never relabelled a session end,
       and a crash is step 7's `CLOSED_UNHANDLED_ERROR`.
    6. The accepted gap, stated as one rather than left to be discovered: a grant exists only
       as a value in this conversation and is never written to disk, so a session that dies
       mid-turn makes no close at all. That is D-53-03, put to the operator on 2026-08-25 and
       accepted after a durable expiry was offered and declined; what catches it is Guardrail
       A — the next session's plan reads the live write-safety state and refuses to open a
       grant over an armed backend. Say plainly that this is a designed gap with a named
       counterpart, and that the answer to a missed close is never a durable grant store.

    Keep the step short — it is one fence and roughly six short paragraphs, in the voice of the
    steps around it. Re-run the file's tests until all four are green and no existing test in it
    regressed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py -q</automated>
  </verify>
  <done>
    The four new tests exist, were observed RED against the unmodified SKILL.md with their
    failure lines recorded in the SUMMARY, and are green after the edit. SKILL.md carries one
    new step 10 whose `write_grant.close_grant(` appears after `build_run_report(`, guarded on
    `write_grant.OPEN`, naming both `CLOSED_BATCH_COMPLETE` and `CLOSED_SESSION_END`. The whole
    contract file passes. `git diff --stat operator-claude-plugin/scripts/` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Whole-suite green, and the scope left deliberately open</name>
  <files>operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_mandatory_report_call_sites.py` lines 55-75 and 290-310
      — the cross-skill call-site TABLE and the step-numbering pin. Confirm the pin it makes on
      a LAST numbered step is scoped to `contact-upload`, not to this skill, so step 10 here
      breaks nothing.
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py` and
      `tests/test_autonomy_switch_prose.py` — both read this SKILL; run them and read any
      failure rather than assuming the new step is invisible to them.
  </read_first>
  <action>
    Run the full plugin suite. Any failure outside the four new tests is a collision with the
    new step — read it and fix the SKILL edit, never the failing neighbour's assertion.

    Confirm the blast radius with `git diff --name-only`: exactly two paths, the SKILL and the
    contract test. Nothing under `operator-claude-plugin/scripts/`, nothing in `n8n/`, no
    `plugin.json`, no CHANGELOG.

    In the SUMMARY, record two things in one sentence each, opening no todo (CLAUDE.md §31: a
    residual with no test and no recorded hit is a sentence in the SUMMARY, not a todo):

    - The sibling gap, with its evidence: `close_grant` has no call site in
      `enrich-records/SKILL.md`, `contact-upload/SKILL.md`, `review-triage/SKILL.md` or
      `suggest-contacts/SKILL.md` either — the same F11 shape on four more lanes, deliberately
      out of scope because this batch item is scoped to `enrich-before-ingest` by name and no
      ruling exists for widening it.
    - What the close is and is not: it records the reason in the conversation's own grant value
      and nothing else — no file is written, no workflow is called, and F11's other half ("`open_grant`
      writes nothing durable") stays true on purpose under D-53-03.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q && git diff --quiet operator-claude-plugin/scripts/ n8n/</automated>
  </verify>
  <done>
    The full plugin suite is green with no new skips. `git diff --name-only` lists exactly
    `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` and
    `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py`. The SUMMARY
    carries the two recorded sentences and no new todo file was created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md prose -> the operator's Claude -> `write_grant` | The skill is executed by a model reading prose. A wrong instruction here is a wrong call against the write-authority module, with no compiler between the two. |
| an open grant -> the next send's arming decision | `covers()` refuses on `state != OPEN`; whatever this step writes into `state`/`closed_reason` is what a later send and a later session read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ss6-01 | Repudiation | step 10's close fence vs step 7's `CLOSED_CEILING_BREACH` / `CLOSED_UNHANDLED_ERROR` closes | high | mitigate | The fence tests `grant.get("state") == write_grant.OPEN` before closing, so a ceiling breach or a crash can never be relabelled `batch_complete` in the operator's account. Pinned by `test_the_skill_never_re_closes_a_grant_another_path_already_closed`. |
| T-ss6-02 | Repudiation | the `CLOSED_SESSION_END` guidance | medium | mitigate | The prose states D-53-03's accepted gap explicitly — a session that dies mid-turn makes no close, Guardrail A refuses at the next open — so an unclosed grant reads as a known, named boundary rather than a silent success. |
| T-ss6-03 | Information disclosure | the new prose vs the existing `never written to disk` pin | medium | mitigate | The step says the close is recorded in the conversation's grant value only; no durable grant store is added, and the existing pin on `never written to disk` stays green as the check. |
| T-ss6-04 | Tampering | a mistyped `write_grant.CLOSED_*` constant in prose | low | mitigate | `test_every_close_reason_the_skill_names_is_a_real_close_reason` resolves every name the SKILL mentions against the module and against `CLOSE_REASONS`, so a typo fails a test instead of raising `ValueError` in front of the operator mid-run. |
| T-ss6-05 | Elevation of privilege | the close path touching arming | low | accept | `close_grant` makes no network call and changes no write-safety flag by design (each send disarms its own window); this plan adds no call that could arm anything, and the disarm-bearing closes stay inside `write_grant`'s own guardrail B. |
| T-ss6-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan — the change is one markdown file and one test file, against modules already in the repo. No legitimacy gate applies. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` is green.
- `write_grant.close_grant(` appears in `enrich-before-ingest/SKILL.md` after `build_run_report(`.
- `git diff --stat operator-claude-plugin/scripts/` and `git diff --stat n8n/` are both empty.
- The SUMMARY carries the RED failure lines observed before the SKILL edit.
</verification>

<success_criteria>
F11 is closed for `enrich-before-ingest`: the skill has a batch-complete close at the end of
every run, session-end guidance that keeps revocation and crash distinct from it, a stated
accepted gap where no close can be made, and four tests that fail if any of those disappears —
with no script change, no new durable store, and no version bump.
</success_criteria>

<output>
Create `.planning/quick/260911-ss6-f11-minor-operator-claude-plugin-skills-enrich-before-ingest/260911-ss6-SUMMARY.md` when done
</output>
