---
quick_id: 260911-3mu
type: quick
status: complete
created: 2026-09-11
mode: orchestrator-executed
---

# Quick task 260911-3mu: close the second-review findings on the walker v1 model

**Spec:** `.planning/quick/260911-1z5-close-review-findings-on-the-walker-v1-m/260911-1z5-REVIEW.md`
(NF-BL-01, NF-MJ-01, NF-MJ-02, NF-MN-01..06, NF-NT-01..08). Executed directly by the
orchestrating session rather than a planner/executor pair: every finding arrived with its
fix snippet and its reproduction, and the whole diff is a test instrument plus tests.

## Tasks

1. **Walker** (`tests/n8n/lib/walkWorkflow.mjs`): record `outputCount` on every fired v1 run
   (both fire sites); `starvedWithData` gains the annihilation arm (`carried >= 1 &&
   outputCount === 0` on a `merge_fired_with_unfilled_input` run); `recordV1Fire(name, site)`
   names its site; a shared delivery cap throws on a Merge-free feedback cycle (NF-NT-04);
   the BL-02 grouping comment is demoted to "consistent with, not isolated by" the recordings;
   NF-NT-01/02 documented in place.
   - verify: `node --test tests/n8n/walkWorkflow.test.mjs` — the NF-BL-01 case is RED against
     the walker at `7f573285` (`starvedWithData` returns `[]` while 2 rows are destroyed) and
     GREEN after.
2. **Tests**: five new `walkWorkflow.test.mjs` cases (annihilation, append non-loss, NF-MJ-01
   overlap pin, NF-MN-05 legacy partial view, NF-NT-04 cycle throw); two tautological `fired`
   asserts replaced; `walkerEngineFidelityV1.test.mjs` asserts the detector's report on
   12354/12355/12356 and rewords the digest message; `enrichmentConvergenceMerge` pins the
   never-delivered set and the response row count; five message sites reworded with per-node
   fire claims moved to `runData`.
   - verify: `node --test tests/n8n/*.test.mjs` all green; `walkerEngineFidelity.test.mjs` zero-diff.
3. **Docs**: todo append (NF-MJ-01), frozen README pointer (NF-NT-05), CHANGELOG count (NF-NT-08).

## Accepted without a test

NF-MN-06 (MN-07 earliness): the creation-site `requiredInputsFor` call refuses a `chooseBranch`
Merge before any append-modelling, but the drain already refused it unconditionally, so the
difference is not observable from outside the walker. Kept the earlier refusal; no test can
distinguish it without instrumenting the walker.

<threat_model>
ASVS level 1, block on high. Scope is an offline test instrument and its tests; no runtime
code, no n8n JSON, no credentials, no network. No threats introduced.
</threat_model>
