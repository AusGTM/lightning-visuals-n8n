---
phase: quick-260911-0tz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/n8n/lib/walkWorkflow.mjs
  - tests/n8n/walkWorkflow.test.mjs                    # the walker's own spec — pre-authorized, see Task A Step 5
  - tests/n8n/walkerEngineFidelityV1.test.mjs          # NEW
  - tests/n8n/creditsSummaryUnderV1.test.mjs           # NEW
  - tests/n8n/fixtures/frozen/wf_enrichment_cloud.v1.2026-09-10.json   # NEW (copy)
  - tests/n8n/fixtures/frozen/README.md
  - .planning/todos/pending/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md   # moved to completed/
  - .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md      # NEW
autonomous: true
requirements: [WALK-V1-01, WALK-V1-02]   # quick-task-local handles, NOT ROADMAP requirement IDs.
                                         # WALK-V1-01 -> Task A (walker models v1 on the two
                                         # observed points, with fidelity cases).
                                         # WALK-V1-02 -> Task B (credits-summary behaviour under
                                         # the fixed walker). Both trace to
                                         # .planning/todos/pending/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md

estimate:
  tokens: 70000
  raw_tokens: 70000        # factor 1.0, applied: false, sample_count 0 (estimate-calibration)
  tasks: 3
  confidence: low          # derived from sample_count 0, not self-rated

must_haves:
  truths:
    - "Walking a v1 graph, a node that RAN and emitted zero items makes NO delivery — it cannot claim a Merge input."
    - "Walking a LEGACY graph (allowLegacy), a zero-item output still delivers, byte-for-byte as executions 12203/12206/12316 recorded it."
    - "Walking a v1 graph, a Merge can fire more than once per execution: deliveries that arrive after a run has filled open a further pending run, and at end-of-run every pending run with at least one filled input fires with whatever arrived."
    - "The walker reproduces executions 12354/12355/12356 on the frozen v1 enrichment graph: Decide Company Action Merge 2 runs of [2,1] items with run 1's input 1 unfilled, Decide Company Action 2 runs of 0 items, Merge Company's [] never a source, Enrichment Gate Merge exactly 1 run of 6 items, Build Response 2 rows, HubSpot Update never run."
    - "With all three providers enabled, the credit lane's outcome is pinned by a test that states the v1 engine rule it relies on and fails if the walker predicts a split or duplicated remaining_credits."
    - "Every pre-existing tests/n8n suite still passes, including the three legacy fidelity cases, unmodified."
  artifacts:
    - tests/n8n/lib/walkWorkflow.mjs
    - tests/n8n/walkerEngineFidelityV1.test.mjs
    - tests/n8n/creditsSummaryUnderV1.test.mjs
    - tests/n8n/fixtures/frozen/wf_enrichment_cloud.v1.2026-09-10.json
    - .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md
  key_links:
    - "walkWorkflow.mjs `order` (v1 vs legacy) gates BOTH new rules — one variable, two branches, so the legacy fixtures keep their recorded meaning."
    - "trace.merges keeps `fired`/`sources`/`itemCounts` (run 0) for its six existing consumer files while gaining `runs[]`."
    - "The frozen v1 graph copy, never a pointer at n8n/, is what makes the 12354-12356 reproduction a reproduction."
---

<objective>
Move `tests/n8n/lib/walkWorkflow.mjs` onto the v1 engine for the two behaviours Gate 11 observed
live (executions 12354/12355/12356), pin them with fidelity cases against a frozen v1 graph copy,
then use the fixed walker to pin what the credit lane does when all three providers are enabled.

Purpose: every committed `n8n/wf_*.json` runs on `executionOrder: "v1"`, and the walker currently
models the LEGACY engine on the one rule that decides whether a Merge input is claimed by a real
row or by an empty one. Until that is fixed the instrument is wrong for every graph it is allowed
to walk, and an offline green means nothing — the exact condition Phase 70 existed to end.

Output: a walker with two explicit engine branches, two new test files, one new frozen graph copy,
one todo closed and one opened.

**Standing rule this plan obeys (D-70-19): the walker moves toward the ENGINE, never toward the
plans and never toward a green suite.** No walker behaviour changes here without its own fidelity
case against a frozen recording, and the three legacy cases in `walkerEngineFidelity.test.mjs`
(12203/12206/12316, all `allowLegacy: true`) must keep passing UNMODIFIED.

**Out of scope, hard line:** `scripts/build_cloud_workflows.py`, any `n8n/wf_*.json`, and any
deploy/arm/bounce. If Task B proves a real double-fire defect, the plan STOPS at the failing test
plus a recorded todo — the builder decision is the operator's (Phase 46 parity rule; never
hand-edit n8n JSON).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md
@tests/n8n/lib/walkWorkflow.mjs
@tests/n8n/v1RuntimeRecordings.test.mjs
@tests/n8n/fixtures/frozen/README.md
@tests/n8n/enrichmentMixedBatch.test.mjs
</context>

<assumption_delta_decision>
Now-primary noun: **a Merge node's RUN LIST** (was: a Merge's single fire).

Decision: **promote**, with compat. `trace.merges[name]` gains `runs: [{sources, itemCounts}, ...]`
as the primary representation; the existing `fired` / `sources` / `itemCounts` keys are RETAINED and
carry run 0's values (`fired` = at least one run fired). Six test files read the old shape
(`ingestMixedBatch`, `ingestTracerFlow`, `mergeInputContract`, `walkerEngineFidelity`,
`walkWorkflow`, `zoominfoLaneFlow`); rewriting them is not this task.

Accepted debt, named: two representations of the same fact live side by side, and a future reader
of `trace.merges[x].itemCounts` on a multi-firing Merge silently sees run 0 only. Retire the flat
keys when the six consumers are next touched anyway — not before.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task A: walk 12354-12356 on a frozen v1 graph — RED first, then the two v1 rules</name>
  <files>tests/n8n/fixtures/frozen/wf_enrichment_cloud.v1.2026-09-10.json, tests/n8n/fixtures/frozen/README.md, tests/n8n/walkerEngineFidelityV1.test.mjs, tests/n8n/lib/walkWorkflow.mjs</files>
  <read_first>
    tests/n8n/lib/walkWorkflow.mjs (the whole file; rule (c) in `propagate` ~L392-440, the Merge
    handler ~L456-532, rule (a) ~L534-556, the report-only stall pass ~L578-608, `trace.merges`
    ~L610-628); tests/n8n/v1RuntimeRecordings.test.mjs (the five assertions this task must now
    reproduce by WALKING, not reading); tests/n8n/fixtures/frozen/README.md; the head of
    tests/n8n/walkerEngineFidelity.test.mjs (fixture-loading idiom).
  </read_first>
  <behavior>
    Written and observed RED against the UNMODIFIED walker before any walker edit. Expected RED
    shape (record whichever actually appears — do not assume): `Decide Company Action Merge` fires
    ONCE, and/or `Merge Company`'s `[]` claims input 1 ahead of a sentinel gate.

    Assert in THIS order, so the RED recorded against the old walker is a clean assertion failure
    rather than a `TypeError` on an undefined `runs`: raw `runData` counts first, `trace.merges`
    second.

    - Decide Company Action Merge: `runData[...].length === 2`, THEN item counts `[2, 1]`, THEN
      `trace.merges[...].runs[1]` has input 1 UNFILLED (`sources[1] === undefined`).
    - Decide Company Action: 2 runs, 0 items each.
    - Merge Company: ran with 0 items, and never appears as a source of Decide Company Action Merge.
    - Enrichment Gate Merge: exactly 1 run, 6 items.
    - Build Response: `runData["Build Response"].length === 1` AND `runData["Build Response"][0].length === 2`
      — NOT `nodeItems(...).length === 2`, which also passes on a 1+1 run split, the exact collapse
      shape this instrument exists to see.
    - HubSpot Update: `runData["HubSpot Update"] === undefined`.
    - The three legacy cases in walkerEngineFidelity.test.mjs still pass, file unmodified.
  </behavior>
  <action>
Step 1 — freeze the graph. `cp n8n/wf_enrichment_cloud.json
tests/n8n/fixtures/frozen/wf_enrichment_cloud.v1.2026-09-10.json`. This is honest: `git diff
ec102a4 -- n8n/wf_enrichment_cloud.json` is empty, so the working copy IS the 287-node,
`executionOrder: "v1"` body that executions 12354/12355/12356 ran (verify both facts before
copying; if either has drifted, STOP and report rather than freeze a different graph). Add a row
to `tests/n8n/fixtures/frozen/README.md`'s table naming the file and the three executions it
serves, and one sentence saying this copy is v1 and therefore needs no `allowLegacy` — unlike the
three legacy fixtures above it.

Step 2 — write `tests/n8n/walkerEngineFidelityV1.test.mjs` as a SIBLING of
`walkerEngineFidelity.test.mjs`, not an addition to it: that file's header declares every case in
it a RECORDED LEGACY divergence passing `allowLegacy`, and appending a v1 case would falsify its
own header. The new file's header states the inverse: these are v1 reproductions, no `allowLegacy`,
against the frozen v1 copy, one case per execution.

Take BOTH inputs from the recordings so the fixture cannot drift from the run it claims:
- trigger items: the recording's own Webhook Trigger item, i.e.
  `[{ body: rd["Webhook Trigger"][0].data.main[0][0].json.body }]` (12354 carries
  `{providers: [], mode: "propose", events: [2 contacts by email], run_id}`).
- `httpStubs`: sourced from the recording's own runData rather than hand-written. A short helper
  over the frozen graph's HTTP-type nodes (`n8n-nodes-base.httpRequest`, `n8n-nodes-base.hubspot`):
  for each such node present in `rd`, stub it with a closure that returns run `k`'s items on call
  `k` — `rd[name][k].data.main[0].map(it => it.json)` — falling back to the last run once calls
  outrun recorded runs. Run-0-only stubbing misaligns if an HTTP node ran more than once; assert
  the fallback is never silently relied on by logging/failing when `rd[name].length > 1` and the
  call count exceeds it. This is the SAME `httpStubs` mechanism `enrichmentMixedBatch.test.mjs`
  uses — fed by the engine's recorded responses instead of by hand — not a second stubbing
  mechanism. The walker throws by name on an unstubbed HTTP node, so any gap surfaces loudly.

Run it. Record the RED in the SUMMARY (which assertion, what the walker produced instead).

Step 3 — the walker, as TWO EXPLICIT BRANCHES keyed on the existing `order` variable, never a
patched single state machine. That is what keeps 12203/12206/12316 green without re-reasoning
about them.

(3a) `propagate`, after the unchanged `alwaysOutputData` substitution:
`if (order === "v1" && outItems.length === 0) return;`
Legacy keeps enqueuing the zero-item wave exactly as today.

(3b) First extract the merge MATHS — the append / combineByPosition / combineAll block that
currently closes over `state.buffers` — into one pure helper
`mergeBuffers(node, buffers, numberInputs)`, called by BOTH branches. Both branches share the maths;
only the arrival state machine differs. ("Byte-identical" below therefore means the legacy
ARRIVAL/first-wins/discard state machine, not the maths, which moves once and is shared. Do not
duplicate 40 lines, and do not refuse to touch the legacy block on that account.)

Then: `if (order === "legacy") { <existing arrival state machine, unchanged> } else { <v1> }`.
The v1 body models n8n's run-indexed `waitingExecution`, NOT the legacy discard rule:
  - keep a list of PENDING runs per Merge, each `{filled: {i: items}, sources: {i: fromName}}`;
  - a delivery to input `i` goes to the EARLIEST pending run whose input `i` is not yet filled;
    if every pending run already has `i` filled, open a NEW pending run. Nothing is ever discarded
    (the legacy "first delivery wins, later ones dropped" rule is legacy-only);
  - a pending run whose every input `0..numberInputs-1` is filled fires immediately, is removed
    from pending, and appends to `runData[name]` and propagates exactly as today (the existing
    append / combineByPosition / combineAll merge maths is REUSED, not re-authored).

  Why run-indexed and not "reset arrived after each fire": under a reset-and-discard model the
  observed `[2, 1]` depends on `Companies Absent Sentinel Gate` happening to reach input 0 before
  `Recompute Not Requested Sentinel Gate`; under run-indexed buffering `[2, 1]` holds in EITHER
  arrival order and only the source NAMES swap. The engine is the target, not the walker's queue
  order.

(3c) The end-of-run drain, replacing the report-only pass's "deliberately does NOT implement that
drain" stance: after the main `while` loop drains, fire the earliest pending run of any Merge whose
filled-input count is `>= requiredInputs` — scanning `mergeState` in `Object.entries` insertion
order (first-delivery order) when two Merges both qualify, never left to accident, because which
one fires first can change which producer claims a downstream input (`Build Response Merge Stage
1/2/3` -> `Build Response Merge`) — propagate it, then RESUME the main `while` loop
(a drained Merge's output can start work downstream, including another Merge); repeat until no
pending run qualifies. Absent inputs contribute `[]` to the merge maths and `undefined` to that
run's `sources`/`itemCounts`. `requiredInputs` is `1` for the `append` and `combine` modes this repo
emits on Merge v3.2 (`packages/nodes-base/nodes/Merge/v3/actions/versionDescription.ts`,
`requiredInputs`); if a Merge with `mode: "chooseBranch"` (`requiredInputs: [0, 1]`) is ever
encountered, THROW by node name rather than guess — this repo emits none today.
`trace.stalled` keeps only Merges that received ZERO deliveries.

(3d) `trace.merges[name]`: add `runs: [{sources, itemCounts}, ...]` in fire order; keep `fired`
(now "at least one run fired") plus `sources`/`itemCounts` carrying run 0, per the
`<assumption_delta_decision>` above.

Step 4 — comments. Rewrite rule (c)'s block in `propagate` to state the v1 answer as OBSERVED
(executions 12354/12355/12356, frozen at `exec_1235{4,5,6}.runData.json`, pinned by
`v1RuntimeRecordings.test.mjs` and now reproduced by `walkerEngineFidelityV1.test.mjs`) and the
legacy behaviour as the recorded legacy model the `allowLegacy` branch preserves. Rewrite the
"deliberately does NOT implement that drain" comment on the stall pass — it is now implemented, and
the open question it cited is closed by observation. Also fix rule (a)'s now-stale trailing
sentence "an empty delivery still SATISFIES a Merge input, but it never STARTS a lane" — true under
legacy, false under v1. Keep the INFERRED-NOT-OBSERVED note about an IF node's empty branch, but
state that under v1 it is moot for a Merge input (an empty branch delivers nothing either way).

Step 5 — GREEN on the new file, then the whole suite.

`tests/n8n/walkWorkflow.test.mjs` is the walker's OWN spec over SYNTHETIC graphs (not recordings),
and four of its cases are pre-decided as in-scope to move — verified at planning time, do not
re-litigate:
- the "zero-item delivery case" (~L138-165): `LaneB`'s `return []` claiming Merge input 1, asserted
  via `trace.merges.Merge.sources[1] === "LaneB"`. Under v1 that is now false.
- the three `trace.stalled.length === 1` cases (~L193, ~L240, ~L384): a Merge with ONE arrived
  input now DRAINS at end-of-run instead of stalling.

Move each to v1 truth, not toward green: a stall case must use a Merge input with ZERO deliveries
(that is the only shape that still stalls under v1); a "zero-item arrival satisfies an input" case
either becomes legacy-only (`settings: {}` on the synthetic graph + `allowLegacy: true`, so it keeps
recording what 12203 showed) or is deleted. This IS the walker's spec following the engine
(D-70-19), not a relaxation.

For every OTHER file: STOP and report. Do not relax an assertion and do not special-case the walker
— name which test, what changed, and whether the break is the legacy model being corrected. Note
that the `trace.stalled` reads in `mergeInputContract.test.mjs`, `ingestMixedBatch.test.mjs`,
`ingestTracerFlow.test.mjs` and `zoominfoLaneFlow.test.mjs` were checked at planning time and are
drain-tolerant (they assert stalls are ABSENT, or filter an expected one) — a break there is a real
finding, not this change's bookkeeping.

Naming, once, in the SUMMARY, not fixed here: `scripts/prove_phase70_runtime.py` consumes this
walker, so its predictions shift with this change. Out of scope to touch.
  </action>
  <verify>
    <automated>node --test tests/n8n/walkerEngineFidelityV1.test.mjs && node --test tests/n8n/walkerEngineFidelity.test.mjs && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    The new v1 file was observed RED against the unmodified walker (recorded in the SUMMARY) and is
    now GREEN; `walkerEngineFidelity.test.mjs` passes with zero diff to that file; the full
    `node --test tests/n8n/*.test.mjs` run passes with no fewer passing assertions than the 1083
    baseline; the frozen v1 graph copy exists and the README names the executions it serves.
    Failing direction: revert 3a alone and the v1 case must go RED on `Merge Company` claiming a
    Merge input; revert 3c alone and it must go RED on `Decide Company Action Merge` firing once.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task B: pin the credit lane under v1 with all three providers enabled</name>
  <files>tests/n8n/creditsSummaryUnderV1.test.mjs</files>
  <read_first>
    tests/n8n/enrichmentMixedBatch.test.mjs (its `baseStubs()` / `run()` idiom — REUSE this shape,
    do not invent a second stubbing mechanism); the committed `n8n/wf_enrichment_cloud.json` nodes
    `Credit Request`, `IF {Lusha,Apollo,ZoomInfo} Credit Requested`, `{Lusha,Apollo} Usage`,
    `ZoomInfo Usage Mint`, `ZoomInfo Usage` (a Code node, not HTTP), `Adapt {Lusha,Apollo,ZoomInfo}
    Usage`, `{Lusha,Apollo,ZoomInfo} Credit Skipped`, `Collect Credits` (Merge, append,
    numberInputs 3, TWO producers per input), `Build Credits Summary`, `Credits Broadcast` (Merge,
    combineAll), `Filter Build Response Rows`, `Build Response`.
  </read_first>
  <behavior>
    Walk the COMMITTED `n8n/wf_enrichment_cloud.json` (v1, so no `allowLegacy`) with
    `providers: ["lusha", "apollo", "zoominfo"]`, `mode: "propose"`, two contact-by-email rows —
    the 12354 shape with the providers turned ON, which is what makes each `Collect Credits` input
    reachable from more than one producer.

    - `runData["Collect Credits"].length === 1`
    - `runData["Build Credits Summary"].length === 1`
    - `runData["Lusha Credit Skipped"] === undefined` (and the Apollo / ZoomInfo twins) — the
      load-bearing negative: the disabled-lane sentinels must not run when the provider IS enabled.
    - `trace.merges["Collect Credits"].runs[0].sources` names the three `Adapt X Usage` nodes.
    - `Build Response` returns 2 rows, and BOTH carry the SAME `remaining_credits`, listing all
      three providers, with Lusha's and ZoomInfo's credits the stubbed numbers (Apollo's may be
      `null` — `extractCredits`'s apollo arm trusts only a top-level numeric `remaining`; assert
      whichever the stub actually produces and say which in the header).
  </behavior>
  <action>
Header must state, in prose, the engine rule the test relies on and where it came from: under v1 a
node that ran and emitted zero items makes no delivery, and a Merge with a partially filled pending
run drains at end-of-run with `requiredInputs: 1` — both from Task A, both observed on executions
12354/12355/12356. Without that rule this test asserts nothing.

Reuse `enrichmentMixedBatch.test.mjs`'s `baseStubs()` shape verbatim for the contact lane's HTTP
nodes and ADD the three credit-lane stubs, shaped so `remaining_credits` is not trivially null:
`Lusha Usage` -> `[{ credits: { remaining: <n> } }]`; `Apollo Usage` -> `[{ remaining: <n> }]`
(read `extractCredits`'s apollo arm before choosing); `ZoomInfo Usage Mint` -> the token shape
`ZoomInfo Usage`'s own mint/cache Code node expects (read it — `access_token` plus an expiry).
The walker throws by name on an unstubbed HTTP node, so a missing one surfaces.

RED-capable by construction, and state the direction in the header: if the walker predicts
`Collect Credits` firing more than once, this test FAILS. Note the failure mode precisely so a
future reader does not look for the wrong symptom — a second `Build Credits Summary` would reach
`Credits Broadcast` (combineAll) on input 1 with input 0 unfilled, producing 0 items, so the FIRST
summary silently wins rather than `remaining_credits` visibly duplicating.

If it FAILS: STOP. Do not touch `scripts/build_cloud_workflows.py` and do not regenerate any
`n8n/wf_*.json`. Mark it `test("...", { todo: true }, ...)` — node:test RUNS a todo test and reports
the failure without failing the exit code, so the RED stays VISIBLE in the output; never `skip`,
which hides exactly what the finding is. Then record the finding as a new todo under `.planning/todos/pending/` in Task C naming
the builder follow-on (`assert_merge_input_contract`, D-70-20, checks at-least-one producer per
input, never at-most-one) — the builder decision is the operator's.
  </action>
  <verify>
    <automated>node --test tests/n8n/creditsSummaryUnderV1.test.mjs && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    The credit lane's behaviour under v1 with all three providers enabled is pinned by a test that
    names its engine rule; either it is GREEN on a single `Collect Credits` fire / single
    `Build Credits Summary` / one shared `remaining_credits`, or it is RED and the finding is
    recorded in Task C with no builder change. Failing direction: flip the request to
    `providers: []` and the three `X Credit Skipped` negatives must invert.
  </done>
</task>

<task type="auto">
  <name>Task C: close the walker todo, open the builder-contract todo</name>
  <files>.planning/todos/pending/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md, .planning/todos/completed/, .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md</files>
  <read_first>
    `ls .planning/todos/completed/` and one recent file in it — match its front-matter and
    resolution-note convention exactly rather than inventing one.
  </read_first>
  <action>
Move `2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md` into
`.planning/todos/completed/` with a resolution note naming this quick task, the two walker branches,
and `tests/n8n/walkerEngineFidelityV1.test.mjs` as the fidelity evidence.

Carry its "Builder follow-on" section OUT into its own pending todo,
`2026-09-11-merge-input-contract-allows-many-producers-per-input.md` — it must NOT be closed by
proximity. Task B green does not answer it: it tests ONE Merge under ONE input shape, while
`Decide Company Action Merge` still double-fired live on 12354-12356. One paragraph, not a plan:
`assert_merge_input_contract` (D-70-20) checks at least one producer per input, not at most one;
six producer edges into a two-input append Merge is the shape that multi-fires under v1; the
open decision is whether >1 producer per input is a contract violation, or whether every consumer
of such a Merge must tolerate N runs. Name `scripts/build_cloud_workflows.py` as the file that
would change, and record that it was deliberately NOT changed here.

If Task B went RED, add the third todo it calls for, with the observed counts.

CLAUDE.md needs no edit: §13.0.3's two v1 rows were added 2026-09-11 and already state both
observations. Do not restate them.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md && ls .planning/todos/completed/2026-09-11-walker-rule-c-zero-item-output-not-a-delivery-under-v1.md && ls .planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md</automated>
  </verify>
  <done>
    The walker todo is in `completed/` with a resolution note; the builder-contract question
    survives as its own pending todo; no `scripts/build_cloud_workflows.py` or `n8n/wf_*.json` diff
    exists anywhere in this task's commits. Failing direction: the `test !` guard fails if the old
    todo is still pending.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen fixture -> `new Function` | The walker executes committed `jsCode` from the frozen graph copy. Repo-controlled JSON only; never attacker-controlled input. |
| live execution recording -> committed fixture | `exec_1235{4,5,6}.runData.json` carry verbatim request headers from a live caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-09 (reused) | Tampering | `walkWorkflow.mjs` `new Function` over `jsCode` | medium | accept | Pre-existing, accepted Phase 70 decision: the same mechanism n8n's own Code node uses, over repo-controlled JSON. The new frozen copy is `cp` of a committed file — no new source of code. |
| T-0TZ-01 | Information Disclosure | `wf_enrichment_cloud.v1.2026-09-10.json` (new frozen copy) | low | mitigate | It is a byte copy of an already-committed file that ships DISARMED (`ALLOW_HUBSPOT_RECORD_WRITES = "false"`, `TEST_RECORD_IDS = ""`) and carries no credential material — n8n credentials are referenced by id, never inlined. Confirm both flag literals are `"false"`/`""` in the copy before committing. |
| T-0TZ-02 | Information Disclosure | the three v1 recordings this task reads | low | accept | Already redacted at freeze time and guarded by `v1RuntimeRecordings.test.mjs`'s secret check. This task adds no new recording, so no new redaction step is owed. |
| T-0TZ-03 | Tampering | `n8n/wf_*.json`, `scripts/build_cloud_workflows.py` | high | mitigate | Out of scope by hard rule. Task C's `<done>` requires zero diff in either; a Task B RED is recorded as a todo, never as a builder edit. Nothing here deploys, bounces or arms. |

No package-manager install is added by this plan, so no legitimacy gate applies.
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` passes, with no fewer passing assertions than the 1083 baseline.
- `tests/n8n/walkerEngineFidelity.test.mjs` has ZERO diff — its three legacy cases pass unmodified.
- `git status` shows no diff under `n8n/` and none in `scripts/build_cloud_workflows.py`.
- The RED observed before the walker change is recorded verbatim in the SUMMARY (which assertion, what the walker produced instead) — a green-only claim is not acceptance here.
- `.venv/bin/python -m pytest` is NOT required: nothing in this plan touches Python.
</verification>

<success_criteria>
The walker models n8n v1 on both behaviours Gate 11 observed, proved by walking the frozen v1 graph
with the recordings' own inputs; the legacy model survives intact behind `allowLegacy`; the credit
lane's v1 behaviour is pinned or its defect is recorded; the walker todo is closed and the builder
question is not closed with it.
</success_criteria>

<output>
Create `.planning/quick/260911-0tz-fix-the-walker-rule-c-first-then-the-cre/260911-0tz-SUMMARY.md` when done
</output>
