---
quick_id: 260911-w6q
phase: quick-260911-w6q
plan: 01
type: execute
wave: 1
depends_on: ["260911-w6o", "260911-w6p"]
files_modified:
  - operator-claude-plugin/skills/review-triage/SKILL.md
  - operator-claude-plugin/tests/test_review_triage_facets.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
files_deleted: []
autonomous: true
requirements: ["F2-3"]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low          # DERIVED: estimate-calibration sample_count 0, factor 1.0

must_haves:
  truths:
    - "The operator asking what needs review sees HubSpot-flagged conflicts AND locally held rows in ONE numbered table, never two separate answers."
    - "A held `new_person` row can be created from that table in this sitting, without leaving review-triage and without a second skill invocation."
    - "A create is reported as landed only after an independent HubSpot re-read finds the person, never from the dispatch response."
    - "A held entry the four facets cannot act on is counted and reachable, never silently dropped."
    - "Nothing in this item makes a live HubSpot, n8n or provider call; nothing is armed."
  artifacts:
    - operator-claude-plugin/skills/review-triage/SKILL.md
    - operator-claude-plugin/tests/test_review_triage_facets.py
  key_links:
    - "held_queue.load() -> w6p facet -> the numbered table (the read join this item exists to create)"
    - "the grant's `contacts` lane -> contact-upload's own dispatch step, followed by heading (the ONE ingest dispatch implementation, never a second copy)"
    - "preingest.match_batch confirm pass -> held_queue mark verb, joined on email (the proof-before-mark link)"
---

<objective>
`review-triage` today reads ONLY the HubSpot `lv_enrichment_needs_review` queue and never
opens `held_queue.json`. Make it read BOTH queues and render ONE numbered table with four
facets, and give a held `new_person` row a `create` that actually lands through the
contact-ingest lane, is confirmed by an independent re-read, and is then marked on the
entry.

Purpose: close the F2 half the operator ruled on 2026-09-11 — "review-triage reads both
queues in one table; create lands through the ingest lane under a review-lane grant".
Today a held row has no route to HubSpot at all
(`.planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md`).

Output: an amended `skills/review-triage/SKILL.md`, one new offline test module, and the
sequence-coverage registry entries the new fences require. No plugin version bump and no
CHANGELOG edit — item 260911-w6r owns the 0.47.0 cut.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/UAT-autonomous-batch-2026-09-09.md
@.planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md
@operator-claude-plugin/skills/review-triage/SKILL.md
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/skills/contact-upload/SKILL.md
@operator-claude-plugin/skills/enrich-records/SKILL.md
@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/scripts/write_grant.py
@operator-claude-plugin/scripts/preingest.py
@operator-claude-plugin/tests/test_skill_sequence_coverage.py
</context>

<upstream_api_contract>
**READ `operator-claude-plugin/scripts/held_queue.py` ON DISK BEFORE EDITING ANYTHING.**
This plan was written before item `260911-w6p` merged. It ASSUMES the following surface;
where the shipped names differ, use the shipped ones and change nothing else. Never add a
second copy of a classifier or a verb — `held_queue.py` is w6p's file and is NOT in this
plan's `files_modified`.

Assumed, from w6p's own task text:

- `held_queue.facet(entry) -> str` — a read-time classifier over a `no_match` entry,
  returning one of three values with module constants in the shape
  `held_queue.NEW_PERSON` / `held_queue.NEEDS_COMPANY` / `held_queue.NOTHING_FOUND`.
  It derives from the entry's enriched `row` (w6o persists that row).
- Durable per-entry verbs `create` / `skip` / `retry` / `drop` writing a status,
  a timestamp and a `run_id` onto the entry — assumed reachable as
  `held_queue.mark(entries, row_id, verb, *, run_id) -> dict` returning the updated
  entries map, and assumed readable back as a plain `entry.get("status")`.

**Two deliberate reductions, to keep this plan's assumptions as small as possible:**
1. Read an entry's terminal state with `entry.get("status")` directly — do NOT assume a
   w6p accessor function for it.
2. Do the facet BUCKETING here in the SKILL fence (a `setdefault` loop over
   `held_queue.facet`), not through an assumed w6p grouping function.

If `facet` or the mark verb is absent or differently named when you read the file, STOP
and report it rather than writing the function yourself.

**Sibling-item overlap, stated so the merge is not a surprise:** item `260911-w6r` also
edits `tests/test_skill_sequence_coverage.py` (its own task text says to pin its changes
with the sequence-coverage tests) and owns both the `CHANGELOG.md` 0.47.0 section naming
F2-1..F2-4 and the closing of
`.planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md`.
**w6r must sequence AFTER this item.** This plan touches neither the CHANGELOG, nor
`plugin.json`, nor that todo file — what now exists goes in this item's SUMMARY instead.

**Two facts to VERIFY on disk before writing the SKILL sentence that depends on each —
do not assume either:**

- **`send_domains` for a create.** This plan's Task 1(c) says a held row's grant domain
  is the domain of its own enriched email. Read the step in
  `skills/contact-upload/SKILL.md` where `send_domains` is bound for a create and mirror
  it EXACTLY. If that lane derives the allowlist domain from something else, the create
  comes back write-blocked and this plan's sentence is wrong — use the lane's own rule
  and say so.
- **`n8n_arming.armed_window`'s own pre-arm scope.** `preflight_before_send` is already
  confirmed lane-scoped. The ARM itself is not. Grep `scripts/n8n_arming.py` for any
  read of other workflows' flags inside the arm path. If the arm reads liveness across
  workflows, the open review window's own flag would refuse the ingest arm mid-sitting
  — in that case sequence the whole create block AFTER the review window's `with` block
  exits and disarms, and say that plainly in the SKILL. That sequencing is cheaper than
  any new guard; do not add one.
</upstream_api_contract>

<assumption_delta_decision>
`assumption-delta scan` is not runnable for a quick item (no ROADMAP phase section) —
outcome recorded as `skipped`, reason `phase_unresolved`. Recorded by hand because the
change is a genuine `pluralization` signal anyway: review-triage's queue goes from ONE
source (HubSpot `lv_enrichment_needs_review`) to TWO (that, plus local `held_queue.json`).

- Primary noun: **the review queue** — now a two-source queue, rendered as one table.
- Decision: **add-alongside**, not promote. The two sources have different schemas
  (a HubSpot record with a frozen candidate vs. a local entry with a hold code and an
  enriched row), different verbs (approve/reject vs. create/skip), and different
  authorities (the backend's review endpoint vs. the ingest lane). A unified store would
  be a third authority that can drift from both.
- Accepted debt: the numbering is the ONLY thing unified. What would force a promote: a
  third queue source, or a verb that has to mean the same thing on both sides.

API-coverage checkpoint: `skipped` — no external API/SDK surface is introduced (every
call in this plan is an existing plugin function). No `COVERAGE.md` is produced.
Schema-push gate: no ORM/schema file is in scope. Skipped silently.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One held new person, end to end — read, render, create, confirm, mark</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/tests/test_review_triage_facets.py</files>
  <read_first>
    operator-claude-plugin/scripts/held_queue.py (the shipped w6p surface — see
    &lt;upstream_api_contract&gt;), operator-claude-plugin/skills/review-triage/SKILL.md
    steps 2 and 4, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md step 7's
    CSV-build fence (`strip_enrichment_extras` then `strip_row_id` then
    `write_dispatch_csv`) and its "follow contact-upload's own steps by heading" list,
    operator-claude-plugin/skills/enrich-before-ingest/SKILL.md step 2's match fence
    (`chunking.plan_chunks` then `preingest.match_batch` then
    `preingest.classify_matches`), operator-claude-plugin/scripts/write_grant.py
    `plan_grant` / `authorize_review_batch` / `authorize_send`.
  </read_first>
  <behavior>
    - Given a held-queue map holding one `no_match` entry whose enriched row carries an
      email and a company, the documented read fence buckets it under the new-person
      facet and it appears in the table.
    - An entry already carrying a terminal status is excluded from the table by that
      same fence.
    - The documented CSV-build fence turns the chosen entry's row into a dispatch CSV
      whose columns are the ingest lane's own (no `row_id`, no enrichment extras), and
      the file holds exactly the chosen rows — never every held row.
    - The documented confirm fence joins the re-read's auto-matched entries back to the
      held rows BY EMAIL and yields a HubSpot object id for the created person.
    - A row absent from the re-read is NOT marked created.
  </behavior>
  <action>
Amend `skills/review-triage/SKILL.md` so a held new-person row can be created in a
sitting. Keep every existing sentence unless this task says to change it — three
literals are pinned by `tests/test_disclosure_audit.py` and must survive verbatim: the
per-record-ritual sentence, the `open_grant(` call passing `confirmation`, and the
absence of any pre-spend-pause mention. Introduce NO autonomy-level vocabulary
(`tests/test_autonomy_switch_prose.py` pins its absence here).

(a) **Step 2 reads both queues.** After the existing HubSpot fetch/render, add a second
read. The held queue is local, needs no network, and costs nothing:

```python
import held_queue

held_state = held_queue.classify_read()
held_entries = held_queue.load()
open_entries = {rid: e for rid, e in held_entries.items() if not e.get("status")}
by_facet = {}
for rid, entry in open_entries.items():
    by_facet.setdefault(held_queue.facet(entry), []).append(rid)
```

State plainly, in prose, that `classify_read()` answering `ANOMALOUS` means the held
queue was NOT read — say so and do not render it as empty, the same rule the HubSpot
`available: false` branch already carries two paragraphs above. Say why the held queue
is local: it holds rows that never reached HubSpot at all, so there is no record to
flag and nothing to search for.

**Say where a `retry`-marked entry went, or this filter silently drops it.** The
`not e.get("status")` test correctly hides an entry already marked created, skipped or
dropped — those are decided. A `retry` mark is NOT decided: it belongs to the next
enrichment run's own resume, not to this table. Say that in one sentence and account for
those entries in the parked line Task 2 adds, so no open entry is both unlisted and
uncounted.

(b) **The table.** Render ONE numbered markdown table over both sources, continuously
numbered from 1, HubSpot conflicts first, then the held new-people rows. Each held row
shows the person (first name, last name), the company, the enriched email the waterfall
found, and the hold's own reason. Say in prose that the email shown is what the run
recovered, not a guess, and name its source. (Task 2 adds the remaining two facets and
the parked line — do not invent them here.)

**Pre-suggest the create, under the table, as a ready answer the operator can send
back verbatim** — one line offering the bulk create for the new-people facet with its
count restated in the same shape `enrich-before-ingest/SKILL.md` step 3's own bulk
example uses. Rendering the ready answer is what keeps this frictionless; it is an
OFFER and never an action, and the count in it is the count of new-people rows actually
shown, never a total that includes a parked or a conflict row.

(c) **Step 4's grant gains the ingest lane.** Change the existing `plan_grant` call so
`lanes` is `["review", "contacts"]` when the sitting includes at least one create, and
`allow_create=True` in that case; `record_domains` for a held row is the domain of its
own enriched email. Keep `lanes=["review"]`, `allow_create=False` and `providers=[]`
for a conflicts-only sitting — a review batch still spends no provider credit, and a
create spends none either (the enrichment already happened; this is the ingest write
only). Keep the existing `authorize_review_batch` window exactly as it is: that window
is the REVIEW workflow's, and it neither covers nor blocks an ingest send.

Say explicitly, in one sentence, why the two windows do not collide: an ingest send's
own pre-flight reads only the lane it is sending on, so the review window's own arm on
the review workflow is never mistaken for a dirty backend
(`write_grant.preflight_before_send` scopes `workflow_ids` to that one lane).

(d) **Build the CSV — this skill's own fence, because holding a row back is impossible
any other way.** Add a step between the decision and the send:

```python
import extraction, preingest

create_rows = [open_entries[rid]["row"] for rid in chosen_row_ids]
created_by_row_id = {rid: open_entries[rid]["row"].get("email") for rid in chosen_row_ids}
create_rows = preingest.strip_enrichment_extras(create_rows)
create_rows = extraction.strip_row_id(create_rows)
extraction.write_dispatch_csv(create_rows, send_path)
send_row_count = len(create_rows)
```

`chosen_row_ids` and `send_path` bind by name from the operator's answered table lines
and this sitting's scratch directory. Say why a rewritten CSV of exactly the chosen rows
is the only shape allowed — a held row nobody chose must be unable to reach the backend.

(e) **The send is contact-upload's, followed by heading.** Do NOT copy the dispatch
ritual into this file. Say, in prose, that from here the operator's create follows
`contact-upload/SKILL.md`'s own steps by heading, unmodified — naming them exactly:
"Dispatch under an open grant, or otherwise only once the operator has said yes to this
send.", "Report the outcome — per record, not a bare acceptance.", "Re-check, only when
the operator asks.", "Retry a transport failure — same dispatch, same arming gate." and
"Clean up." State the two reasons in one clause each: there is exactly ONE ingest
dispatch implementation in this plugin and a second copy here would drift from it; and
because the grant opened at step 4 already covers the contacts lane, that step's
open-grant branch asks the operator nothing further — no export, no second skill
invocation, no extra prompt. Name what this skill hands it: `send_path`,
`send_row_count`, `send_ids` (empty — a create names no existing record),
`send_domains` and `allow_create=True`, plus the open `grant`.

(f) **Confirm by re-reading, then mark.** After the send, add the confirm fence — ONE
call for the whole create batch, never one per row (the F1 lesson: each of these costs
an n8n execution):

```python
import chunking, config_gate, preingest

cfg = config_gate.load_config()
confirm_spec = preingest.build_rows_spec(
    [{"email": email} for email in created_by_row_id.values() if email])
confirm_plan = chunking.plan_chunks(
    confirm_spec, chunking.chunk_ceiling(cfg, key="max_rows_per_match_request"))
confirm_outcome = preingest.match_batch(confirm_plan, cfg)
confirmed = preingest.classify_matches(
    confirm_spec["rows"], confirm_outcome.responses,
    unchecked_row_ids=confirm_outcome.unchecked_row_ids)
landed = {entry["row"]["email"]: entry["hs_object_id"]
          for entry in confirmed["auto_matched"]}
```

Say why this call and not the dispatch's own report: the ingest report is the backend's
account of its own write, and this skill's standing rule is that the verdict comes from
re-reading the record afterwards. Say why the join is on email and not on `row_id`:
`build_rows_spec` mints fresh ids and refuses a row that already carries one, so the
held row's id does not survive the confirm pass — email is the identity the ingest lane
itself resolves on. Say plainly that an `unchecked` confirm row means the re-read did
not complete, which is NOT the same as the person not being there, and that the honest
report is "sent, not yet confirmed" with an offer to re-read.

**`unmatched` on a just-created row means "not yet visible", not "not there".** HubSpot
search lags a fresh create by seconds to about a minute — `preingest.LAG_RETRY_LIMIT`
exists for exactly this. Give an `unmatched` confirm row the SAME wording an `unchecked`
one gets: sent, not yet confirmed, with an offer to re-read. The mark is DEFERRED, never
refused, and never inverted into "the create failed".

Then mark, only for a row the re-read actually found:

```python
import held_queue

entries = held_queue.load()
for row_id, email in created_by_row_id.items():
    if email and email in landed:
        entries = held_queue.mark(entries, row_id, "create", run_id=run_id)
held_queue.save(run_id, entries)
```

(reconcile the verb's name and signature against the shipped `held_queue.py` per
&lt;upstream_api_contract&gt;). Say that a row not in `landed` keeps its open status and
is shown again next sitting — never marked on a hope.

Write `tests/test_review_triage_facets.py` with a module docstring naming this item and
the F2 ruling. Drive the fences above as real Python against a held-queue map built
inline in the test (two entries in the recorded `a254d1eda71246a2a964922cdf5c2bd2`
shape: one new-person row with an email, one already carrying a terminal status),
written to a `tmp_path` file and read back through `held_queue.load(path=...)`. Assert
the five behaviours above. Use a fake transport for `match_batch` — no network, no live
call of any kind. Follow `tests/conftest.py`'s existing path bootstrap and leave its
autouse `no_durable_writes` fixture untouched.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_review_triage_facets.py -x -q</automated>
    <automated>/usr/bin/grep -c 'held_queue' operator-claude-plugin/skills/review-triage/SKILL.md</automated>
    <automated>test "$(/usr/bin/grep -c 'run_report.build_run_report(' operator-claude-plugin/skills/review-triage/SKILL.md)" = "0"</automated>
  </verify>
  <done>
    The new test module passes. `review-triage/SKILL.md` names `held_queue` (grep count
    is non-zero) and still carries no mandatory-report call, so
    `test_mandatory_report_call_sites.py`'s exclusion stays true without being edited.
  </done>
  <reversibility rating="reversible">SKILL.md prose and one new test file; revert is a single-file checkout.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The other three facets — conflicts renumbered, needs-a-company, parked</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/tests/test_review_triage_facets.py</files>
  <read_first>
    operator-claude-plugin/skills/enrich-records/SKILL.md lines 55-95 (the
    "Companies that may not be in HubSpot yet" form, its mandatory-domain rule and its
    website-confirm table), CLAUDE.md §13.0.1 (the ingest lane resolves a company by
    manual id, then email domain, then exact name; a create that resolves none is
    downgraded to review server-side), operator-claude-plugin/scripts/confidence.py
    (`ALL_HOLD_CODES` — six codes, only `no_match` is faceted).
  </read_first>
  <behavior>
    - A held `no_match` entry with an email and no company buckets under the
      needs-a-company facet and its table line offers the company verb, not the plain
      create verb.
    - A held `no_match` entry with no usable email buckets under the nothing-found
      facet and does NOT appear in the table.
    - A held entry whose hold code is any of the five non-`no_match` codes also does
      not appear in the table and IS counted in the parked total.
    - The parked total equals every open entry the table did not list — no open entry is
      both unlisted and uncounted.
  </behavior>
  <action>
Extend the table section written in Task 1 to the full four facets.

(a) **Conflicts keep their verbs and join the numbering.** HubSpot-flagged rows keep
approve and reject exactly as steps 5-8 already describe them, and they are simply the
first block of the same numbered table. Nothing about the approve/reject ritual changes.

(b) **Needs a company.** A held row with an email and no company gets the company verb
instead of the create verb, because its create would not land: per CLAUDE.md §13.0.1 the
ingest lane resolves a row's company by manual id, then the email's own domain, then an
exact name — and a create that resolves none is downgraded to review **server-side**.
Say that in the operator's own terms, and say plainly that this plugin does not and will
not reproduce that downgrade client-side: the backend is the one authority on it.

The company verb hands off BY HEADING to `enrich-records/SKILL.md`'s
"Companies that may not be in HubSpot yet" form, unmodified — do not copy that form's
website-confirm table or its mandatory-domain rule into this file. What this skill
supplies is the domain, derived from the held row's own email address; the company's
NAME comes from the operator, never from this skill, because deriving a company name
from a domain is a guess and nothing here is guessed. Say what happens next, precisely,
because the obvious sentence here is false: **the facet does not flip.** It reads the
held entry's own `row["company"]`, not HubSpot, and nothing writes a company name back
into that row — so the row still reads as needing a company even after the company
exists. What changes is that its create now LANDS, because §13.0.1's ingest lane
resolves the company by the email's own domain regardless of what the row's company
field says. So offer that row's create **in the same sitting**, immediately after the
company create lands, and say the label has not changed and does not need to.

(c) **Parked.** A held row with no usable email is not listed: there is nothing to
create it by. Print exactly one line under the table giving the parked count and the
words the operator can say to see them. Make the parked bucket honest and total: it is
every open held entry the table did not list — the nothing-found rows AND every entry
whose hold code is not `no_match` (there are five others: `unparseable`,
`unadjudicated_conflict`, `unknown_tier`, `ambiguous_candidates`,
`no_table_row_matched`), each shown with its own hold reason when the operator asks.
Say in one sentence why they are parked rather than dropped, in this skill's existing
voice: an unread or unshown queue presented as an empty one is the worst answer this
skill can give.

Add the matching assertions to `tests/test_review_triage_facets.py`: extend the inline
held-queue map with a needs-a-company entry (an email, no company — the recorded Katie
Poggioli shape), a nothing-found entry, and one entry carrying a non-`no_match` hold
code; assert each buckets and renders as the behaviours above state, and assert the
parked total accounts for every open entry the table did not list.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_review_triage_facets.py -x -q</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py operator-claude-plugin/tests/test_disclosure_audit.py operator-claude-plugin/tests/test_autonomy_switch_prose.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py -q</automated>
  </verify>
  <done>
    All four facets are documented and tested; the four pre-existing guard suites over
    this SKILL still pass unmodified.
  </done>
  <reversibility rating="reversible">Prose and test assertions only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The answer vocabulary, and registering every new fence</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md, operator-claude-plugin/tests/test_skill_sequence_coverage.py, operator-claude-plugin/tests/test_review_triage_facets.py</files>
  <read_first>
    operator-claude-plugin/skills/enrich-before-ingest/SKILL.md step 3 (the numbered
    table's answer vocabulary and its two safety rules),
    operator-claude-plugin/tests/test_skill_sequence_coverage.py (the `COVERED` and
    `NOT_A_PIPELINE` registries and the existing `review-triage` entry),
    operator-claude-plugin/skills/review-triage/SKILL.md step 5's opening sentence about
    which words are decision words.
  </read_first>
  <action>
(a) **One vocabulary, ported by heading — never a second one.** Document the answer
forms for the numbered table, taking the rules verbatim in substance from
`enrich-before-ingest/SKILL.md` step 3 and saying that is where they come from:
per-item answers keyed by the row's own label; a blanket decline is accepted; a bare
blanket approval with no named scope is refused outright; a bulk affirmative must
restate its count, in the same shape step 3's own example uses, because restating the
count is what proves the scope was seen rather than assumed; an unanswered row stays
pending and is restated in full next turn, never defaulted in either direction; and one
malformed answer line refuses the WHOLE table before anything is applied, naming the
offending line. The per-facet verbs are the ones Tasks 1 and 2 documented.

(b) **Amend step 5's opening claim.** Its sentence today says the two decisions are
approve and reject and that nothing else is a decision word. That is now false for this
table. Rewrite it so it stays true and stays narrow: on a **HubSpot-flagged conflict**
those two remain the only decision words, and the held-queue rows have their own verbs,
named where their facet is. Do not widen the approve/reject vocabulary itself. Grep the
test suite for the old sentence before editing it and update any test that pins it.

(c) **Register every new fence.** `tests/test_skill_sequence_coverage.py` fails on any
documented python block with two or more scripts-module calls that is neither in
`COVERED` (pointing at a named composition test that actually drives the join) nor in
`NOT_A_PIPELINE` with a reason. Run the suite, read the failure messages — they name the
skill, the block's line and the exact call tuple — and add one key per new sequence:
point the read/bucket fence, the CSV-build fence, the confirm fence and the mark fence
at the corresponding `test_review_triage_facets.py::<test>` names written in Tasks 1-2,
adding a test for any join that is registered but not actually driven. Use
`NOT_A_PIPELINE` only where no result genuinely flows from one call into the next, with
the reason stated in the same voice as the existing `review-triage` entry. Leave
`MAX_GRANDFATHERED` at 0 and add nothing to `GRANDFATHERED_UNCOVERED`.

(d) **Leave the todo alone — record the change in the SUMMARY instead.**
`.planning/todos/pending/2026-09-11-no-plugin-path-turns-an-approved-held-row-into-a-sent-row.md`
is `kind: design` and this item builds the review-triage half of its ruling, but item
260911-w6r owns the other half AND owns closing it; two items appending to the same
Ruling section is a merge conflict for no gain. Do NOT edit, move or annotate that file
here. Instead, state in this item's SUMMARY exactly what now exists — a held row's route
to HubSpot through review-triage — so w6r can close the todo against a written record.
Create no new todo either; a residual is a sentence in the SUMMARY unless it is a real
defect with evidence, per CLAUDE.md §31 rule 1.

Then run the full plugin suite and the n8n suite. The n8n suite must be untouched and
the `n8n/` diff must be empty — this item changes no workflow JSON.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
    <automated>node --test tests/n8n/*.test.mjs 2>&amp;1 | tail -5</automated>
    <automated>test -z "$(git status --porcelain n8n/)"</automated>
  </verify>
  <done>
    The whole plugin suite passes with no registry entry left unclaimed; the n8n suite is
    unchanged and green; `git status --porcelain n8n/` is empty. `plugin.json` and
    `CHANGELOG.md` are untouched (260911-w6r owns the 0.47.0 cut).
  </done>
  <reversibility rating="reversible">Prose, registry entries and one todo annotation.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator conversation -> plugin | Free-text table answers choose which rows are written. Untrusted in the sense that a mis-scoped answer must not widen a write. |
| plugin -> n8n contact-ingest webhook | The only path in this item that can create a HubSpot record. Crossed only inside an armed, record-scoped window under an open grant. |
| local disk (`held_queue.json`) | Row snapshots of real people, read here and marked here. Never widened by this item. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w6q-01 | Tampering | the create send's allowlist | high | mitigate | Task 1(d)/(e): the CSV holds only the rows the operator chose, and the window is contact-upload's own per-send one scoped to that send's records, never the grant's whole set. Test asserts the CSV holds exactly the chosen rows. |
| T-w6q-02 | Repudiation | bulk `create` on the numbered table | medium | mitigate | Task 3(a): a bare blanket affirmative is refused; a bulk affirmative must restate its count; one malformed line refuses the whole table before anything is applied. |
| T-w6q-03 | Spoofing | the confirm pass's verdict | high | mitigate | Task 1(f): "created" is asserted only from an independent HubSpot re-read joined on email, never from the dispatch response or the backend's own report. A row absent from the re-read keeps its open status. |
| T-w6q-04 | Information disclosure | held rows written back to disk | medium | mitigate | This plan does not touch `held_queue.py`; `save()`'s allowlist and forbidden-name refusal apply unchanged to every mark. No new field is persisted. |
| T-w6q-05 | Elevation of privilege | two nested armed windows | high | mitigate | Task 1(c): the ingest window is contact-upload's own on the ingest workflow; `preflight_before_send` scopes its liveness read to the one lane, so neither window can arm or excuse the other. Documented in the SKILL, not merely assumed. |
| T-w6q-06 | Denial of service | confirm re-reads | low | accept | One `match_batch` per create batch is one n8n execution. Bounded by the batch, and cheaper than the six-execution F1 leak it is explicitly written to avoid. |
| T-w6q-SC | Tampering | npm/pip/cargo installs | high | accept | This item installs no package. No `requirements.txt` or `package.json` change is in scope, so the Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — whole plugin suite green.
- `node --test tests/n8n/*.test.mjs` — unchanged and green.
- `git status --porcelain n8n/` empty, and `operator-claude-plugin/plugin.json` /
  `CHANGELOG.md` unmodified.
- No live HubSpot, n8n or provider call made at any point; nothing armed.
</verification>

<success_criteria>
- `review-triage/SKILL.md` reads `held_queue.json` as well as the HubSpot queue and
  renders ONE continuously numbered table with the four facets and the parked line.
- A held new-person row has a documented, tested route to HubSpot: grant (review +
  contacts lanes, `allow_create`), a rewritten CSV of exactly the chosen rows, the send
  delegated by heading to contact-upload's one dispatch implementation, an independent
  re-read, then the mark.
- `held_queue.py` is not modified by this plan.
- `plugin.json` and `CHANGELOG.md` are not modified by this plan.
</success_criteria>

<output>
Create `.planning/quick/260911-w6q-f2-3-operator-claude-plugin-skills-review-triage-skill-md-to/260911-w6q-SUMMARY.md` when done.
</output>
