---
phase: quick-260911-ss5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/match_handoff.py
  - operator-claude-plugin/scripts/run_report.py
  - operator-claude-plugin/tests/test_match_handoff.py
  - operator-claude-plugin/tests/test_run_report.py
  - operator-claude-plugin/tests/test_forbidden_marker_parity.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
autonomous: true

must_haves:
  truths:
    - "The end-of-run report's row-accounting section names what it counted: this run's ENRICHMENT SCOPE (the rows that entered `run_state.start_run`), never `the original batch`. The 2026-09-11 UAT shape — a 4-row input of which 2 matched and 2 were enriched — renders 2/2 with a line that is true."
    - "`build_run_report`'s keyword argument is `enrichment_scope_row_count`; a signature probe finds no parameter left over from the old name."
    - "A run that recorded a handoff renders a `### Matched-id handoff` section naming the durable file `match_handoff-<run_id>.json` and each handed row (row_id -> hs_object_id); a run that recorded none renders that same section saying so plainly."
    - "`match_handoff.record_handoff` persists ONLY `row_id` / `hs_object_id` / `confirmed` — the `row` payload (the operator's spreadsheet names, emails, phone numbers) never reaches disk, proven by a test that passes a full `auto_matched` entry and reads the file back."
    - "An unreadable or foreign-run handoff file is a named `Known gaps` entry, never silently read as no-handoff. An ABSENT one is a gap ONLY on the lane that was supposed to write it — gated on `enrichment_scope_row_count is not None`, which only `enrich-before-ingest` passes — so the three other report-calling skills never raise `REPORT INCOMPLETE` for a file they were never meant to write."
    - "`match_handoff-*.json` is pruned on the same long TTL as its sibling per-run artifacts — the new family does not re-open F2's listing growth."
    - "`enrich-before-ingest/SKILL.md` step 7 records the handoff at the same place it computes `confirmed_ids`, and step 9 passes the renamed keyword; the file still contains neither substring `test_autonomy_switch_prose.py` forbids over it."
  artifacts:
    - operator-claude-plugin/scripts/match_handoff.py
    - operator-claude-plugin/tests/test_match_handoff.py
    - operator-claude-plugin/scripts/run_report.py
    - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  key_links:
    - "SKILL.md step 7 `classified[\"auto_matched\"]` -> `match_handoff.record_handoff(run_id, ...)` -> `match_handoff-<run_id>.json` -> `run_report._build_run_report`'s handoff read -> the rendered `### Matched-id handoff` section. This is the chain F9 found missing end to end: today the ids exist only as a Python list inside one fence."
    - "`run_report._add_gap(gaps, \"match_handoff\", classification)` — the SAME four-word classification contract every sibling store already uses, so the new store degrades exactly like the five primary ones rather than inventing a sixth posture."
    - "`run_report._PRUNE_LONG_TTL_GLOBS` — the new per-run file joins the family it was born into, same as `run_report-*.md` did at F3."
---

<objective>
Close **F9** from `.planning/UAT-autonomous-batch-2026-09-09.md`: the end-of-run report's
row-accounting line reads *"Row accounting 2/2 — matches the original batch's 2 row(s)"*
against a **4-row** input. The count is not wrong; the NAME is. `SKILL.md` line 1139 passes
`len(unmatched_rows)` — this run's enrichment scope — into a parameter named for the original
batch, and the renderer then claims the original batch. That scope was a deliberate
2026-09-09 same-day correction (counting the whole batch produced a false MISMATCH on every
batch with an ordinary matched row), so the SCOPE stays; only its name changes.

The second half of F9 is the real hole: the matched rows — John Miller (3601) and Craig
Sheppard (3501), plus batch 2's 7101 / 37400807974 — have **no durable record anywhere**. They
never call `run_state.start_run`; they are handed to `enrich-records` as `confirmed_ids`, a
Python list inside one fence in one process. "Every row came back exactly once" was
demonstrable only from the chat transcript.

Two changes, exactly as the UAT's own fix shape names them: **name the scope line**, and
**persist the matched-id handoff per run as a durable file the report names**.

Purpose: the operator can add the report's two numbers — enrichment scope + handoff — and get
their batch back, off-chat, from files.
Output: one new per-run durable store, one renamed keyword and two reworded report lines, one
new report section, two SKILL.md edits.

**Out of scope — do not touch.** `CHANGELOG.md` and `plugin.json` (batch item `260911-ss7`
owns the `0.46.0` bump naming F1/F9/F10/F11 — a second edit there is a merge conflict, not a
contribution). The step-2 match-outcome store (`260911-ss4`, F1) — this store holds the
CONFIRMED HANDOFF at step 7, which includes rows confirmed by the operator at step 3 and so is
a later, different fact than step 2's raw classification; the two are independent and neither
reads the other. `write_grant.close_grant` (`260911-ss6`, F11). `run_state.py`,
`run_manifest.py`, `written_records.py`, `held_queue.py`, `remainder_queue.py` — the five
primary stores are read by this plan and modified by none of it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/UAT-autonomous-batch-2026-09-09.md
@operator-claude-plugin/scripts/run_report.py
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `match_handoff.py` — the matched-id handoff, one durable file per run</name>
  <files>operator-claude-plugin/scripts/match_handoff.py, operator-claude-plugin/tests/test_match_handoff.py, operator-claude-plugin/tests/test_forbidden_marker_parity.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/written_records.py` lines 255-270 (`written_records_path`),
      485-500 (`_refuses_real_durable_write_under_pytest`), 572-615 (`classify_read`, `load`).
      This is the closest sibling in shape — one file per run, four-word read classification,
      degrade-never-raise. Copy its SHAPE; do not import from it.
    - `operator-claude-plugin/scripts/run_state.py` lines 1-140 (module docstring). It is the
      house style for a per-run store's docstring, including the "ONE FILE PER RUN
      (`run_state-<run_id>.json`)" naming note and the not-a-dotfile rule (Phase 23 D-04).
    - `operator-claude-plugin/scripts/run_report.py` lines 83-170 — the forbidden-name marker
      tuple and the whole-token matcher (`_tokenised`, `_token_runs`, `_looks_forbidden_*`).
      Your fresh copy needs only the plain `_looks_forbidden(value)` form the other six stores
      expose, not `run_report`'s key/value split (this module has no disarm/webhook vocabulary
      of its own to exempt).
    - `operator-claude-plugin/scripts/preingest.py` lines 505-556 and 620-650 — the exact shape
      of an `auto_matched` entry: `{"row_id", "row", "hs_object_id"}`, plus `"confirmed": True`
      on a row the operator confirmed at step 3. Note that `row` is the operator's whole
      spreadsheet row.
    - `operator-claude-plugin/tests/test_forbidden_marker_parity.py` (whole file, 4KB) — the
      seven-module registry your new module joins.
    - `operator-claude-plugin/tests/test_report_sufficiency.py` lines 1-20 and its
      `_POLL_LOOP_ALLOWED` guard: every script except `watch.py` must contain no `time`/`sched`
      import, no `sleep()`, and no `while` loop. Write this module accordingly (use
      `datetime`, as `run_state.py` does).
  </read_first>
  <behavior>
    - `handoff_path("r1")` resolves to `match_handoff-r1.json` in the same durable directory
      every sibling artifact resolves into (`durable_paths.resolve_state_path().parent`).
    - `record_handoff("r1", entries)` where `entries` carries a full `auto_matched` shape
      (including a `row` dict holding a name, an email and a phone number) writes a file whose
      round-tripped text contains NONE of those three values, and whose entries carry exactly
      `row_id`, `hs_object_id` and `confirmed`.
    - `load("r1")` after that call returns those projected entries, in input order.
    - `load` of an absent file returns `[]`; of a malformed/truncated file returns `[]`; of a
      file whose stored run id is a different run returns `[]` — never a partial list.
    - `classify_read` returns `absent` / `parseable` / `anomalous` / `another_run` for those
      same four states and never raises.
    - `record_handoff("r1", [{"row_id": "row-1", "hs_object_id": "3601"}])` called twice writes
      the same single-entry document (overwrite-whole, idempotent) — it does NOT accumulate.
      A second call with a SHORTER list replaces, because a declined step-3 proposal must be
      able to leave the handoff.
    - **`record_handoff("r1", [])` WRITES A FILE** holding an empty list, and `classify_read`
      then answers `parseable`, not `absent`. An all-unmatched batch legitimately hands nothing
      onward, and that is a different fact from "step 7 never ran" — Task 2's absent-handoff
      gap fires on the second and must not fire on the first. Assert both: the file exists, and
      `load` returns `[]` from it.
    - A grant-shaped key or value anywhere in the payload raises the module's own error and
      writes NOTHING (assert the file does not exist afterwards).
    - Under pytest, a `handoff_path` that resolves into the operator's REAL durable directory
      refuses the write and returns `False` rather than decorating live state.
  </behavior>
  <action>
    Write `operator-claude-plugin/scripts/match_handoff.py` — the plugin's EIGHTH persisted
    artifact family. Public surface, and nothing beyond it: `handoff_path(run_id)`,
    `record_handoff(run_id, entries, path=None) -> bool`, `load(run_id, path=None) -> list`,
    `classify_read(run_id, path=None) -> str`, the four classification words, and the module's
    own error class for a refused payload. No merge verb, no delete verb, no read-modify-write
    — this store is written once per run, from the one list step 7 already computes.

    Schema: a run id, a timestamp, and a list of entries carrying exactly three keys —
    `row_id`, `hs_object_id`, `confirmed` — and no others. `record_handoff` PROJECTS, it does
    not store what it is given. State the reason in the docstring: `auto_matched` entries carry
    the operator's whole spreadsheet `row`, and this file outlives the run; the projection is
    this store's own information-disclosure control, the same discipline `preingest.py`'s
    six-key `CANDIDATE_KEYS` projection already applies on the way in. A second reason worth
    one line: with names never stored, the known open defect in
    `.planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md`
    (a contact literally named Grant is refused by every store's matcher) cannot reach this
    store's payload at all. Do not attempt to fix that defect here — copy the matcher exactly
    as the seven siblings have it, so parity holds.

    `confirmed` is the truthiness of the entry's own confirmed flag — an email auto-match and
    an operator's step-3 confirmation are both in the handoff, and which was which is the one
    distinction worth keeping (it is the difference between a machine's answer and a person's).

    Carry, freshly reimplemented and never imported (D-69-01, the deliberate anti-DRY
    convention every sibling store documents): the ten forbidden-name markers, the whole-token
    matcher, and a recursive scan over keys and scalar values that raises before writing.
    Carry `_refuses_real_durable_write_under_pytest` verbatim in substance from
    `written_records.py`. Write through `durable_paths._atomic_write_0600` (same-package use of
    the shared helper — never a second atomic-write implementation). Filename is not a dotfile.

    Every read degrades rather than raises, for the same reason `run_manifest.py`'s docstring
    gives: a report that cannot read this file must say so, and a report is never worth an
    exception.

    Register the new module in `tests/test_forbidden_marker_parity.py`: add it to
    `_KEY_MATCHER_MODULES` and correct that file's docstring to name eight stores. Both corpus
    tests must then pass over eight modules unchanged — if either fails, your matcher copy
    diverged; fix the copy, never the corpus.

    Write `tests/test_match_handoff.py` covering the behaviours above, mirroring
    `test_written_records.py`'s monkeypatch-of-`durable_paths.resolve_state_path` idiom
    (`test_run_report.py`'s `_patch_durable_dir` is the same pattern if it reads more clearly).
    RED first: write the tests against the not-yet-existing module, observe the collection
    error and failures, then write the module. Record in the summary that they were seen RED.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_match_handoff.py operator-claude-plugin/tests/test_forbidden_marker_parity.py operator-claude-plugin/tests/test_report_sufficiency.py operator-claude-plugin/tests/test_no_backend_imports.py -q</automated>
  </verify>
  <done>`match_handoff.py` exists with the surface above; a full `auto_matched` entry round-trips to three keys with no spreadsheet value on disk; all four read states classify correctly and degrade to `[]`; a grant-shaped payload raises and writes nothing; the eight-module forbidden-marker parity suite is green; the no-poll-loop and no-backend-import guards still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The report names the scope it counted, and names the handoff</name>
  <files>operator-claude-plugin/scripts/run_report.py, operator-claude-plugin/tests/test_run_report.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/run_report.py` lines 578-595 (`_add_gap` — read its
      docstring: ABSENT is a legitimate zero for every store except `written_records`), 695-745
      (`_render_block`'s signature and the whole `### Row accounting` section, including the
      F4 comment above it), 239-252 (the two prune glob tuples), 863-1016
      (`build_run_report`, its docstring's row-count paragraph, and `_build_run_report`'s
      per-store read block).
    - `operator-claude-plugin/tests/test_run_report.py` lines 250-280 (the three row-accounting
      tests) and lines 733-766 (the two parametrized long-TTL prune families).
    - `.planning/UAT-autonomous-batch-2026-09-09.md` line 85 (F9 in full) — the wording this
      section has to stop producing, and the fix shape it names.
  </read_first>
  <behavior>
    - A run registering 2 rows, given a scope count of 2, renders a line naming **this run's
      enrichment scope** and does NOT contain the phrase the F9 finding quotes.
    - The same run given a scope count of 3 still renders `MISMATCH` — the F4 detection is
      unchanged in substance, only in wording.
    - Given no scope count at all, the section still says the comparison was not supplied.
    - A run with a recorded handoff renders a `### Matched-id handoff` section naming the file
      `match_handoff-<run_id>.json` and each entry as row_id -> hs_object_id; the
      row-accounting section carries a one-line scope note stating how many ids were handed
      onward, so scope + handoff are readable together.
    - A run with NO handoff file renders the same section saying none was recorded. Whether
      that also becomes a gap is GATED on the scope count: given no `enrichment_scope_row_count`
      (the three other report-calling skills), it adds NO entry to `Known gaps` and does NOT
      raise the `REPORT INCOMPLETE` banner; given one (the `enrich-before-ingest` lane, whose
      step 7 was supposed to write it), an absent file IS a named gap.
    - A run with a scope count and an EMPTY-but-present handoff file (an all-unmatched batch)
      renders "nothing was handed onward" and produces NO gap — the empty list is an answer.
    - A malformed handoff file, and a handoff file belonging to another run, each produce a
      named `Known gaps` entry regardless of the scope count.
    - `build_run_report`'s returned dict carries a `handoff` key.
    - A `match_handoff-abc.json` aged past the long TTL is pruned; within it, kept.
    - `inspect.signature(run_report.build_run_report)` has a parameter named
      `enrichment_scope_row_count` and none left over from the old name.
  </behavior>
  <action>
    Rename `build_run_report`'s keyword argument to `enrichment_scope_row_count`, threading the
    new name through `_build_run_report` and `_render_block`'s parameter list. A HARD rename —
    no alias, no back-compat shim: the argument is keyword-only. An alias would be exactly the
    dead flexibility that let the misleading name survive the 2026-09-09 correction in the
    first place.

    **Re-run the callers grep from the REPO ROOT before you start**, not from inside the
    plugin: `/usr/bin/grep -rn "original_row_count" --include='*.md' --include='*.py' .
    --exclude-dir=.git --exclude-dir=.venv`. At plan time that returned hits in exactly three
    live files — `scripts/run_report.py`, `tests/test_run_report.py` and
    `skills/enrich-before-ingest/SKILL.md`, all already in `files_modified` — plus `.planning/`
    records, which are history and stay unchanged. If the grep now shows a live hit anywhere
    else (an operator runbook under `docs/`, say), STOP and report it rather than renaming past
    it: an orphaned operator instruction is worse than the misleading name.

    Reword EVERY line of the `### Row accounting` section that names the original batch — there
    are three, not two: both outcome lines AND the `None` branch at lines 724-727. Name what
    was actually counted: this run's enrichment scope, the rows that entered
    `run_state.start_run`. The existing test matches the `None` branch on the substring "not
    provided by the caller", which can survive the rewording unchanged — keep it rather than
    churning a passing assertion. Keep the MISMATCH branch's existing
    diagnosis (a row dispatched outside `chunking.dispatch_plan`) intact; it is still the right
    conclusion, and F4's detection is not being weakened. Update the F4 comment block above the
    section and the `build_run_report` docstring paragraph to say the same thing in the same
    words, and add the F9 reference beside F4's — the two findings are one section's history.

    Add a scope note line to the same section, rendered whenever a handoff was read: state that
    rows matching an existing HubSpot record are deliberately NOT in this run's scope — they
    were handed to `enrich-records` — and give the count, pointing at the section below. This
    is the line that makes the whole batch addable; without it the rename is honest but still
    silent about the other half.

    Add the `### Matched-id handoff` section, immediately after `### Per-record outcomes` (the
    handoff is a per-record fact and belongs beside the other per-record facts, not at the
    bottom under spend). Name the FILE by name only — `match_handoff-<run_id>.json`, never an
    absolute path: `prune_durable_state`'s own comment gives the reason (a rendered path leaks
    the operator's home directory).

    Wire the store into `_build_run_report` beside its siblings: classify, `_add_gap`, load,
    and carry the loaded list into the returned dict under a `handoff` key. `_add_gap` handles
    ANOMALOUS and ANOTHER_RUN unchanged — the sibling contract, no special case.

    **The ABSENT case is the one place this store departs from its siblings, and the departure
    is deliberate.** F9 asks for an absent handoff to be named in `Known gaps`, but a blanket
    ABSENT gap would raise `REPORT INCOMPLETE` on every report the three other calling skills
    (`contact-upload`, `suggest-contacts`, `enrich-records`) produce, for a file they were
    never meant to write — devaluing a banner that exists to mean something. Gate it on a
    signal already in hand: **an absent handoff is a gap if and only if
    `enrichment_scope_row_count is not None`.** That argument is passed by `enrich-before-ingest`
    and by nothing else, so the gap fires exactly on the lane whose step 7 was supposed to
    write the file, and stays silent everywhere else. No new parameter, no caller change.
    Write the reasoning as a comment at the call site so the next reader neither widens it nor
    "fixes" it away. Note the boundary the gate depends on: an all-unmatched batch writes an
    EMPTY file (Task 1), which classifies `parseable`, so "handed nothing onward" never reads
    as "step 7 never ran".

    Update the `REPORT INCOMPLETE` banner sentence so its store inventory stays true (it
    currently counts five primary stores plus one run-audit record; a matched-id handoff is now
    also read). No test pins that sentence — confirm with a grep before editing, then edit.

    Add `match_handoff-*.json` to `_PRUNE_LONG_TTL_GLOBS` and to BOTH parametrized family lists
    in `test_run_report.py` (lines 733-766). An unpruned per-run family is how F2's listing
    growth started.

    Update the three existing row-accounting tests to the new keyword and the new wording, and
    add the new behaviours above as fresh tests in the same section. RED first for every NEW
    assertion; for the three EDITED tests, run them before the source change to observe them
    failing on the renamed keyword rather than assuming they will.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_run_report.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py -q</automated>
    <automated>.venv/bin/python -c "import sys, inspect; sys.path.insert(0, 'operator-claude-plugin/scripts'); import run_report; p = inspect.signature(run_report.build_run_report).parameters; assert 'enrichment_scope_row_count' in p, sorted(p); assert not [n for n in p if n.startswith('original')], sorted(p)"</automated>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0, 'operator-claude-plugin/scripts'); import run_report; assert 'match_handoff-*.json' in run_report._PRUNE_LONG_TTL_GLOBS, run_report._PRUNE_LONG_TTL_GLOBS"</automated>
  </verify>
  <done>No line of the row-accounting section names the original batch, and the MISMATCH branch still fires on a real mismatch; the handoff section renders for the recorded, the empty and the not-recorded cases; unreadable and foreign-run handoff files are named gaps unconditionally, and an absent one is a gap only when a scope count was supplied; the signature probe passes; the new prune family is covered by both parametrized tests; `test_run_report.py` and `test_mandatory_report_call_sites.py` are green with no unrelated test modified.</done>
</task>

<task type="auto">
  <name>Task 3: The skill records the handoff and passes the renamed keyword</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md</files>
  <read_first>
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` lines 1005-1022 (step 7's
      "hand the confirmed-match object ids onward" paragraph and its `confirmed_ids` fence) and
      lines 1118-1150 (step 9's report fence, its F4 comment block, and the paragraph after it).
    - `operator-claude-plugin/tests/test_skill_sequence_coverage.py` lines 1-30 — a fenced
      python block with TWO OR MORE scripts-module calls registers a new documented sequence
      that must be claimed by a named composition test or excluded with a reason. A
      ONE-call fence registers nothing. Confirm after editing by running the file.
    - `operator-claude-plugin/tests/test_write_grant.py` lines 2533-2600 — `_RUNBOOK_PATHS`
      includes this skill, and its fences are AST-parsed as real Python. Any fence you add must
      parse on its own (imports included), not just read like Python.
    - `operator-claude-plugin/tests/test_autonomy_switch_prose.py` lines 206-222 — the
      forbidden-substring test that reads this whole SKILL file. Two words are banned outright;
      one of them is the repo's word for a match's strength and is easy to reach for when
      writing about matched rows. Neither may appear anywhere in your new prose.
  </read_first>
  <action>
    In step 7, record the handoff immediately after the ids are computed, in **its OWN new
    python fence** holding exactly one import and one call — NOT appended to the existing
    `confirmed_ids` fence:

    `import match_handoff` / `match_handoff.record_handoff(run_id, classified["auto_matched"])`

    Pass the entries themselves (the store does its own projection; the fence must not
    hand-build a payload, or the projection stops being the store's guarantee).

    **Why its own fence, when the shorter diff is to append to the neighbour.** Batch item
    `260911-ss4` is editing "every later fence that needs `classified`" to load the step-2
    match outcome instead of re-matching, and the `confirmed_ids` fence is one of those. If
    both items add a call to that one fence, it ends up with two scripts-module calls, which
    registers a NEW documented sequence in `test_skill_sequence_coverage.py` — and whichever
    item merges second goes red with no `COVERED` entry to claim it. Two one-call fences leave
    both items at zero new sequences and no shared edit region. This is the one place in this
    plan where the shortest diff is the wrong one; do not "simplify" it back.

    Add two sentences of prose beside it, saying what the file is for in the operator's terms:
    a matched row is not in this run's enrichment scope and never calls `run_state.start_run`,
    so before this file existed it had no durable record anywhere and "every row came back
    exactly once" could only be shown from the chat. Name the file
    (`match_handoff-<run_id>.json`) and name step 9's report section as where it is read back.
    Cite F9 and the UAT record, matching how the neighbouring F3/F4 citations are written.

    In step 9, pass the renamed keyword in the `build_run_report` fence — the VALUE is
    unchanged (`len(unmatched_rows)`, step 4's list), only the keyword's name. The fence must
    keep its exactly-one-scripts-module-call shape that
    `test_mandatory_report_call_sites.py::test_build_run_report_sits_in_its_own_single_call_fence`
    enforces: change the keyword, add nothing else to that fence.

    Rewrite the long F4 comment above that line. Keep every load-bearing fact it carries — why
    `unmatched_rows` and not `rows`, that matched rows go to `enrich-records` via
    `confirmed_ids`, and that a mismatch still means a dispatch outside
    `chunking.dispatch_plan` — and correct the one thing F9 found: this count is the
    ENRICHMENT SCOPE, which is why the argument now says so, and the rest of the batch is
    accounted for by the handoff file step 7 wrote. Shorter than what it replaces, not longer.

    Add one sentence to the paragraph after the fence, where the block's contents are already
    described, naming the matched-id handoff section as part of what the operator reads.

    Do not touch any other step, do not add a `close_grant` step (that is batch item
    `260911-ss6`), and do not re-derive `classified` anywhere (that is `260911-ss4`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_autonomy_switch_prose.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py -q</automated>
    <automated>.venv/bin/python -c "import pathlib; t = pathlib.Path('operator-claude-plugin/skills/enrich-before-ingest/SKILL.md').read_text(); assert 'match_handoff.record_handoff(' in t; assert 'enrichment_scope_row_count=len(unmatched_rows)' in t; assert t.count('run_report.build_run_report(') == 1"</automated>
  </verify>
  <done>Step 7 records the handoff in its own new fence holding exactly one scripts-module call, leaving the `confirmed_ids` fence untouched; step 9 passes the renamed keyword with the value unchanged; the sequence-coverage registry gained no entry; the four skill-contract, sequence-coverage, prose and call-site suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's spreadsheet -> new durable file on the operator's machine | `auto_matched` entries carry the operator's whole source row (names, emails, phone numbers). This plan creates a file that outlives the run and is listed by any glob over the durable directory. |
| durable stores -> the rendered end-of-run report | The report is what the operator reads INSTEAD of watching the run (AFTER-01). A line that names the wrong scope is a false account of a batch, which is the finding being closed. |
| caller payload -> a new persisted store | Every store in this plugin is a place an arming grant could be parked and rehydrated on a later run (Phase 23 D-11 / GRANT-06). A new store is a new instance of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ss5-01 | Information disclosure | `match_handoff.record_handoff` | medium | mitigate | Project to `row_id` / `hs_object_id` / `confirmed` inside the store, never store what the caller passed. Pinned by a test that hands a full `auto_matched` entry (name + email + phone) and asserts none of the three appears in the file's text. |
| T-ss5-02 | Elevation of privilege | `match_handoff.record_handoff` | high | mitigate | Fresh, per-module whole-token forbidden-name scan over every key and scalar value; raises and writes NOTHING. Registered in `test_forbidden_marker_parity.py` so the eighth copy cannot drift from the seven. |
| T-ss5-03 | Repudiation | `run_report._render_block` row accounting | medium | mitigate | Rename the scope line and the keyword that feeds it; add the handoff section and the scope note, so the batch is accountable from files rather than from chat scrollback. |
| T-ss5-04 | Tampering | `match_handoff` writes under pytest | medium | mitigate | Copy `_refuses_real_durable_write_under_pytest`: a test that forgets to patch the path decorates nothing real. |
| T-ss5-05 | Denial of service | durable directory listing growth | low | mitigate | `match_handoff-*.json` joins `_PRUNE_LONG_TTL_GLOBS`, covered by both parametrized prune tests — F2's exact failure mode, closed on arrival rather than later. |
| T-ss5-06 | Information disclosure | rendered report text | low | mitigate | The section names the file by NAME, never an absolute path, per `prune_durable_state`'s own home-directory rule. |

No package-manager install runs in this plan, so no package-legitimacy gate applies. Nothing
here deploys, arms, or makes any live call.
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green, no
  regression against the pre-task baseline count.
- `git diff --stat` shows no change under `n8n/`, `scripts/build_cloud_workflows.py`,
  `operator-claude-plugin/CHANGELOG.md`, `operator-claude-plugin/plugin.json`, or any of the
  five primary store modules (`run_state.py`, `run_manifest.py`, `written_records.py`,
  `held_queue.py`, `remainder_queue.py`).
- Nothing is deployed, nothing is armed, no HubSpot or n8n call of any kind is made.
</verification>

<success_criteria>
- The report's row-accounting section names this run's enrichment scope, and the F9 shape (a
  4-row batch, 2 matched, 2 enriched) renders a true 2/2 line plus a handoff count that adds
  back to 4.
- The matched ids survive the process: `match_handoff-<run_id>.json` exists, holds ids and no
  spreadsheet values, and is named on the report's own face.
- An unreadable or foreign-run handoff is a named gap. An absent one is a gap on the lane that
  was supposed to write it (scope count supplied) and a plain statement everywhere else — no
  other skill's report gains a `REPORT INCOMPLETE` banner from this plan.
- The full plugin suite is green, with `test_forbidden_marker_parity.py` now covering eight
  stores.
</success_criteria>

<output>
Create `.planning/quick/260911-ss5-f9-major-after-01-the-end-of-run-report-line-matches-the-ori/260911-ss5-SUMMARY.md` when done.

Per CLAUDE.md §31, any residual found while executing (for example: whether the other three
report-calling skills should also record a handoff of their own) is a triaged todo with its
`kind`/`evidence`/`trigger` frontmatter, or one sentence in the SUMMARY — never an untriaged
pending file.
</output>
