---
phase: quick-260911-w6o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/held_queue.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_held_queue.py
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
  - operator-claude-plugin/tests/test_preingest_merge.py
  - .planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md
autonomous: true
requirements: []

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A row the waterfall enriched is persisted to `held_queue.json` carrying what the waterfall found, not the blank the operator's spreadsheet supplied: Jimmy Busteed's entry carries `jbusteed@australianturfclub.com.au`, his phone, his mobile and his LinkedIn — the recorded run `a254d1eda71246a2a964922cdf5c2bd2` wrote `email: \"\"`."
    - "Katie Poggioli's entry carries the one thing her thin enrichment returned — the waterfall's `jobtitle` — rather than only the source row's own."
    - "`held_queue.ROW_FIELD_ALLOWLIST` still refuses an arbitrary spreadsheet column: it is a closed, enumerated tuple, and a key outside it (e.g. `seniority`) is still absent from the stored row."
    - "A person whose firstname is literally `Grant` is persisted, not silently refused — the `row` payload's forbidden-name scan reads KEY NAMES only."
    - "A secret-shaped VALUE inside `observed_signals`, inside `reason`, or a secret-shaped `row_id` still raises `HeldQueueError` and still leaves the previous queue byte-identical."
    - "Persisting an email into a held entry's stored `row` does NOT make that row resumable: a `confidence_held` `no_match` entry stays in `still_held` on resume, because `run_manifest.rows_to_resume` compares fingerprints and reads the CALLER's rows, never `entry[\"row\"]`."
    - "`skills/enrich-before-ingest/SKILL.md`'s persist fence hands `held_queue.build_entry` the MERGED row, sourced from the dispatch step's own `MergeResult`, and states the fresh-process rebuild in prose — adding no new `module.function` call to that fence."
    - "`test_skill_sequence_coverage.py` passes UNCHANGED — the persist fence's registered call tuple is untouched, and `MAX_GRANDFATHERED` is not raised."
    - "Zero `n8n/` diff, zero live HubSpot / n8n / provider call, nothing armed."
  artifacts:
    - operator-claude-plugin/scripts/held_queue.py
    - operator-claude-plugin/tests/test_held_queue.py
  key_links:
    - "SKILL.md step 5 dispatch fence -> `merge_report.rows` -> step 5 persist fence `merged_by_id` -> `held_queue.build_entry(merged_row, ...)`"
    - "`held_queue.ROW_FIELD_ALLOWLIST` -> `_allowlisted_row` -> the stored `entry[\"row\"]` that 260911-w6p faceting and 260911-w6q create both read"
    - "`held_queue.save` -> `_first_forbidden` over `observed_signals`/`reason` (keys AND values, unchanged) vs. a key-name-only scan over `row`"
---

<objective>
Close UAT finding F2-1. `skills/enrich-before-ingest/SKILL.md`'s persist fence hands
`held_queue.build_entry` the SOURCE row — the operator's spreadsheet line — so everything
the waterfall revealed for a held row is discarded the instant it is written to disk. In
the recorded run `a254d1eda71246a2a964922cdf5c2bd2` (2026-09-11, executions 12365-12376, 0
writes) Jimmy Busteed's held entry carries `email: ""` while execution 12372's Lusha reveal
returned `jbusteed@australianturfclub.com.au`, a phone, a mobile and a LinkedIn — a 7-credit
reveal (UAT F10) thrown away at the persist boundary. Katie Poggioli's entry loses the one
field her thin enrichment returned (`jobtitle` "Club Contact").

Purpose: make the held entry carry what the round actually learned, so 260911-w6p can facet
it and 260911-w6q can create from it. This is the prerequisite the other three items read.
Output: a widened-but-still-closed `ROW_FIELD_ALLOWLIST`, a `row` forbidden-name scan
narrowed to key names, and a persist fence that passes the merged row.

**Operator ruling 2026-09-11 (F2), recorded here because the siblings implement it:**
`no_match` holds are faceted at READ time (no new hold code); review-triage reads both
queues in one table; a create lands through the ingest lane under a review-lane grant; the
batch renders the ready answer and never asks. This plan implements none of that — it makes
it possible.

Explicitly NOT in this plan, so the executor does not wait on the wrong sibling:

- the read-time facet classifier and the durable `create/skip/retry/drop` verbs belong to
  **260911-w6p**. Do not add a hold code; `confidence.ALL_HOLD_CODES` stays closed.
- the review-triage two-queue table belongs to **260911-w6q**.
- removing step 6's held-rows `AskUserQuestion` and correcting the end-of-run text belongs
  to **260911-w6r**.
- the version bump and the single `0.47.0` CHANGELOG section naming F2-1..F2-4 belong to
  **260911-w6r**. Do NOT touch `operator-claude-plugin/plugin.json` or `CHANGELOG.md`.

**Pointer for 260911-w6q, stated once here so it is not rediscovered live:** two of the
five newly-admitted keys — `mobilephone` and `lv_linkedin_url` — are NOT in
`extraction.canonical_props()`. A create built from a held entry's row must go through
`preingest.strip_enrichment_extras` before `extraction.write_dispatch_csv`, exactly as step
7's own ingest leg already does, or it raises `non_canonical_key_in_row`.

**Pointer for 260911-w6p — the entry does NOT carry a company domain.** The stored row
carries `company` (the NAME) and, only when the operator's own spreadsheet supplied one,
`company_id`. No domain: neither `extraction.canonical_props()` nor
`preingest.promotable_contact_props()` contains a company-domain key, so a merged row cannot
carry one and this plan deliberately does not invent a slot for it. The facet classifier
must derive the domain it needs from the row's own `email` through
`enrichment._clean_domain`, or source it outside the entry. The enrichment lane's
`Build Response` item DOES carry a per-row `company_domain`
(`scripts/build_cloud_workflows.py:857`), and `observed_signals` is the allowlist-free,
review-facing slot it could be stashed in — but weigh one hazard first, which is why this
plan does not just do it: `observed_signals` values are still forbidden-name scanned, so a
legitimate domain containing a marker token (`grant-...`, `token...`) would raise
`HeldQueueError` and refuse the whole save. That is the same yield-leak class this plan
narrows for `row`, and it should be decided deliberately in w6p rather than inherited here.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/tests/test_held_queue.py
@.planning/UAT-autonomous-batch-2026-09-09.md
@.planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md
</context>

<findings>
Established by reading before planning. The executor may rely on these; each names where
it was read so it can be re-checked rather than re-derived.

1. **The persist fence's input.** `SKILL.md:753` reads
   `entry = held_queue.build_entry(row, verdict.hold_code, verdict.reason, parsed)`, inside
   `for row in unmatched_rows:`. `unmatched_rows` is step 4's classification — the SOURCE
   rows. The merged rows already exist in the SAME step: the dispatch fence above it sets
   `merge_report = preingest.merge_enriched(unmatched_rows, recovery["responses"])`, and
   step 6's preview and step 7's `partition_for_ingest` both already read
   `merge_report.rows`. The persist fence is the one consumer still reading the source row.

2. **`merge_enriched` walks the ROWS** (`preingest.py:801-834`), so `merge_report.rows`
   contains an entry for EVERY unmatched row, including a row the backend never answered
   for (appended as `merged = dict(row)` on the `unanswered` branch). A `row_id`-keyed
   lookup over it is therefore total for this fence's loop — no row can go missing by
   switching the input.

3. **`ROW_FIELD_ALLOWLIST` today is `("row_id",) + enrichment.MATCH_LOOKUP_KEYS`** =
   `row_id, email, firstname, lastname, company, linkedin_url` (`held_queue.py:99`,
   `enrichment.py:79`). `email` and `linkedin_url` are already admitted — so merely passing
   the merged row recovers Jimmy's email. `phone`, `mobilephone` and `jobtitle` are not
   admitted at all, and the waterfall's promoted LinkedIn key is `lv_linkedin_url`, not
   `linkedin_url` (see finding 4). Without a widening, three of the four losses the finding
   names survive.

4. **Which keys a merged row can carry.** `merge_enriched`'s allowlist is
   `extraction.canonical_props()` ∪ `preingest.promotable_contact_props()`. Measured on this
   checkout: canonical = `company, company_id, email, firstname, jobtitle, lastname,
   linkedin_url, phone`; promotable = `city, country, email, hs_country_region_code,
   hs_state_code, jobtitle, lv_linkedin_url, lv_persona_group, mobilephone, phone,
   seniority, state`. `n8n/code/normalizeProviders.js:446` pushes the UNPREFIXED
   `linkedin_url`, but its own comment block (lines 430-441) traces that winner through
   `ENRICH_MERGE`'s `canonicalizeLinkedin()` into the **`lv_`-prefixed** merge candidate
   that `mergeContacts.js:52` keys its policy on — and `preingest.py:750` names
   `lv_linkedin_url` in the list of promotable keys the canonical-only filter used to drop.
   So the RESPONSE carries `lv_linkedin_url`.

5. **A sibling store already made exactly this widening.**
   `suggestion_declines.ROW_FIELD_ALLOWLIST = enrichment.MATCH_LOOKUP_KEYS + ("jobtitle",
   "phone", "company_id")` (`suggestion_declines.py:63`), pinned equal to
   `extraction.canonical_props()` by `test_suggestion_declines.py:409`. The three keys this
   plan adds beyond `mobilephone`/`lv_linkedin_url` are that precedent, not an invention.

6. **BLOCKING safety check — answered NO.** `run_manifest.rows_to_resume` never reads
   `entry["row"]`. Its `held` branch tests `row.get("email")` on the CALLER's rows; its
   `confidence_held` branch (the verdict this fence writes) compares
   `entry["resume_fingerprint"]` against `held_queue.fingerprint(entry["hold_code"],
   current_outcome)`, and `fingerprint()` hashes only `hold_code`, `match_tier` and
   `candidate_count` — never the row. Putting an email into the stored row therefore
   CANNOT make a `no_match` row auto-resume into a send, which would have contradicted
   D-70-11 and the F2 ruling. Task 1 pins this rather than leaving it as an argument.

7. **The forbidden-name guard, and why narrowing it to names is safe HERE.** The module
   docstring already states the ordering: "The forbidden-name scan below is a SECOND line,
   not the first... the allowlist is what actually keeps an arbitrary spreadsheet column off
   disk." Because `_allowlisted_row` filters `row` to a closed enumerated tuple BEFORE the
   scan runs, a forbidden-shaped KEY can never reach the scan through `build_entry` at all —
   the scan's only live effect on `row` is refusing VALUES. That is the effect that drops a
   person named Grant Dewsbury (UAT: "deliberately dropped... a held row of his would be
   refused and dropped silently"), and it is the effect an enriched row makes far more
   likely. Precedent for a key/value asymmetry already ships:
   `run_report._looks_forbidden_value` deliberately drops `arm`/`webhook` from its VALUE
   marker set while every key matcher keeps them
   (`test_forbidden_marker_parity.py:95-103`).

8. **What this does NOT weaken.** `test_forbidden_marker_parity.py` pins `_looks_forbidden`
   (the key matcher) and the ten-marker tuple across seven stores; neither changes.
   `test_held_queue.py`'s value-refusal test targets `observed_signals`
   (`n8n_api_key=super-secret`), not `row` — it must keep passing untouched.
   `test_preingest_merge.py:686` asserts a widened key is dropped using `seniority`, which
   this plan deliberately does NOT admit — that test keeps passing; only its inline comment
   describing the allowlist's composition goes stale and is corrected.

9. **Exact-shape pins on a stored row — checked, none break.** `grep -rn '\["row"\] =='`
   over `operator-claude-plugin/tests/` finds three relevant pins and all three survive the
   widening: `test_held_queue.py:133` (row is `row_id`+`email` only),
   `test_unattended_pair_composition.py:156-165` (the `hold_ingest_no_company` path — row is
   `row_id`+`email`+`company`), and `test_extraction_email_gate.py:72` (which is
   `extraction.hold_emailless`, a different store entirely). No file beyond this plan's
   `files_modified` needs editing for them — but task 1 runs the FULL plugin suite anyway,
   because that grep is evidence, not proof.

10. **The sequence-coverage registry.** The persist fence's registered call tuple
   (`test_skill_sequence_coverage.py:346-354`) is
   `held_queue.load, run_manifest.load, preingest.parse_outcome, confidence.assess,
   held_queue.build_entry, held_queue.save, run_manifest.save, run_manifest.save,
   run_manifest.run_manifest_path, run_state.read_progress`. The extractor reads
   ```python fences and collects `module.function(` calls against real scripts modules
   (`test_a_block_with_only_one_scripts_module_call_yields_no_identity`). A dict
   comprehension over `merge_report.rows` adds no call, so the registry stays untouched —
   which is why task 2 sources the merged rows from the dispatch step's existing
   `MergeResult` and states the fresh-process rebuild in PROSE, not as a second
   `preingest.merge_enriched(` call inside the fence.

11. **Detector checkpoints (quick-batch item, no ROADMAP phase).**
    - *API coverage:* no external API is integrated. This plan is an offline plugin change;
      every HubSpot / n8n / provider call in the touched flow is pre-existing and unchanged.
      Declaration, in place of a matrix: *No external API integration: a local durable-store
      allowlist, a guard scope, and a SKILL.md fence variable.*
    - *Assumption-delta:* `skipped` — `phase_unresolved` (a quick item has no ROADMAP phase
      section to scan). No verdict fabricated.
    - *Schema push:* no ORM schema files in scope (no Prisma/Drizzle/Payload/Supabase/
      TypeORM paths touched). Skipped silently.
</findings>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: An enriched held row survives the write to disk — end to end, on the recorded run's shape</name>
  <files>operator-claude-plugin/scripts/held_queue.py, operator-claude-plugin/tests/test_held_queue.py, operator-claude-plugin/tests/test_preingest_merge.py, .planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md</files>
  <read_first>
operator-claude-plugin/scripts/held_queue.py (whole file — 303 lines),
operator-claude-plugin/tests/test_held_queue.py (whole file),
operator-claude-plugin/scripts/preingest.py lines 705-845 (`merge_enriched`),
operator-claude-plugin/scripts/suggestion_declines.py lines 55-70 (the precedent widening),
operator-claude-plugin/scripts/run_manifest.py lines 362-450 (`rows_to_resume`),
operator-claude-plugin/tests/test_preingest_merge.py lines 686-710,
.planning/UAT-autonomous-batch-2026-09-09.md (finding F10 and section 8, the recorded reveal).
  </read_first>
  <behavior>
This is the whole path in one slice: source rows + the recorded run's response shape ->
`preingest.merge_enriched` -> `held_queue.build_entry` -> `save` -> `load`. Write all five
tests FIRST. **Exactly two of them go RED against the shipped module — tests 1 and 3.**
Tests 2, 4 and 5 are green regression pins from the start and are expected to stay green;
do not hunt for a failure in them. In particular test 4's row-KEY case already raises today
(`_first_forbidden` scans keys as well as values), and test 1's email assertion alone would
also pass today (`email` is already allowlisted) — test 1 goes red on the phone, the mobile,
the LinkedIn and the jobtitle, which is the widening this task is for.

  - Test 1 (the defect, inverted — the recorded run shape). Two unmatched rows built from
    the recorded batch-1 rows the waterfall actually reached, `row-2` and `row-3`:
    `row-3` = Jimmy Busteed, `company` "Australian Turf Club", `email` `""`;
    `row-2` = Katie Poggioli, `company` "Atherton Turf Club", `email`
    `secretary@athertonturfclub.com.au`, `jobtitle` "Secretary". Responses in
    `merge_enriched`'s own per-row item shape: `row-3` carries `email`
    `jbusteed@australianturfclub.com.au`, `phone`, `mobilephone`, `lv_linkedin_url` and a
    `city`; `row-2` carries `jobtitle` "Club Contact". Feed the MERGED rows through
    `build_entry`/`save`/`load` and assert the loaded `row-3` entry carries the email, the
    phone, the mobile and the LinkedIn, and that `row-2`'s carries `jobtitle` "Club
    Contact" (`jobtitle` is the one `refreshable_contact_props()` key, so the provider
    value legitimately replaces "Secretary" — assert the replacement, do not assume the
    source value survived). Assert `city` is ABSENT: the allowlist is still closed.
    RED today because `mobilephone`/`lv_linkedin_url`/`phone`/`jobtitle` are not admitted.
  - Test 2 (the same shape, wrong input — the actual F2-1 defect). Build the entry from
    the SOURCE `row-3` instead of the merged one and assert its stored `email` is `""` —
    the exact state the recorded run wrote. This test documents the defect and must keep
    passing after the fix; it is what makes test 1's assertion mean "the caller changed",
    not "the allowlist changed".
  - Test 3 (Grant Dewsbury). A row whose `firstname` is `Grant` and whose `lastname` is
    `Dewsbury` saves and loads back unchanged. RED today — `_first_forbidden` matches the
    `grant` marker on the value.
  - Test 4 (the guard that stays). `save` still raises `HeldQueueError` for: a
    secret-shaped VALUE inside `observed_signals`; a secret-shaped `row_id` key; a
    secret-shaped `reason`; and a hand-built entry whose `row` carries a forbidden-shaped
    KEY (e.g. `{"n8n_api_key": "x"}`) — the key scan over `row` survives even though the
    value scan does not. Each leaves a previously-saved queue byte-identical.
  - Test 5 (resume neutrality, finding 6). A `confidence_held` manifest verdict plus a
    held entry whose stored `row` NOW carries an email: `run_manifest.rows_to_resume`
    still reports it in `still_held` when the current outcome's fingerprint is unchanged.
    Passes today; it is a permanent pin that this change did not open an unapproved create
    path, and its comment must say so.
  </behavior>
  <action>
Make those tests pass with two edits to `held_queue.py` and nothing else.

(a) **Widen `ROW_FIELD_ALLOWLIST`, keeping it a closed enumerated tuple.** Append exactly
five keys: `jobtitle`, `phone`, `company_id`, `mobilephone`, `lv_linkedin_url`. The first
three mirror `suggestion_declines.ROW_FIELD_ALLOWLIST` verbatim (finding 5) and together
with `MATCH_LOOKUP_KEYS` make the set equal `extraction.canonical_props()` plus `row_id`;
the last two are the deliberate non-canonical additions — `mobilephone` and
`lv_linkedin_url` are what the waterfall promotes for a mobile and a LinkedIn (finding 4),
and both are named as lost in the recorded run. Do NOT import `extraction` or `preingest`
to derive the tuple: `preingest` imports `held_queue`, so that is a cycle, and
`extraction._load_mapping` raises when the column mapping is unresolvable, which would turn
an importable module into an unimportable one. Enumerate, and say in the comment above the
tuple why it is enumerated rather than derived. Do NOT admit `seniority`, the five location
keys, or `lv_persona_group` — none of them is named in the finding, none is needed by the
create 260911-w6q will build, and each is PII this store would then hold with no consumer.
Update the module docstring's "mirrors `enrichment.MATCH_LOOKUP_KEYS` exactly" sentence to
describe what the tuple now is and why, naming this quick id.

(b) **Scope the `row` payload's forbidden-name scan to KEY NAMES.** In `save()`, replace the
`_first_forbidden(entry.get("row"))` call with a key-name-only scan over the row's keys, and
leave the `observed_signals` scan, the `reason` scan and the `row_id` scan exactly as they
are. Keep `_first_forbidden` itself unchanged and still used by the `observed_signals`
branch — do not delete it, do not change its recursion, and do not touch
`_looks_forbidden`, `_FORBIDDEN_NAME_MARKERS`, `_tokenised` or `_FORBIDDEN_TOKEN_RUNS`
(`test_forbidden_marker_parity.py` pins all of them across seven stores). Record the
reasoning at the call site in one short comment: the allowlist filters `row` to a closed
tuple before this scan runs, so the scan's only live effect on `row` was refusing VALUES —
a person's own name, a company's own name, an email — and
`run_report._looks_forbidden_value` is the shipped precedent for a key matcher and a value
matcher legitimately differing.

Then two record edits, neither of them code:

(c) `operator-claude-plugin/tests/test_preingest_merge.py` lines 686-696 — correct ONLY the
inline comment that describes `ROW_FIELD_ALLOWLIST` as "row_id + enrichment.
MATCH_LOOKUP_KEYS" and as "out of scope to widen here". Say what it is now and name this
quick id. Leave every assertion in that test byte-identical; `seniority` is still dropped,
which is the property it pins.

(d) `.planning/todos/pending/2026-09-11-forbidden-name-marker-whole-token-still-refuses-grant-token.md`
— do NOT close it and do NOT remove it from `pending/`. Append a dated note recording that
`held_queue.py`'s `row` payload no longer scans values (so a held row for a person named
Grant now persists), that the other six stores and `held_queue`'s own
`observed_signals`/`reason`/`row_id` scans are UNCHANGED, and that the todo therefore stays
open for those six. Keep the front matter's `kind: defect` and `evidence:` keys valid per
CLAUDE.md §31.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>
  <done>
The whole plugin suite passes — run it in full HERE, not only at plan end: the allowlist is
the cross-cutting change and any store-shape pin elsewhere must be caught in this task, not
the next. Tests 1 and 3 were observed RED against the shipped module before the edit and the
RED output is quoted in the SUMMARY; tests 2, 4 and 5 were green throughout, and the SUMMARY
says so rather than claiming a RED that never happened. `ROW_FIELD_ALLOWLIST` is a closed
enumerated tuple of 11 names. `_looks_forbidden`, `_FORBIDDEN_NAME_MARKERS` and
`_first_forbidden` are unmodified. `git diff --stat` shows no file outside this task's four.
  </done>
  <reversibility rating="reversible">Two edits inside one module plus tests; revert restores the previous allowlist and scan exactly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The persist fence hands `build_entry` the merged row, and a contract test pins it</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py</files>
  <read_first>
operator-claude-plugin/skills/enrich-before-ingest/SKILL.md lines 600-800 (the dispatch
fence that builds `merge_report`, then the "Assess confidence, and hold" persist fence at
~735-770, then step 6's preview and step 7's `partition_for_ingest`),
operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py (whole file — copy
its `_normalized()` idiom and its locate-then-assert style),
operator-claude-plugin/tests/test_skill_sequence_coverage.py lines 346-354 (the registered
tuple this task must NOT change) and lines 563-600 (what the extractor counts as a call).
  </read_first>
  <behavior>
  - Test A (the contract pin). Read `skills/enrich-before-ingest/SKILL.md`, locate the
    persist fence, and assert its `held_queue.build_entry(` call's first argument is the
    merged row (the fence's `row_id`-keyed lookup), NOT the bare loop variable `row`.
    Assert in the same test that the fence names `merge_report` as the source of those
    merged rows. RED against the shipped SKILL.md.
  - Test B (the registry is untouched). Assert the persist fence adds no merge call — the
    merged rows arrive as a variable from the dispatch step, so the registered sequence
    tuple cannot drift. This is the pin that stops a later editor "helpfully" recomputing
    the merge inside the fence and silently breaking `test_skill_sequence_coverage.py`.
    **Scope this assertion to the fenced ```python block ONLY, never to the surrounding
    prose**, and read the block through the same `sequences_in`-style extraction
    `test_skill_sequence_coverage.py` uses rather than a raw whole-file grep — the prose
    above the fence legitimately names the rebuild path, and a whole-file scan would
    refuse the very sentence this task is told to write.
  </behavior>
  <action>
Edit ONLY the persist fence inside step 5 ("Assess confidence, and hold — don't block
(D-61-07)"), plus the prose immediately around it.

In the fence, beside the existing `responses_by_id` line, add a `row_id`-keyed lookup built
from the dispatch step's own `MergeResult` — a dict comprehension over `merge_report.rows`,
no new function call — and change the `build_entry` argument from `row` to that lookup's
value for this `row_id`, falling back to the loop's own `row` when the id is absent so the
loop stays total. Leave every other line of the fence byte-identical: the same
`held_queue.load`/`run_manifest.load` preamble, the same `continue` for a CONFIDENT row, the
same queue-before-manifest write order, the same dual `run_manifest.save`.

Add prose immediately above the fence, in the shape the fence's existing `unmatched_rows`
note already uses: `merge_report` is the dispatch step's own value — the same `MergeResult`
step 6's preview and step 7's `partition_for_ingest` read — and in a fresh process it is
rebuilt through `preingest.merge_enriched` over `unmatched_rows` and `responses`, never by
dispatching again. Write that rebuild reference WITHOUT a call-shaped argument list, so no
extractor or grep can mistake a prose pointer for a fence call. Say in one sentence that
when the re-request pass ran, its returned `MergeResult` is the one to use, because it is
the later and richer merge.

Add a short paragraph after the fence saying plainly WHY the merged row is what is stored:
a held row's whole value to the operator is what the round learned about it, and the
recorded run threw a 7-credit reveal away by storing the spreadsheet line instead. State
the safety property explicitly, because it is the one a reader will worry about: storing
an email on a held entry does not make it sendable — `run_manifest.rows_to_resume` compares
this entry by fingerprint and reads the caller's rows, never the entry's stored row, so a
`no_match` hold still needs the operator's approval to become a create (D-70-11).

Do NOT touch step 6's `AskUserQuestion` or the "work them with review-triage" end-of-run
text — 260911-w6r owns both. Do NOT touch `operator-claude-plugin/plugin.json` or
`CHANGELOG.md`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py operator-claude-plugin/tests/test_skill_sequence_coverage.py operator-claude-plugin/tests/test_batch_finishes_composition.py operator-claude-plugin/tests/test_mandatory_report_call_sites.py -q</automated>
  </verify>
  <done>
Tests A and B pass and were observed RED against the shipped SKILL.md.
`test_skill_sequence_coverage.py` passes with ZERO edits to its registry and
`MAX_GRANDFATHERED` still `0`. `git diff -- operator-claude-plugin/tests/test_skill_sequence_coverage.py`
is empty.
  </done>
  <reversibility rating="reversible">A prose + fence edit in one SKILL.md step and one new contract test.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator spreadsheet -> plugin process | untrusted column names and values enter here; `ROW_FIELD_ALLOWLIST` is the control |
| n8n waterfall response -> plugin process | provider-supplied PII (email, phone, mobile, LinkedIn) enters here via `merge_enriched`'s own allowlist |
| plugin process -> local disk (`held_queue.json`) | the boundary this plan widens: more PII now persists, at 0600, in the plugin durable dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w6o-01 | Information disclosure | `held_queue.json` at rest | medium | mitigate | The widening is five ENUMERATED keys, never "the merged row verbatim": `seniority`, `lv_persona_group` and the five location keys stay out because no consumer needs them. Write path is unchanged — `durable_paths._atomic_write_0600`, pinned by `test_save_writes_at_mode_0600`. Task 1 test 1 asserts a non-admitted key (`city`) is absent from the stored row, so the closedness is a test, not a claim. |
| T-w6o-02 | Information disclosure | `save()`'s `row` forbidden-name scan | low | mitigate | Narrowing to key names is safe only because `_allowlisted_row` already filters `row` to a closed tuple BEFORE the scan (finding 7), so a secret would have to arrive under one of 11 named contact keys. `observed_signals`, `reason` and `row_id` keep full key+value scanning, pinned by task 1 test 4; `run_report._looks_forbidden_value` is the shipped precedent for the asymmetry. |
| T-w6o-03 | Elevation of privilege | a held row becoming a HubSpot create without approval | high | mitigate | The concrete worry: an enriched email on a held entry making a `no_match` row auto-resume into a send, contradicting D-70-11 and the F2 ruling. Verified NOT reachable (finding 6) — `rows_to_resume` reads the caller's rows and compares by `fingerprint()`, which hashes only `hold_code`/`match_tier`/`candidate_count`. Task 1 test 5 pins it permanently. |
| T-w6o-04 | Tampering | provider-supplied `jobtitle` overwriting an operator-supplied one | low | accept | Pre-existing and deliberate: `jobtitle` is the sole `refreshable_contact_props()` key, per the 2026-09-11 operator ruling in `merge_enriched`'s docstring. This plan only persists the outcome; task 1 test 1 asserts the replacement so it is visible rather than assumed. |
| T-w6o-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. No dependency is added; the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — full plugin suite green, no
  pre-existing failure introduced.
- `node --test tests/n8n/*.test.mjs` — unchanged (glob form; the directory form is broken on
  node 24).
- `git diff --stat -- n8n/ scripts/ src/ config/` is EMPTY. This plan touches no workflow
  JSON, no generator, no scoring engine and no config.
- `git diff -- operator-claude-plugin/plugin.json operator-claude-plugin/CHANGELOG.md` is
  EMPTY — 260911-w6r owns the `0.47.0` bump.
- Zero live HubSpot, n8n, or provider calls. Nothing armed, nothing deployed, nothing
  bounced.
</verification>

<success_criteria>
- An entry built from the recorded run's merged `row-3` carries
  `jbusteed@australianturfclub.com.au`, a phone, a mobile and a LinkedIn after a
  `save()`/`load()` round trip; the same entry built from the source row still carries
  `email: ""`, and both facts are asserted in the same test file.
- A row for a person named `Grant Dewsbury` persists; a secret-shaped value in
  `observed_signals` or `reason`, a secret-shaped `row_id`, and a forbidden-shaped KEY in a
  hand-built `row` all still raise and still leave the previous queue byte-identical.
- `run_manifest.rows_to_resume` still reports a `confidence_held` `no_match` row as
  `still_held` when its stored row has gained an email.
- `SKILL.md`'s persist fence passes the merged row and adds no `module.function` call;
  `test_skill_sequence_coverage.py` passes with an unedited registry.
- The pending forbidden-name-marker todo is annotated and still open.
</success_criteria>

<output>
Create `.planning/quick/260911-w6o-f2-1-prerequisite-the-held-queue-entry-stores-the-source-row/260911-w6o-SUMMARY.md` when done.
</output>
