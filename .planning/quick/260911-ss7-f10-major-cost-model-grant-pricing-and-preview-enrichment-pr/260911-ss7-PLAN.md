---
phase: quick-260911-ss7
plan: 01
type: execute
wave: 2
depends_on: ["260911-ss4", "260911-ss5", "260911-ss6"]
files_modified:
  - operator-claude-plugin/config/cost_rates.json
  - operator-claude-plugin/tests/test_cost_guard.py
  - operator-claude-plugin/config/operator.local.example.json
  - docs/LUSHA-V3-CONTRACT.md
  - scripts/enrichment_cost_ledger.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
autonomous: true
requirements: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A contacts batch priced through `cost_guard.estimate_batch` with Lusha in the provider list quotes 7 credits per contact, not 1 — so `preview_enrichment`'s cost block and `write_grant`'s envelope both quote a ceiling at or above the single real reveal this repo has ever observed."
    - "The companies rate (2 credits/company) and the stored-id re-enrich rate (a real measured 0) are UNCHANGED — only the first-time contacts rate moved."
    - "The rate table's file-level `measured_on` stays `2026-07-30`; the new date lives in the entry's own citation, so `zoominfo_per_match` and the Anthropic figures do not read as freshly re-measured and the staleness disclosure never under-states the table's age."
    - "`docs/LUSHA-V3-CONTRACT.md` records the 2026-09-11 observation with its evidence (execution 12372, `creditsCharged: 7` / `0`, balance 3860 -> 3853) and labels the reveal-width explanation as a HYPOTHESIS, never as an isolated mechanism."
    - "The contract's two forward-looking flat-1 claims — the A3 verdict row and the §12 cost-lever bullet — are amended in place rather than left standing as fact beside a contradicting amendment."
    - "`scripts/enrichment_cost_ledger.py`'s `lusha_contacts_first_time_enrich` estimate matches the plugin's, because its citation points at the very section this plan amends."
    - "No live Lusha call is made by any task in this plan. The measurement is execution 12372's own billing block, already recorded in the UAT."
    - "`operator-claude-plugin/.claude-plugin/plugin.json` reads `0.46.0` and `CHANGELOG.md` carries ONE `## [0.46.0] - 2026-09-11` section naming F1, F9, F10 and F11, in the same commit."
  artifacts:
    - operator-claude-plugin/config/cost_rates.json
    - docs/LUSHA-V3-CONTRACT.md
    - operator-claude-plugin/CHANGELOG.md
    - operator-claude-plugin/.claude-plugin/plugin.json
  key_links:
    - "`config/cost_rates.json` -> `cost_guard.load_rates()` -> `cost_guard.estimate_batch()` is the ONE path both `preview_enrichment.py` (line 414) and `write_grant.py` (line 455) price from. There is no second plugin-side literal to chase."
    - "`cost_guard.SUGGESTION_STAGE2_RATE_KEY` IS `lusha_contacts_first_time_enrich`, so `suggest-contacts`' stage-2 line moves with this change. Its tests read the rate dynamically; the CHANGELOG must still say so, because the number the operator sees changes."
    - "`scripts/enrichment_cost_ledger.py`'s ESTIMATES entry cites `docs/LUSHA-V3-CONTRACT.md §7-8` — the section this plan amends — which is why the second, deliberately-separate copy is in scope."
---

<objective>
Close UAT finding **F10** (`.planning/UAT-autonomous-batch-2026-09-09.md`): grant pricing and
`preview_enrichment` price Lusha at 1 credit per contact; execution 12372's `Lusha Enrich`
billing block shows `creditsCharged: 7` for one rich reveal (balance 3860 -> 3853, confirmed by
`scripts/check_provider_credits.py` before/after) and `0` for the `NOT_FOUND` row.

Purpose: the operator-facing price is a CEILING. It over-states deliberately (the rate table's
own words) and it must never under-state a real observed charge.

Output: the plugin's first-time contacts rate raised to the observed 7 and dated; the
observation recorded in the contract of record with its two superseded claims amended in place;
the repo-side ledger's matching literal kept level with the doc it cites; plugin `0.46.0` cut
with one CHANGELOG section naming this batch's four findings.

Explicitly OUT of scope:
- **No rate-learning system.** No mechanism that reads `creditsCharged` off a run and updates a
  table. The fix is one dated constant.
- **No live Lusha call.** The measurement already exists; re-measuring would spend credits to
  learn what execution 12372 recorded.
- **No backend change.** `scripts/build_cloud_workflows.py`'s reveal derivation, the `REVEAL_MAP`
  and the `Lusha Enrich` node are untouched — narrowing the reveal to save credits is an
  unproven cost lever, not this plan's business.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/UAT-autonomous-batch-2026-09-09.md
@operator-claude-plugin/config/cost_rates.json
@docs/LUSHA-V3-CONTRACT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: raise the Lusha first-time contacts rate to the observed 7</name>
  <files>operator-claude-plugin/config/cost_rates.json, operator-claude-plugin/tests/test_cost_guard.py</files>
  <read_first>
    - `operator-claude-plugin/config/cost_rates.json` — the whole file. The
      `lusha_contacts_first_time_enrich` entry is the ONLY one this task edits; note the
      file-level `measured_on` and the note explaining `null` vs `0`.
    - `operator-claude-plugin/scripts/cost_guard.py` lines 43-72 (`PROVIDER_RATE_KEYS`, the
      `ponytail:` comment explaining the first-time-rate choice, `SUGGESTION_STAGE2_RATE_KEY`)
      and lines 88-175 (`load_rates`, `_rate_entry`, `estimate_batch`).
    - `operator-claude-plugin/tests/test_cost_guard.py` lines 58-110 — the rate-table block and
      the two `estimate_batch` tests.
  </read_first>
  <behavior>
    - RED first, both in `tests/test_cost_guard.py`, run and observed failing BEFORE the JSON is
      touched:
      - line 62 `rates["rates"]["lusha_contacts_first_time_enrich"]["value"] == 1` becomes `== 7`.
      - line 102 `test_a_contacts_batch_with_lusha_uses_the_contact_rate`: 10 contacts becomes 70
        credits, not 10.
    - One NEW test in the same file asserting the ceiling relationship the change exists to keep:
      the first-time contacts rate is greater than or equal to the largest per-contact charge this
      repo has observed (7), and strictly greater than the stored-id re-enrich rate (0). Name the
      execution in the test's docstring so a future reader finds the evidence, not just the number.
    - Unchanged and asserted by the tests already in the file: `lusha_companies_match` stays 2
      (`test_a_companies_batch_with_lusha_uses_the_company_rate`, 10 companies -> 20),
      `lusha_contacts_stored_id_reuse` stays a real measured 0, `apollo_per_match` stays `null`,
      and `date.fromisoformat(rates["measured_on"]) == date(2026, 7, 30)` still passes.
  </behavior>
  <action>
    In `operator-claude-plugin/config/cost_rates.json`, change ONLY the
    `lusha_contacts_first_time_enrich` entry:

    Set `value` to `7`. Rewrite `citation` so it carries BOTH measurements and says which request
    shape produced each: the 2026-07-30 probe billed 1 credit on a `search-and-enrich` call that
    sent NO `reveal` key at all (`scripts/probe_lusha_v3.py`'s P1 and T2b steps send
    `{"contacts": [identity]}` only), while the shipped backend always sends a non-empty `reveal`;
    the 2026-09-11 observation is n8n execution 12372's `Lusha Enrich` billing block —
    `creditsCharged: 7` on one rich reveal, `0` on the `NOT_FOUND` row, balance 3860 -> 3853 read
    by `scripts/check_provider_credits.py` before and after. Say in the citation that 7 is the
    worst case observed to date and is quoted as a ceiling. Set `confidence` to a string naming it
    a measured worst case rather than a flat per-contact price — `confidence` is passed through to
    the operator-facing block verbatim and is never branched on, so its wording is the whole
    contract.

    Drop the old citation's trailing comparison to the retired v2 per-field bundling figure. That
    numeric literal is one of two the repo-side ledger module's source is grepped for by
    `tests/test_enrichment_cost_ledger.py`, Task 2 copies this citation into that module, and the
    v2 comparison no longer earns its place beside a re-measured number.

    Do NOT move the file-level `measured_on`. It drives `rate_table_age_days` for the WHOLE table;
    moving it to 2026-09-11 would make `zoominfo_per_match` (carried forward from a v2-era
    measurement) and the Anthropic figures read as freshly re-measured, and would shrink a
    staleness disclosure that should only ever over-state.

    Then run the full plugin suite. Fix any further assertion that priced contacts x Lusha at 1 by
    updating its expected number — never by weakening a direction assertion (the "NOT enough" /
    "insufficient" verdict tests must still assert the same direction). `IDS_SPEC` in
    `tests/test_preview_enrichment.py` is a COMPANIES spec, so its `"6"` cell is 3 x 2 and does not
    move; check before editing anything there.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
  </verify>
  <done>
    Both RED assertions were observed failing before the JSON changed and pass after; the new
    ceiling test passes; the full plugin suite is green; `git diff` on
    `operator-claude-plugin/config/cost_rates.json` touches one entry's `value`, `citation` and
    `confidence` and nothing else.
  </done>
</task>

<task type="auto">
  <name>Task 2: record the observation in the contract of record, and level the two prose copies</name>
  <files>docs/LUSHA-V3-CONTRACT.md, scripts/enrichment_cost_ledger.py, operator-claude-plugin/config/operator.local.example.json</files>
  <read_first>
    - `docs/LUSHA-V3-CONTRACT.md` §1 (status header, and its "every credit figure below is the
      response body's own synchronous `billing.creditsCharged`" caveat), §3.1 (the established
      dated-amendment convention), §6 (the reveal model and the `revealEmail: 1` /
      `revealPhone: 5` sticker prices), §7 (the two-step-vs-combined table), §10 (the A3 verdict
      row) and the cost-lever bullet near line 495.
    - `scripts/enrichment_cost_ledger.py` lines 85-100 — the `lusha_contacts_first_time_enrich`
      ESTIMATES entry and its `docs/LUSHA-V3-CONTRACT.md §7-8` citation.
    - `tests/test_enrichment_cost_ledger.py` lines 355-390 — the two tests that iterate ESTIMATES
      and the one forbidding retired v2 credit arithmetic in that module's source.
    - `operator-claude-plugin/config/operator.local.example.json` line 29's
      `_enrichment_providers_cost_note`.
  </read_first>
  <action>
    **`docs/LUSHA-V3-CONTRACT.md` — three touch points, not one.** A dated amendment alone would
    leave two claims standing as fact elsewhere in the same document.

    (a) Add a dated amendment subsection under §7 (mirroring §3.1's convention: a heading naming
    the date and what was observed). Record, as evidence: n8n execution 12372, 2026-09-11, the
    `Lusha Enrich` node's own `billing.creditsCharged` — 7 for the row whose response carried
    email + phone + mobile + LinkedIn + location + seniority, 0 for the `NOT_FOUND` row; the
    settled balance delta 3860 -> 3853 from `scripts/check_provider_credits.py`. Record what the
    2026-07-30 probes actually sent, because it is the load-bearing difference: the 1-credit
    first-time figure in §7 came from `search-and-enrich` bodies with NO `reveal` key
    (`scripts/probe_lusha_v3.py` P1 / T2b), and no probe has EVER measured a first-time call that
    revealed phones. State the reveal-width explanation as a HYPOTHESIS and say what makes it
    plausible (§6's own sticker prices: `revealEmail: 1` + `revealPhone: 5`, plus the 1-credit
    search, sums to exactly 7) and what is missing (the request's `reveal` value is DERIVED from
    the backend's `REVEAL_MAP` as `["emails","phones"]`, not read off 12372's runData; the cause
    is not isolated; nothing was A/B'd). Note that no code in `n8n/code` parses `creditsCharged` —
    the field is the raw Lusha response body sitting in the node's runData item, and no parser is
    being added. Close the subsection with a named open question in the document's own
    convention: is the shipped `["emails"]`-only first-time call still 1 credit, and would
    narrowing the reveal be a real cost lever? Say what would answer it (one paid live probe) and
    that nobody has spent it.

    (b) Amend the §10 **A3** verdict row in place. Its "cost-differentiation is REFUTED" half was
    measured on the STORED-ID `/contacts/enrich` lane where both arms billed 0 — say so, scope the
    verdict to that lane, and note the first-time lane is unmeasured for a phones-inclusive reveal
    as of 12372. Do not delete the original verdict; amend it the way CLAUDE.md's as-built deltas
    do, so the earlier reading stays legible.

    (c) Amend the cost-lever bullet near line 495 ("flat v3 pricing ... ~1 credit/contact
    first-time enrich") with the same 2026-09-11 date and pointer. Add a one-line pointer in §1's
    status header so a reader who stops at the header cannot miss that a later, larger charge is
    on record.

    In the amendment, name `scripts/build_cloud_workflows.py` lines 6571 and 6607-6608 as
    generator comments that now cite the superseded §6 claim. **Do not edit that file** — it is
    backend scope under the regeneration-parity rule, and a comment correction there would require
    regenerating every workflow JSON.

    **`scripts/enrichment_cost_ledger.py`:** update the `lusha_contacts_first_time_enrich` ESTIMATES
    entry's `value` to 7 and its `citation` to name the 2026-09-11 execution-12372 amendment
    alongside the 2026-07-30 probe, and its `confidence` to match the plugin's wording. This
    second copy is deliberate (the plugin never reads repo paths at runtime) but it cites the
    section being amended, so leaving it at 1 points a citation at a document that no longer
    supports the number. Touch no other entry. `tests/test_enrichment_cost_ledger.py` greps this
    module's whole source for two retired v2 credit-arithmetic literals and fails if either
    appears — so keep the new citation free of any v2-era per-field bundling figure.

    **`operator-claude-plugin/config/operator.local.example.json`:** update
    `_enrichment_providers_cost_note`'s Lusha per-contact clause to the new figure, dated
    2026-09-11 and described as a worst case. Leave the companies figure (2), the stored-id zero,
    the ZoomInfo 1.08, the Apollo-unknown sentence and the Anthropic figure exactly as they are.
    Do not touch `operator-claude-plugin/README.md` — its passage says Lusha is priced at its
    first-time rather than its re-enrich rate and deliberately quotes no number, so it stays true.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_enrichment_cost_ledger.py operator-claude-plugin/tests/ -q</automated>
  </verify>
  <done>
    The contract of record carries the 12372 observation with its evidence and an explicitly
    hypothetical cause; A3 and the cost-lever bullet are scoped rather than silently contradicted;
    §1 points at the amendment; the repo ledger's literal matches the plugin's; the example config
    quotes the new worst-case figure for a Lusha contact instead of the superseded one; both suites green; `git diff
    --stat -- n8n/ src/ scripts/build_cloud_workflows.py` is EMPTY.
  </done>
</task>

<task type="auto">
  <name>Task 3: cut plugin 0.46.0 with one CHANGELOG section naming F1/F9/F10/F11</name>
  <files>operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <precondition>`operator-claude-plugin/.claude-plugin/plugin.json` still reads `"version": "0.45.0"` and `CHANGELOG.md` carries no `## [0.46.0]` section — if either is already bumped a sibling released ahead of this plan, so stop and report rather than cutting a second section.</precondition>
  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` lines 1-60 — the release rule in the preamble and the
      `## [0.45.0] - 2026-09-11` section's shape (a one-paragraph batch preamble naming the quick
      ids, then `### Changed` with one bolded bullet per item naming the todo/finding and the
      quick id).
    - `.planning/quick/260911-ss4-*/*SUMMARY.md`, `.planning/quick/260911-ss5-*/*SUMMARY.md`,
      `.planning/quick/260911-ss6-*/*SUMMARY.md` — glob them; this plan runs after all three.
      Every F1/F9/F11 bullet is DERIVED from what those summaries say shipped. If a summary is
      missing, say so in the bullet rather than describing work you cannot confirm landed.
    - `.planning/UAT-autonomous-batch-2026-09-09.md` findings F1, F9, F10, F11 — the operator-facing
      framing each bullet should answer.
  </read_first>
  <action>
    Insert a single `## [0.46.0] - 2026-09-11` section directly beneath `## [Unreleased]`,
    mirroring 0.45.0's shape: a short preamble naming this batch (`260911-ss4`/`ss5`/`ss6`/`ss7`)
    and the UAT it came from, then one bolded bullet per finding under the right Keep-a-Changelog
    heading — `### Fixed` for the three defects, `### Changed` for the pricing move if that reads
    truer for it.

    Four bullets, one per finding, in operator terms (what the operator sees change), each naming
    its finding id and quick id:
    - **F1** — from ss4's summary: the match outcome is persisted per run and later fences load it
      instead of re-matching, so a batch stops re-POSTing itself. Name the execution amplification
      the UAT measured (8 executions for what should be 2) as the thing this removes.
    - **F9** — from ss5's summary: the end-of-run report's row-accounting line names this run's
      enrichment scope instead of claiming the original batch, and the matched-id handoff is
      persisted per run.
    - **F10** — this plan: Lusha's first-time contact rate in the price a grant quotes moved from
      1 credit to 7, the worst case observed on execution 12372. Say plainly that this is a
      ceiling, not an invoice, and that the preview already over-states on purpose. Say that the
      `suggest-contacts` stage-2 line moves with it, because it prices from the same rate. Say
      that the rate table's own "Rates measured" date is deliberately left where it is — the new
      date rides that one rate's citation — so an operator reading the two side by side does not
      file the mismatch as a defect.
    - **F11** — from ss6's summary: the grant is closed at batch completion
      (`CLOSED_BATCH_COMPLETE`) and the session-end guidance is in the skill.

    Then set `"version": "0.46.0"` in `operator-claude-plugin/.claude-plugin/plugin.json`. Both
    edits land in the SAME commit — the CHANGELOG preamble states the rule, and an unbumped
    version leaves the Update button greyed out no matter what shipped.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q && test "$(/usr/bin/grep -c '^## \[0\.46\.0\]' operator-claude-plugin/CHANGELOG.md)" = 1 && test "$(python3 -c "import json;print(json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version'])")" = 0.46.0</automated>
  </verify>
  <done>
    `operator-claude-plugin/.claude-plugin/plugin.json` reads `0.46.0`; `CHANGELOG.md` carries
    exactly one new `## [0.46.0] - 2026-09-11` section with four finding-named bullets derived from
    the three sibling summaries plus this plan; `## [Unreleased]` is still present and empty above
    it; the plugin suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> operator | The rate table's figures are the operator's only pre-spend price signal; a number here is acted on with money. |
| repo docs -> future planner | `docs/LUSHA-V3-CONTRACT.md` is the contract of record; a claim left standing there is treated as measured fact by later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ss7-01 | Information disclosure (misleading) | `config/cost_rates.json` first-time contacts rate | medium | mitigate | Rate raised to the observed worst case (7) and labelled a ceiling in both `citation` and `confidence`, which render to the operator verbatim; a new test pins rate >= the largest observed charge. |
| T-ss7-02 | Tampering (silent freshness inflation) | `cost_rates.json` file-level `measured_on` | medium | mitigate | `measured_on` deliberately NOT moved; the new date lives in the entry citation, so `rate_table_age_days` keeps over-stating rather than under-stating the table's age for every other rate. |
| T-ss7-03 | Repudiation (unevidenced claim) | `docs/LUSHA-V3-CONTRACT.md` amendment | medium | mitigate | Every figure carries its evidence (execution 12372, `creditsCharged`, balance delta); the reveal-width cause is labelled a hypothesis with its missing isolation named, per the repo's `[observed live]` vs `[documented]` discipline. |
| T-ss7-04 | Denial of service (credit burn) | re-measuring Lusha live | low | accept | No task calls Lusha. The measurement is execution 12372's own billing block, already recorded; re-measuring would spend credits to learn what is on file. |
| T-ss7-05 | Elevation of privilege | n8n arming / HubSpot writes | low | accept | No task deploys, arms, bounces or writes. Diff is confined to two config files, two test/doc files, one repo script's constant table, and the plugin's version + CHANGELOG. |

No package-manager install task exists in this plan, so no `T-ss7-SC` package-legitimacy row is
registered and no legitimacy checkpoint is required.
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` — full plugin suite green.
- `.venv/bin/python -m pytest tests/test_enrichment_cost_ledger.py -q` — green, including
  `test_no_retired_v2_credit_arithmetic_anywhere_in_this_module`.
- `/usr/bin/grep -rn '"value": 1,' operator-claude-plugin/config/cost_rates.json` returns nothing
  for the Lusha contacts entry.
- Read (do not machine-gate) `/usr/bin/grep -rn -i "lusha" operator-claude-plugin/config/ | /usr/bin/grep -v __pycache__` — no forward-looking per-contact price in the plugin's two config
  files still quotes the superseded figure. Historical statements inside a dated 2026-07-30 record
  are correct and stay.
- `git diff --stat -- n8n/ src/ scripts/build_cloud_workflows.py` is EMPTY — no backend, no
  workflow regeneration, no engine-parity obligation.
- `git diff -- operator-claude-plugin/.claude-plugin/plugin.json` shows exactly the version line.
- No `n8n` deploy, bounce, arm or HubSpot call was made; no Lusha call was made.
- Batch ordering: this item is the batch's release cut. It declares `depends_on` on `260911-ss4`,
  `260911-ss5` and `260911-ss6` because its CHANGELOG section names F1, F9 and F11, which those
  three ship. It touches none of their files — they own SKILL.md and the plugin scripts; this plan
  owns the rate table, the contract doc, the ledger constant, the example config, the version and
  the CHANGELOG.

## Multi-Source Coverage Audit

| Source | Item | Covered by |
|---|---|---|
| CONTEXT (item description) | Re-measure the Lusha rate the pricing table uses | Task 1 — raised to the observed 7, dated, no live call |
| CONTEXT (item description) | Record the 7-credit observation in `docs/LUSHA-V3-CONTRACT.md` | Task 2 (a)(b)(c) |
| CONTEXT (item description) | Bump to 0.46.0 with one CHANGELOG section naming F1/F9/F10/F11 | Task 3 |
| CONTEXT (repo facts) | TDD RED first on the rate constant | Task 1 `<behavior>` — two RED assertions observed before the JSON changes |
| CONTEXT (repo facts) | Do NOT re-run a live Lusha call | Objective (out of scope) + T-ss7-04 |
| CONTEXT (repo facts) | Do not build a rate-learning system | Objective (out of scope) — one dated constant |
| CONTEXT (repo facts) | Tests run with `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` | Every `<verify>` |
| CONTEXT (repo facts) | `depends_on` ss4/ss5/ss6 for the release cut | frontmatter + `<verification>` |
| GOAL (UAT F10) | The grant's price must not under-state a real observed charge | Task 1 `must_haves.truths[0]` |
| REQ | none — quick item, `requirements: []` by this repo's quick-plan convention | n/a |
| RESEARCH | no RESEARCH.md for a quick item; the contract doc and execution 12372 are the evidence base | Task 2 `<read_first>` |

No item is MISSING. No deferred idea is planned.
</verification>

<success_criteria>
- A contacts batch with Lusha quotes 7 credits/contact wherever a price is shown to the operator —
  `preview_enrichment`'s cost block, `write_grant`'s envelope, and `suggest-contacts`' stage-2 line.
- The companies rate, the stored-id zero, the Apollo `null` and the table's `measured_on` are
  unchanged.
- `docs/LUSHA-V3-CONTRACT.md` records the observation with evidence, labels its cause a hypothesis,
  and no longer carries a standing unqualified flat-1 claim.
- The repo ledger constant matches the doc it cites.
- Plugin `0.46.0` is cut with one CHANGELOG section naming F1, F9, F10 and F11, version and
  CHANGELOG in the same commit.
- Zero diff in `n8n/`, `src/`, `scripts/build_cloud_workflows.py`; zero live provider calls; nothing
  deployed, bounced or armed.
</success_criteria>

<output>
Create `.planning/quick/260911-ss7-f10-major-cost-model-grant-pricing-and-preview-enrichment-pr/260911-ss7-SUMMARY.md` when done.
</output>
