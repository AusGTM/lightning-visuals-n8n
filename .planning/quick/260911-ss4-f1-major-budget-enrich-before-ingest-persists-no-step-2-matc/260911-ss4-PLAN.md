---
phase: quick-260911-ss4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/match_state.py
  - operator-claude-plugin/scripts/run_report.py
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/tests/test_match_state.py
  - operator-claude-plugin/tests/test_run_report.py
  - operator-claude-plugin/tests/test_forbidden_marker_parity.py
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
  - operator-claude-plugin/tests/test_skill_sequence_coverage.py
  - operator-claude-plugin/tests/test_linkedin_row_composition.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Step 2's match outcome survives the process that produced it: a later fence in a fresh process reads the same four buckets without sending a second propose request."
    - "A batch like the recorded UAT batch 1 costs ONE propose execution for the match, not six."
    - "`skills/enrich-before-ingest/SKILL.md` contains exactly one `preingest.match_batch(` call and it sits in step 2's span."
    - "Step 2 PRINTS `match_run_id` — a fresh process has the id it needs to load, so the re-run-step-2 fallback fires on a genuinely missing file and never as routine."
    - "Every later step that consumes `unmatched_rows` or `classified` states where it comes from: steps 3, 4, 5 and 7 through a `match_state.load(match_run_id)` fence, steps 5, 6 and 9 additionally through prose naming that call as the rebuild path."
    - "A row whose firstname is `Grant` persists — the forbidden-name guard refuses grant-shaped NAMES (document keys, row_ids, row keys), never a person's name."
    - "A missing, unreadable, or other-run match-state file raises `MatchStateError` naming the run; it never returns an empty classification that reads as `no matches`."
    - "`match_state-*.json` prunes on the 7-day family beside `run_state-*.json`."
    - "`tests/test_skill_sequence_coverage.py` passes with its registry updated, not grandfathered: two tuples amended, one added, `MAX_GRANDFATHERED` untouched."
  artifacts:
    - operator-claude-plugin/scripts/match_state.py
    - operator-claude-plugin/tests/test_match_state.py
  key_links:
    - "SKILL.md step 2 fence -> `match_state.save(match_run_id, classified)` -> `match_state-<match_run_id>.json`"
    - "SKILL.md steps 3/4/5/7 fences -> `match_state.load(match_run_id)` -> `classified`"
    - "`run_report._PRUNE_SHORT_TTL_GLOBS` -> `match_state-*.json`"
    - "`test_forbidden_marker_parity._KEY_MATCHER_MODULES` -> `match_state`"
---

<objective>
Close UAT finding F1. `skills/enrich-before-ingest/SKILL.md` persists nothing from its
step-2 match, so every later fence — a fresh process each time — re-ran
`preingest.match_batch` and re-POSTed the whole batch in propose mode. The recorded run
(`a254d1eda71246a2a964922cdf5c2bd2`, 2026-09-11) spent SIX 4-row propose executions
(12365, 12366, 12368, 12370, 12373, 12374) plus TWO 2-row sends (12375, 12376) where two
would have done — a 4x execution amplification per batch against a 2.5K/month Starter
allowance, before any write.

Purpose: make the step-2 outcome a durable per-run artifact so the rest of the round
reads it instead of re-buying it.
Output: a new `scripts/match_state.py` store, its prune/parity registrations, and a
rewired `SKILL.md` whose only `match_batch` call is step 2's.

Explicitly NOT in this plan, and the attribution matters so the executor does not wait on
the wrong sibling:

- the version bump and the single CHANGELOG section naming F1/F9/F10/F11 belong to
  **260911-ss7**. Do not bump `plugin.json`; do not add a CHANGELOG section.
- step 9's report scope line (`original_row_count=len(unmatched_rows)`, the "Matches the
  original batch's N row(s)" wording) and the matched-id handoff persistence belong to
  **260911-ss5**. This plan touches step 9 with ONE sentence of prose only (below) and
  changes no call there.
- the `write_grant.close_grant` step belongs to **260911-ss6**.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/UAT-autonomous-batch-2026-09-09.md
@operator-claude-plugin/scripts/run_state.py
@operator-claude-plugin/scripts/held_queue.py
@operator-claude-plugin/scripts/durable_paths.py
@operator-claude-plugin/scripts/preingest.py
@operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
@operator-claude-plugin/tests/test_skill_sequence_coverage.py
@operator-claude-plugin/tests/test_forbidden_marker_parity.py
</context>

<interface_context>
Shapes this plan joins (read before writing code; do not re-derive):

- `preingest.classify_matches(...)` returns
  `{"auto_matched": [...], "proposed": [...], "unmatched": [...], "unchecked": [...],
  "unknown_response_row_ids": [...]}`. `auto_matched` entries carry
  `row_id`/`row`/`hs_object_id` (plus `confirmed: True` once step 3 applies);
  `proposed` entries carry `row_id`/`row`/`candidates`/`ambiguous`;
  `unmatched`/`unchecked` entries carry `row_id`/`row`.
- `preingest.MatchOutcome.run_id` (D-70-05) is the batch's own correlation handle — the
  id this store keys on.
- `preingest.apply_match_decisions(classified, resolved)` is pure and returns a NEW
  classification of exactly the same five keys.
- `durable_paths.resolve_state_path().parent` is the durable directory every sibling
  store resolves into; `durable_paths._atomic_write_0600(path, text)` is the write.
- `run_report._PRUNE_SHORT_TTL_GLOBS` is currently `("run_state-*.json",)`;
  `PRUNE_SHORT_TTL_DAYS` is 7.
- `tests/conftest.py`'s autouse `no_durable_writes` fixture refuses real durable writes,
  so every test passes an explicit `path=tmp_path / "..."`.
- SKILL.md line map as of this plan (verify before editing, the file moves):
  step 2 fence ~182; step 3 fence ~275; step 4 fence ~294; step 5 spans 312-773 and uses
  `unmatched_rows` at 490 / 579 / 589 / 711 with a second `match_batch` fence at ~653;
  step 6 spans 774-798 and uses `unmatched_rows` at 782; step 7 fence ~1013; step 9 uses
  `unmatched_rows` at 1139.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: match_state store — save, load, classify_read, driven end to end on the recorded shape</name>
  <files>operator-claude-plugin/scripts/match_state.py, operator-claude-plugin/tests/test_match_state.py, operator-claude-plugin/scripts/run_report.py, operator-claude-plugin/tests/test_run_report.py, operator-claude-plugin/tests/test_forbidden_marker_parity.py</files>

  <behavior>
    The end-to-end trace, built from the RECORDED UAT batch-1 shape (not an invented one):
    four rows; `row-1` auto_matched with `hs_object_id` `"3601"` (John Miller, matched by
    email); `row-4` proposed with one candidate `"3501"` (Craig Sheppard, the row the
    operator answered `4. approve` on); `row-2` and `row-3` unmatched (Katie Poggioli,
    Jimmy Busteed — the two rows execution 12372 actually enriched).

    - save(match_run_id, classified, path=...) then load(match_run_id, path=...) — WITHOUT
      reusing the in-memory object — returns a classification equal to what was saved,
      all five keys, `unknown_response_row_ids` included.
    - Feed that loaded value to `preingest.apply_match_decisions(loaded, {"row-4": "3501"})`,
      save again, load again: `[e["hs_object_id"] for e in loaded["auto_matched"]]` is
      `["3601", "3501"]`, `[e["row_id"] for e in loaded["unmatched"]]` is
      `["row-2", "row-3"]`, `proposed` is `[]`.
    - A row whose `firstname` is `"Grant"` (recorded UAT batch 2, row-1, Grant Dewsbury)
      PERSISTS and reads back unchanged. This is the load-bearing case: a value-scanning
      forbidden-name guard would tokenise `"Grant Dewsbury"` to `" grant dewsbury "`,
      refuse the save, and drop the fence straight back into the re-match this store
      exists to stop.
    - A row key named `webhook_secret`, and a `row_id` shaped like a grant, are each
      refused with `MatchStateError` and nothing is written.
    - `load` on an absent file raises `MatchStateError` naming the run id. `load` on a
      present-but-malformed file raises too. Neither ever returns an empty-but-valid
      classification.
    - `load` on a file whose recorded `run_id` is a DIFFERENT run raises rather than
      handing back another batch's rows.
    - `classify_read` returns `ABSENT` / `PARSEABLE` / `ANOMALOUS` without raising.
    - `run_report.prune_durable_state` deletes a `match_state-<id>.json` older than 7 days
      and leaves a fresh one alone.
  </behavior>

  <action>
Create `operator-claude-plugin/scripts/match_state.py`, the plugin's next persisted
artifact, mirroring `run_state.py`'s module shape exactly: module docstring stating what
it holds and why no existing store could, document field constants, `ABSENT`/`PARSEABLE`/
`ANOMALOUS` constants, `match_state_path(run_id)` returning
`durable_paths.resolve_state_path().parent / f"match_state-{run_id}.json"`, and writes
through `durable_paths._atomic_write_0600`. Public surface: `match_state_path`, `save`,
`load`, `classify_read`, `MatchStateError`. Nothing else.

Document schema: `run_id`, `saved_at` (UTC isoformat), and `classification` — the five
keys `preingest.classify_matches` returns, persisted as given.

Four decisions to implement and to record in the docstring, each with its reason:

1. **The forbidden-name guard scans NAMES ONLY — top-level document keys, every
   `row_id`, and every KEY inside a persisted `row` dict. It never scans string
   VALUES.** Phase 23 D-11 is about a grant, a secret, or a token being readable off
   disk under its own name; a contact's `firstname` is not a name in that sense.
   `held_queue._first_forbidden` scans string leaves, and against the recorded batch 2
   that refuses Grant Dewsbury outright. Reimplement the marker tuple, `_tokenised`,
   `_FORBIDDEN_TOKEN_RUNS` and `_looks_forbidden` fresh in this module — per the plugin's
   anti-DRY per-store discipline (D-69-01), never imported from a sibling.
2. **Rows are persisted VERBATIM, with no field allowlist.** `held_queue`'s allowlist
   (`row_id` + `enrichment.MATCH_LOOKUP_KEYS`) is complete because its only consumer is a
   re-send of a MATCH request, and that tuple is the proven projection for one. This
   store's consumers include `extraction.write_dispatch_csv`, whose header is
   `extraction.canonical_props()` — wider, and config-driven from `column_mapping.yaml`.
   An allowlist narrower than that silently drops a column the operator supplied. Say so
   in the docstring so a future narrowing is a decision, not drift.
3. **`load` RAISES `MatchStateError` on absent, unreadable, malformed, or another run's
   file** — naming the run id — mirroring `run_state.mark_dispatched`'s refusal rather
   than `run_state.load`-style degrade-to-empty. An empty classification returned here
   would flow into `classified["unmatched"]` and read as "nothing to enrich", or raise a
   `TypeError` two lines on; either way the fence's recovery is the re-match this fix
   removes. `classify_read` stays the non-raising probe so the SKILL prose has words for
   what it saw.
4. **`save` validates every name BEFORE writing**, so a refused save leaves any previous
   document untouched — `run_manifest.save`'s validate-then-apply discipline.

Constraints on the new module, each enforced by an existing sweep over `scripts/*.py`:
no `requests` and no backend import (`test_no_backend_imports.py`); no `time`/`sched`
import, no `sleep()`, no `while` loop (`test_report_sufficiency.py`'s poll-loop guard).
Every public function takes `path=None` and resolves only when it is `None`, so tests can
pass `tmp_path` under conftest's `no_durable_writes` fixture.

Then two registrations:

- `run_report._PRUNE_SHORT_TTL_GLOBS` gains `"match_state-*.json"` — the 7-day family,
  beside `run_state-*.json`, because this is a within-round working file. Update
  `prune_durable_state`'s docstring family list to name it.
- `tests/test_forbidden_marker_parity.py`: add `match_state` to `_KEY_MATCHER_MODULES`
  and update its docstring's "seven" to "eight". The parity suite checks the MATCHER's
  behaviour, not where a module applies it, so `match_state._looks_forbidden("grant")`
  must still be `True` — decision 1 narrows the application, never the matcher.

Write `tests/test_match_state.py` covering the behaviour block above. TDD: write the
tests first and run them RED (the module does not exist) before creating
`match_state.py`. Do not bump `plugin.json` and do not touch CHANGELOG.md.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_match_state.py operator-claude-plugin/tests/test_run_report.py operator-claude-plugin/tests/test_forbidden_marker_parity.py operator-claude-plugin/tests/test_no_backend_imports.py operator-claude-plugin/tests/test_report_sufficiency.py -q</automated>
  </verify>

  <done>
    The recorded-shape round trip passes across a save/load boundary in both directions;
    a row named Grant persists; a `webhook_secret` row key and a grant-shaped `row_id` are
    each refused with nothing written; an absent, malformed, or other-run file raises;
    `match_state-*.json` prunes at 7 days; the parity suite drives eight matchers. RED was
    observed before `match_state.py` existed. No version bump, no CHANGELOG edit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rewire every SKILL.md fence to load the persisted outcome, and pin it</name>
  <files>operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py, operator-claude-plugin/tests/test_skill_sequence_coverage.py, operator-claude-plugin/tests/test_linkedin_row_composition.py</files>

  <behavior>
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` contains exactly ONE
      occurrence of the literal `preingest.match_batch(`, and `_numbered_step_spans`
      places it inside step 2's span.
    - The literal `match_state.load(match_run_id)` appears in the spans of steps 3, 4, 5,
      6, 7 and 9 — in a python fence for 3, 4, 5 and 7, and in prose naming it as the
      rebuild path for 5, 6 and 9.
    - `match_state.save(` appears in the spans of steps 2 and 3.
    - Step 2's span names `match_run_id` as a value it prints.
    - Every one of these is RED against the current text: step 5's linkedin fence (around
      line 653) is a second `match_batch` call, and no line of the file mentions
      `match_state` at all.
  </behavior>

  <action>
Rewire the fences so the match runs once per batch.

**Step 2 (fence around line 182).** Keep the existing four calls, name the id, print it,
and persist:

  - add `match_state` to the fence's `import` line (a name used in a fence and not
    imported there is a `NameError` in the driver — apply the same rule to every fence
    edited below);
  - bind `match_run_id = outcome.run_id` immediately after `match_batch` —
    `outcome.run_id` is the batch's own D-70-05 correlation handle, not a new id;
  - call `match_state.save(match_run_id, classified)` as the fence's last statement;
  - `print` the id, so a fresh process has something to pass to `load`.

Add prose under the fence, in the operator-facing voice the rest of the file uses:

  - the four-group report LEADS with `match_run_id` on its own line, and that id is
    carried verbatim into every later fence. A later fence with no id is the whole F1
    leak — it re-derives the batch by re-sending it.
  - this match is sent ONCE for the batch; the outcome is written to
    `match_state-<match_run_id>.json` in the plugin's durable state directory; every later
    step reads it from there.
  - two ids now coexist and must not be confused: `match_run_id` is the MATCH's id and is
    what every `match_state` call takes; the bare `run_id` minted at step 5 is the
    ENRICHMENT dispatch's id and is untouched by this change. They are deliberately
    different — `watch.recover_dispatch` correlates settled executions by `run_id`, so
    sharing one id would let step 5's recovery read the propose execution's runData.
  - when the file is not there: `match_state.classify_read` answers `ABSENT` or
    `ANOMALOUS`, and the honest recovery is to re-run step 2 ONCE, saying so — never a
    silent re-match, and never repeatedly.

**Step 3 (fence around line 275).** Three statements, each on its own line and in this
order (the sequence walker records calls pre-order, so nesting one call inside another
would register the wrong tuple): load the classification with
`match_state.load(match_run_id)`, apply the operator's decisions with
`preingest.apply_match_decisions`, save the result back with `match_state.save` under the
same `match_run_id`. One line of prose: the confirmed match is written back here, which is
what makes step 7's `confirmed_ids` include a row confirmed in this step and not only the
email matches.

**Step 4 (fence around line 294).** Prepend `classified = match_state.load(match_run_id)`
before the existing `unmatched_rows` comprehension. Exactly one scripts-module call — keep
it that way, so no new sequence registration is needed.

**Step 5.** Two edits.

  - the linkedin fence (around line 653) currently rebuilds the whole match from
    `rows_from_table` through `match_batch`/`classify_matches` purely to reach one
    unmatched row. Replace that preamble with
    `classified = match_state.load(match_run_id)` and keep the `unmatched_row` pick, the
    `record` literal and the `extraction.validate` call unchanged. `config_gate.load_config`,
    `build_rows_spec`, `rows_from_table`, `plan_chunks`, `chunk_ceiling`, `match_batch`
    and `classify_matches` all leave this fence.
  - the main dispatch fence (around line 486) and the confidence fence (around line 701)
    both consume `unmatched_rows` with no stated source, which is exactly how the recorded
    session refilled them. Do NOT add a load call inside either fence — each is a
    registered multi-call sequence and a new call would churn its COVERED tuple and its
    covering test for no behavioural gain. Instead put ONE sentence of prose immediately
    above each fence (the sequence walker parses only ```python fences, so prose costs no
    registration): "`unmatched_rows` is step 4's value — in a fresh process rebuild it from
    `match_state.load(match_run_id)` and step 4's comprehension, never from
    `preingest.match_batch`."

**Step 6 (fence around line 782).** The same one sentence of prose above the
`render_enriched_preview` fence, same wording, same reason.

**Step 7 (fence around line 1013).** Prepend `classified = match_state.load(match_run_id)`
before the `confirmed_ids` comprehension. One scripts-module call — keep it that way.

**Step 9 (fence around line 1139).** The same one sentence of prose above the
`build_run_report` fence. Change NO call and NO argument here — `original_row_count` and
the report's scope wording are sibling 260911-ss5's work.

Then the three ratchets, each of which is a red suite if skipped:

1. `tests/test_skill_sequence_coverage.py`:
   - the step-2 entry `("config_gate.load_config", "chunking.plan_chunks",
     "chunking.chunk_ceiling", "preingest.match_batch", "preingest.classify_matches")`
     gains a trailing `"match_state.save"`. Its sink is now `match_state.save`, so repoint
     its nodeid to the Task 1 composition test that actually drives a save — do not extend
     `test_chunking.py`'s existing test to reach a store it has no business touching.
   - add a NEW entry for step 3: `("match_state.load",
     "preingest.apply_match_decisions", "match_state.save")`, pointing at the same
     composition test (nodeids may repeat; the checker requires the test to mention the
     sequence's sink, and that test drives `match_state.save`).
   - the linkedin entry's tuple becomes `("match_state.load", "extraction.validate")`.
   - leave `GRANDFATHERED_UNCOVERED` and `MAX_GRANDFATHERED` alone — the ceiling is
     shrink-only and nothing here belongs in it.
2. `tests/test_linkedin_row_composition.py`: rewrite the covering test so it DRIVES the
   new sequence — save a classification containing a linkedin-only unmatched row, load it
   through `match_state.load`, and carry that row into `extraction.validate` — rather than
   driving a match the SKILL no longer documents there.
3. `tests/test_enrich_before_ingest_skill_contract.py`: add the assertions from the
   behaviour block, built on the file's existing `_text()` / `_numbered_step_spans`
   helpers. Run them RED against the unedited SKILL.md first — the second `match_batch`
   at line 653 must be seen failing the span assertion before any prose is edited.

Do not bump `plugin.json`, do not add a CHANGELOG section, and do not touch step 5's
`run_state.new_run_id()` / `run_state.start_run` wiring.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q</automated>
  </verify>

  <done>
    The whole plugin suite is green. `/usr/bin/grep -c 'preingest.match_batch(' operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` returns 1, and the contract test places it in step 2's span. Steps 3, 4, 5 and 7 load the persisted classification in a fence; steps 5, 6 and 9 name that load in prose as the rebuild path. Step 2 prints `match_run_id`. The sequence registry carries no orphaned or unregistered entry and `MAX_GRANDFATHERED` is unchanged. No version bump, no CHANGELOG edit.
  </done>
</task>

</tasks>

<threat_model>
ASVS level 1; blocking threshold `high`. Nothing below is `high`, so nothing here blocks.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory classification -> local disk | operator spreadsheet rows (email, names, phone, LinkedIn) come to rest in a per-run JSON file for the first time |
| local disk -> a later fence in a fresh process | a file another process, another run, or a crash may have written is read back and acted on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ss4-01 | Information Disclosure | `match_state-<run_id>.json` | medium | mitigate | Written only through `durable_paths._atomic_write_0600` (0600, temp-in-target-dir plus `os.replace`, never observable partially written or world-readable). Lands in the same durable directory as `held_queue.json`, which already holds the same class of row data. Registered in `run_report._PRUNE_SHORT_TTL_GLOBS` so it expires in 7 days rather than accumulating (F7 counted 267 stale state files on the operator's own machine). |
| T-ss4-02 | Tampering | `save` name validation | medium | mitigate | Reimplemented Phase 23 D-11 forbidden-name guard over top-level document keys, every `row_id`, and every key inside a persisted `row` — an arming grant, webhook secret, or API token cannot be parked in this store under its own name. Validation runs before any write, so a refusal leaves the previous document untouched. Registered in `test_forbidden_marker_parity._KEY_MATCHER_MODULES` so the matcher cannot drift from the other seven. |
| T-ss4-03 | Spoofing | `load` reading another run's file | medium | mitigate | The document echoes its own `run_id`; `load` compares it to the requested id and raises `MatchStateError` on a mismatch, so one batch's rows can never be enriched or written under another batch's identity. |
| T-ss4-04 | Denial of Service | `load` degrading to empty | low | mitigate | `load` raises on absent or malformed rather than returning an empty classification — an empty one reads as "no matches, nothing to enrich" and would silently skip real rows. `classify_read` is the non-raising probe the SKILL prose uses to say which of the three it saw. |
| T-ss4-05 | Information Disclosure | over-refusing a person's name | low | mitigate | The guard deliberately does NOT scan string values: `held_queue`'s value-scanning form refuses a contact named Grant (recorded: Grant Dewsbury, UAT batch 2), which would drop the fence back into the re-match this plan removes. Pinned by a must-persist test case. |
| T-ss4-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install in this plan — no new dependency, no lockfile change. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` green from the repo root
  (system python lacks deps; never use it).
- `node --test tests/n8n/*.test.mjs` untouched and green — this plan changes no `n8n/` file
  and no `scripts/build_cloud_workflows.py` output. `git diff --stat n8n/` must be empty.
- Zero live calls of any kind: no dispatch, no arming, no HubSpot write, no n8n deploy.
</verification>

<success_criteria>
- One `preingest.match_batch(` in `skills/enrich-before-ingest/SKILL.md`, inside step 2.
- Steps 3, 4, 5 and 7 read the classification from `match_state.load(match_run_id)`;
  steps 5, 6 and 9 name that call in prose as the rebuild path for `unmatched_rows`.
- Step 2 prints `match_run_id`.
- The recorded UAT batch-1 shape round-trips across a save/load boundary and yields
  `confirmed_ids == ["3601", "3501"]` after step 3's decision is applied and re-saved.
- A row whose firstname is `Grant` persists; a grant-shaped `row_id` or row KEY is refused.
- Full plugin suite green; `n8n/` diff empty; plugin version still `0.45.0`.
</success_criteria>

<output>
Create `.planning/quick/260911-ss4-f1-major-budget-enrich-before-ingest-persists-no-step-2-matc/260911-ss4-SUMMARY.md` when done.

Record in it, as prose and not as a new todo (CLAUDE.md §31 rule 1 — no test, no recorded
hit): `held_queue._first_forbidden` scans string leaves, so a held row for a contact named
Grant would be refused by the same mechanism this plan deliberately narrowed. Not observed
live, not fixed here.
</output>
