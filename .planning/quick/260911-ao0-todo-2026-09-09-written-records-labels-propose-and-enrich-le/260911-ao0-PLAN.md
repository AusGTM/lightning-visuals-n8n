---
phase: quick-260911-ao0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/tests/test_written_records.py
  - .planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md
  - .planning/todos/completed/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md
files_deleted:
  - .planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md
autonomous: true
requirements: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The MINIMAL row shape consistent with run `2bc3617b`'s recorded report line — a dict carrying `row_id`, with no `action`, no `object_type` and no `hs_object_id` — is pinned by a test as a shape that renders `row-2 [contacts:unknown]: None -> failed` in the end-of-run report block WHEN it reaches the ledger. (No captured body of that leg exists anywhere in the repo; `.planning/uat/UAT-autonomous-batch-2026-09-09.md` records the run in prose only.)"
    - "The same recorded shape, arriving as the recovered row of a PROPOSE leg, never reaches the ledger and never appears in the end-of-run report at all — the report's per-record section reads `- (no records)`."
    - "The same recorded shape on a WRITE leg still lands and still renders as failed — so the propose-leg pass is attributable to D-70-09's ledger gate, not to a harness that cannot see the ledger."
    - "No production module changes: `written_records.py`, `chunking.py` and `run_report.py` are byte-identical after this task, because D-70-09 already implemented the todo's `## Fix shape` option (a) at `chunking.dispatch_and_recover`'s append site."
    - "The todo is closed with a `## Resolved` section naming D-70-09, the gate's exact call site, and the two new test functions, then moved into `.planning/todos/completed/`."
  artifacts:
    - operator-claude-plugin/tests/test_written_records.py
    - .planning/todos/completed/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md
  key_links:
    - "`chunking.dispatch_and_recover`'s `if can_write and rows:` gate <-> `chunking.envelope_can_write` <-> `enrichment.build_envelope`'s per-leg `mode` — the one place a leg's write capability is decided, read rather than re-derived"
    - "`written_records.append_chunk` (write) <-> `written_records.load()` (glob) — both must resolve into the SAME tmp directory for the new tests to mean anything; `test_written_records._patch_durable_dir` patches `durable_paths.resolve_state_path`, and conftest's autouse `no_durable_writes` explicitly stands aside when it detects that patch"
    - "`run_report._lane_for_entry` + `_OUTCOME_TEXT[FAILED]` <-> the literal report line quoted in the todo"
---

<objective>
Prove the todo `2026-09-09-written-records-labels-propose-and-enrich-legs-failed` is already
closed by Phase 70's D-70-09 ledger gate, on the exact run shape the todo recorded, then
close the todo.

Purpose: the todo asks for one of two fixes. Phase 70 Plan 06 Task 2 shipped option (a) —
"stop recording enrichment legs in `written_records` at all ... `dispatch_plan` should append
only when the leg's mode can write" — as caller discipline at
`chunking.dispatch_and_recover`: `if can_write and rows:`, with `can_write` read off the
BUILT envelope via `envelope_can_write`. What that phase's own regression test
(`test_a_no_write_leg_never_enters_the_ledger`) did NOT do is drive the gate with the
pathological row the todo actually recorded — it uses a well-formed
`{"row_id": "r1", "action": "update", "hs_object_id": "1001"}` — and it asserts on a ledger
DOUBLE, never on the rendered report. So the todo's own acceptance sentence, "the report must
never say failed for a row that was not sent", is not yet pinned end to end.

Output: two regression tests in `operator-claude-plugin/tests/test_written_records.py`, and
the todo moved to `.planning/todos/completed/` with a resolution record. No production code
change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md
@operator-claude-plugin/tests/test_written_records.py
@operator-claude-plugin/scripts/chunking.py
@operator-claude-plugin/scripts/run_report.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the recorded run shape — control first, then the gate</name>
  <files>operator-claude-plugin/tests/test_written_records.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_written_records.py` lines 419-435 (`_patch_durable_dir`)
      and 719-830 (the existing D-70-09 block: `_RecordingLedger`, `ledger_double`,
      `_stub_channel`, `_recovered`, `_A_WRITTEN_ROW`, and the three tests already there).
    - `operator-claude-plugin/scripts/chunking.py` lines 595-685 (`dispatch_and_recover`, the
      `if can_write and rows:` gate) and 379-399 (`envelope_can_write`).
    - `operator-claude-plugin/scripts/run_report.py` lines 474-478 (`_lane_for_entry`),
      705-730 (the per-record render loop), 404-432 (`_OUTCOME_TEXT`).
    - `operator-claude-plugin/tests/conftest.py` lines 688-722 (`no_durable_writes` — it
      stands aside when `durable_paths.resolve_state_path` is already patched, which is what
      keeps `append_chunk`'s write and `load()`'s glob pointed at one directory).
  </read_first>
  <behavior>
    - Control (the reproduction): a ledger seeded DIRECTLY with the recorded shape
      `{"row_id": "row-2"}` — nothing else on the dict — makes `run_report.build_run_report`
      render the literal line the todo quoted, `row-2 [contacts:unknown]: None -> failed`.
      This is the RED evidence: the shape is genuinely pathological, and the test harness can
      genuinely see the ledger.
    - Gate, propose leg: the SAME recorded row, arriving as `recover_dispatch`'s recovered row
      for a contacts `rows` spec (pinned to `mode: propose` by construction inside
      `enrichment.build_envelope`), never reaches the ledger — the report's per-record section
      renders `- (no records)` and the string `row-2 [contacts:unknown]` is absent from the
      whole block.
    - Gate, write leg (same test, parametrized): the SAME recorded row on a
      `{"record_ids": [...], "object_type": "companies"}` spec DOES land and DOES render the
      failed line — so the propose-leg assertion above is attributable to the gate rather than
      to a harness that cannot reach the ledger.
  </behavior>
  <action>
    Append to the existing D-70-09 block at the END of
    `operator-claude-plugin/tests/test_written_records.py` — do not open a new test file and
    do not edit any existing test. Add `import run_report` beside the block's existing
    mid-file `import chunking` / `import pytest` lines, following the same `# noqa: E402`
    idiom already used there.

    Define the recorded row ONCE as a module-level constant next to `_A_WRITTEN_ROW`,
    carrying `row_id` and no other key, with a short comment citing run `2bc3617b` and the
    report line it produced. Reconstruct it from what the report line proves rather than from
    memory: `[contacts:unknown]` is `_lane_for_entry`'s `object_type or "unknown"` /
    `action or "unknown"` pair, so `object_type` was absent (classify_item's `"contacts"`
    default) and `action` was absent; `None -> failed` is the entry's own null `action` and
    `outcome_for_action(None)`'s `ACTION_TO_OUTCOME` fallback.

    Write TWO test functions, named exactly
    `test_the_recorded_shape_renders_failed_when_it_reaches_the_ledger` and
    `test_the_recorded_shape_reaches_the_ledger_only_from_a_write_capable_leg`, both taking
    `tmp_path, monkeypatch` and both calling the file's
    existing `_patch_durable_dir(monkeypatch, tmp_path)` FIRST so the real
    `written_records.append_chunk` write and `run_report`'s `written_records.load()` glob
    resolve into one directory:

    1. The control. Call `written_records.append_chunk(<a run id>, 0, [<the recorded row>])`
       with the REAL function (no `ledger_double` here), then
       `run_report.build_run_report(<that run id>, {})` and assert the returned `block`
       contains the substring `row-2 [contacts:unknown]: None -> failed`.

    2. The gate, parametrized over two spec forms with their expected report reading — the
       contacts `rows` propose form and the companies `record_ids` write form. Use the block's
       existing `_stub_channel` fixture to make `watch.recover_dispatch` answer with the
       recorded row, `chunking.plan_chunks(spec_form, 10)`, then `chunking.dispatch_and_recover`
       with the block's existing `fake_config` / `stub_module_transport_factory()` fixtures and
       an explicit `run_id=`. Do NOT install `ledger_double` for this test — the whole point is
       that the REAL ledger and the REAL report run. Then `build_run_report` on that same run
       id and assert: for the propose form, the block contains `- (no records)` and does NOT
       contain `row-2 [contacts:unknown]`; for the write form, the block DOES contain
       `row-2 [contacts:unknown]: None -> failed`.

    Give both functions a docstring naming the todo filename, run `2bc3617b`, and D-70-09, so
    a later reader finds the provenance without leaving the file.

    Change NOTHING under `operator-claude-plugin/scripts/`. If either test does not pass as
    written, STOP and report — a production edit here would mean D-70-09's gate does not
    actually hold, which is a finding, not something to patch inside this task.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_written_records.py -x -q</automated>
  </verify>
  <done>
    Both `test_the_recorded_shape_renders_failed_when_it_reaches_the_ledger` and
    `test_the_recorded_shape_reaches_the_ledger_only_from_a_write_capable_leg` exist at the end
    of `test_written_records.py`'s D-70-09 block, the second parametrized over a propose leg and
    a write leg (3 collected cases across the two functions). The file's pytest run is green.
    `git diff --stat operator-claude-plugin/scripts/` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the todo with its resolution record</name>
  <files>.planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md, .planning/todos/completed/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md</files>
  <read_first>
    - `.planning/todos/completed/2026-09-09-ingest-review-branch-responds-queue-only-and-reads-as-failed.md`
      (its trailing `## Resolved 2026-09-09` section is the convention to match).
    - `.planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md`
  </read_first>
  <action>
    Append a `## Resolved 2026-09-11` section to the todo body, leaving every existing line
    untouched, and bump the `updated:` front-matter key to `2026-09-11`. The section states:
    the todo's own `## Fix shape` option (a) was already implemented by Phase 70 Plan 06 Task 2
    (D-70-09) as caller discipline at `chunking.dispatch_and_recover`'s append site — the
    `if can_write and rows:` guard, with `can_write` read off the built envelope through
    `chunking.envelope_can_write` rather than re-derived from the spec; that no production
    change was needed by this task; and that what this task added is the missing end-to-end
    proof on the recorded shape, naming the two new test functions in
    `operator-claude-plugin/tests/test_written_records.py` and the run id `2bc3617b`.

    State plainly that the tested row shape was RECONSTRUCTED from the rendered report line,
    because no captured body of that propose leg exists anywhere in the repo — a
    `grep -rl 2bc3617b` over `.planning/` and the plugin's fixtures during planning found only
    prose records (`.planning/uat/UAT-autonomous-batch-2026-09-09.md` § "Round B", the todo
    itself, and `70-06-SUMMARY.md`), never a frozen body. That sentence exists so the next
    reader does not hunt for a fixture that was never captured.

    Record the two facts a future reader would otherwise re-investigate: the sibling surface
    the todo's suspects list pointed at is NOT affected — `report_enrichment._outcome_for_row`
    reads `enrichment_row_ledger`, whose rows come from the `Decide Company Action` /
    `Decide Action` nodes, and those stamp `action: "proposed"` on a propose leg
    (`scripts/build_cloud_workflows.py`), resolving to `no_action`, never the fallback; and the
    todo's third named file, `operator-claude-plugin/scripts/run_report.py`, needed no change
    because its per-record section reads the ledger and nothing else, so an empty ledger is an
    empty section.

    Then move the file to `.planning/todos/completed/` with `git mv`, keeping the filename
    unchanged.
  </action>
  <verify>
    <automated>test ! -e .planning/todos/pending/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md && /usr/bin/grep -q "D-70-09" .planning/todos/completed/2026-09-09-written-records-labels-propose-and-enrich-legs-failed.md && echo OK</automated>
  </verify>
  <done>
    The todo exists only under `.planning/todos/completed/`, carries a trailing `## Resolved`
    section citing D-70-09, the gate's call site and both new test names, and `git status`
    shows the move as a rename rather than a delete plus an add.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> operator's real durable state directory | `written_records.append_chunk` resolves its own path; an unpatched test writes into the operator's live artifacts (bug_001, 53-WALK-RECORD-2.md FINDING A: 413 stray files) |
| n8n runData -> `written_records` ledger -> operator's end-of-run report | untrusted backend row shapes cross into a durable artifact and an operator-facing verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260911-ao0-01 | Tampering | `test_written_records.py`'s new tests calling the REAL `append_chunk` | medium | mitigate | Both tests call the file's existing `_patch_durable_dir(monkeypatch, tmp_path)` FIRST; conftest's autouse `no_durable_writes` plus `_refuses_real_durable_write_under_pytest` are two further independent stops. No new durable path is introduced. |
| T-260911-ao0-02 | Information disclosure | the recorded row constant added to the test file | low | mitigate | The constant carries `row_id` only — deliberately no `email` and no other PII, matching `written_records.py`'s own documented exclusion. The real run's row is reconstructed from the report line, never copied from a live artifact. |
| T-260911-ao0-03 | Repudiation | the operator-facing outcome word for a row that was never sent | high | mitigate | This is the todo's own defect class. Task 1 pins both directions — a never-sent row absent from the report, a sent row still rendered — so a regression that re-opens the gate fails a test rather than silently mislabelling a run. |
| T-260911-ao0-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install of any kind: this task adds tests to an existing suite and moves one markdown file. No dependency is added, so the package-legitimacy gate has nothing to admit. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_written_records.py -x -q` green.
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` green (no collateral damage to
  the ~2300-test plugin suite — this task adds tests and edits none).
- `git diff --stat operator-claude-plugin/scripts/ n8n/ scripts/` empty: no production module,
  no workflow JSON, no builder touched. Nothing deployed, nothing armed.
</verification>

<success_criteria>
- The recorded `2bc3617b` row shape is pinned in both directions: it renders the todo's exact
  report line when it reaches the ledger, and it never reaches the ledger from a propose leg.
- No file under `operator-claude-plugin/scripts/` changed.
- The todo lives in `.planning/todos/completed/` with a resolution section a reader can act on
  without re-deriving the investigation.
</success_criteria>

<output>
Create `.planning/quick/260911-ao0-todo-2026-09-09-written-records-labels-propose-and-enrich-le/260911-ao0-SUMMARY.md` when done
</output>
