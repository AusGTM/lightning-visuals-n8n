---
context: default
phase: 70-one-merge-one-result-channel-n8n-runtime-truth
task: 0
total_tasks: 0
status: complete
last_updated: 2026-09-10T21:01:45Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Frozen n8n runData carries the live webhook secret — redact every `headers` object before committing any recording; `tests/n8n/v1RuntimeRecordings.test.mjs` refuses an unredacted secret.
- [ ] CONSTRAINT: Walker moves toward the engine, never toward a green suite (D-70-19) — every walker change needs a RED-first proof against the previous walker commit; `tests/n8n/walkerEngineFidelity.test.mjs` and `n8n/wf_*.json` stay zero-diff.
- [ ] CONSTRAINT: Never hand-edit `n8n/wf_*.json`; regenerate with `scripts/build_cloud_workflows.py`. Nothing is armed; arming needs `ALLOW_N8N_ARM=true` and a deliberate record-scoped window.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Token-shape secret scan | Scan for Bearer/sk-/pat- found nothing; secret was a plain string under a header name | blocking | Scan by structure (`"headers"` keys) and run the redaction guard test |
| Self-authored acceptance | Reviewer agreeing with a briefed rationale is not confirmation | advisory | Guard test seen RED first, then GREEN |

## Current State

All phases complete (v1.2, Phase 70 sealed at `ec102a4`). Three quick tasks (260911-0tz, 1z5, 3mu) closed after four review passes: no blocker, no major. master == origin/master at `abb71228`. Plugin 0.44.0 released, marketplace clone pulled, installed (restart to load).

Live: five cloud workflows on committed v1 bodies (287/69/55/43/30 nodes), active, disarmed.

## Completed This Session

- Walker v1 model (`tests/n8n/lib/walkWorkflow.mjs`): zero-item output never delivers, run-indexed Merge buffering, producer fan-out fills one pending run, one-run end-of-run drain, fire/delivery caps; `starvedWithData` reports `merge_pending_runs_undrained`, `merge_dropped_rows`.
- Fidelity pins: `walkerEngineFidelityV1.test.mjs` (12354/12355/12356), `creditsSummaryUnderV1.test.mjs`, `v1RuntimeRecordings.test.mjs`.
- Frozen: `tests/n8n/fixtures/frozen/exec_1235{4..8}.runData.json`, `wf_enrichment_cloud.v1.2026-09-10.json`.
- CLAUDE.md §13.0.3 v1 rows `[observed live]`; README, n8n/README, UAT doc, CHANGELOGs swept.

## Open Items

- Builder follow-on: `Associate Carry Merge` drops one row's association result when `HubSpot Associate Company` returns nothing (committed ingest graph). Pinned in `tests/n8n/writeGateShape.test.mjs`. Graph-shape change in `scripts/build_cloud_workflows.py`.
- Todo: `.planning/todos/pending/2026-09-11-merge-input-contract-allows-many-producers-per-input.md` (needs live observation).
- Human: rotate `x-enrichment-secret`; restart Claude Code for plugin 0.44.0.

## Resume

`/gsd-resume-work` then pick an open item above, or `/gsd-progress`.
