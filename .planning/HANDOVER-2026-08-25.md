# Handover — 2026-08-25

Written at the end of a long session, for whoever picks this up next (probably a fresh
context). Everything below is committed; nothing is in flight.

**Where we are:** v1.1 Phase 53 (operator-openable write grant) is **code complete and
released at plugin 0.16.2**. Its one blocking checkpoint — the operator walk — is
**unfinished**, and the walk is what keeps finding the real defects.

---

## 1. Do this first

**VOCAB-05 — kill the arming phrase, keep the consent.** Full requirement in
`.planning/milestones/v1.1-REQUIREMENTS.md` (VOCAB section). Recorded but NOT implemented.

Observed live: the operator was shown a preview ending "Proceed?", said **"yes"**, and was
told approval alone does not dispatch — say the literal string *"arm the enrichment"*. A magic
phrase demanded at the exact moment they were trying to say yes.

**The safety property was never the spelling.** What made the phrase safe is that a casual
"ok" cannot become a write. Preserve that by binding the affirmative to *this send's shown
consequence in the same turn*:

- affirmative answering the proposal just shown → arms **that send only**;
- affirmative answering nothing, or a different question → does **not** arm; ask once more,
  naming what will happen;
- ambiguity → not armed, always;
- `armed` still has no default in code (`dispatch()` raises without it). **That structural
  guarantee does not move.**

**Scope — 4 skills, 3 test files:**

| File | What it holds |
| --- | --- |
| `operator-claude-plugin/skills/enrich-records/SKILL.md` | step 6 arming |
| `.../skills/contact-upload/SKILL.md` | step 5 arming |
| `.../skills/enrich-before-ingest/SKILL.md` | steps 5 and 7 (two phrases) |
| `.../skills/review-triage/SKILL.md` | its own phrase |
| `operator-claude-plugin/tests/test_enrich_skill_contract.py:76` | `'"arm the enrichment"' in body` |
| `.../tests/test_enrich_before_ingest_skill_contract.py:30,53-64` | `ARM_ENRICHMENT_PHRASE` + the combined-phrase refusal list |
| `.../tests/test_preingest_preview.py` | phrase references |

Those pins are **rewritten in place with the reason recorded, never deleted** — the same
discipline as the arm/probe parity pin (53-01 Task 3) and the D-53-05 ordering pin (53-04
Task 1). A sweep across them is how the property gets lost.

**Then:** finish the walk (§3), then **Phase 58**, then **Phase 54** (operator's order,
`17bd329`).

---

## 2. What happened today

Phase 53 executed in 4 waves (`7607825` → `c9e1e3e`), all plans have SUMMARYs. Then the
**partial walk found four defects, none of which 3084 passing tests could see** — three of
them from the operator typing an ordinary sentence:

| Release | Defect |
| --- | --- |
| **0.15.1** | A grant removed the *arming phrase* but left the older *"Ask for approval"* standing — half the friction, none of the protection back (**D-53-06**) |
| **0.16.0** | "John Tsatsimas at Football NSW" had nowhere to go — the backend has resolved contacts by name since Phase 36, the client never offered the form (`people` form added). **And a LinkedIn URL normalised to the domain `linkedin.com`** — which would have created a company under that domain, after which every LinkedIn-sourced company matched that one poisoned record |
| **0.16.1** | A blanket refusal hands research back to an operator who does not want to do it — a company can now be named with **no domain at all** (exact-name search resolves it; creating a *new* company still needs a domain, the dedupe anchor) |
| **0.16.2** | The `people` form shipped in code with 20 green tests while `SKILL.md` never mentioned it — an earlier patch raised partway and aborted before writing. **The skill is what the model reads**, so the operator was correctly told the capability did not exist |

**Earlier the same day** (before Phase 53): contact→company association in the ingest lane
(`b014b69`), the company-lane exact-name fallback found live via HRNSW (`472ac50`), and two
provider defects — Lusha dead on the contacts lane since 2026-07-30 (`Object.prototype` in an
n8n *expression*, refused by that sandbox and swallowed by `onError`), and contact research
called with `name: null` because it read a key nothing sets (`ca954c9`).

---

## 3. The walk (operator does this, in Claude Desktop)

Preconditions are all met: `allow_write_grants: true` is set, master is pushed, the clone is
refreshed, the install is current. **Verify a skill by its CONTENT, never by its version
string** — 0.16.1 carried the right version number and the wrong skill file.

The operator says, in their own words:

> **Update John Tsatsimas from Football NSW**

then answers whatever it asks — and today it wrongly demands the phrase. That is VOCAB-05.
After the fix, a plain **"yes"** must be enough.

**What it should do:** enrich once and **write** — contact `347569451461` currently has the
enrichment derived but never written (`write_blocked`, verified live). Expect `jobtitle` CEO,
`seniority` C-Suite, phone, mobile, `lv_persona_group`, and `email johnt@footballnsw.com.au`
staged for review (email is manual-protected).

**The question the walk must answer:** is revocation at the **next send** enough? At a
2-record chunk ceiling a 40-record send is 20 chunks and all 20 run after a revoke.
Making `dispatch_plan` grant-aware changes the shared dispatch loop every lane uses — its own
phase, not a patch.

---

## 4. Traps that cost time today

- **`STATE.md` has no single writer.** Repaired twice (`4135f41`, `a9d91f9`); three wave
  executors hand-edited it in sequence and the stale reads compounded. A third repair earns a
  WINDOWS entry.
- **Three GSD verbs are broken for v1.1**: `state.advance-plan` (corrupted STATE, miscounts a
  4-plan phase), `requirements.mark-complete` and `roadmap.update-plan-progress` (silent
  no-ops — they read `.planning/REQUIREMENTS.md` / `ROADMAP.md`, while v1.1's live under
  `.planning/milestones/`). **Edit those files directly and diff STATE.md.**
- **Plugin release is four steps**, and steps 3–4 are the operator's: bump + CHANGELOG in the
  same commit, push master, then `fetch --depth=1` + `reset --hard FETCH_HEAD` on
  `~/.claude/plugins/marketplaces/lightning-visuals-operator`, then Update in Desktop.
  Until the clone is refreshed the release is invisible however correct the bump is.
- **`.env` is unreadable from this session.** n8n API access comes from
  `operator-claude-plugin/config/operator.local.json`. There is no HubSpot token here — read
  HubSpot state through a disarmed enrichment probe (costs 1 n8n execution, writes nothing).

## 5. Invariants held all day — check them after any change here

```
.venv/bin/python -m pytest -q                 # 3084 passed, 154 skipped
node --test tests/n8n/*.test.mjs              # 711 passed
git diff --name-only <base> HEAD -- n8n/ scripts/ \
  operator-claude-plugin/scripts/scheduled_arm.py \
  operator-claude-plugin/tests/test_scheduled_arm.py   # must be EMPTY
```

`n8n/`, `scripts/`, `scheduled_arm.py` and the headless authority (`ALLOW_N8N_ARM`) were
untouched by all of Phase 53 — deliberately. Disarm is never gated on anything; the test
pinning that must keep passing byte-identical.

## 6. Operator rulings that are NOT open for re-litigation

- **D-53-02** the computed worst case IS the ceiling — it discloses, it does not constrain, so
  Phase 57 carries all the spend protection.
- **D-53-03** the grant is client-held for the session; a crashed session leaves the backend
  armed (accepted, guardrail A makes it loud).
- **D-53-04** a failed disarm fails that send only; guardrail B bounds the unknown.
- **D-53-05** one grant spans both `enrich-before-ingest` lanes — the write is authorized
  before the enriched preview exists, accepted for speed, recorded in the contract test.
- **D-53-06** a grant removes **both** asks, not one.
- **VOCAB-01..05** the operator never speaks the system's language; the test is a walk
  transcript, not a review. *If the operator has to be told what to type, the wording failed.*

## 7. Queued after the walk

- **Phase 58** — take what the operator actually has: screenshots, search results, any URL, a
  bare name. The contact lane has had extraction adapters since Phase 35; the **company lane
  never has**. Where no domain exists, research one with the `Claude Web Research` node the
  companies branch already runs, and confirm before writing.
- **Phase 54** — single-pass dispatch, killing the double-enrichment (G-3).
- **Phase 55–57**, then **Phase 52** (v1.0 staged canary, deferred — on resume, re-derive
  Phase 51's population/credit sizing, which was dated 2026-08-19 and drifts, and settle the
  FILL-04 third disposition).
- **Known debt:** `wf_enrichment_cloud`'s own contact create does not associate (the
  2026-08-25 association rule is ingest-lane only).
