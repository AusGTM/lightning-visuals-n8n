---
phase: 44-sj-3-dispatch-gate-drain-cap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_cloud_workflows.py
  - scripts/deploy_n8n_workflows.py
  - scripts/verify_live_write_safety.py
  - n8n/code/sj3DispatchGate.js
  - n8n/wf_scheduled_maintenance_cloud.json
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_contact_ingest_cloud.json
  - tests/n8n/sj3DispatchGate.test.mjs
  - tests/n8n/sjPredicates.test.mjs
  - tests/test_write_gate_coverage.py
autonomous: false
requirements: [GATE-01, DRAIN-01, DRAIN-02, DRAIN-03]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A row SJ-3 found but `_writeSafetyAllows` declines never reaches `SJ-3 Dispatch To Enrichment` (GATE-01, D-01/D-02)."
    - "A row `_writeSafetyAllows` permits reaches `SJ-3 Build Dispatch Event` with its payload unchanged (D-03 defence-in-depth preserved)."
    - "Every declined row reaches a HubSpot write that clears `lv_enrichment_requested` (DRAIN-01, D-05/D-06)."
    - "That drain write's patch contains only `lv_enrichment_requested=\"false\"` and `lv_enrichment_status=\"skipped\"` — structurally, as baked literals in the built JSON (DRAIN-02, D-07 as amended 2026-08-10)."
    - "A drained record is distinguishable from an enriched one: `skipped` is written by nothing else in the pipeline, and no consumer reads a cleared flag as evidence of processing (DRAIN-03, D-08)."
    - "The drain's authority is unreachable from `ALLOW_HUBSPOT_RECORD_WRITES` / `ALLOW_HUBSPOT_REVIEW_WRITES` in either direction, and is absent from the arm/overlay system (D-05)."
  artifacts:
    - n8n/code/sj3DispatchGate.js
    - tests/n8n/sj3DispatchGate.test.mjs
    - "scripts/build_cloud_workflows.py — `ALLOW_SJ3_DRAIN_WRITES` in WRITE_SAFETY_DEFAULTS, SJ-3 gate + drain nodes"
    - "n8n/wf_scheduled_maintenance_cloud.json — regenerated with the SJ-3 gate/drain cluster"
    - "tests/test_write_gate_coverage.py — the reviewed drain-node exemption and its stricter replacement assertions"
  key_links:
    - "`SJ-3 Extract Rows` -> `SJ-3 Dispatch Gate` -> `SJ-3 Build Dispatch Event` (the permitted path; breaks GATE-01/GATE-03 if mis-wired)"
    - "`SJ-3 Dispatch Gate` -> `SJ-3 Drain Gate` -> `SJ-3 Drain Clear Flag` (the declined path; breaks DRAIN-01 if mis-wired)"
    - "`NODE_CREDENTIAL_MAP[\"SJ-3 Drain Clear Flag\"]` — an unmapped HubSpot node deploys UNBOUND and 401s only at runtime (this repo's four-time-repeated failure)"
    - "`domain` in SJ-3's `properties_csv` — without it a domain-scoped armed window can never permit an SJ-3 row (BUG 24's class)"
---

<objective>
Build the SJ-3 dispatch gate and the queue drain as one end-to-end slice, offline: a record
the write-safety allowlist declines is filtered out of dispatch (GATE-01) and has its trigger
flag cleared through a structurally narrow write (DRAIN-01/02/03), while a permitted record
reaches dispatch unchanged.

This plan carries the phase's two architecturally risky moves — the first write authority in
the system that is enabled at rest (D-05), and the deliberate amendment of the repo's
write-gate coverage guarantee — so they are proven on the executor's best early-context
tokens rather than discovered after the cap work is already committed.

Purpose: prove the gate/drain architecture end-to-end before expanding it with the cap.
Output: a regenerated `wf_scheduled_maintenance_cloud.json` whose SJ-3 lane declines, drains,
and dispatches correctly, with both suites green. **No deploy in this plan** — the phase ships
gate + drain + cap in one build -> deploy -> bounce (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-CONTEXT.md
@.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-RESEARCH.md
@.planning/WINDOWS.md

Read before editing (exact regions, do not re-read a range twice):
- `scripts/build_cloud_workflows.py:894-949` — `WRITE_SAFETY_DEFAULTS`, `_write_safety_const`,
  `WRITE_SAFETY_GATE_JS` / `_writeSafetyAllows`
- `scripts/build_cloud_workflows.py:5707-5731` — `_hs_update_set_property`,
  `_execute_workflow_node`
- `scripts/build_cloud_workflows.py:5734-5803` — `_write_gate_js` / `splice_write_gates`
  (read to understand why the drain must NOT go through it — RESEARCH Pitfall 2)
- `scripts/build_cloud_workflows.py:5806-5841` — the SJ-3 cluster this plan modifies
- `scripts/build_cloud_workflows.py:5421-5449` — `ENRICH_EXTRACT_SEARCH_ROWS`,
  `ENRICH_SJ3_BUILD_DISPATCH_EVENT`
- `scripts/build_cloud_workflows.py:60-91` — `strip_module` / `inline` (module embedding contract)
- `scripts/deploy_n8n_workflows.py:44-120` — `NODE_CREDENTIAL_MAP`
- `scripts/deploy_n8n_workflows.py:155-200` — the `_OVERLAY_FLAG_SPEC` exclusion comment
- `n8n/code/enrichmentGate.js` (tail) — the `module.exports = { ... }` convention
- `tests/test_write_gate_coverage.py` (full file)
- `tests/n8n/sjPredicates.test.mjs` (full file)
- `tests/n8n/reviewLoop.test.mjs` — the pure-module + wiring two-layer test pattern
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: approve D-05's default-true drain authority (reversibility: costly)</name>
  <decision>
    Introduce `ALLOW_SJ3_DRAIN_WRITES`, defaulting `"true"`, as a new key in
    `WRITE_SAFETY_DEFAULTS` — the first write authority in this system that is enabled at rest.
  </decision>
  <context>
    D-05 is locked; this checkpoint is not a re-litigation of it. It exists because the decision
    is rated **costly**: `WRITE_SAFETY_DEFAULTS` is load-bearing across `build_cloud_workflows.py`'s
    `ENABLE_BAKED_FLAGS` overlay, the plugin's arm/verify/disarm cycle, and
    `scripts/verify_live_write_safety.py`, and a prior spike that flipped an existing flag broke
    64 tests across two packages and was reverted (WINDOWS.md #2). Undoing this means re-deploying
    workflow content and re-baking every write-safety assertion that learns the new constant.

    The bound being accepted, which MUST be written into the code as a comment and not only here:
    this authority may only ever set `lv_enrichment_requested` to `"false"` and
    `lv_enrichment_status` to `"skipped"` on records the gate declined in the same tick. It removes
    queued work; it cannot create or alter data. **It is not precedent for defaulting any other
    write on.** A `false`-defaulting drain was considered and rejected: it would run only inside an
    armed window, which is precisely when the queue is not stuck.

    Residual the operator should see before approving (found during planning, not a new decision):
    `operator-claude-plugin/scripts/scheduled_arm.py` arms the *enrichment* workflow only — the
    maintenance workflow's own baked constants stay disarmed throughout an armed window. If the
    arm window closes between the companion's dispatch and the next SJ-3 tick, that tick sees the
    just-dispatched records as no longer allowlisted, declines them, and drains them — clearing the
    flag on work that is still in flight. Where that matters is a run that then FAILS: D-04 says a
    dispatched-and-failed record stays flagged for retry, and the drain would already have cleared
    it. At daily cadence the odds of a tick landing in that window are low, and recovery is a
    re-queue by SJ-1/SJ-2 on the next scan, not data loss.

    A `lv_enrichment_status !== "running"` guard was considered as a mitigation and **rejected as
    vacuous**: nothing in this pipeline ever writes `"running"` (only `"needs_review"` and
    `"complete"` are written — `build_cloud_workflows.py:2797,2802`), so such a guard could never
    match an in-flight record and would close nothing while implying it had. Recorded here so it is
    not re-proposed. The residual is accepted as stated; closing it properly means giving the
    pipeline a real in-flight marker, which is its own change and outside this phase.
  </context>
  <options>
    <option id="approve">
      <name>Approve as decided (D-05)</name>
      <pros>The drain runs at rest, which is the only state in which the queue actually gets stuck. Closes the runaway.</pros>
      <cons>First enabled-at-rest write; the arm-window residual above stays open, accepted and documented.</cons>
    </option>
    <option id="halt">
      <name>Halt and re-open D-05 with the operator</name>
      <pros>Nothing costly ships un-reconsidered.</pros>
      <cons>Phase 44 cannot proceed; the runaway's structural cause stays open.</cons>
    </option>
  </options>
  <resume-signal>Select: approve or halt</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: End-to-end "one record declined, one record dispatched" — SJ-3 gate + drain, one path each</name>
  <files>
    n8n/code/sj3DispatchGate.js,
    tests/n8n/sj3DispatchGate.test.mjs,
    scripts/build_cloud_workflows.py,
    scripts/deploy_n8n_workflows.py,
    n8n/wf_scheduled_maintenance_cloud.json,
    n8n/wf_enrichment_cloud.json,
    n8n/wf_contact_ingest_cloud.json,
    tests/n8n/sjPredicates.test.mjs
  </files>
  <read_first>
    `scripts/build_cloud_workflows.py:894-949`, `:5421-5449`, `:5707-5731`, `:5806-5841`;
    `scripts/deploy_n8n_workflows.py:44-120`; `n8n/code/enrichmentGate.js` tail;
    `tests/n8n/reviewLoop.test.mjs`; `tests/n8n/sjPredicates.test.mjs`.
  </read_first>
  <behavior>
    New pure module `n8n/code/sj3DispatchGate.js` exporting `sj3Gate(rows, opts)`, where
    `opts.allows(row)` is the injected permission predicate. Injected so the module is unit-testable
    in plain node without the baked write-safety constants — the same purity `reviewApply.js` and
    `dedupeSweep.js` already have.

    - Two rows, `allows` true for both: both annotated `sj3_dispatch: true`, `sj3_drain: false`,
      returned in input order, payloads otherwise untouched.
    - Two rows, `allows` false for both: both annotated `sj3_dispatch: false`, `sj3_drain: true`.
    - Mixed: exactly the permitted subset carries `sj3_dispatch: true`, exactly the declined subset
      carries `sj3_drain: true`, and no row carries both.
    - Empty input: returns an empty array, throws nothing.
    - `sj3_dispatch` and `sj3_drain` are mutually exclusive on every row, always.
    (Cap and tick summary are Plan 02's expansion — do not add them here.)
  </behavior>
  <action>
    Build the thinnest complete SJ-3 slice that touches every layer this phase modifies: pure JS
    module -> Python builder -> generated n8n JSON -> credential map -> node tests. Both terminals
    are wired in this task; the cap is not.

    Write `n8n/code/sj3DispatchGate.js` per the behavior block, following `enrichmentGate.js`'s
    shape and ending in a `module.exports` line so `strip_module` truncates cleanly. Give it a
    header comment stating it never decides *whether* a write is permitted — it only routes rows
    the injected predicate has already judged.

    In `scripts/build_cloud_workflows.py`:

    1. Add `"ALLOW_SJ3_DRAIN_WRITES": "true"` to `WRITE_SAFETY_DEFAULTS` (per D-05, approved at
       the checkpoint above). Directly above it, write the justification as a comment in the same
       register as the existing write-safety commentary: this authority may only ever set
       `lv_enrichment_requested` to `"false"` and `lv_enrichment_status` to `"skipped"` on records
       the gate declined in the same tick; it removes queued work and cannot create or alter data;
       it is not precedent for defaulting any other write on; a `false`-defaulting drain would run
       only inside an armed window, which is precisely when the queue is not stuck. Note that
       adding this key makes `WRITE_SAFETY_GATE_JS` declare it in every gate node of all three
       write-bearing cloud workflows, which is why the two sibling workflow artifacts are
       regenerated in this task too.
    2. Add `domain` to SJ-3's search `properties_csv` (`:5824`), following SJ-1's and SJ-2's
       precedent at `:5855-5857` and `:5889-5891`. Carry the same reason in a comment: BUG 24's
       class — without `domain` on the row, a domain-scoped armed window is structurally unable to
       permit an SJ-3 row, so D-01's per-record gate would silently deny everything in exactly the
       windows `scheduled_arm.py` opens.
    3. Add a `SJ-3 Dispatch Gate` Code node between `SJ-3 Extract Rows` and
       `SJ-3 Build Dispatch Event`. Its body is `WRITE_SAFETY_GATE_JS` (verbatim, per D-02 — one
       definition of "permitted", so the poller cannot drift from the enrichment lane) plus
       `inline("sj3DispatchGate.js")` plus a wrapper that calls `sj3Gate` with an `allows` closure
       delegating to `_writeSafetyAllows` with the `"enrich"` action, the row's `hs_object_id`, and
       the row's `domain`. It returns **all** rows annotated, not only the permitted ones — the
       declined rows are what the drain branch consumes.
    4. Filter the permitted rows inside `ENRICH_SJ3_BUILD_DISPATCH_EVENT` by adding a filter on
       `sj3_dispatch` ahead of its existing map, rather than adding a separate filter node. When
       every row is declined this node returns an empty array, `SJ-3 Dispatch To Enrichment`
       receives zero items and does not run, and the tick costs 1 execution rather than 1+N — the
       zero-items-stops-the-chain behaviour this repo established live at execution 22
       (`build_cloud_workflows.py:4178-4179`). Plan 03 verifies that empirically; do not treat it
       as settled here.
    5. Add a `SJ-3 Drain Gate` Code node fed directly from `SJ-3 Dispatch Gate` (a second consumer
       of the gate's single output — fan-out, not a chain). It declares and reads
       `ALLOW_SJ3_DRAIN_WRITES` with an exact-string comparison against `"true"` per CLAUDE.md §21,
       and passes through only rows carrying `sj3_drain`. It MUST NOT call `_writeSafetyAllows` and
       MUST NOT consult `TEST_RECORD_IDS` or `TEST_RECORD_DOMAINS` (D-06: the stuck queue is
       overwhelmingly non-allowlisted records — that is the failure mode itself — so an allowlisted
       drain would clear only records that were never stuck). Do NOT route this through
       `splice_write_gates` / `_write_gate_js`: both hardcode `_writeSafetyAllows`, whose allowlist
       branch is unconditional (RESEARCH Pitfall 2). Repeat the D-05 bound as a comment on this
       node — but write that comment carefully: Task 2's tests negative-grep this node's **entire**
       jsCode, and a Code node's comments are part of its jsCode, so a substring assertion cannot
       tell prose from executable code. Describe the exclusion in words ("the shared write-safety
       helper", "the record allowlist", "D-06") without spelling either excluded identifier
       verbatim, or the node's own documentation will fail the test that documents it.
    6. Extend `_hs_update_set_property` with an optional trailing parameter carrying additional
       (property, value) literal pairs appended to `customPropertiesValues`, leaving both existing
       call sites byte-unchanged. Add `SJ-3 Drain Clear Flag` using it: resource `company`,
       `lv_enrichment_requested` = `"false"` plus `lv_enrichment_status` = `"skipped"`. Both keys
       and both values are baked literals in the built JSON, which is what makes DRAIN-02's
       narrowness structural rather than a runtime promise. `skipped` is chosen because
       `lv_enrichment_status` is a closed enumeration (`config/hubspot_properties.yaml:308-337`)
       and `skipped` is the one option nothing in the pipeline writes today — so it needs no
       property migration and collides with no existing meaning (D-08, DRAIN-03). Note in a comment
       that this stamp is provenance only: clearing `lv_enrichment_requested` alone already removes
       the record from SJ-3's `EQ "true"` filter.
    7. Wire the fan-out: `SJ-3 Dispatch Gate` -> `SJ-3 Build Dispatch Event` and
       `SJ-3 Dispatch Gate` -> `SJ-3 Drain Gate` -> `SJ-3 Drain Clear Flag`.

    In `scripts/deploy_n8n_workflows.py`, add `SJ-3 Drain Clear Flag` to `NODE_CREDENTIAL_MAP`
    with `hubspotAppToken` / `LV HubSpot`, alongside `SJ-1 Set Requested` at `:115`. An unmapped
    HubSpot node deploys UNBOUND and 401s only at runtime — the failure this repo has taken four
    times, per that map's own comments. Register it in the same commit as the node's creation, per
    the convention stated at `:53-55`.

    Then regenerate all cloud workflow JSON through the builder. Never hand-edit `n8n/*.json`.

    Extend `tests/n8n/sjPredicates.test.mjs`: its existing SJ-3 assertions pin
    `Extract Rows -> Build Dispatch Event -> Dispatch` and will need updating for the spliced gate —
    update them to pin the new chain, and add a `properties_csv` assertion covering `domain` for
    SJ-3 (no test currently asserts on any lane's property list, so this is a new guard, not a
    restored one).

    Write `tests/n8n/sj3DispatchGate.test.mjs` in the two-layer shape `reviewLoop.test.mjs`
    establishes: `createRequire` the module and cover every case in the behavior block, then a
    separate wiring section loading `n8n/wf_scheduled_maintenance_cloud.json` from disk and
    asserting both terminals are reachable — the permitted path through `SJ-3 Build Dispatch Event`
    to the Execute Workflow node, the declined path through `SJ-3 Drain Gate` to
    `SJ-3 Drain Clear Flag` — and that `SJ-3 Drain Gate`'s jsCode contains `ALLOW_SJ3_DRAIN_WRITES`
    and contains neither `_writeSafetyAllows` nor `TEST_RECORD`.
  </action>
  <verify>
    <automated>node --test tests/n8n/sj3DispatchGate.test.mjs tests/n8n/sjPredicates.test.mjs</automated>
    <automated>node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <done>
    The built `n8n/wf_scheduled_maintenance_cloud.json` carries both SJ-3 terminals: a permitted
    row reaches `SJ-3 Dispatch To Enrichment` with its dispatch-event payload unchanged, and a
    declined row reaches `SJ-3 Drain Clear Flag`. `SJ-3 Drain Gate` reads only
    `ALLOW_SJ3_DRAIN_WRITES`. `SJ-3 Drain Clear Flag` is in `NODE_CREDENTIAL_MAP`. SJ-3's search
    requests `domain`. `node --test tests/n8n/*.test.mjs` is green at 636 + the new cases.
  </done>
  <reversibility rating="costly">
    Introduces `ALLOW_SJ3_DRAIN_WRITES` defaulting `"true"` — the first enabled-at-rest write.
    Reverting requires a content redeploy plus re-baking every write-safety assertion. Gated by
    the `checkpoint:decision` above.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Amend the write-gate coverage guarantee for the drain node — deliberately, not by accident</name>
  <files>tests/test_write_gate_coverage.py</files>
  <read_first>`tests/test_write_gate_coverage.py:30-107` and `:146-212` (the dedupe-lane allowlist tests, the shape to mirror)</read_first>
  <action>
    `test_every_write_node_sits_behind_a_write_safety_gate` treats a node as gating if and only if
    the literal substring `_writeSafetyAllows` appears in some upstream node's jsCode, within six
    hops (`_all_paths_cross_a_gate`, `:50-64`). `SJ-3 Drain Clear Flag`'s upstream includes
    `SJ-3 Dispatch Gate`, which embeds `WRITE_SAFETY_GATE_JS` verbatim per D-02 — so the walker
    finds the literal and reports the drain as gated.

    **That is a pass for the wrong reason, and it is the whole justification for this task.** The
    drain write is reachable precisely on the rows `_writeSafetyAllows` *declined*; the string the
    walker matches is, for this node, evidence of the opposite of what the test claims. Leaving it
    to pass silently would hollow out the guarantee for the one node in the repo where it inverts.
    Do not weaken or delete the generic check — every other write node still needs it.

    Add a module-level exemption set naming `SJ-3 Drain Clear Flag` in
    `wf_scheduled_maintenance_cloud.json`, in the same explicit, commented style as
    `NO_WRITE_NODES_EXPECTED` (`:72-76`), with a comment recording the inversion above and the
    D-05/D-06 reason the drain cannot use the shared allowlist. Skip that node in the generic
    walk, and add dedicated replacement assertions that are strictly stronger for it:

    - Its sole feeder is `SJ-3 Drain Gate`.
    - `SJ-3 Drain Gate`'s jsCode contains an exact-string comparison of `ALLOW_SJ3_DRAIN_WRITES`
      against `"true"`, and contains neither the shared write-safety helper's identifier nor any
      record-allowlist constant name — pinning D-06 structurally. Because a Code node's comments
      are part of its jsCode, this negative assertion also constrains that node's prose; note in
      the test's own comment that the drain gate documents its exclusions without naming the
      excluded identifiers, so a future editor does not "improve" the comment into a failure.
    - The built node's `customPropertiesValues` is exactly the two pairs
      `lv_enrichment_requested` / `"false"` and `lv_enrichment_status` / `"skipped"`, in a
      key+value allowlist assertion that fails on any additional key **and** on any other value.
      This is DRAIN-02 as amended 2026-08-10 — a key+value allowlist, deliberately **not**
      a count of keys; state that in the test's own docstring, including why the original
      "exactly one key" wording was amended (the same write is what stamps the provenance
      DRAIN-03 needs).
    - Widening the exemption set or the property allowlist must be a reviewed act, asserted in the
      failure message the way the dedupe-lane tests already do (`:207-212`).

    Also add a short comment to `test_committed_write_safety_constants_are_all_disabled` noting its
    name is now imprecise: it verifies committed literals match their declared defaults (it derives
    the expected literal from `WRITE_SAFETY_DEFAULTS` itself at `:140`), and one entry now defaults
    true. Do not rename it — the name is referenced in prior phase notes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_write_gate_coverage.py -q</automated>
  </verify>
  <done>
    `tests/test_write_gate_coverage.py` passes; the drain node is exempt from the generic walk by
    name with the inversion recorded in a comment, and is covered instead by a feeder assertion, a
    D-06 negative assertion on the drain gate, and a key+value allowlist assertion on the built
    patch. Removing the drain gate or widening the patch fails the suite.
  </done>
</task>

<task type="auto">
  <name>Task 3: Teach the live write-safety verifier the new constant without arming it</name>
  <files>scripts/verify_live_write_safety.py, scripts/deploy_n8n_workflows.py</files>
  <read_first>`scripts/verify_live_write_safety.py:70-100` and `:200-230`; `scripts/deploy_n8n_workflows.py:155-200`</read_first>
  <action>
    `verify_live_write_safety.py` derives `CHECKED_CONSTANTS` from `_OVERLAY_FLAG_SPEC.keys()`
    (`:79`) and its disarmed branch hard-requires every boolean it tracks to read `"false"`
    (`:214-217`). `ALLOW_SJ3_DRAIN_WRITES` rests `"true"`, so adding it to that set would make the
    verifier declare a correctly-disarmed backend armed.

    Keep it out of `_OVERLAY_FLAG_SPEC` (`deploy_n8n_workflows.py:180-193`),
    `n8n_arming.OVERLAY_DISABLED_LITERALS`, and therefore `CHECKED_CONSTANTS` — this is the exact
    shape `ALLOW_JUDGE_ESCALATION` and `ALLOW_WEB_RESEARCH` are already in, for the same on-record
    reason at `deploy_n8n_workflows.py:160-164`: a disabled->enabled overlay has no meaningful entry
    for a flag that defaults enabled. Extend that comment to name `ALLOW_SJ3_DRAIN_WRITES` as the
    third such flag, and state the emergency-off path is the same one it already names — edit the
    default plus rebuild plus disarmed redeploy.

    Then add a separate, explicitly-named assertion to `verify_live_write_safety.py` so the
    constant is not merely unwatched: assert it is present in the live workflow content and reads
    `"true"`. A missing or `"false"` `ALLOW_SJ3_DRAIN_WRITES` means the drain is silently inert and
    the queue can re-form — the failure this phase exists to prevent — so it belongs in the
    verifier's output as its own line, not folded into the armed/disarmed verdict. The existing
    disarmed verdict must keep its current meaning unchanged for the five overlay constants.

    Do not add a test pinning the exclusion: `test_overlayable_flags_is_a_strict_subset_of_config_flag_defaults`
    (strict equality on five names) and `operator-claude-plugin/tests/test_control_flag_parity.py`
    already fail if the constant enters either overlay table. Run both suites to confirm they stay
    green rather than adding a third guard over the same property.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
    <automated>cd operator-claude-plugin && python3 -m pytest -q</automated>
  </verify>
  <done>
    `ALLOW_SJ3_DRAIN_WRITES` is absent from all three overlay/arm tables; the
    `deploy_n8n_workflows.py:160-164` comment names it as the third default-true exclusion;
    `verify_live_write_safety.py` reports the constant's live value on its own line and fails if it
    is missing or `"false"`; the full pytest suite (2427 + new) and the plugin suite are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n scheduled execution -> HubSpot CRM | An unattended, unauthenticated-by-a-human tick issues a PATCH against production records. |
| Build-time constant -> live workflow | A baked write-safety literal is the only thing standing between an activated workflow and unbounded writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Elevation of Privilege | `ALLOW_SJ3_DRAIN_WRITES` (default `"true"`) | high | mitigate | Authority reaches exactly one node (`SJ-3 Drain Gate`), which is not routed through `splice_write_gates` and cannot widen. The write it authorises is two baked literal key/value pairs, pinned by a key+value allowlist test (Task 2). |
| T-44-02 | Tampering | `SJ-3 Drain Clear Flag` patch body | high | mitigate | Keys and values are build-time literals in the generated JSON, not runtime-computed — a widened patch is a builder diff that fails Task 2's allowlist assertion, never a runtime surprise. |
| T-44-03 | Elevation of Privilege | Authority bleed into `ALLOW_HUBSPOT_RECORD_WRITES` / `ALLOW_HUBSPOT_REVIEW_WRITES` | high | mitigate | New constant is read by exactly one node and by no branch of `_writeSafetyAllows`; the existing gate function is unmodified (D-05 non-transitivity, mirroring the review/record precedent). |
| T-44-04 | Repudiation | A drained record indistinguishable from an enriched one | medium | mitigate | `lv_enrichment_status="skipped"`, a closed-enum option written by nothing else in the pipeline (DRAIN-03/D-08). |
| T-44-05 | Denial of Service | Drain silently inert (constant missing or `"false"`) letting the queue re-form | medium | mitigate | Task 3's dedicated `verify_live_write_safety.py` line fails loudly rather than folding into the disarmed verdict. |
| T-44-06 | Spoofing | `SJ-3 Drain Clear Flag` deployed credential-UNBOUND, 401ing only at runtime | medium | mitigate | `NODE_CREDENTIAL_MAP` entry added in the same commit as the node (Task 1), per that map's own stated convention. |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` green (636 baseline + new cases; glob form — dir form fails on node >= 21).
- `.venv/bin/python -m pytest -q` green (2427 baseline + new cases).
- `operator-claude-plugin` pytest suite green.
- `git diff --stat n8n/` shows regenerated JSON only, with no hand edits.
</verification>

<success_criteria>
- A declined SJ-3 row cannot reach `SJ-3 Dispatch To Enrichment` (GATE-01, structural).
- A declined SJ-3 row does reach a write clearing `lv_enrichment_requested` (DRAIN-01).
- That write's built patch is exactly `lv_enrichment_requested="false"` +
  `lv_enrichment_status="skipped"` (DRAIN-02, D-07 as amended).
- A drained record is distinguishable from an enriched one (DRAIN-03, D-08).
- The write-gate coverage guarantee is amended deliberately, with the inversion recorded (D-05/D-06).
- Nothing is deployed. The phase's single build -> deploy -> bounce is Plan 03.
</success_criteria>

<output>
Create `.planning/phases/44-sj-3-dispatch-gate-drain-cap/44-01-SUMMARY.md` when done.
</output>
