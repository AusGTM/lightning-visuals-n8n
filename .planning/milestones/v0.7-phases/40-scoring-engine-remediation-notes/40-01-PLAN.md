---
phase: 40-scoring-engine-remediation-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_hubspot_flow.py
  - scripts/put_hubspot_flow.py
  - config/hubspot_flows/
  - tests/test_flow_rubric_conformance.py
  - .planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md
autonomous: true
requirements: [ENGINE-05, ENGINE-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All four company scoring flows (4626124224, 4626722240, 4626722237, 4625147345) exist as stripped JSON in config/hubspot_flows/{id}-{slug}.before.json and re-parse identically to what GET /automation/v4/flows/{id} returns."
    - "PORTAL-FACTS.md records, from live GETs, whether lv_icp_tier's enum options include Unscored and the verbatim current calculationFormula string of lv_icp_fit_score (Open Questions 1 and 2)."
    - "Org-type enum sweep completeness (ENGINE-06): all nine config/icp_scoring.yaml base_score.org_type values map to org_type_score exactly — governing_body_league 40, content_producer 20, broadcaster 20, individual_club_team 5, regulator 5, gambling_operator 0, hardware_vendor 0, other 0, unknown 0 — and an lv_org_type value not present in that table yields org_type_score 0."
    - "A disposable company with lv_org_type=regulator reaches org_type_score=5 (was 0, F10) and one with lv_org_type=gambling_operator reaches org_type_score=0 (was -20, F9) within 120s of the input write."
    - "The API-driven flow round trip of D-05 is proven or disproven in writing: PUT /automation/v4/flows/{id} either accepts an action-content edit that takes effect live, or PORTAL-FACTS.md records the exact rejection and the portal-UI fallback is invoked for that edit."
  artifacts:
    - scripts/fetch_hubspot_flow.py
    - scripts/put_hubspot_flow.py
    - config/hubspot_flows/4626124224-org-type-score.before.json
    - config/hubspot_flows/4626124224-org-type-score.after.json
    - config/hubspot_flows/4626722240-geography-score.before.json
    - config/hubspot_flows/4626722237-annual-revenue-score.before.json
    - config/hubspot_flows/4625147345-wf1-set-icp-tier.before.json
    - tests/test_flow_rubric_conformance.py
    - .planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md
  key_links:
    - "config/icp_scoring.yaml base_score.org_type -> tests/test_flow_rubric_conformance.py -> config/hubspot_flows/4626124224-org-type-score.after.json (the offline guard every later flow edit reuses)"
    - "GET /automation/v4/flows/{id} -> strip createdAt/updatedAt/dataSources -> PUT same body (Pitfall 1: PUT is replace-not-merge; anything omitted is deleted from the live flow)"
    - "PORTAL-FACTS.md lv_icp_tier enum finding -> 40-06 Task 1 (conditional Unscored option PATCH, Pitfall 5)"
    - "PORTAL-FACTS.md lv_icp_fit_score formula string -> 40-04 Task 3 (extend that exact syntax, never reconstruct it, Pitfall 3)"
  prohibitions:
    - statement: "Never mutate a real company during validation — disposable ZZ-SCORING-TEST-DELETE-ME-* records only, created and deleted in the same run."
      status: flagged
      verification: unverified
    - statement: "Never PUT a flow without its pre-edit snapshot already committed to config/hubspot_flows/."
      status: flagged
      verification: unverified
    - statement: "Never leave a flow disabled at the end of a task — every disable is paired with a re-enable inside the same task, asserted by re-reading isEnabled."
      status: flagged
      verification: unverified
---

<objective>
Prove the Phase 40 remediation architecture end-to-end on one real fix, and leave behind the
tooling, the archived flow definitions, and the offline conformance guard that every remaining
flow edit in this phase reuses.

Task 1 is read-only reconnaissance (D-05's archive requirement plus 40-RESEARCH.md's two Open
Questions). Task 2 is the phase's tracer: a single production-quality fix — the org-type mapper's
point table (ENGINE-06 regulator, ENGINE-05's org-type half) — carried through the complete
path fetch -> archive -> edit -> PUT -> live disposable validation -> re-enable, per D-05/D-07/D-08.
It is the first system-touching task in the phase and it settles 40-RESEARCH.md's assumption A1
(is `PUT /automation/v4/flows/{id}` action-content editable at all?) before four more plans depend
on the answer.

Purpose: an architectural dead-end on the API path costs one commit here instead of ten later.
Output: two reusable scripts, four archived flow definitions, one live-fixed flow, one offline
conformance test, one portal-facts record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-scoring-engine-remediation-notes/40-CONTEXT.md
@.planning/phases/40-scoring-engine-remediation-notes/40-RESEARCH.md
@.planning/phases/40-scoring-engine-remediation-notes/40-PATTERNS.md
@.planning/phases/40-scoring-engine-remediation-notes/COVERAGE.md
@config/icp_scoring.yaml
@src/hubspot_client.py
</context>

## Artifacts this phase produces

Created by this plan:

| Artifact | Kind | Notes |
|---|---|---|
| `scripts/fetch_hubspot_flow.py` | script | GET + strip + archive one or many flows; `--flow-id`, `--label` (before/after) |
| `scripts/put_hubspot_flow.py` | script | PUT a stripped flow body; `--enable` / `--disable`; two-key gate `ALLOW_HUBSPOT_FLOW_WRITE=true DRY_RUN=false` |
| `config/hubspot_flows/{id}-{slug}.before.json` | data | four archived pre-edit flow definitions |
| `config/hubspot_flows/4626124224-org-type-score.after.json` | data | post-edit snapshot of the tracer flow |
| `tests/test_flow_rubric_conformance.py` | test | offline guard: archived `.after.json` branch tables vs `config/icp_scoring.yaml` |
| `.planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md` | doc | live answers to 40-RESEARCH.md Open Questions 1 and 2, plus the D-05 round-trip verdict |

Env vars / flags introduced: `ALLOW_HUBSPOT_FLOW_WRITE`, and the existing `DRY_RUN` /
`HUBSPOT_EXPECTED_PORTAL_ID` (22617666) conventions.

Produced elsewhere in Phase 40 (for orientation): `produces_content_score` and `gambling_score`
company properties + their mapper flows (40-04); `tests/test_scoring_parity.py` and
`scripts/run_scoring_parity.py` (40-02); the `ENRICH_DECIDE_CO_CLOUD` veto-derivation block and
`docs/OPERATOR-VETO-REFRESH.md` (40-03); `batch_update_companies()` and
`scripts/backfill_seed_company_scores.py` (40-07).

<tasks>

<task type="auto">
  <name>Task 1: Archive all four flow definitions and record the two portal facts the phase depends on</name>
  <files>scripts/fetch_hubspot_flow.py, config/hubspot_flows/4626124224-org-type-score.before.json, config/hubspot_flows/4626722240-geography-score.before.json, config/hubspot_flows/4626722237-annual-revenue-score.before.json, config/hubspot_flows/4625147345-wf1-set-icp-tier.before.json, .planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md</files>
  <read_first>
    - `scripts/snapshot_hubspot_schema.py` — the exact analog: argparse + `ROOT`/`sys.path.insert` header, `EXPECTED_PORTAL_ID` + `_has_credentials()` + `_portal_ok()` guards, JSON artifact written to a versioned repo path. Copy this shape; do not invent a new one.
    - `src/hubspot_client.py` — `BASE_URL`, `hs_headers()`. Note that every write helper prints the payload dict only and never `hs_headers()`; keep that discipline here even though this task is read-only.
    - `.planning/phases/40-scoring-engine-remediation-notes/40-RESEARCH.md` — Architecture Patterns Pattern 1 (the fetch/strip contract), Pitfall 1 (PUT is replace-not-merge), Pitfall 3, Pitfall 5, and the Open Questions section (1: `lv_icp_tier` enum options; 2: `lv_icp_fit_score` formula syntax).
    - `.planning/phases/40-scoring-engine-remediation-notes/40-PATTERNS.md` — the `scripts/fetch_hubspot_flow.py` row and the "Portal guard before any live call" shared pattern.
    - `HANDOVER-2026-08-06-icp-scoring.md` §10.1 — the flow ID / trigger / writes table these archives must match.
  </read_first>
  <action>
    Write `scripts/fetch_hubspot_flow.py` following `scripts/snapshot_hubspot_schema.py`'s
    structure exactly: module docstring naming this phase and D-05, `ROOT = Path(__file__).resolve().parent.parent`
    with `sys.path.insert(0, str(ROOT))`, `EXPECTED_PORTAL_ID = "22617666"`, `_has_credentials()`
    and `_portal_ok()` asserted before any network call, argparse with `--flow-id` (repeatable),
    `--label` defaulting to `before`, and `--out-dir` defaulting to `config/hubspot_flows`.

    For each flow id: `GET {BASE_URL}/automation/v4/flows/{flow_id}` with `hs_headers()` and
    `timeout=30`, `raise_for_status()`, then pop `createdAt`, `updatedAt` and `dataSources` from
    the response body (per D-05 and 40-RESEARCH.md Pattern 1 — these are the documented
    round-trip poison fields). Write the stripped body with `json.dump(..., indent=2, sort_keys=True)`
    to `config/hubspot_flows/{flow_id}-{slug}.{label}.json`, where slug is supplied by a module-level
    `FLOW_SLUGS` map: 4626124224 -> `org-type-score`, 4626722240 -> `geography-score`,
    4626722237 -> `annual-revenue-score`, 4625147345 -> `wf1-set-icp-tier`. Sorted keys and a
    stable indent are load-bearing: the `.before.json`/`.after.json` diff is the only evidence
    that a PUT did not silently drop an action (Pitfall 1).

    Run it for all four ids to produce the `.before.json` archives. `.env` is Read/Bash
    permission-blocked — hand the operator a `!`-prefixed invocation that loads dotenv the same
    way `scripts/probe_scoring_recalc_latency.py`'s docstring does, rather than trying to read the
    token.

    Then gather the portal facts into
    `.planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md`, from live reads only:
    (a) `GET /automation/v4/flows` — the full enabled-flow list, to confirm these four are the only
    company scoring flows and no fifth exists; (b) `GET /crm/v3/properties/companies/lv_icp_tier` —
    record every enum option verbatim and state explicitly whether `Unscored` is present
    (40-RESEARCH.md Open Question 1 / Pitfall 5; 40-06 Task 1 is conditional on this answer);
    (c) `GET /crm/v3/properties/companies/lv_icp_fit_score` — record `type`, `fieldType`,
    `calculated`, and the verbatim `calculationFormula` string (Open Question 2; 40-04 Task 3
    extends this exact string rather than reconstructing formula syntax, Pitfall 3); (d) `GET
    /crm/v3/properties/companies` filtered to names ending in `_score` — record which component
    properties already exist and each one's default value, so 40-04 creates only what is missing.
    PORTAL-FACTS.md also carries a "D-05 round-trip verdict" heading left as `pending` for Task 2
    to fill in.

    Finally scaffold `tests/test_flow_rubric_conformance.py`: a pytest module that loads
    `config/icp_scoring.yaml` once and, for each `config/hubspot_flows/*.after.json` present,
    extracts that flow's branch condition table and asserts each mapped point value equals the
    rubric. Structure it so a mapper with no `.after.json` yet is simply not collected (glob-driven
    parametrization), so this file stays green through waves 2 to 4 as each later plan adds its own
    `.after.json`. In this task only the loader, the YAML comparison helper, and the org-type
    extractor are needed; 40-04/40-05/40-06 extend it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_flow_rubric_conformance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ls config/hubspot_flows/*.before.json` lists exactly four files, one per flow id 4626124224, 4626722240, 4626722237, 4625147345.
    - Every archived file parses as JSON and contains none of the keys `createdAt`, `updatedAt`, `dataSources` at top level.
    - Every archived file contains a non-empty `actions` array and an `isEnabled` key.
    - `PORTAL-FACTS.md` contains a heading "lv_icp_tier enum options" listing the options verbatim and an explicit sentence stating whether `Unscored` is present or absent.
    - `PORTAL-FACTS.md` contains the verbatim `calculationFormula` string of `lv_icp_fit_score` inside a fenced block, and names every property token that formula references.
    - `PORTAL-FACTS.md` lists every existing company property whose name ends in `_score`, with its default value.
    - `.venv/bin/python -m pytest tests/test_flow_rubric_conformance.py -q` exits 0.
    - `scripts/fetch_hubspot_flow.py` contains `EXPECTED_PORTAL_ID` and refuses to run when `_portal_ok()` is false.
    - No file written by this task contains the string `Bearer ` or the value of `HUBSPOT_PRIVATE_APP_TOKEN`.
  </acceptance_criteria>
  <done>Four flow definitions archived, both of 40-RESEARCH.md's Open Questions answered from live reads, and the offline conformance guard scaffolded.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end org-type point-table fix — one flow, fetch to re-enable</name>
  <files>scripts/put_hubspot_flow.py, config/hubspot_flows/4626124224-org-type-score.after.json, tests/test_flow_rubric_conformance.py, .planning/phases/40-scoring-engine-remediation-notes/PORTAL-FACTS.md</files>
  <read_first>
    - `config/hubspot_flows/4626124224-org-type-score.before.json` (written by Task 1) — the live branch structure being edited. Read it in full before touching anything; the edit is in-place mutation of this exact body.
    - `config/icp_scoring.yaml` `base_score.org_type` — the nine authoritative point values.
    - `scripts/probe_scoring_recalc_latency.py` — the two-key arming docstring, `EXPECTED_PORTAL_ID`, `COMPANY_NAME_PREFIX = "ZZ-SCORING-TEST-DELETE-ME-"`, and the guaranteed-teardown-on-exception structure. The disposable lifecycle here copies that file.
    - `src/hubspot_client.py` — `create_record`, `patch_record`, `get_record`, `delete_record` signatures and their `dry_run=True` default.
    - `.planning/phases/40-scoring-engine-remediation-notes/40-RESEARCH.md` — Pattern 1 (the disable/edit/PUT sequence), Pitfall 1, Pitfall 2 (why this task exists early), Assumption A1.
    - `.planning/phases/40-scoring-engine-remediation-notes/40-CONTEXT.md` — D-05, D-07, D-08 verbatim.
    - `HANDOVER-2026-08-06-icp-scoring.md` §10.2 rows F9 and F10 — the two live-validated defects this task closes on the org-type side.
  </read_first>
  <action>
    Write `scripts/put_hubspot_flow.py`: same header/guard shape as `fetch_hubspot_flow.py`, plus a
    two-key arm (`DRY_RUN=false` and `ALLOW_HUBSPOT_FLOW_WRITE=true`) mirroring
    `scripts/probe_scoring_recalc_latency.py`'s convention. It takes a path to a flow JSON file and
    a flow id, re-asserts the body carries no `createdAt`/`updatedAt`/`dataSources`, and issues
    `PUT {BASE_URL}/automation/v4/flows/{flow_id}` with the full body. Flags `--disable` and
    `--enable` set `isEnabled` false/true on the body before the PUT. D-08 permits executing these
    PUTs directly in-session, so this script is the reproducible record of what was sent, not an
    arming ceremony — but it still prints only the payload, never `hs_headers()`.

    Then run the tracer slice against flow 4626124224 (Update Score Based on Org Type), in this
    order, per D-07:

    1. Disable: PUT the archived `.before.json` body with `isEnabled` false. Confirm by re-GETting
       that `isEnabled` is false.
    2. Edit in the repo: mutate the archived body in place so the org-type branch table matches
       `config/icp_scoring.yaml` exactly — `regulator` moves from 0 to 5 (F10, ENGINE-06) and
       `gambling_operator` moves from -20 to 0 (F9's org-type half, ENGINE-05: the -20 becomes a
       separate `lv_is_gambling_operator`-driven component in 40-04, so leaving it here would
       double-count). Leave `governing_body_league` 40, `content_producer` 20, `broadcaster` 20,
       `individual_club_team` 5, `hardware_vendor` 0, `other` 0, `unknown` 0 as they are if already
       correct, and correct any that are not. Any `lv_org_type` value with no branch must fall
       through to 0. Mutate only the branch values — never rebuild the body from scratch or from a
       partial diff (Pitfall 1: PUT is replace-not-merge, anything you omit is deleted live).
    3. PUT the edited body while still disabled. If the PUT returns a non-2xx, or returns 2xx but a
       re-GET shows the branch values unchanged, stop and invoke D-05's pre-committed portal-UI
       fallback for this edit: make the same change by hand in the portal, then re-fetch with
       `scripts/fetch_hubspot_flow.py --label after`. Either way, record the outcome under the
       "D-05 round-trip verdict" heading in PORTAL-FACTS.md — this verdict is what 40-04, 40-05 and
       40-06 plan their edits against.
    4. Validate live on disposables: create a `ZZ-SCORING-TEST-DELETE-ME-{uuid4-hex}` company, set
       `lv_org_type=regulator`, poll `org_type_score` until it settles or 120s elapses, assert 5.
       Repeat on a second disposable with `lv_org_type=gambling_operator`, assert 0. Delete both,
       teardown guaranteed in a `finally` block even on exception or interrupt. HANDOVER §10.1
       observed mapper latency is 4 to 25s; poll every 5s.
    5. Re-enable: PUT with `isEnabled` true and confirm by re-GET. Never end this task with the
       flow disabled.
    6. Archive `config/hubspot_flows/4626124224-org-type-score.after.json` via
       `scripts/fetch_hubspot_flow.py --label after`, and diff it against `.before.json` to confirm
       the only changes are the two branch point values and `isEnabled` — no action, branch or
       enrollment-criteria block silently vanished.

    Extend `tests/test_flow_rubric_conformance.py` so the org-type case now asserts against the
    real `.after.json`: every branch value in that file must equal
    `config/icp_scoring.yaml`'s `base_score.org_type` entry of the same name, and the set of
    branch keys must be a subset of the rubric's nine keys.

    Record the reversibility rating for this task in the summary: recoverable by re-PUTting
    `.before.json`, at the cost of a live-behaviour window between the bad PUT and the restore.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_flow_rubric_conformance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `config/hubspot_flows/4626124224-org-type-score.after.json` exists and parses.
    - In that file, the org-type branch mapping to `org_type_score` reads: governing_body_league 40, content_producer 20, broadcaster 20, individual_club_team 5, regulator 5, gambling_operator 0, hardware_vendor 0, other 0, unknown 0.
    - A structural diff of `.before.json` against `.after.json` shows changes confined to org-type branch point values and `isEnabled`; the count of entries in `actions` is unchanged.
    - A live `GET /automation/v4/flows/4626124224` returns `isEnabled` true at task end.
    - A disposable company set to `lv_org_type=regulator` reaches `org_type_score=5` within 120s; one set to `lv_org_type=gambling_operator` reaches `org_type_score=0` within 120s.
    - Both disposable companies are deleted (HubSpot answers 204) before the task reports done; a search for company name prefix `ZZ-SCORING-TEST-DELETE-ME-` returns zero results.
    - `PORTAL-FACTS.md`'s "D-05 round-trip verdict" heading states either that PUT accepted the action-content edit and it took effect live, or the exact failure and that the portal-UI fallback was used.
    - `.venv/bin/python -m pytest tests/test_flow_rubric_conformance.py -q` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">Restoring the pre-edit flow means re-PUTting the archived `.before.json`; recoverable, but a bad PUT is live on real records until it is noticed and restored, and PUT is replace-not-merge so a malformed body can drop actions the snapshot must restore.</reversibility>
  <done>The org-type mapper scores the rubric's nine values correctly on live records, and the API-driven flow-edit path of D-05 is settled in writing for the four plans that depend on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> HubSpot portal 22617666 | Every call in this plan crosses into a live production CRM holding 712 real companies |
| environment -> process | `HUBSPOT_PRIVATE_APP_TOKEN` enters via `hs_headers()`; `.env` is Read/Bash permission-blocked |
| process -> git | Archived flow JSON and PORTAL-FACTS.md are committed; anything sensitive in them is permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | live company records during validation | high | mitigate | Disposable `ZZ-SCORING-TEST-DELETE-ME-*` records only, created and deleted in the same run with teardown in a `finally`; `EXPECTED_PORTAL_ID = "22617666"` asserted before any call |
| T-40-02 | Information disclosure | `hs_headers()` / token in console output or archived JSON | high | mitigate | Write helpers print the payload dict only, never headers (existing `src/hubspot_client.py` discipline); acceptance criterion greps the archives for `Bearer `; `.env` is never read |
| T-40-03 | Tampering | `PUT /automation/v4/flows/{id}` silently deleting omitted actions | high | mitigate | Always PUT the full stripped GET body with in-place mutation; `.before.json`/`.after.json` committed and structurally diffed, with `actions` length asserted unchanged |
| T-40-04 | Denial of service | flow left disabled -> company scoring silently dead | medium | mitigate | Disable and re-enable are paired inside one task; `isEnabled` true asserted by re-GET before the task reports done |
| T-40-05 | Elevation of privilege | `automation` scope reused for flows outside this phase's four | low | accept | Scope is portal-wide and already granted; the four flow ids are hardcoded in `FLOW_SLUGS` and the scripts take explicit ids, so an unintended flow requires a deliberate argument |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — 40-RESEARCH.md's Package Legitimacy Audit records that all dependencies (`requests`, `PyYAML`, `pytest`, `python-dotenv`) are already in `requirements.txt` and already imported by the analog files |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_flow_rubric_conformance.py tests/test_icp_scoring.py -q` exits 0.
- `.venv/bin/python -m pytest -q` exits 0 (no regression in the existing suite).
- Four `.before.json` archives plus one `.after.json` are committed under `config/hubspot_flows/`.
- `PORTAL-FACTS.md` answers Open Questions 1 and 2 and carries a non-`pending` D-05 round-trip verdict.
- A live `GET /automation/v4/flows/4626124224` shows `isEnabled` true.
</verification>

<success_criteria>
- ENGINE-06 is satisfied on the org-type component: every org-type point value in the live flow matches `config/icp_scoring.yaml`, including regulator = 5.
- ENGINE-05's org-type half is satisfied: `lv_org_type=gambling_operator` no longer carries the -20; the deduction moves to its own `lv_is_gambling_operator`-driven component in 40-04.
- The D-05 API path is proven or the portal-UI fallback is proven, in writing, before 40-04/40-05/40-06 commit to either.
- Zero real companies are modified; zero disposable companies survive the run.
</success_criteria>

<output>
Create `.planning/phases/40-scoring-engine-remediation-notes/40-01-SUMMARY.md` when done.
Include: the D-05 round-trip verdict verbatim, the `lv_icp_tier` enum finding, the
`lv_icp_fit_score` formula string, and the list of existing `*_score` properties — 40-04 and
40-06 read these from the summary.
</output>
