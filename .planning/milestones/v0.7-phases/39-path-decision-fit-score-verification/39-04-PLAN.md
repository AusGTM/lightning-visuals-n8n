---
phase: 39-path-decision-fit-score-verification
plan: 04
type: execute
wave: 3
depends_on: ["39-02", "39-03"]
files_modified:
  - .planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json
  - .planning/phases/39-path-decision-fit-score-verification/39-DECISION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [DECIDE-01]
user_setup:
  - service: hubspot
    why: "A score-typed company property only exists after a human builds one criterion in the lead-scoring tool UI; no API can create it on any tier."
    dashboard_config:
      - task: "Build one trivial fit-score criterion on companies, let it settle, then run the armed latency probe and tear the criterion down afterwards"
        location: "app-ap1.hubspot.com (portal 22617666) — lead scoring tool"

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The path decision exists as one standalone file a Phase 40 planner can read start-to-finish without opening anything else first."
    - "The decision cites HANDOVER §5's mechanism comparison rather than re-arguing it, and adds only the new verification and latency evidence."
    - "The recorded verdict follows the pre-committed rules — D-06 on the availability gate, D-04 bands on the recalc gate — and any deviation is an explicit, operator-approved override."
    - "ROADMAP.md and STATE.md each point at the decision file in one line rather than restating it."
  artifacts:
    - .planning/phases/39-path-decision-fit-score-verification/39-DECISION.md
    - .planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json
  key_links:
    - "39-DECISION.md is the single input Phase 40 planning reads for path shape; every downstream phase is shaped by it."
    - "The latency band letter in the decision record traces back to classify_latency_band's named constants, not to a judgement call."
  prohibitions:
    - "The availability gate is never resolved from API evidence alone."
    - "Custom equation properties are never recorded as the fallback — D-06 pre-commits fix-in-place and supersedes ROADMAP criterion 3."
    - "A band-c measurement never auto-advances to a path; it blocks on the operator (D-04)."
    - "HANDOVER §5's mechanism comparison is cited, not re-derived or re-argued (D-07)."
    - "No real HubSpot record is created, patched, or deleted during the live probe run."
    - "The scoring criterion built for the probe is torn down after the run; nothing scoring-related is left live in the portal."
---

<objective>
Take the measurement that resolves the D-04 gate, then write the decision this whole phase
exists to produce: a standalone `39-DECISION.md` carrying the verdict, the evidence links, the
latency figure with its band, the rationale by citation, and the rejected alternatives.

Purpose: Phase 40, 41 and 42 are all path-shaped. This file is the one input their planners
read. Everything before it in Phase 39 was evidence-gathering for this page.
Output: `evidence/recalc_latency_probe.json`, `39-DECISION.md`, and one-line pointers in
ROADMAP.md and STATE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md
@.planning/phases/39-path-decision-fit-score-verification/39-02-SUMMARY.md
@.planning/phases/39-path-decision-fit-score-verification/39-03-SUMMARY.md
@.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md
@scripts/probe_scoring_recalc_latency.py
</context>

## Artifacts this plan produces

- `.planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json` (operator-invoked probe run)
- `.planning/phases/39-path-decision-fit-score-verification/39-DECISION.md`
- one-line pointers appended to `.planning/ROADMAP.md` and `.planning/STATE.md`

<assumption_delta_decision>
**Noun:** scoring engine mechanism.
**Signal:** this phase holds two live candidates for the same responsibility — HubSpot's
native lead-scoring tool, and the four-workflow chain created 2026-08-04 (HANDOVER §10.1).
**Decision:** `promote` — pending the probe outcome, exactly one mechanism is promoted in
`39-DECISION.md` and the other is scheduled for retirement in Phase 42.
**Rationale:** choosing between them is the phase's entire purpose; carrying both past Phase 40
would mean maintaining two rubrics and two parity harnesses for one rubric of record.
**Status:** advisory, non-blocking.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Build one trivial criterion, run the armed latency probe, tear it down</name>
  <what-built>
`scripts/probe_scoring_recalc_latency.py` (plan 39-03) — two-key gated, portal-guarded,
refuses to start unless a score-typed company property already exists, creates exactly one
`ZZ-SCORING-TEST-DELETE-ME-*` company, times three property flips against a monotonic clock,
bands the median against fixed constants, writes the evidence JSON, and deletes the company
with a 204 assertion inside a guaranteed-teardown construct.

The agent cannot run it: `.env` is permission-blocked to Read and Bash in this session, and
the score property this probe polls can only be brought into existence by a human building a
criterion in the portal UI — no API creates that property type on any tier.
  </what-built>
  <how-to-verify>
**Skip this task entirely if plan 39-02 recorded the availability verdict as "not available".**
In that case D-06 pre-commits the path with no recalc measurement needed; record
`skipped — availability gate failed` and move to Task 3.

Otherwise, in portal **22617666** on **app-ap1.hubspot.com**:

1. In the lead scoring tool, create one **company fit score** with a single trivial criterion —
   any one rubric line works, e.g. `lv_org_type is known` awarding a fixed number of points.
   Note the internal name of the score property HubSpot generates; the probe discovers it
   itself, but you will want it for step 5.
2. **Wait until the criteria edit has settled before timing anything.** Editing criteria
   triggers a separate, full-portal bulk recalculation that can take far longer than a
   per-record rescore, and a measurement taken inside that window is contaminated. Give it at
   least 30 minutes, and do not touch the criteria definition again until the probe finishes.
3. Run the probe (the leading command is the `.env`-in-process convention for this repo):

       ALLOW_HUBSPOT_SCORING_PROBE=true DRY_RUN=false .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/probe_scoring_recalc_latency.py', run_name='__main__')"

   Expected: a created-company line, three timed samples, a PASS line naming a band letter and
   a median, and a delete returning 204. Worst case it runs for a while — a single sample can
   poll for up to 65 minutes before the script declares no-fire and stops early.
4. Report back: the full printed output, the band letter, and the median. If the run printed
   exit code 2, it means no score property was found — go back to step 1, the criterion did
   not save.
5. **Tear down:** delete the trivial criterion / score you created in step 1, so nothing
   scoring-related is left live in the portal. Confirm the disposable company is gone by
   searching companies for `ZZ-SCORING-TEST-DELETE-ME` and finding nothing.
  </how-to-verify>
  <read_first>
    - `scripts/probe_scoring_recalc_latency.py` module docstring — it carries the exact command and the measurement contract
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` D-03, D-04, and the "Specific Ideas" note distinguishing the two HubSpot latencies
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md` — the availability verdict that decides whether this task runs at all
  </read_first>
  <acceptance_criteria>
    - Either: `.planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json` exists and contains keys `band`, `median_seconds`, `samples`, `measurement_contract`, and `score_property_name`; or the availability verdict was "not available" and this task is recorded as skipped with that reason.
    - If the file exists, `band` is exactly one of `a`, `b`, `c`.
    - If the file exists, `samples` has 3 entries, or fewer with the last entry `null` (early abort on no-fire).
    - The operator has confirmed a company search for `ZZ-SCORING-TEST-DELETE-ME` returns nothing.
    - The operator has confirmed the trivial criterion was deleted from the portal.
  </acceptance_criteria>
  <resume-signal>Paste the probe output and the band letter, or type "skipped — availability gate failed"</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Band-c review gate — only fires on a recalc pathology</name>
  <decision>Given a band-c recalculation measurement, does the path still go to the lead-scoring tool, or to fix-the-four-workflow-chain-in-place?</decision>
  <context>
**This checkpoint is conditional.** Evaluate the band recorded in Task 1 first:

- Band `a` (median at or below 600 s) — D-04 outcome (a). Do not prompt. Record in the SUMMARY:
  `band a — auto-proceed per D-04, no operator review required`, and continue to Task 3.
- Band `b` (median above 600 s, at or below 3600 s) — D-04 outcome (b). Do not prompt. Record
  `band b — proceed with latency noted per D-04`, and continue to Task 3.
- Availability verdict was "not available" — D-06 pre-commits fix-in-place with no second
  decision round. Do not prompt. Record `availability gate failed — D-06 fallback, no review`,
  and continue to Task 3.
- Band `c` — prompt. This is the one case D-04 reserves for the operator.

When it does fire, present: the raw per-sample latencies, the median, which of the three
band-c conditions was met (over an hour, never fired within the 65-minute poll, or only
changed after a manual in-portal recalculation), and this recommendation — the fix-in-place
chain is not automatically the better answer, because it carries its own tier-lag defect F7
(tier recomputes only when the score changes, so a veto that fires without moving the score
leaves the wrong tier standing). Band c also puts Phase 41's DATA-02 at risk, since imported
companies scoring automatically on landing depends on API-write-triggered recalculation.
  </context>
  <options>
    <option id="proceed-lead-scoring">
      <name>Proceed with the lead-scoring-tool rebuild despite band c</name>
      <pros>Keeps the rubric RevOps-editable and native; the engagement-score half remains available later; the measured latency is recorded honestly as a known constraint.</pros>
      <cons>Phase 41's DATA-02 (imported companies score automatically on landing) may not hold; a manual-only recalculation would need a scheduled trigger this milestone has not scoped.</cons>
    </option>
    <option id="fall-back-fix-in-place">
      <name>Fall back to fixing the four-workflow chain in place</name>
      <pros>The chain demonstrably fires on property-change events within ~30 s end-to-end (HANDOVER §10.1), so the recalc concern does not apply; F1–F10 are already enumerated and validated.</pros>
      <cons>Rubric leaves version control and lives in workflow JSON; F7's tier lag is a live defect that Phase 40 must close; ten defects to fix rather than one mechanism to configure.</cons>
    </option>
    <option id="re-measure">
      <name>Re-measure before deciding</name>
      <pros>Guards against a contaminated window — e.g. the criteria edit had not settled, or the portal was mid-bulk-recalculation.</pros>
      <cons>Costs another probe cycle and up to several hours of polling; only worth it if the operator has specific reason to suspect contamination.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json` — the raw samples, not just the band
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` D-04 (the three outcomes and what each does) and D-05
    - `HANDOVER-2026-08-06-icp-scoring.md` §10.1 (the chain's observed ~30 s end-to-end latency) and §10.2 defect F7
  </read_first>
  <acceptance_criteria>
    - The SUMMARY records exactly one of: `band a — auto-proceed per D-04, no operator review required`; `band b — proceed with latency noted per D-04`; `availability gate failed — D-06 fallback, no review`; or a verbatim operator selection from `proceed-lead-scoring`, `fall-back-fix-in-place`, `re-measure`.
    - No operator prompt was raised for band a, band b, or the availability-failure case.
    - If `re-measure` was selected, Task 3 does not run until a fresh probe has landed a new band.
  </acceptance_criteria>
  <resume-signal>Select: proceed-lead-scoring, fall-back-fix-in-place, or re-measure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 39-DECISION.md and point ROADMAP.md and STATE.md at it</name>
  <files>.planning/phases/39-path-decision-fit-score-verification/39-DECISION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/VERIFICATION-NOTE.md` — the availability verdict and its sources
    - `.planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json` — read the actual numbers; do not restate them from the checkpoint transcript
    - `HANDOVER-2026-08-06-icp-scoring.md` §5 in full — the mechanism comparison this record cites rather than reproduces, and both operator decisions taken that session
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` D-05, D-06, D-07, D-08
    - `.planning/ROADMAP.md` Phase 39 block (lines 30–46) and Phase 40 block, to place the pointer without disturbing the surrounding phases
  </read_first>
  <precondition>Tasks 1 and 2 of this plan are resolved, and the band letter (or the availability-failure reason) is recorded in the execution log.</precondition>
  <reversibility rating="costly">Phase 40's plans, the parity harness shape, and Phase 42's cleanup scope are all shaped by this record; reversing it after Phase 40 is planned means replanning that phase (D-06).</reversibility>
  <action>
Write `.planning/phases/39-path-decision-fit-score-verification/39-DECISION.md` — the D-08
standalone decision record. A Phase 40 planner must be able to read this one file end to end
and know the path shape without opening anything else first. Sections, in this order:

- **Verdict** — one sentence naming the chosen path: the lead-scoring-tool rebuild, or fixing
  the four-workflow chain in place. Then the decision date and the portal it applies to.
- **How the verdict was reached** — the two gates from D-05, each with its status and its
  evidence file. Availability: the in-portal observation, citing
  `evidence/VERIFICATION-NOTE.md` and the screenshot filenames, and stating that the API probe
  contributed supporting evidence only. Recalculation: the band letter, the median, the raw
  samples, and the file they came from — or, if the availability gate failed, a line stating
  the recalc gate was not measured because D-06 pre-commits the fallback.
- **Latency measurement** — the number with its contract attached, not bare: which of the two
  HubSpot latencies was measured (the per-record event-driven rescore, not the criteria-edit
  bulk recalculation), that it is an upper bound quantized to a 5-second poll, and the band
  boundaries in force. Cross-reference `classify_latency_band` in
  `scripts/probe_scoring_recalc_latency.py` as the authority for the bands.
- **Rationale** — cite `HANDOVER-2026-08-06-icp-scoring.md` §5 for the mechanism comparison by
  section reference. Per D-07, do not re-derive or re-argue it; add only what is new, which is
  the verification result and the latency evidence. Also carry forward §5 decision 2 unchanged:
  hard vetoes stay pipeline-owned, n8n keeps writing `lv_anti_icp_flag` and
  `lv_anti_icp_reason`, and Tier D is a view filter on the flag. That constraint applies on
  either path and Phase 40 must not rediscover it.
- **Rejected alternatives** — custom equation properties, rejected twice: HANDOVER §5 on the
  merits (not RevOps-editable, formula-fragile) and D-06 on the fallback question. State
  plainly that D-06 supersedes ROADMAP Phase 39 success criterion 3, which still names
  equation properties as the fallback; the pre-committed fallback is fix-in-place. Also record
  the legacy `calculation_score` HubSpot Score mechanism as unavailable, not rejected — it was
  removed 2026-01-10.
- **What this shapes downstream** — one line each for Phase 40 (which defect set or which
  configuration work), Phase 41 (whether DATA-02's automatic-scoring-on-landing assumption
  holds under the measured band), and Phase 42 (which artifacts become the retirement set).
- **Assumptions carried into the verdict** — the A-BOUNDARY band edges and the A-PRECISION
  measurement contract from `39-01-PLAN.md`, plus RESEARCH.md A1 and A2, each in one line with
  what would change if it turned out wrong.
- **Re-check procedure** — how to re-run both gates if HubSpot changes packaging, pointing at
  the two probe scripts and the portal click-path.

Then add the pointers. In `.planning/ROADMAP.md`, the Phase 39 block already carries a
`**Path decision:** pending` line placed at plan time — replace `pending` with the verdict in
one line, keeping the link to the decision file. Use scoped edits only; ROADMAP.md carries
three concatenated milestones and a whole-file rewrite would drop sibling phases. In
`.planning/STATE.md`, add one line under Current Position naming the verdict and linking the
decision file. Neither pointer restates the rationale — one line each, the decision file
carries the content.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-path-decision-fit-score-verification/39-DECISION.md &amp;&amp; grep -q 'HANDOVER-2026-08-06-icp-scoring.md' .planning/phases/39-path-decision-fit-score-verification/39-DECISION.md &amp;&amp; grep -q '39-DECISION.md' .planning/ROADMAP.md &amp;&amp; grep -q '39-DECISION.md' .planning/STATE.md &amp;&amp; grep -c 'Phase 4[012]' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `39-DECISION.md` exists and its first heading section is a verdict naming exactly one path.
    - `grep -c 'evidence/VERIFICATION-NOTE.md\|evidence/recalc_latency_probe.json\|portal_walkthrough_' 39-DECISION.md` outputs 2 or more.
    - `grep -q '§5' 39-DECISION.md` exits 0 — the rationale cites the handover section rather than reproducing it.
    - `grep -q 'lv_anti_icp_flag' 39-DECISION.md` exits 0 — the pipeline-owned-veto constraint is carried forward.
    - `39-DECISION.md` contains the band letter and, unless the availability gate failed, a numeric median.
    - The `**Path decision:**` line in ROADMAP.md's Phase 39 block names exactly one of `lead-scoring-tool rebuild` or `fix-the-four-workflow-chain-in-place`.
    - `.planning/ROADMAP.md` still contains its Phase 40, Phase 41 and Phase 42 blocks — `grep -c 'Phase 4[012]'` output is unchanged from before the edit.
    - `.planning/STATE.md` contains exactly one new line referencing `39-DECISION.md`.
  </acceptance_criteria>
  <done>Phase 40's planner can read one file and know the path, the constraints that survive on either path, and what evidence backs the choice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator portal session → live scoring configuration | The probe requires a real scoring criterion to exist temporarily in the production portal. |
| probe measurement → downstream phase planning | A single number shapes three subsequent phases; a wrong band silently mis-shapes all of them. |
| agent edits → .planning/ROADMAP.md | ROADMAP carries three concatenated milestones; a whole-file rewrite would destroy phases outside the edit window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-16 | Tampering | trivial scoring criterion left live in production portal | high | mitigate | Task 1's teardown step requires deleting the criterion and confirming no `ZZ-SCORING-TEST-DELETE-ME` company remains, both as acceptance criteria. |
| T-39-17 | Spoofing | contaminated latency reading from the criteria-edit bulk-recalculation window | high | mitigate | Task 1 mandates a 30-minute settle after the criteria edit and forbids touching the criteria definition during the run; the probe's timed loop only ever writes the disposable company's own property. |
| T-39-18 | Elevation of Privilege | a band-c pathology auto-advancing the path without review | high | mitigate | Task 2 is a blocking `checkpoint:decision` for band c specifically, with the auto-advance cases enumerated so the executor cannot broaden them. |
| T-39-19 | Tampering | whole-file ROADMAP.md rewrite dropping sibling phases | high | mitigate | Task 3 mandates scoped edits and asserts the Phase 40/41/42 block count is unchanged after the edit. |
| T-39-20 | Repudiation | decision recorded without traceable evidence | high | mitigate | Acceptance criteria require named evidence filenames and a `§5` citation inside `39-DECISION.md`. |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `39-DECISION.md` present, verdict-first, evidence-linked, §5-citing.
- ROADMAP.md Phase 40/41/42 blocks intact; Phase 39 placeholders filled.
- STATE.md carries exactly one new pointer line.
- No `ZZ-SCORING-TEST-DELETE-ME` company and no probe criterion survive in the portal.
</verification>

<success_criteria>
- The path decision is recorded in one standalone file with rationale, evidence and rejected alternatives (DECIDE-01, D-08).
- The verdict follows D-06 on the availability gate and the D-04 bands on the recalc gate, or carries an explicit operator override.
- Phase 40 planning has exactly one file to read.
</success_criteria>

<output>
Create `.planning/phases/39-path-decision-fit-score-verification/39-04-SUMMARY.md` when done.
</output>
