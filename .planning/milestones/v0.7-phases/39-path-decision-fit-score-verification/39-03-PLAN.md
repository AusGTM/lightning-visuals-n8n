---
phase: 39-path-decision-fit-score-verification
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/hubspot_client.py
  - scripts/probe_scoring_recalc_latency.py
  - tests/test_scoring_probe_helpers.py
autonomous: true
requirements: [DECIDE-01]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The repo can delete a HubSpot record, so a disposable probe artifact can always be torn down instead of left live."
    - "The latency probe refuses to start unless a score-typed company property already exists in the portal — it can never report a false no-fire because nothing was there to fire."
    - "The D-04 outcome bands have exact numeric boundaries in code, asserted one step either side, so a measurement cannot be banded by judgement."
    - "Reported latency is an explicit upper bound at ±5 s resolution, stated as such rather than implied as exact."
  artifacts:
    - src/hubspot_client.py
    - scripts/probe_scoring_recalc_latency.py
    - tests/test_scoring_probe_helpers.py
  key_links:
    - "probe teardown calls delete_record and asserts HTTP 204, so no ZZ-SCORING-TEST-DELETE-ME-* company survives a run."
    - "the two-key gate uses ALLOW_HUBSPOT_SCORING_PROBE, a phase-scoped flag that cannot be armed by a leftover ALLOW_HUBSPOT_PROPERTY_WRITES from a migration script."
  prohibitions:
    - "No real HubSpot record is ever created, patched, or deleted — only a company whose name carries the ZZ-SCORING-TEST-DELETE-ME- prefix."
    - "The disposable-company name prefix is a module constant with no CLI or environment override."
    - "The timed loop never edits a scoring criteria definition — only the disposable company's own property."
    - "The private app token value is never printed, logged, or written into an evidence file."
    - "No probe runs against any portal other than 22617666."
    - "A single armed flag is never sufficient to write — both keys are required."
---

<objective>
Build the write-capable half of Phase 39: the one missing client primitive, and the
two-key-gated disposable-company probe that measures whether HubSpot's lead-scoring
recalculation fires automatically on an API-written property change (the D-04 gate).

Purpose: D-04's gate is behavioural, not a latency number, but the measurement is the
evidence. This plan makes that measurement reproducible, bounded, and impossible to
misreport — the band boundaries and the measurement contract are code with tests, not prose.
Output: `delete_record()`, `scripts/probe_scoring_recalc_latency.py`, and its unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md
@.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md
@.planning/phases/39-path-decision-fit-score-verification/39-PATTERNS.md
@.planning/phases/39-path-decision-fit-score-verification/39-01-SUMMARY.md
@src/hubspot_client.py
@scripts/rollback_canary_proof.py
@scripts/probe_org_type_migration.py
</context>

## Artifacts this plan produces

- `src/hubspot_client.py::delete_record(object_type, record_id, dry_run=True)`
- `scripts/probe_scoring_recalc_latency.py` — `median_latency`, `classify_latency_band`, `find_score_property_name`, `main`
- new test cases appended to `tests/test_scoring_probe_helpers.py`

## Planner assumptions implemented here

The two unresolved edge-coverage probes from planning (A-BOUNDARY and A-PRECISION, stated in
full in `39-01-PLAN.md`) are turned into executable contracts by Tasks 2 and 3. They are
assumptions, not findings: CONTEXT.md D-04 names three outcome bands without numeric edges,
and names no measurement method. If the operator disagrees with either contract, the raw
per-sample latencies in the evidence JSON allow re-banding without re-running the probe.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delete_record() — the one missing client primitive</name>
  <files>src/hubspot_client.py, tests/test_scoring_probe_helpers.py</files>
  <read_first>
    - `src/hubspot_client.py` in full (77 lines) — specifically `patch_record` lines 24–42 and `create_record` lines 45–64: the dry-run short-circuit, what the printed dict contains, and what the sentinel return value looks like
    - `.planning/phases/39-path-decision-fit-score-verification/39-PATTERNS.md` section "`src/hubspot_client.py::delete_record()`" — the exact shape to copy
  </read_first>
  <behavior>
    - `delete_record("companies", "789")` with the default `dry_run=True` makes no network call, prints a JSON dict whose `method` is `DELETE` and whose `url` is `https://api.hubapi.com/crm/v3/objects/companies/789`, and returns `{"dry_run": True}`.
    - The dry-run printed output contains no `Authorization` key and no token substring.
    - `delete_record` accepts `dry_run` as a keyword and defaults it to `True`, matching `patch_record` and `create_record` — a caller that forgets the flag cannot delete anything.
  </behavior>
  <action>
Append `delete_record(object_type: str, record_id: str, dry_run=True)` to
`src/hubspot_client.py`, below `create_record` and above `search_records` or at end of file —
placement is free, the signature convention is not.

Mirror `patch_record`/`create_record` exactly: the `dry_run` branch short-circuits BEFORE any
`requests.delete` call, prints `json.dumps({...}, indent=2, default=str)` carrying only
`dry_run`, `method`, and `url` (never `hs_headers()`, never the token, and there is no payload
on a DELETE), and returns the sentinel `{"dry_run": True}`.

The live branch builds the same URL, calls `requests.delete(url, headers=hs_headers(), timeout=30)`,
calls `raise_for_status()`, and returns the response object itself rather than `r.json()` —
HubSpot answers a successful company delete with 204 No Content, which has no JSON body, so
the caller asserts `r.status_code == 204`. Carry a short comment on that return type so the
next reader does not "fix" it into `r.json()`.

Add the corresponding test cases to `tests/test_scoring_probe_helpers.py`, using pytest's
`capsys` to assert on the printed dry-run output. Write the tests first.

Do not add a caller-side arming check inside the client — the two-key gate is the caller's
job, matching the note already carried on `create_record`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_scoring_probe_helpers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def delete_record' src/hubspot_client.py` outputs 1.
    - `grep -c 'requests.delete' src/hubspot_client.py` outputs 1.
    - `.venv/bin/python -c "import src.hubspot_client as c, inspect; print(inspect.signature(c.delete_record))"` prints a signature whose `dry_run` default is `True`.
    - `.venv/bin/python -m pytest tests/test_scoring_probe_helpers.py -x -q` exits 0.
    - `.venv/bin/python -m pytest` (full suite) exits 0 — no existing test regressed.
  </acceptance_criteria>
  <done>The repo has a fifth thin CRUD wrapper matching the existing four, dry-run-safe by default, so a disposable probe artifact can always be torn down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure latency helpers — median, band classification, score-property discovery</name>
  <files>scripts/probe_scoring_recalc_latency.py, tests/test_scoring_probe_helpers.py</files>
  <read_first>
    - `.planning/phases/39-path-decision-fit-score-verification/39-CONTEXT.md` D-03 and D-04 — the median-of-3 requirement and the three outcome bands in the operator's own words
    - `.planning/phases/39-path-decision-fit-score-verification/39-01-PLAN.md` section "Planner assumptions" — A-BOUNDARY and A-PRECISION, the contracts these functions implement
    - `.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md` "Pattern 2" and "Anti-Patterns to Avoid" (the explicit prohibition on inlining the median into the polling loop)
  </read_first>
  <behavior>
    - `median_latency([10.0, 12.0, 14.0])` returns `12.0`.
    - `median_latency([5.0, 100.0, 6.0])` returns `6.0` — the median resists a single noisy sample, which is D-03's entire reason for taking three.
    - `median_latency([])` raises `ValueError`.
    - `classify_latency_band(1.0)` returns `"a"`; `classify_latency_band(600.0)` returns `"a"` (inclusive upper edge); `classify_latency_band(600.1)` returns `"b"`.
    - `classify_latency_band(3600.0)` returns `"b"` (inclusive upper edge); `classify_latency_band(3600.1)` returns `"c"`.
    - `classify_latency_band(None)` returns `"c"` — no observed change is the no-fire case, not an error.
    - `classify_latency_band(-1.0)` raises `ValueError` — a negative elapsed time is a clock bug, not a fast recalc.
    - `find_score_property_name([{"name": "x", "fieldType": "text"}])` returns `None`.
    - `find_score_property_name([{"name": "x", "fieldType": "text"}, {"name": "hs_lead_score", "fieldType": "calculation_score"}])` returns `"hs_lead_score"`.
  </behavior>
  <action>
Write the tests first, from the `<behavior>` list, then create
`scripts/probe_scoring_recalc_latency.py` containing only the module preamble, constants, and
these three pure functions. Task 3 adds the network orchestration on top.

Preamble copies `scripts/rollback_canary_proof.py` lines 28–39: `ROOT`,
`sys.path.insert(0, str(ROOT))`, `EXPECTED_PORTAL_ID = os.getenv("HUBSPOT_EXPECTED_PORTAL_ID", "22617666")`.

Module constants, each with a one-line comment stating why the value is what it is:
- `COMPANY_NAME_PREFIX = "ZZ-SCORING-TEST-DELETE-ME-"` — module constant only, no CLI or
  environment override, matching the `PROBE_PROPERTY_NAME` discipline in
  `scripts/probe_org_type_migration.py`: there is no legitimate reason for this script to be
  pointable at any other record.
- `POLL_INTERVAL_SECONDS = 5.0` — measurement resolution; every reported latency is an upper
  bound quantized up to the nearest poll.
- `POLL_TIMEOUT_SECONDS = 3900.0` — 65 minutes, deliberately one poll-scale beyond band b's
  3600 s upper edge, so exhausting the timeout is unambiguously band c rather than an
  inconclusive cutoff.
- `SAMPLE_COUNT = 3` — fixed by D-03; also why `median_latency` never hits the even-length
  averaging path.
- `BAND_A_MAX_SECONDS = 600.0` and `BAND_B_MAX_SECONDS = 3600.0` — the A-BOUNDARY contract,
  named rather than inline so the band edges are greppable.

`median_latency(samples: list[float]) -> float`: raise `ValueError` on empty input, otherwise
return `statistics.median(samples)`. Keep it a pure function with a docstring naming what the
samples are (elapsed seconds from property write to first observed score change) and stating
that with `SAMPLE_COUNT` fixed at 3 the result is always one of the input samples verbatim —
no averaging, no interpolation, no floating-point tie-break.

`classify_latency_band(median_seconds: float | None) -> str`: return `"c"` for `None`; raise
`ValueError` for a negative input; return `"a"` when the value is at or below
`BAND_A_MAX_SECONDS`, `"b"` when at or below `BAND_B_MAX_SECONDS`, `"c"` otherwise. Docstring
maps each letter to D-04's own wording — a is event-driven at minutes scale, proceed with the
lead-scoring tool; b is event-driven but slow, still proceed with the latency recorded as
evidence; c is manual-only, no fire on API writes, or hours-plus, which pauses for operator
review.

`find_score_property_name(results: list[dict]) -> str | None`: return the `name` of the first
entry whose `fieldType` equals `calculation_score`, else `None`. This is the precondition
lookup Task 3 hard-fails on.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_scoring_probe_helpers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def median_latency\|def classify_latency_band\|def find_score_property_name' scripts/probe_scoring_recalc_latency.py` outputs 3.
    - `grep -c 'BAND_A_MAX_SECONDS = 600.0\|BAND_B_MAX_SECONDS = 3600.0\|POLL_INTERVAL_SECONDS = 5.0\|POLL_TIMEOUT_SECONDS = 3900.0\|SAMPLE_COUNT = 3' scripts/probe_scoring_recalc_latency.py` outputs 5.
    - `tests/test_scoring_probe_helpers.py` contains assertions for both `600.0` and `600.1`, and for both `3600.0` and `3600.1` — `grep -c '600.1\|3600.1'` outputs 2 or more.
    - `.venv/bin/python -m pytest tests/test_scoring_probe_helpers.py -x -q` exits 0 with at least 9 additional passing cases beyond plan 39-01's.
    - `.venv/bin/python -m pytest` (full suite) exits 0.
  </acceptance_criteria>
  <done>The D-04 band edges and the median contract are executable and asserted one step either side; no future reader has to interpret where a boundary sits.</done>
</task>

<task type="auto">
  <name>Task 3: Two-key-gated disposable-company probe orchestration</name>
  <files>scripts/probe_scoring_recalc_latency.py</files>
  <read_first>
    - `scripts/rollback_canary_proof.py` lines 42–53 (the `_has_credentials` / `_portal_ok` / `_writes_allowed` triad) and lines 79–123 (the `main` skip/refuse ordering, the PASS/FAIL print convention, and the integer return codes)
    - `scripts/probe_org_type_migration.py` — the disposable-artifact naming discipline and the test-record allowlist guard idiom
    - `src/hubspot_client.py` — `create_record`, `patch_record`, `get_record`, and the `delete_record` added in Task 1 of this plan
    - `.planning/phases/39-path-decision-fit-score-verification/39-RESEARCH.md` "Common Pitfalls" 2 and 3 — the two failure modes this orchestration exists to prevent
  </read_first>
  <precondition>Task 1 of this plan has landed `delete_record` in `src/hubspot_client.py`, and Task 2 has landed the three pure helpers in this script.</precondition>
  <action>
Add the module docstring, the gate triad, and `main(argv=None) -> int` to
`scripts/probe_scoring_recalc_latency.py`.

Module docstring states: what the script measures (the per-record, event-driven rescore
latency following an API-written property change — explicitly NOT the criteria-edit
full-portal bulk recalculation, which is a separate and much slower one-time phenomenon); the
two-key arming requirement; that it touches exactly one disposable company and deletes it; and
the operator-invocation one-liner verbatim, since `.env` is Read/Bash permission-blocked:
`ALLOW_HUBSPOT_SCORING_PROBE=true DRY_RUN=false .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/probe_scoring_recalc_latency.py', run_name='__main__')"`

Gate triad, copied from `scripts/rollback_canary_proof.py` lines 42–53 with one substitution:
`_has_credentials()` tests `HUBSPOT_PRIVATE_APP_TOKEN`; `_portal_ok()` compares
`HUBSPOT_PORTAL_ID` to `EXPECTED_PORTAL_ID`; `_writes_allowed()` requires `DRY_RUN` to be
false AND `ALLOW_HUBSPOT_SCORING_PROBE` to be true. Use that phase-scoped flag name only — do
not reuse the generic property-writes flag that the migration scripts arm, because one left
armed from an unrelated run must not be able to arm this probe.

`main` sequence, each step printing one line and returning a distinct nonzero code on failure:

1. No credentials — print a skipped line, return 0, make no call.
2. Portal mismatch — print a refusal naming the expected portal, return 1, make no call.
3. Not two-key armed — print a skipped line naming both required keys, return 0, make no call.
4. **Precondition, hard-fail:** `GET /crm/v3/properties/companies` and run
   `find_score_property_name` over the results. If it returns `None`, print a message stating
   that no score-typed company property exists yet, that the operator must build one trivial
   criterion in the lead-scoring tool first, and that proceeding would measure a property that
   cannot change and would misreport the no-fire outcome — then return 2. Never poll a
   property that does not exist. This is RESEARCH.md Pitfall 2 and it must abort, not warn.
5. Create one company via `create_record("companies", {...}, dry_run=False)` whose `name` is
   `COMPANY_NAME_PREFIX` plus a UTC timestamp, and whose properties set only the minimum the
   scoring criterion needs to be evaluable. Capture the returned record id. Print it.
6. Wrap everything after creation in a construct that guarantees teardown runs even on an
   exception or a keyboard interrupt — the disposable company must not survive a crash.
7. For each of `SAMPLE_COUNT` iterations: read the current value of the discovered score
   property; take `t0` from `time.monotonic()` immediately before dispatching a
   `patch_record(..., dry_run=False)` that flips one scoring-input property on the disposable
   company to a different value than it currently holds; then poll `get_record` every
   `POLL_INTERVAL_SECONDS` until the score property's value differs from the pre-flip value,
   taking `t1` from `time.monotonic()` immediately after the response that first shows the
   change. Append `t1 - t0` to the samples list. If `POLL_TIMEOUT_SECONDS` elapses with no
   change, append `None` and **abort the remaining iterations immediately** — a first sample
   that never fires already determines band c, and continuing would burn hours for no
   additional signal. Never touch the scoring criteria definition inside this loop; the only
   write is to the disposable company's own property (RESEARCH.md Pitfall 3).
8. Compute the median with `median_latency` when every sample is a real number; pass `None` to
   `classify_latency_band` when any sample is `None`.
9. Write `.planning/phases/39-path-decision-fit-score-verification/evidence/recalc_latency_probe.json`
   via plain `json.dump(..., indent=2)`, containing: `probed_at_utc`, `expected_portal_id`,
   the discovered score property name, the disposable company id and name, the raw per-sample
   elapsed seconds at full float precision, which property was flipped and to what values, the
   median, the band letter, the constants in force (`POLL_INTERVAL_SECONDS`,
   `POLL_TIMEOUT_SECONDS`, `BAND_A_MAX_SECONDS`, `BAND_B_MAX_SECONDS`), and a
   `measurement_contract` string stating that latency is measured with a monotonic clock from
   just before the PATCH to just after the first GET showing the change, that it is therefore
   an upper bound quantized up to the nearest poll with ±5 s resolution, and that the figure
   is the per-record event-driven rescore rather than a criteria-edit bulk recalculation.
10. Teardown: `delete_record("companies", record_id, dry_run=False)` and assert the response
    status is 204. Print PASS with the band letter and the median, or FAIL with the reason,
    matching `rollback_canary_proof.py`'s print convention. A failed delete is a FAIL even if
    the measurement succeeded — a surviving `ZZ-SCORING-TEST-DELETE-ME-*` record is a defect.

The gate triad and the precondition abort are the parts worth exercising without a network;
the live path is inherently operator-invoked and is verified by the checkpoint in plan 39-04.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_scoring_probe_helpers.py -x -q &amp;&amp; env -u HUBSPOT_PRIVATE_APP_TOKEN .venv/bin/python scripts/probe_scoring_recalc_latency.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Running the script with `HUBSPOT_PRIVATE_APP_TOKEN` unset exits 0 and prints a line containing `skipped`.
    - Running the script with a dummy token set but `HUBSPOT_PORTAL_ID=99999999` exits 1, prints a line containing `REFUSED`, and makes no HTTP call.
    - Running the script with a dummy token and the correct portal but without both keys exits 0 and prints a line naming both `DRY_RUN` and `ALLOW_HUBSPOT_SCORING_PROBE`.
    - `grep -c 'ALLOW_HUBSPOT_SCORING_PROBE' scripts/probe_scoring_recalc_latency.py` outputs 2 or more, and `grep -c 'ALLOW_HUBSPOT_PROPERTY_WRITES' scripts/probe_scoring_recalc_latency.py` outputs 0.
    - `grep -c 'time.monotonic' scripts/probe_scoring_recalc_latency.py` outputs 2 or more.
    - `grep -c 'delete_record' scripts/probe_scoring_recalc_latency.py` outputs 1 or more, and the call site is inside the guaranteed-teardown construct.
    - `grep -c 'COMPANY_NAME_PREFIX' scripts/probe_scoring_recalc_latency.py` outputs 2 or more, and no `argparse` argument or `os.getenv` call assigns it.
    - `.venv/bin/python -m pytest` (full suite) exits 0.
  </acceptance_criteria>
  <done>An operator can arm the probe with two keys and get a banded, upper-bounded, fully-sourced latency measurement, with the disposable company guaranteed deleted and the no-score-property case aborting loudly instead of reporting a false no-fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → HubSpot CRM write API | This plan is the only part of Phase 39 that can create, modify, or delete a CRM record. |
| environment flags → arming decision | Two independent environment variables are the entire authority for going live. |
| probe artifact → live CRM population | A disposable company briefly exists alongside 712 real companies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-09 | Tampering | write to a real company record | critical | mitigate | Every write targets only the id returned by this run's own `create_record`, whose name is `COMPANY_NAME_PREFIX` + timestamp; the prefix is a module constant with no CLI or env override, asserted by acceptance grep. |
| T-39-10 | Tampering | accidental arming from a stale flag | high | mitigate | Two-key gate uses the phase-scoped `ALLOW_HUBSPOT_SCORING_PROBE`; acceptance criteria assert `ALLOW_HUBSPOT_PROPERTY_WRITES` appears zero times in this script. |
| T-39-11 | Tampering | wrong-portal write | critical | mitigate | `_portal_ok()` refuses with no HTTP call; asserted by a dummy-token/wrong-portal run in acceptance criteria. |
| T-39-12 | Repudiation | disposable company survives the run | high | mitigate | Teardown runs inside a guaranteed-execution construct even on exception or interrupt; a non-204 delete is reported FAIL regardless of measurement success. |
| T-39-13 | Information Disclosure | token reaching stdout or the evidence JSON | high | mitigate | Headers only via `hs_headers()`; `delete_record`'s dry-run branch prints URL only; the evidence writer serializes named fields, never a headers dict. |
| T-39-14 | Spoofing | false no-fire verdict from a missing score property | high | mitigate | Step 4 precondition returns exit code 2 with an explanatory message rather than polling a nonexistent property (RESEARCH.md Pitfall 2). |
| T-39-15 | Repudiation | latency reported without its measurement contract | medium | mitigate | The evidence JSON carries a `measurement_contract` string and all four governing constants alongside the number. |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest` green.
- All three disarmed-invocation paths (no credentials / wrong portal / not armed) behave as specified with no HTTP call.
- No script in this plan references `ALLOW_HUBSPOT_PROPERTY_WRITES`.
</verification>

<success_criteria>
- `delete_record` exists, dry-run-safe by default, matching the existing four wrappers.
- The D-04 band edges and the median contract are code with one-step-either-side tests.
- The probe cannot run without two keys, cannot run against the wrong portal, cannot start without a score property, and cannot leave its disposable artifact behind.
</success_criteria>

<output>
Create `.planning/phases/39-path-decision-fit-score-verification/39-03-SUMMARY.md` when done.
</output>
