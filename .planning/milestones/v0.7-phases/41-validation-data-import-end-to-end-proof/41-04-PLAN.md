---
phase: 41-validation-data-import-end-to-end-proof
plan: 04
type: execute
wave: 3
depends_on: [41-03]
files_modified:
  - .planning/phases/41-validation-data-import-end-to-end-proof/parity-report-final.json
  - .planning/phases/41-validation-data-import-end-to-end-proof/41-RUN-REPORT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "The remaining June companies are released only after a human has reviewed the canary against the run's expectations."
    - "Every landed company carries canonical lv_* scoring inputs and a non-empty, valid-JSON lv_enrichment_provenance blob, written by the pipeline (DATA-01)."
    - "Every landed company's live lv_icp_fit_score and lv_icp_tier match compute_icp_score, computed without any per-record manual touch (DATA-02)."
    - "The write window is closed and the closure is proven by an independent re-read — this is part of the run's definition of done, not an afterthought."
    - "Records parked to needs_review, records with no derivable revenue band, and records vetoed for non-ANZ geography are each counted and named, not absorbed into a pass."
  artifacts:
    - .planning/phases/41-validation-data-import-end-to-end-proof/parity-report-final.json
    - .planning/phases/41-validation-data-import-end-to-end-proof/41-RUN-REPORT.md
  key_links:
    - "The disarm outcome's verified re-read is the only evidence the window is actually closed; a disarm that is not read back is an unproven claim."
    - "parity-report-final.json's per-record provenance sub-object and review fields are the run report's sole data source for the D-12 queue listing — an EQ search on lv_enrichment_needs_review is unreliable while PIPE-01 is open, so per-record reads are the only trustworthy path."
---

<objective>
Close the phase: review the canary, release the remaining June companies through the same armed
window, shut the window and prove it shut, then produce the two committed artifacts that close
DATA-01 and DATA-02 — a parity verdict over the landed population and a run report that names
every record that did not land cleanly.

Purpose: DATA-01 and DATA-02 are the milestone's last two open data requirements. Both close on
evidence produced here.

Output: `parity-report-final.json`, `41-RUN-REPORT.md`, a disarmed pipeline, and the two
requirements marked complete in `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-validation-data-import-end-to-end-proof/41-CONTEXT.md
@.planning/phases/41-validation-data-import-end-to-end-proof/41-01-SUMMARY.md
@.planning/phases/41-validation-data-import-end-to-end-proof/41-03-SUMMARY.md
@.planning/phases/41-validation-data-import-end-to-end-proof/41-canary-evidence.md
</context>

<execution_boundary>
Tasks 1 and 2 are blocking operator interactions; Task 3 is autonomous and reads only committed
files. The write window opened in Plan 03 is still open when this plan begins and MUST be closed
inside Task 2. If this plan is abandoned part-way for any reason, closing the window is the single
step that cannot be deferred — hand the operator the disarm command immediately.
</execution_boundary>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Canary review gate — authorize or halt</name>
  <what-built>Plan 03 drove five real companies through the full chain: queue property, SJ-3 poller, Claude web research, three-source merge with the June table folded in behind fresh research, canonical PATCH, HubSpot mapper flows, computed score and tier. `41-canary-evidence.md` and `parity-report-canary.json` record exactly what landed on each of the five, including the values the June table expected and the values fresh research produced.</what-built>
  <how-to-verify>
Open `41-canary-evidence.md` alongside the five companies in HubSpot (portal 22617666) and confirm,
for each, that the values that landed are the right values for that real company — not merely that
the pipeline wrote something.

Check specifically:
1. **Melbourne Racing Club (`9604614548`)** — did its pre-existing canonical values survive or get
   correctly refreshed? This is the only record in the portal that had prior state, so it is the
   only real test of the non-clobber path.
2. **Racing NSW (`15008671672`)** — `lv_org_type` should be `governing_body_league` with an
   evidence URL in the provenance blob. If it parked to review, the evidence gate is not receiving
   the URL.
3. **QRIC (`16047156820`)** — `lv_org_type` should be `regulator`. If it landed
   `governing_body_league`, the exception list did not fire or fresh research overrode it; the
   evidence file should say which.
4. **Sportsbet (`17861423879`)** — `lv_is_gambling_operator` true, `lv_anti_icp_flag` NOT true. A
   veto here means the gambling deduction has been reconflated with a hard veto.
5. **Supertech Electronics (`15274105699`)** — `lv_anti_icp_flag` true with a hardware-vendor
   reason and tier `D`.

Then check the run-level facts: provider credits unchanged from the Task 2 baseline, every record
carrying a provenance blob, and the observed settle time under the five-minute threshold.

Approving this checkpoint authorizes writing canonical `lv_*` values to the remaining sixty-odd
real customer records. If any enum value looks wrong for the real company, say so here rather than
after sixty-one more records carry the same defect.
  </how-to-verify>
  <resume-signal>Type "release" to authorize the remaining records, or describe what is wrong.</resume-signal>
  <acceptance_criteria>
    - The operator has viewed all five canary records in HubSpot, not only the evidence file.
    - A decision is recorded in the thread: release, or halt with a named defect.
    - If halted, the defect is written into the plan summary and the window is disarmed before any remediation begins.
  </acceptance_criteria>
  <done>A human has confirmed the five canary records carry correct values for the real companies they represent, and has explicitly authorized or refused the release of the rest.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Release the remaining records, disarm, and sweep</name>
  <precondition>Task 1's gate returned "release", and the write window opened in Plan 03 Task 2 is still armed.</precondition>
  <reversibility rating="costly">Writes canonical `lv_*` values to the remaining resolved June companies. Prior canonical state was blank on all of them, so a rollback is a batch property clear — costly, not one-way. The canary gate in Task 1 is the decision point this rating refers to.</reversibility>
  <what-built>The window is armed for all resolved June ids. The five canary records have already landed. The remaining records need only the queue property set; the poller and the pipeline do the rest with no further intervention.</what-built>
  <action>
Compute the release id set (resolved ids minus the five canary ids) and the landed id set from
`41-id-resolution.json` and the live property reads, substitute both into the commands below, then
hand the operator the six-step sequence in `<instructions>` verbatim and wait. Claude executes none
of it — every step needs a value from the permission-blocked `.env`. Size the release from
`41-03-SUMMARY.md`'s measured per-record wall-clock before handing over step 1. Do not advance past
step 3 until the disarm payload's independent re-read is pasted.
  </action>
  <instructions>
1. **Release the remaining records.** Set `lv_enrichment_requested` to the literal string `"true"`
   on every resolved id that is not one of the five canary ids, via
   `src.hubspot_client.batch_update_companies`. Run it once with `dry_run=True` and review the
   payload, then once live. The string form is mandatory — a JSON boolean makes the poller's EQ
   filter silently fail to match. Count the release set first: `batch_update_companies` **raises**
   above 100 updates rather than truncating, so chunk if the count exceeds 100. At a 66-record
   population it will not, but the count comes from the resolution report, not from an assumption.

   **Sizing:** if `41-03-SUMMARY.md` recorded that the five canary records serialized inside one
   SJ-3 execution, extrapolate the per-record wall-clock across the remaining count before
   releasing. If the extrapolation exceeds roughly forty minutes for a single tick, release in two
   or three groups across successive ticks instead of all at once, to stay clear of n8n Cloud's
   execution-duration ceiling. The allowlist does not need re-arming between groups — it already
   covers every resolved id.

2. **Wait and monitor.** Check the n8n execution list periodically rather than continuously. The
   run is complete when no resolved id still reads `lv_enrichment_status` of `running` or
   `queued`.

3. **Disarm — this is part of the run's definition of done, not cleanup:**
   `!.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import sys; sys.argv=['june_run_arm.py','--disarm']; import runpy; runpy.run_path('scripts/june_run_arm.py', run_name='__main__')"`
   Paste the outcome payload. It carries an independent re-read; a disarm without that read-back is
   an unproven claim. Do not proceed to step 4 until the payload reports disarmed.

4. **Read provider credits again** with the same command from Plan 03 Task 2 and paste the output —
   the "after" half of the zero-spend proof.

5. **Run the final parity sweep** (read-only, safe to run disarmed) over the **landed** subset:
   `!PARITY_SAMPLE_IDS=<comma-separated landed ids> PARITY_REQUIRE_PROVENANCE=true PARITY_REPORT_DIR=.planning/phases/41-validation-data-import-end-to-end-proof/ .venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy; runpy.run_path('scripts/run_scoring_parity.py', run_name='__main__')"`
   Rename the produced report to `parity-report-final.json`.

   "Landed" means resolved ids whose `lv_icp_fit_score` is present. A record parked entirely to
   `needs_review` promotes no scoring inputs, so its score formula has null terms and blanks
   entirely while the oracle returns 0 — a structural failure that says nothing about engine
   correctness. Parked records belong in the run report under D-12, not in the parity sample.

6. **Capture the full property read** for every resolved id — landed and parked alike — so Task 3
   can write the run report. The parity report already carries this for the landed subset; for
   parked records, read `lv_enrichment_status`, `lv_enrichment_needs_review`,
   `lv_enrichment_review_reason` and the canonical `lv_*` inputs with per-record GETs. Do not use a
   HubSpot search filtered on `lv_enrichment_needs_review` — that property's boolean-vs-string
   writer defect is an open requirement (PIPE-01, Phase 43), so an EQ filter on it is unreliable
   today.
  </instructions>
  <verification>
Confirm each of the following from the pasted outputs before advancing, and treat any one of them
as a halt rather than a note.

The disarm outcome payload must report the disarmed state **and** carry its independent re-read —
a disarm without that read-back is an unproven claim and the window may still be open. The
after-run credit reading must equal the Plan 03 baseline for every provider that reported a number;
a provider that reported `None` both times is a credential gap, not evidence of spend, and should be
recorded as such. `parity-report-final.json` must exist with `real_findings` empty and
`assertions_executed` equal to the landed count and greater than zero — the greater-than-zero check
is the false-green guard and a sweep that checked nothing must never read as a sweep that found
nothing wrong. Every comparison record in it must show `provenance.present` and
`provenance.valid_json` true, which is DATA-01's automated bar. Finally, confirm no resolved id
still reads `lv_enrichment_status` of `running` or `queued`; a record still in flight after the
disarm will fail its write and land nothing.
  </verification>
  <resume-signal>Paste the disarm outcome payload, the after-credit reading and the parity verdict line, then type "run complete".</resume-signal>
  <acceptance_criteria>
    - Every resolved id that is not a canary id had `lv_enrichment_requested` set to the string `"true"` exactly once.
    - No resolved id still reads `lv_enrichment_status` of `running` or `queued` at the end of the run.
    - The disarm outcome payload reports the disarmed state and includes its independent re-read.
    - The after-run provider credit reading equals the Plan 03 Task 2 baseline for every provider that reported a number.
    - `parity-report-final.json` exists, its `assertions_executed` equals the landed count and is greater than zero, and its `real_findings` array is empty.
    - Every comparison record in `parity-report-final.json` has `provenance.present` true and `provenance.valid_json` true.
    - The per-record property read covering the parked records is captured in the thread or a committed file.
  </acceptance_criteria>
  <done>The full June population has been released through the armed window, the window is closed and proven closed by an independent re-read, no provider credit was spent, and the parity verdict over the landed population is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Run report and requirement closure</name>
  <files>.planning/phases/41-validation-data-import-end-to-end-proof/41-RUN-REPORT.md, .planning/REQUIREMENTS.md</files>
  <precondition>`parity-report-final.json` and `41-id-resolution.json` both exist in the phase directory and the disarm outcome is recorded in the thread.</precondition>
  <read_first>
    - `.planning/phases/41-validation-data-import-end-to-end-proof/41-id-resolution.json`
    - `.planning/phases/41-validation-data-import-end-to-end-proof/parity-report-final.json`
    - `.planning/phases/41-validation-data-import-end-to-end-proof/parity-report-canary.json`
    - `.planning/phases/41-validation-data-import-end-to-end-proof/41-canary-evidence.md`
    - `.planning/phases/41-validation-data-import-end-to-end-proof/41-01-SUMMARY.md` (the exception list, the band distribution and the count of rows with no derivable revenue band)
    - `docs/reports/` for the dated-markdown naming convention already in use
    - `.planning/REQUIREMENTS.md` (DATA-01 / DATA-02 wording and the Traceability table)
  </read_first>
  <behavior>
    - The report states, with counts, how many of the 66 resolved live, were re-matched, were ambiguous and were unmatched.
    - The report names every record parked to `needs_review` with its recorded reason, per D-12's accept-and-report policy.
    - The report names every record with no derivable `lv_revenue_band` and states the score impact, closing out F1's option-c residue explicitly.
    - The report names every record vetoed for non-ANZ geography and explains that this is the rubric working, not an import defect.
    - The report states the DATA-01 and DATA-02 verdicts with the evidence each rests on.
    - The report states the zero-spend evidence: the structural assertion plus the before/after credit readings.
    - The report states the disarm evidence.
  </behavior>
  <action>
Write `.planning/phases/41-validation-data-import-end-to-end-proof/41-RUN-REPORT.md`. Do not build a
report-generator script — the two committed JSON artifacts plus the canary evidence file already
carry every number, and a third script would only add a surface that can disagree with them.

Structure it as: provenance of the source dataset (the sibling-repo path, the committed snapshot,
its sha256, the 49/16/1 confidence split, and that `../icp-analysis/enrich.mjs` produced it via
Perplexity `sonar` on 2026-06-29 — the operator asked for this validation trail explicitly and it
must survive into the record); resolution outcomes with counts; what landed, per record, as a table
keyed by id with `lv_org_type`, `lv_produces_content`, `lv_country_region_normalized`,
`lv_revenue_band`, the veto flag, `lv_icp_fit_score`, `lv_icp_tier` and the provenance sources;
then three explicitly-counted sections for the outcomes that look like failures and are not:

- **Parked to review** — every record whose `lv_enrichment_status` is `needs_review`, with its
  `lv_enrichment_review_reason`. Split them by cause: a June-vs-fresh disagreement on
  `lv_org_type` or `lv_produces_content` (D-04 working as designed), a below-threshold confidence
  (June's 16 medium and 1 low rows cannot clear the field thresholds under D-03's mapping), or an
  evidence-gate miss. D-12 accepts all three; the operator triages afterward.
- **No derivable revenue band** — the records whose `annualrevenue` was blank at resolution time.
  State that each of these scores up to 10 points low against the rubric and may therefore sit one
  tier below its true position, and that this is F1's documented residue rather than an engine
  defect.
- **Non-ANZ hard veto** — the records whose `hq_country` is outside Australia and New Zealand.
  State the count and that tier `D` here is `config/icp_scoring.yaml`'s non-ANZ veto firing
  correctly on real data, which is itself part of the DATA-02 proof.

Close with the DATA-01 verdict (inputs landed plus provenance stamped plus zero provider spend,
each pointing at its evidence), the DATA-02 verdict (score and tier computed with no per-record
manual touch, within the five-minute threshold, matching the oracle across the landed population),
the zero-spend evidence, and the disarm evidence.

Then update `.planning/REQUIREMENTS.md`: mark DATA-01 and DATA-02 complete and set both rows in the
Traceability table to Complete. Use scoped edits — never rewrite the file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `41-RUN-REPORT.md` exists and its resolution counts sum to 66.
    - The report contains all three explicitly-counted sections (parked to review, no derivable revenue band, non-ANZ veto), each with a count and a per-record listing, even when a count is zero.
    - Every record listed as parked carries a non-empty reason string sourced from `lv_enrichment_review_reason`.
    - The report cites `parity-report-final.json`'s `assertions_executed`, `real_findings` length and `verdict` verbatim.
    - The report records the before and after provider credit readings and the structural zero-spend assertion's test name.
    - The report records the disarm outcome including its independent re-read.
    - The report records the source dataset's sha256 and the 49/16/1 confidence split.
    - `.planning/REQUIREMENTS.md` shows DATA-01 and DATA-02 checked and their Traceability rows read Complete.
    - `.venv/bin/python -m pytest -q` still reports at least 2308 passing.
  </acceptance_criteria>
  <done>A committed run report explains what landed, what did not, and why each non-landing was expected; DATA-01 and DATA-02 are marked complete against named evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| armed pipeline -> production HubSpot CRM | the remaining sixty-odd real customer records become writable |
| committed reports -> future phases | Phase 42's cleanup and reconciliation reads this phase's outcome as fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-17 | Elevation of Privilege | write window left open after the run | high | mitigate | Disarm is step 3 of Task 2's operator sitting, is gated on an independent re-read, appears in the plan's `must_haves` and in Task 2's acceptance criteria, and the execution boundary names it as the one step that cannot be deferred if the plan is abandoned |
| T-41-18 | Repudiation | a run that wrote nothing reading as a clean pass | high | mitigate | `parity-report-final.json`'s pre-existing zero-assertions false-green guard, plus an acceptance criterion requiring `assertions_executed` greater than zero and equal to the landed count |
| T-41-19 | Tampering | silent scope reduction by omitting failed records from the report | high | mitigate | Three explicitly-counted sections are required even when a count is zero, and the resolution counts must sum to 66 |
| T-41-20 | Information Disclosure | provenance blob or review reason leaking a credential into a committed report | medium | mitigate | The provenance blob carries source names, confidences and evidence URLs only; no operator command output containing a token is pasted into a committed file |
| T-41-21 | Tampering | unreliable review-queue detection via a boolean-typed EQ filter | medium | mitigate | Task 2 step 6 requires per-record GETs rather than a search filtered on `lv_enrichment_needs_review`, whose writer defect is an open requirement (PIPE-01, Phase 43) |
| T-41-SC | Tampering | package-manager installs | low | accept | This plan installs no npm/pip/cargo package |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` reports at least 2308 passing.
- `parity-report-final.json` reports a non-zero `assertions_executed` equal to the landed count, empty `real_findings`, and provenance present on every record.
- The disarm outcome payload, with its independent re-read, is recorded in `41-RUN-REPORT.md`.
- Provider credit readings are identical before and after the run.
</verification>

<success_criteria>
- The remaining June companies are released, landed and reported.
- The write window is closed and proven closed.
- `parity-report-final.json` and `41-RUN-REPORT.md` are committed.
- DATA-01 and DATA-02 are marked complete in `.planning/REQUIREMENTS.md` against named evidence.
</success_criteria>

<artifacts_this_phase_produces>
The phase-wide index lives in `41-01-PLAN.md`'s `<artifacts_this_phase_produces>` section. This
plan's slice of it:

**Produced here**

| Path | Kind |
|---|---|
| `.planning/phases/41-.../parity-report-final.json` | landed-population parity verdict, provenance enforced |
| `.planning/phases/41-.../41-RUN-REPORT.md` | D-12 run report — the phase's human-readable record of what landed and what did not |
| `.planning/REQUIREMENTS.md` | scoped edits marking DATA-01 and DATA-02 complete |

No new scripts, functions, config keys or CLI flags. The run report is hand-authored from the
committed JSON artifacts deliberately — a report-generator script would add a third surface that
can disagree with the two verdicts it summarizes.

**Consumed from 41-01, 41-02 and 41-03**

`41-01-SUMMARY.md` (exception list, band distribution, count of rows with no derivable revenue
band); `scripts/june_run_arm.py --disarm`; `PARITY_REQUIRE_PROVENANCE`;
`scripts/check_provider_credits.py`; `41-id-resolution.json`; `parity-report-canary.json`;
`41-canary-evidence.md`; `41-03-SUMMARY.md` (per-record wall-clock, used to size the release).

**Live state this plan leaves behind**

The enrichment workflow is **disarmed**, proven by an independent re-read. This is the phase's
definition of done.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/41-validation-data-import-end-to-end-proof/41-04-SUMMARY.md` when done.
Record the landed count, the parked count, the unmatched count, the DATA-01 and DATA-02 verdicts,
and any record whose outcome Phase 42's cleanup and reconciliation needs to know about.
</output>
