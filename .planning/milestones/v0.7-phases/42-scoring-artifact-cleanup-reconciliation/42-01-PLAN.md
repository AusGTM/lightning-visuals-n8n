---
phase: 42-scoring-artifact-cleanup-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_schema_drift.py
  - tests/test_check_schema_drift.py
  - config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json
  - config/hubspot_migration/baseline/portal-schema-contacts-phase42-pre.json
  - .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json
autonomous: false
requirements: [CLEAN-01]

user_setup:
  - service: hubspot
    why: "Live GET of the property schema (portal 22617666) and the automation flow list. `.env` is permission-blocked to Read and Bash, so every live invocation is an operator-pasted `!` command."
    env_vars:
      - name: HUBSPOT_PRIVATE_APP_TOKEN
        source: ".env (already present; loaded by `load_dotenv()` inside the operator command — never read by Claude)"
      - name: HUBSPOT_PORTAL_ID
        source: ".env (must equal 22617666 or every script refuses pre-call)"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running one command against the live portal produces a committed JSON drift report that names, per property, whether the yaml and the live portal agree (D-06 depth: existence + enum option values)."
    - "The same command proves, in the same run, that all eleven do-not-archive properties are live and all six scoring flows are live and enabled (D-01's invariant, executable rather than documentary)."
    - "A property declared in the yaml but absent live is reported as a fabricated entry and fails the run — the guard that stops the five design-only D-04 names being invented into the manifest (F2)."
    - "The lv_icp_tier five-value live enum vs config/icp_scoring.yaml's Needs Review key is reported as an accepted divergence and never contributes a non-zero exit (F5)."
    - "The comparator's scope is an explicit property name list, so the five non-lv_-prefixed *_score properties are inside it (F4)."
  artifacts:
    - scripts/check_schema_drift.py
    - tests/test_check_schema_drift.py
    - config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json
    - config/hubspot_migration/baseline/portal-schema-contacts-phase42-pre.json
    - .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json
  key_links:
    - "DO_NOT_ARCHIVE_COMPANY_PROPERTIES / DO_NOT_ARCHIVE_FLOW_IDS live in scripts/check_schema_drift.py and are imported by scripts/derive_orphan_candidates.py in plan 03 — one definition, two consumers."
    - "The committed phase42-pre snapshot is the ONLY oracle plan 02 may read live property shape from; the superseded CLAUDE.md design list is never an oracle."
    - "scripts/snapshot_hubspot_schema.py's output destination and naming are unchanged (D-09) — this plan only adds a --label value."
---

<objective>
Build and live-prove the standing schema drift checker (D-10), and in the same run capture the
fresh pre-mutation portal snapshot (D-11) that every later task in this phase reads its live
truth from.

This is the phase's tracer: one path through every layer — live HubSpot GET → comparator →
JSON report → meaningful exit code — exercised end to end against portal 22617666 before any
archival mutation exists anywhere in the phase. Zero portal writes (D-05).

**F1 — the do-not-archive set, stated first, before anything else in this phase runs.**
Under the fix-in-place path chosen in Phase 39 and executed in Phase 40, the artifacts ROADMAP
SC1 names as "superseded" ARE the live scoring engine. These are NEVER archived by any task in
Phase 42:

- Company properties: `org_type_score`, `geography_score`, `annual_revenue_score`,
  `produces_content_score`, `gambling_score`, `lv_icp_fit_score`, `lv_icp_tier`,
  `lv_anti_icp_flag`, `lv_org_type`, `lv_produces_content`, `lv_country_region_normalized`
- Automation flows: `4626124224` (org type score), `4626722240` (geography score),
  `4626722237` (annual revenue score), `4625147345` (WF1 set ICP tier), `4634822079`
  (produces content score), `4634822085` (gambling score)

`42-CONTEXT.md` D-01 is authoritative on this. **`39-DECISION.md:107-112` is stale, superseded
prose** — it was written before the fix-in-place path was chosen and, read literally, points an
executor back at archiving the live engine. Do not follow it. There is no separate "1 + 1
placeholder" property to retire: the formula was edited in place on the same
`lv_icp_fit_score` record.

**n8n independence (execution-timing constraint).** Nothing in this plan — or anywhere in
Phase 42 — regenerates or redeploys n8n workflow content. Do NOT run
`scripts/build_cloud_workflows.py` or `scripts/deploy_n8n_workflows.py`. A content rebake
resets the baked write-safety constants to disarmed and would silently close Phase 41's live
import arm window mid-run. Phase 42 is HubSpot-schema-side only.

Purpose: make "zero drift" a re-runnable, provable claim (D-10) instead of a one-time
assertion, and make D-01's do-not-archive set a machine-checked invariant instead of a
paragraph.
Output: a new read-only script, its offline test suite, a fresh committed portal snapshot pair,
and the pre-reconciliation drift report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-CONTEXT.md
@.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-RESEARCH.md
@scripts/snapshot_hubspot_schema.py
@scripts/sync_hubspot_properties.py
@config/hubspot_properties.yaml
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end drift check — live portal GET through to a committed JSON report</name>
  <files>scripts/check_schema_drift.py, config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json, config/hubspot_migration/baseline/portal-schema-contacts-phase42-pre.json, .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json</files>
  <precondition>`HUBSPOT_PRIVATE_APP_TOKEN` and `HUBSPOT_PORTAL_ID=22617666` are present in `.env`, and the private app carries the `automation` scope (granted 2026-08-06, exercised throughout Phase 40). The operator runs the live commands; `.env` is permission-blocked to Read and Bash.</precondition>
  <read_first>
    - `scripts/snapshot_hubspot_schema.py` — the source-of-truth analog for every idiom this new script copies: `_has_credentials()` skip-to-exit-0, `_portal_ok()` guard against `EXPECTED_PORTAL_ID`, `_assert_no_secrets()` before any portal body is written to disk, and `_get_properties_raw()`'s `requests` + `hs_headers()` + `BASE_URL` call shape.
    - `scripts/sync_hubspot_properties.py` lines 64-98 — `load_desired_config()`, `_options_values()`, and `compute_property_diff()`; `_options_values` is the exact enum comparison D-06 asks for and should be reused, not reinvented.
    - `config/hubspot_properties.yaml` — the manifest being compared; note the six per-property keys (`name`, `label`, `type`, `fieldType`, `groupName`, `options`) and that the file currently holds 22 company / 17 contact entries.
    - `config/hubspot_flows/lv_icp_tier-property.after.json` — the live five-option enum shape the comparator must agree with.
    - `config/hubspot_flows/lv_icp_fit_score-property.after.json` — the live calculated property, `fieldType: calculation_equation`, `groupName: companyinformation`.
  </read_first>
  <reversibility rating="reversible">A new read-only script and two GET-derived artifacts; deleting the file undoes it entirely.</reversibility>
  <action>
Create `scripts/check_schema_drift.py` — the D-10 standing drift checker. Read-only: it issues
GET requests only and has no write path of any kind, which is what keeps D-05's no-portal-
mutation guarantee true for the whole reconciliation half of this phase.

Copy verbatim from `scripts/snapshot_hubspot_schema.py`: the module-level `EXPECTED_PORTAL_ID`
constant defaulting to `22617666` with a `HUBSPOT_EXPECTED_PORTAL_ID` env override,
`_has_credentials()` (returns exit 0 with a skipped message when the token is absent, so the
script is safe in CI), `_portal_ok()` (returns exit 1 refusing pre-call on a portal mismatch),
and `_assert_no_secrets()` applied to the serialized report text before it is written.

Define these module constants, and give each a comment naming the decision or finding it
encodes:

`DO_NOT_ARCHIVE_COMPANY_PROPERTIES` — a frozenset of exactly the eleven company property names
listed in this plan's objective. Comment cites D-01 and records that `39-DECISION.md:107-112`
is superseded prose.

`DO_NOT_ARCHIVE_FLOW_IDS` — a mapping of the six flow ids in the objective to their slugs,
matching `scripts/fetch_hubspot_flow.py`'s existing `FLOW_SLUGS` mapping.

`D04_COMPANY_PROPERTY_SCOPE` — the fifteen company property names D-04 names as the full-mirror
target: the eleven above minus `lv_country_region_normalized`, plus `lv_icp_confidence`,
`lv_recommended_motion`, `lv_icp_scored_at`, `lv_icp_scoring_version`,
`lv_named_account_priority`. This is an EXPLICIT NAME LIST, never a prefix regex — per F4,
`tests/test_hubspot_schema_coverage.py:33`'s `PROPERTY_RE` cannot match the five `*_score`
names and reusing it would classify the live engine as unreferenced.

`ACCEPTED_DIVERGENCES` — a list of dicts, each `{id, property, description}`. Seed it with one
entry, id `PARITY-01-tier-label`, recording that the live `lv_icp_tier` enum carries five
values and that `config/icp_scoring.yaml`'s `recommended_motion` map names a sixth tier label
the live enum does not have; the description states this was deliberately deferred in Phase 40
(40-06) and is a documented, accepted divergence, not a defect for this phase.

Comparator scope: every property declared in `config/hubspot_properties.yaml` for both object
types, UNION `D04_COMPANY_PROPERTY_SCOPE` on companies. HubSpot-native firmographic fields
(`name`, `domain`, `country`, `annualrevenue`, `numberofemployees`) stay out of scope per D-04,
as does anything the live API marks `hubspotDefined` that the yaml does not declare.

Implement `classify_property(name, declared, live)` as a pure function taking the yaml entry
(or None) and the live property dict (or None) and returning one status string. This function
must be importable and testable with no network. The state machine, exactly:

  - live and declared, and `_options_values` of both sides are equal, and `type`/`fieldType`
    match → `in_sync`
  - live and declared, enum option value sets differ → `enum_mismatch`
  - live and declared, `type` or `fieldType` differ → `type_mismatch`
  - live and declared, only `label` / `displayOrder` / `description` differ → `cosmetic_only`
    (reported, never a failure, per D-06)
  - live and NOT declared → `missing_from_yaml`
  - NOT live and declared → `fabricated_entry` — this is the F2 guard: it fires when a yaml
    entry exists for a property the portal does not have, which is exactly what fabricating an
    entry from the superseded CLAUDE.md design list would produce
  - NOT live and NOT declared, name in `D04_COMPANY_PROPERTY_SCOPE` → `documented_gap`, i.e.
    a name from the superseded local-MVP design that was never implemented live; reported, not
    a failure, and explicitly NOT a signal to create anything

Implement `_get_live_properties(object_type)` mirroring `sync_hubspot_properties.py:101-106`,
and `_get_live_flows()` issuing `GET /automation/v4/flows` with the same `hs_headers()` /
`BASE_URL` idiom (no wrapper exists for this call yet — Phase 40 hand-rolled it). Assert the
returned property count is non-trivial (a sanity floor of 100) so a silently truncated or
paginated response cannot read as "everything is in sync".

Implement `build_report(...)` returning a dict with keys: `generated_at` (UTC ISO),
`portal_id`, `scope` (`{object_types, property_count}`), `do_not_archive`
(`{properties: [{name, live}], flows: [{id, slug, live, is_enabled}], ok: bool}`), `properties`
(one entry per in-scope name: `{name, object_type, status, declared, live, detail}`),
`accepted_divergences`, `summary` (a count per status), and `exit_code`.

Implement `exit_code_for(report)` — also pure and offline-testable:

  - `2` if `do_not_archive.ok` is false, i.e. any of the eleven properties is absent live or
    any of the six flows is absent or not enabled. Exit 2 is deliberately distinct from
    ordinary drift: it means the scoring engine itself is damaged, which is a different and
    more urgent condition than a stale config file.
  - `1` if any property carries status `enum_mismatch`, `type_mismatch`, `missing_from_yaml`,
    or `fabricated_entry`
  - `0` otherwise. `cosmetic_only`, `documented_gap`, and anything listed in
    `ACCEPTED_DIVERGENCES` never contribute a non-zero code.

CLI: `--out PATH` (required, no default — this is a standing tool, so it must not hardcode a
phase directory) writes the report as indented JSON with a trailing newline, and prints a
one-line human summary plus the exit code to stdout.

Module docstring records D-12's cadence: on demand and before/after any schema change, the
same tier as the parity full run, and deliberately NOT wired into the unattended sweep because
schema drifts rarely and the sweep budget is better spent elsewhere. The docstring also carries
the exact `.env`-blocked operator invocation, copying the format already used in
`scripts/fetch_hubspot_flow.py`'s docstring.

Then prove it end to end live. Hand the operator these two commands, in order, and have them
paste the output back:

First, the D-11 snapshot-first capture (D-09 keeps this script's destination and naming
unchanged; only the label is new):

`!.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['snapshot_hubspot_schema.py', '--label', 'phase42-pre']; runpy.run_path('scripts/snapshot_hubspot_schema.py', run_name='__main__')"`

Second, the drift check itself:

`!.venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); import runpy, sys; sys.argv = ['check_schema_drift.py', '--out', '.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json']; runpy.run_path('scripts/check_schema_drift.py', run_name='__main__')"`

Commit the two snapshot files and the drift report as evidence. **The expected outcome of this
first live run is exit 1, not exit 0** — the yaml has not been expanded yet, so roughly fifteen
company properties should report `missing_from_yaml`. What must be green on this run is
`do_not_archive.ok == true`. If it is not, stop the phase and report: the engine is damaged
independently of anything Phase 42 has done.

This run also settles two questions research could not close offline: the live `groupName`,
`type`, and `fieldType` of the five `*_score` properties, and whether any of
`lv_icp_confidence`, `lv_recommended_motion`, `lv_icp_scored_at`, `lv_icp_scoring_version`,
`lv_named_account_priority` exist live at all. Record both answers in the plan summary — plan
02 reads them from the committed snapshot and from nowhere else.
  </action>
  <verify>
    <automated>test -f config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json && test -f .planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json && .venv/bin/python -c "import json;r=json.load(open('.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json'));assert r['do_not_archive']['ok'] is True, r['do_not_archive'];assert all(p['live'] for p in r['do_not_archive']['properties']);assert all(f['live'] and f['is_enabled'] for f in r['do_not_archive']['flows']);print('do-not-archive invariant holds:', len(r['do_not_archive']['properties']), 'properties,', len(r['do_not_archive']['flows']), 'flows')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_schema_drift.py` exists and defines `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` (11 names), `DO_NOT_ARCHIVE_FLOW_IDS` (6 ids), `D04_COMPANY_PROPERTY_SCOPE` (15 names), `ACCEPTED_DIVERGENCES`, `classify_property`, `exit_code_for`, `build_report`, `_get_live_properties`, `_get_live_flows`.
    - `.venv/bin/python -c "import ast,sys;src=open('scripts/check_schema_drift.py').read();assert 'requests.post' not in src and 'requests.patch' not in src and 'requests.put' not in src and 'requests.delete' not in src, 'drift checker must be GET-only';print('read-only confirmed')"` passes.
    - `.venv/bin/python scripts/check_schema_drift.py --out /tmp/x.json` with no credentials in the environment prints a skipped message and exits 0.
    - The committed `drift-report-phase42-pre.json` has `do_not_archive.ok == true`, with all 11 properties `live: true` and all 6 flows `live: true` and `is_enabled: true`.
    - The committed snapshot pair `portal-schema-{companies,contacts}-phase42-pre.json` exists under `config/hubspot_migration/baseline/` — the same destination and naming `scripts/snapshot_hubspot_schema.py` already used, with only the label differing.
    - The plan summary records, from the committed snapshot, the live `groupName`/`type`/`fieldType` of all five `*_score` properties and the live-existence answer for each of the five design-only names.
  </acceptance_criteria>
  <done>One command against the live portal produces a committed drift report; the do-not-archive invariant is machine-checked and green; the fresh snapshot pair is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline test suite pinning the comparator's state machine and exit codes</name>
  <files>tests/test_check_schema_drift.py</files>
  <read_first>
    - `scripts/check_schema_drift.py` — the module under test, written in Task 1.
    - `tests/test_hubspot_properties_config.py` — the source-of-truth analog for offline yaml-oracle tests in this repo: pure parse and assert, no network, `Path(__file__).resolve().parent.parent` rooting.
    - `config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json` — the committed live snapshot Task 1 produced; use it as the realistic fixture rather than inventing property shapes.
    - `.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-RESEARCH.md` section "Reference Detection" — the four traps these tests exist to pin.
  </read_first>
  <behavior>
    - `classify_property` returns `in_sync` when live and declared agree on type, fieldType, and enum option value set.
    - `classify_property` returns `enum_mismatch` when only the option value sets differ.
    - `classify_property` returns `cosmetic_only` when only label, displayOrder, or description differ.
    - `classify_property` returns `missing_from_yaml` when the property is live and undeclared.
    - `classify_property` returns `fabricated_entry` when the property is declared and absent live.
    - `classify_property` returns `documented_gap` for an in-D04-scope name that is neither live nor declared.
    - `exit_code_for` returns 2 when any do-not-archive property is absent or any do-not-archive flow is disabled, even if every other property is `in_sync`.
    - `exit_code_for` returns 1 for `fabricated_entry` alone.
    - `exit_code_for` returns 0 when the only non-`in_sync` statuses are `cosmetic_only` and `documented_gap`.
    - The five `*_score` names are members of `D04_COMPANY_PROPERTY_SCOPE`, so the comparator sees them despite their lack of a project namespace prefix.
    - The `lv_icp_tier` accepted divergence is present in `ACCEPTED_DIVERGENCES` and contributes nothing to the exit code.
  </behavior>
  <action>
Create `tests/test_check_schema_drift.py` as a pure offline pytest module — no network, no
credentials, no monkeypatched HTTP. Test `classify_property` and `exit_code_for` directly with
constructed dicts; both were written as pure functions in Task 1 precisely so this is possible.

Write one test per behavior listed above. For the enum cases, build the live side in the shape
the API actually returns (a list of dicts carrying `description`, `displayOrder`, `hidden`,
`label`, `value`) and the yaml side in the shape the manifest uses (`label`, `value`,
`displayOrder`, `hidden`) — the extra `description` key on the live side must not register as
drift, since D-06 scopes the comparison to option `value` sets.

Two tests carry more weight than the rest and should say so in their docstrings:

The F4 regression guard — assert every one of the five component score property names is a
member of `D04_COMPANY_PROPERTY_SCOPE`. Its docstring records that the repo's only pre-existing
reference detector uses a namespace-prefix regex that structurally cannot match these five, and
that a comparator built by copying that regex would classify the live scoring engine as
unreferenced.

The F5 accepted-divergence guard — assert the seeded entry is present, and assert that a report
whose only anomaly is that divergence yields exit code 0. Its docstring records that the live
tier enum's value count is five, that the sixth label named in `config/icp_scoring.yaml`'s
`recommended_motion` map was deliberately deferred in Phase 40, and that this phase reports it
rather than fixing it — fixing it would require a portal-side enum addition, which D-05 forbids
in reconciliation and D-07/D-08 do not authorize.

Add one test that loads the committed `portal-schema-companies-phase42-pre.json` snapshot and
asserts every name in `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` appears in its `results[].name` set.
This turns the committed snapshot into a permanent offline tripwire: if a later change removes
one of the eleven from the live schema and someone re-captures the snapshot under the same
label, this test goes red without needing a network call.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_check_schema_drift.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_check_schema_drift.py -x -q` passes with at least 10 tests collected.
    - `.venv/bin/python -c "src=open('tests/test_check_schema_drift.py').read();assert 'requests' not in src and 'HUBSPOT_PRIVATE_APP_TOKEN' not in src, 'offline test tier must not touch network or credentials';print('offline confirmed')"` passes.
    - A test named for the component-score scope guard asserts all five `*_score` names are in `D04_COMPANY_PROPERTY_SCOPE`.
    - A test asserts `exit_code_for` returns 2 for a report whose properties are all `in_sync` but whose `do_not_archive.ok` is false.
    - A test asserts `exit_code_for` returns 0 for a report whose only non-`in_sync` statuses are `cosmetic_only` and `documented_gap`.
  </acceptance_criteria>
  <done>Every branch of the classifier and both non-zero exit codes are pinned by an offline test that runs in under ten seconds.</done>
</task>

<task type="auto">
  <name>Task 3: Full offline suite green, no regression against the 2362-test baseline</name>
  <files>(no files modified — full offline suite run and baseline recording only)</files>
  <read_first>
    - `tests/test_hubspot_schema_coverage.py` lines 1-45 — the pre-existing coverage guard whose oracle is the committed baseline snapshot union the undo manifests; adding a new snapshot label to `config/hubspot_migration/baseline/` changes what that oracle contains, so this file must be re-run and understood, not assumed unaffected.
    - `tests/test_flow_rubric_conformance.py` lines 350-365 — the existing Phase 40 guard naming the five component score properties; the do-not-archive set must stay consistent with it.
  </read_first>
  <action>
Run the full offline pytest tier and confirm the new snapshot files and the new script have not
disturbed any existing test.

The one file with a real chance of reacting is `tests/test_hubspot_schema_coverage.py`: its
oracle is the union of every committed baseline snapshot plus every undo manifest, so adding a
`phase42-pre` snapshot pair widens that union. Widening it can only make previously-referenced
properties MORE covered, never less, so the expected outcome is that the suite stays green — but
confirm that empirically rather than reasoning about it, and if anything goes red, report the
failure rather than adjusting the test.

Do not run `scripts/build_cloud_workflows.py` or `scripts/deploy_n8n_workflows.py` at any point.
Some tests import constants from `build_cloud_workflows` as a module, which is a pure import
and is fine; invoking the builder's generation path is what rebakes the deployed workflow
write-safety constants and would close Phase 41's arm window.

Record the passing test count in the plan summary alongside the 2362 baseline so any later
regression is measurable rather than argued.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` reports zero failures and zero errors.
    - The passing count is greater than or equal to 2362 plus the number of tests added in Task 2.
    - `.venv/bin/python -m pytest tests/test_hubspot_schema_coverage.py tests/test_flow_rubric_conformance.py -x -q` passes.
    - `git status --porcelain n8n/` shows no modifications — no workflow content was regenerated.
  </acceptance_criteria>
  <done>The full offline suite is green and the count is recorded; nothing under `n8n/` changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude/script → HubSpot portal 22617666 | Every call in this plan crosses it; the portal is live production CRM schema. This plan crosses it with GET only. |
| HubSpot API response → committed repo file | Portal response bodies are written to disk and committed to git; a leaked credential would become permanent history. |
| `.env` → operator shell | Credentials are never read by Claude; they enter the process only via `load_dotenv()` inside an operator-pasted command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of Service (of the scoring engine) | `scripts/check_schema_drift.py` do-not-archive constants | high | mitigate | The eleven property names and six flow ids are a module constant checked live on every run; `exit_code_for` returns a dedicated exit 2 when any is absent or disabled, distinct from ordinary drift. Task 1's automated verify asserts the invariant directly against the committed report. |
| T-42-02 | Tampering (silent) | `classify_property` scope selection | high | mitigate | Scope is the explicit `D04_COMPANY_PROPERTY_SCOPE` name list, never a namespace-prefix regex. Task 2 pins all five non-prefixed `*_score` names as in-scope; a prefix regex would silently exclude them and report a false in-sync. |
| T-42-03 | Information Disclosure | Report and snapshot files written to git | high | mitigate | `_assert_no_secrets()` is applied to the serialized report text before write, copied verbatim from `scripts/snapshot_hubspot_schema.py:78-83`; it fails on the bearer token value, the `Authorization` header, and the token env var name. |
| T-42-04 | Tampering | Portal misidentification | medium | mitigate | `_portal_ok()` asserts `HUBSPOT_PORTAL_ID == 22617666` and refuses pre-call, exiting 1 without issuing a request — the same guard every schema-touching script in this repo already carries. |
| T-42-05 | Spoofing/Elevation | Unintended write via the drift tool | medium | mitigate | The script has no write path at all; Task 1's acceptance criteria greps the source for any non-GET `requests` verb and fails if one is present. |
| T-42-06 | Denial of Service (of Phase 41) | n8n workflow content rebake | high | mitigate | This plan never invokes `build_cloud_workflows.py` or `deploy_n8n_workflows.py`; Task 3 asserts `git status --porcelain n8n/` is clean. |

No package-manager installs occur in this plan, so no supply-chain row applies.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` green, count at or above the 2362 baseline plus new tests.
- The committed `drift-report-phase42-pre.json` has `do_not_archive.ok == true`.
- The committed snapshot pair exists under `config/hubspot_migration/baseline/` with the
  `phase42-pre` label and the pre-existing filename pattern.
- `git status --porcelain n8n/` is empty.
</verification>

<success_criteria>
- `scripts/check_schema_drift.py` runs read-only against the live portal and emits a JSON report
  with a meaningful exit code (0 clean / 1 drift / 2 engine invariant violated).
- D-01's do-not-archive set exists as executable code and is live-verified green before any
  later plan in this phase is permitted to mutate anything.
- The live-existence question for the five design-only D-04 names is answered from a fresh GET
  and recorded, so plan 02 never has to guess (F2).
- The `lv_icp_tier` label divergence is reported as accepted, not treated as a defect (F5).
</success_criteria>

<output>
Create `.planning/phases/42-scoring-artifact-cleanup-reconciliation/42-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (plan 01)

**Files**
- `scripts/check_schema_drift.py`
- `tests/test_check_schema_drift.py`
- `config/hubspot_migration/baseline/portal-schema-companies-phase42-pre.json`
- `config/hubspot_migration/baseline/portal-schema-contacts-phase42-pre.json`
- `.planning/phases/42-scoring-artifact-cleanup-reconciliation/drift-report-phase42-pre.json`

**Symbols in `scripts/check_schema_drift.py`**
- `EXPECTED_PORTAL_ID`
- `DO_NOT_ARCHIVE_COMPANY_PROPERTIES` (frozenset, 11 names)
- `DO_NOT_ARCHIVE_FLOW_IDS` (dict, 6 flow id → slug)
- `D04_COMPANY_PROPERTY_SCOPE` (15 names)
- `ACCEPTED_DIVERGENCES` (seeded with id `PARITY-01-tier-label`)
- `_has_credentials()`, `_portal_ok()`, `_assert_no_secrets()`
- `_get_live_properties(object_type)`, `_get_live_flows()`
- `classify_property(name, declared, live)` → one of `in_sync`, `enum_mismatch`, `type_mismatch`,
  `cosmetic_only`, `missing_from_yaml`, `fabricated_entry`, `documented_gap`
- `build_report(...)`, `exit_code_for(report)`, `main(argv)`

**CLI**
- `scripts/check_schema_drift.py --out PATH` — exit 0 clean, 1 drift, 2 do-not-archive invariant
  violated

**Snapshot labels**
- `phase42-pre` (this plan); `phase42-post` is produced by plan 03
