---
phase: 59-frictionless-write-path
plan: 06
type: execute
wave: 5
depends_on: ["59-05"]
files_modified:
  - operator-claude-plugin/scripts/enrichment.py
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/tests/test_enrichment_envelope.py
  - operator-claude-plugin/tests/test_write_grant.py
  - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
autonomous: true
requirements: [D-59-08]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-59-08: every gate the 59-GATE-INVENTORY marks CONVERT and assigns to this plan carries machine-readable resolution guidance naming which legitimate source could supply the missing value — the refusal is no longer a dead end."
    - "D-59-08: the walk's FINDING 1 dead end (a create with no id and no domain cannot be granted) is resolvable — the skill attempts a read-only HubSpot lookup and proposes the scope before the grant is planned."
    - "The authorization control itself is UNCHANGED: plan_grant still hard-refuses an empty record set, _writeSafetyAllows is untouched, and no resolution ever widens what a grant covers."
    - "D-59-08: resolution sources stay the single closed vocabulary 59-05 introduced — this plan extends one vocabulary, it does not create a second."
    - "The inventory is closed out: every CONVERT row has a real owner, and anything still unconverted is named under Unplanned items and reported."
  artifacts:
    - "enrichment.RecordSpecError.resolvable"
    - "the closed-out 59-GATE-INVENTORY.md"
  key_links:
    - "enrichment.RecordSpecError -> a machine-readable resolvable payload, mirroring ExtractionError's existing code-alongside-message shape"
    - "write_grant.plan_grant's empty-record-set refusal -> a refusal message naming the read-only lookup that resolves it, with the lookup itself performed by the skill, never by plan_grant"
    - "enrich-records/SKILL.md -> extraction.RESOLUTION_SOURCES, the same vocabulary 59-05 defined"
---

<objective>
D-59-08, second half: convert the remaining gates the inventory marks CONVERT — the enrichment
lane's identity refusals and the grant lane's empty-record-set dead end — from refuse-and-stop
into refuse-with-a-named-resolution-path, and close the inventory out.

Purpose: the operator's ruling is cross-cutting, and 59-05 converted only the lane it was taken
at. The two dead ends the Phase 53 walk actually hit were `extraction.py`'s identity gate
(59-05) and `plan_grant`'s empty record set (FINDING 1, this plan). Leaving the second one
unconverted would mean the walk's own blocking case still dead-ends after a phase named for
removing friction from the write path.

Output: machine-readable resolution guidance on the enrichment lane's refusals, a grant-lane
resolution flow that leaves the authorization control untouched, and a closed inventory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-frictionless-write-path/59-CONTEXT.md
@.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md
@.planning/phases/59-frictionless-write-path/59-05-SUMMARY.md
@.planning/phases/53-operator-openable-write-grant/53-WALK-RECORD.md
@operator-claude-plugin/scripts/enrichment.py
</context>

<planner_assumptions>
1. **THE SAFETY LINE THAT DOES NOT MOVE IN THIS PLAN.** `write_grant.plan_grant`'s
   empty-record-set refusal is CORRECT and stays a hard refusal — FINDING 1 of the walk records
   it as *"predicted, and correct behaviour"*, and its own message explains why: a grant over
   nothing reads as success while granting nothing. `_writeSafetyAllows()` and the write-safety
   gate nodes are on this phase's explicitly-untouched list. **The conversion is therefore
   entirely in front of the refusal, not inside it:** the refusal message names what would
   resolve it, and the SKILL performs the read-only lookup and proposes the scope BEFORE
   calling `plan_grant`. The walk itself did exactly this by hand — a read-only HubSpot search
   found the company's domain, which was the handle the allowlist needed — so this is codifying
   a proven manual step, not inventing a mechanism.
2. **PLANNER DECISION — `RecordSpecError` carries a machine-readable `resolvable` payload
   alongside its operator prose, following `ExtractionError`'s existing shape.**
   `ExtractionError.__init__(code, message)` already sets the precedent for an exception in
   this codebase carrying structured data next to the sentence the operator reads
   (`extraction.py:81-89`). `RecordSpecError` gets the same treatment rather than a new
   result-object refactor of `build_envelope`, which would touch every caller for no safety
   gain. Reversibility: `reversible` — an added optional attribute with a default.
3. **PLANNER DECISION — one vocabulary, not two.** `enrichment.py` imports and reuses
   `extraction.RESOLUTION_SOURCES` rather than defining its own. Two closed sets that must stay
   equal is the copy-of-one-contract bug `chunking.py`'s own module docstring warns about
   (fact 1: the ceiling is declared twice already and a third copy is forbidden). If a genuine
   import cycle blocks this, move the constant to a small shared module and update both — never
   duplicate the literal.
4. **`enrichment.py`'s companies branch is ALREADY PARTLY CONVERTED**, by an operator ruling
   taken during the same Phase 53 walk (the in-file comment at `enrichment.py:357-363`, dated
   2026-08-25: a company with no usable domain is accepted when it has a name, because the
   guard is "never silently invent a domain", not "go and find one yourself"). This plan
   extends that direction; it does not reverse or re-litigate it. The verbatim-pinned refusal
   at `enrichment.py:368-373` stays verbatim — its own comment says it is pinned by not being
   reworded.
5. **Spec-less probe fallback did not run** (no requirement IDs for this phase). Assumption-delta
   scan returned `detected: false`.
6. **If the inventory assigns this plan a gate not anticipated below, convert it here.** The
   task list names the three the seed identified; the inventory is authoritative. A gate the
   inventory assigns to this plan that turns out to need more than this plan's budget is
   reported to the orchestrator as an unplanned item, never silently reclassified.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The enrichment lane's identity refusals name what would resolve them</name>
  <files>operator-claude-plugin/scripts/enrichment.py, operator-claude-plugin/tests/test_enrichment_envelope.py</files>
  <read_first>
    - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md — the authoritative list of which `enrichment.py` sites this plan owns and what each may resolve from
    - operator-claude-plugin/scripts/enrichment.py lines 140-165 and 220-420 — every `RecordSpecError` site, especially the people branch's identity check around lines 303-318 and the companies branch's no-name-no-domain refusals around lines 355-389
    - operator-claude-plugin/scripts/enrichment.py lines 355-363 — the in-file comment recording the 2026-08-25 operator ruling that already partly converted this branch; the new work must extend it, not reverse it
    - operator-claude-plugin/scripts/extraction.py — `ExtractionError.__init__(code, message)` (the exception-carries-structured-data precedent) and `RESOLUTION_SOURCES` (the closed vocabulary, defined by 59-05)
    - operator-claude-plugin/tests/test_enrichment_envelope.py IN FULL — several tests assert on exact refusal MESSAGES; adding an attribute must not change any of those strings
    - operator-claude-plugin/tests/test_company_research_envelope.py and test_rows_envelope_contract.py — other pins on this module's refusals
  </read_first>
  <behavior>
    - `RecordSpecError` raised for a person with a first name only carries `resolvable` naming the missing identity options (company, email, LinkedIn URL) and, per option, which legitimate source could supply it
    - `RecordSpecError` raised for a company row with neither a name nor a usable domain carries `resolvable` naming what could supply a name
    - Every existing refusal MESSAGE string is unchanged — the attribute is additive
    - `RecordSpecError("just a message")` still constructs and still raises, so no existing raise site or caller breaks
    - `resolvable` defaults to an empty tuple, never None, so a caller can iterate it unconditionally
    - Every `source` value appearing in any `resolvable` payload is a member of the closed vocabulary 59-05 defined
  </behavior>
  <action>
Give `enrichment.RecordSpecError` a machine-readable resolution payload and populate it at the
sites the inventory assigns to this plan.

**The exception.** Extend `RecordSpecError` with an optional keyword-only `resolvable`
parameter defaulting to an empty tuple, stored as an attribute, following
`ExtractionError.__init__(code, message)`'s precedent of structured data alongside the sentence
the operator reads. Positional construction from a bare message MUST keep working — every
existing raise site passes one string, and this change touches none of them structurally.
Document the payload shape in the class docstring: a tuple of dicts, each
`{"field": str, "sources": (str, ...), "detail": str}`, where `field` names what is missing,
`sources` are members of the closed resolution vocabulary, and `detail` says in operator words
what the lookup would be.

**The vocabulary.** Import `RESOLUTION_SOURCES` from `extraction` and validate every `sources`
entry against it at construction — an unrecognised source raises rather than being carried, the
same anti-laundering rule 59-05 enforces on the extraction side. Do NOT redefine the set here;
two closed sets that must stay equal is the copy-of-one-contract bug `chunking.py`'s module
docstring warns about at length. If importing `extraction` from `enrichment` creates a cycle,
move the constant into a small shared module and update both importers in this same commit —
never duplicate the literal.

**The sites.** Populate `resolvable` at the sites the inventory assigns to this plan. At
minimum, and verifying each against the inventory before changing it:

- The people-branch identity refusal (around `enrichment.py:312-318`). Its message already
  names the three things that would resolve it in prose; the payload makes the same three
  machine-readable, each with the sources that could supply it — a read-only HubSpot lookup and
  an earlier operator statement for the company, a provider result or a HubSpot lookup for the
  email, a same-row derivation for a LinkedIn URL already present under another key.
- The companies-branch refusals for a row with neither a usable domain nor a name (around
  `enrichment.py:374-389`). The payload names `name` as the missing field and, where the row
  carried a profile-page URL, a same-row derivation as a candidate source — a profile URL's own
  slug is a proposable company name, never an auto-filled one.
- **The verbatim-pinned refusal at `enrichment.py:368-373` keeps its message EXACTLY as it is.**
  Its own in-file comment states it is pinned by not being reworded. Adding a `resolvable`
  payload to that raise is permitted; changing one character of its message string is not.

**What must not change.** No refusal becomes an acceptance. `build_envelope` still raises for
every input it raises for today, with the same message. This task makes the refusal
ACTIONABLE, it does not make it conditional — the change is refuse-with-a-path, and the skill
in Task 2 is what turns a path into a proposal.

**Tests.** Extend `operator-claude-plugin/tests/test_enrichment_envelope.py` with one test per
`<behavior>` bullet. Include a regression test constructing `RecordSpecError` positionally from
a bare message and asserting `resolvable` is an empty tuple — that is the compatibility
guarantee every untouched raise site depends on. Include a test asserting a `sources` entry
outside the closed vocabulary raises at construction.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrichment_envelope.py operator-claude-plugin/tests/test_company_research_envelope.py operator-claude-plugin/tests/test_rows_envelope_contract.py operator-claude-plugin/tests/test_list_envelope_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolvable" operator-claude-plugin/scripts/enrichment.py` is at least 3
    - `grep -c "RESOLUTION_SOURCES" operator-claude-plugin/scripts/enrichment.py` is at least 1
    - `grep -c "class RecordSpecError" operator-claude-plugin/scripts/enrichment.py` is exactly 1
    - `grep -c "is a profile page rather than a company" operator-claude-plugin/scripts/enrichment.py` is at least 1 — the verbatim-pinned refusal message survives unchanged
    - A test exists constructing `RecordSpecError` from a bare positional message and asserting `resolvable == ()`
    - A test exists asserting a `sources` value outside the closed vocabulary raises at construction
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
  </acceptance_criteria>
  <done>Every enrichment-lane refusal this plan owns tells the caller which legitimate source could supply the missing value, with no refusal weakened and no message reworded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The grant lane's empty record set stops being a dead end — without touching the control</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - .planning/phases/53-operator-openable-write-grant/53-WALK-RECORD.md — FINDING 1 in full, including how the walk resolved it by hand (a read-only HubSpot search that found the company's domain, which was the exact handle the allowlist needed for a create)
    - operator-claude-plugin/scripts/write_grant.py lines 377-450 — `plan_grant`'s refusal ladder in order, and the empty-record-set refusal at lines 423-428
    - operator-claude-plugin/scripts/write_grant.py — `authorize_ungranted_send`'s equivalent refusal, which FINDING 1 records as refusing for the same reason on the other path; both must end up consistent
    - operator-claude-plugin/skills/enrich-records/SKILL.md — the grant-opening flow this task adds a step in front of, read enough of it to place the new step correctly rather than appending it at the end
    - operator-claude-plugin/scripts/extraction.py — `RESOLUTION_SOURCES`, the vocabulary the skill's new step must name
    - operator-claude-plugin/tests/test_write_grant.py — the tests around the empty-record-set refusal, so the refusal's own assertions stay green
  </read_first>
  <behavior>
    - `plan_grant` with an empty record set still returns a refusal — unchanged outcome
    - That refusal's detail now also names what would resolve it: a read-only HubSpot lookup for the record's own id, or for its company's domain
    - `_writeSafetyAllows` semantics and the record-scoped allowlist are untouched — a resolved handle narrows what is granted, it never widens it
    - `enrich-records/SKILL.md` instructs Claude to attempt the read-only lookup and PROPOSE the resulting scope before calling `plan_grant`, and to state which source the handle came from
    - A proposed handle the operator declines results in the original refusal, not in a grant
  </behavior>
  <action>
Convert the walk's FINDING 1 dead end into a resolve-then-propose flow, entirely in front of
the authorization control.

**`write_grant.py` — message only, control untouched.**

Extend the empty-record-set refusal's detail (lines 423-428) with a second sentence naming what
would resolve it: a read-only HubSpot lookup for the record's own object id, or — for a record
that does not exist yet and therefore has no id — for its company's domain, which is the handle
the allowlist can express a create with. Keep the existing explanation of WHY an empty grant is
refused verbatim; it is the reason the refusal is correct and it must still be the first thing
read. Add a dated comment naming D-59-08 and 2026-08-28, stating that the refusal itself is
deliberately unchanged and that the resolution happens in the skill, before the call.

Make `authorize_ungranted_send`'s equivalent refusal consistent with it. FINDING 1 records both
paths refusing for the same reason; a resolution path documented on one and not the other is
the kind of asymmetry that makes an operator think the other path is broken.

**DO NOT** add a HubSpot lookup, a transport call, or any resolution logic to `write_grant.py`.
`plan_grant` is an authorization boundary; giving it a lookup that can change what it grants is
exactly the widening this phase's scope pins as untouched. Its `preflight` guardrail and its
refusal ladder stay as they are.

**`enrich-records/SKILL.md` — the resolve-then-propose step.**

Add a step BEFORE the grant is planned, placed where the flow actually reaches the grant rather
than appended at the end of the file. It instructs Claude to:

- Recognise the case: the operator has named a record the grant cannot be scoped to, because it
  has neither a HubSpot object id nor a usable domain.
- Attempt resolution from the legitimate sources ONLY, naming them by the same identifiers
  59-05 put in `RESOLUTION_SOURCES`: a read-only HubSpot search, the operator's own earlier
  statements, a provider result already in hand, or a stated derivation from another field of
  the same row.
- State explicitly what is still forbidden: Claude's own recall about the company, an inferred
  domain from a company name, a plausible corporate email pattern, and anything the operator
  has no way to check. Say plainly that the change is from refusing to proposing, never from
  refusing to guessing.
- PROPOSE the resolved handle and the scope it produces, naming which source it came from, and
  wait for the operator to confirm. A declined proposal means the original refusal stands.
- Only then call `plan_grant` with the confirmed handle, and note that the resulting grant is
  narrower than or equal to what the operator named — never wider. The per-send narrowing rule
  the walk verified (*"narrower than the grant, never wider"*) is unchanged by this step.
- Reuse the confirm/correct/decline vocabulary `company_domain.py` already established rather
  than inventing a third set of words for the same interaction.

**Tests.** Extend `operator-claude-plugin/tests/test_write_grant.py`: assert the empty-record-set
refusal is still a refusal with its original explanation intact, AND that its detail now names
the read-only lookup. Assert the same for `authorize_ungranted_send`'s equivalent. Add a
structural test asserting `write_grant.py` contains no HubSpot search call added by this task —
a grep-style assertion over the module source for the search endpoint, so a later edit cannot
move the lookup inside the authorization boundary without failing a test.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_write_grant_surface.py operator-claude-plugin/tests/test_write_grant_guardrails.py operator-claude-plugin/tests/test_enrich_skill_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refusing to plan a grant over an empty record set" operator-claude-plugin/scripts/write_grant.py` is at least 1 — the original refusal wording survives
    - `grep -c "D-59-08" operator-claude-plugin/scripts/write_grant.py` is at least 1
    - `grep -c "read-only" operator-claude-plugin/scripts/write_grant.py` is at least 1
    - `grep -c "D-59-08" operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1
    - `grep -c "hubspot_lookup" operator-claude-plugin/skills/enrich-records/SKILL.md` is at least 1
    - A test asserts the empty-record-set outcome is still a refusal
    - A structural test asserts no HubSpot search call was added to `write_grant.py`
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
  </acceptance_criteria>
  <done>The walk's FINDING 1 case is resolvable by a proposed, operator-confirmed read-only lookup; the refusal and the authorization boundary are byte-for-byte the controls they were.</done>
</task>

<task type="auto">
  <name>Task 3: Close the inventory out, then bump and log</name>
  <files>.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md (as written by 59-05 Task 1)
    - .planning/phases/59-frictionless-write-path/59-05-SUMMARY.md — what 59-05 actually converted, which may differ from what its plan anticipated
    - operator-claude-plugin/.claude-plugin/plugin.json (version will be 0.24.0 after 59-05)
    - operator-claude-plugin/CHANGELOG.md (top ~60 lines — match the existing entry format)
  </read_first>
  <action>
Update `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` so its `owner` column
reflects what was actually converted, not what was planned. For each row: mark it converted with
the plan and task that did it, or move it under `Unplanned items` with a plain statement of what
remains.

Then take one honest pass over the whole table. If a gate is still `CONVERT` and still
unconverted after 59-05 and 59-06, it belongs under `Unplanned items` and in this plan's
SUMMARY as an item this phase did not cover — reported to the orchestrator rather than
absorbed. Do NOT reclassify it to `NOT-APPLICABLE` to close the table; the only valid reason for
that decision remains that no legitimate resolution source exists, and a gate's inconvenience is
not that reason. An inventory that honestly lists two unconverted gates is a better artifact
than one that closes cleanly by redefining them.

Then set `version` in `operator-claude-plugin/.claude-plugin/plugin.json` to `0.25.0` and add a
matching CHANGELOG entry, same commit.

The entry names: D-59-08's second half; the `resolvable` payload on `RecordSpecError`; the
grant-lane resolve-then-propose step and — stated plainly, because this is the sentence a
security reviewer will look for — that `plan_grant` still hard-refuses an empty record set,
`_writeSafetyAllows` is untouched, and no resolution ever widens what a grant covers. Name the
single shared resolution vocabulary and say that this release extended it rather than adding a
second. Point at the inventory for anything still unconverted, by name.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - Every row in `59-GATE-INVENTORY.md` whose decision is `CONVERT` names either a converting plan/task or appears under `Unplanned items`
    - `grep -cE "too (hard|difficult|complex)|not worth|low value" .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` is still 0
    - `grep '"version"' operator-claude-plugin/.claude-plugin/plugin.json` contains `0.25.0`
    - `grep -c "0.25.0" operator-claude-plugin/CHANGELOG.md` is at least 1
    - `grep -c "D-59-08" operator-claude-plugin/CHANGELOG.md` is at least 2 (59-05's entry and this one)
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
    - `.venv/bin/python -m pytest -q` exits 0
    - The SUMMARY lists any still-unconverted CONVERT gate by name
  </acceptance_criteria>
  <done>The inventory tells the truth about what this phase converted and what it did not; 0.25.0 ships saying explicitly that the authorization control did not move.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-named records → `plan_grant` | The authorization boundary; what crosses it determines what the backend is allowed to write |
| Claude-resolved handle → the grant's record scope | A resolved value here decides which HubSpot records become writable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-26 | Elevation of Privilege | a resolution WIDENING a grant — a guessed domain making records writable that the operator never named | high | mitigate | `plan_grant`'s empty-record-set refusal is unchanged and still hard-refuses; no lookup, transport call or resolution logic is added to `write_grant.py`, pinned by a structural test asserting no HubSpot search call exists in that module. The resolution happens in the skill, is PROPOSED, and requires operator confirmation before the handle reaches `plan_grant`. The existing per-send narrowing rule (narrower than the grant, never wider) is untouched. |
| T-59-27 | Spoofing | a plausible-but-wrong domain resolving to the wrong company, arming writes on someone else's records | high | mitigate | Legitimate sources are the closed vocabulary only — a read-only HubSpot search returns a real record or nothing. Inferring a domain from a company name is on D-59-08's illegitimate list and the skill states that prohibition explicitly. The handle is proposed with its source named, so the operator can check it against the record they meant. A declined proposal leaves the original refusal standing. |
| T-59-28 | Repudiation | a Claude-resolved handle presented as operator-supplied | high | mitigate | Every `sources` value is validated against the single closed `RESOLUTION_SOURCES` set at construction — an unrecognised source raises rather than being carried unlabelled. The skill requires the source to be named when the scope is proposed. One vocabulary, shared by import, so the two lanes cannot drift into disagreeing about what counts as legitimate. |
| T-59-29 | Tampering | a refusal message reworded, weakening a pinned explanation | medium | mitigate | The acceptance criteria grep for the verbatim survival of both pinned strings (`enrichment.py`'s profile-page refusal and `write_grant.py`'s empty-record-set explanation). Additions are permitted; rewordings are pinned out. |
| T-59-30 | Denial of Service | the inventory closed by reclassification rather than by conversion | medium | mitigate | Task 3 forbids reclassifying a `CONVERT` row to `NOT-APPLICABLE` to close the table, the difficulty-dismissal grep must stay at 0, and any still-unconverted gate must be named in the SUMMARY and reported to the orchestrator. |
| T-59-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan. `59-RESEARCH.md` § Package Legitimacy Audit records "not applicable — this phase installs no external packages". |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths this PLAN creates (new — not pre-existing surface):

- `enrichment.RecordSpecError.resolvable` (new optional attribute, default empty tuple)
  - payload entry keys: `field`, `sources`, `detail`
- a new resolve-then-propose step in `operator-claude-plugin/skills/enrich-records/SKILL.md`
- new structural test in `operator-claude-plugin/tests/test_write_grant.py` asserting no HubSpot
  search call exists in `write_grant.py`
- the closed-out `Unplanned items` section of
  `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md`
- plugin version `0.25.0`

`extraction.RESOLUTION_SOURCES` is NOT new here — it is 59-05's artifact, imported and reused.
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — full plugin suite green
- `.venv/bin/python -m pytest -q` — full root suite green
- No n8n code touched, so `node --test tests/n8n/*.test.mjs` is not required by this plan
- Live verification (an actual grant opened over a resolved handle) is NOT performed: the
  operator ruling of 2026-08-28 puts the Phase 53 walk out of this phase, code only. The
  SUMMARY must say so rather than implying the flow was exercised end to end.
</verification>

<success_criteria>
- Every gate the inventory assigns to this plan carries machine-readable resolution guidance
- The walk's FINDING 1 case is resolvable through a proposed, operator-confirmed read-only
  lookup performed by the skill
- `plan_grant` still hard-refuses an empty record set; `write_grant.py` gained no lookup, pinned
  structurally
- One shared resolution vocabulary, extended rather than duplicated
- The inventory honestly records anything this phase did not convert
- Plugin version 0.25.0
</success_criteria>

<output>
Create `.planning/phases/59-frictionless-write-path/59-06-SUMMARY.md` when done
</output>
