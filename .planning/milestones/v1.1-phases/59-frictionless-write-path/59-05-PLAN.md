---
phase: 59-frictionless-write-path
plan: 05
type: execute
wave: 4
depends_on: ["59-04"]
files_modified:
  - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md
  - operator-claude-plugin/scripts/extraction.py
  - operator-claude-plugin/scripts/preview.py
  - operator-claude-plugin/skills/contact-upload/extraction.md
  - operator-claude-plugin/skills/contact-upload/SKILL.md
  - operator-claude-plugin/tests/test_no_invention_structural.py
  - operator-claude-plugin/tests/test_extraction_resolvable.py
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/CHANGELOG.md
autonomous: true
requirements: [D-59-08]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-59-08: every operator-facing refuse-and-stop gate across the ingest, enrichment and preingest lanes is inventoried in one document, each carrying a CONVERT or NOT-APPLICABLE decision with a stated reason."
    - "D-59-08: a contact or company row that fails the identity gate is reported as RESOLVABLE with the specific missing fields, instead of dead-ending as a bare rejection."
    - "D-59-08: the change is refuse -> propose, never refuse -> guess — no Python function anywhere in extraction.py fills a value into a row, and the structural test that guarantees this is EXTENDED, never relaxed."
    - "D-59-08: a Claude-resolved value carries provenance saying so, from a CLOSED vocabulary of legitimate sources; an unrecognised source rejects the record rather than being accepted as unlabelled."
    - "The no-invention rule's core sentence survives verbatim in both passages of extraction.md."
    - "D-07's contradiction check is unchanged: a field named by an ambiguity that nonetheless carries a value still rejects, resolved or not."
  artifacts:
    - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md
    - "extraction.ExtractionResult.resolvable"
    - "extraction.RESOLUTION_SOURCES"
    - operator-claude-plugin/tests/test_extraction_resolvable.py
  key_links:
    - "extraction.validate() identity-gate branch (extraction.py:531-540) -> the new resolvable list, ADDITIVE to rejected, never replacing it"
    - "a record's optional `resolutions` key -> validated against RESOLUTION_SOURCES -> carried into accepted so provenance cannot be laundered"
    - "preview.build_extraction_preview -> the resolvable group, so the operator actually sees what can be proposed"
    - "test_no_invention_structural.py's forbidden-substring list -> extended to cover the new surface"
---

<objective>
D-59-08, first half: inventory every operator-facing refuse-and-stop gate across the ingest,
enrichment and preingest lanes, and convert the one the operator's ruling was actually taken at
— `extraction.py`'s identity gate — from refuse-and-stop into resolve-then-propose.

Purpose: the operator's ruling, given during the Phase 53 walk after a LinkedIn-sourced row
dead-ended: *"instead of immediate refusal, Claude operator side should try to resolve and
propose. The goal of this system is to guide and be assistive, not just deterministic."* The
safety line that must not move is `refuse` -> `propose`, never `refuse` -> `guess`.

Output: a gate inventory document that drives 59-06, plus the extraction-lane conversion with
its provenance control and its extended structural guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-frictionless-write-path/59-CONTEXT.md
@.planning/phases/59-frictionless-write-path/59-RESEARCH.md
@.planning/phases/59-frictionless-write-path/59-PATTERNS.md
@operator-claude-plugin/scripts/extraction.py
@operator-claude-plugin/scripts/company_domain.py
</context>

<planner_assumptions>
1. **THE LOAD-BEARING ARCHITECTURAL FINDING, and the reason this conversion is CLASSIFICATION
   rather than FILLING.** `extraction.py`'s module docstring (lines 56-60) states there is no
   function anywhere in that module that applies or resolves an ambiguity, because the
   correction path does not need one — the operator answers in chat, Claude rewrites the
   artifact, and `validate()` runs again. `test_no_invention_structural.py`'s
   `test_no_ambiguity_resolution_or_application_function_exists_in_extraction_module` enforces
   that structurally by forbidding function names matching `resolve_ambig`, `apply_ambig`,
   `confirm_ambig` and `fill_ambig`. **D-59-08 must therefore be implemented WITHIN that
   architecture, not against it:** `validate()` gains a classification of which rejections have
   a legitimate resolution path, and the resolution itself happens exactly where every other
   correction already happens — Claude rewrites the artifact and `validate()` runs again. No
   Python function ever enters a value into a row. This is not a workaround; it is the same
   property D-59-08 says must survive ("never silently fill a gap to get a row past a gate"),
   already enforced structurally by a test that predates the ruling.
2. **PLANNER DECISION — `resolvable` is ADDITIVE to `rejected`, never a replacement.** A row
   that fails the identity gate stays in `rejected` exactly as it does today AND is also
   listed in a new `resolvable` group. Moving it out of `rejected` would silently change the
   behaviour of every existing reader (`preview.py:206`, `extraction.py:777`'s CLI) from "do
   not dispatch this" to "dispatch this", which is a safety regression dressed as a feature.
   The additive shape means nothing that reads `rejected` changes at all.
3. **PLANNER REVERSIBILITY RATING — `costly`.** Amending the no-invention rule is a safety
   contract with recorded-edit discipline, and `resolvable` / `resolutions` / `RESOLUTION_SOURCES`
   become a schema other lanes will read. Reverting means another recorded edit and a schema
   removal. It is not `one-way`: no migration, no persisted data, no published external
   contract. No `checkpoint:decision` is inserted — but the edit MUST be recorded in-file with
   its reason and date, which is the discipline D-59-08 explicitly names.
4. **The inventory is seeded, not invented from scratch.** Research located the refusal sites;
   Task 1's job is to decide each one, not to rediscover them. If Task 1 finds a gate not on
   the seed list, it is added with a decision — and if that decision is CONVERT and no task in
   59-05 or 59-06 covers it, that is reported to the orchestrator as an unplanned item rather
   than silently dropped.
5. **Spec-less probe fallback did not run** (no requirement IDs for this phase). Assumption-delta
   scan returned `detected: false`, so no `<assumption_delta_decision>` block is emitted.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The gate inventory — every operator-facing refuse-and-stop, decided</name>
  <files>.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/59-frictionless-write-path/59-CONTEXT.md § D-59-08 IN FULL, especially the legitimate/illegitimate resolution-source table and the provenance requirement
    - operator-claude-plugin/scripts/extraction.py lines 470-560 (the per-record pre-flight and its identity-gate rejection)
    - operator-claude-plugin/scripts/enrichment.py — every `raise RecordSpecError` site (lines 152, 232, 250, 258, 289, 296, 314, 344, 352, 368, 377, 385, 406, 417), reading enough surrounding context to say what each refuses and what could legitimately resolve it
    - operator-claude-plugin/scripts/preingest.py lines 60-90 (`RowSpecError`) and lines 260-290 (`ClassifyError`), plus `classify_matches`/`resolve_proposed` and the `proposed`/`auto_matched`/`unmatched`/`unchecked` grouping
    - operator-claude-plugin/scripts/chunking.py lines 105-225 (`chunk_ceiling`, `plan_chunks` and every `ChunkPlanError`)
    - operator-claude-plugin/scripts/write_grant.py around line 425 (the empty-record-set refusal — this is FINDING 1 of the Phase 53 walk, the other dead end the ruling came out of)
    - operator-claude-plugin/scripts/company_domain.py IN FULL — the already-converted precedent; its confirm/correct/decline vocabulary is the shape everything else should reach for
    - operator-claude-plugin/scripts/config_gate.py lines 118-140 (`ConfigError`) — the canonical NOT-APPLICABLE case
    - operator-claude-plugin/scripts/name_split.py and header_suggest.py — their error classes, to decide them
  </read_first>
  <action>
Write `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md`: one table row per
operator-facing gate that today refuses and stops, across the ingest, enrichment and preingest
lanes.

Columns: `gate id`, `site` (file:line), `lane`, `what it refuses today`, `decision`
(`CONVERT` / `ALREADY-CONVERTED` / `NOT-APPLICABLE`), `legitimate resolution source` (which
entry from D-59-08's left-hand column could supply the missing value), `owner` (which plan in
this phase converts it, or `deferred` with a named reason).

DECISION RULE, applied uniformly — this is what stops the inventory becoming a place scope
quietly shrinks:
- `CONVERT` is the DEFAULT. A gate converts unless a reason below applies.
- `NOT-APPLICABLE` requires the reason to be, specifically, **that no legitimate resolution
  source exists for what the gate refuses**. An admin-setup failure is the canonical example:
  `config_gate.ConfigError` refuses because a credentials file is missing, and no HubSpot read,
  no operator statement, no provider result and no same-row derivation can produce a webhook
  secret. Difficulty, rarity, effort and perceived payoff are NOT reasons — the planner has no
  authority to judge a feature too demanding to build, and neither does the executor. Only the
  absence of a resolution source counts.
- `ALREADY-CONVERTED` is for `company_domain.py`, which is Phase 58's confirm/correct/decline
  lane and is the precedent everything else reuses. Record it so the inventory reads as a
  complete picture rather than a to-do list.
- A gate that refuses because its INPUT IS MALFORMED (a record that is not an object, a `rows`
  value that is not a list) is `NOT-APPLICABLE` under the same rule: there is nothing to look
  up, only a caller bug to fix. Say that explicitly per row rather than leaving it implied.

SEED — start from these, verify each against the source before deciding it, and add anything
the reads above turn up that is not listed:

| lane | site | starting read |
| --- | --- | --- |
| ingest | `extraction.py` identity gate, ~line 531-540 | the ruling's own origin — CONVERT, owned by Task 2 of this plan |
| enrichment | `enrichment.py`'s `RecordSpecError` sites, especially the companies branch's domain requirement and the people branch's name/email/linkedin requirement | candidates for 59-06 |
| grant | `write_grant.py`'s empty-record-set refusal, ~line 425 | FINDING 1 of the walk; the walk itself resolved it by a read-only HubSpot search for the company's domain — candidate for 59-06 |
| preingest | `preingest.py`'s `RowSpecError` and `ClassifyError` | decide each |
| preingest | `preingest.py`'s `unmatched` group | likely ALREADY-CONVERTED in shape — it already proposes rather than refusing |
| ingest | `name_split.py`'s `NameSplitError`, `header_suggest.py`'s `HeaderSuggestError` | decide each |
| chunking | `chunking.py`'s `ChunkPlanError` sites — separate the "no ids given" refusals from the "ceiling unconfigured" refusal, they decide differently | decide each |
| config | `config_gate.ConfigError` | NOT-APPLICABLE, the reason stated as the template for every other N/A |

Close the document with two short sections:

- **Resolution sources, restated as the implementable contract.** Copy D-59-08's two-column
  table (legitimate / illegitimate) verbatim into the inventory so a later converter reads it
  in the same file as the gate list, not two documents away. Add one line naming the closed
  source vocabulary Task 2 introduces (`RESOLUTION_SOURCES`), so 59-06 extends one vocabulary
  rather than inventing a second.
- **Unplanned items.** If any gate is decided `CONVERT` and is owned by neither Task 2 of this
  plan nor 59-06, list it here under a heading that makes it findable, and report it in the
  SUMMARY as an item this phase did not cover. Do NOT quietly re-decide it as `NOT-APPLICABLE`
  to make the list close cleanly — an unconverted gate honestly recorded is the correct
  outcome; a gate reclassified to make the phase look complete is not.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md && grep -cE "CONVERT|NOT-APPLICABLE|ALREADY-CONVERTED" .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` exists
    - It contains at least 8 decided rows, each carrying a `file:line` site reference
    - Every row whose decision is `NOT-APPLICABLE` carries a reason containing the phrase `no legitimate resolution source`
    - `grep -c "config_gate" 59-GATE-INVENTORY.md` is at least 1 and `grep -c "company_domain" 59-GATE-INVENTORY.md` is at least 1
    - `grep -c "RESOLUTION_SOURCES" 59-GATE-INVENTORY.md` is at least 1
    - The document contains an `Unplanned items` heading (empty is a valid state, but the heading must exist so an empty list is a claim rather than an omission)
    - `grep -cE "too (hard|difficult|complex)|not worth|low value" .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` is 0 — no gate is dismissed on difficulty
  </acceptance_criteria>
  <done>Every operator-facing refuse-and-stop gate in the three lanes is listed with a decision and a stated reason, and anything this phase does not convert is named rather than lost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The identity gate proposes instead of dead-ending, with unlaunderable provenance</name>
  <files>operator-claude-plugin/scripts/extraction.py, operator-claude-plugin/scripts/preview.py, operator-claude-plugin/tests/test_extraction_resolvable.py, operator-claude-plugin/tests/test_no_invention_structural.py</files>
  <read_first>
    - operator-claude-plugin/tests/test_no_invention_structural.py IN FULL — a Wave 0 requirement named in 59-VALIDATION.md; its final test is the structural guarantee this task must extend rather than relax, and its other four tests pin D-07 behaviour this task must not disturb
    - operator-claude-plugin/scripts/extraction.py lines 1-160 (module docstring and the identity helpers) and lines 417-663 (`validate()` in full, including the reassembly and D-07 passes)
    - operator-claude-plugin/scripts/company_domain.py IN FULL — the confirm/correct/decline vocabulary and its `DomainDecisionError` refusal cases, the shape to follow
    - operator-claude-plugin/scripts/preview.py lines 180-212 — `build_extraction_preview`, and its explicit duck-typing contract (it must keep working for any object with the four original attributes)
    - operator-claude-plugin/tests/test_extraction_contract.py and test_identity_preflight.py — the existing pins on the identity pre-flight that must keep passing
    - .planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md (Task 1's output — the source vocabulary is defined once, here, and 59-06 extends it)
  </read_first>
  <behavior>
    - A contact row with only `firstname` and `lastname` (no email, no company) is rejected AS TODAY, and ALSO appears in `resolvable` naming `company` as the missing field
    - A company row with no name is rejected as today, and also appears in `resolvable` naming `name`
    - A record with NO usable resolution path (a malformed record, a bad `record_type`, missing provenance) is rejected and does NOT appear in `resolvable`
    - A record carrying `resolutions: [{"field": "company", "source": "hubspot_lookup", "detail": "..."}]` and a `company` value in its row is ACCEPTED, and the accepted entry carries that resolution through
    - A record whose `resolutions` names a source outside the closed vocabulary is REJECTED, with a reason naming the bad source
    - A record whose `resolutions` names a field its row does not actually carry is REJECTED
    - A record whose `resolutions` names a field that an ambiguity ALSO names is REJECTED by the unchanged D-07 contradiction check — being resolved does not exempt a field from it
    - `build_extraction_preview` returns a `resolvable` key, and still works when handed an object with only the four original attributes
  </behavior>
  <action>
Convert `extraction.py`'s identity gate from refuse-and-stop into refuse-and-classify-as-
proposable, add the provenance control, and extend the structural guarantee.

**`extraction.py` — the classification.**

- Add `resolvable: list = field(default_factory=list)` to `ExtractionResult`, documented in the
  same comment style as `ambiguities` and `collapses`. Entry shape, mirroring the `ambiguities`
  entry shape so the two read alike:
  `{"index": int, "record_type": str, "missing": [str, ...], "reason": str}`.
  `index` is the raw artifact record index (the same `i` the rejection uses), `missing` is the
  field names from the record type's identity groups that were absent, and `reason` states in
  operator-readable words what could be looked up.
- At the identity-gate branch (currently `extraction.py:531-540`): keep the existing
  `rejected.append(...)` EXACTLY as it is, and add a `resolvable.append(...)` alongside it.
  Additive, never a move — see planner assumption 2. Put a comment at the site naming D-59-08
  and stating that the rejection is retained deliberately so no existing reader of `rejected`
  changes behaviour.
- Compute `missing` from the record type's own identity groups (`record_groups`), not from a
  hardcoded field list — the same no-literals discipline `identity_groups()` and
  `canonical_props()` already enforce. Pick the group with the FEWEST absent fields, so the
  proposal names the shortest path to a valid row rather than every possibility.
- Return `resolvable` from `validate()`, add it to the `__main__` CLI's JSON output at
  `extraction.py:772-783`, and update the module docstring's contract section to describe the
  new group and the `resolutions` record key.
- Do NOT name any new function with a substring matching the forbidden list in
  `test_no_invention_structural.py`, and do NOT add any function that writes a value into a
  row. A helper that CLASSIFIES (for example one returning the missing field names) is correct;
  a helper that fills one is the exact surface D-07 keeps closed.

**`extraction.py` — the provenance control.**

- Add a module-level `RESOLUTION_SOURCES` frozenset holding exactly the four legitimate sources
  D-59-08's table names, as short identifiers: a read-only HubSpot lookup, an earlier operator
  statement in the conversation, an enrichment-waterfall provider result, and a stated
  derivation from another field of the same row. Name them plainly
  (`hubspot_lookup`, `operator_statement`, `provider_result`, `same_row_derivation`) and put
  D-59-08's illegitimate column in the constant's comment, so the closed set's boundary is
  documented where it is enforced.
- In the per-record pre-flight, read the record's OPTIONAL `resolutions` key. It is a RECORD
  key alongside `row` and `provenance`, never a `row` key, so the canonical-prop strip does not
  touch it. Validate it BEFORE the identity check, and reject the record on any of: not a list;
  an entry that is not a dict; an entry whose `field` is absent or is not a key the cleaned row
  actually carries; an entry whose `source` is not in `RESOLUTION_SOURCES`. Each rejection's
  reason names the offending value. This is the anti-laundering control and it is the single
  most safety-critical line in this plan: without it, a Claude-resolved value can be presented
  as source-derived, and D-59-08's audit-trail requirement fails silently.
- Carry a validated `resolutions` list through onto the `accepted` entry, alongside
  `provenance` and `record_type`, so a downstream reader can tell which fields came from the
  operator's own input and which from a resolution they approved. A record with no
  `resolutions` key gets an empty list, not a missing key — the preview renders this group
  unconditionally, and an absent key would read as a crash rather than as "nothing resolved"
  (the same reasoning `test_validate_with_no_ambiguities_returns_empty_list_not_a_missing_key`
  already pins for ambiguities).
- Merging: when two records collapse in `dedupe()`, their `resolutions` lists concatenate the
  way `provenance` already becomes a list — a merged row must not lose the record of which of
  its fields were resolved. Verify how `dedupe()` currently merges provenance before writing
  this, and follow the same rule rather than a parallel one.
- The D-07 contradiction pass is UNCHANGED. A field named by an ambiguity that nonetheless
  carries a value still rejects the record, whether or not `resolutions` names that field.
  Being resolved is not an exemption; a resolution the extraction was simultaneously unsure
  about is exactly the contradiction D-07 exists to catch.

**`preview.py`.**

- Add a `resolvable` key to `build_extraction_preview`'s returned dict, read via
  `getattr(result, "resolvable", [])`. The `getattr` default is required, not stylistic: that
  function's docstring documents an explicit duck-typing contract (anything with the four named
  attributes), and a bare attribute access would break every caller passing a shim. Update the
  docstring's attribute list to mention the new optional attribute.

**Tests — new file `operator-claude-plugin/tests/test_extraction_resolvable.py`.**

One test per `<behavior>` bullet above. Follow `test_no_invention_structural.py`'s `_record()`
helper shape so the fixtures read the same. Include at least one test proving a rejected
malformed record does NOT appear in `resolvable` — a classification that fires for everything
classifies nothing.

**Tests — EXTEND `operator-claude-plugin/tests/test_no_invention_structural.py`, never relax it.**

- `test_no_ambiguity_resolution_or_application_function_exists_in_extraction_module` keeps its
  existing four forbidden substrings unchanged. ADD to that same list the equivalents covering
  the new surface — names that would indicate a function filling an identity value rather than
  classifying one (`fill_identity`, `apply_resolution`, `confirm_resolution`,
  `resolve_identity`). Extend the test's docstring with a dated D-59-08 note: the ruling widened
  where propose-mode is used and did NOT widen where a Python function may enter a value into a
  row, so the guarantee now covers the resolution surface as well as the ambiguity surface.
- Add one new test to this file asserting `RESOLUTION_SOURCES` is a closed set and that a
  record naming a source outside it is rejected — the structural half of the anti-laundering
  control, living next to the structural half of the no-invention control.
- The other four existing tests in this file must pass unmodified.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_extraction_resolvable.py operator-claude-plugin/tests/test_no_invention_structural.py operator-claude-plugin/tests/test_extraction_contract.py operator-claude-plugin/tests/test_identity_preflight.py operator-claude-plugin/tests/test_company_extraction.py operator-claude-plugin/tests/test_preview_rendering.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RESOLUTION_SOURCES" operator-claude-plugin/scripts/extraction.py` is at least 1
    - `grep -c "resolvable" operator-claude-plugin/scripts/extraction.py` is at least 4
    - `grep -c "hubspot_lookup" operator-claude-plugin/scripts/extraction.py` is at least 1
    - `grep -c 'getattr(result, "resolvable"' operator-claude-plugin/scripts/preview.py` is 1
    - `grep -cE "fill_identity|apply_resolution|confirm_resolution|resolve_identity" operator-claude-plugin/tests/test_no_invention_structural.py` is at least 1 (the forbidden list was extended)
    - `grep -c "resolve_ambig" operator-claude-plugin/tests/test_no_invention_structural.py` is at least 1 (the original list survives)
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_no_invention_structural.py -x` exits 0
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
    - A test exists asserting a record with a `source` outside the closed vocabulary is rejected with a reason naming that source
  </acceptance_criteria>
  <done>The identity gate reports what could be resolved instead of only what failed; a resolved value cannot be presented as source-derived; and no Python function fills a row.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the no-invention rule with recorded-edit discipline, then bump and log</name>
  <files>operator-claude-plugin/skills/contact-upload/extraction.md, operator-claude-plugin/skills/contact-upload/SKILL.md, operator-claude-plugin/.claude-plugin/plugin.json, operator-claude-plugin/CHANGELOG.md</files>
  <read_first>
    - operator-claude-plugin/skills/contact-upload/extraction.md lines 20-40 (passage 1, the numbered no-invention rule) and lines 355-375 (passage 2, the company-row restatement) — read both fully before editing either, they must end up consistent
    - operator-claude-plugin/skills/contact-upload/SKILL.md lines 193-210 — the paragraph documenting which groups the extraction preview shows (provenance, rejected rows, unmapped keys, the ambiguity block); Task 2 adds a group to that surface and this paragraph is where the skill documents it
    - .planning/phases/59-frictionless-write-path/59-CONTEXT.md § D-59-08, the paragraph beginning "Known text that must be amended"
    - operator-claude-plugin/scripts/extraction.py (as changed by Task 2) — the skill must describe the actual `resolvable` / `resolutions` / `RESOLUTION_SOURCES` surface, not an idealised one
    - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py lines 155-175 — the house style for a RECORDED EDIT note, to match
  </read_first>
  <action>
Amend both no-invention passages in
`operator-claude-plugin/skills/contact-upload/extraction.md`, then ship.

**WHAT SURVIVES VERBATIM, in BOTH passages.** The sentence forbidding filling a gap to get a
row past the identity rule is untouched — same words, same emphasis. It is the core of the rule
and D-59-08 preserves it explicitly. Do not reword it, do not merge it into a longer sentence,
do not move it below the new text.

**WHAT IS REWRITTEN, in BOTH passages.** The clause asserting that a rejection with a stated
reason is the correct outcome (`extraction.md:27-30` for contacts, `extraction.md:364-368` for
companies) no longer describes the intended behaviour. Rewrite each so rejection is the LAST
resort — reached after a resolution was attempted and either failed to find anything or was
declined by the operator. The rewritten text must state, in the skill's own voice:

- Where a resolved value may come from, listing D-59-08's four legitimate sources by the same
  identifiers Task 2 put in `RESOLUTION_SOURCES`, so the prose and the enforced vocabulary
  cannot drift.
- What is still forbidden, listing D-59-08's illegitimate column: Claude's own recall about the
  person or company, inference from what companies like this usually do, a plausible corporate
  email pattern, and anything the operator would have no way to check. Say plainly that the
  change is from refusing to proposing, never from refusing to guessing.
- That a resolved value is PROPOSED and must be confirmed before it becomes part of the row —
  it is never written into the artifact and dispatched on Claude's own authority.
- That a confirmed resolution is recorded in the record's `resolutions` key with its source, so
  an operator reading the row back can tell which fields came from their input and which from a
  resolution they approved, and that a source outside the closed set is rejected by
  `validate()` rather than accepted unlabelled.
- That the resolution loop is the SAME one ambiguities already use: Claude rewrites the
  artifact and `validate()` runs again. There is no Python function that fills a value.

**RECORDED-EDIT DISCIPLINE.** Both passages get a dated in-file note: D-59-08, operator,
2026-08-28, stating what the clause used to say, why it no longer describes intended behaviour,
and that the gap-filling prohibition it sat beside is unchanged. Nothing is silently deleted and
nothing is quietly weakened — a reader who remembers the old rule must be able to find out what
happened to it from this file alone.

**THE SKILL'S DOCUMENTED OUTPUT SHAPE.** Task 2 adds a group to `validate()`'s result, to
`extraction.py`'s `__main__` JSON output and to `build_extraction_preview`'s returned dict.
`contact-upload/SKILL.md:198-203` is where the skill tells Claude which groups that preview
shows, and this project's own convention is that a SKILL.md whose documented call shape changes
is updated in the SAME task as the code. Extend that paragraph to name the new resolvable group
alongside the rejected rows, the unmapped keys and the ambiguity block, and say what Claude does
with it: propose a resolution for each entry from the legitimate sources, wait for the
operator's confirmation, then rewrite the artifact and re-validate. Keep the existing
"presented once, never one interruption per row" discipline — a resolvable group that
interrupted per row would be exactly the friction this phase exists to remove.

**PINNING TESTS.** Research found no test pinning either rewritten clause
(`grep -rln "rejected with a stated reason"` across both test directories returned nothing). Run
that grep yourself before concluding it; if a pin DOES exist, re-point it in this same commit
with the reason in the test body, exactly as 59-03 did for its own pinned wording. Do not spend
time hunting beyond that one grep.

**SHIP.** Set `version` in `operator-claude-plugin/.claude-plugin/plugin.json` to `0.24.0` and
add a matching CHANGELOG entry, same commit. The entry names D-59-08, the gate inventory, the
`resolvable` group, the `resolutions` provenance key and its closed source vocabulary, the
extraction.md amendment, and — stated plainly, because it is the part most likely to be
misread — that the no-invention rule was NOT relaxed: the gap-filling prohibition survives
verbatim, the structural test was extended rather than weakened, and a resolution still requires
the operator's confirmation. Also name what this release does not yet convert, pointing at the
inventory's `Unplanned items` section and at 59-06.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Never fill a gap to make a row satisfy the identity rule" operator-claude-plugin/skills/contact-upload/extraction.md` is at least 1 — the sentence survives verbatim
    - `grep -c "never invented" operator-claude-plugin/skills/contact-upload/extraction.md` is at least 1 — passage 2's own gap-filling prohibition survives
    - `grep -c "A row that gets rejected with a stated reason is the correct outcome" operator-claude-plugin/skills/contact-upload/extraction.md` is 0
    - `grep -c "D-59-08" operator-claude-plugin/skills/contact-upload/extraction.md` is at least 2 (a dated note at each passage)
    - `grep -c "2026-08-28" operator-claude-plugin/skills/contact-upload/extraction.md` is at least 2
    - `grep -c "hubspot_lookup" operator-claude-plugin/skills/contact-upload/extraction.md` is at least 1 — the prose names the same identifiers the code enforces
    - `grep -c "resolvable" operator-claude-plugin/skills/contact-upload/SKILL.md` is at least 1 — the skill documents the group the preview now returns
    - `grep '"version"' operator-claude-plugin/.claude-plugin/plugin.json` contains `0.24.0`
    - `grep -c "D-59-08" operator-claude-plugin/CHANGELOG.md` is at least 1
    - `.venv/bin/python -m pytest operator-claude-plugin/tests -q` exits 0
  </acceptance_criteria>
  <done>Rejection is documented as the last resort after a proposal, the gap-filling prohibition is verbatim intact, both edits are dated in-file, and 0.24.0 ships saying exactly what did and did not change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's extraction artifact → `validate()` | Untrusted model output crosses here; `validate()` is the input-validation control (ASVS V5) |
| resolved value → the operator's audit trail | A value's claimed origin determines whether the operator can check it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-20 | Repudiation | provenance laundering — a Claude-recalled value labelled as a HubSpot lookup, or resolved and left unlabelled | high | mitigate | `RESOLUTION_SOURCES` is a CLOSED frozenset; a `resolutions` entry naming a source outside it REJECTS the record rather than being accepted unlabelled, and an entry naming a field the row does not carry rejects too. A record with no `resolutions` key gets an empty list, so "nothing resolved" is an assertion rather than a missing key. Pinned by a new test in `test_no_invention_structural.py`. |
| T-59-21 | Spoofing | a fabricated value reaching HubSpot because refusal stopped being the default | high | mitigate | The change is classification only: `validate()` gains a `resolvable` list and NO function that writes a value into a row. The structural test forbidding fill/apply/confirm/resolve function names is EXTENDED to cover the new surface. A resolved value only enters via an artifact Claude rewrote and the operator confirmed, then re-validated — the same loop ambiguities already use. |
| T-59-22 | Elevation of Privilege | a row silently starting to dispatch because it moved out of `rejected` | high | mitigate | `resolvable` is strictly ADDITIVE. The identity-gate `rejected.append(...)` is unchanged, so every existing reader (`preview.py:206`, the CLI at `extraction.py:777`) sees exactly what it sees today. Pinned by the acceptance criteria requiring the existing identity-preflight and extraction-contract tests to pass unmodified. |
| T-59-23 | Tampering | D-07's contradiction check bypassed for a field marked resolved | high | mitigate | The D-07 pass is explicitly unchanged and the behaviour is pinned by a test: a field named by an ambiguity that carries a value still rejects, resolved or not. Being resolved is not an exemption. |
| T-59-24 | Information Disclosure | a resolution `detail` string carrying a credential or an internal path into the artifact and the preview | medium | mitigate | `detail` is free text authored by Claude for the operator to read, not by an external system. `write_dispatch_csv`'s existing STRUCT-01 guard raises on any row key outside the canonical set, so `resolutions` — a RECORD key, never a ROW key — cannot reach the dispatch CSV. Accepted residual: the preview may render whatever `detail` says; it is operator-facing by design. |
| T-59-25 | Denial of Service | the inventory closing cleanly by reclassifying an inconvenient gate | medium | mitigate | `NOT-APPLICABLE` requires the stated reason "no legitimate resolution source exists"; the acceptance criteria grep for that phrase on every N/A row and grep to 0 for difficulty-based dismissals. An unconverted `CONVERT` gate must appear under `Unplanned items` and be reported to the orchestrator. |
| T-59-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan. `59-RESEARCH.md` § Package Legitimacy Audit records "not applicable — this phase installs no external packages". |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths this PLAN creates (new — not pre-existing surface):

- `.planning/phases/59-frictionless-write-path/59-GATE-INVENTORY.md` (new document)
- `extraction.RESOLUTION_SOURCES` (new frozenset constant)
  - member identifiers: `hubspot_lookup`, `operator_statement`, `provider_result`,
    `same_row_derivation`
- `extraction.ExtractionResult.resolvable` (new field)
  - entry keys: `index`, `record_type`, `missing`, `reason`
- extraction artifact record key `resolutions` (new, optional)
  - entry keys: `field`, `source`, `detail`
- `resolutions` carried onto each `ExtractionResult.accepted` entry (new key)
- `resolvable` key in `preview.build_extraction_preview`'s returned dict (new)
- `resolvable` key in `extraction.py`'s `__main__` JSON output (new)
- `operator-claude-plugin/tests/test_extraction_resolvable.py` (new test file)
- plugin version `0.24.0`
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests -q` — full plugin suite green
- `.venv/bin/python -m pytest -q` — full root suite green
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_no_invention_structural.py -x` —
  the extended structural guarantee, run on its own so a failure here is unmissable
- No n8n code touched, so `node --test tests/n8n/*.test.mjs` is not required by this plan
</verification>

<success_criteria>
- A decided inventory of every operator-facing refuse-and-stop gate exists, with unconverted
  items named rather than dropped
- The identity gate classifies resolvable failures instead of only rejecting them, additively
- A resolved value carries an unlaunderable source from a closed vocabulary, enforced by
  rejection
- No Python function in `extraction.py` fills a value into a row, and the structural test
  proving it now covers the resolution surface too
- The no-invention rule's core sentence is verbatim intact in both passages; both amendments
  are dated in-file
- Plugin version 0.24.0
</success_criteria>

<output>
Create `.planning/phases/59-frictionless-write-path/59-05-SUMMARY.md` when done
</output>
