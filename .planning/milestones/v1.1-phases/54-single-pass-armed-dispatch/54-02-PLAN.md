---
phase: 54-single-pass-armed-dispatch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/report_enrichment.py
  - operator-claude-plugin/tests/test_report_enrichment.py
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/tests/test_enrich_skill_contract.py
  - .planning/milestones/v1.1-REQUIREMENTS.md
  - .planning/milestones/v1.1-ROADMAP.md
autonomous: true
requirements: [G-3]

must_haves:
  truths:
    - "A run that held a row for identity confirmation reports what happened in the operator's words, and says the confirmation will cost a second pass."
    - "A look-only run reports that nothing was saved and that saving it costs the same again."
    - "G-3's text in REQUIREMENTS.md and the Phase 54 goal text in the roadmap describe what is actually shipped, dated, with the fix that closed it named."
    - "The two remaining two-pass shapes are named as legitimate — a rehearsal and an identity hold — and neither is described as the G-3 defect."
  artifacts:
    - operator-claude-plugin/scripts/report_enrichment.py
    - .planning/milestones/v1.1-REQUIREMENTS.md
  key_links:
    - "report_enrichment._ACTION_TO_OUTCOME -> the action words the two decision nodes actually emit (`needs_match_review`, `proposed`)"
    - "enrich-records/SKILL.md §2 identity paragraph -> the cost sentence that must sit beside the hold it already describes"
  prohibitions:
    - "The projected figure is never presented as a measurement."
    - "No write without an armed, record-scoped window."
    - "`approve` never means two different things."
    - "No hand-edit of n8n/wf_*.json."
    - "The SJ-3 residual is recorded, never silently fixed and never silently dropped."
---

<objective>
Two shapes still cost two full passes and both are legitimate: a look-only rehearsal, and an
identity hold that the operator must confirm before anything is written. Neither is the G-3
defect. This plan makes each one say, where the operator reads it, that proceeding costs a second
pass — and corrects the now-stale text that still describes G-3 as unfixed.

Purpose: OP-54-01. The phase's whole value is honest accounting; a report that renders a held row
as `unknown` and requirement text that describes a defect fixed a day earlier are the two places
the account is currently wrong.

Output: two new outcome words in the report, one cost sentence in the enrich lane, and corrected
G-3/Phase-54 language in the milestone documents.

**Execution budget: 0 n8n executions, 0 provider credits, 0 Anthropic calls.** Every change here
is offline text and pure-Python rendering.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-single-pass-armed-dispatch/54-RESEARCH.md
@operator-claude-plugin/scripts/report_enrichment.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Name the two outcomes the report currently renders as unknown</name>
  <files>operator-claude-plugin/scripts/report_enrichment.py, operator-claude-plugin/tests/test_report_enrichment.py</files>
  <read_first>
    - operator-claude-plugin/scripts/report_enrichment.py lines 30-130 (`_ACTION_TO_OUTCOME`, `SUCCESS_OUTCOMES`, `_OUTCOME_REASON`, `_outcome_for_row`, `_review_state_for_row`)
    - scripts/build_cloud_workflows.py lines 1608-1628 (the contacts decision node — where `proposed` and `needs_match_review` are assigned, and why each exits via the false lane)
    - scripts/build_cloud_workflows.py lines 3337-3350 (the companies decision node — `proposed` is assigned there too; there is no companies match lane)
    - operator-claude-plugin/tests/test_report_enrichment.py (the whole file — in particular the exact-dict-equality count test that this edit moves)
  </read_first>
  <behavior>
    - A row whose action is the medium-tier identity hold renders as a named outcome, not `unknown`, and its reason says a confirmation re-runs the whole lookup and costs the same again.
    - A row whose action is the look-only mode renders as a named outcome, not `unknown`, and its reason says nothing was saved and saving it costs the same again.
    - Neither new outcome joins `SUCCESS_OUTCOMES` — nothing was written on either path.
    - Existing rows (create, enrich, write_blocked, skip) render byte-identically to before.
  </behavior>
  <action>
    Add two entries to `_ACTION_TO_OUTCOME` in `operator-claude-plugin/scripts/report_enrichment.py`,
    keyed on the action words the decision nodes actually emit — the medium-tier identity hold and
    the look-only mode — mapping each to its own outcome word. Add the matching entries to
    `_OUTCOME_REASON`. Do NOT add either outcome to `SUCCESS_OUTCOMES`: neither wrote anything.

    The reason text is operator-facing and follows VOCAB-01..03 — name the consequence, not the
    mechanism. For the identity hold: this row matched somebody with the same surname at the same
    company and was held rather than written over; confirming it and sending again re-runs the whole
    lookup for that person, so it costs the same as this run did. For the look-only mode: this was a
    look, nothing was saved; saving it means checking it again at the same cost. Do not say
    "waterfall", "propose mode", "write-safety gate" or any node name in text an operator reads.

    `test_report_enrichment.py` holds a count test asserting an exact dict equality over the outcome
    tally; two new outcome words move it. Re-point that assertion IN PLACE — rewrite it, never
    delete it, with a comment directly above recording the date, this phase, and why the shape
    changed (the D-53-01/D-53-05 discipline). Research §5 predicted this exact pin would move; that
    is the expected, deliberate edit, not a sweep.

    Add tests: one row per new action word asserting its outcome word, asserting neither counts as a
    success, and asserting each row's reason states the second-pass cost.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_report_sufficiency.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_report_enrichment.py operator-claude-plugin/tests/test_report_sufficiency.py -q` exits 0.
    - `python3 -c "import sys;sys.path.insert(0,'operator-claude-plugin/scripts');import report_enrichment as r;assert 'needs_match_review' in r._ACTION_TO_OUTCOME and 'proposed' in r._ACTION_TO_OUTCOME;assert not (set(r._ACTION_TO_OUTCOME[k] for k in ('needs_match_review','proposed')) & r.SUCCESS_OUTCOMES);print('ok')"` prints `ok`.
    - Both new `_OUTCOME_REASON` strings contain the words "costs" and "again".
    - `git diff -- operator-claude-plugin/tests/test_report_enrichment.py` shows the moved count assertion has a dated comment naming Phase 54 immediately above it.
    - The two pre-existing `write_blocked` reporting tests named in 54-RESEARCH.md §5 are unmodified: `git diff -U0 -- operator-claude-plugin/tests/test_report_enrichment.py | grep -cE '^[+-][^+-].*write_blocked'` is 0 (`-U0` so no context line or hunk header can false-fail this).
  </acceptance_criteria>
  <done>No run outcome an operator can reach renders as `unknown`, and the two that cost a second pass say so where the result is read.</done>
</task>

<task type="auto">
  <name>Task 2: Put the second-pass cost beside the identity hold the lane already describes</name>
  <files>operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/tests/test_enrich_skill_contract.py</files>
  <read_first>
    - operator-claude-plugin/skills/enrich-records/SKILL.md lines 45-70 (§2's "People, named the way you would name them" — the paragraph at lines 57-60 already says a same-surname match is held and never written over; this task adds the cost, it does not restate the hold)
    - operator-claude-plugin/tests/test_enrich_skill_contract.py (the whole file — the assertion style and how it locates SKILL.md prose)
    - operator-claude-plugin/scripts/enrichment.py lines 320-400 (`spec.get("propose")` — the look-only mode's only entry point today)
  </read_first>
  <action>
    In `operator-claude-plugin/skills/enrich-records/SKILL.md`, extend the identity paragraph at
    §2 (the one ending "so a second John Tsatsimas is surfaced rather than silently overwritten")
    with ONE sentence: confirming a held match and sending again re-runs the lookup for that person
    and costs the same as the first run did. Operator register, VOCAB-01..03 — the consequence, not
    the mechanism, and no node or mode names. Do not restructure §2; add the sentence where the hold
    is already described.

    Then record, in the plan's SUMMARY rather than in the skill, a fact the executor must verify
    rather than assume: the look-only mode has NO operator-facing surface today — it is reachable
    only through `enrichment.build_envelope`'s spec key, used by the Phase 58 spike. Grep the three
    lane skills for an operator-facing offer of a look-only run before writing any wording for it.
    If there is genuinely none, do NOT invent a surface to hang a disclosure on — Task 1's report
    outcome is the disclosure, and the absence of an operator surface is a finding to state in the
    SUMMARY. If a surface does exist, add the same one-sentence cost disclosure there too.

    Add an assertion to `operator-claude-plugin/tests/test_enrich_skill_contract.py` pinning that
    §2's identity paragraph states the second-pass cost — matching on the operator-facing words
    (costs / again / confirm), not on a node name.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_skill_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_skill_contract.py -q` exits 0.
    - A new assertion in `test_enrich_skill_contract.py` fails when the added sentence is deleted from SKILL.md (verify by deleting it locally, running the test, restoring).
    - `grep -icE "waterfall|propose mode|write.safety|node" operator-claude-plugin/skills/enrich-records/SKILL.md` is not increased by this diff: `git diff -- operator-claude-plugin/skills/enrich-records/SKILL.md | grep -icE '^\+.*(waterfall|write.safety gate)'` is 0.
    - The SUMMARY states, as a checked fact, whether an operator-facing look-only surface exists.
  </acceptance_criteria>
  <done>An operator told their row was held also learns, in the same breath, that confirming it costs another full run.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the G-3 and Phase 54 text that still describes a fixed defect</name>
  <files>.planning/milestones/v1.1-REQUIREMENTS.md, .planning/milestones/v1.1-ROADMAP.md</files>
  <read_first>
    - .planning/milestones/v1.1-REQUIREMENTS.md lines 20-35 (G-2/G-3/G-4 — G-3's text at lines 27-31 is what changes)
    - .planning/milestones/v1.1-ROADMAP.md lines 59-71 (the Phase 54 entry — bullet 2's "`write_blocked`-then-arm path stays reachable" is the stale sentence)
    - .planning/phases/54-single-pass-armed-dispatch/54-RESEARCH.md §1 and "State of the Art" (the dated evidence: `authorize_ungranted_send`, plugin 0.18.0, live-verified 2026-08-26)
    - .planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md if 54-01 has landed it; otherwise cite it as the artifact that carries the numbers
  </read_first>
  <action>
    Rewrite G-3's paragraph in `.planning/milestones/v1.1-REQUIREMENTS.md` so it describes what
    happened, not what was true when it was written on 2026-08-25. Keep the original defect
    description as the recorded history — this document is the milestone's audit trail, so the
    correction is an amendment with a date, not a silent overwrite. The amendment states: the
    accidental mechanism was closed by `write_grant.authorize_ungranted_send` (plugin 0.18.0,
    2026-08-25) which opens the armed window before the dispatch for granted and ungranted sends
    alike, live-verified by the operator walk on 2026-08-26; the measured saving is in
    `54-MEASUREMENT.md`; and two shapes still cost two full passes by design — a look-only rehearsal
    and an identity hold awaiting confirmation — neither of which is this defect. Name the scheduled
    poller's double pass as a recorded residual (OP-54-02, WINDOWS entry 27), explicitly out of
    scope by operator ruling.

    In `.planning/milestones/v1.1-ROADMAP.md`, amend the Phase 54 entry: bullet 2's claim that the
    refuse-then-arm ceremony "stays reachable for the ungranted case" no longer describes any
    documented interactive lane and must be replaced by the two shapes that ARE reachable, each
    named as a rehearsal or a hold rather than as a defect. Bullet 3's projection numbers stay as
    the projection they were, with a pointer to the measured figures — do not overwrite a projection
    with a measurement and leave no trace that it was ever a projection (OP-54-05 applies to this
    document too).

    Use `Edit` with scoped replacements on both files. Never `Write` either one — a whole-file write
    of a roadmap destroys every phase entry outside the diff window.
  </action>
  <verify>
    <automated>grep -q "authorize_ungranted_send" .planning/milestones/v1.1-REQUIREMENTS.md &amp;&amp; grep -q "54-MEASUREMENT.md" .planning/milestones/v1.1-REQUIREMENTS.md &amp;&amp; grep -q "OP-54-02" .planning/milestones/v1.1-REQUIREMENTS.md &amp;&amp; test "$(grep -c '^### Phase ' .planning/milestones/v1.1-ROADMAP.md)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - The automated command above exits 0 — the amendment names the shipped fix, points at the measurement artifact, and cites OP-54-02.
    - `grep -c '^### Phase ' .planning/milestones/v1.1-ROADMAP.md` is unchanged from before the edit (no phase entry lost to a whole-file write).
    - `git diff --stat -- .planning/milestones/v1.1-ROADMAP.md` shows fewer than 30 changed lines — a scoped amendment, not a rewrite.
    - G-3's original 2026-08-25 defect text is still readable in REQUIREMENTS.md alongside the dated amendment; `git diff -- .planning/milestones/v1.1-REQUIREMENTS.md | grep -c '^-.*Two full provider passes'` is 0.
    - The roadmap's Phase 54 bullet 3 still shows its numbers as a projection and points at the measured figures rather than replacing them.
  </acceptance_criteria>
  <done>Anyone reading G-3 or the Phase 54 goal learns what shipped, when, what it was measured at, and which two-pass shapes remain on purpose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator <- report text | Everything an operator decides to spend money on is decided from this prose |
| milestone documents -> future planners | A stale requirement description causes a future phase to rebuild something already shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-05 | Repudiation | `_ACTION_TO_OUTCOME` | high | mitigate | A held or look-only row rendering as `unknown` lets an operator believe a write landed; Task 1 names both outcomes and pins them with tests, and neither joins `SUCCESS_OUTCOMES` |
| T-54-06 | Spoofing (of consent) | enrich-records §2 wording | medium | mitigate | An operator confirming a held match without knowing it costs a second full run has not consented to that spend; Task 2 states the cost beside the hold, pinned by a contract test |
| T-54-07 | Tampering | v1.1-ROADMAP.md | medium | mitigate | `Edit`-only, scoped; a phase-entry count and a diff-size ceiling are acceptance criteria, because a whole-file `Write` here silently deletes sibling phases |
| T-54-08 | Information Disclosure | operator-facing reason strings | low | accept | Reason strings name record-level facts the operator already supplied; no credential or internal identifier is added |
| T-54-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install in this plan; if one becomes necessary the package-legitimacy gate and its blocking human checkpoint apply first |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` green (modulo the 4 known `tests/test_merge_policy.py` failures, outside this path).
- No run outcome reachable by an operator renders as `unknown`.
- Both milestone documents still contain every phase entry they had before the edit.
- n8n executions spent by this plan: 0. Provider credits: 0. Anthropic calls: 0.
</verification>

<success_criteria>
The two legitimate two-pass shapes are disclosed at the point of use, and the milestone's own text
about G-3 matches what is shipped and measured.
</success_criteria>

<output>
Create `.planning/phases/54-single-pass-armed-dispatch/54-02-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- Two named report outcomes replacing `unknown`, with their second-pass cost stated
- One cost sentence in `enrich-records/SKILL.md` §2, pinned by a contract test
- Dated amendments to G-3 in `v1.1-REQUIREMENTS.md` and to the Phase 54 entry in `v1.1-ROADMAP.md`
- A stated finding: whether an operator-facing look-only surface exists at all
</output>
