---
phase: 54-single-pass-armed-dispatch
plan: 05
type: execute
wave: 3
depends_on: ["54-04", "54-01"]
files_modified:
  - .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md
  - .planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md
autonomous: false
requirements: [G-3]

must_haves:
  truths:
    - "One real flagged contact is approved through the deployed endpoint under an operator-authorized, record-scoped armed window, and the resulting HubSpot state is confirmed by an independent read."
    - "The window is opened by the operator, closed after the one submit, and the disarm is verified by re-read."
    - "The proof states which branch it exercised, and does not claim the other one."
    - "Nothing outside the one named record was written."
  artifacts:
    - .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md
  key_links:
    - "review queue read -> the one contact id that is proven -> the record-scoped allowlist that permits exactly it"
    - "the submit -> an independent HubSpot read of that record's review properties afterwards"
  prohibitions:
    - "The projected figure is never presented as a measurement."
    - "No write without an armed, record-scoped window."
    - "`approve` never means two different things."
    - "No hand-edit of n8n/wf_*.json."
    - "The SJ-3 residual is recorded, never silently fixed and never silently dropped."
---

<objective>
Prove OP-54-04 on one real record: an operator approves a flagged contact and HubSpot changes.

Purpose: this phase's own standard is that a claim is either measured or labelled. A contacts apply
path that has only ever run in node tests is a structural claim, not a proven one.

Output: one dated live proof with before and after reads, and an explicit statement of which branch
it exercised.

**Execution budget: 2-4 n8n executions** — one preview submit and one real submit, each of which may
produce a verification execution. Provider credits: 0, Anthropic calls: 0 (the review-decision
workflow has no waterfall on it). Record the actual count.

**This plan writes to a live CRM record. Every write here is the operator's, not Claude's.** Claude
never sets an arming variable and never opens a window.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-single-pass-armed-dispatch/54-03-SUMMARY.md
@.planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md
@operator-claude-plugin/skills/review-triage/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find one flagged contact and write down exactly what approving it would change</name>
  <precondition>At least one contact in HubSpot has `lv_enrichment_needs_review` set. If the queue holds none, this plan halts and reports what would create one: a contact enriched through the permissive contact lane whose email or another field is promoted into a blank at review-level confidence. Manufacturing one is an enrichment send with its own cost and is the operator's call, not this task's.</precondition>
  <files>.planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md</files>
  <read_first>
    - operator-claude-plugin/scripts/review_queue.py (the queue read and its rendering — reuse it; do not write a second HubSpot search)
    - operator-claude-plugin/skills/review-triage/SKILL.md steps 1-5 (the queue read and the preview, both of which work while disarmed)
    - .planning/phases/54-single-pass-armed-dispatch/54-03-SUMMARY.md (which branch a no-candidate contact takes, and the exact message it returns)
  </read_first>
  <action>
    Read the live review queue for contacts, read-only. Pick ONE record and record its id, its
    review reason, and the current live values of its review family properties — this is the BEFORE
    read, and it is taken independently, not inferred from the queue rendering.

    Run the review-decision PREVIEW for an approve on that record. The preview works while disarmed
    by design and writes nothing; it returns the exact property patch a real submit would send.

    Open `.planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md` with: the date, the record
    id, the BEFORE property values, the preview's exact proposed patch, and which branch this record
    will take — the promote branch (a held candidate) or the already-applied clear branch. State
    plainly which one, because the phase's claim is only as wide as what this proof exercises.

    Do not submit. Do not arm anything. Leave the file ending at the proposed patch.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md &amp;&amp; grep -qiE "before" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md &amp;&amp; grep -qiE "branch" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - 54-LIVE-PROOF.md names one contact id, its BEFORE property values from an independent read, and the preview's exact proposed patch.
    - The file states which branch the record takes, by name.
    - `git diff` shows no change to any file other than 54-LIVE-PROOF.md — nothing was armed, nothing submitted.
    - If the queue held no flagged contact, the file says so and names what would create one, and the plan halts here.
  </acceptance_criteria>
  <done>The operator can read exactly what they would be authorizing, before they authorize it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator approves one flagged contact under a window they open</name>
  <what-built>
    A contacts approve now writes to HubSpot through the same engine and the same write gate
    companies use (54-03), deployed and bounced into the running instance and verified disarmed
    (54-04). Task 1 above has read one flagged contact and printed the exact patch an approve would
    send.
  </what-built>
  <how-to-verify>
    This is the one write in the phase, and it is yours to authorize.

    1. Read Task 1's proposed patch in `54-LIVE-PROOF.md`. It names one contact id and the exact
       properties that would change.
    2. If it is what you want, open a record-scoped write window for that ONE contact id and nothing
       else, through the review-triage skill's own step 6 — your yes is the arm, and it binds to the
       patch just shown (VOCAB-05). Anything ambiguous is not consent and must not be treated as an
       arm.
    3. Submit the approve for that record.
    4. Close the window and confirm the disarm by re-read: the review write flag not enabled, both
       record allowlists empty, the workflow still active.

    Expected afterwards, on an independent read of that contact:
    - `lv_enrichment_needs_review` reads `false`
    - `lv_enrichment_reviewed_at` carries this submit's timestamp
    - `lv_enrichment_reviewed_by` carries your reviewer label, if you supplied one
    - the review reason is cleared
    - on the promote branch only, the promoted field(s) hold their new values

    Expected of everything else: unchanged. No other contact's `hs_lastmodifieddate` moves.

    Report back: the record id, what the read-back showed, the disarm verdict, and the number of n8n
    executions the submit consumed.
  </how-to-verify>
  <resume-signal>Type "approved" with the read-back results, or describe what happened instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the proof, and record its limits</name>
  <files>.planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md</files>
  <read_first>
    - .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md (Task 1's BEFORE half)
    - .planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md (the basis-word convention this file follows)
    - .planning/phases/54-single-pass-armed-dispatch/54-03-SUMMARY.md (the checkpoint decision and the promote branch's producer status)
  </read_first>
  <action>
    Complete `54-LIVE-PROOF.md` with the AFTER half: the independent read-back of that record's
    review properties, the disarm verification, the workflow's active state, and the exact count of
    n8n executions the submit consumed.

    Then a limits section, in the same register as the measurement report:
    - Which branch was exercised, and which was not. If the record took the already-applied clear
      branch, say that the promote branch remains proven by node tests only, and why — no producer
      stages contact candidates in this deployment. Do not describe the promote branch as
      live-proven.
    - That no other record was touched, with the evidence for it (an allowlist scoped to one id, and
      the disarm re-read).
    - The execution count as measured, not projected.

    Add a one-line cross-reference to this file from `54-MEASUREMENT.md`'s residual section so the
    two artifacts read as one account of the phase.
  </action>
  <verify>
    <automated>grep -qiE "disarm" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md &amp;&amp; grep -qiE "execution" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md &amp;&amp; grep -qiE "branch" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md &amp;&amp; grep -q "54-LIVE-PROOF" .planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - 54-LIVE-PROOF.md contains a BEFORE read, the submitted patch, an AFTER read, the disarm verdict, and the measured execution count.
    - The limits section names the branch NOT exercised and does not claim it.
    - 54-MEASUREMENT.md cross-references 54-LIVE-PROOF.md.
    - `grep -c "TODO\|TBD" .planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md` is 0.
  </acceptance_criteria>
  <done>The phase's contacts claim is exactly as wide as the evidence behind it, and the window it used is closed and verified closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator consent -> live CRM write | The only write in this phase crosses here |
| armed window -> the rest of the portal | An allowlist wider than one id is a wider blast radius than the proof needs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-18 | Elevation of Privilege | the armed window | critical | mitigate | Operator-opened only, scoped to the single contact id from Task 1, closed after one submit, disarm confirmed by independent re-read; Claude sets no arming variable in any task of this plan |
| T-54-19 | Spoofing (of consent) | the approve submit | high | mitigate | The yes binds to the exact patch Task 1 printed (VOCAB-05); an ambiguous affirmative is not an arm, and the checkpoint says so in the operator's own instructions |
| T-54-20 | Repudiation | the proof artifact | high | mitigate | BEFORE and AFTER are independent reads, not the submit's own response; the execution count is recorded as measured; the unexercised branch is named as unproven |
| T-54-21 | Tampering | blast radius | high | mitigate | Record-scoped allowlist plus a post-hoc statement that no other record's last-modified moved |
| T-54-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install in this plan; if one becomes necessary the package-legitimacy gate and its blocking human checkpoint apply first |
</threat_model>

<verification>
- One contact id, read before and after by independent reads, shows its review flag cleared and a reviewed-at stamp.
- The window is closed: review write flag not enabled, both allowlists empty, workflow active.
- The proof names the branch it exercised and does not claim the other.
- n8n executions spent: recorded exactly (expected 2-4). Provider credits: 0. Anthropic calls: 0.
</verification>

<success_criteria>
An operator approving a flagged contact changed a real HubSpot record, under a window they opened
and that is verified closed, and the phase's claim about contacts is bounded by what that proof
shows.
</success_criteria>

<output>
Create `.planning/phases/54-single-pass-armed-dispatch/54-05-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `.planning/phases/54-single-pass-armed-dispatch/54-LIVE-PROOF.md` — before read, submitted patch, after read, disarm verdict, measured execution count, and the branch NOT exercised
</output>
