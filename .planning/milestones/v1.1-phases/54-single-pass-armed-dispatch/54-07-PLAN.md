---
phase: 54-single-pass-armed-dispatch
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/tests/test_write_grant.py
autonomous: true
gap_closure: true
requirements: [G-3]

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The rendered Anthropic-spend sentence in a grant envelope states one bound, not two opposite ones, and the bound it states matches how config/cost_rates.json's anthropic_usd_per_record was actually derived — a dated observed average from two canary executions, which is a projection, not a ceiling and not a lower bound (WR-04)."
    - "The pinning test asserts the sentence's meaning rather than pinning the contradiction, and would fail if either of the two mutually exclusive framings came back."
  artifacts:
    - operator-claude-plugin/scripts/write_grant.py
    - operator-claude-plugin/tests/test_write_grant.py
  key_links:
    - "config/cost_rates.json anthropic_usd_per_record (confidence: measured, citation: Phase 22 canary executions 332/337) -> envelope()'s anthropic_usd -> the rendered block sentence -> the test that pins it"
---

<objective>
Close WR-04 — the operator-facing Anthropic-spend sentence that calls the same number both an
upper bound and a lower bound, and the test that pins the contradiction instead of catching it.

Purpose: the operator declined residual status for this on 2026-08-27. It has NO functional
consequence — both framings point at the identical, correctly-computed figure — so no urgency is
claimed. It is a clarity defect this phase's own relabelling commit introduced, on the one
sentence a cost disclosure exists to make unambiguous.

Output: one framing, chosen to match the rate's real derivation, and a test that can tell the
difference.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-single-pass-armed-dispatch/54-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State one bound in the Anthropic-spend sentence and make the test check the meaning</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - operator-claude-plugin/scripts/write_grant.py around lines 236-262 (where `anthropic_usd` and its `PROJECTED` basis are assembled) and around lines 290-310 (the rendered block: the provider-credits table header, then the Anthropic sentence, then the executions line).
    - operator-claude-plugin/config/cost_rates.json, the `anthropic_usd_per_record` entry — value 0.068624, `confidence: "measured"`, citation "Phase 22 armed canary cost ledger, executions 332 and 337, both 2026-07-30 — consistent across both fires. This is the all-in observed figure and the one the estimator multiplies by record count."
    - operator-claude-plugin/tests/test_write_grant.py::test_the_anthropic_figure_is_labelled_projected_never_measured — its final assertion is the pin to replace; the three basis assertions above it are correct and must survive untouched.
  </read_first>
  <behavior>
    - The rendered block still shows the same dollar figure and still labels its basis `PROJECTED`, never `MEASURED` — that is 54's own T-54-03 fix and must not regress.
    - The Anthropic sentence asserts exactly one relationship between the displayed figure and real
      spend. Two mutually exclusive bound-words no longer appear together.
    - A test reading only the Anthropic sentence (not the whole rendered block) fails if either
      discarded framing returns.
    - The provider-credits table header, which legitimately describes credits as a ceiling, is
      untouched and must not be caught by the new assertion.
  </behavior>
  <action>
    Pick the framing the rate's own derivation supports, and delete the other.

    The evidence decides it: `anthropic_usd_per_record` is an observed all-in average from two
    canary executions on one dated run, multiplied by record count. An average of two observations
    is neither an upper bound real spend cannot exceed nor a lower bound it is guaranteed to reach
    — real spend can land either side of it. So neither of the two bound-words currently in that
    sentence is correct, and the review's own suggested replacement is the right one: describe the
    figure as a projection from the dated rate table above, not a measurement, and keep the
    existing parenthetical explaining that this repo never reads back real Anthropic usage. Do not
    keep one bound-word and drop the other; drop both.

    Then replace that test's final assertion. Scope it to the Anthropic sentence, not to the whole
    rendered block: extract the single line of `figures["block"]` containing the Anthropic-spend
    text and assert on that line alone. This scoping is load-bearing — the provider-credits table
    header a few lines above legitimately describes credits as a ceiling (credits genuinely are
    one), so a block-wide negative assertion would fail on correct, unrelated text.

    On that extracted line, assert the projection wording is present AND that neither discarded
    bound-word appears. Leave the three `basis` assertions in that test exactly as they are.

    Update the test's docstring to say what it now guards: not just that the figure is labelled
    projected, but that the sentence commits to one bound — with the one-line reason from
    cost_rates.json's citation, so a future reader can re-derive the choice instead of re-arguing
    it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
    The rendered Anthropic-spend sentence states one relationship between the figure and real
    spend. The pinning test reads only that sentence, asserts the projection framing, rejects both
    discarded bound-words, and still asserts the `PROJECTED`/`MEASURED` bases unchanged. The
    provider-credits table header is untouched. The full Python suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the grant envelope -> the operator's spend decision | this rendered text is what an operator consents on before a write is armed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.07-01 | Repudiation | `write_grant.envelope()` rendered cost block | medium | mitigate | The sentence commits to one bound so an operator's consent is given against an unambiguous claim; the test pins the meaning rather than a substring |
| T-54.07-02 | Information Disclosure | presenting a projection as a measurement | medium | mitigate | The `PROJECTED` basis assertions from 54-01 survive untouched and are re-run by this task's verify |
| T-54.07-03 | Tampering | live HubSpot / n8n state | low | accept | This plan touches no built workflow, no live instance and no arming path; nothing is deployed and 0 n8n executions are consumed |
| T-54.07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; package-legitimacy gate not triggered |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — full Python suite green.
- No built workflow JSON changes; no deploy; no arming; 0 n8n executions.
</verification>

<success_criteria>
- WR-04: the Anthropic-spend sentence states exactly one bound, chosen against `cost_rates.json`'s
  own citation rather than by preference, and the pinning test would catch a regression to either
  discarded framing.
- The provider-credits table's own legitimate ceiling wording is not collateral damage.
</success_criteria>

<output>
Create `.planning/phases/54-single-pass-armed-dispatch/54-07-SUMMARY.md` when done
</output>
