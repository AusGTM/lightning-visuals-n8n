---
phase: 54-single-pass-armed-dispatch
plan: 04
type: execute
wave: 2
depends_on: ["54-03"]
files_modified:
  - n8n/wf_review_decision_cloud.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - operator-claude-plugin/skills/review-triage/SKILL.md
  - .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md
autonomous: true
requirements: [G-3]

must_haves:
  truths:
    - "The built review-decision workflow JSON is regenerated by scripts/build_cloud_workflows.py and never hand-edited."
    - "The deployed workflow is reloaded by a deactivate/reactivate bounce, and the new contacts behaviour is read back out of the deployed instance, not out of the local file."
    - "The deploy is DISARMED: the review write flag and both record allowlists are empty in the deployed instance afterwards, confirmed by an independent re-read."
    - "review-triage/SKILL.md no longer tells the operator that every contact has nothing to approve."
    - "The outcome vocabulary is identical across the source module, the client tuple and the committed workflow JSON."
  artifacts:
    - n8n/wf_review_decision_cloud.json
    - .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md
  key_links:
    - "n8n/code/reviewDecision.js -> build_review_decision_cloud() inlines it verbatim -> n8n/wf_review_decision_cloud.json -> the deployed instance"
    - "the deployed instance -> the operator surface review-triage/SKILL.md describes"
  prohibitions:
    - "The projected figure is never presented as a measurement."
    - "No write without an armed, record-scoped window."
    - "`approve` never means two different things."
    - "No hand-edit of n8n/wf_*.json."
    - "The SJ-3 residual is recorded, never silently fixed and never silently dropped."
---

<objective>
Get 54-03's contacts approve branch into the RUNNING n8n instance, disarmed, and correct the
operator-facing sentence that currently states the opposite of what the endpoint now does.

Purpose: a stored workflow proves nothing — only a bounce reloads a running instance, and only a
read out of the deployed content proves what will execute (project memory:
`n8n-stored-vs-running-content`).

Output: a regenerated workflow JSON, a deployed and bounced instance, a dated deploy record, and
truthful triage wording.

**Execution budget: 0-1 n8n executions.** The deploy and bounce themselves consume none; the
read-back is an API read. If a smoke execution is used to read deployed content, it is one, and it
is recorded in the deploy record. No provider credits, no Anthropic calls — this workflow has no
waterfall on it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-single-pass-armed-dispatch/54-PATTERNS.md
@.planning/phases/54-single-pass-armed-dispatch/54-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the workflow JSON from source and diff what moved</name>
  <files>n8n/wf_review_decision_cloud.json</files>
  <read_first>
    - scripts/build_cloud_workflows.py `build_review_decision_cloud()` (around lines 3310-3345 — the docstring naming the `Review Contact/Company Decision Update` nodes and the inlining of `reviewDecision.js`)
    - scripts/build_cloud_workflows.py around line 7226 and the `splice_write_gates` call around lines 7631-7632 (the shared review write gate — this plan must not change it)
    - .planning/phases/54-single-pass-armed-dispatch/54-PATTERNS.md (the deploy sequence: edit source, rebuild, diff, disarmed deploy, bounce, independent re-read)
    - CLAUDE.md §10.3.1 and §13.0 (never hand-edit `n8n/wf_*.json`; `scripts/build_cloud_workflows.py` is the sole author)
  </read_first>
  <action>
    Run `python3 scripts/build_cloud_workflows.py` and let it regenerate the built workflow JSONs.
    Do not open `n8n/wf_review_decision_cloud.json` in an editor and do not patch it by hand — the
    builder is its only author.

    Diff the result. The expected change is confined to the inlined `reviewDecision.js` source inside
    the review-decision workflow's Code node (plus `reviewApply.js`'s new parameter wherever that
    module is inlined). Assert, by reading the diff:
    - no node was added or removed in `wf_review_decision_cloud.json`;
    - no connection changed;
    - the write-gate constants and the allowlist constants are untouched;
    - the SECOND built file that embeds `reviewApply.js` — `n8n/wf_scheduled_maintenance_cloud.json`,
      whose `Apply Review` node is built by `ENRICH_APPLY_REVIEW` around line 6350 — changes too, and
      its change is limited to the third-parameter edit. That node calls `reviewApply(candidateJson,
      row)` with two arguments, so its behavior is unchanged by construction; verify that by reading
      the diff, not by assuming it.

    If the diff touches anything else, STOP and report it rather than deploying — an unexplained node
    delta in a built file means the source edit reached further than intended.

    Note the known diff noise this repo already documents (credentials, `webhookId`, the
    executeWorkflow placeholder) and do not count it as a change.
  </action>
  <verify>
    <automated>python3 scripts/build_cloud_workflows.py &amp;&amp; python3 -c "import json;d=json.load(open('n8n/wf_review_decision_cloud.json'));print('nodes',len(d['nodes']))" &amp;&amp; .venv/bin/python -m pytest operator-claude-plugin/tests/test_review_outcome_parity.py operator-claude-plugin/tests/test_review_decision.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/build_cloud_workflows.py` exits 0 and `git status --porcelain n8n/` shows exactly two modified files: `wf_review_decision_cloud.json` and `wf_scheduled_maintenance_cloud.json`. A third modified built file stops the task.
    - The node count in BOTH those files is identical before and after (record all four numbers in the SUMMARY).
    - `git diff -U0 -- n8n/wf_scheduled_maintenance_cloud.json | grep -cE '^[+-].*reviewApply\('` is 0 — the maintenance loop's own two-argument call site is unchanged.
    - `git diff -- n8n/wf_review_decision_cloud.json | grep -cE '^\+.*"name": "'` is 0 — no node added.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_review_outcome_parity.py -q` exits 0 — the three-way outcome vocabulary still matches, which is only true if no new outcome word was invented.
    - `git diff -- n8n/ | grep -cE '^\+.*ALLOW_HUBSPOT_(REVIEW|RECORD)_WRITES = "true"'` is 0 — the built file ships disarmed.
  </acceptance_criteria>
  <done>The built workflow carries the new contacts branch and nothing else moved.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy disarmed, bounce, and read the new behaviour back out of the running instance</name>
  <files>.planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md</files>
  <read_first>
    - scripts/deploy_n8n_workflows.py (its CLI, the `ALLOW_N8N_DEPLOY` gate around line 248, and its fresh-GET diff derivation)
    - operator-claude-plugin/scripts/n8n_control.py `apply_mutation` (the deactivate/PUT/restore-prior-active sequence — the reactivation is what forces the running instance to reload, D-18)
    - scripts/verify_live_write_safety.py (the existing read-only verifier for the deployed write-safety constants — reuse it, do not write a second one)
    - project memory `n8n-stored-vs-running-content` (a bare PUT never reloads a running workflow; a stored read-back proves stored content only)
  </read_first>
  <action>
    Deploy the regenerated review-decision workflow with the existing deploy script, DISARMED. The
    deploy gate is a deploy gate, not an arming variable — this task must not set any arming
    variable, must not call `armed_window` or `arm_for_dispatch`, and must not add a record to any
    allowlist. Arming for the live proof belongs to 54-05 and to the operator.

    Sequence, in order, with each step's evidence recorded:
    1. Deploy the workflow.
    2. Bounce it — deactivate then reactivate — so the running instance reloads. A PUT alone does not.
    3. Read the deployed content back INDEPENDENTLY (a fresh GET, not the response of the PUT) and
       confirm the deployed Code node contains the contacts approve branch. Confirm the node count
       matches Task 1's.
    4. Run the read-only write-safety verifier against the deployed instance and confirm the review
       write flag is not enabled and both record allowlists are empty.
    5. Confirm the workflow is active afterwards.

    Write `.planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md` with: the date, the
    workflow id, the node count before and after, the exact commands run, the read-back evidence for
    step 3, the verifier output for step 4, the active state for step 5, and the count of n8n
    executions this task consumed. If any step's evidence is a stored read rather than a fresh GET,
    say so — that distinction is the whole point of the bounce.

    Deploy the REVIEW-DECISION workflow only. `n8n/wf_scheduled_maintenance_cloud.json` also changed
    in Task 1 (it embeds the same engine module) — state in the deploy record that its delta is
    committed but NOT deployed by this plan, that it is behavior-identical for the two-argument call
    site it uses, and that it rides the next maintenance deploy. Deploying the maintenance workflow
    here would put a scheduled-job workflow's bounce inside this phase's blast radius for no gain.

    If the deploy is refused for want of credentials, record `skipped (no n8n creds)` as this repo's
    prior phases do, make no PUT, and hand the operator the exact command. A skipped deploy is a
    disclosed outcome, not a failure to hide — but 54-05 cannot start until it has landed.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md &amp;&amp; grep -qiE "bounce|deactivat" .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md &amp;&amp; grep -qiE "allowlist" .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md</automated>
  </verify>
  <acceptance_criteria>
    - 54-DEPLOY-RECORD.md records all five steps with their evidence, dated, plus the execution count consumed.
    - The read-back in step 3 is stated as a fresh GET against the deployed workflow, distinct from the PUT response.
    - Step 4's recorded verifier output shows the review write flag not enabled and both allowlists empty.
    - Step 5 records the workflow active.
    - `grep -rn "arm_for_dispatch\|armed_window" .planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md` returns no match — nothing was armed by this task.
    - The deploy record states that `wf_scheduled_maintenance_cloud.json`'s delta is committed and not deployed, and why.
  </acceptance_criteria>
  <done>The running instance answers a contacts approve the new way, and it is armed for nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the triage sentence that says every contact has nothing to approve</name>
  <files>operator-claude-plugin/skills/review-triage/SKILL.md</files>
  <read_first>
    - operator-claude-plugin/skills/review-triage/SKILL.md lines 105-135 (step 5's non-writing-outcome list — the sentence at lines 120-122 is the one that becomes false, and step 6's "that yes is the arm" wording which must NOT change)
    - .planning/phases/54-single-pass-armed-dispatch/54-03-SUMMARY.md (which option the operator chose at 54-03's checkpoint, and the exact message the contacts branch now returns)
    - .planning/WALK-2026-08-25-vocab05.md (VOCAB-05: an affirmative binds to the shown proposal; ambiguity is not consent — the register step 6 already uses)
  </read_first>
  <action>
    Rewrite step 5's non-writing-outcome bullet that currently reads "Every contact is in this
    position, and so is every record flagged as a possible duplicate. There is a reason to record,
    but nothing to approve."

    Two halves, and only one is now false. Contacts: approving now writes — say what it does in
    operator words, matching the message the endpoint actually returns (from 54-03's SUMMARY): the
    value was already saved when the record was flagged, and approving takes it out of the queue and
    records who decided. Duplicates: unchanged, still nothing to promote — keep that half.

    Do not weaken step 6. The confirm-then-submit ceremony, and "that yes is the arm" bound to the
    exact write just shown, stay exactly as they are: a contacts approve is now a real write, so it
    needs step 6's consent more than before, not less. VOCAB-05 — the affirmative binds to the
    proposal shown; anything ambiguous is not consent and is not armed.

    Add nothing about node names, gates or engines. The operator reads consequences.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q -k "review"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q -k "review"` exits 0.
    - `grep -c "Every contact is in this position" operator-claude-plugin/skills/review-triage/SKILL.md` is 0.
    - The duplicates half of the original bullet is still present.
    - `git diff -- operator-claude-plugin/skills/review-triage/SKILL.md | grep -cE '^\-.*that yes is the arm'` is 0 — step 6's consent wording is untouched.
    - The rewritten bullet's wording matches the message the deployed branch returns, quoted in the SUMMARY.
  </acceptance_criteria>
  <done>The triage skill describes what the endpoint now does, and the consent step it depends on is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local built JSON -> deployed n8n instance | A PUT crosses into the system that holds HubSpot credentials |
| deployed instance -> operator's mental model | What the skill says the endpoint does is what an operator consents to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-14 | Elevation of Privilege | the deploy | high | mitigate | Deploy DISARMED; the write-safety verifier's read-only output showing the flag off and both allowlists empty is an acceptance criterion, and the deploy record is grep-asserted to contain no arming call |
| T-54-15 | Tampering | `n8n/wf_review_decision_cloud.json` | high | mitigate | Builder-authored only; a node-count equality and a no-node-added diff assertion catch a hand-edit or an over-reaching source change before the PUT |
| T-54-16 | Repudiation | stored-versus-running content | high | mitigate | Bounce required, and the read-back must be a fresh GET distinct from the PUT response — recorded as such in the deploy record |
| T-54-17 | Spoofing (of consent) | review-triage step 6 | high | mitigate | Step 6's consent ceremony is explicitly untouched and diff-asserted; a contacts approve is now a real write, so its consent requirement increases rather than relaxes (VOCAB-05) |
| T-54-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install in this plan; if one becomes necessary the package-legitimacy gate and its blocking human checkpoint apply first |
</threat_model>

<verification>
- `node --test tests/n8n/*.test.mjs` and `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` green.
- The deployed instance's own content contains the new contacts branch, read by a fresh GET after a bounce.
- The deployed instance is disarmed: review write flag off, both allowlists empty, workflow active.
- n8n executions spent: 0-1, recorded exactly in 54-DEPLOY-RECORD.md. Provider credits: 0. Anthropic calls: 0.
</verification>

<success_criteria>
The running review-decision workflow answers a contacts approve with a write, it is armed for
nothing, and the operator-facing triage wording matches it.
</success_criteria>

<output>
Create `.planning/phases/54-single-pass-armed-dispatch/54-04-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `n8n/wf_review_decision_cloud.json` — regenerated by the builder, node count unchanged
- `.planning/phases/54-single-pass-armed-dispatch/54-DEPLOY-RECORD.md` — deploy, bounce, fresh-GET read-back, disarm verification, execution count
- Corrected step-5 wording in `review-triage/SKILL.md`, with step 6's consent ceremony untouched
</output>
