---
phase: "61-autonomous-batch-runs"
plan: "61-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md
  - operator-claude-plugin/tests/test_spike_verdict_61.py
autonomous: false
requirements: [RUN-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the spike's six questions — enumerated as the literal ids `Q-01` through `Q-06`, four per substrate plus arithmetic and the concurrency cap — carries a verdict AND a basis word, and the doc test asserts every id is present (REVIEW-C2)."
    - "The execution-count arithmetic for a 40-record and a 300-record batch is stated per candidate substrate, against the 2,500/month budget, with its inputs named."
    - "A question that cannot be answered without a live call is recorded as `unknown` with the read-only command that would answer it — never guessed and never silently dropped."
    - "Every premise names the plans and tasks that depend on it, so an `[unknown]` has an owner and 61-05's halt gate is a lookup rather than a judgement (REVIEW-04)."
    - "Every load-bearing `[unknown]` is put in front of the operator BY NAME at the checkpoint, with its command — the operator either returns the probe result or decides explicitly to proceed under it (REVIEW-04)."
    - "The run-state location is DECIDED by the operator at a checkpoint, not chosen by the executor."
  artifacts:
    - .planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md
    - operator-claude-plugin/tests/test_spike_verdict_61.py
  key_links:
    - "61-05's premise guard reads this doc's `## Premises` section by its exact heading — the doc is a machine-read contract, not only prose."
---

<objective>
Decide what n8n Cloud's execution model actually permits for submit/poll/resume, BEFORE any
task is planned on top of it (D-61-08 carries Phase 55's spike-first warning verbatim).

Purpose: the current shape holds a connection open for ~100 s per 2-record chunk. Whether a
run can outlive that window, where its state can live, and what a batch costs in executions
against a hard 2,500/month budget are all decided by the platform, not by our code. Planning
61-05 against an unknown execution model is how this phase fails.

Output: `61-SPIKE-VERDICT.md` — a verdict per question with an explicit basis word, and an
operator decision on run-state location.

This plan writes NO production code. Its deliverable is a decision document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-autonomous-batch-runs/61-CONTEXT.md
@.planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md
@operator-claude-plugin/scripts/chunking.py
@operator-claude-plugin/scripts/watch.py
@operator-claude-plugin/scripts/run_manifest.py
@operator-claude-plugin/scripts/executions_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the candidate async substrates from repo evidence, with a verdict each</name>
  <files>.planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md, operator-claude-plugin/tests/test_spike_verdict_61.py</files>
  <read_first>
    scripts/build_cloud_workflows.py lines 4640-4720 (the `responseMode: responseNode` +
    "Respond to Webhook" wiring and its D-22 zero-items note) and lines 5420-5440
    (`respondWith: allIncomingItems`); operator-claude-plugin/scripts/chunking.py's module
    docstring (the ~100 s Cloudflare ceiling and why a timeout is a CHUNK FAILURE);
    operator-claude-plugin/scripts/watch.py lines 1-70 (the bounded watch, the measured
    32-39 s single-record run, and the ~100 s webhook response window it names).
  </read_first>
  <action>
    Create `61-SPIKE-VERDICT.md` with a `## Substrates` section carrying one subsection per
    candidate. REVIEW-C2: the six questions this spike answers get STABLE IDS, written into the
    doc as the literal tokens `Q-01`…`Q-06`, because `must_haves` refers to "the spike's six
    questions" and an unenumerated set cannot be asserted. The four per-substrate questions,
    each stated by id in every substrate subsection:
    - `Q-01` — does a run started this way outlive the ~100 s synchronous window?
    - `Q-02` — does the client get a durable handle back?
    - `Q-03` — what survives an n8n Cloud restart mid-run?
    - `Q-04` — how many n8n executions does it cost per record and per progress read?
    And the two that are answered once for the whole spike, in Task 2's section:
    - `Q-05` — what does a 40-record and a 300-record batch cost in executions, per substrate,
      against the 2,500/month budget?
    - `Q-06` — does n8n Cloud impose a concurrent-execution cap on this plan?

    The four candidates to assess, all four, none skipped:
    1. Respond-immediately — `responseMode: responseNode` with the Respond node placed at the
       FRONT of the chain rather than the end, so the HTTP response fires before the work.
    2. Wait node + `$execution.resumeUrl` — the execution itself as the state machine.
    3. Sub-workflow dispatch (`Execute Workflow` with wait-for-completion off).
    4. Today's synchronous client-driven chunk loop, kept, with the ceiling raised.
       REVIEW-C3: label this one **`BASELINE — not eligible`** in its own subsection heading.
       It is assessed so the other three have something to be measured against, and it is the
       shape this phase exists to replace (D-61-08: a run bounded by the synchronous window is
       not an async run). Selecting it is not an available outcome of this spike, and writing
       the label into the template is what stops the verdict prose promoting it by accident.

    Every claim carries a basis word from this closed set, as its own bracketed token on the
    line: `[measured]` (read out of live execution history already committed to this repo),
    `[derived]` (arithmetic over a measured input, inputs named), `[documented]` (an n8n
    behaviour stated in this repo's own code comments or a cited n8n doc URL), `[unknown]`.

    NO LIVE CALLS. A claim that would need one is `[unknown]` plus the exact read-only command
    an admin could run — never a guess dressed as a finding. This is the same discipline
    54-MEASUREMENT.md applied when it refused to report a provider-credit delta it could not
    support.

    Per D-61-08, record explicitly that the client already mints its own `run_id`
    (`chunking.DispatchOutcome.run_id`), so "submit returns a run id" is already solved
    client-side — the open question is progress readable WHILE the run is in flight.

    Create `operator-claude-plugin/tests/test_spike_verdict_61.py` in this task, carrying three
    test functions named so `-k substrates`, `-k arithmetic` and `-k premises` each select one:
    Tasks 2 and 3 verify by selecting their own function out of this same file, so it must exist
    from the first task rather than arriving at the end. Write the substrates function fully
    here; leave the other two as the assertions Tasks 2 and 3 will fill, each currently failing
    against the unwritten section it checks — a test that passes over a section nobody has
    written yet is not a gate.

    The test reads the verdict doc as text (no import, no execution, no network). Resolve the
    doc's path by globbing BOTH `.planning/phases/` and `.planning/milestones/*-phases/`: this
    repo archives a phase directory into the milestone tree at milestone completion, and a
    hardcoded phase path would make this test fail spuriously the day that happens.

    REVIEW-A7 (all three reviewers, "claim line is a fragile heuristic"): DEFINE "claim line"
    narrowly, in the test's own module docstring, and implement exactly that definition — a
    claim line is a bullet (`- `) or numbered (`N. `) entry inside one of the three checked
    sections, after excluding headings, blank lines, table rows (a line whose stripped form
    starts with `|`), and anything inside a fenced block. Prose paragraphs, commands and
    formulas are NOT claim lines. A lint whose subject is undefined fights the author over
    formatting instead of over substance; write the definition down once and let the test and
    the author read the same rule.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_spike_verdict_61.py -q -k substrates</automated>
  </verify>
  <done>All four substrates have a subsection, the fourth labelled `BASELINE — not eligible`; each answers `Q-01` through `Q-04` by id; every claim line carries exactly one basis token.</done>
</task>

<task type="auto">
  <name>Task 2: Cost a 40-record and a 300-record batch in executions, per substrate, offline</name>
  <files>.planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md</files>
  <read_first>
    .planning/phases/54-single-pass-armed-dispatch/54-MEASUREMENT.md in full — it carries the
    only `[measured]` execution figure this repo owns (1 execution for a single-record,
    single-chunk send, execution `11960`) and the named residual (the multi-chunk case was
    never counted). operator-claude-plugin/scripts/chunking.py `chunk_ceiling` (the ceiling is
    read from config, never defaulted). operator-claude-plugin/scripts/write_grant.py's
    `envelope()` `projected_executions` formula and the comment naming it unverified.
  </read_first>
  <action>
    Add a `## Execution arithmetic` section, headed with the id `Q-05` per REVIEW-C2. For a
    40-record and a 300-record batch, state the
    projected n8n execution count per substrate from Task 1, showing the formula and naming
    every input (the configured `max_records_per_chunk`, `max_rows_per_match_request`, and the
    measured 1-execution-per-single-record-chunk data point). Compare each total against the
    2,500 executions/month budget and say plainly which substrates fit and which do not.

    Cost the PROGRESS READ separately and per run-state store, because this is the number that
    decides the store and it is not the same number as the run itself:
    - n8n `staticData` — has no public read endpoint, so a progress read is a webhook round
      trip: 1 execution per poll. State what a 30-minute run at the substrate's own poll
      interval costs.
    - A HubSpot object (a property or a custom object on the record) — 0 n8n executions per
      poll; costs a HubSpot API read instead.
    - The n8n public executions API (`executions_client.py`, already built, read-only) — 0 n8n
      executions per poll.
    - The client's own `run_manifest.py` — 0 of everything, but readable only by the process
      that wrote it, which is the constraint that decides whether it is sufficient.

    Note against the arithmetic that `envelope()`'s `chunk_count + record_count` projection
    already disagreed with a measurement once (54-MEASUREMENT: projected 2, measured 1) — so
    label any figure that leans on that formula `[derived]`, never `[measured]`.

    Record under the id `Q-06` whether n8n Cloud imposes a concurrent-execution cap on this
    plan. If that cannot be
    established from repo evidence or a cited n8n doc, it is `[unknown]` with the command —
    a cap changes whether a fan-out substrate is viable at all, and guessing it is exactly the
    documented-vs-actual failure this project has now paid for three times in two days.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_spike_verdict_61.py -q -k arithmetic</automated>
  </verify>
  <done>Both batch sizes are costed per substrate against 2,500/month; per-poll cost is stated for all four run-state stores; the concurrency cap carries a verdict or an `[unknown]` with its command.</done>
</task>

<task type="auto">
  <name>Task 3: Write the machine-readable premises block and its completeness test</name>
  <files>.planning/phases/61-autonomous-batch-runs/61-SPIKE-VERDICT.md, operator-claude-plugin/tests/test_spike_verdict_61.py</files>
  <read_first>
    operator-claude-plugin/tests/test_skill_sequence_coverage.py lines 1-60 — the same
    pure-text-and-AST-analysis discipline (never imports or executes what it inspects) is what
    this new test follows.
  </read_first>
  <action>
    Add a `## Premises` section to the verdict doc: the load-bearing facts 61-05 will be written
    against. This section is a contract, not a summary — 61-05's Task 1 re-reads it by this exact
    heading and halts if a premise is contradicted at execution time.

    Each premise is a numbered entry carrying, in a fixed order: a premise id (`P-01`, `P-02`,
    …), one sentence, its basis token, its source, and — REVIEW-04, the finding that a recorded
    `[unknown]` today has no owner — **the plans and tasks that depend on it**, written as ids
    (`61-05 T2`, `61-06 T2`). That last column is what turns 61-05's halt gate from a judgement
    into a lookup: 61-05 Task 1 selects the premises naming its own task ids and halts if any of
    THOSE is unresolved, rather than an executor deciding by feel which unknown was load-bearing.
    A premise nothing depends on is either deleted or marked `dependents: none (context only)`.

    Add a `## Unresolved` section listing every `[unknown]` premise by its id, with the exact
    read-only command that would resolve it and who can run it (operator or n8n admin — never
    the executor, which is why they are unresolved). This is the list Task 4's checkpoint reads
    out. REVIEW-04 in full: an unknown is not owned by being recorded; it is owned by being put
    in front of the person who can answer it, with the answer's dependents named.

    Then complete `operator-claude-plugin/tests/test_spike_verdict_61.py`, created in Task 1.
    Fill its premises function and raise the whole file to assert:
    - every required heading is present, by exact string, including `## Unresolved`;
    - REVIEW-C2: each of the six question ids `Q-01`…`Q-06` appears in the doc by its literal
      token, `Q-01`–`Q-04` inside EVERY substrate subsection and `Q-05`/`Q-06` inside
      `## Execution arithmetic`. This assertion lives here rather than in Task 1 because this
      task raises the whole file; asserting an id set against sections two of three tasks have
      not written yet is a test that fails for the wrong reason;
    - all four substrate subsections exist, the fourth carrying the literal
      `BASELINE — not eligible` (REVIEW-C3), and each contains all four sub-question answers;
    - every claim line (as Task 1's docstring defines it) inside `## Substrates`,
      `## Execution arithmetic` and `## Premises` carries exactly one basis token;
    - the `## Premises` list is non-empty; every entry carries an id, a basis token AND a
      non-empty dependents field;
    - every premise whose basis token is `[unknown]` appears in `## Unresolved` with a command,
      and every `## Unresolved` entry names a premise id that exists;
    - no claim line is a placeholder (reject a line whose body is `TBD`, `TODO`, or empty).

    The last assertion is what makes this test worth having: without it a stub document passes
    a structural check. Register the test path in this plan's summary so 61-05 can find it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_spike_verdict_61.py -q</automated>
  </verify>
  <done>The test passes against the real doc and fails against a doc with any section stubbed — demonstrate the second by running it once against a temp copy with one section emptied. Every premise carries a dependents field; every `[unknown]` premise appears in `## Unresolved` with its command and its owner.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Operator decides where run state lives</name>
  <decision>Where run state lives for an async batch run — and what to do about each unresolved premise.</decision>
  <context>
    D-61-08 names this an open design decision with a different failure mode per option when
    n8n restarts mid-run. Tasks 1-2 have now costed each option in n8n executions per progress
    poll against a hard 2,500/month budget — that arithmetic, not a preference, is what should
    decide it. This choice shapes every task in 61-05 and is costly to reverse once built.

    READ OUT `## Unresolved` IN FULL BEFORE ASKING FOR THE DECISION. REVIEW-04: the spike is
    allowed to finish with `[unknown]`s, and 61-05 must halt on an unresolved premise its own
    tasks depend on — so a spike that ends without the operator having SEEN those unknowns
    produces a formally complete document and a stalled phase. For each entry, present: the
    premise id, the one-line question, the read-only command, who can run it, and which
    plan/task halts without it. Restart survival, detached sub-workflow behaviour, the
    concurrent-execution cap and execution billing are the four most likely to land here; none
    of them is knowable from repo source (61-REVIEWS U1/U2/U3 grade them uncheckable).

    The operator has three ways to close each one, and the choice is recorded in the doc next
    to the premise: return the probe result (the executor then rewrites that premise with its
    new basis token); decide explicitly to proceed under the unknown, naming the substrate
    chosen despite it; or defer, which means 61-05 halts by design at Task 1 rather than
    improvising. Deferring is a legitimate answer — a stall the operator chose is cheaper than
    an async substrate built on a guess.

    REVIEW-C1 — THE OPTION TEXTS BELOW ARE PLANNER-WRITTEN PROVISIONALS, NOT FINDINGS. They
    were composed before the spike ran, so each pro/con that leans on a spike question carries
    a bracketed basis token naming that question. Before reading an option out, RESTATE its
    pros and cons from the completed verdict doc and read each one WITH the basis it actually
    earned there: where the underlying premise closed as `[unknown]`, say so out loud
    ("restart survival is unestablished — P-NN is in `## Unresolved`") instead of reading the
    provisional sentence as an established fact. An operator selecting a substrate on wording
    stronger than the evidence is the exact failure this whole spike exists to prevent, and it
    would be this checkpoint's own prose that caused it.
  </context>
  <options>
    <option id="n8n-static-data">
      <name>n8n workflow staticData</name>
      <pros>State lives with the run; no new external dependency.</pros>
      <cons>No public read endpoint — every progress poll is a webhook round trip [basis: Q-04], and whether that round trip is billed as an execution against the 2,500/month budget is Q-04's own answer, not a given. Read Task 2's figure and its token, not this line.</cons>
    </option>
    <option id="hubspot-object">
      <name>A HubSpot property or custom object on the run</name>
      <pros>Zero n8n executions per progress read [basis: Q-04]; survives an n8n restart [basis: Q-03 — restart survival is the question most likely to close `[unknown]`; if it did, this pro is unestablished and must be read out as such]; visible to the operator in the CRM they already use.</pros>
      <cons>Writes to HubSpot for bookkeeping, on a portal with no rollback; needs a property that does not exist yet.</cons>
    </option>
    <option id="executions-api">
      <name>The n8n public executions API, read via the existing executions_client.py</name>
      <pros>Zero n8n executions per read [basis: Q-04]; the client is already built and read-only; needs nothing new deployed.</pros>
      <cons>Correlation to a specific run is a time-proximity guess today — this one IS established from repo source (watch.py's own D-12 note: neither deployed workflow returns an execution id).</cons>
    </option>
    <option id="client-manifest">
      <name>The client's existing run_manifest.py, extended</name>
      <pros>Already built, already durable across a plugin update, already carries per-row verdicts and a resume rule; zero executions, zero HubSpot writes — all established from repo source, not from a spike question.</pros>
      <cons>Readable only by the machine that wrote it, so it cannot report on a run that outlives the session that started it.</cons>
    </option>
  </options>
  <resume-signal>Select: n8n-static-data, hubspot-object, executions-api, client-manifest, or a named combination — AND, for each entry in `## Unresolved`, either its probe result, "proceed under P-NN", or "defer".</resume-signal>
</task>

</tasks>

<review_dispositions>
Cross-AI review (`61-REVIEWS.md`, 2026-08-30). Findings assigned to this plan:

- **HIGH 4 — load-bearing `[unknown]`s unowned before 61-05's halt gate.** INCORPORATED, Task 3
  (premises carry a dependents column; new `## Unresolved` section with command + owner; both
  asserted by `test_spike_verdict_61.py`) and Task 4 (the checkpoint reads `## Unresolved` out
  by name and the resume-signal will not close without a per-unknown answer).
- **HIGH 9 — run-state substrate prejudged by later plans.** INCORPORATED here as the half this
  plan owns: the `## Unresolved`/dependents machinery makes the checkpoint's verdict readable by
  61-05 mechanically. The other half is in 61-04 (where it is REJECTED with reasons for the held
  queue specifically) and 61-05 (accepted).
- **Actionable — "claim line" undefined in the spike lint.** INCORPORATED, Task 1: the test's
  module docstring defines it (bullet or numbered entry; headings, blanks, table rows and fenced
  blocks excluded) and the implementation follows that definition.
- **REJECTED — "split the spike into an offline pass and a read-only probe pass" (gpt
  suggestion).** The probe commands cannot be run by the executor at all: the phase's hard
  constraint is no live calls at execution, and every unresolved question needs operator or n8n
  admin credentials. A second executor pass would be a pass that can only re-record the same
  `[unknown]`. The probes are routed to the human who can run them, at Task 4, which is the
  same work without the extra wave.
**Cycle 2 residuals (C1–C3), all INCORPORATED:**

- **C1 — checkpoint option texts assert facts the spike may leave `[unknown]`.** Task 4
  `<context>`: the option texts are declared planner-written provisionals, each spike-dependent
  pro/con carries a `[basis: Q-NN]` token, and the checkpoint must restate them from the
  completed verdict and say "unestablished" out loud where the premise closed `[unknown]`.
- **C2 — no stable enumerated question set.** Task 1 assigns the literal ids `Q-01`–`Q-04`
  (per substrate) and `Q-05`/`Q-06` (arithmetic, concurrency cap); Task 3's assertion list
  requires every id to be present by literal token; the `must_haves` truth names them. The
  assertion sits in Task 3 because that task raises the whole file — asserting an id set from
  Task 1 against sections not yet written fails for the wrong reason.
- **C3 — candidate 4 not labelled baseline.** Task 1: candidate 4's subsection heading carries
  the literal `BASELINE — not eligible`, asserted by the doc test in Task 3, so the verdict
  prose cannot promote the shape this phase exists to replace.

- **REJECTED (MEDIUM, not in the 23) — "the document test validates formatting, not truth".**
  True and deliberate: a text lint cannot verify that n8n Cloud restarts the way a sentence says
  it does. Truth is defended by the basis tokens (a claim that cannot be sourced must be
  `[unknown]`), by `## Unresolved` reaching the operator, and by 61-05 Task 1 re-asserting the
  premises at execution. Making the lint attempt truth verification would be the guess this
  plan exists to prevent, wearing a test's clothes.
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor | A verdict document is read as fact by a later plan; an unmarked guess becomes a silent premise |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-01 | Spoofing | 61-SPIKE-VERDICT.md | high | mitigate | Every claim carries a basis token; `test_spike_verdict_61.py` fails a claim line without one, so a guess cannot pass as a finding |
| T-61-02 | Tampering | 61-SPIKE-VERDICT.md | medium | mitigate | 61-05 Task 1 re-asserts the `## Premises` block at execution time and halts on contradiction rather than improvising |
| T-61-03 | Information disclosure | verdict doc | low | accept | The doc records execution counts and n8n behaviour; it names no credential, no record id, and no API key |
</threat_model>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/test_spike_verdict_61.py -q` passes.
- Root suite still green: `.venv/bin/python -m pytest -q` (baseline 3365 / 154).
- Plugin suite still green: `.venv/bin/python -m pytest operator-claude-plugin/tests -q` (baseline 1725 / 5).
- Zero n8n executions, zero HubSpot calls, zero provider credits, zero Anthropic calls spent
  by this plan — assert this in the summary, with the same wording 54-MEASUREMENT.md used.
</verification>

<success_criteria>
The operator has selected a run-state location, and 61-05 can be executed against a `## Premises`
block that a test proves is complete and free of placeholders.
</success_criteria>

<output>
Create `.planning/phases/61-autonomous-batch-runs/61-01-SUMMARY.md` when done.
</output>
