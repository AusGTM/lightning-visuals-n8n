---
phase: 53-operator-openable-write-grant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/write_grant.py
  - operator-claude-plugin/scripts/n8n_arming.py
  - operator-claude-plugin/scripts/config_gate.py
  - operator-claude-plugin/tests/test_write_grant.py
  - operator-claude-plugin/tests/test_control_arming.py
autonomous: true
requirements: [GRANT-01, GRANT-03, GRANT-06]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator whose admin has set `allow_write_grants` to the JSON boolean true in operator.local.json can open a write grant and have a dispatch arm under it, with no shell environment variable set anywhere (D-53-01, closes G-2)."
    - "A dispatch that presents no grant behaves exactly as it does today: it refuses unless ALLOW_N8N_ARM reads exactly 'true', at zero HTTP cost (D-53-01, the headless path)."
    - "The settings key is the authority and is re-read from config at arm time, so a fabricated grant object cannot arm a backend whose admin never enabled write grants."
    - "The grant names the lanes it covers, the record set it covers and whether creates are included (GRANT-01)."
    - "GRANT-03 IS ENFORCED INSIDE arm_for_dispatch, not only at a helper a caller could bypass: the grant branch refuses unless the workflow being armed is one of the grant's own workflow ids AND every requested record id and domain is already in the grant's lists. A grant on one lane cannot authorize arming another lane's workflow."
    - "A grant may span both lanes of enrich-before-ingest (D-53-05, operator, 2026-08-25) — the collapse widens WHEN the approval is given, never WHAT it covers."
    - "Opening a grant performs no mutation: it reads, computes and returns; every arm still happens per send inside n8n_arming.armed_window."
    - "plan_grant and open_grant ship in their FINAL shape here — a proposal, then a confirmation with no default that must read exactly yes — so no later wave reshapes a function wave-1 tests already bind to."
    - "The grant exists only as a value returned into the conversation — no file is written, no environment variable is set, no default is supplied for an absent grant (GRANT-06)."
    - "test_control_arming.py::test_the_probe_and_the_arm_gate_use_the_same_comparison is re-pointed in exactly one deliberate edit, with the reason recorded in the test file, and is not deleted or weakened."
  prohibitions:
    - "n8n_arming.disarm gains no authority check of any kind; test_the_disarm_is_NOT_gated_on_the_kill_switch is not edited."
    - "ALLOW_N8N_ARM is not renamed, not removed, and its comparison against the exact string 'true' is not loosened."
    - "scheduled_arm.py is not edited, and tests/test_scheduled_arm.py passes unchanged."
    - "No grant value is written to disk, to the config file, to an environment variable, or to any durable path."
    - "arm_for_dispatch's empty-allowlist refusal is not relaxed for a grant-authorized arm."
    - "No new write-safety constant is declared in any n8n workflow; the declaring set does not move and test_control_flag_parity.py passes unchanged."
  artifacts:
    - operator-claude-plugin/scripts/write_grant.py
    - operator-claude-plugin/tests/test_write_grant.py
  key_links:
    - "config_gate.write_grants_enabled is the ONE definition of the settings-key comparison. config_gate is a leaf (json, pathlib, durable_paths — stdlib only), so both write_grant and n8n_arming import it and neither duplicates the comparison; there is no cycle and no text pin is needed."
    - "n8n_arming._arm_gate(config, grant) decides AUTHORITY; arm_for_dispatch's grant branch decides SCOPE. Both must pass before any transport is constructed."
    - "write_grant.open_grant resolves each named lane to its workflow id through executions_client.resolve_workflow_id, the same resolver scheduled_arm.py already uses."
---

<objective>
Make an operator-openable write grant real, end to end, on one record: an admin sets one key in
`operator.local.json`, the operator opens a grant in conversation, a dispatch arms under it, and the
window disarms as it does today. No shell environment variable is involved on the interactive path.

Purpose: G-2 is the milestone's blocker. `n8n_arming._arm_gate()` requires `ALLOW_N8N_ARM=true` in
the session's shell environment, which an operator in Claude Desktop cannot set, so the documented
operator path ends in a refusal only an admin with terminal access can clear. Every HubSpot write
this client has seen land was landed by an admin from a terminal. This plan moves the interactive
path's authority to a place an operator's own surface can actually satisfy, and leaves the headless
path's authority exactly where it is.

Output: `scripts/write_grant.py` (the grant object, its authority, open and close),
`n8n_arming._arm_gate` taking config and an optional grant, `tests/test_write_grant.py`, and one
deliberate re-point of the arm/probe parity pin.

Scope note: the envelope arithmetic, revocation, expiry and the two proposed guardrails are plan
53-02. The operator-facing config and confirmation surface is 53-03. Skills, docs and the release
are 53-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-REQUIREMENTS.md
@.planning/milestones/v1.1-ROADMAP.md
@.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md
@.planning/quick/260825-contact-company-association/UAT.md
</context>

## Source coverage audit

Every source item this phase must cover, and where it lands. No item is deferred silently.

| Source | Item | Covered by |
|---|---|---|
| GOAL | An operator in Claude Desktop can authorize writes without a terminal | 53-01 T1/T2 |
| GOAL | The authorization is bounded | 53-01 T1 (lanes + record set + creates, enforced in `arm_for_dispatch`), 53-02 T1 (ceiling) |
| GOAL | The authorization is expiring and revocable | 53-02 T2 |
| REQ | GRANT-01 — one exchange, states object types, record set, creates, ceilings | 53-01 T1 (shape), 53-02 T1 (ceilings), 53-03 T2 (the exchange) |
| REQ | GRANT-02 — envelope shown as arithmetic before acceptance | 53-02 T1 (**partial by design** — see below) |
| REQ | GRANT-03 — scoped to a named batch, not a duration | 53-01 T1 |
| REQ | GRANT-04 — expiry on completion, ceiling breach, revocation, session end, error | 53-02 T2 |
| REQ | GRANT-05 — revocation effective at the next SEND boundary (re-scoped by the operator 2026-08-25; the requirement text is updated to match) | 53-02 T2 |
| REQ | GRANT-06 — never inferred, defaulted, remembered, or written to disk | 53-01 T1 + prohibitions in every plan |
| CONTEXT | D-53-01 — admin-set settings key; ALLOW_N8N_ARM retained for headless | 53-01 T1/T2/T3 |
| CONTEXT | D-53-02 — computed worst case is the ceiling | 53-02 T1 |
| CONTEXT | D-53-03 — client-held, session only | 53-01 T1, 53-02 T2 |
| CONTEXT | D-53-03 guardrail — refuse to open when writes already armed | 53-02 T3 |
| CONTEXT | D-53-04 — the grant subsumes the per-send phrase | 53-03 T2, 53-04 T1 |
| CONTEXT | D-53-04 guardrail — bound the disarm unknown, and disarm on the two paths that observed live writes | 53-02 T3 |
| CONTEXT | D-53-05 — one grant spans both lanes of enrich-before-ingest | 53-01 T1 (shape), 53-04 T1 (the one deliberate contract-test edit) |
| UAT | G-1 — three arming surfaces for one write | 53-03 T2, 53-04 T1 |
| UAT | G-2 — write path unreachable from the operator's surface | 53-01 T1/T2, proven in 53-04 T3 |

**GRANT-02 is partial in this phase, and this is the operator's own decision, not a subtraction.**
D-53-02 records it: a ceiling derived from the batch cannot block anything the batch already
implies, so its job here is disclosure. The honest cost model is Phase 54's (the double provider
pass is what makes today's arithmetic wrong), and the refusal-before-starting against the remaining
monthly allowance is Phase 57's. The v1.1 roadmap's Phase 53 entry does not list GRANT-02 among
what this phase closes.

**Not in this phase, and not gaps:** single-pass dispatch (54), async submit-and-poll (55), the
unattended pair pipeline (56), ceilings that bind and post-run proof (57). Deferred by name in
53-CONTEXT.md: expiry inside the shared write-safety gate, a disarm watchdog job, admin-set maximum
ceilings, an admin-minted session token.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — an admin-set key, an opened grant, one send armed under it</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/scripts/n8n_arming.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - `operator-claude-plugin/scripts/n8n_arming.py` (lines 178-260, 262-345, 400-452) — `ARM_ENV_VAR`, `_arm_gate`, `DISPATCH_FLAGS`, `arm_for_dispatch`'s empty-allowlist refusal and its `consequence` sentence, `armed_window.__enter__`/`__exit__`
    - `operator-claude-plugin/scripts/config_gate.py` (lines 40-90, 140-180) — `CAPABILITY_KEYS`, `_CAPABILITY_DESCRIPTIONS`, `require_capability`'s refusal wording convention
    - `operator-claude-plugin/scripts/scheduled_arm.py` (lines 100-125, 190-250) — `_resolve_workflow_ids`, `ENRICHMENT_WORKFLOW_NAME`, and how it composes `armed_window` around a chunked dispatch without touching the gate
    - `operator-claude-plugin/scripts/status.py` (lines 108-140) — `describe_workflow` and `WRITE_SAFETY_FLAGS`, the existing live write-safety read
    - `operator-claude-plugin/scripts/executions_client.py` — `resolve_workflow_id`'s signature
    - `operator-claude-plugin/tests/conftest.py` (lines 1-40) — the flat-name import path and the autouse no-network fixture; `fake_config` and `stub_module_transport_factory` fixtures
    - `.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md` — D-53-01 and D-53-03 verbatim
  </read_first>
  <behavior>
    - Given a config carrying `allow_write_grants` as the JSON boolean true, and a grant opened over
      one record id on the enrichment lane, `arm_for_dispatch` for that record and that lane's
      workflow id proceeds to the mutation with no environment variable set at all.
    - Given the same config and the same grant, `arm_for_dispatch` for a record id NOT in the
      grant's record set refuses inside `arm_for_dispatch` itself, before any transport is
      constructed — not at a helper the caller could have skipped.
    - Given the same config and the same grant, `arm_for_dispatch` for a workflow id that is not
      among the grant's own workflow ids refuses, so a grant cannot authorize arming a workflow it
      does not cover.
    - Given a config WITHOUT the key and a grant object presented anyway, the arm refuses and names
      the missing key.
    - Given no grant and no environment variable, the arm refuses with an empty call log — today's
      behaviour, unchanged.
    - `plan_grant` makes no PUT, POST or activate/deactivate call; only reads appear in the
      transport call log.
    - `open_grant` called without a confirmation argument raises TypeError; with any confirmation
      other than the exact string yes it refuses and opens nothing; with something that is not a
      proposal it refuses.
    - A grant proposed over an empty record set is refused at plan time, not at arm time.
  </behavior>
  <action>
First, add the settings-key authority to `operator-claude-plugin/scripts/config_gate.py` — one
module-level constant `WRITE_GRANT_SETTINGS_KEY = "allow_write_grants"` and one function
`write_grants_enabled(config)` returning True only when `config.get(WRITE_GRANT_SETTINGS_KEY) is
True`. `config_gate` is this plugin's leaf config module (it imports json, pathlib and
`durable_paths`, and `durable_paths` imports only stdlib), so both `write_grant` and `n8n_arming`
can import it with no cycle, and the comparison exists in exactly one place. Do NOT duplicate the
comparison into `n8n_arming` and do not add a text pin holding two copies in agreement — a text pin
asserts a literal appears twice, which is token presence rather than semantic agreement, and one
definition needs neither.

Write the comment saying why identity rather than truthiness: `bool` is an `int` subclass in Python,
the same gotcha `n8n_cadence._read_positive_float`'s WR-03 comment already documents, so the string
"true", the integer 1, the float 1.0 and the string "yes" all read False, and a `1` that parsed as
authority would be a silently weaker gate than the exact-string comparison this key replaces.

Add the key to neither `CAPABILITY_KEYS` nor `_CAPABILITY_DESCRIPTIONS`. D-53-01 is explicit that
`CAPABILITY_KEYS` means "these keys are present" rather than "an admin authorized this", and the
refusal wording depends on the distinction. Living in the same MODULE as `CAPABILITY_KEYS` is not
the same as living in the table; say so in a comment next to the constant.

Then create `operator-claude-plugin/scripts/write_grant.py`. Its module docstring states the three
things a reader needs and cannot infer: (1) per D-53-01 this is the repository's first deliberate
exception to "authority gates are environment variables compared against the exact string true" —
the interactive arm's authority is now an admin-set key in `operator.local.json`, while the probe,
deploy and headless-arm gates stay environment-gated, and a reader who changes one must not assume
the others followed; (2) per D-53-03 the grant is held in the conversation for the session and is
never persisted, inferred or defaulted; (3) the grant is authority and envelope, NOT a held-open
armed window — every send still opens and closes its own `armed_window`, because that is what keeps
the guaranteed disarm the milestone's "what must NOT be lost" list names, and because D-53-04's
"a failed disarm fails that send only" presupposes a per-send disarm exists to fail.

Define the grant as a plain dict, not a class. Deliberate: it has to survive being carried in a
conversation and handed back into a later Python invocation, so it must be JSON-shaped, and a
dataclass would only add a serialization round trip. Its keys: `kind` fixed to `"write_grant"`,
`state` (`"open"` or `"closed"`), `lanes` (the list of lane names it covers), `workflow_ids` (lane
name to resolved workflow id), `object_type`, `record_ids`, `record_domains`, `allow_create`,
`opened_at` as an ISO-8601 UTC string, `label` (the operator's own name for the batch), and
`envelope` (left as None here — 53-02 fills it). Add `closed_reason` and
`consecutive_disarm_failures`, both initialised here so 53-02 has somewhere to write without
reshaping the dict.

`LANES` maps lane name to workflow name: `"enrichment"` to `"LV Enrichment (Cloud template)"` and
`"contacts"` to the contact ingest workflow's name. Read `scheduled_arm.ENRICHMENT_WORKFLOW_NAME`
rather than respelling it. **The review lane is deliberately not grantable** and the docstring says
so: `ALLOW_HUBSPOT_REVIEW_WRITES` is excluded from `n8n_arming.DISPATCH_FLAGS` by 30-01's D-02/D-08e
precisely so arming a dispatch grants nothing on the review path, and `ALLOW_REVIEW_SUBMIT` is its
own environment gate; folding review into a dispatch grant would revoke that separation.

**A grant MAY span both the enrichment and the contacts lanes — D-53-05, operator, 2026-08-25.**
The planner raised the cost of this in full and the operator accepted it explicitly, for speed.
Record the trade in `LANES`'s own comment rather than only in planning, because it removes a
protection a previous phase deliberately installed: with one grant across both lanes, the ingest
authorization is necessarily given before the enriched preview exists, so held rows and merge
conflicts that the enriched preview is the only place to see are authorized unseen (37-CONTEXT
sec 6.3 is the protection being traded). What still holds, and what the tests must show: the
allowlist stays record-scoped to the batch, so the collapse widens WHEN the approval is given and
never WHAT it covers; the enriched preview is still rendered; and revocation still works — the
default flips from ask-again to proceed-unless-stopped.

`plan_grant(config, *, lanes, object_type, record_ids, record_domains, allow_create, label,
transport=None)` performs, in this order: refuse when `config_gate.write_grants_enabled(config)` is
False, with a refusal that names the key, names `operator.local.json`, and says the n8n admin sets
it — the `config_gate.require_capability` register, but not that function, because this is a
settings key and not a capability row. Then refuse an unknown lane by name. Then refuse an empty
record set, reusing `arm_for_dispatch`'s own reasoning verbatim in the message: the deployed
`_writeSafetyAllows` denies everything on an empty allowlist, so a grant over nothing would read as
a grant while granting nothing. Then resolve each named lane's workflow id through
`executions_client.resolve_workflow_id` and refuse when any lane does not resolve. Then return a
PROPOSAL dict — not a grant.

`open_grant(proposal, confirmation, config)` takes the proposal and returns the grant with `state`
set to open. **Ship this pair in its final shape here.** `confirmation` has no default, so a caller
that forgets it gets a TypeError rather than a silent open; only the exact string yes proceeds; and
because it takes a proposal, a caller that skips planning has nothing to open. That is
`control_actions.execute_action`'s contract, reproduced deliberately. Defining the final shape in
wave 1 is the point: a function reshaped in wave 3 would leave wave-1 tests binding to a signature
that no longer exists.

Leave a named seam for 53-02's guardrail A: `plan_grant` accepts `preflight=None` and, when
callable, invokes it with `(config, workflow_ids, transport)` before the proposal is built,
returning its refusal unchanged. The seam exists here so the guardrail lands as a fill rather than a
reshape.

`close_grant(grant, reason)` returns a copy with `state` closed and `closed_reason` set. It performs
no network call and never disarms: with per-send windows there is no window open at close time. Say
that in the docstring, because a reader expecting a disarm here would otherwise think one was
forgotten. 53-02 adds the two guardrail-B paths that DO disarm, for the specific reason that those
two have just observed or inferred a live-write state.

`covers(grant, *, lane, workflow_id, record_ids, record_domains)` returns None when the send is
inside the grant and a refusal dict when it is not. Refuse when the grant is not open, when the lane
is not among the grant's lanes, when `workflow_id` is not the id the grant resolved for that lane,
and when any requested id or domain is outside the grant's own lists. Name the specific offending
values in the refusal — a refusal that says only "outside the grant" makes the operator diff two
lists by eye.

Then edit `operator-claude-plugin/scripts/n8n_arming.py`, and keep two decisions separate.

**Authority** is `_arm_gate(config, grant=None)`, with two branches. With a grant present: return
None when `config_gate.write_grants_enabled(config)` is True and the grant reads open; refuse
naming the key and the file when the key is absent or is anything but the JSON boolean true; refuse
naming the grant's `closed_reason` when the grant is closed. With no grant: the existing environment
branch, byte-identical in behaviour, still comparing against the exact string "true" and still
costing zero HTTP calls.

**Scope** is enforced inside `arm_for_dispatch`'s own grant branch, immediately after the authority
check and before any transport is constructed. This is where GRANT-03 actually binds: compare the
`workflow_id` being armed against the grant's own workflow ids and refuse a mismatch, then assert
that every entry in the normalised `ids` and `domains` lists is already present in the grant's
`record_ids` / `record_domains` and refuse naming the offenders. Do it here, not only in a helper a
later lane skill is supposed to call, because a caller can reach `arm_for_dispatch` directly and a
scope check that can be bypassed is not a scope check. `covers` and `arm_for_dispatch` therefore
answer the same question at two depths on purpose — call `covers` from the grant branch so there is
one implementation and one refusal wording, not two.

The no-grant refusal's detail must keep naming `ARM_ENV_VAR` and must additionally name the grant
route, so an operator reading it learns both remedies rather than only the one they cannot use. Keep
`ARM_ENV_VAR` in the string: `test_with_the_gate_unset_the_arm_refuses_and_makes_no_call_at_all`
asserts on it and keeps binding on the headless path without edits.

Thread the grant through `arm_for_dispatch(workflow_id, record_ids, record_domains, allow_create,
config, transport=None, grant=None)` — keyword, defaulting to None, so every existing caller,
`scheduled_arm.py` included, is unchanged and stays on the environment gate. Do not touch `disarm`.
Do not touch `armed_window` beyond adding the same pass-through `grant=None` keyword so a
grant-authorized send can use the context manager it already has.

Then create `operator-claude-plugin/tests/test_write_grant.py` in the plain-pytest style of
`test_control_arming.py`, reusing its `_base_workflow` shape and the `fake_config` /
`stub_module_transport_factory` fixtures from conftest. The tracer test walks the whole path in one
function: a config with the key set, `plan_grant` then `open_grant` with an explicit yes over one
record id, `arm_for_dispatch` for that record and that lane's workflow id with the grant, an
`armed_window` body, and the disarm — asserting the arm verified, the disarm verified, and that the
environment variable was never set during the test. Add the per-behaviour tests listed above
alongside it, and assert for the two scope refusals that `transport.calls` is empty, which is what
distinguishes a refusal that happened before transport construction from one that merely did not
mutate.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_control_arming.py operator-claude-plugin/tests/test_scheduled_arm.py operator-claude-plugin/tests/test_control_flag_parity.py -q</automated>
    Manual read-back: `git diff operator-claude-plugin/scripts/n8n_arming.py` shows changes confined
    to `_arm_gate`, `arm_for_dispatch`'s signature and `armed_window`'s pass-through — nothing inside
    `disarm`, `set_write_safety` or `disarmed_targets`.
  </verify>
  <done>A dispatch arms with the settings key and an open grant and no environment variable set; a dispatch with neither refuses at zero HTTP cost; a send whose records or workflow id fall outside the grant is refused INSIDE arm_for_dispatch with an empty transport call log; the settings-key comparison exists once, in config_gate; open_grant requires a proposal and an explicit yes with no default; `test_scheduled_arm.py` and `test_control_flag_parity.py` pass without being edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The authority's edges — near misses, absent key, forged grant, headless untouched</name>
  <files>operator-claude-plugin/scripts/write_grant.py, operator-claude-plugin/tests/test_write_grant.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_control_arming.py` (lines 62-105) — `test_every_near_miss_value_refuses`'s parametrisation, the shape to mirror for the settings key
    - `operator-claude-plugin/scripts/control_actions.py` (lines 246-270) — `execute_action`'s no-default confirmation and its exact-yes comparison, the other side of the confirmation parity test
    - `operator-claude-plugin/scripts/n8n_cadence.py` (lines 416-432) — `_read_positive_float`'s WR-03 comment on `bool` being an `int` subclass
    - `operator-claude-plugin/scripts/config_gate.py` (lines 140-180) — the refusal that names the missing key, the file and who holds the value
  </read_first>
  <behavior>
    - The settings key parametrised over the string "true", the string "True", the string "TRUE",
      the string "1", the string "yes", the integer 1, the float 1.0, the string "", the JSON value
      false, the JSON value null and an absent key: every one refuses. Only the JSON boolean true
      authorizes.
    - A config missing the key entirely refuses with a message naming the key and
      `operator.local.json` — the degrade-safely-when-absent path, which is what an existing
      operator's settings file looks like the day this ships.
    - A hand-built dict shaped like an open grant, presented against a config without the key, is
      refused: the grant carries the envelope, the settings key carries the authority.
    - With a grant present but `state` closed, the arm refuses and the refusal names the reason the
      grant closed.
    - ONE parametrised confirmation list — "y", "Y", "YES", "Yes", "yes ", " yes", "ok", "sure",
      "", None, True — drives BOTH `control_actions.execute_action` and `write_grant.open_grant`,
      and both refuse every row while the exact string yes proceeds on both.
    - Omitting the confirmation argument raises TypeError on both functions.
  </behavior>
  <action>
Add the edge coverage above to `tests/test_write_grant.py`, parametrised the way
`test_every_near_miss_value_refuses` is, so a future edit that loosens the comparison fails on
eleven rows rather than one.

Test the two confirmation gates BEHAVIOURALLY against one shared parameter list rather than by
inspecting source text for a literal. A source pin asserting a string appears in two files proves
token presence, not semantic agreement — a `.strip().lower()` added on one side still contains the
literal and still matches, while accepting inputs the other refuses. Drive the same list through
`control_actions.execute_action` and `write_grant.open_grant` in one parametrised test and assert
both refuse every row, then assert both proceed on the exact string yes and both raise TypeError
when the argument is omitted. That is the property the pin was reaching for, checked directly.

There is no settings-key text pin to write. `config_gate.write_grants_enabled` is the single
definition (Task 1), so there is nothing to hold in agreement — a duplicated comparison is what
would have needed one, and Task 1 removed the duplication instead.

Where the refusals need wording, follow `config_gate.require_capability`'s convention exactly: name
the missing key, name `operator.local.json`, say the n8n admin holds the value, and never
interpolate a configured value into the message. Add a test asserting no config value reaches any
refusal string.

Add a test asserting `WRITE_GRANT_SETTINGS_KEY` is absent from `config_gate.CAPABILITY_KEYS` and
from `_CAPABILITY_DESCRIPTIONS`, so a later well-meaning edit that folds it in fails loudly rather
than quietly changing what a refusal means (D-53-01).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_write_grant.py operator-claude-plugin/tests/test_config_gate.py operator-claude-plugin/tests/test_control_surface.py -q</automated>
  </verify>
  <done>Eleven near-miss values for the settings key all refuse and only the JSON boolean true authorizes; an absent key refuses by name; a forged grant against an unkeyed config refuses; one shared confirmation list drives both `execute_action` and `open_grant` behaviourally and both refuse every row and both raise TypeError on omission; no source-text pin is introduced; the key is pinned as absent from `CAPABILITY_KEYS`.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the arm/probe parity pin — one edit, reason recorded in the test</name>
  <files>operator-claude-plugin/tests/test_control_arming.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_control_arming.py` (lines 62-105) — the three gate tests named in D-53-01, in full
    - `operator-claude-plugin/scripts/probe_n8n_semantics.py` — its gate's comparison, the other half of the pin
    - `.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md` — D-53-01's paragraph on decoupling, verbatim
  </read_first>
  <action>
Exactly one test changes in this file: `test_the_probe_and_the_arm_gate_use_the_same_comparison`.

Note before editing why the change is not optional even though the test would still pass. Its
current assertion is a regex for a comparison against the exact string "true" in each of the two
sources. After Task 1 the environment branch still contains that comparison, so the regex still
matches — but the test's NAME and DOCSTRING now assert a coupling that no longer holds: the arm gate
as a whole is no longer environment-gated, only its headless branch is. A test whose assertion
passes while its claim has become false is worse than a failing one, because it reads as evidence
for something nobody checked.

Rename it to state what is now true — that the probe gate and the arm gate's HEADLESS branch share
the comparison — and extend it with a second assertion pinning the interactive branch's identity
comparison against the JSON boolean. Replace the docstring with the reason, in prose, naming D-53-01
and the date, and stating the three-way split explicitly: the probe and deploy gates stay
environment-gated, the headless arm stays environment-gated, and the interactive arm moved to the
settings key. A reader arriving at this file six months from now must find the reason here rather
than having to locate a planning document.

Leave `test_with_the_gate_unset_the_arm_refuses_and_makes_no_call_at_all` untouched. It keeps
binding on the headless path and its assertion on `ARM_ENV_VAR` appearing in the refusal detail is
already satisfied by Task 1's wording.

Leave `test_the_disarm_is_NOT_gated_on_the_kill_switch` untouched, in every character. If any edit
in this phase makes it fail, the edit is wrong and the test is right.

Leave every other test in the file untouched. D-53-01 asks for one deliberate edit, not a sweep, and
the roadmap's risk line for this phase names a sweeping update to these tests as how the property
gets lost.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_control_arming.py -q</automated>
    Manual read-back: `git diff --stat operator-claude-plugin/tests/test_control_arming.py` shows one
    file changed and a diff confined to the single renamed test; `git diff
    operator-claude-plugin/tests/test_control_arming.py` contains no deletion of a test function.
  </verify>
  <done>Exactly one test in the file is renamed and re-documented, its new docstring records the D-53-01 reason and the three-way gate split, no test is deleted, and the disarm test is byte-identical to its pre-phase state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator conversation → grant object | The grant is a value Claude holds and passes back; nothing outside the settings key stops a malformed or fabricated one from being presented |
| `operator.local.json` → authority decision | The admin's key is the only thing standing between an operator and a live write; it is read fresh on every arm, never cached |
| Plugin → n8n workflow content | The arm rewrites write-safety declarations in a live workflow; unchanged from Phase 28, but now reachable without terminal access |
| Headless cron environment → arm authority | `ALLOW_N8N_ARM` still gates `scheduled_arm.py`; nothing in this plan may widen that path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01 | Elevation of Privilege | forged grant dict | high | mitigate | Authority is the settings key re-read from config at arm time, not a property of the grant object; a fabricated grant against an unkeyed config is refused, pinned by a test in Task 2 |
| T-53-02 | Elevation of Privilege | settings key parsed loosely | high | mitigate | Identity comparison against the JSON boolean true; eleven near-miss values pinned as refusing; the `bool`-is-an-`int` gotcha named in a source comment |
| T-53-03 | Tampering | the headless path widened by accident | high | mitigate | `grant` is a keyword defaulting to None, so every existing caller stays on the environment branch; `test_scheduled_arm.py` passes unedited and is named in the verify |
| T-53-04 | Denial of Service | disarm made harder | critical | mitigate | `disarm` is not edited; `test_the_disarm_is_NOT_gated_on_the_kill_switch` is untouched and named in the verify |
| T-53-05 | Repudiation | a gate test that passes while its claim is false | medium | mitigate | Task 3 re-points the parity pin's claim and records the reason in the test file rather than only in planning |
| T-53-06 | Information Disclosure | a config value in a refusal string | medium | mitigate | Refusals name keys and files only; a test asserts no configured value reaches a refusal message, mirroring T-27-12 |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; `operator-claude-plugin/requirements.txt` is unchanged and no package-manager task exists |
</threat_model>

<flagged_assumptions>
- **Per-send armed windows, not one window held open for the session.** D-53-03 says the grant is
  "today's `armed_window` guarantee, extended in duration", which reads two ways. This plan takes the
  reading in which the GRANT is extended and the WINDOW stays per-send. Three things force it:
  D-53-04's "a failed disarm fails that send only" needs a per-send disarm to exist; D-53-04's
  proposed guardrail ("a pre-flight read showing writes still live at the start of the next send")
  presupposes writes are off between sends; and no Python process survives between conversation
  turns, so a session-long window could not use `armed_window.__exit__` and would forfeit the
  guaranteed disarm the milestone's must-not-lose list names. The reading makes the operator's
  accepted exposure smaller than the language they accepted, not larger. Flagged because it is the
  one place this plan chose between readings.
- **"Confirmed by explicit yes" is satisfied once, at grant open, for exactly this batch.** Under a
  grant, individual sends are not separately confirmed — that is D-53-04's whole point. The
  invariant is not regressed because the yes is still explicit, still preceded by a shown
  consequence, and still scoped to a named record set rather than to a duration. Stated here so a
  later reader does not read the missing per-send confirmation as an omission.
- **Opening a grant mutates nothing.** It reads, computes and returns. This is why it does not go
  through `n8n_control.apply_mutation` and why its absence there is not a violation of the
  "arming goes through apply_mutation" invariant — the arms it authorizes still do.
- **D-53-05 removes a protection on purpose, and this plan does not soften that.** A grant spanning
  both lanes means the ingest authorization is given before the enriched preview exists, so held
  rows and merge conflicts are authorized unseen. The operator was shown the cost in full and
  accepted it for speed. What this plan enforces instead, and must be read as the whole of the
  remaining protection: the allowlist stays record-scoped to the batch, and that scope is now
  checked inside `arm_for_dispatch` rather than at a helper a caller could skip. The collapse widens
  WHEN approval is given; it must never widen WHAT it covers, and Task 1's two scope refusals are
  what hold that line.
</flagged_assumptions>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0.
- `.venv/bin/python -m pytest` exits 0 (full Python suite — the backend suite must be unaffected).
- `node --test tests/n8n/*.test.mjs` exits 0 (glob form only; the directory form is broken on node 24).
- `git status --porcelain` shows no modification under `n8n/`, `scripts/build_cloud_workflows.py`,
  or `operator-claude-plugin/scripts/scheduled_arm.py`.
- `git diff --stat operator-claude-plugin/tests/` lists exactly two test files: the new
  `test_write_grant.py` and `test_control_arming.py`.
</verification>

<success_criteria>
- A dispatch arms under an admin-set settings key and an operator-opened grant, with no shell
  environment variable set anywhere — G-2's blocker is removed on the interactive path.
- The headless path is byte-identical in behaviour and `scheduled_arm.py` is unedited.
- Disarm remains ungated and its test unedited.
- The grant is a value in the conversation only: no file, no environment variable, no default.
- The one gate test that had to move, moved once, deliberately, with its reason in the file.
</success_criteria>

<output>
Create `.planning/phases/53-operator-openable-write-grant/53-01-SUMMARY.md` when done.
</output>
