---
phase: 53-operator-openable-write-grant
plan: 04
type: execute
wave: 4
depends_on: ["53-03"]
files_modified:
  - operator-claude-plugin/skills/enrich-records/SKILL.md
  - operator-claude-plugin/skills/contact-upload/SKILL.md
  - operator-claude-plugin/skills/enrich-before-ingest/SKILL.md
  - operator-claude-plugin/skills/backend-control/SKILL.md
  - operator-claude-plugin/README.md
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
  - operator-claude-plugin/tests/test_enrich_skill_contract.py
  - operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py
autonomous: false
requirements: [GRANT-01, GRANT-04, GRANT-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each lane skill states that, with a grant covering that lane open, its per-turn arming phrase is not asked again, and that with no grant open its behaviour is exactly what it is today (D-53-04)."
    - "One grant spans both lanes of enrich-before-ingest (D-53-05, operator, 2026-08-25). The operator is not asked a second time between the enrich and the ingest."
    - "test_enrich_before_ingest_skill_contract.py's ordering pin is changed by ONE deliberate edit that records D-53-05, its date, who took it, and what protection was traded for what — never deleted, never weakened by a sweep, exactly the discipline the arm/probe parity pin got."
    - "What the collapse does NOT touch is verified, not assumed: the allowlist stays record-scoped to the batch, the enriched preview is still rendered, and revocation still works — the default flips from ask-again to proceed-unless-stopped."
    - "backend-control names opening, revoking and closing a grant among the things an operator can ask for, in its existing one-action-one-confirmation register."
    - "The README tells an admin which key to set and an operator what a grant is, what it is bounded to, and how it ends."
    - "The plugin version is bumped and the CHANGELOG section is cut in the same commit, per the release checklist, so an installed operator is actually offered the update."
    - "An operator walks the whole path from their own chair — set key, open grant, send, revoke, close — and records the result, the same walk that found G-2."
    - "Every operator-facing surface that mentions revocation says it bites at the next SEND and does not stop a dispatch already running."
  prohibitions:
    - "Only the ordering/combined-phrase pin moves, and only by the one recorded edit; every other assertion in both skill-contract tests is untouched."
    - "No assertion in either contract test is DELETED; the ordering pin is rewritten in place with its decision record, so a future reader can find out the protection was removed on purpose and by whom."
    - "The collapse never widens the allowlist. Any skill wording implying a grant covers records outside the named batch is wrong."
    - "The per-turn arming phrases are not removed from any skill; the ungranted path keeps them verbatim."
    - "The marketplace clone refresh and the push to master are not performed unattended; both are the operator's step."
    - "No live HubSpot write is performed by any task in this plan without the operator's explicit in-session approval at the checkpoint."
  artifacts:
    - operator-claude-plugin/CHANGELOG.md
    - operator-claude-plugin/.claude-plugin/plugin.json
  key_links:
    - "Each lane skill's grant bypass sentence sits inside that lane's own numbered arming step; under D-53-05 a single grant may satisfy both, and the enrich-before-ingest skill says which lanes the open grant covers."
    - "The release checklist at the bottom of operator-claude-plugin/CHANGELOG.md is the procedure; a bump without a clone refresh reaches nobody."
    - "The checkpoint's walk is the only evidence that G-2 is actually closed from the operator's surface — no test can produce it."
---

<objective>
Ship it: tell each lane skill what a grant means for its own arming step, tell an admin and an
operator what the key and the grant are, cut the release so an installed operator is offered the
update, and have the operator walk the path from their own chair.

Purpose: G-2 was found by walking the documented operator path end to end, and nothing else would
have found it — every component was correct and the composition was not. The same is true of this
phase's fix. The code can pass every test in plans 01 to 03 and still leave an operator unable to
find the grant, or find it and be told by a skill to say an arming phrase anyway. This plan is where
the surfaces agree with each other and where a human confirms they do.

Output: four SKILL.md updates, the README, an additive assertion in each skill-contract test, the
CHANGELOG cut and version bump, and a recorded operator walk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md
@.planning/phases/53-operator-openable-write-grant/53-01-SUMMARY.md
@.planning/phases/53-operator-openable-write-grant/53-02-SUMMARY.md
@.planning/phases/53-operator-openable-write-grant/53-03-SUMMARY.md
@.planning/quick/260825-contact-company-association/UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The three lane skills — one grant, or today's phrase, never both</name>
  <files>operator-claude-plugin/skills/enrich-records/SKILL.md, operator-claude-plugin/skills/contact-upload/SKILL.md, operator-claude-plugin/skills/enrich-before-ingest/SKILL.md, operator-claude-plugin/tests/test_enrich_skill_contract.py, operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py</files>
  <read_first>
    - `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` (lines 1-140) — in full: the combined-phrase list, the heading offsets, the same-step exclusion, and the reason each is there
    - `operator-claude-plugin/skills/enrich-before-ingest/SKILL.md` (steps 5-7) — the two arming steps and the conditional-skip idiom step 7 already uses
    - `operator-claude-plugin/skills/enrich-records/SKILL.md` (steps 4-8) — the preview, the approval, the arming check and the dispatch
    - `operator-claude-plugin/skills/contact-upload/SKILL.md` (around the arming step) — the same shape in the other lane
    - `operator-claude-plugin/scripts/write_grant.py` — `authorize_send`'s signature and return shape, and `n8n_arming.armed_window`'s `grant` keyword, for the call sequence the skill must show
    - `.planning/phases/53-operator-openable-write-grant/53-CONTEXT.md` — D-53-04 verbatim
  </read_first>
  <action>
Read `test_enrich_before_ingest_skill_contract.py` first and completely, before touching any skill
file. It encodes a safety property that is not obvious from the skill text: the enriched preview
must land in the operator's turn before the ingest authorization can be given, because an
authorization given earlier grants the HubSpot write before the operator can see what they are
approving (37-CONTEXT sec 6.3). **D-53-05 removes that protection deliberately** — the operator was
shown its full cost and accepted it, for speed, on 2026-08-25 — so this task's job is not to
preserve it but to record its removal where a future reader will find it, and to hold the line on
everything the collapse does not touch.

Make the contract-test change ONE deliberate edit, the same discipline the arm/probe parity pin got
in 53-01. Rewrite the ordering pin and the combined-phrase pin in place so they assert what is now
true — that a single grant may authorize both lanes — and replace their docstrings with the decision
record: D-53-05, 2026-08-25, taken by the operator, and in plain prose what was traded for what. Say
that held rows and merge conflicts, which the enriched preview is the only place to see before a
write, are now authorized unseen. Do not delete either test function and do not let the edit spread
into the other assertions in the file; a sweep across this file is how the record of the trade gets
lost along with the property.

**A THIRD pin in that file collides with D-53-05, and its disposition is decided here rather than
left to the executor.** `test_the_skill_states_the_grant_never_outlives_its_turn_and_arms_no_other_lane`
(line ~274) asserts two things:

- `"never outlives" in body or "never written to disk" in body` — an OR. Under a session grant the
  first half stops being true, but `"never written to disk"` remains exactly true (GRANT-06), so the
  skill satisfies this assertion by saying the durable thing rather than the stale one. The
  assertion itself does NOT move.
- `"arming one lane does not arm any other lane" in body` — **this sentence stays, because it stays
  true.** D-53-05 spans lanes at the level of the GRANT (the authorization), not at the level of the
  ARM: each send still opens its own `armed_window` over one lane's workflow id and that send's own
  records, which is exactly what 53-01's scope check in `arm_for_dispatch` enforces. The assertion
  does not move either.

What the skill MUST do is write that distinction down, because the sentence would otherwise keep
passing while a reader draws the wrong conclusion from it: say, in the `enrich-before-ingest`
arming step, that a grant may authorize both lanes while each individual arm still covers one lane
and only that send's records. Add an assertion for that sentence. This is the same failure mode
53-01 Task 3 fixes for the parity pin — a pin whose literal survives while its claim has quietly
changed meaning — and it is fixed here the same way: by making the prose carry the distinction and
pinning the prose.

In each of `enrich-records`, `contact-upload` and `enrich-before-ingest`, amend that lane's own
numbered arming step — not a new step, and not a preamble. Add the conditional in the idiom step 7 of
`enrich-before-ingest` already uses for its own skip: if an open grant covers this lane and these
records, the arming phrase is not asked for and the dispatch runs under the grant; if no grant is
open, everything below is exactly as it is today. In `enrich-before-ingest` the same grant may
satisfy both steps, and the skill states which lanes the open grant covers rather than leaving the
operator to infer it.

Say what a grant does not remove, in each lane, in one line: the preview still runs, the records are
still named, each send still arms and disarms its own window bounded to that send's records, and a
failed disarm is still reported loudly. An operator who reads "you will not be asked again" without
that line will reasonably conclude the safety went away with the question.

In `enrich-before-ingest` specifically, say what the operator is accepting when they open a grant
covering both lanes, at the point they open it: the HubSpot write is being authorized before the
enriched preview exists, so any row held for review and any merge conflict where the source value is
kept over a differing provider value is authorized before they have seen it. The enriched preview is
still rendered and still worth reading — it is now a report rather than a gate. Say that they can
stop the run by revoking, and say honestly what revoking does: it refuses the next send, and it does
not stop a dispatch already running.

**Show the granted dispatch's call sequence concretely, the way the ungranted step already does.**
`enrich-records` step 7 gives the exact Python for today's path (`resolve_providers`, `plan_chunks`,
`dispatch_plan`), and a grant branch written only in prose would leave the executor and the operator
guessing at a sequence that is new. In each lane skill's grant branch, show it in the same register:
call `write_grant.authorize_send` for that lane and this send's records; on a refusal, stop and
report it; otherwise open `n8n_arming.armed_window` for that lane's workflow id with **this send's
records as the allowlist** and the grant passed through, and run the same `dispatch_plan` call
inside the window. Say in words next to the code that the window's allowlist is the send's records
and NOT the grant's whole record set — that narrowing is what keeps each window strictly smaller
than the grant, and a skill that passed the grant's full list would widen every window to the whole
batch while every test still passed.

Then extend both skill-contract tests. In `test_enrich_before_ingest_skill_contract.py` the ONE
edit above is the only rewrite; everything else is additive — add an assertion that the skill names
the lanes an open grant covers, and an assertion that the acceptance sentence (the write authorized
before the enriched preview exists) is present, so a later edit cannot quietly drop the disclosure
after the protection was traded for it. In `test_enrich_skill_contract.py`, add assertions that the
grant conditional, the "what a grant does not remove" line and the revocation-bites-at-the-next-send
line all appear; rewrite nothing there. If any assertion outside the one recorded edit fails, the
skill edit is wrong and the assertion is right.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_enrich_skill_contract.py operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py operator-claude-plugin/tests/test_plugin_manifest.py operator-claude-plugin/tests/test_status_skill.py -q</automated>
    Manual read-back: `git diff operator-claude-plugin/tests/` shows additions only in the two
    contract tests — no removed assertion, and no changed assertion body EXCEPT the single recorded
    D-53-05 edit to the ordering and combined-phrase pins (and, if disposition (a) is taken, the
    never-outlives/other-lane pin named in Task 1). That carve-out is the whole point of the edit:
    an additions-only diff here would mean the trade went unrecorded.
  </verify>
  <done>Each lane skill's arming step carries the grant conditional plus the line saying what a grant does not remove and the revocation-bites-at-the-next-send line; each grant branch shows the concrete authorize_send then armed_window then dispatch_plan sequence with the send's own records as the allowlist; enrich-before-ingest states which lanes an open grant covers and says the HubSpot write is authorized before the enriched preview exists; the ordering pin is rewritten in exactly one edit carrying the D-53-05 decision record, no test function is deleted, and every other assertion in both files is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The control surface, the README, and the release cut</name>
  <files>operator-claude-plugin/skills/backend-control/SKILL.md, operator-claude-plugin/README.md, operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json</files>
  <read_first>
    - `operator-claude-plugin/skills/backend-control/SKILL.md` — the one-action-one-confirmation framing and what it currently lists
    - `operator-claude-plugin/README.md` (around the two arming-phrase passages) — the operator-facing register
    - `operator-claude-plugin/CHANGELOG.md` (lines 1-40) — the Unreleased heading, the format, and the warning above it
    - `operator-claude-plugin/CHANGELOG.md` (lines 728-760) — the four-step release checklist and the two traps it records
    - `operator-claude-plugin/.claude-plugin/plugin.json` — the current version string
    - `.planning/phases/53-operator-openable-write-grant/53-01-SUMMARY.md`, `53-02-SUMMARY.md`, `53-03-SUMMARY.md` — what actually shipped, for the entry
  </read_first>
  <action>
In `backend-control`, add opening, revoking and closing a grant to what an operator can ask for,
inside the existing one-action-one-confirmation framing rather than as a parallel surface —
53-CONTEXT says the grant extends that surface. Say what an operator gets when they ask to open one:
the envelope as arithmetic, then one explicit yes. Say what revoking does and when it takes effect:
it refuses the next SEND, and it does not stop a dispatch already running — at the 2-record chunk
ceiling that can be many chunks (a 40-record send is 20 chunks, all of which run after a revoke).

In `README.md`, amend both arming-phrase passages the same way Task 1 amended the skills, and add
the admin's line: the key, the file, and that it is set once. Say plainly that the environment
variable is still the authority for the scheduled and cron paths, so an admin who finds both
documented does not conclude one replaced the other.

Then cut the release. Bump `.claude-plugin/plugin.json`'s version from its current value to the next minor — this is added
operator-facing capability with no removal, so a minor is right, and semver here is against this
client's surface rather than the backend's milestone number. Do it in the SAME commit as the
CHANGELOG entry; the checklist records that an unbumped version leaves the Update button greyed out
however much shipped.

Cut the CHANGELOG section: the Unreleased heading stays on top and empty, and the phase's work moves
under a dated version heading. Write the entry for an operator and an admin, not for a developer.
Under Added: an admin can enable write grants with one key in the settings file, and an operator can
then authorize HubSpot writes from inside Claude with no terminal — naming that this is what made
the documented write path reachable at all. Under Added: the grant is bounded to a named batch of
records on the lanes it names, shows its worst-case cost as arithmetic before the yes, and ends on completion,
revocation, session end, error or ceiling breach. Under Changed: while a grant is open the per-send
arming phrase is not asked again; with no grant open nothing changes; and `backend-control` now
lists opening, revoking and closing a grant among the actions it can take.

Write the honest lines the entry needs, because an operator reading only this file is entitled
to them. Two of them are about what was traded: a grant may cover both lanes of
`enrich-before-ingest`, which means the HubSpot write is authorized before the enriched preview
exists (D-53-05, operator, 2026-08-25); and revoking a grant refuses the next send but does not stop
a dispatch already running. First: the environment variable is unchanged and remains the authority for the scheduled
and cron paths — it was not replaced, it was narrowed to where it belongs. Second: the cost figure
shown at grant open discloses what the batch can cost; it does not prevent it, and the refusal that
does is a later phase. A changelog that let that read as a spend guard would be the same class of
over-claim this repository has already corrected once.

Do NOT push and do NOT refresh the marketplace clone. Steps 3 and 4 of the checklist are the
operator's, and the clone refresh in particular touches a path outside this repository. Print both
commands at the end of the task so the operator can run them, and say plainly that until step 4 runs
the update is invisible to an installed plugin however correct the bump is.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; v=json.loads(pathlib.Path('operator-claude-plugin/.claude-plugin/plugin.json').read_text())['version']; c=pathlib.Path('operator-claude-plugin/CHANGELOG.md').read_text(); assert f'[{v}]' in c, f'CHANGELOG has no section for version {v}'; assert c.index('## [Unreleased]') < c.index(f'## [{v}]'), 'Unreleased must sit above the cut section'; print('release consistency ok', v)"</automated>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_plugin_manifest.py -q</automated>
  </verify>
  <done>backend-control lists open, revoke and close inside its existing framing; the README carries the admin line and the still-env-gated-for-cron clause; the version string is bumped, the CHANGELOG carries a dated section for exactly that version with the Unreleased heading empty above it, the entry names both honest lines (the env var narrowed rather than replaced, and the ceiling discloses rather than prevents), and the push and clone-refresh commands are printed rather than run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The operator walks it — the same walk that found G-2</name>
  <what-built>
Phase 53 makes the HubSpot write path reachable from an operator's own surface for the first time.

1. **Authority moved.** The interactive arm no longer needs `ALLOW_N8N_ARM` in the session's shell
   environment. It needs one key, `allow_write_grants`, set to the JSON boolean true by an admin in
   `operator.local.json` — the file that already lives outside the versioned install directory and
   survives plugin updates. `ALLOW_N8N_ARM` is unchanged and remains the sole authority for the
   scheduled and cron paths, which have no operator to confirm anything.
2. **A grant.** The operator names a batch and the lanes it covers — one grant may cover both the
   enrich and the ingest lanes of `enrich-before-ingest` (D-53-05, your decision on 2026-08-25);
   the plugin shows the record count, the
   worst-case provider credits per provider, the worst-case Anthropic dollars, the projected n8n
   executions and the configured monthly allowance; the operator says yes once. Every send in that
   batch then arms and disarms its own window, bounded to that send's records, without asking again.
3. **Two guardrails you asked to be surfaced rather than assumed.** Opening a grant first reads the
   live write-safety state and refuses to open if it finds writes already armed, naming what it
   found and offering a disarm. And two consecutive disarm failures — or a pre-flight read finding
   writes still live before the next send — end the grant and block further sends. One isolated
   disarm failure still fails only that send, as you chose.
4. **Nothing about record scoping, guaranteed disarm, the ungated disarm, or the arm's independent
   re-read changed.** Each has a test naming the property it defends.

Zero live HubSpot writes have been performed by this phase so far. The plugin version is bumped and
the CHANGELOG is cut, but the push and the marketplace clone refresh have deliberately not been run.
  </what-built>
  <how-to-verify>
Do this from Claude Desktop, as the operator, not from a terminal — the point of the walk is that
the terminal is not needed. The one exception is step 1, which is the admin's job by design.

1. **As admin:** open `~/.claude/plugins/data/operator-claude-plugin-lightning-visuals-operator/operator.local.json`
   and set `allow_write_grants` to `true`. Confirm the file already had your other values in it and
   that you did not have to recreate anything.
2. **Ask the plugin whether it is set up.** Confirm the answer now includes a line saying write
   grants are enabled. Then, if you can, try it once with the key set to `"true"` as a STRING rather
   than a boolean, and confirm it reports NOT enabled — the near-miss behaviour is deliberate and
   worth seeing once.
3. **Ask to open a write grant** over one or two records you are willing to have written. Read what
   comes back and answer these:
   - Does it name the lane, the records, and whether creates are included?
   - Are the cost figures ones you find plausible, and is the rate table's age shown?
   - Does it say clearly that the figure discloses rather than prevents, and that the remaining
     monthly allowance is not yet being checked?
   - Is there exactly one yes to give?
4. **Send the batch.** Confirm you were NOT asked for an arming phrase, and that the report still
   tells you what was armed, what was sent and whether the disarm verified.
5. **Revoke the grant** mid-batch if the batch is long enough, or immediately if it is not. Confirm
   the next send refuses and says the grant was revoked — and confirm that if a dispatch was already
   running, it finished its chunks rather than stopping. That is the re-scoped GRANT-05 behaviour,
   not a bug; the question below asks whether it is enough.
6. **Try to open a second grant covering a record NOT in the first**, then attempt a send for a
   record outside it. Confirm it is refused by name before anything is armed.
7. **Read the refusal you get with the key unset.** Ask an admin to unset it, ask to open a grant,
   and confirm the refusal names the key, the file and who sets it — and that it does not tell you to
   set a shell environment variable, which is the thing you cannot do.

Then decide the open question this phase leaves with you: **is revocation at the next SEND enough?**
You chose send boundaries over chunk-by-chunk on 2026-08-25, and here is what that means in
practice. `dispatch_plan` loops its chunks internally with no grant-aware hook, so a revoke arriving
mid-dispatch stops nothing in that dispatch — at a chunk ceiling of 2 records, a 40-record send is 20
chunks and all 20 run. Revoking refuses the send AFTER the one in flight. If that gap is wider than
you are comfortable with once you have seen it in step 5, say so: making `dispatch_plan`
grant-aware is buildable, but it changes the shared dispatch loop every lane in this plugin uses and
would be its own phase.

While you are in step 4, also confirm the D-53-05 disclosure landed: when you opened a grant
covering both lanes, were you told in plain words that the HubSpot write was being authorized before
the enriched preview existed? That sentence is the whole of what you got in exchange for the
protection you traded, and if it is not there, the trade is worse than the one you agreed to.

Finally: if you approve, run the two release steps that were deliberately left to you — push to
master, then refresh the marketplace clone with the two commands printed at the end of the previous
task. Until the clone is refreshed, an installed plugin will not see the update however correct the
version bump is.
  </how-to-verify>
  <resume-signal>Type "approved" once the walk is done and the release steps are run, or describe what refused, what read wrongly, or what you had to reach for a terminal to do. Also answer the one-lane question in the paragraph above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill prose → operator behaviour | The skills are the operator's instructions; a skill that still demands a phrase under a grant, or drops the safety line, changes behaviour without changing code |
| Skill-contract tests → the two-stage ordering | Under D-53-05 these tests no longer HOLD the enriched-preview-before-ingest-authorization property — they hold the RECORD that it was traded, and the disclosure that replaced it. A weakened assertion loses the record silently, which is now the thing to protect |
| Released version string → what an operator is actually running | An unbumped version or an unrefreshed clone leaves the operator on old code while every document says otherwise |
| Operator walk → the claim that G-2 is closed | No automated test can prove a path is reachable from Claude Desktop; only the walk can |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-18 | Tampering | the traded protection lost without a record | high | mitigate | The ordering pin is rewritten in ONE edit carrying D-53-05's date, author and the trade in prose; no test function is deleted; the verify names a diff read-back confirming the rest of both files is untouched |
| T-53-18b | Elevation of Privilege | the collapse widening WHAT is covered, not just WHEN | critical | mitigate | The allowlist stays record-scoped and the scope check moved inside `arm_for_dispatch` (53-01); a skill claiming a grant covers records outside the named batch fails 53-03 T3's invariant tests |
| T-53-19 | Spoofing | a skill claiming safety went away with the question | medium | mitigate | Each lane skill carries the explicit line naming what a grant does not remove: the preview, the record scoping, the per-send disarm, the loud disarm failure |
| T-53-20 | Repudiation | shipped work an operator never receives | medium | mitigate | Version bump and CHANGELOG cut in one commit with an automated consistency check; push and clone refresh printed as the operator's own steps with the consequence of skipping them stated |
| T-53-21 | Elevation of Privilege | a live write during the walk | high | mitigate | The checkpoint is `gate="blocking"` and the plan is `autonomous: false`; the operator chooses the records and is told to pick ones they are willing to have written |
| T-53-22 | Information Disclosure | an admin's settings value quoted back in chat | medium | mitigate | The initialize surface reports enabled or not enabled, never the value; carried from T-27-12's convention and pinned in 53-03 |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

<flagged_assumptions>
- **D-53-05 trades a real protection and this plan does not soften it.** With one grant across both
  lanes, held rows and merge conflicts are authorized before the enriched preview renders them. The
  operator was shown the cost in full and accepted it for speed. The plan's whole remaining defence
  is that the allowlist stays record-scoped to the named batch and that the disclosure sentence
  reaches the operator at the yes — the checkpoint verifies the second, because the first is
  testable and the second is not.
- **GRANT-05's send boundary is put to the operator with the arithmetic.** A revoke mid-dispatch
  stops nothing in that dispatch; at a 2-record chunk ceiling a 40-record send runs 20 chunks
  regardless. That follows from `dispatch_plan` having no grant-aware loop, not from a choice this
  plan made, and closing the gap means changing the shared dispatch loop every lane uses.
- **The walk cannot be automated and is the phase's only evidence for its headline claim.** Every
  component of the pre-phase path was individually correct and the composition was broken; that is
  what G-2 was. The same failure mode is available to this phase and only a human at the operator's
  own surface can rule it out.
- **The push and clone refresh are outside this repository's blast radius** and are left to the
  operator deliberately. A plan that ran them would be mutating an install directory nobody asked it
  to touch, and the CHANGELOG already records that a reinstall from a stale clone re-copies whatever
  the clone already holds.
</flagged_assumptions>

<verification>
- `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0.
- `.venv/bin/python -m pytest` exits 0 (full Python suite).
- `node --test tests/n8n/*.test.mjs` exits 0.
- `git diff operator-claude-plugin/tests/` for this plan contains no REMOVED assertion, and no
  altered assertion except the single recorded D-53-05 edit described in Task 1.
- `git status --porcelain` shows no modification under `n8n/` or to `scheduled_arm.py`.
- The plugin version string and the CHANGELOG's top dated section agree, checked by the automated
  command in Task 2.
</verification>

<success_criteria>
- An operator reading any lane skill knows whether they need an arming phrase, and knows what a
  grant does not remove.
- The REMOVAL of the enriched-preview-before-ingest-authorization property (D-53-05, operator,
  2026-08-25) is recorded in the contract test with its date and author, and the disclosure that
  replaced it — that the HubSpot write is authorized before the enriched preview exists — is
  present in the skill prose AND pinned by an assertion. A verifier must NOT read this criterion
  as "the ordering property still holds": it was deliberately traded, and what must survive is the
  record of the trade plus the disclosure bought with it.
- The release is cut correctly and the two operator-only steps are named with the consequence of
  skipping them.
- The operator has walked the whole path from Claude Desktop, without a terminal except for the
  admin's one key, and has recorded the result.
- The one-lane question has an operator's answer on the record.
</success_criteria>

<output>
Create `.planning/phases/53-operator-openable-write-grant/53-04-SUMMARY.md` when done.
</output>
