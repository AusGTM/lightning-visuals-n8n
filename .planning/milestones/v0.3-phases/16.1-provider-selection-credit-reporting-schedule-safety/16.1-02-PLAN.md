---
phase: 16.1-provider-selection-credit-reporting-schedule-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "16.1-01"
files_modified:
  - scripts/build_cloud_workflows.py
  - scripts/deploy_n8n_workflows.py
  - scripts/check_provider_credits.py
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_scheduled_maintenance_cloud.json
  - tests/test_remaining_credits_response.py
  - tests/test_deploy_n8n_workflows.py
  - tests/test_check_provider_credits.py
  - tests/test_schedules_inactive.py
autonomous: true
requirements:
  - "SC-4 (credit endpoints validated vs docs AND live curl; non-authorizing keys degrade gracefully — extractCredits + the check script, importing the side-effect-free registry)"
  - "SC-5 (webhook response carries remaining_credits per provider; a credit-check failure never fails the run) — reviews C1: credit checks run ONCE per run via a single-item branch (not once-per-row); C3: honest per-batch response semantics"
  - "SC-6 (scripts/check_provider_credits.py — read-only, .env-keyed, two-key/no-creds skip; its functions unit-tested offline with mocked requests, reviews A6)"
  - "SC-7 (scheduled workflows emit active: false; deploy leaves them inactive; documented precisely — reviews A5) "
  - "SC-8 (offline acceptance tests: remaining_credits single-item + response shape, credit-node deploy binding, deploy-never-activates, check-script failure paths, schedules-inactive; suite green; builder rebuild deterministic)"
  - "reviews C2 (new credit HTTP nodes registered in deploy NODE_CREDENTIAL_MAP so they deploy bound, not 401-forever)"

must_haves:
  truths:
    - "providerSelection.js extractCredits encodes the three LIVE-validated (16.1-RESEARCH.md Task 1) paths — Lusha raw.credits.remaining; ZoomInfo raw.data[0].attributes.usage[] where limitType==\"uniqueIdLimit\" -> usageRemaining (fallback: the entry with a non-zero totalLimit); Apollo -> null — and returns null on ANY shape mismatch (SC-4)."
    - "Credit checks execute EXACTLY ONCE per webhook run (reviews C1): a dedicated SINGLE-ITEM credit branch off Parse HubSpot Event — a Code node emitting exactly one {providers_requested} item regardless of event count -> per-provider IF gates -> at most one credit HTTP per provider (onError:continueRegularOutput). They are NOT fed by the multi-row terminal/enrichment flow (which would fire one usage call per row and trip the live-observed Lusha 5 req/min 429). Balances are read at run START; the RESPONSE still carries them (the plan no longer claims the QUERY is post-enrichment)."
    - "Build Response reads each credit node BY NAME via the guarded nodeAll idiom (a not-requested/unexecuted credit node -> [] -> extractCredits -> null; the throw guard is at the read site, not just the extractor — reviews LOW-3), applies extractCredits, and assembles remaining_credits: [{provider, credits}] for exactly the providers_requested set; providers none/blank/absent -> zero credit HTTP calls -> remaining_credits [] (SC-5, CONTEXT decisions 2 and 6)."
    - "A credit-check failure (4xx/5xx/timeout/malformed) degrades to credits: null and NEVER fails the enrichment run; every credit HTTP uses onError:continueRegularOutput and every extractor is null-safe (SC-5, CONTEXT decision 6)."
    - "The webhook returns via responseMode:responseNode + a Respond to Webhook node fed by a Build Response convergence reachable from every enrichment terminal (5 real terminal nodes + the 2 re-pointed IF-enrich-false lanes + the Plan-01 unsupported-object-type terminal); response semantics are stated HONESTLY as per-batch first-arrival (parity with today's lastNode, NOT a hard determinism guarantee) — the true 0-event/empty-body + multi-terminal arrival ordering is a Track B execution-level test, not something the static graph proves (reviews C3)."
    - "The new credit HTTP nodes are registered in deploy's NODE_CREDENTIAL_MAP (Lusha usage -> LV Lusha, Apollo usage -> LV Apollo, ZoomInfo usage-mint -> LV ZoomInfo with a node name DISTINCT from ZoomInfo Mint; the ZoomInfo usage GET is secret-free/bearer-only) so they deploy BOUND, not unbound->401->credits:null-forever; a deploy-binding test asserts every credit-requiring node receives a bound credential (reviews C2)."
    - "scripts/check_provider_credits.py imports PROVIDER_REGISTRY from the side-effect-free scripts/provider_registry.py (NOT build_cloud_workflows.py, which writes codegen at import); it is read-only + .env-keyed, mirrors provision_n8n_credentials.py's no-creds skip-to-exit-0, mints the ZoomInfo bearer inline, applies the three extractors, degrades to null on 4xx/timeout without raising, and NEVER prints a secret value (SC-4/SC-6, reviews A3)."
    - "check_provider_credits.py's functions are unit-tested offline with MOCKED requests in tests/test_check_provider_credits.py (no live call): no-creds->0 calls exit 0; Apollo 403->null; ZoomInfo mint-fail->null with NO usage GET; timeout/malformed->null; captured output never contains supplied fake secrets (reviews A6)."
    - "build_scheduled_maintenance_cloud() emits \"active\": false; deploy DROPS the active field from its create/update payload and NEVER POSTs /activate, so a newly-created scheduled workflow stays inactive — but an ALREADY-active workflow is not deactivated on update (stated precisely, not overclaimed); a pytest asserts the JSON marker AND that deploy issues no /activate call (SC-7, reviews A5)."
    - "The full offline suite stays green and the builder rebuild is byte-deterministic (SC-8)."
  artifacts:
    - "single-item credit branch (Credit Request node + per-provider IF gates + credit HTTP nodes) + Build Response (guarded nodeAll, remaining_credits) + Respond to Webhook in build_enrichment_cloud()"
    - "deploy NODE_CREDENTIAL_MAP entries for the credit nodes (reviews C2) + a deploy-binding + no-/activate test"
    - "scripts/check_provider_credits.py (imports scripts.provider_registry) + tests/test_check_provider_credits.py (hermetic)"
    - "\"active\": false on build_scheduled_maintenance_cloud() + the documented operator note + tests/test_schedules_inactive.py"
    - "tests/test_remaining_credits_response.py, tests/test_deploy_n8n_workflows.py (credit-binding + no-activate additions)"
  key_links:
    - "Parse HubSpot Event -> single-item Credit Request node -> per-provider IF gates -> credit HTTP (one call each) -> Build Response reads by guarded nodeAll -> remaining_credits (run-start query, response-carried)"
    - "providers_requested (Plan 01) -> gates each credit-check node AND filters remaining_credits to the requested set (none -> zero credit calls -> [])"
    - "credit HTTP onError:continueRegularOutput + null-safe extractCredits + guarded nodeAll read -> credits: null never fails the run"
    - "every enrichment terminal (5 real nodes + 2 re-pointed IF-enrich-false lanes + unsupported terminal) -> Build Response -> Respond to Webhook"
    - "credit node names -> deploy NODE_CREDENTIAL_MAP (LV Lusha/LV Apollo/LV ZoomInfo) -> bound credential id at deploy (else fail-closed)"
    - "scripts/provider_registry.py credit descriptors -> both the runtime credit nodes AND check_provider_credits.py (one source; clean import)"
    - "build_scheduled_maintenance_cloud() active:false -> test_schedules_inactive.py + deploy-never-/activate guard -> stays inactive on create"
  prohibitions:
    - "Credit checks are NOT fed by the multi-row terminal/enrichment flow (that fires one usage call per row -> Lusha 429 -> all null) — they run once per run off a single-item branch (reviews C1)."
    - "A credit-check failure NEVER fails or blocks the enrichment run — writeback completes regardless of credit-node outcome (graceful degrade)."
    - "No secret value (LUSHA_API_KEY / APOLLO_API_KEY / ZOOMINFO_CLIENT_ID / ZOOMINFO_CLIENT_SECRET) is printed by check_provider_credits.py or inlined into any committed workflow JSON — the script logs credit numbers + HTTP status only; the graph uses bound credentials."
    - "check_provider_credits.py makes NO live call in the offline suite — its unit tests mock requests; the script itself stays live-only (pytest collects tests/test_check_provider_credits.py, not the script)."
    - "The enrichment webhook workflow is NOT set active in this phase (activation is Track B); only the scheduled-maintenance workflow gains the explicit active:false intent marker; the plan does NOT claim deploy deactivates an already-active workflow (reviews A5)."
    - "The plan does NOT claim the webhook response is hard-deterministic across a mixed batch — it states per-batch first-arrival honestly and defers the runtime ordering + 0-event proof to a Track B execution-level test (reviews C3)."
    - "The frozen Code files and Plan 01's fan-out topology are not altered — this plan adds the credit branch + convergence + deploy bindings downstream/aside, it does not re-touch the provider gates."
---

<objective>
Layer credit reporting and schedule safety onto the caller-controlled webhook Plan 01 delivered,
with the cross-AI review fixes folded in. The webhook response gains a `remaining_credits` node,
but the credit checks run EXACTLY ONCE per webhook via a dedicated single-item branch off
`Parse HubSpot Event` (not once-per-row, which would trip Lusha's live-observed 5 req/min limit);
the new credit HTTP nodes are registered in deploy's `NODE_CREDENTIAL_MAP` so they deploy BOUND
(not 401-forever); the response semantics are stated honestly (per-batch first-arrival, not hard
determinism); a new read-only `scripts/check_provider_credits.py` (importing the side-effect-free
registry) gives the operator the same balances from the CLI and is unit-tested offline with mocked
requests; and the scheduled-maintenance workflow ships an explicit `active: false` with a precise
guarantee (inactive-on-create; deploy never activates) and a deploy no-/activate guard.

Purpose: These are the remaining pre-live cost-safety guarantees, corrected against three
source-grounded cross-AI reviews (C1 credit multiplicity, C2 unbound deploy, C3 response
overclaim, A3/A5/A6). All build on Plan 01's providerSelection.js (extractCredits),
scripts/provider_registry.py (credit descriptors), and providers_requested threading.

Output: a single-item credit branch + convergence in the built Cloud workflow, deploy
NODE_CREDENTIAL_MAP entries + a binding/no-activate test, one new live-only script importing the
side-effect-free registry + its hermetic test, an explicit precise `active: false` on the scheduled
workflow + guard — with zero live calls in the suite, a green full suite, and a deterministic
rebuild. Live credit checks + the runtime response-ordering/0-event proof are documented Track B
(execution-level) items.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-provider-selection-credit-reporting-schedule-safety/16.1-RESEARCH.md
@.planning/phases/16.1-provider-selection-credit-reporting-schedule-safety/16.1-CONTEXT.md
@.planning/phases/16.1-provider-selection-credit-reporting-schedule-safety/16.1-REVIEWS.md

# Analog + target sources (read the specific ranges named in each task's read_first)
@scripts/build_cloud_workflows.py
@scripts/deploy_n8n_workflows.py
@scripts/provision_n8n_credentials.py
@n8n/code/providerSelection.js
@scripts/provider_registry.py
@tests/test_cloud_write_path.py
@tests/test_deploy_n8n_workflows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: remaining_credits via a SINGLE-ITEM credit branch + honest response convergence (reviews C1, C3, LOW-3)</name>
  <files>scripts/build_cloud_workflows.py, n8n/wf_enrichment_cloud.json, tests/test_remaining_credits_response.py</files>
  <read_first>
    - 16.1-REVIEWS.md Synthesis C1 + C3 and kimi HIGH-1/HIGH-2, gpt Blocker-2/High-3 (the once-per-run credit branch + honest response semantics — the load-bearing corrections)
    - 16.1-RESEARCH.md Task 1 (the three credit endpoints + extractors, incl. ZoomInfo's `Accept: application/vnd.api+json` requirement and inline mint) and Task 2's `responseMode: "lastNode"` finding
    - scripts/build_cloud_workflows.py build_enrichment_cloud(): the webhook node (~2574-2580, responseMode currently "lastNode"); ENRICH_PARSE_EVENT_CLOUD (~2530-2558, the single point every row flows through — the credit branch forks here); the terminal branches — five real nodes (contacts HubSpot Create / HubSpot Update, Skip (NoOp); companies HubSpot Company Create / HubSpot Company Update ~2854-2861/~2892-2899) PLUS the two empty IF-enrich-false lanes (contacts :2860, companies :2898) to re-point + the Plan-01 unsupported-object-type terminal; the ZoomInfo split-mint shape (`_zoom_split_contacts_subgraph` ~2458-2478) to reuse for the ZoomInfo usage bearer
    - scripts/provider_registry.py (Plan 01) credit descriptors + `_write_safety_const` bake idiom (~739-763)
    - n8n/code/providerSelection.js extractCredits (Plan 01); ENRICH_NORMALIZE_SCORE_CLOUD's guarded `nodeAll` (~1058-1062) — the read-by-name guard Build Response reuses (reviews LOW-3)
    - tests/test_cloud_companies_branch.py (the BFS-reachability pattern to mirror)
  </read_first>
  <action>
    Add the credit-reporting branch + response convergence per the reviews.

    (1) SINGLE-ITEM CREDIT BRANCH (reviews C1) — fork a dedicated branch off `Parse HubSpot Event`
    (NOT off the terminal/enrichment flow): a `Credit Request` Code node that emits EXACTLY ONE item
    `{providers_requested}` regardless of event count (run-once-for-all-items; read providers_requested
    from `$('Parse HubSpot Event').first().json`). Then per-provider `IF <provider> Credit Requested`
    gates (membership in providers_requested): true -> the provider's credit-check HTTP node; false ->
    bypass (dead-ends — Build Response reads by name). Each credit HTTP is credential-bound and fires
    ONCE (one input item): Lusha GET usage (LV Lusha header), Apollo POST usage_stats (LV Apollo header),
    ZoomInfo GET usage — mint the bearer via the same LV ZoomInfo Basic-auth split-mint shape (node name
    DISTINCT from ZoomInfo Mint, e.g. `ZoomInfo Usage Mint`), then GET with `Accept: application/vnd.api+json`.
    Every credit node carries `onError: continueRegularOutput`. Because this branch runs off the first
    node, it completes at run START (before the deep enrichment chain reaches terminals); the balances
    are read at run start and CARRIED in the response — the plan does NOT claim the query is
    post-enrichment (run-start balances are also better for burn-gating).

    (2) BUILD RESPONSE + CONVERGENCE — add a `Build Response` Code node that reads each credit node BY
    NAME using the guarded nodeAll idiom (`function nodeAll(name){ try { return $(name).all(); } catch(e){ return []; } }`
    — reviews LOW-3; a not-requested credit node yields [] -> extractCredits -> null), applies
    extractCredits, and assembles `remaining_credits: [{provider, credits}]` for exactly
    providers_requested (none -> []). Switch the webhook to `responseMode: "responseNode"` and add a
    `Respond to Webhook` node fed by Build Response. Route EVERY terminal into Build Response's trigger:
    the FIVE real terminal nodes (contacts HubSpot Create, HubSpot Update, Skip (NoOp); companies HubSpot
    Company Create, HubSpot Company Update), the TWO previously-empty IF-enrich-false lanes RE-POINTED
    with real new edges (contacts `IF Enrich` output index 1 :2860; companies `IF Company Enrich` output
    index 1 :2898), and the Plan-01 `unsupported` object-type terminal (so malformed/unsupported events
    still respond).

    (3) HONEST RESPONSE SEMANTICS (reviews C3) — in the sticky notes / plan-visible comment, state the
    semantics truthfully: with multiple terminal edges into Build Response the node fires on the first
    arrival and n8n returns the FIRST Respond execution, so a mixed create/update/skip batch returns the
    first-arriving batch's rows (parity with today's `lastNode`, NOT a hard determinism guarantee). Do
    NOT claim the response is deterministic across a mixed batch. The TRUE 0-event/empty-body case (no
    terminal fires -> no Respond -> HubSpot retry risk) and the exact multi-terminal arrival ordering are
    NOT provable by the static graph — record them as Track B execution-level test items (an n8n run with
    a mixed event array + an empty-body request), per gpt/kimi. Rebuild the JSON.

    (4) TEST — tests/test_remaining_credits_response.py (offline, structural, mirroring the BFS in
    tests/test_cloud_companies_branch.py): assert (a) the credit branch forks off Parse HubSpot Event
    through a single-item `Credit Request` node, and each credit HTTP node's ONLY inbound is its
    `IF <provider> Credit Requested` true lane fed from that single-item node — NOT from any terminal /
    multi-row enrichment node (the C1 once-per-run structural proof: credit HTTPs are not downstream of
    the row flow); (b) each credit HTTP carries onError:continueRegularOutput and is credential-bound
    (no inline secret / no env-var expression), and the ZoomInfo usage path sets the vnd.api+json Accept
    header; (c) Build Response is reachable from every terminal branch — the five real terminal nodes AND
    the two re-pointed IF-enrich-false lanes (output index 1 edges into Build Response, not []) AND the
    unsupported terminal — so the test fails if only the five real nodes converge; (d) Build Response's
    jsCode references remaining_credits + providers_requested and uses the guarded nodeAll. NOTE in the
    test docstring that per-batch response ordering + 0-event behavior are Track B execution-level items,
    not asserted here (honest C3). No live call.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -m pytest tests/test_remaining_credits_response.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_remaining_credits_response.py passes: each credit HTTP node's only inbound is its IF <provider> Credit Requested true lane fed from the single-item Credit Request node off Parse HubSpot Event (NOT from a terminal/enrichment node) — the reviews-C1 once-per-run structural proof.
    - The convergence node is reachable from every terminal branch — the five real terminal nodes AND the two re-pointed IF-enrich-false lanes (output index 1 edges into Build Response, not []) AND the Plan-01 unsupported terminal; the test fails if only the five real nodes converge.
    - Build Response reads credit nodes via the guarded nodeAll and its jsCode references remaining_credits + providers_requested; providers none/absent -> zero credit HTTP calls (Credit Request emits nothing to any gate's true lane) -> remaining_credits [].
    - Every credit-check HTTP node carries onError:continueRegularOutput and is credential-bound; `grep -rE '\$env\b|\$vars\b' n8n/wf_enrichment_cloud.json` still returns nothing; the ZoomInfo usage path sets the vnd.api+json Accept header.
    - `grep -c '"responseMode": "responseNode"' n8n/wf_enrichment_cloud.json` returns 1 — noted as proving the MODE FLAG, not response determinism (the plan states per-batch first-arrival honestly).
    - The full offline suite (`.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs`) shows zero regressions vs the post-Plan-01 baseline; a second builder run is byte-identical.
    - Plan 01's provider-gate topology tests still pass (the credit branch + convergence are aside/downstream, not touching the gates).
  </acceptance_criteria>
  <reversibility rating="reversible">Builder-generated nodes; revert the function edit and rebuild.</reversibility>
  <done>The webhook carries per-provider remaining_credits gathered by a single-item credit branch (one call per provider per run, run-start, gated to providers_requested, graceful-null), converging with every terminal into one honest per-batch Respond node — proven offline (structure) with a deterministic rebuild; runtime ordering + 0-event are documented Track B.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/check_provider_credits.py (imports the side-effect-free registry) + hermetic failure-path tests (reviews A3, A6)</name>
  <files>scripts/check_provider_credits.py, tests/test_check_provider_credits.py</files>
  <read_first>
    - scripts/provision_n8n_credentials.py (FULL — the idiom: `_has_*()` skip-to-exit-0 no-creds banner, the two-key/DRY_RUN gate, never-print-a-secret discipline)
    - tests/test_deploy_n8n_workflows.py (the hermetic monkeypatch/mock-requests pattern to mirror for the A6 tests — no requests-mock dependency)
    - 16.1-RESEARCH.md Task 1 (the three live-validated endpoints/auth/paths + the ZoomInfo inline-mint Basic-auth POST) and 16.1-REVIEWS.md A3 (import the side-effect-free registry, NOT the builder) + A6 (the failure-path test matrix)
    - scripts/provider_registry.py (Plan 01) — import PROVIDER_REGISTRY credit descriptors from HERE (build_cloud_workflows.py writes codegen at import, :32-33 — do NOT import it)
    - n8n/code/providerSelection.js extractCredits (the three parsers to re-apply in Python — small; re-implement, do not build a JSONPath engine)
    - the provider key env-var names (LUSHA_API_KEY, APOLLO_API_KEY, ZOOMINFO_CLIENT_ID, ZOOMINFO_CLIENT_SECRET)
  </read_first>
  <action>
    Write scripts/check_provider_credits.py — a read-only CLI printing each provider's remaining credits.
    Import PROVIDER_REGISTRY credit descriptors from scripts.provider_registry (the side-effect-free
    module Plan 01 created) so the "read-only" script never triggers build_cloud_workflows.py's
    import-time codegen writes (reviews A3). Mirror provision_n8n_credentials.py: a `_has_<provider>()`
    per provider; when NONE are present, print a "skipped (no provider creds)" banner and exit 0 (zero
    calls). For each provider whose key(s) are present, call ONLY the read-only usage endpoint (Lusha GET
    /v3/account/usage with api_key header; Apollo POST usage_stats with X-Api-Key header; ZoomInfo — mint
    the bearer inline via Basic-auth POST /gtm/oauth/v1/token body grant_type=client_credentials only,
    then GET /gtm/data/v1/users/usage with `Accept: application/vnd.api+json`) and apply the matching
    null-safe extractor (Lusha credits.remaining; ZoomInfo uniqueIdLimit usageRemaining + non-zero-totalLimit
    fallback; Apollo -> null). On ANY 4xx/5xx/timeout/malformed report `credits: null` — NEVER raise, NEVER
    print a key/secret value (credit numbers + HTTP status only). Live-only utility (lives in scripts/,
    no test_ prefix — pytest doesn't collect the script itself).

    Write tests/test_check_provider_credits.py (reviews A6 — hermetic, mocking requests exactly like
    tests/test_deploy_n8n_workflows.py; NO live call): no-creds -> zero requests + exit 0; partial creds
    -> only the configured providers are called; Lusha success extraction; Apollo 403 -> null; ZoomInfo
    mint failure -> null with NO usage GET issued; timeout and malformed JSON -> null (no raise); captured
    stdout never contains the supplied fake secret values. This unit-tests the script's functions offline
    while the script stays live-only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_check_provider_credits.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_check_provider_credits.py passes (hermetic): no-creds -> 0 requests + exit 0; Apollo 403 -> null; ZoomInfo mint-fail -> null with no usage GET; timeout/malformed -> null without raising; captured output never contains the fake secret values.
    - check_provider_credits.py imports PROVIDER_REGISTRY from scripts.provider_registry (grep: it does NOT `import build_cloud_workflows`), so running it writes no generated JS files (reviews A3).
    - The script prints NO secret value: grep the file — no f-string interpolates LUSHA_API_KEY / APOLLO_API_KEY / ZOOMINFO_CLIENT_ID / ZOOMINFO_CLIENT_SECRET into a printed line.
    - Apollo is reported as `credits: null` (non-master key — the live-validated graceful-degrade case).
    - `.venv/bin/python -m pytest -q` collects tests/test_check_provider_credits.py (the test) but NOT check_provider_credits.py (the script), and the full suite stays green.
  </acceptance_criteria>
  <reversibility rating="reversible">New standalone read-only script + its test; deleting them has no effect on the pipeline.</reversibility>
  <done>The operator has a read-only per-provider balance check that imports the side-effect-free registry, skips cleanly with no creds, degrades Apollo/failures to null, never prints a secret, and whose failure paths are proven offline with mocked requests (never live in the suite).</done>
</task>

<task type="auto">
  <name>Task 3: Deploy binding for the credit nodes + schedules ship inactive + deploy no-/activate guard (reviews C2, A5; SC-7)</name>
  <files>scripts/deploy_n8n_workflows.py, scripts/build_cloud_workflows.py, n8n/wf_scheduled_maintenance_cloud.json, tests/test_deploy_n8n_workflows.py, tests/test_schedules_inactive.py</files>
  <read_first>
    - scripts/deploy_n8n_workflows.py: NODE_CREDENTIAL_MAP (:44-65 — the node-name -> {cred_type, cred_name} map; add the credit nodes here), bind_credentials (:135-154 — binds mapped nodes, fails closed on unresolvable id, leaves unmapped nodes untouched), `_create_workflow_live`/`_update_workflow_live` (:157-168 — payload keeps only name/nodes/connections/settings, so `active` is dropped), the no-/activate docstring (:25-26)
    - Task 1's credit node NAMES (Lusha usage, Apollo usage, ZoomInfo Usage Mint) — the exact names to register
    - scripts/build_cloud_workflows.py build_scheduled_maintenance_cloud() return dict (~3341-3347 — add the "active" key here) + its sticky notes for the operator-enable documentation
    - 16.1-REVIEWS.md C2 (deploy binding) + A5 (active discarded at deploy; deploy never /activate) + kimi LOW-1 (the /activate guard test); RESEARCH Task 4 (the active-is-intent-marker caveat)
    - tests/test_deploy_n8n_workflows.py (the existing deploy tests to extend: bind_credentials fixture + the two-key gate + the mock-requests-records-calls pattern)
  </read_first>
  <action>
    (1) DEPLOY BINDING (reviews C2) — in scripts/deploy_n8n_workflows.py add NODE_CREDENTIAL_MAP entries
    for Task 1's credit nodes reusing the EXISTING credentials (no provision change): the Lusha usage node
    -> {httpHeaderAuth, "LV Lusha"}, the Apollo usage node -> {httpHeaderAuth, "LV Apollo"}, and the
    ZoomInfo usage-mint node (`ZoomInfo Usage Mint`, name DISTINCT from `ZoomInfo Mint`/`ZoomInfo Mint
    Company`) -> {httpBasicAuth, "LV ZoomInfo"}. The ZoomInfo usage GET node is secret-free (bearer only)
    and needs no binding, like the existing ZoomInfo Enrich Code node. Extend tests/test_deploy_n8n_workflows.py
    to load the built wf_enrichment_cloud.json, identify every credential-requiring node (generic-auth
    marker), call bind_credentials with a mocked name->id map, and assert every credit node receives its
    expected bound credential — and that an unmapped credit node would fail closed (the map covers them).

    (2) DEPLOY NO-/ACTIVATE GUARD (reviews A5, kimi LOW-1) — add a test asserting deploy issues NO POST to
    any `/activate` endpoint for either workflow (mock requests, record calls; the only functional layer,
    since the n8n API ignores `active` on create). Do NOT add an activate call.

    (3) SCHEDULES INACTIVE (SC-7, reviews A5 precise wording) — add `"active": False` to
    build_scheduled_maintenance_cloud()'s returned dict (SJ-1/2/3 are branches in this one workflow, so one
    key covers them). Update its sticky note to state the guarantee PRECISELY: the JSON marker is intent +
    a test hook; the FUNCTIONAL guarantee is that deploy never activates, so a newly-created scheduled
    workflow stays inactive — but deploy does NOT deactivate an ALREADY-active workflow on update (that is
    an operator checkpoint, not automated here). Leave build_enrichment_cloud() with no active key
    (activation is Track B). Rebuild the JSON. Add tests/test_schedules_inactive.py asserting
    wf_scheduled_maintenance_cloud.json["active"] is False and the enrichment webhook workflow has no
    "active": true.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && .venv/bin/python -m pytest tests/test_deploy_n8n_workflows.py tests/test_schedules_inactive.py -q && .venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_deploy_n8n_workflows.py passes: bind_credentials binds every credit node in the built wf_enrichment_cloud.json to its expected credential (Lusha usage->LV Lusha, Apollo usage->LV Apollo, ZoomInfo Usage Mint->LV ZoomInfo); an unmapped credit node fails closed.
    - A deploy test asserts NO POST to any `/activate` endpoint is issued for either workflow (reviews A5/kimi LOW-1).
    - tests/test_schedules_inactive.py passes: wf_scheduled_maintenance_cloud.json["active"] is False; `grep -c '"active": false' n8n/wf_scheduled_maintenance_cloud.json` returns 1; the enrichment webhook workflow has no `"active": true`.
    - The scheduled-maintenance sticky note documents the PRECISE guarantee (inactive-on-create via no-activate; update does not deactivate an already-active workflow) — not overclaimed.
    - Full offline suite green: `.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs` — zero regressions vs the post-Plan-01 baseline; a second builder run is byte-identical.
  </acceptance_criteria>
  <reversibility rating="reversible">Deploy-map + one-key builder edit + field assertions; revert and rebuild.</reversibility>
  <done>The credit nodes deploy bound (registered in NODE_CREDENTIAL_MAP, binding-tested), deploy is proven to never call /activate, and the scheduled-maintenance workflow emits an explicit active:false with a precise (not overclaimed) guarantee and a pytest guard — all offline, deterministic rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud node -> provider usage API | credit checks read balances with bound credentials; a failure must degrade, never break enrichment, and must not exceed the provider rate limit |
| webhook -> caller | remaining_credits is returned post-enrichment; it must not leak secrets |
| local script -> provider usage API | check_provider_credits.py reads keys from .env; never prints a secret, never runs live in the offline suite, never triggers codegen writes on import |
| deploy -> n8n Cloud | credit nodes must deploy BOUND (mapped credentials); scheduled workflow must not be activated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.1-14 | Denial of Service (rate limit) | credit checks fire once per row -> Lusha 5 req/min 429 -> all credits null, SC-5 silently defeated (reviews C1) | high | mitigate | Task 1 single-item credit branch off Parse HubSpot Event -> at most one usage call per provider per run; topology test asserts credit HTTPs are fed by the single-item node, not the multi-row flow |
| T-16.1-15 | Tampering (config) | new credit HTTP nodes deploy UNBOUND -> 401 -> credits:null forever, every offline test still green (reviews C2) | high | mitigate | Task 3 registers the credit nodes in deploy NODE_CREDENTIAL_MAP (LV Lusha/Apollo/ZoomInfo); a deploy-binding test asserts each credit node gets a bound credential (bind_credentials fails closed otherwise) |
| T-16.1-05 | Denial of Service (availability) | a credit-check failure aborts the enrichment run | high | mitigate | Task 1 credit nodes use onError:continueRegularOutput + null-safe extractCredits + guarded nodeAll read; Build Response reports credits:null on any failure; enrichment writeback completes regardless (SC-5) |
| T-16.1-06 | Elevation of Privilege (cost) | a scheduled workflow ships active -> unattended provider burn | high | mitigate | Task 3 bakes active:false + a pytest guard + a deploy no-/activate guard; deploy does not auto-activate (deploy_n8n_workflows.py:25-26); operator enables explicitly (SC-7) |
| T-16.1-07 | Information Disclosure | check_provider_credits.py or the response leaks a provider secret | high | mitigate | Task 2 prints credit numbers + HTTP status only (grep guard + A6 captured-output assertion); Task 1 credit nodes are credential-bound with zero env-var/inline-secret in the built JSON |
| T-16.1-16 | Tampering (repo integrity) | the read-only credit script rewrites codegen files on import (reviews A3) | low | mitigate | Task 2 imports scripts.provider_registry (side-effect-free), not build_cloud_workflows.py |
| T-16.1-09 | Spoofing (nondeterminism) | responseMode returns whichever terminal finished first; 0-event body -> no Respond -> HubSpot retry (reviews C3) | medium | mitigate/defer | Task 1 converges every terminal (5 nodes + 2 re-pointed false-lanes + unsupported terminal) into one Respond node and states per-batch first-arrival semantics HONESTLY; the true 0-event + arrival-ordering proof is a Track B execution-level test (static graph cannot prove it) |
| T-16.1-17 | Tampering (config) | plan claims deploy deactivates an already-active scheduled workflow (it does not) | low | mitigate | Task 3 states the guarantee precisely (inactive-on-create only); deactivating an already-active workflow is an operator checkpoint, not automated |
| T-16.1-08 | Tampering (data) | credit endpoint/auth drift between runtime node and CLI script | medium | mitigate | both consume scripts.provider_registry credit descriptors; extractors validated live (RESEARCH Task 1) + unit-tested (providerSelection.test.mjs) |
| T-16.1-SC | Tampering (supply chain) | npm/pip installs | low | accept | No new package — reuse requests/urllib + existing HTTP/Code node types |
</threat_model>

<verification>
- Full offline suite green: `.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs` — zero regressions; all Phase-16.1 tests pass.
- remaining_credits (C1): each credit HTTP is fed by the single-item Credit Request node off Parse HubSpot Event (one call per provider per run), gated to providers_requested (none -> zero calls -> []), read by Build Response via guarded nodeAll, degrading to null on failure without failing the run (tests/test_remaining_credits_response.py).
- Convergence: Build Response reachable from every terminal (5 real nodes + 2 re-pointed IF-enrich-false lanes + unsupported terminal); response semantics stated honestly as per-batch first-arrival (C3).
- Deploy binding (C2): every credit node is in NODE_CREDENTIAL_MAP and binds; deploy issues no /activate (A5) — tests/test_deploy_n8n_workflows.py.
- check_provider_credits.py: imports the side-effect-free registry (A3), skips to exit 0 with no creds, degrades Apollo/failures to null, never prints a secret; failure paths hermetically tested (A6).
- Schedules inactive: wf_scheduled_maintenance_cloud.json["active"] is False with a precise (not overclaimed) guarantee (SC-7/A5).
- No new env-var expression / inline secret in the rebuilt Cloud JSON; deterministic rebuild.

## Track B (execution-level / live — NOT offline-provable, documented not fixed)
- Runtime response ordering across a mixed create/update/skip batch + the 0-event/empty-body no-Respond behavior (HubSpot retry) — needs an n8n execution-level test with a mixed event array + an empty-body request (gpt/kimi C3).
- Live `check_provider_credits.py` with real .env keys (Apollo reports null with a non-master key).
- Deactivating an already-active scheduled workflow on update is an operator checkpoint (A5).
</verification>

<success_criteria>
Credit reporting + schedule safety complete when SC-4/5/6/7/8 are proven offline with the reviews
fixes: credit checks run once per run via a single-item branch (C1) and deploy bound (C2), the
response carries per-provider remaining_credits with honest per-batch semantics (C3), the read-only
CLI balance check imports the side-effect-free registry (A3) with hermetically-tested failure paths
(A6), the scheduled workflow emits active:false with a precise guarantee + deploy no-/activate guard
(A5/SC-7), and the full offline suite stays green with a deterministic rebuild.
</success_criteria>

<output>
Create `.planning/phases/16.1-provider-selection-credit-reporting-schedule-safety/16.1-02-SUMMARY.md` when done.
</output>
