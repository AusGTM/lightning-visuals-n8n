---
phase: 16.1-provider-selection-credit-reporting-schedule-safety
verified: 2026-07-24T00:00:00Z
status: passed
score: 8/8 success criteria verified
behavior_unverified: 0
overrides_applied: 0
verdict: ACHIEVED
re_verification:
  previous_status: none
  note: initial verification
---

# Phase 16.1: Provider Selection, Credit Reporting & Schedule Safety — Verification Report

**Phase Goal:** The on-demand enrichment webhook is caller-controlled and cost-safe before going live — the caller chooses which provider adapters run (or none), the response reports remaining provider credits, and scheduled workflows ship disabled.
**Verified:** 2026-07-24
**Status:** passed
**Verdict:** **GOAL ACHIEVED**
**Re-verification:** No — initial verification

## Method

Goal-backward against the delivered code, not the SUMMARYs. Independently:
ran both test suites (`332 pytest / 169 node`, 0 fail), reran the builder twice
and diffed `n8n/wf_*.json` (empty), and parsed the emitted
`wf_enrichment_cloud.json` / `wf_scheduled_maintenance_cloud.json` graphs to
confirm topology invariants directly rather than trusting the coverage table.

## Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | `providers` node parsed (all/list/none/blank/absent→none), threaded to both branches | ✓ VERIFIED | `providerSelection.js` `resolveEnabledProviders`: `"all"`→all names, array→intersection (unknown dropped, lowercased), everything else (`none`/`""`/`null`/`undefined`/unrecognized)→`[]`. `parseWebhookBody` handles bare-array / envelope `{providers,events}` / single-event (`parsed.providers ?? event.providers`). `ENRICH_PARSE_EVENT_CLOUD` threads `provider_enabled`/`providers_requested` onto every emitted row. Both branches' gates read `$('Parse HubSpot Event').item.json.provider_enabled.<name>`. |
| 2 | Disabled provider HTTP node does NOT execute; none/blank/absent still runs the rest with zero provider calls | ✓ VERIFIED | Parsed graph: each provider HTTP node's ONLY inbound edge is its own `IF <provider> Enabled` true-output (index 0) — Lusha Enrich, Apollo Match, Lusha Company, Apollo Org all confirmed. Zero-provider bypass chain traced: contacts `IF Lusha Enabled→IF Apollo Enabled→IF ZoomInfo Enabled→Normalize + Score`; companies `…→Normalize + Score Company`. Convergence always reached with zero providers. |
| 3 | Adapter registry single-source + parity with `normalizeProviders.js` MAPPERS; shared `_provider_gate_bypass_chain` used by both branches | ✓ VERIFIED | `scripts/provider_registry.py` `PROVIDER_REGISTRY` keys `{lusha,apollo,zoominfo}` == `normalizeProviders.js` MAPPERS keys (parity test green). Side-effect-free (imports write no files — test green). `_provider_gate_bypass_chain(...)` defined once, called identically at builder L2922 (contacts) and L3056 (companies). |
| 4 | Credit endpoints live-curl-validated + graceful degrade (Apollo non-master→null) | ✓ VERIFIED | `16.1-RESEARCH.md` records live curls this session: Lusha `GET /v3/account/usage`→200 `credits.remaining`; ZoomInfo `GET /gtm/data/v1/users/usage`→200 only with `Accept: application/vnd.api+json` (406 otherwise), balance at `usage[limitType=uniqueIdLimit].usageRemaining`; Apollo `POST usage_stats`→403 (non-master)→degrades to null. Registry/extractors match. Credit HTTP nodes carry `onError: continueRegularOutput`. *Note: live curl is historical evidence, not re-runnable offline — accepted on documented evidence + degrade code present.* |
| 5 | `remaining_credits` per-provider via `responseNode` convergence; single-item credit branch (once per provider per run); credit failure never fails enrichment | ✓ VERIFIED | Webhook `responseMode: responseNode`→`Respond to Webhook`←`Build Response` (8 inbound terminals incl. Unsupported). `Credit Request` forks off `Parse HubSpot Event` (single item, never `$input`); each credit HTTP node's only inbound is its own `IF <provider> Credit Requested` true-lane → at most once/provider/run. `Build Response` uses guarded `nodeAll` + `extractCredits`, `remaining_credits` = exactly `providers_requested` (none→`[]`). `onError: continueRegularOutput` on all credit nodes. |
| 6 | `check_provider_credits.py` read-only, `.env`-keyed, two-key/no-creds skip, excluded from offline suite | ✓ VERIFIED | Read-only GET/POST usage calls only; `_has_lusha/_has_apollo/_has_zoominfo` (ZoomInfo two-key), no-creds→banner+exit 0, zero requests. Imports `provider_registry` not the builder. Every failure path → `credits: None`, never raises; never prints a secret. Script itself NOT collected by pytest (confirmed via `--collect-only`); its 15 hermetic mocked tests are. |
| 7 | Scheduled-maintenance emits `active: false`; deploy never activates; enrichment stays operator-controlled | ✓ VERIFIED | `wf_scheduled_maintenance_cloud.json` has `active: false`. `deploy_n8n_workflows.py` has NO `/activate` call site (only doc prose); NODE_CREDENTIAL_MAP binds the three credit nodes to LV Lusha/Apollo/ZoomInfo. Honestly scoped: n8n Public API ignores `active` on create, so real guarantee = deploy never POSTs `/activate` (inactive-on-create). Enrichment webhook ships with no `active` key (operator-enabled) — matches Decision 7. |
| 8 | Offline acceptance tests green; builder rebuild deterministic | ✓ VERIFIED | `332 passed` pytest, `169 pass` node — reproduced independently. Builder run twice → `git diff --stat n8n/wf_*.json` empty. |

**Score: 8/8 criteria verified.**

## Locked-Decision Fidelity

- **Decision 1 (no global kill-switch):** `grep` for `ALLOW_PROVIDER_CALLS` / `MAX_RECORDS_PER_RUN` in `scripts/` + `n8n/` code → none. ✓
- **Decision 2 (absent/blank/none → enrich nothing):** resolve falls to `[]` for all non-`all`/non-array inputs. ✓
- **Decision 3 (gate before paid call):** provider HTTP node's sole inbound is its gate's true lane — never call-then-discard. ✓
- **Decision 8 (contacts research/judge NOT built; symmetric fan-out + seam):** Contacts branch traced `Normalize + Score → Merge Winners` directly — NO research/judge nodes. The 10 research/judge nodes present are all on the **companies** branch (`Normalize + Score Company → Research Trigger Gate → … → Merge Company`, pre-existing from Phase 13/14/16). In-graph sticky note marks the Phase 16.2 seam at `Normalize + Score → Merge Winners`. Shared `_provider_gate_bypass_chain` is the reuse seam. ✓

## Track-B Deferred Items (honestly recorded, not dropped)

Confirmed flagged in source, not silently omitted:
- **Lusha Company POST-vs-live-GET mismatch** — builder L3030 `Track B flag (reviews LOW-5, NOT fixed in 16.1)`.
- **0-event/empty-body response + multi-terminal arrival ordering** — builder L2736–2737 + L3395, documented as per-batch FIRST-ARRIVAL parity (not hard determinism), Track B execution-level test items.
- **Apollo master key** — `providerSelection.js` L81 + `check_provider_credits.py` L80: non-master 403 → null degrade, master-key shape read defensively.

## Anti-Patterns

No `TBD`/`FIXME`/`XXX` debt markers in any modified source file. No stubs — every artifact is substantive and wired.

## Notes / Non-Blocking Observations

- **SC-4 live-curl is historical.** The endpoint validation was performed during the RESEARCH step and is documented with `[VERIFIED: live curl 200/403]` anchors + real balances. It is not re-runnable in this offline verification (no live calls in scope). Graceful-degrade paths ARE offline-verified (Apollo 403→null test green). This is the correct scope boundary, not a gap.
- **Baseline delta.** Task cited a `267/147` pre-phase baseline; current `332/169` is fully green with no failing pre-existing tests (regressions would surface as failures, none did). Increases are additive new test files (providerSelection 22 node cases; new pytest files). No regression.
- **"Enrichment webhook stays active"** (SC-7 phrasing) is operator-controlled, not deploy-forced — consistent with the honestly-documented inactive-on-create reality. Not a defect.

## Gaps

None.

---
_Verified: 2026-07-24_
_Verifier: Claude (gsd-verifier)_
