---
phase: 16.3-companies-stale-timestamp-fix
verified: 2026-07-28T00:00:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 16.3: Companies Stale Timestamp Fix Verification Report

**Phase Goal:** The companies path stamps `lv_*_verified_at` cache-key datetimes ONLY when a field is actually promoted, so a needs_review/stale-but-unpromoted candidate can never mark itself fresh and suppress the next stale-refresh (CLAUDE.md §19.5).
**Verified:** 2026-07-28
**Status:** passed
**Re-verification:** No — initial verification

## Method

All claims were independently reproduced, not taken from SUMMARY.md prose:
- Read `n8n/code/mergeCompanies.js` / `mergeContacts.js` as they now stand.
- Extracted `Merge Company` jsCode directly from the committed `n8n/wf_enrichment_cloud.json` with a Python one-liner and diffed it against the source file.
- Used `git worktree` to check out commits `402d8a1` (RED) and `318a73e` (GREEN) in isolation (no working-tree mutation) and ran `node --test tests/n8n/mergeCompanyStaleTimestamp.test.mjs` against each.
- Manually broke the vacuity guard's precondition (simulated an upstream-dropped candidate) to prove the guard is load-bearing, not decorative.
- Re-ran the Task 3 14-pair comparison independently via `build_enrichment_cloud()` / `build_enrichment_local_live()` and a fresh comment-stripped diff, rather than trusting the SUMMARY's "14-pair proof" claim.
- Ran the full offline suite myself (`pytest -q`, `node --test tests/n8n/*.test.mjs`) and ran the builder twice to check determinism.
- Diffed `mergeCompanies.js` and `src/merge_policy.py` across the whole phase span (`35acd37..a49f4a7`).

## Goal Achievement — Per-Criterion Table (ROADMAP.md Phase 16.3, SC-1 through SC-6)

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| SC-1 | `mergeCompanies.js` stamps `cacheKeys[COMPANY_CACHE_KEY_FIELDS[field]]` only inside `decision === "promote"`, structurally mirroring `mergeContacts.js` | ✓ VERIFIED | Read both files: the 3-line block sits inside the promote branch in both, same shape/ordering (`mergeCompanies.js:209-216`, `mergeContacts.js:183-194`). `git diff 35acd37 a49f4a7 -- n8n/code/mergeCompanies.js` shows the ONLY change is this relocation + comment — no gate/policy/provenance/decisions change. |
| SC-2 | Unit tests mirror `mergeContacts.test.mjs:126-144` onto `mergeCompanies.test.mjs` | ✓ VERIFIED | Two tests present at `tests/n8n/mergeCompanies.test.mjs:132-150` (unpromoted-gated emits no stamp / promoted emits stamp). Ran `node --test tests/n8n/mergeCompanies.test.mjs` myself — both pass. |
| SC-3 | End-to-end functional validation through the COMPILED `Merge Company` node body, with red-before-green as durable evidence | ✓ VERIFIED | Verified the pinned fixture is byte-identical to a live re-extract of `Merge Company` from the pre-fix commit (`402d8a1^`). Checked out `402d8a1` in an isolated `git worktree` and ran the test myself: 3 pass / 1 fail (the POST-body no-stamp assertion) — matches SUMMARY exactly, confirmed as the RIGHT failure. Checked out `318a73e` and reran: 4/4 pass, same row, proving red→green. Independently broke the vacuity guard's precondition (candidate dropped upstream) and confirmed `decisions` comes back empty — the guard would correctly fail in that scenario, so it is not vacuous. |
| SC-4 | `companies_jscode_frozen.json` re-baselined as an explicit act in its own commit; diff confined to `Merge Company`, other 6 frozen nodes byte-identical | ✓ VERIFIED | `git show 467a7d7 --stat` touches exactly 1 file, 2 insertions/2 deletions (both variants' `Merge Company` entry). Independently re-ran the 14-pair comparison (calling `build_enrichment_cloud()`/`build_enrichment_local_live()` myself) against the pre-467a7d7 fixture: exactly 2/14 differ, both `Merge Company`; other 12 byte-identical. Independently produced the comment-stripped diff: surviving diff is exactly the relocated 3-line block, both variants — matches SUMMARY's claim verbatim, verified not trusted. |
| SC-5 | `wf_enrichment_cloud.json` / `wf_enrichment_local_live.json` regenerated; builder deterministic (rebuild twice, no diff) | ✓ VERIFIED | Confirmed emitted `Merge Company` jsCode in committed `wf_enrichment_cloud.json` matches source. Ran `scripts/build_cloud_workflows.py` twice myself at current HEAD — `git status --short n8n/` stayed clean both times. |
| SC-6 | Full offline suite green, zero regressions vs 346 pytest / 228 (→234) node baseline; no live calls | ✓ VERIFIED | Ran `.venv/bin/python -m pytest -q` myself: 346 passed. Ran `node --test tests/n8n/*.test.mjs` myself: 234 passed, 0 failed. `test_companies_factory_frozen.py` confirmed 4/4 pass individually. Grepped test files for API-key/token/live-URL patterns — none found. Node count delta (228→234 = +6) reconciles exactly with the 4 new differential tests + 2 mirrored unit tests. |

**Score:** 6/6 truths verified

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `n8n/code/mergeCompanies.js` | cacheKeys write relocated inside promote branch | ✓ VERIFIED | Read in full; confirmed structural mirror of contacts twin |
| `n8n/code/mergeContacts.js` | stale NOTE retired | ✓ VERIFIED | `grep -c "same latent issue"` returns 0; comment now correctly states both paths carry the fix |
| `tests/fixtures/merge_company_prefix_jscode.json` | pinned pre-fix jsCode | ✓ VERIFIED | Byte-identical to a fresh extraction from `402d8a1^`'s committed `wf_enrichment_cloud.json` |
| `tests/n8n/mergeCompanyStaleTimestamp.test.mjs` | compiled-node-body differential w/ vacuity guard | ✓ VERIFIED | 4 tests; reads jsCode via `new Function`, not `mergeCompanies()` directly (confirmed by reading the harness) |
| `tests/n8n/mergeCompanies.test.mjs` | mirrored unit tests | ✓ VERIFIED | Present and passing |
| `tests/fixtures/companies_jscode_frozen.json` | bounded re-baseline | ✓ VERIFIED | Isolated commit, 2/14 pairs changed, both `Merge Company` |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `mergeCompanies.js` promote branch | `Merge Company` Code node | `inline()` at build time | ✓ WIRED | Extracted jsCode from committed `wf_enrichment_cloud.json`; contains the fix verbatim |
| `Merge Company` jsCode | `companies_jscode_frozen.json` | byte-identity guard | ✓ WIRED | `test_companies_factory_frozen.py` 4/4 pass |
| `ENRICH_MERGE_CO` wrapper `cacheKeys` spread | `mergeCompanies()` outputs | shallow spread, no synthesis | ✓ WIRED | Read `build_cloud_workflows.py:2210-2218` myself: `cacheKeys: {...merged.cacheKeys, ...researchMerged.cacheKeys}` — both operands are `mergeCompanies()` return values, no independent cache-key write in the wrapper. Companies research-fold path inherits the fix for free, as claimed. |

## Test-Substrate Check (explicit user requirement)

Confirmed `mergeCompanyStaleTimestamp.test.mjs` extracts `Merge Company` jsCode from built workflow JSON (`loadPostBody()` reads `n8n/wf_enrichment_cloud.json` and finds the node) and executes it via `new Function(...)`, exactly matching the `researchChainRowFlow.test.mjs` mechanism. It does NOT `require()`/import `mergeCompanies()` and call it directly for the POST-body assertions — the PRE and POST bodies are both raw jsCode strings run through the same `runMergeCompany()` helper.

## Scope Fence

| Prohibition | Status | Evidence |
|---|---|---|
| No shared-helper extraction between mergeCompanies.js/mergeContacts.js | ✓ HELD | `git diff --stat` across phase shows only the two files themselves changed, no new module |
| No promotion/gating/field-policy/ICP-scoring behavior change | ✓ HELD | `git diff 35acd37 a49f4a7 -- n8n/code/mergeCompanies.js` is exactly the 3-line relocation + comment |
| `src/merge_policy.py` unchanged | ✓ HELD | `git diff 35acd37 a49f4a7 -- src/merge_policy.py` is empty. Its equivalent unconditional-write shape at `:279-287` was found and correctly recorded as NOT fixed, out of fence. |
| No fetch-by-objectId work | ✓ HELD | No `objectId`/`fetchByObjectId` reference in any changed file |
| No live calls / credentials in tests | ✓ HELD | Grepped test files for API keys/tokens/live URLs — none found |

## Anti-Patterns Found

None. No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers in any file touched by this phase.

## Deviations Noted (from SUMMARY, independently confirmed benign)

- `n8n/wf_scheduled_maintenance_cloud.json` also regenerated (not in plan's `files_modified`). Confirmed benign: its `Apply Review` node also inlines `mergeCompanies.js`; the diff contains `COMPANY_CACHE_KEY_FIELDS` at the same relocated positions as the other regenerated workflow files — same fix, correct propagation, not scope creep.

## Human Verification Required

None. All 6 success criteria are mechanically verifiable and were independently reproduced against the actual repository state (not inferred from SUMMARY.md).

## Gaps Summary

None found. All 6 ROADMAP success criteria verified by direct, independent reproduction: source diff inspection, isolated git-worktree execution of the RED and GREEN commits' test suite, an original re-derivation of the Task 3 14-pair/comment-stripped-diff bounded-blast-radius proof, a manual stress-test of the vacuity guard's protective value, a from-scratch full-suite run (346 pytest / 234 node, 0 failures), and a two-pass deterministic-rebuild check.

---
_Verified: 2026-07-28_
_Verifier: Claude (gsd-verifier)_
