---
phase: 16.3-companies-stale-timestamp-fix
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - tests/fixtures/merge_company_prefix_jscode.json
  - tests/n8n/mergeCompanyStaleTimestamp.test.mjs
  - n8n/code/mergeCompanies.js
  - n8n/code/mergeContacts.js
  - tests/n8n/mergeCompanies.test.mjs
  - n8n/wf_enrichment_cloud.json
  - n8n/wf_enrichment_local_live.json
  - n8n/wf_enrichment_local.json
  - n8n/wf_contact_ingest_cloud.json
  - n8n/wf_contact_ingest_local.json
  - tests/fixtures/companies_jscode_frozen.json
requirements:
  - "SC-1 (mergeCompanies.js stamps cacheKeys[COMPANY_CACHE_KEY_FIELDS[field]] only inside the decision === \"promote\" branch — a structural mirror of mergeContacts.js:183-194)"
  - "SC-2 (unit tests mirror tests/n8n/mergeContacts.test.mjs:126-144 onto tests/n8n/mergeCompanies.test.mjs)"
  - "SC-3 (END-TO-END FUNCTIONAL validation through the COMPILED Merge Company node body, with red-before-green as durable evidence)"
  - "SC-4 (tests/fixtures/companies_jscode_frozen.json re-baselined as an explicit reviewed act in its own commit; diff confined to Merge Company, other six frozen nodes byte-identical)"
  - "SC-5 (wf_enrichment_cloud.json / wf_enrichment_local_live.json regenerated; builder deterministic — rebuild twice, no diff)"
  - "SC-6 (full offline suite green, zero regressions vs the 346 pytest / 228 node baseline; no live calls)"

must_haves:
  truths:
    - "A stale-but-unpromoted companies field emits NO cache-key datetime. Concretely: an unevidenced `lv_org_type` research candidate (a value in EVIDENCE_GATED_ORG_TYPES with no evidence URL) is gated to `needs_review` by mergeCompanies.js's own `_needsEvidence` check and therefore leaves `cacheKeys` empty — so the record's existing (stale) `lv_org_type_verified_at` survives untouched and CLAUDE.md §19.5's monthly stale-refresh scan re-picks the record up on its next pass. Implements D-01 (fix by structural mirror of mergeContacts.js:183-194, no shared-helper extraction)."
    - "That truth holds through the COMPILED `Merge Company` Code-node body — the jsCode string emitted by `build_enrichment_cloud()` and committed to n8n/wf_enrichment_cloud.json — not merely through the pure `mergeCompanies()` export. The compiled body additionally exercises the ENRICH_MERGE_CO research fold's `cacheKeys: { ...merged.cacheKeys, ...researchMerged.cacheKeys }` shallow spread (build_cloud_workflows.py:2216), which is the ONLY place a companies research-lane cache key can reach the patch. Implements D-03 (end-to-end means through the compiled node body)."
    - "The bug is DEMONSTRATED, not asserted: the identical row driven through the PRE-FIX compiled body emits a fresh `lv_org_type_verified_at`, and through the POST-FIX compiled body emits none. The pre-fix body is pinned as a permanent fixture so the red half is a running differential test, not a stashed console log that decays. Implements D-02 (red-before-green is required evidence)."
    - "A genuinely promotable field is unaffected: an EVIDENCED `lv_org_type` still promotes and still stamps `lv_org_type_verified_at` in the compiled body — proving the fix is a narrowing of WHEN the stamp is written, not a removal of the stamp."
    - "The companies research-fold path inherits the fix with no second edit: because `researchMerged.cacheKeys` is itself produced by the fixed `mergeCompanies()`, the ENRICH_MERGE_CO spread can only carry stamps for promoted fields. This is VERIFIED by the compiled-body test, not assumed — it is the companies counterpart of mergeContacts.js:260-263's third `CONTACT_CACHE_KEY_FIELDS` read, which is already promote-gated by its `if (decision.decision !== \"promote\") continue;` guard at :256."
    - "The re-baseline of tests/fixtures/companies_jscode_frozen.json is BOUNDED and PROVEN bounded before it is written: across both `cloud` and `local_live`, exactly one of the seven FROZEN_NODE_NAMES (`Merge Company`) differs pre/post, and stripping comment-only lines from the two `Merge Company` strings leaves a diff consisting solely of the relocated 3-line `COMPANY_CACHE_KEY_FIELDS` block. Any movement in the other six nodes is a STOP condition. Implements D-04 (re-baseline is its own reviewed commit) and D-05 (both builders in scope)."
    - "A second `.venv/bin/python scripts/build_cloud_workflows.py` run produces byte-identical wf_*.json, and the full offline suite (`.venv/bin/python -m pytest -q` && `node --test tests/n8n/*.test.mjs`) is green with zero regressions vs the 346 pytest / 228 node baseline (new tests are net-positive). Implements D-06 (rebuild is mandatory and deterministic)."
  artifacts:
    - "tests/fixtures/merge_company_prefix_jscode.json — the PRE-FIX compiled `Merge Company` jsCode, captured before the source edit; the durable red-evidence artifact"
    - "tests/n8n/mergeCompanyStaleTimestamp.test.mjs — the compiled-node-body differential test (pre-fix body stamps / post-fix body does not / evidenced control still stamps)"
    - "n8n/code/mergeCompanies.js — the cacheKeys write relocated inside the `decision === \"promote\"` branch"
    - "n8n/code/mergeContacts.js — the now-stale NOTE naming mergeCompanies as the outstanding twin updated/removed"
    - "tests/n8n/mergeCompanies.test.mjs — the two mirrored unit tests (unpromoted emits no stamp / promoted does)"
    - "tests/fixtures/companies_jscode_frozen.json — re-baselined, isolated commit"
  key_links:
    - "mergeCompanies.js `decision === \"promote\"` branch -> `cacheKeys[COMPANY_CACHE_KEY_FIELDS[field]]` -> ENRICH_MERGE_CO's `{...merged.cacheKeys, ...researchMerged.cacheKeys}` spread (build_cloud_workflows.py:2216) -> `Decide Company Action`'s `{...merge.canonicalPatch, ...(merge.cacheKeys||{})}` patch -> HubSpot `lv_org_type_verified_at` -> the enrichmentGate / SJ-2 stale predicates that read it"
    - "the emitted `Merge Company` jsCode string -> tests/fixtures/companies_jscode_frozen.json -> tests/test_companies_factory_frozen.py (the guard that MUST go red on the fix and green after the bounded re-baseline)"
  prohibitions:
    - "NO shared-helper extraction between mergeCompanies.js and mergeContacts.js — deliberately separate files since Phase 11 (CONTEXT Scope Fence, D-01)."
    - "NO change to promotion/gating logic, DEFAULT_COMPANY_POLICY, field policy, ICP scoring, provider handling, or any decision/provenance/decisions-array shape. The ONLY behavior change is WHEN a cache-key datetime is written."
    - "NO edit to src/merge_policy.py. Its `source_metadata()` cache-key write is the Python harness's own lane and is out of this phase's fence — if it shows the same unconditional shape, RECORD it in the SUMMARY as a finding, do not fix it."
    - "NO re-baseline of any frozen node other than `Merge Company`. If `Research Trigger Gate` / `Build Research Request` / `Validate Research Output` / `Judge Gate` / `Build Judge Request` / `Apply Judge Verdict` move in either variant, STOP and report — that is unintended blast radius, not something to absorb into the baseline."
    - "NO fetch-by-objectId work (Phase 16.4). NO live calls, no credential use, no n8n deploy."
    - "Do NOT regenerate tests/fixtures/companies_jscode_frozen.json by blind overwrite, and do NOT fold it into the source-fix commit — diff first, prove the shape, then write, in its own commit."
---

<objective>
Mirror the Phase 16.2 gpt #6 stale-timestamp fix onto the companies path: move
`cacheKeys[COMPANY_CACHE_KEY_FIELDS[field]] = verifiedAt` (n8n/code/mergeCompanies.js:205-207)
inside the `decision === "promote"` branch, so a `needs_review`/stale-but-unpromoted companies
field can never mark itself fresh and suppress CLAUDE.md §19.5's monthly stale-refresh forever.

Purpose: the code change is ~3 lines; the PHASE is the two things around it. (a) PROVE the bug
end-to-end through the COMPILED `Merge Company` node body, because the whole reason gpt #6 survived
into a shipped chain is that the offline suite exercises JS functions in isolation — and because on
the companies path the only cache-key-bearing fields (lv_org_type / lv_produces_content) reach
`cacheKeys` exclusively via the ENRICH_MERGE_CO research fold's shallow spread, a wrapper-level
surface no pure-function test touches. (b) Perform the frozen-fixture re-baseline this fix
necessarily invalidates as a deliberate, bounded, separately-committed act — diff-proven to touch
only `Merge Company` — rather than letting it degrade into "made the test pass".

Output: the fixed merge module, an updated stale NOTE on the contacts twin, mirrored unit tests, a
permanent pre-fix/post-fix compiled-body differential test with its pinned red-evidence fixture,
regenerated deterministic workflow JSON, and a reviewed re-baseline of the companies byte-identity
fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-companies-stale-timestamp-fix/16.3-CONTEXT.md
@.planning/phases/16.2-contacts-research-judge-mirror/16.2-02-SUMMARY.md

# Target + reference sources (read the specific ranges named in each task's read_first)
@n8n/code/mergeCompanies.js
@n8n/code/mergeContacts.js
@tests/n8n/researchChainRowFlow.test.mjs
@tests/test_companies_factory_frozen.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: RED — pin the pre-fix compiled `Merge Company` body and prove the bug through it end-to-end</name>
  <files>tests/fixtures/merge_company_prefix_jscode.json, tests/n8n/mergeCompanyStaleTimestamp.test.mjs</files>
  <precondition>The working tree has no uncommitted edit to n8n/code/mergeCompanies.js or scripts/build_cloud_workflows.py, and `.venv/bin/python -m pytest tests/test_companies_factory_frozen.py -q` is GREEN — i.e. the committed wf_enrichment_*.json genuinely carries the PRE-FIX compiled body about to be pinned. If it is not green, STOP: the pin would capture an unknown state.</precondition>
  <read_first>
    - tests/n8n/researchChainRowFlow.test.mjs:57-109 — the `new Function("$", "$input", "$json", "$node", "$now", "$today", '"use strict";\n' + node.parameters.jsCode)` execution mechanism over the repo's OWN committed jsCode, and its file-header note on why executing committed jsCode here is safe. This is the substrate to reuse; do NOT invent a new harness.
    - scripts/build_cloud_workflows.py:2125-2224 (ENRICH_MERGE_CO) — the row shape the compiled node consumes: `row.scored` (absent -> the `merge: null` skip branch at :2154), `row.existingRecord`, `row.research_candidate` ({matched, confidence, data, evidence_by_field}) at :2191-2208, and the cacheKeys shallow spread at :2216. The node reads ONLY `$input`.
    - n8n/code/mergeCompanies.js:85-88 (COMPANY_CACHE_KEY_FIELDS — the 2 cache-key fields), :93-99 (`_needsEvidence`), :113-116 (the evidence gate returning needs_review), :177-225 (the per-field loop; the unconditional stamp at :205-207 immediately ABOVE the promote branch at :209-211)
    - n8n/code/taxonomy.generated.js:68-73 (EVIDENCE_GATED_ORG_TYPES = content_producer, gambling_operator, governing_body_league, hardware_vendor — any of these as an unevidenced `lv_org_type` is gated to needs_review)
    - tests/test_companies_factory_frozen.py:32-40 (FROZEN_NODE_NAMES) and :43-48 (`_extract_code_nodes`) — the extraction pattern to reuse for the capture
  </read_first>
  <action>
    Capture the red evidence FIRST, from the current HEAD build, before any source edit exists.

    (1) CAPTURE. Run `.venv/bin/python scripts/build_cloud_workflows.py` to confirm the committed
    artifacts are current, then write tests/fixtures/merge_company_prefix_jscode.json containing the
    `Merge Company` node's `parameters.jsCode` string extracted from the committed
    n8n/wf_enrichment_cloud.json, under a single top-level key (e.g. `{"cloud": {"Merge Company": "..."}}`,
    matching companies_jscode_frozen.json's shape so the two read alike). Pin the CLOUD variant only and
    say why in a file-adjacent comment inside the test: the `Merge Company` jsCode is character-identical
    across the cloud and local_live builds (ENRICH_MERGE_CO bakes no config flags), so one copy guards both.
    This fixture is WRITE-ONCE evidence — it is never regenerated by any later phase.

    (2) DIFFERENTIAL TEST. Write tests/n8n/mergeCompanyStaleTimestamp.test.mjs with a single shared
    helper `runMergeCompany(jsCode, row)` built on the researchChainRowFlow.test.mjs mechanism: construct
    the `$input` context over `[row]`, execute the jsCode via `new Function`, and return the first output
    item's `json`. The test file loads TWO bodies: PRE (from the fixture written in step 1) and POST (the
    `Merge Company` jsCode read live out of the committed n8n/wf_enrichment_cloud.json, so the assertion
    tracks the shipped artifact, not a rebuilt-in-memory string).

    Build ONE stale-unpromoted row modelling the real CLAUDE.md §19.5 failure — a company whose
    lv_org_type was verified long ago and is being re-researched:
      - `existingRecord`: carries `lv_org_type` set to some prior value AND `lv_org_type_verified_at` set
        to a fixed, obviously-stale ISO date (e.g. 200+ days before a hard-coded `now`), plus domain/name.
      - `scored`: a minimal `{ best: {}, winners: {}, sourcesByField: {} }` — non-null so the node does not
        take its `merge: null` skip branch, and empty so the firmographic lane contributes nothing.
      - `research_candidate`: `{ matched: true, confidence: 90, data: { lv_org_type: <a value from
        EVIDENCE_GATED_ORG_TYPES> }, evidence_by_field: {} }` — high confidence, gated value, NO evidence
        URL, so mergeCompanies' `_needsEvidence` gate returns needs_review and the field cannot promote.

    Assert, in this order:
      (a) VACUITY GUARD (must come first, and must fail loudly rather than pass emptily): on the POST body,
          `out.merge` is a non-null object AND `out.merge.decisions` contains an entry for `lv_org_type`
          whose `decision` is `needs_review` AND `lv_org_type` is absent from `out.merge.canonicalPatch`.
          Without this, a future change that simply drops the candidate before the merge would make the
          cache-key assertions pass for the wrong reason.
      (b) RED: the PRE body emits `lv_org_type_verified_at` in `out.merge.cacheKeys`, and its value is NOT
          the stale timestamp carried on existingRecord — i.e. the pre-fix build actively refreshes a field
          it refused to promote. Include the §19.5 consequence in the assertion message.
      (c) GREEN: the POST body emits NO `lv_org_type_verified_at` key at all for the SAME row.
      (d) CONTROL (no over-fix): the same row with a non-blank `evidence_by_field` entry for `lv_org_type`
          promotes on the POST body (`out.merge.canonicalPatch.lv_org_type` set) AND still emits
          `lv_org_type_verified_at`. Note in a comment that both a plain string URL and a `{url}` object
          appear as evidence_by_field values in-repo; mergeCompanies only `_isBlank`-checks it, so either
          shape satisfies the gate — pick one and state which.

    At the end of this task assertions (a), (b) and (d) PASS and (c) FAILS against the shipped artifact.
    That failure IS the deliverable: capture the verbatim `node --test` output for (c) — it is the
    red-before-green evidence recorded in the SUMMARY. Do NOT edit any production file to make it pass.

    Commit the fixture and the test with the repo's RED convention: `test(16.3-01): failing compiled-node
    test proving Merge Company refreshes lv_org_type_verified_at on an unpromoted field`. State in the
    commit body that exactly one assertion is expected red until Task 2 lands.
  </action>
  <verify>
    <automated>node --test tests/n8n/mergeCompanyStaleTimestamp.test.mjs; echo "EXPECTED RED at this point — capture the output verbatim for the SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/merge_company_prefix_jscode.json exists and its pinned string is byte-identical to the `Merge Company` `parameters.jsCode` in the committed n8n/wf_enrichment_cloud.json at this commit (mechanically: a `git stash`-free re-extract compares equal).
    - `node --test tests/n8n/mergeCompanyStaleTimestamp.test.mjs` reports exactly ONE failing assertion, and it is the POST-body no-stamp assertion (c). The verbatim failure output is captured for the SUMMARY.
    - The vacuity guard (a) PASSES: the compiled body produced a real merge whose `decisions` carries a `needs_review` for `lv_org_type` and whose `canonicalPatch` omits it.
    - Assertion (b) PASSES: the PRE body's emitted `lv_org_type_verified_at` differs from the stale timestamp on `existingRecord` — the refresh is demonstrated, not inferred.
    - `git diff --stat` for this commit touches ONLY tests/fixtures/merge_company_prefix_jscode.json and tests/n8n/mergeCompanyStaleTimestamp.test.mjs — zero production files.
  </acceptance_criteria>
  <reversibility rating="reversible">Two new test-only files; delete both to revert.</reversibility>
  <done>The bug exists as a running, reproducible, permanently-pinned differential against the SHIPPED compiled node body — red captured, with a vacuity guard proving the red is real and not an empty pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — relocate the cacheKeys write, retire the stale contacts NOTE, mirror the unit tests, rebuild</name>
  <files>n8n/code/mergeCompanies.js, n8n/code/mergeContacts.js, tests/n8n/mergeCompanies.test.mjs, n8n/wf_enrichment_cloud.json, n8n/wf_enrichment_local_live.json, n8n/wf_enrichment_local.json, n8n/wf_contact_ingest_cloud.json, n8n/wf_contact_ingest_local.json</files>
  <read_first>
    - n8n/code/mergeContacts.js:183-194 — the reference fix, line-for-line: the cacheKeys write sits INSIDE `if (decision === "promote") { canonicalPatch[field] = value; ... }`, carrying an explanatory comment. Note :189-190, the two-line NOTE naming mergeCompanies as the outstanding twin — now false.
    - n8n/code/mergeContacts.js:243-264 (foldContactResearch) — specifically the `if (decision.decision !== "promote") continue;` guard at :256 that already promote-gates the THIRD cache-key read at :260-263. Understand it before answering the companies fold question below.
    - n8n/code/mergeCompanies.js:177-225 — the per-field loop being edited.
    - tests/n8n/mergeContacts.test.mjs:126-144 — the two tests to mirror, including their naming style and the reason comment on why `stale_refreshable` + a non-blank current value yields needs_review.
    - tests/n8n/mergeCompanies.test.mjs:117-124 (the existing cache-key test — it uses a PROMOTED `lv_org_type: "other"`, an ungated value, so it stays green) and tests/n8n/parity.test.mjs:271-274 (also a promoted lv_org_type). Confirm by reading, not by assuming, that no existing assertion depends on a stamp for an UNPROMOTED field.
    - scripts/build_cloud_workflows.py:224 and :865 — mergeContacts.js is inlined at TWO sites, so the NOTE edit regenerates five workflow JSONs (both contact_ingest variants and all three enrichment variants). Expect and accept that diff; it is comment text only.
  </read_first>
  <behavior>
    - mergeCompanies({}, {lv_org_type: "other"}, undefined, {confidence: 95}) -> promotes -> cacheKeys.lv_org_type_verified_at IS set (unchanged).
    - mergeCompanies({}, {lv_org_type: <EVIDENCE_GATED value>}, undefined, {confidence: 90}) -> needs_review (no evidence URL) -> cacheKeys is EMPTY.
    - mergeCompanies({industry: "Sports"}, {industry: "Sports & Entertainment"}, ...) -> needs_review; industry has no cache-key mapping either way (unchanged).
    - mergeCompanies({}, {lv_produces_content: true}, undefined, {confidence: 95, evidence: {lv_produces_content: "https://..."}}) -> promotes -> cacheKeys.lv_produces_content_verified_at IS set.
    - provenance, decisions, canonicalPatch shapes are bit-for-bit unchanged in every case — only cacheKeys narrows.
  </behavior>
  <action>
    (1) THE FIX. In n8n/code/mergeCompanies.js, move the three-line `COMPANY_CACHE_KEY_FIELDS` block from
    its current position above the promote branch to INSIDE that branch, immediately after
    `canonicalPatch[field] = value;` — a structural mirror of the contacts twin, same shape, same ordering.
    Carry a short comment naming this phase and the gpt #6 origin, and state the consequence it prevents
    (an unpromoted field marking itself fresh and suppressing the next stale-refresh). Change NOTHING else
    in the file: no policy edit, no gate edit, no provenance/decisions edit, no helper extraction.

    (2) RETIRE THE STALE NOTE. In n8n/code/mergeContacts.js, the two-line NOTE inside the promote branch
    (:189-190) asserts the companies twin is still unfixed. Rewrite it to record that both paths now carry
    the fix and name this phase, or delete it. Do not otherwise touch the file.

    (3) ANSWER THE FOLD QUESTION BY INSPECTION, NOT ASSUMPTION. mergeContacts.js reads its cache-key map a
    THIRD time inside foldContactResearch. Determine whether the companies research-fold path — the SECOND
    `mergeCompanies()` call in the ENRICH_MERGE_CO wrapper (build_cloud_workflows.py:2206-2218) — has an
    equivalent surface needing its own fix, or inherits it for free. Read the wrapper's cacheKeys handling
    at :2216 and decide. If it inherits (i.e. the wrapper only spreads maps produced by the now-fixed
    mergeCompanies and never synthesizes a cache key itself), record that conclusion WITH the line
    reference in the SUMMARY and change nothing. If it synthesizes or re-derives a cache key anywhere,
    STOP and report before editing the builder — a builder edit is outside this task's file list.

    (4) MIRROR THE UNIT TESTS. Add two tests to tests/n8n/mergeCompanies.test.mjs mirroring
    mergeContacts.test.mjs:126-144: an unpromoted field emits no stamp, a promoted one still does. Use the
    companies-native withhold mechanism (the evidence gate on a gated `lv_org_type` value, read from
    `DEFAULT_COMPANY_POLICY.lv_org_type.require_evidence_url_for` rather than hand-typed — the TX-4
    discipline the existing tests at :89-105 already follow) and assert both `cacheKeys` and the
    corresponding `decisions` entry so the test states WHY the field was withheld.

    (5) REBUILD. Run `.venv/bin/python scripts/build_cloud_workflows.py`, then run it a second time and
    confirm byte-identical output. Commit the source fix, the mirrored tests, and ALL regenerated workflow
    JSON together.

    EXPECTED RED IN THIS COMMIT: tests/test_companies_factory_frozen.py now fails on `Merge Company` for
    BOTH the cloud and local_live variants. That is correct and intended — the frozen fixture is
    deliberately re-baselined in its own commit (Task 3), never folded into this one. Note the expected
    failure in the commit body so the intermediate red is visibly deliberate. Every OTHER test must be
    green at this commit; if anything else went red, stop and report rather than adjusting it.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/build_cloud_workflows.py && node --test tests/n8n/*.test.mjs && .venv/bin/python -m pytest -q --deselect tests/test_companies_factory_frozen.py</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/n8n/mergeCompanyStaleTimestamp.test.mjs` is now FULLY green: the same row that stamps on the pinned PRE body emits no stamp on the rebuilt POST body, and the evidenced control still stamps. Red-to-green is demonstrated across two commits with the row held constant.
    - `node --test tests/n8n/mergeCompanies.test.mjs` passes, including the two mirrored cases.
    - `grep -c "same latent issue" n8n/code/mergeContacts.js` returns 0 — the retired NOTE no longer points at a fixed file.
    - `git diff n8n/code/mergeCompanies.js` shows ONLY the relocation of the three-line cache-key block plus its comment: no policy, gate, provenance, decisions, or signature change, and no new exported helper.
    - The full offline suite is green EXCEPT tests/test_companies_factory_frozen.py, which fails on exactly `Merge Company` in both the cloud and local_live tests. Zero other regressions vs the 346 pytest / 228 node baseline.
    - A second builder run yields byte-identical wf_*.json (deterministic rebuild).
    - The companies-fold conclusion from step (3) is recorded with its line reference, and no builder file was edited.
  </acceptance_criteria>
  <reversibility rating="reversible">Two single-purpose JS edits plus regenerated artifacts; revert the two source files and rebuild.</reversibility>
  <done>mergeCompanies stamps its cache-key datetimes only on promote, the contacts NOTE no longer points at a fixed file, the compiled-body differential is green, and the only remaining red is the frozen fixture that Task 3 re-baselines deliberately.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded re-baseline of the frozen companies fixture — prove the blast radius, then commit it alone</name>
  <files>tests/fixtures/companies_jscode_frozen.json</files>
  <read_first>
    - tests/test_companies_factory_frozen.py:1-40 — the file header stating the fixture is re-baselined ONLY by an explicit reviewed act, and FROZEN_NODE_NAMES at :32-40 (the seven pinned nodes)
    - tests/test_companies_factory_frozen.py:55-106 — the four tests: the two in-test-build guards (cloud, local_live) and the two committed-JSON currency checks
    - .planning/phases/16.3-companies-stale-timestamp-fix/16.3-CONTEXT.md — Reasoned Decision 4 (diff first, assert the shape, then write) and the Scope Fence STOP condition on any other frozen node moving
  </read_first>
  <action>
    (1) PROVE THE DIFF IS BOUNDED, BEFORE WRITING ANYTHING. Write a throwaway comparison script into the
    session scratchpad directory — NOT into the repo — that loads tests/fixtures/companies_jscode_frozen.json
    and calls `build_enrichment_cloud()` and `build_enrichment_local_live()` (the same import path
    tests/test_companies_factory_frozen.py uses), then reports, for each of the 14 {variant, node} pairs,
    whether the freshly-built jsCode equals the fixture. Require the report to show:
      - exactly TWO differing pairs: {cloud, Merge Company} and {local_live, Merge Company};
      - the other TWELVE pairs byte-identical.
    If any other node differs, STOP and report — that is the unintended-blast-radius signal the scope fence
    names, not something to absorb.

    (2) PROVE THE `Merge Company` DIFF ITSELF IS THE EXPECTED ONE. Extend the same throwaway script: strip
    comment-only lines (lines whose trimmed form starts with `//`) from both the fixture and the fresh
    `Merge Company` strings, then diff the remainder line-wise. The surviving diff must consist solely of
    the three-line cache-key block removed at its old location and re-added inside the promote branch —
    no other executable line added, removed, or reordered. Capture this stripped diff verbatim; it is the
    reviewed evidence recorded in the SUMMARY.

    (3) RE-BASELINE. Only after (1) and (2) both hold, overwrite the `Merge Company` entries for BOTH
    variants in tests/fixtures/companies_jscode_frozen.json with the freshly-built strings, leaving the
    other twelve entries untouched. Verify the file's structure and the twelve untouched strings are
    unchanged (`git diff --stat` on the fixture should show a change confined to the two `Merge Company`
    values).

    (4) COMMIT ALONE. This commit touches tests/fixtures/companies_jscode_frozen.json and nothing else.
    Message must state WHY the fixture changed (the Phase 16.3 stale-timestamp fix relocated the cache-key
    write inside mergeCompanies' promote branch, and mergeCompanies.js is inlined verbatim into
    `Merge Company`) and WHAT the diff is bounded to (only `Merge Company`, both variants; the other six
    frozen nodes byte-identical; the stripped diff is the relocated three-line block only).

    (5) FINAL VERIFICATION. Run the full offline suite and record the exact counts against the 346 pytest /
    228 node baseline (new tests are net-positive; regressions must be zero). Run the builder twice more and
    confirm byte-identical output. Confirm no live call was made anywhere in the suite and no credential was
    read.

    (6) RECORD THE OUT-OF-FENCE FINDING. src/merge_policy.py's `source_metadata()` writes the same two
    company cache-key datetimes on the Python harness lane. State in the SUMMARY whether it exhibits the
    same unconditional shape, with a line reference, explicitly flagged as FOUND-NOT-FIXED and out of this
    phase's fence. Do not edit it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs && .venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && echo "DETERMINISTIC REBUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - The scratchpad comparison report shows exactly 2 of 14 {variant, node} pairs differing, both named `Merge Company`; the other 12 are byte-identical. The report is captured in the SUMMARY.
    - The comment-stripped `Merge Company` diff contains only the relocated three-line cache-key block — no other executable line changed. The verbatim stripped diff is captured in the SUMMARY.
    - `git show --stat HEAD` for the re-baseline commit lists exactly one file: tests/fixtures/companies_jscode_frozen.json. Its message states the why and the bound.
    - `.venv/bin/python -m pytest -q` is fully green including tests/test_companies_factory_frozen.py (all four tests), and `node --test tests/n8n/*.test.mjs` is fully green.
    - Recorded counts show >= 346 pytest and >= 228 node passing with zero regressions; the deltas are attributable to the tests added in Tasks 1 and 2.
    - A repeat builder run leaves `git status` clean under n8n/ (deterministic rebuild).
    - No file was written into the repo by the throwaway comparison script.
  </acceptance_criteria>
  <reversibility rating="costly">Re-baselining a byte-identity fixture intentionally discards the old pinned strings; recoverable from git history, but the guard's protection is only as good as the review of this diff — which is why the bounded proof precedes the write.</reversibility>
  <done>The frozen companies fixture is re-baselined as a proven-bounded, separately-committed, reviewed act; the full offline suite is green with zero regressions and a deterministic rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n/code/mergeCompanies.js -> `Merge Company` Code node | `inline()` concatenates the FULL module text into the emitted jsCode; any edit silently changes the shipped companies runtime in both the cloud and local_live workflows |
| n8n/code/mergeContacts.js -> five workflow JSONs | the same inlining, at two builder sites — a comment-only edit still regenerates both contact_ingest variants and all three enrichment variants |
| frozen fixture -> byte-identity guard | the fixture IS the guard's ground truth; overwriting it without proof converts a regression detector into a rubber stamp |
| committed jsCode -> `new Function` in the test harness | the test executes the repo's own committed workflow jsCode, as researchChainRowFlow.test.mjs already does; no external or untrusted input is interpolated |
| merged cacheKeys -> HubSpot `lv_*_verified_at` -> stale-refresh predicates | a wrongly-refreshed datetime is not a visible error — it silently and permanently removes a record from the §19.5 refresh population |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.3-01 | Tampering (silent data staleness) | the bug itself: an unpromoted companies field marks itself fresh, permanently suppressing CLAUDE.md §19.5's monthly stale-refresh scan | high | mitigate | Task 2 relocates the write inside the promote branch; Task 1 + Task 2 prove the behavior change through the COMPILED node body with a pinned pre-fix differential, so the fix is demonstrated on the shipped artifact rather than only on the pure function |
| T-16.3-02 | Tampering (guard erosion) | the frozen byte-identity fixture is re-baselined by blind overwrite, absorbing an unrelated change into the baseline and disarming the guard for every future phase | high | mitigate | Task 3 requires a 14-pair comparison proving only `Merge Company` moved, plus a comment-stripped line diff proving the change is exactly the relocated block, BEFORE the write; any other node moving is a hard STOP; the re-baseline is an isolated commit whose message states why and what it is bounded to |
| T-16.3-03 | Tampering (vacuous test) | the end-to-end assertion passes because the candidate never reached the merge, not because the fix works — a green test that proves nothing | medium | mitigate | Task 1's vacuity guard asserts, before any cache-key assertion, that the compiled body produced a real merge carrying a `needs_review` decision for `lv_org_type` with the field absent from canonicalPatch |
| T-16.3-04 | Tampering (over-fix) | the stamp is suppressed for genuinely promoted fields too, silently making every company look permanently stale and inflating research spend | medium | mitigate | Task 1's evidenced control case and Task 2's mirrored promoted-field unit test both assert the stamp still fires on promote, through the compiled body and the pure function respectively |
| T-16.3-05 | Repudiation (evidence decay) | "red before green" recorded only as prose in a SUMMARY cannot be re-verified later | low | mitigate | The pre-fix compiled body is pinned as a permanent fixture and the red half runs forever as a differential assertion, not a stashed console log |
| T-16.3-SC | Tampering (supply chain) | npm/pip installs | low | accept | No package is installed or added — pure JS edits plus node:test / pytest already in the repo. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Compiled-body differential: the identical stale-unpromoted row stamps on the pinned PRE body and does not on the shipped POST body; the evidenced control still stamps; the vacuity guard proves the merge actually ran.
- Pure-function mirror: tests/n8n/mergeCompanies.test.mjs carries the companies twins of mergeContacts.test.mjs:126-144.
- Fold path: the companies research-fold conclusion is reached by reading build_cloud_workflows.py:2216 and recorded with a line reference — inherits or needs its own fix, decided by inspection, never assumed.
- Bounded re-baseline: 2 of 14 {variant, node} pairs differ, both `Merge Company`; the comment-stripped diff is the relocated three-line block only; the fixture change ships alone.
- Stale NOTE retired: `grep -c "same latent issue" n8n/code/mergeContacts.js` == 0.
- Determinism + suite: two consecutive builder runs leave n8n/ clean; full offline suite green with zero regressions vs 346 pytest / 228 node.
- Fence held: no shared-helper extraction, no gating/policy/ICP/provider change, no builder edit, no src/merge_policy.py edit, no live call, no credential read, no deploy.
</verification>

<success_criteria>
The phase is complete when mergeCompanies.js stamps `cacheKeys[COMPANY_CACHE_KEY_FIELDS[field]]` only inside
its `decision === "promote"` branch (SC-1), mirrored unit tests pin both halves of that behavior (SC-2), the
bug and its absence are demonstrated end-to-end through the COMPILED `Merge Company` node body with durable
red-before-green evidence and a vacuity guard (SC-3), tests/fixtures/companies_jscode_frozen.json is
re-baselined in its own commit with the diff proven confined to `Merge Company` across both builders (SC-4),
the workflow JSON is regenerated and the builder proven deterministic (SC-5), and the full offline suite is
green with zero regressions and no live calls (SC-6).
</success_criteria>

<source_coverage_audit>
| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP 16.3 goal — cache-key datetimes stamped ONLY on promote, mirroring the contacts fix | Task 2 (1) |
| REQ | ROADMAP 16.3 SC-1 (structural mirror) | Task 2 (1) |
| REQ | SC-2 (mirrored unit tests) | Task 2 (4) |
| REQ | SC-3 (end-to-end functional through the compiled body; red-before-green as evidence) | Task 1 (all), Task 2 acceptance |
| REQ | SC-4 (bounded, reviewed, isolated fixture re-baseline) | Task 3 (1)(2)(3)(4) |
| REQ | SC-5 (regenerate wf JSON; deterministic builder) | Task 2 (5), Task 3 (5) |
| REQ | SC-6 (full suite green, zero regressions, no live calls) | Task 3 (5) |
| RESEARCH | — deliberately skipped for this phase (bug fix with an exact in-repo reference implementation); no RESEARCH.md exists | n/a |
| CONTEXT | D-01 fix by structural mirror, no shared-helper extraction | Task 2 (1) + prohibitions |
| CONTEXT | D-02 red-before-green is required evidence | Task 1 (2), pinned fixture |
| CONTEXT | D-03 end-to-end means through the compiled node body | Task 1 (2), Task 2 acceptance |
| CONTEXT | D-04 fixture re-baseline is its own reviewed commit, diff-proven first | Task 3 |
| CONTEXT | D-05 both builders in scope | Task 3 (1) covers cloud + local_live |
| CONTEXT | D-06 rebuild mandatory and deterministic | Task 2 (5), Task 3 (5) |
| USER | stale NOTE at mergeContacts.js:189-190 must be updated/removed | Task 2 (2) + grep gate |
| USER | companies research-fold path: equivalent surface or inherits free — verify, do not assume | Task 2 (3) |
| USER | environment gotchas (`.venv/bin/python -m pytest`, `node --test tests/n8n/*.test.mjs` glob form) | every `<verify>` block |
</source_coverage_audit>

<output>
Create `.planning/phases/16.3-companies-stale-timestamp-fix/16.3-01-SUMMARY.md` when done. It MUST carry
verbatim: the Task 1 red `node --test` output, the Task 3 14-pair comparison report, the comment-stripped
`Merge Company` diff, the final suite counts vs 346/228, the companies-fold conclusion with its line
reference, and the src/merge_policy.py found-not-fixed finding.
</output>
