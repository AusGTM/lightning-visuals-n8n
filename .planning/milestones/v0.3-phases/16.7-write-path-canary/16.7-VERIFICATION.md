---
phase: 16.7-write-path-canary
verified: 2026-07-29T00:00:00Z
status: passed
score: 7/7 ROADMAP criteria verified (plus BUG 10 closure, BUG 11 fix, contact-search non-regression)
behavior_unverified: 0
overrides_applied: 0
re_verification: No — initial verification
---

# Phase 16.7: Write-Path Canary Verification Report

**Phase Goal:** The first HubSpot write in the project's history, bounded to one allowlisted test
record, proving LIVE that the non-clobber merge writes what policy permits and protects what it
does not.

**Verified:** 2026-07-29
**Status:** passed
**Re-verification:** No — initial verification

**Verification method:** Static/offline only — no deploy, no webhook fire, no HubSpot write was
made during this verification, per the phase's own scope fence. Live claims (criteria 1, 3, 4, 6,
7, and BUG 10 closure) were checked for internal evidentiary rigor (do they quote a literal
`runData`/record value, not a paraphrase) and cross-checked for consistency across three
independently-written documents (16.7-02-SUMMARY.md, 16.8-01-SUMMARY.md, STATE.md's `stopped_at`
narrative), which agree on every quoted value. Code and offline-test claims were independently
re-derived from the repository.

## Important scoping note

16.7-02-PLAN.md's live window discovered BUG 12 (`Set Data Quality + Gap Flag` destroyed the row,
making the contacts write path structurally incapable of writing to any record). 16.7-02-SUMMARY.md's
body, written before the fix, correctly records criteria 1/3/4/7 as NOT MET / PARTIAL / N/A at that
point. The phase's own ADDENDUM (dated the same day, after Phase 16.8 fixed BUG 12 and the **same
armed window continued** to execution 15) supersedes every one of those findings and points to
`16.8-01-SUMMARY.md` for the full evidence. This verification treats the ADDENDUM as authoritative
for the final criteria outcomes — that is what the phase itself instructs a reader to do, and the
frontmatter `outcome:` field of 16.7-02-SUMMARY.md is written to match ("...fixed in 16.8, then
PROVEN live in the same armed window"). The document structure (pre-addendum table showing failures,
addendum overriding them) is unusual but not misleading: it is dated, explicitly labelled, and
consistent with the frontmatter summary — verified, not a gap.

## Per-Criterion Verdicts (ROADMAP 16.7, criteria 1-7)

| # | Criterion | Verdict | Evidence (quoted) |
|---|-----------|---------|--------------------|
| 1 | Exactly one record modified (201 advances, 9604614548 byte-identical) | ✓ VERIFIED | Execution 15: contact 201 `lastmodifieddate` `2026-07-18T01:14:03.751Z` → `2026-07-28T23:56:34.177Z`. Company `9604614548` `hs_lastmodifieddate` `2026-07-28T03:42:15.843Z` — **byte-identical** throughout the whole session (16.8-01-SUMMARY.md, also independently stated in 16.7-02-SUMMARY.md's execution-12 quote). |
| 2 | `ALLOW_HUBSPOT_CREATE` stays `"false"`; no create node executes; no new record | ✓ VERIFIED | Post-restore read-back table (16.7-02-SUMMARY.md): `` `ALLOW_HUBSPOT_CREATE` `"false"` ``. 16.8-01-SUMMARY.md: "No create node ever ran; `ALLOW_HUBSPOT_CREATE` stayed `"false"`." `HubSpot Create`, `HubSpot Company Create`, `HubSpot Company Update` absent from execution 15's `runData`. |
| 3 | A policy-protected field is provably NOT overwritten across an actual write, quoted before/after | ✓ VERIFIED | Same PATCH that wrote `seniority`: `mobilephone` **`+61 439 135 604` → `+61 439 135 604` (unchanged)** while the candidate `+61 413 232 200` at confidence 85 **cleared** the `fill_blank_only` 85 threshold and was refused purely by class ("Current value exists and field is fill_blank_only"). `jobtitle` **`"Carmody, our developer. His email copied above"` unchanged** against candidate `"Product Manager"`. `email` refused, "Best confidence 85 below threshold 95." (16.8-01-SUMMARY.md, corroborated verbatim in `STATE.md`.) |
| 4 | A policy-permitted field written with full metadata, quoted post-run | ✓ VERIFIED | `seniority` `null` → **`"Non-Manager"`**. Provenance entry quoted verbatim: `{"confidence": 85, "source": "waterfall", "validation_status": "provider_only", "value": "Non-Manager", "verified_at": "2026-07-28T23:56:33.743Z"}`. Restatement of the stale ROADMAP wording (`_verified_by_model` does not exist in the Phase-15 provenance model) is **explicit** in three places: ROADMAP.md's "Criterion 4 restated" note, 16.7-02-PLAN.md's scope_note, and 16.7-02-SUMMARY.md's closing line — not a silent reinterpretation. |
| 5 | Judged node-by-node from `runData`, never HTTP status | ✓ VERIFIED | Every quoted claim above cites a `runData` field or a fresh HubSpot record read. 16.7-02-SUMMARY.md explicitly notes the prior run's "plausible skip-shaped 200" trap and states the discipline was followed. |
| 6 | Deployment restored to disabled build, verified by read-back | ✓ VERIFIED | First restore (16.7-02-SUMMARY.md, quoted table): `` `ALLOW_HUBSPOT_RECORD_WRITES "false"`, `ALLOW_HUBSPOT_CREATE "false"`, `TEST_RECORD_IDS ""`, `TEST_RECORD_DOMAINS ""` ``, both research flags `false`, `active` still `true`. Second restore after the 16.8 continuation (16.8-01-SUMMARY.md): "all four write-safety constants and both research flags at their disabled literals, `active` still true" — same claim, less granularly quoted than the first restore but corroborated independently by `STATE.md`: "All three workflows read back DISARMED, only LV Enrichment active." |
| 7 | Every change to 201 reversible, rollback EXERCISED and verified by re-read | ✓ VERIFIED | `PATCH {"properties": {"seniority": "", "lv_contact_enrichment_provenance": ""}}` → HTTP 200; re-read confirms every other property byte-identical to the Task-1 snapshot. Honest caveat recorded (not omitted): HubSpot returns `""` for a cleared property vs `null` for never-set, and `lastmodifieddate` is HubSpot-managed and cannot be reverted — both stated plainly, not glossed over. `scripts/rollback_canary_proof.py` was checked and correctly rejected as unfit (archives a property *definition*, not record values). |

**Score:** 7/7 ROADMAP criteria verified.

## Additional Checkable-in-Repo Items

| Item | Verdict | Evidence |
|---|---|---|
| BUG 11 fix: `HubSpot Update`/`HubSpot Company Update` are credential-bound httpRequest PATCH carrying `{properties: $json.properties}`, `onError` absent | ✓ VERIFIED | Inspected `git show 6728237:n8n/wf_enrichment_cloud.json` (the built artifact at the point 16.7/16.8's live proof ran) directly: both nodes `type: n8n-nodes-base.httpRequest`, `method: PATCH`, `url: =https://api.hubapi.com/crm/v3/objects/{contacts,companies}/{{ $json.hs_object_id }}`, `jsonBody: ={{ JSON.stringify({ properties: $json.properties }) }}`, no `onError` key on either node. Matches the SUMMARY's "Predicted Live PATCH" shape exactly (method, URL pattern, single `properties` key). |
| Offline oracle + structural guard exist and are meaningful | ✓ VERIFIED | Read `tests/n8n/writePatchBodyFlow.test.mjs` (210 lines) and `tests/test_write_node_transport.py` (159 lines) in full — both drive real merge-shaped rows through the BUILT node bodies/expressions rather than asserting against hand-typed literals; neither is a stub or tautology. Ran both: `node --test tests/n8n/*.test.mjs` → **278/278 pass**; `.venv/bin/python -m pytest -q tests/test_write_node_transport.py` → included in the 542-pass run below. |
| Contact search nodes NOT regressed by the write-node transport swap | ✓ VERIFIED | Same `git show` inspection: `HubSpot Search` and `HubSpot Fetch By Id` remain `n8n-nodes-base.hubspot`, `resource: contact`, `operation: search`, `onError: continueRegularOutput` — unchanged from BUG 10's fix, untouched by BUG 11's write-node fix. |
| Full offline suite, actual counts | ✓ VERIFIED | Ran independently: `.venv/bin/python -m pytest -q` → **542 passed**. `node --test tests/n8n/*.test.mjs` (glob form) → **278 passed, 0 failed**. (Current HEAD is past 16.7/16.8, at 16.10; counts match STATE.md's final tally "Suite 504 -> 542 pytest / 278 node," confirming no regression since.) |
| BUG 10 closure: `Merge Company` NON-NULL; containment proven by an un-allowlisted company refused while writes were armed | ✓ VERIFIED (live-quote evidence, cross-corroborated) | Execution 12, quoted: `Merge Company` `merge` **non-null**, keys `['canonicalPatch','provenance','cacheKeys','decisions']`; `Decide Company Action` → `action: "write_blocked"`; `HubSpot Company Create/Update`, `HubSpot Create/Update` all **absent** from `runData` while `ALLOW_HUBSPOT_RECORD_WRITES` read back `"true"` moments earlier. Same values independently restated in `STATE.md`'s `stopped_at` narrative, written at a different time by a different session step — internally consistent, not a single unverifiable assertion. |

## Requirements Coverage

SC-3 and SC-4 (16.7-01 enabler requirements) and 16.7's ROADMAP criteria 1-7 all map to verified
evidence above. SC-16.6-5 (company `create`/`update` live-defect status) is explicitly left open by
16.7-01 (creates untouched, pinned unverified) — this is a stated, in-scope deferral, not a gap; it
was picked up later in Phase 16.9 (outside this phase's scope).

## Anti-Patterns Found

None in the files this phase touched (`scripts/build_cloud_workflows.py`, `n8n/wf_enrichment_cloud.json`,
`tests/n8n/writePatchBodyFlow.test.mjs`, `tests/test_write_node_transport.py`). No `TBD`/`FIXME`/`XXX`
markers, no placeholder returns, no swallowed-error patterns on the write nodes (the absence of
`onError` is the deliberate, tested, documented design — not an omission).

## Human Verification Required

None. Every criterion resolved to VERIFIED against either directly-inspectable repository code (the
node transport, the tests, the offline suite) or cross-corroborated quoted live values across
independently-authored documents (16.7-02-SUMMARY.md, 16.8-01-SUMMARY.md, STATE.md). No item was
left to presence-only inference.

## Gaps Summary

None blocking. One minor observation, not rising to a gap: the second (post-16.8) deployment
restore in 16.8-01-SUMMARY.md states the disarmed literals qualitatively ("at their disabled
literals") rather than quoting each string as the first restore in 16.7-02-SUMMARY.md did. This is
corroborated independently by STATE.md's stopped_at narrative ("All three workflows read back
DISARMED, only LV Enrichment active") and does not affect the criterion-6 verdict, but a stricter
phase would have re-quoted the literal table a second time.

---

_Verified: 2026-07-29_
_Verifier: Claude (gsd-verifier)_
