---
status: complete
phase: 16.9-create-path-fix-and-company-writes
source: [16.9-VERIFICATION.md SC-2, SC-3, SC-4]
started: 2026-07-29T17:20:00+10:00
updated: 2026-07-29T17:20:00+10:00
---

## Current Test

[testing complete]

## Tests

### 1. SC-2 — a guard asserts every property the Cloud workflows write/filter on exists in the portal schema
expected: |
  BUG 14 (a property referenced by workflow logic but absent from the live portal) must be
  catchable offline instead of live with PROPERTY_DOESNT_EXIST.
result: pass
source: automated
evidence: |
  tests/test_hubspot_schema_coverage.py, committed 2026-07-29 (239b8af). Oracle is the
  Phase 15 portal snapshot UNION properties recorded as created by a migration undo
  manifest — deliberately NOT config/hubspot_properties.yaml, since BUG 14's property was
  declared there the whole time and still did not exist. All three cloud workflows clean.

  Non-vacuity proven three ways rather than asserted: an injected Code node naming an
  invented property is caught and attributed to its node; the sticky-note exclusion is
  shown to exclude; and BUG 14's own oracle is reconstructed by dropping manifest
  73c5342c, which must make `lv_enrichment_status` go missing — so the guard is
  demonstrably the one that would have caught it.

  Two findings surfaced while building it, both resolved rather than suppressed:
  lv_enrichment_status/requested are snapshot-stale but manifest-present (which is why
  manifests are in the oracle at all), and lv_icp_scored_at appeared only inside a sticky
  note whose prose states SJ-1/2/3 never reference it.

### 2. SC-3 — a companies run reaches HubSpot Company Update with HTTP 2xx and the patch lands
expected: |
  Claimed MET by the SUMMARY via execution 17; the verifier marked it UNCERTAIN because no
  independently-checkable artifact is committed and it had no credentials to spot-check.
result: blocked
blocked_by: deploy-permission
reason: |
  Cannot be independently re-confirmed now: execution 17's throwaway company was deleted
  afterwards (by design), so there is no surviving record to re-read, and re-running it
  needs an armed deploy.

  What IS independently checkable today, and checks out: the node body is a
  credential-bound httpRequest PATCH to
  /crm/v3/objects/companies/{{ $json.hs_object_id }} with
  {{ JSON.stringify({properties: $json.properties}) }}, and both properties the run wrote
  (`lv_enrichment_status`, `lv_enrichment_requested`) are confirmed present in the portal
  via manifest 73c5342c. So the claim is consistent with the artifact and with the portal
  schema — which is corroboration, not confirmation. Left blocked rather than passed.

### 3. SC-4 — company:create runs once against a domain with no existing record, and what it creates is deleted
expected: |
  The phase goal names both create and update. create reached neither "writes correctly"
  nor "blocker on the record" — it was simply never exercised, with no blocker documented.
result: pass
source: automated
evidence: |
  RESOLVED AND EXECUTED IN FULL, 2026-07-29 (final update). After BUG 19's fix, the
  operator ran the armed deploy (ENABLE_BAKED_FLAGS with the throwaway-domain allowlist)
  and the complete SC-4 cycle executed:

    execution 34 (webhook): Company Gate action=create -> Decide Company Action create
    with seeded properties -> IF Company Create routed -> **HubSpot Company Create
    EXECUTED, error null, HTTP 201, id 278611609073** with domain and name from the seed.
    HubSpot fresh read: name='SC4 Armed Canary', domain='lv-sc4-n8n-canary-delete-me
    .example', lv_enrichment_status='complete'. DELETE 204, re-read 404. Disarmed build
    restored immediately and read back (RECORD_WRITES "false", empty allowlist, all three
    workflows still active); no other execution occurred during the armed window.

  SC-4's letter — "company:create runs once against a domain with no existing record, and
  whatever it creates is deleted afterwards" — is met, through the n8n node itself.

  THE HISTORY THAT MADE IT SAFE, kept because SC-4's real yield was BUG 19: |
  The HubSpot half was exercised on 2026-07-29 against a throwaway (created, verified,
  deleted, re-read 404 — the create/verify/delete the goal authorized). The endpoint
  contract works: HTTP 201. What it exposed is BUG 19.

  The create payload carries NO `domain` and NO `name`:

    POST /crm/v3/objects/companies  <node's exact payload>   -> 201, id=278591842756
    GET  .../278591842756          -> name=None, domain=None, lv_org_type set
    POST /companies/search  domain EQ <throwaway>            -> 200, total=0
    DELETE -> 204     GET -> 404

  That search is the node whose zero-result answer decided `action: "create"`. A record
  created without a domain is invisible to it, so the next run creates another orphan, and
  the next. Unbounded duplicates — at SJ-1's current 712 matches, hourly.

  Root cause: the create path reuses the UPDATE path's non-clobber policy. `domain` and
  `email` are manual_protected — correct for an update, incoherent for a create. All five
  decide-node payload builders share it. See
  .planning/debug/bug-19-create-omits-identity-fields.md.

  STILL BLOCKED for the n8n half: running create THROUGH the node needs an armed deploy.
  But SC-4's purpose — find out whether create works — is answered: it does not, and the
  reason is now precise rather than unknown. Fixing BUG 19 must come before the n8n run,
  or that run will create an orphan in the production portal.

## Summary

total: 3
passed: 2
issues: 0
pending: 0
blocked: 1
skipped: 0

## Gaps

```yaml
- gap_id: G-16.9-3
  truth: "company:create writes a record that can be found again by the search that gates creation"
  status: resolved
  resolved_by: "BUG 19 fix, 2026-07-29 (same session) — identity seeded from each lane's own row on the create branch of all five decide-node payload builders; live-proven with a seeded throwaway (201 -> domain search total=1 id-match after ~6s index lag -> deleted, 404)"
  resolved_at: 2026-07-29
  reason: "BUG 19 — the create payload omitted domain/name (companies) and email (contacts), because it reused the update path's manual_protected non-clobber policy. Confirmed live: created record had name=None/domain=None and a domain EQ search returned total=0, so every later run re-creates it."
  severity: blocker
  test: 3
  artifacts:
    - .planning/debug/bug-19-create-omits-identity-fields.md
    - tests/test_create_payload_identity.py
    - tests/n8n/createIdentitySeed.test.mjs
  missing: []
```

## Notes

SC-2 closed outright. SC-4 closed in full (armed n8n run, execution 34). SC-3 remains the
one blocked row — execution 17's evidence is unrecoverable by design (throwaway deleted)
and re-running update armed was outside the goal's canary budget; its claim is corroborated
by artifact + portal schema + the fact that the create node, on the byte-identical
transport, just live-201'd. Verification flips to passed on the verifier's own framing:
SC-2 and SC-4 were the two failed criteria that set gaps_found.

SC-4's arc, completed same-day: exercised the HubSpot half -> found BUG 19 (create writes
an identity-less record the gating search can never find; unbounded duplicates) -> fixed
all five decide lanes (identity seeded on the create branch only; each lane's row shape
read first, per the BUG 16 lesson) -> live-proved the fixed payload closes the loop
(201 -> domain search finds it, id-match, after ~6s index lag -> deleted, 404).

Along the way the ORIGINAL canary's search step was found confounded — HubSpot search is
eventually consistent, so an immediate total=0 proves nothing alone; the polling re-run is
the clean proof. Also noted, not fixed: that ~seconds index window means two runs for one
domain inside it could still double-create; inherent to search-gated creation, dedupe sweep
is the backstop.

(SC-4's letter has since been met in full — see test 3's final update.)

FINAL DISARMED INCREMENT (execution 32, 2026-07-29): the COMPLETE companies create path
ran through n8n for the throwaway domain — envelope search no-match, Adapt
lookup_failed=false, **Company Gate action=create** (the create decision's first-ever live
firing), Merge non-null, and Decide Company Action emitting `write_blocked` with
`properties: {domain: "lv-sc4-n8n-canary-delete-me.example", name: "SC4 n8n Canary",
lv_enrichment_status: "complete"}` — the BUG 19-seeded payload, visible in live runData,
byte-equal in shape to the object the direct-API canary already POSTed and proved
findable. IF Company Create emitted 0 items; the write node stayed unreached (disarmed,
correct). The one remaining untested link is the armed node performing its POST — the
same credential-bound httpRequest transport as HubSpot Company Update, which executed
live with HTTP 2xx in execution 17.
