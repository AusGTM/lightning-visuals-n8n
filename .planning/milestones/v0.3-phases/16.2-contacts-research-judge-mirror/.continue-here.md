---
context: phase
phase: 16.2-contacts-research-judge-mirror
plan: 2
task: 2
total_tasks: 2
status: complete
last_updated: 2026-07-24T11:22:50Z
---

# Milestone 3 (v0.3) — CODE-COMPLETE. Paused clean.

Phases **16, 16.1, 16.2 all Complete + gsd-verifier PASS**. Everything committed + **pushed** to `origin/feat/company-enrichment-icp-research` (HEAD `7debbfa`). No PR opened. What remains is the **live Track B operator runbook** + follow-ups — all non-gating.

<current_state>
Suite green: **346 pytest / 228 node**, deterministic builder, companies byte-identity guard + both research-chain row-flow regression tests green. Cost-safe for live: providers gated per-request (`providers` payload, blank/none = zero calls), scheduled-maintenance ships `active:false`.
</current_state>

<completed_this_session>
- **16.1** (`a12e439`→`cbd593a`, verify `db1f1d7`): per-request `providers` selection (IF-gate bypass chain, disabled provider's HTTP node never fires), `remaining_credits` in the webhook response (single-item credit branch → `Respond to Webhook` responseNode), `scripts/check_provider_credits.py`, schedules `active:false`. 8/8.
- **bd682a2** (fix): the 3-model cross-AI review of 16.2 caught a **latent row-loss bug in the SHIPPED companies research/judge chain** — HTTP nodes replace `$json`, so `Validate Research Output`/`Apply Judge Verdict` lost `existingRecord`/`scored` → `Merge Company` returned `merge:null` when web research ran. Confirmed via an item-flow simulation over the emitted jsCode; fixed via node-name row recovery; guarded by `tests/n8n/researchChainRowFlow.test.mjs`.
- **16.2** (`c944496`→`3f3aa16`, verify `56011d7`): contacts research→judge mirror via parameterized `EnrichTarget` factories (companies byte-identical), `contactResearch.js`/`contactJudge.js`, `mergeContacts.foldContactResearch` (write-safety), `chosen_field` allowlist, row-recovery mirror + `contactResearchChainRowFlow.test.mjs`. 7/7.
- **docs** (`7debbfa`, authored as **dr-robert-li**): staleness sweep + enrichment mermaid regenerated to the as-built 88-node graph.
</completed_this_session>

<remaining_work>
Track B (live, non-gating): 1) provision creds + deploy workflows (`DRY_RUN=false ALLOW_N8N_DEPLOY=true`) + activate; 2) **fetch-by-objectId** node — genuine HubSpot private-app events carry only `objectId`/`objectType`; both chains are verified against caller-envelope/direct-field payloads only; 3) **first live run must verify the bd682a2 fix + the full conflict→judge lane** (NEITHER research/judge chain has run live yet). Follow-up: companies-side stale-timestamp fix (done on contacts only). Optional: open a PR, or `/gsd-complete-milestone`.
</remaining_work>

<blockers>None.</blockers>

## Anti-patterns (advisory)
- ENV: `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` (dir form broken on node 24).
- Editing `n8n/code/*.js` or `scripts/build_cloud_workflows.py` regenerates `n8n/wf_*.json` (deterministic) — rebuild + commit the JSON. Companies emitted output is byte-identity-guarded (`tests/test_companies_factory_frozen.py`); contact logic lives in sibling modules co-inlined only into contact nodes.
- **HTTP-hop row loss:** any n8n Code node after an HTTP node must recover the row by node-name (`$('PreHttpNode').all()[i]`), never `{...it.json}` — see memory `companies-research-lane-rowloss`.
- `.env.example` has a pre-existing uncommitted change (adds `N8N_ENRICHMENT_WEBHOOK_SECRET`) — currency-improving, held out of commits per prior handoffs.

<next_action>
Open a PR for `feat/company-enrichment-icp-research`, OR run the Track B live operator runbook (`16-UAT.md`) — cost-safe now, but VERIFY the bd682a2 row-loss fix + conflict lane on the first live run.
</next_action>
