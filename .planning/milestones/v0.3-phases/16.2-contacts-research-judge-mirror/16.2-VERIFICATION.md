---
phase: 16.2-contacts-research-judge-mirror
verified: 2026-07-24T00:00:00Z
status: passed
score: 7/7 must-haves verified
verdict: ACHIEVED
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
follow_ups:
  - "Row-flow integration harness (contactResearchChainRowFlow / researchChainRowFlow) runs every chain node UNCONDITIONALLY and does not evaluate the IF branches; its seed row is a deliberate gap (not a conflict) to avoid the cap/demote path. No single test drives the full conflict→escalation→judge→cap-demote lane through the COMPILED node bodies in item-flow form (that path is covered only by pure-function unit tests in contactJudge.test.mjs). Honestly disclosed in 16.2-02-SUMMARY 'Issues Encountered'. Minor coverage gap, not a blocker."
  - "Track-B (recorded in 16.2-02-SUMMARY, NOT yet surfaced into ROADMAP/STATE): (a) mergeCompanies.js carries the identical stale-timestamp latent bug, deliberately NOT fixed here to protect the companies byte-identity guard; (b) genuine HubSpot private-app property-change events carry only objectId/objectType — the contact chain is verified against synthetic/direct-field payloads only, so a fetch-by-objectId node is required before live private-app events can drive it. Real live-readiness caveat, explicitly scoped out (shared 16.1 Track-B deferral), consistent with the OFF-by-default posture."
---

# Phase 16.2: Contacts Research + Judge Mirror — Verification Report

**Phase Goal:** The contacts enrichment path mirrors the companies path — the same web-research → judge → verdict chain runs on contacts, reusing Phase 16.1's node factories, so both object types share parameterized components (different targets, one set of building blocks).
**Verified:** 2026-07-24
**Status:** passed — **VERDICT: ACHIEVED**
**Re-verification:** No — initial verification

## Goal Achievement

The phase goal is achieved. The contacts branch runs the full ten-node
research→judge→verdict chain, emitted by the SAME target-parameterized Python
factories the companies branch uses (companies output proven byte-identical),
with the bd682a2 row-recovery mirrored across both HTTP hops, a
security-hardened `chosen_field` allowlist companies doesn't need, and a
write-safety fold that honestly implements the reviews-redefined SC-3 (gap-fill
+ stale-refresh + existing-record conflict — NOT provider-vs-research
adjudication). All 7 review-hardened must-haves verified against delivered code;
suites green (346 pytest / 228 node, 0 failures); rebuild deterministic.

### Observable Truths (review-hardened SCs 1–7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Contact chain wired at the 16.1 seam; factories shared (parameterized by target), not duplicated | ✓ VERIFIED | Emitted `wf_enrichment_cloud.json`: `Normalize + Score → Contact Research Trigger Gate → IF → Build → Contact Web Research → Validate → Contact Judge Gate → IF → Build → Contact Judge Call → Apply Contact Judge Verdict → Merge Winners`; both IF-false lanes fan into `Merge Winners`. Emitted by `target=CONTACTS_TARGET` through the same six factories; byte-identity guard (SC-2) proves companies factory output unchanged. local-live variant has all 10 nodes too. |
| 2 | Byte-identity: companies emitted JSON unchanged; 4 shared modules git-unchanged | ✓ VERIFIED | `git diff 7ab7543(pre-16.2) HEAD -- judge.js webResearch.js scoreEnrichment.js mergeCompanies.js` = empty. `tests/test_companies_factory_frozen.py` 4 passed (builds live in-test, not a committed-JSON compare). Deterministic rebuild: `build_cloud_workflows.py` ×2 → `git diff --stat n8n/wf_*.json` empty. |
| 3 | Row-recovery mirror: Validate/Apply recover the row by paired index across both HTTP hops; contact data reaches Merge Winners (not merge:null) | ✓ VERIFIED | Emitted `Validate Contact Research` recovers `$("Build Contact Research Request").all()` by index `preHttp[i].json`; `Apply Contact Judge Verdict` recovers from `Build Contact Judge Request`. `contactResearchChainRowFlow.test.mjs` executes the ACTUAL emitted jsCode via `new Function` across both mocked HTTP hops and asserts `Validate` recovered `scored`+`existingRecord`, `Apply` recovered `existingRecord`, and `final.merge !== null` + real merge object. Green. |
| 4 | chosen_field security: allowlist {jobtitle,seniority} + decision/confidence gate; injectable markers cleared at entry; chosen_field:"email" verdict never writes email | ✓ VERIFIED | `applyContactJudgeVerdict`: `promotes` requires `decision∈{promote,confirm}` AND `confidence≥JUDGE_MIN_CONFIDENCE` AND `_CONTACT_JUDGE_FIELDS.indexOf(v.chosen_field)!==-1` — else routes to `applyContactUnadjudicated`. Entry node `_stripInjectableMarkers` destructures out research_candidate/judge_verdict/judge_flags/judge_confidence_by_field/judge_promoted_fields. Test line 190 `SECURITY: chosen_field:'email'...confidence:95` asserts `!("email" in result.data)` and promoted_field !== "email"; row-flow test also proves no forged email reaches canonicalPatch. |
| 5 | Overlap precedence write-safety: research wins jobtitle/seniority only when judge-promoted OR genuine gap (provider absent AND existing blank); unadjudicated value never overwrites non-blank provider/existing | ✓ VERIFIED | `foldContactResearch`: `genuineGap = !providerHasField && _isBlank(existing[field])`; `researchWins = judgePromoted || genuineGap`. Uses fresh `judge_flags.promoted_field` (Merge Winners: `judgePromotedFields = rc.judge_flags.promoted_field ? [...] : []`), NOT caller-injectable `judge_confidence_by_field`. Withheld → `stage_only`/`withheld_by_overlap_precedence`. Tests: both-conflict-promote-one (line 207), gap-fill-requires-existing-blank (line 197, kimi HIGH-2). |
| 6 | SC-3 honest: no residual provider-vs-research adjudication claim; escalation existing-record only | ✓ VERIFIED | `computeContactEscalation(researchCandidate, existingRecord)` compares research only to existingRecord (jobtitle_conflict/seniority_conflict/confidence_band); explicit comment "NO provider-vs-research comparison (SC-3 honest mirror)". CONTACTS_TARGET gate predicate: `provider_gap` + `jobtitle_stale_refresh`, explicit "NOT a provider-vs-research comparison trigger (SC-3 honest-mirror decision)". No positive adjudication claim in delivered code. |
| 7 | seniority + provenance in contact search list; credential bookkeeping; no new global switch; jobtitle+seniority only (PII) | ✓ VERIFIED | Emitted `HubSpot Search` node carries `seniority` + `lv_contact_enrichment_provenance` in properties. `Contact Web Research`/`Contact Judge Call` in `SECRET_TO_CREDENTIAL_BOUND_NODES` (parity), `NODE_CREDENTIAL_MAP` (deploy → LV Anthropic), and deploy-binding test (test_deploy_n8n_workflows.py:431-432). `CONFIG_FLAG_DEFAULTS` unchanged in 16.2 (reuses inherited 6 flags via `_flag_const`). Build Contact Research Request: 0 substantive phone/email/mobilephone hits; research fields = ["jobtitle","seniority"]. |

**Score:** 7/7 truths verified (0 present-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/build_cloud_workflows.py` | EnrichTarget/COMPANIES_TARGET/CONTACTS_TARGET, parameterized factories, contact chain wired at seam | ✓ VERIFIED | EnrichTarget dataclass + entry_strip_markers; 10 contact nodes + connections in both cloud/local-live builders; seniority/provenance search additions; ENRICH_MERGE fold call |
| `n8n/code/contactResearch.js` | contact research candidate + evidence-URL-gated validation | ✓ VERIFIED | `contactResearchCandidateFromHttpItem` (never-throws), `validateContactResearch` (https:-only evidence gate); reuses webResearch.js helpers by require() |
| `n8n/code/contactJudge.js` | 2-arg escalation, security-hardened verdict | ✓ VERIFIED | computeContactEscalation (2-arg, clock-free, existing-record only), applyContactUnadjudicated, buildContactJudgeRequestBody (JG-2, no tools), applyContactJudgeVerdict (allowlist) |
| `n8n/code/mergeContacts.js` | foldContactResearch + stale-timestamp fix | ✓ VERIFIED | foldContactResearch write-safety gate; cacheKeys stamped only inside `if (decision === "promote")` (gpt #6) |
| `scripts/deploy_n8n_workflows.py` | NODE_CREDENTIAL_MAP for 2 anthropic nodes | ✓ VERIFIED | Contact Web Research + Contact Judge Call → LV Anthropic |
| `n8n/wf_enrichment_cloud.json` | emitted chain | ✓ VERIFIED | 10 contact nodes, correct topology, row-recovery present, current (deterministic rebuild = no diff) |
| new tests | mirror companies coverage | ✓ VERIFIED | contactResearch/contactJudge/contactResearchChainRowFlow/mergeContacts.test.mjs + test_cloud_contacts_branch.py all in the green 228-node/346-pytest run |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| Normalize + Score | Contact Research Trigger Gate | direct connection (16.1 seam) | ✓ WIRED |
| Contact Web Research (HTTP) | Validate Contact Research | paired-index recovery from Build Contact Research Request | ✓ WIRED |
| Contact Judge Call (HTTP) | Apply Contact Judge Verdict | paired-index recovery from Build Contact Judge Request | ✓ WIRED |
| Apply Contact Judge Verdict | Merge Winners | foldContactResearch(merged, researchMerged, judgePromotedFields, existingRecord) | ✓ WIRED |
| Contact Web Research / Contact Judge Call | LV Anthropic credential | SECRET_TO_CREDENTIAL_BOUND_NODES + NODE_CREDENTIAL_MAP + deploy test | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full pytest suite | `.venv/bin/python -m pytest -q` | 346 passed, 0 failed | ✓ PASS |
| Full node suite | `node --test tests/n8n/*.test.mjs` | 228 pass, 0 fail | ✓ PASS |
| Deterministic rebuild | `build_cloud_workflows.py` ×2 → `git diff --stat n8n/wf_*.json` | empty (current + deterministic) | ✓ PASS |
| Companies byte-identity guard | `pytest tests/test_companies_factory_frozen.py` | 4 passed | ✓ PASS |
| Companies row-flow regression | `node --test tests/n8n/researchChainRowFlow.test.mjs` | 1 pass, 0 fail | ✓ PASS |
| Contact row-flow (merge != null) | `contactResearchChainRowFlow.test.mjs` (in suite) | row survives both hops, real merge | ✓ PASS |

### Anti-Patterns Found

None blocking. Shared modules (judge.js/webResearch.js/scoreEnrichment.js/mergeCompanies.js) git-unchanged; CONTACTS_TARGET is genuinely wired (not a stub); tests execute real emitted jsCode, not isolated pure functions only.

### Human Verification Required

None required for goal achievement. Two follow-ups (see frontmatter) are for milestone-completion awareness, not phase-blocking:

1. **Full conflict-lane item-flow coverage.** The row-flow harness runs nodes unconditionally with a gap (not conflict) seed, so the conflict→escalation→judge→cap-demote path is exercised only by pure-function unit tests, never through the compiled node bodies end-to-end. Honestly disclosed. Consider a conflict-seed item-flow test in a future pass.
2. **Live-readiness caveat (fetch-by-objectId).** Genuine HubSpot private-app events carry only objectId/objectType; the chain is verified against synthetic/direct-field payloads. A fetch-by-objectId node is required before live private-app events can drive contacts (shared 16.1 Track-B deferral, OFF-by-default posture makes this non-urgent).

### Gaps Summary

No gaps. All 7 review-hardened success criteria verified against delivered code. The two X-blockers the reviews raised (X1 row-loss, X2 SC-3 over-promise) were both correctly resolved: X1 by mirroring the bd682a2 row-recovery in both contact wrappers (proven by an item-flow test executing real emitted jsCode), X2 by the honest SC-3 redefinition implemented as a write-safety fold with existing-record-only escalation. HIGH-2 residual-clobber, the chosen_field security vector, seniority fetch, stale-timestamp, credential deploy-binding, and evidence-URL validation are all closed with substantive tests. Track-B carve-outs are honestly recorded (not silently dropped).

---

_Verified: 2026-07-24_
_Verifier: Claude (gsd-verifier)_
