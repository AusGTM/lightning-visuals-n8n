---
phase: 16.2-contacts-research-judge-mirror
plan: 01
subsystem: infra
tags: [n8n, codegen, python-builder, code-node, judge, web-research, merge-policy]

# Dependency graph
requires:
  - phase: 16.1-provider-selection-credit-reporting-schedule-safety
    provides: contacts branch (Normalize + Score -> Merge Winners seam) + the 6-flag
      cost-safety cap set (ALLOW_WEB_RESEARCH/MAX_WEB_RESEARCH_PER_RUN/
      ALLOW_SONNET_ESCALATION/MAX_SONNET_VALIDATIONS_PER_RUN/ANTHROPIC_SONNET_MODEL/
      WEB_RESEARCH_MAX_SEARCHES) this plan's CONTACTS_TARGET reuses unchanged
  - phase: 16-company-enrichment-icp-scoring (bd682a2)
    provides: the companies research/judge/validate/apply-verdict row-recovery-across-
      HTTP-hops fix this plan's frozen fixture pins as the correct post-fix baseline
provides:
  - "EnrichTarget dataclass + COMPANIES_TARGET (byte-identical default) + CONTACTS_TARGET
    (unwired) config in scripts/build_cloud_workflows.py"
  - "The six companies research/judge/validate/apply-verdict factories parameterized by
    `target`, proven byte-identical by a frozen-snapshot guard"
  - "mergeContacts.js additive evidence/confidenceByField/_needsEvidence port — the
    substrate Plan 02's contact research fold writes into"
affects: [16.2-02-PLAN.md]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Target-parameterized Python factory (dataclass config, default reproduces the
      pre-refactor string exactly) as the reuse mechanism for n8n Code-node codegen
      where the underlying JS module text is inlined verbatim and cannot be edited
      without changing every consumer's emitted bytes."
    - "Frozen-snapshot regression guard that CALLS the build function in-test (not a
      fixture-vs-committed-JSON compare) so a shared-module edit with no rebuild still
      fails loudly."

key-files:
  created:
    - tests/test_companies_factory_frozen.py
    - tests/fixtures/companies_jscode_frozen.json
    - tests/n8n/mergeContacts.test.mjs
  modified:
    - scripts/build_cloud_workflows.py
    - n8n/code/mergeContacts.js
    - tests/test_judge_spec.py
    - n8n/wf_enrichment_cloud.json
    - n8n/wf_enrichment_local.json
    - n8n/wf_enrichment_local_live.json
    - n8n/wf_contact_ingest_cloud.json
    - n8n/wf_contact_ingest_local.json

key-decisions:
  - "Guard-first sequencing: captured the frozen companies jsCode fixture from the
    CURRENT (post-bd682a2) HEAD and proved the guard green BEFORE touching any factory,
    so every subsequent edit was checked against a known-correct baseline rather than
    trusted by inspection."
  - "Parameterization surface is opaque JS-source-fragment fields on an EnrichTarget
    dataclass (prompt text, gap predicate, pass-1 body, row-recovery node names, etc.)
    spliced in by shared scaffolding — never edits to judge.js/webResearch.js/
    scoreEnrichment.js/mergeCompanies.js, honoring the inline() byte-identity mechanism
    and computeEscalation's 2-arg arity discipline."
  - "CONTACTS_TARGET is authored but genuinely inert this plan: its inline_modules name
    contactResearch.js/contactJudge.js, which do not exist yet, and no call site passes
    target=CONTACTS_TARGET, so inline() is never invoked against those names — the build
    cannot fail on a missing sibling module."
  - "mergeContacts.js's decisions[] now unconditionally carries evidence_url (null when
    absent), mirroring mergeCompanies.js's own precedent exactly rather than only adding
    the key when opts.evidence is supplied — additivity is judged at the shape level
    (new key, old behavior unchanged for every existing value), consistent with how
    mergeCompanies' own port was accepted."

requirements-completed:
  - "SC-2 (the research/judge node factories are shared with the companies branch by
    target-parameterization, NOT duplicated — one factory serves both, altering the
    chain touches one place; companies emitted jsCode stays byte-identical, proven by a
    frozen-snapshot guard)"
  - "SC-4 (partial: the companies-byte-identity guard + the mergeContacts additive-port
    test; full offline suite green; builder rebuild deterministic; no live calls)"

coverage:
  - id: D1
    description: "The six companies research/judge/validate/apply-verdict Code-node
      factories in scripts/build_cloud_workflows.py accept a `target` config that
      defaults to COMPANIES_TARGET, reproducing today's exact emitted jsCode string for
      both the cloud and local-live variants."
    requirement: "SC-2"
    verification:
      - kind: unit
        ref: "tests/test_companies_factory_frozen.py#test_companies_cloud_jscode_is_byte_identical_to_frozen_fixture"
        status: pass
      - kind: unit
        ref: "tests/test_companies_factory_frozen.py#test_companies_local_live_jscode_is_byte_identical_to_frozen_fixture"
        status: pass
    human_judgment: false
  - id: D2
    description: "The committed wf_enrichment_cloud.json / wf_enrichment_local_live.json
      companies node jsCode matches a fresh builder run (source/artifact currency), and a
      second builder run is byte-identical to the first (deterministic rebuild)."
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "tests/test_companies_factory_frozen.py#test_committed_wf_enrichment_cloud_json_is_current"
        status: pass
      - kind: unit
        ref: "tests/test_companies_factory_frozen.py#test_committed_wf_enrichment_local_live_json_is_current"
        status: pass
    human_judgment: false
  - id: D3
    description: "mergeContacts.js gains the additive evidence/confidenceByField opts +
      _needsEvidence evidence-URL gate ported from mergeCompanies.js; the one existing
      ENRICH_MERGE provider caller (which omits both opts) is behaviorally unchanged."
    requirement: "SC-2"
    verification:
      - kind: unit
        ref: "tests/n8n/mergeContacts.test.mjs (9 cases: additive-shape unchanged,
          evidence-gate withhold, evidence-gate promote, confidenceByField override,
          email hard-guard + cache-key regression)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The four shared JS modules judge.js/webResearch.js/scoreEnrichment.js/
      mergeCompanies.js remain git-unchanged, and the full offline suite (pytest + node)
      is green with zero regressions vs the measured baseline, no live calls."
    requirement: "SC-4"
    verification:
      - kind: other
        ref: "git diff HEAD~2 -- n8n/code/judge.js n8n/code/webResearch.js n8n/code/scoreEnrichment.js n8n/code/mergeCompanies.js (empty)"
        status: pass
      - kind: unit
        ref: ".venv/bin/python -m pytest -q (336 passed)"
        status: pass
      - kind: unit
        ref: "node --test tests/n8n/*.test.mjs (179 passed)"
        status: pass
    human_judgment: false

# Metrics
duration: 55min
completed: 2026-07-24
status: complete
---

# Phase 16.2 Plan 01: Companies Byte-Identity Guard + Factory Parameterization + Additive mergeContacts Port Summary

**Target-parameterized the six companies research/judge/validate/apply-verdict Code-node factories in `scripts/build_cloud_workflows.py` behind a companies-defaulting `EnrichTarget` config (proven byte-identical by a guard test that builds and compares live, not a committed-JSON diff), and additively ported mergeCompanies' evidence/confidenceByField/_needsEvidence machinery onto `mergeContacts.js`.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-07-24T02:48:00Z (approx, from session start)
- **Completed:** 2026-07-24T03:43:20Z
- **Tasks:** 2/2 completed
- **Files modified:** 8 (2 created test files + 1 fixture + 5 modified source/workflow files, plus 2 collateral n8n/wf_contact_ingest_*.json regenerated as a side effect of the mergeContacts.js port)

## Accomplishments
- Landed the load-bearing frozen-snapshot guard (`tests/test_companies_factory_frozen.py` + `tests/fixtures/companies_jscode_frozen.json`) FIRST, against the current post-bd682a2 baseline, before any factory edit — the guard calls `build_enrichment_cloud()`/`build_enrichment_local_live()` in-test so a shared-module edit with no rebuild still fails.
- Parameterized `_enrich_research_gate_js`, `_enrich_build_research_request_js`, `ENRICH_VALIDATE_RESEARCH`, `_enrich_judge_gate_js`, `_enrich_build_judge_request_js`, `ENRICH_APPLY_JUDGE_VERDICT` by a new `EnrichTarget` dataclass config (`target=None -> COMPANIES_TARGET`), with every companies call site passing no target arg — proven byte-identical (both cloud and local-live variants) both immediately after the refactor and after Task 2's unrelated mergeContacts change.
- Authored `CONTACTS_TARGET` (label, gate-predicate JS with provider-aware provider_gap/jobtitle_stale_refresh triggers, contact research prompt, task_name, required_fields, max_tokens=2048, validate/judge-gate/build-judge/apply-verdict inline-module lists + function names, pre-HTTP row-recovery node names) — genuinely unwired: no call site passes it, so the (not-yet-existing) `contactResearch.js`/`contactJudge.js` sibling modules it names by string are never `inline()`d in this plan.
- Ported `evidence`/`confidenceByField` opts and the `_needsEvidence` evidence-URL gate onto `mergeContacts.js`, matching `mergeCompanies.js`'s shape exactly (gate runs before class branches, per-field confidence resolution, `provenance.evidence_url` when non-blank, `decisions.evidence_url` unconditionally) — additive for the one existing ENRICH_MERGE provider caller.
- Confirmed deterministic rebuild (`build_cloud_workflows.py` run twice, byte-identical output) after each task, and that `n8n/code/judge.js`, `webResearch.js`, `scoreEnrichment.js`, `mergeCompanies.js` are git-unchanged across both commits.

## Task Commits

1. **Task 1: Companies-byte-identity guard + parameterize the six research/judge factories** - `c944496` (refactor)
2. **Task 2: Additive mergeContacts evidence/confidenceByField port** - `28058b9` (feat)

_No plan-metadata commit yet — this commit follows in the state-update step below._

## Files Created/Modified
- `tests/test_companies_factory_frozen.py` - the byte-identity guard: calls `build_enrichment_cloud()`/`build_enrichment_local_live()` in-test and compares 7 companies Code-node jsCode strings against the frozen fixture, plus a currency check against the committed `wf_enrichment_*.json`
- `tests/fixtures/companies_jscode_frozen.json` - the frozen per-target (cloud/local_live) jsCode snapshot, captured from the post-bd682a2 HEAD before any factory edit
- `scripts/build_cloud_workflows.py` - `EnrichTarget` dataclass, `COMPANIES_TARGET`/`CONTACTS_TARGET` config instances, and the six factories/consts rewritten to splice target-supplied JS fragments into shared scaffolding
- `tests/test_judge_spec.py` - `test_prompt_parity_vendor_flags`'s source-slice markers updated to follow the vendor-flag prompt text from the (now-thin) factory function body to `COMPANIES_TARGET`'s assignment block, where that text now lives as data
- `n8n/code/mergeContacts.js` - additive `_needsEvidence` + `_gate` evidenceUrl/value params + `mergeContacts` opts.evidence/opts.confidenceByField
- `tests/n8n/mergeContacts.test.mjs` - 9 cases proving the port additive + the new evidence/confidence behavior
- `n8n/wf_enrichment_cloud.json`, `n8n/wf_enrichment_local.json`, `n8n/wf_enrichment_local_live.json`, `n8n/wf_contact_ingest_cloud.json`, `n8n/wf_contact_ingest_local.json` - regenerated (Task 2 only — the contacts "Merge Winners" node's inlined mergeContacts.js text changed; Task 1's rebuild was a byte-identical no-op diff, confirmed and not re-committed)

## Decisions Made
- Guard-first sequencing (capture the fixture from HEAD, prove the guard green, THEN refactor) rather than writing the guard and the refactor together — this let every subsequent edit be checked against a known-correct baseline via `pytest`, with byte-level diffs pinpointing exact whitespace mismatches during development rather than requiring manual verification of the concatenation logic.
- `CONTACTS_TARGET`'s field values (gap predicate JS, research prompt, escalation-trigger names, function names) are informative/structural for Plan 02, not executed or validated by any test in this plan — per the plan's explicit instruction not to `inline()` non-existent sibling modules. Content was written to genuinely reflect RESEARCH's Task 2/3 design (provider-aware gap predicate reading `existingRecord` + `scored.winners`, no A/R/G/T grounding in the contact judge pass-1) rather than as a placeholder, so Plan 02 has a real starting point.
- Extended `mergeContacts.js`'s `decisions[]` to unconditionally carry `evidence_url` (null when absent) rather than only when `opts.evidence` is supplied, matching `mergeCompanies.js`'s own precedent for the identical port — verified this doesn't break any existing test (170 node tests were green both before and after this specific change, isolated by running the node suite before writing the new test file).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed `test_prompt_parity_vendor_flags` after moving prompt text into the target config**
- **Found during:** Task 1, running the full pytest suite after parameterizing `_enrich_build_research_request_js`
- **Issue:** This pre-existing test slices raw Python *source text* between the string markers `"def _enrich_build_research_request_js"` and `"\n# Validate Research Output"` and asserts `lv_is_hardware_vendor`/`lv_is_gambling_operator` appear in that slice. The refactor moved the company research-prompt text (which contains those literals) out of the function body and into `COMPANIES_TARGET`'s field values — the function is now a thin scaffold that only concatenates `target` fields, so the slice-based check failed even though the emitted jsCode (verified byte-identical by the frozen guard) is unchanged.
- **Fix:** Updated the test's start/end markers to slice `"COMPANIES_TARGET = EnrichTarget("` through `"\n\n# CONTACTS_TARGET"` instead — the same parity intent (production prompt vs. `src/web_research.py`'s dev-oracle prompt must not drift), now pointed at where that data actually lives.
- **Files modified:** `tests/test_judge_spec.py`
- **Verification:** Full pytest suite green (336 passed) after the fix; the frozen-snapshot guard independently confirms the actual emitted jsCode is unaffected.
- **Committed in:** `c944496` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix, collateral to the refactor)
**Impact on plan:** Necessary for suite-wide correctness; no scope creep — the fix only relocates which source-text region a pre-existing parity check inspects.

## Issues Encountered
None beyond the deviation above. The byte-identity reconstruction (matching whitespace/newline placement exactly across six factories) was iteratively verified against the frozen guard rather than hand-checked, per the plan's own guard-first rationale.

## User Setup Required
None - no external service configuration required. Pure codegen + JS module changes, no live calls.

## Next Phase Readiness
- The reuse substrate (target-parameterized factories + `CONTACTS_TARGET` config + `mergeContacts.js`'s evidence/confidenceByField machinery) is in place and proven byte-identical for companies. Plan 02 (16.2-02) can write `contactResearch.js`/`contactJudge.js`, wire the contact call sites (`target=CONTACTS_TARGET`), and fold the adjudicated research candidate into `ENRICH_MERGE` via a second `mergeContacts` call — the exact structural analog of `ENRICH_MERGE_CO`'s companies fold — without touching any of the six factories' scaffolding or the four frozen shared JS modules.
- `CONTACTS_TARGET`'s field values are a real design (not a stub) but unvalidated by execution — Plan 02 should sanity-check the gap-predicate call signature (`needsResearch(row.existingRecord, row.scored)`, 2-arg, gate runs after Normalize + Score) and the pass-1 body (`computeContactEscalation` only, no `scoreResearchCandidates`) against its own sibling-module implementation before wiring.
- No blockers.

---
*Phase: 16.2-contacts-research-judge-mirror*
*Completed: 2026-07-24*

## Self-Check: PASSED
All created/modified files verified present on disk; both task commits (c944496, 28058b9) verified present in git history.
