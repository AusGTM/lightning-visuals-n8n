---
phase: 16.8-row-carry-fix
verified: 2026-07-29T00:00:00Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 16.8: Row-Carry Fix (BUG 12) Verification Report

**Phase Goal:** The contacts write path can actually reach HubSpot. Before this phase it could not, for any record, under any flag combination.
**Verified:** 2026-07-29
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `Set Data Quality + Gap Flag` is a Code node that spreads `...it.json` | ✓ VERIFIED | Read `n8n/wf_enrichment_cloud.json`: node `"type": "n8n-nodes-base.code"`, `typeVersion 2`, `jsCode` contains `return $input.all().map((it) => ({ json: { ...it.json, data_quality: "scored_waterfall", gap_flag: it.json.gap_flag === true } }));` |
| 2 | Topology unchanged — sits exactly between `Merge Winners` and `Decide Action` | ✓ VERIFIED | `connections["Merge Winners"] -> "Set Data Quality + Gap Flag"`; `connections["Set Data Quality + Gap Flag"] -> "Decide Action"` — confirmed by direct JSON parse |
| 3 | `tests/test_row_carry.py` guards the fix with 3 checks; sweep is not vacuous | ✓ VERIFIED | File has `test_the_specific_bug_12_node_carries_the_row` (pinned by name), `test_mid_chain_set_nodes_carry_the_row_through` (sweep, parametrized over all 3 `wf_*_cloud.json`), `test_every_row_replacing_entry_is_still_a_real_node_somewhere` (staleness). Sweep empirically proven non-vacuous: injecting a synthetic mid-chain `n8n-nodes-base.set` node without `includeOtherFields` into an in-memory copy of the workflow reproduced the exact offender-detection logic and correctly flagged it (see Anti-Patterns/Notes below) |
| 4 | Offline suite green, zero regressions | ✓ VERIFIED | `.venv/bin/python -m pytest -q` → `542 passed, 1 warning`. `node --test tests/n8n/*.test.mjs` → `tests 278 / pass 278 / fail 0`. SUMMARY claims 533/278 at the 16.8 commit itself (`6728237`); repo HEAD is now past 16.9/16.10 which add tests — STATE.md documents the exact progression `504 -> 533` (post-16.8) `-> 542` (post-16.9/16.10), consistent with the 542 observed today |
| 5 | Deterministic rebuild | ✓ VERIFIED | `.venv/bin/python scripts/build_cloud_workflows.py` run twice; `git status --porcelain n8n/` empty after both runs (confirmed with an unaliased `git` binary — the shell's `rtk`-hooked `git` prints a non-standard `ok` token on a clean tree, so the raw binary was used to avoid a false read) |
| 6 | Live: execution 15 reached `HubSpot Update` with real `hs_object_id`/patch, error `null`, status `success` | ✓ VERIFIED (documentary) | Quoted values cross-checked against static code, not re-queried live (task instructions explicitly forbid firing webhooks/writing to HubSpot). `Decide Action`'s own jsCode (read from the committed workflow) computes `hs_object_id = (row.existingRecord && row.existingRecord.hs_object_id) || null` and `properties = _buildContactPatch(row.merge)` — exactly the root-cause mechanism SUMMARY describes as broken pre-fix and working post-fix. The company byte-identical oracle value `2026-07-28T03:42:15.843Z` and contact-201 pre-run value `2026-07-18T01:14:03.751Z` quoted in the SUMMARY match, verbatim, the "Zero-write oracles" values recorded independently in `.planning/ROADMAP.md` line 580 (written for Phase 16.7, before 16.8 ran) — strong cross-document corroboration |
| 7 | Non-clobber fresh-record-read table (seniority written, mobilephone/jobtitle refused) | ✓ VERIFIED (documentary) | `n8n/code/mergeContacts.js` gate logic reproduces the claimed outcome exactly: `mobilephone` is `class: fill_blank_only, min_confidence: 85` — with an existing value present, the gate (`_gate()` lines 109-113) returns `stage_only` regardless of confidence once it clears the threshold ("refused by class alone" — matches SUMMARY verbatim); `jobtitle` is `stale_refreshable` — with an existing value, `_gate()` (lines 115-119) returns `needs_review`, never promoting, matching "kept its existing value"; `seniority` is `system_owned` — promotes unconditionally once confidence clears 75, matching the written outcome. `_isBlank()` (line 34-37) treats `""`, `null`, `undefined` identically, corroborating the rollback caveat's "functionally blank, not byte-identical" claim. No `_verified_by_model` key exists anywhere in `mergeContacts.js` (`grep` returns 0 matches), matching the Phase-15-restated criterion-4 claim |
| 8 | Wrong turn (`includeOtherFields`) reported honestly, not hidden | ✓ VERIFIED | Git history itself preserves it: commit `fb87cb5` ("fix(16.8): BUG 12...") adds `options.includeOtherFields: true` to the Set node and states in its own commit message, written at the time, "The fix is the node's own includeOtherFields option..." — i.e. this was genuinely believed to be the fix when committed, not a knowingly-fake attempt. Commit `6728237` ("feat(16.8): row-carry fix proves the write path...") then supersedes it, converting the node to `n8n-nodes-base.code` and stating the option "did not work" per execution 14. The SUMMARY (`16.8-01-SUMMARY.md` lines 26-33) and ROADMAP (`### Phase 16.8`, "Plans:" line) both narrate this identically. Nothing was squashed or hidden |

**Score:** 8/8 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `n8n/wf_enrichment_cloud.json` (`Set Data Quality + Gap Flag` node) | Code node, row-spreading, in place between Merge Winners/Decide Action | ✓ VERIFIED | See truths 1-2 |
| `tests/test_row_carry.py` | 3 guard tests, offline-checkable | ✓ VERIFIED | 111 lines, exists, all 3 tests present and pass |
| `.planning/phases/16.8-row-carry-fix/16.8-01-SUMMARY.md` | Honest phase record including the wrong turn | ✓ VERIFIED | Present, ~139 lines, records wrong turn explicitly |
| `.planning/phases/16.8-row-carry-fix/16.8-01-PLAN.md` | — | ✗ MISSING | Phase was executed directly with no PLAN.md — **process gap**, not a goal-blocking defect (see Anti-Patterns) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `Merge Winners` | `Set Data Quality + Gap Flag` | `connections` edge | ✓ WIRED | Confirmed by JSON parse |
| `Set Data Quality + Gap Flag` | `Decide Action` | `connections` edge | ✓ WIRED | Confirmed by JSON parse |
| `Decide Action` | `IF Enrich` → `HubSpot Update` | row now carries `existingRecord`/`merge` so `action`/`hs_object_id`/`properties` are non-empty | ✓ WIRED (by code inspection) | `Decide Action`'s own jsCode resolves `hs_object_id`/`properties` from `row.existingRecord`/`row.merge`, which the row-carry fix restores |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Row-carry sweep actually catches a regression | in-memory synthetic offending Set node injected into `wf_enrichment_cloud.json`, sweep logic re-run | `offenders: ['FAKE Set Node']` | ✓ PASS |
| Pytest suite | `.venv/bin/python -m pytest -q` | `542 passed, 1 warning in 1.92s` | ✓ PASS |
| Node suite | `node --test tests/n8n/*.test.mjs` | `tests 278 / pass 278 / fail 0` | ✓ PASS |
| Rebuild determinism | `scripts/build_cloud_workflows.py` x2 + `git status --porcelain n8n/` | empty | ✓ PASS |
| Firing a live webhook or querying execution 15/HubSpot directly | — | — | ? SKIP — explicitly forbidden by task instructions (deployment must stay disarmed) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/phases/16.8-row-carry-fix/` | — | No `16.8-01-PLAN.md` exists — phase was executed directly, conversationally, contrary to the repo's own documented GSD workflow (`.planning/ operates as source of truth; read STATE.md and plan a phase before coding`) | ℹ️ Info | Does not block the goal (the fix and its test/live proof are real and verified), but is a process deviation the ROADMAP entry itself doesn't call out explicitly. Noted per task instructions. |
| `tests/test_row_carry.py` | 30-43 | `ROW_REPLACING_BY_DESIGN` waiver list currently covers **100%** of existing `n8n-nodes-base.set` nodes across all 3 cloud workflows (6/6) | ℹ️ Info | Not a defect — the bug node itself was moved OFF `n8n-nodes-base.set` entirely (now Code), so today's sweep has zero live offenders to find by construction. Empirically confirmed (via synthetic-node injection above) that the sweep does correctly flag a *future* non-waived mid-chain Set node — the guard is a regression guard, not a current-state detector, and that is an accurate characterization of what it protects against. |

### Requirements Coverage

Not applicable — Phase 16.8 is an INSERTED bug-fix phase (found live during 16.7) with no REQUIREMENTS.md mapping. `grep -n "16.8" .planning/REQUIREMENTS.md` returns zero matches; no orphaned requirements.

### Human Verification Required

None. All must-haves resolved to VERIFIED via static/offline evidence, cross-document consistency (SUMMARY / ROADMAP / STATE.md all quote identical timestamps and outcomes independently), git-history corroboration of the wrong turn, and code-logic reproduction of the claimed non-clobber outcomes. The live-execution claims (truths 6-7) were deliberately NOT re-queried against HubSpot/n8n per the task's explicit instruction to keep the deployment disarmed; they are marked VERIFIED on the strength of that corroboration rather than an independently reproduced live call, and that distinction is stated plainly above rather than glossed over.

### Gaps Summary

No gaps. The row-carry defect (BUG 12) is fixed as a Code node, the topology is unchanged, the guard test is real (empirically proven non-vacuous), the offline suite is green, the rebuild is deterministic, and the wrong turn (the `includeOtherFields` platform-option attempt) is honestly recorded in both the SUMMARY and — more strongly — in git history itself (commit `fb87cb5` committed the wrong turn believing it was the fix; commit `6728237` supersedes it after live falsification). ROADMAP criterion 2 ("use the platform feature, not a hand-rolled spread") was deliberately NOT met, and that deviation is recorded in the ROADMAP's own Phase 16.8 entry, not silently substituted. The only process gap is the missing `16.8-01-PLAN.md` (direct execution, no upfront plan document) — informational, not goal-blocking.

---

_Verified: 2026-07-29_
_Verifier: Claude (gsd-verifier)_
