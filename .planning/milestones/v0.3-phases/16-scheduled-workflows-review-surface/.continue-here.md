---
context: phase
phase: 16-scheduled-workflows-review-surface
task: 10
total_tasks: 10
status: complete
last_updated: 2026-07-23T10:00:12.698Z
---

# Phase 16 — COMPLETE (offline). Milestone 3 (v0.3) code-complete.

No blocking constraints. Execution was clean (2 plans, 1 checkpoint resolved, verifier passed 9/9). What remains is the **live operator runbook only** — non-gating, Phase-15 precedent.

<current_state>
Phase 16 (n8n Cloud Deployment, Scheduled Workflows & Review Surface) fully executed, cross-AI reviewed, and verified 9/9 offline. Milestone 3 (Phases 11–16) is code-complete. Full suite green: **267 pytest / 147 node**, builder deterministic, frozen files intact, zero live calls. Everything is committed and **pushed** to `origin/feat/company-enrichment-icp-research` (HEAD `42f5fa1`). No PR opened yet.
</current_state>

<completed_work>
- 16-01 Deployable (6 tasks, `34e6dcf`→`7f8e054`): deploy + provision scripts (Public API, two-key gated, fail-closed credential binding), ZoomInfo split-code-node, companies-branch port into `build_enrichment_cloud()`, webhook auth + real HubSpot nodes + `WRITE_SAFETY_DEFAULTS`, SJ-3 control properties.
- 16-02 Complete (4 tasks, `1b436e1`→`da0dc2c`): SJ-1/2/3 scheduleTrigger workflows (inputs-only predicates), `dedupeSweep.js` wired classify-only, §22.2 review loop (`reviewApply.js` refetch + compare-and-set + fail-closed), RT-5 direct `decideAction` test.
- Cross-AI review (3 OpenRouter models via opencode) → reviews-mode replan → all findings landed (`aa1ea07`); verifier 9/9 (`7617366`); deploy-glob fix (`8e4b5b7`); docs mermaid + staleness sweep (`42f5fa1`, authored as dr-robert-li).
</completed_work>

<remaining_work>
LIVE operator runbook (see `16-UAT.md`) — all non-gating:
1. Provision + deploy: `set -a; . ./.env; set +a` then `DRY_RUN=false ALLOW_N8N_DEPLOY=true .venv/bin/python scripts/provision_n8n_credentials.py` then the same for `deploy_n8n_workflows.py`. (Both dry-run-verified this session; all 7 secrets present in `.env` incl. `N8N_ENRICHMENT_WEBHOOK_SECRET`.)
2. Activate both workflows: `POST /api/v1/workflows/{id}/activate` (or MCP).
3. Wire `Review Apply Update` node `updateFields` = `{...canonicalPatch, ...clearPatch}` (ships `{}` placeholder — live approval won't write until wired).
4. Decide real webhook caller/auth: the gate is `X-Enrichment-Secret` (header auth); HubSpot's native webhooks send `X-HubSpot-Signature` instead — so a direct HubSpot→n8n call needs an edge gateway or signature verification. Optional fetch-by-objectId node (Build Identity is a body-field shim, 16-01 Deviation 3).
5. Optional: `/gsd-complete-milestone` or `/gsd-ship` for v0.3.
</remaining_work>

<decisions_made>
- ZoomInfo = **split-code-node** (credential-bound HTTP Mint node + secret-free cache Code nodes) — n8n Cloud blocks `$env`/`$vars`, Code nodes can't read credentials.
- Two plans in one phase (no 16/17 split) — shared `build_cloud_workflows.py`.
- `deploy_n8n_workflows.py` globs `wf_*_cloud.json` only (excludes `$env`-laden local fixtures) — the cross-AI/verifier foot-gun, fixed + regression-tested.
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Remote: `github.com/AusGTM/lightning-visuals-n8n`, branch `feat/company-enrichment-icp-research` pushed to HEAD `42f5fa1`.
- n8n Cloud instance is EMPTY (0 workflows, 0 credentials) — clean live deploy. Dry-runs confirmed API key valid + wrong-instance guard passing.
- `.env` has all 7 secrets. `.env.example` shows an uncommitted (pre-existing, session-start) change — not part of Phase 16.
- `/gsd-review` reviewer_instances persisted: gpt-5.6-sol / kimi-k3 / gemini-3.6-flash (opencode→OpenRouter); run `/gsd-review --phase N` WITHOUT `--all`.

## Critical Anti-Patterns (advisory)
- ENV: use `.venv/bin/python -m pytest -q` (system python lacks deps) and `node --test tests/n8n/*.test.mjs` (dir form `node --test tests/n8n/` is BROKEN on node 24). See memory `test-suite-run-commands`.
- Editing an inlined `n8n/code/*.js` file regenerates `n8n/wf_*.json` via the builder — re-run `build_cloud_workflows.py` and commit the regenerated JSON.

## Provider credit-check endpoints (verified against official docs 2026-07-24)
For a future `scripts/check_provider_credits.py` (read-only, `.env`-keyed, no-creds skip) + the still-unbuilt provider kill-switch/`MAX_RECORDS_PER_RUN` cap:
- **Lusha:** `GET https://api.lusha.com/v3/account/usage`, header `api_key`, field `credits.remaining` (5 req/min). Verify the `/v3` version segment against a live 200 (docs SPA showed unversioned too). Doc: docs.lusha.com/apis/openapi/account-management/getaccountusagestats
- **Apollo:** `POST https://api.apollo.io/api/v1/usage_stats/api_usage_stats`, header `X-Api-Key` **must be a MASTER key** (else 403); returns rate-limit + consumption (no separate balance); costs 0 credits. Doc: docs.apollo.io/reference/view-api-usage-stats
- **ZoomInfo GTM:** `GET https://api.zoominfo.com/gtm/data/v1/users/usage`, OAuth2 bearer needs a `api:data:*` scope; fields `usageRemaining`/`totalLimit`/`currentUsage`/`limitType` (fully field-verified). Doc: docs.zoominfo.com/reference/userinterface_userusage

## Cost-safety gap (open, before any live activation)
No `ALLOW_PROVIDER_CALLS` switch and no `MAX_RECORDS_PER_RUN` cap exist. LLM is safe (ALLOW_WEB_RESEARCH + ALLOW_SONNET_ESCALATION both false), HubSpot data writes are safe (ALLOW_HUBSPOT_RECORD_WRITES false + empty allowlist). But SJ-1/SJ-2 "Set Requested" is an UNGATED control-property write, so activating the SCHEDULED workflow would flag the ~646-company gap backlog → SJ-3 feeds it to the provider waterfall → provider credits burn unattended. Webhook workflow is safe (fires only on your POST). **Recommendation: build the provider kill-switch (default off) + per-run cap (mirror WRITE_SAFETY_DEFAULTS, offline-testable) BEFORE activating any schedule.**

<context>
Milestone 3 is done. The pipeline is built, tested, reviewed, and deployable; only the live operator run stands between it and production. No code work is pending EXCEPT the recommended pre-live provider kill-switch/cap above.
</context>

<next_action>
Start with: run the live operator runbook (§ remaining_work items 1–2) if going to production, OR `/gsd-complete-milestone` to close v0.3.
</next_action>
