---
status: complete
phase: 15.5-tiered-candidate-adjudication
source: [15.5-VERIFICATION.md human_verification, 15.5-01-SUMMARY.md]
started: 2026-07-29T13:10:00+10:00
updated: 2026-07-29T13:10:00+10:00
---

## Current Test

[testing complete]

## Tests

### 1. The TA-4/TS-1 recency proof is non-tautological
expected: |
  15.5-VERIFICATION.md's single human_verification item. The shipped proof for
  "fresh vs stale page_age produce IDENTICAL canonicalPatch" called mergeCompanies with the
  LITERAL SAME opts twice (`{source:'claude_web', confidence:90}`), making it f(x)===f(x) —
  no evidence about recency at all. The maintainer decision asked for: accept the structural
  argument, or require the test to actually vary page_age end-to-end.
result: pass
source: automated
evidence: |
  Already resolved, and the frontmatter simply never caught up. `8c7432f` (2026-07-23,
  same day as the verification) — "test(15.5): make the TA-4/TS-1 recency proof
  non-vacuous (verifier finding)" — rewrote it to vary recency where it is actually
  consumed:

    fresh = scoreResearchCandidates(rc("2026-06-01"), ..., {now})
    stale = scoreResearchCandidates(rc("2021-04-06"), ..., {now})   // Wyong's real listing
    assert freshR > staleR  and  fresh.score !== stale.score        // recency genuinely moves
    assert deepEqual(merge(fresh), merge(stale))                    // canonicalPatch stable

  So the higher-rigor option was taken, not the structural-acceptance one.

### 2. The production `confidenceByField` path is itself recency-invariant
expected: |
  The verifier's item named a specific route: "derive confidenceByField from each via the
  REAL production wiring (Apply Judge Verdict's judge_confidence_by_field), and feed both
  through mergeCompanies". Test 1's rewrite varies page_age but merges with a FLAT
  confidence — so it never exercises `confidenceByField`, which is the one parameter
  through which recency could actually leak into the promotion gate.
result: pass
source: automated
evidence: |
  Gap closed today with a new test in tests/n8n/mergeCompanies.test.mjs. It mirrors the
  production derivation (build_cloud_workflows.py:2225-2231):

      judge_confidence_by_field[verdict.chosen_field] = verdict.confidence

  — the judge's own per-field confidence, never the A/R/G/T composite — builds that map
  from two rows differing ONLY in page_age, and asserts the merges match. The composite is
  in scope and deliberately not consulted, so the test fails if anyone rewires it.

  NON-VACUITY MEASURED, NOT ASSUMED. Simulating the leak (confidenceByField =
  composite*100) yields, on BOTH lanes: promoted=false, provenance.confidence=48.3. The two
  lanes therefore still produce EQUAL canonicalPatches ({} === {}), so the deepEqual would
  PASS under a real leak — necessary but not sufficient. What catches it is the promotion
  assertion (`stale.canonicalPatch[field] === "broadcaster"`) and the provenance-confidence
  assertion (`=== 88`). Both measured; the finding is recorded in the test's own comment so
  an equality-only rewrite cannot quietly re-vacuum it.

### 3. Suite green, no regressions
expected: Full offline suite passes with the added test.
result: pass
source: automated
evidence: 584 pytest / 283 node (+1 node test), zero failures.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

## Notes

The phase's verification had been `human_needed` since 2026-07-23 over a single item — a
test-rigor decision. Two thirds of it had already been answered the same day by `8c7432f`
and never recorded; the remaining third (the production `confidenceByField` route) is closed
here rather than waived, because it is the only parameter that could carry recency into the
promotion gate and asserting it costs one test.

The underlying invariant was never in doubt structurally — mergeCompanies has no recency or
page_age parameter — but this phase's own D2 finding is precisely that a plausible-looking
wiring of the composite into that parameter breaks promotion arithmetically. An invariant
worth stating structurally was worth pinning empirically.
