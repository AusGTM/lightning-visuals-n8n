---
phase: 16.4-fetch-by-objectid
plan: 02
type: execute
wave: 2
depends_on:
  - 16.4-01
autonomous: true
files_modified:
  - tests/n8n/adaptFetchById.test.mjs
  - tests/n8n/bareEventChainFlow.test.mjs
  - tests/test_fetch_by_id_topology.py
requirements:
  - "SC-3 (the existing direct-field / caller-envelope path keeps working, including the 16.1 per-request `providers` selection resolution — ASSERTED by a regression case, not assumed)"
  - "SC-4 (row-flow integrity across the new HTTP hop proven the bd682a2 way — node-name recovery, not the current item — by an item-flow regression mirroring tests/n8n/researchChainRowFlow.test.mjs)"
  - "SC-5 (all three tiers present: UNIT identity/row extraction from a fetched-record shape, INTEGRATION Parse Event -> gate -> fetch -> identity resolution over compiled node bodies, FUNCTIONAL/E2E from 16.4-01)"
  - "SC-6 (a fetch failure — errored item, malformed body, 0 results on a known id — degrades to lookup_failed and therefore to skip: no create, no clobber; proven at the unit tier AND through the compiled chain)"
  - "SC-7 (full offline suite green, zero regressions; builder deterministic; no live calls anywhere in the suite)"

must_haves:
  truths:
    - "Every failure surface of the fetch adapter maps to `lookup_failed: true` and therefore, through the EXISTING create->skip override in `ENRICH_GATE`/`ENRICH_CO_GATE`, to `action: \"skip\"` — no create, no clobber. Proven at the unit tier for the errored item, the malformed body, and the 0-results-on-a-known-id case, and proven through the COMPILED chain for at least the 0-results case, which is the one whose meaning DIFFERS from the sibling search adapter's identical input."
    - "The row survives the new HTTP hop intact: after `Adapt (Company) Fetch By Id`, the row still carries `object_id`, `object_type`, `provider_enabled` and a non-empty `providers_requested` — the 16.1 per-request provider selection is not silently dropped by the node that replaces the current item. This is the bd682a2 bug class asserted directly rather than inferred from the run completing."
    - "The direct-field / caller-envelope path — the payload shape the ENTIRE existing offline suite drives — still resolves identity the OLD way: with an `email` in the body the bare-event gate is false, `Build Identity` populates `identity_keys.email` itself, and the row reaches `Enrichment Gate` through `HubSpot Search` -> `Adapt Search` with `identity_keys` untouched by the new lane. Asserted as its own regression case, since back-compat here is a hard requirement rather than a hoped-for side effect."
    - "The topology is pinned mechanically: both gates sit immediately downstream of their identity builder and immediately upstream of both lanes; both fetch nodes are native credential-registered HubSpot `search` nodes filtered on `hs_object_id`/`EQ`; both adapters converge on their existing gate; `Route By Object Type`'s exact true/false edge targets are unchanged; and no `n8n-nodes-base.hubspot` node anywhere in the built cloud workflow uses a single-record retrieval operation."
    - "Every `n8n-nodes-base.hubspot` node in n8n/wf_enrichment_cloud.json is registered in `NODE_CREDENTIAL_MAP` — a GENERIC guard, so the next phase's HubSpot node cannot repeat the unbound-deploy failure without going red. The pre-existing unmapped HubSpot nodes in wf_scheduled_maintenance_cloud.json (9) and wf_contact_ingest_cloud.json (1) are RECORDED as a found-not-fixed finding, not fixed here."
    - "The MEDIUM-confidence dependency the whole design rests on — that HubSpot's CRM v3 Search API accepts `hs_object_id` as a filterable property with `EQ` in portal 22617666 — is surfaced as an explicit Track-B live checkpoint carrying its own verification command and its bounded fallback, not buried in a research appendix."
    - "The full offline suite is green with zero regressions against the baseline recorded in 16.4-01, the builder is deterministic across two consecutive runs, and no test in the suite makes a live call or reads a credential."
  artifacts:
    - "tests/n8n/adaptFetchById.test.mjs — the UNIT tier: adaptFetchByIdResult's four branches and backfillIdentityKeys' fill-only-blanks semantics for both object types, exercised against the pure module directly"
    - "tests/n8n/bareEventChainFlow.test.mjs — extended with the INTEGRATION tier: the row-flow regression across the new hop, the caller-envelope back-compat case, and the lookup-failure-degrades case"
    - "tests/test_fetch_by_id_topology.py — the pytest topology tier: gate placement, edge targets, node shapes, allowed HubSpot operations, the generic credential-map guard, and the belt-and-braces Route-By-Object-Type regression assertion"
  key_links:
    - "adaptFetchByIdResult's `lookup_failed: true` -> the row -> ENRICH_GATE:836 / ENRICH_CO_GATE:1560 create->skip override -> `IF Provider Processing Needed` false lane -> `Skip (NoOp)` -> no HubSpot write"
    - "`Parse HubSpot Event`'s `providers_requested`/`provider_enabled` -> the fetch hop -> `Enrichment Gate` -> the per-provider `IF <provider> Enabled` gates, which read `$('Parse HubSpot Event').item.json.provider_enabled.<name>` by node name"
    - "built cloud workflow HubSpot nodes -> NODE_CREDENTIAL_MAP -> bind_credentials() -> `LV HubSpot`"
  prohibitions:
    - "NO production-source change. This plan is TEST-ONLY: if a test goes red for a real reason, STOP and report rather than editing n8n/code/, scripts/build_cloud_workflows.py or scripts/deploy_n8n_workflows.py — a needed source fix means 16.4-01 shipped incomplete and should be amended deliberately."
    - "NO live calls, NO credential use, NO n8n deploy, NO portal writes. Offline suite only."
    - "NO re-baseline of tests/fixtures/companies_jscode_frozen.json and NO edit to tests/test_companies_factory_frozen.py."
    - "NO edit to tests/test_cloud_write_path.py, tests/test_cloud_contacts_branch.py or tests/test_cloud_companies_branch.py. If any of them goes red, that is a design signal about 16.4-01, not a test to update."
    - "NO change to provider fan-out, credit reporting, ICP scoring, merge/promotion policy, or the research/judge chains. NO new global kill-switches. NO fixing src/merge_policy.py:279-287."
    - "Do NOT widen the generic credential guard to wf_scheduled_maintenance_cloud.json or wf_contact_ingest_cloud.json — they carry pre-existing unmapped HubSpot nodes that are out of this phase's fence. Record the finding; do not chase it."
---

<objective>
Complete the three-tier coverage the phase was commissioned for and turn 16.4-01's design guarantees into
mechanically-checkable assertions. 16.4-01 wired and demonstrated the lane; this plan proves the parts a
single happy-path e2e cannot: every failure surface, the row-flow integrity across the new hop, and the
back-compat of the caller-envelope path the entire existing offline suite drives.

Purpose: the specific bug class this hop risks re-introducing already shipped undetected once (bd682a2 —
post-HTTP Code nodes spreading the response instead of recovering the row, so `Merge Company` returned
`merge:null`). It survived because the offline suite exercised JS functions in isolation and never the n8n
item-flow across an HTTP hop. A new hop without its own row-flow regression is that bug waiting for a
second turn.

Output: the unit tier against the pure module, the integration/row-flow tier over compiled node bodies, the
pytest topology tier with a generic credential guard, the recorded Track-B live checkpoint for the one
MEDIUM-confidence external assumption, and a final green suite with a deterministic rebuild.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.4-fetch-by-objectid/16.4-CONTEXT.md
@.planning/phases/16.4-fetch-by-objectid/16.4-RESEARCH.md
@.planning/phases/16.4-fetch-by-objectid/16.4-01-SUMMARY.md

# Targets + references (read the specific ranges named in each task's read_first)
@n8n/code/adaptFetchById.js
@tests/n8n/bareEventChainFlow.test.mjs
@tests/test_cloud_write_path.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UNIT tier on the pure adapter + INTEGRATION tier — row-flow across the hop, caller-envelope back-compat, and safe failure degradation</name>
  <files>tests/n8n/adaptFetchById.test.mjs, tests/n8n/bareEventChainFlow.test.mjs</files>
  <read_first>
    - n8n/code/adaptFetchById.js (read in full) — the exact export names, argument order, and return keys as shipped by 16.4-01. Test what exists, do not test what this plan imagines.
    - tests/n8n/enrichmentGate.test.mjs:1-30 — the `createRequire` import idiom every pure-module unit test in this repo uses to load a CommonJS module from an `.mjs` test.
    - tests/n8n/bareEventChainFlow.test.mjs (read in full) — 16.4-01's harness: the runner signature, the `outputs` map backing `$(name)`, the HTTP-node modelling, the two chain definitions and the seed-body shape. Extend it; do not fork the runner.
    - tests/n8n/researchChainRowFlow.test.mjs:1-18 and :110-128 — the bd682a2 header narrative and the shape of a row-survival assertion (`trace[node].field`), which is the style to mirror for the new hop.
    - scripts/build_cloud_workflows.py:796-816 (`ENRICH_BUILD_IDENTITY`) — the caller-envelope path's own identity derivation, so the back-compat case asserts the OLD behavior precisely (including `domain` falling back to the email's domain part).
    - scripts/build_cloud_workflows.py:820-839 and :1546-1563 — the two gate wrappers' `lookup_failed === true && action === "create"` -> `"skip"` overrides, so the degradation assertion targets the real mechanism.
    - n8n/code/providerSelection.js:33-58 (`parseWebhookBody`, `resolveEnabledProviders`) — note that a PLAIN HubSpot event ARRAY body carries no `providers` slot and therefore resolves to zero providers enabled (CONTEXT Locked Decision 2). That is the true live shape and needs its own case; the `{providers, events}` envelope is what makes `providers_requested` non-empty.
  </read_first>
  <behavior>
    UNIT — `adaptFetchByIdResult(item)`:
    - undefined/missing item -> `lookup_failed: true`, `existingRecord` deeply empty, diagnostic names an error.
    - `{ error: ... }` and `{ json: { error: ... } }` -> `lookup_failed: true`, and the diagnostic CARRIES the error text (this is the Track-B diagnosability property, so assert the text is present, not just that a diagnostic exists).
    - `{ json: { results: [], total: 0 } }` -> `lookup_failed: true` and the diagnostic names zero-results DISTINCTLY from the error case. This is the one case whose meaning inverts relative to the sibling search adapter, so it gets an explicit comment naming why.
    - `{ json: { results: [ { id: "789", properties: {...} } ] } }` -> `lookup_failed: false`, `existingRecord.hs_object_id === "789"`, and the flat properties spread onto existingRecord.
    - the single-object and bare-id response shapes the sibling adapter also handles -> `lookup_failed: false`.

    UNIT — `backfillIdentityKeys(objectType, existingRecord, currentIdentityKeys)`:
    - contacts, all-null identity_keys + a full fetched record -> `email`, `firstName`, `lastName`, `companyName`, `linkedin_url` populated, and `domain` derived from the backfilled email.
    - contacts, identity_keys already carrying an email + a fetched record with a DIFFERENT email -> the caller's email survives unchanged (fill-only-blanks, never overwrite).
    - companies, all-null -> `domain` and `companyName` populated from the record's `domain`/`name`.
    - empty/absent existingRecord -> the input identity_keys returned unchanged, no throw, no invented keys.
    - an unknown objectType -> returns the input unchanged rather than throwing.

    INTEGRATION (over compiled node bodies, both branches):
    - row-flow: after the fetch hop, `object_id`, `object_type`, `provider_enabled` and a non-empty `providers_requested` are all still on the row.
    - back-compat: a caller-envelope body carrying `email` (contacts) / `domain` (companies) resolves identity via `Build Identity` -> `HubSpot Search` -> `Adapt Search` with `identity_keys` matching the pre-16.4 derivation, and `providers_requested` still resolved from the envelope.
    - safe degradation: a 0-result fetch response yields `lookup_failed: true` on the row and `action === "skip"` out of the gate — no create.
    - the true-live plain-array body (no envelope) resolves to zero providers enabled and STILL reaches the gate with a populated identity key.
  </behavior>
  <action>
    (1) UNIT TIER. Create tests/n8n/adaptFetchById.test.mjs exercising the pure module directly via
    `createRequire`, covering every case in the behavior block above. Keep the assertions concrete: assert
    the actual returned values, not just truthiness. On the 0-results case, write the comment that explains
    why this adapter's answer differs from the sibling search adapter's answer for the identical input —
    the id came from HubSpot's own event, so zero results means deleted/merged/stale-event, and a create
    could not set a server-assigned `hs_object_id` anyway.

    (2) INTEGRATION TIER — ROW-FLOW REGRESSION. Extend tests/n8n/bareEventChainFlow.test.mjs with a
    dedicated row-flow test per branch, in the style of researchChainRowFlow.test.mjs: drive the same chain
    16.4-01 established and assert on the TRACE at the adapter step that `object_id`, `object_type`,
    `provider_enabled` and `providers_requested` all survived the fetch hop, with failure messages that
    name the bd682a2 bug class so a future reader knows what a red here means. Assert `providers_requested`
    is the non-empty array the envelope requested, not merely that the key exists — a dropped-then-defaulted
    empty array is exactly the silent failure being guarded against.

    (3) INTEGRATION TIER — CALLER-ENVELOPE BACK-COMPAT. Add a case per branch driving the DIRECT-FIELD
    envelope the whole existing suite uses: `{ providers: [...], events: [{ objectId: ..., objectType: ...,
    email: "someone@exampleco.example" }] }` for contacts (and `domain` for companies), through the lane
    `Parse HubSpot Event -> Build Identity -> HubSpot Search (http, mocked) -> Adapt Search ->
    Enrichment Gate`. Assert `identity_keys` matches the pre-16.4 derivation exactly — for contacts, the
    normalized email AND the domain derived from it — and that `providers_requested` still resolves from
    the envelope. This case must NOT touch the fetch nodes at all: it is the proof that the false lane is
    untouched by this phase.

    (4) INTEGRATION TIER — SAFE DEGRADATION. Add a case per branch driving the bare-event lane with the
    fetch mocked as `{ results: [], total: 0 }`. Assert the adapter's output carries `lookup_failed: true`,
    that the gate's output `action` is `"skip"` (NOT `"create"`), and that the diagnostic distinguishes
    zero-results from an error. Add the errored-response variant for at least one branch. This is the
    compiled-chain proof of SC-6 sitting on top of the unit-tier proof, because the create->skip override
    lives in the gate WRAPPER, not in the frozen `enrichmentGate.js` module the unit tier can reach.

    (5) INTEGRATION TIER — THE TRUE LIVE SHAPE. Add one case driving a PLAIN HubSpot event array body
    (no envelope, no `providers` key) — the literal shape a private-app subscription posts. Assert
    `providers_requested` is empty and every `provider_enabled` flag is false (the documented safe default),
    AND that identity resolution still succeeds through the fetch lane. This case is why the phase matters:
    it is the only payload shape a real HubSpot webhook can produce.

    (6) Run the two node test files plus the whole node suite. If anything goes red for a reason that
    implicates 16.4-01's production code rather than these tests, STOP and report — do not edit
    n8n/code/adaptFetchById.js or the builder from this plan.

    Commit with `test(16.4-02): unit + integration tiers for the fetch-by-objectId lane`.
  </action>
  <verify>
    <automated>node --test tests/n8n/adaptFetchById.test.mjs tests/n8n/bareEventChainFlow.test.mjs && node --test tests/n8n/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/n8n/adaptFetchById.test.mjs` passes and covers all four `adaptFetchByIdResult` outcome classes (missing/errored, malformed, zero-results, match) and all five `backfillIdentityKeys` cases in the behavior block, including the fill-only-blanks non-overwrite case.
    - The errored-case assertion checks that the error TEXT is present in the diagnostic, and the zero-results assertion checks the diagnostic differs from the errored one — the two live failure modes are distinguishable.
    - `node --test tests/n8n/bareEventChainFlow.test.mjs` passes with, per branch: a row-flow case asserting `object_id`, `object_type`, `provider_enabled` and a NON-EMPTY `providers_requested` present after the fetch hop; a caller-envelope back-compat case that never executes a fetch node and whose resulting `identity_keys` matches the pre-16.4 derivation; and a zero-results case whose gate output `action` is exactly `"skip"`.
    - At least one test drives a PLAIN event-array body (no envelope) and asserts zero providers enabled plus a successfully resolved identity key.
    - `node --test tests/n8n/*.test.mjs` is fully green with zero regressions against the baseline recorded in 16.4-01.
    - `git status` shows only the two test files changed by this task — zero production files.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions; delete to revert.</reversibility>
  <done>Every failure surface, the row's survival across the new hop, the untouched caller-envelope lane, and the true live plain-array payload shape are all asserted by running tests over the pure module and the shipped compiled node bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Topology + credential guards in pytest, the Track-B live checkpoint, and the final suite/determinism sweep</name>
  <files>tests/test_fetch_by_id_topology.py</files>
  <read_first>
    - tests/test_cloud_write_path.py:29-49 — `_load()`, `_node()` and the `_reachable_from()` BFS helper; the style to mirror. Also :75-85, the pinned `Route By Object Type` edge assertion this task adds a belt-and-braces duplicate of (that test must ALSO keep passing unmodified).
    - tests/test_cloud_companies_branch.py:41-62 — COMPANY_BRANCH_NODES and its reachability assertion, for the naming/structure convention.
    - scripts/deploy_n8n_workflows.py:44-78 (NODE_CREDENTIAL_MAP) and `bind_credentials()` — note again that unmapped nodes are skipped SILENTLY; that silence is what the generic guard converts into a test failure.
    - tests/test_deploy_n8n_workflows.py:355-365 — the existing per-category "is it registered" assertion style to generalize from.
    - .planning/phases/16.4-fetch-by-objectid/16.4-RESEARCH.md — the Assumptions Log (A1: `hs_object_id` filterability, MEDIUM confidence, corroborated by practitioner usage but not by HubSpot's own Search guide nor against portal 22617666) and Open Question 2, which is the material for the Track-B checkpoint written below.
    - .planning/STATE.md "Blockers/Concerns" — the house format for recording a found-not-fixed item, which the unmapped-HubSpot-nodes finding follows.
  </read_first>
  <action>
    (1) TOPOLOGY TIER. Create tests/test_fetch_by_id_topology.py, loading the committed
    n8n/wf_enrichment_cloud.json and asserting, parameterized over the two branches where the shapes are
    symmetric:
      - both gates exist, are `n8n-nodes-base.if`, and their true/false lanes target the fetch node and the
        existing search node respectively;
      - each identity builder's single outgoing edge targets its gate — i.e. the gate is immediately
        downstream of the builder, which is the placement decision, pinned;
      - both fetch nodes are `n8n-nodes-base.hubspot`, typeVersion 2.1, `operation == "search"`, the correct
        `resource`, `onError == "continueRegularOutput"`, with exactly one filter on `hs_object_id`/`EQ`
        whose value expression names the branch's own identity builder;
      - both adapters are `n8n-nodes-base.code` whose single outgoing edge targets the branch's existing
        gate node, so both lanes converge there;
      - the contacts fetch property CSV contains `company` and `lv_linkedin_url`; the companies fetch CSV
        is identical to the companies search CSV;
      - EVERY `n8n-nodes-base.hubspot` node in the workflow has `parameters.operation` in
        `{"search", "create", "update"}` — the mechanical guard against the legacy single-record retrieval
        operation whose response shape would silently corrupt every field it touched;
      - the comment-stripped jsCode of each adapter (drop lines whose trimmed form starts with `//`) reads
        BOTH its identity builder and its fetch node by name, and contains ZERO occurrences of the bare
        current-item token — the bd682a2 guard, stated structurally;
      - belt-and-braces: `Route By Object Type`'s true/false edges still target `Build Company Identity` /
        `Build Identity` exactly. This duplicates an assertion in test_cloud_write_path.py deliberately —
        that file is the pinned original and must keep passing unmodified; this copy makes the regression
        visible from the new phase's own test file too.

    (2) GENERIC CREDENTIAL GUARD, scoped. In the same file, assert that every `n8n-nodes-base.hubspot` node
    in n8n/wf_enrichment_cloud.json is a key in `deploy_n8n_workflows.NODE_CREDENTIAL_MAP` and maps to the
    `LV HubSpot` / `hubspotAppToken` pair. Import the module the way tests/test_deploy_n8n_workflows.py
    already does. Scope this to the enrichment cloud workflow ONLY, and say why in the docstring:
    wf_scheduled_maintenance_cloud.json currently carries 9 unmapped HubSpot nodes and
    wf_contact_ingest_cloud.json 1 — a pre-existing gap outside this phase's fence. Widening the guard now
    would go red on work this phase did not do.

    (3) RECORD THE FOUND-NOT-FIXED FINDING. In the SUMMARY (and as a STATE.md Blockers/Concerns entry
    following the house format), record those 10 unmapped HubSpot nodes by workflow and by name, with the
    consequence: `bind_credentials()` skips unmapped nodes silently, so any of them deployed today would be
    unbound and 401 at runtime. Do not fix them — record them.

    (4) THE TRACK-B LIVE CHECKPOINT. Add a `## Track B live checkpoint` section to the SUMMARY recording,
    explicitly and prominently, that the entire design rests on ONE unproven external fact: that HubSpot's
    CRM v3 Search API accepts `hs_object_id` as a filterable property with the `EQ` operator in portal
    22617666. It is corroborated by practitioner usage and by this repo's own nodes already REQUESTING
    `hs_object_id` as a returned property, but has never been proven as a FILTER property against this
    portal, and this phase's fence forbids proving it. The section must carry:
      - the exact single `curl` an operator runs against `/crm/v3/objects/contacts/search` with a
        `filterGroups` body filtering `hs_object_id EQ <a known test contact id>` to settle it in one call,
        together with the pass condition (a 200 whose `results` contains that id) and the fail conditions
        (a 400 naming the property, or a 200 with zero results for an id known to exist);
      - the diagnostic surface if it fails in flight instead: `Adapt (Company) Fetch By Id`'s
        `fetch_diagnostic` distinguishes an errored response (carrying the error text) from a genuine
        zero-result response, visible per-node in n8n's execution view, and a systemic failure presents as
        every bare event skipping rather than as silent wrong data;
      - the bounded fallback if it does fail: replace the two fetch nodes with credential-bound raw HTTP
        GETs to `/crm/v3/objects/{type}/{id}?properties=...` — the true v3 single-object endpoint, which
        DOES support a `properties` querystring and DOES return flat values. Note that this changes only
        the two nodes' type/parameters: the adapter already handles the single-object response shape, so
        `adaptFetchById.js`, both gates, both wrappers and every downstream consumer are unaffected.
      - Also carry forward the still-open Track-B items this phase does not touch: the
        `N8N_ENRICHMENT_WEBHOOK_SECRET` credential gap and the Lusha Company GET/POST mismatch.

    (5) FINAL SWEEP. Run the full offline suite both ways and record the EXACT counts against the baseline
    recorded in 16.4-01's precondition; regressions must be zero and the deltas must be attributable to the
    tests added in 16.4-01 and this plan. Run `.venv/bin/python scripts/build_cloud_workflows.py` twice
    more and confirm `git status` stays clean under `n8n/`. Confirm no test in the suite made a live call
    or read a credential (the suite runs with no `HUBSPOT_*`/`N8N_*` env set; state that you verified it
    rather than assuming it).

    Commit with `test(16.4-02): fetch-by-objectId topology + credential guards, Track-B checkpoint recorded`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs && .venv/bin/python scripts/build_cloud_workflows.py && git diff --quiet -- n8n/ && echo "DETERMINISTIC REBUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_fetch_by_id_topology.py` passes, covering both branches for: gate existence and true/false targets, gate-immediately-downstream-of-builder, fetch node type/typeVersion/resource/operation/onError/filter, adapter convergence on the existing gate, the two property-CSV assertions, the allowed-operations guard over all HubSpot nodes, the comment-stripped node-name-recovery guard, and the Route-By-Object-Type belt-and-braces assertion.
    - The generic credential guard asserts every HubSpot node in n8n/wf_enrichment_cloud.json is in NODE_CREDENTIAL_MAP bound to `LV HubSpot`/`hubspotAppToken`, and its docstring states why it is scoped to that one workflow.
    - `.venv/bin/python -m pytest -q` is FULLY green — including tests/test_cloud_write_path.py, tests/test_cloud_contacts_branch.py, tests/test_cloud_companies_branch.py, tests/test_architecture_guard.py, tests/test_deploy_n8n_workflows.py and tests/test_companies_factory_frozen.py — all UNMODIFIED, and tests/fixtures/companies_jscode_frozen.json unchanged in `git status`.
    - `node --test tests/n8n/*.test.mjs` is fully green. Recorded pytest and node counts show zero regressions vs the 16.4-01 baseline, with every delta attributable to a named new test file.
    - Two consecutive builder runs leave `git status` clean under `n8n/`.
    - The SUMMARY carries a `## Track B live checkpoint` section containing the verbatim verification `curl`, its pass and fail conditions, the in-flight diagnostic surface, and the bounded fallback with an explicit statement of what the fallback does and does not change.
    - The SUMMARY and a STATE.md Blockers/Concerns entry record the 10 pre-existing unmapped HubSpot nodes by workflow and name, flagged found-not-fixed with their consequence.
    - `git status` shows this task changed only tests/test_fetch_by_id_topology.py plus the planning documents — zero production files.
  </acceptance_criteria>
  <reversibility rating="reversible">One new pytest file plus documentation; delete to revert.</reversibility>
  <done>The lane's topology, credential binding and failure semantics are pinned by tests that fail loudly if any of them regress; the one unproven external dependency is recorded as an actionable Track-B checkpoint with a bounded fallback; and the full offline suite is green with a deterministic rebuild and zero live calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed workflow jsCode -> `new Function` in the test harness | the tests execute the repo's OWN committed node bodies over a fixed in-repo node list, as researchChainRowFlow.test.mjs already does; no external or untrusted input is interpolated into a function body |
| built workflow JSON -> NODE_CREDENTIAL_MAP -> bind_credentials() | an unmapped node is skipped silently; the generic guard is what converts that silence into a test failure |
| this phase's assertions -> HubSpot's actual Search API behavior | the `hs_object_id` filterability assumption cannot be settled offline; the boundary is crossed only by the recorded Track-B checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.4-07 | Tampering (vacuous test) | a row-flow assertion that passes because the key exists having been re-defaulted, not because the row survived — e.g. `providers_requested` present but silently reset to `[]` | high | mitigate | Task 1 asserts `providers_requested` equals the NON-EMPTY array the envelope requested, and the back-compat case asserts the full pre-16.4 `identity_keys` derivation rather than mere presence |
| T-16.4-08 | Tampering (green-by-editing) | a genuine 16.4-01 defect surfaced by these tests gets absorbed by weakening the test instead of fixing the source | high | mitigate | This plan is TEST-ONLY by frontmatter prohibition; a red implicating production code is a STOP-and-report condition, and the three pinned pre-existing test files are explicitly off-limits to edit |
| T-16.4-09 | Repudiation (buried assumption) | the MEDIUM-confidence `hs_object_id` filterability dependency stays in a research appendix and nobody acts on it before the first live run | medium | mitigate | Task 2 (4) makes it a first-class SUMMARY section with a one-call verification command, explicit pass/fail conditions, an in-flight diagnostic surface, and a bounded fallback whose blast radius is stated |
| T-16.4-10 | Spoofing (unbound deploy recurrence) | a future phase adds a HubSpot node and forgets NODE_CREDENTIAL_MAP, repeating the gpt #9 unbound-deploy failure | medium | mitigate | Task 2 (2) adds a GENERIC guard over all HubSpot nodes in the enrichment cloud workflow rather than another hand-maintained name list |
| T-16.4-11 | Information disclosure | a test accidentally reading a real credential or making a live call | low | mitigate | No test touches env or network; Task 2 (5) requires explicitly verifying the suite ran with no HubSpot/n8n env set rather than assuming it |
| T-16.4-SC | Tampering (supply chain) | npm/pip installs | low | accept | Zero packages added — node:test and pytest are already present. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- UNIT: every `adaptFetchByIdResult` outcome class and every `backfillIdentityKeys` case asserted against the pure module, including fill-only-blanks and the two distinguishable failure diagnostics.
- INTEGRATION: per branch, the row survives the fetch hop with `object_id`/`object_type`/`provider_enabled`/non-empty `providers_requested`; the caller-envelope lane resolves identity the old way without touching a fetch node; a zero-result fetch degrades to `action == "skip"`; a plain event-array body yields zero providers enabled and still resolves an identity key.
- TOPOLOGY: gate placement, both lanes' targets, fetch node shapes and filters, adapter convergence, property CSVs, allowed HubSpot operations, comment-stripped node-name recovery, and the Route-By-Object-Type belt-and-braces regression.
- CREDENTIALS: every HubSpot node in the enrichment cloud workflow is registered and bound to `LV HubSpot`; the 10 pre-existing unmapped nodes elsewhere are recorded, not fixed.
- TRACK B: the `hs_object_id` filterability checkpoint is recorded with its verification command, pass/fail conditions, in-flight diagnostic and bounded fallback.
- SUITE: pytest and node both fully green with zero regressions vs the 16.4-01 baseline; two consecutive builder runs leave n8n/ clean; no live call, no credential read, no deploy; frozen companies fixture untouched.
</verification>

<success_criteria>
Plan complete when the caller-envelope path is proven still to resolve identity the old way including the
16.1 provider selection (SC-3); the row's survival across the new HTTP hop is proven by a bd682a2-style
item-flow regression over compiled node bodies (SC-4); all three test tiers exist and pass — unit against
the pure adapter, integration over compiled node bodies, and 16.4-01's end-to-end functional tier (SC-5);
every fetch failure surface is proven to degrade to skip with no create and no clobber, at both the unit and
compiled-chain levels (SC-6); and the full offline suite is green with zero regressions, a deterministic
builder, no live calls, and the one unproven external dependency recorded as an actionable Track-B
checkpoint (SC-7).
</success_criteria>

<source_coverage_audit>
| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP 16.4 goal — the lane must be trustworthy enough to unblock live Track-B verification | Task 1 (all), Task 2 (1)(2)(4) |
| REQ | SC-3 (caller-envelope back-compat incl. 16.1 providers resolution) | Task 1 (3) |
| REQ | SC-4 (row-flow integrity, bd682a2 style, node-name recovery) | Task 1 (2), Task 2 (1) structural guard |
| REQ | SC-5 (three tiers) | Task 1 (1) unit + (2)(3)(4)(5) integration; e2e delivered in 16.4-01 |
| REQ | SC-6 (fetch failure degrades safely, no create/no clobber) | Task 1 (1)(4) |
| REQ | SC-7 (suite green, deterministic builder, no live calls) | Task 2 (5) |
| REQ | SC-1/SC-2 (regression protection for the lane 16.4-01 built) | Task 2 (1) |
| RESEARCH | test strategy: unit `adaptFetchById.test.mjs`, integration row-flow, pytest topology mirroring test_cloud_write_path.py | Task 1 (1)(2), Task 2 (1) |
| RESEARCH | Pitfall 1 (the legacy single-record retrieval operation is unusable) -> a mechanical guard, not just prose | Task 2 (1) allowed-operations assertion |
| RESEARCH | Pitfall 2 (missing identity backfill silently nulls the provider waterfall) -> assert non-null identity at the gate | Task 1 (2), 16.4-01 e2e |
| RESEARCH | Assumption A1 / Open Question 2 (`hs_object_id` filterability, MEDIUM) -> Track-B checkpoint, not a blocker | Task 2 (4) |
| RESEARCH | belt-and-braces regression assertion on Route By Object Type's exact edges | Task 2 (1) |
| CONTEXT | Reasoned Default 5 (three tiers + a bd682a2-style row-flow regression) | Task 1 (1)(2) |
| CONTEXT | back-compat is mandatory — the whole existing suite drives the envelope path | Task 1 (3) |
| CONTEXT | frozen companies fixture must not move | Task 2 acceptance |
| CONTEXT | Scope Fence (offline only; no fan-out/scoring/merge/research change; no kill switches; no merge_policy.py fix) | frontmatter prohibitions |
| USER | three tiers explicitly required | Task 1 (1)-(5), Task 2 (1) |
| USER | row-flow regression naming the four fields that must survive | Task 1 (2) |
| USER | back-compat asserted, not assumed | Task 1 (3) |
| USER | surface the MEDIUM-confidence dependency: Track-B checkpoint AND a diagnosable live failure | Task 2 (4); the diagnostic itself shipped in 16.4-01 |
| USER | frozen-node movement is STOP-and-report | frontmatter prohibitions + Task 2 acceptance |
| USER | env gotchas (`.venv/bin/python -m pytest`, `node --test tests/n8n/*.test.mjs` glob form; deterministic rebuild) | every `<verify>` block; Task 2 (5) |
</source_coverage_audit>

<output>
Create `.planning/phases/16.4-fetch-by-objectid/16.4-02-SUMMARY.md` when done. It MUST carry: the final
pytest and node counts against the 16.4-01 baseline with every delta attributed; the `## Track B live
checkpoint` section in full (verification curl, pass/fail conditions, in-flight diagnostic, bounded
fallback, plus the carried-forward webhook-secret and Lusha GET/POST items); the found-not-fixed record of
the 10 unmapped HubSpot nodes by workflow and name; the deterministic-rebuild evidence; and confirmation
that the frozen companies fixture and the three pinned branch/write-path test files were never modified.
</output>
