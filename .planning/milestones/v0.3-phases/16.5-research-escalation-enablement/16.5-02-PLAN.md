---
phase: 16.5-research-escalation-enablement
plan: 02
type: execute
wave: 2
depends_on: ["16.5-01"]
autonomous: false
files_modified:
  - .planning/phases/16.5-research-escalation-enablement/16.5-02-SUMMARY.md
requirements:
  - "SC-7 (LIVE: one enabled run fires against contact 201, the research-then-judge lane executes, the row survives every HTTP hop to Merge Winners, zero HubSpot writes occur, and the run is inspected node-by-node rather than judged by its response body)"
  - "SC-4 (Criterion 5 confirmed on the DEPLOYED build by API read-back, not only on the in-memory transform)"
  - "SC-5 (cost caps and HubSpot write safety confirmed in force on the DEPLOYED build and in the executed run)"

must_haves:
  truths:
    - "The workflow ACTUALLY RUNNING on n8n Cloud is read back from the API and confirmed to declare both flags enabled, the two cost caps unchanged, and both HubSpot write-safety constants still disabled — BEFORE anything is fired. A deploy's own HTTP 200 is not evidence of what was deployed; today's canary returned a plausible 200 while a fetch was silently 400-ing, and nine live-only bugs this session all shared the shape of an offline-green artifact failing on first real API contact."
    - "The live run fires the TRUE live webhook shape — a bare HubSpot event array, zero providers — because that is the only envelope under which the contacts research gate can fire. With all three providers enabled the waterfall supplies both jobtitle and seniority winners, the provider_gap predicate is false, and research never runs; canary step 3 passed with all providers precisely because the research lane stayed dark. This is a finding from the gate's own predicate, confirmed against contact 201's real properties in Task 1 before a cent is spent."
    - "The run is judged NODE BY NODE from the execution API's `runData`, never from the webhook's response body. The webhook uses responseMode `responseNode`, so a well-formed 200 says nothing about whether any lane executed. Every HTTP node's output item is inspected for an error payload, because `onError: continueRegularOutput` converts a failed call into a normal-looking item that flows downstream."
    - "The bd682a2 row-survival property is verified on the live path in its contacts mirror: `Validate Contact Research`'s output still carries `existingRecord`, `scored`, `identity_keys` and `object_id` after the research HTTP hop, `Apply Contact Judge Verdict`'s output still carries them after the judge HTTP hop, and `Merge Winners` emits a NON-NULL merge. A null merge there is the exact symptom the fix was written to prevent."
    - "ZERO HubSpot writes occur, proven three ways and not one: no create/update node appears in the execution's `runData` at all; `Decide Action` reports the write-blocked outcome; and contact 201's `lastmodifieddate` is byte-identical before and after the run."
    - "The deployment is RESTORED to the disabled build before this plan ends, unconditionally — including after a failed or abandoned run — and the restoration is confirmed by API read-back, not by the redeploy's exit code. `LV Enrichment` is ACTIVE; leaving it armed means every subsequent event spends on LLM calls."
    - "A first-run failure is EXPECTED PATH, not an exception. Nine live-only bugs surfaced today in exactly this pattern. The plan carries an explicit diagnostic protocol for the case n8n swallows a node error, and iterating on a discovered bug is the normal course of this task, not a reason to abandon it."
  artifacts:
    - "the deployed `LV Enrichment` workflow, enabled in flight and then restored to disabled — no repo artifact changes"
    - "the captured execution JSON in the session scratchpad, and the node-by-node findings transcribed into 16.5-02-SUMMARY.md"
  key_links:
    - "`ENABLE_BAKED_FLAGS` on the deploy command -> `enable_baked_flags()` -> PUT -> the LIVE workflow's `Contact Research Trigger Gate` jsCode declaring the flag enabled, CONFIRMED by GET read-back"
    - "the bare event array -> `Parse HubSpot Event` (zero providers) -> `Build Identity` -> `IF Bare Event` -> `HubSpot Fetch By Id` -> `Adapt Fetch By Id` -> `Enrichment Gate` -> `Normalize + Score` -> `Contact Research Trigger Gate` (research_needed true) -> `Contact Web Research` -> `Validate Contact Research` -> `Contact Judge Gate` -> `Contact Judge Call` -> `Apply Contact Judge Verdict` -> `Merge Winners` (non-null merge) -> `Decide Action` (write blocked)"
    - "`GET /api/v1/executions/{id}?includeData=true` -> `data.resultData.runData[<node name>]` -> per-node output items — the ONLY acceptable evidence for every claim in this plan"
    - "the plain redeploy (no `ENABLE_BAKED_FLAGS`) -> GET read-back showing the flags disabled -> production disarmed"
  prohibitions:
    - "NO source file edit in this plan except the SUMMARY. If a live bug is found, STOP, record it fully, restore the deployment to disabled, and report — the fix is a new plan or a quick task, not an in-flight patch during a live session."
    - "NO enabling of `ALLOW_HUBSPOT_RECORD_WRITES` / `ALLOW_HUBSPOT_CREATE`, NO population of the test-record allowlists, NO change to the cost caps. Writes stay off for this run."
    - "NO activation or deactivation of `LV Scheduled Maintenance` or `LV Contact Ingest`."
    - "NO judging the run by the webhook's HTTP response body or status code."
    - "NO echoing of any secret value into a command's stdout, a file, or the SUMMARY. Source `.env` into the subshell and reference variables by name only."
    - "NO more than the ONE contacts run this plan authorises without returning to the operator — the cost caps bound a single execution, not a retry loop."
    - "NO use of `gsd-tools query state.advance-plan` or `state.update-progress` (known miscounting bug in this repo)."
  user_setup:
    - service: n8n-cloud
      why: "the enabled deploy, the webhook fire, and every execution inspection require the live instance"
      env_vars:
        - name: N8N_URL
          source: "already present in ./.env"
        - name: N8N_API_KEY
          source: "already present in ./.env — MUST be the key issued under Robert, not Alex (project-ownership gotcha, STATE.md)"
        - name: N8N_ENRICHMENT_WEBHOOK_SECRET
          source: "already present in ./.env — sent as the X-Enrichment-Secret header"
    - service: hubspot
      why: "read-only before/after snapshot of contact 201 to prove zero writes"
      env_vars:
        - name: HUBSPOT_PRIVATE_APP_TOKEN
          source: "already present in ./.env"
---

<objective>
Fire the last untested path in the system. With Plan 01's overlay, deploy a research- and
escalation-enabled `LV Enrichment`, confirm by API read-back that the enabled build is what is
actually running, fire ONE live run against contact 201, and verify node-by-node from the execution
API that the research-then-judge lane executed, that the row survived both HTTP hops to a non-null
merge at `Merge Winners`, and that zero HubSpot writes occurred — then restore the deployment to
disabled.

Purpose: ROADMAP criterion 7. The web-research lane has never executed live. Canary steps 2 and 3
could not reach it because the research gate is baked off; step 3 passed with all three providers
precisely BECAUSE the research lane stayed dark.

Output: a verified live execution, a node-by-node evidence record, and a disarmed deployment.
</objective>

<scope_note>
HONEST SCOPE STATEMENT, because the phase's framing conflates two things and shipping the
conflation would be exactly the false-success this phase exists to prevent.

`bd682a2` is titled "recover row across HTTP hops in **companies** research/judge chain". Its fix
lands in `Validate Research Output` and `Apply Judge Verdict`, and its symptom is `Merge Company`
returning a null merge. Contact 201 drives the CONTACTS branch, whose row-recovery is the 16.2-02
mirror of that fix — same bug class, same idiom, same `$('Build ... Request')` recovery, different
nodes.

So: THIS plan verifies the CONTACTS mirror live at `Merge Winners`. That is ROADMAP criterion 7 in
full, and it is the first live execution of any research-then-judge lane in this system. The LITERAL
`bd682a2` — `Merge Company` on the companies branch — is verified by Plan 03, which fires the same
enabled build against a real company record. Plan 03 goes one step beyond criterion 7 deliberately;
if the operator chooses to stop after this plan, the phase's own record must say that the companies
lane remains live-unverified rather than implying otherwise.
</scope_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.5-research-escalation-enablement/16.5-CONTEXT.md
@.planning/phases/16.5-research-escalation-enablement/16.5-01-SUMMARY.md
</context>

<live_command_conventions>
Read once, apply to every command in this plan.

  - The live environment variables are NOT exported into this shell and `.env` cannot be read
    directly. Every live command runs in a subshell that sources it: prefix with
    `set -a; . ./.env; set +a;` and reference variables by name only. NEVER echo, print, cat, or
    write a secret value anywhere — not to stdout, not to a file, not to the SUMMARY.
  - The n8n workflow under test is `LV Enrichment`, id `950HPb7a1GgSAIyZ`, project `T9IPFKpIn2aUYYj3`,
    and it is ACTIVE. The other two workflows stay inactive and untouched.
  - n8n Public API calls carry the header `X-N8N-API-KEY` with `$N8N_API_KEY`.
  - The webhook is `POST $N8N_URL/webhook/hubspot/enrichment/event` with header
    `X-Enrichment-Secret: $N8N_ENRICHMENT_WEBHOOK_SECRET` and a JSON body.
  - HubSpot reads use `Authorization: Bearer $HUBSPOT_PRIVATE_APP_TOKEN` against
    `https://api.hubapi.com`.
  - Write every captured API response to the session scratchpad directory as a file, then analyse the
    file. Execution payloads are large; re-fetching to re-read a field is both slow and a chance to
    look at a different execution than the one you reasoned about.
  - Read back the DEPLOYED workflow with `GET $N8N_URL/api/v1/workflows/950HPb7a1GgSAIyZ`. List
    executions with `GET $N8N_URL/api/v1/executions?workflowId=950HPb7a1GgSAIyZ&limit=5`. Fetch one
    with `GET $N8N_URL/api/v1/executions/<id>?includeData=true`; its per-node record lives at
    `data.resultData.runData[<node name>]`.
</live_command_conventions>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — prove against the real portal that the research gate will fire, and record the zero-write baseline</name>
  <files>(no repo file modified; captured JSON goes to the session scratchpad)</files>
  <precondition>Plan 16.5-01 is committed and its full offline suite is green — `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` both at or above the baseline recorded in 16.5-01's Task 1 precondition, with zero regressions. `16.5-01-SUMMARY.md` exists and states the offline oracle's prediction for the contacts branch. The working tree is clean. Nothing in this task is authorised to run if the offline suite is red.</precondition>
  <read_first>
    - `.planning/phases/16.5-research-escalation-enablement/16.5-01-SUMMARY.md` — specifically the recorded PREDICTION for the contacts branch (does the research gate fire, on what record shape, does the judge escalate) and the exact operator-flag command form. This task's job is to check that prediction against the real portal before any money is spent.
    - `tests/n8n/enabledResearchLaneFlow.test.mjs` — the contacts chain node list and the mocked fetch record shape it asserts against. The real record must match the shape the prediction depends on, or the prediction is void.
    - The `Contact Research Trigger Gate` node body in `n8n/wf_enrichment_cloud.json` — the `needsResearch(existingRecord, scored)` predicate: a provider_gap fires when a target field is blank in BOTH `existingRecord` and `scored.winners`; the stale branch needs `jobtitle` present AND `lv_jobtitle_verified_at` present and older than the TTL.
    - `.planning/STATE.md` — the canary step 2 and step 3 records (what contact 201 looked like, which nodes ran, what was confirmed unchanged), and the nine live-only bugs.
  </read_first>
  <action>
    Zero writes, zero spend, zero deploy in this task. It exists so that the paid run in Task 3 is a
    confirmation rather than a gamble.

    (1) SNAPSHOT CONTACT 201, READ ONLY. Fetch contact 201 from HubSpot requesting exactly the
    properties `Adapt Fetch By Id`'s own property list requests plus `lastmodifieddate`. Write the
    response to the scratchpad. Record, in the SUMMARY, the value or blankness of each of: `jobtitle`,
    `seniority`, `lv_jobtitle_verified_at`, `lv_mobilephone_verified_at`,
    `lv_contact_enrichment_provenance`, and `lastmodifieddate`. That `lastmodifieddate` is the
    zero-write oracle for Task 3 — capture it verbatim.

    (2) EVALUATE THE GATE PREDICATE BY HAND against those real values, with zero providers enabled
    (which a bare event array produces). For each of the two contact research fields, state whether it
    is blank in the record; then state whether `needsResearch` returns true and through which branch.
    Show the reasoning explicitly rather than asserting a conclusion — this single step is what
    decides whether the paid run exercises anything at all.

    (3) THE GO / NO-GO, and take it seriously.
      - If the predicate fires: state it, name the branch, and proceed. This is the expected case
        given the record shape observed at canary and asserted in the offline oracle.
      - If the predicate does NOT fire on the bare-event, zero-provider envelope: STOP. Do not deploy,
        do not fire, do not "try it and see" — a run that cannot reach the research node proves
        nothing and still arms production. Report the exact property values that make the predicate
        false, and propose the smallest envelope or record change that would make it true, for the
        operator to approve. Note that any change to contact 201's properties is a HubSpot WRITE and
        is outside this phase's fence; a different existing test contact is the cheaper answer.

    (4) BASELINE THE COST SURFACE. Record the current provider credit balances (Lusha and ZoomInfo;
    Apollo's usage endpoint 403s on this non-master key, which is the known limitation, so record
    "unavailable — known 403" rather than omitting it). A zero-provider run must leave all of them
    unchanged, so this doubles as an independent check that the envelope really did disable the
    waterfall.

    (5) CONFIRM THE DEPLOYED BUILD IS CURRENTLY DISABLED. Read back the live workflow and record the
    literal each `ALLOW_WEB_RESEARCH` and `ALLOW_SONNET_ESCALATION` declaration currently carries. This
    is both the starting state and the target state Task 3's restore must return to, and reading it now
    proves the read-back method works before it is load-bearing.

    Commit nothing in this task; its output is the recorded pre-flight in the SUMMARY.
  </action>
  <verify>
    <human-check>The SUMMARY records contact 201's exact property values, the hand-evaluated gate predicate with its branch, an explicit GO or NO-GO, the verbatim `lastmodifieddate`, the provider credit baselines, and the currently-deployed literal for both flags — and the GO agrees with the offline oracle's prediction in 16.5-01-SUMMARY.md, or the disagreement is stated and explained.</human-check>
  </verify>
  <acceptance_criteria>
    - Contact 201's real property values are recorded verbatim, including `lastmodifieddate`, and the record was fetched read-only (a GET; no PATCH, POST or PUT was issued to `api.hubapi.com` in this task).
    - The `needsResearch` predicate is evaluated by hand against those real values with zero providers, and the result plus the firing branch is stated.
    - An explicit GO or NO-GO is recorded. A NO-GO halts the plan with no deploy and no fire.
    - The recorded outcome is compared against the offline oracle's prediction from `16.5-01-SUMMARY.md`, and any disagreement is called out rather than reconciled silently.
    - Lusha and ZoomInfo credit balances are recorded; Apollo is recorded as unavailable with its known cause.
    - The currently-deployed `LV Enrichment` is read back and both flags are confirmed to carry the disabled literal.
    - No secret value appears in any command output, scratchpad file, or the SUMMARY.
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only. Nothing changed anywhere.</reversibility>
  <done>The real portal has confirmed or refuted the offline prediction, the zero-write and zero-spend baselines are captured, and the read-back method is proven — so the paid run can be judged against something.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Arm production: deploy a research- and escalation-enabled `LV Enrichment` to the live, ACTIVE n8n Cloud workflow, and fire one run against contact 201.</decision>
  <context>
This is the first time LLM-spending paths will be enabled on a live, active workflow in this project,
so it wants a deliberate yes rather than an implied one.

WHAT IS BEING ARMED. Two baked constants flip from disabled to enabled, in flight, in the deployed
copy only: `ALLOW_WEB_RESEARCH` (a web-search-enabled Anthropic call, per record) and
`ALLOW_SONNET_ESCALATION` (a Sonnet arbitration, only when the judge gate's conflict predicate fires).
Nothing else changes — Plan 01 asserts by line diff that these are the only differing lines, and that
the cost caps and both HubSpot write-safety constants are untouched on the enabled build.

WHAT IT COSTS FOR THIS RUN. One contact, one research call, at most one Sonnet arbitration. Cents.
The per-run caps (10 research, 10 escalations) remain in force and are not overlayable.

THE REAL EXPOSURE IS NOT THIS RUN, IT IS THE WINDOW. `LV Enrichment` is ACTIVE. While the enabled
build is deployed, ANY event arriving at the webhook — not just ours — can spend. Every event to date
has been a manual curl and no HubSpot webhook subscription is known to point at this URL, but that is
an assumption worth stating out loud rather than relying on. The window is bounded two ways: the
per-run caps, and Task 3's restore, which redeploys the disabled build and confirms it by read-back
before this plan ends.

WHAT STAYS OFF. HubSpot record writes and creates stay disabled, the test-record allowlists stay
empty (and an empty allowlist denies everything), and the other two workflows stay inactive.

EXPECT A FAILURE. Nine live-only bugs surfaced in this system today, every one of them code that
passed a full offline suite, deployed clean, and failed on first real contact with an external API.
A first-run failure here is the expected path and is exactly the value this run buys.
  </context>
  <options>
    <option id="arm-both">
      <name>Arm both flags (recommended)</name>
      <pros>Exercises the full research-then-judge lane in one run — the escalation path only exists to adjudicate the conflict that research produces, and contact 201's live provider disagreement on jobtitle is precisely the conflict the judge was built for. Verifying research without escalation would leave half the lane untested and cost a second armed window to reach.</pros>
      <cons>Two spending paths armed instead of one; a Sonnet arbitration cost on top of the research call. Both are capped and both are cents at one record.</cons>
    </option>
    <option id="arm-research-only">
      <name>Arm research only</name>
      <pros>Smallest possible cost surface. The overlay makes the flags independently selectable precisely so this is available.</pros>
      <cons>The judge gate's cost cap is applied with a zero budget when escalation is off, so any row that trips a conflict trigger is demoted by the unadjudicated fail-safe and the judge nodes never execute. The escalation half of the lane stays untested and needs its own armed window later.</cons>
    </option>
    <option id="abort">
      <name>Do not arm; stop after Plan 01</name>
      <pros>Zero spend, zero armed window. Plan 01's mechanism and offline oracle stand on their own and are independently valuable.</pros>
      <cons>ROADMAP criterion 7 is not met; the research-then-judge lane remains the one path in this system that has never executed live, and the row-survival fix it contains remains verified only by inspection.</cons>
    </option>
  </options>
  <resume-signal>Select: arm-both, arm-research-only, or abort. If arming, confirm in the same message that no HubSpot webhook subscription is currently pointed at this n8n URL, or that an unbounded armed window for the duration of one run is acceptable.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy enabled, fire contact 201, inspect node-by-node, restore to disabled</name>
  <files>(no repo file modified; captured JSON goes to the session scratchpad)</files>
  <read_first>
    - `16.5-01-SUMMARY.md` — the exact operator-flag command form and the recorded rewrite counts per flag. The counts you observe in the live deploy's own stdout must match them; a different count means the artifact moved between planning and deploying.
    - Task 1's recorded pre-flight — the `lastmodifieddate`, the credit baselines, the currently-deployed disabled literals.
    - `tests/n8n/enabledResearchLaneFlow.test.mjs` — the contacts chain node list and its per-node assertions. The live inspection below is the same list of claims, read out of `runData` instead of a harness.
    - `.planning/STATE.md` — bug 6 in particular (a CSV-vs-array property shape that returned a plausible 200 while every search 400-ed), as the standing reminder of why a response body is not evidence.
  </read_first>
  <action>
    HALT PROTOCOL, read before starting. If you stop this task for ANY reason — a failure, a
    surprise, an ambiguity, running low on context, an operator interruption — your FIRST action
    before reporting is step (7), the restore. A halted task must never leave the active workflow
    armed. Step (7) is two commands and a read-back; it is never the thing to skip.

    (1) DEPLOY THE ENABLED BUILD. Run the deploy script with the write gate open and the operator flag
    naming the constants the checkpoint authorised (both, or research only). Capture its full stdout.
    The overlay's own printed plan must name each requested flag, a non-zero rewrite count, and
    `LV Enrichment` as the workflow rewritten; the counts must equal those recorded in
    `16.5-01-SUMMARY.md`. If the script REFUSES, that is the fail-closed guard doing its job — record
    the message verbatim, do not work around it, and stop.

    (2) READ BACK WHAT IS ACTUALLY RUNNING. This is a hard gate and the deploy's exit code does not
    substitute for it. GET the live workflow and assert against the response body, recording each
    finding verbatim:
      - every `ALLOW_WEB_RESEARCH` and `ALLOW_SONNET_ESCALATION` declaration carries the enabled
        literal (or, for research-only, escalation still carries the disabled literal);
      - every `MAX_WEB_RESEARCH_PER_RUN` and `MAX_SONNET_VALIDATIONS_PER_RUN` declaration carries the
        same value recorded in Task 1;
      - every `ALLOW_HUBSPOT_RECORD_WRITES` and `ALLOW_HUBSPOT_CREATE` declaration still carries the
        disabled literal, and both test-record allowlist constants are still empty;
      - the architecture guard's env-expression pattern finds nothing in the deployed body;
      - `active` is true. n8n may deactivate a workflow on update; if it did, re-activate it via the
        API before firing and record that you did. The deploy script must not gain activation logic —
        two existing tests pin that — so this is an operator action, not a code change.

    (3) FIRE ONE RUN. POST the TRUE live webhook shape to the enrichment webhook: a bare HubSpot event
    ARRAY of exactly one event carrying `objectId` 201, `objectType` "contact", a propertyChange
    `subscriptionType`, a `propertyName`, and an `occurredAt` — and NO `providers` key at any level.
    A plain array resolves to zero providers enabled, which is what makes the research gate reachable
    per Task 1's predicate evaluation, and it is the shape a genuine HubSpot private-app webhook
    actually sends.

    Record the HTTP status and the response body. Then set them aside. THE RESPONSE PROVES NOTHING:
    the webhook uses responseMode `responseNode`, so a well-formed 200 is returned regardless of what
    any lane did, and today's canary returned exactly such a 200 while a fetch was silently 400-ing.

    (4) FETCH THE EXECUTION. List the workflow's recent executions, take the newest, and fetch it with
    data included. Write it to the scratchpad. Record the execution id, its `status`, and its
    `stoppedAt`.

    (5) INSPECT NODE BY NODE. For each claim below, quote the actual value out of `runData` into the
    SUMMARY. A claim with no quoted evidence is not established.

      (a) THE LANE EXECUTED. `Contact Research Trigger Gate` is present in `runData`, and its output
          item has `research_needed` true with no `research_skip_reason`. If instead the skip reason
          names the disabled flag, the deployed build is not the enabled one and step (2) lied — go
          back to (2) rather than firing again.
      (b) THE RESEARCH CALL REALLY HAPPENED. `Contact Web Research` is present in `runData`, and its
          output item is a genuine Anthropic response rather than an error payload. Inspect BOTH the
          node record's own error field and the output item's json for an error key: with
          `onError: continueRegularOutput`, a failed call becomes a normal-looking item that flows
          downstream and every subsequent node continues as if nothing went wrong.
      (c) ROW SURVIVAL, FIRST HOP — the bd682a2 property in its contacts mirror. `Validate Contact
          Research`'s output item still carries `existingRecord`, `scored`, `identity_keys` and
          `object_id`. Quote each key's presence. Their loss is precisely the bug the fix prevents.
      (d) ESCALATION. `Contact Judge Gate`'s output `needs_judge` and `judge_reasons`. If escalation
          was armed and a reason fired, `Contact Judge Call` must be present in `runData` with a real
          response; if no reason fired, record that and say why from the record's values — a judge that
          legitimately had nothing to adjudicate is a valid outcome, a judge that was silently capped
          is not.
      (e) ROW SURVIVAL, SECOND HOP. `Apply Contact Judge Verdict`'s output still carries
          `existingRecord` and `scored` (only applicable if the judge lane ran).
      (f) THE MERGE IS REAL. `Merge Winners`'s output `merge` is NON-NULL and carries a patch. A null
          merge here is the exact symptom the row-recovery fix was written to prevent, and it is the
          single most important value in this entire phase.
      (g) ZERO WRITES, THREE WAYS. No create or update node name appears anywhere in `runData`;
          `Decide Action`'s output reports the write-blocked outcome; and a fresh read of contact 201
          returns a `lastmodifieddate` byte-identical to Task 1's.
      (h) COST STAYED BOUNDED. Exactly one research call and at most one escalation appear in the run.
          Lusha and ZoomInfo credit balances are unchanged from Task 1's baseline, confirming the
          zero-provider envelope really did bypass the waterfall.

    (6) IF SOMETHING FAILED — and expect it to. This is the expected path, not the exception.
      - Read the failing node's record in `runData`: both its own error field and its output item's
        json, since `onError: continueRegularOutput` puts the real failure in the item rather than
        failing the node.
      - If BOTH are empty or unhelpful and the true error is being swallowed, the documented remedy
        used successfully today is to temporarily clear `onError` on that ONE node via a targeted
        workflow update, re-fire, read the now-hard failure with its real upstream message, and then
        restore `onError` immediately. Treat that as a diagnostic excursion: record what you changed,
        change exactly one node, and put it back in the same session.
      - Then STOP. Record the finding in full — the node, the real error text, the request shape that
        produced it, and your read of the root cause — restore per (7), and report. Fixing it is a new
        plan or a quick task with its own tests; an in-flight patch during a live armed session is how
        an unreviewed change reaches production.

    (7) RESTORE — UNCONDITIONAL, and the last thing that happens regardless of how (1) through (6)
    went. Redeploy with the write gate open and the operator flag ABSENT, so the plain committed
    (disabled) build is pushed. Then read the live workflow back and confirm every `ALLOW_WEB_RESEARCH`
    and `ALLOW_SONNET_ESCALATION` declaration once again carries the disabled literal, and that
    `active` is still true (re-activating if the update cleared it). Record the read-back verbatim.
    The redeploy's exit code is not the evidence; the read-back is.

    Commit only the SUMMARY, with `docs(16.5-02): live enabled run against contact 201 — node-by-node evidence`.
  </action>
  <verify>
    <human-check>The SUMMARY carries, quoted from `runData`, each of: `Contact Research Trigger Gate` with `research_needed` true; a genuine `Contact Web Research` response; `Validate Contact Research` still carrying `existingRecord`/`scored`/`identity_keys`/`object_id`; `Contact Judge Gate`'s `needs_judge` and `judge_reasons`; `Merge Winners` with a non-null merge; `Decide Action` write-blocked; the absence of any create/update node from `runData`; contact 201's unchanged `lastmodifieddate`; and the post-restore read-back showing both flags disabled again on the live workflow.</human-check>
  </verify>
  <acceptance_criteria>
    - The deploy's own stdout named each armed flag with a non-zero rewrite count matching `16.5-01-SUMMARY.md`, and named `LV Enrichment` as the rewritten workflow.
    - An API read-back of the live workflow BEFORE firing confirmed: the armed flag(s) carrying the enabled literal; both cost caps carrying the values recorded in Task 1; both HubSpot write-safety constants carrying the disabled literal with empty test-record allowlists; zero matches for the architecture guard's env-expression pattern; and `active` true.
    - The fired body was a bare HubSpot event array of one contact event with no `providers` key at any level.
    - `runData` contains `Contact Research Trigger Gate` with output `research_needed` true and no skip reason.
    - `runData` contains `Contact Web Research` whose output item is a real Anthropic response, with both the node's error field and the item's error key inspected and quoted as absent.
    - `runData`'s `Validate Contact Research` output item carries `existingRecord`, `scored`, `identity_keys` and `object_id` — each quoted.
    - `runData`'s `Merge Winners` output carries a NON-NULL `merge` with a patch — quoted.
    - `Decide Action` reports the write-blocked outcome; no create or update node name appears anywhere in `runData`; and contact 201's `lastmodifieddate`, re-read after the run, is byte-identical to Task 1's recorded value.
    - Lusha and ZoomInfo credit balances after the run are identical to Task 1's baseline.
    - After the restore, an API read-back of the live workflow shows every `ALLOW_WEB_RESEARCH` and `ALLOW_SONNET_ESCALATION` declaration carrying the disabled literal, and `active` true. This criterion holds whether or not the run succeeded.
    - `git status --porcelain` shows no modified file other than the new SUMMARY; no source file, test or workflow JSON was edited in this plan.
    - No secret value appears in any command output, scratchpad file, or the SUMMARY.
  </acceptance_criteria>
  <reversibility rating="costly">The deploy itself is fully reversible by the restore in step (7), which is mandatory and read-back-verified. What is NOT reversible is the spend and the fact that an active production workflow was briefly armed — bounded by the per-run caps and by the restore, but genuinely one-way. The blocking checkpoint above is what authorises it.</reversibility>
  <done>The research-then-judge lane has executed live for the first time, every claim about it is backed by a quoted value from the execution API rather than a response code, contact 201 is provably unmodified, and the deployed workflow is disabled again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the deploy PUT -> the live ACTIVE `LV Enrichment` | the deployed artifact has no on-disk twin; only an API read-back establishes what is running |
| the public webhook -> `Parse HubSpot Event` | while armed, ANY caller reaching the webhook with the shared secret can trigger LLM spend, not just this plan's fire |
| `onError: continueRegularOutput` on every HTTP node -> downstream Code nodes | a failed external call becomes a normal-looking item; the run reports success while a lane silently did nothing |
| the webhook's responseNode reply -> the operator's judgement | a well-formed 200 is returned regardless of what executed; treating it as evidence is how today's silently-400-ing fetch looked healthy |
| the armed deployment -> time | exposure is a function of how long the enabled build stays deployed, not of how many runs are fired |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.5-07 | Repudiation (false success) | judging the run by the webhook's status or body, or by the deploy's exit code | high | mitigate | Every claim in Task 3 must quote a value out of `runData`; step (2) makes an API read-back a hard gate before firing; the plan states explicitly that the response proves nothing and why |
| T-16.5-08 | Tampering (swallowed failure) | an HTTP node failing under `onError: continueRegularOutput`, so the lane appears to run while doing nothing | high | mitigate | Task 3 (5)(b) inspects BOTH the node record's error field and the output item's json for every HTTP node; (6) carries the temporary single-node `onError`-clearing diagnostic used successfully today, with mandatory same-session restoration |
| T-16.5-09 | Denial of Service (cost) | the armed window on an ACTIVE workflow lets any inbound event spend | medium | mitigate | The blocking checkpoint states the exposure and asks the operator to confirm no HubSpot subscription targets this URL; per-run caps stay in force and are asserted on the deployed build; Task 3 (7) restores unconditionally with a read-back, and the HALT PROTOCOL makes restore the first action on any abort |
| T-16.5-10 | Tampering (unintended CRM write) | an enabled run reaching a HubSpot create or update on the live portal | high | mitigate | Write-safety constants asserted disabled on the DEPLOYED build before firing; zero writes proven three independent ways after (no write node in `runData`, the write-blocked decision, and an unchanged `lastmodifieddate`) rather than by any single signal |
| T-16.5-11 | Elevation of Privilege (unreviewed hotfix) | patching a discovered live bug in-flight during an armed session | medium | mitigate | Frontmatter prohibits any source edit in this plan; (6) requires stop-record-restore-report, with the fix routed to its own plan or quick task carrying its own tests |
| T-16.5-12 | Information Disclosure | a secret value reaching stdout, a scratchpad file, or the SUMMARY | medium | mitigate | `<live_command_conventions>` mandates sourcing `.env` into a subshell and referencing variables by name only; an explicit acceptance criterion on every task asserts no secret value appears anywhere |
| T-16.5-SC | Tampering (supply chain) | npm/pip installs | low | accept | Zero packages added; only curl and already-present tooling. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Pre-flight: contact 201's real properties recorded, the research predicate hand-evaluated to a stated GO with its firing branch, agreement (or explicit disagreement) with the offline oracle, and the `lastmodifieddate` plus credit baselines captured.
- Deployed state: an API read-back before firing confirms the armed flags enabled, the cost caps unchanged, write safety disabled with empty allowlists, no env expressions, and the workflow active.
- Execution: `runData` shows the research gate firing, a genuine research response, the row carrying `existingRecord`/`scored`/`identity_keys`/`object_id` past the research hop, the judge gate's decision with its reasons, and a non-null merge at `Merge Winners`.
- Zero writes: no create/update node in `runData`, the write-blocked decision, and an unchanged `lastmodifieddate` — three independent signals, not one.
- Restore: an API read-back after the plan shows both flags disabled again and the workflow still active, unconditionally.
- Fence: no repo file modified but the SUMMARY; no secret disclosed; the other two workflows untouched.
</verification>

<success_criteria>
Plan complete when a research- and escalation-enabled build has been deployed as an explicit operator
act and CONFIRMED BY READ-BACK to be what is running; one live run against contact 201 has executed
the research-then-judge lane; that lane's execution is established node-by-node from the execution API
with the row surviving each HTTP hop to a non-null merge at `Merge Winners`; zero HubSpot writes are
proven three independent ways; and the deployment has been restored to the disabled build and
confirmed so by read-back — with a first-run failure treated as a recorded finding and a restored
deployment, not as a plan that failed.
</success_criteria>

<source_coverage_audit>
| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP 16.5 goal — the live run that finally exercises the web-research to judge lane against real HubSpot | Task 3 (3)(5) |
| REQ | SC-7 live run, research to judge lane executes, row survives every HTTP hop, zero HubSpot writes, inspected node-by-node not by response body | Task 3 (5)(a)-(h); `<scope_note>` records that the LITERAL companies bd682a2 is Plan 03 |
| REQ | SC-4 Criterion 5 on the enabled build | Task 3 (2) — asserted against the DEPLOYED body, not only the in-memory transform |
| REQ | SC-5 cost caps and write safety in force | Task 3 (2) on the deployed build, (5)(g)(h) in the executed run |
| CONTEXT | the phase exists to reach the lane the baked-off flags made unreachable | Task 1 (2) predicate evaluation; Task 3 (3) envelope choice |
| CONTEXT | scope fence: no write-safety/cap change, no other-workflow activation, no committed enabled JSON | frontmatter prohibitions; Task 3 (2)(7) |
| CONTEXT | deferred: any decision to leave research enabled permanently | Task 3 (7) restores unconditionally; permanence is not decided here |
| CONTEXT | deferred: the live jobtitle provider disagreement rubric | surfaced by Task 3 (5)(d) as an observation only; no rubric tuning |
| USER | the live run is a task with explicit inspection steps | Task 3 (5) |
| USER | verify the research node fired, the row survived every HTTP hop to Merge Winners, zero HubSpot writes | Task 3 (5)(a)(c)(e)(f)(g) |
| USER | judging by an HTTP 200 response body is NOT acceptable | Task 3 (3) explicit set-aside + T-16.5-07 |
| USER | plan for the live step to FAIL and need iteration; first-run failure is expected path | Task 3 (6), the checkpoint's closing paragraph, and `<success_criteria>` |
| USER | n8n swallows node errors under `onError: continueRegularOutput`; capture the real error | Task 3 (5)(b) and (6) incl. the temporary single-node clearing excursion |
| USER | research is a web-search call per record, escalation only on conflict; consider independent toggles | the checkpoint offers arm-both vs arm-research-only, with the judge cost-cap consequence of the latter stated |
| USER | do not use `gsd-tools query state.advance-plan` / `state.update-progress` | frontmatter prohibition |
| STATE | nine live-only bugs, all offline-green then failing on first real API contact | the checkpoint's expectation-setting, Task 3 (6), and the read-back-before-firing gate |
| STATE | canary steps 2 and 3 — contact 201's shape, the zero-provider bare event, unchanged `lastmodifieddate` as the zero-write oracle | Task 1 (1)(4); Task 3 (3)(5)(g) |
| STATE | project-ownership gotcha — confirm whose key is in `N8N_API_KEY` | `user_setup` note on `N8N_API_KEY` |
</source_coverage_audit>

<output>
Create `.planning/phases/16.5-research-escalation-enablement/16.5-02-SUMMARY.md` when done. It MUST
record: contact 201's pre-flight property values and the hand-evaluated gate predicate with its GO or
NO-GO; the checkpoint decision taken; the deploy's printed rewrite plan; the verbatim pre-fire
read-back of the deployed flags, caps and write-safety constants; the execution id; the quoted
per-node evidence for every claim in Task 3 (5); the three independent zero-write proofs; the verbatim
post-restore read-back; every live-only bug or surprise found, in full, with the request shape that
produced it; and an explicit statement of whether the LITERAL companies `bd682a2` remains
live-unverified.
</output>
</content>
</invoke>
