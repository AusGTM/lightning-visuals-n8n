---
plan: 16.5-02
status: complete
completed: 2026-07-28
criterion: "ROADMAP 16.5 criterion 7 (contacts) — MET"
---

# Phase 16.5 Plan 02 — LIVE research/judge run (contacts) — PASSED

**The web-research → judge lane executed live for the first time, and the `bd682a2` row-recovery
mirror is verified against the real API.**

## Pre-flight (Task 1, read-only)

Contact 201, fetched read-only:

| property | value |
|---|---|
| `jobtitle` | `"Carmody, our developer. His email copied above"` (present) |
| `seniority` | **BLANK** |
| `lv_jobtitle_verified_at` | BLANK |
| `lv_contact_enrichment_provenance` | BLANK |
| `lastmodifieddate` | `2026-07-18T01:14:03.751Z` ← zero-write oracle |

Gate predicate evaluated by hand (`CONTACT_RESEARCH_FIELDS = ["jobtitle","seniority"]`,
`JOBTITLE_STALE_DAYS = 180`), zero providers so `winners = {}`:

- `jobtitle`: present in record → `blank && noWinner` false.
- `seniority`: **blank in record AND no winner → `provider_gap` fires.**
- stale branch: `lv_jobtitle_verified_at` blank → cannot fire (irrelevant, gap already true).

**GO**, via `provider_gap` on `seniority`. This agrees with 16.5-01's offline oracle.

It also confirms the planner's key claim: with all three providers enabled, ZoomInfo/Apollo supply
`seniority` as a winner, the gap closes, and research stays dark — which is why canary step 3 passed
without ever reaching this lane.

Baselines: Lusha 4101, ZoomInfo 9344, Apollo unavailable (known 403, non-master key). All four
deployed flag declarations `false`.

## Deploy (Task 2)

`ENABLE_BAKED_FLAGS=ALLOW_WEB_RESEARCH,ALLOW_SONNET_ESCALATION` → overlay reported
`rewritten 2x` for each flag, in `LV Enrichment` only. Fail-closed confirmed live: a bogus flag name
was REFUSED before any HTTP call.

Read-back of the DEPLOYED workflow:

| | |
|---|---|
| `ALLOW_WEB_RESEARCH` | `['true','true']` |
| `ALLOW_SONNET_ESCALATION` | `['true','true']` |
| `ALLOW_HUBSPOT_RECORD_WRITES` | `['"false"']` — untouched |
| `ALLOW_HUBSPOT_CREATE` | `['"false"']` — untouched |
| `MAX_WEB_RESEARCH_PER_RUN` / `MAX_SONNET_VALIDATIONS_PER_RUN` | `10` / `10` — untouched |
| `$env`/`$vars` present | **False** |

Committed `n8n/wf_enrichment_cloud.json` still carried `const ALLOW_WEB_RESEARCH = false;` ×2 and
`git status --short n8n/` was empty throughout — criterion 1 held by construction.

## The live run (Task 3)

Bare HubSpot event array, **zero providers**, contact 201. Execution 9, 32 nodes, `status: success`,
**zero error nodes**. Chain reached:

```
... Adapt Fetch By Id -> Enrichment Gate -> IF Provider Processing Needed
 -> Normalize + Score -> Contact Research Trigger Gate -> IF Contact Research Needed
 -> Build Contact Research Request -> Contact Web Research -> Validate Contact Research
 -> Contact Judge Gate -> IF Contact Needs Judge -> Merge Winners -> ... -> Respond to Webhook
```

### `bd682a2` mirror — the load-bearing evidence

- `Contact Web Research` output top-level keys: `content, id, model, role, stop_details,
  stop_reason, stop_sequence, type, usage` — i.e. the HTTP node **did** replace `$json` with the
  Anthropic response. That replacement is the exact mechanism of the bug.
- `Validate Contact Research` nonetheless carries `existingRecord ✓`, `scored ✓`,
  `identity_keys ✓`, `object_id: 201` — **the row was recovered by node name across that hop.**
- `Merge Winners`: **`merge is null? False`** (`{"canonicalPatch":{},"provenance":{},"cacheKeys":{},
  "decisions":[]}`). The bug's symptom was `merge: null`. It did not occur.

Research returned `matched: false`, `confidence: 0`, both fields null — Claude found no public
role evidence for this contact. **The lane worked; the research simply found nothing.** The judge
gate evaluated and did not escalate (no conflict to adjudicate), so `Build Contact Judge Request` /
`Contact Judge Call` did not run — the Sonnet escalation path remains live-unexercised.

### Safety, verified after the run

- Write nodes executed: **NONE**.
- Contact 201 `lastmodifieddate` **unchanged** (`2026-07-18T01:14:03.751Z`); `seniority` and
  provenance still null.
- Credits **unchanged** (Lusha 4101, ZoomInfo 9344) — independent confirmation the zero-provider
  envelope really did disable the waterfall.

## Restore

Redeployed without `ENABLE_BAKED_FLAGS`; read-back confirmed `['false','false']` for both flags,
`onError: continueRegularOutput` restored on both fetch nodes, workflow still active.

## Honest scope statement

This verifies the **contacts mirror** of `bd682a2`. The literal fix (`Merge Company`, companies
branch) is Plan 03 — attempted, and **blocked by a newly discovered bug**; see
`16.5-03-SUMMARY.md`. The companies research lane remains **live-unverified**.
