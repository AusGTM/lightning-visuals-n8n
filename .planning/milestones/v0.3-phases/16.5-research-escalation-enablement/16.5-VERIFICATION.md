---
phase: 16.5-research-escalation-enablement
verified: 2026-07-29T00:00:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 16.5: Deliberate Research/Escalation Enablement Verification Report

**Phase Goal:** An operator can deploy a research- and escalation-enabled build as an explicit, auditable act, WITHOUT the committed workflow JSON ever carrying those flags on — and the live run that follows exercises the web-research → judge lane against real HubSpot.

**Verified:** 2026-07-29
**Status:** passed
**Re-verification:** No — initial verification

No deployment, webhook fire, or HubSpot/n8n call was made during this verification. All checks are static (code/test reading) or local (running the existing offline suites).

## Goal Achievement

### Per-Criterion Verdicts

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | Committed `n8n/wf_*.json` still carry both flags `false` | ✓ VERIFIED | `grep -n "const ALLOW_WEB_RESEARCH\|const ALLOW_SONNET_ESCALATION" n8n/wf_enrichment_cloud.json` → both declared exactly `= false;` at 2 sites each (contacts mirror line ~615/724, companies branch line ~1384/1493). `git status --porcelain n8n/` → empty. Test: `tests/test_enabled_build_invariants.py::test_committed_build_flag_declarations_are_always_disabled` (parametrized over every `wf_*_cloud.json` × both flags) is paired with a non-vacuity test (`test_enrichment_workflow_declares_both_flags_at_least_once`) that pins the guard is actually exercised, not passing on an empty match set. Both pass: `59 passed` in the combined run below. |
| 2 | Enablement is an explicit operator act via `ENABLE_BAKED_FLAGS`, name deliberately distinct from `.env` flag names | ✓ VERIFIED | `scripts/deploy_n8n_workflows.py:345-361` — `_requested_overlay_flags()` reads `ENABLE_BAKED_FLAGS` only; docstring states the rationale explicitly (this repo's `.env` already defines `ALLOW_WEB_RESEARCH`/`ALLOW_SONNET_ESCALATION` for the Python harness lane, so a same-name overlay would arm production on a routine deploy). `test_ambient_env_names_have_zero_effect_on_the_captured_put_body` (in `test_deploy_flag_overlay.py`, referenced by 16.5-01-SUMMARY coverage D2) proves the ambient names are inert. Unset/empty `ENABLE_BAKED_FLAGS` → empty dict → no rewrite (line 362-364). |
| 3 | `enable_baked_flags()` is pure, deep-copying, and fails closed via post-rewrite re-scan — survives the 2026-07-29 widening (`7f0dce4`) | ✓ VERIFIED | Read current code, not the SUMMARY's description. `deploy_n8n_workflows.py:264-342`: deep copy at line 306 (`json.loads(json.dumps(workflow))`), never mutates input (tested by `test_enable_baked_flags_is_pure_input_untouched`). Fail-closed re-scan is UNCHANGED in contract post-widening — lines 327-340 still re-scan every requested flag's declaration with a looser regex (`const\s+{flag}\s*=\s*([^;]+);`) than the exact-literal replace, and raise `ValueError` on any surviving non-target literal. The widening (`_OVERLAY_FLAG_SPEC`, lines 123-133) added 4 new flags (write-safety + 2 value-bearing allowlist flags) to the SAME mechanism — same replace-then-rescan code path, not a parallel/looser one. Drift tests still pass post-widening: `test_enable_baked_flags_raises_on_spacing_variant_the_exact_rewrite_cannot_match`, `test_enable_baked_flags_raises_on_numeric_literal_variant`, and the write-safety-specific `test_rescan_refuses_when_a_declaration_survives_in_an_unreachable_form` (in `test_deploy_write_safety_overlay.py`) — all pass in the combined run below. |
| 4 | Zero `$env`/`$vars` in the enabled build; `test_architecture_guard.py` green | ✓ VERIFIED | `test_enabled_build_has_zero_env_or_vars_expressions` and `test_enabled_build_has_no_secret_lookup_form_for_any_secret_name` (both in `test_enabled_build_invariants.py`) run the SAME regex `test_architecture_guard.py` uses against the TRANSFORMED (enabled, in-memory) artifact, not just the committed one. `.venv/bin/python -m pytest tests/test_architecture_guard.py -q` → `38 passed`. |
| 5 | Cost caps intact; enabling research does NOT enable writes; separation is structural post-widening | ✓ VERIFIED | Read `_requested_overlay_flags()` (lines 407-417): `if requested.keys() & _WRITE_ENABLING_FLAGS: ... if not allowlisted: raise ValueError("refusing to enable HubSpot writes without an allowlist...")` — a write-enabling flag with no non-empty `TEST_RECORD_IDS`/`TEST_RECORD_DOMAINS` in the SAME request is refused before any rewrite happens. `ALLOW_HUBSPOT_CREATE` without `ALLOW_HUBSPOT_RECORD_WRITES` is separately refused (lines 418-422). Cost caps (`MAX_WEB_RESEARCH_PER_RUN`, `MAX_SONNET_VALIDATIONS_PER_RUN`) and `ANTHROPIC_SONNET_MODEL` are absent from `_OVERLAY_FLAG_SPEC` entirely (lines 123-133) — requesting them raises `ValueError` (`test_enable_baked_flags_rejects_non_overlayable_names`, `test_cost_caps_and_models_stay_non_overlayable`). `test_unrequested_write_flags_are_untouched_by_a_research_only_request` proves a research-only `ENABLE_BAKED_FLAGS` request leaves `ALLOW_HUBSPOT_RECORD_WRITES`/`TEST_RECORD_IDS` at their disabled literals. `test_enabled_build_cost_caps_are_unchanged` proves the caps are byte-identical pre/post-overlay on the real artifact. |
| 6 | Full offline suite green, zero regressions vs 422/272 baseline; builder deterministic | ✓ VERIFIED | Ran fresh, not taken from SUMMARY: `.venv/bin/python -m pytest -q` → **542 passed**, 1 pre-existing unrelated warning (fastapi deprecation). `node --test tests/n8n/*.test.mjs` → **278 passing, 0 failing**. Both exceed the 422/272 phase-16.5 baseline and the ROADMAP closure's stated 533/278 (pytest count grew further to 542 from subsequent phases 16.6–16.10's own test additions — expected growth, not a discrepancy; zero failures either way). |
| 7 | Live: research→judge lane exercised on both contacts (Merge Winners) and companies (Merge Company) branches, zero writes, judged node-by-node | ✓ VERIFIED | **Contacts** (16.5-02-SUMMARY.md, execution 9, 2026-07-28): quoted `runData` — `Validate Contact Research` carries `existingRecord ✓, scored ✓, identity_keys ✓, object_id: 201` across the HTTP hop; `Merge Winners` → `merge is null? False`. Zero write nodes executed; contact 201 `lastmodifieddate` unchanged; credits unchanged. **Companies** (literal `bd682a2`): blocked live on 2026-07-28 by BUG 10 (18 nodes, died at `Normalize + Score Company` — documented honestly in 16.5-03-SUMMARY.md as `superseded`, not silently dropped). Resolved and independently re-verified live 2026-07-29 in the Phase 16.7 armed window, execution 12 — cross-checked directly against `16.7-02-SUMMARY.md`'s own quoted `runData`: `HubSpot Company Fetch By Id` returns a real record (not null), `Company Gate` → `action: "enrich"` (not overridden to skip), `Normalize + Score Company` emits 1 row (was 0), **`Merge Company` → merge NON-NULL** with keys `['canonicalPatch','provenance','cacheKeys','decisions']`, `Decide Company Action` → `write_blocked`, company `9604614548` `hs_lastmodifieddate` byte-identical before/after. The two SUMMARY files (16.5-03's RESOLVED addendum and 16.7-02) independently quote the same execution number (12), the same node count (26 vs the blocked run's 18), and the same result shape — this is a traceable, cross-confirmed evidence chain, not a bare narrative assertion. |

**Score:** 7/7 criteria verified (0 present-but-behavior-unverified)

### Fresh Test Run Output (this verification, not inherited from any SUMMARY)

```
$ .venv/bin/python -m pytest -q
542 passed, 1 warning in 1.99s

$ node --test tests/n8n/*.test.mjs
tests 278
pass 278
fail 0

$ .venv/bin/python -m pytest tests/test_enabled_build_invariants.py tests/test_deploy_flag_overlay.py \
    tests/test_deploy_write_safety_overlay.py tests/test_builder_flag_parity.py -q
59 passed in 0.38s

$ .venv/bin/python -m pytest tests/test_architecture_guard.py -q
38 passed in 0.07s

$ node --test tests/n8n/enabledResearchLaneFlow.test.mjs
tests 3
pass 3
fail 0
```

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/deploy_n8n_workflows.py::enable_baked_flags()` | Pure, deep-copying, fail-closed overlay | ✓ VERIFIED | Present, wired into `main()` above the write gate (lines 458-503), exercised by 59 passing tests across 3 files |
| `scripts/deploy_n8n_workflows.py::_requested_overlay_flags()` | Reads `ENABLE_BAKED_FLAGS`, refuses writes without allowlist | ✓ VERIFIED | Present (lines 345-423), write-safety fail-safe confirmed by test |
| `tests/test_enabled_build_invariants.py` | Committed-stays-disabled + Criterion 5-on-enabled-build + diff-is-only-flag-lines | ✓ VERIFIED | 13 tests, all pass, non-vacuous (explicit non-vacuity test present) |
| `tests/test_deploy_flag_overlay.py` | Mechanism tests: purity, exactness, fail-closed drift, rejection | ✓ VERIFIED | All pass |
| `tests/test_deploy_write_safety_overlay.py` | Write-safety widening's own guard tests (16.7, post-dates 16.5-01 but is the thing criterion 3/5 must survive) | ✓ VERIFIED | All pass, including the rescan-refuses-on-drift test for the widened flags |
| `tests/n8n/enabledResearchLaneFlow.test.mjs` | Offline oracle: enabled build drives both lanes to non-null merge; disabled control | ✓ VERIFIED | 3/3 pass |
| `n8n/wf_enrichment_cloud.json` (committed) | Both flags `false` × 2 declarations each | ✓ VERIFIED | Confirmed by direct grep, matches test assertions |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `main()` | `enable_baked_flags()` | overlay applied above write gate, before credential binding | ✓ WIRED | `deploy_n8n_workflows.py:462-503` — refuses with zero HTTP calls on `ValueError` or zero-match |
| `ENABLE_BAKED_FLAGS` env var | `_requested_overlay_flags()` | comma-separated parse, distinct name from `.env` research flags | ✓ WIRED | Confirmed inert against `ALLOW_WEB_RESEARCH`/`ALLOW_SONNET_ESCALATION` env vars by dedicated test |
| Write-enabling flags | allowlist requirement | same-request guard in `_requested_overlay_flags()` | ✓ WIRED | Structural (code-level `if`), not a convention; tested |
| 16.5-03-SUMMARY.md RESOLVED addendum | 16.7-02-SUMMARY.md | quoted execution 12, `Merge Company` non-null | ✓ WIRED (traceable) | Node counts (26 vs 18) and execution number cross-confirm between the two independently-committed documents |

### Anti-Patterns Found

None blocking. `16.5-03-SUMMARY.md` is legitimately marked `status: superseded` rather than `complete` — this is honest bookkeeping, not a hidden gap: the plan's OWN live attempt was blocked by a newly discovered bug (BUG 10), it explicitly states "The companies research lane remains live-unverified" at the time of writing, and the resolution is a dated addendum pointing to the actual verifying evidence in a different phase's SUMMARY, not a silent edit of the original record. Counting Phase 16.5 as "3/3 plans, criterion 7 met on both branches" is defensible because the ROADMAP's own criterion 7 planning note anticipated exactly this contingency ("If Plan 03 is declined, the phase record must state that the literal companies fix remains live-unverified") and the phase record does state it, then updates it with a cross-referenced, quoted resolution once the dependency (Phase 16.6) closed it. No debt markers (`TBD`/`FIXME`/`XXX`) found in the touched files.

### Requirements Coverage

Not applicable — this repo's phase tracking uses ROADMAP.md Success Criteria directly (draft-refine-at-plan-time style), not a separate REQUIREMENTS.md ID scheme for this phase.

## Gaps Summary

None. All 7 ROADMAP criteria independently verified against current code and freshly-run test suites, not inherited from SUMMARY.md or ROADMAP closure-block claims. The one legitimate self-certification risk flagged for scrutiny — Phase 16.5 closed retroactively, same day, by the same session producing the closing evidence — resolves cleanly on inspection: the companies-branch live evidence is quoted `runData` in a separate phase's (16.7) independently-committed SUMMARY, cross-referenced by execution number and node count, not a bare narrative claim inserted into 16.5's own record.

---
*Verified: 2026-07-29*
*Verifier: Claude (gsd-verifier)*
