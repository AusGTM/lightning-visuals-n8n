---
phase: 16.5-research-escalation-enablement
plan: 03
type: execute
wave: 3
depends_on: ["16.5-02"]
autonomous: false
files_modified:
  - .planning/phases/16.5-research-escalation-enablement/16.5-03-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
requirements:
  - "SC-7 (LIVE, literal bd682a2: one enabled run fires against a real company record, the companies research-then-judge lane executes, and the row survives every HTTP hop to a NON-NULL merge at Merge Company — the exact node and the exact symptom the bd682a2 fix was written for)"
  - "SC-5 (cost caps and HubSpot write safety confirmed in force on the deployed build and in the executed companies run)"

must_haves:
  truths:
    - "`bd682a2` is titled 'recover row across HTTP hops in COMPANIES research/judge chain'. Its fix lands in `Validate Research Output` and `Apply Judge Verdict`; its symptom is `Merge Company` returning a null merge on the research-true lane. Plan 02 verified the CONTACTS mirror of that fix. This plan verifies the LITERAL one, on the branch it was written for, by asserting a non-null merge at `Merge Company` in a live execution."
    - "The companies branch has NEVER executed live — not once, in any canary step. Every live run to date drove contacts. This is therefore the highest-risk single action in the phase, and a first-run failure is the expected path: nine live-only bugs surfaced in the contacts branch this session on exactly this pattern, every one of them code that passed a full offline suite and deployed clean."
    - "The research gate is confirmed to be reachable for the chosen company BEFORE anything is armed or spent. The companies predicate fires when `lv_org_type` is unresolved or evidence-gated, or `lv_produces_content` is blank; whether that holds for the real record is a portal fact, checked read-only first."
    - "The run is judged NODE BY NODE from the execution API's `runData`, never from the webhook's response body — the same discipline Plan 02 established, for the same reason."
    - "ZERO HubSpot writes occur, proven three ways: no create or update node appears in `runData`; `Decide Company Action` reports the write-blocked outcome; and the company's `hs_lastmodifieddate` is byte-identical before and after."
    - "The deployment is RESTORED to the disabled build before this plan ends, unconditionally — including after a failed or abandoned run — and confirmed by API read-back rather than by the redeploy's exit code."
    - "The phase's own record states plainly what was and was not proven live. If the companies run fails or is abandoned, the record says the literal bd682a2 remains live-unverified. Claiming otherwise would be the same false-success this phase was built to prevent."
  artifacts:
    - "the deployed `LV Enrichment` workflow, enabled in flight for one companies run and then restored to disabled — no source artifact changes"
    - ".planning/STATE.md and .planning/ROADMAP.md — hand-edited phase closure"
  key_links:
    - "the bare company event array -> `Parse HubSpot Event` (zero providers) -> `Build Company Identity` -> `IF Company Bare Event` -> `HubSpot Company Fetch By Id` -> `Adapt Company Fetch By Id` -> `Company Gate` -> `Build Company Requests` -> `Normalize + Score Company` -> `Research Trigger Gate` (research_needed true) -> `Claude Web Research` -> `Validate Research Output` -> `Judge Gate` -> `Judge Call` -> `Apply Judge Verdict` -> `Merge Company` (NON-NULL merge — the bd682a2 assertion proper) -> `Decide Company Action` (write blocked)"
    - "`GET /api/v1/executions/{id}?includeData=true` -> `data.resultData.runData[<node name>]` -> the only acceptable evidence for every claim here"
    - "the plain redeploy (no `ENABLE_BAKED_FLAGS`) -> GET read-back showing both flags disabled -> production disarmed"
  prohibitions:
    - "NO source file edit in this plan except the SUMMARY, STATE.md and ROADMAP.md. A live bug found here is recorded and reported, not patched in flight during an armed session."
    - "NO enabling of `ALLOW_HUBSPOT_RECORD_WRITES` / `ALLOW_HUBSPOT_CREATE`, NO population of the test-record allowlists, NO change to the cost caps."
    - "NO activation or deactivation of `LV Scheduled Maintenance` or `LV Contact Ingest`."
    - "NO judging the run by the webhook's HTTP response body or status code."
    - "NO echoing of any secret value into a command's stdout, a file, or any planning document."
    - "NO more than the ONE companies run this plan authorises without returning to the operator."
    - "NO use of `gsd-tools query state.advance-plan` or `state.update-progress` — hand-edit STATE.md (known miscounting bug: ROADMAP carries three concatenated milestones and the tool mis-derives the progress bar)."
  user_setup:
    - service: n8n-cloud
      why: "the enabled deploy, the webhook fire, and every execution inspection require the live instance"
      env_vars:
        - name: N8N_URL
          source: "already present in ./.env"
        - name: N8N_API_KEY
          source: "already present in ./.env — MUST be the key issued under Robert, not Alex (project-ownership gotcha, STATE.md)"
        - name: N8N_ENRICHMENT_WEBHOOK_SECRET
          source: "already present in ./.env — sent as the X-Enrichment-Secret header"
    - service: hubspot
      why: "read-only before/after snapshot of the company record to prove zero writes"
      env_vars:
        - name: HUBSPOT_PRIVATE_APP_TOKEN
          source: "already present in ./.env"
---

<objective>
Verify `bd682a2` — the companies research-lane row-loss fix — where it actually lives: on the
companies branch, at `Merge Company`, in a live execution. Plan 02 proved the contacts mirror; this
plan proves the literal fix, and it is also the first live execution of the companies branch in this
project's history.

Purpose: the fix was found by cross-AI inspection, corrected, and guarded offline, but the lane it
lives in has never once run against the real API. Its symptom is specific and checkable — a null
merge at `Merge Company` on the research-true lane — and until that node emits a real merge in a real
execution, the fix is verified by reasoning only.

Output: a verified live companies execution, a node-by-node evidence record, a disarmed deployment,
and a phase closure record that states exactly what was and was not proven.
</objective>

<scope_note>
This plan goes one step beyond ROADMAP criterion 7, which names contact 201 only, and it does so
deliberately: criterion 7 also claims to verify `bd682a2`, and contact 201 cannot — that commit is
companies. The cheapest honest resolution is one more run on the same enabled build.

If the operator prefers to close the phase at criterion 7, the checkpoint below offers that, and the
recording task then states in STATE.md and the SUMMARY that the literal companies `bd682a2` remains
live-unverified. What is NOT acceptable is closing the phase while implying it was verified.
</scope_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.5-research-escalation-enablement/16.5-CONTEXT.md
@.planning/phases/16.5-research-escalation-enablement/16.5-01-SUMMARY.md
@.planning/phases/16.5-research-escalation-enablement/16.5-02-SUMMARY.md
</context>

<live_command_conventions>
Identical to Plan 02. Read once, apply to every command here.

  - Live environment variables are NOT exported into this shell and `.env` cannot be read directly.
    Prefix every live command with `set -a; . ./.env; set +a;` and reference variables by name only.
    NEVER echo, print, cat, or write a secret value anywhere.
  - The workflow under test is `LV Enrichment`, id `950HPb7a1GgSAIyZ`, project `T9IPFKpIn2aUYYj3`,
    ACTIVE. The other two workflows stay inactive and untouched.
  - n8n Public API calls carry `X-N8N-API-KEY` with `$N8N_API_KEY`.
  - The webhook is `POST $N8N_URL/webhook/hubspot/enrichment/event` with header
    `X-Enrichment-Secret: $N8N_ENRICHMENT_WEBHOOK_SECRET`.
  - HubSpot reads use `Authorization: Bearer $HUBSPOT_PRIVATE_APP_TOKEN` against
    `https://api.hubapi.com`.
  - Write every captured API response to the session scratchpad as a file, then analyse the file.
  - Read back the deployed workflow with `GET $N8N_URL/api/v1/workflows/950HPb7a1GgSAIyZ`. List
    executions with `GET $N8N_URL/api/v1/executions?workflowId=950HPb7a1GgSAIyZ&limit=5`. Fetch one
    with `GET $N8N_URL/api/v1/executions/<id>?includeData=true`; per-node records live at
    `data.resultData.runData[<node name>]`.
</live_command_conventions>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — choose the company, prove its research gate will fire, record the zero-write baseline</name>
  <files>(no repo file modified; captured JSON goes to the session scratchpad)</files>
  <precondition>Plan 16.5-02 is complete and its SUMMARY records a post-restore read-back showing both flags carrying the disabled literal on the live workflow, and `active` true. If the deployment is currently armed, disarm and read back before doing anything else in this plan. The working tree is clean.</precondition>
  <read_first>
    - `16.5-02-SUMMARY.md` in full — the working command forms, the read-back method, the execution-inspection method, and every live-only bug or surprise it recorded. If the contacts run surfaced a bug in a SHARED node (`Parse HubSpot Event`, the fetch-by-id lane, the write-safety gate), it will very likely bite the companies branch too; anticipate it rather than rediscovering it.
    - `tests/n8n/enabledResearchLaneFlow.test.mjs` — the COMPANIES chain node list and its per-node assertions. The live inspection in Task 3 is that same list of claims, read out of `runData`.
    - The `Research Trigger Gate` node body in `n8n/wf_enrichment_cloud.json` — the `needsResearch(existingRecord)` predicate: it fires when `lv_org_type` is absent, empty, `unknown`, or one of the evidence-gated org types, OR when `lv_produces_content` is blank.
    - The `HubSpot Company Fetch By Id` node's property list in `n8n/wf_enrichment_cloud.json` — the exact properties the live run will have available in `existingRecord`. The predicate can only see these.
    - `.planning/STATE.md` — the `hs_object_id` filterability probe, which verified company 9604614548 (Melbourne Racing Club, mrc.racing.com) live and read-only on portal 22617666, and the enrich-first note that org type is verified for only 66 of 712 companies.
  </read_first>
  <action>
    Zero writes, zero spend, zero deploy in this task.

    (1) CHOOSE THE COMPANY. Default to 9604614548 (Melbourne Racing Club) — it is the one company id
    already proven live and read-only against this portal by the `hs_object_id` probe, so the fetch
    lane is known to resolve it. If its predicate turns out not to fire, pick another company from the
    portal whose `lv_org_type` or `lv_produces_content` is blank, and state which and why.

    (2) SNAPSHOT IT, READ ONLY. Fetch the company requesting exactly the properties
    `HubSpot Company Fetch By Id` requests, plus `hs_lastmodifieddate`. Write the response to the
    scratchpad. Record verbatim: `name`, `domain`, `lv_org_type`, `lv_produces_content`,
    `lv_enrichment_provenance` (or blankness), and `hs_lastmodifieddate`. That last value is the
    zero-write oracle for Task 3.

    (3) EVALUATE THE GATE PREDICATE BY HAND against those real values, with zero providers enabled
    (which a bare event array produces). State whether `needsResearch` returns true and through which
    clause. Show the reasoning; do not assert a conclusion.

    (4) THE GO / NO-GO.
      - Fires: state it, name the clause, proceed.
      - Does not fire: STOP. Do not deploy, do not fire. Report the property values that make it false
        and name a different company that would satisfy it. Changing the record's properties to force
        the gate is a HubSpot WRITE and is outside the fence.

    (5) BASELINE THE COST SURFACE. Record Lusha and ZoomInfo credit balances (Apollo's usage endpoint
    403s on this non-master key — record "unavailable, known 403"). A zero-provider run must leave them
    unchanged.

    (6) CONFIRM THE DEPLOYED BUILD IS CURRENTLY DISABLED, by read-back, and record the literal each
    flag carries. This is both the starting state and the state Task 3's restore must return to.

    Commit nothing; the output is the recorded pre-flight in the SUMMARY.
  </action>
  <verify>
    <human-check>The SUMMARY records the chosen company id with its exact property values, the hand-evaluated companies gate predicate with its firing clause, an explicit GO or NO-GO, the verbatim `hs_lastmodifieddate`, the credit baselines, and a read-back confirming both flags currently disabled on the live workflow.</human-check>
  </verify>
  <acceptance_criteria>
    - The chosen company's real property values are recorded verbatim including `hs_lastmodifieddate`, fetched read-only (a GET; no PATCH, POST or PUT was issued to `api.hubapi.com` in this task).
    - The `needsResearch` predicate is evaluated by hand against those values with zero providers, and the result plus the firing clause is stated.
    - An explicit GO or NO-GO is recorded; a NO-GO halts the plan with no deploy and no fire.
    - Lusha and ZoomInfo credit balances are recorded; Apollo is recorded as unavailable with its known cause.
    - An API read-back confirms both flags currently carry the disabled literal on the live workflow.
    - No secret value appears in any command output, scratchpad file, or planning document.
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only. Nothing changed anywhere.</reversibility>
  <done>A specific company is chosen, its research gate is proven reachable from real portal values, and the zero-write and zero-spend baselines are captured.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Run the companies branch live: re-arm the enabled build for one run against the chosen company record, to verify the literal `bd682a2` at `Merge Company`.</decision>
  <context>
ROADMAP criterion 7 names contact 201 and Plan 02 satisfied it. This plan is a deliberate step beyond
it, and it exists because criterion 7 ALSO claims to verify `bd682a2` — a commit whose title,
diff and symptom are all companies. Contact 201 cannot verify it. Plan 02 verified the contacts
mirror of the same fix, which is real value but is not the same claim.

WHAT THIS BUYS. The one node whose null merge was the original symptom — `Merge Company` — emits a
real merge in a real execution, or it does not. Either answer is worth having. It is also the first
live execution of the companies branch in this project's history, so it exercises the companies
fetch-by-id lane, the companies gate, the companies scoring and the companies write-safety decision
for the first time as well.

WHAT IT COSTS. One company, one research call, at most one Sonnet arbitration. Cents. The per-run
caps stay in force and are not overlayable. The armed window is one more short deploy-fire-restore
cycle on the ACTIVE workflow, bounded exactly as Plan 02's was.

WHAT THE RISK ACTUALLY IS. The companies branch has never run live. Plan 02's contacts run exercised
shared infrastructure — the webhook, the event parser, the fetch-by-id lane, the write-safety gate —
but every companies-specific node is untested against real APIs. Nine live-only bugs surfaced in the
contacts branch this session; expect this branch to have its own. That is the point of running it,
but it means this is more likely than not to end in a recorded finding rather than a clean pass, and
the plan is written for that outcome.

WHAT STAYS OFF. HubSpot record writes and creates stay disabled, the test-record allowlists stay
empty, and the other two workflows stay inactive.
  </context>
  <options>
    <option id="run-companies">
      <name>Run it (recommended)</name>
      <pros>Verifies the literal `bd682a2` on the branch it was written for, and gives the companies branch its first live execution. One more short armed window on a build already proven safe by Plan 02. Any failure found is a real latent production bug found cheaply.</pros>
      <cons>Highest-risk action in the phase — a branch with zero live history is likely to surface its own live-only bugs, which will need their own follow-up plan. One more armed window on an active workflow.</cons>
    </option>
    <option id="close-at-criterion-7">
      <name>Close the phase at criterion 7</name>
      <pros>Criterion 7 as written is already met by Plan 02; zero further spend and zero further armed window. The companies lane stays covered by its offline row-flow guards and by Plan 01's offline oracle.</pros>
      <cons>The literal `bd682a2` remains verified by inspection only, and the companies branch remains entirely live-untested. Task 3 must then be skipped and Task 4 must record both facts explicitly in STATE.md and the SUMMARY — the phase cannot close implying otherwise.</cons>
    </option>
  </options>
  <resume-signal>Select: run-companies or close-at-criterion-7.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy enabled, fire the company run, inspect node-by-node, restore to disabled</name>
  <files>(no repo file modified; captured JSON goes to the session scratchpad)</files>
  <read_first>
    - `16.5-01-SUMMARY.md` — the operator-flag command form and the recorded per-flag rewrite counts. The live deploy's own stdout must report the same counts.
    - `16.5-02-SUMMARY.md` — the working command forms and every finding from the contacts run. Reuse the method exactly; do not reinvent it.
    - Task 1's recorded pre-flight — the chosen company id, its `hs_lastmodifieddate`, the credit baselines, the currently-deployed disabled literals.
    - `tests/n8n/enabledResearchLaneFlow.test.mjs` — the companies chain node list, which is the list of claims to establish below.
  </read_first>
  <action>
    Skip this entire task if the checkpoint selected close-at-criterion-7; go straight to Task 4.

    HALT PROTOCOL, read before starting. If you stop for ANY reason — a failure, a surprise, an
    ambiguity, running low on context, an operator interruption — your FIRST action before reporting
    is step (7), the restore. A halted task must never leave the active workflow armed.

    (1) DEPLOY THE ENABLED BUILD, with the write gate open and the operator flag naming both
    constants. Capture the full stdout. The overlay's printed plan must name each flag with a non-zero
    rewrite count matching `16.5-01-SUMMARY.md`, and name `LV Enrichment`. A REFUSAL is the fail-closed
    guard working — record the message verbatim, do not work around it, stop.

    (2) READ BACK WHAT IS ACTUALLY RUNNING — hard gate, not substitutable by the deploy's exit code.
    GET the live workflow and assert, recording each finding verbatim: both flags carrying the enabled
    literal; both cost caps carrying the values recorded in Task 1; both HubSpot write-safety constants
    still disabled with empty test-record allowlists; zero matches for the architecture guard's
    env-expression pattern in the deployed body; and `active` true (re-activate via the API and record
    it if the update cleared it — the deploy script must not gain activation logic, two existing tests
    pin that).

    (3) FIRE ONE RUN. POST a bare HubSpot event ARRAY of exactly one event carrying the chosen
    `objectId`, `objectType` "company", a propertyChange `subscriptionType`, a `propertyName`, and an
    `occurredAt` — and NO `providers` key at any level. Record the status and body, then set them
    aside: the webhook uses responseMode `responseNode`, so a well-formed 200 says nothing about what
    executed.

    (4) FETCH THE EXECUTION. List recent executions, take the newest, fetch it with data included,
    write it to the scratchpad. Record the execution id, `status`, and `stoppedAt`.

    (5) INSPECT NODE BY NODE. Quote the actual value out of `runData` for every claim. A claim with no
    quoted evidence is not established.

      (a) THE COMPANIES LANE WAS TAKEN AT ALL — new ground, since this branch has never run live.
          `Build Company Identity`, `IF Company Bare Event`, `HubSpot Company Fetch By Id`,
          `Adapt Company Fetch By Id` and `Company Gate` are each present in `runData`, and
          `Adapt Company Fetch By Id`'s output carries a non-null `identity_keys.domain` plus an
          ok-shaped `fetch_diagnostic`. If the fetch failed, its diagnostic distinguishes an errored
          response from a genuine zero-result one — quote whichever it is.
      (b) THE RESEARCH GATE FIRED. `Research Trigger Gate` output has `research_needed` true with no
          `research_skip_reason`. A skip reason naming the disabled flag means the deployed build is
          not the enabled one and step (2) lied — return to (2) rather than firing again.
      (c) THE RESEARCH CALL REALLY HAPPENED. `Claude Web Research` is present with a genuine Anthropic
          response. Inspect BOTH the node record's own error field and the output item's json for an
          error key: `onError: continueRegularOutput` turns a failed call into a normal-looking item
          that flows downstream and lets every later node continue as if nothing went wrong.
      (d) ROW SURVIVAL, FIRST HOP — bd682a2's own fix site. `Validate Research Output`'s output item
          still carries `existingRecord`, `scored`, `identity_keys` and `object_id`. Quote each.
      (e) ESCALATION. `Judge Gate`'s `needs_judge` and its reasons; if a reason fired, `Judge Call`
          must be present with a real response. A judge that legitimately had nothing to adjudicate is
          a valid outcome — say so from the record's values. A judge silently capped is not.
      (f) ROW SURVIVAL, SECOND HOP — bd682a2's other fix site. `Apply Judge Verdict`'s output still
          carries `existingRecord` and `scored` (applicable only if the judge lane ran).
      (g) THE MERGE IS REAL — THE bd682a2 ASSERTION PROPER, and the single most important value in
          this phase. `Merge Company`'s output `merge` is NON-NULL and carries a patch. A null merge
          here is verbatim the symptom the fix was written to prevent, live.
      (h) ZERO WRITES, THREE WAYS. No create or update node name appears anywhere in `runData`;
          `Decide Company Action` reports the write-blocked outcome; and a fresh read of the company
          returns an `hs_lastmodifieddate` byte-identical to Task 1's.
      (i) COST STAYED BOUNDED. Exactly one research call and at most one escalation in the run; Lusha
          and ZoomInfo balances unchanged from Task 1's baseline.

    (6) IF SOMETHING FAILED — likely, and the point.
      - Read the failing node's record in `runData`: its own error field AND its output item's json,
        since `onError: continueRegularOutput` puts the real failure in the item rather than failing
        the node.
      - If both are empty and the true error is swallowed, use the remedy that worked today:
        temporarily clear `onError` on that ONE node via a targeted workflow update, re-fire, read the
        now-hard failure with its real upstream message, then restore `onError` immediately in the same
        session. Record exactly what you changed and that you put it back.
      - Then STOP. Record the finding in full — the node, the real error text, the request shape that
        produced it, your read of the root cause — restore per (7), and report. The fix is a new plan
        or a quick task with its own tests, never an in-flight patch during an armed session.

    (7) RESTORE — UNCONDITIONAL, and the last thing that happens regardless of how (1) through (6)
    went. Redeploy with the write gate open and the operator flag ABSENT. Read the live workflow back
    and confirm both flags once again carry the disabled literal and `active` is still true
    (re-activating if the update cleared it). Record the read-back verbatim. The redeploy's exit code
    is not the evidence.
  </action>
  <verify>
    <human-check>The SUMMARY carries, quoted from `runData`: the five companies-lane nodes present with a non-null `identity_keys.domain` and an ok `fetch_diagnostic`; `Research Trigger Gate` with `research_needed` true; a genuine `Claude Web Research` response with both error surfaces inspected; `Validate Research Output` still carrying `existingRecord`/`scored`/`identity_keys`/`object_id`; `Judge Gate`'s decision and reasons; `Merge Company` with a NON-NULL merge; `Decide Company Action` write-blocked; the absence of any create/update node; the company's unchanged `hs_lastmodifieddate`; and the post-restore read-back showing both flags disabled again.</human-check>
  </verify>
  <acceptance_criteria>
    - The deploy's stdout named both flags with non-zero rewrite counts matching `16.5-01-SUMMARY.md`, and named `LV Enrichment`.
    - A pre-fire API read-back confirmed both flags enabled, both cost caps at Task 1's values, both write-safety constants disabled with empty allowlists, zero env-expression matches, and `active` true.
    - The fired body was a bare HubSpot event array of one company event with no `providers` key at any level.
    - `runData` contains `Build Company Identity`, `IF Company Bare Event`, `HubSpot Company Fetch By Id`, `Adapt Company Fetch By Id` and `Company Gate`, with `Adapt Company Fetch By Id`'s output carrying a non-null `identity_keys.domain` and its `fetch_diagnostic` quoted.
    - `runData`'s `Research Trigger Gate` output has `research_needed` true and no skip reason.
    - `runData`'s `Claude Web Research` output item is a real Anthropic response, with both the node's error field and the item's error key inspected and quoted as absent.
    - `runData`'s `Validate Research Output` output item carries `existingRecord`, `scored`, `identity_keys` and `object_id` — each quoted.
    - `runData`'s `Merge Company` output carries a NON-NULL `merge` with a patch — quoted. This is the literal `bd682a2` verification.
    - `Decide Company Action` reports the write-blocked outcome; no create or update node name appears anywhere in `runData`; and the company's `hs_lastmodifieddate`, re-read after the run, is byte-identical to Task 1's recorded value.
    - Lusha and ZoomInfo credit balances after the run are identical to Task 1's baseline.
    - After the restore, an API read-back shows both flags carrying the disabled literal and `active` true. This criterion holds whether or not the run succeeded.
    - `git status --porcelain` shows no modified file other than planning documents; no source file, test or workflow JSON was edited.
    - No secret value appears in any command output, scratchpad file, or planning document.
  </acceptance_criteria>
  <reversibility rating="costly">The deploy is fully reversible by the mandatory read-back-verified restore in step (7). Not reversible: the spend, and the fact that an active production workflow was briefly armed a second time. The blocking checkpoint authorises it.</reversibility>
  <done>The companies research-then-judge lane has executed live for the first time, `Merge Company` has emitted a real merge (or has not, recorded verbatim either way), the company record is provably unmodified, and the deployed workflow is disabled again.</done>
</task>

<task type="auto">
  <name>Task 4: Close the phase — record what was proven live and what was not</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/16.5-research-escalation-enablement/16.5-03-SUMMARY.md</files>
  <read_first>
    - `16.5-01-SUMMARY.md`, `16.5-02-SUMMARY.md`, and the findings from Task 3 — the three inputs to this record.
    - `.planning/STATE.md` — the "Still NOT verified live" lines about `bd682a2` (they appear twice) and the "Next command" paragraph at the end, all of which this phase either resolves or must correct. Note the hand-maintained progress-bar warning and its cross-check instruction.
    - `.planning/ROADMAP.md` — the Phase 16.5 entry with its seven criteria, and the Milestone 3 Progress table row for 16.5.
  </read_first>
  <action>
    Write the record so that a reader six months from now cannot mistake what was proven for what was
    inferred. This phase exists because of a class of failure where something looked verified and was
    not; the record is the last place that failure can still occur.

    (1) THE SUMMARY. Complete `16.5-03-SUMMARY.md` per the output block below.

    (2) STATE.md, hand-edited. Do NOT run `gsd-tools query state.advance-plan` or
    `state.update-progress` — this repo's ROADMAP carries three concatenated milestones and the tool
    mis-derives the progress bar (see MEMORY). Update:
      - `current_phase` / `current_phase_name` / `status` / `stopped_at` / `last_updated` /
        `last_activity` / `last_activity_desc` for phase 16.5;
      - the Current Position and Progress lines, cross-checked by hand against the ROADMAP Milestone 3
        table rather than recalculated;
      - the Accumulated Context decisions, with the enablement mechanism, its fail-closed contract, the
        exact operator command, and WHY `ALLOW_WEB_RESEARCH` as an env-var name was deliberately not
        used (the `.env` collision that would have armed production on a routine deploy);
      - BOTH "Still NOT verified live: the `bd682a2` research-lane row-loss fix" paragraphs — resolve
        them if Task 3 verified it, or correct them to say precisely what Plan 02 proved (the contacts
        mirror) and what remains unproven (the literal companies fix), if it did not;
      - the trailing "Next command" paragraph, which currently instructs a rebuild-with-flags-enabled
        that this phase has superseded — replace it with the actual next step;
      - any new live-only bug found in Plan 02 or Plan 03, added to the Blockers/Concerns list with the
        same fidelity as the existing nine (the request shape, the real error text, the root cause) so
        it can be picked up as its own plan.

    (3) ROADMAP.md, scoped edits only — never a whole-file write; it carries three milestones and a
    whole-file rewrite would destroy entries outside the diff window. Update:
      - the Phase 16.5 `**Plans**:` line with the actual plan count and list;
      - each of the seven success criteria marked with its outcome, and criterion 7 specifically
        annotated with WHICH branch was verified live and which was not;
      - the Milestone 3 Progress table row for 16.5.

    (4) THE HONESTY CHECK, and treat it as a real gate rather than a closing formality. Re-read what
    you have written and confirm every "verified" claim points at a quoted value from an execution
    API response, not at a test that passed, a deploy that returned 200, or a piece of reasoning. Any
    claim that cannot survive that test gets reworded to what the evidence actually supports. If the
    companies run was skipped or failed, the record must say the literal `bd682a2` remains
    live-unverified, in those words.

    Commit with `docs(16.5): close phase — enablement mechanism, live research/judge verification, phase record`.
  </action>
  <verify>
    <human-check>STATE.md and ROADMAP.md reflect the actual outcomes; both "Still NOT verified live" paragraphs and the trailing "Next command" paragraph in STATE.md have been resolved or corrected; every criterion in ROADMAP's Phase 16.5 entry carries an outcome with criterion 7 annotated per branch; and every "verified" claim in the SUMMARY points at a quoted execution-API value.</human-check>
  </verify>
  <acceptance_criteria>
    - `16.5-03-SUMMARY.md` exists and satisfies the output block below.
    - `.planning/STATE.md` records the phase-16.5 outcome, the enablement mechanism and its rationale, both `bd682a2` paragraphs resolved or corrected, the superseded "Next command" paragraph replaced, and any new live-only bug added to Blockers/Concerns with its request shape, real error text and root cause.
    - `.planning/ROADMAP.md`'s Phase 16.5 entry carries the actual plan list and an outcome against each of the seven criteria, with criterion 7 annotated to say which branch was verified live; the Milestone 3 Progress table row for 16.5 is updated.
    - ROADMAP.md was edited with scoped replacements; `git diff .planning/ROADMAP.md` shows changes confined to the Phase 16.5 entry and the Milestone 3 table row, with no other phase entry touched.
    - Neither `gsd-tools query state.advance-plan` nor `state.update-progress` was run; the STATE.md progress figure was cross-checked by hand against the ROADMAP Milestone 3 table.
    - The phrase asserting that the literal companies `bd682a2` was verified live appears in the record ONLY if Task 3 produced a quoted non-null `Merge Company` merge from a live execution.
    - `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` are both green at or above the baseline recorded in 16.5-01, with zero regressions.
  </acceptance_criteria>
  <reversibility rating="reversible">Planning-document edits only.</reversibility>
  <done>The phase record states exactly what executed live, what was quoted as evidence, and what remains unproven — with no claim that outruns its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the deploy PUT -> the live ACTIVE `LV Enrichment` | the deployed artifact has no on-disk twin; only an API read-back establishes what is running |
| the companies branch -> real external APIs, for the first time ever | every companies-specific node is untested against a live API; shared nodes were exercised by Plan 02, these were not |
| `onError: continueRegularOutput` on every HTTP node -> downstream Code nodes | a failed external call becomes a normal-looking item and the run reports success while a lane did nothing |
| the phase record -> every future reader | an overstated claim here is indistinguishable from a verified one and is the last place this phase's own failure mode can recur |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.5-13 | Repudiation (overstated record) | closing the phase implying `bd682a2` was verified when only its contacts mirror was, or when the companies run failed | high | mitigate | `<scope_note>` states the distinction; the checkpoint's close-at-criterion-7 option carries the explicit recording obligation; Task 4 (4) is a gate requiring every verified claim to point at a quoted execution-API value, and an acceptance criterion forbids the literal-verification phrase without a quoted non-null `Merge Company` merge |
| T-16.5-14 | Tampering (swallowed failure) | a companies HTTP node failing under `onError: continueRegularOutput` so the lane appears to run | high | mitigate | Task 3 (5)(a)(c) inspect both error surfaces per node and require the fetch diagnostic to be quoted; (6) carries the temporary single-node `onError`-clearing excursion with mandatory same-session restoration |
| T-16.5-15 | Denial of Service (cost) | a second armed window on an ACTIVE workflow | medium | mitigate | Blocking checkpoint authorises it explicitly; caps stay in force and are asserted on the deployed build; Task 3 (7) restores unconditionally with a read-back, and the HALT PROTOCOL makes restore the first action on any abort |
| T-16.5-16 | Tampering (unintended CRM write) | an enabled companies run reaching a HubSpot create or update — the branch's write path has never run live | high | mitigate | Write-safety constants asserted disabled on the DEPLOYED build before firing; zero writes proven three independent ways after (no write node in `runData`, the write-blocked decision, unchanged `hs_lastmodifieddate`) |
| T-16.5-17 | Tampering (planning-document loss) | a whole-file write to ROADMAP.md destroying phase entries outside the diff window | medium | mitigate | Task 4 (3) mandates scoped edits and an acceptance criterion asserts the diff is confined to the Phase 16.5 entry and the Milestone 3 table row |
| T-16.5-18 | Elevation of Privilege (unreviewed hotfix) | patching a discovered live bug in flight during an armed session | medium | mitigate | Frontmatter prohibits source edits; (6) requires stop-record-restore-report with the fix routed to its own plan |
| T-16.5-SC | Tampering (supply chain) | npm/pip installs | low | accept | Zero packages added. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Pre-flight: the chosen company's real properties recorded, the companies research predicate hand-evaluated to a stated GO with its clause, `hs_lastmodifieddate` and credit baselines captured, and the deployed build confirmed disabled.
- Deployed state: a pre-fire read-back confirms both flags enabled, caps unchanged, write safety disabled with empty allowlists, no env expressions, workflow active.
- Execution: `runData` shows the companies fetch-by-id lane resolving an identity, the research gate firing, a genuine research response, the row carrying `existingRecord`/`scored`/`identity_keys`/`object_id` past the research hop and `existingRecord`/`scored` past the judge hop, and a NON-NULL merge at `Merge Company`.
- Zero writes: no create/update node in `runData`, the write-blocked decision, and an unchanged `hs_lastmodifieddate`.
- Restore: a post-run read-back shows both flags disabled and the workflow active, unconditionally.
- Record: STATE.md and ROADMAP.md updated by scoped hand edits, every verified claim backed by a quoted execution-API value, and anything unproven said plainly.
</verification>

<success_criteria>
Plan complete when the companies research-then-judge lane has executed live and `Merge Company`'s
non-null merge has been quoted from the execution API — verifying the literal `bd682a2` for the first
time — or, if the operator closed at criterion 7 or the run surfaced a live-only bug, when that
outcome is recorded in full with the bug's request shape, real error text and root cause; when zero
HubSpot writes are proven three independent ways; when the deployment is restored to the disabled
build and confirmed by read-back; and when STATE.md and ROADMAP.md state exactly what executed live
and what remains unproven, with no claim outrunning its evidence.
</success_criteria>

<source_coverage_audit>
| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP 16.5 goal — the live run verifying the `bd682a2` row-loss fix that has never executed live | Task 3 (5)(d)(f)(g) — on the companies branch the commit actually names |
| REQ | SC-7 live run, research to judge lane executes, row survives every HTTP hop, zero writes, node-by-node inspection | Task 3 (5)(a)-(i); the contacts half is Plan 02 |
| REQ | SC-5 caps and write safety in force | Task 3 (2) on the deployed build, (5)(h)(i) in the run |
| CONTEXT | `bd682a2` is the COMPANIES research-lane fix and has never executed live | `<scope_note>`, must_haves truth 1, Task 3 (5)(g) |
| CONTEXT | scope fence: no write-safety/cap change, no other-workflow activation, no committed enabled JSON | frontmatter prohibitions; Task 3 (2)(7) |
| CONTEXT | deferred: any decision to leave research enabled permanently | Task 3 (7) restores unconditionally; permanence not decided |
| CONTEXT | deferred: the provider jobtitle disagreement rubric | out of scope here (companies branch) |
| USER | the live run is a task with explicit inspection steps | Task 3 (5) |
| USER | verify the research node fired, the row survived every HTTP hop, zero HubSpot writes | Task 3 (5)(b)(d)(f)(g)(h) |
| USER | judging by an HTTP 200 response body is NOT acceptable | Task 3 (3) set-aside + T-16.5-13 |
| USER | plan for the live step to FAIL and need iteration | Task 3 (6); the checkpoint states a recorded finding is the likelier outcome; `<success_criteria>` admits it as completion |
| USER | n8n swallows node errors under `onError: continueRegularOutput` — capture the real error | Task 3 (5)(c) and (6) incl. the temporary single-node clearing excursion |
| USER | do not use `gsd-tools query state.advance-plan` / `state.update-progress` | Task 4 (2) + acceptance criterion |
| STATE | company 9604614548 verified live and read-only by the `hs_object_id` probe | Task 1 (1) |
| STATE | the two "Still NOT verified live" paragraphs and the superseded "Next command" paragraph | Task 4 (2) |
| STATE | nine live-only bugs; record new ones with the same fidelity | Task 4 (2) + T-16.5-14 |
| STATE | project-ownership gotcha — confirm whose key is in `N8N_API_KEY` | `user_setup` note |
</source_coverage_audit>

<output>
Create `.planning/phases/16.5-research-escalation-enablement/16.5-03-SUMMARY.md` when done. It MUST
record: the checkpoint decision taken; the chosen company id with its pre-flight property values and
the hand-evaluated gate predicate; the deploy's printed rewrite plan; the verbatim pre-fire read-back
of the deployed flags, caps and write-safety constants; the execution id; the quoted per-node evidence
for every claim in Task 3 (5), with `Merge Company`'s merge value quoted in full whether null or not;
the three independent zero-write proofs; the verbatim post-restore read-back; every live-only bug
found, with the request shape, the real error text and the root cause; and an explicit,
unambiguous statement of whether the literal companies `bd682a2` is now verified live — the words
"verified live" used only if a non-null `Merge Company` merge was quoted from a real execution.
</output>
</content>
</invoke>
