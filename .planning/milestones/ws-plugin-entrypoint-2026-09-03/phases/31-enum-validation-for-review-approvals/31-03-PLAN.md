---
phase: 31-enum-validation-for-review-approvals
plan: 03
type: execute
wave: 3
depends_on: ["31-01", "31-02"]
files_modified:
  - tests/test_phase31_two_sided_contracts.py
  - .planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-FINDINGS.md
autonomous: false
requirements: [REVIEW-02, REVIEW-05]
user_setup:
  - service: n8n-cloud
    why: "The fix only reaches the running tenant through a disarmed redeploy plus a deactivate/activate bounce of the active workflows — a PUT alone leaves the old body serving (HANDOFF §2.1)."
    env_vars:
      - name: ALLOW_N8N_DEPLOY
        source: "the operator's own shell, for the duration of the disarmed deploy only"
    dashboard_config:
      - task: "Confirm the four workflows are back to their pre-run active states after the bounce"
        location: "n8n Cloud -> Workflows"

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every contract this phase touched is pinned by a test that reads BOTH sides, and the phase can name each pair (ROADMAP criterion 5)."
    - "Committed workflow artifacts are disarmed, verified by the closing gate test rather than by inspection."
    - "The fix reaches the live tenant only through an operator-directed disarmed redeploy followed by a bounce of the active workflows, closed by a disarmed read-back (ROADMAP criterion 5)."
    - "No arming happens anywhere in this phase — none is needed, because nothing here is verified by a live write."
  artifacts:
    - tests/test_phase31_two_sided_contracts.py
    - .planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-FINDINGS.md
  key_links:
    - "scripts/deploy_n8n_workflows.py PUTs without activating -> a deactivate/activate bounce is what makes the running content match the stored content"
    - "scripts/verify_live_write_safety.py --expectation disarmed is the read-back that closes the window"
    - "the RB-9 step 8 re-run needs a FRESH needs_review fixture: the original canary record was cleared 2026-08-03"
---

<objective>
Close the phase. Prove the five-times-burned rule was honoured — every contract this phase touched
is tested from both sides — then get the fix onto the live tenant the only way the milestone
permits: a disarmed redeploy plus a bounce of the active workflows, read back disarmed.

Per CONTEXT § "Process invariants": no arming is needed anywhere in this phase. Committed
artifacts stay disarmed. The live step is operator-directed and never autonomous.

Purpose: the fix is real in the running tenant and the phase leaves no untested seam.
Output: one contract-inventory test, one findings record, one operator-verified deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/HANDOFF.md
@.planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-01-SUMMARY.md
@.planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-02-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol | File | Kind |
|---|---|---|
| `PHASE_31_CONTRACTS`, `test_every_touched_contract_is_pinned_from_both_sides` | `tests/test_phase31_two_sided_contracts.py` | new pytest file |
| `31-FINDINGS.md` | phase directory | new operator-gate record |

Built by sibling plans (do not build here): everything under `n8n/`, `scripts/` and
`operator-claude-plugin/` (31-01 and 31-02).

<tasks>

<task type="auto">
  <name>Task 1: Inventory the phase's two-sided contracts as a test</name>

  <files>tests/test_phase31_two_sided_contracts.py</files>

  <read_first>
- `.planning/workstreams/plugin-entrypoint/HANDOFF.md` §2 item 2 and §3 BUG 31 — the four-then-five recorded instances of "a contract held in two places, tested on only one".
- `operator-claude-plugin/tests/test_control_flag_parity.py` — the idiom.
- `tests/test_hubspot_enums_generated_currency.py` and `operator-claude-plugin/tests/test_review_outcome_parity.py` as built in 31-01 and 31-02 — the two pins that already exist; this test must reference them, not duplicate them.
- `n8n/code/hubspotEnums.js`, `n8n/code/reviewApply.js`, `n8n/code/reviewDecision.js` and `operator-claude-plugin/scripts/review_decision.py` as they now stand.
  </read_first>

  <action>
Create `tests/test_phase31_two_sided_contracts.py` holding a single module-level table
`PHASE_31_CONTRACTS`: one row per contract this phase created or changed, each row naming the two
sides that must agree and the test file that reads both. The rows are:

  1. the pinned HubSpot schema snapshot and `n8n/code/hubspotEnums.generated.js` —
     `tests/test_hubspot_enums_generated_currency.py`
  2. `DEFAULT_COMPANY_POLICY`'s enumeration-typed keys and the generator's `ENUM_PROPERTIES`
     list — `tests/test_hubspot_enums_generated_currency.py`
  3. `reviewApply`'s `invalid` return key and `reviewDecision`'s consumption of it —
     `tests/n8n/hubspotEnumValidation.test.mjs`
  4. `reviewDecision.js`'s outcome words, the committed `wf_review_decision_cloud.json` node's
     emitted outcomes, and the client's `OUTCOMES` tuple —
     `operator-claude-plugin/tests/test_review_outcome_parity.py`
  5. the `Build Review Decision` allowlist pre-check and the committed
     `Review Decision Update Write Gate` — `tests/n8n/reviewAllowlistRefusal.test.mjs`

Write one test that asserts every named test file EXISTS on disk and is non-empty, and one that
asserts each named source file on both sides of each row exists. This is an inventory guard, not a
re-implementation: it fails when a phase-31 pin is deleted or renamed without its row being
updated, which is the failure mode the five recorded instances all share. Do not re-assert the
contracts themselves here — the named files already do that, and a second copy would be a sixth
instance of the very defect.

Then run the phase's closing gate: `tests/test_control_disarmed_artifacts.py`, the full pytest
suite, and the full node suite in FILE form. Record all three counts against the pre-phase
baseline in the summary.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase31_two_sided_contracts.py tests/test_control_disarmed_artifacts.py -q</automated>
  </verify>

  <acceptance_criteria>
- `tests/test_phase31_two_sided_contracts.py` defines `PHASE_31_CONTRACTS` with exactly the five rows listed above, each naming two sides and a test file.
- `.venv/bin/python -m pytest tests/test_phase31_two_sided_contracts.py -q` passes; deleting any one named test file makes it fail (demonstrate on a copy, then restore).
- `.venv/bin/python -m pytest tests/test_control_disarmed_artifacts.py -q` passes.
- `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` totals 0.
- `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` both report totals at or above the pre-phase baseline with zero failures; both counts are written into the summary.
  </acceptance_criteria>

  <done>
Every contract this phase touched has a named test that reads both of its sides, and a deleted pin
fails the suite instead of passing quietly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator gate — disarmed redeploy and bounce</name>

  <precondition>The operator has a shell with `.env` sourced and `N8N_EXPECTED_URL` set to the pinned tenant; the agent cannot read `.env` and must not attempt to.</precondition>

  <reversibility rating="costly">A redeploy replaces stored workflow content on the live tenant. Reversible by redeploying the previous commit and bouncing again — demonstrated working the same day in RB-9 step 9 — but it costs a full deploy cycle and a re-verification.</reversibility>

  <what-built>
31-01 and 31-02 changed `n8n/code/*.js` and `scripts/build_cloud_workflows.py` and regenerated the
committed workflow JSON. Those artifacts are disarmed and every suite passes. The running tenant
still serves the OLD bodies: `deploy_n8n_workflows.py` PUTs without activating, so a workflow that
is currently active keeps executing its previous content until it is bounced (HANDOFF §2.1, proven
live by runs 1122/1123). Nothing in this phase requires arming, and nothing here writes to HubSpot.
  </what-built>

  <how-to-verify>
Run each step as its own `!`-prefixed line, re-sourcing `.env` inside each line.

1. Confirm the starting state, read-only:
   `.venv/bin/python scripts/verify_live_write_safety.py --expectation disarmed`
   Expect `VERDICT: disarmed PASS`. Note the workflow and declaring-node counts it prints —
   derive them here, never compare against a remembered figure.

2. Record which workflows are currently active, so step 4 can restore exactly that set.

3. Deploy DISARMED (no `ENABLE_BAKED_FLAGS`, no arming variable of any kind) with
   `scripts/deploy_n8n_workflows.py`. Expect a `200` per workflow.

4. Bounce every workflow that was active in step 2: deactivate then activate each, and confirm
   each pair returns `200`. This is the step that makes the running content match what was just
   stored; skipping it is the reload gap.

5. Read back: `.venv/bin/python scripts/verify_live_write_safety.py --expectation disarmed`
   Expect `VERDICT: disarmed PASS` again, with a declaring-node count at least as large as
   step 1's — 31-02 added one node that declares the write-safety constants, so a count that
   did NOT grow means the deploy did not land.

6. Confirm the active set matches step 2 exactly.

Then, separately and only when you want it: re-running RB-9 step 8 needs a FRESH `needs_review`
fixture. Company `9604614548` was cleared manually on 2026-08-03, so the original candidate is
gone. One enrichment run against a test company holding a conflicting staged value produces a new
one. That re-run is RB-9's business, not this task's — this task is complete at step 6.

Record the outcome of every step in
`.planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-FINDINGS.md`,
including the two declaring-node counts and anything that did not match the prediction.
  </how-to-verify>

  <resume-signal>Type "approved" with the two read-back verdicts and the two declaring-node counts, or describe what did not match.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> live n8n tenant | committed workflow content becomes running content |
| agent -> credentials | `.env` is permission-blocked to the agent; credentials enter only through the operator's own shell lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-11 | Tampering | the live redeploy | critical | mitigate | operator-directed only, disarmed, no `ENABLE_BAKED_FLAGS`, closed by `verify_live_write_safety.py --expectation disarmed` on both sides of the deploy |
| T-31-12 | Elevation of privilege | an accidental arming during the deploy | critical | mitigate | this phase needs no armed window; no arming variable is exported at any step, and the read-back asserts every write-enabling boolean is disabled wherever it is declared |
| T-31-13 | Repudiation | a deploy that silently did not land | high | mitigate | the step-5 declaring-node count must have grown, because 31-02 adds a declaring node — an unchanged count is the deploy failing quietly |
| T-31-14 | Spoofing | deploying into the wrong tenant | high | mitigate | `N8N_EXPECTED_URL` is set and pins the tenant to an exact match; the host-suffix fallback is not relied on |

No package-manager installs (npm/pip/cargo) occur in this plan, so no package-legitimacy
checkpoint applies.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` pass at or above the pre-phase baseline.
- `tests/test_control_disarmed_artifacts.py` passes and `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` totals 0.
- Two live `disarmed PASS` verdicts are recorded in `31-FINDINGS.md`, before and after the deploy.
- The active workflow set after the bounce matches the set recorded before it.
</verification>

<success_criteria>
- ROADMAP criterion 5 satisfied end to end: both-sides tests exist for every touched contract, committed artifacts are disarmed, and the live tenant received the fix by a disarmed redeploy plus a bounce.
- `31-FINDINGS.md` exists and records both read-back verdicts and both declaring-node counts.
- No arming occurred at any point in the phase.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/31-enum-validation-for-review-approvals/31-03-SUMMARY.md` when done.
Record: the two read-back verdicts, the two declaring-node counts, the active-set comparison, and
the note that RB-9 step 8's re-run still needs a fresh `needs_review` fixture.
</output>
