---
phase: 34-header-mapping-tolerance
plan: 04
type: execute
wave: 3
depends_on: ["34-03"]
files_modified:
  - .planning/workstreams/plugin-entrypoint/STATE.md
  - operator-claude-plugin/UAT.md
  - operator-claude-plugin/CHANGELOG.md
  - operator-claude-plugin/.claude-plugin/plugin.json
autonomous: false
requirements: [INGEST-02, STRUCT-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The scope amendment is entry 6 in STATE.md's 'Accepted requirement amendments' table, worded per 34-CONTEXT.md §4: suggestion with per-header operator confirmation is permitted in the client, and silent client-side column mapping remains excluded (ROADMAP criterion 6, DoD 4)."
    - "The table's own preamble no longer says 'Five places' — a count that contradicts its rows is how a reader concludes the table is decorative."
    - "`operator-claude-plugin/.claude-plugin/plugin.json`'s `version` is bumped to `0.8.0` in the SAME commit as the CHANGELOG cut. An unbumped version means Claude Desktop's Update button stays greyed out no matter what shipped (ROADMAP criterion 7, DoD 6)."
    - "All four gate commands are green at the release commit: plugin suite, full python suite, node suite, and the disarmed-artifact grep printing 0 (ROADMAP criterion 7)."
    - "UAT 2.2's Result cell records the fix as shipped and AWAITING the operator's re-walk — never as PASS. A verified fix and an observed pass are different claims, which is the discipline this milestone runs on (ROADMAP criterion 7, DoD 5)."
    - statement: "The marketplace clone is refreshed and the operator's installed plugin actually offers the update."
      verification: backstop
    - statement: "The operator re-walks UAT 2.2 against `22-messy-headers.csv` and re-marks it themselves."
      verification: backstop
  artifacts:
    - .planning/workstreams/plugin-entrypoint/STATE.md
    - operator-claude-plugin/CHANGELOG.md
  key_links:
    - "The version bump and the CHANGELOG cut must be ONE commit. Separated, the pushed state can carry shipped work under a version string that already exists, and the marketplace clone will happily serve it as 'no update available'."
    - "Refreshing the marketplace clone is a `git -C ~/.claude/plugins/marketplaces/...` operation on the operator's own machine. Non-negotiable 5 forbids tests and scripts from touching `~/.claude/plugins/`; this is the documented release step, performed by the operator at the checkpoint, not automated here."
    - "Flipping 2.2 to PASS would make the whole UAT record worth less than the tests it is supposed to backstop. The Result cell says fixed-awaiting-walk; the operator's own walk is what changes it."
  prohibitions:
    - statement: "UAT 2.2 is not marked PASS by this plan — only the operator marks it."
      status: enforced
      verification: "`grep -n '^| 2.2 ' operator-claude-plugin/UAT.md` shows a Result cell containing an awaiting-walk marker and no `**PASS**`; task 3's checkpoint hands the mark to the operator."
    - statement: "No arming or `ENABLE_BAKED_FLAGS` change is made."
      status: enforced
      verification: "`files_modified` lists four documentation and manifest paths; `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = \"true\"' n8n/*.json` prints 0 at the release commit."
    - statement: "No header is rewritten without an explicit per-header operator confirmation, and the client never maps data values."
      status: enforced
      verification: "Recorded, not re-implemented: the STATE.md amendment states the boundary in prose and the enforcing tests were red-checked in 34-01 and 34-03."
    - statement: "No name-splitter is introduced."
      status: enforced
      verification: "The CHANGELOG entry states the `Full Name` outcome as a refusal with its reason, not a capability."
---

<objective>
Record what moved, ship it, and hand the verdict back to the operator.

Half B put a suggestion layer in the client, which touches REQUIREMENTS.md's Out of Scope line
about column mapping living only in n8n. That line moved deliberately and must say so in the one
table this workstream keeps for exactly that purpose — five amendments already sit there with
this shape, and a scope line that moves silently is how the next reader concludes the exclusion
never meant anything.

Then the release: version bumped in the same commit as the CHANGELOG cut, suites green, clone
refreshed. And then the part this plan explicitly does not do — mark UAT 2.2. The fix is
verified; the pass is the operator's to observe.

Purpose: INGEST-02 and STRUCT-04 reach an installed operator, and the scope move is on the record.
Output: STATE.md amendment 6, a re-marked UAT row awaiting its walk, a `0.8.0` CHANGELOG cut and
version bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/plugin-entrypoint/phases/34-header-mapping-tolerance/34-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/STATE.md
@.planning/workstreams/plugin-entrypoint/REQUIREMENTS.md
@operator-claude-plugin/CHANGELOG.md
@operator-claude-plugin/UAT.md
</context>

## Artifacts this plan produces

The full phase-wide table lives in `34-01-PLAN.md` § "Artifacts this phase produces". This plan
contributes exactly:

| Artifact | Change |
|---|---|
| `.planning/workstreams/plugin-entrypoint/STATE.md` | Row 6 in "Accepted requirement amendments"; the preamble's count corrected |
| `operator-claude-plugin/UAT.md` | Row 2.2's Result cell — fixed, awaiting the operator's re-walk |
| `operator-claude-plugin/CHANGELOG.md` | `## [0.8.0] - <date>` section; `## [Unreleased]` left on top and empty |
| `operator-claude-plugin/.claude-plugin/plugin.json` | `version` `0.7.3` → `0.8.0` |

**No new symbols, files, flags or aliases.** This plan writes prose and one version string.

<tasks>

<task type="auto">
  <name>Task 1: Amendment 6 on the record, and UAT 2.2 marked fixed-awaiting-walk</name>

  <read_first>
    - `.planning/workstreams/plugin-entrypoint/STATE.md` — the "Accepted requirement amendments" section in full: its preamble sentence, its column headers, and all five existing rows, so row 6 matches their shape rather than inventing one
    - `.planning/workstreams/plugin-entrypoint/REQUIREMENTS.md` — the "Out of Scope (explicit exclusions)" section, specifically the "Re-implementing column mapping, phone/email normalization, verification, or dedupe" line this amendment qualifies
    - `34-CONTEXT.md` § 4 — the amendment's required wording, verbatim
    - `operator-claude-plugin/UAT.md` — row 2.2 and its neighbours; note how rows 2.4/2.5 carry a pending marker and how 2.1/2.3 carry a walked PASS, so the new mark reads as neither
  </read_first>

  <files>.planning/workstreams/plugin-entrypoint/STATE.md, operator-claude-plugin/UAT.md</files>

  <action>
Add row 6 to STATE.md's "Accepted requirement amendments" table, in the same four-column shape the
five existing rows use. The Requirement column names the REQUIREMENTS.md Out of Scope line this
qualifies. The Amendment column carries 34-CONTEXT.md §4's wording — header-alias SUGGESTION with
per-header operator confirmation is permitted in the client; silent client-side column mapping
remains excluded; the backend's `Map Columns` stays the single authority on what a header means;
the client only helps the operator produce a file the backend can read and never rewrites a
header without an explicit yes. Do not paraphrase it shorter. The Source column names Phase 34 and
the decision's origin.

Correct the preamble sentence above the table, which currently says "Five places". A count that
contradicts the rows beneath it is the small kind of wrong that teaches a reader the table is
decorative, and this table's whole value is that it is not.

Then re-mark UAT 2.2's Result cell. It must say three things and no more: the alias half is
fixed and names the three aliases now in the table; the two headers the criterion could not cover
deterministically are handled by suggest-and-confirm (`Ph.`) and an honest refusal (`Full Name`);
and the row is AWAITING the operator's re-walk. Do not write `**PASS**`. Do not write anything
that could be read as one. A verified fix and an observed pass are different claims, and this
milestone's entire method is that the second one is only ever made by the person who observed it.
Keep the reference to the originating todo so the trail stays walkable.

Commit both files by explicit path.
  </action>

  <verify>
    <automated>grep -c '^| 6 |' .planning/workstreams/plugin-entrypoint/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^| 6 |' .planning/workstreams/plugin-entrypoint/STATE.md` prints 1.
    - `grep -n 'Five places' .planning/workstreams/plugin-entrypoint/STATE.md` returns nothing.
    - Row 6's Amendment cell contains both halves of the boundary — that suggestion with per-header confirmation is permitted, and that silent client-side column mapping remains excluded. Both phrases are present; a row carrying only the permission is the silent scope move this row exists to prevent.
    - `grep -n '^| 2.2 ' operator-claude-plugin/UAT.md` returns one line whose Result cell contains an awaiting-walk marker and does NOT contain `**PASS**`.
    - The 2.2 Result cell names `e-mail address`, `org.` and `linkedin profile`, and names both `Ph.` and `Full Name` with their respective outcomes.
    - The commit names both paths explicitly.
  </acceptance_criteria>

  <done>The scope move is on the record in the table this workstream keeps for it, and UAT 2.2 says exactly what is true: fixed, not yet walked.</done>
</task>

<task type="auto">
  <name>Task 2: Cut `0.8.0` — CHANGELOG and version in one commit, all four gates green</name>

  <reversibility rating="costly">A published version string can only be superseded by another bump; an operator who pulls a bad `0.8.0` needs `0.8.1` rather than a revert. Reversible, but not for free.</reversibility>

  <read_first>
    - `operator-claude-plugin/CHANGELOG.md` — the `## [Unreleased]` block, the `## [0.7.3]` section's shape, and the release checklist at the bottom in full
    - `operator-claude-plugin/.claude-plugin/plugin.json` — the current `version` value
    - `34-CONTEXT.md` § 5 item 6 and § 7 — the release checklist and the four exact test commands
    - the SUMMARY files from 34-01, 34-02 and 34-03 — the CHANGELOG entry describes what those three plans actually shipped, not what this plan assumes they shipped
  </read_first>

  <files>operator-claude-plugin/CHANGELOG.md, operator-claude-plugin/.claude-plugin/plugin.json</files>

  <action>
Write the `## [0.8.0] - <today>` section, moving the shipped work out from under `## [Unreleased]`
and leaving that heading on top and empty, exactly as the checklist's step 2 requires.

The entry covers three things an operator would notice. First, the three new deterministic
aliases, named, with the note that the backend was redeployed and bounced so the running
`Map Columns` node knows them — this half is not client-only and the CHANGELOG's own preamble
says backend changes live in the repo-root file, so say plainly that the client change here is the
widened prediction and point at the backend redeploy rather than claiming it. Second, the
suggest-and-confirm step: a header the alias table does not know is now proposed with that
column's own sample values beside it and rewritten only after a per-header yes, and the corrected
file is what gets previewed and sent. Third, `Full Name`: refused with its reason named, because
this system deliberately has no name-splitter. State that third item as a refusal, not as a
capability — a CHANGELOG line that reads like a feature is how the next reader goes looking for
the splitter.

Bump `.claude-plugin/plugin.json`'s `version` from `0.7.3` to `0.8.0`. New behaviour with no
breaking change to an existing operator flow is a minor bump against this client's own surface,
not the backend's milestone number.

Both edits go in ONE commit, named by explicit path. Separated, the pushed state can carry shipped
work under a version string that already exists, and the clone will serve it as "no update
available" — the failure the checklist exists because of.

Before committing, run all four gate commands from CONTEXT.md §7 in order and paste their output
into the SUMMARY as literal output rather than a claim.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/ -q && .venv/bin/python -m pytest -q && node --test tests/n8n/*.test.mjs && grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` reports at least 960 passed, 5 skipped, plus this phase's new tests, with zero failures.
    - `.venv/bin/python -m pytest -q` reports at least 1841 passed, 6 skipped, plus this phase's new tests, with zero failures.
    - `node --test tests/n8n/*.test.mjs` reports at least 550 passing, with zero failures.
    - `grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json` prints 0 for every file.
    - `.venv/bin/python -c "import json; print(json.load(open('operator-claude-plugin/.claude-plugin/plugin.json'))['version'])"` prints `0.8.0`.
    - `grep -c '^## \[0.8.0\]' operator-claude-plugin/CHANGELOG.md` prints 1, and the `## [Unreleased]` heading is still present with no entries under it.
    - `git show --stat HEAD` for the release commit lists both `operator-claude-plugin/CHANGELOG.md` and `operator-claude-plugin/.claude-plugin/plugin.json` — one commit, both files.
    - The SUMMARY carries the literal output of all four gate commands.
  </acceptance_criteria>

  <done>`0.8.0` is cut with its version bumped in the same commit, and every gate is green at that commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator refreshes the clone, re-walks UAT 2.2, and marks it themselves</name>
  <what-built>
    Phase 34 in full. `e-mail address`, `org.` and `linkedin profile` map deterministically in the
    repo YAML, the plugin's shipped copy and `n8n/code/columnMap.js`, pinned equal by
    `tests/n8n/columnMapAliasParity.test.mjs`, rebuilt, deployed disarmed and bounced into the
    running backend. `scripts/header_suggest.py` proposes a canonical prop for a header the table
    does not know, shows that column's own sample values beside the proposal, rewrites nothing
    without a per-header yes, refuses `Full Name` with its reason, and writes a corrected copy
    whose header row is the only thing that changed. contact-upload SKILL.md step 2b runs it
    before the preview, so the one preview the operator sees is of the file that will be sent.
    STATE.md amendment 6 records the scope move. `0.8.0` is cut with its version bumped in the
    same commit, all four gates green.
  </what-built>
  <how-to-verify>
    1. **Push and refresh the clone** — the two steps this plan deliberately did not automate,
       because non-negotiable 5 keeps tests and scripts out of `~/.claude/plugins/`:
       ```
       git push
       git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator fetch --depth=1 origin master
       git -C ~/.claude/plugins/marketplaces/lightning-visuals-operator reset --hard FETCH_HEAD
       ```
       Then confirm Claude Desktop offers the update. A greyed-out Update button means either the
       version did not bump or the clone did not fetch — check both before assuming either.
    2. **Update the plugin and verify by CONTENT, not by version string.** The version in
       `plugin.json` is hand-written; the thing that proves the new code installed is that the
       installed copy's `scripts/` directory contains `header_suggest.py`.
    3. **Re-walk UAT 2.2.** In a fresh Claude session, hand the plugin
       `operator-claude-plugin/tests/samples/22-messy-headers.csv`. Expect: `E-mail Address`,
       `Org.`, `Position` and `LinkedIn Profile` all shown mapping with no action from you; one
       question about `Ph.`, with the phone-shaped values from that column shown beside it, asked
       on its own and not bundled with anything else; `Full Name` refused with a reason that names
       why and offers no split; `Notes` reported as dropped. Confirm `Ph.` → `phone` and check the
       re-preview shows five of seven mapping.
    4. **Then mark 2.2 yourself** in `operator-claude-plugin/UAT.md`. This plan left it
       fixed-awaiting-walk on purpose. If what you saw does not match step 3, say what differed
       rather than approving — the criterion is "reads them without you renaming anything first",
       and a confirmation turn you had to answer is a deliberate, recorded amendment to that
       criterion (STATE.md row 6), not an unnoticed shortfall.
  </how-to-verify>
  <resume-signal>Type "approved" once the update installed, the walk matched, and you have marked 2.2 — or describe what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit → installed operator | A version string the operator's client compares decides whether shipped code ever reaches them |
| verified fix → claimed pass | A test result crossing into a UAT record is a different claim from an observed walk |
| documented scope → implemented scope | An exclusion in REQUIREMENTS.md and a shipped client capability can disagree silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-15 | Repudiation | a scope line that moved without a record | high | mitigate | STATE.md row 6 carries 34-CONTEXT.md §4's wording including both halves — what is permitted AND what remains excluded — and the preamble count is corrected so the table cannot read as stale |
| T-34-16 | Spoofing | a UAT row claiming an observation nobody made | high | mitigate | Task 1's acceptance criteria forbid `**PASS**` in row 2.2; task 3 hands the mark to the operator after their own walk |
| T-34-17 | Tampering | shipped work stranded under an unbumped version | medium | mitigate | The bump and the CHANGELOG cut are one commit, asserted by `git show --stat`; task 3 has the operator verify the installed copy by CONTENT (`header_suggest.py` present) rather than by the hand-written version string |
| T-34-18 | Information Disclosure | automating a write into the operator's real plugin directory | medium | mitigate | The clone refresh is performed by the operator at the checkpoint, never by a script — non-negotiable 5 keeps tests and scripts out of `~/.claude/plugins/`, where real config and live credentials live |
| T-34-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this phase — 34-RESEARCH.md's Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
```
grep -c '^| 6 |' .planning/workstreams/plugin-entrypoint/STATE.md      # 1
grep -n 'Five places' .planning/workstreams/plugin-entrypoint/STATE.md # no match
grep -n '^| 2.2 ' operator-claude-plugin/UAT.md                         # awaiting-walk, no **PASS**
.venv/bin/python -m pytest operator-claude-plugin/tests/ -q             # >= 960/5 + new
.venv/bin/python -m pytest -q                                           # >= 1841/6 + new
node --test tests/n8n/*.test.mjs                                        # >= 550
grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json                      # 0 everywhere
```
</verification>

<success_criteria>
- STATE.md carries amendment 6 with both halves of the boundary, and the preamble count matches.
- UAT 2.2 says fixed-awaiting-walk and never PASS.
- `0.8.0` cut with the version bumped in the same commit; all four gates green at that commit.
- The operator has pushed, refreshed the clone, verified the install by content, re-walked 2.2 and
  marked it themselves.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/34-header-mapping-tolerance/34-04-SUMMARY.md` when done.
</output>
