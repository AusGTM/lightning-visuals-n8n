---
phase: 37-enrich-before-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - operator-claude-plugin/scripts/enrichment.py
  - operator-claude-plugin/scripts/chunking.py
  - operator-claude-plugin/config/operator.local.example.json
  - operator-claude-plugin/tests/test_rows_envelope_contract.py
  - operator-claude-plugin/tests/test_chunking.py
  - operator-claude-plugin/tests/test_retry_reuses_dispatch.py
  - tests/n8n/rowsEnvelopeContract.test.mjs
  - tests/test_match_ceiling_contract.py
autonomous: true
requirements: [STRUCT-01, PREVIEW-03, DISPATCH-03]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`enrichment.build_envelope({'rows': [...], 'object_type': 'contacts'}, providers)` emits `events` as a flat array of lookup-key objects — `row_id` plus `objectType` plus only the keys in `enrichment.MATCH_LOOKUP_KEYS` — and the literal is byte-identical to the one `tests/n8n/rowsEnvelopeContract.test.mjs` feeds to the backend's own `parseWebhookBody`."
    - "The exact rows envelope the client emits, run through the backend's `parseWebhookBody` and `Build Identity`'s identity-key derivation, yields `laneOf(...) === 'name'` — the MEDIUM match lane — never `'none'`. A field-name mistake that routed every row to `'none'` would be silently skipped by the enrichment gate while both suites stayed green; that is the D-19 class this pin exists to catch."
    - "A rows envelope carries `mode: 'propose'` structurally — set inside `build_envelope`'s rows branch, not supplied by a caller — so a rows form can never reach the backend's write path."
    - "`plan_chunks` on a rows spec splits `spec['rows']` positionally at the ceiling exactly as the existing branch splits `spec['record_ids']`; a 3-row spec at ceiling 2 yields row counts (2, 1) and no chunk ever exceeds the ceiling."
    - "`plan_chunks` raises `ChunkPlanError` on an empty rows list rather than planning a chunk that would produce an empty `events` array — the backend refuses an empty array with a single non-row-shaped item that carries no `row_id` to join on."
    - "`chunk_ceiling(config, key='max_rows_per_match_request')` reads that key with the SAME no-fallback refusal the default key gets — absent, non-int, or below 1 raises."
    - "`operator.local.example.json` ships `max_rows_per_match_request` equal to the backend's landed `ENRICH_MAX_PROPOSE_RECORDS` BY CONSTRUCTION — the value is read out of `scripts/build_cloud_workflows.py`, never computed from an independent client-side formula, so the two cannot drift into disagreeing about the same bound."
    - "The value is still marked PROVISIONAL: 20 is the backend's own conservative propose-mode bound and no live propose run has measured per-row latency for that path. The note names the probe that retires the label, and states that a measured raise moves both numbers together, backend first."
    - "`tests/test_match_ceiling_contract.py` pins the client's match ceiling at or below `ENRICH_MAX_PROPOSE_RECORDS`, and fails loud rather than passing quietly if that constant cannot be found. The operator is `<=` rather than `==` precisely so the prescribed backend-first raise leaves a legal intermediate state instead of a red suite."
    - "The match ceiling's note carries the provisional marker; the waterfall ceiling's notes must not gain it. The two keys carry opposite provenance claims and the two tests assert opposite things about the same word."
    - "`test_retry_reuses_dispatch.py`'s send-shaped scan sees a function whose `transport` parameter defaults to the bare `requests` module and whose body calls `transport.post`/`transport.put` — so `enrichment.dispatch_enrichment` appears in the scan for the first time and is allowlisted deliberately, with reasoning, rather than being invisible."
    - "`events` order equals the input rows' order, and `row_id` is the join key downstream so ordering is never load-bearing."
    - "A row value that is `None` or an empty string is emitted as `null` in the envelope, never as the string `\"None\"`."
    - statement: "The rows-spec cost figure the operator approves is an upper bound on the actual spend for a pre-ingest batch — Lusha priced at its first-time rate with no stored-id discount assumed."
      verification: backstop
  artifacts:
    - operator-claude-plugin/scripts/enrichment.py
    - operator-claude-plugin/scripts/chunking.py
    - operator-claude-plugin/config/operator.local.example.json
    - operator-claude-plugin/tests/test_rows_envelope_contract.py
    - tests/n8n/rowsEnvelopeContract.test.mjs
    - tests/test_match_ceiling_contract.py
  key_links:
    - "`enrichment.build_envelope` rows branch -> `tests/n8n/rowsEnvelopeContract.test.mjs` -> `n8n/code/providerSelection.js::parseWebhookBody` -> `n8n/code/matchProposal.js::laneOf` — the py<->js seam that shipped the D-19 bug once."
    - "`operator.local.example.json::max_rows_per_match_request` -> `tests/test_match_ceiling_contract.py` -> `scripts/build_cloud_workflows.py::ENRICH_MAX_PROPOSE_RECORDS` — the cross-repo pin. The waterfall key's own pin still targets `ENRICH_MAX_LIST_RECORDS`; two keys, two constants, two tests."
    - "`chunking.chunk_ceiling(config, key=)` -> `preingest.match_batch` (plan 37-03) — the key parameter is what lets match read its own ceiling without inheriting the waterfall's refusal wording."
  prohibitions:
    - "MUST NOT reuse `max_records_per_chunk` as the match ceiling. It is 2 because a full-waterfall record took 37.44 s; a match call runs no provider and no model. Fusing them makes a match refusal print waterfall-timing wording that is untrue of the call that raised it."
    - "MUST NOT present the provisional match ceiling as a measured one. 20 is the backend's own conservative bound, not an observed per-row latency, and a number that reads as measured when it is not is how a wrong bound survives a review."
    - "MUST NOT compute the client match ceiling from an independent client-side formula. Two files deriving the same bound by different routes agree only until one of them is edited, and the disagreement surfaces as a refusal for a batch the preview already approved."
    - "MUST NOT write the rows envelope so that a caller can supply `mode` — a rows form describes records that are NOT in HubSpot, and a caller-supplied `mode: 'write'` would ask the backend to create them, which is exactly the incomplete-contact outcome the phase's governing rule forbids."
---

<objective>
Land the wire shape and the ceiling this phase's whole client half is built on, and make the
arming guard able to see the send path it has never seen.

Purpose: the riskiest thing in Phase 37 is not any single function — it is the seam. A rows
envelope whose field names the backend does not read routes every row to lane `none`, where the
enrichment gate skips it, and both test suites stay green. That exact class of bug (D-19,
flat-vs-nested `list`) shipped once and killed the whole list lane. This plan proves the seam
first, on the agent's best early-context tokens, before anything is built on top of it.

Output: `enrichment.build_envelope`'s rows branch pinned byte-identically from both languages;
`chunking` able to plan and re-batch a rows spec against its own configured ceiling;
`test_retry_reuses_dispatch.py`'s AST guard extended so its own failure message becomes true of
the code it guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/plugin-entrypoint/ROADMAP.md
@.planning/workstreams/plugin-entrypoint/STATE.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-CONTEXT.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-RESEARCH.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-PATTERNS.md
@.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-VALIDATION.md
</context>

## Artifacts this phase produces

The whole phase, so every plan reads the same list. This plan builds the rows-marked entries.

| Artifact | Kind | Plan |
|---|---|---|
| `operator-claude-plugin/scripts/preingest.py` | new module | 37-03 |
| `preingest.rows_from_table(path)` | pure | 37-04 |
| `preingest.build_rows_spec(rows)` | pure | 37-03 |
| `preingest.fetch_matches(chunk, config, transport=requests.post)` | network, unarmed | 37-03 |
| `preingest.match_batch(plan, config)` | network loop | 37-03 |
| `preingest.classify_matches(rows, response)` | pure | 37-03 |
| `preingest.apply_match_decisions(classified, resolved)` | pure | 37-04 |
| `preingest.merge_enriched(rows, responses)` | pure | 37-04 |
| `preingest.render_enriched_preview(...)` | pure | 37-05 |
| `extraction.hold_emailless(rows)` | pure | 37-02 |
| `enrichment.MATCH_LOOKUP_KEYS` | frozen constant | **37-01** |
| `enrichment.build_envelope` rows branch | edit | **37-01** |
| `chunking.chunk_ceiling(config, key=)` + rows branches | edit | **37-01** |
| `config_gate.CAPABILITY_KEYS["match"]` | new capability key | 37-03 |
| `operator.local.example.json::max_rows_per_match_request` | config key | **37-01** |
| `skills/enrich-before-ingest/SKILL.md` | new skill | 37-06 |
| `tests/n8n/rowsEnvelopeContract.test.mjs` | new test | **37-01** |
| `operator-claude-plugin/tests/test_rows_envelope_contract.py` | new test | **37-01** |
| `tests/test_match_ceiling_contract.py` | new test | **37-01** |
| `operator-claude-plugin/tests/test_extraction_email_gate.py` | new test | 37-02 |
| `operator-claude-plugin/tests/test_preingest_match.py` | new test | 37-03 |
| `operator-claude-plugin/tests/test_preingest_merge.py` | new test | 37-04 |
| `operator-claude-plugin/tests/test_enrich_before_ingest_skill_contract.py` | new test | 37-06 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The rows envelope, pinned byte-identical from both languages</name>
  <reversibility rating="costly">The rows envelope is a cross-repo wire contract: changing a
  field name later means changing the Python branch, the Python pin, the JS pin, and every
  caller in one coordinated commit. No migration and no published external contract, so not
  one-way.</reversibility>

  <read_first>
    - `operator-claude-plugin/scripts/enrichment.py` — `build_envelope` (line 140), especially
      the docstring's D-19 paragraph explaining why the `list` envelope is nested; the
      `record_ids` branch (lines 177-189) is the dict-comprehension-over-a-list shape this new
      branch mirrors; `normalize_object_type` (line 127).
    - `operator-claude-plugin/tests/test_list_envelope_contract.py` — the Python half of the
      existing pair, in full. The new file is its sibling.
    - `tests/n8n/listEnvelopeContract.test.mjs` — the JS half of the existing pair, in full.
    - `n8n/code/providerSelection.js` — `parseWebhookBody` (line 38) and `module.exports`
      (line 108). This is what the JS pin imports.
    - `n8n/code/matchProposal.js` — `laneOf` (line 30) and `module.exports` (line 137).
    - `scripts/build_cloud_workflows.py` lines 933-958 — `ENRICH_BUILD_IDENTITY`'s wrapper,
      which is where `row.firstname` / `row.lastname` / `row.company` / `row.email` become
      `identity_keys.firstName` / `.lastName` / `.companyName` / `.email` and then `lane`.
      These four field names ARE the contract.
    - `37-RESEARCH.md` § "PHASE 36 SHIPPED-VS-ASSUMED CONTRACT CHECK" items 2 and 6, and
      § C.14.
    - `37-PATTERNS.md` § "py↔js envelope-contract pair".
  </read_first>

  <files>operator-claude-plugin/scripts/enrichment.py, operator-claude-plugin/tests/test_rows_envelope_contract.py, tests/n8n/rowsEnvelopeContract.test.mjs</files>

  <behavior>
    - `build_envelope({"rows": [{"row_id": "r1", "firstname": "Jane", "lastname": "Doe", "company": "GCTC"}], "object_type": "contacts"}, [])` returns
      `{"providers": [], "mode": "propose", "events": [{"row_id": "r1", "objectType": "contacts", "email": None, "firstname": "Jane", "lastname": "Doe", "company": "GCTC"}]}`.
    - A row carrying `phone`, `jobtitle` or `linkedin_url` produces an event dict with none of
      those keys — the projection is `MATCH_LOOKUP_KEYS`, not the row's own key set.
    - A row with no `row_id` raises `RecordSpecError` naming the join-key rule.
    - An empty rows list raises `RecordSpecError`.
    - `object_type` normalizes through the existing table (`"contact"` -> `"contacts"`).
    - A row value of `""` or `None` emits `None`, never the string `"None"`.
    - The JS half: `parseWebhookBody(CLIENT_ENVELOPE)` returns `mode === "propose"`,
      `providers` deep-equal `[]`, and `events.length === 1`; deriving `identity_keys` from
      that event exactly as `ENRICH_BUILD_IDENTITY` does and calling `laneOf` yields `"name"`.
    - The JS half's regression counterpart: an envelope whose per-event keys use the camelCase
      spellings (`firstName`/`lastName`/`companyName`) instead of the flat HubSpot-property
      spellings routes to lane `"none"` — asserted as the failure this pin exists to catch.
  </behavior>

  <action>
Add a module-level frozen constant `MATCH_LOOKUP_KEYS = ("email", "firstname", "lastname", "company")`
to `enrichment.py`, directly above `build_envelope`, with a comment naming it the frozen lookup
allowlist: these are the only row fields that cross the boundary on a rows envelope, because
they are the only ones `Build Identity` reads into `identity_keys` and the only ones the match
search filters on. State that widening it widens what leaves the operator's machine.

Add a `rows` branch to `build_envelope`, placed after the `list` branch and before the
`record_ids` branch. It accepts `{"rows": [...], "object_type": ...}`. Refuse a non-list or
empty `rows` value with `RecordSpecError`, wording it like the existing empty-`record_ids`
refusal. Refuse a row missing a non-empty `row_id` with `RecordSpecError` that names why:
`row_id` is the join key, and a row without one can never be matched back to its response.

Set `envelope["mode"] = "propose"` inside this branch only — never read from `spec`, never a
parameter. Extend the `build_envelope` docstring with a paragraph explaining that a rows form
describes records that are NOT in HubSpot, so the write mode is structurally unreachable from
this shape rather than being a caller's responsibility.

Build `envelope["events"]` as one dict per row, in input order:
`{"row_id": str(row["row_id"]), "objectType": normalize_object_type(spec.get("object_type"))}`
followed by one key per name in `MATCH_LOOKUP_KEYS`, whose value is the row's value for that
key normalized to `None` when it is absent, `None`, or a string that strips to empty. Do not
copy any other key off the row.

Create `operator-claude-plugin/tests/test_rows_envelope_contract.py` mirroring
`test_list_envelope_contract.py`'s structure exactly: a module docstring naming the D-19 bug
and stating that the JS twin is `tests/n8n/rowsEnvelopeContract.test.mjs`; a module-level
`CLIENT_ENVELOPE` literal with the comment "Keep byte-identical with the JS twin"; a test
asserting `build_envelope` PRODUCES exactly that literal; plus tests for the excluded-key
projection, the missing-`row_id` refusal, the empty-rows refusal, the object-type
normalization, the input-order preservation, and the empty-value-to-`None` rule.

Create `tests/n8n/rowsEnvelopeContract.test.mjs` mirroring `listEnvelopeContract.test.mjs`:
require `parseWebhookBody` from `n8n/code/providerSelection.js` and `laneOf` from
`n8n/code/matchProposal.js`; declare the SAME `CLIENT_ENVELOPE` literal with the same
keep-byte-identical comment; assert the backend ACCEPTS it (mode, providers, events length);
then reproduce `ENRICH_BUILD_IDENTITY`'s identity-key derivation inline over the parsed event
(a small helper in the test file, commented as mirroring `build_cloud_workflows.py`'s
`ENRICH_BUILD_IDENTITY` wrapper) and assert `laneOf` returns `"name"`. Add the camelCase
regression test asserting the wrong spelling yields `"none"`, with a comment stating that a
row in lane `none` is skipped by the enrichment gate and returns a clean 200, which is why
this cannot be left to either side's own suite.

RED-CHECK, explicitly: before wiring the branch, run the two new test files and confirm the
Python pin fails on `RecordSpecError` (no rows branch yet) and the JS pin fails its `laneOf`
assertion. After the branch lands and both pass, temporarily rename the event key `lastname`
to `lastName` in `build_envelope`'s rows branch, re-run BOTH files, confirm the Python pin
fails on the literal comparison AND the JS pin fails on `laneOf` returning `"none"`, then
restore. Record in the SUMMARY that both halves went red on the same single-character change —
that is the property, not the coverage.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_rows_envelope_contract.py operator-claude-plugin/tests/test_enrichment_envelope.py operator-claude-plugin/tests/test_list_envelope_contract.py -q && node --test tests/n8n/rowsEnvelopeContract.test.mjs tests/n8n/listEnvelopeContract.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_rows_envelope_contract.py -q` exits 0.
    - `node --test tests/n8n/rowsEnvelopeContract.test.mjs` exits 0.
    - The `CLIENT_ENVELOPE` literal in `operator-claude-plugin/tests/test_rows_envelope_contract.py`
      and the one in `tests/n8n/rowsEnvelopeContract.test.mjs` describe the same object — same
      keys, same values, same event-key set.
    - Renaming any single event key in `build_envelope`'s rows branch makes BOTH new test files
      fail; the SUMMARY records the observed failure text from each.
    - `enrichment.MATCH_LOOKUP_KEYS` evaluates to a 4-tuple and `"phone" not in enrichment.MATCH_LOOKUP_KEYS`.
    - `enrichment.build_envelope({"rows": [], "object_type": "contacts"}, [])` raises `RecordSpecError`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` reports no fewer than 1052
      passed (the pre-plan baseline) and 5 skipped.
  </acceptance_criteria>

  <done>A rows envelope built by the client is proven, from the backend's own parser and lane
  router, to reach the MEDIUM match lane — and a one-character field-name mistake takes both
  language halves red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A rows spec chunks against its own configured ceiling</name>
  <reversibility rating="costly">`max_rows_per_match_request` ships in the committed operator
  config template. Renaming or removing it after operators have copied the template means every
  existing config silently loses the key and `chunk_ceiling`'s no-fallback rule refuses.</reversibility>
  <precondition>`scripts/build_cloud_workflows.py` declares `ENRICH_MAX_PROPOSE_RECORDS` at
  module level with an integer value of at least 1, distinct from `ENRICH_MAX_LIST_RECORDS`,
  and `Parse HubSpot Event` selects between the two on the return-only predicate. READ THE
  NUMBER, not merely the name — it is the value this task ships into the client config, and a
  constant that exists but reads 0 or a non-integer would produce a client ceiling that refuses
  every batch while both halves of the pin agree. If the constant is absent, non-integer, or
  below 1, HALT and report; do not soften the pin to make it pass. As of planning it reads
  **20** at `scripts/build_cloud_workflows.py:3512` (landed by 36-06).</precondition>

  <read_first>
    - `operator-claude-plugin/scripts/chunking.py` in full — especially the module docstring's
      fact 1 (the ceiling is read from config, never defaulted), `CEILING_KEY` (line 40),
      `chunk_ceiling` (line 93), `plan_chunks` (line 115) and its `record_ids` branch,
      and `failed_batch` (line 245).
    - `operator-claude-plugin/config/operator.local.example.json` — the `max_records_per_chunk`
      entry with its `_note` and `_provenance_note` siblings, for the shape to mirror.
    - `tests/test_chunk_ceiling_contract.py` in full — this is the cross-repo pin the new file
      mirrors, including `test_the_client_ceiling_carries_its_measured_provenance`'s exact
      wording constraints on the note.
    - `scripts/build_cloud_workflows.py` around lines 3495-3520 — the `MAX_EVENTS` refusal inside
      `ENRICH_PARSE_EVENT_CLOUD`, `ENRICH_MAX_LIST_RECORDS = 2` with its 37.44 s derivation
      comment, `ENRICH_MAX_PROPOSE_RECORDS = 20` (landed by 36-06), and the substitution that
      wires the propose value in. Read the current file for the exact line numbers and the
      current value — both are cited here as of planning, not as of execution.
    - `.planning/workstreams/plugin-entrypoint/phases/36-enrichment-propose-mode/36-06-SUMMARY.md`
      — what the mode-aware amendment actually shipped and why 20 was chosen.
    - `37-CONTEXT.md` § 13, the ceiling ruling paragraph. It supersedes § 4.2 and supersedes
      37-RESEARCH.md's blocking finding and 37-VALIDATION.md's open-decision block, both of
      which describe the pre-ruling world.
    - `37-RESEARCH.md` § C.15 — the config-key + note + provenance-note shape (still current);
      § "PHASE 36 SHIPPED-VS-ASSUMED CONTRACT CHECK" item 1 is now HISTORICAL, retained only
      as the reason the ruling was needed.
  </read_first>

  <files>operator-claude-plugin/scripts/chunking.py, operator-claude-plugin/config/operator.local.example.json, operator-claude-plugin/tests/test_chunking.py, tests/test_match_ceiling_contract.py</files>

  <behavior>
    - `chunk_ceiling(cfg, key="max_rows_per_match_request")` returns the shipped template's
      match ceiling, which is strictly greater than its `max_records_per_chunk`.
    - `chunk_ceiling({}, key="max_rows_per_match_request")` raises `ChunkPlanError` whose message
      names that key, not `max_records_per_chunk`.
    - `chunk_ceiling(cfg)` with no `key` argument behaves exactly as today.
    - `plan_chunks({"rows": [r1, r2, r3], "object_type": "contacts"}, 2)` yields two chunks with
      `row_counts == (2, 1)` and `record_count == 3`; each chunk is `{"rows": [...], "object_type": ...}`.
    - `plan_chunks({"rows": [], ...}, 2)` raises `ChunkPlanError`.
    - `failed_batch` over rows-shaped chunks rebuilds ONE rows spec whose rows keep their
      original order and contain no row from a successful chunk.
    - `failed_batch` over `record_ids`-shaped chunks is unchanged.
  </behavior>

  <action>
**THE RULING (37-CONTEXT.md §13) — this task is the only place it lands on the client.** The
operator ruled that the unconditional two-row refusal is an unnecessarily strict boundary for a
propose call: the 2 was derived from full-waterfall timing and a match call runs zero provider
calls and no model calls. **That backend edit has LANDED** — `ENRICH_MAX_PROPOSE_RECORDS = 20`,
shipped by 36-06 — so this task no longer waits on it and no longer guesses at it. It reads that
number and ships it as the client's match ceiling, still marked PROVISIONAL until a live propose
run measures the path.

Add a `key` parameter to `chunk_ceiling`: `chunk_ceiling(config, key=CEILING_KEY)`. Every
message it raises must interpolate `key` rather than the module constant, so a match refusal
names `max_rows_per_match_request` and never prints the waterfall-derived wording. The
no-fallback rule is unchanged and applies identically to the new key.

Add a `rows` branch to `plan_chunks`, positioned beside the existing `record_ids` branch and
built the same way: refuse a non-list or empty `rows` value with `ChunkPlanError`, then slice
`spec["rows"]` at `ceiling` into chunks of `{"rows": [...], "object_type": spec.get("object_type")}`,
returning a `ChunkPlan` whose `row_counts` are the per-chunk lengths and whose `record_count`
is the total. Keep the existing `list` and `record_ids` behaviour byte-identical — this is an
added branch, not a rewrite.

Add the matching branch to `failed_batch`: when the failed chunks are rows-shaped, concatenate
their `rows` in order into one `{"rows": [...], "object_type": ...}` spec, mirroring the
`record_ids` rebuild exactly.

Add to `operator.local.example.json`, immediately after the `max_records_per_chunk` block, the
key `max_rows_per_match_request`, plus a `_max_rows_per_match_request_note` and a
`_max_rows_per_match_request_provenance_note`, mirroring the two-note shape its neighbour uses.

**Derive the value BY READING the backend, not by an independent formula.** Regex-locate
`ENRICH_MAX_PROPOSE_RECORDS` in `scripts/build_cloud_workflows.py` — the SAME locate the pin
test performs — and ship that number. At planning time it reads 20 (line 3512, landed by
36-06). Do not compute a client-side latency estimate and do not ship a number the backend did
not supply: an independently derived ceiling is exactly how the two halves drift, and the first
draft of this task shipped 25 against a backend that refuses above 20, which the pin would have
caught only after the config was already written.

The note must state that the client ceiling equals the backend's propose-mode ceiling **by
construction**: it is not a coincidence, not a separate calculation that happens to agree, and
not a bound the client independently believes in. The client refuses above the number the
backend refuses above, and it knows that number because it read it. Say plainly that raising it
means raising `ENRICH_MAX_PROPOSE_RECORDS` first and this key second — backend first, always,
because a client that chunks above the backend's bound produces a refusal for a batch the
preview approved.

The provenance note must mark the value PROVISIONAL and name the probe that retires that label:
20 is the backend's own conservative propose-mode bound, and no live propose run has yet
measured per-row latency for that path. The first live propose run records its wall-clock
latency per row; the number is then re-derived from that measurement with B4's headroom rule and
moved in BOTH files together, backend first. Put the provisional marker ONLY in this key's
notes; the neighbouring `_max_records_per_chunk` notes must not gain it, because the test on
those asserts its absence.

Create `tests/test_match_ceiling_contract.py` mirroring `tests/test_chunk_ceiling_contract.py`'s
structure and its read-config-as-DATA discipline (never an import across the PLUGIN-04
boundary). It must:
  - locate `ENRICH_MAX_PROPOSE_RECORDS` in `scripts/build_cloud_workflows.py` by regex and fail
    loud with a named message if it is absent, exactly as `_backend_ceiling`'s assert does. This
    is the SAME locate the config value was derived from, which is what makes the agreement
    structural rather than a coincidence two files happen to share;
  - assert the client's `max_rows_per_match_request` is less than or equal to that constant,
    since a client that chunks above the backend's bound gets a refusal for a batch the preview
    approved — the same failure shape the existing pin was written to stop. Use `<=`, not `==`,
    and comment why: the prescribed raise moves the backend first and the client second, and an
    equality pin would make that correct intermediate commit red;
  - assert the match ceiling is strictly greater than `max_records_per_chunk`, so a later edit
    that quietly collapses the two keys back onto one number fails here;
  - assert the provenance note carries the provisional marker and names the probe.

Its module docstring must state why the two keys exist separately: they are bounded by
different things — one by provider-waterfall latency, one by two HubSpot searches — and the
assertion that will change when the probe lands is the provisional marker, not the separation.

Extend `operator-claude-plugin/tests/test_chunking.py` with the rows-branch cases in the
`behavior` list above, following the file's existing test-naming style.

RED-CHECK: for each new assertion, revert the one line that satisfies it (drop the `key`
parameter; drop the rows branch; set the match ceiling equal to `max_records_per_chunk`; set it
above the backend's propose ceiling; delete the provisional marker from the note) and confirm
the specific new test fails with a message that names the right thing, then restore. Name each
reverted line and quote each failure in the SUMMARY.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py tests/test_match_ceiling_contract.py tests/test_chunk_ceiling_contract.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_match_ceiling_contract.py -q` exits 0.
    - `json.load(open("operator-claude-plugin/config/operator.local.example.json"))["max_rows_per_match_request"]`
      equals the integer on the `ENRICH_MAX_PROPOSE_RECORDS` line of
      `scripts/build_cloud_workflows.py` (20 at planning time), and is strictly greater than the
      same file's `max_records_per_chunk`.
    - Setting `max_rows_per_match_request` above the backend's propose-mode ceiling makes
      `tests/test_match_ceiling_contract.py` fail with a message naming both numbers.
    - Setting it equal to `max_records_per_chunk` makes a different named test in the same file
      fail, so the collapse-back-to-one-number regression is caught on its own terms.
    - Deleting the provisional marker from `_max_rows_per_match_request_provenance_note` makes
      a named test fail; the same word appears in no `_max_records_per_chunk` note, so
      `tests/test_chunk_ceiling_contract.py::test_the_client_ceiling_carries_its_measured_provenance`
      still passes.
    - `chunking.chunk_ceiling({}, key="max_rows_per_match_request")` raises `ChunkPlanError`
      and the exception text contains `max_rows_per_match_request` and does not contain
      `max_records_per_chunk`.
    - `chunking.plan_chunks({"rows": [{"row_id": "r1"}, {"row_id": "r2"}, {"row_id": "r3"}], "object_type": "contacts"}, 2).row_counts == (2, 1)`.
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_chunking.py -q` exits 0 with
      strictly more tests than before this task.
    - The SUMMARY records the match ceiling as PROVISIONAL, names the live probe that replaces
      it, and states the derivation's assumed terms — never as a measurement.
  </acceptance_criteria>

  <done>A rows spec plans, splits and re-batches through the same code path record IDs already
  use, against its own ceiling — a real number, honestly labelled as not yet measured, bounded
  by the backend's own propose-mode limit.</done>
</task>

<task type="auto">
  <name>Task 3: Close the arming guard's module-shaped blind spot</name>

  <read_first>
    - `operator-claude-plugin/tests/test_retry_reuses_dispatch.py` lines 91-211 in full —
      `_SEND_CALL_ATTRS`, `_is_requests_send_attribute`, `_has_send_shaped_transport_default`,
      `_calls_requests_send_verb_directly`, `_send_shaped_function_names`,
      `_EXPECTED_SEND_SHAPED`, and `test_exactly_one_module_defines_the_send_shaped_function`
      with its failure message.
    - `operator-claude-plugin/scripts/enrichment.py` line 201 — `dispatch_enrichment`'s
      signature (`transport=requests`, the bare module) and line 222 (`transport.post(...)`).
    - `operator-claude-plugin/scripts/dispatch.py` line 26 — `transport=requests.post`, the
      attribute-shaped default the existing matcher already sees.
    - `operator-claude-plugin/scripts/executions_client.py` — its `transport=requests.get`
      defaults, which the extension must continue NOT to flag.
    - `37-RESEARCH.md` § B.9 for the exact predicates.
    - `37-CONTEXT.md` § 7.
  </read_first>

  <files>operator-claude-plugin/tests/test_retry_reuses_dispatch.py</files>

  <action>
Add two module-level helpers beside the existing pair, named to match their factoring:

`_has_bare_requests_module_transport_default(func_def)` — true when the function's own
`transport` parameter has a default that is an `ast.Name` whose `id` is `requests` (the bare
module, not an attribute). Reuse `_has_send_shaped_transport_default`'s positional/keyword-only
default-pairing logic; do not duplicate the zip arithmetic if it can be factored out.

`_calls_transport_send_verb(func_def)` — true when the function body contains an `ast.Call`
whose `func` is an `ast.Attribute` with `attr` in `_SEND_CALL_ATTRS` and whose `.value` is an
`ast.Name` equal to that function's own `transport` parameter name. Binding to the parameter's
own name is what keeps a read-only fetcher that happens to call `client.post` on some other
object out of the set.

Extend `_send_shaped_function_names` so a function qualifies when it matches the existing
attribute-shaped predicate, OR calls a send verb directly, OR satisfies BOTH new helpers
together. The conjunction matters: a bare-module default alone is not a send, and a
`transport.post` call alone is already covered.

Add `("enrichment.py", ["dispatch_enrichment"])` to `_EXPECTED_SEND_SHAPED`, keeping the list
sorted. Write the reasoning as a comment above the list in the same register as the existing
status-POST paragraph: this is the enrichment lane's single send; its `armed` parameter has no
default and it raises before constructing a transport; it is listed here because the guard can
now see it, not because it is exempt from anything. State plainly that until this change the
guard's own failure message — that a second dispatch path would let a retry bypass the arming
gate — was not true of the code it guarded, because this function was invisible to it.

Add a test asserting `dispatch_enrichment`'s `armed` parameter carries no default, mirroring
`test_dispatch_armed_parameter_still_carries_no_default` exactly.

RED-CHECK: with the extension in place, remove `("enrichment.py", ["dispatch_enrichment"])`
from the expected list and confirm `test_exactly_one_module_defines_the_send_shaped_function`
fails naming `enrichment.py` and `dispatch_enrichment`; restore. Then revert
`_calls_transport_send_verb` to return `False` unconditionally and confirm the same test now
fails in the OTHER direction (the allowlisted entry is no longer found); restore. Record both
failure texts in the SUMMARY — the second one is what proves the new predicate is load-bearing
rather than decorative.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest operator-claude-plugin/tests/test_retry_reuses_dispatch.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/test_retry_reuses_dispatch.py -q` exits 0.
    - Removing the `enrichment.py` entry from `_EXPECTED_SEND_SHAPED` makes
      `test_exactly_one_module_defines_the_send_shaped_function` fail with a message naming
      `dispatch_enrichment`.
    - Neutering `_calls_transport_send_verb` makes the same test fail, proving the predicate is
      what finds the function.
    - No function in `operator-claude-plugin/scripts/executions_client.py` appears in the
      offenders list (its transports default to the read verb).
    - `.venv/bin/python -m pytest operator-claude-plugin/tests/ -q` exits 0.
  </acceptance_criteria>

  <done>Every send-shaped function in the plugin is either allowlisted with a stated reason or
  fails the guard — including the one that has been invisible since Phase 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> n8n webhook | The rows envelope leaves the operator's machine carrying spreadsheet-derived values. |
| n8n response -> client | Backend-shaped JSON, including HubSpot-derived candidate objects, re-enters the client. |
| committed config template -> operator config | `operator.local.example.json` is copied by hand into a real config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure | `enrichment.build_envelope` rows branch | medium | mitigate | The event projection is `MATCH_LOOKUP_KEYS` only — a row's `phone`, `jobtitle` and `linkedin_url` never cross the boundary on a match/propose call. Pinned by the excluded-key test in `test_rows_envelope_contract.py`. |
| T-37-02 | Elevation of Privilege | `enrichment.dispatch_enrichment` | high | mitigate | Task 3 makes the function visible to the AST arming guard for the first time and allowlists it with its reasoning, so a second module-shaped send path can no longer be added invisibly. |
| T-37-03 | Tampering | rows envelope `mode` | high | mitigate | `mode: "propose"` is set inside the rows branch and is not readable from `spec` or from a parameter, so a rows form cannot ask the backend to write. |
| T-37-04 | Denial of Service | match request size | medium | mitigate | The client's match ceiling is pinned at or below the backend's propose-mode ceiling by `tests/test_match_ceiling_contract.py`, which fails loud rather than passing quietly when that backend constant is absent. A batch above the bound is refused whole before any provider runs, so nothing is spent — but the operator would see a refusal for a batch the preview approved, which is what the pin exists to stop. |
| T-37-14 | Repudiation | provisional match ceiling | medium | mitigate | The value is read out of the backend's own `ENRICH_MAX_PROPOSE_RECORDS` rather than computed client-side, so it cannot claim a bound the backend does not enforce. It is still marked PROVISIONAL because no live propose run has measured per-row latency, and the provisional marker is asserted present by the new pin and asserted absent from the measured neighbour's notes by the existing one — so the measured and unmeasured numbers cannot be confused for each other. |
| T-37-SC | Tampering | package installs | low | accept | This plan installs no npm/pip/cargo package. `requests`, `pyyaml` and `openpyxl` are pre-existing dependencies; no package-legitimacy audit is required. |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest operator-claude-plugin/tests/ -q     # >= 1052 passed / 5 skipped
.venv/bin/python -m pytest -q                                    # >= 1962 passed / 6 skipped
node --test tests/n8n/*.test.mjs                                 # >= 617 pass (FILE glob only)
grep -c 'ALLOW_HUBSPOT_[A-Z_]* = "true"' n8n/*.json              # 0 for every file
git status --short operator-claude-plugin/scratch                # empty
```
No file under `n8n/*.json` is hand-edited by this plan; `tests/n8n/rowsEnvelopeContract.test.mjs`
reads `n8n/code/*.js` modules only.
</verification>

<success_criteria>
- The rows envelope literal is identical in both new test files and both halves go red on the
  same one-character field-name change.
- The backend's own `parseWebhookBody` + `laneOf` route the client's exact envelope to `"name"`.
- `chunk_ceiling(config, key=...)` reads a second key with the same no-fallback refusal.
- `max_rows_per_match_request` ships equal to the backend's landed `ENRICH_MAX_PROPOSE_RECORDS`
  by construction — read from it, not derived beside it — still marked PROVISIONAL, bounded
  cross-repo by that same constant, with the replacing probe and the backend-first raise order
  both named.
- `dispatch_enrichment` appears in the AST guard's expected set with stated reasoning.
- All suites at or above their corrected baselines.
</success_criteria>

<output>
Create `.planning/workstreams/plugin-entrypoint/phases/37-enrich-before-ingest/37-01-SUMMARY.md` when done.
</output>
