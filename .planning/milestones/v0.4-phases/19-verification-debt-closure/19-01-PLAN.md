---
phase: 19-verification-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-verification-debt-closure/19-LEDGER.md
  - .planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md
  - .planning/phases/19-verification-debt-closure/19-01-SUMMARY.md
  - .planning/STATE.md
  # conditional, only if a re-run surfaces a defect (Task 3 step 3):
  - .planning/debug/
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "All six re-run items are enumerated by name in a committed artifact — 11, 15.5, 16, 16.4, 16.6, 16.9 — with the reconstruction basis stated (no literal v0.3 goal ledger exists; RESEARCH.md Assumption A1)."
    - "Each of the six carries an outcome from the fixed vocabulary passed / human_needed / failed, produced by a check RUN IN THIS PHASE — never a restatement of the 2026-07-29 verdict."
    - "Each of the six carries an evidence pointer traceable to the check that produced it: a command plus its observed output, or an HTTP status plus the record id it returned."
    - "Every item RESEARCH.md classifies as LIVE (16, 16.4, 16.6) has live-call evidence, not a test-suite citation."
    - "Every defect a re-run surfaces is captured as a .planning/debug/ brief or a STATE.md Blockers/Concerns row, and the ledger row points at it by path."
    - "The offline suite is at or above the floor measured at the start of this phase, and no HubSpot or n8n write gate was armed at any point."
  artifacts:
    - ".planning/phases/19-verification-debt-closure/19-LEDGER.md"
    - ".planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md"
    - ".planning/phases/19-verification-debt-closure/19-01-SUMMARY.md"
  key_links:
    - "ledger row -> the v0.3 VERIFICATION.md/UAT.md path whose verdict it re-checks"
    - "ledger row -> the concrete command or live HTTP evidence produced this phase"
    - "16.9 ledger row -> 19-OPERATOR-RUNBOOK.md, the step that would close it"
    - "any non-passed ledger row -> the debug brief or STATE.md row that captured its finding"
    - "ledger header -> the measured pytest/node floor, and the same two counts re-measured in the footer"
  prohibitions:
    - statement: "Never arm ALLOW_HUBSPOT_RECORD_WRITES, ALLOW_HUBSPOT_CREATE, DRY_RUN=false, or ALLOW_N8N_DEPLOY=true, and never deploy an armed workflow — arming is operator-only in this environment."
      status: pending
    - statement: "Never write lv_icp_fit_score or lv_icp_tier — HubSpot owns those derived outputs (Approach C)."
      status: pending
    - statement: "No credential, token, or API-key value appears in any committed file."
      status: pending
    - statement: "No file under .planning/milestones/ is edited — archived v0.3 history is read-only."
      status: pending
    - statement: "Nothing is silently dropped to close the ledger — an item that turns out already covered or moot is still recorded as one of the six outcomes, with the superseding evidence named."
      status: pending
    - statement: "No new verification harness, script, or test file is committed — every mechanism this phase needs already exists in the repo."
      status: pending
---

<objective>
Close VERIFY-01: identify the six `/gsd-verify-work` re-runs carried out of v0.3, re-execute each
against post-v0.3-ship state, and record every outcome durably — capturing, not absorbing, any
defect a re-run surfaces.

Purpose: "passed on 2026-07-29" and "passed against current code" are different claims. Phase 17
(contacts transport swap) and Phase 18 (company industry normalization + sponsorship/persona
producers) both landed on code paths downstream of all six verdicts AFTER they were recorded. This
phase turns an unitemized deferred-work row into an itemized, evidenced ledger.

Output: `19-LEDGER.md` (the artifact the v0.3 archive session never wrote), `19-OPERATOR-RUNBOOK.md`
(the armed-window ceremony for the one item this executor is prohibited from running), and a
`STATE.md` Deferred Items row that points at both.

**This phase builds nothing.** No production code, no test files, no scripts. Every command it runs
already exists.

## The six (RESEARCH.md reconstruction — Assumption A1)

No file in this repository enumerates "the six." RESEARCH.md proved this exhaustively (repo-wide
grep + `git log -S"goal ledger"`). The reconstruction below is the only set of exactly six v0.3
phases whose `/gsd-verify-work` history shows a genuine re-run (a documented `## Resolution`
section) or a complete absence of any verify-work run:

| # | Item | Classification | Original verdict |
|---|------|----------------|------------------|
| 1 | Phase 11 — Company Branch & Provider Contract Hardening | OFFLINE | never verified (executed outside GSD) |
| 2 | Phase 15.5 — Tiered Candidate Adjudication | OFFLINE | human_needed -> passed |
| 3 | Phase 16 — Scheduled Workflows & Review Surface | LIVE, read-only | human_needed -> passed |
| 4 | Phase 16.4 — Fetch by ObjectId | LIVE, read-only | human_needed -> passed |
| 5 | Phase 16.6 — Companies Search Transport Fix (BUG 10) | LIVE, read-only | gaps_found -> passed |
| 6 | Phase 16.9 — Create Path Fix and Company Writes | LIVE, WRITE | gaps_found -> passed (SC-3 residual) |

The plan proceeds on this set. It is flagged as an inference in the ledger header, not presented as
fact — that honesty is itself part of criterion 1 ("identified explicitly").

## Probe decisions (resolved here; no CONTEXT.md exists for this phase)

- **P-EMPTY — a re-run that turns out moot.** If an item's claim is already covered by newer
  evidence (e.g. Phase 17's live canary implicitly re-exercised part of 16.4), it is recorded
  `passed` with the superseding evidence named in its Evidence cell. It is NEVER removed from the
  table, and it never becomes a bare citation of the 2026-07-29 verdict. Six rows in, six rows out.
- **P-ORDER — execution and listing order.** The ledger lists all six in stable phase order
  (11, 15.5, 16, 16.4, 16.6, 16.9), which is also the ordering of increasing risk: offline, then
  live read-only, then human-gated. Execution order matches, with one stated exception: item 16's
  deployment-drift probe runs FIRST (Task 1) because its result changes how items 16.4 / 16.6 /
  16.9 must be interpreted — a live check against a stale deployment verifies the wrong artifact.
- **P-ADJ — overlapping items stay separate.** 16.4 and 16.6 both issue read-only company searches;
  16.6 and 16.9 both touch the live company write-lane surface. Each remains its OWN ledger row with
  its OWN outcome and its OWN evidence cell. One HTTP response may satisfy two rows; the rows are
  never merged, and neither row cites the other's outcome in place of evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-verification-debt-closure/19-RESEARCH.md
@.planning/phases/19-verification-debt-closure/19-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Measure the floor, probe deployment drift (item 16), stand up the ledger</name>
  <files>.planning/phases/19-verification-debt-closure/19-LEDGER.md</files>

  <precondition>`.venv/bin/python -m pytest --version` succeeds — the repo venv is the only interpreter with the test dependencies installed (system python lacks them; knowledge-base `test-suite-run-commands`).</precondition>

  <reversibility rating="reversible">Every action is a read or a new markdown file; nothing deployed, nothing overwritten.</reversibility>

  <read_first>
    - `.planning/phases/19-verification-debt-closure/19-RESEARCH.md` — the six-item reconstruction, the "Recommended Recording Structure" block, and Open Question 2 / Assumption A2 (was Phase 18 redeployed?)
    - `.planning/milestones/v0.3-phases/16-scheduled-workflows-review-surface/16-VERIFICATION.md` — its `## Resolution — 2026-07-29` section (the verdict this re-check must not merely restate) and its `## Human Verification Required` section
    - `scripts/deploy_n8n_workflows.py` lines 143-207 (`_has_n8n`, `_instance_ok`, `_writes_allowed`, `_load_local_workflows`, `_get_live_workflows`, `compute_workflow_diff`) and lines 471-540 (`main`'s dry-run path)
    - `.planning/phases/17-enrichment-contacts-reachability-bug-23/17-02-SUMMARY.md` lines 150-195 — the in-process python-dotenv driver pattern (`.env` is not shell-readable in this session)
  </read_first>

  <action>
Measure the floor, settle the drift question, and create the ledger.

**1. Measure the offline floor before touching anything else.** Run `.venv/bin/python -m pytest -q`
and `node --test tests/n8n/*.test.mjs` (file-glob form — the directory form is broken on this Node
version). Record both integer counts verbatim. Two stale figures are in circulation (596/309 and
596/285); neither is trusted — the number measured here is this phase's floor.

**2. Build a scratchpad-only driver for live calls.** Under the session scratchpad directory (NOT
in the repo), write a small python driver that calls `dotenv.load_dotenv()` and then imports
`scripts.deploy_n8n_workflows` and uses its own read-only helpers. Never shell-source `.env`
(permission-blocked). Never echo, print, or persist a key or token value anywhere. This file is not
committed and is not a new harness — it is the same in-process pattern Phase 17-02 established.

**3. Item 16, part A — deployment inventory.** Call `deploy_n8n_workflows.main([])` with `DRY_RUN`
unset (its default is true) and `ALLOW_N8N_DEPLOY` unset. Capture verbatim its three printed lines:
`Workflows to create: [...]`, `Workflows to update: [...]`, and the `DRY RUN (default)` line. A
NON-EMPTY create list means a workflow that should exist live is missing — a finding, not a pass.
If it instead prints `skipped (no n8n creds)` or `REFUSED:`, record item 16 as `human_needed` with
that exact line as evidence; do not substitute an offline check for it.

**4. Item 16, part B — the drift probe (settles RESEARCH Open Question 2 / Assumption A2).**
`compute_workflow_diff` matches on workflow `name` only and never compares node content — every
existing workflow lands in `update` regardless of whether it is current, so part A's output alone
CANNOT answer whether Phase 18's rebuilt JSON was redeployed. Do a real content probe: fetch the
`LV Enrichment` workflow live via `_get_live_workflows()`; if the list response omits a `nodes` key,
GET `/api/v1/workflows/{id}` for that workflow instead. Serialize the live nodes and substring-check
for the two Phase-18 markers that are present in the committed `n8n/wf_enrichment_cloud.json`:
`_personaGroup` (the Phase 18 persona producer) and `_industryText` (the Phase 18 industry
normalizer). Both present -> the live deployment is Phase-18-current. Either absent -> the live
deployment predates Phase 18, the deployed code is behind git, and THAT is the defect Task 3 must
capture as a debug brief. Record the present/absent result for each marker by name.

**5. Item 16, part C — the "still active, still credential-bound" half of the original verdict.**
From the same live fetch, record per workflow: its `name`, its `active` value, its total node count,
and the count of nodes carrying a bound `credentials` key. Compare against the last recorded state
in `STATE.md`'s Session Continuity block (LV Contact Ingest 19/4, LV Enrichment 94/22, LV Scheduled
Maintenance 30/9, all `active=false`) and record any difference as part of item 16's evidence.

**6. Create `.planning/phases/19-verification-debt-closure/19-LEDGER.md`.** It contains:
  - A header stating: (a) that no literal v0.3 goal ledger file exists and this itemization is
    RESEARCH.md's evidence-based reconstruction (Assumption A1), (b) the two measured suite counts
    from step 1 labelled as the phase floor, (c) the three probe decisions P-EMPTY / P-ORDER /
    P-ADJ copied from this plan's `<objective>`.
  - A six-row table in the stable order 11, 15.5, 16, 16.4, 16.6, 16.9 with columns:
    `Item | What it verifies | Original verdict + artifact path | Method | Outcome | Evidence |
    Defect captured at`.
  - Item 16's row filled completely (Outcome from the fixed vocabulary, Evidence carrying the
    part-A lines, both marker results, and the active/bound counts). The other five rows carry
    Outcome `pending` until their own task fills them.
  - Each row's "Original verdict + artifact path" cell links the real
    `.planning/milestones/v0.3-phases/...` file (phase 11's row links its `phase-11-01-SUMMARY.md`,
    since it has no VERIFICATION.md at all — that absence is the point).

**7. Arm nothing.** Do not set `DRY_RUN=false`, `ALLOW_N8N_DEPLOY=true`, or any `ENABLE_BAKED_FLAGS`
value. Do not modify any file under `scripts/`, `tests/`, `n8n/`, `src/`, or `.planning/milestones/`.
  </action>

  <verify>
    <automated>test -f .planning/phases/19-verification-debt-closure/19-LEDGER.md &amp;&amp; grep -cE '^\| *(11|15\.5|16|16\.4|16\.6|16\.9) *\|' .planning/phases/19-verification-debt-closure/19-LEDGER.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/19-verification-debt-closure/19-LEDGER.md` exists.
    - `grep -cE '^\| *(11|15\.5|16|16\.4|16\.6|16\.9) *\|' 19-LEDGER.md` outputs exactly `6` — all six items enumerated, one row each, none merged.
    - The ledger header contains two integers explicitly labelled as the measured pytest count and the measured node count, and both match the tail output of the two step-1 commands.
    - The ledger header states that no literal v0.3 goal ledger file exists and names the reconstruction as an inference (Assumption A1).
    - The ledger header reproduces all three probe decisions P-EMPTY, P-ORDER, P-ADJ.
    - Item 16's Outcome cell is exactly one of `passed`, `human_needed`, `failed`.
    - Item 16's Evidence cell quotes the `Workflows to create:` line verbatim AND names both `_personaGroup` and `_industryText` with a present/absent result for each AND carries the per-workflow active + bound-node counts.
    - `git status --porcelain scripts/ tests/ n8n/ src/ .planning/milestones/` produces no output.
    - `grep -rnE '(pat-na[0-9]{1,2}-[A-Za-z0-9]{8}|X-N8N-API-KEY: *[A-Za-z0-9]|Bearer +[A-Za-z0-9_.-]{20,})' .planning/phases/19-verification-debt-closure/` returns no matches (exit 1).
  </acceptance_criteria>

  <done>Item 16 is closed with a recorded outcome backed by live read-only evidence; the deployment-drift question is answered by name; `19-LEDGER.md` exists with all six items enumerated in stable order and the measured suite floor on record.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the two offline items (11, 15.5) and the two remaining live read-only items (16.4, 16.6)</name>
  <files>.planning/phases/19-verification-debt-closure/19-LEDGER.md</files>

  <precondition>`HUBSPOT_PRIVATE_APP_TOKEN` loads via the in-process dotenv driver (`.env` is not shell-readable this session). If it does not load, halt items 16.4 and 16.6, record both `human_needed` with that reason, and do NOT substitute a test-suite citation for their live evidence.</precondition>

  <reversibility rating="reversible">All checks are test re-runs and read-only HTTP searches; the only write is markdown into the ledger.</reversibility>

  <read_first>
    - `.planning/milestones/v0.3-phases/phase-11/phase-11-01-SUMMARY.md` — the five original guarantees, the three provider defects, and the "Carried Forward" TX-4 debt
    - `.planning/milestones/v0.3-phases/15.5-tiered-candidate-adjudication/15.5-VERIFICATION.md` — `## Finding 1` (the test-rigor gap) and `## Resolution — 2026-07-29`
    - `tests/n8n/mergeCompanies.test.mjs` lines 150-280 — the TA-4/TS-1 trio (lines ~194, ~224, ~263) and the DELIBERATE-BREAK (D2) test (~line 235)
    - `tests/n8n/enrichment.test.mjs` lines 330-375 — the four Phase-11 provider shape tests
    - `scripts/build_cloud_workflows.py` lines 2277-2360 — the `ENRICH_MERGE_CO` cross-provider size-conflict block
    - `.planning/milestones/v0.3-phases/16.4-fetch-by-objectid/16.4-VERIFICATION.md` — `## Human Verification Required` + `## Resolution — 2026-07-29`
    - `.planning/milestones/v0.3-phases/16.6-companies-search-transport-fix/16.6-VERIFICATION.md` — `## Gaps Summary` + `## Resolution — 2026-07-29`
  </read_first>

  <action>
Four items, offline first (P-ORDER). Fill each row in `19-LEDGER.md` as it completes.

**Item 11 (OFFLINE).** Re-check the five original guarantees against CURRENT source, not against the
2026-07-20 summary's claims:
  (a) *Sibling companies branch.* Confirm `n8n/wf_enrichment_cloud.json` still carries the companies
      chain as a sibling of contacts — the nodes `Build Company Identity`, `Merge Company`, and
      `Decide Company Action` exist and the companies chain is not nested under the contacts chain.
  (b) *Non-clobber field-ownership merge.* `node --test tests/n8n/mergeCompanies.test.mjs` green —
      specifically the `domain` hard guard, the evidence-URL gate that runs before the class
      branches, and the below-min_confidence withholding.
  (c) *The three provider unit/shape defects.* `node --test tests/n8n/enrichment.test.mjs` green,
      naming these four tests by title in the evidence: "ZoomInfo company revenue is THOUSANDS",
      "ZoomInfo revenue falls back to revenue*1000 with no revenueRange", "ZoomInfo live naicsCodes
      are objects", "Lusha live company unwraps `data` and reads `employees`".
  (d) *Cross-provider size-conflict detector withholds promotion.* Run
      `.venv/bin/python -m pytest tests/test_judge_spec.py::test_ro2_judge_gate_cannot_see_size_conflicts -q`
      AND read `scripts/build_cloud_workflows.py` 2277-2360 to confirm a conflicted field is still
      skipped before promotion and still surfaces through `conflicts` / `needs_review`.
  (e) *Numbered spec exists.* `docs/WEB-RESEARCH-SPEC.md` present, and
      `.venv/bin/python -m pytest tests/test_web_research_spec.py -q` green.
  (f) *TX-4, phase 11's own carried-forward debt (closed Phase 12).* Confirm still closed:
      `.venv/bin/python -m pytest tests/test_taxonomy_conformance.py -q` green.
  (g) *The ZoomInfo GTM companies contract.* This sub-claim was originally live-probed. It is an
      external platform fact, not this repository's code, and RESEARCH.md classifies item 11 as
      OFFLINE. Record it in the Evidence cell explicitly as carried at citation level with no fresh
      live probe this phase, naming the `zoominfo-gtm-companies-contract` knowledge-base entry. State
      the limit; do not let it disappear inside a bare `passed`.

**Item 15.5 (OFFLINE).** The original `human_needed` was a test-rigor call, not a live gap: the
TA-4/TS-1 recency test used to call `mergeCompanies` with literally identical arguments twice. Confirm
the fix is still present and still non-tautological — in `tests/n8n/mergeCompanies.test.mjs`, the test
titled "TA-4/TS-1 via the PRODUCTION confidenceByField path" (~line 263) must still route through
`scoreResearchCandidates` / `judge_confidence_by_field` rather than re-invoking with identical
arguments, and the "DELIBERATE-BREAK (D2)" test (~line 235) must still be present. Run
`node --test tests/n8n/mergeCompanies.test.mjs tests/n8n/researchScoring.test.mjs` and record the pass
counts plus the two test titles located.

**Item 16.4 (LIVE, read-only).** Reuse Task 1's scratchpad driver. Three read-only
`POST /crm/v3/objects/{type}/search` calls against portal `22617666`, and no other HTTP verb:
  - contacts, filter `hs_object_id` `EQ` `"201"` — expect 200, `total == 1`, `results[0].id == "201"`
  - companies, filter `hs_object_id` `EQ` `"9604614548"` — expect 200, `total == 1`, matching id
  - contacts, filter on the nonexistent property `lv_not_a_real_prop_xyz` — expect 400, which is the
    distinguishability claim itself: a systemic filterability failure returns 400 while a legitimate
    miss returns 200 with `total: 0`.
Record all three status codes and both returned ids. Any deviation is the item's outcome, recorded as
observed.

**Item 16.6 (LIVE, read-only) — a SEPARATE row from 16.4 (P-ADJ), even though both search companies.**
Replay the transport BUG 10 fixed rather than inventing a new request: extract the `Company Search`
node's committed `parameters.jsonBody` from `n8n/wf_enrichment_cloud.json`, substitute literal values
for its n8n expressions, and issue it read-only against the live search API for company
`9604614548`. Confirm a REAL record returns — not a null body, not zero results. Additionally request
`lv_sponsorship_reliant` in the `properties` array: Phase 18 added that field to the company research
payload and it has never been proven live through this transport. Record which of three outcomes
occurred — property returned with a value, property returned empty/absent, or the request 400s naming
the property. Absent or 400 is a finding for Task 3 to capture, not something to retry away.

**Fill the ledger.** Rows 11, 15.5, 16.4, 16.6 each get an Outcome from the fixed vocabulary and an
Evidence cell carrying the command run plus its observed result. P-EMPTY applies: an item already
covered by newer evidence records `passed` with that superseding evidence named, never omitted.

**Change no code.** This task modifies no file under `scripts/`, `tests/`, `n8n/`, `src/`, or
`.planning/milestones/`. If a test fails, that is the item's recorded outcome — do not fix the test
to make the row green.
  </action>

  <verify>
    <automated>node --test tests/n8n/mergeCompanies.test.mjs tests/n8n/enrichment.test.mjs tests/n8n/researchScoring.test.mjs &amp;&amp; .venv/bin/python -m pytest tests/test_judge_spec.py tests/test_web_research_spec.py tests/test_taxonomy_conformance.py -q &amp;&amp; grep -E '^\| *(11|15\.5|16|16\.4|16\.6) *\|' .planning/phases/19-verification-debt-closure/19-LEDGER.md | grep -cE '\| *(passed|human_needed|failed) *\|'</automated>
  </verify>

  <acceptance_criteria>
    - Both suite commands exit 0; the node run and the pytest run each report zero failures.
    - The final grep outputs exactly `5` — rows 11, 15.5, 16, 16.4 and 16.6 each carry an Outcome cell of exactly `passed`, `human_needed`, or `failed`.
    - Item 11's Evidence cell names all four Phase-11 provider tests by their test titles, names `test_ro2_judge_gate_cannot_see_size_conflicts`, and states the ZoomInfo-GTM-contract citation-level limit in words.
    - Item 15.5's Evidence cell names both located test titles ("TA-4/TS-1 via the PRODUCTION confidenceByField path" and "DELIBERATE-BREAK (D2)") and states that the first still routes through the production scoring path.
    - Item 16.4's Evidence cell contains three HTTP status codes and both object ids `201` and `9604614548`.
    - Item 16.6's Evidence cell names the `Company Search` node and states the `lv_sponsorship_reliant` result as exactly one of: returned with a value / returned empty or absent / request returned 400.
    - Items 16.4 and 16.6 are two distinct table rows with two distinct Evidence cells; neither cell's only content is a pointer to the other.
    - `git status --porcelain scripts/ tests/ n8n/ src/ .planning/milestones/` produces no output.
    - `grep -rnE '(pat-na[0-9]{1,2}-[A-Za-z0-9]{8}|X-N8N-API-KEY: *[A-Za-z0-9]|Bearer +[A-Za-z0-9_.-]{20,})' .planning/phases/19-verification-debt-closure/` returns no matches (exit 1).
  </acceptance_criteria>

  <done>Items 11, 15.5, 16.4 and 16.6 each carry a recorded outcome and evidence produced in this phase — offline items from re-run suites against current source, live items from read-only HTTP calls with observed status codes. No production or test code changed.</done>
</task>

<task type="auto">
  <name>Task 3: Record item 16.9 human_needed with its operator runbook, capture every surfaced defect, close the ledger</name>
  <files>.planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md, .planning/phases/19-verification-debt-closure/19-LEDGER.md, .planning/STATE.md</files>

  <precondition>Tasks 1 and 2 have each recorded their outcomes in `19-LEDGER.md` — this task's defect sweep reads those rows and cannot classify findings that have not been written down yet.</precondition>

  <reversibility rating="reversible">Recording `human_needed` and writing a runbook are both reversible markdown edits. The armed `company:update` canary the runbook describes is `costly` and is deliberately NOT performed in this plan — arming is operator-only in this environment, which is exactly why 16.9 closes as `human_needed` rather than as an interactive checkpoint.</reversibility>

  <read_first>
    - `.planning/milestones/v0.3-phases/16.9-create-path-fix-and-company-writes/16.9-VERIFICATION.md` — `## Gaps Summary` and `## Resolution — 2026-07-29`, specifically that SC-4 (`company:create`, execution 34) WAS independently re-confirmed while SC-3 (`company:update`) was NOT and its evidence is "unrecoverable by design"
    - `.planning/phases/17-enrichment-contacts-reachability-bug-23/17-CANARY-EVIDENCE.md` — the armed-window ceremony shape the runbook must mirror (arm, fire once, read back, restore disarmed, read the deployment back)
    - `scripts/deploy_n8n_workflows.py` lines 110-141 — `_OVERLAY_FLAG_SPEC`, `_WRITE_ENABLING_FLAGS`, `_ALLOWLIST_FLAGS`, and the `|` allowlist-separator rule, so the runbook names the real flags with their real syntax
    - `.planning/debug/bug-23-enrichment-contact-nomatch-chain-stop.md` — the debug-brief shape to mirror if a defect is captured
    - `.planning/STATE.md` — the `### Blockers/Concerns` section and the `## Deferred Items` table row for the six re-runs
  </read_first>

  <action>
Close the last item, capture what the phase surfaced, and gate the ledger.

**1. Item 16.9 — record `human_needed`. Arm nothing.** Fill its ledger row with a two-part Evidence
cell: (a) `company:create` / SC-4 was already independently re-confirmed live (execution 34: create,
fresh-read confirm, DELETE, 404 re-read, disarmed restore) — cite `16.9-VERIFICATION.md`'s Resolution
and state explicitly that it is NOT re-armed here; (b) `company:update` / SC-3 is the residual the
ORIGINAL verifier itself flagged as not independently re-confirmed, it now additionally carries Phase
18's new company payload fields, and closing it requires an operator armed-window canary that this
executor is prohibited from running. The row's Method cell reads `live write — operator only`; its
Outcome cell reads exactly `human_needed`; its "Defect captured at" cell points at
`19-OPERATOR-RUNBOOK.md`.

**2. Write `.planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md`.** It gives the
operator the exact ceremony, with real flag names and real syntax read from `_OVERLAY_FLAG_SPEC`:
  - Scope: one allowlisted test company, one `company:update`, nothing else.
  - Arm: `scripts/deploy_n8n_workflows.py` invoked with `DRY_RUN=false`, `ALLOW_N8N_DEPLOY=true`, and
    `ENABLE_BAKED_FLAGS` requesting `ALLOW_HUBSPOT_RECORD_WRITES=true` together with a non-empty
    `TEST_RECORD_IDS` allowlist (the script refuses a write-enabling flag without an allowlist in the
    same invocation; a multi-id allowlist uses `|` because `,` already separates flag entries).
    `ALLOW_HUBSPOT_CREATE` stays untouched — create is already closed, re-arming it adds risk for no
    verification value.
  - Fire once, read the record back, and confirm no record outside the allowlist changed.
  - Disarm: redeploy with the same command minus `ENABLE_BAKED_FLAGS`, then read the deployment back
    from the live instance and confirm the write-safety literal is back to its disabled value.
  - Timing: HubSpot search is eventually consistent (~6s-3min); wait before the read-back rather than
    treating a fast empty read as a failure.
  - Pass/fail condition, and where the outcome is written back: the 16.9 row of `19-LEDGER.md`,
    replacing `human_needed` with the observed result.

**3. Defect capture sweep (ROADMAP criterion 3 — nothing silently dropped).** Walk all six ledger
rows. For every finding that is not a clean `passed` — deployment drift from Task 1's marker probe, a
400 or absent `lv_sponsorship_reliant` from item 16.6, any failing test, any zero-result search, any
active/bound-count difference from Task 1 step 5 — create a `.planning/debug/bug-NN-<slug>.md` brief
mirroring `bug-23-...`'s shape (symptom, evidence, root-cause hypothesis, scope fence) or add a row to
`STATE.md`'s `### Blockers/Concerns`. Put the resulting path in that row's "Defect captured at" cell.
A clean `passed` row's cell reads `—`. No finding may exist only as ledger prose.

**4. Re-measure the suite and record the footer.** Run `.venv/bin/python -m pytest -q` and
`node --test tests/n8n/*.test.mjs`. Both counts must be at or above Task 1's measured floor. Write
both into a `19-LEDGER.md` footer alongside the floor, so the two are comparable on the page.

**5. Ledger completeness gate.** All six rows carry an Outcome from the fixed vocabulary; no row was
removed; 16.4 and 16.6 remain separate rows (P-ADJ); every non-`passed` row points at a path that
exists on disk.

**6. Update `.planning/STATE.md` by hand.** Do NOT use `gsd-tools state.update-progress` or
`state.advance-plan` — this repo's ROADMAP concatenates multiple milestones and the tool has
previously miscounted and corrupted `STATE.md`. Edit the `## Deferred Items` row reading "Six
`/gsd-verify-work` re-runs carried from the original goal ledger" so it names `19-LEDGER.md` and
summarises the six outcomes, and add any new `### Blockers/Concerns` row created in step 3.

**7. Prohibited throughout this task:** performing the armed canary the runbook describes; setting any
write-enabling deploy flag; writing `lv_icp_fit_score` or `lv_icp_tier`; editing anything under
`.planning/milestones/`; modifying any file under `scripts/`, `tests/`, `n8n/`, or `src/`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q 2>&amp;1 | tail -2 &amp;&amp; node --test tests/n8n/*.test.mjs 2>&amp;1 | tail -6 &amp;&amp; test -f .planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md &amp;&amp; grep -E '^\| *(11|15\.5|16|16\.4|16\.6|16\.9) *\|' .planning/phases/19-verification-debt-closure/19-LEDGER.md | grep -cE '\| *(passed|human_needed|failed) *\|'</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md` exists and `grep -cE 'ENABLE_BAKED_FLAGS|TEST_RECORD_IDS' 19-OPERATOR-RUNBOOK.md` returns 2 or more.
    - The runbook contains a disarm step and a post-disarm read-back-from-the-live-instance step, both as distinct numbered steps.
    - Item 16.9's Outcome cell is exactly `human_needed`, and its "Defect captured at" cell resolves to `19-OPERATOR-RUNBOOK.md`.
    - The final grep outputs exactly `6` — every one of the six rows carries an Outcome from the fixed vocabulary.
    - Every ledger row whose Outcome is not `passed` has a "Defect captured at" cell containing a repo-relative path that exists on disk (`test -f` succeeds for each).
    - The ledger footer records a re-measured pytest count and node count, each greater than or equal to the floor recorded in the header; both suite commands exit 0.
    - `.planning/STATE.md`'s Deferred Items row for the six re-runs contains the string `19-LEDGER.md`.
    - `git status --porcelain scripts/ tests/ n8n/ src/ .planning/milestones/` produces no output.
    - `grep -rnE '(pat-na[0-9]{1,2}-[A-Za-z0-9]{8}|X-N8N-API-KEY: *[A-Za-z0-9]|Bearer +[A-Za-z0-9_.-]{20,})' .planning/phases/19-verification-debt-closure/ .planning/debug/` returns no matches (exit 1).
  </acceptance_criteria>

  <done>All six items carry a recorded outcome; 16.9 is closed as `human_needed` with a concrete operator runbook rather than left ambiguous; every surfaced finding has a captured brief or backlog row referenced by path; the suite is at or above its floor and `STATE.md` points at the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> n8n Cloud API | An API key crosses here; a mis-set flag turns a read into a deploy |
| executor -> HubSpot CRM v3 API | A private-app token crosses here; a mis-chosen HTTP verb turns a search into a write |
| executor -> committed `.planning/` files | Live API responses are transcribed here; response bodies and headers can carry secrets |
| executor -> `.planning/milestones/` | Archived v0.3 history — read-only evidence this phase cites but must never alter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | `scripts/deploy_n8n_workflows.py` invocation in Tasks 1 and 3 | high | mitigate | Both tasks call it with `DRY_RUN` unset (default true) and `ALLOW_N8N_DEPLOY` unset, so `_writes_allowed()` returns false and `main()` returns before any create/update HTTP call. `ENABLE_BAKED_FLAGS` is never set. Item 16.9's armed invocation is written into `19-OPERATOR-RUNBOOK.md` for a human, never executed. Asserted by Task 1 and Task 3's `git status --porcelain n8n/` criterion and by 16.9's Outcome cell reading `human_needed`. |
| T-19-02 | Tampering | HubSpot CRM v3 API calls in Task 2 (items 16.4, 16.6) | high | mitigate | Only `POST /crm/v3/objects/{type}/search` is issued — the sole endpoint used, stated in the action. No PATCH, no object-create POST. `lv_sponsorship_reliant` is READ into the `properties` request array, never written. |
| T-19-03 | Information Disclosure | `19-LEDGER.md`, `19-OPERATOR-RUNBOOK.md`, `19-01-SUMMARY.md`, any new `.planning/debug/` brief | high | mitigate | The in-process dotenv driver never echoes or persists a key or token value; only status codes, record ids, and property names are transcribed. Enforced by the token-shaped-string grep in all three tasks' acceptance criteria, scoped over the phase dir and `.planning/debug/`. |
| T-19-04 | Tampering | `.planning/milestones/` archived v0.3 artifacts | medium | mitigate | Every task's acceptance criteria include `git status --porcelain .planning/milestones/` producing no output; the ledger links these files by path and never rewrites their verdicts. |
| T-19-05 | Repudiation | The six recorded outcomes | medium | mitigate | Each Evidence cell must carry the command run plus its observed output, or an HTTP status plus a record id — a bare restatement of the 2026-07-29 verdict is explicitly disallowed by P-EMPTY and by each task's `<done>`. |
| T-19-06 | Elevation of Privilege | `ALLOW_HUBSPOT_CREATE` re-arming during the 16.9 runbook | medium | accept | The runbook explicitly scopes the operator to the update flag and leaves create untouched; residual risk sits with the operator session, outside this plan's execution envelope, and is bounded by the same allowlist the script enforces. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages — no `requirements.txt`, `package.json`, or lockfile is modified, so the supply-chain surface is unchanged. No legitimacy audit is required (RESEARCH.md "Package Legitimacy Audit: not applicable"). |
</threat_model>

<verification>
Phase-level checks, run after Task 3:

1. `grep -E '^\| *(11|15\.5|16|16\.4|16\.6|16\.9) *\|' .planning/phases/19-verification-debt-closure/19-LEDGER.md | grep -cE '\| *(passed|human_needed|failed) *\|'` outputs `6`.
2. The three items RESEARCH.md classifies LIVE (16, 16.4, 16.6) each have an Evidence cell containing an HTTP status code or a live n8n API result — not a pytest/node command alone. This is the explicit warning sign in RESEARCH.md Pitfall 2.
3. Every non-`passed` row's "Defect captured at" path exists on disk.
4. `.venv/bin/python -m pytest -q` and `node --test tests/n8n/*.test.mjs` both exit 0 at counts greater than or equal to the floor recorded in the ledger header.
5. `git status --porcelain scripts/ tests/ n8n/ src/ .planning/milestones/` produces no output — this phase built nothing and altered no history.
6. `.planning/STATE.md`'s Deferred Items row for the six re-runs names `19-LEDGER.md`.
</verification>

<success_criteria>
Mapped to ROADMAP Phase 19's three success criteria:

1. **Every one of the six re-runs is identified explicitly and re-executed against post-v0.3-ship
   state.** `19-LEDGER.md` names all six (11, 15.5, 16, 16.4, 16.6, 16.9) in stable order, states the
   reconstruction basis and its Assumption-A1 status, and each row's Method cell names the check that
   was run in this phase.
2. **Each run's outcome is recorded against the item it verifies.** Six rows, six Outcome cells drawn
   from `passed` / `human_needed` / `failed`, each with its own Evidence cell. 16.9 closing as
   `human_needed` with a runbook is a legitimate terminal state, not an open item.
3. **Any defect a re-run surfaces is captured — nothing silently dropped.** Every non-`passed` row
   points at a `.planning/debug/` brief or a `STATE.md` Blockers/Concerns row that exists on disk;
   P-EMPTY forbids removing a row that turns out moot.
</success_criteria>

<artifacts>
## Artifacts this phase produces

| Artifact | Task | Purpose |
|----------|------|---------|
| `.planning/phases/19-verification-debt-closure/19-LEDGER.md` | 1 (created), 2 and 3 (filled) | The itemization the v0.3 archive session never wrote: six rows, each with item, what it verifies, original verdict + artifact path, method, outcome, evidence, and defect-capture pointer. Header carries the reconstruction basis, the measured suite floor, and P-EMPTY / P-ORDER / P-ADJ; footer carries the re-measured suite counts. |
| `.planning/phases/19-verification-debt-closure/19-OPERATOR-RUNBOOK.md` | 3 | The armed-window ceremony for item 16.9's `company:update` canary — the one check this executor is prohibited from running. Names the real deploy flags, the allowlist requirement, the disarm step, and the live read-back. |
| `.planning/debug/bug-NN-<slug>.md` (conditional) | 3 | One brief per defect surfaced by a re-run — most likely candidates: live deployment behind git (Task 1 marker probe), `lv_sponsorship_reliant` absent or 400 through the live company-search transport (item 16.6). Omitted only if every row is a clean `passed`. |
| `.planning/STATE.md` (edited) | 3 | Deferred Items row repointed at `19-LEDGER.md`; any new Blockers/Concerns row. Hand-edited — `gsd-tools state.update-progress` miscounts this repo's concatenated ROADMAP. |
| `.planning/phases/19-verification-debt-closure/19-01-SUMMARY.md` | on completion | Execution summary, one row per item, linking evidence. |
</artifacts>

<source_audit>
## Multi-Source Coverage Audit

**GOAL** (ROADMAP Phase 19): "The verification backlog carried out of v0.3 is closed — every deferred
`/gsd-verify-work` re-run has actually been executed against current state, with its outcome on
record." -> COVERED by plan 19-01, tasks 1-3.

**REQ**

| ID | Item | Status | Where |
|----|------|--------|-------|
| VERIFY-01 | Six re-runs executed and outcomes recorded | COVERED | 19-01, all three tasks; `19-LEDGER.md` |

**RESEARCH** (`19-RESEARCH.md`)

| Item | Status | Where |
|------|--------|-------|
| Six-item reconstruction (11, 15.5, 16, 16.4, 16.6, 16.9) | COVERED | `<objective>` table; Task 1 step 6 ledger header |
| Offline / live-read-only / live-write classification | COVERED | Task 1 (16), Task 2 (11, 15.5, 16.4, 16.6), Task 3 (16.9) |
| `deploy_n8n_workflows.py` DRY_RUN read-only mechanism | COVERED | Task 1 step 3 |
| In-process python-dotenv live-call pattern | COVERED | Task 1 step 2; reused Task 2 |
| `19-LEDGER.md` as the recording format | COVERED | Task 1 step 6 |
| 16.9 scoped as operator-gated, not executor-run | COVERED | Task 3 steps 1-2 |
| Open Q1 — is this actually the six? | COVERED | Ledger header states the inference and its Assumption-A1 status (Task 1 step 6a) |
| Open Q2 / A2 — was Phase 18 redeployed? | COVERED | Task 1 step 4 marker probe (`_personaGroup`, `_industryText`) — and the finding that `compute_workflow_diff` cannot answer it |
| Open Q3 — create vs update canary | COVERED | Task 3 step 1: create cited as already closed, update is the residual |
| A3 — stale baseline figures | COVERED | Task 1 step 1 re-measures; neither stale figure trusted |
| Pitfall 1 — treating the six as settled fact | COVERED | Ledger header inference statement |
| Pitfall 2 — offline-green mistaken for live-proven | COVERED | `<verification>` check 2 |
| Pitfall 3 — HubSpot eventual consistency | COVERED | Task 2 targets the standing records 201 / 9604614548; Task 3 runbook states the wait |
| Anti-pattern: silently marking all six passed | COVERED | Prohibition 5; P-EMPTY; T-19-05 |
| Anti-pattern: building a new harness | COVERED | Prohibition 6; scratchpad-only driver, `git status` criteria |

**CONTEXT** — no `CONTEXT.md` exists for this phase and no `/gsd-discuss-phase` session ran. There
are no D-NN locked decisions to cite. The three specless probe items are resolved in-plan as
P-EMPTY / P-ORDER / P-ADJ and carried into the ledger header.

**Result: every source item COVERED. No gaps, no split required.**
</source_audit>

<output>
Create `.planning/phases/19-verification-debt-closure/19-01-SUMMARY.md` when done.
</output>