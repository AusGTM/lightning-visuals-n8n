# Stress session sheet — 2026-09-15

Runbook: `RUNBOOK.md`. Fill each stage from the plugin session's report; paste ids, counts and
property values only — never a runData block.

## Pre-flight (done)

| Item | Result |
| --- | --- |
| Plugin | 0.49.1 installed and loaded; `initialize` → already set up; `allow_write_grants` on |
| Write safety | `verify_live_write_safety.py` → disarmed PASS, 5 workflows, 17 declaring nodes |
| Baseline execution id | **12421** (Scheduled Maintenance, 2026-09-14T14:00Z); Enrichment last 12415, Ingest last 12414 |
| Pre-existing queues | companies awaiting review 17; contacts queued enrich 11; contacts awaiting review 0; companies queued 0 |
| Provider balances | unknown — status surface reports lusha/apollo/zoominfo `not_configured`; credit deltas must come from the provider dashboards |
| Leftovers | `352522004980` deleted (`204`) |
| Snapshot | `uat-reset-snapshot.json` — 12/32 domains protected; Perth Racing, Racing Victoria, HRNSW absent by domain → name-match test |
| Budget | ~60–90 executions; provider spend approved by client |

## Attempt 1 — Stage A — contacts, no providers — RUN, ZERO WRITES (blocker F-A3)

| Field | Value |
| --- | --- |
| run_id | `aa059535b5f644e6a9c39f695ff76bfb` (second attempt; the interrupted first attempt also landed as execution `12426`) |
| execution range | ingest `12426`, `12429` (both `success`, 50 nodes ran) |
| preview: mapped / unresolved / refused / deduped | 14 / 1 (`UAT Marker`) / 0 at preview / 0 — preview did NOT flag the malformed emails or the 3 duplicate `priya` rows |
| created / updated / held / refused | 0 / 0 / 47 review / 1 skip |
| `1251` phone + title unchanged | unchanged — because nothing was written at all |
| dense row check | not reachable — no create happened |
| grant / arm / disarm | grant 13 domains + create; arm verified (deactivate → PUT → reactivate, second read); disarm clean, all flags `false` |
| findings | F-A1..F-A5 below |

Root cause (read from runData of `12429`, headers redacted): the three per-row HubSpot search
HTTP nodes fire one request per item in a burst — 48 items each. HubSpot answered
`429 "You have reached your secondly limit."` on 33/48 (`HubSpot Search by Email`), 38/48
(`Company Search by Domain`), 42/48 (`Company Search by Name`). `Adapt Search Results` sets the
batch-wide `lookup_failed: true` on any errored item (designed fail-safe, builder line ~1282),
`Decide Action` downgrades every `create` to `review` when `lookup_failed` (line ~889), and the
row reason reads "valid email, no existing match" — so Colin Telfer (`1251`, exists) reported as
no match. Nodes carry `onError: continueRegularOutput`, no `retryOnFail`, no `options.batching`
(builder `_live_http`). Prior UATs were ≤ 9 rows and never reached the limit.

## Mid-session fix deploy (F-A3/F-A4) — 2026-09-15T05:58Z

| Item | Result |
| --- | --- |
| fix commits | `248754c6`, `2b6840f0` (builder `_http_node(batch_interval_ms=250)` on the three ingest search nodes; `Decide Action` names a failed lookup) |
| deploy | `DRY_RUN=false ALLOW_N8N_DEPLOY=true scripts/deploy_n8n_workflows.py` → 5 PUTs at 200 (only ingest body differs) |
| bounce | all five active, 30/78/287/55/43, `executionOrder: v1`, write flags `false` |
| read-back | live `AwbBeShdPgV48eiY` `updatedAt 2026-09-15T05:58:12Z`: `options.batching {batchSize 1, batchInterval 250}` on all three search nodes; `Decide Action` jsCode carries the lookup-failed reason |
| write safety | `verify_live_write_safety.py` → disarmed PASS |
| note | first dry-run attempt printed the table with no PUTs — `deploy_n8n_workflows.py` defaults to DRY_RUN; runbook step 2 corrected to name both env flags |

## Attempt 1 — Stage B — companies — RUN, 24 enriched / 11 created / 1 skipped (ground truth from runData)

| Field | Value |
| --- | --- |
| run_id | `6891d018e84f4d869eb8080292dac6c5` |
| execution range | enrichment `12432`–`12449` (18 executions, 17 chunks × 2 + Illawarra name-only), all `success` |
| domain table | 34 CSV domains confirmed; Gosford → `theentertainmentgrounds.com.au` (researched); Illawarra → declined to name-only; `WWW.THEVALLEY.COM.AU` and `https://www.vrc.com.au/` normalised; row 33 duplicate collapsed (35 rows sent + 1 name-only = 36) |
| matched by domain / by name / created / skipped | 24 `enrich` (updates on existing ids incl. ATC `9605284724`, MRC, BRC, Hawkesbury, Newcastle, GCTC, SCTC, Darwin, Tasracing, HRV, RWWA, Moonee Valley; HRNSW matched a record `10204524171` by NAME) / 11 `create` / 1 `skip` (Illawarra, "no existing record") |
| created ids | Racing Victoria `288177737153`, Perth Racing `288108751332`, Wyong `288126705122`, Canberra RC `288177737169`, Pakenham `288135239150`, Murray Bridge `288195624435`, NZTR `288197421512`, Sky Racing `288140625347`, Wagga Rowing `288179567075`, Gosford `288097166812`, Country Racing Collective `288135240183` (domain `gmail.com`) |
| tiers | not yet read back — do it in HubSpot for Daktronics, NYRA, Tabcorp, Sky Racing |
| duplicate companies | **4 confirmed after re-snapshot**: Perth Racing `288108751332` (name+TLD differ, F-B2); Racing Victoria `288177737153`, Wyong `288126705122`, Canberra RC `288177737169` (portal records stored as `www.` domains, F-B7). HRNSW matched the portal's second pre-existing record `10204524171` by name |
| 429s | none on the enrichment lane (2 rows per execution stays under the search limit) |
| plugin accounting | 54 "recovered rows" = 35 real outcomes + 19 unjoinable research-node rows; the plugin's report counted 11 creates + 1 skip and could not see the 24 updates |
| disarm | clean |
| findings | F-B1..F-B6 |

## Reset 1 — 2026-09-15 (after attempt 1, before restarting at Stage A)

| Item | Result |
| --- | --- |
| dry run | 06:03Z — 10 companies, 0 contacts, 21 skipped; no protected or pre-existing id listed |
| execute | 06:04Z — 10 × `204`, 0 failed (`uat-reset-report-20260915T060409Z.json`) |
| missed | Gosford `288097166812` — its domain (`theentertainmentgrounds.com.au`, researched in-session) is not in the CSV, so the domain rule could not reach it; deleted separately 06:06Z with `--extra-company-id 288097166812 --since 2026-09-15T05:00:00Z` → `204`, GET now `404` (`uat-reset-report-20260915T060651Z.json`). Portal company set is back to pre-UAT: 11/11 UAT-created companies removed, 0 pre-existing touched |
| expected deletions | 11 companies created 2026-09-15 (`288177737153` RV, `288108751332` Perth dup, `288126705122` Wyong, `288177737169` Canberra, `288135239150` Pakenham, `288195624435` Murray Bridge, `288197421512` NZTR, `288140625347` Sky Racing, `288179567075` Wagga, `288097166812` Gosford, `288135240183` gmail via `--extra-company-id`); 0 contacts (Stage A wrote nothing, Stage C never ran) |
| not reversible | enrichment written onto the 24 pre-existing companies (property history keeps prior values) |
| re-snapshot | 06:05Z — **21/32 protected** (was 12 pre-fix). The `www.` form surfaced 9 more pre-existing records: HRNSW `10204524171`, **Racing Victoria `18756544380`**, **Wyong `10215097384`**, Scone `14748141740`, **Canberra RC `10152138518`**, GRNSW `15140397666`, Tabcorp `39461318458`, Daktronics `17663335094`, NYRA `22798600364` |
| new baseline execution id | |

## Attempt 2 — full run A–F by the plugin session (raw transcript: `session.log`, gitignored)

Baseline execution `12451`; final `12512`; delta ≈ 60 (estimate was 60–90). Nothing armed at close; `verify_live_write_safety.py` → disarmed PASS. Two errored executions all session, both explained below.

### Stage A — contacts, no providers — FAIL (create branch aborted)

| Field | Value |
| --- | --- |
| run_id / execution | `8f31173312fd43528300a55f257c04d8` / ingest `12454` (status **error**, lastNode `HubSpot Create`) |
| F-A3 throttle | **live and working**: `HubSpot Search by Email` 48/48 ok, `Company Search by Domain` 48/48 ok, `Company Search by Name` 47/48 ok — ONE residual 429 on the third node. `lookup_failed: false` on all 48 (the plugin session's note "fix not live" was wrong; deploy 05:58Z preceded the 08:54Z run) |
| Decide Action | 1 update (`1251`), 19 create, 27 review, 1 skip |
| what landed | `1251` updated (phone/title unchanged, SAFE-01 ok). `HubSpot Create` node **errored** ("Your request is invalid…" = HubSpot 409 on the duplicate `uat.priya.whitcombe` rows 37/38) → no output, no associations, no ingest response; the other ~17 creates had already been sent and exist WITHOUT associations (confirm via reset dry run) |
| findings | F-A6 (new, blocker), F-A5 upgraded, F-A3 residual |

### Stage B — companies, full waterfall — PASS with known gaps

| Field | Value |
| --- | --- |
| run_id / executions | `d533902e60964f23a1c27a2dc18bb825` / enrichment `12458`–`12475`, 18/18 success, 0 rate-limit errors |
| outcomes | 9 created, rest enriched in place; Illawarra name-only → skip "no existing record" (no 400 this time — F-B4 narrower: only a mislabeled `lookup_failed: true` on a clean no-match) |
| created ids | `288175986120`, `288219132367` (Wagga), `288138913272`, `288219131362`, `288130345437`, `288063484393`, `288078432752`, `288220924387`, `288186815993`, `288186815969`, `288138913236` (log-derived; includes Gosford under the researched domain) |
| vetoes | Daktronics anti-ICP true (non-ANZ + no-content + hardware) Tier D; NYRA true Tier D; Tabcorp deduction only; Sky Racing no veto; 19 provider conflicts → needs_review with field + sources named |
| duplicates | expected recurrence of Perth Racing / RV / Wyong / Canberra (F-B2/F-B7) — reset removes them |

### Reset 2 — 2026-09-15T10:19Z (after attempt 2)

| Item | Result |
| --- | --- |
| dry run | 24 contacts (22 `uat.`-marked Stage A orphans + Stage D creates; 2 real people Stage C created at run companies), 11 companies (all `288…`, created this session, incl. Gosford under its researched domain and the `gmail.com` row); 22 skipped; no pre-existing id listed |
| execute | 35 × `204`, 0 failed (`uat-reset-report-20260915T101941Z.json`) |
| not reversible | enrichment on 24 pre-existing companies (Stage B) and 4 pre-existing contacts Stage C matched-updated (`22801`, `6551`, `35551`, `17951`); review reject on `9605273630`, approve on `14752488879` (8 fields, verified) |
| portal state | back to pre-UAT record set |

## Stage C — suggest-contacts — PASS

| Field | Value |
| --- | --- |
| crawl | 11 created companies crawled; real people found on own sites (e.g. Perth Racing, Wyong, Thoroughbred Park team pages) |
| enrichment / gate | 10 people enriched; 6 with company-domain emails; **email-domain gate held Kiersten Duke** (tabcorp.com.au ≠ skyracing.com.au); 3 held no-email |
| ingest execution | `12482` success: 2 created (`353098015204` Nick Ristovic, `353130794444` Andrew Hurley), 4 matched → UPDATED pre-existing real contacts (`22801`, `6551`, `35551`, `17951`), 6/6 associated; both legs disarmed |

### Stage D — enrich-before-ingest, providers on — PASS

| Field | Value |
| --- | --- |
| executions | enrichment `12488`–`12494` (13 unmatched rows enriched), ingest `12496` success |
| outcomes | 6 created + associated (Declan Okafor → Perth, Harriet Kowalczyk → RV, **Hamish Ashdown → Wagga `288219132367`** (row 41 association proven), Bram Delacroix → Perth, Noor Maalouf → RV, **Jimmy Busteed → ATC** with revealed email); 3 held with reasons (no sajc/tasracingclub company; gmail) |
| ids | `353017090529`, `353170919924`, `353020930543`, `353064583631`, `353115878842`, `353062602212` |

### Stage E — review queue — PARTIAL

| Field | Value |
| --- | --- |
| reject | Port Macquarie `9605273630` — reason recorded, stays queued ✓ |
| approve #1 | MRC `9604614548` **FAILED**: execution `12502` error at `Review Decision Update` — HubSpot 400 "Cannot deserialize … from Array value": `lv_content_type: ['unknown']` sent as a raw array. **F-E1 (new, high)** |
| approve #2 | Coffs Harbour `14752488879` (array-free candidate) → verified by re-read, 8 fields hold ✓ |

### Stage F — status + sweep — PASS

All five on, nothing running, nothing armed, delta ≈ 60. Sweep preview fired one grouped notice (4 items), all this session's own footprint: burn-rate projection 2790/30d vs 2500 (skewed by the session), the two errored executions, review backlog 39 > 25 threshold. Sweep behaved as designed (named condition, not silence).

## Stage C — suggest-contacts

| Field | Value |
| --- | --- |
| companies offered / crawled / sites refused | |
| proposals ready / held (industry site, email-domain mismatch) / sent | |
| execution range | |
| findings | |

## Stage D — enrich-before-ingest (optional)

| Field | Value |
| --- | --- |
| grants / yeses | |
| rows 41–42 associated to Wagga | |
| Busteed reveal + Lusha credits charged | |
| execution range | |
| findings | |

## Stage E — review queue

| Field | Value |
| --- | --- |
| approved (id, property, re-read confirmed) / rejected (id, reason) / left | |

## Stage F — status + sweep

| Field | Value |
| --- | --- |
| execution delta vs 12421 | |
| anything armed / running long | |
| sweep preview result | |

## Close-out

| Item | Result |
| --- | --- |
| `verify_live_write_safety.py` after session | |
| reset dry run: contacts / companies / skipped | |
| reset execute: 204 count / failures; report file | |
| post-reset: `email contains uat.` → 0; `1251` intact | |

## Findings

| Id | Stage | Severity | What | Evidence |
| --- | --- | --- | --- | --- |
| F-A1 | A | low | grant envelope quotes Lusha 91 / ZoomInfo 14 / $0.89 for the provider-free `contact-upload` lane | plan_grant output, Stage A |
| F-A2 | A | low | envelope projects 20 executions at 7 chunks; `contact-upload` is one POST (2 executions observed) | plan_grant output vs `12426`/`12429` |
| F-A3 | A | **blocker** | ingest lane's per-row HubSpot searches exceed HubSpot's per-second search limit at 48 rows → every row `lookup_failed` → zero writes, existing contact reported "no existing match" | `12429` runData: 33/38/42 of 48 items `429` on the three search nodes; `Adapt Search Results` 48× `lookup_failed: true` |
| F-A4 | A | medium | row reason says "valid email, no existing match" when the truth is "lookup failed (429)"; operator cannot tell a rate-limit from a genuine miss | `Decide Action` output `12429` |
| F-A5 | A | low | preview did not flag 2 malformed emails (`@@`, no TLD) or the 3 duplicate `priya` rows (incl. upper-case variant); backend verify later marked 3 `INVALID_DOMAIN`/3 `NO_MX` | preview output vs README expectations |
| F-B1 | B | low | grant envelope priced Lusha at the contact rate (34 × 7 = 238) for a companies batch (2 credits/company measured) | plan_grant / preview_enrichment output |
| F-B2 | B | **high** | exact-domain + exact-name matching recreated **Perth Racing** (`288108751332`; portal `9604794662` "Perth Racing (Previously WA Turf Club)" / `perthracing.org.au` — name AND TLD differ). HRNSW matched pre-existing `10204524171` by name (portal holds two HRNSW records — data condition) | HubSpot GET 2026-09-15; exec `12434` searches `total: 0` |
| F-A6 | A | **blocker** | `HubSpot Create` (ingest lane) has no continue-on-error: one duplicate-email 409 aborts the node, discards every create's output, skips all associations and the ingest response, and marks the execution `error` — while the non-duplicate creates have already landed in HubSpot orphaned | exec `12454` lastNode `HubSpot Create`, status error; 0 associations; ~17 contacts created without a company |
| F-A5 | A | **high** (was low) | duplicate rows within one CSV (rows 37/38 = row 3, incl. case variant) are not collapsed before create — the trigger for F-A6 | preview + exec `12454` |
| F-A3r | A | medium | throttle at 250 ms leaves no headroom: 1 residual 429 on the third search node in a 48-row send (HubSpot search budget is account-wide and shared with scheduled jobs) | exec `12454` `Company Search by Name` 47/48 |
| F-E1 | E | **high** | review-approve writeback sends array-valued candidates (`lv_content_type: ['unknown']`) raw; HubSpot 400s the whole approve | exec `12502` `Review Decision Update` |
| F-C1 | C | low | suggest-contacts UPDATED 4 pre-existing real contacts as "matched" — expected behaviour, but the reset cannot undo those enrichments | exec `12482` `HubSpot Update` ids `22801`, `6551`, `35551`, `17951` |
| F-B7 | B | **high** (upgraded from medium after re-snapshot) | `domain EQ <bare>` misses records HubSpot stores as `www.<bare>`. Attempt 1 therefore DUPLICATED three more companies whose portal record carries a `www.` domain: **Racing Victoria** `288177737153` (portal `18756544380`), **Wyong** `288126705122` (portal `10215097384`), **Canberra RC** `288177737169` (portal `10152138518`). With Perth Racing that is 4 duplicates out of 11 creates. Confirmed pre-UAT: `18756544380` "Racing Victorias" (sic) `www.racingvictoria.com.au` created 2024-01-17; `10215097384` "Wyong" `www.wyongraceclub.com.au` 2022-12-01; `10152138518` "Thoroughbred Park" `www.thoroughbredpark.com.au` 2022-11-22. Their NAMES also differ from the CSV, so the exact-name fallback could never have caught them — only a `www.`-tolerant domain match can. Fix candidate: companies branch domain search tries `[bare, www.bare]` (or normalises the stored domain). `uat_reset.py --snapshot` already queries both forms | re-snapshot 06:05Z (21 vs 12 protected); `HubSpot Company Create` output execs `12434`/`12435`/`12437` |
| F-B3 | B | medium | a company with website `gmail.com` was accepted and CREATED (`288135240183`); freemail is not a company domain — the plugin's domain clean did not refuse it, and the backend created it | exec `12449` `HubSpot Company Create` |
| F-B4 | B | medium | name-only company (Illawarra) sends `HubSpot Company Search` with `domain EQ ""` → HubSpot 400 "operator EQ requires a value"; row then `skip` "no existing record" — reason should say name-only rows cannot be created without a domain | exec `12449` search-node error |
| F-B5 | B | medium | plugin run report cannot see `enrich` (update) outcomes — 24 updates reported as "unaccounted"; 19 research-node rows counted as unjoinable rows | plugin report vs runData `Decide Company Action` / `HubSpot Company Update` |
| F-B6 | B | low | provider balances unreadable through the plugin, so the approved spend ran unbounded by the guard; ceiling checked against executions only | preview_enrichment output |

---

# Attempt 2 — full run Stages A–E (F-A3 fix NOT deployed live)

Run 2026-09-15 by the plugin session. Baseline latest execution **12451**; final latest execution **12512**; delta **≈60 executions** (within the 60–90 estimate). All five workflows read **disarmed** at every stage boundary. Two executions errored all session — `12454` (Stage A) and `12502` (Stage E) — both explained below; no runaway, no 405/empty-id PATCH, no `ALLOW_*` armed after any send.

> **Header note.** The F-A3/F-A4 throttle fix is committed (`248754c6`) but was **not deployed to the live n8n instance** for this run, so F-A3 recurs on high-volume search (Stage A, and the Stage D match lane). Small batches (Stage C 6 rows, Stage D ingest 9 rows) did not trip it.

## Baseline

| Item | Result |
| --- | --- |
| Workflows | 5 on, nothing running, all last-run success, all `ALLOW_*` off |
| Latest execution id | **12451** (12450/12451 = own backend-status polls) |
| Queues | companies review 37; contacts enrich 11; companies enrich 0; contacts review 0 |
| Provider balances (status endpoint) | unknown — lusha/apollo/zoominfo `not_configured`; readable later from enrich runData |

## Stage A — contacts, no providers — **FAIL (execution errored)**

| Field | Value |
| --- | --- |
| run_id | `8f31173312fd43528300a55f257c04d8` |
| execution | ingest **`12454`** (LV Contact Ingest) — **status `error`** |
| preview | 48 rows, 14 mapped, `UAT Marker` dropped, `Mobile`→mobilephone, 0 provider credits / $0 — preview did **not** flag the 3 duplicate Priya rows or the malformed emails |
| Decide Action | update 1, create 19 (gated in), review 27, skip 1 |
| created / updated / held / refused | **≈12 created (confirmed via Stage D match, see below) / 1 updated (Telfer 1251) / 0 holds persisted / 0** |
| create branch | **aborted** at `HubSpot Create`: "Contact already exists. Existing ID: 353064513008" — Priya rows 3/37/38 (case-variant duplicate email) were not collapsed; the node has no continue-on-error, so the throw killed the whole create branch → **no company associations ran, no holds persisted** |
| F-A3 | **recurring** — 429 on `HubSpot Company Search by Name` |
| `1251` (Telfer) | update ran (same-value → SAFE-01 preserved) |
| grant / arm / disarm | grant 1 id + 11 domains + create; armed; **disarm clean, all flags `false`** |

**Stage A created contacts (confirmed by the Stage D read-only match — carry fresh `353xxx` ids):** `353064513008` Priya, `353170855400` Tomas, `353017033200` Ngaire, `353017033201` Mei, `352991487433` Kofi Tuilagi, `353097965016` Bao, `353033198012` Siobhan, `353113995725` Rafael (MRC), `353112168900` Lachlan, `353005623767` Yuki Huang, `353033198014` Rafael Petrakis (ATC), `353113995723` Kofi Huang. These landed **without company associations** (the abort killed the association branch) — a real leftover the companies-only reset will not clean.

## Stage B — companies, full waterfall — **PASS (with expected known gaps)**

| Field | Value |
| --- | --- |
| run_id | `d533902e60964f23a1c27a2dc18bb825` |
| execution range | enrichment **`12458`–`12475`** (18/18 `success`, 0 error) |
| domain table | rows 31/32 normalised (`vrc.com.au`, `thevalley.com.au`); row 33 deduped; row 34 Gosford → researched `theentertainmentgrounds.com.au`; row 35 Illawarra LinkedIn → declined to name-only; row 36 gmail → passed through (F-B3 test) |
| created / matched / needs-review | **11 created / 22 matched-updated / 19 needs_review** (Illawarra name-only → skip) |
| match wins | HRNSW matched by **name** `10204524171`; ATC `9605284724`, MRC `9604614548`, BRC `9605284723`, Scone `14748141740`, VRC→Flemington `9605291627`, Auckland→Ellerslie `9680571285`, Moonee Valley `9605273633` matched not recreated |
| vetoes | Daktronics `anti_icp=true` (non-ANZ + no-content + hardware) Tier D; NYRA `anti_icp=true` (non-ANZ) Tier D; **Tabcorp `anti_icp=false` (gambling = deduction, no veto)**; Sky Racing no veto |
| conflicts | needs_review names the field + both sources (e.g. Perth "lv_revenue_band: lusha=50-500M vs apollo=5-50M") — §15.0 material-conflict suppression working |
| disarm | clean, all flags `false` |

**Created company ids (11):** `288063484393` Racing Victoria (dup), `288175986120` Perth Racing (dup, F-B2), `288219131362` Wyong (dup, F-B7), `288078432752` Canberra (dup, F-B7), `288186815969` Pakenham, `288138913236` Murray Bridge, `288186815993` NZ Thoroughbred, `288220924387` Sky Racing, `288219132367` Wagga (fictitious), `288130345437` Country Racing Collective (gmail, F-B3), `288138913272` Gosford (researched domain).

## Stage C — suggest-contacts — **PASS**

| Field | Value |
| --- | --- |
| enrich run_id | `4879314ef8b54a408e9e1a31fb1caf3e` (5 execs) |
| ingest run_id | `a7ab096ab6b3426c97520b463d592420` (exec **`12482`** `success`) |
| companies crawled / with people | 11 accepted / **5 yielded people** (Perth, Gosford, Wyong, Canberra, Sky) |
| sites refused (fences) | Pakenham (SSL), NZ Racing (403) — fallback correctly not fired |
| DNS-fail / clean | Murray Bridge (ENOTFOUND), **Wagga (ENOTFOUND — fictitious, fails cleanly)** |
| no proposals | Racing Victoria (own-site governance-only, no names), Country Racing Collective (gmail, not a site) |
| proposals ready / held / sent | **6 sendable / 4 held / 6 sent** (all associated) |
| email-domain gate | **held Kiersten Duke** — `tabcorp.com.au` ≠ `skyracing.com.au` (Sky is Tabcorp-owned) — plus 3 no-email holds |
| disarm | clean, both legs |

**Sent (6, all associated to correct company):** CREATE `353098015204` Nick Ristovic→Perth; UPDATE `22801` James Oldring→Perth; UPDATE `6551` Daniel Lacey→Gosford; CREATE `353130794444` Andrew Hurley→Wyong; UPDATE `35551` Greg Purcell→Wyong; UPDATE `17951` Darren Pearce→Canberra. **Created contact ids: `353098015204`, `353130794444`.**

## Stage D — enrich-before-ingest, providers on — **PASS**

| Field | Value |
| --- | --- |
| match | read-only, `match_run_id` `bf80369c05f0449fb328d8eed09eff38`, execs `12483`–`12485` (enrichment lane, propose mode, **providers skipped, 0 credits**). 48 rows → auto_matched 15, proposed 2, unmatched 12, **unchecked 19** (one of 3 match chunks unanswered — F-A3 on the match lane) |
| enrich run_id | `6e5a35ef4c3f4452914d3e71ba3acdc0`, execs `12488`–`12494` (7 chunks), recovered 13, **disarm clean** |
| Busteed reveal | **`jbusteed@australianturfclub.com.au`** + mobile, Lusha `creditsCharged=1` |
| fictitious rows | Lusha `creditsCharged=0` (NOT_FOUND) — matches expectation |
| provider balances (from enrich runData) | Lusha total 4200 / used 492 / **remaining 3708**; ZoomInfo remaining 9358; Apollo null (no pool, expected). Session Lusha spend ≈32 credits (Stage C ~28 across 6 reveals at 6–7 each, Stage D ~1) — no anomalous drop |
| first ingest | **refused (GRANT-03)** — the grant scoped to pre-enrich email domains did not cover Busteed's revealed ATC domain nor Wagga id; guard correct |
| second ingest | 9 sendable → **6 created + associated, 3 review**; disarm clean |

**Stage D created contact ids (6):** `353017090529` Declan Okafor→Perth; `353170919924` Harriet Kowalczyk→RV; **`353020930543` Hamish Ashdown→Wagga `288219132367`** (row-41 association ✓); `353064583631` Bram Delacroix→Perth; `353115878842` Noor Maalouf→RV; **`353062602212` Jimmy Busteed→ATC `9605284724`** (reveal create+associate ✓). Held (review, correct): Bram Ravenscroft (no sajc.com.au company), Noor Maalouf (no tasracingclub.com.au company), Zara Rangi (gmail — no company to match).

## Stage E — review queue — **PASS (with F-S7 defect)**

| Field | Value |
| --- | --- |
| queue | 40 companies |
| grant / allowlist | lanes=[review]; `ALLOW_HUBSPOT_REVIEW_WRITES=true`, ids scoped by record; disarm clean |
| APPROVE (verified) | **Coffs Harbour `14752488879`** → `verified` (re-read holds all 8 approved fields), **cleared from queue** |
| APPROVE (failed) | MRC `9604614548` → `unparseable_response`; exec `12502` error — HubSpot 400 "Cannot deserialize from Array value" because the candidate `lv_content_type=['unknown']` was sent as a raw JSON array (**F-S7**); stays queued |
| REJECT | Port Macquarie `9605273630` → "rejection reason recorded; record stays in review queue"; still queued |
| LEAVE | Thoroughbred Park `10152138518` → untouched, still queued |

## Stage F — status + sweep — **PASS**

- Status: 5 workflows on, nothing running, **every `ALLOW_*` off** — nothing armed after the session's sends.
- Execution delta: **60** (12451 → 12511); within the 60–90 estimate.
- Sweep: **a named condition, not silence** — one grouped notice, 4 items: `burn_rate_alarm` (3.9/hr → ~2790/30d vs 2500, inflated by the compressed test run), `failed_scheduled_run` ×2 (`12502`, `12454`), `review_backlog` 39 > 25. Read-only.

## Findings

| id | stage | severity | what | evidence |
| --- | --- | --- | --- | --- |
| **F-S1** | A | Critical | A within-batch duplicate email aborts the **entire** create branch. `HubSpot Create` has no continue-on-error, so one collision (Priya rows 3/37/38) throws and kills the branch → some creates land but **no associations, no hold persistence**, and the execution reports `error`. | exec `12454` status error; `HubSpot Create` err "Contact already exists. Existing ID: 353064513008"; Stage D match confirms ≈12 created, 0 associated |
| **F-S2** | A | High | **F-A3 recurring** — 429 on `HubSpot Company Search by Name` on the 48-row ingest. The throttle fix (`248754c6`) is committed but not deployed live. Fails the "0 errored items on search nodes" criterion. | `execution_errors 12454` → `HubSpot Company Search by Name` cause `rate_limit` |
| **F-S3** | A | Medium | Case-insensitive dedup did **not** collapse rows 37/38 onto row 3 (Priya, `UAT.PRIYA…` vs `uat.priya…`). Root cause of F-S1. | CSV rows 3/37/38 share the email; all three reached `create` gating |
| **F-S4** | B | Low | A name-only / no-domain company search stamps `lookup_failed=true` on a **clean no-match** (company-lane analog of F-A4). Outcome (skip, "no existing record") is still correct — no 400 this run (improvement over attempt-1 F-B4). | exec `12475`, Illawarra `Build Response` `action=skip lookup_failed=true` |
| **F-S5** | D | Medium | **F-A3 degrades the read-only match lane too** — 1 of 3 match chunks went unanswered, leaving 16 rows `unchecked` (unknown match state, incl. Busteed). Same 429 volume root cause. | `classify_matches` unchecked=19 (16 + 3 legit no-email) |
| **F-S6** | D | Low (design) | enrich-before-ingest scopes the grant to **pre-enrichment** email domains; a reveal that produces a new-domain email (Busteed: no email → `australianturfclub.com.au`) is then out-of-grant at ingest and refused. The guard is correct; grant scoping should include the association/company domains up front. | first ingest `INGEST_AUTH_REFUSED` GRANT-03 naming ATC domain + Wagga id |
| **F-S7** | E | High | **Review-approve fails on array/multi-checkbox fields.** A company whose approved candidate includes `lv_content_type` (e.g. `['unknown']`) sends the raw JSON array to `Review Decision Update` → HubSpot 400 "Cannot deserialize from Array value" → approve returns `unparseable_response`, flag not cleared. Serialize array fields to a string before the PATCH. | exec `12502` error; `Review Decision Update` desc "Cannot deserialize value of type … from Array value" |
| F-B2 | B | (known) | Perth Racing duplicated — exact matching cannot see the portal's differing name/domain. **Reproduced as expected.** | created `288175986120` alongside existing `9604794662` |
| F-B3 | B | (known) | `gmail.com` company (Country Racing Collective) **created** under the freemail domain — no client-side freemail refusal. **Reproduced as expected.** | created `288130345437` domain `gmail.com` |
| F-B7 | B | (known) | www-prefixed / name-variant existing companies recreated as duplicates (Racing Victoria, Wyong, Canberra). **Reproduced as expected.** | created `288063484393`, `288219131362`, `288078432752` |

## Reset notes (for the operator — reset is yours)

- Companies-only reset (`uat_reset.py`) covers the 11 created companies via the CSV, **plus** two created under domains the rule can't see: `--extra-company-id 288130345437` (gmail) and `--extra-company-id 288138913272 --since <attempt-2 start>` (Gosford researched domain).
- **Contacts are NOT covered by the companies reset.** UAT leftover contacts created this session: ≈12 from Stage A (the `353xxx` list above, unassociated), 2 from Stage C (`353098015204`, `353130794444`), 6 from Stage D (`353017090529`, `353170919924`, `353020930543`, `353064583631`, `353115878842`, `353062602212`). `1251` (Telfer) must be preserved.
- Two review records were left changed on purpose: Coffs Harbour `14752488879` (approved/cleared) and Port Macquarie `9605273630` (reject reason recorded, still flagged).
