Run the stress test end to end per tests/stress-tests/RUNBOOK.md, Stages A–F, then the close-out read-back. Work from the repo root /Users/robertli/Desktop/consulting/lightning-visuals/lv-n8n-poc. This is ATTEMPT 3 on plugin 0.50.0 and the Phase 73 backend (deployed + bounced disarmed 2026-09-16, node counts 30/80/287/55/43, all v1; portal reset confirmed 0-to-delete, 21 protected). Decisions are pre-made — do not ask me between stages; only stop on a runbook stop condition or if anything reads armed at a stage boundary.

Baseline first: "What's the backend doing?" — record the latest execution id, Lusha and ZoomInfo balances (F-B6: they must now read as numbers, not not_configured; Apollo may read unknown).

Stage A — contact-upload of tests/stress-tests/uat-stress-mixed-batch-2026-09-13.csv with NO providers. Open the write grant and arm/dispatch/disarm yourself (write autonomy is on). If the permission classifier blocks the arming step, write the runner to a file and print the exact `cd … && python3 <file>` line for me to run with `!`, then wait for my paste. Pass criteria (attempt 2 FAILED here): the preview shows rows 37 and 38 collapsed as duplicate_in_csv naming row 3 as the winner and only the deduped file is sent (F-A5); the ingest execution ends success, not error — HubSpot Create must not abort the batch (F-A6); 0 errored items and 0 429s on the three HubSpot search nodes at the 400 ms throttle (F-A3r); 0 rows with lookup_failed; contact 1251 → update with phone/title unchanged; dense rows 3–14 → create WITH company association (no orphans); sparse/malformed rows held by name with reasons; the grant envelope prices this lane at 0 provider credits and 1 execution per POST (F-A1/A2). Record explicitly that the create_failed lane (F-A6) was NOT exercised — the dedupe removes its trigger; it stays offline-proven, never tag it observed-live.

Stage B — enrich-records companies form on tests/stress-tests/uat-stress-companies-2026-09-14.csv, FULL waterfall. Domain table: accept all CSV domains; Gosford Race Club → research the website; Illawarra Turf Club → decline to name-only. Pass criteria: Racing Victoria, Wyong and Canberra RC are MATCHED to their www.-stored portal records, not recreated (F-B7); the gmail.com row is refused with the freemail reason and held for review, never created (F-B3); the Illawarra name-only row is held for review with a reason that names the match outcome, "no domain", and "supply one" — no 400, no skip (F-B4); Perth Racing may still duplicate (F-B2, known gap — record, do not stop); the grant envelope prices companies at 2 Lusha credits each (F-B1); the run report accounts every enrich/update outcome with zero "unaccounted" and no research rows as unjoinable (F-B5). Cross-check against the executions' runData (Decide Company Action / HubSpot Company Create / HubSpot Company Update). List every created company id.

Stage C — accept the suggest-contacts offer for all companies with nobody named; default fetch budget; send the ready proposals with the same grant discipline. Verify two created people by re-read: associated to the right company, email domain equals the company domain or held with the domain-mismatch reason.

Stage D — enrich-before-ingest on the same contacts CSV with providers ON. Pass: no new contact lands incomplete; held rows carry clear hold reasons; the run report's held-row list contains only THIS run's rows (run_manifest scoped per run_id, D-73-14).

Stage E — "What needs review?" Work three items: approve one whose candidate carries an array-valued field such as lv_content_type (attempt 2 FAILED here with a HubSpot 400 — pass = the approve lands and the re-read shows the value, F-E1), reject one with a reason, leave one. Confirm the approved write by re-read.

Stage F — "What's the backend doing?" (execution delta since baseline, nothing running long, nothing armed, balances still numeric) then "Run the backend sweep" — expect silence-means-healthy or a named condition.

Close-out: run the disarmed read-back and report per stage exactly as .planning/phases/73-ga-fix-list-from-stress-attempt-2/73-UAT.md's "Attempt 3, Stages A–F" section asks — run id, execution range, outcome counts, created ids, disarm check at each boundary — plus a findings table F-S1.. with severity. Write that section into 73-UAT.md. Never paste a Webhook Trigger runData block anywhere (it carries the secret). Do NOT run the reset — that is mine.
